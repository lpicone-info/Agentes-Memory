
## 2026-06-01 15:03 UTC - SVN Infomedical / d_soli_deriv_aportes_titu

- Interlocutor por Discord pidio analizar un DataWindow con SQL embebido que fallaba para usuarios nuevos con error de permisos de ejecucion. Diagnostico: el SELECT invocaba la funcion escalar `dbo.afn_soli_deriv_aportes_sueldo(SOLI_DERIV_APORTES.soli)`, por lo que usuarios/roles nuevos necesitaban `EXECUTE` sobre esa funcion, salvo que se encapsulara en un stored procedure con ownership chaining adecuado.
- En la rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/25.08.001_Agente03`, usando credenciales `wguise`, se migro el DataObject `d_soli_deriv_aportes_titu` de SQL embebido a stored procedure y luego se revirtio por pedido del usuario, para finalmente reaplicar el cambio tras actualizar la working copy.
- Revision/flujo SVN: primer commit de migracion `r83526`; revert solicitado y commiteado como `r83527`; luego update a `r83528`; cambio reaplicado y commiteado como `r83529` con mensaje `Migrar d_soli_deriv_aportes_titu a stored procedure`.
- Estado final informado al usuario: working copy limpia; agregada `backend/procedures/afs_soli_deriv_aportes_titu.sql`; modificado `exe/Source Infomedical/d_soli_deriv_aportes_titu.srd`.
- Detalles tecnicos finales: el DataWindow ejecuta `dbo.afs_soli_deriv_aportes_titu @prm_soli = :ksoli`; mantiene el argumento `ksoli`; el procedure conserva la logica actualizada tras el update, incluyendo llamada a `dbo.afn_soli_deriv_aportes_sueldo` y columna `vigen_desde_ori`; se revisaron usos en `wp_solici.srw` y `wp_soli_masivas.srw`, sin requerir cambios porque siguen pasando un solo parametro.
- Validaciones realizadas: no se uso `RTRIM` ni finalizadores `;` en el SQL nuevo; SRD preservado como UTF-8 con BOM y CRLF; SQL nuevo sin BOM y CRLF; no se pudo compilar PowerBuilder ni ejecutar contra SQL Server desde el entorno local.
