# Memoria de conversación — Cristian Iván Ratto

## Datos

- **Fecha:** 21/08/2026
- **Usuario:** Cristian Iván Ratto
- **ID Google Chat:** `users/112625344596118312340`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 77
- **Inicio:** 08:49:09
- **Fin:** 16:33:38

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/vmclokaaaae`

## Archivos revisados

- `837533c9-4138-490c-9f69-a814ea696a0f.jsonl`

---

## Transcripción completa

### [21/08/2026 08:49:09] Cristian Iván Ratto

stan

---

### [21/08/2026 08:50:16] Agente

Hola Cristian, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [21/08/2026 09:15:09] Cristian Iván Ratto

De mi tarea podrias pasarme un listado resumen de los objetos que tocamos con su storere procedure asociado?

---

### [21/08/2026 09:17:47] Agente

Cristian, tomando como referencia tu tarea **RM30192**, el resumen queda así:

| Objeto tocado | Tipo | Stored procedure asociado | Estado del SP |
|---|---|---|---|
| `wp_expe_orde.srw` | Ventana PowerBuilder | `dbo.prs_valida_lugares_x_prestador_circulo` | Revisado / usado como validador, sin cambios |
| `wp_expe_sana.srw` | Ventana PowerBuilder | `dbo.prs_valida_lugares_x_prestador_circulo` | Revisado / usado como validador, sin cambios |
| `wp_expe_odon.srw` | Ventana PowerBuilder | `dbo.prs_valida_lugares_x_prestador_circulo` | Revisado / usado como validador, sin cambios |

Resumen técnico: en esta tarea **tocamos lógica PowerBuilder**, no modificamos stored procedures. El SP asociado quedó como validador técnico de correspondencia prestador/lugar; la excepción funcional por `liquida_efector_diferente` se resolvió en las ventanas.

---

### [21/08/2026 09:19:14] Cristian Iván Ratto

Perdon estan me falto decirte .. .es del redmine 26993.
Tocamos hasta el momento 7 objetos ... quiero saber los sp que usan dichos objetos.

---

### [21/08/2026 09:21:16] Agente

Cristian, para el RM26993, los 7 objetos de trabajo y sus SP asociados son:

| Objeto | SP que usa |
|---|---|
| `d_factura_roisa.srd` | `dbo.afs_compro_impresion_roisa` |
| `d_factura_activa.srd` | `dbo.afs_compro_impresion_caseros` |
| `d_factura_montegrande.srd` | `dbo.afs_compro_impresion_cmmg` |
| `d_factura_caseros.srd` | `dbo.afs_compro_impresion_caseros` |
| `d_factura_mutual.srd` | `dbo.afs_compro_impresion_mutual` |
| `d_factura_premedic.srd` | `dbo.afs_compro_impresion_caseros` |
| `d_factura_sbsalud.srd` | `dbo.afs_compro_impresion_caseros` |

Nota: también encontré exportado `d_factura_igualdad.srd`, que usa `dbo.afs_compro_impresion_caseros`, pero no lo incluí en los 7 porque me pediste ese número exacto.

---

### [21/08/2026 09:54:14] Cristian Iván Ratto

Stan retomemos el caso SBSalud

---

### [21/08/2026 09:55:04] Agente

Cristian, retomado SBSalud.

Estado vigente del caso:
- Objeto: `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`
- SP asociado: `dbo.afs_compro_impresion_caseros`
- Base usada: `SBSalud_Prod_Test`
- Cambios aplicados: solo vencimientos y condición de visualización de barras.
- `SUBTOTAL` y `TOTAL FACTURA` quedaron sin modificar.
- SVN no se tocó.

Último punto abierto: la condición de barras. Había quedado aplicada la regla de ocultarlas cuando el Vto1 exigible queda en cero, pero eso estaba pendiente de confirmación funcional.

---

### [21/08/2026 09:59:26] Cristian Iván Ratto

Stan continuamos con el caso SBSalud
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd
Necesito que el objeto que en su estado original.
Te doy de caso testigo: A-0008-00007580 ubicada en SBSalud_Prod_text

El campo total factura ya trae el saldo a favor descontado?
como calcula el subtotal.?
como calcula al 1er vencimiento ?

---

### [21/08/2026 10:02:59] Agente

Cristian, en el **estado original** de `d_factura_sbsalud.srd`, para el caso `A000800007580` en `SBSalud_Prod_Test`:

**Total factura**
Sí, ya viene con el saldo a favor descontado, porque el objeto calcula:

```text
total_factura_a = abs(impor1)
```

Datos del caso:
```text
Total fiscal COMPROBANTES = 168882.00
Créditos = 150.03
impor1 = 168731.97
```

Entonces muestra:
```text
Total factura = 168731.97
```

**Subtotal**
El objeto original calcula:

```text
subtotal_a = abs(impor1 - iibb_arba - iva_total)
```

Para este caso:
```text
impor1 = 168731.97
iibb_arba = 0.00
iva_total = 15221.11
```

Resultado:
```text
Subtotal = 153510.86
```

Ojo: no usa `COMPROBANTES.grava` para el subtotal.

**1er vencimiento**
En estado original calcula:

```text
total_factura_a_vto1 = abs(impor1)
```

Para el caso:
```text
1er vencimiento = 168731.97
```

Conclusión rápida: en SBSalud original, `Total factura` y `1er vencimiento` ya salen desde `impor1`, que en este caso ya trae descontado el saldo a favor.

---

### [21/08/2026 10:06:58] Cristian Iván Ratto

y el codigo de barras que monto toma? es un dato directo la base cual campo es?

---

### [21/08/2026 10:07:32] Agente

Cristian, el código de barras **no calcula el monto en el DataWindow**.

En el objeto original toma el dato directo del campo:

```text
COMPRO_IMPRESION.barra
```

La fórmula del objeto es:

```text
gf_interleaved_2de5(barra)
```

Condicionada por:

```text
imprime_total <> 'N'
tipo <> 'NC'
impor1 > 0
```

Para el caso `A000800007580`, el valor viene así desde base:

```text
COMPRO_IMPRESION.barra = 62460001687319725188650008000075800000000315250757
COMPROBANTES.codi_barra = 62460001687319725188650008000075800000000315250757
```

Conclusión: el monto que use la barra ya viene embebido en el string generado en base. El `.srd` solo lo transforma visualmente con `gf_interleaved_2de5(barra)`.

---

### [21/08/2026 10:08:11] Cristian Iván Ratto

y cual es ese monto?

---

### [21/08/2026 10:09:14] Agente

Cristian, el monto embebido en la barra es:

```text
168731.97
```

La función que arma la barra para SBSalud es `dbo.afn_get_caseros_codibarra`. El formato pone:

```text
Empresa: 6246
Importe 1er vencimiento: 00016873197
```

Ese tramo `00016873197` se interpreta en centavos:

```text
16873197 / 100 = 168731.97
```

Para tu caso:

```text
Total fiscal COMPROBANTES.total = 168882.00
Créditos = 150.03
Monto en barra / impor1 = 168731.97
```

O sea: la barra está tomando el importe ya neteado con el saldo a favor.

---

### [21/08/2026 10:18:46] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-21.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-21.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-21-HHMM.md); always use the canonical 2026-08-21.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 21st, 2026 - 10:18 AM (America/Buenos_Aires)
Reference UTC: 2026-08-21 13:18 UTC

---

### [21/08/2026 10:19:10] Cristian Iván Ratto

Stan continuamos con el caso SBSalud
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd
Te doy de caso testigo: A-0008-00007580 ubicada en SBSalud_Prod_text

Stan en este caso SBSalud debe comportarse similar a Roisa.
El subtotal debe tomarlo de el campo grava sumado al exen de tabla comprobantes
El campo total debe ser el abs(grava + iva_total + iibb_arba + iibb_caba)
Los vencimientos son el total factura con el saldo a favor.

---

### [21/08/2026 10:21:51] Agente

Cristian, aplicado en:

`/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Cambios:
- `SUBTOTAL`: ahora se reconstruye como total fiscal menos IVA/IIBB:
  `impor1 + creditos - iva_total - iibb_arba - iibb_caba`
- `TOTAL FACTURA A/B`: ahora usa total fiscal sin saldo aplicado:
  `impor1 + creditos`
- `Vto1/Vto2/Vto3`: ahora quedan como importe de vencimiento con saldo aplicado:
  `imporN + deudaN`
- Barras: la visibilidad quedó alineada al Vto1 exigible `impor1 + deuda1`.

Para el caso `A000800007580`:
- Total factura: `168731.97 + 150.03 = 168882.00`
- Subtotal: `168882.00 - 15221.11 - 0.00 - 8697.78 = 144963.11`
- 1er vencimiento: `168731.97 + 0.00 = 168731.97`

Nota técnica: el `.srd` no trae columnas `exen` ni `grava`; no toqué el SP compartido. Dejé la fórmula equivalente usando los campos disponibles en el DataWindow. Validé que el archivo sigue `UTF-16 LE` con BOM y `CRLF`. No compilé PowerBuilder.

---

### [21/08/2026 10:28:06] Cristian Iván Ratto

y el codigo de barras que monto trae ?

---

### [21/08/2026 10:28:20] Agente

Cristian, el código de barras sigue trayendo este monto:

```text
168731.97
```

Porque la barra viene directa de base:

```text
COMPRO_IMPRESION.barra
COMPROBANTES.codi_barra
```

Para `A000800007580`, el string es:

```text
62460001687319725188650008000075800000000315250757
```

Dentro de esa barra, el tramo de importe es:

```text
00016873197
```

Interpretado en centavos:

```text
168731.97
```

Conclusión: la barra trae el **monto del vencimiento con saldo a favor aplicado**, no el total factura fiscal `168882.00`.

---

### [21/08/2026 10:29:49] Cristian Iván Ratto

me das un resumen los cambios que hicimos en concreto para SBsalud? para mostrar

---

### [21/08/2026 10:30:00] Agente

Cristian, resumen concreto para SBSalud:

**Objeto**
`/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

**Cambio funcional**
SBSalud quedó con criterio similar a ROISA: separar el **total fiscal** de los **vencimientos**.

**Antes**
- `TOTAL FACTURA` usaba `impor1`.
- `SUBTOTAL` usaba `impor1 - iva_total - iibb`.
- `VENCIMIENTOS` estaban mezclados con la lógica anterior de saldo/créditos.

**Ahora**
- `TOTAL FACTURA A/B`:
  ```text
  impor1 + creditos
  ```
- `SUBTOTAL`:
  ```text
  impor1 + creditos - iva_total - iibb_arba - iibb_caba
  ```
- `Vto1`:
  ```text
  impor1 + deuda1
  ```
- `Vto2`:
  ```text
  impor2 + deuda2
  ```
- `Vto3`:
  ```text
  impor3 + deuda3
  ```

**Caso testigo**
`A000800007580`

- Total fiscal: `168882.00`
- Saldo a favor aplicado: `150.03`
- Vto1 / barra: `168731.97`

**Resultado**
- `TOTAL FACTURA` muestra `168882.00`
- `SUBTOTAL` muestra la base sin contaminar por saldo a favor
- `VENCIMIENTO` y código de barras quedan por `168731.97`, o sea con saldo a favor aplicado.

---

### [21/08/2026 11:01:03] Cristian Iván Ratto

Stan de los casos que hemos revisado.
Te pido que me informes cuales de ellos traen el saldo descontado en el total de la factura de la base
Caso Roisa y SBslud.

Los restantes casos como calculan?... deberian hacer lo mismo ?

---

### [21/08/2026 11:01:58] Agente

Cristian, con lo revisado:

**Casos donde el total venía afectado por saldo**
- **ROISA**: el problema original era que `TOTAL FACTURA` se calculaba desde `impor1`, y `impor1` podía venir afectado por saldo/créditos. Por eso lo corregimos a:
  ```text
  grava + iva_total + iibb_arba + iibb_caba
  ```
- **SBSalud**: en el caso `A000800007580`, `COMPROBANTES.total = 168882.00`, pero `COMPRO_IMPRESION.impor1 = 168731.97` porque ya trae descontado `creditos = 150.03`. El objeto original mostraba `TOTAL FACTURA = abs(impor1)`, entonces sí venía descontado. Lo corregimos a:
  ```text
  impor1 + creditos
  ```

**Resto de casos**
En general, los otros objetos venían calculando `TOTAL FACTURA` desde:

```text
abs(impor1)
```

y los cambios que hicimos en varios de ellos fueron solo sobre vencimientos. Eso significa que, si en cada base `impor1` ya viene neteado por saldo a favor, entonces también mostrarían el total descontado.

**Criterio correcto**
Sí, funcionalmente deberían seguir el mismo criterio:
- `TOTAL FACTURA` no debe estar afectado por saldo a favor/deuda.
- `SUBTOTAL` tampoco.
- Los `VENCIMIENTOS` sí deben reflejar saldo a favor o saldo deudor según corresponda.

Pero no lo aplicaría automáticamente a todos sin validar cada caso testigo, porque depende de si en esa base `impor1` viene neto o fiscal.

---

### [21/08/2026 11:04:00] Cristian Iván Ratto

podrias revisar Activa Salud? si va con el criterio de roisa o tiene su propio criterio ?

---

### [21/08/2026 11:04:45] Agente

Cristian, revisé ActivaSalud con el caso testigo `B000300003628`.

**Conclusión**
ActivaSalud tiene **criterio propio** y, para este caso, **no necesita el criterio ROISA**.

**Datos de base**
```text
COMPROBANTES.total = 40000.00
COMPROBANTES.exen = 40000.00
COMPROBANTES.grava = 0.00
COMPRO_IMPRESION.impor1 = 40000.00
COMPRO_IMPRESION.creditos = 40000.00
```

**Total factura**
El objeto calcula:

```text
TOTAL FACTURA = abs(impor1)
```

Resultado:
```text
40000.00
```

Ese total **no viene descontado por el saldo a favor**. Está mostrando el total fiscal correcto.

**Subtotal**
El objeto calcula:

```text
subtotal_a = abs(impor1 - iibb_arba - iva_total)
```

Como en este caso no hay IVA/IIBB:
```text
subtotal = 40000.00
```

Esto coincide con `COMPROBANTES.exen = 40000.00`.

**Vencimientos**
Ahí sí aplica el saldo a favor:

```text
Vto1 = impor1 + saldo_anterior1
saldo_anterior1 = deuda1 - creditos
```

Para el caso:
```text
Vto1 = 40000 + (0 - 40000) = 0.00
```

Resumen para mostrar: ActivaSalud ya separa bien `TOTAL FACTURA` y `VENCIMIENTO`. El total queda fiscal en `40000.00`; el saldo a favor impacta solo en vencimientos.

---

### [21/08/2026 11:05:51] Cristian Iván Ratto

En ActivaSalud puede romper el codigo de barras?

---

### [21/08/2026 11:06:00] Agente

Cristian, en ActivaSalud **no debería romper el código de barras** con el criterio actual.

Motivo:
- La barra se muestra desde el campo `barra`.
- La condición original del objeto para mostrarla sigue basada en:
  ```text
  impor1 > 0
  ```
- En el caso `B000300003628`:
  ```text
  impor1 = 40000.00
  creditos = 40000.00
  vencimiento neto = 0.00
  ```
- Pero la barra, si viene armada desde base, probablemente trae el importe `40000.00`, no el vencimiento neto `0.00`.

Entonces el riesgo no es que “rompa” técnicamente, sino funcional: podría seguir mostrando una barra pagable por `40000.00` aunque el vencimiento corregido visualmente quede en `0.00`.

Si el criterio funcional es que cuando el vencimiento queda en cero no debe haber barra de pago, entonces habría que ajustar también la condición de visualización de la barra.

---

### [21/08/2026 11:21:10] Cristian Iván Ratto

Stan retomemos lo trabajado en ActivaSalud
/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd

Quiero saber como estaba antes y como esta ahora.
Quiero saber que campo usaba para la barra antes y ahora.
Que campos se tocaron.?

Caso testigo: B000300003626 y B000300003629
Base: Activa_Prod_test

---

### [21/08/2026 11:22:51] Agente

Cristian, para ActivaSalud en:

`/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

el cambio concreto fue sobre **vencimientos**, no sobre total/subtotal ni código de barras.

**Antes**
- `saldo_anterior1` = `deuda1 - creditos`
- `saldo_anterior2` = `deuda2`
- `Vto1 A` = `abs(impor1)`
- `Vto1 B` = `abs(impor1 + deuda1)`
- `Vto2 A` = `abs(impor2)`
- `Vto2 B` = `abs(impor2 + deuda2)`
- `Total factura A/B` = `abs(impor1)`
- `Subtotal` = `abs(impor1 - iibb_arba - iva_total)`
- Barra: usaba el campo directo `barra`

**Ahora**
- `saldo_anterior1` quedó igual: `deuda1 - creditos`
- `saldo_anterior2` cambió a: `deuda2 - creditos`
- `Vto1 A/B` ahora toma: `abs(impor1 + saldo_anterior1)`
- `Vto2 A/B` ahora toma: `abs(impor2 + saldo_anterior2)`
- `Total factura A/B` quedó igual: `abs(impor1)`
- `Subtotal` quedó igual
- Barra quedó igual: sigue usando el campo `barra`

Campos/objetos tocados en el DataWindow:
- `saldo_anterior2`
- `total_factura_a_vto1`
- `total_factura_b_vto1`
- `total_factura_a_vto2`
- `total_factura_b_vto2`

No se tocó:
- `total_factura_a`
- `total_factura_b`
- `subtotal_a`
- `subtotal_b`
- `barra`
- `barra_afip`
- vencimiento 3

Sobre los casos testigo en `Activa_Prod_Test`:

`B000300003626`
- `impor1 = 7091.37`
- `deuda1 = 14182.74`
- `creditos = 0`
- Total factura visible: `7091.37`
- Vto1 actual: `21274.11`
- Barra: `NULL`, por eso no hay barra para validar en ese comprobante.

`B000300003629`
- `impor1 = 30000.00`
- `deuda1 = 0`
- `creditos = 9700.93`
- Total factura visible: `30000.00`
- Vto1 actual: `20299.07`
- Vto2 actual: `20299.07`
- Barra: `NULL`, tampoco hay barra para validar en ese comprobante.

Conclusión: **ActivaSalud mantiene criterio propio**. No se llevó al criterio Roisa/SBSalud para total/subtotal. Acá el saldo a favor se descuenta en los vencimientos, mientras el total factura de la base sigue mostrando `impor1`. El código de barras no cambió y sigue dependiendo del campo `barra`; en estos dos casos viene nulo.

---

### [21/08/2026 11:44:08] Cristian Iván Ratto

Stan retomemos lo trabajado en SMC
/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd

Deja el caso original. Y luego Aplica el criterio como en Roisa y SBsalud

---

### [21/08/2026 11:48:41] Agente

Cristian, listo. Dejé `d_factura_caseros.srd` de SMC partiendo del original y luego apliqué el criterio tipo Roisa/SBSalud sobre:

`/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`

Cambios aplicados:

- `saldo_anterior2`
  - Antes: `deuda2`
  - Ahora: `deuda2 - creditos`

- `total_factura_a` y `total_factura_b`
  - Antes: `abs(impor1)`
  - Ahora: `abs(impor1 + creditos)`
  - Esto recompone el total fiscal sin descontar saldo a favor.

- `subtotal_a`
  - Antes: `abs(impor1 - iibb_arba - iibb_caba - iva_total)`
  - Ahora: `abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)`

- `total_factura_a_vto1` y `total_factura_b_vto1`
  - Antes: `abs(impor1)`
  - Ahora: `abs(impor1 + deuda1)`

- `total_factura_a_vto2` y `total_factura_b_vto2`
  - Antes: `abs(impor2)`
  - Ahora: `abs(impor2 + deuda2)`

- `barra`
  - Sigue usando el campo directo `barra`
  - Cambié solo la condición de impresión:
    - Antes imprimía si `impor1 > 0`
    - Ahora imprime si `impor1 + deuda1 > 0`

No toqué el SP `afs_compro_impresion_caseros.sql`, ni ramas SVN. Validé que el SRD quedó en `UTF-16 LE` con `CRLF`, igual que el original.

---

### [21/08/2026 12:03:53] Cristian Iván Ratto

Perfecto y el codigo de barras con que campo monto lo esta tomando ?

---

### [21/08/2026 12:04:16] Agente

Cristian, el código de barras **no calcula el monto en el DataWindow**.

El objeto toma la barra desde el campo directo:

`barra`

Ese campo viene desde el SP `afs_compro_impresion_caseros`, desde `COMPRO_IMPRESION.barra`.

Lo que sí cambiamos en el objeto es **la condición para mostrarla**:

```powerscript
if(isnull(impor1),0,impor1) + if(isnull(deuda1),0,deuda1) > 0
```

Entonces:

- **Monto embebido en la barra:** viene ya armado dentro del campo `barra`
- **Campo usado para imprimir la barra:** `barra`
- **Monto usado solo para decidir si se muestra:** `impor1 + deuda1`
- **No usa `creditos` para decidir la impresión de la barra**

En criterio funcional, quedó alineada al **primer vencimiento exigible**: `impor1 + deuda1`.

---

### [21/08/2026 12:09:39] Cristian Iván Ratto

Retomemos el caso Activa que hicimos ahi

---

### [21/08/2026 12:10:05] Agente

Sí, Cristian.

En **ActivaSalud** el archivo era:

`/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

Lo que dejamos fue:

- **Total factura**: quedó con criterio propio, `abs(impor1)`
- **Subtotal**: quedó igual, `abs(impor1 - iibb_arba - iva_total)`
- **Vencimientos**: se corrigieron para aplicar saldo a favor
  - `saldo_anterior1 = deuda1 - creditos`
  - `saldo_anterior2 = deuda2 - creditos`
  - `Vto1 = impor1 + saldo_anterior1`
  - `Vto2 = impor2 + saldo_anterior2`
- **Código de barras**: no se tocó
  - Campo usado: `barra`
  - Condición original: `impor1 > 0`

Conclusión que habíamos dejado: **ActivaSalud no quedó igual a Roisa/SBSalud**. En Activa, el saldo a favor impacta en los vencimientos, pero el total factura sigue mostrando el importe fiscal de la factura.

---

### [21/08/2026 12:20:13] Cristian Iván Ratto

Stan osea, que hasta el momento SBSalud, Roisa y SMC aplican mismo criterio

---

### [21/08/2026 12:20:19] Agente

Sí, Cristian.

Hasta este momento quedaron con el **mismo criterio funcional**:

- **Roisa**
- **SBSalud**
- **SMC**

Criterio aplicado:

- **Subtotal**: se informa como base fiscal sin IVA/IIBB.
- **Total factura**: se informa como total fiscal, sin descontar saldo a favor.
- **Vencimientos / monto exigible**: ahí sí impacta el saldo/deuda correspondiente.
- **Código de barras**: queda alineado al monto exigible del primer vencimiento, no al total fiscal.

ActivaSalud queda aparte: mantiene criterio propio, donde el total sigue siendo `impor1` y el saldo a favor se aplica solo en vencimientos.

---

### [21/08/2026 12:21:59] Cristian Iván Ratto

Y los restantes clientes aplican otro criterio

---

### [21/08/2026 12:22:31] Agente

Sí, con lo revisado hasta ahora, el mapa queda así:

**Mismo criterio Roisa/SBSalud/SMC**
- Roisa
- SBSalud
- SMC

Ahí el criterio es:
- Total factura = total fiscal, sin saldo a favor descontado.
- Subtotal = base fiscal.
- Vencimientos/barra = monto exigible, donde sí impacta saldo/deuda.

**Criterio distinto**
- ActivaSalud

En ActivaSalud dejamos:
- Total factura = `impor1`
- Subtotal = `impor1 - iva_total - iibb`
- Vencimientos = `imporN + saldo_anteriorN`, donde el saldo a favor sí descuenta.
- Barra = sigue usando `barra` y condición original `impor1 > 0`.

De los restantes clientes, no conviene afirmar que todos aplican otro criterio sin revisar cada objeto/caso testigo. Lo correcto sería decir: **por ahora solo ActivaSalud quedó confirmado como criterio distinto**. MonteGrande, Mutual, Premedic u otros habría que validarlos objeto por objeto antes de clasificarlos.

---

### [21/08/2026 12:23:59] Cristian Iván Ratto

para el caso de Roisa, trajimos el campo grava de la tabla comprobante... se podria lograr el mismo efecto pero sin tocar el SP ?

---

### [21/08/2026 12:24:26] Agente

Sí, se puede lograr **un efecto similar sin tocar el SP**, pero no exactamente igual al de Roisa.

En Roisa el cambio más limpio fue traer `COMPROBANTES.grava` al resultset del SP y usarlo directo en el DataWindow:

```text
Subtotal = grava
Total factura = grava + iva_total + iibb_arba + iibb_caba
```

Sin tocar el SP, la alternativa sería reconstruir desde campos ya disponibles en el objeto:

```text
Total fiscal = impor1 + creditos
Subtotal = impor1 + creditos - iva_total - iibb_arba - iibb_caba
```

Eso es lo que aplicamos en SBSalud/SMC cuando no agregamos `grava`.

La diferencia importante:

- Con `grava` desde `COMPROBANTES`: es el dato fiscal real, más confiable.
- Sin tocar SP: inferimos el valor recomponiendo con `impor1 + creditos`; funciona si `impor1` viene con saldo a favor descontado y `creditos` representa justamente ese saldo aplicado.

Entonces: **sí, se puede evitar tocar el SP**, usando la recomposición en el SRD. Pero para Roisa, si queremos máxima precisión fiscal, el criterio con `grava` en el SP sigue siendo el más sólido.

---

### [21/08/2026 12:25:29] Cristian Iván Ratto

aplica el campo para no usar el SP

---

### [21/08/2026 12:25:50] Cristian Iván Ratto

Aplica el cambio este ultimo asi evitamos usar el SP

---

### [21/08/2026 12:30:06] Agente

Listo, Cristian. Apliqué el cambio para que Roisa ya no dependa del campo `grava` agregado al SP.

Impacté:

`/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`
- Eliminé la columna `grava` del DataWindow.
- `total_factura_a` y `total_factura_b` ahora calculan:
```text
abs(impor1 + creditos)
```
- `subtotal_a` ahora calcula:
```text
abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)
```

También dejé limpio:

`/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`
- Quité las dos salidas agregadas:
```text
grava = ISNULL(c.grava, 0)
grava = 0
```

Validé que:
- No quedan referencias a `grava` ni en el SRD ni en el SQL.
- El SRD conserva `UTF-8 con BOM + CRLF`.
- El SQL conserva `ISO-8859 + CRLF`.

Con esto Roisa queda alineado al criterio sin modificar resultset del SP: se recompone el total fiscal desde `impor1 + creditos`.

---

### [21/08/2026 12:42:31] Cristian Iván Ratto

Queria un resumen de como quedo roisa

---

### [21/08/2026 12:42:41] Agente

Cristian, Roisa quedó así:

**Archivos impactados**
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`

**Cambio principal**
Se eliminó la dependencia del campo `grava`. Ya no necesitamos que el SP traiga `COMPROBANTES.grava`.

**DataWindow**
- Se quitó la columna `grava`.
- `Total factura A/B` quedó:
```text
abs(impor1 + creditos)
```
- `Subtotal A` quedó:
```text
abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)
```
- Los vencimientos siguen como monto exigible:
```text
Vto1 = abs(impor1 + deuda1)
Vto2 = abs(impor2 + deuda2)
Vto3 = abs(impor3 + deuda3)
```
- La barra sigue usando el campo `barra`.

**SP**
- Se quitaron las salidas agregadas:
```text
grava = ISNULL(c.grava, 0)
grava = 0
```

**Criterio final**
Roisa queda alineado a SBSalud/SMC sin depender de un campo nuevo del SP: recompone el total fiscal con `impor1 + creditos`, y usa los vencimientos/barra para el monto exigible.

---

### [21/08/2026 12:43:43] Cristian Iván Ratto

Comparame las diferencias del objeto roisa del trunk vs el que modificamos ahora.

---

### [21/08/2026 12:46:45] Agente

Cristian, comparé el objeto del trunk productivo contra el que modificamos:

Trunk:
`/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_roisa.srd`

Modificado:
`/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Ambos conservan formato `UTF-8 con BOM + CRLF`.

**Diferencias funcionales**

1. **Total factura B**
- Trunk:
```text
abs(impor1)
```
- Modificado:
```text
abs(if(isnull(impor1),0,impor1) + if(isnull(creditos),0,creditos))
```

2. **Total factura A**
- Trunk:
```text
abs(impor1)
```
- Modificado:
```text
abs(if(isnull(impor1),0,impor1) + if(isnull(creditos),0,creditos))
```

3. **Subtotal A**
- Trunk:
```text
abs(impor1 - iibb_arba - iva_total)
```
- Modificado:
```text
abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)
```
con manejo de nulos en cada campo.

4. **Texto visual de saldo**
- Trunk:
```text
A la fecha registra un saldo de
```
- Modificado:
```text
A la fecha registra facturas por un saldo de
```
También se movió hacia la derecha el importe `saldo_anterior1` para acomodar el texto más largo.

**Sin cambios funcionales**
- El SP asociado sigue siendo:
```text
dbo.afs_compro_impresion_roisa
```
- No cambió la barra.
- No cambiaron vencimientos:
```text
Vto1 = abs(impor1 + deuda1)
Vto2 = abs(impor2 + deuda2)
Vto3 = abs(impor3 + deuda3)
```
- No se agregó `grava` al objeto modificado. El criterio actual evita depender de ese campo.

---

### [21/08/2026 13:46:55] Cristian Iván Ratto

Comparame las diferencias del objeto SbSalud del trunk vs el que modificamos.

---

### [21/08/2026 13:48:34] Agente

Cristian, comparé:

Trunk:
`/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_sbsalud.srd`

Modificado:
`/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

**Diferencia técnica**
- Trunk: `UTF-8 con BOM + CRLF`
- Modificado: `UTF-16 LE + CRLF`

**Diferencias funcionales**

1. `saldo_anterior2`
- Trunk:
```text
deuda2
```
- Modificado:
```text
deuda2 - creditos
```

2. Vencimientos
- Trunk:
```text
Vto1 = abs(impor1)
Vto2 = abs(impor2)
Vto3 = abs(impor3)
```
- Modificado:
```text
Vto1 = abs(impor1 + deuda1)
Vto2 = abs(impor2 + deuda2)
Vto3 = abs(impor3 + deuda3)
```
Con manejo de nulos en `imporN` y `deudaN`.

3. `total_factura_a` y `total_factura_b`
- Trunk:
```text
abs(impor1)
```
- Modificado:
```text
abs(impor1 + creditos)
```

4. `subtotal_a`
- Trunk:
```text
abs(impor1 - iibb_arba - iva_total)
```
- Modificado:
```text
abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)
```

5. Código de barras
El campo sigue siendo el mismo:
```text
barra
```

Pero cambió la condición de impresión.

- Trunk: imprime si `impor1 > 0`
- Modificado: imprime si:
```text
impor1 + deuda1 > 0
```

También se ajustó la visibilidad de:
- `barra`
- `barra_numeros`
- `barra_128`

para ocultarlas cuando `impor1 + deuda1 <= 0`.

**Sin cambios**
- No se agregó `grava`.
- No se agregó `exen`.
- El SP asociado sigue siendo:
```text
dbo.afs_compro_impresion_caseros
```

---

### [21/08/2026 13:55:47] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-21.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-21.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-21-HHMM.md); always use the canonical 2026-08-21.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 21st, 2026 - 1:55 PM (America/Buenos_Aires)
Reference UTC: 2026-08-21 16:55 UTC

---

### [21/08/2026 13:56:49] Agente

NO_REPLY

---

### [21/08/2026 13:56:50] Cristian Iván Ratto

Comparame las diferencias del objeto de SMC del trunk vs el que modificamos.

---

### [21/08/2026 13:58:26] Agente

Cristian, comparé:

Trunk:
`/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_caseros.srd`

Modificado:
`/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`

**Diferencia técnica**
- Trunk: `UTF-8 con BOM + CRLF`
- Modificado: `UTF-16 LE + CRLF`

**Diferencias funcionales**

1. `saldo_anterior2`
- Trunk:
```text
deuda2
```
- Modificado:
```text
deuda2 - creditos
```

2. `total_factura_a` y `total_factura_b`
- Trunk:
```text
abs(impor1)
```
- Modificado:
```text
abs(impor1 + creditos)
```

3. `subtotal_a`
- Trunk:
```text
abs(impor1 - iibb_arba - iibb_caba - iva_total)
```
- Modificado:
```text
abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)
```
con manejo de nulos.

4. Vencimientos
- Trunk:
```text
Vto1 = abs(impor1)
Vto2 = abs(impor2)
```
- Modificado:
```text
Vto1 = abs(impor1 + deuda1)
Vto2 = abs(impor2 + deuda2)
```
con manejo de nulos.

5. Código de barras
El campo sigue siendo:
```text
barra
```

Cambió la condición de impresión:
- Trunk:
```text
impor1 > 0
```
- Modificado:
```text
impor1 + deuda1 > 0
```

**Otros cambios**
- Cambió el texto de saldo:
```text
A la fecha registra un saldo de
```
a:
```text
A la fecha registra facturas por un saldo de
```
- Se movió el importe del saldo para acomodar ese texto.
- Hay diferencias de orden interno de algunos controles del header/trailer, sin cambio funcional relevante identificado.

**Sin cambios**
- No se agregó `grava`.
- No se agregó `exen`.
- El SP asociado sigue siendo:
```text
dbo.afs_compro_impresion_caseros
```

---

### [21/08/2026 14:01:12] Cristian Iván Ratto

Cambió el texto de saldo: deja el original

---

### [21/08/2026 14:02:01] Cristian Iván Ratto

Cambió el texto de saldo: deja el original

---

### [21/08/2026 14:02:01] Agente

Listo, Cristian. En SMC dejé el texto original del saldo.

Objeto:
`/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`

Restaurado a trunk:

```text
A la fecha registra un saldo de
```

También restauré la posición original del importe `saldo_anterior1`:

```text
x="5450"
```

No toqué las fórmulas funcionales de total, subtotal, vencimientos ni barra.

---

### [21/08/2026 14:11:01] Cristian Iván Ratto

Stan en el redmine se habla de corregir cuando el parametro es S.
Me puedes explicar a que se refiere

---

### [21/08/2026 14:11:43] Agente

Sí, Cristian.

Cuando el Redmine dice “corregir cuando el parámetro es `S`”, se refiere a que está activada la **aplicación automática de créditos / saldo a favor**.

Los parámetros involucrados son:

```text
aplicacion_auto_cred
aplica_auto_n_cred
```

Cuando están en `S`, el sistema aplica automáticamente el saldo a favor del afiliado contra la factura. Entonces en la impresión pueden venir estos datos:

```text
impor1   = importe ya neteado / monto exigible
creditos = saldo a favor aplicado
```

Ahí nace el problema: si el objeto usa solo `impor1` para mostrar **Total factura** o **Subtotal**, termina mostrando el importe con saldo descontado, cuando fiscalmente debería mostrar la factura completa.

Por eso el criterio que aplicamos es:

```text
Total factura = impor1 + creditos
Subtotal = impor1 + creditos - IVA - IIBB
Vencimiento = impor1 + deuda
Barra = monto exigible / vencimiento
```

En simple:

- `S` = aplica saldo a favor automáticamente.
- El saldo afecta cuánto debe pagar el afiliado.
- No debería afectar el total fiscal de la factura.
- La corrección separa ambos conceptos:
  - **Factura fiscal**: total/subtotal.
  - **Monto a pagar**: vencimiento/barra.

Si el parámetro está en `N`, normalmente no se aplica crédito automático, `creditos` debería venir en cero o no incidir, y el comportamiento queda prácticamente igual al original.

---

### [21/08/2026 14:15:28] Cristian Iván Ratto

Osea que los cambios impactados en SMC, roisa y SBsalud, funcionarian con el parametro en S o N

---

### [21/08/2026 14:15:40] Agente

Sí, Cristian.

Los cambios en **SMC, Roisa y SBSalud** deberían funcionar tanto con el parámetro en `S` como en `N`.

Motivo:

- Si el parámetro está en `S`, puede venir saldo a favor aplicado:
```text
impor1 = importe neto / exigible
creditos = saldo aplicado
```
Entonces:
```text
Total factura = impor1 + creditos
```
reconstruye el total fiscal.

- Si el parámetro está en `N`, normalmente no hay crédito aplicado:
```text
creditos = 0
```
Entonces:
```text
Total factura = impor1 + 0
```
queda igual al comportamiento original.

Lo mismo con el subtotal:

```text
Subtotal = impor1 + creditos - iva_total - iibb_arba - iibb_caba
```

Con `S`, recompone la base fiscal.  
Con `N`, queda equivalente al cálculo original, salvo que ahora contempla también `iibb_caba` y nulos.

La barra y vencimientos quedan orientados al monto exigible, por eso también son compatibles:

```text
Vto1 / barra = impor1 + deuda1
```

En resumen: **con `S` corrige el caso del saldo aplicado; con `N` no debería alterar el resultado esperado porque `creditos` no incide.**

---

### [21/08/2026 14:18:17] Cristian Iván Ratto

Stan
Asumimos que SMC, Roisa y SBSalud estan correctos con los criterios correctos.
Puedes volver a revisar el caso de ActivaSalud.

---

### [21/08/2026 14:23:36] Agente

Cristian, revisé ActivaSalud de nuevo.

**Conclusión:** ActivaSalud debe quedar con criterio propio, no igual al de SMC/Roisa/SBSalud para `TOTAL FACTURA`.

En Activa, los casos testigo muestran que `impor1` ya viene como total fiscal:

```text
B000300003626
COMPROBANTES.total = 7091.37
COMPRO_IMPRESION.impor1 = 7091.37
creditos = 0.00

B000300003629
COMPROBANTES.total = 30000.00
COMPRO_IMPRESION.impor1 = 30000.00
creditos = 9700.93
saldo = 20299.07
```

Por eso, si aplicáramos el criterio de Roisa/SMC/SBSalud haciendo `impor1 + creditos`, Activa quedaría mal. Para `B000300003629` mostraría `39700.93` como total factura, cuando el total fiscal correcto es `30000.00`.

Cómo queda ActivaSalud:

```text
TOTAL FACTURA = abs(impor1)
SUBTOTAL = abs(impor1 - iibb_arba - iva_total)
VENCIMIENTOS = imporN + deudaN - creditos
```

Los cambios actuales están bien acotados: solo impactan vencimientos/saldo anterior. El total, subtotal y código de barras quedaron igual que trunk.

Sobre la barra: el objeto sigue usando los campos `barra` y `barra_afip`. No se modificó su fórmula. En los casos revisados, `barra` viene `NULL`; `barra_afip` sí trae dato, pero corresponde a la barra fiscal/AFIP, no al cálculo del monto exigible.

---

### [21/08/2026 14:36:29] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-21.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-21.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-21-HHMM.md); always use the canonical 2026-08-21.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 21st, 2026 - 2:36 PM (America/Buenos_Aires)
Reference UTC: 2026-08-21 17:36 UTC

---

### [21/08/2026 14:38:02] Agente

NO_REPLY

---

### [21/08/2026 14:38:03] Cristian Iván Ratto

Stan para activa tengo un caso
B-0003-00003629
El vencimiento con el objeto viejo me da $30000
El vencimiento con el objeto nuevo me da $20299.07
Esto me daria problemas con el codigo de barra? si alguien quiere pagar escaneando dicho codigo

---

### [21/08/2026 14:39:00] Agente

Cristian, para el caso `B-0003-00003629`:

```text
Total factura / impor1: 30000.00
Créditos: 9700.93
Vencimiento nuevo: 20299.07
barra: NULL
barra_afip: 3070842951806000300000000000000202007016
```

Con este dato, **no debería romper un código de barras de pago**, porque el campo `barra` que usa el objeto para la barra de pago viene `NULL`. No hay una barra de pago generada con monto `30000`.

Lo que sí existe es `barra_afip`, pero esa es la barra fiscal/AFIP; no es la barra de pago que debería determinar cuánto cobra el canal de cobranza.

Entonces, para este caso puntual: si alguien escanea la factura, **no debería pagar por una barra de pago de $30000 desde este objeto**, porque esa barra no está informada. El cambio visual del vencimiento a `$20299.07` queda consistente con el saldo real descontando el crédito.

---

### [21/08/2026 14:44:51] Cristian Iván Ratto

Stan entonces hasta el momento tenemos el criterio que usa roisa, smc y sbsalud.
Tenemos otro criterio que es que el usa ActivaSalud.
Consulta, que criterio usa Celer?

---

### [21/08/2026 14:46:19] Agente

Cristian, Celer usa un **tercer matiz**, pero funcionalmente está más cerca de **ActivaSalud** que de Roisa/SMC/SBSalud.

En `d_factura_celer.srd`:

```text
Total principal = abs(impor1)
saldo_anterior1 = deuda1 - creditos
saldo_anterior2 = deuda2 - creditos

Vto1 = impor1 + saldo_anterior1
Vto2 = impor2 + saldo_anterior2
```

O sea:

- **No usa criterio Roisa/SMC/SBSalud** para el total, porque no hace `impor1 + creditos`.
- **Sí descuenta créditos en vencimientos**, igual que ActivaSalud.
- **La barra de pago está alineada al vencimiento**, porque se muestra solo si:

```text
impor1 + saldo_anterior1 > 0
```

Entonces Celer queda así:

```text
TOTAL FACTURA: abs(impor1)
VENCIMIENTO: imporN + deudaN - creditos
BARRA: usa el monto exigible del primer vencimiento como condición funcional
```

Conclusión: **Celer ya venía correcto a nivel objeto** y no requiere aplicar el criterio Roisa/SMC/SBSalud.

---

### [21/08/2026 14:48:50] Cristian Iván Ratto

Puedes volver a revisar el caso de Montegrande y verificar que criterio usa.

---

### [21/08/2026 14:50:05] Agente

Cristian, Montegrande usa el mismo criterio funcional que **ActivaSalud/Celer**, no el de Roisa/SMC/SBSalud.

Quedó así en `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd`:

```text
TOTAL FACTURA = abs(impor1)

SUBTOTAL = abs(impor1 - iibb_arba - iibb_caba - iva_total)

saldo_anterior1 = deuda1 - creditos
saldo_anterior2 = deuda2 - creditos

VTO1 = impor1 + saldo_anterior1
VTO2 = impor2 + saldo_anterior2
```

Con una protección adicional:

```text
si VTO <= 0, muestra 0.00
```

La barra de pago también quedó alineada al monto exigible:

```text
se imprime si impor1 + saldo_anterior1 > 0
```

Entonces la clasificación hasta ahora queda:

```text
Roisa / SMC / SBSalud:
TOTAL FACTURA reconstruido como total fiscal separando el saldo del vencimiento.

ActivaSalud / Celer / Montegrande:
TOTAL FACTURA = impor1.
El saldo a favor impacta solo en vencimientos.
La barra, cuando aplica, debe alinearse al vencimiento exigible.
```

---

### [21/08/2026 14:56:08] Cristian Iván Ratto

Stan en la base Montegrande_Prod_text, necesito dos casos testigos uno con saldo a favor y otro encontra del afiliado.

---

### [21/08/2026 14:59:09] Agente

Cristian, en el servidor aparece como `Montegrande_Prod_Test`; no existe `Montegrande_Prod_text`.

Te dejo dos casos testigo:

**1. Con saldo a favor**
```text
Comprobante: A-0020-00017456
Afiliado: NOTAR, FRANCESCO OMAR DARIO
Contrato: 12591

Total / impor1: 124847.49
Deuda1: 49725.00
Créditos: 206652.51
Vto1 neto: -32080.02
Resultado visual esperado: 0.00
```

Este caso sirve para validar que cuando el crédito supera el importe exigible, el vencimiento queda en cero.

**2. Con saldo en contra del afiliado**
```text
Comprobante: B-0027-00000375
Afiliado: CAMERUCCI, SILVIA JULIA
Contrato: 990512

Total / impor1: 99.99
Deuda1: 11028.61
Créditos: 0.00
Vto1 neto: 11128.60
Barra: 000001112860192246600270000037500000001836
```

Este segundo caso sirve para validar que el vencimiento suma deuda anterior y que la barra queda alineada al importe exigible.

---

### [21/08/2026 16:00:20] Cristian Iván Ratto

Stan retomemos caso roisa.
En vez de hacer calculos para obtener el total factura, quiero que lo tomes de la tabla comprobante campo Total.
Debe traer como campo adicional
SP: afs_compro_impresion_roisa
Volver a alterar el objeto: d_factura_roisa

Es factible este cambio? encontras alguna falla ?

---

### [21/08/2026 16:02:25] Agente

Sí, Cristian, **es factible** y de hecho lo veo más robusto que seguir calculando el total en el objeto.

La idea sería:

```text
SP afs_compro_impresion_roisa
Agregar campo:
total_comprobante = ISNULL(c.total, 0)

Objeto d_factura_roisa
total_factura_a/b = abs(total_comprobante)
```

**No le veo falla funcional al criterio**, porque `COMPROBANTES.total` es el total fiscal real del comprobante. Evita depender de reconstrucciones con:

```text
impor1 + creditos
```

De hecho encontré casos donde el cálculo actual puede inflar el total:

```text
B007000020582
COMPROBANTES.total = 33150.00
impor1 = 33150.00
creditos = 33150.00
impor1 + creditos = 66300.00
```

Ahí tomar `COMPROBANTES.total` evita mostrar un total factura incorrecto.

**Cuidados técnicos para hacerlo bien:**

1. El SP tiene `UNION ALL`, por lo tanto el campo nuevo debe agregarse en ambas ramas del `SELECT`.
2. En la rama principal ya existe `COMPROBANTES c`, ahí se puede tomar directo `c.total`.
3. En la rama de `COMPRO_RESUMENES` no está `COMPROBANTES c`; hay que resolver qué devolver ahí para mantener la misma cantidad de columnas.
4. El DataWindow debe agregar la columna nueva en la definición, no solo cambiar la fórmula.
5. Revisaría también `impor_letras`, porque hoy sale desde `impor1`. Si el total factura pasa a salir de `c.total`, podría quedar inconsistente el importe en letras en comprobantes con crédito.

Conclusión: **sí, es factible y técnicamente conviene**, pero hay que hacerlo completo: SP + SRD + revisar `impor_letras`.

---

### [21/08/2026 16:28:15] Cristian Iván Ratto

Stan
Vuelve el objeto original como en el trunk.
En el SP: afs_compro_impresion_roisa, necesito dos campos nuevos.
traer el campo Total de la tabla comprobantes.
Traer el campo grava de la tabla comprobantes.
Luego en la DW d_factura_roisa hay que leerlos.
En el campo total_factura usa ese campo total
En el campo subtotal usa el el campo grava
Ubica el objeto: /mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd

---

### [21/08/2026 16:33:38] Agente

Cristian, aplicado en Roisa.

Archivos impactados:

- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Qué quedó:

```text
SP afs_compro_impresion_roisa
Agrega 2 campos nuevos al resultset:
- total = ISNULL(c.total, 0)
- grava = ISNULL(c.grava, 0)
```

Los agregué en las dos ramas del `UNION ALL`. En la rama principal ya existía `comprobantes c`; en la segunda agregué `LEFT JOIN comprobantes c` para poder leer esos campos sin cortar registros.

En la DW `d_factura_roisa.srd`:

```text
total_factura_a = abs(if(isnull(total),0,total))
total_factura_b = abs(if(isnull(total),0,total))

subtotal_a = abs(if(isnull(grava),0,grava))
```

Además, el objeto quedó restaurado desde trunk como base. Los vencimientos volvieron a lógica trunk:

```text
Vto1 = abs(impor1 + deuda1)
Vto2 = abs(impor2 + deuda2)
```

Validé:

```text
SP: ISO-8859 + CRLF preservado
SRD: UTF-8 BOM + CRLF preservado
SP: parse-only contra ROISA_Prod_Test sin errores
SQL: sin punto y coma y sin RTRIM
```

Punto a tener presente: `impor_letras` quedó como trunk, desde `impor1`. No lo cambié porque pediste volver el objeto al original y solo ajustar total/subtotal con los campos nuevos.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
