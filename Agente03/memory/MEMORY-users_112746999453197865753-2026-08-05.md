# Memoria curada diaria

- Fecha: 2026-08-05
- Usuario: Walter Guise
- id_googlechat: users/112746999453197865753
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/hf8ohyaaaae

## Resumen de la actividad

### Tema 1: Performance de `afu_compro_expor_aplica`

- Solicitud o contexto: Walter pidio analizar, en la rama productiva, el SP `afu_compro_expor_aplica` porque habia problemas de performance.
- Respuesta o acciones del agente: Se hizo analisis estatico sobre `wguise-Infomedical-trunk`, revisando `backend/procedures/afu_compro_expor_aplica.sql`. Se identifico que el SP procesa el lote con cursor fila por fila y que por cada comprobante repite llamadas y escrituras como `afu_obtener_nro_compro`, `afs_dame_interes_compro`, inserciones en `comprobantes` y `compro_pagos`, llamada a `afu_aplicar_compro`, relacion en `compro_exportacion` y actualizacion del registro original.
- Resultado: Se recomendo reducir lecturas repetidas dentro del cursor, especialmente unificar lecturas a `param_afi`, precargar datos invariantes y revisar el patron fila a fila como principal origen de costo. No se modificaron archivos.
- Estado: Informativo

### Tema 2: Ubicacion de `TriggerEvent('ue_aceptar')` en `wp_export_debitos`

- Solicitud o contexto: Walter consulto si mover `TriggerEvent('ue_aceptar')` al final del evento ayudaria al problema.
- Respuesta o acciones del agente: Se explico que mover el evento puede cambiar el momento del refresh o busqueda visual en la ventana, pero no reduce el costo de `afu_compro_expor_aplica`. Se reviso el flujo de `wp_export_debitos.srw`, que ejecuta el SP, hace `COMMIT`, cierra archivos, resetea grillas y muestra el mensaje final.
- Resultado: Se recomendo, si el objetivo era refrescar la grilla luego de aplicar, ubicarlo al final exitoso despues de los `Reset()`, preferentemente con `PostEvent('ue_aceptar')` si solo refresca y no participa de la transaccion. Quedo aclarado que no resuelve la performance del SP.
- Estado: Informativo

### Tema 3: Etiqueta `TOTAL FACTURA` en `d_factura`

- Solicitud o contexto: Walter consulto por que en `d_factura` la etiqueta `TOTAL FACTURA` muestra `impor1` de `compro_impresion`, valor que coincide con `comprobantes.saldo` y no con `comprobantes.total`.
- Respuesta o acciones del agente: Se analizo que `d_factura` toma los datos desde `compro_impresion` via `afs_compro_impresion_caseros` y calcula la etiqueta como `abs(impor1 + deuda1)`. Tambien se identifico que `afi_compro_impresion` carga `impor1` con un `CASE` que usa `C.saldo` cuando corresponde y `C.total` en otros casos.
- Resultado: Se concluyo que, para ese circuito, `impor1` representa el importe a cobrar o saldo pendiente, no necesariamente el total contable original del comprobante. Se aclaro que la etiqueta `TOTAL FACTURA` es funcionalmente confusa y que el concepto real seria mas cercano a `TOTAL A PAGAR` o `IMPORTE A PAGAR`.
- Estado: Informativo

### Tema 4: `afu_cod_oper_isss` y bajas de integrantes sin derivacion de aportes

- Solicitud o contexto: Walter pidio analizar `afu_cod_oper_isss` en la rama productiva para el caso en que llega un movimiento de baja y el CUIL no existe en `afi_deriv_aportes`, indicando que para integrantes no debe informarse como baja sino no hacer nada.
- Respuesta o acciones del agente: Se reviso el bloque donde `@cur_paren <> 'T'` y se detecto que el SP informa `BI` cuando el movimiento es de baja, existe el CUIL en `afiliados` y el afiliado esta activo, pero no valida que el integrante exista en `afi_deriv_aportes`.
- Resultado: Se recomendo acotar solamente el `CASE` que asigna `cod_op = 'BI'`, agregando una validacion `EXISTS` contra `afi_deriv_aportes` enlazada por `prepaga`, `contra` e `inte` via `afiliados`, y por el CUIT de la novedad. No se dejo registro de modificacion de archivo en esta conversacion.
- Estado: Informativo
