
## Tarea #29140 - API Node Ospecon / Cartilla

- Usuario Cristian retomó Redmine `#29140`, cliente `Ospecon`, proyecto vinculado a Presupuestos Ospecon.
- La tarea corresponde a una API Node.js ubicada en carpeta `api` de la rama `V25.08.001`/trunk SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`; se pidió consultar código sin modificarlo.
- SP principal de cartilla: `dbo.prs_carti_cap_y_gba_geo`. Ejemplo inicial en `SMC_Prox_Ver`:
  ```sql
  EXECUTE dbo.prs_carti_cap_y_gba_geo
  @prm_rubro = 3,
  @prm_espe = NULL,
  @prm_subespe = NULL,
  @prm_zona = 'CAPITAL FEDERAL',
  @prm_plan = 'BLANCO 1',
  @prm_prepaga = 2,
  @prm_contra = NULL
  ```
- Combo de localidades: `aps_localidades @prm_provin = 2, @prm_zona = 9` (Buenos Aires / Sur 1). En `SMC_Prox_Ver` ese SP devolvía 130 localidades.
- Bug probable encontrado en API: ruta `GET /getLocalidades` en `api/src/routes/cartillas/combo.js` lee parámetros desde `req.body` en `api/src/controllers/cartillas/combo/comboLocalidades.js` (`const {provin, zona} = req.body;`). Si el cliente llama con query string, `provin` y `zona` llegan `undefined`, el SP recibe NULL y devuelve error funcional. Recomendación: para GET leer `req.query` o cambiar endpoint a POST si se conserva body.
- Otra observación API: cuando `aps_localidades` devuelve error funcional `cod = 1`, la API responde con `cod: 0` arriba y embebe el error en `datosLocalidades`, lo que puede ocultar el problema.
- Relación entre `prs_carti_cap_y_gba_geo` y `aps_localidades`: no hay llamada directa; la relación es por datos. `aps_localidades` usa códigos de provincia/zona; `prs_carti_cap_y_gba_geo` recibe zona textual homologada desde `ZONAS_APP_HOMOLOG` y luego llama a `prs_carti_cap_y_gba_app` pasando zona/provincia.
- Inconsistencia detectada en homologación: en `SMC_Prox_Ver`, `ZONAS` tiene `9 = SUR 1`, pero `ZONAS_APP_HOMOLOG` homologa zonas `8/9/10` como `SUR`. Por eso `prs_carti_cap_y_gba_geo @prm_zona = 'SUR 1'` no trae, mientras `@prm_zona = 'SUR'` sí puede traer.
- En API del SP principal (`consultaCartilla.js`) se pasan `rubro`, `espe`, `subEspe`, `zona`, `plan`, `prepaga`, `contra`, pero no se pasa `provin`, aunque el SP principal tiene parámetro `@prm_provin`.
- En base `Ospecon_Prod` se encontró set válido donde `prs_carti_cap_y_gba_geo` devuelve datos con `localidades_deno`:
  ```sql
  use ospecon_prod
  EXECUTE dbo.prs_carti_cap_y_gba_geo
  @prm_rubro = 3,
  @prm_espe = NULL,
  @prm_subespe = NULL,
  @prm_zona = 'SUR',
  @prm_plan = 'BASE',
  @prm_prepaga = 1,
  @prm_contra = NULL,
  @prm_provin = 20
  ```
  Resultado observado: 107 registros, localidades ejemplo `NEUQUEN`, `PLOTTIER`, `SAN MARTIN DE LOS ANDES`, `CENTENARIO`, `ZAPALA`, `VILLA LA ANGOSTURA`, `PLAZA HUINCUL`.
- Combo relacionado en `Ospecon_Prod`: `EXECUTE aps_localidades @prm_provin = 20, @prm_zona = 5` devolvió 469 localidades para Neuquén / zona SUR.
- Otro set válido en `Ospecon_Prod`:
  ```sql
  EXECUTE dbo.prs_carti_cap_y_gba_geo
  @prm_rubro = 3,
  @prm_espe = NULL,
  @prm_subespe = NULL,
  @prm_zona = 'SUR',
  @prm_plan = 'BASE',
  @prm_prepaga = 1,
  @prm_contra = NULL,
  @prm_provin = 21
  ```
  Resultado observado: 358 registros con `localidades_deno`, ejemplos `SANTA ROSA`, `GENERAL PICO`, `REALICO`, `TOAY`.


## 2026-09-03 15:25 ART - RM29794 PowerBuilder export CSV/TXT
- Redmine 29794: trabajar solo sobre objetos locales en `/mnt/Agentes/cratto/RM29794/ObjetosModificados/exe/Source Infomedical`; no tocar ramas SVN.
- Objetos foco: `wp_export_comprobantes_ec_csv.srw`, `d_export_comprobantes_ec_lista.srd`, `de_export_comprobantes_ec_sel.srd`.
- Para exportacion se agregaron DataObjects locales nuevos: `d_export_comprobantes_csv.srd` para `dbo.afs_generar_comprobantes_csv` y `d_export_estado_cuenta_csv.srd` para `dbo.afs_generar_estado_cuenta_csv`.
- Diagnostico SPs RM29794: `afs_generar_compros_y_ecs_csv` es SP madre/orquestador para batch/sqlcmd/bcp y no conviene para PB. Los SP hijos devuelven resultsets tabulares adecuados para que PB genere archivos.
- Casos validados en `Premedic_Desarrollo`: `afs_generar_comprobantes_csv @prm_prepaga=1, @prm_emi_grupo=1, @prm_factu_perio='2026-08-01'` devuelve 1022 filas; `afs_generar_estado_cuenta_csv @prm_prepaga=1, @prm_emi_grupo=2, @prm_factu_perio='2026-08-01'` devuelve 3938 filas.
- Implementacion en `wp_export_comprobantes_ec_csv.srw`: boton Exportar ejecuta los dos SP hijos en DataStores y guarda archivos en path seleccionado por usuario, nombres `COMPRO_<periodo>_<prepaga>_<timestamp>.txt` y `EC_<periodo>_<prepaga>_<timestamp>.csv`, sin encabezados y separado por `;`.
- Correccion import DataObjects: agregar `arguments=(("prm_prepaga", number),("prm_emi_grupo", number),("prm_factu_perio", datetime))` porque PB fallaba importando por falta de argumentos en la definicion de stored procedure.
- Correccion regenerate `wp_export_comprobantes_ec_csv.srw`: normalizar literales de barra invertida con comillas simples y cambiar helper `wf_valor_number` a `DOUBLE` por compatibilidad con `GetItemNumber()`.
- Ultimos cambios solicitados/aplicados en `wp_export_comprobantes_ec_csv.srw`: ventana no redimensionable (`resizable=false`), boton `Exportar` inicia deshabilitado, `Buscar` lo habilita solo si la grilla trae filas, `Limpiar` lo vuelve a bloquear.
- Estado de cuenta: el retrieve y guardado de EC solo se hace si `dbo.fn_get_parametros_general('Afilmed', 'Facturacion', 'genera_estado_cuenta')` devuelve `S`.
- Mensaje de error estandar mantenido: `Error al generar la exportacion SCV.`


## 2026-09-03 16:23 ART - RM29794 ajustes PB y SPs CSV
- Redmine 29794: se mantiene criterio operativo de no tocar ramas SVN; trabajo solo en `/mnt/Agentes/cratto/RM29794/ObjetosModificados` y objetos locales.
- En `wp_export_comprobantes_ec_csv.srw`, tras fallo de import/regenerate por consulta SQL embebida, se reemplazo la lectura directa de `PARAMETROS_GENERALES`/`fn_get_parametros_general` por el patron estandar del sistema: `lib.of_trae_param_gral('Afilmed', 'Facturacion', 'genera_estado_cuenta', ls_tipo_dato, ls_genera_estado_cuenta)`. Ya no debe quedar referencia directa a `PARAMETROS_GENERALES` ni `fn_get_parametros_general` en la ventana.
- Usuario agrego objeto SQL local `/mnt/Agentes/cratto/RM29794/ObjetosModificados/afs_generar_comprobantes_csv.sql` y pidio completar al final el campo `linea` concatenando todos los campos.
- En `afs_generar_comprobantes_csv.sql` se completo `SELECT linea = CONCAT(...)` para modo de salida concatenada, con los 34 campos en el mismo orden que la salida tabular, separados por `@var_delimiter` (`;`). Validado en `Premedic_Desarrollo` dentro de transaccion con rollback: modo tabular y modo linea devolvieron 1022 filas para `@prm_prepaga=1`, `@prm_emi_grupo=1`, `@prm_factu_perio='2026-08-01'`.
- Usuario pidio aplicar mismo criterio en `afs_generar_estado_cuenta_csv` con parametro modo y salidas, sin tocar SVN.
- En `/mnt/Agentes/cratto/RM29794/ObjetosModificados/afs_generar_estado_cuenta_csv.sql` se agrego `@prm_modo tinyint = 0` y `@var_delimiter char(1) = ';'`.
- Criterio de salida para `afs_generar_estado_cuenta_csv.sql`: `@prm_modo = 0` conserva salida tabular original; `@prm_modo <> 0` devuelve una sola columna `linea` con campos de estado de cuenta concatenados por `;`.
- Validacion de `afs_generar_estado_cuenta_csv.sql`: script compila en `Premedic_Desarrollo` dentro de transaccion con rollback. No se pudo comparar cantidad de filas entre modo 0 y modo 1 porque en `Premedic_Desarrollo` la clave `genera_estado_cuenta = N` hace que el SP retorne antes del SELECT, conforme logica original.


## 2026-09-03 16:41 ART - Informes de prompts Redmine/SVN para Walter
- Usuario Walter/Wguise pidio generar informes markdown desde prompts SVN estandar y documentos fuente en rutas Windows mapeadas a `/mnt/Agentes/wguise`.
- Redmine 29663: se genero `/mnt/Agentes/wguise/R29663/ticket_29663_01_bug.md` desde prompt SVN `prompts_std/01_bug.md`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`, fuentes `R29663_Analisis_Mafalda.docx` y `R29663.doc` completo, con foco indicado en `HIST 4 - MP 02/09/2026` desde pagina 10. Se uso Redmine 29663 como contexto complementario y validaciones solo lectura en `Ospecon_Prox_Ver`. Resultado: 323 lineas, ASCII, sin BOM ni CRLF. No se modifico codigo ni datos.
- Hallazgo principal RM29663: la ventana pasa bien el prestador; la causa esta en el SQL de `d_resupresconv_carti_lugar`. El segundo bloque del `UNION` usa `PRESTAD_CATE_HABI` y trae convenios por categoria sin respetar la relacion real circulo/efector/lugar. Correccion conceptual: diferenciar prestadores circulo y no circulo usando `prestad_circu_cate` y `prestad_lugares_circu` segun HIST 4.
- Redmine 30307: se genero `/mnt/Agentes/wguise/R30307/ticket_30307_02_nuevo.md` desde prompt SVN `prompts_std/02_req_nuevo.md`, rama SVN `V25.08.001`, fuente `X:\wguise\R30307\R-09_Documento de Requerimiento Detallado.doc`. Redmine 30307 quedo solo como contexto complementario. Resultado: 379 lineas, ASCII, sin BOM ni CRLF. No se modifico codigo ni datos.
- Hallazgo principal RM30307: requerimiento para parametrizar fecha de comprobantes de facturacion masiva. Con parametro `S`, fecha base = `factu_perio`; con `N`, se conserva fecha del dia. Punto natural de implementacion: backend, principalmente `afi_generar_compro_masiva`, reutilizando proteccion de correlatividad existente en `afi_fac_compro_a_generar` / `afs_busca_fecha_compro`. Quedan definiciones sobre nombre/almacenamiento del parametro y alcance inicial de recibos.
