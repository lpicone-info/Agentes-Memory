
## 2026-08-21 13:55 ART - RM26993 facturas / saldos

Contexto durable para continuar el caso RM26993 con Cristian.

Objetos y criterios trabajados:
- Roisa: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`; SP asociado `dbo.afs_compro_impresion_roisa`; SQL local `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql` quedó sin salida nueva `grava`.
- SBSalud: `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`; SP asociado `dbo.afs_compro_impresion_caseros`.
- SMC/Caseros: `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`; SP asociado `dbo.afs_compro_impresion_caseros`.
- ActivaSalud: `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`; SP asociado `dbo.afs_compro_impresion_caseros`.

Criterio funcional confirmado igual para Roisa, SBSalud y SMC:
- Total factura debe mostrar total fiscal sin saldo a favor descontado.
- Subtotal debe mostrar base fiscal sin contaminar por saldo a favor.
- Vencimientos y codigo de barras deben representar el monto exigible, donde si aplica saldo/deuda.
- Se evito depender de traer `grava` desde SP; se recompone en el SRD con campos existentes.

Roisa estado final:
- Se elimino dependencia de `grava` en SRD y SQL.
- `total_factura_a` y `total_factura_b`: `abs(if(isnull(impor1),0,impor1) + if(isnull(creditos),0,creditos))`.
- `subtotal_a`: `abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)` con manejo de nulos.
- Vencimientos siguen: `Vto1 = abs(impor1 + deuda1)`, `Vto2 = abs(impor2 + deuda2)`, `Vto3 = abs(impor3 + deuda3)`.
- Barra sigue usando campo `barra`; no se cambio funcionalmente.
- Comparado contra trunk `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_roisa.srd`: diferencias principales son total A/B, subtotal A y texto visual de saldo (`A la fecha registra facturas por un saldo de`). Formato trunk y modificado UTF-8 BOM + CRLF.

SBSalud estado final:
- Caso testigo usado: `A-0008-00007580` / `A000800007580` en `SBSalud_Prod_Test` (el usuario escribio alguna vez `SBSalud_Prod_text`).
- En base: `COMPROBANTES.total = 168882.00`, `creditos = 150.03`, `impor1 = 168731.97`, `iva_total = 15221.11`, `iibb_arba = 0.00`, `iibb_caba = 8697.78`.
- Antes el total y Vto1 usaban `abs(impor1)`, por eso venian con saldo a favor descontado.
- Final: `total_factura_a/b = abs(impor1 + creditos)` -> caso testigo `168882.00`.
- Final: `subtotal_a = abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)` -> caso testigo `144963.11`.
- Final: vencimientos `Vto1 = abs(impor1 + deuda1)`, `Vto2 = abs(impor2 + deuda2)`, `Vto3 = abs(impor3 + deuda3)`.
- `saldo_anterior2`: de `deuda2` a `deuda2 - creditos`.
- Barra usa campo directo `barra` desde base (`COMPRO_IMPRESION.barra` / `COMPROBANTES.codi_barra`), no calcula monto en DataWindow.
- Caso testigo barra: `62460001687319725188650008000075800000000315250757`; importe embebido `00016873197` = `168731.97`.
- Visibilidad de `barra`, `barra_numeros`, `barra_128`: ahora condicionada a `impor1 + deuda1 > 0` en vez de `impor1 > 0`.
- Comparado contra trunk `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_sbsalud.srd`: trunk UTF-8 BOM + CRLF; modificado detectado UTF-16 LE + CRLF. Diferencias funcionales: total A/B, subtotal A, vencimientos, `saldo_anterior2`, condicion de barras. No se agrego `grava` ni `exen`; SP sigue `dbo.afs_compro_impresion_caseros`.

SMC/Caseros estado final:
- Se pidio dejar caso original y luego aplicar criterio como Roisa/SBSalud.
- `saldo_anterior2`: `deuda2` -> `deuda2 - creditos`.
- `total_factura_a/b`: `abs(impor1)` -> `abs(impor1 + creditos)`.
- `subtotal_a`: `abs(impor1 - iibb_arba - iibb_caba - iva_total)` -> `abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)`.
- `total_factura_a_vto1/b_vto1`: `abs(impor1)` -> `abs(impor1 + deuda1)`.
- `total_factura_a_vto2/b_vto2`: `abs(impor2)` -> `abs(impor2 + deuda2)`.
- Barra sigue usando campo directo `barra`; condicion de impresion cambio de `impor1 > 0` a `impor1 + deuda1 > 0`. No usa `creditos` para decidir impresion de barra.
- No se toco SP `afs_compro_impresion_caseros.sql` ni ramas SVN. SRD quedo UTF-16 LE con CRLF.

ActivaSalud criterio distinto:
- Archivo `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`.
- Quedo con criterio propio, no se llevo a Roisa/SBSalud/SMC.
- Total factura queda `abs(impor1)`; subtotal queda `abs(impor1 - iibb_arba - iva_total)`.
- Se tocaron vencimientos/saldo: `saldo_anterior1 = deuda1 - creditos` (ya estaba), `saldo_anterior2 = deuda2 - creditos`; `Vto1 = abs(impor1 + saldo_anterior1)`, `Vto2 = abs(impor2 + saldo_anterior2)` para A/B.
- Campos tocados: `saldo_anterior2`, `total_factura_a_vto1`, `total_factura_b_vto1`, `total_factura_a_vto2`, `total_factura_b_vto2`.
- No se tocaron: `total_factura_a`, `total_factura_b`, `subtotal_a`, `subtotal_b`, `barra`, `barra_afip`, vencimiento 3.
- Barra sigue usando campo `barra`; condicion original `impor1 > 0`.
- Casos testigo en `Activa_Prod_Test`: `B000300003626` (`impor1=7091.37`, `deuda1=14182.74`, `creditos=0`, total visible `7091.37`, Vto1 actual `21274.11`, barra NULL); `B000300003629` (`impor1=30000.00`, `deuda1=0`, `creditos=9700.93`, total visible `30000.00`, Vto1/Vto2 `20299.07`, barra NULL). Caso previo `B000300003628`: total fiscal `40000.00`, `exen=40000.00`, `grava=0`, `impor1=40000.00`, `creditos=40000.00`, Vto1 queda `0.00`; total no venia descontado.

Resumen de clasificacion actual:
- Confirmados con mismo criterio: Roisa, SBSalud, SMC.
- Confirmado con criterio distinto: ActivaSalud.
- Otros clientes del RM26993 (MonteGrande, Mutual, Premedic, etc.) no deben clasificarse sin revisar objeto/caso testigo.
## 2026-08-21 13:55 ART - RM26993 facturas / saldos

Contexto durable para continuar el caso RM26993 con Cristian.

Objetos y criterios trabajados:
- Roisa: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`; SP asociado `dbo.afs_compro_impresion_roisa`; SQL local `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql` quedó sin salida nueva `grava`.
- SBSalud: `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`; SP asociado `dbo.afs_compro_impresion_caseros`.
- SMC/Caseros: `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`; SP asociado `dbo.afs_compro_impresion_caseros`.
- ActivaSalud: `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`; SP asociado `dbo.afs_compro_impresion_caseros`.

Criterio funcional confirmado igual para Roisa, SBSalud y SMC:
- Total factura debe mostrar total fiscal sin saldo a favor descontado.
- Subtotal debe mostrar base fiscal sin contaminar por saldo a favor.
- Vencimientos y codigo de barras deben representar el monto exigible, donde si aplica saldo/deuda.
- Se evito depender de traer `grava` desde SP; se recompone en el SRD con campos existentes.

Roisa estado final:
- Se elimino dependencia de `grava` en SRD y SQL.
- `total_factura_a` y `total_factura_b`: `abs(if(isnull(impor1),0,impor1) + if(isnull(creditos),0,creditos))`.
- `subtotal_a`: `abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)` con manejo de nulos.
- Vencimientos siguen: `Vto1 = abs(impor1 + deuda1)`, `Vto2 = abs(impor2 + deuda2)`, `Vto3 = abs(impor3 + deuda3)`.
- Barra sigue usando campo `barra`; no se cambio funcionalmente.
- Comparado contra trunk `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_roisa.srd`: diferencias principales son total A/B, subtotal A y texto visual de saldo (`A la fecha registra facturas por un saldo de`). Formato trunk y modificado UTF-8 BOM + CRLF.

SBSalud estado final:
- Caso testigo usado: `A-0008-00007580` / `A000800007580` en `SBSalud_Prod_Test` (el usuario escribio alguna vez `SBSalud_Prod_text`).
- En base: `COMPROBANTES.total = 168882.00`, `creditos = 150.03`, `impor1 = 168731.97`, `iva_total = 15221.11`, `iibb_arba = 0.00`, `iibb_caba = 8697.78`.
- Antes el total y Vto1 usaban `abs(impor1)`, por eso venian con saldo a favor descontado.
- Final: `total_factura_a/b = abs(impor1 + creditos)` -> caso testigo `168882.00`.
- Final: `subtotal_a = abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)` -> caso testigo `144963.11`.
- Final: vencimientos `Vto1 = abs(impor1 + deuda1)`, `Vto2 = abs(impor2 + deuda2)`, `Vto3 = abs(impor3 + deuda3)`.
- `saldo_anterior2`: de `deuda2` a `deuda2 - creditos`.
- Barra usa campo directo `barra` desde base (`COMPRO_IMPRESION.barra` / `COMPROBANTES.codi_barra`), no calcula monto en DataWindow.
- Caso testigo barra: `62460001687319725188650008000075800000000315250757`; importe embebido `00016873197` = `168731.97`.
- Visibilidad de `barra`, `barra_numeros`, `barra_128`: ahora condicionada a `impor1 + deuda1 > 0` en vez de `impor1 > 0`.
- Comparado contra trunk `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_sbsalud.srd`: trunk UTF-8 BOM + CRLF; modificado detectado UTF-16 LE + CRLF. Diferencias funcionales: total A/B, subtotal A, vencimientos, `saldo_anterior2`, condicion de barras. No se agrego `grava` ni `exen`; SP sigue `dbo.afs_compro_impresion_caseros`.

SMC/Caseros estado final:
- Se pidio dejar caso original y luego aplicar criterio como Roisa/SBSalud.
- `saldo_anterior2`: `deuda2` -> `deuda2 - creditos`.
- `total_factura_a/b`: `abs(impor1)` -> `abs(impor1 + creditos)`.
- `subtotal_a`: `abs(impor1 - iibb_arba - iibb_caba - iva_total)` -> `abs(impor1 + creditos - iva_total - iibb_arba - iibb_caba)`.
- `total_factura_a_vto1/b_vto1`: `abs(impor1)` -> `abs(impor1 + deuda1)`.
- `total_factura_a_vto2/b_vto2`: `abs(impor2)` -> `abs(impor2 + deuda2)`.
- Barra sigue usando campo directo `barra`; condicion de impresion cambio de `impor1 > 0` a `impor1 + deuda1 > 0`. No usa `creditos` para decidir impresion de barra.
- No se toco SP `afs_compro_impresion_caseros.sql` ni ramas SVN. SRD quedo UTF-16 LE con CRLF.

ActivaSalud criterio distinto:
- Archivo `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`.
- Quedo con criterio propio, no se llevo a Roisa/SBSalud/SMC.
- Total factura queda `abs(impor1)`; subtotal queda `abs(impor1 - iibb_arba - iva_total)`.
- Se tocaron vencimientos/saldo: `saldo_anterior1 = deuda1 - creditos` (ya estaba), `saldo_anterior2 = deuda2 - creditos`; `Vto1 = abs(impor1 + saldo_anterior1)`, `Vto2 = abs(impor2 + saldo_anterior2)` para A/B.
- Campos tocados: `saldo_anterior2`, `total_factura_a_vto1`, `total_factura_b_vto1`, `total_factura_a_vto2`, `total_factura_b_vto2`.
- No se tocaron: `total_factura_a`, `total_factura_b`, `subtotal_a`, `subtotal_b`, `barra`, `barra_afip`, vencimiento 3.
- Barra sigue usando campo `barra`; condicion original `impor1 > 0`.
- Casos testigo en `Activa_Prod_Test`: `B000300003626` (`impor1=7091.37`, `deuda1=14182.74`, `creditos=0`, total visible `7091.37`, Vto1 actual `21274.11`, barra NULL); `B000300003629` (`impor1=30000.00`, `deuda1=0`, `creditos=9700.93`, total visible `30000.00`, Vto1/Vto2 `20299.07`, barra NULL). Caso previo `B000300003628`: total fiscal `40000.00`, `exen=40000.00`, `grava=0`, `impor1=40000.00`, `creditos=40000.00`, Vto1 queda `0.00`; total no venia descontado.

Resumen de clasificacion actual:
- Confirmados con mismo criterio: Roisa, SBSalud, SMC.
- Confirmado con criterio distinto: ActivaSalud.
- Otros clientes del RM26993 (MonteGrande, Mutual, Premedic, etc.) no deben clasificarse sin revisar objeto/caso testigo.

## 2026-08-21 14:36 ART - RM26993 continuidad

Memoria adicional posterior al flush de 13:55:
- Cristian pidio comparar SMC trunk vs modificado. Trunk: `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/d_factura_caseros.srd`; modificado: `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`.
- En SMC se informaron diferencias: trunk UTF-8 BOM + CRLF; modificado UTF-16 LE + CRLF. Funcionalmente: `saldo_anterior2`, `total_factura_a/b`, `subtotal_a`, Vto1/Vto2, y condicion de barra `impor1 > 0` -> `impor1 + deuda1 > 0`. Campo de barra sigue `barra`. Sin `grava` ni `exen`; SP sigue `dbo.afs_compro_impresion_caseros`.
- Cristian detecto que en SMC habia cambiado texto visual de saldo y pidio dejar original. Se restauro en `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd` el texto `A la fecha registra un saldo de` y la posicion original del importe `saldo_anterior1` con `x="5450"`. No se tocaron formulas funcionales.
- Explicacion Redmine parametro `S`: refiere a aplicacion automatica de creditos/saldo a favor (`aplicacion_auto_cred` / `aplica_auto_n_cred`). Con `S`, `impor1` puede venir como monto neto/exigible y `creditos` como saldo aplicado; por eso total/subtotal deben recomponerse y vencimientos/barra representar monto exigible. Con `N`, `creditos` normalmente no incide.
- Se confirmo a Cristian que cambios de SMC, Roisa y SBSalud funcionan con parametro en `S` o `N`: con `S` corrigen saldo aplicado; con `N`, `creditos = 0` deja resultado equivalente al original salvo mejoras de nulos/IIBB CABA.
- Nueva revision de ActivaSalud: se reafirmo que NO debe aplicar criterio Roisa/SMC/SBSalud para total factura. Casos en `Activa_Prod_Test`: `B000300003626` tiene `COMPROBANTES.total = 7091.37` e `impor1 = 7091.37`; `B000300003629` tiene `COMPROBANTES.total = 30000.00`, `impor1 = 30000.00`, `creditos = 9700.93`, saldo/exigible `20299.07`. Si Activa usara `impor1 + creditos`, el total de `B000300003629` quedaria mal en `39700.93` en vez de `30000.00`.
- ActivaSalud debe quedar: `TOTAL FACTURA = abs(impor1)`, `SUBTOTAL = abs(impor1 - iibb_arba - iva_total)`, `VENCIMIENTOS = imporN + deudaN - creditos` via `saldo_anteriorN`. Total, subtotal y codigo de barras igual que trunk; cambios solo en vencimientos/saldo anterior.
- En Activa, el objeto sigue usando `barra` y `barra_afip`; en casos revisados `barra` viene NULL y `barra_afip` trae dato fiscal/AFIP, no monto exigible.
