# Memoria curada diaria

- Fecha: 2026-07-29
- Usuario: Juana Virginia Mendoza
- id_googlechat: users/117659435859951903029
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/5gqh-kaaaae

## Resumen de la actividad

### Tema 1: Script `insertacae.sql` para `Premedic_Prox_Ver`

- Solicitud o contexto: Juana pidio generar `insertacae.sql` en `A:\jvmendoza\Smoke`, con sentencias `INSERT` para `compro_cae` tomando comprobantes de `Premedic_Prox_Ver` con `factu_perio = 01/01/2026`, usando CAE aleatorio con formato del campo y sin repetir.
- Respuesta o acciones del agente: Se consulto `dbo.comprobantes`, se generaron valores CAE numericos de 14 digitos unicos y se escribio el script en `/mnt/Agentes/jvmendoza/Smoke/insertacae.sql`.
- Resultado: El archivo contiene 95.256 inserts, sin ejecutar cambios en base. Se valido cantidad fuente, cantidad generada, unicidad y formato CAE, ausencia de `;`, ASCII sin BOM y finales CRLF.
- Estado: Resuelto

### Tema 2: Script `Mutualcae.sql` para `MutualRioIV_Prox_Ver`

- Solicitud o contexto: Juana pidio generar `Mutualcae.sql` en `A:\jvmendoza\Smoke`, con inserts para `compro_cae` desde `MutualRioIV_Prox_Ver` con `factu_perio = 01/01/2026`, CAE aleatorio y unico.
- Respuesta o acciones del agente: Se consulto `dbo.comprobantes` en `MutualRioIV_Prox_Ver`, se generaron CAE unicos de 14 digitos y se escribio `/mnt/Agentes/jvmendoza/Smoke/Mutualcae.sql`.
- Resultado: El archivo contiene 8.590 inserts. Se valido cantidad fuente, cantidad generada, unicidad, formato CAE, ausencia de `;`, ASCII sin BOM y finales CRLF. No se ejecutaron inserts en base.
- Estado: Resuelto

### Tema 3: Analisis de scripts Redmine 29805

- Solicitud o contexto: Juana pidio analizar los scripts mencionados en la nota `La solucion para la Base de Datos actual en produccion (Sin configuracion de facturacion) se comitea en el repositorio del ticket para acceso de ATC` del Redmine 29805 y dejar un Word en `A:\jvmendoza\Analisis` explicando brevemente que hace cada uno.
- Respuesta o acciones del agente: Se revisaron los scripts `Corre_Primero_aplica.sql` y `Corre_tercero_cancela_CI.sql` del repositorio del ticket y se genero un documento Word.
- Resultado: Se dejo `/mnt/Agentes/jvmendoza/Analisis/Analisis_Redmine_29805.docx`. El documento explica que prepara, que selecciona y que ejecuta cada script, respetando el orden indicado en Redmine. Se valido apertura con `python-docx` y presencia de referencias a ambos scripts. No se modificaron scripts ni se ejecuto nada en base.
- Estado: Resuelto

### Tema 4: Analisis de error en Redmine 30164 sobre solucion ejecutada en `Premedic_Prox_Ver`

- Solicitud o contexto: Juana pidio analizar el Redmine 30164, revisar la solucion dejada en la carpeta del repositorio que ejecuto en `Premedic_Prox_Ver`, explicar por que dio el error dejado en `A:\jvmendoza\Analisis\R30164` y guardar el analisis con posible causa en esa misma ruta.
- Respuesta o acciones del agente: Se comparo la solucion de R30164 con R29805 y se revisaron resultados en `Premedic_Prox_Ver`. Se encontro que R30164 amplio el universo hasta `31/03/2026`, pero el segundo script seguia llamando a `afu_cance_saldos` con `@factu_perio = '20251201'`. Los errores se concentraban en CI de enero, febrero y marzo 2026, con registros en `AJU_INTERNOS` como `compro_tipo = DI` y `compro_nro = NULL`.
- Resultado: Se dejo `/mnt/Agentes/jvmendoza/Analisis/R30164/Analisis_R30164_error_cancelacion.docx` con evidencia, causa probable, punto tecnico y recomendacion. La causa probable informada fue que la aplicacion falla con `El comprobante de debito no existe` porque no se genero comprobante DI asociado para esos ajustes. No se modificaron scripts ni se ejecutaron cambios en base.
- Estado: Resuelto

### Tema 5: Consulta sobre reemplazar parametros en `afu_cance_saldos`

- Solicitud o contexto: Juana consulto, sin modificar nada, si se podia cambiar la llamada a `@fecha = @prm_periodo_hasta` y `@factu_perio = @prm_periodo_hasta`.
- Respuesta o acciones del agente: Se indico que no conviene cambiarlo directo. `@factu_perio = @prm_periodo_hasta` podria atacar la causa, pero es riesgoso porque `@prm_periodo_hasta = '20260331'` es fecha de corte y no necesariamente periodo de facturacion real; los CI fallidos tienen `factu_perio` `20260101`, `20260201` y `20260301`. Se explico que `@fecha = @prm_periodo_hasta` no resolveria porque `afu_cance_saldos` pisa internamente `@fecha` con `GETDATE()`.
- Resultado: Se recomendo como hipotesis tecnica mas segura revisar `@factu_perio = @csr_factu_perio`, y no considerar `@fecha` como solucion salvo cambio de logica interna en `afu_cance_saldos`.
- Estado: Informativo
