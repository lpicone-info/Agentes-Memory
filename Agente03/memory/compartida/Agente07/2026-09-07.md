## 2026-09-07 - Memoria durable

- Usuario en Google Chat directo identificado como Maru en la conversacion previa.
- Se verifico en SVN trunk de Presmed, revision r84553, la carga de prestaciones a expedientes de ordenes (`wp_expe_orde`). Consulta funcional: cuando el expediente esta cargado a un circulo y el usuario ingresa prestador efector y prestaciones, con que datos se busca convenio.
- Conclusion tecnica respondida: el convenio se busca usando el prestador del expediente (`exp.prestad`), no el prestador efector ingresado (`efe_prestad`). Para expedientes cargados a un circulo, el prestador usado para convenio es el circulo del expediente.
- Datos de busqueda de convenio en `wp_expe_orde.srw`: `li_cate = dw_det.object.efe_cate[1]`, `ll_prestad = exp.prestad`, `li_lugar = dw_det.object.lugar[1]`. Es decir, usa categoria del efector y lugar seleccionado, pero prestador del expediente.
- Evidencia relacionada: en validacion/grabacion se llama `val.of_orden_a_liquidar(..., exp.prestad, str_i.lugar, ..., li_cate, ...)`; `uo_nv_valdatos.sru` / `of_convenio` pasa el prestador recibido como `@prestad` a `prs_valida_of_convenio` sin reemplazarlo por efector.
- Ruta local usada en la investigacion: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/exe/Source Infomedical/`.

- Usuario en Google Chat directo identificado como Pato en la conversacion actual.
- Para RM29856 se genero archivo QA en `/mnt/Agentes/pbossa/Incidentes/RM29856/Matriz de Casos de Prueba AutorizacionesPresmed.xlsx` con matriz de casos de prueba para la seccion 4.3.1 Autorizaciones Presmed: 8 casos consolidados, 50 criterios cubiertos, 0 sin cubrir. Luego se agrego hoja `Reglas consideradas` con 16 reglas funcionales.
- DB profile usado para validaciones SQL: `Ospecon_Prox_Ver` / `ospecon_prox_ver`.
- Query validada para prestadores utilizables simultaneamente en Materiales, Traslados y Odontologia con convenios vigentes: Materiales por convenio `convenios.servi_tipo IN (2,13)`, Traslados por `prestad_servicios.servi_tipo = 6`, Odontologia por convenio `convenios.servi_tipo = 9`, prestador vigente `prestadores.baja_fecha IS NULL` e `inhab_fecha IS NULL`, convenios/cartillas vigentes. Resultado estricto actual en Ospecon_Prox_Ver: 0 registros.
- Query ampliada validada para listar proveedores/prestadores de Materiales, Traslados, Odontologia y Prestaciones con convenios asociados vigentes, prestaciones relacionadas y plan. Tablas usadas: `prestadores`, `carti_convenios`, `convenios`, `servi_tipos`, `conve_prestaciones`, `prestaciones`, `nomencladores`, `planes`. Rubros: Odontologia si `c.servi_tipo = 9`, Traslados si `c.servi_tipo = 6 OR n.traslado = 'S'`, Materiales si `n.materiales = 'S'`, resto Prestaciones. Validacion en Ospecon_Prox_Ver sin errores; conteos observados: Materiales 456, Odontologia 152, Prestaciones 5.716.853, Traslados 418.
- Analisis copagos por orden/prestacion: `Paga al prestador` indica quien cobra, no si suma o reemplaza. El criterio del copago por orden (`Suma`/`Reemplaza`) define si se agregan ambos o si el copago por orden reemplaza al copago por prestacion. Con ambos vigentes y criterio `Suma`, se cobran los dos.
- Caso copago 4500: autorizacion `sucur=1`, `auto=357`, afiliado `428223/00`, plan `BASE`, prestador efector `704`, prestacion `170101 - ELECTROCARDIOGRAMA EN CONSULTORIO`, nomenclador `5 - PRACTICAS ESPECIALIZADAS PMO`, cobertura `cober=62926`. Sale de `cober_copagos` vigente desde `01/01/2026`, unidad `Pe`, valor `4500.00`, franquicia `0.00`, baja `NULL`. No habia copago especifico para prestador 704 en `cober_prestad_copagos`. Configuracion funcional: `Copagos del Plan`, sobre cobertura/prestacion del plan.
- Caso copago por orden 1000: plan `BASE`, prestador efector `50`, nomenclador `15 - TRASLADOS`, prestacion `770503 - ADICIONAL SADOFE`, fecha `07/09/2026`. Sale de `cober_orden_copagos`: `prepaga=1`, `plan_codi=BASE`, `nomen=15`, vigencia desde `01/01/2026`, valor `1000.00`, `pago_copa=A`, cantidad `2`, `suma_reemp=S`, baja `NULL`. No habia configuracion especifica para prestador 50 en `cober_prestad_copa_orden`, ni exclusion en `prestac_copa_orden_exclu`.
- Diferencia con copago 1200 visto por usuario: el 1200 corresponde a plan BASE + nomenclador `5 - PRACTICAS ESPECIALIZADAS PMO`; el 1000 corresponde a plan BASE + nomenclador `15 - TRASLADOS`.
- Criterio QA sobre pantalla de autorizaciones/copagos: es esperable que la columna `Paga copago` del renglon quede vacia cuando el copago unitario del item es 0 y el importe mostrado es `Coseguro p/ Orden`; esa columna representa copago por item/prestacion. Pero si la pantalla muestra importe de copago por orden sin mostrar su tipo (`pago_copa`, por ejemplo `A` adelantado), es una deficiencia UX/visualizacion recomendable de reportar como mejora/ajuste.

- Usuario en Google Chat directo identificado como Maru en conversacion actual.
- Se respondio consulta sobre paso a paso de busqueda de convenio en trunk para carga de prestaciones en expedientes de ordenes de circulo: efector elegido filtra categorias y lugares, pero `wf_convenios(ai_row)` usa `exp.prestad` como prestador para convenio, `dw_det.object.lugar[1]` como lugar y `dw_det.object.efe_cate[1]` como categoria. Evidencia principal en `wp_expe_orde.srw` y validacion/grabacion con `val.of_orden_a_liquidar(..., exp.prestad, str_i.lugar, ..., li_cate, ...)`.
- Se verifico en trunk revision r84555 la validacion de expedientes de sanatorio/internacion al ingresar codigo de autorizacion y salir del campo `auto`: evento `itemchanged` del DW `d_expe_sana_tot` en `wp_expe_sana.srw`. Validaciones principales: fecha obligatoria si regla 17, lectura UOCRA por regla 112, normalizacion de sucursal/auto, duplicado en `expe_internaciones`, recupero/legajo, existencia con `val.of_autoriz`, prepaga/entidad del expediente, para circulo validacion de `exp.prestad` contra `au.efe_sana` y `au.efe_sana_lugar` mediante `wf_valida_prestador` / `prs_valida_lugares_x_prestador_circulo`, control de efector distinto para no circulo, carga de efector/lugar desde autorizacion, plan/credencial/gravado, advertencia por tipos P/E/S no internacion, uso previo, historia, Recupero SUR, normas de convenio y recarga de lugares.
- Codigo puntual de advertencia por autorizacion no internacion mostrado a Maru: en `wp_expe_sana.srw`, caso `auto`, `IF au.tipo='P' THEN mbox(...'La autorización es de Prestaciones'...) ELSEIF au.tipo='E' THEN ... 'Excepciones' ELSEIF au.tipo='S' THEN ... 'Situaciones terapeúticas' END IF`; solo advierte y no corta flujo.
- Se listaron perfiles de BBDD cargados y luego se agrego perfil `Montegrande_Prod`: Motor MS SQL, host `172.16.10.120`, puerto `1433`, base `Montegrande_Prod`, usuario `sa`, notas `Montegrande_Prod`. Validacion de conexion OK con `SELECT DB_NAME()`. Archivo modificado: `/home/desarrollo-ia-07/.openclaw/workspace/DB_PROFILE.md`, profile ubicado aprox. linea 421.
- RM28620: Maru pidio analizar defecto en trunk usando BBDD `Montegrande_Prod` y dejar DOCX en `/mnt/Agentes/mpariso/Incidentes/RM28620/`. Resultado generado: `/mnt/Agentes/mpariso/Incidentes/RM28620/RM28620 - Análisis Bender.docx`.
- Conclusion RM28620: el filtro de forma de pago llega correctamente desde pantalla al SP `afs_compro_deudas_pf`; la causa esta en el SP, que determina la forma de pago vigente contra `P.factu_perio` tomado de periodos historicos de `compro_impresion`, pero calcula deuda sobre los ultimos cuatro periodos del reporte. Un contrato con historial de formas de pago, como contrato `5429`, puede aparecer filtrando por forma anterior `AB` y tambien por vigente `E`.
- Solucion propuesta RM28620: ajustar `afs_compro_deudas_pf` para evaluar vigencia de `afi_pago_formas` contra la maxima fecha considerada por el reporte (`@var_perio1`), reemplazando `apf2.vigen_desde <= P.factu_perio` por `apf2.vigen_desde <= @var_perio1` en las dos ramas del `INSERT`.
- Validaciones RM28620: Redmine consultado, trunk actualizado a revision r84559, DTI local revisado, PB/SQL analizado, datos verificados en `Montegrande_Prod`, DOCX validado como Word correcto. Update del repo de Incidentes fallo por autenticacion SVN; se uso la copia local disponible del DTI.
