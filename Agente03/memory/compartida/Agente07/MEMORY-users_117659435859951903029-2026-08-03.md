# Memoria curada diaria

- Fecha: 2026-08-03
- Usuario: Juana Virginia Mendoza
- id_googlechat: users/117659435859951903029
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/5gqh-kaaaae

## Resumen de la actividad

### Tema 1: Saludo inicial y preferencias

- Solicitud o contexto: Juana inició la conversación con un saludo.
- Respuesta o acciones del agente: Se ejecutó el bootstrap operativo y se cargaron sus preferencias.
- Resultado: El agente confirmó disponibilidad.
- Estado: Informativo

### Tema 2: Uso de la marca `Por defecto` en sistemas de derivación de aportes

- Solicitud o contexto: Juana preguntó para qué sirve la marca `Por defecto` en Afilmed, módulo `Configuración / Aportes / Sistema de derivación de aportes`.
- Respuesta o acciones del agente: Se revisó código PowerBuilder y SQL relacionado con `deriv_aportes_sistema`. Se identificó que la marca se guarda en `DERIV_APORTES_SISTEMA.defecto` con valores `S/N`, que al grabar se valida que no haya otro sistema activo marcado por defecto, y que procesos automáticos de aportes de desempleo y jubilado seleccionan el sistema con `defecto = 'S'`.
- Resultado: Se explicó que la marca define el sistema predeterminado para procesos automáticos cuando no se elige una derivación específica.
- Estado: Resuelto

### Tema 3: Archivo de importación de transferencias para Redmine `R30030`

- Solicitud o contexto: Juana pidió armar un archivo igual al modelo de `A:\jvmendoza\Testing\R30030` para el contrato `85332` en `SMC_PROD_TEST`, destinado a importar desde trunk, con nombre `Transferencia30030.txt`.
- Respuesta o acciones del agente: Se tomó como modelo `Transferencia.txt`, se reemplazó el CUIL aportante por el titular del contrato `85332`, se generó un nuevo control de cabecera/pie porque el del modelo ya estaba importado y se validó contra `SMC_PROD_TEST` sin exponer credenciales.
- Resultado: Se creó `/mnt/Agentes/jvmendoza/Testing/R30030/Transferencia30030.txt`, ASCII, 13 líneas CRLF, mismo tamaño y longitudes que el modelo. Se validó que el control nuevo no existiera en `afi_aportes_real_lote` y que `afs_valida_cuil_ddjj_trans` resolviera el CUIL contra contrato `85332`, integrante `00`, sin rechazo. No se hicieron escrituras en base.
- Estado: Resuelto

### Tema 4: Casos de prueba para Redmine `29348`

- Solicitud o contexto: Juana pidió armar casos de prueba para la solución del Redmine `29348`, usando la plantilla `MatrizModelo.xlsx` ubicada en `A:\jvmendoza\Testing`, dejando un Word llamado `Casos_R29348` en `A:\jvmendoza\Testing\R29348`. Indicó que probaría en `MutualRioIV_Prod_Test` con Afilmed de trunk.
- Respuesta o acciones del agente: Se revisó el Redmine `29348`, el SP de trunk `afs_export_recaudo_cab` y datos base en `MutualRioIV_Prod_Test`. Se generó un documento Word siguiendo las columnas de la plantilla: `#`, `Descripcion`, `Precondiciones`, `Pasos a Seguir`, `Resultado Esperado`.
- Resultado: Se creó `/mnt/Agentes/jvmendoza/Testing/R29348/Casos_R29348.docx` con 9 casos de prueba y se validó que el `.docx` fuera un paquete Word válido con la matriz incluida.
- Estado: Resuelto
