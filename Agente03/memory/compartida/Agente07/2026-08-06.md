
## 2026-08-06 - Conversacion Google Chat directa

- Usuario identificado como Maru. Prefiere respuestas tecnicas, directas y con evidencia en rama/manuales cuando consulta comportamiento funcional.
- Se registro el profile de BBDD `Ospicine_Prod` en `DB_PROFILE.md` y se valido conexion OK. Datos no sensibles utiles: motor MS SQL, host `172.16.10.108`, puerto `1433`, base `Ospicine_Prod`, usuario `sa`, notas `Ospicine_Prod`. No almacenar ni repetir password.
- Analisis previo sobre `liquida_efector_diferente`: en `trunk` r84262, previo a RM28808, el parametro se usa en `wp_expe_orde.srw` al cargar ordenes de expediente. Si prestador/efector de autorizacion difiere del prestador del expediente y no es prestador circulo, con valor `S` permite continuar con confirmacion; con `N` o default bloquea el ingreso. Fue incorporado por RM `#21807`: r74100 agrega parametro default `N` en `parametros_generales.sql`; r74101 modifica `wp_expe_orde.srw`. RM28808 no incorporo el parametro.
- Analisis RM30191: usando documentacion de Incidentes, rama `trunk` y BBDD `Ospicine_Prod`, la hipotesis del documento de testeo era correcta: el problema depende del orden de lineas en importacion DDJJ. `wp_impor_ddjj.srw` arma listas en orden de DataWindow, `afi_aportes_ddjj_lista` consume token por token y llama `afi_aportes_ddjj`. El SP baja aportes existentes solo si `aap.origrect <= tmp.origrect`; si llega rectificativa antes que original, el original queda activo sin `baja_fecha`. En lote `20260805134614157` los 7 casos documentados reproducian el sintoma; ademas habia 2 originales activos mas del mismo lote que chocaban contra rectificativas activas de lotes anteriores. El script propuesto no era aprobable como produccion: sin filtro por lote afectaria 27 originales activos; el filtro `AND o.lote = 20260805134614157` estaba documentado fuera del bloque y no limitaba realmente el update; solo baja `origrect = 0`; no define criterio contra lotes anteriores; no actualiza `baja_usuario`; usa `;` fuera del estandar local. No se ejecuto UPDATE en produccion. El comportamiento no fue introducido por RM reciente: condicion `origrect <= @origrect` existe al menos desde trunk r39315 (2013); #28598 y cambios recientes no modificaron esa regla.
- Analisis copagos en rama `V25.08.001`: `franquicia` en copagos por plan/prestacion es un importe fijo adicional que se suma al copago (`copago + franquicia`), comparte criterio/vigencia/forma de pago y no aplica a copagos por orden. Tablas relevantes: `cober_copagos.sql`, `cober_prestad_copagos.sql`; SPs: `prs_valida_of_copagos_row.SQL`, `pru_expe_copagos.sql`.
- Copagos por plan/orden en `V25.08.001`: `ciclo` y `ciclo_valor` pertenecen a `COBER_ORDEN_COPAGOS`; el copago se calcula agrupado por orden, no por prestacion individual. `ciclo` define cantidad/bloques normales y `ciclo_valor` el importe excedente por lo que supera los ciclos. Ejemplo validado: orden con 9 prestaciones, copago $5000 cada 3 prestaciones, 2 ciclos normales y excedente $2000: prestaciones 1-3 = $5000, 4-6 = $5000, 7-9 = $2000 c/u; total $16000.
- Exencion de copago por afiliado en Afilmed `V25.08.001`: se configura desde `Afilmed -> Tarifas -> Exenciones de copagos`. En codigo: `m_afilia.srm` menu `m_exencionesdecopagos` cuelga de `m_tarifas`, texto visible `Exenciones de copagos`, click abre `wp_abm_afi_exen_copagos`; ventana titulada `Exenciones de copagos`. Manual: `Manuales/Afilmed/9. Tarifas/Tarifas - Tarifas.docx` indica `Tarifas -> Exencion de copagos`. Consulta/listado: `Afilmed -> Tarifas -> Consultas -> Exenciones de copagos`, abre `wp_cons_afi_exen_copagos`.
