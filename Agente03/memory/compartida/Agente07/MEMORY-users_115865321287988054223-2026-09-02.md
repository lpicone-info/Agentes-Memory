# Memoria de conversación — Patricia de Lourdes Bossa

## Datos

- **Fecha:** 02/09/2026
- **Usuario:** Patricia de Lourdes Bossa
- **ID Google Chat:** `users/115865321287988054223`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 17
- **Inicio:** 09:55:29
- **Fin:** 16:53:30

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/jrvbkkaaaae`

## Archivos revisados

- `393cb62f-c524-4f11-908c-db2da8cfbbaa.jsonl`

---

## Transcripción completa

### [02/09/2026 09:55:29] Patricia de Lourdes Bossa

hola

---

### [02/09/2026 09:57:36] Agente

Hola Pato, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [02/09/2026 09:58:33] Patricia de Lourdes Bossa

estoy probando un caso del RM30265 en smc_prox_ver, de una autorizacion que tiene dos sucursales, decime si es correcto y por qué que me muestre este mensaje que te muestro en el print. El prestador que ingresa es el 1992:

---

### [02/09/2026 09:59:29] Agente

Pato, con lo que se ve en el print, el mensaje **puede ser correcto**, pero depende de un dato clave: si la autorización ingresada pertenece realmente al prestador `1992` en alguna de sus sucursales/lugares de atención.

La regla funcional detrás del mensaje es esta: el portal toma el prestador logueado `1992` y valida que la autorización buscada esté asociada a ese prestador. Si encuentra la autorización pero asociada a otro prestador, otro lugar de atención o una sucursal que no corresponde, muestra:

`La autorización no pertenece al prestador`

Ahora bien, para el **RM30265**, hay una salvedad importante: ayer quedó identificado que en `prox_ver` el cambio no parecía estar publicado completo. El flujo viejo del portal todavía podía estar enviando la `sucur` seleccionada junto con el número de autorización. Entonces, si la misma autorización existe en más de una sucursal, el portal puede estar validando contra la sucursal equivocada y devolver ese mensaje aunque la autorización exista para el prestador en otra sucursal.

Conclusión técnica:

- Si la autorización **no pertenece al prestador 1992**, el mensaje es correcto.
- Si la autorización **sí pertenece al prestador 1992 pero en otra sucursal**, entonces el mensaje probablemente evidencia que el RM30265 no está aplicado correctamente en ese `prox_ver`.

Para confirmarlo con certeza necesito el **número de autorización** que ingresaste en la prueba. Ahí se valida en `SMC_Prox_Ver` contra `sucur`, `auto` y prestador `1992`.

---

### [02/09/2026 10:01:39] Patricia de Lourdes Bossa

es la 160

---

### [02/09/2026 10:06:00] Agente

Pato, para la autorización `160` el mensaje **es correcto**.

Validé en `SMC_Prox_Ver`:

- La autorización `160` existe en varias sucursales.
- Las vigentes están en sucursales `14`, `20`, `21`, `23` y `25`.
- Ninguna tiene como prestador efectivo al `1992`.
- En la sucursal `23` aparece `ori_prestad = 1992`, pero el prestador efectivo es `2056`.

El portal valida contra el prestador efectivo de la autorización: `ISNULL(efe_prestad, efe_sana)`. No valida contra el prestador originante/demandante. Por eso, aunque el `1992` figure relacionado como `ori_prestad`, para operar esa autorización no alcanza: la autorización pertenece efectivamente al prestador `2056`.

También probé el SP `ars_auto_detalle` con:

`auto = 160`, `prestad = 1992`, `sucur = NULL`

y devuelve directamente:

`La autorización no pertenece al prestador`

Conclusión: si el usuario logueado es el prestador `1992`, el mensaje que ves en el print corresponde. El caso RM30265 corrige la búsqueda por sucursal cuando el número está repetido, pero no cambia la regla de pertenencia al prestador efectivo.

---

### [02/09/2026 10:46:14] Patricia de Lourdes Bossa

genera una query sobre la BD smc_prox_ver que me de autorizaciones sin saldo, proba que devuelva datos y que no de error

---

### [02/09/2026 10:47:58] Agente

Pato, la probé en `SMC_Prox_Ver`: devuelve datos y no da error. Primer resultado validado: sucursal `21`, auto `2049519`, prestador `1658`, cantidad autorizada `1`, consumida `1`, saldo `0`.

Query:

```sql
WITH consumos AS (
    SELECT sucur, auto, nomen, prestac, pieza, cara, canti_consumida = SUM(canti)
    FROM (
        SELECT AR.sucur_rel sucur, AR.auto_rel auto, APIR.nomen, APIR.prestac,
               ISNULL(APIR.pieza, 0) pieza, ISNULL(APIR.cara, '0') cara,
               SUM(ISNULL(APIR.canti, 0)) canti
        FROM AUTO_REMOTAS AR
        JOIN AUTO_PRESTAC_ITEMS_REMOTAS APIR ON APIR.transac = AR.transac
        WHERE AR.tipo_transac IN ('02A', '02D')
          AND AR.esta = 'A'
          AND AR.baja_fecha IS NULL
          AND APIR.esta = 'A'
          AND APIR.baja_fecha IS NULL
        GROUP BY AR.sucur_rel, AR.auto_rel, APIR.nomen, APIR.prestac,
                 ISNULL(APIR.pieza, 0), ISNULL(APIR.cara, '0')

        UNION ALL

        SELECT EO.sucur, EO.auto, EOI.nomen, EOI.prestac,
               0 pieza, '0' cara, SUM(ISNULL(EOI.canti, 0)) canti
        FROM EXPE_ORDENES EO
        JOIN EXPE_ORDEN_ITEMS EOI
          ON EOI.ofi = EO.ofi
         AND EOI.expe = EO.expe
         AND EOI.item = EO.item
        WHERE EO.baja_fecha IS NULL
          AND EOI.baja_fecha IS NULL
          AND EOI.ok = 'S'
        GROUP BY EO.sucur, EO.auto, EOI.nomen, EOI.prestac

        UNION ALL

        SELECT EO.sucur, EO.auto, EOI.nomen, EOI.prestac,
               ISNULL(EOI.pieza, 0) pieza, ISNULL(EOI.cara, '0') cara,
               SUM(ISNULL(EOI.canti, 0)) canti
        FROM EXPE_ODON EO
        JOIN EXPE_ODON_ITEMS EOI
          ON EOI.ofi = EO.ofi
         AND EOI.expe = EO.expe
         AND EOI.item = EO.item
        WHERE EO.baja_fecha IS NULL
          AND EOI.baja_fecha IS NULL
        GROUP BY EO.sucur, EO.auto, EOI.nomen, EOI.prestac,
                 ISNULL(EOI.pieza, 0), ISNULL(EOI.cara, '0')
    ) X
    GROUP BY sucur, auto, nomen, prestac, pieza, cara
),
items_saldo AS (
    SELECT A.sucur, A.auto, A.fecha, A.esta, A.tipo,
           prestad = ISNULL(A.efe_prestad, A.efe_sana),
           API.nomen, API.prestac,
           canti_autorizada = ISNULL(API.canti, 0),
           canti_consumida = ISNULL(C.canti_consumida, 0),
           saldo = ISNULL(API.canti, 0) - ISNULL(C.canti_consumida, 0)
    FROM AUTORIZACIONES A
    JOIN AUTO_PRESTAC_ITEMS API
      ON API.sucur = A.sucur
     AND API.auto = A.auto
     AND API.baja_fecha IS NULL
    LEFT JOIN consumos C
      ON C.sucur = API.sucur
     AND C.auto = API.auto
     AND C.nomen = API.nomen
     AND C.prestac = API.prestac
     AND C.pieza = API.pieza
     AND C.cara = API.cara
    WHERE A.baja_fecha IS NULL
      AND A.tipo = 'P'
      AND A.esta = 'A'
)
SELECT TOP 50
       sucur, auto, fecha, prestad,
       items = COUNT(1),
       canti_autorizada = SUM(canti_autorizada),
       canti_consumida = SUM(canti_consumida),
       saldo_total = SUM(saldo),
       saldo_max_item = MAX(saldo)
FROM items_saldo
GROUP BY sucur, auto, fecha, prestad
HAVING MAX(saldo) <= 0
ORDER BY fecha DESC, sucur, auto;
```

Criterio: trae autorizaciones de prestaciones activas donde ningún ítem tiene saldo disponible para consumirse en `02A/02D`.

---

### [02/09/2026 12:45:19] Patricia de Lourdes Bossa

explicame funcionalmente que condiciones previas debe haber para que se cargue un egreso de internacion en el Portal del Prestador, por ejemplo la autorizacion , los datos que tiene que tener cargada la misma, etc

---

### [02/09/2026 12:46:05] Patricia de Lourdes Bossa

explicame funcionalmente que condiciones previas debe haber para que se cargue un egreso de internacion en el Portal del Prestador, por ejemplo la autorización , los datos que tiene que tener cargada la misma, etc. En version 25.08.001

---

### [02/09/2026 12:49:14] Agente

Pato, en versión `25.08.001`, para poder cargar un **egreso de internación** en el Portal del Prestador (`02E`) tienen que darse estas condiciones funcionales:

1. Debe existir una autorización válida.
La autorización se busca por número y el sistema resuelve la sucursal automáticamente, salvo el caso particular de otras transacciones. Para egreso (`02E`) no debería depender de que el usuario cargue manualmente la sucursal.

2. La autorización debe pertenecer al prestador logueado.
El portal compara el prestador efectivo de la autorización contra el prestador de sesión. Si es centro de atención, valida que el prestador efectivo pertenezca al centro.

3. La autorización puede ser de internación/cirugía, no exige que sea tipo prestación.
En `02E` no aplica la validación “la autorización debe ser de prestaciones”. Eso está exceptuado para egresos.

4. La autorización no puede estar rechazada.
Si la autorización está en estado `R`, el portal no permite informar el egreso.

5. La autorización debe tener fecha de internación cargada.
Este es el punto clave. Si `inter_fecha` viene vacía, el portal muestra:
`La autorización no posee fecha de internación`

Funcionalmente significa que primero tuvo que haberse registrado el ingreso/internación sobre esa autorización.

6. La autorización no debe tener un egreso ya registrado.
Si ya existe un registro activo en `auto_egresos` para esa sucursal/autorización, el portal rechaza con:
`Autorización con egreso registrado`

7. Al informar el egreso, el usuario debe cargar datos propios del egreso:
Fecha de egreso, hora de egreso, al menos un diagnóstico de egreso y motivo de egreso.

8. La fecha de egreso debe ser coherente.
La base valida que la fecha de egreso:
- no sea menor que la fecha de internación;
- no sea mayor que la fecha del día.

En resumen funcional: para cargar un egreso, la autorización tiene que ser una autorización existente, vigente para el prestador que opera, no rechazada, con internación ya informada (`inter_fecha` cargada), sin egreso previo activo, y el usuario debe completar fecha/hora/motivo/diagnóstico de egreso.

---

### [02/09/2026 16:38:10] Patricia de Lourdes Bossa

tengo esta autorizacion: 2049709 de la base smc_prox_ver qye quiero cargar en el portal de prestadores, en la transaccion 03A y me dice autorizacion inexistene como muestra el prit debajo. A que se debe? estoy entrando con un usuario del prestador 52

---

### [02/09/2026 16:42:58] Agente

Pato, se debe a la **sucursal**, no al prestador.

Validé en `SMC_Prox_Ver`:

- La autorización `2049709` existe.
- Está en `sucur = 1` (`CASEROS`).
- Es tipo `P`, estado `A`.
- Pertenece al prestador efectivo `52`.
- Tiene prestación `420101`.

Pero el usuario con el que entrás, `a_uSMCPxV_52`, tiene sucursal de sesión:

`17 - WEBPOSSUC`

Y la pantalla `03A` busca la autorización llamando a:

```sql
EXEC ars_auto_detalle @prm_sucur = ?, @prm_auto = ?
```

Ese SP filtra así:

```sql
WHERE A.sucur = @prm_sucur
  AND A.auto = @prm_auto
```

Probé:

```sql
EXEC ars_auto_detalle @prm_sucur = 17, @prm_auto = 2049709
```

y no devuelve filas. Por eso el portal muestra **“Autorización inexistente”**.

Con la sucursal correcta sí aparece:

```sql
EXEC ars_auto_detalle @prm_sucur = 1, @prm_auto = 2049709
```

Conclusión funcional: la autorización existe y es del prestador `52`, pero para `03A` el portal la está buscando bajo la sucursal del usuario (`17`) y la autorización está cargada en sucursal `1`. Por eso la trata como inexistente.

---

### [02/09/2026 16:46:37] Patricia de Lourdes Bossa

explicame funcionalmente para que usan la transaccion 03A

---

### [02/09/2026 16:48:11] Agente

Pato, funcionalmente la transacción `03A` se usa para **consultar la resolución/estado de una autorización ya generada**.

No sirve para pedir una autorización nueva ni para registrar una prestación. Para eso están otras transacciones:

`02C` solicita autorización de prestación.  
`02I` solicita autorización de internación.  
`02A` registra/consume una prestación.  
`03A` consulta cómo quedó una autorización existente.

En el flujo, la `03A` toma una preautorización/autorización informada y valida:

- que exista la autorización relacionada;
- que no esté dada de baja;
- que corresponda al mismo afiliado informado;
- que el prestador sea válido;
- que los ítems de la autorización no estén rechazados o pendientes.

Según eso devuelve el estado funcional:

- autorización inexistente;
- solicitud anulada;
- solicitud no correspondiente al asociado;
- solicitud rechazada;
- solicitud pendiente;
- o autorización aprobada/consultable.

Por eso en tu caso anterior con la autorización `2049709`, la `03A` no estaba intentando autorizar algo nuevo: estaba intentando **rescatar/consultar esa autorización**. Fallaba porque la buscaba en la sucursal de sesión `17`, pero la autorización real estaba en sucursal `1`.

Referencia técnica: en versión `25.08.001`, `03A` figura como `Consulta resolución` y ejecuta el SP `prs_cons_auto_remotas`.

---

### [02/09/2026 16:52:38] Patricia de Lourdes Bossa

porque esta relacionado la sucursal del usuario? para la busqueda el usaurio solo puede consultar autorizaciones generadas a traves de la suc 17: webpos?

---

### [02/09/2026 16:53:30] Agente

Sí, Pato. Está relacionado porque en `03A` la autorización no se busca solo por número, sino por:

`sucursal + número de autorización`

Funcionalmente, el número de autorización no se trata como único global. La sucursal forma parte de la identificación de la autorización. Entonces, si la pantalla/servicio arma la consulta con `sucur = 17`, va a buscar:

`17 / 2049709`

aunque la autorización real exista como:

`1 / 2049709`

Por eso devuelve “autorización inexistente”.

No significa exactamente que el prestador `52` **solo pueda consultar autorizaciones generadas por la sucursal 17** por una regla funcional de permisos. Lo que pasa es más técnico-funcional: si el flujo no informa o no resuelve la sucursal real de la autorización, toma la sucursal asociada/default del usuario o del WebPos, que en tu caso es `17 - WEBPOSSUC`. Con esa sucursal arma la relación y busca mal.

Para que la `03A` consulte correctamente una autorización de otra sucursal, debería pasar o resolver la sucursal real de la autorización. En tu caso, debería buscar `sucur = 1`, no `17`.

Conclusión: el prestador `52` sí corresponde a la autorización, pero la búsqueda de `03A` queda condicionada por la sucursal usada para identificarla. Si usa `17`, solo va a encontrar autorizaciones relacionadas bajo `17`; no va a encontrar la `2049709` porque está cargada en `1`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
