# Memoria de conversación — Patricia de Lourdes Bossa

## Datos

- **Fecha:** 12/08/2026
- **Usuario:** Patricia de Lourdes Bossa
- **ID Google Chat:** `users/115865321287988054223`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 23
- **Inicio:** 09:37:54
- **Fin:** 16:14:43

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/jrvbkkaaaae`

## Archivos revisados

- `6dd59fba-8ae3-4349-9911-d652daae1151.jsonl`

---

## Transcripción completa

### [12/08/2026 09:37:54] Patricia de Lourdes Bossa

Hola

---

### [12/08/2026 09:39:02] Agente

Hola Pato, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [12/08/2026 09:40:20] Patricia de Lourdes Bossa

dame la info de este SP afs_valida_valor_descuento sobre la base Premedic_prod_test, indicame como toma los valroes de descuento considerando el parametro de edad tope calculada

---

### [12/08/2026 09:42:06] Agente

Pato, en `Premedic_Prod_Test` el SP `dbo.afs_valida_valor_descuento` existe y el parámetro actual:

`dbo.fn_get_parametros_general('Afilmed', 'Facturacion', 'Edad Tope Calculada') = 'N'`

Con ese valor actual, el SP **no usa la edad calculada para reemplazar el tope**. Toma el `@tope` desde los datos vigentes de solicitud/afiliación y busca el descuento por rango de tope.

**Cómo toma el descuento**

Para parentescos cuyo `PARENTESCOS.tipo_fliar = 'GFP'`, busca en `TARI_FAMILIAR_DESC`:

```sql
A.tope = (
  SELECT MIN(tope)
  FROM TARI_FAMILIAR_DESC
  WHERE tope >= @tope
)
```

Es decir: toma el menor `tope` configurado que sea mayor o igual al `@tope` usado por el SP. De esa fila obtiene:

- `@valor = TARI_FAMILIAR_DESC.valor`
- `@vigen_valor = TARI_FAMILIAR_DESC.vigen_desde`
- `@var_cod_nota_desc = TARI_FAMILIAR_DESC.cod_deno_desc`

Para familiares/adicionales que no son `GFP`, busca en `TARI_FAMI_ADIC_DESC`, pero ahí no usa `@tope`: usa directamente la edad del integrante:

```sql
tope >= @var_edad_integrante
```

**Efecto de Edad Tope Calculada**

Solo aplica en el bloque `GFP` y únicamente si se cumplen ambas condiciones:

```sql
@var_parama_edad_tope_calculada = 'S'
AND dbo.afn_puede_cambiar_tope(...) = 1
```

Si eso pasa, el SP reemplaza:

```sql
SET @tope = @var_edad_busqueda
```

`@var_edad_busqueda` sale de:

- edad del titular, por defecto
- si `TARI_FAMILIAR.etario = 'A'` y existe cónyuge, usa la mayor edad entre titular y cónyuge

Pero en esta base el parámetro está en `N`, por lo tanto ese reemplazo no se ejecuta.

Además, `afn_puede_cambiar_tope` usa `PARAM_AFI`: `Max_Edad_Cambio_Tope = 65`, `Max_Antig_Cambio_Tope = 10`, `ley_prepagas = 'S'`. Si el afiliado tiene edad mayor a 65 y antigüedad mayor o igual a 10, devuelve `0`, y el SP tampoco cambia el tope.

Validación ejecutada: lectura directa del SP, funciones auxiliares y parámetros en `Premedic_Prod_Test`.

---

### [12/08/2026 09:50:23] Patricia de Lourdes Bossa

decime que pasaria si estuviera el parametro en S

---

### [12/08/2026 09:50:35] Agente

Pato, si el parámetro `Edad Tope Calculada` estuviera en `S`, el SP podría reemplazar el `@tope` original por una edad calculada, pero solo para el grupo familiar principal (`tipo_fliar = 'GFP'`) y solo si:

```sql
dbo.afn_puede_cambiar_tope(@prepaga, @contra, @inte, @perio) = 1
```

En ese caso haría esto:

```sql
SET @tope = @var_edad_busqueda
```

`@var_edad_busqueda` se calcula así:

- Si la tarifa familiar tiene `etario = 'A'` y hay cónyuge, toma la mayor edad entre titular y cónyuge.
- En cualquier otro caso, toma la edad del titular.

Después busca el descuento en `TARI_FAMILIAR_DESC` usando ese nuevo `@tope`:

```sql
MIN(tope) WHERE tope >= @tope
```

Ejemplo: si el tope original era `30`, pero la edad calculada del titular/cónyuge es `47`, buscaría el primer registro de descuento cuyo `tope` sea `>= 47`. Entonces podría devolver un descuento distinto al que correspondería con el tope original.

La función `afn_puede_cambiar_tope` devuelve `0` cuando aplica la restricción de ley de prepagas: edad mayor a `65` y antigüedad mayor o igual a `10` años. En ese caso, aunque el parámetro esté en `S`, el SP no reemplaza el tope.

---

### [12/08/2026 09:57:59] Patricia de Lourdes Bossa

que significa etario = 'A'

---

### [12/08/2026 09:58:14] Agente

Pato, en este SP `etario = 'A'` significa que el cálculo del rango etario para el descuento se hace tomando como referencia al **adulto de mayor edad del grupo principal**.

La lógica concreta es:

```sql
IF @var_etario = 'A' AND @var_edad_conyugue IS NOT NULL
```

Si se cumple, compara:

- edad del titular
- edad del cónyuge

Y usa la mayor:

```sql
@var_edad_busqueda = mayor edad entre titular y cónyuge
```

Si `etario` no es `'A'`, o no hay cónyuge, usa la edad del titular.

No puedo afirmar solo por el código si la letra `A` significa formalmente “Adulto”, “Ambos” u otra denominación funcional; lo verificable en el SP es que con `A` evalúa titular vs cónyuge y toma la mayor edad.

---

### [12/08/2026 10:55:46] Patricia de Lourdes Bossa

agrega en mi archivo de preferencias lo siguiente: Cuando pbossa requiera armar criterios de aceptación o casos de
prueba en base a algún documento de especificación funcional, en relación a
algún RM, utilizar el formato Gherkin, tomando como ejemplo o plantilla los
criterios de aceptación de la EF correspondiente al RM 28243, sección 7, y
dejar un documento con el nombre "Criterios de aceptación IA.docx" en
una nueva subcarpeta llamada RMxxxxx (donde xxxxx debe ser reemplazado por el
número de redmine al que pertenezca la especificación funcional sobre la que se
armarán los criterios de aceptación), dentro de la carpeta /mnt/Agentes/pbossa/Incidentes.
Los criterios de aceptación deben ser redactados de forma clara, orientados a
analistas funcionales que deberán tomar esos criterios de aceptación y hacer la
ejecución de las pruebas, con el máximo nivel de refinamiento posible.

---

### [12/08/2026 10:57:18] Patricia de Lourdes Bossa

agrega en mi archivo de preferencias lo siguiente: Cuando pbossa requiera armar criterios de aceptación o casos de
prueba en base a algún documento de especificación funcional, en relación a
algún RM, utilizar el formato Gherkin, tomando como ejemplo o plantilla los
criterios de aceptación de la EF correspondiente al RM 28243, sección 7, y
dejar un documento con el nombre "Criterios de aceptación IA.docx" en
una nueva subcarpeta llamada RMxxxxx (donde xxxxx debe ser reemplazado por el
número de redmine al que pertenezca la especificación funcional sobre la que se
armarán los criterios de aceptación), dentro de la carpeta /mnt/Agentes/pbossa/Incidentes.
Los criterios de aceptación deben ser redactados de forma clara, orientados a
analistas funcionales que deberán tomar esos criterios de aceptación y hacer la
ejecución de las pruebas, con el máximo nivel de refinamiento posible.

---

### [12/08/2026 10:58:02] Agente

Pato, agregué la preferencia en `/mnt/Agentes/pbossa/preferencias/preferencias.md`.

Validación ejecutada: el archivo quedó en `us-ascii`, sin BOM y con finales de línea `LF`, igual que antes.

---

### [12/08/2026 11:01:59] Patricia de Lourdes Bossa

en presmed, version productiva, decime que Sp se ejcuta para la busqueda de autorizaciones en la siguiente pantalla:

---

### [12/08/2026 11:05:46] Agente

Pato, la búsqueda de autorizaciones en esa pantalla ejecuta el SP:

`dbo.prs_auto_pedidos`

La cadena técnica es:

En `wp_auto_pedidos.srw`, el evento `ue_aceptar` toma los filtros de pantalla y ejecuta:

```powerscript
dw_det.Retrieve(ld_desde, ld_hasta, ls_tipo, ls_con_auto, li_operador, ls_baja, li_entidad, ls_requiere_material, ls_estado, ls_contra, ls_inte)
```

Ese `dw_det` usa el DataWindow `d_auto_pedidos_lst`, que tiene definido:

```sql
execute dbo.prs_auto_pedidos
```

con estos parámetros:

```sql
@prm_fecha_desde
@prm_fecha_hasta
@prm_tipo
@prm_con_Auto
@prm_operador
@prm_bajas
@prm_entidad
@prm_requiere_material
@prm_estado
@prm_contra
@prm_inte
```

Observación importante: en el código productivo registrado no encontré que esa llamada envíe parámetro de `Localidad`. El filtro resaltado en la imagen no aparece en los DataWindows productivos registrados que revisé (`trunk` y `V25.08.001`). Con esa evidencia, la búsqueda base sigue pasando por `prs_auto_pedidos`, pero esa versión visual podría tener una modificación no reflejada en la working copy disponible.

---

### [12/08/2026 11:23:03] Patricia de Lourdes Bossa

para que tome en cuenta un nuevo filtro localidad que modificamos le tenemos que hacer al SP de busqueda de pedidos, y otra consulta esa busqueda se ejeucta desde el boton Buscar?

---

### [12/08/2026 11:25:58] Agente

Pato, sí: para que el nuevo filtro `Localidad` afecte la búsqueda, hay que tocar la cadena completa, no solo la pantalla.

Impacto mínimo:

1. Agregar el argumento `localidad` en el DataWindow `d_auto_pedidos_lst`.
2. Pasarlo desde `wp_auto_pedidos.srw` en el `dw_det.Retrieve(...)`.
3. Agregar el parámetro en `dbo.prs_auto_pedidos`.
4. Incorporar el filtro en el `WHERE` del SP, usando la tabla/campo correcto según qué represente “Localidad” en la pantalla: localidad del afiliado, prestador, entidad, domicilio, etc.

La búsqueda sí se ejecuta desde el botón `Buscar`, pero de forma heredada:

- El botón es `cb_buscar`, con texto `&Buscar`: [wp_auto_pedidos.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/exe/Source%20Infomedical/wp_auto_pedidos.srw:914)
- Su `clicked` hace `call super::clicked`: [wp_auto_pedidos.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/exe/Source%20Infomedical/wp_auto_pedidos.srw:924)
- La clase base `cb_ace_clase.clicked` ejecuta `parent.postevent("ue_aceptar")`: [cb_ace_clase.sru](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/exe/Source%20Infomedical/cb_ace_clase.sru:42)
- En `ue_aceptar`, la ventana hace el `dw_det.Retrieve(...)`: [wp_auto_pedidos.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/exe/Source%20Infomedical/wp_auto_pedidos.srw:515)
- Ese DataWindow ejecuta `dbo.prs_auto_pedidos`: [d_auto_pedidos_lst.srd](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/exe/Source%20Infomedical/d_auto_pedidos_lst.srd:47)

Conclusión: el botón `Buscar` dispara `ue_aceptar`, `ue_aceptar` ejecuta el `Retrieve`, y el `Retrieve` termina llamando al SP `prs_auto_pedidos`.

---

### [12/08/2026 11:32:53] Patricia de Lourdes Bossa

dentro de la version productiva, ese sp SP prs_auto_pedidos, se usa en otra ventana?

---

### [12/08/2026 11:33:56] Agente

Pato, en la versión productiva `mpariso-V25.08.001`, con la evidencia de fuentes disponibles, el SP `dbo.prs_auto_pedidos` **no se usa en otra ventana**.

Encontré una sola llamada al SP:

- `d_auto_pedidos_lst.srd` ejecuta `dbo.prs_auto_pedidos`.
- Ese DataWindow está asignado únicamente en `wp_auto_pedidos.srw`.

Referencia clave:

```powerscript
string dataobject = "d_auto_pedidos_lst"
```

en `wp_auto_pedidos.srw`.

Conclusión técnica: si modifican `prs_auto_pedidos` para agregar el filtro `Localidad`, el impacto directo en esta versión productiva queda acotado a la pantalla `wp_auto_pedidos`, siempre que no exista alguna carga dinámica externa no presente en las fuentes exportadas.

---

### [12/08/2026 15:18:45] Patricia de Lourdes Bossa

Hola

---

### [12/08/2026 15:19:00] Agente

Hola Pato, estoy disponible. ¿En qué avanzamos?

---

### [12/08/2026 15:20:40] Patricia de Lourdes Bossa

Quiero que armes los casos de prueba para el RM29985, según las definiciones de mi archivo de preferencias, teniendo en cuenta, la documentación del incidente que se encuentra en la carpeta EF, dentro de la carpeta del incidente. Haceme las preguntas que consideres necesarias para llevar a cabo esta tarea, si hiciera falta.

---

### [12/08/2026 15:26:56] Agente

Pato, dejé armado el documento:

[Criterios de aceptación IA.docx](</mnt/Agentes/pbossa/Incidentes/RM29985/Criterios de aceptación IA.docx>)

Incluí 28 escenarios en formato Gherkin, orientados a ejecución funcional: carga del filtro `Localidad`, valor por defecto `Todas`, botón `Limpiar`, localidades activas, orden alfabético, búsqueda por domicilios vigentes/no dados de baja, no duplicación de pedidos, columna en grilla e impresión.

Nota: en la copia local del RM29985 no encontré subcarpeta `EF`; usé los dos documentos funcionales disponibles en la raíz del incidente. Validé que el `.docx` se generó correctamente y que contiene los 28 escenarios.

---

### [12/08/2026 16:13:33] Patricia de Lourdes Bossa

los criterios que me enviaste los fusione en el siguiente doc Criterios fusionadosV2.docx, dentro dela carpeta /mnt/Agentes/pbossa/Incidentes/RM29985, podrias verificar si estan todos contemplados?

---

### [12/08/2026 16:14:43] Agente

Pato, verifiqué `Criterios fusionadosV2.docx` contra los 28 escenarios originales.

Conclusión: **están prácticamente todos contemplados**, porque el documento fusionado compacta los 28 escenarios en 16 escenarios agrupados. No veo faltantes funcionales críticos, pero sí detecté **2 ajustes recomendados**:

1. Falta explícito el criterio original de **visualización del nuevo filtro Localidad**:
   - Original Escenario 1: que al ingresar a la pantalla se vea el nuevo filtro `Localidad` junto con los demás filtros.
   - En el fusionado queda implícito por los escenarios de valor por defecto y carga del combo, pero conviene agregarlo explícitamente.

2. En el Escenario 6 del fusionado hay un error de redacción:
   - Dice: “buscar pedidos **sin registrar** por localidad del afiliado”.
   - Debería decir: “buscar pedidos **sin restringir** por localidad del afiliado”.

También haría un ajuste menor en el Escenario 16: “desempate geográfico” no es el término más preciso; sería mejor “reglas de visualización y desempate por vigencia del domicilio”.

Fuera de esas observaciones, la cobertura está bien: incluye combo, `Todas`, `Limpiar`, localidades activas, orden, vigencia, baja, múltiples domicilios, no duplicación, tipo de domicilio, otros filtros, sin resultados, columna en grilla e impresión.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
