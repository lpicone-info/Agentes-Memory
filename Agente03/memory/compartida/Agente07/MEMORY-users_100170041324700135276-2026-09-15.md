# Memoria de conversación — Pablo Raul Fernandez

## Datos

- **Fecha:** 15/09/2026
- **Usuario:** Pablo Raul Fernandez
- **ID Google Chat:** `users/100170041324700135276`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 14
- **Inicio:** 08:36:59
- **Fin:** 17:13:39

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/45efmkaaaae`

## Archivos revisados

- `986ed4ae-ee25-401a-9958-84591e80ad4a.jsonl`

---

## Transcripción completa

### [15/09/2026 08:36:59] Pablo Raul Fernandez

buen dia bender

---

### [15/09/2026 08:38:18] Agente

Hola Pablo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [15/09/2026 09:40:19] Pablo Raul Fernandez

Bender, necesito que me orientes sobre este error, que sucede en la base de smc_prox_ver. El mismo sucede cuando me posiciono sobre el campo 'Cant', cargo el numero 2, y luego presiono la tecla TAB, para avanzar al siguiente control.

---

### [15/09/2026 09:40:37] Agente

Pablo, el error no parece estar en el TAB en sí, sino en el `ItemChanged` del DataWindow `dw_prestac`.

El dato clave del mensaje es:

```text
PowerBuilder application execution error (R0036)
Name not found accessing external object property nomen
line 544 in itemchanged event of object dw_prestac of w_clase_auto
```

Diagnóstico probable:

Cuando cargás `Cant = 2` y salís del campo con TAB, PowerBuilder dispara el evento `ItemChanged`. En la línea 544 ese código intenta acceder a una propiedad llamada `nomen`, pero el objeto sobre el que la está leyendo no la tiene disponible.

Ejemplos típicos:

```powerscript
lo_obj.nomen
```

o alguna variable `any`, estructura, objeto externo, DataStore/DataWindowChild u objeto dinámico que debería traer una propiedad/columna `nomen`, pero no la trae.

Para orientarlo técnicamente, yo revisaría en este orden:

1. Abrir `w_clase_auto`, control `dw_prestac`, evento `ItemChanged`.
2. Ir directo a la línea 544.
3. Buscar la referencia exacta a `nomen`.
4. Ver sobre qué objeto se accede: no alcanza con saber que falta `nomen`; hay que identificar si viene de una query, estructura, DataWindowChild, objeto externo o función.
5. Comparar esa consulta/DataWindow contra la base `smc_prox_ver`: puede faltar una columna, alias o configuración que en otra base sí existe.

La hipótesis más fuerte: en `smc_prox_ver` hay una diferencia de datos, metadata o SQL asociado al producto/prestación que hace que el objeto esperado no incluya `nomen`. El error aparece al salir de `Cant` porque ahí se recalculan/importan datos de autorización, valores, nomenclador o validaciones del renglón.

Si querés, el siguiente paso concreto es ubicar esa línea 544 en el código PowerBuilder y revisar juntos qué objeto debería tener la propiedad `nomen`. Ahí se puede determinar si el fix es de código o de datos/configuración en `smc_prox_ver`.

---

### [15/09/2026 11:59:02] Pablo Raul Fernandez

para smc_prox_ver, hice una autorizacion de farmacia, la 2049903. Quiero saber los items que fueron cargados, en que tabla puedo verlos.

---

### [15/09/2026 12:02:11] Agente

Pablo, para autorización de farmacia en `SMC_Prox_Ver`, los ítems cargados se ven en:

`AUTO_PRODU_ITEMS`

La cabecera está en:

`AUTORIZACIONES`

Para tu autorización `2049903`, validé en base:

```sql
SELECT *
FROM AUTORIZACIONES
WHERE auto = 2049903;

SELECT api.*
FROM AUTO_PRODU_ITEMS api
WHERE api.auto = 2049903;
```

Resultado actual:

| sucur | auto | produ | producto | canti | esta | recha |
|---:|---:|---:|---|---:|---|---|
| 1 | 2049903 | 32734 | INSULINA LANTUS cart.x 5 x 3 ml | 1 | A | NULL |
| 1 | 2049903 | 41517 | INSULINA LANTUS SOLOSTAR 100UI/ml lap.prellx5x3ml | 1 | A | NULL |
| 1 | 2049903 | 46016 | METFORMINA ROSPAW 500 comp.rec.x 30 | 1 | A | NULL |

Consulta más legible:

```sql
SELECT api.sucur, api.auto, api.produ, p.deno, api.medides,
       api.canti, api.esta, api.recha, api.valor, api.porce,
       api.obser, api.alta_fecha, api.baja_fecha
FROM AUTO_PRODU_ITEMS api
LEFT JOIN PRODUCTOS p ON p.produ = api.produ
WHERE api.auto = 2049903
ORDER BY api.sucur, api.auto, api.produ, api.medides;
```

Dato importante: aunque mencionaste que cargaste `Cant = 2`, en base hoy los 3 ítems quedaron con `canti = 1`.

---

### [15/09/2026 12:07:33] Pablo Raul Fernandez

para esta ultima consulta, con este numero de autorizacion, no mencione que cargue 2 items. De hecho no mencione cantidad.

---

### [15/09/2026 12:07:39] Agente

Tenés razón, Pablo. Me equivoqué al arrastrar el contexto del error anterior donde habías mencionado `Cant = 2`.

Para esta autorización `2049903`, lo correcto es decir solamente esto: en `AUTO_PRODU_ITEMS` hay 3 registros cargados, y cada uno figura con `canti = 1`.

No debí inferir que vos habías cargado cantidad 2 en esta autorización.

---

### [15/09/2026 14:45:37] Pablo Raul Fernandez

Necesito me digas para el afiliado 99993 integrante 00, un producto de farmacia que, al querer autorizar, quede en estado RECHAZADO (no quedar pendiente). Y para este afiliado 82500/01, un producto de farmacia en estado Aceptado , y otro de farmacia en estado RECHAZADO (no quedar pendiente)

---

### [15/09/2026 14:47:09] Agente

Pablo, validé en `SMC_Prox_Ver` contra autorizaciones de farmacia (`tipo = 'S'`) y con ítems no pendientes.

Para afiliado `99993/00`, producto que queda rechazado:

| producto | descripción | evidencia |
|---|---|---|
| `48117` | `CAPTECH 500 mg comp.x 120` | Auto `1/2048591`, `AUTO_PRODU_ITEMS.esta = R`, `recha = 170` |

Motivo `170`: `Producto no cubierto p/ Familia`.

Para afiliado `82500/01`:

| caso | producto | descripción | evidencia |
|---|---|---|---|
| Aceptado | `21643` | `INSULINA HUMULIN 70/30 3.0 ml cart.x 5` | Auto `1/2049868`, `esta = A`, `recha = NULL` |
| Rechazado funcional | `50352` | `XELEVIA MET 50/1000 mg comp.rec.x 56` | Auto `16/12721`, `recha = 170` |

Aclaración técnica: para `82500/01` no encontré ítems históricos con `AUTO_PRODU_ITEMS.esta = 'R'`. Los casos rechazados reales figuran con `recha = 170` y `esta = 'A'` en el ítem, que es el patrón que ya aparece en esa base para farmacia.

---

### [15/09/2026 15:04:04] Pablo Raul Fernandez

Dado el siguiente caso de prueba, realizado en la base smc_prox_ver, quiero saber como verificarlo en base, para saber que no copio la presentacion de un producto en el otro. La autorizacion es la 2049920, y el caso de prueba es el siguiente: Caso de prueba 9:
Conservación de presentación por producto en la grabación inicial - Pendiente


Dado que se cargan dos o más productos con presentaciones diferentes


Cuando el usuario graba la autorización


Entonces cada registro vigente de auto_produ_items debe guardar la
presentación correspondiente a su propio producto


Y el sistema no debe copiar la presentación de un producto al resto
de los ítems

---

### [15/09/2026 15:04:49] Agente

Pablo, para verificar ese caso en base, el campo a controlar es:

`AUTO_PRODU_ITEMS.presen`

Comparalo contra la presentación propia del producto en:

`PRODUCTOS.presen`

Consulta recomendada:

```sql
SELECT
    a.sucur,
    a.auto,
    api.produ,
    p.deno AS producto,
    p.presen AS presentacion_del_producto,
    api.presen AS presentacion_grabada_en_item,
    CASE
        WHEN ISNULL(LTRIM(RTRIM(api.presen)), '') =
             ISNULL(LTRIM(RTRIM(p.presen)), '')
        THEN 'OK'
        ELSE 'DIFIERE'
    END AS control_presentacion,
    api.canti,
    api.esta,
    api.recha,
    api.baja_fecha
FROM AUTORIZACIONES a
JOIN AUTO_PRODU_ITEMS api
    ON api.sucur = a.sucur
   AND api.auto = a.auto
LEFT JOIN PRODUCTOS p
    ON p.produ = api.produ
WHERE a.auto = 2049920
  AND api.baja_fecha IS NULL
ORDER BY api.produ, api.medides;
```

Resultado para la autorización `2049920`:

| producto | presentación producto | presentación grabada en ítem | control |
|---|---|---|---|
| `21643 - INSULINA HUMULIN 70/30 3.0 ml cart.x 5` | `70/30 3.0 ml cart.x 5` | vacío | `DIFIERE` |
| `46016 - METFORMINA ROSPAW 500 comp.rec.x 30` | `comp.rec.x 30` | vacío | `DIFIERE` |

Conclusión: en esta autorización no veo que haya copiado la presentación de un producto al otro, pero tampoco está conservando la presentación propia de cada producto en `AUTO_PRODU_ITEMS.presen`. Para el caso de prueba tal como está redactado, esto no cumple el esperado: los registros vigentes deberían tener grabada su presentación correspondiente.

---

### [15/09/2026 17:12:10] Pablo Raul Fernandez

para el rm30253 - al grabar una autorización de productos de farmacia, es esperable que en el campo AUTO_PRODU_ITEMS.presense grabe producto.presen, siendo seleccion de producto o de monodroga, cuando cargo un producto de farmacia?

---

### [15/09/2026 17:13:39] Agente

Sí, Pablo: para el RM30253, si se carga un producto de farmacia, es esperable que `AUTO_PRODU_ITEMS.presen` conserve la presentación correspondiente al ítem cargado.

La regla práctica sería:

- Si la selección fue por producto concreto: `AUTO_PRODU_ITEMS.presen` debería grabar la presentación de ese producto, es decir `PRODUCTOS.presen` para ese `produ`.
- Si la selección fue por monodroga: `AUTO_PRODU_ITEMS.presen` debería grabar la presentación elegida para esa monodroga. Esa presentación también se obtiene desde `PRODUCTOS.presen`, filtrando por `PRODUCTOS.droga_prin`.

Lo importante para el caso de prueba es que `AUTO_PRODU_ITEMS.presen` no quede vacío ni arrastre la presentación de otro renglón.

Para verificar producto directo:

```sql
SELECT
    api.produ,
    p.deno,
    p.presen AS presen_producto,
    api.presen AS presen_grabada,
    CASE
        WHEN LTRIM(RTRIM(api.presen)) = LTRIM(RTRIM(p.presen))
        THEN 'OK'
        ELSE 'ERROR'
    END AS control
FROM auto_produ_items api
JOIN productos p ON p.produ = api.produ
WHERE api.auto = 2049920
  AND api.baja_fecha IS NULL;
```

Para monodroga, el control no debería depender de un único `produ`, sino de `droga_prin` + `presen`:

```sql
SELECT
    api.droga_prin,
    api.presen AS presen_grabada,
    CASE
        WHEN EXISTS (
            SELECT 1
            FROM productos p
            WHERE p.droga_prin = api.droga_prin
              AND LTRIM(RTRIM(p.presen)) = LTRIM(RTRIM(api.presen))
              AND p.baja_fecha IS NULL
        )
        THEN 'OK'
        ELSE 'ERROR'
    END AS control
FROM auto_produ_items api
WHERE api.auto = 2049920
  AND api.baja_fecha IS NULL;
```

En síntesis: sí, el campo esperado es `AUTO_PRODU_ITEMS.presen`; para producto debe coincidir con `PRODUCTOS.presen`, y para monodroga debe coincidir con una presentación válida de esa monodroga tomada desde `PRODUCTOS`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
