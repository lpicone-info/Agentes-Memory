# Memoria de conversación — Facundo Farias

## Datos

- **Fecha:** 01/09/2026
- **Usuario:** Facundo Farias
- **ID Google Chat:** `users/110088613929347470679`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 12
- **Inicio:** 09:04:05
- **Fin:** 16:23:48

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/9tgygkaaaae`

## Archivos revisados

- `2ab91a8f-a5bf-4cae-8e48-d9bb941eb4c8.jsonl`

---

## Transcripción completa

### [01/09/2026 09:04:05] Facundo Farias

Hola Bender

---

### [01/09/2026 09:06:20] Agente

Hola Facundo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [01/09/2026 09:31:50] Facundo Farias

Por R30003 Necesito pases esta a imagen a texto lo que se encuentra en verde como "Comentarios":

---

### [01/09/2026 09:31:56] Agente

Por R30003, el texto en verde dice:

**Comentarios:** ver porque no se puede autorizar, el afiliado trajo la nota del prestador efecto que dice qu si tiene convenio pero el sistema la recha por falta del mismo, Se pide revisar el tema lo antes posible para poder darle una respuesta al afiliado, dejo su nuevo contacto que es 9996666999966, gracias.

---

### [01/09/2026 12:51:56] Facundo Farias

transac 6535140 rechaza por recha 20. Por que no ingresa al recha=90 ya que la prestación requiere un nivel superior

---

### [01/09/2026 13:00:18] Agente

Facundo, para la transac `6535140` en `SMC_Prox_Ver` no entra a `recha=90` por dos motivos concretos:

1. La transacción es `tipo_transac = 02C`.
2. Para `02C`, el SP `prs_valida_of_auto_remotas` solo asigna `recha=90` si la cobertura tiene `auto = 'S'`.

En este caso la cobertura del plan `MASTER 2` para la prestación `2 / 150103` está así:

`auto = N`  
`auto_inter = S`  
`nivel configurado = 2`  
`nivel usuario ffa_prest = 0`

Como es `02C`, el sistema mira `auto`, no `auto_inter`. Entonces aunque la prestación tenga nivel `2`, no cumple la condición para rechazar por nivel superior.

Además el ítem ya quedó con `recha=20`:

`Prestacion sin convenio para este plan`

Y como viene de WebPos/Activia (`aufree = -1`), el cursor de validación de nivel solo toma ítems con `recha IS NULL`; por eso el `20` no se pisa por `90`.

Diagnóstico: está rechazando primero por convenio/plan. Para que entre a `90`, además de tener nivel superior, la cobertura ambulatoria del plan debería requerir autorización (`auto = S`) y el ítem no debería venir ya rechazado por convenio.

---

### [01/09/2026 13:06:10] Facundo Farias

es distinto al flujo de presmed

---

### [01/09/2026 13:06:43] Agente

Sí, es distinto al flujo de Presmed.

En `prs_valida_of_auto_remotas` se separa por `aufree`:

- Presmed: normalmente `@aufree IS NULL`.
- WebPos/Activia: `@aufree IS NOT NULL`, en tu caso `aufree = -1`.

Para esta transac `6535140`, al venir por WebPos/Activia, entra al flujo remoto con `chk_conve_remotas = S`. Ahí primero valida convenio/cobertura y el ítem queda con `recha=20`. Después, la validación de nivel solo puede marcar `90` si el ítem no tiene rechazo previo y si para `02C` la cobertura tiene `auto = S`.

Tu caso:

`tipo_transac = 02C`  
`prestacion = 2 / 150103`  
`nivel prestacion = 2`  
`nivel usuario = 0`  
`coberturas.auto = N`  
`coberturas.auto_inter = S`  
`recha previo = 20`

Entonces no entra al `90`.

La diferencia fuerte con Presmed es que en WebPos se agregó lógica específica (`valida_nivel_webpos`) y la validación de nivel queda condicionada por la configuración de cobertura del tipo remoto (`auto` para `02C`, `auto_inter` para `02I`) y por no tener rechazo previo. En Presmed el circuito no se comporta exactamente igual porque no viene con `aufree` y usa otra combinación de validaciones/configuración.

---

### [01/09/2026 13:47:09] Facundo Farias

TRANSAC 6535144 por que no rechaza por recha 90? si prestac 150103 posee coberturas.auto=S

---

### [01/09/2026 13:47:50] Agente

Sí, `coberturas.auto = S`, pero igual no llega al `recha=90` porque el ítem ya viene rechazado antes con `recha=20`.

Datos reales de la `6535144`:

`tipo_transac = 02C`  
`aufree = -1`  
`usuario = ffa_prest`  
`nivel usuario = 0`  
`prestac = 2 / 150103`  
`nivel configurado = 2`  
`coberturas.auto = S`  
`valida_nivel_webpos = S`  
`item.recha = 20`  
`item.nivel = NULL`

La condición de nivel está en `prs_valida_of_auto_remotas`, pero el cursor toma ítems así:

```sql
AND (recha IS NULL OR @aufree IS NULL)
```

Como esta transacción viene por WebPos/Activia (`@aufree = -1`, no nulo), solo valida nivel si `recha IS NULL`.

Entonces el flujo queda así:

1. Primero valida convenio remoto.
2. La prestación queda con `recha=20`: “Prestacion sin convenio para este plan”.
3. Después intenta validar nivel, pero el ítem ya tiene rechazo.
4. Por eso no lo toma el cursor de nivel y no asigna `90`.

Conclusión: para `6535144`, aunque `auto=S` y la prestación sea nivel 2, gana el rechazo previo `20`. El `90` solo entraría si al momento de validar nivel el ítem todavía no tuviera `recha`.

---

### [01/09/2026 16:19:02] Facundo Farias

transac 6535173 rechaza por recha 668 cuando tiene la misma condición que transac 6535172. Por que no rechaza por recha 1253?

---

### [01/09/2026 16:23:48] Agente

Facundo, la `6535173` sí tiene la condición para `1253`:

`prestación 2/200201` tiene `sexo = F`  
afiliado `38690954/00` tiene `sexo = M`

Pero termina en `668` porque en esa transacción también se cumple una condición posterior de convenio:

`tipo_transac = 01C`  
`efe_lugar = 12`  
`conve = 10347`  
la prestación está deshabilitada en `conve_prestac_deshab` para la fecha de la transacción.

El flujo queda así:

1. `prs_valida_of_cobertura_remoto` puede detectar `1253` por sexo.
2. Luego `prs_valida_of_convenio_remoto` sigue validando, porque solo corta especialmente cuando el error es `20`.
3. En convenio ejecuta `prs_valida_prest_deshab`.
4. Como encuentra la prestación deshabilitada para el convenio del prestador, asigna `668`.

La diferencia contra `6535172` no es la condición de sexo, sino el resultado de convenio:

`6535172`: `tipo_transac=01B`, `efe_lugar=1`, queda `conve=NULL`, rechazo final `1253`.  
`6535173`: `tipo_transac=01C`, `efe_lugar=12`, queda `conve=10347`, rechazo final `668`.

Conclusión: `6535173` no queda en `1253` porque el flujo posterior de convenio encuentra una deshabilitación vigente y termina pisando/ganando con `668`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
