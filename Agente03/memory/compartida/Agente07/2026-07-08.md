# 2026-07-08

- Usuario en Google Chat directo: Mariela.
- Se investigó en SVN `mpariso-V25.08.001` / `branches/V25.08.001`, revisión local `83864`, el parámetro general `Afilmed / Facturacion / Edad Tope Calculada`.
  - Uso funcional: decide si facturación usa la edad calculada al período como tope para buscar tramo tarifario (`tope >= edad_calculada`) cuando corresponde; si no aplica, usa el tope registrado vigente. En V25.08.001 el default observado fue `S` en `backend/data/PARAMETROS_GENERALES.sql`.
  - Accesos/funcionalidades detectadas: Prefacturación (`wp_prefac_proceso_mensual`, `afi_generar_prefacturas` -> `afi_prefac_prefacturar` -> `afs_prefac_cuota_categoria`), Facturación masiva (`wp_fac_proceso_mensual`, `uo_nv_factu.of_gene_compro_masiva`), Comprobantes por generar (`wp_soli_comprobantes`), Test de cuota (`wp_test_cuota`), Ficha del afiliado (`wp_ficha_afiliado` / `wr_ficha_afiliado`), Cancelación de saldos parciales (`wp_cancela_saldos`), Interfaz cajero no visible (`wp_ctacte_mostrador_casa`), importación externa (`import.sra` / `w_importar`, `afs_valida_valor_cuota_import`).
  - Rutinas directas revisadas: `afs_valida_valor_cuota`, `afs_valida_valor_facargo`, `afs_valida_valor_descuento`, `afs_valida_valor_recargo`, `afi_prefac_prefacturar`, `afs_prefac_cuota_categoria`, `afs_valida_valor_cuota_import`, `afn_valida_valor_cuota`, `afn_valida_valor_facargo`.
- Se investigó en SVN `mpariso-Infomedical-trunk` / `^/trunk`, revisión local `83864`, el mismo parámetro.
  - Uso funcional igual: edad calculada al período para seleccionar tramo por edad tope; en trunk el default observado fue `N` en `backend/data/parametros_generales.sql`.
  - Accesos/funcionalidades detectadas: Prefacturación (`wp_prefac_proceso_mensual`), Facturación masiva (`wp_fac_proceso_mensual`), Consulta Prefacturas por grupo familiar (`wp_ver_prefacturas`, puede abrir `wp_test_cuota`), Comprobantes por generar (`wp_soli_comprobantes`), Test de cuota (`wp_test_cuota`), Ficha del afiliado (`wp_ficha_afiliado` / `wr_ficha_afiliado`), Cancelación de saldos parciales (`wp_cancela_saldos`), Interfaz cajero no visible (`wp_ctacte_mostrador_casa`), importación externa (`import.sra` / `w_importar`), y procesos SQL sin pantalla directa: `afj_job_facturacion_procesar`, `afi_simular_prefacturas`, `esi_generar_compro_xsoli`, `afi_genera_compro`.
  - Validación realizada: `svn info` y búsquedas con `rg` en backend, menús PowerBuilder y procesos. No se modificaron archivos de repositorio.

- Usuario en Google Chat directo: Francisco. Preferencias del usuario leidas/actualizadas en `/mnt/Agentes/Francisco/Preferencias/preferencias.md`.
  - Ruta base cuando diga "guardalo en mi carpeta": `/mnt/Agentes/Francisco`.
  - Credencial Redmine preferida registrada como `fvmiano` (archivo local de secreto: `/home/desarrollo-ia-07/.openclaw/secrets/redmine/fvmiano.env`). No exponer API key.
  - Cuando mencione version/rama `trunk`, refiere al repositorio `mpariso-Infomedical-trunk`.
  - Cuando mencione rama/version `prox_ver` o `proxima version`, refiere al repositorio `mpariso-V25.08.001`.
  - Para consultas de base de datos, preferir la base del cliente reportante del incidente segun proyecto Redmine; primero revisar si `perfiles SQL.txt` tiene credencial para esa base; si no, usar perfiles ya cargados. Si Francisco indica una base o usuario especifico, esa instruccion prevalece.
- Repositorios disponibles registrados para Francisco:
  - SVN `mpariso-Infomedical-trunk`: `~/.openclaw/workspace/svn/mpariso-Infomedical-trunk`, remoto `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`.
  - SVN `mpariso-V25.08.001`: `~/.openclaw/workspace/svn/mpariso-V25.08.001`, remoto `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`.
  - SVN `mpariso-Incidentes-Incidentes`: `~/.openclaw/workspace/svn/mpariso-Incidentes-Incidentes`, remoto `https://reposrv01.infomedical.com:18553/svn/Incidentes/Incidentes`.
  - SVN `fvmiano-ManualesProyectoEstandar-ManualesProyectoEstandar`: `~/.openclaw/workspace/svn/fvmiano-ManualesProyectoEstandar-ManualesProyectoEstandar`, remoto `https://srv-infosvn.infomedical.com/svn/ManualesProyectoEstandar/ManualesProyectoEstandar`, usuario asociado `fvmiano`, auth aislada en `~/.openclaw/workspace/.svn-auth/fvmiano-ManualesProyectoEstandar-ManualesProyectoEstandar/`.
- Perfiles DB disponibles recordados:
  - `SMC_PROD_TEST`: MS SQL Server `172.16.10.108:1433`, base `SMC_Prod_Test`, usuario `sa`.
  - `Quimicos_Prod`: MS SQL Server `172.16.10.108:1433`, base `Quimicos_Prod`, usuario `sa`.
  - `SMC_Prox_Ver`: MS SQL `172.16.10.108:1433`, base `SMC_Prox_Ver`, usuario `sa`, estado OK.
- Trabajo realizado RM 29911: se genero `/mnt/Agentes/Francisco/R29911.docx` con interpretacion, analisis de adjunto/RM relacionado 29406, causa probable, solucion propuesta y pasos. Diagnostico central: los rechazos se registran, pero la rutina comun que arma candidatos para derivaciones no excluye registros con errores vigentes antes de llamar al motor que actualiza/crea `AFI_DERIV_APORTES`.
