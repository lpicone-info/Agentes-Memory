# Memoria de conversación — Pablo Raul Fernandez

## Datos

- **Fecha:** 10/09/2026
- **Usuario:** Pablo Raul Fernandez
- **ID Google Chat:** `users/100170041324700135276`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 34
- **Inicio:** 08:55:57
- **Fin:** 17:42:56

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/45efmkaaaae`

## Archivos revisados

- `6d01119d-096f-49ea-b064-8d55e2f32667.jsonl`

---

## Transcripción completa

### [10/09/2026 08:55:57] Pablo Raul Fernandez

buen dia Bender

---

### [10/09/2026 08:57:04] Agente

Hola Pablo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [10/09/2026 08:57:57] Pablo Raul Fernandez

Bender, en la base de Roisa_Prod_Test, estoy teniendo un bug en pantalla de Pedidos de Autorizaciones, que adjunto. Entiendo que puede estar faltando correr algun sp. Necesito la compares con la base de Premedic_Prod_Test, y me digas que puede estar faltando.

---

### [10/09/2026 09:03:06] Agente

Pablo, comparé `Roisa_Prod_Test` contra `Premedic_Prod_Test`.

**Hallazgo principal:** en Roisa falta el objeto `dbo.pedidos_attach`. En Premedic existe con 9 columnas y PK sobre `nro_pedido, item`. En Roisa existe `dbo.pedidos_attach_2` con la misma estructura, pero está en el filegroup `FG01`, cuyo archivo está en estado `RECOVERY_PENDING`.

Eso es consistente con un bug en la pantalla de Pedidos de Autorizaciones, porque en Roisa hay objetos que llaman explícitamente a `PEDIDOS_ATTACH` / `pedidos_attach`:

- `dbo.prs_auto_pedidos`
- `dbo.aru_grabar_pedido_attach`
- `dbo.prs_auto_attach`
- `dbo.ars_pedidos_app`
- `dbo.pru_decode_pedido_attach_base64`
- `dbo.GR_Pedidos_attach_control`
- `dbo.GR_Pedidos_defectuosos`

En particular, `dbo.prs_auto_pedidos` consulta `PEDIDOS_ATTACH` para calcular adjuntos, pendientes incompletos y defectuosos. Si la pantalla ejecuta ese SP, puede fallar por `Invalid object name 'PEDIDOS_ATTACH'`.

También detecté 4 SP que existen en Premedic y no en Roisa:

- `dbo.mcc_auto`
- `dbo.pri_auto_afi_importac`
- `dbo.prs_valida_of_auto_remotas_`
- `dbo.tru_ingre_prestac_8583`

Pero por relación directa con la pantalla, el faltante fuerte es `dbo.pedidos_attach`, no un SP.

**Conclusión:** no parece faltar “correr un SP” como primer causa. Parece faltar restaurar/crear correctamente la tabla `dbo.pedidos_attach`, o resolver el filegroup `FG01` de Roisa que quedó en `RECOVERY_PENDING`. No ejecuté cambios, solo consultas de diagnóstico.

---

### [10/09/2026 09:23:09] Pablo Raul Fernandez

necesito entonces correr dbo.pedidos_Attach en roisa_prod_test. Decime donde encuentro el archivo para poder correrlo. Si podes dejarme el link de descarga

---

### [10/09/2026 09:23:21] Agente

Pablo, para buscar el archivo tengo que saber en qué repositorio o rama querés que lo busque.

En el workspace hay varias working copies SVN registradas, por ejemplo `mpariso-Infomedical-trunk`, `mpariso-V25.08.001` y `ffarias-V25.08.001_PROX`. Decime cuál uso para buscar el script de `dbo.pedidos_attach` y te paso la ruta exacta y el link/localización de descarga si el archivo existe.

---

### [10/09/2026 09:23:58] Pablo Raul Fernandez

el de mpariso-infomedical-trunk

---

### [10/09/2026 09:24:58] Agente

Pablo, el archivo está en:

[mpariso-Infomedical-trunk/backend/tables/pedidos_attach.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/tables/pedidos_attach.sql)

Ruta local completa:

```text
/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/tables/pedidos_attach.sql
```

Ese es el script correcto: crea `dbo.pedidos_attach`, la PK `pk_pedidos_attach`, la FK contra `dbo.PEDIDOS` y agrega la columna `ubicacion`.

Ojo técnico: en Roisa existe `pedidos_attach_2`, pero está sobre un filegroup en `RECOVERY_PENDING`. Correr este script te va a crear `dbo.pedidos_attach` en el filegroup default si no existe, pero no recupera datos previos de `pedidos_attach_2`.

---

### [10/09/2026 09:33:39] Pablo Raul Fernandez

decime en link para descargar, o bien, podes dejarme en el espacio personal del agente el archivo.

---

### [10/09/2026 09:34:22] Agente

Pablo, te lo dejé acá:

[pedidos_attach.sql](/mnt/Agentes/pablo.fernandez/pedidos_attach.sql)

Validación: quedó idéntico al archivo del SVN, con hash `11425229e1424d5c2f50cc1547305dbba89f8ca8cd80351715c28bc81abba4ae`, encoding ASCII y finales de línea CRLF.

---

### [10/09/2026 09:45:55] Pablo Raul Fernandez

decime en link para descargar, o bien, podes dejarme en el espacio personal del agente el archivo.

---

### [10/09/2026 09:46:07] Agente

Pablo, ya quedó copiado en tu espacio personal:

[pedidos_attach.sql](/mnt/Agentes/pablo.fernandez/pedidos_attach.sql)

Ruta completa:
`/mnt/Agentes/pablo.fernandez/pedidos_attach.sql`

---

### [10/09/2026 10:57:10] Pablo Raul Fernandez

Necesito ejecutar el sp aru_auto_pedido, en premedic_prod_test. Indicame los parametros para ejecutar y a que corresponde cada uno.

---

### [10/09/2026 10:57:39] Agente

Pablo, `dbo.aru_auto_pedido` en `Premedic_Prod_Test` recibe 8 parámetros:

```sql
DECLARE @recha varchar(max)
DECLARE @aju int

EXEC dbo.aru_auto_pedido
  @prm_nro_pedido = <nro_pedido>,
  @prm_tipo_pedido = '<tipo_pedido>',
  @prm_Contra = '<contrato>',
  @prm_Inte = '<integrante>',
  @prm_alta_fecha = '<fecha_alta>',
  @prm_prepaga = <prepaga>,
  @recha = @recha OUTPUT,
  @prm_aju = @aju OUTPUT

SELECT @recha AS recha, @aju AS aju
```

Parámetros:

- `@prm_nro_pedido int`: número del pedido a procesar. Actualiza ese registro en `pedidos`.
- `@prm_tipo_pedido char(2)`: tipo de pedido. Se busca en `pedidos_config.auto_tipo`.
- `@prm_Contra char(15)`: contrato del afiliado.
- `@prm_Inte char(2)`: integrante del contrato.
- `@prm_alta_fecha smalldatetime`: fecha usada para validar/generar la autorización.
- `@prm_prepaga smallint`: código de prepaga.
- `@recha varchar(max) OUTPUT`: devuelve texto de rechazo si corresponde.
- `@prm_aju int OUTPUT`: devuelve el ajuste generado si la autorización queda aprobada y aplica generación de ajuste.

El SP no es solo consulta: genera autorización remota, inserta mensajes, actualiza `pedidos`, `autorizaciones` y puede ejecutar `aru_generar_ajuste`.

---

### [10/09/2026 11:02:32] Pablo Raul Fernandez

Entra a Pedidos de Autorizacion, en premedic_prod_test, busca el pedido Nro 966716, y armame los datos para ejecutar el sp para que pueda obtener el mensaje de rechazo (cuyo id es 1002), y luego necesito ver ese rechazo en la tabla correspondiente.

---

### [10/09/2026 11:03:54] Agente

Pablo, para el pedido `966716` en `Premedic_Prod_Test`, los datos reales para ejecutar `dbo.aru_auto_pedido` son:

```sql
DECLARE @recha varchar(max)
DECLARE @aju int

EXEC dbo.aru_auto_pedido
  @prm_nro_pedido = 966716,
  @prm_tipo_pedido = 'P ',
  @prm_Contra = '33262139',
  @prm_Inte = '02',
  @prm_alta_fecha = '20251202',
  @prm_prepaga = 1,
  @recha = @recha OUTPUT,
  @prm_aju = @aju OUTPUT

SELECT @recha AS mensaje_rechazo, @aju AS ajuste
```

Correspondencia:

- `@prm_nro_pedido`: `pedidos.Nro_pedido`
- `@prm_tipo_pedido`: `pedidos.tipo`
- `@prm_Contra`: `pedidos.contra`
- `@prm_Inte`: `pedidos.inte`
- `@prm_alta_fecha`: `pedidos.alta_fecha`
- `@prm_prepaga`: `pedidos.prepaga`

Para ver luego el rechazo grabado:

```sql
SELECT PM.id_pedido_msj,
       PM.nro_pedido,
       PM.id_msj,
       PMC.titulo_msj,
       PMC.texto_msj,
       PM.usuari_alta,
       PM.alta_fecha,
       PM.modi_fecha,
       PM.baja_fecha
FROM pedidos_mensajes PM
INNER JOIN pedidos_mensajes_config PMC
        ON PMC.id_msj = PM.id_msj
WHERE PM.nro_pedido = 966716
  AND PM.id_msj = 1002
```

Dato importante: actualmente `pedidos_config` para tipo `'P '` tiene `genera_auto = 'N'` y `mensaje_recha = NULL`. Con esa configuración, `aru_auto_pedido` no debería insertar el mensaje `1002`. El mensaje `1002` existe en `pedidos_mensajes_config`, pero todavía no hay registros en `pedidos_mensajes` para el pedido `966716`.

---

### [10/09/2026 12:37:52] Pablo Raul Fernandez

Buscame un afiliado en la base de ospecon_prox_ver, en donde tenga asociado un plan que tenga mas de 10 caracteres, o el plan sea el mas largo posible en texto. Por ejemplo, hay un plan que se llama Modelo... Si no encontras afiliados con ese plan, busca un afiliado activo o dado de baja, pero que el plan sea el mas largo posible en denominacion.

---

### [10/09/2026 12:38:15] Pablo Raul Fernandez

Buscame un afiliado en la base de ospecon_prox_ver, en donde tenga asociado un plan que tenga mas de 10 caracteres, o el plan sea el mas largo posible en texto. Por ejemplo, hay un plan que se llama Modelo... Si no encontras afiliados con ese plan, busca un afiliado activo o dado de baja, pero que el plan sea el mas largo posible en denominacion.

---

### [10/09/2026 12:38:48] Pablo Raul Fernandez

Buscame un afiliado en la base de ospecon_prox_ver, en donde tenga asociado un plan que tenga mas de 10 caracteres, o el plan sea el mas largo posible en texto. Por ejemplo, hay un plan que se llama Modelo... Si no encontras afiliados con ese plan, busca un afiliado activo o dado de baja, pero que el plan sea el mas largo posible en denominacion.

---

### [10/09/2026 12:40:08] Pablo Raul Fernandez

Buscame un afiliado en la base de ospecon_prox_ver, en donde tenga asociado un plan que tenga mas de 10 caracteres, o el plan sea el mas largo posible en texto. Por ejemplo, hay un plan que se llama Modelo... Si no encontras afiliados con ese plan, busca un afiliado activo o dado de baja, pero que el plan sea el mas largo posible en denominacion.

---

### [10/09/2026 12:41:25] Agente

Pablo, encontré un afiliado activo en `Ospecon_Prox_Ver` asociado al plan con la denominación más larga:

- **Prepaga:** `1`
- **Contrato:** `133407`
- **Integrante:** `00`
- **Afiliado:** HERNANDEZ, JONATHAN ALEJANDRO
- **Estado:** activo (`baja_fecha IS NULL`)
- **Código de plan:** `DESEMPLEO`
- **Denominación:** `AFILIADOS QUE ACCEDEN AL COBRO DE CUOTAS DE FONDO DE DESEMPLEO Y LIQUIDAN CAPITAS A LA O.S.PE.CON.`
- **Longitud de denominación:** 98 caracteres
- **Vigencia del plan desde:** `01/05/2026`

No encontré afiliados asociados a un plan cuya denominación contenga `Modelo`.

---

### [10/09/2026 12:43:16] Pablo Raul Fernandez

no, no tienen que contener modelo, sino que el codigo de plan, debe ser lo mas largo posible. Mas largo que desempleo por ejemplo

---

### [10/09/2026 12:45:40] Pablo Raul Fernandez

no, no tienen que contener modelo, sino que el codigo de plan, debe ser lo mas largo posible. Mas largo que desempleo por ejemplo

---

### [10/09/2026 12:47:20] Agente

Correcto. Busqué por longitud del **código `plan_codi`**.

El máximo posible en esta base es de **10 caracteres**, porque la columna está definida como `char(10)`. Encontré afiliados activos con códigos de plan de longitud 10, superiores a `DESEMPLEO` que tiene 9 caracteres efectivos.

Un caso válido es:

- **Prepaga:** `1`
- **Contrato:** `424630`
- **Integrante:** `00`
- **Afiliado:** `LEGUIZA, JUAN CARLOS`
- **Plan código:** `MONOTRIBUT`
- **Longitud del código:** `10`
- **Vigencia del plan desde:** `2025-10-01`
- **Afiliado activo:** sí
- **Plan vigente:** sí
- **Denominación:** `MONOTRIBUTISTAS QUE HAYAN ELEGIDO (HASTA OCTUBRE 2024) OSPECON COMO O.S.`

También hay casos con los códigos `DOMESTICAS` y `EXTRAORDIN`, ambos de 10 caracteres.

---

### [10/09/2026 12:48:50] Pablo Raul Fernandez

quiero afiliados con planes que se muestren en este label que adjunto, que sean lo mas largo posible.

---

### [10/09/2026 12:53:44] Agente

Pablo, ahora sí tomé como referencia el valor que aparece en el control de la pantalla: el **código del plan vigente**, no la denominación.

Para el afiliado de la imagen (`424630/00`), el plan actualmente visible es:

- `ESP` - `PLAN ESPECIAL`
- El plan histórico `MONOTRIBUT` ya no es el vigente; por eso no aparece en el label.

El máximo código vigente encontrado en `Ospecon_Prox_Ver` es de **10 caracteres**. Estos son los afiliados disponibles:

| Contrato | Inte | Afiliado | Plan visible | Vigencia |
|---|---:|---|---|---|
| `446212` | `00` | OLINDES, JUANA | `DOMESTICAS` | 01/05/2026 |
| `446212` | `01` | OLINDES, TESTX | `DOMESTICAS` | 01/05/2026 |
| `258` | `00` | VARAS CARLOS ALBER, VARAS CARLOS ALBER | `EXTRAORDIN` | 01/04/2026 |

Los tres están activos (`baja_fecha IS NULL`).

Denominaciones:

- `DOMESTICAS`: Empleados de casas particulares que hayan elegido OSPECON como O.S.
- `EXTRAORDIN`: DNI de personas autorizadas y registradas a recibir servicio.

La longitud máxima efectiva del código es **10 caracteres**.

---

### [10/09/2026 15:39:44] Pablo Raul Fernandez

Rm30224 - Necesitaria me digas un usuario y base, para probar el redmine indicado, segun los siguientes criterios: ·

Base sugerida:
IgualdadSalud_Prod_Test o una base de testing equivalente con prepagas activas
sin RNOS para reproducir la condición reportada.

Usuario de prueba test2, o
equivalente, perteneciente únicamente al grupo fich_afilia.

Grupo fich_afilia con permiso
vigente para Ficha del afiliado y sin permiso vigente para Cuenta corriente de
aportes.

Ítem de menú
m_consultadecuentacorrientedeaportes asociado a la ventana wp_ctacte_aportes.
·
Usuario/grupo alternativo con
permiso vigente para Cuenta corriente de aportes, para validar el caso positivo
y la regresión del acceso autorizado.

Nota = Necesito revises la base indicada en las sugerencias, y en el admin, si el grupo esta creado y el usuario. En caso negativo. necesitaria otra base con otro usuario, con las condiciones

---

### [10/09/2026 15:50:17] Agente

Pablo, revisé `IgualdadSalud_Prod_Test` y **no sirve completa para la prueba**: tiene prepagas activas sin RNOS, pero **no existe `test2`** y tampoco existe el grupo exacto `fich_afilia`. Sí existe un grupo parecido, `consulta_afi`, con permiso a Ficha del afiliado y sin permiso a Cuenta corriente de aportes, pero no tiene usuarios asociados.

La mejor base alternativa que encontré es:

**Base:** `SMC_Prod_Test`  
**Usuario para caso negativo:** `cvelazquez`  
**Grupo:** `98 - OSPG_AFILIA`  
**Condición:** usuario vigente, pertenece únicamente a ese grupo, con permiso vigente para `m_fichadelafiliado` y sin permiso para `m_consultadecuentacorrientedeaportes`.

Además, la base cumple la condición de prepagas activas sin RNOS:

- Prepaga `1` - `NO USAR -O.S.E.P.J.A.N.A.`
- Prepaga `3` - `ASI RNAS`

Para validar el caso positivo/regresión en la misma base:

**Base:** `SMC_Prod_Test`  
**Usuario positivo:** `comercial`  
**Grupo:** `94 - comercial_mdp`  
**Condición:** usuario vigente, pertenece únicamente a ese grupo, con permiso vigente para Cuenta corriente de aportes.

Validé también el menú en `SMC_Prod_Test`:

- `m_consultadecuentacorrientedeaportes` está asociado a `wp_ctacte_aportes`
- `m_fichadelafiliado` está asociado a `wp_ficha_afiliado`

Nota: los usuarios consultados tienen password `NULL` en `zz_usuari`; si la app no permite ingresar así, habría que blanquear/asignar clave desde Admin antes de probar.

---

### [10/09/2026 16:14:50] Pablo Raul Fernandez

Revisa por favor el admin de la base igualdad_prod_test, al usuario testficha, y decime a que se refiere el error adjunto, cuando quiero ingresar a afilmed. Analizame y decime la causa del error. Con otro usuario, por ejemplo, info1, no sucede este error.

---

### [10/09/2026 16:20:31] Agente

Pablo, el error adjunto se refiere a un permiso SQL nativo faltante:

`testficha` no tiene permiso `SELECT` sobre `dbo.zz_errorbase` en `IgualdadSalud_Prod_Test`.

Validé que:

- `dbo.zz_errorbase` existe.
- `testficha` existe en `zz_usuari`.
- `testficha` pertenece únicamente al grupo/rol `fich_afilia`.
- El rol SQL `fich_afilia` existe, pero tiene **0 permisos explícitos**.
- Al simular el usuario:
  - `testficha`: `HAS_PERMS_BY_NAME('dbo.zz_errorbase','OBJECT','SELECT') = 0`
  - `info1`: `HAS_PERMS_BY_NAME('dbo.zz_errorbase','OBJECT','SELECT') = 1`

`info1` no falla porque pertenece al rol `infotesting`, y ese rol tiene `GRANT SELECT ON SCHEMA::dbo`, además de muchos permisos adicionales. Por eso puede leer `zz_errorbase`.

La causa concreta: el grupo/rol nuevo `fich_afilia` fue creado, pero no se le aplicaron los grants SQL necesarios para que Afilmed pueda iniciar correctamente. El DataWindow `d_errorbase` intenta leer `zz_errorbase` para resolver/mostrar errores, y SQL Server devuelve el código `229`: permiso denegado.

Además vi un posible problema secundario en Admin: `fich_afilia` tiene cargado un solo acceso en `zz_accesos`:

`m_afilia / m_fichadelafiliado`

Pero en `zz_menues`, la ventana `wp_ficha_afiliado` figura asociada a:

`m_afilia / m_afilia`

Eso puede afectar el acceso funcional después del login, pero el error de la captura ocurre antes: falta permiso SQL sobre `zz_errorbase`.

No ejecuté cambios. El fix mínimo para destrabar ese error sería otorgar `SELECT` sobre `dbo.zz_errorbase` al rol `fich_afilia`; luego habría que validar si aparecen otros objetos faltantes para la Ficha del afiliado.

---

### [10/09/2026 17:06:40] Pablo Raul Fernandez

Quiero que accedas a la base: IgualdadSalud_prod_test, y me muestres en que parte del menu puedo visualizar la prepaga activa sin RNOS, y el usuario que uso para probar es TestFicha, y verifica ademas que el usuario no pueda acceder por permisos a la cuenta corriente, segun indica el caso de prueba, para el siguiente caso de prueba: Caso de prueba 1: Usuario
sin permiso: ocultamiento del botón Cuenta corriente de aportes


Dado que existe una prepaga activa sin RNOS en la base de prueba


Y que el usuario de prueba pertenece únicamente al grupo fich_afilia


Y que el grupo fich_afilia tiene acceso vigente a Ficha del afiliado
y no tiene acceso vigente a Cuenta corriente de aportes


Cuando el usuario ingresa a Afilmed


Entonces el sistema debe permitir el acceso a la Ficha del afiliado


Y el botón Cuenta corriente de aportes no debe estar visible en la
barra de herramientas

---

### [10/09/2026 17:11:18] Agente

Pablo, revisé `IgualdadSalud_Prod_Test` sin realizar modificaciones.

**Prepaga activa sin RNOS**

Se visualiza desde Afilmed en:

`Estadísticas > Organización > Entidades`

En la tabla `prepagas` encontré estas prepagas vigentes (`baja_fecha IS NULL`) sin RNOS:

- `1` - IGUALDAD SALUD
- `6` - PRIVADO
- `8` - PRENSA
- `9` - PRENSA MONOTRIBUTISTA

El menú corresponde al identificador `m_prepagas`, con descripción **Entidades**.

**Usuario `TestFicha`**

- Usuario vigente desde `09/09/2026` hasta `08/10/2026`.
- Pertenece únicamente al grupo `fich_afilia`.
- El grupo `fich_afilia` está vigente.
- Tiene acceso vigente a `m_fichadelafiliado`.
- La opción corresponde a:

`Solicitudes > Listados > Ficha del afiliado`

Ventana asociada: `wp_ficha_afiliado`.

**Cuenta corriente de aportes**

El grupo `fich_afilia` no tiene acceso vigente a:

- `m_consultadecuentacorrientedeaportes`
- Descripción: **Cuenta corriente de aportes**
- Ventana: `wp_ctacte_aportes`

Tampoco posee accesos vigentes a las opciones relacionadas con cuentas corrientes.

**Conclusión**

El caso de prueba queda correctamente configurado:

- Existe una prepaga activa sin RNOS.
- `TestFicha` pertenece únicamente a `fich_afilia`.
- Puede acceder a **Ficha del afiliado**.
- No tiene permiso de menú para **Cuenta corriente de aportes**, por lo que el botón no debería mostrarse en la barra de herramientas.

No ejecuté cambios en la base.

---

### [10/09/2026 17:41:29] Pablo Raul Fernandez

Segun el siguiente caso de prueba, para la base igualdadsalud_prod_test, quiero saber como dejo el permiso de consultadecuentacorrientedeaportes al grupo fich_afilia, pero que no este vigente, segun lo que indica: Caso de prueba 2: Usuario
sin permiso: ocultamiento del ítem de menú Cuenta corriente de aportes


Dado que el usuario de prueba pertenece únicamente al grupo fich_afilia


Y que el permiso m_consultadecuentacorrientedeaportes no se encuentra
vigente para ese grupo


Cuando el usuario despliega el menú de afiliados hasta Cuentas corrientes
/ Afiliados / Consultas


Entonces el ítem Cuenta corriente de aportes no debe estar disponible para
el usuario


Y el usuario no debe poder abrir la ventana wp_ctacte_aportes desde
el menú

---

### [10/09/2026 17:42:56] Agente

Pablo, verifiqué el estado actual. En `IgualdadSalud_Prod_Test` el permiso **existe y está vigente**:

- Grupo: `8 - fich_afilia`
- Menú: `m_afilia`
- Ítem: `m_consultadecuentacorrientedeaportes`
- `baja_fecha`: `NULL`

Para dejarlo registrado, pero **no vigente**, corresponde actualizar `baja_fecha`:

```sql
UPDATE dbo.zz_accesos
SET baja_fecha = GETDATE()
WHERE grupo = 8
  AND menu_id = 'm_afilia'
  AND item = 'm_consultadecuentacorrientedeaportes'
```

La opción está ubicada en:

`Afiliados > Cuentas corrientes > Afiliados > Consultas > Cuenta corriente de aportes`

Y abre la ventana `wp_ctacte_aportes`.

Luego se valida así:

```sql
SELECT grupo,
       menu_id,
       item,
       baja_fecha
FROM dbo.zz_accesos
WHERE grupo = 8
  AND menu_id = 'm_afilia'
  AND item = 'm_consultadecuentacorrientedeaportes'
```

Con `baja_fecha` no nula, `TestFicha` conservará el acceso vigente a **Ficha del afiliado**, pero no debería visualizar ni poder abrir **Cuenta corriente de aportes** desde el menú.

No ejecuté el `UPDATE`. Confirmame explícitamente si querés que lo aplique en `IgualdadSalud_Prod_Test`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
