# Memoria durable - 2026-08-05

## Conversacion con Pato - Afilmed / Presmed
- Usuario identificado en la conversacion como Pato.
- Presmed v25.08.001: ante autorizacion 235 de cirugia en expediente 7, el error mostrado se atribuyo a validacion de fechas: fecha del comprobante del item 2 anterior a la fecha de internacion. Comprobante citado: `FC A 0000100000236`. Revisar fecha de internacion, fecha del comprobante/factura, fecha del item 2 o autorizacion adjunta.
- Presmed/AutoRem v25.03.006: URL de web service para consultar pedidos/autorizaciones encontrada en evidencia R29689: `http://10.194.0.15:5007/api/autorizaciones/pedidos/getPedidosAuto`. Tambien aparecio URL generica `http://10.194.0.15/api/autorizaciones?id_afiliado={id}`.
- Transacciones remotas AutoRem: servicio SOAP `TransactionServ`. WSDL productivo/desarrollo indicado: `http://10.194.0.15:8080/AutoRemWebAplication/TransactionServ?wsdl`. Endpoint: `http://10.194.0.15:8080/AutoRemWebAplication/TransactionServ`. Operaciones principales: `EjecutarTransaccion`, `AnularTransaccion`, `consultarTransacciones`. Contexto `/AutoRemWebAplication`, mapping `/TransactionServ`.
- Transacciones remotas AutoRem local con Tomcat: WSDL `http://localhost:8080/AutoRemWebAplication/TransactionServ?wsdl`; endpoint `http://localhost:8080/AutoRemWebAplication/TransactionServ`. Desplegar WAR `AutoRemWebAplication.war` y probar WSDL en navegador antes de invocar `EjecutarTransaccion` con parametro SOAP `mensajeXml`.
- Afilmed productiva/trunk: `afi_edad_topes` no se graba en prefacturacion/facturacion mensual normal; se consulta como dato de entrada. Evidencia principal en `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/afi_prefac_prefacturar.sql` alrededor de linea 447.
- Grabacion/modificacion de `afi_edad_topes`: `afu_afi_edad_topes.sql` hace UPDATE/INSERT; lo llama `afu_soli_abordar1.sql` al abordar solicitudes con cambio de tope o motivos 6/7; `afi_afi_edad_topes.sql` inserta topes masivamente para familiares/adicionales.
- Uso de `afi_edad_topes` en prefacturacion: `afi_prefac_prefacturar` arma `#PREFAC_AFILIADOS_PERIODO` y para integrantes no titulares busca el registro vigente con `MAX(vigen_desde) <= @var_apli_perio` y baja nula o posterior; carga `edad_vigencia = vigen_desde` y `edad_tope = tope`. Si falta tope para FAC genera error 335 `Edad tope no definida para el familiar a cargo`; si el tope es futuro puede derivar en error 1202 `El grupo tiene un integrante a futuro`.
- Parametro `Afilmed / Facturacion / Edad Tope Calculada = S`: permite recalcular el tope usando edad real via `afn_puede_cambiar_tope`; si corresponde, pisa `edad_tope` con `AFI.edad`.
- `afs_prefac_cuota_categoria.sql` usa `edad_tope` para buscar importes tarifarios, recargos/descuentos, tomando el menor tope tarifario que cubre la edad/tope.
- En facturacion, la cadena mensual usa `afi_edad_topes` indirectamente en validadores. `afi_compro_impresion` llama a `afs_valida_valor_descuento`; este SP busca el tope vigente en `AFI_EDAD_TOPES` cuando resuelve integrante no titular/FAC.
