# Memoria de conversación — Facundo Farias

## Datos

- **Fecha:** 20/08/2026
- **Usuario:** Facundo Farias
- **ID Google Chat:** `users/110088613929347470679`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 27
- **Inicio:** 08:54:14
- **Fin:** 16:43:39

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/9tgygkaaaae`

## Archivos revisados

- `f55b2d56-6703-48b5-8d33-cf5ba40bf0a9.jsonl`

---

## Transcripción completa

### [20/08/2026 08:54:14] Facundo Farias

Hola

---

### [20/08/2026 08:55:48] Agente

Hola Facundo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [20/08/2026 10:04:58] Facundo Farias

En mi archivo de preferencia que está en \MNT\Agentes\ffarias\preferencias quiero agregar una preferencia

---

### [20/08/2026 10:05:06] Agente

Facundo, indicame la preferencia exacta que querés agregar en `/mnt/Agentes/ffarias/preferencias/preferencias.md`.

Cuando me pases el texto, leo el archivo actual y la incorporo respetando el formato existente.

---

### [20/08/2026 10:05:54] Facundo Farias

Cuando te diga "25.08" quiero que tomes de referencia el ambiente https://srv-infosvn.infomedical.com/svn/ProyectoEstandar/Estandar/Prox_Ver/25.08.001

---

### [20/08/2026 10:06:49] Agente

Facundo, agregué la preferencia en `/mnt/Agentes/ffarias/preferencias/preferencias.md`.

Validación ejecutada: el archivo quedó en ASCII, sin BOM y con finales de línea LF, igual que antes.

---

### [20/08/2026 10:09:21] Facundo Farias

en 25.08 base ospecon_prox_ver es posible crear una autorización tipo=C o tipo=I con inter_fecha=null?

---

### [20/08/2026 10:15:46] Agente

Sí, pero con condición funcional.

**Evidencia**
En `Ospecon_Prox_Ver`, `autorizaciones.inter_fecha` es nullable y `pru_autorizaciones` recibe `@inter_fecha smalldatetime = NULL`, insertándolo directo en la tabla. Referencia: [pru_autorizaciones.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/backend/procedures/pru_autorizaciones.sql:61).

Datos vigentes encontrados:
`tipo C`: 35 vigentes, 17 con `inter_fecha NULL`.
`tipo I`: 40 vigentes, 2 con `inter_fecha NULL`.
En todos los casos nulos encontrados, `ambu_inter = 'A'`.

**Regla de pantalla**
Para `tipo = C`, la fecha es obligatoria solo si `ambu_inter = 'I'` y el parámetro `inter_fecha_cirugias` está en `S`. En Ospecon está en `S`. Referencia: [w_clase_auto_internacion.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/exe/Source%20Infomedical/w_clase_auto_internacion.srw:3834).

Para `tipo = I`, la pantalla valida `inter_fecha` cuando `ambu_inter <> 'A'`. Referencia: [w_clase_auto_internacion.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/exe/Source%20Infomedical/w_clase_auto_internacion.srw:4266).

**Conclusión**
Se puede crear con `inter_fecha = NULL` si la autorización queda como ambulatoria/recuperación (`ambu_inter = 'A'`).

No debería poder crearse desde la pantalla con `inter_fecha = NULL` si es internación (`ambu_inter = 'I'`): para `C` por parámetro activo y para `I` por validación directa.

---

### [20/08/2026 12:43:36] Facundo Farias

Para autorizaciones tipo=C e I se puede asociar autorizaciones tipo =P ? o sea, asociarlas en sucur2, auto2

---

### [20/08/2026 12:45:14] Agente

Facundo, por flujo funcional no: una autorización `tipo = C` o `tipo = I` no debería asociarse en `sucur2/auto2` contra una autorización `tipo = P`.

La validación de pantalla para `auto2` en tipos `C`/`I` llama a `prs_auto_valida_inicial_asoc`. Ese SP en `Ospecon_Prox_Ver` valida que la autorización asociada sea vigente, inicial, internada (`ambu_inter = 'I'`), con `inter_fecha` y `dias`, y que su `tipo` esté dentro de `I/C`. No incluye `P`.

Probé además con una autorización vigente `tipo = P` (`sucur=1`, `auto=279`) y el SP devolvió `valida = 0` tanto para `@prm_tipo = 'C'` como para `@prm_tipo = 'I'`.

Dato técnico: la tabla no tiene FK/check sobre `sucur2/auto2`, así que por SQL directo se podría grabar casi cualquier referencia, pero eso quedaría por fuera de la regla funcional de la aplicación.

---

### [20/08/2026 14:24:23] Facundo Farias

Sobre base smc_prox_ver transac=6514948 rechaza por 668. No rechaza por 90 por que no requiere la prestación 010211 autorizacion?

---

### [20/08/2026 14:28:48] Agente

Facundo, no es porque la prestación `010211` no requiera autorización.

**Evidencia en `SMC_Prox_Ver`:**
La transacción `6514948` es `02Q`, prestador/sanatorio `52`, plan `CLASSIC1`, prestación `010211`, y quedó:
`auto_remotas.recha = 668`
`auto_prestac_items_remotas.recha = 668`
`conve = NULL`, `cober = NULL`.

La prestación `010211` sí está en `prestac_auto_niveles` con `nivel = 2`.

El 668 sale antes por convenio/prestador: el SP `prs_valida_of_convenio` asigna `668` cuando `prs_valida_prest_deshab` detecta la prestación deshabilitada para el convenio del prestador. Para el prestador `52` el convenio vigente es `10347`; la prestación existe en `conve_prestaciones`, pero la validación la está tomando como no habilitada para el caso evaluado.

Además, el 90 es “La autorización de esta prestación depende de un nivel superior” y se maneja como pendiente. En esta transacción no llega a ese escenario porque ya quedó rechazada por 668. También el bloque de salida que trata 90 está acotado a `02C`, `02L`, `02D`; esta transacción es `02Q`.

Conclusión: `010211` sí requiere autorización por nivel, pero el rechazo aplicable en esta transacción es 668 porque falla antes la habilitación de prestación para convenio/prestador.

---

### [20/08/2026 14:34:44] Facundo Farias

Misma situación en PRESMED sobre 25.08 si valida que no posee nivel, o sea, el orden de validación es primero recha 90

---

### [20/08/2026 14:35:16] Facundo Farias

Misma situación en PRESMED, autorización tipo=C Cirugía sobre 25.08 si valida que no posee nivel, o sea, el orden de validación es primero recha 90

---

### [20/08/2026 14:37:07] Agente

Sí, Facundo: para **PRESMED, autorización `tipo=C` Cirugía en 25.08**, la validación de nivel va antes que la validación de convenio/deshabilitación.

La lógica está en [w_clase_auto_internacion.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/exe/Source%20Infomedical/w_clase_auto_internacion.srw:3028): en “Validacion - 5 => NIVEL” busca `prestac_auto_niveles`; si `gi_usuari_nivel < li_nivel`, setea:

`dw_prestac.object.esta = 'P'`
`dw_prestac.object.recha = 90`
`dw_inter.object.recha = 90`

Después recién entra la validación de convenio/deshabilitación.

Ajuste conceptual: no valida que la prestación “no posee nivel”; valida que **la prestación posee un nivel mayor al nivel del usuario**. En ese caso corresponde primero `recha = 90` (“depende de un nivel superior”).

---

### [20/08/2026 14:50:34] Facundo Farias

Pre-compaction memory flush. Store durable memories only in memory/2026-08-20.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-20.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-20-HHMM.md); always use the canonical 2026-08-20.md filename. If nothing to store, reply with NO_REPLY.
Current time: Thursday, August 20th, 2026 - 2:50 PM (America/Buenos_Aires)
Reference UTC: 2026-08-20 17:50 UTC

---

### [20/08/2026 14:51:19] Agente

NO_REPLY

---

### [20/08/2026 14:51:19] Facundo Farias

Por que sale recha 90 si la prestación no requiere ser autorizada?

---

### [20/08/2026 14:52:38] Agente

Porque en PRESMED 25.08 el `recha 90` no depende solamente de que la cobertura diga “requiere autorización”.

En autorización `tipo=C`, el código consulta directo `prestac_auto_niveles` para la prestación. Si existe nivel y el usuario tiene nivel menor, setea `recha = 90` y estado `P`, aunque la prestación no venga marcada como “requiere autorización” por cobertura.

La condición está así:

`prestac_auto_niveles.nivel > gi_usuari_nivel`  
entonces:
`dw_prestac.recha = 90`
`dw_inter.recha = 90`
`dw_prestac.esta = 'P'`

La validación vieja que condicionaba esto a `istr_cober.AUTO = 'S'` quedó como bloque separado/más abajo, pero antes hay una validación nueva que no mira `AUTO = 'S'`.

Entonces, para este caso: si `010211` está en `prestac_auto_niveles` con nivel superior al usuario, sale `90` por control de nivel, no por el flag de “requiere autorización” de cobertura.

---

### [20/08/2026 14:57:40] Facundo Farias

en smc_prox_ver genere la autorización 2049667 con inter_fecha=null y ambu_inter=I esto es correcto?

---

### [20/08/2026 14:58:25] Agente

Sí, Facundo: para `SMC_Prox_Ver` esa autorización quedó consistente con la configuración actual.

Verifiqué `autorizaciones.auto = 2049667`:

`tipo = C`
`ambu_inter = I`
`inter_fecha = NULL`
`dias = 1`
`esta = P`
`recha = 90`
ítem `010211`, `nivel = 2`

La clave está en el parámetro:

`Presmed / Autorizaciones / inter_fecha_cirugias = N`

Según la descripción del propio parámetro: `N` significa que **no es obligatoria la fecha de internación en autorizaciones de cirugías**. Entonces, aunque `ambu_inter = I`, al ser `tipo = C` y estar ese parámetro en `N`, no corresponde bloquear por `inter_fecha NULL`.

El `recha 90` sale por nivel de la prestación `010211`, no por la fecha de internación.

---

### [20/08/2026 15:00:52] Facundo Farias

o sea, con respecto a mi pregunta anterior y tu respuesta "Para tipo = C, la fecha es obligatoria solo si ambu_inter = 'I' y el parámetro inter_fecha_cirugias está en S"

---

### [20/08/2026 15:01:01] Agente

Correcto, Facundo.

Mi respuesta anterior aplica así:

Para `tipo = C`, `inter_fecha` es obligatoria solo si:

`ambu_inter = 'I'`
y
`parametros_generales.valor = 'S'` para `Presmed / Autorizaciones / inter_fecha_cirugias`

En `SMC_Prox_Ver` ese parámetro está en `N`, entonces **no es obligatorio informar `inter_fecha`**, aunque `ambu_inter = 'I'`.

Por eso la autorización `2049667` con `tipo=C`, `ambu_inter=I`, `inter_fecha=NULL` es válida respecto de esa regla.

---

### [20/08/2026 15:58:02] Facundo Farias

con respecto a orden validacion donde presmed toma primero recha 90 a diferencia del portal de prestadores que toma recha 668. Presmed es validación desde ventana y no por SP, correcto?

---

### [20/08/2026 15:58:11] Agente

Correcto, Facundo.

En **PRESMED ventana** la validación de la cirugía pasa por PowerBuilder, principalmente `w_clase_auto_internacion.srw`. Ahí el orden está armado en la lógica de pantalla: primero evalúa el bloque de **nivel** contra `prestac_auto_niveles` y, si el usuario no alcanza el nivel, setea `recha = 90` y estado `P`.

En cambio, en **portal/prestadores/remotas**, la validación va por SPs como `prs_valida_of_cobertura_remoto`, `prs_valida_of_convenio` y flujo de `auto_remotas`. Ahí puede quedar primero `668` por prestación deshabilitada para convenio/prestador, antes de llegar al escenario funcional de pendiente por nivel.

Entonces sí: la diferencia viene de que PRESMED valida desde ventana PowerBuilder, no por el mismo camino de SP remoto del portal.

---

### [20/08/2026 16:41:31] Facundo Farias

Transac 6514972 y 6514973 a diferencia de la transac 6514974, responden con otro recha. Por orden de validación se debe validar recha 4 primero

---

### [20/08/2026 16:43:39] Agente

Sí, Facundo: si el criterio funcional es **“por orden de validación debe salir primero recha 4”**, entonces `6514972` y `6514973` no están respetando ese criterio.

Evidencia en `SMC_Prox_Ver`:

`6514972` queda con `recha 20`:
`Prestacion sin convenio para este plan`

`6514973` queda con `recha 32`:
`Prestador inhabilitado`

`6514974` queda con `recha 4`:
`Afiliado actualmente moroso`

Las tres tienen mismo afiliado/prestación/base de caso. Además, `recha 4` está en grupo de rechazo `1`, orden `2`; `recha 32` está en grupo `2`, orden `1`; y `recha 20` está en grupo `6`, orden `1`. Por prioridad funcional, el grupo afiliado debería evaluarse antes que prestador/convenio.

El problema está en el flujo de `pri_transac_remotas`: la validación temprana de afiliado/morosidad (`prs_valida_of_afiliado_remoto`) está condicionada a que exista una autorización relacionada sin ítems rechazados. Para estas transacciones sin autorización relacionada, el flujo sigue y puede terminar antes con convenio (`20`) o prestador (`32`). En cambio `6514974`, por su camino `02C`, termina exponiendo `4`.

Conclusión: sí, por orden de validación debería priorizarse `recha 4`; el comportamiento actual muestra diferencia de flujo según tipo de transacción, no una prioridad uniforme de rechazos.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
