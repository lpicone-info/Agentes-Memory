# Memoria curada diaria

- Fecha: 2026-07-24
- Usuario: Gabriel Santana
- id_googlechat: users/112125759360053189817
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/7aicqkaaaae

## Resumen de la actividad

### Tema 1: Configuracion del formato de importacion de DDJJ en Obras Sociales

- Solicitud o contexto: Gabriel consulto donde se configura el archivo solicitado por el proceso de importacion de DDJJ en Afilmed. Primero se identifico una opcion incorrecta relacionada con usuarios prepagas y Gabriel aclaro que no aplicaba al proceso mencionado.
- Respuesta o acciones del agente: Se reviso codigo PowerBuilder de Afilmed en repositorios SVN, especialmente menus y ventanas de importacion/configuracion. Se corrigio el diagnostico y se ubico el flujo de `Aportes > Obra social > Afip > Importacion de DDJJ`.
- Resultado: La configuracion correcta se informo como `Configuracion > Definicion de formatos de archivos > Importacion de aportes y cuentas`, ventana `wp_aportes_form`, seleccionando `Archivo: D. JURADAS A.F.I.P.`, la obra social correspondiente o `(0) Todas`, y `Tipo registro: CABECERA`, `DETALLE` o `TOTALES`. Se indico que usa `homolog_formatos` y `homolog_formatos_cab`.
- Estado: Resuelto

### Tema 2: Comparacion de dos archivos CSV de DDJJ de OSEN

- Solicitud o contexto: Gabriel pregunto si se podian comparar dos archivos para detectar diferencias de formato y luego envio `DDJJ_04-2026_OSEN...csv` y `DDJJ_05-2026_OSEN...csv`.
- Respuesta o acciones del agente: Se recibio el primer archivo, se identifico como CSV separado por `;`, sin cabecera y con 16 campos por linea. Tras recibir el segundo, se compararon ambos archivos completos en estructura, separador, campos, encoding y fin de linea.
- Resultado: Se concluyo que el formato coincidia salvo en el campo 14, presumiblemente periodo: el archivo de abril venia como `AAAAMM` y el de mayo como `MMAAAA`. Se informo que, si Afilmed espera `AAAAMM`, el archivo de mayo esta invertido y podria fallar o importar incorrectamente ese campo.
- Estado: Resuelto

### Tema 3: Verificacion de formato especial CONSALUD para DDJJ

- Solicitud o contexto: Gabriel pidio verificar si CONSALUD tenia un formato especial o hardcodeado para importacion de DDJJ, o si se habia enviado un script por Redmine impactando tablas Homolog.
- Respuesta o acciones del agente: Se reviso codigo, SVN de Incidentes y la base `Consalud_Prod_Test`, buscando evidencias de hardcode en stored procedures y scripts de homologacion.
- Resultado: Se informo que no se encontro formato DDJJ hardcodeado en SP de importacion. La evidencia apunto a configuracion por tablas Homolog mediante el incidente `R27747`, con scripts `script_HOMOLOG_CONSALUD.sql`, y a una configuracion base en `R20735`. Se valido que `homolog_formatos_cab` y `homolog_formatos` tenian configuracion vigente para `enti_tipo = D`, `enti_codi = 0`, separador `;`, 16 columnas de detalle y periodo con mascara `YYYYMM`. Se aclaro que existen hardcodes de CONSALUD en otros procesos, pero no para definir el formato del archivo DDJJ.
- Estado: Resuelto

### Tema 4: Configuracion no visible para OS Todas en Consalud_Prod_Test

- Solicitud o contexto: Gabriel informo que en `Configuracion > Definicion de formatos de archivos > Importacion de aportes y cuentas`, usando `Consalud_Prod_Test`, no podia ver la configuracion para la opcion de obra social `TODAS`.
- Respuesta o acciones del agente: Se valido contra la base `Consalud_Prod_Test` que la configuracion estuviera cargada en tablas Homolog y se reviso como recupera datos la ventana `wp_aportes_form`.
- Resultado: Se confirmo que la configuracion existe para `enti_tipo = D`, `enti_codi = 0`, con cabecera, detalle y totales. Para verla, se indico seleccionar `Archivo: D. JURADAS A.F.I.P.`, `Obra Social: (0)Todas` y `Tipo registro: DETALLE`. Si no aparece, se marco como causa probable que la pantalla no quede con `cuenta = 0` o que la sesion no este conectada realmente a `Consalud_Prod_Test`.
- Estado: Resuelto
