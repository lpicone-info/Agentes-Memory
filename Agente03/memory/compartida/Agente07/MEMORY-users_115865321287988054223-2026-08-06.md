# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Patricia de Lourdes Bossa
- id_googlechat: users/115865321287988054223
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/jrvbkkaaaae

## Resumen de la actividad

### Tema 1: Query de prestador, prestacion, convenio y plan en Ospecon_Prox_Ver

- Solicitud o contexto: Patricia pidio una query sobre `Ospecon_Prox_Ver` para mostrar la relacion entre prestador, prestacion, convenio y plan, incluyendo si el convenio tiene valor o no.
- Respuesta o acciones del agente: Se analizo la relacion por cartilla `planes.carti -> carti_convenios -> convenio/prestador`, con `conve_prestaciones`, `prestaciones` y `conve_prestac_valores`.
- Resultado: Se entrego una query que lista prestador, plan, cartilla, convenio, prestacion y valor vigente, con columna `estado_valor` para distinguir `CON VALOR` y `SIN VALOR`.
- Estado: Resuelto

### Tema 2: Query de convenios sin valor

- Solicitud o contexto: Patricia pidio una variante que muestre solo convenios sin valor en la misma base.
- Respuesta o acciones del agente: Se adapto la consulta anterior usando `INNER JOIN conve_prestaciones` y `LEFT JOIN` al ultimo valor vigente, filtrando `cpv.conve_prestac IS NULL`.
- Resultado: Se entrego una query que devuelve solo la relacion prestador/plan/convenio/prestacion cuando no existe valor vigente cargado.
- Estado: Resuelto

### Tema 3: Centro de atencion y circulo en Web POS o Presmed

- Solicitud o contexto: Patricia pregunto si conceptualmente centro de atencion y circulo son lo mismo en Web POS o Presmed productivo.
- Respuesta o acciones del agente: Se dio una explicacion funcional sin asumir sinonimia.
- Resultado: Se aclaro que no son lo mismo: centro de atencion representa un lugar fisico u operativo de atencion; circulo representa una agrupacion funcional o de red. Pueden relacionarse, pero no deben tomarse como sinonimos sin revisar las tablas concretas.
- Estado: Informativo

### Tema 4: Pantalla y query de centro medico con prestadores asociados

- Solicitud o contexto: Patricia pidio donde ver funcionalmente la relacion de un centro medico con sus prestadores y una query para consultarla.
- Respuesta o acciones del agente: Primero se pidio precisar modulo/repositorio. Luego, sobre `25.08.001`, se reviso Presmed.
- Resultado: Se indico la consulta `Presmed > Prestadores > Consultas > Prestadores Asociados a Centros`, ventana `wp_cons_prestad_lugares_centros`, y la carga/mantenimiento desde `Presmed > Prestadores > Prestadores > boton Asociados`, ventana `wr_prestad_asoc`. Tambien se entrego una query base con `prestad_asociados`, `prestadores`, `prestad_lugares` y `servi_tipos`.
- Estado: Resuelto

### Tema 5: Usuario como centro medico en Admin y puntos de atencion

- Solicitud o contexto: Patricia informo un error al guardar un usuario como centro medico en Admin `25.08.001`, para acceder al portal de prestadores.
- Respuesta o acciones del agente: Se reviso la ventana `wp_sucur_clini.srw` y la validacion del campo cuando `es_centro = 'S'`.
- Resultado: Se explico que Admin valida el codigo informado contra `puntos_atencion.cod_punto_atencion`, no contra `prestadores.prestad`. Si el codigo `1655` existe como prestador pero no como punto de atencion, muestra "Centro de atencion inexistente". Se dieron queries para verificar `prestadores`, `puntos_atencion` y relaciones con `prestad_lugares`.
- Estado: Resuelto

### Tema 6: Carga de puntos de atencion en Presmed

- Solicitud o contexto: Patricia no encontraba donde cargar puntos de atencion en Presmed `25.08.001` para Ospecon.
- Respuesta o acciones del agente: Se reviso el menu de Presmed en la rama indicada.
- Resultado: Se indico que la carga esta en `Presmed > Prestadores > Centros de Atencion`, ventana `wp_puntos_atencion`, titulo `Centros de Atencion`, tabla `puntos_atencion`. Se aclaro que si no aparece, puede deberse a permisos de menu o ambiente/ejecutable distinto.
- Estado: Resuelto

### Tema 7: Insercion manual de punto de atencion identity

- Solicitud o contexto: Patricia intento insertar `cod_punto_atencion = 1655` en `puntos_atencion` y SQL Server respondio que no puede insertar valor explicito en una columna identity con `IDENTITY_INSERT` apagado.
- Respuesta o acciones del agente: Se explico la causa y se dieron dos alternativas: insertar solo `deno` si no importa el codigo, o habilitar `IDENTITY_INSERT` si el codigo debe ser exactamente `1655`.
- Resultado: Se entrego un bloque transaccional para validar inexistencia, activar `IDENTITY_INSERT`, insertar `cod_punto_atencion = 1655`, apagar `IDENTITY_INSERT` y confirmar. Tambien se aclaro que Admin busca exactamente el valor del campo Codigo contra `puntos_atencion.cod_punto_atencion`.
- Estado: Resuelto

### Tema 8: Error en portal de prestadores al informar prestacion

- Solicitud o contexto: Patricia informo que al entrar al portal de prestadores `25.08.001` e informar una prestacion aparecia un error.
- Respuesta o acciones del agente: Se reviso el flujo PHP, especialmente `02A.php`, `frame_prestador.php` y el SP `ars_centro_prestadores`.
- Resultado: Se explico que el centro `1655` existia como `puntos_atencion`, pero el portal no encontraba prestadores/lugares asociados al centro. El array de prestadores quedaba vacio y luego se intentaba usar el primer elemento, provocando errores. Se indico que `ars_centro_prestadores` busca en `prestad_lugares.cod_punto_atencion = 1655`, y que la correccion funcional es asignar el punto de atencion al lugar del prestador desde Presmed o mediante update controlado sobre `prestad_lugares`.
- Estado: Resuelto

### Tema 9: Origen de datos de combos de centros y prestadores

- Solicitud o contexto: Patricia pregunto de que tabla toma los centros de atencion para mostrarlos en combos del portal y luego en la pantalla de Presmed.
- Respuesta o acciones del agente: Se distinguieron dos contextos: portal de prestadores y pantalla de Presmed.
- Resultado: Para el portal se explico que el centro se lee desde `puntos_atencion` mediante `prs_puntos_atencion`, pero el combo de prestadores se arma desde `ars_centro_prestadores`, cruzando `prestad_lugares.cod_punto_atencion` con `prestadores`. Para Presmed se indico que el combo `Centros de Atencion` usa el DataWindow `dd_puntos_atencion`, con `SELECT cod_punto_atencion, deno FROM puntos_atencion WHERE baja_fecha IS NULL ORDER BY deno`, guardando el codigo en `prestad_lugares.cod_punto_atencion`.
- Estado: Resuelto

### Tema 10: Combo de puntos de atencion sin datos pese a tabla cargada

- Solicitud o contexto: Patricia pregunto por que el combo no mostraba datos si la tabla `puntos_atencion` tenia registros.
- Respuesta o acciones del agente: Se reviso `wp_presta_insti.srw`.
- Resultado: Se explico que el DataWindow `dd_puntos_atencion` existe, pero la carga del combo aparece comentada: `Retrieve()` y `GetChild()` estaban deshabilitados. Por eso, si la query a `puntos_atencion` devuelve datos activos, el problema no es la tabla sino la carga del child DataWindow en la pantalla. Se recomendo recuperar el child DDDW y hacer `Retrieve()` sobre ese child.
- Estado: Resuelto

### Tema 11: Agregar nomenclador de laboratorio a convenio

- Solicitud o contexto: Patricia pregunto como agregar un nomenclador de laboratorio a un convenio.
- Respuesta o acciones del agente: Se explico la logica funcional y tecnica de convenios en Presmed.
- Resultado: Se aclaro que Presmed no agrega un nomenclador completo al convenio como cabecera unica; el convenio queda asociado cuando tiene prestaciones de ese nomenclador en `conve_prestaciones`. Se indicaron dos caminos: carga manual desde `Presmed > Convenios > Convenios` (`wp_convenios`) o carga masiva desde `Presmed > Convenios > Importacion de Prestaciones - Valores a Convenios` (`wp_imp_convenios`). Se listaron las tablas `nomencladores`, `prestaciones`, `conve_prestaciones` y `conve_prestac_valores`, y se recomendo usar SPs del sistema como `pru_conve_prestaciones` o `pru_conve_prestac_valores` antes que inserts directos.
- Estado: Resuelto
