# Memoria de conversación — Facundo Farias

## Datos

- **Fecha:** 10/09/2026
- **Usuario:** Facundo Farias
- **ID Google Chat:** `users/110088613929347470679`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 33
- **Inicio:** 09:59:08
- **Fin:** 15:49:07

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/9tgygkaaaae`

## Archivos revisados

- `dc41c0ab-f375-42a6-a486-25a8f1d0e134.jsonl`

---

## Transcripción completa

### [10/09/2026 09:59:08] Facundo Farias

hola

---

### [10/09/2026 10:00:12] Agente

Hola Facundo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [10/09/2026 10:24:44] Facundo Farias

decime que repositorios svn tenes registrados

---

### [10/09/2026 10:24:52] Agente

Facundo, tengo registrados estos repositorios SVN:

- `mpariso-Infomedical-trunk`
- `mpariso-V25.08.001`
- `mpariso-Incidentes-Incidentes`
- `mpariso-ManualesProyectoEstandar-ManualesProyectoEstandar`
- `ffarias-V25.08.001_PROX`

---

### [10/09/2026 10:25:07] Facundo Farias

ffarias-V25.08.001_PROX a que corresponde?

---

### [10/09/2026 10:25:16] Agente

Facundo, `ffarias-V25.08.001_PROX` corresponde a esta working copy SVN:

- **Ruta local:** `~/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`
- **Remoto:** `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001_PROX`
- **Rama:** `V25.08.001_PROX`
- **Usuario SVN asociado:** `ffarias`
- **Uso registrado:** repositorio de consulta; solo permite `svn update`, no `commit`, `add` ni `delete`.

---

### [10/09/2026 10:26:08] Facundo Farias

por favor borrala: ffarias-V25.08.001_PROX

---

### [10/09/2026 10:27:02] Agente

Encontré la referencia lógica en `REPOSITORIOS.md`.

Referencia lógica:
`ffarias-V25.08.001_PROX`

Tipo:
`SVN`

Carpeta local que se va a eliminar:
`/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`

También se va a eliminar la entrada correspondiente de `REPOSITORIOS.md`.

Confirmación 1 de 2:
Respondé exactamente: `CONFIRMO ELIMINAR SVN`

---

### [10/09/2026 10:27:41] Facundo Farias

CONFIRMO ELIMINAR SVN

---

### [10/09/2026 10:27:48] Agente

Confirmación 2 de 2.

Última validación antes de eliminar:

Referencia lógica:
`ffarias-V25.08.001_PROX`

Carpeta local:
`/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`

Esta acción eliminará la carpeta local y removerá la entrada correspondiente de `REPOSITORIOS.md`.

Para continuar, respondé exactamente:
`CONFIRMO DEFINITIVAMENTE ELIMINAR SVN`

---

### [10/09/2026 10:28:31] Facundo Farias

CONFIRMO DEFINITIVAMENTE ELIMINAR SVN

---

### [10/09/2026 10:30:16] Agente

Eliminación SVN completada.

Referencia lógica eliminada:
`ffarias-V25.08.001_PROX`

Carpeta local eliminada:
`/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`

`REPOSITORIOS.md` actualizado: entrada eliminada correctamente.

Validaciones realizadas:
carpeta local eliminada OK; entrada removida de `REPOSITORIOS.md` OK; encoding UTF-8 sin BOM y finales LF preservados. Dejé respaldo en `REPOSITORIOS.md.bak-svn-delete-20260910-102928`.

---

### [10/09/2026 10:31:01] Facundo Farias

no eliminaste `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001_PROX` verdad?

---

### [10/09/2026 10:31:09] Agente

Correcto, Facundo. No eliminé nada del remoto SVN.

Solo hice estas dos acciones locales:

- Eliminé la carpeta local:
`/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`

- Removí su entrada de:
`REPOSITORIOS.md`

No ejecuté `svn delete`, `svn remove`, `svn commit` ni ninguna operación remota sobre `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001_PROX`.

---

### [10/09/2026 11:01:02] Facundo Farias

en TRUNK sobre la base: premedic_prod_test
 necesito me digas en presmed ventana pedidos de autorizacion, por qué no aparece en el combo "mensaje si la autorización es rechazada" los mensajes de rechazo configurados:

---

### [10/09/2026 11:05:32] Agente

Facundo, en `TRUNK` el combo no está filtrando por tipo de pedido, sucursal ni prestación. Debería traer todos los mensajes activos de `PEDIDOS_MENSAJES_CONFIG`.

Validé en `Premedic_Prod_Test` y hay 4 mensajes activos (`baja_fecha IS NULL`), así que el problema no es falta de configuración en base.

La causa más probable está en el cambio `r29281`: la tabla quedó con `texto_msj varchar(MAX)`, pero los DataWindows del combo siguen declarando `texto_msj` como `char(200)`.

Evidencia:

- [backend/tables/pedidos_mensajes_config.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/tables/pedidos_mensajes_config.sql:17): `texto_msj varchar(MAX)`
- [dd_pedidos_msjs_config_texto.srd](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/exe/Source%20Infomedical/dd_pedidos_msjs_config_texto.srd:10): `texto_msj` definido como `char(200)`
- [d_abm_pedidos_auto.srd](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/exe/Source%20Infomedical/d_abm_pedidos_auto.srd:45): el combo usa `dd_pedidos_msjs_config_texto`

Además, en base hay mensajes con textos de 395 y 1203 caracteres. Eso deja inconsistente el tipo del resultset contra la definición del DDDW y puede hacer que el retrieve del drop-down falle, quedando el combo vacío.

Solución recomendada: ajustar `dd_pedidos_msjs_config_texto` y también `dd_pedidos_msjs_config` para que el `SELECT` devuelva un texto casteado/truncado para visualización, por ejemplo:

```sql
CONVERT(varchar(200), pedidos_mensajes_config.texto_msj) AS texto_msj
```

Así se mantiene `varchar(MAX)` para guardar textos largos, pero el combo recibe el tipo/longitud que espera.

---

### [10/09/2026 11:59:06] Facundo Farias

En version 25.08.001 base smc_prox_ver. La transac 6555348 por que rechaza remotas la 02I si lo mismo desde PRESMED no rechaza?

---

### [10/09/2026 12:05:47] Agente

Facundo, la `02I` remota `6555348` rechaza por diferencia de circuito de validación.

En `SMC_Prox_Ver`, la transacción está así:

- `auto_remotas.transac = 6555348`
- `tipo_transac = 02I`
- `esta = R`
- `recha = 25`
- Motivo: `El sanatorio no tiene definida la prestacion para los convenios del plan`
- Ítem: `nomen = 2`, `prestac = 180107`, `conve = 10230`
- Prestación: `ECOGRAFIA CEREBRAL -CON MODO B Y A- Y/O TRANSCRANEANA`

La causa concreta: el convenio `10230 - IRURETA SUSANA` tiene cargada la prestación `180107` solo con `concep = 5` honorarios. No tiene cargado el `concep = 6` gastos.

Probé la misma validación que usa la remota:

- `prs_valida_of_convenio` con `ayu_espe = 'H'` devuelve OK, convenio `10230`, valor `110000`.
- `prs_valida_of_convenio` con `ayu_espe = 'P'` devuelve `erro = 25`.

Ese segundo llamado es el que hace que la remota termine rechazada. En código está en [prs_valida_of_convenio_remoto.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/backend/procedures/prs_valida_of_convenio_remoto.sql:212): primero valida honorarios y después gastos. El rechazo 25 se arma en [prs_valida_of_convenio.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001/backend/procedures/prs_valida_of_convenio.sql:549) cuando `@sanatorio = 'T'` y no encuentra el concepto requerido.

Por eso desde PRESMED no te rechaza “lo mismo”: el circuito local de internación usa `prs_valida_of_convenio_inter`, y con los mismos datos devuelve `erro = 0`, porque encuentra el convenio por honorarios para esta práctica no quirúrgica. La remota, en cambio, además exige resolver gastos como sanatorio y ahí no encuentra configuración.

Solución de datos: agregar al convenio `10230` la prestación `2 / 180107` con `concep = 6` y su valor vigente, si funcionalmente corresponde que el sanatorio tenga gastos definidos para esa práctica.

---

### [10/09/2026 13:15:43] Facundo Farias

en conve_prestaciones tenemos para un convenio relacionadas las prestaciones con sus conceptos. Necesito un convenio que SOLO tenga prestaciones con concepto 6 - gastos
Siempre smc prox ver, dame también el query para obtener estos datos

---

### [10/09/2026 13:16:04] Facundo Farias

en conve_prestaciones tenemos para un convenio relacionadas las prestaciones con sus conceptos. Necesito un convenio que SOLO tenga prestaciones con concepto 6 - gastos
Siempre smc prox ver, dame también el query para obtener estos datos

---

### [10/09/2026 13:16:08] Facundo Farias

en conve_prestaciones tenemos para un convenio relacionadas las prestaciones con sus conceptos. Necesito un convenio que SOLO tenga prestaciones con concepto 6 - gastos
Siempre smc prox ver, dame también el query para obtener estos datos

---

### [10/09/2026 13:16:57] Agente

En `SMC_Prox_Ver` encontré varios convenios que tienen exclusivamente prestaciones vigentes con `concep = 6`.

Un convenio simple para usar como prueba es:

- **Convenio:** `10439`
- **Descripción:** `DIAVERUM`
- **Prestaciones vigentes:** `1`
- **Concepto:** `6 - Gastos`

Detalle:

```text
conve  nomen  prestac  concep  conve_prestac
10439  2      270102   6       632207
```

Query para obtener todos los convenios que solo tienen concepto 6:

```sql
SELECT
    cp.conve,
    c.deno AS convenio,
    COUNT(*) AS cantidad_relaciones,
    COUNT(DISTINCT cp.conve_prestac) AS cantidad_prestaciones
FROM dbo.conve_prestaciones AS cp
INNER JOIN dbo.convenios AS c
    ON c.conve = cp.conve
WHERE cp.baja_fecha IS NULL
  AND c.baja_fecha IS NULL
GROUP BY
    cp.conve,
    c.deno
HAVING SUM(
           CASE
               WHEN cp.concep <> 6 OR cp.concep IS NULL THEN 1
               ELSE 0
           END
       ) = 0
ORDER BY
    cantidad_relaciones,
    cp.conve;
```

Para obtener un solo convenio, preferentemente el que tenga menos prestaciones:

```sql
SELECT TOP 1
    cp.conve,
    c.deno AS convenio,
    COUNT(*) AS cantidad_relaciones,
    COUNT(DISTINCT cp.conve_prestac) AS cantidad_prestaciones
FROM dbo.conve_prestaciones AS cp
INNER JOIN dbo.convenios AS c
    ON c.conve = cp.conve
WHERE cp.baja_fecha IS NULL
  AND c.baja_fecha IS NULL
GROUP BY
    cp.conve,
    c.deno
HAVING SUM(
           CASE
               WHEN cp.concep <> 6 OR cp.concep IS NULL THEN 1
               ELSE 0
           END
       ) = 0
ORDER BY
    cantidad_relaciones,
    cp.conve;
```

Para consultar el detalle del convenio `10439`:

```sql
SELECT
    cp.conve,
    c.deno AS convenio,
    cp.nomen,
    RTRIM(cp.prestac) AS prestac,
    cp.concep,
    cp.conve_prestac,
    cp.alta_fecha,
    cp.fecha_hasta
FROM dbo.conve_prestaciones AS cp
INNER JOIN dbo.convenios AS c
    ON c.conve = cp.conve
WHERE cp.conve = 10439
  AND cp.baja_fecha IS NULL
  AND c.baja_fecha IS NULL
ORDER BY
    cp.nomen,
    RTRIM(cp.prestac);
```

La consulta devuelve solo relaciones vigentes y descarta convenios que tengan aunque sea una prestación con concepto distinto de `6`.

---

### [10/09/2026 15:09:04] Facundo Farias

TRANSACCIONES TIPO 02I DESDE REMOTAS, EL TAG DEL XML <ArancelPrestacion></ArancelPrestacion> SE CONSIDERA PARA TRANSACCIONAR?

---

### [10/09/2026 15:11:18] Agente

Facundo: para una `02I` remota, `ArancelPrestacion` se lee y se guarda, pero no se usa para decidir la transacción.

Flujo encontrado:

- El XML se parsea en `DetalleProcedimientos.java`.
- Se toma `<ArancelPrestacion>` y se pasa a `pri_auto_prestac_items_remotas`.
- El SP lo guarda en `auto_prestac_items_remotas.aran_prestac`.
- Pero en `pri_transac_remotas` para `02I` no hay uso de `aran_prestac`.
- Tampoco aparece en `prs_valida_of_convenio_remoto` ni en `prs_valida_of_auto_prestac_int`.

O sea: para aceptar/rechazar una `02I`, no cambia si `ArancelPrestacion` viene `0`, `1`, `2`, `4` o `NULL`. El rechazo que vimos antes por `recha = 25` sigue saliendo por convenio/concepto, no por este tag.

Sí se usa `aran_prestac` en otros circuitos, especialmente cirugía/`02Q` y armado posterior de expediente, pero no como condición de validación de una `02I`.

Detalle importante: en Java hay este bug:

```java
String arancelPrestacionInteger = XmlUtils.obtenerValor(rootElement,"ArancelPrestacion");
if(!StringUtils.getInstance().isNullOrEmpty(arancelPrestacionInteger));
    this.arancelPrestacion = Integer.parseInt(arancelPrestacionInteger);
```

Ese `;` al final del `if` hace que siempre intente hacer `Integer.parseInt(...)`. Entonces si viene exactamente vacío:

```xml
<ArancelPrestacion></ArancelPrestacion>
```

podría fallar en el parseo antes de llegar a transaccionar, dependiendo de qué devuelva `XmlUtils.obtenerValor`: `""` o `null`.

Conclusión directa: para `02I`, si el mensaje ya llegó a `auto_remotas` y se transaccionó, `ArancelPrestacion` no fue considerado para aceptar/rechazar. Si viene vacío y falla, el problema sería de parseo del XML, no de la validación de la transacción.

---

### [10/09/2026 15:18:58] Facundo Farias

Pre-compaction memory flush. Store durable memories only in memory/2026-09-10.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-10.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-10-HHMM.md); always use the canonical 2026-09-10.md filename. If nothing to store, reply with NO_REPLY.
Current time: Thursday, September 10th, 2026 - 3:18 PM (America/Buenos_Aires)
Reference UTC: 2026-09-10 18:18 UTC

---

### [10/09/2026 15:19:52] Agente

NO_REPLY

---

### [10/09/2026 15:19:53] Facundo Farias

o sea no importa el valor que coloque en el tag, el sistema no lo usar

---

### [10/09/2026 15:20:02] Agente

Correcto, Facundo.

Para `02I` remota, el valor de `<ArancelPrestacion>` no se usa para aceptar/rechazar ni para calcular el convenio en la transacción.

Solo queda guardado en:

```sql
auto_prestac_items_remotas.aran_prestac
```

Pero la validación de `02I` pasa por convenio/prestación/concepto, especialmente `prs_valida_of_convenio_remoto`, y ahí `aran_prestac` no participa.

Única salvedad: si el tag viene vacío y el parser Java intenta convertirlo a entero, podría fallar antes de llegar a base por el bug del `if (...);`. Pero si llega a base, el valor no influye en la validación de la `02I`.

---

### [10/09/2026 15:39:37] Facundo Farias

Este xml no responde, me decis que tengo mal?

<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ser="http://service.autorem.com/">
 <soapenv:Header/>
 <soapenv:Body>
 <ser:EjecutarTransaccion>
 <!--Optional:-->
 <mensajeXml>
<![CDATA[
<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<Mensaje>
<EncabezadoMensaje>
 <VersionMsj>1.0</VersionMsj>
 <NroReferencia>036747</NroReferencia>
 <TipoMsj>OL</TipoMsj>
 <TipoTransaccion>02I</TipoTransaccion>
 <IdMsj>03678966</IdMsj>
 <InicioTrx>
 <FechaTrx>20260910</FechaTrx>
 <HoraTrx>090000</HoraTrx>
 </InicioTrx>
 <Terminal>
 <TipoTerminal>PC</TipoTerminal>
 <NumeroTerminal>98026301</NumeroTerminal>
 </Terminal>
 <Validador>
 <NombreConcentrador>33707000509</NombreConcentrador>
 <NombreValidador>30699630450</NombreValidador>
 </Validador>
 <Financiador>
 <CodigoFinanciador>3501</CodigoFinanciador>
 </Financiador>
 <Prestador>
 <CuitPrestador>30676643601</CuitPrestador>
 <SucursalPrestador></SucursalPrestador>
 <CodigoPrestador></CodigoPrestador>
 <lugarAtencion>
 <codigo>1</codigo>
 </lugarAtencion>
 </Prestador>
 </EncabezadoMensaje>
 <EncabezadoAtencion>
 <Prescriptor>
 <ProvinciaPrescriptor>B</ProvinciaPrescriptor>
 <TipoPrescriptor>M</TipoPrescriptor>
 <NroMatriculaPrescriptor>9999</NroMatriculaPrescriptor>
 </Prescriptor>
 <Credencial>
 <NumeroCredencial></NumeroCredencial>
 <ModoIngreso>M</ModoIngreso>
 </Credencial>
 <Beneficiario>
 <Entidad>2</Entidad>
 <Contra>107529</Contra>
 <Inte>00</Inte>
 </Beneficiario>
 <Atencion>
 <FechaAtencion>20260910</FechaAtencion>
 <HoraAtencion>160000</HoraAtencion>
 </Atencion>
<Internacion>
<InterFecha>20260910</InterFecha>
<InterHora>101000</InterHora>
<ICD>002</ICD>
<InterTipo>A</InterTipo>
<InterOri>1</InterOri>
 <RecienNacido>N</RecienNacido>
 <InterSala>1</InterSala>
 </Internacion>
 </EncabezadoAtencion>

 <DetalleProcedimientos>
 <NroItem>1</NroItem>
 <CodPrestacion>660001</CodPrestacion>
 <Nomenclador>18</Nomenclador>
 <TipoPrestacion>1</TipoPrestacion>
 <ArancelPrestacion>0</ArancelPrestacion>
 <CantidadSolicitada>01</CantidadSolicitada>
 </DetalleProcedimientos>


</Mensaje>
]]>

 </mensajeXml>
 </ser:EjecutarTransaccion>
 </soapenv:Body>
</soapenv:Envelope>

---

### [10/09/2026 15:44:47] Agente

Facundo, lo que tenés mal en ese XML es este dato:

```xml
<ICD>002</ICD>
```

En `SMC_Prox_Ver` no existe un ICD activo `002`. El servicio sí procesó el XML y grabó estas transacciones:

```text
6555350 -> rechazada, recha 704
6555351 -> rechazada, recha 704
```

El rechazo `704` es:

```text
ICD inválido
```

Validé además que estos datos están bien:

```text
CUIT prestador 30676643601 -> prestador 239, LADIAC S.A., activo
Prestación 18 / 660001 -> existe, activa
ArancelPrestacion = 0 -> se parsea y queda grabado, pero no define el rechazo
```

Sobre tu pregunta anterior: para `02I`, `<ArancelPrestacion>` se carga en `auto_prestac_items_remotas.aran_prestac`, pero no es lo que valida/rechaza la transacción. En este caso el rechazo viene por el ICD.

Probá reemplazando:

```xml
<ICD>002</ICD>
```

por un ICD válido, por ejemplo:

```xml
<ICD>998</ICD>
```

Query para verificarlo:

```sql
SELECT icd, deno, baja_fecha
FROM dbo.icds
WHERE icd = '002';

SELECT recha, deno
FROM dbo.recha_motivos
WHERE recha = 704;

SELECT transac, recha, esta, ingre_icd, tipo_transac, prepaga, contra, inte, cuit_prestad
FROM dbo.auto_remotas
WHERE transac IN (6555350, 6555351);
```

---

### [10/09/2026 15:45:37] Facundo Farias

responde esto: <mensajeXml>
<![CDATA[
<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<Mensaje>
 <EncabezadoMensaje>
 <VersionMsj>1.0</VersionMsj>
 <NroReferencia>7769888</NroReferencia>
 <EsCentro>N</EsCentro>
 <TipoMsj>OL</TipoMsj>
 <TipoTransaccion>02I</TipoTransaccion>
 <IdMsj>036747</IdMsj>
 <InicioTrx>
 <FechaTrx>20260908</FechaTrx>
 <HoraTrx>125800</HoraTrx>
 </InicioTrx>
 <Terminal>
 <TipoTerminal>PC</TipoTerminal>
 <NumeroTerminal></NumeroTerminal>
 </Terminal>
 <Validador>
 <NombreConcentrador>33707000509</NombreConcentrador>
 <NombreValidador>30699630450</NombreValidador>
 </Validador>
 <Financiador>
 <CodigoFinanciador></CodigoFinanciador>
 <CuitFinanciador></CuitFinanciador>
 </Financiador>

 <Prestador>
 <CuitPrestador>30676643601</CuitPrestador>
 </Prestador>

 </EncabezadoMensaje>
 <EncabezadoAtencion>
<Prescriptor>
 <ProvinciaPrescriptor></ProvinciaPrescriptor>
 <TipoPrescriptor></TipoPrescriptor>
 <NroMatriculaPrescriptor></NroMatriculaPrescriptor>
 <ApellidoPrescriptor>SANATORIO MODELO DE CASEROS</ApellidoPrescriptor>
 </Prescriptor>

 <Credencial>
 <NumeroCredencial></NumeroCredencial>
 <ModoIngreso>M</ModoIngreso>
 <CodigoSeguridad></CodigoSeguridad>
 </Credencial>
 <Beneficiario>
 <Entidad>2</Entidad>
 <Contra>107529</Contra>
 <Inte>00</Inte>
 </Beneficiario>

 <Atencion>
 <FechaAtencion>20260909</FechaAtencion>
 </Atencion>
<Preautorizacion>
 <CodigoPreautorizacion></CodigoPreautorizacion>
 <Sucursal>1</Sucursal>
 </Preautorizacion>
 <Diagnostico>
 <CodDiagnostico>I15</CodDiagnostico>
 </Diagnostico>
 </EncabezadoAtencion>
 <Internacion>
<InterFecha>20260909</InterFecha>
<InterHora>101000</InterHora>
<ICD>E66</ICD>
<InterTipo>A</InterTipo>
<InterOri>1</InterOri>
 <RecienNacido>N</RecienNacido>
 <InterSala>1</InterSala>
 </Internacion>

 <DetalleProcedimientos>
 <NroItem>1</NroItem>
 <CodPrestacion>660001</CodPrestacion>
 <Nomenclador>18</Nomenclador>
 <TipoPrestacion>3</TipoPrestacion>
 <ArancelPrestacion>4</ArancelPrestacion>
 <CantidadSolicitada>01</CantidadSolicitada>
 </DetalleProcedimientos>


</Mensaje>
]]>

 </mensajeXml>

---

### [10/09/2026 15:45:58] Facundo Farias

Responde esto ese xml:
 <Rta>
 <DescripcionRtaGeneral>No se ejecutar la transaccion::prs_valida_of_auto_remotas(?,?,?)</DescripcionRtaGeneral>
 <MensajeDisplay>No se ejecutar la transaccion::prs_valida_of_auto_remotas(?,?,?)</MensajeDisplay>
 <MensajePrinter>Ocurrio un error en el sistema</MensajePrinter>
 </Rta>

---

### [10/09/2026 15:49:07] Agente

Facundo, el problema del XML es la ubicación de `<Internacion>`.

Lo estás enviando **fuera** de `<EncabezadoAtencion>`:

```xml
</EncabezadoAtencion>
<Internacion>
...
</Internacion>
```

En remotas, el parser busca `<Internacion>` **dentro de `EncabezadoAtencion`**. Por eso la transacción `6555352` quedó grabada así en `SMC_Prox_Ver`:

```text
ingre_icd     = I15
inter_fecha   = NULL
inter_hora    = NULL
inter_tipo    = NULL
origen_inter  = 0
inter_sala    = NULL
```

O sea: ignoró el bloque de internación completo y tomó como diagnóstico `I15` desde `<Diagnostico>`. Después al ejecutar:

```text
prs_valida_of_auto_remotas(?,?,?)
```

termina devolviendo el error general:

```xml
<Rta>
  <DescripcionRtaGeneral>No se ejecutar la transaccion::prs_valida_of_auto_remotas(?,?,?)</DescripcionRtaGeneral>
  <MensajeDisplay>No se ejecutar la transaccion::prs_valida_of_auto_remotas(?,?,?)</MensajeDisplay>
  <MensajePrinter>Ocurrio un error en el sistema</MensajePrinter>
</Rta>
```

Corregilo dejando `<Internacion>` antes del cierre de `<EncabezadoAtencion>`:

```xml
<Diagnostico>
  <CodDiagnostico>I15</CodDiagnostico>
</Diagnostico>

<Internacion>
  <InterFecha>20260909</InterFecha>
  <InterHora>101000</InterHora>
  <ICD>E66</ICD>
  <InterTipo>A</InterTipo>
  <InterOri>1</InterOri>
  <RecienNacido>N</RecienNacido>
  <InterSala>1</InterSala>
</Internacion>

</EncabezadoAtencion>
```

Validé que `E66` e `I15` existen activos en `icds`, y que el ítem `18 / 660001` se cargó autorizado con `aran_prestac = 4`. El error no viene por `ArancelPrestacion`; viene porque la internación no está entrando al parser por estar fuera del nodo esperado.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
