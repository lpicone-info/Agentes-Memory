# 2026-07-20

- Usuario en Google Chat directo: Andre.
- Rol operativo cargado: Mafalda / Agente08, analista tecnica de incidencias InvGate, PowerBuilder 2022 y MS SQL Server. Responder con evidencia, clasificacion y recomendaciones cuando se analicen tickets.
- Ticket InvGate 5959: se genero documento en `/mnt/Agentes/abiondi/invgate/analisis_ticket_5959.docx`. Analisis de solo lectura contra `medimas_prod` y repo `estandar-trunk`; incluyo 3 capturas del ticket. Hallazgo consultado luego: ventana `wp_lista_histo_pend.srw` ordena `dw_lista` por `fecha A` despues del `Retrieve`; el SP `afs_lista_histo_pend.sql` no tiene `ORDER BY`. Para historias con fecha de baja tambien aplica el mismo criterio: orden por `fecha` ascendente; `Fecha Baja` proviene de `SITU_HISTO_AUDITORIA.fecha_hora` para actividad `Baja Historia`, no ordena.
- Se registro profile de base `roisa_prod` en `/home/desarrollo-ia-08/.openclaw/workspace/DB_PROFILE.md`: motor MS SQL, host `172.16.10.115`, puerto `1433`, base `roisa_prod`, usuario `info1`, notas `roisa_prod`. Estado de conexion verificado: OK.
- Ticket InvGate 5965: se genero documento en `/mnt/Agentes/abiondi/invgate/analisis_ticket_5965.docx` usando `roisa_prod`. Limitacion relevante: la base ya reflejaba estado posterior al recupero indicado; practica `408014` figura activa y `408013` ya asociada al convenio `10962`. No se encontro auditoria de prestaciones para reconstruir exactamente la baja/recupero de `408014`; esa parte quedo respaldada por comentarios y capturas del ticket.
