# Memoria de conversación — Valeria Saravia

## Datos

- **Fecha:** 14/08/2026
- **Usuario:** Valeria Saravia
- **ID Google Chat:** `users/104838565899717376763`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 73
- **Inicio:** 08:08:23
- **Fin:** 17:44:25

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/jzuyrkaaaae`

## Archivos revisados

- `3ac7b8d5-98e2-4555-b396-9226d85cde93.jsonl`

---

## Transcripción completa

### [14/08/2026 08:08:23] Valeria Saravia

buenos días

---

### [14/08/2026 08:09:39] Agente

Hola Vale, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [14/08/2026 09:17:36] Valeria Saravia

volviendo a nuestra conversación de ayer, sobre el mensaje "No se encuentra activo"
podrás pasarme la consulta para verlo por base de datos? mi intención es poder demostrar q ese error persiste

---

### [14/08/2026 09:19:21] Agente

Vale, para `Fiat_Prod`, usá esta consulta. Demuestra los ítems donde el procedimiento sigue devolviendo `No se encuentra activo`.

```sql
DECLARE @lote DECIMAL(18,0)

SET @lote = 86

CREATE TABLE #errores
(
  item DECIMAL(18,0),
  deno VARCHAR(100)
)

INSERT INTO #errores
EXEC dbo.prs_export_discapacidad_lote_error @lote

SELECT
  lote = @lote,
  e.item,
  error = e.deno
FROM #errores e
WHERE e.deno = 'No se encuentra activo'
AND e.item IN (536, 711, 804)
ORDER BY e.item

DROP TABLE #errores
```

Para el mismo afiliado original del Redmine, también podés probar lote `84`:

```sql
DECLARE @lote DECIMAL(18,0)

SET @lote = 84

CREATE TABLE #errores
(
  item DECIMAL(18,0),
  deno VARCHAR(100)
)

INSERT INTO #errores
EXEC dbo.prs_export_discapacidad_lote_error @lote

SELECT
  lote = @lote,
  e.item,
  error = e.deno
FROM #errores e
WHERE e.deno = 'No se encuentra activo'
AND e.item IN (307, 378)
ORDER BY e.item

DROP TABLE #errores
```

El origen validado es el SP `prs_export_discapacidad_lote_error`: cuando no encuentra afiliado activo según la lógica actual, marca el error `No se encuentra activo`. Source: `memory/2026-08-13.md#L40`

---

### [14/08/2026 09:22:14] Valeria Saravia

realmente con esto queda demostrado que el problema persiste? como debería justificarlo? debo hacer un nuevo caso, de ser así, como debería hacer la prueba?

---

### [14/08/2026 09:22:36] Agente

Vale, **con esa consulta sola demostrás que el mensaje persiste**, pero para justificar el problema completo necesitás sumar una segunda evidencia: que el afiliado **sí estaba vigente históricamente para el período**, aunque el proceso lo marque como no activo.

**Clasificación:** Error.

**Evidencia**
El SP `prs_export_discapacidad_lote_error` sigue devolviendo `No se encuentra activo` para ítems actuales del lote, por ejemplo:

- Lote `86`, ítems `536`, `711`, `804`
- Lote `84`, ítems `307`, `378`

Para el afiliado `51415/02`, CUIL `27246701747`, el caso se justifica porque:

- Períodos: `02/2025` y `03/2025`
- Vigencia histórica: desde `19/04/2012` hasta `13/04/2025`
- Reingreso actual: `14/04/2025`

Entonces, para febrero y marzo 2025 el afiliado estaba vigente por historial, pero el proceso valida contra el ingreso actual y lo rechaza.

**Conclusión**
Sí, el problema persiste si hoy el SP sigue mostrando `No se encuentra activo` para esos ítems. Lo importante es explicarlo así: no alcanza con mostrar el mensaje, hay que mostrar que para ese período el afiliado tenía una vigencia válida.

**Qué haría**
No abriría un caso nuevo si el Redmine `28528` sigue abierto o sin resolver. Agregaría una nota en ese mismo caso con esta prueba. Solo haría un nuevo caso si internamente les piden registrar una reproducción nueva separada.

Podés justificarlo así:

```text
Se vuelve a validar el caso en Fiat_Prod. El procedimiento prs_export_discapacidad_lote_error continúa devolviendo el mensaje "No se encuentra activo" para el afiliado 51415/02, CUIL 27246701747, en períodos 02/2025 y 03/2025.

El afiliado posee una vigencia histórica desde 19/04/2012 hasta 13/04/2025, por lo que para esos períodos debería considerarse activo. Sin embargo, el proceso toma el ingreso actual 14/04/2025 y por eso lo rechaza incorrectamente.

Esto confirma que el error de validación de vigencia persiste.
```

**Prueba sugerida**
1. Ejecutar la consulta del SP para lote `86` y verificar que devuelve `No se encuentra activo` en ítems `536`, `711` u `804`.
2. Verificar en base la vigencia histórica del afiliado `51415/02`.
3. Adjuntar ambas evidencias al Redmine: resultado del SP + vigencia histórica.

---

### [14/08/2026 09:24:36] Valeria Saravia

ok, podrás armarme un documento con todo este tema que vimos y dejarlo en mi carpeta?

---

### [14/08/2026 09:24:53] Agente

Vale, puedo hacerlo. Para dejarlo correctamente en tu carpeta `/mnt/Agentes/vesaravia`, necesito que me indiques el **nombre del archivo y la extensión**.

Ejemplo: `prueba_error_no_se_encuentra_activo.docx` o `redmine_28528_validacion.docx`.

---

### [14/08/2026 09:25:25] Valeria Saravia

redmine_28528_validacion.docx

---

### [14/08/2026 09:27:05] Agente

Vale, ya dejé generado el documento en tu carpeta:

`/mnt/Agentes/vesaravia/redmine_28528_validacion.docx`

Validé que el archivo existe y que el `.docx` abre correctamente. Contiene el resumen del caso, evidencia, consultas SQL, justificación y texto sugerido para cargar en Redmine.

---

### [14/08/2026 10:31:16] Valeria Saravia

volviendo al tema, como hago para generar un nuevo caso de este tema?

---

### [14/08/2026 10:32:08] Agente

Vale, para generar un nuevo caso, lo haría como **incidencia por error**, referenciando el Redmine `28528` como antecedente.

**Título sugerido**
```text
SSS Integración - Valida incorrectamente afiliado histórico como "No se encuentra activo"
```

**Descripción sugerida**
```text
Al generar/revisar errores del archivo SSS de Integración, el sistema informa "No se encuentra activo" para un afiliado que se encontraba vigente históricamente en el período informado.

Caso observado en Fiat_Prod para el afiliado 51415/02, CUIL 27246701747, CEBALLOS, PATRICIA SILVINA.

El afiliado posee vigencia histórica desde 19/04/2012 hasta 13/04/2025. Para los períodos 02/2025 y 03/2025 debería considerarse activo. Sin embargo, el proceso toma el ingreso actual 14/04/2025 y lo rechaza como no activo.

Antecedente relacionado: Redmine 28528.
```

**Prueba para cargar en el caso**
1. Ingresar a:
```text
Presmed -> Liquidación -> Mecanismo de Integración -> Generación de Archivos para la SSS
```

2. Revisar alguno de estos casos:
```text
Lote 86 - Ítem 536 - Período 02/2025 - Expediente 2-39495
Lote 86 - Ítem 711 - Período 02/2025 - Expediente 2-39494
Lote 86 - Ítem 804 - Período 02/2025 - Expediente 2-39496
Lote 84 - Ítem 307 - Período 03/2025 - Expediente 2-37615
Lote 84 - Ítem 378 - Período 03/2025 - Expediente 2-37266
```

3. Ejecutar la consulta del documento para demostrar que el SP devuelve:
```text
No se encuentra activo
```

4. Adjuntar evidencia de la vigencia histórica:
```text
Ingreso histórico: 19/04/2012
Baja histórica: 13/04/2025
Reingreso actual: 14/04/2025
```

**Resultado actual**
```text
El proceso devuelve "No se encuentra activo".
```

**Resultado esperado**
```text
El afiliado debería considerarse activo para los períodos 02/2025 y 03/2025, porque estaba vigente según su historial de ingresos.
```

**Justificación técnica**
```text
El procedimiento prs_export_discapacidad_lote_error valida la vigencia tomando el ingreso actual del afiliado. En casos con baja histórica y reingreso posterior, no considera correctamente la vigencia histórica aplicable al período del archivo, por eso genera el rechazo "No se encuentra activo".
```

No usaría como prueba principal los expedientes `39661`, `39662`, `39663`, porque ya habíamos visto que pueden confundir al estar asociados actualmente a otros afiliados.

---

### [14/08/2026 10:34:46] Valeria Saravia

en la base de datos de fiat_prod, hay un caso más reciente?

---

### [14/08/2026 10:37:35] Agente

Vale, sí: **en `Fiat_Prod` hay casos más recientes**, pero hay que separar dos cosas.

**Más reciente por base / SP**
Encontré el lote `90`, con ítems `62`, `63` y `64`.

El SP `prs_export_discapacidad_lote_error` devuelve para esos ítems:

```text
No se encuentra activo
```

Datos:

```text
Lote 90 - Ítem 62 - Período 11/2025 - Expediente 1-42559 - CUIL 27447942564
Lote 90 - Ítem 63 - Período 11/2025 - Expediente 1-42758 - CUIL 27447942564
Lote 90 - Ítem 64 - Período 11/2025 - Expediente 1-42702 - CUIL 27447942564
```

Pero estos tres ítems tienen:

```text
OK = S
```

Entonces **sirven como evidencia técnica de que el SP sigue devolviendo el mensaje**, pero **no son los mejores para demostrarlo en pantalla como error pendiente**, porque operativamente figuran aceptados.

**Más reciente útil para prueba operativa**
Busqué casos con el mismo patrón pero con `OK <> S`. El más reciente que encontré sigue siendo el lote `82`:

```text
Lote 82 - Ítem 736 - OK = N - Rechazo 400 - Período 02/2025 - Expediente 2-36802
Lote 82 - Ítem 338 - OK = N - Rechazo 400 - Período 02/2025 - Expediente 2-36723
Lote 82 - Ítem 337 - OK = N - Rechazo 400 - Período 02/2025 - Expediente 2-36636
Lote 82 - Ítem 138 - OK = N - Rechazo 400 - Período 03/2025 - Expediente 2-37266
```

**Conclusión**
Para un nuevo caso, yo usaría esta explicación:

```text
Se detectan casos más recientes donde el procedimiento continúa devolviendo "No se encuentra activo", por ejemplo lote 90 ítems 62, 63 y 64. Sin embargo, esos registros figuran con OK = S, por lo que sirven como evidencia técnica pero no como reproducción operativa visible.

Como caso operativo pendiente, se mantiene evidencia en lote 82 con ítems OK = N, donde además el afiliado posee vigencia histórica válida para el período informado.
```

Para cargar un caso nuevo, usaría lote `82` como prueba funcional y mencionaría lote `90` como evidencia adicional de persistencia técnica del SP.

---

### [14/08/2026 10:42:59] Valeria Saravia

quien pone
OK = S?
el usuario final del sistema es q hace eso? xq quizás, estaba en "N" y lo cambiaron a "S"

---

### [14/08/2026 10:45:32] Agente

Vale, sí: **`OK = S` lo puede poner un usuario final del sistema**, pero no como edición manual directa del campo, sino mediante la opción **“Aceptados”** de la pantalla.

**Evidencia**
En la pantalla [wp_discapacidad_gen_arch_sss.srw](/home/desarrollo-ia-08/.openclaw/workspace/svn/estandar-trunk/exe/Source%20Infomedical/wp_discapacidad_gen_arch_sss.srw:525), el evento `ue_reg_aceptados` lee un archivo `TXT/DBF/DAT` y llama al procedimiento:

```text
pru_discapacidad_items_lote_recha
```

pasándole:

```text
@prm_ok = 'S'
@prm_recha = NULL
```

Después, el procedimiento [pru_discapacidad_items_lote_recha.sql](/home/desarrollo-ia-08/.openclaw/workspace/svn/estandar-trunk/backend/procedures/pru_discapacidad_items_lote_recha.sql:55) hace:

```sql
UPDATE isss_integracion_lotes_items
SET ok = @prm_ok,
    recha = CASE WHEN @prm_ok = 'S' THEN NULL ELSE @prm_recha END
```

Además, cuando pasa a `S`, da de baja los errores asociados en `ISSS_INTEGRACION_ITEMS_ERRORES`.

**Conclusión**
Sí, tu hipótesis es válida: **un ítem pudo haber estado en `N` y luego pasar a `S`** si alguien importó el archivo de aceptados desde la pantalla.

Lo que no puedo afirmar solo con el estado actual es que “antes estaba en N”, porque no vi una auditoría granular del cambio `OK N -> S` por ítem. La auditoría del lote registra acciones generales, usuario y fecha, pero el flujo de importación de aceptados no deja claramente una línea específica diciendo “este ítem cambió de N a S”.

Para explicarlo, yo diría:

```text
El hecho de que hoy el ítem figure con OK = S no descarta que previamente haya tenido error. El sistema permite actualizar el estado a OK = S mediante la importación del archivo de aceptados, proceso que ejecuta pru_discapacidad_items_lote_recha y limpia el rechazo del ítem. Por eso, los lotes más recientes con OK = S sirven como evidencia técnica de que el SP aún devuelve "No se encuentra activo", pero no como prueba operativa de rechazo pendiente.
```

---

### [14/08/2026 10:56:23] Valeria Saravia

podes analizar el tk 6103 en mutualrioIV_prod? el cliente dice q "se perdieron items", como pudo haber pasado eso?

---

### [14/08/2026 11:04:00] Agente

Vale, analicé `mutualrioiv_prod` en modo lectura. Con `tk 6103` encontré dos posibles referencias:

**1. Expediente `1-6103`**
Acá sí hay algo relacionado con “ítems perdidos”.

**Evidencia**
- `expedientes`: existe `ofi = 1`, `expe = 6103`
- Cabecera: `item_nro = 315`
- `expe_ordenes`: `315` órdenes, todas activas
- `expe_orden_items`: `364` registros en total
- Activos: `363`
- Dados de baja: `1`

El único ítem interno dado de baja es:

```text
Expediente 1-6103
Item 310
Orden 1
Prestación 340601 - MAMOGRAFIA (SENOGRAFIA)
Alta: 17/12/2024 16:08
Baja: 07/04/2025
```

En el mismo item `310` quedó activa otra prestación:

```text
Item 310
Orden 2
Prestación 340602 - MAMOGRAFIA, PROYECCION AXILAR
Alta: 07/04/2025
Baja: NULL
```

La auditoría del expediente muestra para el item `310`:

```text
07/04/2025 10:55:40
Actividad: M
Usuario: PALMAA
Item: 310
```

**Conclusión**
No veo que se hayan “perdido” ítems masivamente. Lo que sí veo es que **un ítem fue modificado**: la prestación `340601` quedó dada de baja y fue reemplazada por `340602`.

Eso puede verse como “se perdió” si la pantalla solo muestra registros vigentes, porque el registro con `baja_fecha` ya no aparece en la vista normal.

**2. Autorización `40-6103`**
También existe una autorización `sucur = 40`, `auto = 6103`, pero ahí no veo pérdida:

```text
Tiene 4 ítems activos
Ítems 1, 2, 3 y 4
Todos con baja_fecha = NULL
Observación: Ingresado vía remota
```

**Diagnóstico**
Clasificación: **Configuración / operación sobre datos**, no veo evidencia de error del sistema.

La explicación más probable es:

```text
El usuario PALMAA modificó el item 310 del expediente 1-6103 el 07/04/2025. Como parte de esa modificación, la prestación original 340601 quedó dada de baja y se cargó/actualizó la prestación 340602. El ítem no fue eliminado físicamente; quedó no vigente por baja_fecha.
```

Para responderle al cliente, usaría:

```text
No se observa pérdida masiva de ítems. El expediente conserva 315 órdenes activas. Se detecta un único detalle dado de baja en el item 310: la prestación 340601 quedó con baja_fecha el 07/04/2025 y fue reemplazada por la prestación 340602, quedando auditado como modificación realizada por el usuario PALMAA.
```

---

### [14/08/2026 11:13:40] Valeria Saravia

ok, si desde el sistema no se puede ver, podremos verlo por consulta a la base de datos?

---

### [14/08/2026 11:13:58] Agente

Sí, Vale. Por base lo podés ver porque el registro **no desapareció**, quedó con `baja_fecha`.

En `mutualrioiv_prod`, ejecutá esta consulta:

```sql
SELECT
  eoi.ofi,
  eoi.expe,
  eoi.item,
  eoi.orden,
  eoi.nomen,
  eoi.prestac,
  prestacion = p.exten_deno,
  eoi.canti,
  eoi.real_valor,
  eoi.ok,
  eoi.alta_fecha,
  eoi.baja_fecha
FROM expe_orden_items eoi
LEFT JOIN prestaciones p
ON p.nomen = eoi.nomen
AND p.prestac = eoi.prestac
WHERE eoi.ofi = 1
AND eoi.expe = 6103
AND eoi.item = 310
ORDER BY eoi.orden
```

Ahí deberías ver las dos prestaciones del item `310`:

```text
Orden 1 - 340601 - MAMOGRAFIA (SENOGRAFIA) - baja_fecha = 07/04/2025
Orden 2 - 340602 - MAMOGRAFIA, PROYECCION AXILAR - baja_fecha = NULL
```

Y para ver la auditoría del cambio:

```sql
SELECT
  fecha_hora,
  activi,
  usuario,
  item,
  ambu_inter
FROM expe_auditoria
WHERE ofi = 1
AND expe = 6103
AND item = 310
ORDER BY fecha_hora
```

Esa consulta muestra la modificación:

```text
07/04/2025 10:55:40 - Actividad M - Usuario PALMAA - Item 310
```

---

### [14/08/2026 11:27:18] Valeria Saravia

fijate la pestaña items, del 19 , 20 pasa al 23, 24, 25 ¿qué pasó con los items 21 y 22?

---

### [14/08/2026 11:34:51] Agente

Vale, los ítems `21` y `22` **no se perdieron de la base**. Están en `expe_internaciones`, activos, sin `baja_fecha`.

Lo que pasa es que la pestaña `Items` ejecuta el SP `prs_expe_internaciones`, y ese SP **no los devuelve**. Por eso en pantalla ves `19`, `20`, `23`, `24`, `25`.

**Evidencia principal**

```text
Expediente 1-29753

Item 21:
- Afiliado 407141/00 - GIACONE, HUGO
- Sucursal/autorización: 40-753858
- Total: 522.687,90
- baja_fecha: NULL
- medi_grupo: 1
- lugar: NULL
- errores activos: 81 y 82

Item 22:
- Afiliado 407141/00 - GIACONE, HUGO
- Sucursal/autorización: 40-753858
- Total: 522.687,90
- baja_fecha: NULL
- medi_grupo: 1
- lugar: NULL
- errores activos: 81 y 82

Item 23:
- Afiliado 407141/00 - GIACONE, HUGO
- Sin autorización
- Total: 522.687,90
- baja_fecha: NULL
- medi_grupo: 0
- lugar: 1
- aparece en pantalla
```

Los errores activos de `21` y `22` son:

```text
81 - Autorizacion no es de internacion
82 - Prestador de autorizacion difiere del prestador del expediente
```

**Qué pudo haber pasado**

La hipótesis más consistente es esta: primero se cargaron los ítems `21` y `22` usando la autorización `40-753858`, pero esa autorización no quedó válida para el circuito de internación del expediente. Esos registros quedaron activos en base, pero con marca `medi_grupo = 1` y errores asociados.

Después se cargó el mismo caso nuevamente como ítem `23`, ya sin autorización, con `medi_grupo = 0` y `lugar = 1`. Ese sí entra en la consulta que muestra la pantalla.

Entonces, para el cliente yo lo explicaría así:

```text
No se observa pérdida física de los ítems 21 y 22. Ambos registros existen en la base de datos y no tienen baja_fecha. Sin embargo, no se visualizan en la pestaña Items porque el procedimiento que alimenta la grilla no los devuelve.

Los ítems 21 y 22 quedaron asociados a la autorización 40-753858 y poseen errores activos: "Autorización no es de internación" y "Prestador de autorización difiere del prestador del expediente". Luego aparece el ítem 23 para el mismo afiliado e importe, sin autorización, que sí se visualiza en pantalla.
```

**Consulta para verlo por base**

```sql
SELECT
  ei.item,
  ei.sucur,
  ei.auto,
  ei.contra,
  ei.inte,
  ei.fecha,
  ei.total,
  ei.baja_fecha,
  ei.medi_grupo,
  ei.lugar,
  ei.efe_prestad,
  ei.efe_nombre
FROM expe_internaciones ei
WHERE ei.ofi = 1
AND ei.expe = 29753
AND ei.item BETWEEN 19 AND 25
ORDER BY ei.item
```

Y para los errores:

```sql
SELECT
  e.item,
  e.recha,
  motivo = r.deno,
  e.baja_fecha
FROM expe_inter_errores e
JOIN recha_motivos r
ON r.recha = e.recha
WHERE e.ofi = 1
AND e.expe = 29753
AND e.item IN (21, 22, 23)
ORDER BY e.item, e.recha
```

---

### [14/08/2026 11:39:01] Valeria Saravia

Los ítems 21 y 22 quedaron asociados a la autorización 40-753858 -> como veo esto en el sistema?

---

### [14/08/2026 11:39:18] Agente

Vale, en el sistema lo podés intentar ver desde la **autorización**, no desde la pestaña `Items` del expediente, porque justamente ahí los ítems `21` y `22` no aparecen.

Probá así:

1. Entrá a la pantalla de **Autorizaciones**.
2. Buscá la autorización:

```text
Sucursal: 40
Autorización: 753858
```

3. Revisá los datos de la autorización:
```text
Afiliado: 407141/00 - GIACONE, HUGO
```

4. Si la pantalla muestra prestaciones/detalle asociado, verificá si corresponde al mismo importe o prestación de los ítems `21` y `22`.

La explicación sería:

```text
Los ítems 21 y 22 no se ven en la pestaña Items del expediente, pero en base están asociados a la autorización 40-753858. Para validarlo desde el sistema, se debe consultar directamente la autorización 40-753858 y comparar afiliado/datos con el expediente.
```

Si desde la pantalla de autorización tampoco se ve el vínculo con el expediente, entonces la prueba más fuerte queda por base, con esta consulta:

```sql
SELECT
  item,
  sucur,
  auto,
  contra,
  inte,
  fecha,
  total,
  baja_fecha,
  medi_grupo,
  lugar
FROM expe_internaciones
WHERE ofi = 1
AND expe = 29753
AND item IN (21, 22)
ORDER BY item
```

Ahí el campo `sucur = 40` y `auto = 753858` demuestra que quedaron asociados a esa autorización.

---

### [14/08/2026 11:50:16] Valeria Saravia

pero esto sería un error? como lo puede solucionar el usuario final?

---

### [14/08/2026 11:50:37] Agente

Vale, con lo visto hasta ahora yo lo clasificaría así:

**Clasificación:** Configuración / operación de carga, con posible problema de visualización.

No parece que “se hayan perdido” los ítems, porque `21` y `22` existen en base y están activos. El problema es que quedaron cargados con una autorización que el sistema marca como inválida para ese expediente:

```text
Autorización 40-753858
Errores:
81 - Autorización no es de internación
82 - Prestador de autorización difiere del prestador del expediente
```

Después aparece el ítem `23`, para el mismo afiliado e importe, pero sin esa autorización, y ese sí se ve en la pestaña.

**Cómo lo podría solucionar el usuario final**

Si el sistema le permite ver/corregir esos ítems desde alguna opción de auditoría, errores o carga de internaciones, debería:

1. Revisar la autorización `40-753858`.
2. Confirmar si realmente corresponde a internación.
3. Confirmar si el prestador de la autorización coincide con el prestador del expediente.
4. Si la autorización no corresponde, dejar sin efecto esos ítems o corregirlos con la autorización correcta.
5. Usar como válido el ítem cargado correctamente, que parecería ser el `23`.

Pero hay un punto importante: si desde la pantalla normal no puede ver los ítems `21` y `22`, entonces **el usuario final no puede resolverlo completamente desde esa grilla**, porque no tiene sobre qué operar. En ese caso lo resolvería soporte con una revisión funcional/técnica, o habría que entrar por una pantalla que permita ver ítems con errores/no listados.

Yo lo justificaría así:

```text
No se detecta pérdida de datos. Los ítems 21 y 22 existen en base y no están dados de baja, pero no se visualizan en la pestaña Items porque quedaron asociados a una autorización con errores activos. La corrección funcional debería consistir en revisar la autorización 40-753858 y corregir/anular la carga incorrecta. Si el sistema no permite acceder a esos ítems desde pantalla, se requiere intervención de soporte.
```

---

### [14/08/2026 11:54:33] Valeria Saravia

esta es la autorización que me indicas, se la puede ver desde el sistema, veo q está cargada como prestación y x la categoría, supongo q si se corresponde al expediente

---

### [14/08/2026 11:54:58] Agente

Sí, Vale, la autorización **se ve desde el sistema** y está **aceptada**, pero eso no invalida lo que vimos: puede estar bien cargada como autorización de prestaciones y aun así **no ser válida para el circuito de internación del expediente**.

En la imagen se ve:

```text
Autorización: 40-753858
Afiliado: 407141/00 - GIACONE, HUGO
Estado: Aceptado
Prestador efector: 6589 - LOMBARDI, JOSÉ
Prestaciones: colonofibroscopia, esofagogastroscopia, sala de recuperación
```

El punto clave es este: el error `81 - Autorización no es de internación` sugiere que para el sistema esa autorización está cargada como **prestación ambulatoria / prestación común**, no como autorización de internación. Que tenga categoría o lugar asociado no necesariamente la convierte en autorización válida para internación.

Y el error `82 - Prestador de autorización difiere del prestador del expediente` indica que, al compararla contra el expediente, el sistema detecta que el prestador/efector esperado no coincide con el que quedó en la autorización o en el ítem del expediente.

Para demostrarlo por base, usaría esta comparación:

```sql
SELECT
  ei.ofi,
  ei.expe,
  ei.item,
  ei.sucur,
  ei.auto,
  ei.efe_prestad AS prestador_item,
  ei.efe_nombre AS nombre_prestador_item,
  ei.lugar,
  ei.medi_grupo,
  ei.baja_fecha
FROM expe_internaciones ei
WHERE ei.ofi = 1
AND ei.expe = 29753
AND ei.item IN (21, 22, 23)
ORDER BY ei.item
```

Y después:

```sql
SELECT
  e.item,
  e.recha,
  motivo = r.deno,
  e.baja_fecha
FROM expe_inter_errores e
JOIN recha_motivos r
  ON r.recha = e.recha
WHERE e.ofi = 1
AND e.expe = 29753
AND e.item IN (21, 22)
ORDER BY e.item, e.recha
```

Conclusión para el cliente:

```text
La autorización 40-753858 existe y está aceptada en el sistema, pero los ítems 21 y 22 no se visualizan en la pestaña Items porque quedaron con errores activos de validación contra el expediente: la autorización no es considerada de internación y el prestador de la autorización difiere del prestador del expediente. No hay pérdida física de datos; los ítems existen en base, pero el sistema no los muestra en la grilla por esa condición.
```

O sea: tu observación es correcta, la autorización existe. Pero el problema no parece ser que “no exista”, sino que **no pasa las validaciones necesarias para quedar visible/usable dentro del expediente de internación**.

---

### [14/08/2026 11:58:02] Valeria Saravia

seguro el cliente se queja de que debería poder verla en el expediente, como justifico q no le van a aparecer esos items en el expediente? podría hacer yo un caso de prueba donde ponga esos items a un expediente de internación y luego verificar que el expediente no los muestra?

---

### [14/08/2026 12:01:19] Agente

Sí, Vale. La justificación más firme es esta:

**No aparecen en el expediente porque no son considerados ítems válidos de internación para la grilla.** En base existen y están activos, pero quedaron con datos distintos a los ítems visibles:

```text
Item 21 - sucur 40 - auto 753858 - medi_grupo 1 - lugar NULL - efe_prestad NULL
Item 22 - sucur 40 - auto 753858 - medi_grupo 1 - lugar NULL - efe_prestad 65
Item 23 - sin autorización - medi_grupo 0 - lugar 1 - efe_prestad 65
```

Además, la autorización `40-753858` está registrada como:

```text
tipo = P
ambu_inter = A
inter_fecha = NULL
```

Mientras que una autorización de internación visible, como la del item `34`, aparece como:

```text
tipo = I
ambu_inter = I
inter_fecha informada
```

Entonces yo lo justificaría así:

```text
Los ítems 21 y 22 no se perdieron: existen en expe_internaciones y no tienen baja_fecha. Sin embargo, no se visualizan en la pestaña Items del expediente porque quedaron asociados a la autorización 40-753858, que está registrada como autorización de prestación/ambulatoria y no como autorización de internación. Además, los ítems quedaron con medi_grupo = 1 y sin lugar de internación, condición distinta a los ítems que sí muestra la grilla del expediente.
```

Sobre hacer un caso de prueba: **sí, pero no lo haría en producción ni modificando datos por SQL**. Lo haría en una base de prueba/homologación, desde el sistema:

1. Crear o tomar un expediente de internación de prueba.
2. Asociarle una autorización de tipo prestación/ambulatoria similar a `40-753858`.
3. Verificar si el sistema permite grabar los ítems.
4. Reabrir el expediente y revisar la pestaña `Items`.
5. Comparar por base `expe_internaciones` contra `EXEC prs_expe_internaciones`.

Si el sistema permite cargar esos ítems pero después no los muestra en el expediente, ahí sí lo plantearía como **Error de circuito/visualización**, porque deja registros activos inaccesibles para el usuario final.

---

### [14/08/2026 12:23:28] Valeria Saravia

ok, quisiera hacer la consulta a la base de datos para demostrar al cliente q los items si están en el expediente pero q el sistema no los muestra xq son prácticas ambulatorias, como sería esa consulta?

---

### [14/08/2026 12:24:10] Agente

Vale, ejecutá esto en `mutualrioiv_prod`. La consulta compara:

- lo que existe en `expe_internaciones`
- lo que devuelve el SP que usa la pestaña `Items`
- el tipo de autorización asociada

```sql
IF OBJECT_ID('tempdb..#pantalla') IS NOT NULL
  DROP TABLE #pantalla

CREATE TABLE #pantalla
(
  item INT,
  sucur SMALLINT NULL,
  auto INT NULL,
  inter_fecha SMALLDATETIME NULL,
  contra CHAR(15),
  inte CHAR(2),
  creden CHAR(20) NULL,
  afi CHAR(45),
  plan_codi CHAR(10) NULL,
  obser VARCHAR(255) NULL,
  fecha SMALLDATETIME,
  dias SMALLINT NULL,
  total DECIMAL(18,2),
  fin CHAR(1) NULL,
  factu_total CHAR(1),
  audi_vis CHAR(1) NULL,
  revisar CHAR(1) NULL,
  ok CHAR(1) NULL,
  lugar INT NULL,
  erro CHAR(1) NULL,
  efe_prestad INT NULL,
  efe_cate INT NULL,
  efe_nombre CHAR(50) NULL,
  es_grava CHAR(1) NULL,
  histo INT NULL,
  bot_visible CHAR(1),
  prepaga SMALLINT NULL,
  nro_liqui_externo INT NULL,
  nro_nodo INT NULL
)

INSERT INTO #pantalla
EXEC dbo.prs_expe_internaciones @ofi = 1, @expe = 29753

SELECT
  ei.ofi,
  ei.expe,
  ei.item,
  ei.sucur,
  ei.auto,
  ei.contra,
  ei.inte,
  ei.fecha,
  ei.total,
  ei.baja_fecha,
  ei.medi_grupo,
  ei.lugar,
  ei.efe_prestad,
  autorizacion_tipo = a.tipo,
  autorizacion_ambu_inter = a.ambu_inter,
  autorizacion_inter_fecha = a.inter_fecha,
  estado_en_pantalla =
    CASE
      WHEN p.item IS NULL THEN 'NO APARECE EN PESTAÑA ITEMS'
      ELSE 'APARECE EN PESTAÑA ITEMS'
    END,
  motivo_probable =
    CASE
      WHEN p.item IS NULL
       AND a.tipo = 'P'
       AND a.ambu_inter = 'A'
        THEN 'Ítem asociado a autorización de práctica ambulatoria'
      WHEN p.item IS NULL
        THEN 'Ítem existe en base pero no es devuelto por el SP de pantalla'
      ELSE 'Ítem devuelto por el SP de pantalla'
    END
FROM expe_internaciones ei
LEFT JOIN autorizaciones a
  ON a.sucur = ei.sucur
 AND a.auto = ei.auto
LEFT JOIN #pantalla p
  ON p.item = ei.item
WHERE ei.ofi = 1
AND ei.expe = 29753
AND ei.item BETWEEN 19 AND 25
ORDER BY ei.item

DROP TABLE #pantalla
```

Con eso deberías poder mostrar que los ítems `21` y `22` **sí existen en el expediente**, no tienen `baja_fecha`, pero **no aparecen en la pestaña Items**. La clave para justificarlo es que están asociados a la autorización `40-753858`, que figura como `tipo = P` y `ambu_inter = A`, es decir práctica ambulatoria, no internación.

---

### [14/08/2026 12:35:01] Valeria Saravia

entonces xq si aparecen los items 23 y 24 si en el tipo de autorización tienen NULL y en ambulatorio/internación tienen NULL?

---

### [14/08/2026 12:35:20] Agente

Porque `NULL` en la autorización **no significa “ambulatorio”**. En los ítems `23` y `24`, esos campos están en `NULL` porque **no tienen autorización asociada**.

La diferencia real es esta:

```text
Item 21
- Tiene autorización: 40-753858
- Autorización tipo = P
- ambu_inter = A
- medi_grupo = 1
- lugar = NULL
- No aparece

Item 22
- Tiene autorización: 40-753858
- Autorización tipo = P
- ambu_inter = A
- medi_grupo = 1
- lugar = NULL
- No aparece

Item 23
- No tiene autorización asociada
- tipo = NULL
- ambu_inter = NULL
- medi_grupo = 0
- lugar = 1
- Sí aparece

Item 24
- No tiene autorización asociada
- tipo = NULL
- ambu_inter = NULL
- medi_grupo = 0
- lugar = 1
- Sí aparece
```

Entonces la explicación correcta no sería “no aparecen solo porque son prácticas ambulatorias”, sino:

```text
Los ítems 21 y 22 existen en el expediente, pero quedaron asociados a una autorización de práctica ambulatoria, no de internación. Además, quedaron con medi_grupo = 1 y sin lugar de internación. En cambio, los ítems 23 y 24 no tienen autorización asociada, pero sí quedaron cargados como ítems de internación del expediente, con medi_grupo = 0 y lugar = 1, por eso el sistema los muestra.
```

Para el cliente, yo lo diría así:

```text
No se trata de que todo ítem sin autorización no aparezca. Los ítems 23 y 24 aparecen porque están cargados como ítems propios del expediente de internación. Los ítems 21 y 22 no aparecen porque quedaron vinculados a la autorización 40-753858, la cual corresponde a una autorización ambulatoria/práctica, y no quedaron parametrizados como ítems visibles de internación.
```

---

### [14/08/2026 12:47:37] Valeria Saravia

voy a responder lo siguiente, decime si queda claro o agrego algo más:

Buenas tardes, Ricardo,
Se analiza el caso y lo que se observa es que los items 21 y 22 si existen en la base de datos (ver imagen) pero el sistema no los muestra ya que están asociados a una prestación de una autorización ambulatoria (acá les voy a adjuntar la captura de pantalla de la autorización) y el expediente es de internación.

---

### [14/08/2026 12:47:51] Agente

Sí, Vale, queda claro. Yo solo ajustaría un poco la redacción para que no parezca que el sistema “decide ocultarlos” arbitrariamente, sino que **no los devuelve en la pestaña por cómo quedaron asociados**.

Podrías responder así:

```text
Buenas tardes, Ricardo.

Se analizó el caso y se observa que los ítems 21 y 22 no se perdieron: existen en la base de datos y no poseen baja_fecha.

Lo que ocurre es que esos ítems no se visualizan en la pestaña Items del expediente porque quedaron asociados a la autorización 40-753858, que corresponde a una autorización de prestación ambulatoria. El expediente, en cambio, es de internación.

En la consulta adjunta se puede ver que los ítems 21 y 22 existen para el expediente 1-29753, pero figuran como “NO APARECE EN PESTAÑA ITEMS”. También se observa que la autorización asociada tiene tipo = P y ambu_inter = A.

Por este motivo, no se trata de una pérdida física de ítems, sino de ítems existentes en base que no son mostrados por la grilla del expediente de internación por la condición en la que quedaron cargados.
```

Agregaría las dos capturas: la consulta de base y la pantalla de la autorización. Con eso queda bastante defendible.

---

### [14/08/2026 13:04:52] Valeria Saravia

pero es error del sistema o error de usuario?

---

### [14/08/2026 13:05:28] Agente

Vale, con la evidencia actual yo lo clasificaría como:

**Posible error de sistema, originado por una carga/flujo de usuario permitido por el sistema.**

No diría directamente “error de usuario”, porque si el sistema permitió que esos ítems queden asociados al expediente y después no los muestra en la pestaña `Items`, hay un problema funcional: quedan registros activos en base que el usuario no puede ver ni gestionar desde la pantalla.

La forma más prudente de justificarlo sería:

```text
No se observa pérdida física de ítems. Los ítems 21 y 22 existen en base y no tienen baja_fecha.

Sin embargo, quedaron asociados a una autorización ambulatoria/prestacional dentro de un expediente de internación, y por esa condición no son devueltos por la grilla de Items del expediente.

Si esa carga fue permitida por el sistema y luego los ítems quedan inaccesibles para el usuario final desde la pantalla del expediente, corresponde analizarlo como un posible error funcional del circuito o de visualización, no simplemente como error de usuario.
```

Para el cliente, yo evitaría culpar al usuario. Diría algo como:

```text
La situación no corresponde a una pérdida de datos. Los ítems existen en base, pero no se visualizan en la pestaña Items por la forma en que quedaron cargados/asociados al expediente.

Se requiere revisar el circuito porque, si el sistema permite generar esos ítems en un expediente de internación y luego no los muestra para su gestión, podría tratarse de una inconsistencia funcional.
```

Mi recomendación: abrirlo como **incidencia para análisis técnico-funcional**, no como “mal uso del usuario”, hasta reproducir si el sistema permite generar nuevamente esa condición desde pantalla.

---

### [14/08/2026 13:29:06] Valeria Saravia

como se puede solucionar, deben rehacer el expediente?

---

### [14/08/2026 13:29:34] Agente

Vale, **no necesariamente deberían rehacer todo el expediente**.

La solución funcional más razonable sería:

1. **No tocar por SQL en producción** como primera opción.
2. Revisar si desde el sistema pueden **anular/corregir los ítems 21 y 22** o dejarlos sin efecto.
3. Cargar nuevamente esos conceptos como ítems válidos del expediente de internación, con el circuito correcto.
4. Si corresponde usar autorización, debería ser una **autorización de internación**, no una autorización ambulatoria/prestacional.
5. Si no corresponde autorización, cargarlos como los ítems 23 y 24: propios del expediente, con `lugar` informado y visibles en la pestaña.

Yo lo explicaría así al cliente:

```text
No sería necesario rehacer el expediente completo si el sistema permite corregir/anular los ítems afectados y volver a cargarlos correctamente dentro del circuito de internación.

El problema puntual está en los ítems 21 y 22, que existen en base pero quedaron asociados a una autorización ambulatoria/prestacional, por eso no se visualizan en la pestaña Items del expediente de internación.

La corrección debería consistir en dejar sin efecto esos ítems y volver a cargarlos por el circuito correcto de internación. Solo si el sistema no permite corregirlos desde pantalla, debería intervenir soporte para evaluar la alternativa operativa más segura.
```

Si el cliente pregunta “¿los rehacemos?”, yo respondería: **rehacer los ítems sí; rehacer todo el expediente, solo si no tienen otra opción operativa desde el sistema.**

---

### [14/08/2026 13:33:11] Valeria Saravia

"creo" q el cliente se refiere a como cerrar esta imagen, la respuesta debe estar en el manual de liquidaciones (pagina 44 si mal no recuerdo)

---

### [14/08/2026 13:34:03] Valeria Saravia

creo" q el cliente se refiere a como poder liquidar cuando le aparece esta imagen, la respuesta debe estar en el manual de liquidaciones (pagina 44 si mal no recuerdo)

---

### [14/08/2026 13:36:20] Agente

Vale, sí: la imagen apunta a **diferencias entre el total cargado en la cabecera del ítem y el subtotal del detalle**.

En este caso:

```text
Item 21 - Total 522.687,90 - Subtotal 0 - Diferencia 522.687,90
Item 22 - Total 522.687,90 - Subtotal 0 - Diferencia 522.687,90
```

O sea, el sistema detecta que esos ítems tienen importe total informado, pero **no tienen detalle cargado que respalde ese total**. Según el manual de liquidación de expedientes de sanatorios/internación, antes de cerrar la carga se debe ingresar el detalle de cada internación y presionar **Aceptar** para grabar los cambios. Luego recién se debe usar **Cerrar carga**. El manual aclara que este mensaje aparece cuando “las sumas entre lo ingresado y lo cargado no coinciden entre lo declarado en la cabecera y los totales del detalle”.

Respuesta sugerida para el cliente:

```text
Buenas tardes.

El mensaje no indica pérdida de ítems, sino una diferencia entre el total declarado en la cabecera de la internación y el subtotal cargado en el detalle.

Para los ítems 21 y 22 el sistema informa Total = 522.687,90 y Subtotal = 0, por lo que queda una diferencia por el mismo importe. Para poder cerrar/liquidar el expediente, se debe ingresar al detalle de esos ítems, cargar las prestaciones/valores correspondientes y grabar con Aceptar. Luego, cuando el subtotal coincida con el total informado, se podrá realizar el cierre de carga.

Tal como indica el manual de liquidaciones de internación, en este circuito los valores facturados deben cargarse en el detalle para que el total registrado coincida con lo informado en el comprobante.
```

Agregaría una salvedad: si desde pantalla no pueden acceder a los ítems `21` y `22` para completar el detalle, entonces ya no es solo procedimiento de usuario y debería revisarlo soporte, porque quedarían ítems con diferencia pero sin posibilidad operativa de corrección desde la grilla.

---

### [14/08/2026 13:44:07] Valeria Saravia

y es q como hacen para agregar ese importe si los items 21 y 22 no se ven...

---

### [14/08/2026 13:44:35] Agente

Exacto, Vale. Ese es el punto fuerte del caso.

Si los ítems `21` y `22` **no aparecen en la pestaña Items**, entonces el usuario no tiene forma operativa de entrar a esos ítems para cargarles el detalle y corregir la diferencia de importe. Por eso no sería solo “deben completar el subtotal”, porque la pantalla no les ofrece los ítems para hacerlo.

Yo respondería así:

```text
Buenas tardes, Ricardo.

Se analizó el caso y se observa que los ítems 21 y 22 no se perdieron: existen en la base de datos y no poseen baja_fecha.

El inconveniente es que esos ítems no se visualizan en la pestaña Items del expediente, por lo que el usuario no puede acceder desde pantalla para cargar o corregir el detalle correspondiente.

El mensaje de diferencia entre Total y Subtotal se produce porque los ítems tienen importe informado en la cabecera, pero no poseen detalle cargado. Sin embargo, al no visualizarse en la grilla, el usuario final no tiene una acción disponible desde el sistema para completar ese detalle.

Por este motivo, corresponde revisar el caso como una inconsistencia funcional: existen ítems activos asociados al expediente, pero no son devueltos por la pantalla para su gestión.
```

Y agregaría al cierre:

```text
La corrección no debería ser rehacer todo el expediente como primera opción, sino revisar cómo regularizar esos ítems para que puedan ser gestionados o dejados sin efecto desde el circuito correcto.
```

Con eso queda claro que el problema no es simplemente que falte cargar el importe, sino que **no pueden cargarlo porque los ítems quedaron inaccesibles desde la pantalla**.

---

### [14/08/2026 13:48:43] Valeria Saravia

te parece bien si me indicas cual es la consulta para conocer la cabecera de los items 20 y 21 y además mostrar q figuran sin detalle para derivar el incidente como tarea de corrección al sector de análisis y desarrollo xq veo q el usuario final no lo va a poder corregir desde la interfaz del sistema, estás de acuerdo? o queda otra cosa por ver de mi parte desde el lado funcional para poder ayudar al usuario?

---

### [14/08/2026 13:49:27] Agente

Sí, Vale, estoy de acuerdo: si los ítems existen en cabecera, tienen importe, no tienen detalle y además no se ven en la interfaz, el usuario final no tiene forma funcional de corregirlo. Yo lo derivaría a Análisis/Desarrollo como inconsistencia de circuito/visualización.

Ojo: por lo que veníamos viendo, los conflictivos eran `21` y `22`. Si querés mostrar el salto desde pantalla, incluiría `20, 21, 22, 23`.

Consulta para cabecera + existencia de detalle:

```sql
SELECT
  ei.ofi,
  ei.expe,
  ei.item,
  ei.sucur,
  ei.auto,
  ei.contra,
  ei.inte,
  ei.fecha,
  ei.total AS total_cabecera,
  ei.baja_fecha,
  ei.medi_grupo,
  ei.lugar,
  ei.efe_prestad,
  detalle_prestaciones = (
    SELECT COUNT(*)
    FROM expe_inter_prestaciones eip
    WHERE eip.ofi = ei.ofi
      AND eip.expe = ei.expe
      AND eip.item = ei.item
      AND eip.baja_fecha IS NULL
  ),
  detalle_totales = (
    SELECT COUNT(*)
    FROM expe_inter_totales eit
    WHERE eit.ofi = ei.ofi
      AND eit.expe = ei.expe
      AND eit.item = ei.item
      AND eit.baja_fecha IS NULL
  ),
  detalle_dias = (
    SELECT COUNT(*)
    FROM expe_inter_dias_totales edt
    WHERE edt.ofi = ei.ofi
      AND edt.expe = ei.expe
      AND edt.item = ei.item
      AND edt.baja_fecha IS NULL
  ),
  situacion =
    CASE
      WHEN ei.total > 0
       AND NOT EXISTS (
          SELECT 1
          FROM expe_inter_prestaciones eip
          WHERE eip.ofi = ei.ofi
            AND eip.expe = ei.expe
            AND eip.item = ei.item
            AND eip.baja_fecha IS NULL
       )
       AND NOT EXISTS (
          SELECT 1
          FROM expe_inter_totales eit
          WHERE eit.ofi = ei.ofi
            AND eit.expe = ei.expe
            AND eit.item = ei.item
            AND eit.baja_fecha IS NULL
       )
       AND NOT EXISTS (
          SELECT 1
          FROM expe_inter_dias_totales edt
          WHERE edt.ofi = ei.ofi
            AND edt.expe = ei.expe
            AND edt.item = ei.item
            AND edt.baja_fecha IS NULL
       )
        THEN 'TIENE CABECERA CON IMPORTE, PERO NO TIENE DETALLE ACTIVO'
      ELSE 'TIENE DETALLE O NO PRESENTA DIFERENCIA'
    END
FROM expe_internaciones ei
WHERE ei.ofi = 1
  AND ei.expe = 29753
  AND ei.item IN (20, 21, 22, 23)
ORDER BY ei.item
```

Para derivarlo, yo diría:

```text
Se verifica por base que los ítems 21 y 22 existen en la cabecera del expediente, no poseen baja_fecha y tienen total informado. Sin embargo, no poseen detalle activo en las tablas de detalle de internación.

Dado que estos ítems no se visualizan en la pestaña Items del expediente, el usuario final no puede acceder desde la interfaz para cargar o corregir el detalle requerido para cerrar/liquidar.

Se deriva a Análisis y Desarrollo para revisar la inconsistencia, ya que existen registros activos con importe en cabecera, sin detalle y sin posibilidad operativa de corrección desde pantalla.
```

Desde lo funcional, solo validaría una cosa más antes de derivar: capturar la pantalla donde se ve que la grilla pasa de `20` a `23`, y la pantalla/mensaje donde el cierre reclama diferencia de importe. Con eso más la consulta, queda bien sustentado.

---

### [14/08/2026 13:53:11] Valeria Saravia

pero yo creo q en este caso, la verdadera corrección del sistema, no es q aparezca el detalle ára poder corregir, xq se trata de prestaciones de autorizaciones ambulatorias, y estamos tratando un expediente de internación. La solución sería q el sistema no permita q lo ambulatorio pase a un expediente de internación. Dicho esto, al cliente n ole va a quedar otra q volver a dar de alta el expediente. Decime q opinas, no me des la razón

---

### [14/08/2026 13:53:35] Agente

Sí, tu lectura me parece más correcta, con una precisión.

La corrección de fondo **no debería ser “mostrar los ítems 21 y 22 para que el usuario los corrija”**, porque eso podría legitimar una carga que funcionalmente no corresponde: prestaciones de una autorización ambulatoria dentro de un expediente de internación.

La corrección de sistema más sana sería:

```text
El sistema no debería permitir asociar o generar ítems de internación a partir de una autorización ambulatoria/prestacional, o debería bloquearlo con un mensaje claro antes de grabar.
```

Lo que sí agregaría es que, para este caso ya generado, hay dos planos:

```text
1. Corrección preventiva del sistema:
   impedir que vuelva a ocurrir la asociación de autorizaciones ambulatorias en expedientes de internación.

2. Regularización del caso actual:
   como los ítems quedaron activos, sin detalle, no visibles y no gestionables desde pantalla, el cliente probablemente deba rehacer la carga por el circuito correcto.
```

Sobre “volver a dar de alta el expediente”: no lo afirmaría como única salida técnica sin que soporte/desarrollo confirme si pueden regularizar solo los ítems afectados. Pero funcionalmente, para el usuario final, sí: **si no puede acceder a esos ítems y necesita liquidar, lo operativo sería rehacer la carga correctamente**, ya sea rehaciendo el expediente o generando uno nuevo según el procedimiento que usen.

Yo lo formularía así:

```text
La solución funcional para el usuario final no parece ser completar el detalle de los ítems 21 y 22, porque esos ítems provienen de una autorización ambulatoria y el expediente es de internación. La corrección esperada del sistema sería impedir que ese tipo de autorización genere ítems dentro de un expediente de internación.

Para el caso actual, al no poder gestionar esos ítems desde la interfaz, corresponde derivar a Análisis/Desarrollo para evaluar la regularización técnica. Operativamente, el cliente debería rehacer la carga por el circuito correcto de internación, salvo que soporte indique

---

### [14/08/2026 13:54:21] Valeria Saravia

pero yo creo q en este caso, la verdadera corrección del sistema, no es q aparezca el detalle para poder corregir, xq se trata de prestaciones de autorizaciones ambulatorias, y estamos tratando un expediente de internación.
Entonces, la solución sería q el sistema no permita q lo ambulatorio pase a un expediente de internación.
Dicho esto, al cliente no le va a quedar otra opción q volver a dar de alta el expediente...
Decime q opinas, no me des la razón!

---

### [14/08/2026 13:54:37] Agente

Vale, mi opinión sin darte la razón automáticamente: **sí, el enfoque correcto de corrección del sistema no sería “mostrar esos ítems para que los completen” como solución principal**.

Si los ítems vienen de una autorización ambulatoria/prestacional y el expediente es de internación, mostrarlos para que el usuario los edite podría maquillar el problema, pero no corregir la causa. La corrección real debería ser una validación previa: **el sistema no debería permitir asociar/cargar ítems de una autorización ambulatoria dentro de un expediente de internación**, o al menos debería bloquearlo con un mensaje claro antes de grabar.

Lo que sí matizaría es esto: **no afirmaría todavía que necesariamente deben volver a dar de alta todo el expediente**, salvo que funcionalmente no tengan forma de anular/rearmar solo esos ítems o el expediente haya quedado bloqueado para cerrar. Yo lo diría así:

```text
La corrección funcional del sistema debería estar en el origen de la carga: no permitir que prestaciones provenientes de una autorización ambulatoria queden asociadas a un expediente de internación, ya que luego esos ítems no son gestionables correctamente desde la pantalla del expediente.

Para este caso puntual, como los ítems ya quedaron grabados en una condición inconsistente y el usuario no puede acceder a ellos desde la interfaz para corregirlos, se requiere intervención de soporte/análisis. Si no existe una opción funcional para anular o rehacer solo esos ítems, el cliente deberá generar nuevamente la carga del expediente por el circuito correcto.
```

Yo no pondría “no le queda otra que rehacer el expediente” como primera frase, porque suena muy definitivo. Pondría: **“si no tienen opción funcional para regularizarlo desde pantalla, deberán rehacer la carga por el circuito correcto”**. Esa formulación es más defendible.

---

### [14/08/2026 14:22:02] Valeria Saravia

este tipo de expediente, tiene para cargar por un lado lo que es ambulatorio (marcado en esta imagen con rojo) y por otro lado lo q es internación, xq no lo veo en la parte de ambulatorio a los items 21 y 22?

---

### [14/08/2026 14:22:28] Agente

Vale, justamente eso refuerza la inconsistencia.

Los ítems `21` y `22` **no aparecen en Ambulatoria** porque no quedaron grabados como ítems ambulatorios del expediente. Quedaron en la tabla/circuito de **internación** (`expe_internaciones`), pero asociados a una autorización que es ambulatoria/prestacional.

Entonces quedan en una zona inválida:

```text
No aparecen en Internación
porque la grilla de internación no los devuelve como ítems válidos gestionables.

No aparecen en Ambulatoria
porque no fueron grabados como órdenes/prácticas ambulatorias del expediente.
```

La captura muestra Ambulatoria, pero ahí el sistema lista los ítems del circuito ambulatorio del expediente. Por eso ves otro ítem, por ejemplo `Item 1`, pero no los `21` y `22`.

Consulta para demostrarlo:

```sql
-- Cabecera donde realmente existen los items 21 y 22
SELECT
  'EXPE_INTERNACIONES' AS origen,
  item,
  sucur,
  auto,
  total,
  baja_fecha,
  medi_grupo,
  lugar,
  efe_prestad
FROM expe_internaciones
WHERE ofi = 1
AND expe = 29753
AND item IN (21, 22)

-- Verificar si existen como items ambulatorios del expediente
SELECT
  'EXPE_ORDENES' AS origen,
  eo.item,
  eo.sucur,
  eo.auto,
  eo.total,
  eo.baja_fecha
FROM expe_ordenes eo
WHERE eo.ofi = 1
AND eo.expe = 29753
AND eo.item IN (21, 22)
```

Si la segunda consulta no devuelve registros, queda demostrado que **no están en el circuito ambulatorio visible**, aunque la autorización asociada sea ambulatoria.

Yo lo explicaría así:

```text
Los ítems 21 y 22 no se visualizan tampoco en la solapa Ambulatoria porque no fueron grabados como órdenes ambulatorias del expediente. Existen en la cabecera de internación, pero asociados a una autorización ambulatoria/prestacional. Por eso no quedan disponibles ni en la grilla de internación ni en la grilla ambulatoria.
```

Esto apunta más claramente a un error de circuito: el sistema permitió generar registros de internación a partir de una autorización ambulatoria, pero luego esos registros quedan fuera de las vistas operativas del usuario.

---

### [14/08/2026 14:25:09] Valeria Saravia

como esta es una base de prueba, que te parece si probamos poniendo fecha de baja a los items 21 y 22, esto sería solución? de ser así, puedo generar la tarea para el sector de análisis y desarrollo con esta sugerencia

---

### [14/08/2026 14:25:38] Agente

Sí, en base de prueba me parece una validación razonable, pero la tomaría como **prueba de regularización**, no como solución definitiva del sistema.

Poner `baja_fecha` a los ítems `21` y `22` probablemente permitiría que dejen de interferir en el cierre/liquidación, porque quedarían dados de baja. Pero la corrección de fondo sigue siendo otra: **el sistema no debería permitir que prestaciones de una autorización ambulatoria queden generadas como ítems de internación no gestionables desde pantalla**.

Yo generaría la tarea a Análisis/Desarrollo con esta idea:

```text
Se solicita analizar inconsistencia en expediente de internación 1-29753.

Los ítems 21 y 22 existen en expe_internaciones, no poseen baja_fecha y tienen importe en cabecera, pero no poseen detalle activo y no son visibles desde la pestaña Items del expediente. Además, quedaron asociados a la autorización 40-753858, correspondiente a una autorización ambulatoria/prestacional.

Como el usuario final no puede acceder a esos ítems desde la interfaz para corregirlos o anularlos, se solicita evaluar la regularización del caso, por ejemplo dando de baja lógica los ítems inconsistentes si corresponde.

También se solicita revisar la causa funcional para evitar que el sistema permita generar ítems de internación a partir de autorizaciones ambulatorias, ya que luego quedan fuera del circuito operativo del expediente.
```

Antes de sugerirlo como corrección, en prueba yo validaría dos cosas:

1. Dar de baja `21` y `22`.
2. Reintentar el cierre/liquidación del expediente.

Si con eso el expediente avanza, la tarea queda mucho más sólida: “la baja lógica regulariza el caso puntual, pero se requiere corrección preventiva en el circuito”.

---

### [14/08/2026 14:28:36] Valeria Saravia

ok, podrás generar la consulta q ponga fecha de baja a los items 21 y 22 así la pruebo, aprovechando que se trata de una copia de la base de datos, puedo probar la solución propuesta

---

### [14/08/2026 14:29:01] Agente

Sí, Vale. Para probar en la copia de `mutualrioiv_prod`, usaría este script:

```sql
-- Ver estado actual antes del cambio
SELECT
  ofi,
  expe,
  item,
  sucur,
  auto,
  total,
  baja_fecha,
  medi_grupo,
  lugar,
  efe_prestad
FROM expe_internaciones
WHERE ofi = 1
AND expe = 29753
AND item IN (21, 22)
ORDER BY item


-- Baja lógica de los ítems 21 y 22
UPDATE expe_internaciones
SET baja_fecha = GETDATE()
WHERE ofi = 1
AND expe = 29753
AND item IN (21, 22)
AND baja_fecha IS NULL


-- Verificación posterior
SELECT
  ofi,
  expe,
  item,
  sucur,
  auto,
  total,
  baja_fecha,
  medi_grupo,
  lugar,
  efe_prestad
FROM expe_internaciones
WHERE ofi = 1
AND expe = 29753
AND item IN (21, 22)
ORDER BY item
```

Después de correr eso, probá desde el sistema si el expediente permite cerrar/liquidar.

Si necesitás volver atrás en la copia, como originalmente estaban sin baja, podés ejecutar:

```sql
UPDATE expe_internaciones
SET baja_fecha = NULL
WHERE ofi = 1
AND expe = 29753
AND item IN (21, 22)
```

Si con esta baja lógica el expediente permite avanzar, la sugerencia para Análisis/Desarrollo queda bien sustentada: regularizar el caso puntual dando de baja los ítems inconsistentes y corregir el circuito para que no se generen ítems de internación desde autorizaciones ambulatorias.

---

### [14/08/2026 14:34:21] Valeria Saravia

ya me dejó cerrar, podes dejarme todo el análisis del tk 6103 en mi carpeta con la solución propuesta?

---

### [14/08/2026 14:36:18] Valeria Saravia

ya me dejó cerrar, podes dejarme todo el análisis del tk 6103 en mi carpeta con la solución propuesta?

---

### [14/08/2026 14:36:18] Agente

Vale, ya dejé el documento generado en tu carpeta:

`/mnt/Agentes/vesaravia/tk_6103_analisis_solucion.docx`

También dejé el Markdown fuente por si querés reutilizar el texto:

`/mnt/Agentes/vesaravia/tk_6103_analisis_solucion.md`

Validé que el `.docx` esté correcto y no corrupto. Contiene el análisis del TK 6103, evidencia por base, explicación funcional, prueba de baja lógica en copia y texto sugerido para derivar a Análisis/Desarrollo.

---

### [14/08/2026 16:22:49] Valeria Saravia

vi con mi jefa la solución de poner fecha de baja y me da el ok, el tema es q debo decir al usuario que luego de aplicar el script para poner la fecha de baja a los items 20 y 21, debe agergar el registro manualmente sin poner numero de autorización, entonces estoy probando esto último y el sistema no me permite, xq?

---

### [14/08/2026 16:23:34] Agente

Vale, por la captura, **el sistema no te está bloqueando por no poner autorización**. Te está bloqueando porque los nuevos ítems que cargaste tienen **total en cabecera**, pero **no tienen detalle cargado**.

El mensaje lo dice:

```text
Item 44  Total=123.00  Subtotal=0  Diferencia=123.00
Item 45  Total=369.00  Subtotal=0  Diferencia=369.00
Item 46  Total=1.00    Subtotal=0  Diferencia=1.00
```

O sea: cargaste los ítems manualmente, pero falta entrar a **“Valores por item”** y cargar el detalle/importes para cada uno. Hasta que el subtotal del detalle no coincida con el total de la cabecera, no te va a dejar cerrar.

Entonces la indicación al usuario debería ser algo así:

```text
Luego de aplicar la baja lógica sobre los ítems inconsistentes, se deberán cargar nuevamente los ítems en forma manual, sin asociar número de autorización, pero ingresando también el detalle correspondiente desde la pestaña “Valores por item”.

No alcanza con informar el total en la cabecera del ítem. El sistema valida que el subtotal del detalle coincida con el total cargado. Si el detalle queda en cero, al cerrar carga se mostrará la diferencia entre totales y subtotales.
```

Ojo con un detalle: antes hablábamos de los ítems `21` y `22`, pero en tu mensaje pusiste `20` y `21`. Verificaría bien eso antes de pasar el script definitivo. En la prueba actual los nuevos ítems parecen ser `44`, `45` y `46`.

---

### [14/08/2026 17:02:35] Valeria Saravia

listo, pude agregar Valores por item y cerrar el expediente.
Ahora te consulto, xq tiene este comportamiento el sistema, notas algún cambio entre la versión 25.03.005 y la 25.03.004 o la 25.03.003?

---

### [14/08/2026 17:07:29] Agente

Vale, mi lectura es esta:

**El comportamiento se da porque el sistema dejó grabados ítems en `expe_internaciones` con una autorización ambulatoria/prestacional**, pero esos ítems no quedaron cargados como internación válida: tenían `lugar = NULL`, sin detalle y con autorización `tipo = P / ambu_inter = A`. Por eso interferían en el cierre, pero no eran gestionables correctamente desde la interfaz.

Sobre versiones: **no veo evidencia local de que esto haya cambiado puntualmente entre `25.03.005`, `25.03.004` y `25.03.003` como causa directa del caso**. Lo que sí encontré es un cambio relacionado en `R28808`, probado sobre versión `25.03.004`, donde se ajustó el manejo del **lugar de atención** en expedientes de prestaciones, odontología e internación. Ese cambio toca `pri_expe_internaciones` y agrega `@prm_lugar`, que hoy se guarda en `expe_internaciones.lugar`.

La carga manual que hiciste y te dejó cerrar funcionó porque entra por el circuito correcto: la pantalla de internación setea `ambu_inter = 'I'` y toma el `lugar` de internación. Los ítems conflictivos venían de otra condición: autorización ambulatoria asociada a cabecera de internación.

Yo lo derivaría así:

```text
Se detecta que el sistema permitió generar ítems de internación asociados a una autorización ambulatoria/prestacional. Los registros quedaron activos en expe_internaciones, con importe en cabecera, sin detalle y sin lugar de internación, por lo que impactaban en el cierre del expediente pero no podían ser gestionados correctamente desde la interfaz.

La regularización puntual fue aplicar baja lógica sobre los ítems inconsistentes y volver a cargar los conceptos manualmente por el circuito correcto de internación, sin número de autorización y cargando Valores por item.

Se solicita analizar la corrección preventiva: el sistema debería impedir que una autorización ambulatoria/prestacional sea asociada o genere ítems dentro de un expediente de internación.
```

No lo vendería como “cambio de la `25.03.005`” salvo que Desarrollo confirme con SVN completo. Con lo visto, lo más sólido es plantearlo como **validación faltante del circuito**, posiblemente no cubierta por el ajuste de `R28808`.

---

### [14/08/2026 17:26:53] Valeria Saravia

te agrego información:
 Solución temporal: agregar fecha
de baja a estos ítems 21 y 22 para que queden de baja y el cliente pueda cerrar el
expediente.

Pero, Una vez realizada la baja
lógica de los ítems, se prueba agregar la autorización al expediente, al hacer
doble clic en este registro para ver “valor por item”, se obtiene el siguiente error y
al aceptar, se cierra el sistema:

---

### [14/08/2026 17:29:58] Agente

Vale, esta nueva evidencia **refuerza que debe derivarse a Análisis/Desarrollo como error del sistema**, no como operatoria del usuario.

La solución temporal de poner `baja_fecha` a los ítems `21` y `22` sirve para regularizar el expediente ya afectado. Pero al intentar volver a agregar la autorización al expediente, el sistema genera un error técnico y se cierra:

```text
PowerBuilder application execution error (R0037)
Type mismatch accessing external object property efe_nombre
at line 28 in function wf_autoriz of object wpp_expe_sana.
```

Revisé el código y el error coincide con [wp_expe_sana.srw](/home/desarrollo-ia-08/.openclaw/workspace/svn/estandar-trunk/exe/Source%20Infomedical/wp_expe_sana.srw:2170):

```powerscript
tab_1.tabpage_1.dw_det.object.efe_nombre[il_fila] = is_null
```

Ahí `efe_nombre` es un campo de texto, pero `is_null` está declarado como `int`. Por eso PowerBuilder da `Type mismatch` y termina la aplicación.

Yo lo plantearía así en la tarea:

```text
Solución temporal aplicada/probada:
Se asignó baja lógica a los ítems 21 y 22 del expediente 1-29753, lo que permitió cerrar el expediente.

Nuevo hallazgo:
Al intentar agregar nuevamente la autorización 40-753858 al expediente y acceder a “Valores por item”, el sistema genera un error PowerBuilder R0037 y se cierra.

El error indica:
Type mismatch accessing external object property efe_nombre
line 28, function wf_autoriz, object wpp_expe_sana.

Conclusión:
Además de la inconsistencia original, se detecta un error técnico en la pantalla de expedientes de internación al procesar una autorización ambulatoria/prestacional. Se solicita revisar la validación para impedir que autorizaciones ambulatorias sean asociadas a expedientes de internación y corregir el error de tipo en wf_autoriz para que el sistema no finalice abruptamente.
```

Mi recomendación: no indicarle al cliente que vuelva a cargar esa autorización. La salida operativa validada es: baja lógica de los ítems inconsistentes y carga manual sin autorización, con sus “Valores por item”. El alta de autorización ambulatoria en expediente de internación debe quedar como caso para corregir.

---

### [14/08/2026 17:33:49] Valeria Saravia

Pre-compaction memory flush. Store durable memories only in memory/2026-08-14.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-14.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-14-HHMM.md); always use the canonical 2026-08-14.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 14th, 2026 - 5:33 PM (America/Buenos_Aires)
Reference UTC: 2026-08-14 20:33 UTC

---

### [14/08/2026 17:35:51] Agente

NO_REPLY

---

### [14/08/2026 17:35:52] Valeria Saravia

yo no se si realmente tiene q existir la validacion "impedir que autorizaciones ambulatorias sean asociadas a expedientes de internación"
xq antes el sistema (y eso validalo vos, por favor) permitía hacer esto (creo)

---

### [14/08/2026 17:39:06] Agente

Vale, validé contra código local y tu duda es correcta: **no conviene afirmar todavía que la regla funcional deba ser “impedir siempre autorizaciones ambulatorias en expedientes de internación”**.

Lo que sí pude verificar es esto:

- En versiones/código anterior ya existía la lógica de buscar autorizaciones de internación con `of_autoriz_row(..., 'I', ...)`.
- Pero cuando el usuario ingresa una autorización manualmente, el código anterior **no bloqueaba claramente** las autorizaciones tipo `P`; había un bloque comentado.
- En el código actual, si la autorización es tipo `P`, el sistema muestra el mensaje:
  `La autorización es de Prestaciones`
  pero **no corta el flujo con RETURN**, o sea: avisa, pero sigue.
- Por eso parece que históricamente el sistema **permitía o al menos toleraba** ingresar una autorización prestacional/ambulatoria en ese contexto.

Entonces yo no lo redactaría como:

```text
El sistema debe impedir autorizaciones ambulatorias en expedientes de internación.
```

Lo redactaría más preciso:

```text
Se solicita analizar si corresponde permitir autorizaciones de tipo prestación/ambulatoria dentro de expedientes de internación.

Actualmente el sistema permite continuar luego de informar que la autorización es de Prestaciones. Esa carga puede generar ítems activos en expe_internaciones que luego no quedan gestionables correctamente desde la interfaz y bloquean el cierre del expediente.

En caso de que funcionalmente no corresponda asociarlas, el sistema debería bloquear la carga con un mensaje claro antes de grabar. Si funcionalmente sí corresponde permitirlas, entonces debe corregirse el circuito para que los ítems generados queden visibles, con detalle gestionable y sin provocar errores al acceder a Valores por item.
```

Mi conclusión: **el incidente no debería cerrarse diciendo directamente “falta bloquear ambulatorias”**, sino como **inconsistencia de circuito**. Desarrollo/Análisis debe definir la regla funcional final: bloquearlas o soportarlas correctamente. Lo que sí es error claro es que hoy quedan registros inconsistentes y además el sistema puede cerrarse con el error PowerBuilder al acceder a valores.

---

### [14/08/2026 17:42:11] Valeria Saravia

ok, por favor, armar un documento final concreto con el caso y dejarlo en mi carpeta bajo el nombre TK6103

---

### [14/08/2026 17:42:43] Valeria Saravia

ok, por favor, armar un documento final concreto con el caso y dejarlo en mi carpeta bajo el nombre TK6103, que sea un word por favor

---

### [14/08/2026 17:44:25] Agente

Vale, ya dejé el documento final en tu carpeta:

`/mnt/Agentes/vesaravia/TK6103.docx`

También dejé el fuente en Markdown por si necesitás reutilizar el texto:

`/mnt/Agentes/vesaravia/TK6103.md`

Validé que el `.docx` existe y que el archivo no está corrupto.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
