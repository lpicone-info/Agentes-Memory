# Memoria curada diaria

- Fecha: 2026-08-05
- Usuario: Jean González
- id_googlechat: users/108895259487888979827
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/1fptbkaaaae

## Resumen de la actividad

### Tema 1: InvGate S6048 Ospecon

- Solicitud o contexto: Jean pidió analizar InvGate `6048`, guardar documento `S6048` y usar perfil `Ospecon_Prod`.
- Respuesta o acciones del agente: Se usó la skill de InvGate, se revisaron capturas, fuentes `estandar-trunk` rev. `84249` y base en solo lectura.
- Resultado: Se generó `/mnt/Agentes/jpinto/S6048.docx`. Clasificación `Error`: el ticket mostraba falla técnica al impactar Prestaci vía linked server `PresmedViejo`, pero al validar los datos ya figuraban impactados. La solicitud `165445` no existía en el perfil consultado; se encontró correlato por contrato/CUIL como solicitud `161157`.
- Estado: Resuelto

### Tema 2: Comparación de SP entre ROISA_Prod y SANAR_Prod

- Solicitud o contexto: Se pidió comparar `aru_grabar_pedido` y luego varios SP entre `ROISA_Prod` y `SANAR_Prod`, especialmente salida `nro_aju`.
- Respuesta o acciones del agente: Se consultó metadata de parámetros, outputs, fechas y resultsets inferibles en ambas bases. No se pudo acceder al cuerpo de los SP por `OBJECT_DEFINITION`, `sp_helptext` ni `syscomments`.
- Resultado: No hay diferencia visible de firma: `aru_grabar_pedido` no devuelve `nro_aju` en ninguna base; su único output visible es `@prm_Nro_peido_out`. Los SP con `@prm_aju OUTPUT` son `aru_auto_pedido`, `aru_generar_ajuste` y `aru_generar_comprobante`. Diferencias sensibles de fecha: Roisa tiene varios SP de configuración/comprobantes con `2026-07-07`, Sanar `2026-04-07`, pero sin cuerpo no se pudo afirmar igualdad funcional total.
- Estado: Resuelto

### Tema 3: Documentación aru_grabar_pedido y parámetro prm_sel

- Solicitud o contexto: Se pidió buscar en manuales contenido sobre `aru_grabar_pedido`, en especial `@prm_sel`, para `SANAR_PROD`.
- Respuesta o acciones del agente: Se revisó repositorio de manuales.
- Resultado: No se encontró definición funcional de `@prm_sel`; aparece una vez en `Compartido Soporte de Aplicaciones/FAQ.docx` como ejemplo con `@PRM_SEL = `. Se documentó que `aru_grabar_pedido` graba pedidos en `PEDIDOS`, requiere parámetros como contrato, integrante, tipo, adjuntos y origen, devuelve pedido en `@prm_Nro_peido_out` y luego se usa `aru_grabar_pedido_attach`.
- Estado: Resuelto

### Tema 4: InvGate S6052 ROISA

- Solicitud o contexto: Se pidió analizar InvGate `6052`, guardar como `S6052` y usar perfil `roisa_prod`.
- Respuesta o acciones del agente: Se analizó ticket, fuente y base en modo solo lectura.
- Resultado: Se generó `/mnt/Agentes/jpinto/S6052.docx`. Clasificación consulta de configuración/dato funcional: el campo Centro de Atención de la grilla sale de `prestad_lugares.cod_punto_atencion` unido con `puntos_atencion.deno`, no directamente del domicilio del prestador; domicilio propio queda en `prestad_lugares.domi` y la asociación con círculo se maneja aparte.
- Estado: Resuelto

### Tema 5: Alta perfil PensandoSalud_Prod e InvGate S6056

- Solicitud o contexto: Se pidió sumar profile `PensandoSalud_Prod` usando host `172.16.10.108`, base `PensandoSalud_Prod` y credenciales equivalentes a `medimas_prod`; luego analizar InvGate `6056` con ese perfil.
- Respuesta o acciones del agente: Se ejecutó la skill de alta de perfil, se validó conexión OK y se analizó el ticket.
- Resultado: Se actualizó `DB_PROFILE.md` con profile `PensandoSalud_Prod` y estado OK. Se generó `/mnt/Agentes/jpinto/S6056.docx`. Clasificación consulta funcional: ruta `Convenios > Consultas > Cuadro comparativo de convenios`; la prestación `420101` existe como `CONSULTA EN CONSULTORIO` en nomenclador 2 y tiene valores activos en 61 convenios.
- Estado: Resuelto
