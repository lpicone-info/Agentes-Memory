## 2026-07-23 - Durable conversation memory

- User: Andre / abiondi. Conversation in Google Chat direct.
- DB profile `igualdadsalud_prod` already registered in `/home/desarrollo-ia-08/.openclaw/workspace/DB_PROFILE.md` with MS SQL, host `172.16.10.108`, port `1433`, database `igualdadsalud_prod`, user `info1`; connection status recorded as OK.
- Ticket 5940 analysis was generated in `/mnt/Agentes/abiondi/invgate/analisis_ticket_5940.docx`. Finding: process tries to insert existing items for expediente `2598`; duplicate key `(1, 2598, 2, 1)` in `EXPE_ORDEN_ITEMS`. No DB changes were executed.
- For expediente `2640`, same duplicate-key error was analyzed. Classification: Error. Cause: transaction `02A` `133513` relates to authorization `sucur_rel=1 / auto_rel=9108`, which matches two active `AUTO_REMOTAS` rows (`133204` and `133272`) due to join on `ar.sucur_rel = ar2.sucur`, `ar.auto_rel = ar2.auto`, `ar2.esta='A'`, `ar2.baja_fecha IS NULL`. This duplicates items and attempts duplicate insert into `EXPE_ORDEN_ITEMS`, key `(1, 2640, 17, 1)`. `EXPE_ORDENES.baja_fecha` does not prevent the error because PK is `(ofi, expe, item, orden)` and duplication occurs in the SP batch before insert.
- Solution recommended for expediente `2640`: verify functionally which active `AUTO_REMOTAS` for authorization `1/9108` is correct; likely leave latest `133272` active and baja/anular previous `133204` through normal system mechanism. If both must remain active by business rule, adjust `pri_expe_transac_remotas_server` to avoid multiplying related authorizations.
- Evidence about duplicate `AUTO_REMOTAS`: `133204` and `133272` were both active for authorization `1/9108`, user `mgomez`, same day `10/06/2026`; `133272` differs by having 12 items vs 11 for `133204`, with extra prestation `660711`. Hypothesis: Presmed created a new remote transaction when authorization was modified, but the later step that should baja previous transactions did not complete.
- Redmine/SVN antecedent found: Defecto `#6746`, commit message: "Se está duplicando la autorización en auto_remotas y auto_prestac_items_remotas cuando se hace y luego se modifica una autorización desde Presmed". Revisions: `r57153` 02/01/2017 added call to `prd_auto_remotas_transac` in `w_clase_auto.srw`; `r57172` 05/01/2017 created SP `prd_auto_remotas_transac`; `r57197` 11/01/2017 adjusted `li_sucur` before executing SP. Later related adjustments on the SP: `R8000`, `9115`, `R14628`, `R21481`.
- User asked to improve document `R26749-09_Requerimiento_V3` for client-requested development to load medications in internacion. Updated documents delivered in `/mnt/Agentes/abiondi/documentos/R26749-09_Requerimiento_V4_Actualizado.docx` and `.doc`. Content added: functional scope, gap against current system, proposed data model, validations, recommended stages, acceptance criteria, risks, and pending definitions. No folder `R26749` was found under `/mnt/Agentes/abiondi`; worked from the found document and current Presmed/expedientes code review.
