# Memoria curada diaria

- Fecha: 2026-08-05
- Usuario: Atencion al Cliente
- id_googlechat: users/113373607595606911178
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/8rjymkaaaae

## Resumen de la actividad

### Tema 1: Parámetros e importación de novedades SSS en ospecon_ayd

- Solicitud o contexto: Se consultó en qué tablas se guardan los datos de la ventana de parámetros SSS y los lotes de importación de novedades, usando base `ospecon_ayd` y rama `25.08.001`.
- Respuesta o acciones del agente: Se revisó fuente PowerBuilder, DataWindows, procedures y base. Se identificó `dbo.isss_param` para parámetros; `ISSS_NOVEDADES_SSSALUD_CAB` e `ISSS_NOVEDADES_SSSALUD_ITEMS` como persistencia del lote; `AFI_IMPORTACIONES_ISSS` y `AFI_IMPORTACIONES` como tablas operativas; `SOLICITUDES` y `SOLI_AFILIADOS` como salida del proceso.
- Resultado: Se informó trazabilidad completa del lote y del circuito de generación de solicitudes, con conteos validados en base: 1 cabecera, 34.095 ítems, 34.095 registros en `AFI_IMPORTACIONES_ISSS` y 34.093 en `AFI_IMPORTACIONES`.
- Estado: Resuelto

### Tema 2: Análisis de bajas totales SSS con CUIT distinto

- Solicitud o contexto: Se pidió buscar en el lote SSS contratos con baja total donde el CUIT informado difiera del CUIT vigente en derivación de aportes, excluyendo luego marcas `NP` y corrigiendo el criterio para bajas totales reales.
- Respuesta o acciones del agente: Se recalculó el universo con distintos criterios y finalmente se aplicó `soli_moti = 3` más exclusión de contratos con `cod_op = NP`.
- Resultado: Resultado final: 0 registros, 0 contratos y 0 afiliados con baja total real y CUIT distinto bajo el criterio solicitado.
- Estado: Resuelto

### Tema 3: R30189 ventana parámetros SSS

- Solicitud o contexto: Se pidió analizar el documento `R30189` por error en la ventana de parámetros SSS y dejar documento en carpeta Redmine.
- Respuesta o acciones del agente: Se analizó `wp_isss_param`, `d_isss_param`, `dd_tarifas_vig`, datos reales en `ospecon_ayd` y se generaron documentos en `/mnt/Agentes/abiondi/redmine/R30189_analisis.docx` y `.md`.
- Resultado: Clasificación `Error`: la ventana guarda bien plan y tarifa en `isss_param`, pero puede mostrar una descripción incorrecta porque la DDDW de tarifas vigentes no filtra por `plan_codi` y usa solo `tari` como clave visible.
- Estado: Resuelto

### Tema 4: Errores de afiliación SSS por estado civil y parentesco

- Solicitud o contexto: Se pidió analizar errores al procesar afiliaciones SSS por `civil_esta` nulo y luego por parentesco no homologado tras actualizar `ISSS_NOVEDADES_SSSALUD_ITEMS`.
- Respuesta o acciones del agente: Se generaron documentos `analisis_error_procesar_afiliacion_sss_civil_esta.docx/.md` y `analisis_error_parentesco_sss.docx/.md`; se revisaron tablas operativas y flujo de importación.
- Resultado: Para `456265/00`, el error de estado civil fue clasificado como `Error`: `afi_import_batch_isss` permite grabar `AFILIADOS.civil_esta = NULL` y luego falla al insertar `SOLI_AFILIADOS`. Para destrabar se indicó actualizar indispensablemente `AFILIADOS.civil_esta = 9` y también `AFI_IMPORTACIONES_ISSS.civil_esta = 9` por coherencia. Para parentesco, la validación usa `AFI_IMPORTACIONES_ISSS.paren`, no solo `ISSS_NOVEDADES_SSSALUD_ITEMS`; al momento del análisis ya no había diferencias entre tablas.
- Estado: Resuelto

### Tema 5: Afiliación versus cambio de derivación SSS

- Solicitud o contexto: Se pidió explicar por qué algunos casos con CUIT distinto generan cambio de derivación y otros quedan como afiliación sin impactar el nuevo CUIT, con ejemplos `100296` y `101607`.
- Respuesta o acciones del agente: Se analizó el circuito SSS y se generó documento `analisis_sss_afiliacion_vs_cambio_derivacion.docx/.md`.
- Resultado: Clasificación `Error / inconsistencia de clasificación operativa`: `100296` quedó como `ME / soli_moti 13` y sí genera nueva derivación; `101607` quedó como `AT / soli_moti 1`, genera solicitud de afiliación y copia la derivación vigente, sin usar `empre_cuit` de la novedad.
- Estado: Resuelto

### Tema 6: R30191 importación DDJJ Ospicine

- Solicitud o contexto: Se pidió analizar documento `30191` por error de importación DDJJ en `ospicine_prod`, rama trunk, reportado en ticket cliente `6024`.
- Respuesta o acciones del agente: Se revisó el lote `20260805134614157`, archivo `DDJJ_NOMINA_OS104504_F260713.txt`, base y fuente; se generaron `/mnt/Agentes/abiondi/redmine/30191/R30191_analisis.docx` y `.md`.
- Resultado: Clasificación `Error`: si la rectificativa (`origrect = 1`) viene antes que el original (`origrect = 0`), el importador no cierra correctamente el original porque la condición `origrect <= origrect_actual` queda falsa al procesar el original. Se detectaron 7 originales activos con rectificativa activa para mismo `cuit + cuil + periodo`. No se modificaron datos ni código.
- Estado: Resuelto

### Tema 7: R30181 pedido de cambio ROISA

- Solicitud o contexto: Se pidió mejorar el pedido de cambio `30181` en documentos usando `roisa_prod` y trunk, y luego identificar el procedimiento a ajustar.
- Respuesta o acciones del agente: Se generaron `/mnt/Agentes/abiondi/documentos/30181/R30181_mejorado.docx` y `.md` con alcance, comportamiento actual/esperado, evidencia, impacto, riesgos, criterios de aceptación y casos de prueba.
- Resultado: Se concluyó que no conviene pedir simplemente quitar la validación; el ajuste correcto es flexibilizar el bloqueo inicial para expedientes de prestador tipo círculo cuando no hay efectores asociados, permitiendo lugar propio del círculo o carga manual de efector con categoría del círculo, manteniendo validaciones de categoría, convenio/cartilla y prestador existente. Quedó una solicitud posterior sin respuesta final porque el usuario cerró la jornada.
- Estado: Pendiente
