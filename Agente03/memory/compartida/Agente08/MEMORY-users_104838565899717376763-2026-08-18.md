# Memoria de conversación — Valeria Saravia

## Datos

- **Fecha:** 18/08/2026
- **Usuario:** Valeria Saravia
- **ID Google Chat:** `users/104838565899717376763`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 51
- **Inicio:** 08:01:48
- **Fin:** 20:50:47

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/jzuyrkaaaae`

## Archivos revisados

- `1ace4f54-f0d5-4382-bb74-d2074e17dba5.jsonl`

---

## Transcripción completa

### [18/08/2026 08:01:48] Valeria Saravia

buen día

---

### [18/08/2026 08:03:03] Agente

Hola Vale, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [18/08/2026 08:14:11] Valeria Saravia

ya analizamos el Tk 6040 en premedic_prod, e cliente respondió, asi que necesito retomar el tema, podes ver su respuesta para ver como seguir o podrás sugerirme que prueba se puede hacer con algún afiliado en situación similar?

---

### [18/08/2026 08:22:06] Agente

**Clasificación:** Error probable / regla funcional de facturación a validar.

Vale, vi la respuesta nueva del cliente. Lo que están preguntando ya no es si el DI `X000102308473` está anulado, sino por qué la facturación masiva lo generó originalmente con intereses viejos y cómo evitar que vuelva a pasar.

**Evidencia verificada**
- En el TK 6040, el afiliado `38856511 - IRIGOYEN, MARIANO NICOLAS` tiene reingreso `01/07/2026`.
- La DI problemática `X000102308473`, período `08/2026`, fue generada por facturación masiva (`proceso_origen = FM`) y quedó anulada el `05/08/2026`.
- Esa DI incluía conceptos `INT` de `07/2021` a `12/2021`.
- La DI posterior `X000102319524`, generada luego por `FI`, no incluye esos intereses.
- Encontré otros casos vigentes similares generados por masiva en agosto:
  - `26622182`, DI `X000102304345`, reingreso `01/07/2026`, con intereses viejos.
  - `31791393`, DI `X000102306461`, reingreso `01/07/2026`, con intereses viejos.
  - `41803033`, DI `X000102299390`, reingreso `01/07/2026`, con intereses de `04/2021` a `07/2021`.

**Conclusión**
La respuesta al cliente debería ir por este lado: el caso no se explica solo por el comprobante anulado. La masiva parece estar tomando deuda/intereses históricos del mismo contrato sin cortar el cálculo por la fecha de reingreso vigente. Eso permite que, ante un reingreso, arrastre intereses de una etapa anterior del afiliado.

**Prueba sugerida**
Usaría como caso de prueba el afiliado `41803033 - OLIVARES, JOAQUIN FACUNDO`, porque hoy tiene un caso vigente y claro:
- reingreso `01/07/2026`
- DI masiva `X000102299390`
- período `08/2026`
- conceptos `INT` de `04/2021`, `05/2021`, `06/2021` y `07/2021`

La prueba sería revisar en pantalla la cuenta corriente con y sin anulados, confirmar que la DI vigente muestra intereses viejos, y luego pedir validación funcional/desarrollo sobre la regla: en reingresos, la facturación masiva no debería calcular intereses de comprobantes anteriores a la nueva alta del afiliado.

No modifiqué ticket, datos ni código.

---

### [18/08/2026 08:25:48] Valeria Saravia

te parece si reincorporamos a algún afiliado y corremos el proceso de facturación a ver como sale? q afiliado podría ser q al reincorporar tengo esos saldos con intereses?

---

### [18/08/2026 08:27:25] Agente

Sí, me parece buena prueba, pero la haría en copia/base de prueba, no directo sobre `Premedic_Prod`, porque reincorporar y correr facturación cambia datos.

**Afiliado recomendado para probar:** `29422106 / 00 - PONCE, RODRIGO JAVIER`.

Lo elijo porque está bastante limpio para la prueba:
- Está dado de baja: `31/12/2021`.
- Tiene deuda vieja pendiente en DI.
- El período pendiente es viejo: `02/2021`.
- El saldo pendiente es bajo: `$1.000,00`.
- No tiene DI vigente de julio/agosto 2026, así que la prueba no se mezcla con facturación actual.

**Prueba que haría**
1. Reincorporar al afiliado con alta nueva, por ejemplo `01/07/2026`.
2. Generar/facturar el período siguiente con el mismo circuito de facturación masiva.
3. Revisar la DI generada.
4. Validar si aparecen ítems `INT` asociados a comprobantes/períodos anteriores al reingreso.
5. Comparar contra una facturación manual/refacturación del mismo caso, porque en el TK 6040 la masiva (`FM`) generó intereses viejos y la posterior (`FI`) no.

Como alternativa para demostrar que el problema ya existe sin reincorporar nada, podés usar `41803033 / 00 - OLIVARES, JOAQUIN FACUNDO`: ya está reingresado al `01/07/2026` y tiene una DI masiva de agosto `X000102299390` con intereses de `04/2021` a `07/2021`.

---

### [18/08/2026 08:35:57] Valeria Saravia

Pre-compaction memory flush. Store durable memories only in memory/2026-08-18.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-18.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-18-HHMM.md); always use the canonical 2026-08-18.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 18th, 2026 - 8:35 AM (America/Buenos_Aires)
Reference UTC: 2026-08-18 11:35 UTC

---

### [18/08/2026 08:36:41] Agente

NO_REPLY

---

### [18/08/2026 08:36:42] Valeria Saravia

hay algún parámetro o regla q impida que la facturación lleve intereses en caso de reincorporación de afiliados?

---

### [18/08/2026 08:38:09] Agente

**Clasificación:** Nuevo desarrollo requerido, si la necesidad es excluir intereses solo en reincorporaciones.

Vale, no encontré un parámetro específico que diga “en reincorporación de afiliado no calcular intereses anteriores al nuevo ingreso”.

Lo que sí existe es parametrización general de intereses:

- `zz_sistema_reg 247 = S`: por default se calculan intereses en subcuentas.
- `zz_sistema_reg 217 = S`: toma como fecha desde para interés el primer vencimiento del comprobante.
- `zz_sistema_reg 172 = N`: no está ignorando intereses posteriores al último vencimiento.
- `zz_sistema_reg 213 = N`: no está usando la regla de calcular según último vencimiento del último comprobante generado.
- `subcta_contratos.calc_intereses`: permite marcar si una cuenta/subcuenta calcula intereses, pero no es una condición puntual por reincorporación.

**Conclusión:** hoy el sistema no parece tener una regla funcional que corte los intereses por `ingre_fecha` cuando el afiliado fue reincorporado. Por eso la masiva puede tomar deuda histórica del mismo contrato y calcular intereses aunque correspondan a una etapa anterior.

No usaría `calc_intereses = N` como solución del caso, porque apagaría intereses para una cuenta/subcuenta en general, no solo para reincorporados. Para resolverlo bien habría que pedir ajuste de lógica: cuando el afiliado tenga reingreso, la facturación no debería generar intereses sobre comprobantes con período anterior a la nueva `ingre_fecha`.

---

### [18/08/2026 08:52:36] Valeria Saravia

cuando me dices: "Para resolverlo bien habría que pedir ajuste de lógica: cuando el afiliado tenga reingreso, la facturación no debería generar intereses sobre comprobantes con período anterior a la nueva ingre_fecha." esto debería ser x parámetro? xq yo creo q para reafiliarte, primero hay a ponerse al día, al menos así me pasó a mi cuando reingresé a una obra social (osppt de tucumán), primero pasé x caja pagando unos intereses viejos y luego con el comprobante de pago, me reingresaron

---

### [18/08/2026 08:52:55] Agente

Sí, tu planteo es correcto. Lo que yo dije como “no debería generar intereses anteriores al reingreso” no necesariamente debería ser un parámetro para apagar intereses. Mejor dicho: **hay que definir funcionalmente qué debe pasar con la deuda histórica al momento de la reafiliación**.

**Clasificación:** definición funcional / posible nuevo desarrollo.

Hay dos criterios posibles:

1. **Criterio de regularización obligatoria**
   Antes de reincorporar al afiliado, el sistema debería exigir que salde la deuda/intereses anteriores. En ese caso, el problema no sería que la masiva calcule intereses, sino que el sistema permite reingresar al afiliado con deuda histórica sin bloquear o advertir.

2. **Criterio de no arrastre automático**
   Si el cliente decide reincorporar sin cobrar deuda vieja, entonces la facturación nueva no debería mezclar deuda/intereses anteriores con la cuota del nuevo período. Esa deuda debería quedar como saldo histórico separado, no aparecer automáticamente en la DI nueva.

Para mí, el punto fuerte para responder o escalar es este:

> El sistema actualmente permite que una reincorporación quede activa aunque existan saldos históricos del período anterior. Luego, al correr la facturación masiva, esos saldos pueden generar intereses y quedar incluidos en la nueva DI. Se requiere definición funcional del cliente: si la deuda histórica debe cobrarse antes del reingreso, el sistema debería bloquear o advertir la reincorporación hasta regularizarla; si no debe cobrarse, la facturación debe excluir saldos anteriores a la nueva fecha de ingreso.

Yo no lo plantearía como “poner un parámetro para no calcular intereses”, porque eso puede apagar intereses válidos. Lo plantearía como regla de negocio específica de reincorporación.

---

### [18/08/2026 08:58:05] Valeria Saravia

OK, tengo 2 consultas:
1- al ser muchos clientes y cada uno va a tener su regla de negocios, entonces, pensé en un parámetro para q les sirva a todos, pueden activar o desactivar el parámetro según sus necesidades, o como sugeris vos que se puede hacer q sirva a todo el universo de clientes?
2- si el sistema funcionó siempre así, entonces se trata de un nuevo desarrollo (sería un presupuesto), vos podes confirmar si siempre el sistema funcionó de esta manera?

---

### [18/08/2026 09:00:08] Agente

Vale, separo las dos:

1. Sí, para servir a todos los clientes yo lo haría **parametrizable**, pero no como “calcula intereses sí/no” general, porque eso ya existe y es demasiado amplio.

Lo correcto sería un parámetro específico de negocio, por ejemplo:

`Intereses en reincorporación`

Con valores posibles:
- `S`: al reincorporar, la facturación puede arrastrar deuda/intereses históricos anteriores al nuevo ingreso.
- `N`: al reincorporar, la facturación nueva no calcula intereses sobre períodos anteriores a la nueva `ingre_fecha`.
- Opcional más completo: `B`, para bloquear o advertir el reingreso si el afiliado tiene deuda histórica pendiente.

Para mí, el mejor diseño sería no solo activar/desactivar, sino definir el comportamiento:

- `PERMITE`: deja reincorporar y luego la masiva puede tomar deuda vieja.
- `NO_CALCULA_INTERESES_ANTERIORES`: deja reincorporar, pero no suma intereses anteriores al nuevo ingreso.
- `BLOQUEA_CON_DEUDA`: no permite reincorporar hasta regularizar deuda histórica.
- `ADVIERTE_CON_DEUDA`: permite continuar, pero mostrando advertencia.

Eso cubre mejor el universo de clientes, porque algunos van a querer cobrar todo antes de reingresar, otros van a permitir reingreso sin arrastre automático, y otros solo van a querer advertencia operativa.

2. Sobre si siempre funcionó así: **no puedo confirmar “siempre” en términos absolutos**, pero sí puedo confirmar con evidencia que la lógica de cálculo de intereses es histórica y no encontré en el código vigente una excepción por reincorporación.

Evidencia:
- `afs_dame_interes.sql`: procedimiento de intereses con historial desde 2008.
- `afs_dame_interes_genera_vto.sql`: también desde 2008.
- `afu_fac_compro_a_generar_vtos_interes.sql`: cálculo de vencimientos/intereses dentro de facturación, con cambios históricos 2019, 2021 y merge 2024.
- En esos procedimientos no encontré condición por `reincorporación`, `reingreso` o corte por nueva `ingre_fecha`.
- En `Premedic_Prod`, las reglas vigentes tienen intereses habilitados: `247 = S`, `217 = S`.

Entonces, para presupuesto, yo lo plantearía así:

> Actualmente el sistema no cuenta con una regla específica para tratar intereses históricos en casos de reincorporación de afiliados. La lógica vigente de facturación calcula intereses según saldos/comprobantes pendientes y parametrización general, sin distinguir si la deuda corresponde a una afiliación anterior al nuevo ingreso. Se requiere nuevo desarrollo parametrizable para definir el comportamiento por cliente ante reincorporaciones con deuda histórica.

---

### [18/08/2026 09:13:29] Valeria Saravia

ok, podes hacer un documento con todo lo q vimos sobre el tk 6040, incluyendo todos los parámetros y reglas analizados y propuesta de solución?

---

### [18/08/2026 09:13:56] Valeria Saravia

ok, podes hacer un documento word con todo lo q vimos sobre el tk 6040, incluyendo todos los parámetros y reglas analizados y propuesta de solución? y dejarlo en mi carpeta vesaravia

---

### [18/08/2026 09:16:21] Agente

Vale, generé el Word con el análisis consolidado del TK 6040:

`/mnt/Agentes/vesaravia/analisis_ticket_6040_reincorporacion_intereses.docx`

Incluye el caso original, respuesta nueva del cliente, evidencia de base, casos similares, parámetros/reglas revisados, historial de fuente, prueba sugerida y propuesta de solución parametrizable. Validé el `.docx` con `unzip -t` y no tiene errores.

---

### [18/08/2026 11:56:56] Valeria Saravia

analizar tk 6115 en la base de datos medimas_prod

---

### [18/08/2026 11:59:00] Valeria Saravia

analizar tk 6115 en la base de datos medimas_prod

---

### [18/08/2026 11:59:11] Valeria Saravia

analizar tk 6115 en la base de datos medimas_prod

---

### [18/08/2026 11:59:18] Valeria Saravia

analizar tk 6115 en la base de datos medimas_prod

---

### [18/08/2026 11:59:24] Valeria Saravia

analizar tk 6115 en la base de datos medimas_prod

---

### [18/08/2026 14:57:16] Valeria Saravia

acá se debe configurar el submodulo solicitudes para poder adjuntar certificado a una solicitud?

---

### [18/08/2026 14:58:02] Agente

Sí, Vale. Para que el adjunto **Certificado** quede disponible asociado a una **Solicitud**, no alcanza solo con tener el tipo de adjunto creado y marcado como `Certificado`; hay que vincularlo desde el botón **SubModulo**.

Con lo que ya habíamos visto, la pantalla correcta es:

`Configuración -> Tipos de Adjuntos`

Ahí seleccionás el tipo `10 - Certificado` y entrás por **SubModulo**. En esa configuración deberías asociarlo al módulo/submódulo **Solicitudes** y, si corresponde, definir si es **obligatorio** para algún motivo de solicitud.

Ojo con esta diferencia:

- Si querés que **se pueda adjuntar** certificado: configurar el tipo en el submódulo Solicitudes.
- Si querés que **sea obligatorio** adjuntar certificado para cierta solicitud/motivo: además debe quedar marcado como obligatorio en esa configuración específica.

La marca `Certificado` que se ve en la grilla clasifica el tipo de adjunto, pero la disponibilidad por circuito se maneja desde **SubModulo**.

---

### [18/08/2026 15:41:41] Valeria Saravia

cual sería el sql para consultar por comprobantes q tienen cae?

---

### [18/08/2026 15:43:07] Agente

Sí, Vale. La tabla clave es `COMPRO_CAE`. Se cruza con `COMPROBANTES` por `ente + compro_tipo + compro_nro`.

SQL base:

```sql
SELECT
    c.ente,
    c.compro_tipo,
    c.compro_nro,
    c.fecha,
    c.factu_perio,
    c.cuenta,
    c.subcta,
    c.total,
    c.saldo,
    cae.cae,
    cae.cae_vto,
    cae.fecha_solicitud
FROM comprobantes c
INNER JOIN compro_cae cae
    ON  cae.ente        = c.ente
    AND cae.compro_tipo = c.compro_tipo
    AND cae.compro_nro  = c.compro_nro
WHERE c.baja_fecha IS NULL
  AND c.anu_fecha IS NULL
  AND cae.baja_fecha IS NULL
  AND cae.cae IS NOT NULL
ORDER BY c.fecha DESC, c.compro_tipo, c.compro_nro;
```

Si querés filtrarlo por período facturado:

```sql
AND c.factu_perio = '202608'
```

O por número de comprobante:

```sql
AND c.compro_tipo = 'FC'
AND c.compro_nro = 'B000100012345'
```

Para mí, la condición importante es esta:

```sql
cae.cae IS NOT NULL
AND cae.baja_fecha IS NULL
```

porque ahí verificás que el comprobante tiene CAE activo.

---

### [18/08/2026 15:48:29] Valeria Saravia

y si quiero consultar por un comprobante en particular, cual sería la consulta?

---

### [18/08/2026 15:48:41] Agente

Para un comprobante puntual usaría esta:

```sql
SELECT
    c.ente,
    c.compro_tipo,
    c.compro_nro,
    c.fecha,
    c.factu_perio,
    c.cuenta,
    c.subcta,
    c.total,
    c.saldo,
    c.anu_fecha,
    c.baja_fecha,
    cae.cae,
    cae.cae_vto,
    cae.fecha_solicitud,
    cae.cae_obser
FROM comprobantes c
LEFT JOIN compro_cae cae
    ON  cae.ente        = c.ente
    AND cae.compro_tipo = c.compro_tipo
    AND cae.compro_nro  = c.compro_nro
    AND cae.baja_fecha IS NULL
WHERE c.ente = 1
  AND c.compro_tipo = 'FC'
  AND c.compro_nro = 'B000100012345';
```

Cambiás estos tres datos:

```sql
c.ente = 1
c.compro_tipo = 'FC'
c.compro_nro = 'B000100012345'
```

La dejo con `LEFT JOIN` para que te devuelva el comprobante aunque no tenga CAE. Si `cae.cae` sale `NULL`, entonces no tiene CAE activo registrado.

---

### [18/08/2026 16:01:10] Valeria Saravia

volviendo al tk 6040, quiero hacer la prueba con el afiliado q me indicaste, para eso debo correr primero la prefacturación, pero hay varios afiliados q los q no se pudo aplicar, hay alguna manera de obviarlos en la prueba? te recuerdo q trabajamos sobre la copia de la base de datos del cliente, como para poder aplicar alguna consulta sql para estos contratos q no pueden facturar

---

### [18/08/2026 16:04:30] Agente

**Clasificación:** prueba controlada en copia.

Sí, Vale. Para esta prueba no necesitás resolver ni tocar los 36 contratos con error de cápita. La forma más limpia es **ejecutar la prefacturación solo para el contrato de prueba**, porque el SP `afi_generar_prefacturas` tiene parámetro `@contra`.

Para `PONCE, RODRIGO JAVIER`, usaría algo así en la copia:

```sql
DECLARE @prepaga smallint = 1;
DECLARE @factu_perio smalldatetime = '20260901';
DECLARE @contra char(15) = '29422106';

EXEC dbo.afi_generar_prefacturas
    @prepaga       = @prepaga,
    @factu_perio   = @factu_perio,
    @emi_grupo     = NULL,
    @contra        = @contra,
    @logguear      = 1,
    @prefac_indi   = 0,
    @es_simulacion = 0;

SELECT *
FROM prefacturas
WHERE prepaga = @prepaga
  AND contra = @contra
  AND baja_fecha IS NULL
ORDER BY apli_perio, factu_concep;
```

Eso debería obviar los otros afiliados, porque no corre por todo el grupo `UNICO`, sino solo por `contra = '29422106'`.

Si querés hacerlo desde pantalla sí o sí, ahí la alternativa sería crear/usar un **grupo de emisión exclusivo de prueba**, mover temporalmente solo ese contrato a ese grupo en la copia, correr la prefacturación para ese grupo y luego restaurar. Pero para esta validación puntual, prefiero ejecutar el SP por contrato: es más directo, deja menos ruido y evita tocar los 36 casos con error de cápita.

No modificaría los contratos que “no se pudo aplicar” solo para pasar la prefacturación, porque el error es de tarifa/cápita y no forma parte del objetivo del TK 6040.

---

### [18/08/2026 16:11:01] Valeria Saravia

lo que pasa es q si luego de pre-facturar, quiero facturar, el sistema indica q no se generaron los comprobantes, entonces los genero, vuelvo a intentar facturar, pero el sistema vuelve a indicar que no se generaron los comprobantes (te muestro la secuencia que hago, 1 2 3):

---

### [18/08/2026 16:12:46] Agente

Sí, Vale. Ahí hay dos temas:

1. En la facturación estás usando **Grupo de emisión = (Todos)**.
2. En la generación de comprobantes estás usando **Grupo de emisión = UNICO**.

Entonces, aunque generes para `UNICO`, cuando volvés a facturar con `(Todos)` el sistema vuelve a validar todos los grupos y puede seguir encontrando comprobantes pendientes.

Además, el bloqueo no valida solo “comprobante generado”; en código revisa:

```sql
comprobantes.impre_fecha IS NULL
```

O sea: para el sistema siguen faltando comprobantes **impresos/marcados como impresos**, no solo generados.

Para ver exactamente qué bloquea, ejecutá:

```sql
EXEC dbo.afs_fac_comp_no_imp
    @perio = '20260901',
    @prepaga = 1,
    @emi_grupo = NULL; -- NULL = Todos
```

Si querés ver solo `UNICO`, primero habría que confirmar el código numérico del grupo:

```sql
SELECT emi_grupo, deno
FROM emi_grupos
WHERE baja_fecha IS NULL
ORDER BY emi_grupo;
```

Y luego:

```sql
EXEC dbo.afs_fac_comp_no_imp
    @perio = '20260901',
    @prepaga = 1,
    @emi_grupo = <codigo_unico>;
```

Para la prueba del TK 6040, haría esto:

1. Facturación: usar **UNICO**, no `(Todos)`, si vas a generar solo `UNICO`.
2. Si sigue bloqueando, abrir el detalle cuando pregunta “¿desea ver los detalles?” o ejecutar `afs_fac_comp_no_imp`.
3. En copia, si solo necesitás destrabar la prueba, se puede marcar como impresos los comprobantes pendientes que devuelve ese control. Pero eso debe hacerse solo en la copia.

SQL de bypass controlado en copia:

```sql
-- Backup antes de tocar nada
SELECT C.*
INTO zz_bkp_tk6040_comprobantes_sin_impre
FROM comprobantes C
WHERE C.prepaga = 1
  AND C.factu_perio <= '20260901'
  AND C.impre_fecha IS NULL
  AND C.baja_fecha IS NULL;

-- Marcar como impresos solo para destrabar la prueba en copia
UPDATE C
SET impre_fecha = GETDATE()
FROM comprobantes C
WHERE C.prepaga = 1
  AND C.factu_perio <= '20260901'
  AND C.impre_fecha IS NULL
  AND C.baja_fecha IS NULL;
```

Pero primero miraría el detalle con `afs_fac_comp_no_imp`, porque puede ser suficiente con facturar `UNICO` en vez de `(Todos)`.

---

### [18/08/2026 16:30:05] Valeria Saravia

facturar único no fue suficiente, entonces apliqué el script q me pasaste

---

### [18/08/2026 16:30:23] Agente

Perfecto, Vale. Que haya dado `1 fila afectada` indica que había un solo comprobante bloqueando por `impre_fecha IS NULL`.

Ahora validaría así:

```sql
EXEC dbo.afs_fac_comp_no_imp
    @perio = '20260901',
    @prepaga = 1,
    @emi_grupo = NULL;
```

Si no devuelve filas, ya podés volver a intentar la facturación de `09/2026`.

Después de facturar, revisá qué se generó para Ponce:

```sql
SELECT
    c.ente,
    c.compro_tipo,
    c.compro_nro,
    c.fecha,
    c.factu_perio,
    c.contra,
    c.total,
    c.saldo,
    c.proceso_origen,
    c.impre_fecha
FROM comprobantes c
WHERE c.prepaga = 1
  AND c.contra = '29422106'
  AND c.factu_perio = '20260901'
  AND c.baja_fecha IS NULL
ORDER BY c.fecha DESC;
```

Y para ver si arrastró intereses viejos:

```sql
SELECT
    p.contra,
    p.inte,
    p.factu_concep,
    p.apli_perio,
    p.factu_perio,
    p.impor,
    p.plan_codi,
    p.tari,
    p.gene_fecha
FROM prefacturas p
WHERE p.prepaga = 1
  AND p.contra = '29422106'
  AND p.factu_perio = '20260901'
  AND p.baja_fecha IS NULL
ORDER BY p.apli_perio, p.factu_concep;
```

El dato clave es si aparece `factu_concep = 'INT'` con `apli_perio` anterior a la fecha de reingreso. Ahí ya tendríamos reproducida la situación del TK 6040 en la copia.

---

### [18/08/2026 16:32:13] Valeria Saravia

[User sent media without caption]

---

### [18/08/2026 16:32:24] Agente

Bien, eso confirma que ya no quedan comprobantes pendientes de impresión para el control de `afu_fac_cierre`.

Ahora podés volver a intentar la facturación de `09/2026`. Si vuelve a fallar, ya sería por otro control distinto, no por “comprobantes del período anterior”.

Después de facturar, corré las consultas para `29422106` y miramos si generó intereses viejos:

```sql
SELECT *
FROM prefacturas
WHERE prepaga = 1
  AND contra = '29422106'
  AND factu_perio = '20260901'
  AND baja_fecha IS NULL
ORDER BY apli_perio, factu_concep;
```

Y si ya generó comprobante:

```sql
SELECT *
FROM comprobantes
WHERE prepaga = 1
  AND contra = '29422106'
  AND factu_perio = '20260901'
  AND baja_fecha IS NULL
ORDER BY fecha DESC;
```

---

### [18/08/2026 17:01:16] Valeria Saravia

ya mandé a facturar hará 30 minutos, pero parece q se clavó (base da datos premedic_prod)

---

### [18/08/2026 17:04:04] Agente

Vale, revisé `Premedic_Prod`: no parece clavado del todo.

La sesión activa es:

```text
SPID: 62
Usuario: info1
Equipo: INFONBOOK051
Programa: afilia
Proceso: afi_generar_compro_masiva
Periodo: 2026-09-01
Grupo emisión: 1
Inicio aprox.: 16:25
Tiempo corriendo: 38 min
Bloqueada por otra sesión: no
```

Está ejecutando dentro de `afi_genera_aportes_ajustes`, puntualmente actualizando `#FAC_APORTES`. En una medición posterior siguió aumentando CPU/lecturas, o sea: **está trabajando**, no quedó congelada sin actividad.

Mi recomendación: esperaría un poco más si la base es copia y no molesta a otros. Si necesitás cortarlo, no lo haría todavía desde SQL sin confirmación, porque tiene transacción abierta y muchos locks; un `KILL 62` puede disparar rollback y tardar bastante.

Si decidís cortarlo, avisame explícitamente y primero vuelvo a medir el estado del SPID para confirmar que sigue siendo la misma ejecución.

---

### [18/08/2026 17:16:35] Valeria Saravia

esto me tira la facturación

---

### [18/08/2026 17:24:28] Agente

**Clasificación:** Configuración.

Vale, la facturación se frena porque el grupo `UNICO` tiene configurado:

```text
compro_tipo = DI
compro_tipo_nc = NULL
```

Al haber contratos con total negativo en la prefacturación, el sistema intenta generar comprobante crédito. Como `compro_tipo_nc` está vacío, usa el default `NC`. Pero en `ENTES_PUNTOS` no existe relación activa para `ente 1 / punto 0001 / NC`.

Verifiqué además que hay `1601` contratos negativos en `09/2026`, todos con letra estimada `X`. En el punto `0001` sí existe:

```text
CI X
```

Entonces, para la prueba en la copia, lo más coherente no es agregar `NC`, sino configurar el grupo `UNICO` para que el crédito sea `CI`:

```sql
-- Backup antes
SELECT *
INTO zz_bkp_tk6040_emi_grupos_unico
FROM emi_grupos
WHERE emi_grupo = 1;

-- Ajuste en copia
UPDATE emi_grupos
SET compro_tipo_nc = 'CI'
WHERE emi_grupo = 1
  AND baja_fecha IS NULL;
```

Luego volvería a facturar con grupo `UNICO`, punto `0001`, período `09/2026`.

Esto no resuelve todavía el TK 6040; solo destraba la facturación de prueba para poder ver si al afiliado reincorporado le arrastra intereses históricos.

---

### [18/08/2026 17:36:32] Valeria Saravia

[User sent media without caption]

---

### [18/08/2026 17:36:43] Agente

Perfecto, Vale. Quedó aplicado en la copia:

- Backup creado: `zz_bkp_tk6040_emi_grupos_unico`
- Grupo `UNICO` actualizado: `compro_tipo_nc = 'CI'`
- Resultado: `1 fila afectada` en cada paso

Ahora volvé a correr la facturación de `09/2026` con:

```text
Grupo de emisión: UNICO
Punto de venta: 0001
```

Si pasa, después revisamos el resultado de Ponce:

```sql
SELECT *
FROM comprobantes
WHERE prepaga = 1
  AND contra = '29422106'
  AND factu_perio = '20260901'
  AND baja_fecha IS NULL
ORDER BY fecha DESC;
```

Y para ver si arrastró intereses:

```sql
SELECT *
FROM prefacturas
WHERE prepaga = 1
  AND contra = '29422106'
  AND factu_perio = '20260901'
  AND baja_fecha IS NULL
ORDER BY apli_perio, factu_concep;
```

---

### [18/08/2026 18:48:39] Valeria Saravia

esto me tira la facturación

---

### [18/08/2026 18:52:08] Agente

Sí, Vale. Ya vi por qué sigue fallando: ahora ya no es `NC`, quedó faltando esta relación:

```text
ente 1 / punto 0001 / comprobante DI / letra B
```

En la prefacturación activa hay 1 contrato directo con importe positivo y letra `B`. El grupo `UNICO` genera débitos con `DI`, pero el punto `0001` tiene `DI A`, `DI C` y `DI X`; no tiene `DI B`.

En la copia, aplicaría esto:

```sql
-- Backup antes
SELECT *
INTO zz_bkp_tk6040_entes_puntos_0001
FROM entes_puntos
WHERE ente = 1
  AND punto = '0001';

-- Agregar relacion faltante para la prueba
INSERT INTO entes_puntos
(
    ente,
    punto,
    compro_tipo,
    tipo,
    nro,
    alta_fecha,
    baja_fecha,
    cautoimp,
    fec_vig_cai,
    actu_fecha,
    tipo_fact
)
SELECT
    1,
    '0001',
    'DI',
    'B',
    0,
    GETDATE(),
    NULL,
    NULL,
    NULL,
    GETDATE(),
    NULL
WHERE NOT EXISTS (
    SELECT 1
    FROM entes_puntos
    WHERE ente = 1
      AND punto = '0001'
      AND compro_tipo = 'DI'
      AND tipo = 'B'
      AND baja_fecha IS NULL
);
```

Después volvé a correr la facturación. Si aparece otro mensaje, ya sería el siguiente control; pero este error puntual ahora apunta a `DI B`.

---

### [18/08/2026 20:38:28] Valeria Saravia

al fin! muchas gracias!

---

### [18/08/2026 20:38:37] Agente

Excelente, Vale. Quedó destrabada la facturación: generó `76471` comprobantes en `46 min 31 seg`.

Ahora el siguiente paso para el TK 6040 es revisar el afiliado de prueba:

```sql
SELECT *
FROM comprobantes
WHERE prepaga = 1
  AND contra = '29422106'
  AND factu_perio = '20260901'
  AND baja_fecha IS NULL
ORDER BY fecha DESC;
```

Y confirmar si arrastró intereses viejos:

```sql
SELECT *
FROM prefacturas
WHERE prepaga = 1
  AND contra = '29422106'
  AND factu_perio = '20260901'
  AND baja_fecha IS NULL
ORDER BY apli_perio, factu_concep;
```

---

### [18/08/2026 20:42:44] Valeria Saravia

no veo intereses viejos

---

### [18/08/2026 20:43:37] Valeria Saravia

no veo intereses, tampoco veo nada generado en la cta cte

---

### [18/08/2026 20:43:53] Agente

Sí, Vale: con esa evidencia **la prueba con Ponce no reprodujo el problema del TK 6040**.

Lo que se ve generado es:

```text
DI X000102352485
Período: 09/2026
Total/exento: 101.401,00
Conceptos: CUO y FAC
Sin conceptos INT
```

Además, las prefacturas activas son de `07/2026`, `08/2026` y `09/2026`, no de períodos viejos tipo `2021`. Por eso no aparecen intereses históricos.

Sobre que no lo veas en cuenta corriente: el comprobante sí está en `comprobantes`, pero aparece con `impre_fecha = NULL`. Puede que la pantalla de cuenta corriente esté filtrando solo comprobantes impresos/emitidos o que todavía falte el paso de impresión/marcado posterior. Para validar por SQL:

```sql
SELECT *
FROM comprobantes
WHERE prepaga = 1
  AND contra = '29422106'
  AND compro_nro = 'X000102352485';
```

Y buscarlo sin filtro de período:

```sql
SELECT *
FROM comprobantes
WHERE prepaga = 1
  AND contra = '29422106'
ORDER BY fecha DESC;
```

**Conclusión:** Ponce no sirve para demostrar el arrastre de intereses. Para seguir, conviene probar con un afiliado que ya tenga deuda/intereses viejos pendientes más parecidos al caso original, o directamente usar como evidencia el caso ya existente `41803033 - OLIVARES`, que sí tenía DI masiva con `INT` de períodos viejos.

---

### [18/08/2026 20:46:43] Valeria Saravia

quiero probar con un afiliado que ya tenga deuda/intereses viejos pendientes más parecidos al caso original, me podes pasar ejemplos? xq puedo volver a pisar la base de datos para volver a facturar el mes 9 desde cero

---

### [18/08/2026 20:50:47] Agente

Sí, Vale. Para volver a pisar la base y refacturar `09/2026` desde cero, usaría estos candidatos, en este orden:

**1. Mejor candidato: `38614013 / 00 - DURE, MAGALI`**

Es el más parecido al caso original porque tiene reingreso reciente:

```text
Reingreso: 01/07/2026
Factura masiva 09/2026: DI X000102336456
Proceso: FM
Intereses viejos generados:
- 08/2022: $7,12
- 10/2022: $10.584,68
Total INT: $10.591,80
```

**2. Segundo candidato: `95973490 / 00 - VARGAS JAIMES, KAREN LOREDANA`**

Tiene más variedad de períodos viejos:

```text
Reingreso: 01/07/2025
Factura masiva 09/2026: DI X000102347507
Proceso: FM
Intereses viejos: 03/2022, 07/2022, 01/2023, 06/2023, 07/2023, 08/2023
Total INT: $13.806,30
```

**3. Tercer candidato: `31987177 / 00 - SAMMAURO, DAMIAN EZEQUIEL`**

Es fuerte por importe, aunque el reingreso no es tan cercano como el caso TK 6040:

```text
Reingreso: 01/09/2025
Factura masiva 09/2026: DI X000102334948
Proceso: FM
Intereses viejos: 05/2024, 06/2024, 08/2024
Total INT: $338.820,94
```

Para mí, si vas a restaurar la copia y repetir la facturación de septiembre, probá primero con `38614013`. Es el más defendible para el TK 6040 porque comparte el patrón clave: reingreso `01/07/2026` + facturación masiva posterior + intereses de períodos anteriores al reingreso.

Después de refacturar, validá así:

```sql
SELECT
    c.contra,
    c.compro_tipo,
    c.compro_nro,
    c.factu_perio,
    c.proceso_origen,
    ci.factu_concep,
    ci.apli_perio,
    ci.valor,
    ci.saldo
FROM comprobantes c
JOIN compro_items ci
    ON  ci.ente = c.ente
    AND ci.compro_tipo = c.compro_tipo
    AND ci.compro_nro = c.compro_nro
    AND ci.baja_fecha IS NULL
WHERE c.prepaga = 1
  AND c.contra = '38614013'
  AND c.factu_perio = '20260901'
  AND c.baja_fecha IS NULL
ORDER BY ci.apli_perio, ci.factu_concep;
```

Dato importante: en estos casos los `INT` los vi en `compro_items`, no necesariamente en `prefacturas`. Por eso para confirmar el resultado miraría directamente el comprobante generado y sus ítems.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
