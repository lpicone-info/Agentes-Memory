# 2026-08-04

- Usuario Cristian / cratto trabaja por Google Chat directo con Stan (Agente03). Prefiere respuestas técnicas, directas, orientadas a PowerBuilder 2022, T-SQL, Redmine y SVN.
- Se registró el profile de BD `ROISA_Prod_Test` en `DB_PROFILE.md` y la conexión fue validada OK. Motor MS SQL, host `172.16.10.115`, puerto `1433`, base `ROISA_Prod_Test`, usuario `sa`. No guardar ni repetir password en memoria.
- Ticket Redmine usado: `26993`. Rama SVN usada: `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`, working copy local `svn/wguise-Infomedical-trunk`, revisión observada `84123`.
- Para ROISA se corrigieron `exe/Source Infomedical/d_factura_roisa.srd` y `backend/procedures/afs_compro_impresion_roisa.sql`: subtotal/TOTAL FACTURA e importe en letras reconstruyen total del comprobante con `impor1 + creditos` para que el saldo a favor no afecte el total impreso. Validado con casos `B007000020517` y `B007000017622` en `ROISA_Prod_Test`. Objetos copiados y validados byte a byte en `/mnt/Agentes/cratto/RM26993/ObjetosRoisa`.
- Para Igualdad Salud/Caseros se corrigieron `exe/Source Infomedical/d_factura_caseros.srd` y `backend/procedures/afs_compro_impresion_caseros.sql`: subtotal/TOTAL FACTURA con `impor1 + creditos`, primer vencimiento con `impor1 + deuda1 - creditos` con piso cero, código de barras solo si neto primer vencimiento > 0, e importe en letras alineado al total del comprobante. Objetos copiados y validados byte a byte en `/mnt/Agentes/cratto/RM26993/ObjetosIgualdadSalud`.
- Patrones de comparación útiles para estos casos de factura: `d_factura_femesa.srd`, `d_factura_ospania.srd`, `d_factura_celer.srd`, `d_factura_britanico.srd`. FEMESA considera vencimiento con `deuda1 - creditos + impor1`; otros formatos usan `saldo_anterior1` para ocultar o ajustar importes cuando el saldo a favor cubre la factura.
- Validaciones realizadas en ambos trabajos: revisión de `svn diff`, preservación de encoding/BOM/EOL (SRD UTF-8 con BOM + CRLF; SQL ISO-8859-1 + CRLF), y `svn status` con solo archivos modificados esperados. No se ejecutó `svn commit`.

## 2026-08-04 - Durable Memory Flush

- Walter/wguise requested generation of Redmine ticket analysis for ticket 26993 using prompt SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/01_bug.md`, source document `\\infomed03n\Agentes\wguise\R26993\R26993_V2.docx`, output folder `\\infomed03n\Agentes\wguise\R26993\`, file name `ticket_26993_01_bug.md`, productive SVN branch, no DB profile. Result delivered at `/mnt/Agentes/wguise/R26993/ticket_26993_01_bug.md`. Key finding: fiscal total and applied saldo/vencimiento handling are separated incorrectly in `afi_compro_impresion.sql`/DataWindows; no DB validation because database input was empty.
- Walter asked about PowerBuilder `TriggerEvent('ue_aceptar')` and why local struct `caren.dw_titu` seemed to lose value after assignment from `idw_titu`. Explained that `TriggerEvent` is synchronous and does not destroy locals; `caren.dw_titu` is a reference to the same DataWindow object, so resets/retrieves/reassignments of `idw_titu` affect what `caren.dw_titu` sees.
- Productive SVN review for `wp_solici` event `ue_carencias`: evidence found in `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/wp_solici.srw`. `ue_carencias` assigns `caren.dw_titu = idw_titu` around line 5344 and triggers `ue_aceptar` around line 5356. `ue_aceptar` calls `ue_grabar` around line 27400; `ue_grabar`, even with `ib_cp = TRUE`, still triggers `ue_cambia_moti` around line 5272. Comment indicates reset of window objects. `IF NOT ib_cp` only avoids `ue_extras`; it does not avoid `ue_cambia_moti`. Other flows via `ue_consulta` explicitly retrieve `dw_soli` and `idw_titu` around lines 740 and 795. Diagnosis: `caren.dw_titu` keeps the reference, but the referenced `idw_titu` is recargado/limpiado by aceptar/grabar/cambiar motivo. Suggested correction: condition/protect `ue_cambia_moti` when coming from carencias/preexistencias with `ib_cp = TRUE`, or move cleanup until after `capre.of_caren(caren)`.
