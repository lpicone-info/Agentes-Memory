# Memoria de conversación — Marcelo Viciconti

## Datos

- **Fecha:** 21/08/2026
- **Usuario:** Marcelo Viciconti
- **ID Google Chat:** `users/103434398249633426446`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 12
- **Inicio:** 12:21:20
- **Fin:** 13:25:12

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/25hnskaaaae`

## Archivos revisados

- `b17f2f96-168c-4d52-9c5f-638d5f6dd440.jsonl`

---

## Transcripción completa

### [21/08/2026 12:21:20] Marcelo Viciconti

En la subcarpeta r29098 de mi carpeta compartida con Ud, lea el documento .doc que alli se encuentra.
Refresque sus repositorios, sobre todo en la rama
https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R29098/backend, revision 84396.
Con esa info, y prestando especial atencion a las tareas de Bases de Datos, analice si el trabajo llevado a cabo en los objetos commiteados en la rama r29089 se alinean a los pedido en la especificacion funcional.
Ademas, genere casos de pruebas en SQL para verificar los resultados.

---

### [21/08/2026 12:30:21] Agente

Marcelo, analicé el `.doc` de `r29098`, refresqué las working copies SVN registradas y revisé la revisión `84396` de:

`/branches/R29098/backend`

La revisión contiene estos objetos BD commiteados:

```text
A function/afn_excluye_Recal_Tari.sql
M procedures/afi_prefac_prefacturar.sql
M procedures/afs_prefac_cuota_categoria.sql
M procedures/afs_valida_valor_cuota.sql
M procedures/afs_valida_valor_descuento.sql
M procedures/afs_valida_valor_facargo.sql
M procedures/afs_valida_valor_recargo.sql
M procedures/afu_tarifas.sql
M tables/tarifas.sql
```

**Conclusión**
El alcance de objetos coincide bastante bien con lo pedido en la especificación funcional para BD. Pero no lo considero alineado al 100%.

**Hallazgos**
1. `afs_valida_valor_cuota.sql`: para tarifas por cápita, el código sigue aplicando `@inte_tope = @inte_tope_calc` cuando el parámetro general está en `S`, sin validar `excl_calc_edadT`. Esto contradice el pedido funcional para “Tarifas por Cápita”, donde la exclusión también debe omitir el recálculo.

2. `afn_excluye_Recal_Tari.sql`: la función no implementa lo sugerido por la especificación: no recibe `@tipo` ni `@etaria`, no filtra `baja_fecha IS NULL`, y no resuelve por máxima vigencia. En la tabla real `TARIFAS` no aparece `vigen_desde`, por lo que esa parte del documento parece no ajustarse exactamente al modelo físico, pero sí falta al menos contemplar `baja_fecha`.

3. `afn_excluye_Recal_Tari.sql`: si no encuentra tarifa devuelve `NULL`. Como los SP validan `@var_excl_calc_edad = 0`, un `NULL` puede impedir el recálculo aunque funcionalmente el default esperado es `N`. Recomiendo que la función devuelva `0` por defecto.

4. `afu_tarifas.sql` y `tables/tarifas.sql`: estos sí están alineados con el pedido central: agregan `TARIFAS.excl_calc_edadT char(1) NOT NULL DEFAULT('N')` y el parámetro `@excl_calc_edadT char(1) = 'N'`, persistiéndolo en insert/update.

**Casos SQL sugeridos**
No los ejecuté contra base porque no indicaste profile. Como casos mínimos:

```sql
-- Caso 1: estructura de columna
SELECT  c.name,
        t.name AS tipo,
        c.max_length,
        c.is_nullable,
        dc.definition AS default_definition
FROM    sys.columns c
JOIN    sys.types t ON t.user_type_id = c.user_type_id
LEFT JOIN sys.default_constraints dc ON dc.object_id = c.default_object_id
WHERE   c.object_id = OBJECT_ID('dbo.TARIFAS')
AND     c.name = 'excl_calc_edadT'

-- Caso 2: parametro nuevo en afu_tarifas
SELECT  p.name,
        TYPE_NAME(p.user_type_id) AS tipo,
        p.max_length,
        p.has_default_value
FROM    sys.parameters p
WHERE   p.object_id = OBJECT_ID('dbo.afu_tarifas')
AND     p.name = '@excl_calc_edadT'

-- Caso 3: funcion debe devolver 0/1 para una tarifa vigente
DECLARE @prepaga smallint = 1
DECLARE @plan_codi char(10) = '500'
DECLARE @tari char(3) = 'NT5'

SELECT  excl_calc_edadT,
        dbo.afn_excluye_Recal_Tari(@prepaga, @plan_codi, @tari) AS resultado_funcion
FROM    dbo.TARIFAS
WHERE   prepaga = @prepaga
AND     plan_codi = @plan_codi
AND     tari = @tari

-- Caso 4: detectar riesgo de funcion devolviendo NULL
SELECT  dbo.afn_excluye_Recal_Tari(9999, 'NO_EXISTE', 'XXX') AS resultado_funcion
-- Esperado funcional recomendado: 0
-- Riesgo actual: NULL

-- Caso 5: validar que los SP impactados referencian la funcion
SELECT  OBJECT_NAME(sm.object_id) AS objeto
FROM    sys.sql_modules sm
WHERE   sm.definition LIKE '%afn_excluye_Recal_Tari%'
AND     OBJECT_NAME(sm.object_id) IN (
          'afi_prefac_prefacturar',
          'afs_prefac_cuota_categoria',
          'afs_valida_valor_cuota',
          'afs_valida_valor_facargo',
          'afs_valida_valor_recargo',
          'afs_valida_valor_descuento'
        )
ORDER BY objeto
```

Validación ejecutada: lectura del `.doc`, refresh SVN de repositorios registrados a revisión `84396`, `svn log -v -r 84396` sobre `R29098/backend`, extracción de SQL con `svn cat`, y revisión textual de los objetos commiteados.

---

### [21/08/2026 12:40:27] Marcelo Viciconti

Use la base de datos SMC_Desarrollo

---

### [21/08/2026 12:40:53] Agente

Marcelo, `SMC_Desarrollo` no está registrado en `DB_PROFILE.md`.

Profiles SMC disponibles:
`SMC_PROD_TEST` -> `SMC_Prod_Test`  
`SMC_PROX_VER` -> `SMC_Prox_Ver`

Para ejecutar o validar casos SQL necesito que me indiques el profile a usar, o que autorices explícitamente usar un profile existente contra la base `SMC_Desarrollo`.

---

### [21/08/2026 12:43:10] Marcelo Viciconti

Le autorizo a usar SMC_Desarrollo con las mismas credenciales que SMC_Prod_Test. Sino digame como dar la autorizacion.

---

### [21/08/2026 12:44:55] Agente

Marcelo, autorización suficiente. Me conecté a `SMC_Desarrollo` usando las credenciales de `SMC_PROD_TEST` y validé contra `SQL2019TEST`.

**Resultado**
La base tiene instalados los objetos de `r29098`, pero confirmo dos observaciones técnicas:

1. `TARIFAS.excl_calc_edadT` existe como `char(1) NOT NULL DEFAULT('N')`.
2. `afn_excluye_Recal_Tari` existe, pero devuelve `NULL` si no encuentra la tarifa. Probé:
   `dbo.afn_excluye_Recal_Tari(32767, 'NO_EXISTE', 'XXX')` -> `NULL`.
3. En `SMC_Desarrollo` no hay tarifas con exclusión activa todavía:
   `C/N = 24`, `F/N = 196`, ningún registro con `S`.
4. Confirmé el gap en `afs_valida_valor_cuota`: en el tramo de cápita, si el parámetro general está en `S`, usa `@inte_tope_calc` sin consultar la exclusión de tarifa. Eso no queda alineado con el pedido funcional para Tarifas por Cápita.

Casos SQL de validación, solo lectura:

```sql
SET NOCOUNT ON

SELECT  c.name,
        t.name AS tipo,
        c.max_length,
        c.is_nullable,
        dc.definition AS default_definition
FROM    sys.columns c
JOIN    sys.types t ON t.user_type_id = c.user_type_id
LEFT JOIN sys.default_constraints dc ON dc.object_id = c.default_object_id
WHERE   c.object_id = OBJECT_ID('dbo.TARIFAS')
AND     c.name = 'excl_calc_edadT'

SELECT  tipo,
        excl_calc_edadT,
        COUNT(*) AS cantidad
FROM    dbo.TARIFAS
GROUP BY tipo, excl_calc_edadT
ORDER BY tipo, excl_calc_edadT

SELECT  missing_tariff_result =
        dbo.afn_excluye_Recal_Tari(32767, 'NO_EXISTE', 'XXX')

SELECT  o.name,
        CASE WHEN sm.definition LIKE '%afn_excluye_Recal_Tari%' THEN 1 ELSE 0 END AS usa_funcion,
        CASE WHEN sm.definition LIKE '%excl_calc_edad%' THEN 1 ELSE 0 END AS usa_marca
FROM    sys.objects o
JOIN    sys.sql_modules sm ON sm.object_id = o.object_id
WHERE   o.name IN (
          'afi_prefac_prefacturar',
          'afs_prefac_cuota_categoria',
          'afs_valida_valor_cuota',
          'afs_valida_valor_facargo',
          'afs_valida_valor_recargo',
          'afs_valida_valor_descuento',
          'afu_tarifas'
        )
ORDER BY o.name

SELECT  OBJECT_DEFINITION(OBJECT_ID('dbo.afs_valida_valor_cuota')) AS definicion
WHERE   OBJECT_DEFINITION(OBJECT_ID('dbo.afs_valida_valor_cuota')) LIKE '%IF @var_parama_edad_tope_calculada = ''S''%SET @inte_tope = @inte_tope_calc%'
```

Conclusión final: el commit cubre el grueso del requerimiento para estructura familiar, prefacturación y conceptos relacionados, pero falta ajustar el flujo de cápita en `afs_valida_valor_cuota` y conviene corregir `afn_excluye_Recal_Tari` para devolver `0` ante tarifa no encontrada.

---

### [21/08/2026 13:02:30] Marcelo Viciconti

1) Si, el campo es char, pero la funcion devuelve bit (S=1, N=0, NULL=0)
2) Ahi modifique la funcion para que si no existe registro devuelva 0 (o sea, que no excluya), aunque estoy seguro que siempre deberia ingresar valores validos.
3) Le cambio el setting a alguna tarifa y le informo?
4) Puede ser que esto sea solo para los tipo de tarifas 'F' solamente? Donde nombra capitados?

---

### [21/08/2026 13:03:09] Agente

Marcelo:

1. Correcto. Validé en `SMC_Desarrollo`: la función ahora devuelve `0` cuando no encuentra registro. Ese punto queda corregido.

2. Coincido con su criterio: aunque normalmente deberían entrar valores válidos, defensivamente está bien que `NULL` sea tratado como `0`, o sea “no excluye”.

3. Sí. Para probar, cambie una tarifa a `excl_calc_edadT = 'S'` y pásame:
`prepaga`, `plan_codi`, `tari`, `tipo`, y si es posible un contrato/solicitud que use esa tarifa.  
Hoy en `SMC_Desarrollo` están todas en `N`: `F = 196`, `C = 24`.

4. La especificación sí nombra Cápita, pero hay que separar alcance:

- Para ABM de tarifas, el documento nombra explícitamente “Estructura Familiar y Cápita”.
- En criterios de aceptación también tiene sección “Tarifas por Cápita”, escenarios 7 a 12.
- Para prefacturación, el código y el SP `afs_prefac_cuota_categoria` operan claramente sobre `tari_tipo = 'F'`.
- En Test de Cuota, el documento menciona `afs_valida_valor_cuota`, y ese SP sí tiene rama `@tari_tipo = 'C'`.

Mi ajuste de criterio: si funcionalmente la exclusión de recálculo aplica solo al cálculo etario familiar, entonces no hay que tocar la rama `C` de `afs_valida_valor_cuota`. Pero el documento, tal como está escrito, deja abierta la aplicación a Cápita por ABM y Test de Cuota. Ahí conviene confirmar con funcional si “Cápita” solo requiere guardar/mostrar el check o si también debe afectar el cálculo.

---

### [21/08/2026 13:22:12] Marcelo Viciconti

Acabo de commitear los cambios, que quedaron en la revision 84397.
Fijese si ahora los cambios a afs_valida_valor_cuota son los corrrectos.

para los casos de prueba:


prepaga = 1
contra = '23421335'
plan_codi = 'BLANCO'
tari = 'TMP'

---

### [21/08/2026 13:24:09] Agente

Marcelo, sí: los cambios de `84397` en `afs_valida_valor_cuota` ahora están correctos para el punto que había observado.

Validé:

- Revisión `84397` modifica solo:
  - `function/afn_excluye_Recal_Tari.sql`
  - `procedures/afs_valida_valor_cuota.sql`
- La función ahora devuelve `0` si no encuentra tarifa.
- La tarifa indicada existe:
  - `prepaga = 1`
  - `plan_codi = BLANCO`
  - `tari = TMP`
  - `tipo = F`
  - `excl_calc_edadT = S`
- El contrato `23421335` tiene esa tarifa vigente con `tope = 130`.

Ejecuté `afs_valida_valor_cuota` para `20260801` y devolvió:

```text
plan_codi = BLANCO
tari_tipo = F
tari = TMP
tari_cate = 101
tope = 130
err = 0
excluye = 1
```

Además, el titular tiene edad calculada `53` y `afn_puede_cambiar_tope = 1`. O sea: si no existiera la exclusión, el flujo tendría condiciones para recalcular, pero al estar `excl_calc_edadT = S`, conserva el tope contractual `130`. Eso confirma que la exclusión prevalece correctamente.

Sobre Cápita: el ajuste también quedó técnicamente correcto. Ahora en la rama `@tari_tipo = 'C'` solo usa `@inte_tope_calc` cuando:

```sql
@var_parama_edad_tope_calculada = 'S'
AND @var_excl_calc_edad = 0
```

Con eso, si una tarifa Cápita estuviera marcada con exclusión, no recalcula el tope.

---

### [21/08/2026 13:25:08] Marcelo Viciconti

Gracias

---

### [21/08/2026 13:25:12] Agente

De nada, Marcelo.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
