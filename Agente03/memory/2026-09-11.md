# Memoria diaria - 2026-09-11

- Usuario identificado en el contexto de trabajo como Walter; agente: Stan/Agente03.
- Flujo Redmine/SVN para ticket 28918, usuario `wguise`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI`.
- Se generaron y verificaron estos documentos en `\\infomed03n\Agentes\wguise\R28918\`:
  - `ticket_28918_01_bug.md`: análisis. Defecto confirmado: `afs_soli_pendientes_cab` resuelve correctamente el usuario de solicitudes de precarga mediante `Modificacion Solicitud`, pero `afs_soli_pendientes_export` toma solo `Alta Solicitud`, por lo que el filtro por usuario descarta esas filas.
  - `ticket_28918_02_req_nuevo.md`: requerimiento. Alinear `afs_soli_pendientes_export` con la lógica de `afs_soli_pendientes_cab`, contemplando solicitudes originadas en precarga.
  - `ticket_28918_03_spec.md`: especificación. Modificar únicamente el SP `afs_soli_pendientes_export`; incorporar resolución de usuario para precarga usando la primera `Modificación Solicitud`. Sin objetos PB ni scripts de esquema/datos. Complejidad media. Rollback: restaurar definición anterior del SP.
- Estado del flujo: especificación técnica OK, sin pendientes de alcance; lista para revisión de desarrollador y líder técnico antes de `04_dev`.
- La base consultada previamente fue `Premedic_Prod_Test`.

- Contexto Redmine 27498: tarea de asignación automática/manual de sucursal en ingreso de prestaciones; para Órdenes, la autorización con una sucursal debe autocompletar y bloquear combo, múltiples debe mostrar mensaje y habilitar combo acotado, inexistente debe informar error; mantener validaciones existentes y excluir Ajustes.
- Usuario confirmó cambios aplicados y commit propio en SVN branch `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498`; comenzó pruebas sobre Órdenes.
- Caso de prueba reportado: base `SMC_Desarrollo`, expediente `27211`, autorización `59995`; aparece `Seleccione la sucursal de la autorizacion`, pero el combo mostraba aparentemente todas las sucursales.
- Verificación de rama R27498: `wp_expe_orde.srw` llama a `uo_nv_auto_sucur_expe.of_preparar_combo_sucur_auto(dw_det, 'sucur', ll_auto)` y el DDDW nuevo `dd_auto_sucursales_x_auto.srd` existe y usa `dbo.prs_auto_sucursales_x_auto`. El objeto `d_expe_orde_item.srd` mantenía `dddw.name=dd_sucursales`, causando que el combo no quedara filtrado.
- Se dejó propuesta en `/mnt/Agentes/cratto/R27498/propuestos/exe/Source Infomedical/`: `d_expe_orde_item.srd` y `dd_auto_sucursales_x_auto.srd`. Cambio funcional único: columna `sucur` de `d_expe_orde_item.srd` pasó de `dd_sucursales` a `dd_auto_sucursales_x_auto`; se preservaron BOM UTF-8 y finales CRLF. No se modificó SVN ni se hizo commit desde la propuesta. Rama consultada en revisión `84633`.
- Profile `SOS_Desarrollo` figura registrado en `DB_PROFILE.md` según contexto previo: MS SQL, host `172.16.10.108`, puerto `1433`, base `SOS_Desarrollo`; credenciales no persistir en memoria.

- Flujo Redmine/SVN para ticket 30125, usuario `wguise`, rama `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI`; se analizó `R30125.doc` y el circuito de `wp_importar_trans` al hacer clic en `pb_help`, incluyendo clases heredadas y ventanas relacionadas.
- Informe generado/actualizado en `\\infomed03n\Agentes\wguise\R30125\ticket_30125_01_bug.md`. Diagnóstico final confirmado: truncamiento por asignar una cuenta `LONG` a una variable `INTEGER`, no un problema del valor negativo original.
- Tipos verificados en el circuito: `str_aportes_lote.cuenta`, `dw_sel.object.cuenta[1]`, `de_busaportes_lote_real.cuenta` y `dw_ing.object.cuenta[1]` son `LONG`; en `wr_busaportes_lote.ue_agregar`, la variable `ll_cuenta` era `INTEGER` y recibía `dw_ing.object.cuenta[1]`. Para cuenta `50623`, el desborde produce `-14913` (`50623 - 65536`), que luego se usa en el filtro de consulta.
- Se detectó el mismo patrón en `wr_busaportes_lote_real`, utilizado por otros circuitos. Corrección recomendada: cambiar ambas variables receptoras de cuenta a `LONG`. No se modificó código fuente, base de datos ni repositorio.
- Continuación ticket Redmine 27498 en rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498`: al corregir `d_expe_orde_item.srd` para usar el DDDW parametrizado `dd_auto_sucursales_x_auto`, apareció el error de PowerBuilder `Specify Retrieval Arguments` porque el DataWindow intentaba recuperar automáticamente un DDDW que requiere `@auto`.
- Se revisó nuevamente la rama y se dejó una propuesta en `/mnt/Agentes/cratto/R27498/propuestos`: en `d_expe_orde_item.srd` se agregó `dddw.autoretrieve=no`, dejando que `uo_nv_auto_sucur_expe.of_preparar_combo_sucur_auto(...)` ejecute explícitamente `ldwc_sucur.Retrieve(al_auto)`. `dd_auto_sucursales_x_auto.srd` conserva el argumento `@auto = :auto` y el procedimiento `dbo.prs_auto_sucursales_x_auto`.
- La propuesta preservó BOM UTF-8 y finales CRLF; no se modificó ni se confirmó nada en SVN. Caso de prueba relevante: base `SMC_Desarrollo`, expediente `27211`, autorización `57351`, con sucursales esperadas `1 - CASEROS` y `17 - WEBPOSSUC`.

- Flujo Redmine/SVN para ticket 30205, usuario `wguise`, rama `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI`; se analizó `R30205.doc` y la ventana PowerBuilder asociada al botón Generar usando `IgualdadSalud_Prod_Test`.
- Informe generado en `\\infomed03n\Agentes\wguise\R30205\ticket_30205_01_bug.md`. Diagnóstico de performance: procesamiento fila por fila en PowerBuilder, `Retrieve` de DataWindows dentro de ciclos, dos `COMMIT` por credencial, relectura de `lds_creden_impre`, generación individual de archivos `.psr`, espera activa de hasta 10 segundos por archivo, varias operaciones SQL por mail y registro individual por credencial enviada.
- En la base `IgualdadSalud_Prod_Test`, la configuración activa utiliza `d_cred_prn_smc`, que ejecuta `afs_datos_creden`. No se detectaron cursores ni ciclos `WHILE` en los procedimientos de mail; el costo se acumula por la cantidad de invocaciones desde PowerBuilder. No se modificó código, objetos ni datos de negocio.


## Registro de continuidad - 2026-09-11 13:30

### Tema: Análisis Redmine 28918 para s.altamiranda

- Solicitud o contexto: Se solicitó determinar los cambios a realizar y generar un set de pruebas para el ticket Redmine `28918`, usando la base `smc_desarrollo`, la rama SVN `wguise-V25.08.001`, el prompt `01_bug.md` y la documentación de atención al cliente disponible en `\\unidad-de-red\s.altamiranda\R28918\R28918.docx`.
- Respuesta o acciones del agente: Se revisaron el ticket, la documentación, la rama SVN y la lógica de base de datos. Se compararon los resultados de `afs_soli_pendientes_cab` y `afs_soli_pendientes_export`, y se verificó la revisión SVN `84590` junto con el prompt en revisión `84634`.
- Resultado: El cambio queda acotado a `backend/procedures/afs_soli_pendientes_export.sql`: debe alinearse la asignación del usuario de solicitudes de precarga con `afs_soli_pendientes_cab`, utilizando la primera actividad `Modificacion Solicitud`. No se requieren cambios iniciales en PowerBuilder ni en DataWindows. Se generó el informe `ticket_28918_01_bug.md` con causa raíz, evidencia, reproducción, criterios de aceptación y 18 casos de prueba en `/mnt/Agentes/s.altamiranda/R28918/`.
- Estado: Resuelto. La working copy SVN quedó sin modificaciones y se verificó el archivo entregable.


## Registro de continuidad - 2026-09-11 14:03

### Tema: Casos de prueba Redmine 27498 para Honorarios en SMC_Desarrollo

- Se solicitaron casos para validar las solapas de Honorarios: `Prácticas`, `Cirugías` y `Cons. / Práct. c/demandante`, cubriendo autorización asociada a una sola sucursal y a varias sucursales.
- En `SMC_Desarrollo` no se encontraron seis casos reales vigentes.
- Casos disponibles para la solapa `Prácticas`:
  - Una sucursal: expediente `1607`, autorización `1329374`, sucursal esperada `17 - WEBPOSSUC`.
  - Varias sucursales: expediente `1607`, autorización `15033`, sucursales esperadas `1 - CASEROS`, `2 - HURLINGHAM`, `8 - LUZ Y FUERZA`, `14 - MAR DEL PLATA`, `16 - ISIDRO CASANOVA`, `20 - MESA OPERATIVA`.
- No hay registros vigentes `expe_honorarios` tipo `G` ni `O`; tampoco expedientes tipo `H` vigentes con `ciru_canti > 0` o `deman_canti > 0`. Por lo tanto, no se pudieron identificar casos reales para `Cirugías` ni `Cons. / Práct. c/demandante`.
- Queries de control utilizadas:
  ```sql
  SELECT tipo, COUNT(*) filas
  FROM dbo.expe_honorarios
  WHERE baja_fecha IS NULL
  GROUP BY tipo
  ORDER BY tipo;

  SELECT COUNT(*) total,
         SUM(CASE WHEN ISNULL(ciru_canti,0) > 0 THEN 1 ELSE 0 END) con_ciru,
         SUM(CASE WHEN ISNULL(deman_canti,0) > 0 THEN 1 ELSE 0 END) con_deman
  FROM dbo.expedientes
  WHERE baja_fecha IS NULL
    AND tipo = 'H';
  ```


## Registro de continuidad - 2026-09-11 15:25

### Tema: Redmine 27498 - Correccion propuesta para Honorarios en SMC_Desarrollo

- Usuario reporto en Honorarios, expediente `1607` de `SMC_Desarrollo`, solapa `Practicas`: en estado inicial el combo `Sucursal` aparecia desbloqueado y poblado; el caso de una sucursal funcionaba OK; en autorizacion con varias sucursales se mostraba el mensaje correcto pero el combo quedaba poblado con muchas sucursales en lugar de quedar acotado a la autorizacion; autorizacion inexistente OK.
- Se reviso la rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498` y se preparo propuesta en `/mnt/Agentes/cratto/R27498/propuestos`.
- Diagnostico: faltaba inicializar/bloquear la sucursal al abrir las solapas de Honorarios y limpiar/reiniciar el DDDW antes de recuperarlo con la autorizacion, para evitar datos previos o generales en el combo.
- Objetos propuestos:
  - `/mnt/Agentes/cratto/R27498/propuestos/exe/Source Infomedical/wp_expe_hono.srw`
  - `/mnt/Agentes/cratto/R27498/propuestos/exe/Source Infomedical/uo_nv_auto_sucur_expe.sru`
  - `/mnt/Agentes/cratto/R27498/propuestos/exe/Source Infomedical/d_expe_hono_prac.srd`
  - `/mnt/Agentes/cratto/R27498/propuestos/exe/Source Infomedical/d_expe_hono_ciru.srd`
  - `/mnt/Agentes/cratto/R27498/propuestos/exe/Source Infomedical/dd_auto_sucursales_x_auto.srd`
  - Informe: `/mnt/Agentes/cratto/R27498/propuestos/R27498_honorarios_sucursal_correccion.md`
- Cambio funcional resumido: en `wp_expe_hono.srw`, al abrir `Practicas`, `Cirugias` u `Ordenes`, dejar `sucur` en `NULL`, combo bloqueado y DDDW reiniciado; en `uo_nv_auto_sucur_expe.sru`, reiniciar el DDDW antes de ejecutar el `Retrieve` parametrizado por autorizacion.
- Se conservaron los DataWindows de Practicas y Cirugias apuntando a `dd_auto_sucursales_x_auto`. Se validaron encoding y finales de linea originales. No se modifico SVN, no se hizo commit ni cambios en base.


## Registro de continuidad - 2026-09-11 17:32

### Tema: Guardado automatico de items en ingreso de expedientes de internaciones

- Solicitud o contexto: Se consulto si al cargar items en la solapa Items el sistema persiste los registros automaticamente, antes de confirmar el expediente, y si existe alguna regla o configuracion que controle ese comportamiento.
- Respuesta o acciones del agente: Se valido la rama productiva (`trunk`) sobre `wp_expe_sana.srw` y los procedimientos SQL asociados. Se revisaron el boton `cb_grab`, el evento `ue_agregar_det`, los eventos de cambio de solapa y la confirmacion general.
- Resultado: El guardado automatico esta implementado en la ventana, no en una regla de configuracion. El boton Aceptar de Items dispara `iw_ventana.triggerevent('ue_agregar_det')`; ese evento recorre filas modificadas de `dw_p`, `dw_e` y `dw_d`, ejecuta `pri_expe_inter_prestaciones`, `pru_expe_inter_prestaciones` y `prd_expe_inter_prestaciones` segun corresponda, y realiza `COMMIT`. Al cambiar desde Items a la solapa principal, `selectionchanging` puede preguntar `Graba cambios efectuados?` y, ante respuesta afirmativa, disparar el guardado. La confirmacion general tambien invoca previamente `ue_agregar`.
- Evidencia relevante: `wp_expe_sana.srw` contiene la logica de grabacion de prestaciones; `pri_expe_inter_prestaciones` inserta en `expe_inter_prestaciones`, `pru_expe_inter_prestaciones` actualiza registros y `prd_expe_inter_prestaciones` registra la baja mediante `baja_fecha`.
- Reglas o base de datos: No se encontro una regla de `zz_sistema_reg` que controle el momento de persistencia ni triggers SQL directos sobre `expe_inter_prestaciones` que expliquen el comportamiento.
- Estado: Resuelto. El comportamiento observado es intencional desde el codigo de la ventana: la carga de Items funciona como una operacion independiente y puede quedar persistida aunque luego no se confirme el expediente general o se cierre la ventana.


## Registro de continuidad - 2026-09-11 17:42

### Tema: Redmine 30244 - informe BUG basado exclusivamente en historias 2 y 3

- Se solicitó generar/actualizar el informe `ticket_30244_01_bug.md` usando el prompt SVN `01_bug.md`, ticket `30244`, usuario `facevedo`, base `MutualRioIV_Prod`, rama `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk` y los documentos `R30244_AnalisisAgente.docx` y `R30244.docx`.
- Restricción explícita: basarse únicamente en la historia 2 y la historia 3 del documento de análisis y diseño.
- Resultado: informe generado en `\\infomed03n\\Agentes\\facevedo\\R30244\\ticket_30244_01_bug.md` (equivalente local reportado como `/mnt/Agentes/facevedo/R30244/ticket_30244_01_bug.md`). Se validó que no contiene referencias a historia 1, datos de `29753`, `753858` ni ítems ajenos; UTF-8 sin BOM y finales de línea uniformes.
- No se modificaron el trunk ni la base de datos.

### Profile de base de datos

- Se confirmó que existe y funciona el profile `MutualRioIV_Prod` en `DB_PROFILE.md`, MS SQL sobre `172.16.10.108:1433`, base `MutualRioIV_Prod`, usuario `sa`.
- Previamente se eliminó el equivalente `mutualRioIV_prod` mediante doble confirmación y se registró nuevamente con el nombre exacto `MutualRioIV_Prod`; la conexión fue validada con `SELECT DB_NAME()`.
- No persistir la contraseña en memoria.


## Registro de continuidad - 2026-09-11 18:31

### Tema: Validación Redmine 30125 sobre V25.08.002_PROVI

- Solicitud o contexto: Se solicitó validar el ticket Redmine `30125` con el prompt SVN `05_validar.md`, las revisiones `84645` y `84646`, el documento `R30125.doc`, el análisis `ticket_30125_01_bug.md`, la especificación `R30125_solucion.docx` y salida `ticket_30125_05_validar.md`. En el pedido se indicó el usuario `s.altamiaranda`, rutas bajo `\\unidad-de-red\s.altamiaranda\R30125\` y la base `SQLSRV01\\MutualRioIV_Desarrollo`.
- Resultado: La validación quedó bloqueada antes de iniciar porque el usuario indicado contiene una discrepancia (`s.altamiaranda` frente a la credencial registrada `s.altamiranda`) y las rutas con `s.altamiaranda` no existen. La carpeta local disponible es `/mnt/Agentes/s.altamiranda/R30125/`.
- Estado del repositorio: La working copy local de `V25.08.002_PROVI` se encuentra en revisión `84644`, mientras que se solicitaron validar las revisiones remotas `84645` y `84646`. Se dejó pendiente autorización explícita para consultar esas revisiones sin ejecutar `svn update` sobre la working copy.
- Pendiente: Confirmar el usuario correcto, la carpeta local a utilizar y si se deben consultar las revisiones remotas sin actualizar la working copy.
