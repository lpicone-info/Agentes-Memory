# Memoria de conversación — Marcelo Viciconti

## Datos

- **Fecha:** 28/08/2026
- **Usuario:** Marcelo Viciconti
- **ID Google Chat:** `users/103434398249633426446`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 8
- **Inicio:** 12:01:07
- **Fin:** 17:24:39

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/25hnskaaaae`

## Archivos revisados

- `09f960a1-a191-418d-a156-b42821203272.jsonl`

---

## Transcripción completa

### [28/08/2026 12:01:07] Marcelo Viciconti

El siguiente es un requerimiento que la gerencia está planteando y necesito que haga un analisis de los objetos de bases de datos que sufriran ofuscacion en sus datos:

Con el objetivo de dar cumplimiento a la Ley N° 25.326 de Protección de Datos Personales (Hábeas Data) y minimizar los riesgos de seguridad ante eventuales filtraciones, les informamos que implementaremos un Protocolo de Enmascaramiento y Seguridad sobre las copias de bases de datos que recibimos mensualmente de nuestros clientes (Obras Sociales y Prepagas) para los entornos de desarrollo y testing.
A continuación, les detallo el plan de acción, el alcance del tratamiento de datos y las excepciones definidas por necesidades de negocio:

1. ¿Qué vamos a hacer y con qué propósito?

Aplicaremos un script de ofuscación estándar sobre toda copia de base de datos antes de ser restaurada en nuestros entornos de desarrollo/QA.

• Propósito: Proteger la privacidad de los afiliados (eliminando datos identificables directos y de salud) y garantizar un entorno seguro de trabajo sin alterar la lógica del sistema.

2. Matriz de tratamiento de datos de los Afiliados
• Datos que SE MODIFICAN (Ofuscados / Ficticios): en todas las tablas donde se encuentren
Apellido
Nombre
Calle
Teléfono
Celular
Email

• Datos que NO SE MODIFICAN (Se conservan reales) y sus motivos:

CUIT / CUIL / DNI
Motivo: Es imprescindible mantener los identificadores reales para validar los archivos y padrones que ingresan desde las interfaces de ARCA/SUPERINTENDENCIA DE SERVICIOS DE SALUD/ANSES/IIBB y otras entidades externas sin romper las pruebas de integración.

Localidad / Código Postal: No se modificarán.
Motivo: Este dato es crítico para el correcto cálculo impositivo de Ingresos Brutos (IIBB) y alícuotas por jurisdicción territorial.

Fecha de Nacimiento: No se modificarán.
Motivo: Se requiere mantener el rango etario exacto para que los cálculos de cuotas, planes y ajustes de facturación por grupo familiar coincidan 100% con los valores esperados.

Si no hay objeciones sobre lo propuesto,
1. Proyectos especiales y Proyecto estándar definirá en qué tablas – campos se encuentran los datos sensibles para que se generé el script enmascaramiento

2. Ejecutar pruebas sobre una base de datos que se utilice de forma continua en atención al cliente, diseño y desarrollo para ( sobre la que tengamos la posibilidad de, en caso de ser necesario, restaurarla)

3. Finalizada la prueba. Se debe incorporar como parte del proceso de restauración de bases de datos, la ejecución de: “script enmascaramiento” de los datos definidos

Genere un plan listando los objetos a UPDATEear que cumplan con este requisito y la forma de hacerlo.
Por ejemplo: UPDATE AFILIADOS SET ape = 'contrato ' + contra, nombre = 'afiliado ' + inte;

---

### [28/08/2026 12:02:29] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [28/08/2026 12:03:55] Marcelo Viciconti

process el chat que acabo de darle

---

### [28/08/2026 12:04:15] Agente

Marcelo, puedo procesarlo, pero para listar **objetos reales a UPDATEear** necesito que me indiques explícitamente dónde buscar:

- Profile de base de datos de `DB_PROFILE.md`, por ejemplo `SMC_PROD_TEST`, `Premedic_Prod_Test`, etc.
- O repositorio/rama donde estén los scripts/modelo de datos, por ejemplo `wguise-Infomedical-trunk`, `wguise-V25.08.001`, etc.

Sin ese dato no puedo inferir tablas/campos por regla operativa.

Con lo que pasaste, el alcance funcional queda así:

**Campos a ofuscar**
- Apellido
- Nombre
- Calle
- Teléfono
- Celular
- Email

**Campos a conservar**
- CUIT / CUIL / DNI
- Localidad
- Código Postal
- Fecha de nacimiento

**Plan técnico propuesto**
1. Relevar metadatos de la base:
   - Buscar columnas cuyo nombre sugiera datos sensibles: `ape`, `apellido`, `nom`, `nombre`, `calle`, `domicilio`, `telefono`, `tel`, `celular`, `mail`, `email`.
   - Excluir columnas de documentos, CUIT/CUIL, localidad, CP y fecha de nacimiento.

2. Clasificar objetos:
   - Tablas maestras de afiliados/personas.
   - Tablas históricas o auditoría con datos identificables.
   - Tablas de contactos/domicilios.
   - Tablas de titulares, integrantes, grupo familiar, adherentes o beneficiarios.
   - Tablas temporales persistentes o staging si contienen datos reales restaurados.

3. Generar matriz:
   - Tabla
   - Campo
   - Tipo de dato
   - Criterio de ofuscación
   - Riesgo funcional
   - Sentencia UPDATE propuesta

4. Definir criterio de enmascaramiento:
   - Apellido: valor ficticio derivado de identificador interno.
   - Nombre: valor ficticio derivado de identificador interno.
   - Calle: valor genérico ficticio.
   - Teléfono/Celular: número sintético fijo o derivado.
   - Email: email sintético con dominio no real.

Ejemplo conceptual:

```sql
UPDATE afiliados
SET ape = 'contrato ' + CONVERT(varchar(20), contra),
    nombre = 'afiliado ' + CONVERT(varchar(20), inte)
```

Para avanzar con el listado real necesito que me indiques el **profile de base de datos** o el **repositorio/rama** donde debo relevar los objetos.

---

### [28/08/2026 12:04:47] Marcelo Viciconti

use el trunk o la base de datos SMC_Desarrollo

---

### [28/08/2026 12:08:03] Agente

Marcelo, procesé `SMC_Desarrollo` y validé conexión. No ejecuté updates.

**Evidencia**
- Base analizada: `SMC_Desarrollo`
- Motor: SQL Server 2019
- Relevamiento por metadata: 120 tablas candidatas con columnas compatibles con datos a ofuscar.
- El refresh del trunk falló por certificado SSL no confiable del SVN, por eso el análisis real lo basé en base de datos.

**Objetos principales a UPDATEear**

```text
afiliados: ape, nombre, email
afi_domicilios: calle, tele
AFI_DOMICILIOS_INTE: calle, tele
afi_celulares: celular
afi_mails: mail
afi_resp_pagador: mail, celular
soli_afiliados: ape, nombre
soli_domicilios: calle, tele
SOLI_DOMICILIOS_INTE: calle, tele
soli_celulares: celular, anter_celular
soli_mails: mail, mail_anter
soli_resp_pagador: mail, celular
precarga_soli_afiliados: ape, nombre, Mail, cel
PRECARGA_DOMICILIOS: calle, tele
PRECARGA_SOLICITUDES: calle, tele, celular
AFILIADOS_VALIDACIONES: ape, nombre, calle, mail
afi_padron_usuarios_sss: ape_nombre, calle, tel_fijo, tel_celu, e_mail
afi_expor: ape, nombre, domicilio, telefono
afi_importaciones: ape, nombre, mail, telefono
afi_importaciones_isss: ape, nombre
afi_cuentas_impor: ape_nomb, calle
afi_aportes_desempleo: ape_nom
afi_aportes_jubilados: ape_nom
afi_verif_errores: ape, nombre
ajustes_impor: ape, nombre
exportacion_debitos: ape, nombre
isss_afiliados: ape, nombre, calle, tele
isss_afiliados_alta: ape_nom, calle, tel_par, tel_lab, ape_nom_cony, tel_par_cony, tel_lab_cony
isss_importa_novedades: ape_nom, dom_calle, dom_tele
ISSS_NOVEDADES_RNAS_EXPORT: ape, nombre, calle, tele
isss_novedades_sssalud: ape, nombre, calle, tele
isss_novedades_sssalud_items: ape, nombre
isss_respuesta_sssalud: ape_nom, calle, tele
```

**Objetos a revisar antes de incluir**
Estas tablas tienen datos personales o de contacto, pero pueden corresponder a prestadores, proveedores, usuarios internos, responsables de pago o datos operativos no necesariamente de afiliados:

```text
auto_remotas, autorizaciones, expe_ordenes, expe_farma_recetas, expe_honorarios,
expe_internaciones, expe_odon, situ_historias, pedidos, MAIL_ENVIO, MAIL_TO,
cuenta_domicilios, subcta_domicilios, subcuentas, prestadores, prestad_lugares,
farmacias, mandatarias, promotores, proveedores, puntos_atencion, sucursales,
zz_usuari, zz_usuari_api
```

**Forma recomendada**
Usar valores determinísticos por clave funcional para que las pruebas sigan siendo trazables sin exponer identidad real:

```sql
UPDATE afiliados
SET ape = 'contrato ' + CONVERT(varchar(20), contra),
  nombre = 'afiliado ' + CONVERT(varchar(20), inte),
  email = 'afi_' + CONVERT(varchar(20), contra) + '_' + CONVERT(varchar(20), inte) + '@no-real.local'
```

```sql
UPDATE afi_domicilios
SET calle = 'calle ' + CONVERT(varchar(20), contra),
  tele = '110000' + RIGHT('0000' + CONVERT(varchar(4), ABS(CHECKSUM(contra, inte)) % 10000), 4)
```

```sql
UPDATE afi_celulares
SET celular = '115000' + RIGHT('0000' + CONVERT(varchar(4), ABS(CHECKSUM(contra, inte)) % 10000), 4)
```

**Plan de ejecución**
1. Generar script por bloques: afiliados maestros, domicilios/contactos, solicitudes, precarga, padrones/interfases, exportaciones/importaciones.
2. Antes de cada bloque, guardar conteo afectado por tabla.
3. Ejecutar en una copia restaurable.
4. Validar que no cambien `CUIT/CUIL/DNI`, localidad, código postal ni fecha de nacimiento.
5. Validar procesos críticos: padrón SSS/ARCA/ANSES/IIBB, facturación por edad, cálculo territorial e interfaces.
6. Incorporar el script al proceso de restauración solo después de comparar resultados funcionales contra base sin ofuscar.

**Riesgos**
- `ape_nom`, `ape_nombre`, `nomyape` mezclan apellido y nombre en un solo campo: conviene reemplazarlos completos por un texto determinístico.
- `auto_remotas`, `autorizaciones` y `expe_*` pueden contener nombres de prestadores, no siempre afiliados.
- Tablas temporales persistentes con muchos registros, como `TEMP_AUTO_REMOTAS_PAGING_DATA`, deben revisarse antes de decidir si entran al script final.

---

### [28/08/2026 17:22:42] Marcelo Viciconti

en la ventana wp_soli_pasar_afiliados_c, en el evento ue_aceptar
se esta ejecutando el siguiente bloque que veo poco performante

 FOR li_conta = 1 TO ldst_grupo_rel.rowcount()
 IF DATETIME(TODAY(), NOW()) > ldst_grupo_rel.object.vigen_desde[li_conta] AND (DATETIME(TODAY(), NOW()) < ldst_grupo_rel.object.vigen_hasta[li_conta] OR ISNULL(ldst_grupo_rel.object.vigen_hasta[li_conta])) THEN
 IF ldst_grupo_rel.object.baja_fecha[li_conta] <= DATETIME(TODAY(), NOW()) OR ISNULL(ldst_grupo_rel.object.baja_fecha[li_conta]) THEN
 li_find = ldst_titu.find('contra = "' + ldst_grupo_rel.object.contra_rel[li_conta] + '"', 1, ldst_titu.rowcount())
 IF li_find > 0 THEN
 IF ls_text = '' THEN
 ls_text = '- ' + ldst_grupo_rel.object.contra_rel[li_conta] + ' ' + ldst_titu.object.nombre[li_find]
 ELSE
 ls_text = ls_text + '~r ' + '- ' + ldst_grupo_rel.object.contra_rel[li_conta] + ' ' + ldst_titu.object.nombre[li_find]
 END IF
 ELSE
 IF ls_text = '' THEN
 ls_text = '- ' + ldst_grupo_rel.object.contra_rel[li_conta]
 ELSE
 ls_text = ls_text + '~r ' + '- ' + ldst_grupo_rel.object.contra_rel[li_conta]
 END IF
 END IF
 END IF
 END IF
 NEXT


el retrieve que hace de ldst_titu, lo veo muy poco performante. y Despues se utiliza en el bloque de codigo que te mencione.
Tiene alguna razon de ser? Me gustaria quitarlo de PB y pasarlo a un sp.
Pero preciso entender que está haciendo.
Espero su opinion

---

### [28/08/2026 17:24:39] Agente

Marcelo, mi opinión: **sí, tiene razón funcional, pero la implementación actual no se justifica como está**.

**Qué está haciendo**
Ese bloque corre solo cuando:

- `glob.gs_reglas[118] = 'S'`
- `li_soli_moti = 3`
- Es decir, para una solicitud de baja total.

La lógica busca si el contrato que se está pasando a afiliados es **contrato principal** de otros contratos relacionados en `afi_relaciones`. Si encuentra relaciones vigentes, arma un texto para advertir:

```text
El/los Contrato/s:
- contrato nombre titular
Tiene/n una relación de Grupos de Familias con el contrato seleccionado, Desea continuar?
```

O sea: **no bloquea por sí mismo**, solo advierte y permite cancelar o continuar.

**El problema de performance**
En [wp_soli_pasar_afiliados_c.srw](/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source%20Infomedical/wp_soli_pasar_afiliados_c.srw:390), el código hace:

```powerscript
ldst_titu.dataobject = 'dd_afiliados_titulares'
ldst_titu.retrieve()
```

Ese DataWindow ejecuta:

```sql
select contra,
       ape + ' ' + nombre
from afiliados
where paren = 'T'
```

En `SMC_Desarrollo` eso trae **108.101 titulares** cada vez que se acepta la ventana, aunque después quizás no haya ninguna baja total seleccionada. Luego hace `Find()` en memoria por cada contrato relacionado.

Eso es claramente evitable.

**Dato clave**
El DataStore `ldst_grupo_rel` usa `d_afi_relacion_grp_familia_ctr_princ`, y ese DataWindow **ya trae el nombre del contrato relacionado**:

```sql
afiliados_b.ape + ',' + afiliados_b.nombre dependiente
```

Por lo tanto, para el mensaje actual no haría falta `ldst_titu` en absoluto. El bloque podría usar directamente:

```powerscript
ldst_grupo_rel.object.dependiente[li_conta]
```

**Mi recomendación**
No pasaría todo el control a SP como primera opción. Haría una corrección más chica y segura:

1. Eliminar el `ldst_titu.retrieve()` global.
2. Usar la columna `dependiente` que ya devuelve `d_afi_relacion_grp_familia_ctr_princ`.
3. Mover el filtro de vigencia/baja al SQL del DataWindow o a un SP si quieren estandarizarlo.
4. Crear SP solo si este control se reutiliza en otros puntos o si quieren centralizar la regla 118 fuera de PB.

**SP sugerido si se decide bajarlo a SQL**
Conceptualmente debería recibir `@contra` y devolver solo contratos relacionados vigentes:

```sql
SELECT ar.contra_rel,
  afi_rel.ape + ' ' + afi_rel.nombre AS nombre_titular
FROM afi_relaciones ar
JOIN afiliados afi_pri
  ON afi_pri.prepaga = ar.prepaga
  AND afi_pri.contra = ar.contra
  AND afi_pri.paren = 'T'
JOIN afiliados afi_rel
  ON afi_rel.prepaga = ar.prepaga
  AND afi_rel.contra = ar.contra_rel
  AND afi_rel.paren = 'T'
WHERE ar.contra = @contra
  AND GETDATE() > ar.vigen_desde
  AND (GETDATE() < ar.vigen_hasta OR ar.vigen_hasta IS NULL)
  AND (ar.baja_fecha <= GETDATE() OR ar.baja_fecha IS NULL)
```

**Observación funcional**
La condición de baja me llama la atención:

```powerscript
baja_fecha <= ahora OR baja_fecha IS NULL
```

Eso incluye relaciones con baja ya efectiva. Si el objetivo es “relaciones vigentes”, normalmente esperaría:

```sql
ar.baja_fecha IS NULL OR ar.baja_fecha > GETDATE()
```

No lo cambiaría sin validación funcional, porque puede ser una regla histórica intencional, pero lo marcaría como punto a revisar.

**Conclusión**
Sí: el retrieve de `ldst_titu` parece sobrar. Hoy carga más de 100 mil titulares para resolver un nombre que el otro DataWindow ya trae. La mejora más segura es sacar ese DataStore y usar `dependiente`; después, si quieren dejarlo más prolijo, encapsular la consulta vigente en un SP.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
