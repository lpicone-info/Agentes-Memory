# Memoria de conversación — Cristian Iván Ratto

## Datos

- **Fecha:** 24/08/2026
- **Usuario:** Cristian Iván Ratto
- **ID Google Chat:** `users/112625344596118312340`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 56
- **Inicio:** 09:07:30
- **Fin:** 17:27:14

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/vmclokaaaae`

## Archivos revisados

- `ba2f293e-e0d4-499a-ac72-485c36f541fa.jsonl`

---

## Transcripción completa

### [24/08/2026 09:07:30] Cristian Iván Ratto

stan

---

### [24/08/2026 09:08:36] Agente

Hola Cristian, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [24/08/2026 11:22:27] Cristian Iván Ratto

Stan comenzamos con nuevo redmine el 30003

---

### [24/08/2026 11:22:41] Agente

Cristian, para comenzar con el Redmine 30003 necesito que me indiques explícitamente:

- Repositorio o rama SVN donde analizar.
- Profile de base de datos, si aplica al caso.

Voy a usar la credencial Redmine `cratto`, según tus preferencias.

---

### [24/08/2026 11:30:35] Cristian Iván Ratto

TICKET REDMINE : [https://redmine.infomedical.com.ar/issues/30003]
USUARIO : [cratto]
BASE DE DATOS : [Ospecon_prod]
RAMA SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001]
La ruta de trabajo sera: /mnt/Agentes/cratto/RM30003

Contame que esta sucediendo con ese ticket.

---

### [24/08/2026 11:31:54] Cristian Iván Ratto

TICKET REDMINE : [https://redmine.infomedical.com.ar/issues/30003]
USUARIO : [cratto]
BASE DE DATOS : [Ospecon_prod]
RAMA SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001]

La ruta de trabajo sera: /mnt/Agentes/cratto/RM30003
Documento redmine: /mnt/Agentes/cratto/RM30003/R30003.doc

Contame que esta sucediendo con ese ticket.

---

### [24/08/2026 11:35:51] Agente

Cristian, el ticket 30003 está pidiendo un ajuste de visualización en autorizaciones: cuando el usuario transiciona una autorización puede cargar un comentario largo, pero después, al abrir la auditoría o al volver a transicionar desde otra bandeja, ese comentario queda mostrado en una grilla como una línea horizontal y obliga a desplazarse.

La causa probable está en `d_auditoria_transiciones.srd`: el campo `comentarios` se muestra con alto fijo y sin `height.autosize`, mientras el `detail` sí tiene autosize. Además hay un segundo punto: el SP `prs_audit_auto_transiciones` devuelve `comentarios = IsNull(CONVERT(VARCHAR(255), AU.comentario),'')`, aunque la tabla guarda `comentario` como `varchar(max)` y el insert recibe hasta `varchar(900)`. O sea: además del problema visual, hoy la consulta/DataWindow pueden estar limitando el texto a 255 caracteres.

Flujo detectado:
- La transición se carga en `wp_transiciones_autorizaciones.srw`, con `mle_comentario`.
- Al aceptar, guarda auditoría con `of_audit_auto_transiciones(...)`.
- Eso ejecuta `pri_audit_auto_transiciones`.
- La ventana de auditoría `wr_auto_audi.srw` muestra `d_auditoria_transiciones`.
- Ahí el comentario queda angosto/fijo y recuperado como `char(255)`.

Conclusión: el ticket no parece pedir lógica nueva de negocio, sino corregir presentación y posiblemente longitud recuperada del comentario. El ajuste debería concentrarse en `d_auditoria_transiciones.srd` y revisar `prs_audit_auto_transiciones` para no truncar a 255 si el requerimiento real es “ver completo” comentarios largos.

Validé Redmine, documento `R30003.doc`, rama `V25.08.001`, profile `ospecon_prod` y definiciones SQL. No hice cambios.

---

### [24/08/2026 11:37:54] Cristian Iván Ratto

Resuelve unicamente el autosize en esta ruta de trabajo para no tocar ramas svn
/mnt/Agentes/cratto/RM30003/d_auditoria_transiciones.srd

---

### [24/08/2026 11:38:35] Agente

Cristian, no puedo aplicar el cambio porque el archivo exacto no existe:

`/mnt/Agentes/cratto/RM30003/d_auditoria_transiciones.srd`

En esa ruta solo está `R30003.doc`. No toqué SVN ni creé archivos alternativos.

---

### [24/08/2026 11:44:02] Cristian Iván Ratto

ahi exporte el objeto, vuelve a intentar de resolver el problema con autosize:
/mnt/Agentes/cratto/RM30003/d_auditoria_transiciones.srd

---

### [24/08/2026 11:45:21] Agente

Cristian, resuelto únicamente en:

`/mnt/Agentes/cratto/RM30003/d_auditoria_transiciones.srd`

Cambio aplicado: al control `column` del campo `comentarios` le agregué `height.autosize=yes`. No toqué la rama SVN.

Validaciones:
- Archivo sigue en UTF-16 LE con BOM.
- Finales de línea CRLF preservados, sin mezcla.
- `svn status` de `wguise-V25.08.001` no muestra cambios.

---

### [24/08/2026 12:03:03] Cristian Iván Ratto

Me buscar en ospecon_desarrollo, una autorizacion en condiciones para transicionar ?

---

### [24/08/2026 12:04:33] Agente

Cristian, encontré una autorización en `Ospecon_Desarrollo` en condiciones para transicionar:

Autorización: `sucur 334 / auto 1556`  
Tipo: `P`  
Contrato: `423286`  
Integrante: `01`  
Estado: `P`  
Circuito: `19`  
Sector actual: `2 - AUDITORIA MEDICA`  
En revisión: `N`

Validé con los mismos SP del flujo:
- `prs_obtener_grupo_rechazo_autorizacion`: grupo `5`
- `prs_calcular_auto_sector_anterior`: sector anterior `1`
- `prs_obtener_transiciones_circuito`: devuelve transiciones posibles

Destinos disponibles principales: `AAPESA`, `SEF`, `PRONTO PAGO`, `OXIGENOTERAPIA`, `DIALISIS`, `ODONTOLOGIA`, `AUDITORIA MEDICA ASM`, `CONVENIOS`, `AFILIACIONES` y `Carga Inicial`.

No hice cambios en base, solo lectura.

---

### [24/08/2026 12:49:09] Cristian Iván Ratto

Stan redmine 30003 solucionado.

---

### [24/08/2026 12:49:15] Agente

Perfecto Cristian. Queda registrado en contexto: Redmine 30003 resuelto con ajuste de `autosize` en `d_auditoria_transiciones.srd`, sin tocar rama SVN.

---

### [24/08/2026 14:34:06] Cristian Iván Ratto

stan volvemos al redmine 26993

---

### [24/08/2026 14:34:59] Agente

Cristian, no encontré contexto previo confiable del Redmine 26993 en memoria/sesiones.

Para retomarlo necesito que me indiques explícitamente:
- Ticket/URL Redmine 26993.
- Usuario Redmine.
- Base de datos.
- Rama SVN o ruta de trabajo.
- Documento o carpeta asociada, si existe.

---

### [24/08/2026 14:51:42] Cristian Iván Ratto

mLevanta igual lo que tengas respecto a ese tiquet 26993

---

### [24/08/2026 14:52:14] Agente

Cristian, levanté el Redmine 26993.

Estado actual: `Desarrollo en Progreso`  
Asignado a: `Cristian Ratto`  
Versión: `25.03.006`  
Proyecto: `Presupuestos Interno`  
Módulo: `Afilmed - Facturación`  
Prioridad entregable: `Critico`  
Ticket InvGate: `3278`

Tema: impresión de comprobantes. Al primer vencimiento debe considerar débitos y créditos. El caso original reporta que una factura con saldo a favor suficiente termina mostrando importe al primer vencimiento cuando debería quedar en `0`.

Últimas notas relevantes:
- Documento funcional vigente: `https://reposrv01.infomedical.com:18553/svn/Incidentes/Incidentes/R26993/R26993_V2.docx`
- Excel de análisis: `https://reposrv01.infomedical.com:18553/svn/Incidentes/Incidentes/R26993/An%C3%A1lisis_comprobantes_V2.xlsx`
- Walter te lo pasó para verlo en trunk el `24/07/2026`.
- El `04/08/2026` dejaste análisis de casos genéricos, CELER y ROISA.
- El `12/08/2026` dejaste definición clave: no tocar SP ni cálculos ya hechos en BD, salvo ROISA, donde sí hay que tocar SP para traer `grava` desde `comprobantes`.
- El `13/08/2026` quedó pausado por RM 30192.
- El `20/08/2026` dejaste nota: tarea retomada.

No entré en código ni base.

---

### [24/08/2026 14:56:35] Cristian Iván Ratto

quiero ver el caso roisa antes y despues

---

### [24/08/2026 14:56:48] Agente

Cristian, con lo levantado del RM 26993, el caso ROISA queda así:

**Antes**
Objeto: `d_factura_roisa.srd`

- Subtotal para comprobantes `A` y `B`:
`abs(impor1 - iibb_arba - iva_total)`

- Subtotal distinto de `A` y `B`:
`abs(impor1)`

- Total factura para `A`:
`abs(impor1)`

- Total factura distinto de `A`:
`abs(impor1)`

**Después definido**
ROISA es caso especial porque hay que usar `grava` desde tabla `comprobantes`.

- Subtotal:
`grava` de `comprobantes`

- Total factura:
`grava + iva + iibb`

La definición vigente también aclara que, salvo este caso ROISA, no se deben tocar SP ni cálculos ya hechos en base. Para ROISA sí aplica tocar el SP para traer `grava`.

---

### [24/08/2026 14:57:09] Cristian Iván Ratto

y los vencimientos?

---

### [24/08/2026 14:57:17] Agente

Cristian, en las notas levantadas del RM no quedó definida una fórmula específica de ROISA para los vencimientos.

Lo documentado para ROISA fue solo:

- `subtotal = gravado de tabla comprobantes`
- `total factura = gravado + iva + iibb`

Para vencimientos, la referencia genérica previa decía:

- `Vto1 = abs(impor1 + deuda1)`

Pero en la definición del `12/08/2026` se aclaró que para ROISA el caso especial es traer `grava` desde `comprobantes`; no aparece una definición explícita de `vto1`, `vto2` o vencimientos ROISA en lo que levanté.

---

### [24/08/2026 14:58:01] Cristian Iván Ratto

el vencimiento no lo tocamos ?

---

### [24/08/2026 14:58:11] Agente

Cristian, con la definición que levantamos: **para ROISA no tocaríamos vencimientos salvo que el objeto ya los calcule usando el total modificado**.

Lo explícito para ROISA fue:

- tocar SP solo para traer `grava`
- `subtotal = grava`
- `total factura = grava + iva + iibb`

No quedó definido cambiar `vto1`, `vto2` ni fórmulas de vencimiento ROISA. Entonces el criterio seguro sería: modificar subtotal/total y revisar si los vencimientos dependen de esos campos; si tienen expresión propia, no cambiarlos sin definición funcional adicional.

---

### [24/08/2026 14:59:13] Cristian Iván Ratto

en concreto que hicimos para roisa entonces

---

### [24/08/2026 14:59:21] Agente

Cristian, para ROISA lo concreto definido fue:

- En `d_factura_roisa.srd`, cambiar el criterio de **Subtotal** para que salga desde `grava` de la tabla `comprobantes`.
- Cambiar **Total factura** para que sea: `grava + iva + iibb`.
- Tocar el SP solo lo necesario para traer ese campo `grava`, porque ROISA era la excepción a la regla de “no tocar SP”.
- No quedó definido tocar vencimientos (`vto1`, `vto2`) en ROISA.

O sea: ROISA apunta a corregir **Subtotal** y **Total factura** usando el gravado real del comprobante. Los vencimientos quedan como están, salvo que después se confirme funcionalmente otra cosa.

---

### [24/08/2026 15:00:52] Cristian Iván Ratto

estamos usando
total = ISNULL(c.total, 0),
grava = ISNULL(c.grava, 0)
verdad?

---

### [24/08/2026 15:01:27] Agente

Sí, Cristian. Para ROISA quedó así en el SP:

```text
total = ISNULL(c.total, 0)
grava = ISNULL(c.grava, 0)
```

Y en la DW:

```text
total_factura_a = abs(if(isnull(total),0,total))
total_factura_b = abs(if(isnull(total),0,total))
subtotal_a = abs(if(isnull(grava),0,grava))
```

Los vencimientos quedaron con lógica trunk:

```text
Vto1 = abs(impor1 + deuda1)
Vto2 = abs(impor2 + deuda2)
```

---

### [24/08/2026 15:09:08] Cristian Iván Ratto

Stan quiero las diferencias entre ente objeto d_factura_roisa para el trunk vs este otro objeto: /mnt/Agentes/cratto/RM26993/d_factura_roisa.srd

---

### [24/08/2026 15:09:53] Agente

Cristian, el archivo exacto no existe:

`/mnt/Agentes/cratto/RM26993/d_factura_roisa.srd`

En el RM sí encontré estos dos objetos con ese nombre:

`/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

`/mnt/Agentes/cratto/RM26993/Objetos_OriginalesDeImpresion (BKP)/d_factura_roisa.srd`

Necesito que me confirmes cuál de esos querés comparar contra trunk.

---

### [24/08/2026 15:10:20] Cristian Iván Ratto

Stan quiero las diferencias entre ente objeto d_factura_roisa para el trunk vs este otro objeto: /mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd

---

### [24/08/2026 15:11:28] Agente

Cristian, diferencias entre trunk y `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`:

Ambos archivos están en `UTF-8 con BOM` y `CRLF`.

**Campos agregados**
En `ObjetosRoisa` se agregaron al `table()`:

```text
total decimal(2) dbname="total"
grava decimal(2) dbname="grava"
```

**Expresiones cambiadas**
`total_factura_b`:
```text
trunk:       abs(impor1)
ObjetosRoisa: abs(if(isnull(total),0,total))
```

`subtotal_a`:
```text
trunk:       abs(impor1 - iibb_arba - iva_total)
ObjetosRoisa: abs(if(isnull(grava),0,grava))
```

`total_factura_a`:
```text
trunk:       abs(impor1)
ObjetosRoisa: abs(if(isnull(total),0,total))
```

**Sin cambios relevantes**
`subtotal_b` sigue:
```text
abs(impor1)
```

Vencimientos siguen igual que trunk:
```text
total_factura_a_vto1 = abs(impor1 + deuda1)
total_factura_a_vto2 = abs(impor2 + deuda2)
total_factura_b_vto1 = abs(impor1 + deuda1)
total_factura_b_vto2 = abs(impor2 + deuda2)
```

La diferencia funcional concreta es: el objeto Roisa usa los nuevos campos `total` y `grava` solo para total factura y subtotal A/B; no cambia vencimientos.

---

### [24/08/2026 15:17:39] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-24.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-24.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-24-HHMM.md); always use the canonical 2026-08-24.md filename. If nothing to store, reply with NO_REPLY.
Current time: Monday, August 24th, 2026 - 3:17 PM (America/Buenos_Aires)
Reference UTC: 2026-08-24 18:17 UTC

---

### [24/08/2026 15:18:38] Agente

NO_REPLY

---

### [24/08/2026 15:18:39] Cristian Iván Ratto

Stan modifica el objeto para que
En los vencimientos que sea:
saldo del comprobante - deuda

---

### [24/08/2026 15:19:57] Agente

Cristian, modifiqué `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`.

Vencimientos quedaron así:

```text
Vto1 = abs(if(isnull(total),0,total) - if(isnull(deuda1),0,deuda1))
Vto2 = abs(if(isnull(total),0,total) - if(isnull(deuda2),0,deuda2))
Vto3 = abs(if(isnull(total),0,total) - if(isnull(deuda3),0,deuda3))
```

Aplicado tanto para comprobantes `A` como para no `A`.

Validé:
- UTF-8 con BOM preservado.
- CRLF preservado, sin mezcla.
- Ya no quedan expresiones `abs(imporN + deudaN)` en vencimientos.

---

### [24/08/2026 15:29:56] Cristian Iván Ratto

Stan modifica el objeto para que
En los vencimientos que sea: saldo del comprobante - deuda

---

### [24/08/2026 15:30:03] Agente

Cristian, ya quedó aplicado en `d_factura_roisa.srd`.

Los vencimientos quedaron como:

```text
abs(total - deuda1)
abs(total - deuda2)
abs(total - deuda3)
```

Con nulos controlados:

```text
abs(if(isnull(total),0,total) - if(isnull(deudaN),0,deudaN))
```

---

### [24/08/2026 15:34:55] Cristian Iván Ratto

Stan los vencimientos los quiero con el campo saldo un campo nuevo que debes traer de la tabla comprobantes modificando el SP de roisa.
/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql

---

### [24/08/2026 15:37:40] Agente

Cristian, aplicado.

Archivos impactados:

- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Qué quedó:

En el SP agregué `saldo` en ambas ramas del `UNION ALL`:

```sql
total = ISNULL(c.total, 0),
grava = ISNULL(c.grava, 0),
saldo = ISNULL(c.saldo, 0)
```

En la DW agregué el campo:

```text
saldo decimal(2) dbname="saldo"
```

Y los vencimientos quedaron usando `saldo - deuda`:

```text
Vto1 = abs(if(isnull(saldo),0,saldo) - if(isnull(deuda1),0,deuda1))
Vto2 = abs(if(isnull(saldo),0,saldo) - if(isnull(deuda2),0,deuda2))
Vto3 = abs(if(isnull(saldo),0,saldo) - if(isnull(deuda3),0,deuda3))
```

Validé encoding y finales:
- SQL: ISO-8859, CRLF preservado.
- SRD: UTF-8 con BOM, CRLF preservado.
- SQL sin `;` y sin `RTRIM`.
- Quedaron 6 expresiones de vencimiento con `saldo`, y 0 con `total` para vencimientos.

---

### [24/08/2026 15:46:21] Cristian Iván Ratto

Stan el cambio esta casi ok.
Los vencimientos deben ser el saldo + la deuda (no menos como te indique)
Que los vencimientos se muestren con dígitos despues de la coma.

---

### [24/08/2026 15:47:50] Agente

Cristian, corregido en `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`.

Los vencimientos ahora quedan como suma:

```text
Vto1 = abs(if(isnull(saldo),0,saldo) + if(isnull(deuda1),0,deuda1))
Vto2 = abs(if(isnull(saldo),0,saldo) + if(isnull(deuda2),0,deuda2))
Vto3 = abs(if(isnull(saldo),0,saldo) + if(isnull(deuda3),0,deuda3))
```

También cambié el formato de los seis importes de vencimiento a:

```text
#,##0.00
```

Validado:
- 6 expresiones con `saldo + deuda`.
- 0 expresiones con `saldo - deuda`.
- UTF-8 con BOM y CRLF preservados.

---

### [24/08/2026 15:57:06] Cristian Iván Ratto

En concreto STan que cambios hicimos al final con roisa

---

### [24/08/2026 15:57:16] Agente

Cristian, al final ROISA quedó así:

SP: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`

Se agregaron 3 campos desde `comprobantes` en ambas ramas del `UNION ALL`:

```sql
total = ISNULL(c.total, 0),
grava = ISNULL(c.grava, 0),
saldo = ISNULL(c.saldo, 0)
```

DW: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Se agregaron los campos:

```text
total
grava
saldo
```

Y se cambiaron estas expresiones:

```text
total_factura_a = abs(if(isnull(total),0,total))
total_factura_b = abs(if(isnull(total),0,total))

subtotal_a = abs(if(isnull(grava),0,grava))

vto1 = abs(if(isnull(saldo),0,saldo) + if(isnull(deuda1),0,deuda1))
vto2 = abs(if(isnull(saldo),0,saldo) + if(isnull(deuda2),0,deuda2))
vto3 = abs(if(isnull(saldo),0,saldo) + if(isnull(deuda3),0,deuda3))
```

Los vencimientos quedaron con formato `#,##0.00`. No se tocó rama SVN.

---

### [24/08/2026 16:06:03] Cristian Iván Ratto

Stan hasta el momento damos por cerrado caso Roisa.

---

### [24/08/2026 16:06:09] Agente

Perfecto Cristian. Queda cerrado en contexto el caso ROISA del RM26993 con los cambios finales sobre `total`, `grava`, `saldo`, subtotal, total factura y vencimientos.

---

### [24/08/2026 16:20:26] Cristian Iván Ratto

Stan trabajaremos con SBSalud, se pueden aplicar los mismos cambios que roisa?
Utiliza esta ruta para la modificacion.
Objeto DW: /mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd

---

### [24/08/2026 16:22:12] Agente

Cristian, con el objeto DW solo **no conviene aplicar todavía los mismos cambios de ROISA**.

Lo que vi en `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`:

- Usa el SP `dbo.afs_compro_impresion_caseros`.
- Ese SP, en `SBSalud_Prod_Test`, existe pero **no devuelve** `total`, `grava` ni `saldo`.
- La tabla `comprobantes` sí tiene esos campos: `total`, `grava`, `saldo`.

Entonces para aplicar el mismo criterio final de ROISA hay que modificar también el SP `afs_compro_impresion_caseros`, agregando al resultset:

```sql
total = ISNULL(c.total, 0)
grava = ISNULL(c.grava, 0)
saldo = ISNULL(c.saldo, 0)
```

Después sí se puede modificar la DW para usar:

```text
total_factura = total
subtotal = grava
vencimientos = saldo + deuda
```

No modifiqué el objeto todavía, porque agregar esos campos solo en la DW sin que el SP los devuelva puede romper el retrieve.

---

### [24/08/2026 16:27:01] Cristian Iván Ratto

Perfecto has esos cambios en
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd
 /mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql

---

### [24/08/2026 16:34:55] Agente

Cristian, aplicado en SBSalud.

Impacté estos dos archivos:

- [/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql](/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql)
- [/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd](/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd)

Cambios hechos:

- El SP ahora devuelve desde `comprobantes`:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `saldo = ISNULL(c.saldo, 0)`
- En la segunda rama del `UNION ALL` agregué el `LEFT JOIN comprobantes c`.
- El DW incorporó las columnas `total`, `grava` y `saldo`.
- `total_factura_a/b` ahora toman `total`.
- `subtotal_a` ahora toma `grava`.
- Los vencimientos ahora calculan `saldo + deuda1/2/3`.
- Los vencimientos quedaron con formato `#,##0.00`.

Validé:

- SQL conserva `ISO-8859` y `CRLF`.
- SRD conserva `UTF-16 LE BOM` y `CRLF`.
- El `UNION ALL` principal quedó balanceado: 84 expresiones por rama.
- `SET PARSEONLY ON` contra `SBSalud_Prod_Test`: OK.
- No toqué ramas SVN.

---

### [24/08/2026 17:06:07] Cristian Iván Ratto

Stan quiero de la base SBsalud_prod_test un afiliado activo para hacerle un ajuste para dejarle saldo a favor

---

### [24/08/2026 17:09:14] Cristian Iván Ratto

Stan quiero de la base SBsalud_prod_test un afiliado activo para hacerle un ajuste para dejarle saldo a favor

---

### [24/08/2026 17:09:14] Agente

Cristian, encontré un caso usable en `SBSalud_Prod_Test`.

Usaría este afiliado:

- Afiliado: `FIGUEROA NAVAS, ALBERTO LUIS`
- Prepaga: `1`
- Contra: `8149`
- Inte: `00`
- Parentesco: `T`
- Plan: `SBPMO`
- Estado operativo: activo por `afiliados.baja_fecha IS NULL`
- Comprobante abierto:
  - Tipo: `FC`
  - Nro: `B000800459737`
  - Fecha: `17/07/2026`
  - Período: `01/08/2026`
  - Total: `34950.00`
  - Saldo: `24128.87`

Para dejarle saldo a favor, el ajuste debería superar `24128.87`.

También vi como alternativa más reciente:

- `ROMERO, LUCIA SOLANGE`
- Contra `5223`
- FC `B000800459739`
- Fecha `18/08/2026`
- Saldo `93200.00`

Solo hice consultas de lectura.

---

### [24/08/2026 17:25:13] Cristian Iván Ratto

Stan podrias verificar la cuenta corriente de este afiliado 5223 e indicarme si tiene saldo a favor o en contra.

Le hice una NC de credito B000800070347 por 50 mil pesos.
Y me parecio ver una FC nueva que se hizo sola.
Mi intension era ponerle saldo a favor del cliente y luego yo hacer una fc

---

### [24/08/2026 17:27:14] Agente

Cristian, verifiqué el contrato `5223` en `SBSalud_Prod_Test`.

Resultado: quedó **saldo en contra**, no saldo a favor.

Saldo abierto actual:

- `FC B000800459739`: saldo `93200.00`
- `FC B000900017706`: saldo `68803.68`

Total saldo en contra: `162003.68`

La NC que hiciste:

- `NC B000800070347`
- Importe: `-50000.00`
- Saldo actual de la NC: `0.00`
- Observación: `SALDO A FAVOR`

Pero no quedó como crédito disponible porque se aplicó completa automáticamente:

- Contra `FC B000800457548`: `2400.68`
- Contra `FC B000800459394`: `2670.00`
- Contra `FC B000900017706`: `44929.32`

Total aplicado: `50000.00`

Sobre la FC nueva: no hay ninguna FC posterior a la NC. La `FC B000800459739` ya existía desde el `18/08/2026`, antes de la NC del `24/08/2026`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
