# Memoria de conversación — Cristian Iván Ratto

## Datos

- **Fecha:** 25/08/2026
- **Usuario:** Cristian Iván Ratto
- **ID Google Chat:** `users/112625344596118312340`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 70
- **Inicio:** 08:32:44
- **Fin:** 16:44:30

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/vmclokaaaae`

## Archivos revisados

- `5e3dfa48-cedb-457b-9d61-6f53fda40521.jsonl`

---

## Transcripción completa

### [25/08/2026 08:32:44] Cristian Iván Ratto

Stan

---

### [25/08/2026 08:33:52] Agente

Hola Cristian, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [25/08/2026 08:34:34] Cristian Iván Ratto

Stan retomemos el caso SBSalud como quedó al final ?

---

### [25/08/2026 08:36:14] Agente

Cristian, al final SBSalud quedó así:

Impactado en `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/`:

- `afs_compro_impresion_caseros.sql`
- `d_factura_sbsalud.srd`

Criterio funcional final:

- El SP devuelve desde `comprobantes`: `total`, `grava` y `saldo`.
- `TOTAL FACTURA A/B` toma `total`.
- `SUBTOTAL A` toma `grava`.
- Los vencimientos calculan `saldo + deuda1/2/3`.
- Los vencimientos quedaron con formato `#,##0.00`.
- El código de barras sigue viniendo desde base, no se recalcula en el DataWindow.

Validaciones que habían quedado informadas:

- SQL conservó `ISO-8859` y `CRLF`.
- SRD conservó `UTF-16 LE BOM` y `CRLF`.
- El `UNION ALL` quedó balanceado con 84 expresiones por rama.
- `SET PARSEONLY ON` contra `SBSalud_Prod_Test`: OK.
- No se tocaron ramas SVN.

Después de eso revisamos el afiliado contrato `5223`: quedó con saldo en contra por `162003.68`; la NC `B000800070347` de `50000.00` se aplicó completa automáticamente, por eso no quedó crédito disponible.

Source: `memory/MEMORY-users_112625344596118312340-2026-08-24.md#L710-L764` y `#L790-L828`.

---

### [25/08/2026 09:52:05] Cristian Iván Ratto

Stan a SBSalud vuelvelo como estaba antes

---

### [25/08/2026 09:55:34] Agente

Cristian, SBSalud quedó vuelto al estado anterior.

Archivos impactados:

- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Qué quedó restaurado:

- El `.srd` quedó igual al backup original de `Objetos_OriginalesDeImpresion (BKP)`.
- El SQL volvió a la versión de referencia sin `total`, `grava`, `saldo` ni `LEFT JOIN comprobantes c`.
- El DataWindow volvió a calcular `TOTAL FACTURA`, `SUBTOTAL`, vencimientos y barras como antes.

Validé:

- `srd_matches_original=0`: igualdad exacta contra el backup original.
- `sql_matches_reference=0`: igualdad exacta contra la referencia SVN leída.
- SQL: `ISO-8859` con `CRLF`.
- SRD: `UTF-16 LE` con `CRLF`.
- `SET PARSEONLY ON` contra `SBSalud_Prod_Test`: OK.
- No toqué ramas SVN.

---

### [25/08/2026 09:59:32] Cristian Iván Ratto

me haces un resumen al final de como quedo SBsalud ?

---

### [25/08/2026 09:59:43] Agente

Cristian, SBSalud quedó restaurado al estado original/anterior.

Archivos actuales:

- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Estado final:

- El DataWindow volvió al backup original de `Objetos_OriginalesDeImpresion (BKP)`.
- El SP volvió a la versión de referencia sin los campos agregados `total`, `grava` y `saldo`.
- Se quitó el `LEFT JOIN comprobantes c` que se había agregado.
- `TOTAL FACTURA` volvió a calcularse como `abs(impor1)`.
- `SUBTOTAL` volvió a calcularse como `abs(impor1 - iibb_arba - iva_total)`.
- Los vencimientos volvieron a calcularse como `abs(impor1)`, `abs(impor2)` y `abs(impor3)`.
- Las barras volvieron a su visibilidad original, sin condicionar por `saldo + deuda`.

Validación final:

- `.srd` igual exacto al backup original.
- `.sql` igual exacto a la referencia SVN.
- SQL conserva `ISO-8859` + `CRLF`.
- SRD conserva `UTF-16 LE` + `CRLF`.
- `SET PARSEONLY ON` contra `SBSalud_Prod_Test`: OK.
- No se tocó SVN.

---

### [25/08/2026 10:14:23] Cristian Iván Ratto

Stan aplica el criterio de Roisa a SBSalud, pero los vencimientos no los toques.

---

### [25/08/2026 10:18:15] Agente

Cristian, aplicado SBSalud con criterio Roisa solo en la parte fiscal.

Archivos impactados:

- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Qué cambié:

- El SP ahora devuelve `total` y `grava` desde `comprobantes`.
- En el DataWindow agregué las columnas `total` y `grava`.
- `TOTAL FACTURA A/B` ahora calcula: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL` ahora calcula: `abs(if(isnull(grava),0,grava))`.

Qué dejé sin tocar:

- Vto1, Vto2 y Vto3 siguen como estaban: `abs(impor1)`, `abs(impor2)`, `abs(impor3)`.
- Barras sin cambios.
- No agregué `saldo`.

Validé:

- SQL conserva `ISO-8859` + `CRLF`.
- SRD conserva `UTF-16 LE BOM` + `CRLF`.
- `SET PARSEONLY ON`: OK.
- `SET NOEXEC ON`: OK.
- No toqué SVN.

---

### [25/08/2026 10:37:00] Cristian Iván Ratto

Stan el caso Roisa quedo bien, solo necesito que agregues una formula de visualizacion del codigo de barras.
Cuando el vencimiento sea 0, no mostremos el codigo de barras.

---

### [25/08/2026 10:39:09] Agente

Cristian, aplicado en ROISA.

Archivo impactado:

- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Cambio realizado:

- Agregué fórmula de visualización en las tres salidas de código de barras:
  - `barra`
  - `barra_128`
  - `barra_numeros`

Condición aplicada:

```text
abs(if(isnull(saldo),0,saldo) + if(isnull(deuda1),0,deuda1)) = 0
```

Si ese importe del vencimiento queda en `0`, no se muestra el código de barras.

Validé:

- Solo cambiaron las fórmulas `visible` de las barras.
- No toqué vencimientos.
- No toqué subtotal ni total factura.
- No toqué el SP.
- El `.srd` conserva `UTF-8 BOM` y `CRLF`.
- No toqué SVN.

---

### [25/08/2026 10:47:49] Cristian Iván Ratto

Caso Roisa OK Cerrado.

---

### [25/08/2026 10:47:56] Agente

Perfecto Cristian, caso ROISA queda cerrado como OK.

Estado final registrado:

- `Subtotal`: usa `COMPROBANTES.grava`.
- `Total factura A/B`: usa `grava + iva_total + iibb_arba + iibb_caba`.
- Vencimientos: sin cambios respecto al criterio ya validado.
- Código de barras: se oculta cuando el vencimiento queda en `0`.
- Archivos finales en `/mnt/Agentes/cratto/RM26993/ObjetosRoisa`.
- SVN no fue tocado.

---

### [25/08/2026 11:49:23] Cristian Iván Ratto

Stan retomamos el caso SBsalud.
Como esta ahora estaria casi bien, faltan 2 ajustes.
Los vencimientos que lo tomen del saldo (campo nuevo del SP)
Aplica formula de visualizacion para vencimientos en 0 para que la barra no se vea.

---

### [25/08/2026 11:53:08] Agente

Cristian, aplicado en SBSalud.

Archivos impactados:

- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Cambios:

- El SP ahora también devuelve `saldo = ISNULL(c.saldo, 0)`.
- El DW incorporó la columna `saldo`.
- Vencimientos:
  - `Vto1`: `abs(saldo + deuda1)`
  - `Vto2`: `abs(saldo + deuda2)`
  - `Vto3`: `abs(saldo + deuda3)`
- Barras:
  - `barra`, `barra_128` y `barra_numeros` se ocultan si `abs(saldo + deuda1) = 0`.

Se mantiene lo anterior:

- `TOTAL FACTURA A/B` sigue tomando `total`.
- `SUBTOTAL` sigue tomando `grava`.

Validé:

- SQL conserva `ISO-8859` + `CRLF`.
- SRD conserva `UTF-16 LE BOM` + `CRLF`.
- `SET PARSEONLY ON`: OK.
- `SET NOEXEC ON`: OK.
- No toqué SVN.

---

### [25/08/2026 11:57:02] Cristian Iván Ratto

Consulta stan entiendo que el codigo de barra se arma en el afi_compro_impresion.
Queria saber para este cliente SbSalud como lo hace?
Es una consulta, no modifiques nada.

---

### [25/08/2026 12:00:12] Agente

Cristian, correcto: para SBSalud la barra se arma antes, en `dbo.afi_compro_impresion`. El SP de impresión `afs_compro_impresion_caseros` no la calcula; solo lee:

```text
COMPRO_IMPRESION.barra
COMPRO_IMPRESION.barra_afip
```

Después el DataWindow solo la dibuja con:

```text
gf_interleaved_2de5(barra)
gf_code_128(barra)
```

Para SBSalud, `afi_compro_impresion` usa el criterio de `contra_forma = 'K'`, definido como `@con_contra_forma_sb_Salud`.

La generación entra por la misma rama que Caseros:

```text
dbo.afn_get_caseros_codibarra(
  cpagofacil,
  compro_nro,
  importe,
  importe2,
  vto1,
  vto2
)
```

En esta base SBSalud tiene `cpagofacil = 6246` y no tiene `barra_ente_rec`, por eso cae en tipo barra `PF`.

El importe que manda a la barra para SBSalud sale de `COMPROBANTES` así:

```text
importe = si C.saldo > 0 entonces C.saldo, sino C.total
importe2 = si C.saldo <= 0 entonces 0, sino C.saldo + intereses del 2do vencimiento
```

La función `afn_get_caseros_codibarra` arma la cadena con:

```text
empresa/cpagofacil
importe 1er vencimiento
fecha 1er vencimiento en juliano
letra del comprobante
número del comprobante
moneda
diferencia del 2do vencimiento
días entre vto1 y vto2
dígitos verificadores
```

Conclusión: en SBSalud la barra se genera con el **saldo vigente del comprobante** cuando hay saldo positivo. No toma el `TOTAL FACTURA` fiscal que estamos mostrando ahora en el DW. No modifiqué nada.

---

### [25/08/2026 12:04:52] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 12:04 PM (America/Buenos_Aires)
Reference UTC: 2026-08-25 15:04 UTC

---

### [25/08/2026 12:05:40] Agente

NO_REPLY

---

### [25/08/2026 12:05:41] Cristian Iván Ratto

Hay un parametro general que se toma para saber si los saldos se tienen en cuenta al momento de obtener el total de la FC.
Cual es?.
Es tenido en cuenta al momento de armar el codigo de barras?

---

### [25/08/2026 12:06:59] Agente

Cristian, el parámetro general relacionado es:

```text
Afilmed / Facturacion / aplicacion_auto_cred
```

En `SBSalud_Prod_Test` está en:

```text
aplicacion_auto_cred = S
```

Pero separo dos cosas:

Para armar el **importe base de la FC**, `afi_compro_impresion` toma `COMPROBANTES.saldo` sin consultar ese parámetro en esa línea. Para SBSalud entra por `contra_forma = 'K'` y hace:

```text
importe = si C.saldo > 0 entonces C.saldo, sino C.total
importe2 = si C.saldo <= 0 entonces 0, sino C.saldo + intereses vto2
importe3 = si C.saldo <= 0 entonces 0, sino C.saldo + intereses vto3
```

El parámetro `aplicacion_auto_cred` se usa después para registrar/aplicar la diferencia:

```text
diferencia = C.total - C.saldo
creditos = diferencia
```

Sobre el código de barras: **sí, queda afectado por ese criterio de saldo**, pero no porque la función del código de barras lea el parámetro directamente.

Para SBSalud la barra se arma con:

```text
dbo.afn_get_caseros_codibarra(
  cpagofacil,
  compro_nro,
  importe,
  importe2,
  vto1,
  vto2
)
```

Como `importe` ya fue calculado usando `C.saldo` cuando hay saldo positivo, la barra termina tomando el saldo/vencimiento, no el total fiscal.

Aclaración: si `@reg_cod_barra = 'N'`, no regenera la barra y copia `COMPROBANTES.codi_barra` existente.

---

### [25/08/2026 12:13:49] Cristian Iván Ratto

Stan entonce spara SBSalud en los vencimientos usa solo el campo Saldo

---

### [25/08/2026 12:13:56] Cristian Iván Ratto

Stan entonce spara SBSalud en los vencimientos usa solo el campo Saldo

---

### [25/08/2026 12:13:56] Agente

Sí, Cristian.

Para SBSalud, en el objeto que dejamos ahora, los vencimientos toman como base el campo nuevo `saldo` que viene de `COMPROBANTES.saldo` por el SP:

```text
Vto1 = abs(saldo + deuda1)
Vto2 = abs(saldo + deuda2)
Vto3 = abs(saldo + deuda3)
```

Entonces no usan `total` ni `impor1` como base principal. Usan `saldo`, y le suman la deuda/interés correspondiente de cada vencimiento.

---

### [25/08/2026 12:14:45] Cristian Iván Ratto

perdon te dije mal que el campo saldo ( sin deuda)

---

### [25/08/2026 12:14:52] Agente

Entendido, Cristian.

Entonces el criterio correcto para SBSalud queda:

```text
Vto1 = abs(saldo)
Vto2 = abs(saldo)
Vto3 = abs(saldo)
```

Sin sumar `deuda1`, `deuda2` ni `deuda3`.

Y la barra debería ocultarse cuando:

```text
abs(saldo) = 0
```

No modifico nada todavía porque esto fue corrección de criterio en la conversación, no pedido explícito de aplicación.

---

### [25/08/2026 12:15:14] Cristian Iván Ratto

por favor aplicalo

---

### [25/08/2026 12:16:26] Agente

Cristian, aplicado.

Cambio en `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`:

- `Vto1`, `Vto2` y `Vto3` ahora toman solo:
```text
abs(saldo)
```

- La visibilidad de `barra`, `barra_128` y `barra_numeros` ahora oculta la barra cuando:
```text
abs(saldo) = 0
```

No toqué el SP en este ajuste; ya tenía `total`, `grava` y `saldo`.

Validé:

- SRD conserva `UTF-16 LE BOM` + `CRLF`.
- SQL sigue `ISO-8859` + `CRLF`.
- No quedó `saldo + deuda` en las fórmulas de vencimiento/barra.
- No toqué SVN.

---

### [25/08/2026 12:24:31] Cristian Iván Ratto

el afiliad 5223, tiene en su cuenta corriente un saldo en contra de 167828,68 para dejarlo en 0 en su cuenta corriente tengo que hacer una nota de credito por ese mismo monto ?

---

### [25/08/2026 12:24:56] Agente

Sí, Cristian. Si la cuenta corriente del afiliado `5223` tiene **saldo en contra de 167828,68**, para dejarla en `0` necesitás una **nota de crédito por 167828,68**.

Punto importante: debería aplicarse contra las FC abiertas que componen ese saldo. Si la base tiene aplicación automática de créditos activa, la NC puede imputarse automáticamente contra esas facturas; si no, habría que aplicarla manualmente.

Criterio:

```text
Saldo en contra:  167828,68
NC necesaria:    -167828,68
Saldo final:           0,00
```

Con ese importe quedaría cancelada, siempre que no haya otros comprobantes abiertos o intereses adicionales generados después del cálculo.

---

### [25/08/2026 13:04:15] Cristian Iván Ratto

Estan pasemos al caso SMC.
Quiero que me cuentes como arma la barra.
Quiero saber si sucede lo mismo que en SBsalud, quiero saber que monto toma.

---

### [25/08/2026 13:06:22] Agente

Cristian, para SMC revisé `SMC_Prod_Test`.

SMC usa el mismo circuito base que SBSalud para la barra:

- DataWindow: `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`
- SP de impresión: `dbo.afs_compro_impresion_caseros`
- La barra viene desde `COMPRO_IMPRESION.barra`
- Se arma antes en `dbo.afi_compro_impresion`

SMC tiene `contra_forma = 'C'`, o sea entra como **Caseros**. SBSalud entra como `contra_forma = 'K'`, pero el código los trata parecido en esta parte y ambos terminan usando:

```text
dbo.afn_get_caseros_codibarra(
  cpagofacil,
  compro_nro,
  importe,
  importe2,
  vto1,
  vto2
)
```

Para SMC el `cpagofacil` observado es:

```text
1504
```

El monto que toma para el código de barras es:

```text
si COMPROBANTES.saldo > 0 => usa saldo
si COMPROBANTES.saldo <= 0 => usa total
```

Ejemplos reales en `SMC_Prod_Test`:

```text
FC B000100002586
total = 331500.00
saldo = 111021.16
barra = 150400011102116...
monto barra = 111021.16
```

```text
FC B000100002585
total = 110500.00
saldo = 0.00
barra = 150400011050000...
monto barra = 110500.00
```

Conclusión: **sí, sucede lo mismo que en SBSalud cuando hay saldo positivo**: la barra toma `saldo`, no el total fiscal.  
Diferencia importante: si el saldo queda en `0`, la generación puede dejar una barra con el `total`, salvo que después el DataWindow la oculte por fórmula de visualización. No modifiqué nada.

---

### [25/08/2026 13:26:50] Cristian Iván Ratto

Stan aplica los mismos criterios de SbSalud a SMC

---

### [25/08/2026 13:31:42] Agente

Cristian, aplicado en SMC.

Archivo impactado:
`/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`

Cambios realizados:
- Agregué columnas DW: `total`, `grava`, `saldo`.
- `TOTAL FACTURA A/B` ahora toma `total`.
- `SUBTOTAL` ahora toma `grava`.
- Vencimientos `Vto1` y `Vto2` ahora toman solo `saldo`, sin deuda:
  `abs(if(isnull(saldo),0,saldo))`
- La barra de pago ahora se oculta cuando el saldo es 0.
- `barra_afip` no fue modificada.

No modifiqué el SQL `afs_compro_impresion_caseros.sql` porque ya traía:
`total = ISNULL(c.total, 0)`, `grava = ISNULL(c.grava, 0)` y `saldo = ISNULL(c.saldo, 0)`.

Validé encoding y finales de línea:
- SQL: ISO-8859 + CRLF, sin cambios.
- SRD: UTF-16 LE con BOM + LF, preservado.
- No toqué ramas SVN.

---

### [25/08/2026 14:30:28] Cristian Iván Ratto

Stan nuestra carpeta de trabajo para ActivaSalud es
/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd
Quiero saber que hace con el codigo de barra, como lo calcula con que importe y acual criterio de los viste se parece.

---

### [25/08/2026 14:35:10] Agente

Cristian, ActivaSalud hoy queda así:

El DataWindow `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd` **no calcula el código de barras de pago**. Solo intenta dibujar el campo `barra` con:

```text
gf_interleaved_2de5(barra)
```

y lo muestra si:

```text
impor1 > 0
```

Pero en `Activa_Prod_Test` confirmé que `COMPRO_IMPRESION.barra` está siempre `NULL`:

```text
total_registros = 10934
con_barra = 0
con_barra_afip = 10308
```

Entonces, para ActivaSalud **no hay barra de pago generada**, por lo tanto no hay importe cobrable codificado en `barra`.

Lo que sí tiene es `barra_afip`, que es fiscal/AFIP, no barra de pago. Esa se arma en `afi_compro_impresion` con:

```text
afn_get_afip_codibarra(ente, compro_tipo, compro_nro, cae, cae_vto)
```

Sobre el criterio: **no se parece a SBSalud/SMC ni a ROISA para la barra de pago**. Activa tiene `contra_forma = 'G'`, y esa forma no entra en la rama Caseros/SBSalud/SMC que arma la barra con importe/saldo. Se parece más al criterio propio que ya habíamos visto para Activa: total fiscal por `impor1`, vencimientos neteados con `creditos`, y sin barra de pago efectiva.

---

### [25/08/2026 14:36:48] Cristian Iván Ratto

Aun no modifiques nada.... que pasaria si para ActivaSalud hacemos que:
El campo total que lo tome del Total que llega por el SP.
El campo subtotal que lo tome de grava que llega del SP.
Que los vencimientos sean el campo Saldo que llega del SP.

---

### [25/08/2026 14:37:41] Agente

Cristian, sin modificar nada: para ActivaSalud eso sería llevarlo más cerca del criterio SBSalud/SMC, pero con una diferencia importante.

Hoy el SP de ActivaSalud **no devuelve** `total`, `grava` ni `saldo` como columnas al DataWindow. Para hacer eso habría que tocar también `afs_compro_impresion_caseros.sql`, no solo el SRD.

Impacto funcional esperado:

- `TOTAL FACTURA = total`  
  En principio quedaría bien. Para los casos vistos, `COMPROBANTES.total` coincide con el total fiscal que hoy muestra por `impor1`.

- `SUBTOTAL = grava`  
  Este punto es delicado. En Activa vimos comprobantes exentos, por ejemplo `B000300003628`, donde:
  ```text
  total = 40000
  exen = 40000
  grava = 0
  ```
  Si el subtotal pasa a `grava`, ese comprobante mostraría subtotal `0`, cuando hoy muestra `40000`. Solo sería correcto si funcionalmente quieren que “subtotal” sea estrictamente base gravada y no incluya exento.

- `VENCIMIENTOS = saldo`  
  Para casos con crédito queda bien:
  ```text
  B000300003628 saldo = 0
  B000300003629 saldo = 20299.07
  ```
  Pero para casos con deuda acumulada puede cambiar el resultado. Ejemplo `B000300003626`:
  ```text
  impor1 = 7091.37
  deuda1 = 14182.74
  saldo = 7091.37
  ```
  Hoy el vencimiento contempla deuda y da `21274.11`. Si usamos solo `saldo`, pasaría a `7091.37`, o sea dejaría afuera la deuda previa.

Conclusión: `total = total` es razonable. `vencimientos = saldo` sirve si el criterio buscado es mostrar solo saldo actual sin deuda. El punto más riesgoso es `subtotal = grava`, porque en ActivaSalud puede hacer que facturas exentas muestren subtotal `0`.

---

### [25/08/2026 14:51:15] Cristian Iván Ratto

Stan nuestra carpeta de trabajo para ActivaSalud es
/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd
Como lo esta haciendo ahora?.
Es una consulta que te hago.

---

### [25/08/2026 14:52:01] Agente

Cristian, ahora ActivaSalud lo hace así:

- **SP usado por el DW**: `dbo.afs_compro_impresion_caseros`
- **Total factura A/B**: toma `abs(impor1)`
- **Subtotal A**: toma `abs(impor1 - iibb_arba - iva_total)`
- **Subtotal B / otros**: toma `abs(impor1)`
- **Vto1 A/B**: toma `abs(impor1 + saldo_anterior1)`
- **Vto2 A/B**: toma `abs(impor2 + saldo_anterior2)`
- **Vto3 A**: toma `abs(impor3)`
- **Vto3 B**: toma `abs(impor3 + deuda3)`

Los saldos anteriores están así:

```text
saldo_anterior1 = deuda1 - creditos
saldo_anterior2 = deuda2 - creditos
```

La **barra de pago** usa el campo directo `barra`:

```text
gf_interleaved_2de5(barra)
```

y la condición visual actual es:

```text
impor1 > 0
```

Pero el SP de ActivaSalud solo trae `a.barra`; no trae `total`, `grava` ni `saldo` como columnas al DataWindow. Y en la base testigo que revisamos, `COMPRO_IMPRESION.barra` estaba siempre `NULL`, por lo que no hay barra de pago efectiva. Sí existe `barra_afip`, que es fiscal/AFIP y se muestra aparte.

---

### [25/08/2026 14:55:09] Cristian Iván Ratto

Quiero aplicar en ActivaSalud:
El campo total que lo tome del Total que llega por el SP.
El campo subtotal que lo tome de grava que llega del SP.
Que los vencimientos sean el campo Saldo que llega del SP.
REcuerda la formula para esconder la barra en caso del que el vencimiento sea 0 (si es que aplica)
/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd

---

### [25/08/2026 15:00:25] Agente

Cristian, aplicado en ActivaSalud.

Archivos impactados:
- `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

Qué quedó:
- El SP ahora devuelve `total`, `grava` y `saldo` desde `comprobantes`.
- El DW incorporó columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA A/B` ahora toma:
  `abs(if(isnull(total),0,total))`
- `SUBTOTAL` ahora toma:
  `abs(if(isnull(grava),0,grava))`
- Vencimientos `Vto1`, `Vto2` y `Vto3` ahora toman:
  `abs(if(isnull(saldo),0,saldo))`
- La barra de pago ahora se oculta si el saldo/vencimiento es 0.
- `barra_afip` no fue modificada.

Validé:
- SQL mantiene ANSI/ISO-8859 sin BOM y CRLF.
- SRD mantiene UTF-16 LE con BOM y CRLF.
- SQL parseado con `SET PARSEONLY ON` sin errores.
- No toqué ramas SVN ni hice commit.

---

### [25/08/2026 15:19:19] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 3:19 PM (America/Buenos_Aires)
Reference UTC: 2026-08-25 18:19 UTC

---

### [25/08/2026 15:20:18] Agente

NO_REPLY

---

### [25/08/2026 15:20:19] Cristian Iván Ratto

Stan lo que hiciste esta bien, te pido un cambio que los vencimientos sean el saldo + deuda.
Que la barra se oculta si el vencimiento es 0

---

### [25/08/2026 15:22:29] Agente

Cristian, aplicado el ajuste en ActivaSalud.

Archivo impactado:
`/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

Quedó así:
- `Vto1 A/B`: `abs(saldo + deuda1)`
- `Vto2 A/B`: `abs(saldo + deuda2)`
- `Vto3 A/B`: `abs(saldo + deuda3)`
- La barra de pago se oculta si el primer vencimiento da 0:
  `abs(saldo + deuda1) = 0`

Se mantiene lo anterior:
- `TOTAL FACTURA A/B` toma `total`.
- `SUBTOTAL` toma `grava`.
- `barra_afip` no fue modificada.

Validé que el SRD conserve `UTF-16 LE con BOM` y `CRLF`. No toqué el SQL ni ramas SVN.

---

### [25/08/2026 15:30:44] Cristian Iván Ratto

Noto que la barra no se muestra en la FC ni con nuestra modificación ni antes tampoco. Sabes el motivo ?

---

### [25/08/2026 15:31:37] Agente

Cristian, sí. El motivo es que en ActivaSalud **el campo `barra` viene `NULL` desde origen**.

El DW solo la dibuja si existe:

```text
gf_interleaved_2de5(barra)
```

Antes la condición era:

```text
barra no null + impor1 > 0
```

Ahora quedó:

```text
barra no null + abs(saldo + deuda1) > 0
```

Pero en ambos casos, si `barra` viene `NULL`, no hay nada para mostrar.

Validé en `Activa_Prod_Test`:

```text
COMPRO_IMPRESION total_registros = 10934
con_barra = 0
con_barra_afip = 10308
```

La causa funcional está en `afi_compro_impresion`: ActivaSalud tiene `contra_forma = 'G'`. Esa forma **no entra** en la rama que genera `barra` tipo Caseros/SBSalud/SMC. Además `barra_ente_rec` está `NULL`, entonces cae como tipo `PF`, pero la generación PF no contempla `contra_forma = 'G'`.

Por eso no se veía antes y tampoco se ve ahora: **no es un problema visual del SRD, es que ActivaSalud no está generando barra de pago en `COMPRO_IMPRESION.barra`**. Lo que sí tiene es `barra_afip`, pero esa es fiscal/AFIP, no la barra de pago.

---

### [25/08/2026 15:41:57] Cristian Iván Ratto

Quiero aplicar los cambios en Montegrande:
/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd

El campo total que lo tome del Total que llega por el SP.
El campo subtotal que lo tome de grava que llega del SP.
Que los vencimientos sean el campo Saldo + Deuda que llega del SP.
REcuerda la formula para esconder la barra en caso del que el vencimiento sea 0 (si es que aplica)

---

### [25/08/2026 15:42:07] Cristian Iván Ratto

Quiero aplicar los cambios en Montegrande:
/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd

El campo total que lo tome del Total que llega por el SP.
El campo subtotal que lo tome de grava que llega del SP.
Que los vencimientos sean el campo Saldo + Deuda que llega del SP.
REcuerda la formula para esconder la barra en caso del que el vencimiento sea 0 (si es que aplica)

---

### [25/08/2026 15:49:09] Agente

Cristian, aplicado en [d_factura_montegrande.srd](/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd).

Quedó así:
- Agregué columnas esperadas del SP: `total`, `grava`, `saldo`.
- `TOTAL FACTURA` A/B toma `abs(total)`.
- `SUBTOTAL` A/B toma `abs(grava)`.
- `Vto1` toma `abs(saldo + deuda1)`.
- `Vto2` toma `abs(saldo + deuda2)`.
- La barra de entes recaudadores se oculta cuando `abs(saldo + deuda1) = 0`.

Validé que el SRD mantuvo `UTF-16 LE BOM` y `CRLF`. No toqué SVN ni base de datos.

Dato importante: consulté el SP `dbo.afs_compro_impresion_cmmg` en `Montegrande_Prod_Test` y hoy no expone `total`, `grava` ni `saldo`. El SRD ya quedó preparado, pero para que funcione en ejecución el SP también tiene que devolver esos tres campos.

---

### [25/08/2026 15:50:04] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 3:50 PM (America/Buenos_Aires)
Reference UTC: 2026-08-25 18:50 UTC

---

### [25/08/2026 15:51:40] Agente

NO_REPLY

---

### [25/08/2026 15:51:41] Cristian Iván Ratto

Has el cambio en en el sp dbo.afs_compro_impresion_cmmg

---

### [25/08/2026 15:56:01] Agente

Cristian, cambio aplicado en `dbo.afs_compro_impresion_cmmg` sobre `Montegrande_Prod_Test`.

Qué quedó:
- El SP ahora devuelve `total = ISNULL(c.total, 0)`.
- Devuelve `grava = ISNULL(c.grava, 0)`.
- Devuelve `saldo = ISNULL(c.saldo, 0)`.
- Lo agregué en las dos ramas de salida del procedimiento.
- En la rama que no tenía `comprobantes c`, agregué el `INNER JOIN comprobantes c`.

Validación:
- `PARSEONLY` OK antes de aplicar.
- `ALTER PROCEDURE` ejecutado correctamente.
- Verifiqué definición final: `c.total`, `c.grava` y `c.saldo` aparecen 2 veces, una por cada rama.
- No hay `;` ni `RTRIM`.
- No toqué SVN.

---

### [25/08/2026 15:58:17] Cristian Iván Ratto

Stan exporta ese objeto modificado aca:
/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/dbo.afs_compro_impresion_cmmg.sql

---

### [25/08/2026 15:59:56] Agente

Cristian, exportado acá:

`/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/dbo.afs_compro_impresion_cmmg.sql`

Validé:
- Archivo en `ISO-8859` con `CRLF`, sin BOM.
- Contiene `CREATE PROCEDURE dbo.afs_compro_impresion_cmmg`.
- Incluye `total`, `grava` y `saldo` en las dos ramas.
- No contiene `;` ni `RTRIM`.
- No toqué SVN.

---

### [25/08/2026 16:01:53] Usuario

/codex status

---

### [25/08/2026 16:01:53] Agente

Codex app-server: connected
Models: gpt-5.5, gpt-5.4, gpt-5.4-mini
Account: agente99＿ia@infomedical.com.ar
Rate limits: Codex: primary 31% left ⏱12m 15s · secondary 89% left ⏱6d 19h
MCP servers: 1
Skills: 5

---

### [25/08/2026 16:13:43] Usuario

/codex status

---

### [25/08/2026 16:13:43] Agente

Codex app-server: connected
Models: gpt-5.5, gpt-5.4, gpt-5.4-mini
Account: agente99＿ia@infomedical.com.ar
Rate limits: Codex: primary 22% left ⏱25s · secondary 88% left ⏱6d 19h
MCP servers: 1
Skills: 5

---

### [25/08/2026 16:15:31] Usuario

/codex status

---

### [25/08/2026 16:15:31] Agente

Codex app-server: connected
Models: gpt-5.5, gpt-5.4, gpt-5.4-mini
Account: agente99＿ia@infomedical.com.ar
Rate limits: Codex: primary 100% left ⏱4h 59m · secondary 88% left ⏱6d 18h
MCP servers: 1
Skills: 5

---

### [25/08/2026 16:25:15] Cristian Iván Ratto

Stan para el caso montegrande, como estaba antes vs como esta ahora.
No modifiques nada, es una consulta.

---

### [25/08/2026 16:26:00] Agente

Cristian, consulta sin cambios ejecutados.

**Antes**
- El SRD `d_factura_montegrande.srd` no tenía columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA` tomaba `abs(impor1)`.
- `SUBTOTAL`:
  - A/B tomaba `abs(impor1 - iibb_arba - iibb_caba - iva_total)`.
  - En otra condición tomaba `abs(impor1)`.
- Vencimientos:
  - `Vto1`: `impor1 + saldo_anterior1`.
  - `Vto2`: `impor2 + saldo_anterior2`.
  - `saldo_anterior1/2` venía de `deudaN - creditos`.
- Barra: se mostraba si `impor1 + saldo_anterior1 > 0`.
- El SP `dbo.afs_compro_impresion_cmmg` no devolvía `total`, `grava`, `saldo`.

**Ahora**
- El SRD espera los campos nuevos `total`, `grava`, `saldo`.
- `TOTAL FACTURA` A/B toma `abs(total)`.
- `SUBTOTAL` A/B toma `abs(grava)`.
- Vencimientos:
  - `Vto1`: `abs(saldo + deuda1)`.
  - `Vto2`: `abs(saldo + deuda2)`.
- Barra: se oculta cuando `abs(saldo + deuda1) = 0`.
- El SP ahora devuelve `total`, `grava`, `saldo` desde `comprobantes c` en sus dos ramas.
- También quedó exportado el SP modificado en:
  `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/dbo.afs_compro_impresion_cmmg.sql`

Resumen corto: antes Montegrande calculaba casi todo desde `impor1/impor2` más deuda/créditos; ahora toma el total real, el gravado real y el saldo real del comprobante, y los vencimientos quedan como `saldo + deuda`.

---

### [25/08/2026 16:33:07] Cristian Iván Ratto

Stan consulta, el subtotal no deberia ser gravado + exento ?

---

### [25/08/2026 16:33:55] Agente

Cristian, conceptualmente sí: si el comprobante maneja importes exentos, el `SUBTOTAL` debería ser `gravado + exento`.

Pero en Montegrande, con lo que validé recién:
- La tabla `comprobantes` tiene `total`, `grava`, `saldo`.
- No aparece un campo `exento` en `comprobantes`.
- El SP exportado ahora devuelve solo `total`, `grava`, `saldo`.
- El SRD quedó tomando `SUBTOTAL = abs(grava)` porque ese fue el criterio pedido y porque no hay `exento` disponible desde ese origen.

Entonces: si Montegrande necesita reflejar exento, habría que identificar de dónde sale ese importe exento, porque no está como columna directa `comprobantes.exento`. Con la estructura actual, el subtotal solo puede tomar `grava` sin inventar el exento.

---

### [25/08/2026 16:42:15] Cristian Iván Ratto

Stan comparame Roisa como estaba antes (trunk) vs como esta ahora

---

### [25/08/2026 16:44:30] Agente

Cristian, comparado sin modificar nada.

**Roisa en trunk**
- SRD no tenía columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA` A/B: `abs(impor1)`.
- `SUBTOTAL` A/B: `abs(impor1 - iibb_arba - iva_total)`.
- Vencimientos A/B:
  - Vto1: `abs(impor1 + deuda1)`
  - Vto2: `abs(impor2 + deuda2)`
  - Vto3: `abs(impor3 + deuda3)`
- Barra: ocultaba solo si `barra` era null o si `LEFT(barra,4) = '0448'`.
- SP `afs_compro_impresion_roisa`: no devolvía `total`, `grava`, `saldo`.

**Roisa actual**
- SRD tiene columnas nuevas: `total`, `grava`, `saldo`.
- `TOTAL FACTURA` A/B: `abs(total)`.
- `SUBTOTAL` A/B: `abs(grava)`.
- Vencimientos A/B:
  - Vto1: `abs(saldo + deuda1)`
  - Vto2: `abs(saldo + deuda2)`
  - Vto3: `abs(saldo + deuda3)`
- Barra: además de la regla de `0448`, se oculta si `abs(saldo + deuda1) = 0`.
- SP actual devuelve:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `saldo = ISNULL(c.saldo, 0)`

Resumen corto: trunk calculaba factura y vencimientos desde `imporN + deudaN`; ahora Roisa toma `total`, `grava` y `saldo` desde `COMPROBANTES`, y los vencimientos quedan basados en `saldo + deudaN`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
