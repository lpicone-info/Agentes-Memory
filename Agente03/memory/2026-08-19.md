
## 2026-08-19 16:00 ART - wp_expe_sana / RM30192 durable context

- Usuario: Cristian. Prefiere comunicación directa y técnica en español. Agente: Stan.
- Objeto trabajado: `/mnt/Agentes/cratto/RM30192/wp_expe_sana/wp_expe_sana.srw`. Restricción explícita reiterada: no tocar ramas SVN.
- Cambio ya realizado en `wp_expe_sana.srw`: en `doubleclicked` de `dw_det`, para casos no círculo, `dw_lugar` se arma usando `THIS.object.efe_prestad[row]` cuando la fila tiene prestador efector; fallback a `exp.prestad` solo si no hay efector. Se pasa `ii_lugar` para sugerir/conservar el lugar asociado al efector de la fila. Archivo preservado UTF-16LE BOM y CRLF.
- Consulta resuelta: `is_tipo_p` representa el tipo de prestador del expediente (`exp.prestad`), no del efector. El efector de fila está en `dw_det.object.efe_prestad[row]`. Para obtener su tipo, consultar `prestadores.tipo` por ese `efe_prestad` o usar `val.of_prestador(efe_prestad, es)` y leer `es.tipo` si la estructura viene completa.
- Cambio ya realizado en `wp_expe_sana.srw`: agregada propiedad de formulario `is_tipo_p_efe` y window function `wf_set_tipo_p_efe(long al_fila)`. La función toma `dw_det.object.efe_prestad[al_fila]`, consulta `prestadores.tipo`, carga `is_tipo_p_efe`, y limpia la propiedad si no hay fila válida, efector, o consulta OK. Invocada cuando el efector se carga/cambia por ayuda, autorización, edición de `efe_prestad`, edición de `efe_nombre`, doble click sobre `dw_det`, y limpiezas por validación fallida. Validado estáticamente; no se compiló PB. En `ROISA_Prod_Test`, `prestadores.prestad = 936` devuelve `tipo = I`.
- Caso funcional actual: Base indicada por usuario `roisa_prod_text` (en ejecuciones previas se validó como `ROISA_Prod_Test`). Expediente `82883`, autorización `740068`. Problema: autorización con prestador de círculo pero lugar no asociado a círculo debería disparar en `itemchanged`: `El prestador de la orden no se corresponde con el del expediente.`
- Evidencia obtenida del caso 82883/740068: expediente `82883` tiene `expedientes.prestad = 425` y `prestadores.tipo = C`; autorización `740068` tiene `efe_sana = 936` y `efe_sana_lugar = 2`; asociación vigente de círculo encontrada en `prestad_lugares_circu`: `425 / 936 / lugar 1`; no existe vigente `425 / 936 / lugar 2`. Ejecutar `prs_valida_lugares_x_prestador_circulo('C', 1, 82883, 425, 936, 2)` devuelve `0`.
- Propuesta técnica dada al usuario: no tocar SP; corregir orden de validación en `wp_expe_sana`, dentro de `itemchanged`, `CASE 'auto'`. Luego de `val.of_autoriz(...)` y antes del bloque `IF is_tipo_p <> 'C' AND NOT lib.of_vacio(au.efe_sana) AND au.efe_sana <> exp.prestad THEN`, agregar validación prioritaria para `is_tipo_p = 'C'` usando `wf_valida_prestador(is_tipo_p, exp.ofi, exp.expe, exp.prestad, au.efe_sana, au.efe_sana_lugar, Ref li_existe)`. Si la función falla o `li_existe = 0`, mostrar `mbox(iw_ventana,"El prestador de la orden no se corresponde con el expediente",1,0)`, limpiar `THIS.object.AUTO[row] = li_null`, hacer `THIS.SetFocus()`, `THIS.SetColumn('auto')`, y `RETURN 1`.
- Punto clave de la propuesta: validar con `au.efe_sana_lugar` directo, no con `ii_lugar`, porque `ii_lugar` puede quedar afectado por la carga del combo `dw_lugar`.
- Criterio funcional: separar explícitamente `is_tipo_p = 'C'` (validar siempre círculo + efector + lugar y bloquear si falla) de `is_tipo_p <> 'C'` (recién ahí aplicar `liquida_efector_diferente`). Esto respeta Redmine/documento funcional según el análisis previo: la excepción `liquida_efector_diferente = S` aplica a expedientes no círculo, no a expediente de círculo.

## 2026-08-19 18:51 ART - RM29098/RM30181 durable context

- Usuario frecuente: Walter (`wguise`). Prefiere respuestas tecnicas, directas y en espanol; trabaja con prompts SVN, Redmine, rama productiva y salida en `\\infomed03n\Agentes\wguise\...`.
- RM29098: se genero `\\infomed03n\Agentes\wguise\R29098\ticket_29098_02_nuevo.md` desde `R-01_R29098_Documento_Especificacion_Funcional.doc`, prompt SVN `02_req_nuevo.md`, ticket Redmine 29098 y rama productiva. Informe validado: 518 lineas, UTF-8 sin BOM, LF.
- RM29098 resumen funcional: agregar una marca por tarifa para excluirla del recalculo de edad tope. El parametro general `Edad Tope Calculada` mantiene prioridad absoluta: si esta en `N`, no recalcula nunca; si esta en `S`, la nueva marca en `TARIFAS` define si la tarifa participa o queda exceptuada. Impacto: ABM Tarifas, Prefacturacion, Test de Cuota y validaciones usadas en comprobantes/impresion.
- RM29098 criterio tecnico conversado: en `afi_prefac_prefacturar` alcanza validar la tarifa con `@prepaga`, `@plan_codi` y `@tari`; no hacen falta `@tipo` ni `@etaria` porque la PK de `tarifas` en productiva es `prepaga, plan_codi, tari`. Solo usar `tipo/etaria` si la regla funcional agregara condiciones por esos atributos.
- RM29098 propuesta para `afi_prefac_prefacturar`: dentro del bloque `@var_parama_edad_tope_calculada = 'S'`, primero calcular `puede_cambiar_tope` como hoy con `afn_puede_cambiar_tope`; luego validar la nueva marca de tarifa usando `#PREFAC_CONTRATOS_PERIODO` (tiene `prepaga`, `contra`, `plan_codi`, `tari`, `tari_tipo`, `tari_cate`, `tari_tope`, `tari_vigen`) joineada desde `#PREFAC_AFILIADOS_PERIODO` por `prepaga/contra`; si la tarifa esta excluida, actualizar el mismo campo `puede_cambiar_tope = 0` para que siga el flujo actual.
- RM29098 recomendacion de funcion: `dbo.afn_excluye_recal_tari(@prepaga, @plan_codi, @tari)` devolviendo `1` si excluye y `0` si no. Usarla para forzar `puede_cambiar_tope = 0` cuando corresponda. Punto pendiente: definir nombre fisico final del campo (`excl_calc_edadT` vs `excl_calc_EdadT`) y retorno exacto de funcion.
- RM30181: se genero `\\infomed03n\Agentes\wguise\R30181\ticket_30181_01_bug.md` desde `R30181.docx`, prompt SVN `01_bug.md`, ticket Redmine 30181 y rama productiva. Informe validado: 330 lineas, UTF-8 sin BOM, LF.
- RM30181 resumen tecnico: el bloqueo se produce porque `prs_lugares_x_prestador_circulo` decide si trata el caso como circulo segun existencia en `prestad_lugares_circu`. Para invocacion `(circulo, NULL, NULL)`, si no hay relaciones vigentes cae al camino de efector, pero el efector llega `NULL`, devuelve cero filas y dispara `Prestador sin lugar de atencion cargado`, aunque el circulo tenga lugar propio vigente.
- RM30181 correccion conceptual recomendada: `prs_lugares_x_prestador_circulo` deberia decidir por presencia de `@prm_prestad_circulo`, no por existencia previa de relaciones. Punto funcional a confirmar antes de desarrollar: para expedientes de circulo sin efector seleccionado, el lugar propio vigente del circulo debe ser condicion suficiente para permitir el ingreso aunque no existan efectores relacionados.
- RM30181 codigo revisado durante el informe: `wp_expe_orde`, `wp_expe_sana`, `wp_expe_odon`, `dd_lugarxprestad_2`, `prs_lugares_x_prestador_circulo`.
