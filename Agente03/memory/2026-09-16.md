## Ticket Redmine 28621
- Usuario: wguise. Rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI`.
- Documento de atención: `\\infomed03n\Agentes\wguise\R28621\R28621.doc`.
- Informe generado en `\\infomed03n\Agentes\wguise\R28621\ticket_28621_01_bug.md`.
- Se registró el profile de base `UnoSalud_Prod` (MS SQL) en `DB_PROFILE.md`; no almacenar aquí credenciales.
- Resultado del análisis: defecto de código en `dbo.afs_afi_expor_sss`. La tabla `afi_padron_usuarios_sss` conserva `cod_preex` como `0/1`, pero el SP lo transforma con `CASE WHEN cod_preex IS NULL THEN 0 WHEN cod_preex IS NOT NULL THEN 1 END`; por eso cualquier valor `0` no nulo termina exportándose como `1`.
- Validaciones: conexión a la base OK; para periodo 31/08/2025 y prepaga 23 el lote vigente es `id_lote = 26`; tabla: 9959 registros con `cod_preex=0` y 49 con `cod_preex=1`; SP: 10008 registros con `cod_preex=1`; archivo generado ASCII/LF con 174 líneas.
- Pendiente antes de elaborar `03_spec`: validar discrepancia documental: el documento menciona lote 24, mientras la base indica lote vigente 26.


## Registro de contexto - 2026-09-16

- Se analizó el ticket Redmine 28621 sobre la rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI`, con base `UnoSalud_Prod_Test` y usuario operativo `mviciconti`.
- Se refrescó la rama a la revisión 84686, se leyó el prompt SVN `01_bug`, y se revisaron el ticket, el documento `R28621.doc` y el archivo de prueba `20250915.txt`.
- Resultado técnico: defecto confirmado en `afs_afi_expor_sss`. El procedimiento transforma `cod_preex` mediante un `CASE` que devuelve 0 solo cuando el valor es NULL y 1 cuando no es NULL; por ello un `cod_preex = 0` almacenado correctamente se lista/exporta como 1.
- Se validó en base que el lote vigente para el período y prepaga analizados era `id_lote = 26`; la tabla tenía 9959 registros con `cod_preex=0` y 49 con `cod_preex=1`, mientras el SP devolvía 10008 registros con `cod_preex=1`.
- Se generó y validó el entregable `ticket_28621_01_bug.md` en `/mnt/Agentes/mviciconti/R28621/`. El documento quedó clasificado como defecto de código. No se almacenan contraseñas ni credenciales.
- Quedó registrada como posible tarea posterior la discrepancia documental entre el lote 24 indicado en el documento y el lote vigente 26 observado en la base.


## Registro de contexto - PowerBuilder logos y objetos R30205

- Usuario trabaja en PowerBuilder 2022 sobre la pantalla `wp_gene_crede_provi` y el DataWindow `d_cred_prn_smc_picture`, asociados al ticket/R30205.
- La contra forma funcional se obtiene de `dbo.prepa_afi_reglas.contra_forma`, con vigencia `baja_fecha IS NULL`; `prepaga` es la clave principal.
- Para logos, la tabla `logos` contiene el BLOB `logo`, consultado con `SELECTBLOB logo INTO :lb_logo FROM logos WHERE prepaga = :li_prepa USING SQLCA`.
- Un control OLE suelto acepta correctamente `ole_logo.ObjectData = lb_logo`; un OLE dentro de un DataWindow no permite asignar `ObjectData` vía `lds_creden_impre.Object.ole_logo.ObjectData`, generando `type mismatch accessing external object property ObjectData`.
- El enfoque usado para el DataWindow es guardar el BLOB temporalmente en disco, detectar la extensión por firma binaria y asignar el archivo al bitmap `p_logo` mediante `lds_creden_impre.Modify("p_logo.filename='" + is_ruta_logo + "'")`. Firmas: BMP `42 4D`, PNG `89 50 4E 47`, GIF `47 49 46`, JPG `FF D8 FF`.
- Importante: PowerBuilder no reconoce `BlobLen`; la longitud del BLOB debe obtenerse con `Len(lb_logo)`. `BlobLen` produjo errores C0051 y errores derivados de condición no booleana.
- Archivos involucrados solicitados por el usuario: `\\infomed03n\\cratto\\R30205\\MiPropuesta\\d_cred_prn_smc_picture.srd` y `\\infomed03n\\cratto\\R30205\\MiPropuesta\\wp_gene_crede_provi.srw`; en el workspace se mencionó la ruta montada `/mnt/Agentes/cratto/R30205/MiPropuesta/`.
- El usuario reportó errores de importación persistentes en esos objetos. Se indicó que el SRW tenía problemas de BOM/encoding y que debía quedar UTF-16LE con un solo BOM y CRLF, pero no se pudo compilar PowerBuilder desde el entorno. Cualquier nueva edición debe preservar cuidadosamente el formato nativo del SRW/SRD y validar en el IDE PowerBuilder.
- El usuario prefiere que el agente corrija directamente los objetos y revise antes de entregarlos; comunicar explícitamente que no hay compilador PowerBuilder disponible si la validación es solo estática.


## Registro de contexto - flush 2026-09-16 16:37 ART

- El usuario volvió a reportar errores de importación en `d_cred_prn_smc_picture.srd` y `wp_gene_crede_provi.srw` del trabajo R30205.
- La memoria durable debe conservar que el problema de `BlobLen` ya fue identificado: en PowerBuilder 2022 se debe usar `Len(blob)`; no volver a introducir `BlobLen`.
- Para `wp_gene_crede_provi.srw`, las modificaciones relacionadas con logos deben usar sintaxis PowerBuilder compatible y respetar el formato nativo del objeto; el SRW debe conservar UTF-16LE con un solo BOM y finales CRLF. No se dispone de compilador PowerBuilder en este entorno, por lo que la validación final requiere importar/compilar en el IDE del usuario.
- El usuario necesita una revisión cuidadosa de ambos objetos antes de entregarlos; no afirmar compilación exitosa sin evidencia del IDE PowerBuilder.
