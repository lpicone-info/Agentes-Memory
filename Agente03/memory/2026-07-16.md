
## 2026-07-16 15:09 UTC - Flush pre-compaction

- Caso trabajado para Walter/wguise: solicitud basada en prompt SVN `branches/prompts_std/01_bug.md`, Redmine `#30095`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`, documento fuente `\\infomed03n\Agentes\wguise\R30095\R30095.doc`, salida `\\infomed03n\Agentes\wguise\R30095\ticket_30095_01_bug.md`.
- Validacion inicial detuvo el flujo porque el profile solicitado `Ospecon_Prod` no estaba registrado en `DB_PROFILE.md`; el usuario autorizo usar `Ospecon_Desarrollo`.
- Resultado generado: `/mnt/Agentes/wguise/R30095/ticket_30095_01_bug.md`.
- Hallazgo funcional principal documentado: posible perdida de `AUTORIZACIONES.pago_copa` al cambiar solo el estado del item desde bandeja; el flujo conserva el copago del item, pero la cabecera puede quedar sin tipo de pago porque en `w_clase_auto.srw` la rama de cambio de estado en modo consulta evita refrescar/preservar `dw_copa_orden.pago_copa`.
- Correccion conceptual propuesta en el informe: preservar el tipo de copago por orden cuando no corresponde recalcular importes; validar si debe tomarse de `dw_copa_orden` o recuperarse desde `AUTORIZACIONES.pago_copa` al abrir desde bandeja.
- Validaciones reportadas al usuario: archivo creado en ASCII, sin BOM, finales LF; se validaron secciones del informe, rama SVN sin cambios locales y metadata de `Ospecon_Desarrollo` para tablas/campos criticos; Redmine usado como contexto complementario y sin adjuntos via API.

## 2026-07-16 18:42 UTC - Flush pre-compaction

- Conversacion directa Google Chat con Cristian sobre archivo PowerBuilder `wp_exporta_asientos_expe`: en el `CASE 'Periodo'` no se aplicaba mascara a dato de base con formato `2015-02-01`; el usuario necesita salida `dd/mm/aaaa`.
- Diagnostico comunicado: en PowerBuilder/DataWindow la mascara correcta de anio es `yyyy`, no `aaaa`; la mascara no convierte strings, solo formatea si el valor ya es `date/datetime` o debe parsearse previamente desde `yyyy-mm-dd`.
- Tras revisar el adjunto, se identifico como fuente PowerBuilder UTF-16 `wp_exporta_asientos_expe`, no audio. El cambio local del usuario habia quitado `periodo` del grupo ignorado y agregado `CASE 'periodo'` con `String(Date(...), ls_mascara)`.
- Hallazgo clave: `idw_data_det` usa el DataWindow `d_exp_asientos_gecom_det`, y ese DataWindow no tiene columna `periodo`; por eso no toma la mascara aunque exista el `CASE`.
- Correccion propuesta al usuario: agregar `periodo` al SP `prs_exp_asientos_gecom_det`, refrescar/agregar la columna `periodo` en `d_exp_asientos_gecom_det.srd` como `datetime`, mantener el `CASE 'periodo'` en `wp_exporta_asientos_expe.srw`, y usar mascara `dd/mm/yyyy`. Si el SP devuelve `2015-02-01` como fecha/datetime, la salida esperada es `01/02/2015`.
