# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- Index candidates discussed but not applied: `impre_creden_provi_tmp(sp_id)` and `afi_credenciales(creden)` with included columns. User later requested DROP scripts; scripts were provided, no execution confirmed. [confidence=0.51 evidence=memory/2026-09-15.md:5-5]
- Initial performance hypothesis involved `ue_imprimir_creden`: `afi_impre_creden` inserts rows into `impre_creden_provi_tmp`; `lds_creden_impre.Retrieve()` invokes `afs_datos_creden`, which filters by `@@SPID`. Because Retrieve runs inside the FOR after each insert, the SP process [confidence=0.51 evidence=memory/2026-09-15.md:4-4]
- Key later finding from human testing: primary bottleneck is PowerBuilder DataWindow object `d_cred_prn_smc`, specifically nested DataWindow `dw_1`, which retrieves the logo from `logos.logo` as a BLOB through `d_logo_smc`. Removing only `dw_1` made Retrieves and Filters practical [confidence=0.51 evidence=memory/2026-09-15.md:6-6]
