# Memoria 2026-09-21

## Investigacion: check `Visto` en carga de expediente
- Usuario: Walter.
- Repositorio analizado: `wguise-Infomedical-trunk`.
- Pregunta funcional: en carga de expediente, en los items cargados aparece un check `Visto`; se queria saber cuando se visualiza y si esta ligado a importacion de expedientes.
- Conclusion comunicada: el check `Visto` se visualiza cuando el expediente tiene `expedientes.entrada` informado. En carga manual normal, con `entrada` nulo, se oculta.
- Interpretacion funcional: `Visto` actua como indicador de item visualizado/auditado para expedientes importados o generados por entrada especial.
- Evidencia relevante encontrada:
  - `d_expe_cab.srd`: campo `entrada` proveniente de `expedientes.entrada`, con valores `(Import)=I`, `Ajus p/ord=O`, `Ajus p/hon=H`, `(Trans.rem)=T`.
  - `wp_expe_orde.srw`: si `dw_cab.object.entrada[1]` es `NULL`, oculta `audi_vis` y ancho `0`; al leer/visualizar item con entrada informada marca `audi_vis = 'S'`; existe logica comentada como `marcar auditoria si es importacion` y procedimiento `pru_expe_ordenes_audi_vis`.
  - `wp_expe_hono.srw`: comportamiento equivalente para honorarios, procedimiento `pru_expe_honorarios_audi_vis`.
  - `wp_expe_sana.srw`: comportamiento equivalente para sanatorios/internaciones, procedimiento `pru_expe_internaciones_vis`.

## Cambio solicitado: check `Visto` visible siempre
- Usuario: Walter.
- Contexto: sobre la investigacion previa del check `Visto` en carga de expediente (`audi_vis`).
- Nueva necesidad funcional: quieren que el check este visible siempre, sin depender de `expedientes.entrada`.
- Analisis comunicado: el objeto `audi_vis` ya existe visible por diseno en las DataWindows; lo oculta codigo PowerBuilder cuando `dw_cab.object.entrada[1]` es nulo.
- Cambio minimo sugerido: quitar o neutralizar bloques que hacen `Object.audi_vis.Visible = '0'`, `Object.audi_vis.Width = '0'` y `Object.audi_vis_t.Visible = '0'` cuando `entrada` es null.
- Archivos principales a tocar/revisar: `wp_expe_orde.srw`, `wp_expe_hono.srw`, `wp_expe_sana.srw`; revisar tambien `wp_expe_a_audit_orde.srw` si aplica pantalla de auditoria.
- Riesgo funcional comunicado: hacerlo visible no implica marcarlo. Si quieren que tambien marque como visto en expedientes manuales, habria que remover condiciones `if not isnull(dw_cab.object.entrada[1])` donde asignan `this.object.audi_vis[row] = 'S'` y llaman a `pru_expe_ordenes_audi_vis`, `pru_expe_honorarios_audi_vis`, `pru_expe_internaciones_vis`.
- Advertencia: el segundo cambio redefine semantica de `audi_vis`, de indicador de importacion/entrada especial a indicador general de visualizacion, por lo que hay que revisar reportes, auditorias y procesos posteriores.

## Investigacion: `afs_compro_deudas_pf` no filtra por `@prm_origen`
- Usuario: Walter.
- Pregunta: por que el procedimiento `afs_compro_deudas_pf` no tiene en cuenta los origenes recibidos por ventana (`@prm_origen`) y si existe alguna regla para no filtrar por origen.
- Conclusion comunicada: no se encontro regla que anule el filtro por origen ni uso de `zz_sistema_reg`; el problema parece ser implementacion del SP.
- Flujo PB: ventana `wp_deudores_fp` arma `ls_origen` desde `dw_ori`, concatena seleccionados con coma y lo envia al retrieve. `dd_origenes_list` contempla el valor especial `'NULL'` para "Sin Origen".
- Hallazgo SQL: `afs_compro_deudas_pf` declara `@prm_origen VARCHAR(100)`; si `@prm_origen IS NULL` filtra `AND ci.ori IS NULL`; si viene informado entra al `ELSE`, pero no filtra por `ci.ori` ni por `afi.ori`.
- Otro hallazgo: el origen mostrado de salida termina pisado por `ci.ori`, por lo que tecnicamente el origen relevante del listado parece ser `compro_impresion.ori`.
- Correccion sugerida: aplicar patron de lista con `dbo.Split(@prm_origen, ',')` y contemplar `origen.token = ci.ori OR (origen.token = 'NULL' AND ci.ori IS NULL)`.
- Punto funcional pendiente: confirmar si el filtro correcto debe ser por `compro_impresion.ori` (`ci.ori`) o por `afiliados.ori` (`afi.ori`).

## Prompt 01_bug Redmine 1234
- Usuario: Walter / `wguise`.
- Solicitud recibida: generar informe desde prompt SVN `branches/prompts_std/01_bug.md`, ticket Redmine `1234`, rama SVN trunk, documento `\\infomed03n\Agentes\wguise\R1234\01.BUG.docx`, salida `\\infomed03n\Agentes\wguise\R1234\ticket_1234_01_bug.md`.
- Resultado: informe generado en `\\infomed03n\Agentes\wguise\R1234\ticket_1234_01_bug.md` con estado `BLOQUEADO`.
- Motivos comunicados: `BASE DE DATOS` vino vacia y el prompt `01_bug` la exige como precondicion; ademas Redmine `1234` no coincide con el contenido del documento `01.BUG.docx`.
- Diferencia detectada: el ticket trataba sobre Liquidaciones / Expedientes / Debitos a ordenes, mientras el documento hablaba de solicitudes Afilmed, derivaciones de aportes y sueldo oculto `0.01`.
- Validacion: archivo creado correctamente con 147 lineas.

## Prompt 01_bug Redmine 30405
- Usuario: Walter / `wguise`.
- Solicitud recibida: generar informe desde prompt SVN `branches/prompts_std/01_bug.md`, ticket Redmine `30405`, base `premedic_prod_test`, rama SVN trunk, documento `\\infomed03n\Agentes\wguise\R30405\01.BUG.docx`, salida `\\infomed03n\Agentes\wguise\R30405\ticket_30405_01_bug.md`.
- Resultado: informe generado en `/mnt/Agentes/wguise/R30405/ticket_30405_01_bug.md`.
- Desenlace del informe: `Defecto de codigo`.
- Resumen ejecutivo comunicado: el valor `0.01` se usa como mascara visual para ocultar sueldo, pero en el circuito de solicitudes de incorporacion puede terminar persistido en `soli_deriv_aportes`.
- Causa tecnica comunicada: la proteccion existente en `afu_soli_deriv_aportes` solo cubre `UPDATE/MERGE MATCHED`; no cubre inserts o clonados tecnicos. Eso permite que el titular quede con sueldo `0.01`, afectando estimacion de aportes y facturacion.
- Fuentes usadas: Redmine 30405, `01.BUG.docx`, SVN trunk y consultas de lectura sobre `Premedic_Prod_Test`.
- Validacion pendiente humana principal: confirmar si `0.01` debe ser exclusivamente valor visual y nunca persistirse, y definir como resolver inserts nuevos sin sueldo real previo.

## Redmine 30405 - cratto - sueldo tecnico 0.01 en SOLI_DERIV_APORTES
- Usuario: Cristian / `cratto`.
- Ticket: Redmine `30405`.
- Base usada segun prompt: `Premedic_prod` / `Premedic_Prod`; en un informe previo aparecio `Premedic_Prod_Test` como observacion.
- Rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`, revision analizada comunicada `84721`.
- Tema central: valor tecnico `0.01` usado para ocultamiento visual de sueldo en Solicitudes / Derivacion de Aportes.
- Conclusion tecnica conversada: antes del valor tecnico `0.01`, en un `INSERT` el sueldo se tomaba directamente de `SOLI_DERIV_APORTES.sueldo` (`s.sueldo`) y se copiaba a `AFI_DERIV_APORTES.sueldo`; el SP de impacto no recalculaba sueldo.
- Redmine/DTI no define explicitamente que hacer para alta nueva sin fuente real, pero si indica que `0.01` no debio persistirse en BBDD y que el ocultamiento debia ser visual.
- Fuente funcional sugerida por DTI para recomponer datos afectados: solicitud de afiliacion original, cuando la correspondencia sea inequivoca.
- Analisis comunicado a Cristian: si es un registro nuevo y no existe titular/fuente real, el problema conceptual esta en el circuito front/solicitud: el front no deberia mandar `0.01` como dato persistible; debe ocultar visualmente conservando sueldo real o no aplicar ocultamiento operativo en alta cuando el sueldo es necesario.
- Pantallas/objetos PB donde aparece el ocultamiento: `wp_solici.srw` con `idw_aportes` / `idw_aportes2`, DataWindows `d_soli_deriv_aportes_titu.srd` y `d_soli_deriv_aportes_fami`; tambien `wp_soli_masivas.srw`; SP auxiliar `afs_soli_deriv_aportes_inte.sql` aplica logica similar para integrantes.
- Objeto clave de ocultamiento: `d_soli_deriv_aportes_titu.srd`, cuyo retrieve devuelve `0.01` cuando `dbo.afn_soli_deriv_aportes_sueldo(soli) = 'S'`, si no devuelve `SOLI_DERIV_APORTES.sueldo`.
- Reenfoque pedido por Cristian para informe: encontrar problemas solo en `SOLI_DERIV_APORTES`, cuando se actualiza el campo sueldo/saldo para el titular; no proteger ni pensar logicas en `AFI_DERIV_APORTES`.
- Informe `01_bug` generado para cratto en `/mnt/Agentes/cratto/R30405/ticket_30405_01_bug.md` con desenlace `Defecto de codigo`; causa raiz enfocada en `SOLI_DERIV_APORTES`, `AFI_DERIV_APORTES` queda como consumidor posterior del dato incorrecto.
- Especificacion tecnica `03_spec` generada en `/mnt/Agentes/cratto/R30405/ticket_30405_03_spec.md` desde prompt SVN `branches/prompts_std/03_spec.md`.
- Spec: objeto a tocar `backend/procedures/afu_soli_deriv_aportes.sql`; scripts de datos previstos `2` (saneamiento y rollback); complejidad `Media`; estado `CON OBSERVACIONES` por diferencia de base entre analisis previo y prompt.
- Validaciones comunicadas: archivos ASCII/UTF-8 sin BOM, finales LF, working copy SVN sin cambios.

## Comprobantes: envio de PDF por API en lugar de BLOB
- Usuario: Hugo.
- Repositorio/rama analizada: `wguise-V25.08.001`.
- Flujo actual desde `wp_fac_compro_impresion` con envio de mail: Bullzip genera el PDF local; `uo_nv_factu.of_compro_envia_mail` lo lee desde disco, lo convierte a BLOB y el mailer persiste el contenido en `MAIL_ATTACH_BINARY.email_attach_binary`.
- Logica de autorizaciones PB usada como patron: generar archivo, subirlo a API, guardar la referencia en `id_archivo_imagen`, dejar `blob_file` nulo; el mailer conserva la referencia y `nvo_main_infomailer` descarga desde API al momento del envio.
- Objetos preparados/modificados para comprobantes y dejados en `/mnt/Agentes/harboleas/objetos_modificados/wp_fac_compro_mail_api/`: `uo_nv_factu.sru` y `nvuo_mailer_client.sru`.
- Cambio propuesto: en `uo_nv_factu.sru`, usar `comprobantes.path_archivo_comp` y `comprobantes.path_archivo_ec_comp` como referencias API en `lstr_blob_attach_files[].id_archivo_imagen`, con `blob_file` nulo; en `nvuo_mailer_client.sru`, evitar `UPDATEBLOB MAIL_ATTACH_BINARY` cuando hay `id_archivo_imagen`, guardando nombre y referencia.
- Confirmacion comunicada al usuario: API es mas eficiente en almacenamiento, escrituras/lecturas BLOB, crecimiento de `MAIL_ATTACH_BINARY` y procesamiento masivo; no necesariamente mas rapida por mail individual porque Infomailer agrega una descarga de red. Bullzip sigue siendo el generador del PDF; la arquitectura es Bullzip -> API -> referencia en `MAIL_ATTACH_BINARY` -> descarga de Infomailer -> SMTP.
- Validaciones previas comunicadas: no se modifico la working copy SVN; objetos con UTF-8 BOM y CRLF como originales; no se compilo en PowerBuilder por no disponer del entorno PB.


## Redmine 30405 - flujo de Sueldo Bruto al cargar contrato
- Usuario: Cristian / `cratto`.
- Pregunta: como se llena el campo `Sueldo Bruto` de la solapa `Deriv. Aportes OS` del titular al ingresar el numero de contrato.
- Flujo identificado: `wp_solici.srw` ejecuta `ds_afi_aportes.Retrieve(dw_soli.object.prepaga[1], idw_titu.object.contra[1], dw_soli.object.fecha[1], li_moti)`; el DataWindow usa `d_afi_aportes_soli` y consulta derivaciones vigentes de `AFI_DERIV_APORTES`.
- Fuente del sueldo: `AFI_DERIV_APORTES.sueldo` (`ada.sueldo` en `backend/procedures/afs_afi_aportes_soli.sql`). Luego PowerBuilder copia los resultados a la solapa titular mediante `idw_aportes.of_append(ds_afi_aportes)`.
- Ocultamiento: si `dbo.fn_get_parametros_general('Afilmed','General','Sueldo') = 'S'`, la consulta devuelve `0.01` en vez de `ada.sueldo`; por eso la pantalla muestra el valor tecnico aunque en base exista sueldo real.
- `wf_verificar_aportes_anteriores` no calcula ni carga sueldo; solo ejecuta `afu_baja_deriv_aportes_antiguos`.
- Para solicitudes existentes, `d_soli_deriv_aportes_titu.srd` tambien puede devolver `0.01` cuando `dbo.afn_soli_deriv_aportes_sueldo(soli) = 'S'`; el problema de Redmine 30405 aparece cuando ese valor visual termina persistido en `SOLI_DERIV_APORTES`.


## Redmine 28269 - exportacion de afiliados SSS y planes exclusivos OS
- Usuario identificado en la solicitud: `s.altamiranda`.
- Ticket Redmine: `28269`.
- Base indicada: `SQLSRV01\\SMC_Desarrollo` / profile operativo `smc_desarrollo`.
- Rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`.
- Documento de atencion al cliente: `R28269-Requerimiento.doc`; documento de analisis y diseno no informado.
- Informe `01_bug` generado como `ticket_28269_01_bug.md`; salida operativa comunicada en `/mnt/Agentes/s.altamiranda/R28269/`, equivalente a la ruta de red indicada.
- Resultado del analisis: `Defecto de codigo`.
- Causa raiz: en `wp_export_afi_x_sss.srw`, las tres cargas del filtro de planes no excluyen `planes.exclusivo_OS = 'S'`.
- El procedimiento `dbo.afs_afi_expor_sss` ya aplica la exclusion durante la generacion de novedades y no requiere modificacion.
- En la base `SMC_Desarrollo`, para `ASI RNAS`, se observaron 4 planes principales vigentes, de los cuales 2 son exclusivos OS.
- Especificacion `03_spec` generada como `ticket_28269_03_spec.md`.
- Alcance especificado: modificar solamente `wp_export_afi_x_sss.srw`; sin scripts SQL y sin cambios en stored procedures; complejidad baja; estado `OK`.
- Cambios tecnicos propuestos en la especificacion: agregar filtro `exclusivo_OS = 'N'` en las tres cargas de `lb_plan`; incorporar control `st_leyenda_planes` con el texto requerido; crear, registrar y destruir el control.
- La especificacion quedo preparada para revision previa al Prompt 04; no se aplicaron cambios de codigo.

## Consulta de producto por troquel
- Se consulto en `smc_desarrollo`: `select * from productos where troquel = 4041791`.
- Resultado comunicado: producto `INSULINA HUMULIN 70/30 100 UI fco.x 10 ml`, `produ = 18086`.
- Datos relevantes: `rubro = GEN` / `Genérico`; droga principal `05368`; denominacion `insulina humana`; clase/familia `00103 - Hipoglucemiante`; sin baja vigente.
- Criterio para determinar monodroga: cruzar `productos` con `produ_drogas` y `drogas`; el troquel devuelve una unica droga asociada (`05368 - insulina humana`) y `productos.droga_prin` coincide con ella.
- Clasificacion comunicada: `Genérico / Monodroga / Hipoglucemiante`.


## Redmine 30405 - actualizacion final de spec/dev sobre sueldo tecnico
- Usuario: Cristian / `cratto`; ticket Redmine `30405`; base de trabajo `Premedic_prod_test`; rama SVN trunk.
- Nueva definicion funcional incorporada a `ticket_30405_03_spec.md`: en `afu_soli_deriv_aportes`, al evaluar el sueldo del titular `tipo_aportante = 'T'`, buscar el sueldo vigente en `AFI_DERIV_APORTES` siguiendo el criterio de `afs_afi_aportes_soli`, relacionando mediante `SOLI_AFILIADOS` con `prepaga`, `contra` e `inte`, validando vigencia y `baja_fecha` respecto de `@prm_alta_fecha`.
- Para integrantes/hijos `tipo_aportante = 'G'`, se permite recibir y persistir el valor tecnico `0.01`; no se debe aplicar la proteccion destinada al titular.
- La propuesta debe cubrir `MERGE WHEN MATCHED`, `MERGE WHEN NOT MATCHED` e insert/clonado RM29367. Para titular `T`, si llega `0.01` y existe sueldo real vigente, conservar el sueldo real; para `G`, dejar pasar `0.01`.
- `04_dev` en modo `PROPONER` regenerado en `/mnt/Agentes/cratto/R30405/ticket_30405_04_dev.md`. Archivos propuestos principales: `/mnt/Agentes/cratto/R30405/propuestos/backend/procedures/afu_soli_deriv_aportes.sql` y `CHANGELOG.md`.
- Validaciones comunicadas: `PARSEONLY` correcto para SP y scripts; formato del SP ISO-8859-1 con CRLF preservado; working copy SVN limpia; no se ejecutaron DDL/DML ni commit; archivo ajeno `afu_soli_deriv_aportes - opcion1.sql` no utilizado ni modificado.
- Informe/spec/dev no deben agregar protecciones en `AFI_DERIV_APORTES`; el alcance del ticket se concentra en evitar que `SOLI_DERIV_APORTES` guarde accidentalmente `0.01` para el titular.
