# Memoria curada diaria

- Fecha: 2026-08-04
- Usuario: Walter Guise
- id_googlechat: users/112746999453197865753
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/hf8ohyaaaae

## Resumen de la actividad

### Tema 1: Informe técnico para Redmine 26993 sobre impresión de facturas

- Solicitud o contexto: Walter pidió generar `ticket_26993_01_bug.md` usando el prompt SVN `branches/prompts_std/01_bug.md`, Redmine 26993, rama productiva, documento fuente `R26993_V2.docx` y salida en `/mnt/Agentes/wguise/R26993/`.
- Respuesta o acciones del agente: Se usó `R26993_V2.docx` como evidencia principal, Redmine 26993 como contexto complementario y la rama productiva SVN como evidencia de código. Se creó `/mnt/Agentes/wguise/R26993/ticket_26993_01_bug.md`.
- Resultado: El informe diagnosticó separación incorrecta entre total fiscal del comprobante y saldo aplicado al vencimiento. Se dejó asentado que `afi_compro_impresion.sql` calcula y persiste importes, deuda y créditos, pero los DataWindows los interpretan distinto por formato: algunos no descuentan créditos en primer vencimiento y otros afectan subtotal/TOTAL FACTURA con créditos.
- Validaciones: Archivo creado, lectura inicial OK, 317 líneas, texto ASCII, finales LF. No se ejecutó validación contra base porque `BASE DE DATOS` vino vacío.
- Pendiente o condicionado: Se indicó que requiere validación humana confirmar alcance exacto por cliente/formato y casos donde el saldo deudor debe sumarse al primer vencimiento.
- Estado: Pendiente

### Tema 2: Diagnóstico de pérdida aparente de caren.dw_titu en wp_solici.ue_carencias

- Solicitud o contexto: Walter mostró una secuencia PowerBuilder en la que se asigna `caren.dw_titu = idw_titu`, se ejecuta `THIS.TRIGGEREVENT('ue_aceptar')` y luego `capre.of_caren(caren)`, observando que `caren.dw_titu` llegaba vacío. Preguntó si `TriggerEvent` podía perder valores y pidió revisar la rama productiva, ventana `wp_solici`, evento `ue_carencias`.
- Respuesta o acciones del agente: Se explicó primero que `TriggerEvent` es sincrónico y no elimina variables locales por sí mismo. Luego se revisó `wp_solici.srw` en la rama productiva. Se verificó que `caren.dw_titu` apunta al mismo objeto que `idw_titu`, por lo que si el flujo disparado por `ue_aceptar` recarga o resetea la ventana, la referencia sigue viva pero apunta a un objeto ya modificado.
- Resultado: Se ubicó evidencia en `wp_solici.srw`: asignación de `caren.dw_titu = idw_titu`, ejecución de `THIS.TRIGGEREVENT('ue_aceptar')`, llamada posterior de `ue_aceptar` a `ue_grabar`, y ejecución de `THIS.TRIGGEREVENT('ue_cambia_moti')` aun con `ib_cp = TRUE`. También se señaló que otros caminos pueden disparar `ue_consulta`, donde hay `Retrieve` de `dw_soli` e `idw_titu`.
- Estado: Resuelto

### Tema 3: Propuesta de solución para evitar limpieza antes de capre.of_caren(caren)

- Solicitud o contexto: Walter pidió una solución porque `capre.of_caren(caren)` recibe `caren` vacío y consultó si se podía resolver con `RowCopy` en lugar de asignar referencias.
- Respuesta o acciones del agente: Se propuso como primera alternativa de menor impacto corregir la secuencia para evitar que `ue_grabar` ejecute `ue_cambia_moti` durante el flujo especial de carencias/preexistencias. La recomendación fue mover `THIS.TRIGGEREVENT('ue_cambia_moti')` dentro del `IF NOT ib_cp`, junto con `ue_extras`, y si hace falta limpiar luego de grabar carencias hacerlo al final de `ue_carencias` después de `capre.of_caren(caren)`.
- Resultado: Se desaconsejó usar `RowCopy` como primera solución porque `caren.dw_titu` está declarado como `dw_clase`, una referencia a DataWindow visual; copiar filas a un `ds_clase` exigiría cambiar tipos o agregar campos nuevos en `str_caren_preexis`, impactando también `uo_nv_caren_preexis`.
- Estado: En curso
