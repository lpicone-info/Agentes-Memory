# Memoria de conversación — Gabriel Pulka

## Datos

- **Fecha:** 08/09/2026
- **Usuario:** Gabriel Pulka
- **ID Google Chat:** `users/106028274881752592012`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 10
- **Inicio:** 10:48:43
- **Fin:** 17:11:42

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/_rsuhyaaaae`

## Archivos revisados

- `5075fffe-85d5-42ae-ae7b-c810be236bcd.jsonl`

---

## Transcripción completa

### [08/09/2026 10:48:43] Gabriel Pulka

hola

---

### [08/09/2026 10:50:28] Agente

Hola Gabriel, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [08/09/2026 10:55:52] Gabriel Pulka

ponete en contexto de gpulka trunk
en el batch de facturacion electronica wp_main_facturacion_electronica_batch
a partir de unos cambios de versiones de tls , se queda en el upload, seguro no conecta con el web service
haceme un resumen de la secuencia de pasos y estados por los que va pasando, podes usar pruebascoreinfomedical para decirme tambien la url del servicio

---

### [08/09/2026 11:03:55] Agente

Gabriel, en `gpulka-Coomeva-trunk`, el batch `wp_main_facturacion_electronica_batch` usa esta URL real en `PruebasCoreINFOMEDICAL`:

`https://wscenf.cen.biz/isows/InvoiceService?wsdl`

La toma desde `AFILMED_PARAMS`: `Afilmed / FacturacionElectronica.url`.

**Secuencia**
1. Arranca `facturacion_electronica_batch`, abre login batch y luego `wp_main_facturacion_electronica_batch`.
2. En cada timer conecta a BD, ejecuta `ue_procesar()` y después se desconecta.
3. Inicializa el WS leyendo `FacturacionElectronica.url`.
4. Trae datos generales: usuario WS, password WS, `company_id`, `account_id`, rutas PDF/XML.
5. Trae entes que facturan.
6. Recorre métodos en este orden: `UPLOAD`, `DOCUMENT_STATUS`, `PDF`, `SIGNED_XML`, `FAIL`, `REJECTED`.
7. En `UPLOAD`, antes de enviar, ejecuta `afu_fact_elect_actualiza_contenido`: los comprobantes `INSERTED` pasan a `PENDING` con `xml_contenido`.
8. Luego busca pendientes `PENDING` por ente y llama `of_upload()`.
9. Si Carvajal responde `UploadResponse`, toma `transactionId` y actualiza a `UPLOADED`.
10. Si falla conexión/TLS/send/respuesta XML, no actualiza a `UPLOADED`; queda en `PENDING` y vuelve a intentarlo.
11. `DOCUMENT_STATUS` toma `UPLOADED`, `RECEIVED` o `RETRY`; consulta estado y puede pasar a `ACCEPTED`, `RECEIVED`, `RETRY`, `FAIL` o `REJECTED`.
12. `PDF` toma `ACCEPTED` con `pdf = 0`, descarga PDF y marca `pdf = 1`.
13. `SIGNED_XML` toma `ACCEPTED` con `signed_xml = -1`, descarga XML firmado y marca `signed_xml = 1`.
14. `FAIL` y `REJECTED` re-solicitan generando un nuevo registro `INSERTED` y dando de baja el anterior, con tope de 20 intentos y solo últimos 15 días.

Punto técnico del síntoma: el envío HTTP no usa `HTTPClient` de PB, usa OLE `Msxml2.XMLHTTP.6.0` en `uo_nv_conexion_https`. Si cambió TLS, el problema puede estar en Windows/MSXML/Schannel de la máquina donde corre el batch. El fallo esperable queda atrapado en `of_send()` como `Error en Send`, y funcionalmente el comprobante queda en `PENDING`.

Validé desde este entorno que el endpoint negocia TLS 1.2 y TLS 1.3 correctamente. La prueba HTTP simple llegó al host y devolvió `405` por usar `HEAD`, pero el handshake TLS fue correcto. En la base vi actualmente: `PENDING = 1508`, `UPLOADED = 7081`, `FAIL = 1442`, `REJECTED = 471`.

---

### [08/09/2026 11:50:17] Gabriel Pulka

hola

---

### [08/09/2026 11:50:25] Agente

Hola Gabriel. Estoy disponible.

---

### [08/09/2026 11:50:34] Gabriel Pulka

hay una maquina que les falla en esta linea, y otra que si anda, cual podra ser la diferencia? alguna dependencia q falte instalar?
ioo_xmlhttp.send(is_body) en uo_nv_conexion_https

---

### [08/09/2026 11:51:20] Agente

**Evidencia**
La línea `ioo_xmlhttp.send(is_body)` usa `Msxml2.XMLHTTP.6.0`, no `HTTPClient` de PowerBuilder. Si llegó hasta `send`, MSXML 6 probablemente está instalado y el objeto OLE se pudo crear.

El endpoint actual es:

`https://wscenf.cen.biz/isows/InvoiceService?wsdl`

Desde este entorno el host negocia `TLS 1.2` y `TLS 1.3`; `TLS 1.0/1.1` no sirven para este caso.

**Hipótesis Más Probable**
La diferencia entre la máquina que falla y la que anda está en Windows, no en el batch:

1. TLS 1.2 deshabilitado para cliente en Schannel.
2. Windows viejo o sin updates de seguridad/cipher suites.
3. Certificados raíz/intermedios desactualizados, especialmente cadena GoDaddy.
4. Proxy distinto por usuario Windows o por configuración de Internet Options.
5. Firewall/antivirus interceptando HTTPS.
6. Diferencia 32/64 bits con `msxml6.dll`, menos probable si el objeto abre y falla recién en `send`.

No parece una dependencia funcional del sistema Afilmed. Si faltara MSXML, fallaría antes, en `ConnectToNewObject("Msxml2.XMLHTTP.6.0")`.

**Qué Comparar**
En ambas máquinas revisar:

```bat
reg query "HKCR\Msxml2.XMLHTTP.6.0\CLSID"
reg query "HKLM\SYSTEM\CurrentControlSet\Control\SecurityProviders\SCHANNEL\Protocols\TLS 1.2\Client"
```

Valores esperados para TLS 1.2 cliente:

```text
Enabled = 1
DisabledByDefault = 0
```

También validar desde la máquina que falla con el mismo componente:

```js
var x = new ActiveXObject("Msxml2.XMLHTTP.6.0");
x.open("GET", "https://wscenf.cen.biz/isows/InvoiceService?wsdl", false);
x.send();
WScript.Echo(x.status + " " + x.statusText);
WScript.Echo(x.responseText);
```

Ejecutarlo con:

```bat
cscript //nologo test-msxml.js
```

Si eso falla igual, queda confirmado que el problema está en MSXML/Windows/TLS/proxy/certificados, no en PowerBuilder ni en el XML del upload.

---

### [08/09/2026 17:06:58] Gabriel Pulka

redmine 30315, realiza en la carpeta de gabriel el script propuesto, sin variables ni selects de antes y despues

---

### [08/09/2026 17:11:42] Agente

Gabriel, dejé el script en [R30315M11944.sql](/mnt/Agentes/gpulka/R30315M11944.sql).

Contenido ajustado a lo pedido: solo `UPDATE`, sin `DECLARE`, sin `SELECT` antes/después, sin `;` y sin `RTRIM`. No ejecuté el script contra la base.

Validé:
- Redmine `30315` con credencial `gpulka`.
- Caso: contrato `957505`, inte `01` a cónyuge, inte `04` titular.
- `PruebasCoreINFOMEDICAL`: no hay responsable pagador vigente desde `2026-02-01`; el script igual deja cerrado cualquier vigente que pudiera existir.
- Archivo ASCII con LF.
- Parse SQL OK con `SET PARSEONLY ON`, 3 batches.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
