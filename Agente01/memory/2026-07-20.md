
## 2026-07-20 17:05 UTC - Solicitudes automaticas RM29363 / traslado a contrato nuevo

- Usuario en Google Chat directo: Savina. Prefiere respuestas funcionales, directas, en español, con poco detalle tecnico salvo que lo pida.
- Tema activo: Version 104 - solicitudes automaticas - RM29363. Desarrollo/proceso de XML para traslados a contrato nuevo y comparacion con traslado a contrato existente.
- Archivo generado previamente: `/mnt/Agentes/svarela/RM29363_XML_solicitudes_automaticas_traslado_contrato_nuevo.sql`.
  - Contiene XML generico completo para `soli_moti = 47`.
  - Contiene XML con datos reales de `InstallQA_89`: origen `prepaga 1 / contrato 1000996 / integrante 02`, afiliada `CC 1130627214 DIANA YURANY VILLA GOMEZ`.
  - Wrapper SQL con `EXEC dbo.afi_insertar_soli_auto` comentado para evitar insertar en `SOLI_AUTO_*`.
  - Validaciones hechas: existen `dbo.afi_insertar_soli_auto` y `dbo.afi_insertar_soli_auto_traslado_nuevo`; `id_soli_externa = 293630302` no existe en `SOLI_AUTO_CONTRATO` ni `GMS_CAB`; XML compila como tipo `XML`; archivo UTF-8 sin BOM y LF.
- Repositorio/codigo consultado: `/home/luispicone/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas/backend/procedures/`.
  - Referencias usadas: `afi_insertar_soli_auto_traslado_nuevo.sql`, `afu_soli_auto_app_contactos_gmstxt.sql`, `gms_genera_solicitudes.sql`, `afu_afi_domicilios_contra.sql`.
- Respuestas funcionales ya dadas:
  - Parrafo macro para documento funcional de traslado a contrato nuevo: proceso masivo recibe informacion de app Coomeva para generar solicitudes automaticas de traslado desde contrato origen hacia nuevo contrato destino; registra cabecera, datos del nuevo contrato, integrantes, relacion con contrato anterior, contacto, facturacion y anexos; usa circuitos habituales de validacion e impacto; contempla creacion del nuevo contrato destino.
  - Estructura de XML por titulos para traslado a contrato nuevo:
    1. `<solicitud>` cabecera principal.
    2. `<facturacion>` datos de facturacion del nuevo contrato.
    3. `<contactos>` agrupador de domicilios, emails y telefonos.
    4. `<domicilio>` domicilio segun `tipo_reg`.
    5. `<email>` correo segun `tipo_reg`, puede incluir `envia_factura`.
    6. `<telefono>` telefono segun `tipo_reg`.
    7. `<responsable_pagador>` datos del responsable de pago.
    8. `<empleado>` datos del empleado cuando corresponda.
    9. `<integrantes>` bloque principal agrupador de afiliados trasladados.
    10. `<integrante>` subtitulo/registro repetible por cada afiliado.
    11. `<anexos>` bloque agrupador de anexos.
    12. `<anexo>` subtitulo/registro repetible por cada anexo.
  - Aclaracion importante: `<integrantes>` es el titulo principal/agrupador; `<integrante>` es subtitulo o registro repetible dentro del bloque. Igual criterio para `<anexos>` y `<anexo>`.
- `tipo_reg` en contactos/domicilios:
  - `C` = Contrato. Domicilio/email/telefono del contrato; para este desarrollo se usa como domicilio de facturacion/cobro del contrato.
  - `R` = Responsable pagador.
  - `E` = Empleado.
  - `I` = Integrante, debe venir asociado a `inte`, por ejemplo `<domicilio tipo_reg="I" inte="01" ... />`.
- Domicilio de facturacion/cobro:
  - En XML se declara dentro de `<contactos>` como `<domicilio tipo_reg="C" ... />`, no dentro de `<facturacion>`.
  - Proceso automatico: primero `SOLI_AUTO_DOMICILIOS`; luego `GMSTXT_DOMICILIOS` / `GMSDEF_DOMICILIOS`; al impactar queda en `AFI_DOMICILIOS_CONTRA`.
  - En GMS, `tipo_reg = 'C'` pasa funcionalmente a `domi_tipo = '1'` y `corresp_cobro = 'O'`.
  - En proceso manual: primero `SOLI_DOMICILIOS_CONTRA`; luego al impactar tambien termina en `AFI_DOMICILIOS_CONTRA`.
- Email de facturacion:
  - Corresponde al email del contrato, no al responsable pagador ni al integrante.
  - En XML se representa con `<email tipo_reg="C" email_tipo="1" email="contrato293630302@dominio.com" envia_factura="S" />`.
  - `envia_factura="S"` representa check Enviar factura marcado; `envia_factura="N"` no marcado.
  - No tratarlo como automatico en el XML: para email de contrato `tipo_reg="C"` debe informarse `envia_factura` con `S` o `N`; si no viene, la validacion lo rechaza como dato incompleto.
  - Persistencia: primero `SOLI_AUTO_EMAILS`; luego `GMSTXT_EMAILS` / `GMSDEF_EMAILS`; al generar solicitud `SOLI_EMAILS_CONTRA`; al impactar queda en `AFI_EMAILS_CONTRA`, campo clave `AFI_EMAILS_CONTRA.envia_factura`.

## 2026-07-20 20:53 UTC - Solicitudes automaticas RM29363 / Traslado a contrato nuevo

- Usuario en Google Chat: Savina. Está documentando y probando `version 104 - solicitudes automaticas - RM29363`, especialmente `soli_moti = 47` traslado a contrato nuevo en `InstallQA_89`.
- Se generó/indicó previamente el archivo `/mnt/Agentes/svarela/RM29363_XML_solicitudes_automaticas_traslado_contrato_nuevo.sql` con XML genérico y XML real. También se creó/ajustó `/mnt/Agentes/svarela/RM29363_selects_verificacion_traslado_contrato_nuevo.sql` para verificar por contexto de proceso usando `id_proceso`, `id_soli_externa`, solicitud, contrato, documento, etc., evitando traer histórico no relacionado.
- Estructura funcional XML de traslado a contrato nuevo: nodo raíz `<solicitud>`, `<facturacion />`, `<contactos>` con `<domicilio />`, `<email />`, `<telefono />`, `<responsable_pagador />`, `<empleado />` si aplica, `<integrantes>` con subtítulos repetibles `<integrante />`, y `<anexos>` con subtítulos repetibles `<anexo />`.
- `tipo_reg` en contactos: `C` = contrato/facturación/cobro; `R` = responsable pagador; `E` = empleado; `I` = integrante. No es obligatorio enviar todos los tipos. Si no aplica, omitir el tag; no enviar atributos vacíos. Para `tipo_reg="I"` debe venir `inte`.
- Para traslado a contrato nuevo son mínimos esperados en contactos: domicilio de contrato `domicilio tipo_reg="C"` y email de contrato/facturación `email tipo_reg="C"`. Enviar factura va en el email de contrato: `envia_factura="S"` o `"N"`; si falta, valida como dato incompleto.
- Domicilio de facturación: en XML va bajo `<contactos>` como `domicilio tipo_reg="C"`; se guarda primero en `SOLI_AUTO_DOMICILIOS`, luego `GMSTXT_DOMICILIOS/GMSDEF_DOMICILIOS`, en solicitud como `SOLI_DOMICILIOS_CONTRA`, y al impactar en `AFI_DOMICILIOS_CONTRA`. En GMS `tipo_reg='C'` se convierte funcionalmente a domicilio de contrato/cobro.
- Email de facturación: XML `email tipo_reg="C" email_tipo="1" email="..." envia_factura="S|N"`; fluye por `SOLI_AUTO_EMAILS`, `GMSTXT_EMAILS/GMSDEF_EMAILS`, `SOLI_EMAILS_CONTRA`, y al impactar `AFI_EMAILS_CONTRA.envia_factura`.
- Si no se envía domicilio/email/teléfono del integrante `tipo_reg="I"`, el desarrollo actual de `soli_moti=47` no los completa automáticamente desde Registro Único porque `afu_soli_auto_app_contactos_gmstxt` toma solo contactos del XML y hace `RETURN` antes de la lógica genérica de Registro Único. Para que quede en la solicitud impactada, hay que enviarlo en XML o ajustar backend para tomar `anter_prepa`, `anter_contra`, `anter_inte` desde `SOLI_AUTO_INTEGRANTES` y completar desde `REGISTRO_UNICO_*` cuando falte.
- Forma de pago/facturación: en manual se carga desde ventana PowerBuilder y termina en tablas de solicitud como `SOLI_PAGO_FORMAS`; en automático viene en `<facturacion emi_grupo="..." emi_modo="..." vto_grupo="..." pago_forma="..." banco="" banco_suc="" tipo_cta="" cta="" tarje="" titular="..." />`, primero en interfaz automática y luego GMS/SOLI/AFI. Domicilio/email de facturación no van en `<facturacion>`, van en `<contactos>` tipo `C`.
- Observaciones: manual usa botón Observaciones (`wp_soli_observaciones`, `wp_alta_total.srw`, `wf_grabar_observaciones`, `afu_soli_observaciones`) y graba en `SOLI_OBSERVACIONES`. Automático no toma observación desde XML hoy; al generar solicitud inserta observación estándar en `SOLI_OBSERVACIONES` desde `gms_genera_solicitudes`.
- Habeas Data: en XML va como atributo de cabecera `<solicitud habeas_data="S|N" ...>`. Manual lo inicializa marcado (`S`). Automático lee `/solicitud/@habeas_data` y guarda en `SOLI_AUTO_CONTRATO.habeas_data`; si `S`, GMS graba Habeas Data en solicitud; si `N`, no. Si no viene, default actual es `N`. En venta nueva masiva (`soli_moti=1`) Habeas Data es obligatorio en datos principales/GMS (`GMSTXT_MULTI/GMSDEF_MULTI.habeas_data`); Enviar Factura va en emails (`GMSTXT_EMAILS/GMSDEF_EMAILS.envia_factura`) para `tipo_reg='C'`.
- Tablas recomendadas para control de traslado a contrato nuevo: interfaz `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_RESP_PAGADOR`, `SOLI_AUTO_EMPLEADO`, `SOLI_AUTO_DOMICILIOS`, `SOLI_AUTO_EMAILS`, `SOLI_AUTO_TELEFONOS`, `SOLI_AUTO_ANEXOS`, `SOLI_AUTO_FACT_AUDIT`; intermedias `GMS_CAB`, `GMSTXT/GMSDEF_MULTI`, `INTEGRANTES`, `CLASE_PLAN`, `DOMICILIOS`, `EMAILS`, `TELEFONOS`, `RESP_PAGADOR`, `ADICIONALES`; solicitud `SOLICITUDES`, `SOLI_AFILIADOS`, `SOLI_CLASE`, `SOLI_PLANES`, `SOLI_TITULARES`, `SOLI_PAGO_FORMAS`, `SOLI_DOMICILIOS_CONTRA`, `SOLI_EMAILS_CONTRA`, `SOLI_DOMICILIOS`, `SOLI_EMAILS`, `SOLI_TELEFONOS`, `SOLI_RESP_PAGADOR*`, `SOLI_HAB_DATA_EXCEDENTE`, `SOLI_ADICIONALES`, `SOLI_PARENTESCOS`, `SOLI_SEGUIMIENTO`, `SOLI_OBSERVACIONES`; finales `AFI_AFILIADOS`, `AFI_CLASE`, `AFI_PLANES`, `AFI_TITULARES`, `AFI_PAGO_FORMAS`, `AFI_DATOS_CONTRATO_GRAL`, `AFI_DOMICILIOS_CONTRA`, `AFI_EMAILS_CONTRA`, `AFI_DOMICILIOS`, `AFI_EMAILS`, `AFI_TELEFONOS`, `AFI_RESP_PAGADOR`, `AFI_ADICIONALES`, `AFI_HISTO_PAREN`, `AFI_ESTADOS`, `AFI_ESTADOS_CONTRA`, `AFI_CARENCIAS`, `AFI_SUBSIDIOS_CONCEP_CONTRA`.
- Caso probado `id_soli_externa=5000` en `InstallQA_89`: existe en `SOLI_AUTO_CONTRATO` con `id_proceso=123`, `soli_moti=47`, `soli=NULL`, `prepaga=1`, `contra=1`, `proce_fecha=2026-07-20 17:02:43`; no generó solicitud ni contrato. GMS_CAB `cab_id=295768`, `estado=E`, `puede_procesarse=N`, `canti_errores=11`, observación `id_proceso=123 / id_soli_externa=5000`. Errores: faltan domicilio/teléfono/email de integrante o Registro Único vigente; además existe cambio de titularidad futuro al `01/08/2026` en contrato origen `1000996` para el beneficiario `CC 1130627214 DIANA YURANY VILLA GOMEZ`.
- Candidato alternativo validado en `InstallQA_89` para procesar traslado a contrato nuevo: origen `prepaga=1`, `contrato=1036112`, `integrante origen=02`, documento `CC 43759566`, nombre `SOL MUNERA`/`SOL MUNERA CORTES`, parentesco actual `C`, fecha aplicación sugerida `2026-07-01`. Validaciones: afiliado vigente, no titular en origen, queda titular remanente activo, tiene domicilio/email/teléfono en Registro Único, sin cambio de titularidad futuro para ese integrante.
- XML completo entregado para el candidato `1036112` con `id_soli_externa="5001"`, `soli_moti="47"`, fechas `2026-07-01`, cuenta destino `2`, subcuenta `FSMP`, plan/tarifa `OPLF/FPLUNQ`, asesor `20`, sucursal `76001`, `habeas_data="N"`, `vip="N"`, campaña `134`; facturación `emi_grupo=1`, `emi_modo=1`, `vto_grupo=2`, `pago_forma=E`, titular `SOL MUNERA CORTES`; contactos tipo `C`, `R` e `I` completos; responsable pagador `CC 43759566`; integrante destino `inte="01"`, `paren_destino="T"`, `anter_prepa="1"`, `anter_contra="1036112"`, `anter_inte="02"`, documento `CC 43759566`, `MUNERA CORTES SOL FARLEY`, `sexo=F`, `naci_fecha=1978-09-20`, `obra_soc=129`, `antig_fecha=2025-01-21`, `ingre_fecha=2026-07-01`; anexos vacío.
