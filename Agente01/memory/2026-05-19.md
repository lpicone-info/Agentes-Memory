# Memory - 2026-05-19

## Cierre de tramo - Discord DM / contexto bmora
- Interlocutora indicada por el contexto de chat como Belkis / usuario `bmora_info`; no usar nombre propio ni asumir identidad en futuras sesiones salvo metadata confiable o identificación explícita del runtime.
- Pedido de cierre recibido: "por este momento terminamos, guarda todo lo que vimos en tu memoria persistente".
- No hubo historial recuperable de mensajes recientes en la sesión actual; por eso este registro consolida el estado operativo relevante disponible en memoria y contexto persistente, sin inferir detalles no observados.

## Estado operativo relevante para retomar
- Contexto principal asociado a `bmora`: Redmine `29194` sobre cambio automático Asociado/Familiar en Afilmed.
- Se había analizado el DEF de facturación para CC automático Asociado/Familiar contra el sistema vigente.
- Resultado recordado: el flujo está implementado sustancialmente en prefacturación/verificación, con lógica central en `afi_cambio_cober_cooperativa` y generación de solicitudes por GMS tipo `CC_COOP`.
- Diferencia crítica previamente detectada: la exclusión de casos donde la vigencia del plan coincide con la vigencia de la solicitud aparece comentada en el procedimiento, por lo que actualmente no se aplica.
- Para el alcance de eventos desde `01/08/2024`, no quedó evidencia de lógica explícita ya implementada; requiere definición funcional/técnica precisa si se continúa.

## Repositorios y credenciales operativas
- Para documentación Afilmed asociada a `bmora`, usar la working copy SVN registrada de documentación y sus credenciales/configuración aisladas.
- Para código Afilmed asociado a `bmora`, usar la working copy SVN registrada de `bmora-Coomeva-trunk` y sus credenciales/configuración aisladas.
- No exponer credenciales, API keys ni detalles internos en Discord.

## Pendientes para próxima continuidad
- Confirmar si Redmine `29194` sigue siendo el foco de trabajo.
- Si se retoma `29194`, validar funcionalmente si debe reactivarse o rediseñarse la exclusión por misma vigencia que hoy está comentada.
- Definir fuente y criterio exacto para "eventos desde 01/08/2024" antes de proponer cambios de código.
- Mantener el criterio de vigencia: en tablas con `baja_fecha`, analizar por defecto registros vigentes con `baja_fecha IS NULL`, salvo pedido explícito de incluir bajas.

## Cierre de tramo - gpulka / Redmine 29362
- Interlocutor no identificado de forma confiable por metadata como Luis; responder genéricamente sin usar nombre propio salvo identificación explícita.
- Tarea trabajada en working copy SVN `gpulka`, `Version104`, vinculada a Redmine `29362`.
- Commit SVN `r36885`: ajuste inicial para `29362`.
  - `backend/tables/SOLI_AUTO_INTEGRANTES.sql`: los campos `anter_prepa`, `anter_contra`, `anter_inte` quedaron directamente en el `CREATE TABLE`; se quitaron de `ALTER TABLE`.
  - `backend/procedures/afu_genera_soli_auto_app.sql`: para `soli_moti = 48`, `SOLI_AUTO_CONTRATO` no trae `cuenta_dest`, `subcta_dest`, `plan_dest`, `tari_dest`, `plan_emp_dest`, `tari_emp_dest`; se calculan desde el contrato destino (`contra` del XML) a `fecha_aplicacion`.
  - `Redmines/29362/29362.md`: documentado el cambio.
- Commit SVN `r36886`: corrección del cálculo de cobertura destino para usar el patrón correcto de otros SPs.
  - En `AFI_CLASE/AFI_PLANES` usar la máxima `vigen_desde` no dada de baja para `prepaga + contra`, con `vigen_desde <= fecha_aplicacion` y baja no vigente a esa fecha.
  - Se reemplazó el enfoque previo de `SELECT TOP 1 ... ORDER BY vigen_desde DESC`.
- Validaciones realizadas durante el tramo:
  - Se preservaron encoding/BOM/finales de línea.
  - SQL revisado sin `; `ni `RTRIM`.
  - No había cliente SQL Server local inicialmente para `PARSEONLY`.

## Pruebas SQL - soli_moti 48 / GMS
- Se analizó un script de prueba en base `InstallQA_89` con `ROLLBACK` para `soli_moti = 48`.
- Primer diagnóstico: los datos no estaban ausentes; el script verificaba contra `contra = '200734'`, pero el XML usaba `contra="200"`.
- Resultado observado por `cab_id`:
  - `GMS_CAB.id` ejemplo: `295203`.
  - `GMSTXT_CLASE_PLAN.contra = 200`.
  - `GMSTXT_ADICIONALES.contra = 200`.
  - Clase/plan ejemplo: `cuenta=2`, `subcta=FSMP`, `plan=OROF`, `tari=FORON`.
  - Adicional ejemplo: `inte=01`, `plan_codi_adi=ANA01`, `tari_adi=TARANA01`.
- Se recomendó verificar por `cab_id` generado desde `GMS_CAB`, no por contrato fijo ni por `MAX(id)` si hay riesgo de múltiples pendientes.
- El script corregido con `EXEC dbo.afi_insertar_soli_auto @var_data_xml = @x` y filtros por `contra = '200'` funcionó aparentemente; revisión posterior en `InstallQA_89` con `ROLLBACK` dio:
  - `id_proceso=1`, `cab_id=295207`.
  - `GMS_CAB=1`, `GMSTXT_CLASE_PLAN=1`, `GMSTXT_INTEGRANTES=1`, `GMSTXT_ADICIONALES=1`.
  - Datos principales: `contra=200`, `cuenta=2`, `subcta=FSMP`, `plan=OROF`, `tari=FORON`, `fecha_ini_servicio=20260501`, `asesor=1234`.
  - Observaciones: `GMSTXT_ADICIONALES` puede copiar `plan_codi_sup/tari_sup` desde adicional si no es plan superador; `transaccion` queda `INC` por columnas/parámetros `VARCHAR(3)`.

## Consulta de horas laborables
- Se calculó `126 hs` laborables desde `26/05/2026` asumiendo jornada de 8 horas y sin feriados.
- Resultado comunicado: martes `16/06/2026`, a las 6 horas de jornada; si inicia 09:00, sería `16/06/2026 15:00`.

## Análisis pendiente/importante - circuito GMS para traslados 47/48
- El usuario pidió analizar el circuito GMS completo: desde `GMSTXT`, validaciones, pase a `GMSDEF`, más validaciones, `gms_genera_solicitudes`, generación en `SOLICITUDES/SOLI_XXX` e impacto `afi_XXXX`.
- Foco funcional: solicitudes de traslado a contrato nuevo y traslado a contrato existente, comparando contra ventanas `wp_alta_parcial` y `wp_alta_total`.
- Conclusión comunicada:
  - `47` debe comportarse como Alta Total con traslado: transacción tipo `VTAN`, pero `soli_moti = 47`.
  - `48` debe comportarse como Alta Parcial/Inclusión con traslado: transacción tipo `INC`, pero `soli_moti = 48`.
  - GMS valida mayormente por `transaccion`, no por semántica completa de `soli_moti`.
  - `gms_traer_cod_transaccion` no tiene transacciones propias para `47/48`.
  - `gmsdef_val_alta` llama `gmsdef_val_alta_total`, pero `gmsdef_val_alta_parcial` está comentado.
  - Para `48`, al entrar como `INC`, hoy no ejecuta validaciones específicas de Alta Parcial.
  - `GMSTXT_INTEGRANTES/GMSDEF_INTEGRANTES` solo tienen `anter_prepa`; no tienen `anter_contra`, `anter_inte`, `anter_plan`, insuficiente para traslado real si hay más de una afiliación posible.
  - `gms_genera_solicitudes` tiene agregados parciales para `48`, pero `47` está incompleto; varios bloques de impacto excluyen `47/48`.
- Validaciones faltantes o a reforzar para ambos `47/48`:
  - Armar estructura equivalente a `#INTEGRANTES_ANTERIORES`.
  - Validar existencia y vigencia de origen `anter_prepa + anter_contra + anter_inte` a `fecha_aplicacion`.
  - Validar documento contra integrante origen.
  - Ejecutar o encapsular lógica de `afs_val_traslado_aceptada`.
  - Validar cambio de titularidad futura, baja total/parcial derivada, morosidad cuando corresponde, cobertura destino vigente, edad contra grupo de plan, EPS/obra social, aceptación contratante/auditoría, preexistencias/estado salud, reglas de afiliación, campañas y asesor.
- Específico `47`:
  - Validar datos de Alta Total: responsable pagador, domicilio/email/teléfono, forma de pago, titular, clase/plan, adicionales, facturación.
  - Validar subcuenta inhabilitada con rechazo directo, como ventana.
- Específico `48`:
  - Validar contrato destino vigente, no duplicidad del integrante, parentesco destino/real, cobertura destino desde contrato vigente y no desde XML, y no ambigüedad del origen.
- Propuesta SPs:
  - `gmsdef_val_traslado`: dispatcher para `soli_moti IN (47,48)`.
  - `gmsdef_val_traslado_origen`: valida origen y arma/simula `#INTEGRANTES_ANTERIORES`.
  - `gmsdef_val_traslado_aceptada`: wrapper SQL de `afs_val_traslado_aceptada`.
  - `gmsdef_val_traslado_destino`: destino nuevo para `47`; contrato existente/composición/no duplicidad para `48`.
  - `gmsdef_val_traslado_cobertura`: cobertura destino, reutilizando `gmsdef_valida_clase_plan`, `gmsdef_valida_clase_plan_adi`, `afs_cta_subcta_plan_tari_hab`.
  - `gmsdef_val_traslado_reglas_afil`: reglas de afiliación sobre datos materializados en `GMSDEF_*`.
  - `gmsdef_val_traslado_auditoria`: estado salud, preexistencias, aceptación contratante y preguntas obligatorias si se impacta directo.
  - Extender `gmstxt_pasar_a_def` y tablas `GMSTXT_INTEGRANTES/GMSDEF_INTEGRANTES` con `anter_contra`, `anter_inte`, `anter_plan`, o crear `GMSTXT_TRASLADO_ORIGEN/GMSDEF_TRASLADO_ORIGEN`; se recomendó tabla nueva para menor riesgo sobre integrantes.
- Cambios propuestos en `gmsdef_validar`:
  - Agregar llamada a `gmsdef_val_traslado` cuando existan `soli_moti IN (47,48)`.
  - Rehabilitar o replicar `gmsdef_val_alta_parcial` para `48`.
  - Revisar `gms_adicionales_generar`; si `47/48` usan `VTAN/INC`, puede seguir sirviendo.
- Cambios propuestos en `gms_genera_solicitudes`:
  - Incluir `47` en carga desde `GMSDEF_INTEGRANTES`, igual que `1/2/48`.
  - Para `47`, calcular `anter_*` desde datos de origen, no por búsqueda ambigua de documento.
  - Para `48`, no depender de `SOLI_AUTO_INTEGRANTES` si viene por GMS genérico; usar `GMSDEF_TRASLADO_ORIGEN` o columnas nuevas.
  - Incluir `47/48` en generación de `SOLI_ADICIONALES` (hoy solo `1,2,6`).
  - Incluir `47/48` en generación/impacto análogo a `SOLI_IMPRE_CERTIFICADO`, `SOLI_ESTADO_AUDITORIA`, `SOLI_PREEXISTENCIAS`, `AFI_PREEXISTENCIAS`, `AFI_GRAVADOS`, `AFI_EMAILS_CONTRA`, `AFI_HISTO_PAREN`, `AFI_ESTADO_SALUD`, `AFI_ESTADO_SALUD_MEDICO`, `AFU_CAMBIO_PREEXISTENCIA`, `AFU_SOLI_AFI_ESTADO_VIP`, `AFU_REGISTRO_UNICO_PERSONA`, `AFU_AFI_CAMPANIAS`.
  - Para `47`, ejecutar impacto completo tipo `afi_impacta_alta_total`; para `48`, impacto completo tipo `afi_impacta_alta_parcial`.
