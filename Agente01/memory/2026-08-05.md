# Memoria - 2026-08-05

## Google Chat directo - Savina
- Usuario trabajando sobre Version 104 en rama SVN `svarela-Version104-Afilmed-SolicitudesAutomaticas`.
- Working copy validada en `/home/luispicone/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas`.
- La rama fue actualizada desde revision `37499` a `37512`, coincidiendo con `HEAD` remoto `37512`.
- Archivos actualizados en esa revision: `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql`, `backend/procedures/gms_genera_solicitudes.sql`, `backend/procedures/gmsdef_val_cambiocober_planadi.sql`, `backend/procedures/gmsdef_val_datos_ident_inte.sql`, `backend/procedures/afu_soli_auto_app_contactos_gmstxt.sql`.
- Tema funcional trabajado: T.C. nuevo automatico (`soli_moti = 47`) entre cuentas colectivas, una persona, sin SARLAFT y sin datos de contacto individual porque deberian estar en Registro Unico.
- Hallazgo: para `soli_moti = 47`, `afs_soli_auto_obligatorios.sql` no obliga SARLAFT, pero actualmente exige contacto de contrato: `domicilio tipo_reg="C"` y `email tipo_reg="C"`. Telefono no es obligatorio si no viene nodo.
- Hallazgo: `afi_insertar_soli_auto_traslado_nuevo.sql` lee facturacion desde XML: `emi_grupo`, `emi_modo`, `vto_grupo`, `pago_forma`, banco/cuenta/tarjeta/titular/vencimiento, y guarda en `SOLI_AUTO_CONTRATO`.
- Flujo posterior: `afu_genera_soli_auto_app.sql` toma esos campos de `SOLI_AUTO_CONTRATO` y los manda a `gmstxt_multi_ins`; luego `gms_genera_solicitudes.sql` usa `GMSDEF_MULTI` para grabar `SOLI_PAGO_FORMAS` y `SOLI_TITULARES`.
- Para cuenta colectiva, el corte y datos de facturacion funcionales salen de la subcuenta destino mediante `afs_dato_fact_x_subcta_x_fpago`, que consulta principalmente `SUBCTA_CONTRATO` junto con `EMI_GRUPOS`, `VTO_GRUPOS`, `EMI_MODOS`, `CUENTAS`, `CUENTA_TIPOS`, `CUENTA_TIPOS_USO`, `TIPO_ASOCIADO`.
- Comparacion con ventana: la ventana individual `wp_soli_datos_facturacion.srw` llama a `f_afs_dato_fact_x_subcta_x_fpago` con cuenta, subcuenta y fecha de aplicacion, precarga los campos `_subcta` y no deja editar porque vienen de configuracion.
- Criterio tecnico acordado verbalmente: si el XML automatico debe replicar traslado por ventana, no deberia requerir que se envien datos de facturacion manuales; backend deberia resolver `vto_grupo`, `emi_grupo`, `emi_modo` automaticamente desde cuenta/subcuenta destino y fecha. Estado actual no esta alineado porque `afs_soli_auto_obligatorios` exige `facturacion/@vto_grupo` para `soli_moti = 47`.

## Google Chat directo - Sebastian - Redmine 29362 y 29363
- Redmine `29362`, casos 9 y 10: solucion recordada de ayer commiteada en SVN `r37509` (`29362: ajustar casos 9 y 10 traslado existente`). Caso 9 ajusto `backend/procedures/gmsdef_val_datos_ident_inte.sql` para `soli_moti = 48`, permitiendo `BB Gestante` en traslado a contrato existente cuando origen vigente ya era `BB Gestante` o `BB Nacido`, evitando rechazo `8079`. Caso 10 ajusto `backend/procedures/afu_soli_auto_app_contactos_gmstxt.sql` para mapear contactos con la numeracion real del integrante destino calculada contra maximo historico del contrato destino.
- Para `29362` se armaron ejemplos de ejecucion del flujo completo `afi_insertar_soli_auto` -> `afu_genera_soli_auto_app` -> `gms_validar_todo` -> `gms_genera_solicitudes`. Caso 9 de ejemplo: destino `1/244512`, origen `1/1004685/03`, documento `BG 10473972601`, esperado sin `GMS_ERR.recha_motivo = 8079`. Caso 10 de ejemplo: destino `1/1000896`, origenes `1/183074/01` y `1/183074/04`, documentos `TI 1046726035` y `RC 1236888201`, esperado que `GMSDEF_INTEGRANTES.inte` y `GMSTXT_DOMICILIOS/TELEFONOS/EMAILS.inte` coincidan; en base actual deberian quedar `05/06` por historico previo.
- Redmine `29363`: contexto recuperado de traslado automatico a contrato nuevo (`soli_moti = 47`). Casos ya resueltos en SVN `r37510` (`29363: ajustar casos 13 16 y 17 traslado nuevo`): caso 13 excepciona validacion de edad `8086/8142` cuando plan padre origen y destino comparten `plan_grupo` (el `8066` por telefono integrante faltante era XML, no fix); caso 16 impacta SARLAFT en `AFI_SARLAFT` y `AFI_SARLAFT_AUTORIZA_CONTACTO`; caso 17 exige `vto_grupo` y recalcula `emi_grupo` desde corte, ignorando `emi_grupo` libre de XML para individuales.
- Redmine `29363`, caso 18: analisis funcional acordado con Sebastian. No es solo repeticion del caso 17: para destino colectivo, el XML no debe gobernar datos de facturacion ni domicilio/email de facturacion; deben tomarse de configuracion de `SUBCTA_CONTRATO`, `SUBCTA_DOMICILIOS` y `SUBCUENTAS` segun `cuenta_dest/subcta_dest` y fecha de aplicacion. Ejemplo validado en `InstallQA_89`: `cuenta=56`, `subcta=2242`, colectivo (`CUENTAS.cuenta=56`, `CUENTA_TIPOS_USO.es_colectivo='S'`), con `vto_grupo=2`, `emi_modo=0`, `emi_grupo=2`, `factu='E'`, `factu_def='E'`, domicilio `CL 13B 64-65`, complemento `CA 38`, localidad/barrio `05001`, CP `1111`, email `pruebasgisprestador@gmail.com`.
- Se implemento fix del caso 18 en working copy `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, archivo `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql`. Cambios: detecta colectivo via `CUENTAS -> CUENTA_TIPOS -> CUENTA_TIPOS_USO`; para colectivo ignora XML de `facturacion/@vto_grupo`, `@emi_modo`, `@emi_grupo` y toma valores desde `SUBCTA_CONTRATO`; limpia datos de forma de pago XML y deja forma `E`; descarta domicilio `tipo_reg=C` del XML y lo toma de `SUBCTA_DOMICILIOS`; descarta email `tipo_reg=C` del XML y toma email de `SUBCUENTAS`, usando `SUBCTA_CONTRATO.envio_email`.
- Revision complementaria: `backend/procedures/afu_genera_soli_auto_app.sql` no requiere ajuste para caso 18 porque consume lo saneado en `SOLI_AUTO_CONTRATO` y delega contactos en `afu_soli_auto_app_contactos_gmstxt`; el punto correcto de saneamiento es el parser `afi_insertar_soli_auto_traslado_nuevo.sql`.
- Validaciones del fix caso 18: parseo SQL OK contra `InstallQA_89` con `PARSEONLY`; sin `RTRIM` ni `;` nuevos; encoding/EOL preservado ASCII CRLF sin BOM. Prueba de datos simulada: XML con `9/9/9` devuelve configuracion colectiva `vto_grupo=2`, `emi_modo=0`, `emi_grupo=2`, domicilio `CL 13B 64-65 CA 38`, email `pruebasgisprestador@gmail.com`.
- Estado final de la tarea caso 18: modificado solo `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql` en la WC `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`. No se hizo commit SVN.
