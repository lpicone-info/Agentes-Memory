# Memoria durable - 2026-08-28

## GPULKA version 104 - Redmine 29365
- Gabriel pidio ponerse en contexto de `gpulka` version 104, hacer `svn update`, analizar `REDMINE_29365_ANALISIS_FUNCIONAL_TECNICO_CAMBIO_RESP_PAGADOR_SOLI_AUTO.docx` en su carpeta, implementar la replicacion de la logica obligatoria de contratos relacionados de `wp_cambio_resp_pagador` en SOLI_AUTO para cambio de responsable pagador, y commitear.
- Se trabajo sobre `backend/procedures/gms_genera_solicitudes.sql`.
- Se identifico que en `wp_cambio_resp_pagador` la seleccion de contratos relacionados es obligatoria cuando el cambio afecta a una persona sin `codigo_unico_persona_nuevo`, o sea cuando el nuevo responsable no se resolvio contra Registro Unico.
- Se implemento para solicitudes APP/SOLI_AUTO con `soli_moti = 22`: antes de `afi_impacta_cambio_resp_pagador`, si el responsable pagador nuevo activo proviene de `SOLI_AUTO_RESP_PAGADOR` y no existe en `REGISTRO_UNICO_DOCUMENTO`, se insertan automaticamente en `SOLI_CONTRATOS_RELACIONADOS` todos los contratos relacionados que muestra la ventana, con `modifica = 'S'`.
- La implementacion respeta el `item` real de la fila activa del nuevo responsable pagador en `SOLI_RESP_PAGADOR`, contemplando casos donde la solicitud finaliza al responsable anterior y da de alta uno nuevo.
- Casos de aplicacion explicados a Gabriel: solicitud APP cambio RP (`soli_moti = 22`), responsable nuevo activo, responsable nuevo no existe en Registro Unico por tipo/nro documento, y existen contratos relacionados vigentes del responsable actual a la fecha de aplicacion.
- Casos excluidos: responsable nuevo ya existe en Registro Unico, no hay responsable nuevo activo, no es solicitud APP/SOLI_AUTO, o no hay contratos relacionados detectables con la misma logica de la ventana.
- Validaciones hechas: `svn update` dejo working copy en `r37696` antes del cambio; commit publicado en SVN `r37697` con mensaje `Redmine 29365 - Relacionados obligatorios en cambio RP APP`; se valido que el archivo conservara `ISO-8859-1` y finales `CRLF`; el bloque nuevo no introduce `RTRIM` ni terminadores `;`.
- Estado SVN final informado: solo quedaban no versionados previos en `Redmines/29361` y `Redmines/29362`, no incluidos en el commit.

# Memoria durable - 2026-08-28

## GPULKA version 104 - Redmine 29365
- Gabriel pidio ponerse en contexto de `gpulka` version 104, hacer `svn update`, analizar `REDMINE_29365_ANALISIS_FUNCIONAL_TECNICO_CAMBIO_RESP_PAGADOR_SOLI_AUTO.docx` en su carpeta, implementar la replicacion de la logica obligatoria de contratos relacionados de `wp_cambio_resp_pagador` en SOLI_AUTO para cambio de responsable pagador, y commitear.
- Se trabajo sobre `backend/procedures/gms_genera_solicitudes.sql`.
- Se identifico que en `wp_cambio_resp_pagador` la seleccion de contratos relacionados es obligatoria cuando el cambio afecta a una persona sin `codigo_unico_persona_nuevo`, o sea cuando el nuevo responsable no se resolvio contra Registro Unico.
- Se implemento para solicitudes APP/SOLI_AUTO con `soli_moti = 22`: antes de `afi_impacta_cambio_resp_pagador`, si el responsable pagador nuevo activo proviene de `SOLI_AUTO_RESP_PAGADOR` y no existe en `REGISTRO_UNICO_DOCUMENTO`, se insertan automaticamente en `SOLI_CONTRATOS_RELACIONADOS` todos los contratos relacionados que muestra la ventana, con `modifica = 'S'`.
- La implementacion respeta el `item` real de la fila activa del nuevo responsable pagador en `SOLI_RESP_PAGADOR`, contemplando casos donde la solicitud finaliza al responsable anterior y da de alta uno nuevo.
- Casos de aplicacion explicados a Gabriel: solicitud APP cambio RP (`soli_moti = 22`), responsable nuevo activo, responsable nuevo no existe en Registro Unico por tipo/nro documento, y existen contratos relacionados vigentes del responsable actual a la fecha de aplicacion.
- Casos excluidos: responsable nuevo ya existe en Registro Unico, no hay responsable nuevo activo, no es solicitud APP/SOLI_AUTO, o no hay contratos relacionados detectables con la misma logica de la ventana.
- Validaciones hechas: `svn update` dejo working copy en `r37696` antes del cambio; commit publicado en SVN `r37697` con mensaje `Redmine 29365 - Relacionados obligatorios en cambio RP APP`; se valido que el archivo conservara `ISO-8859-1` y finales `CRLF`; el bloque nuevo no introduce `RTRIM` ni terminadores `;`.
- Estado SVN final informado: solo quedaban no versionados previos en `Redmines/29361` y `Redmines/29362`, no incluidos en el commit.

## Contexto operativo con Savina - v104 / InstallQA_89
- Savina indico trabajar con `v104` y base `instsllqa_89` / `InstallQA_89`.
- Para `soli_moti = 49`, se entrego XML validado en `InstallQA_89` con `dbo.afi_insertar_soli_auto` dentro de transaccion y `ROLLBACK`; resultado informado: `OK_RECEPCION_XML_ROLLBACK`.
- XML entregado: `id_soli_externa="49001046"`, `soli_moti="49"`, `prepaga="1"`, `contra="1000046"`, `fecha_aplicacion="2026-08-01"`, empleado `CC 14695651`, `LLANOS SOTO PAULINO ALBERTO`, nacimiento `1982-08-01`, sexo `M`, estado civil `S`, pais `CO`, estrato `4`, profesion `951`, ocupacion `1`, cargo `0`, rango salarial `8`, red_coomeva `N`, nomina `M56477`, centro costo `0000`, sucursal empresa `01`, oficina empresa `1`.
- Se verifico que el empleado `CC 14695651` existia en Registro Unico con persona vigente, 2 domicilios, 2 telefonos y 2 emails vigentes.
- Savina pidio luego 10 contratos que al dia actual tengan RP vigente en `AFI_RESP_PAGADOR` sin `baja_fecha`, y que ese RP tambien sea RP de otros contratos activos sin baja.
- Criterio usado para la consulta: `baja_fecha IS NULL`, `vigen_desde <= hoy`, y `vigen_hasta IS NULL OR vigen_hasta >= hoy`.
- Lista informada en `InstallQA_89`: contratos `1006721`, `334977`, `335682`, `36688`, `375363`, `45643`, `51053`, `62964`, `66378` con RP `PEREZ OLGA`, documento `CC 31226406`, 9 contratos activos; y contrato `128456` con RP `GARCIA OLGA`, documento `CC 42994298`, 8 contratos activos.

## GPULKA version 104 - Redmine 29365 complemento soli_moti 49
- Gabriel detecto que en `afu_genera_soli_auto_app` faltaba el tratamiento para solicitud de cambio de datos de empleado y pidio verificar circuito completo de punta a punta, incluyendo batch de solicitudes y superbatch de facturacion, tomando como referencia `soli_moti = 22` / transaccion `RP`.
- Se corrigio `backend/procedures/afu_genera_soli_auto_app.sql`: en el CASE de mapeo de transaccion para traslado se agrego `WHEN 49 THEN 'RP'`; antes, el proceso cortaba con error `No existe mapeo de transaccion para traslado` para `soli_moti = 49`.
- Se corrigio `backend/procedures/gms_genera_solicitudes.sql`: se incorporo `49` junto con `22` para tomar solo integrantes activos, marcar `inte_modif = 'S'` y ejecutar el impacto completo de cambio RP/empleado.
- En la logica de contratos relacionados obligatorios de `gms_genera_solicitudes.sql`, se parametrizo el tipo: `22` usa `tipo = 'R'` y `49` usa `tipo = 'E'`.
- Funcionamiento informado: para solicitudes APP de 22 o 49, cuando la persona nueva no existe en Registro Unico, el backend simula la seleccion obligatoria de la ventana insertando todos los contratos relacionados en `SOLI_CONTRATOS_RELACIONADOS` con `modifica = 'S'`.
- Revision de circuito: insercion SOLI_AUTO ya derivaba `22/49` al parser correcto; validaciones obligatorias ya contemplaban 49 como empleado y lo fuerzan a contrato colectivo; convivencia ya procesa `(22,49)`; superbatch/facturacion no requirio cambio porque toma APP pendientes en forma generica y para colectivos queda sin reversion/facturacion automatica con traza de omision.
- Commit SVN publicado: `r37699` con mensaje `Redmine 29365 - Completa cambio datos empleado APP`.
- Validacion realizada: revision de diff, encoding y EOL; no se ejecuto `DROP/CREATE PROCEDURE` contra base.

## GPULKA version 104 - Redmine 29365 parentescos obligatorios en soli_moti 49
- Gabriel pidio que el tag obligatorio de `parentescos`, ya implementado para cambio de responsable pagador (`soli_moti = 22`), tambien aplique para cambio de datos de empleado (`soli_moti = 49`), y pidio verificar que lo comun entre ambas solicitudes funcione igual.
- Se modifico `backend/procedures/afs_soli_auto_obligatorios.sql`: para `soli_moti IN (22, 49)` ahora se exige en el XML `/solicitud/parentescos/parentesco` con `inte`, `paren_actual`, `paren_real_actual` y `paren_nuevo`; tambien se dejo de tratar `parentescos` como campo exclusivo de RP cuando la solicitud es 49.
- Se modifico `backend/procedures/afi_insertar_soli_auto_cambio_resp_pagador.sql`: para `soli_moti = 49` ahora acepta el tag `parentescos` y lo persiste en `SOLI_AUTO_PARENTESCOS`, igual que 22.
- Se modifico `backend/procedures/gms_genera_solicitudes.sql`: al generar la solicitud final, ahora copia `SOLI_AUTO_PARENTESCOS` a `SOLI_PARENTESCOS` tambien para `soli_moti = 49`.
- Funcionamiento comun verificado: `22` trabaja con tipo `R`, `49` con tipo `E`; la logica de contratos relacionados obligatorios corre para solicitudes APP `soli_moti IN (22,49)` cuando la persona nueva no existe en Registro Unico; en ese caso GMS inserta relacionados en `SOLI_CONTRATOS_RELACIONADOS` con `modifica = 'S'` simulando seleccion completa.
- Se verifico que `afi_genera_soli_auto_cambio_datos_parentescos` ya estaba llamado dentro del bloque comun 22/49; con el cambio, 49 ahora tambien llega con datos en `SOLI_AUTO_PARENTESCOS`.
- Commit SVN publicado: `r37701` con mensaje `Redmine 29365 - Parentescos obligatorios en empleado APP`.
- Validacion realizada: revision de diff, `svn status -u`, encoding/EOL y control de que el diff no introdujera `RTRIM` ni terminadores `;`; no se ejecutaron procedures contra base.
