# Session: 2026-08-14 17:55:01 GMT-3

- **Session Key**: agent:main:googlechat:direct:spaces/_rsuhyaaaae
- **Session ID**: 45a99a71-93aa-4f0d-a8ac-426fa81980eb
- **Source**: googlechat

## Conversation Summary

assistant: Gabriel, en `PruebasCoreINFOMEDICAL` validé este caso:

```sql
declare @prm_vigen_desde datetime,
@prm_vigen_hasta datetime,
@prm_recha smallint

exec afs_chequea_morosidad_contra @prm_prepaga = 1,
 @prm_contra = '732160',
 @prm_inte = '01',
 @prm_fecha = '20260801',
 @prm_chequea_baja = 'S',
 @prm_vigen_desde = @prm_vigen_desde output,
 @prm_vigen_hasta = @prm_vigen_hasta output,
 @prm_recha = @prm_recha output

select @prm_vigen_desde,
 @prm_vigen_hasta,
 @prm_recha
```

Resultado validado:

```text
@prm_vigen_desde = 2026-08-01 00:00:00
@prm_vigen_hasta = 2026-09-30 00:00:00
@prm_recha = 4
```
user: ese sp no aplica a colectivos no? que se usa para buscar por nit? es decir, el docu_nro de la tabla cuentas
assistant: Gabriel, correcto: para colectivo no entraría directo por `afs_chequea_morosidad_contra` buscando por contrato/NIT.

La rama colectiva está en `afs_valida_morosidad`: si `CUENTA_TIPOS_USO.es_colectivo = 'S'`, llama a `dbo.afs_chequea_inhabilitacion_cuenta_subcta`. Evidencia: [afs_valida_morosidad.sql](/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk/backend/procedures/afs_valida_morosidad.sql:89) y [afs_chequea_inhabilitacion_cuenta_subcta.sql](/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk/backend/procedures/afs_chequea_inhabilitacion_cuenta_subcta.sql:15).

Para buscar por NIT (`CUENTAS.docu_nro`), primero resolvés `cuenta/subcta` y después llamás al SP:

```sql
select C.cuenta,
       S.subcta,
       C.docu_tipo,
       C.docu_nro
from CUENTAS C
join CUENTA_TIPOS CT on CT.cuenta_tipo = C.cuenta_tipo
join CUENTA_TIPOS_USO CU on CU.cuenta_tipo_uso = CT.cuenta_tipo_uso
join SUBCUENTAS S on S.cuenta = C.cuenta
where C.docu_nro = '890301033'
and CU.es_colectivo = 'S'
and (C.baja_fecha is null or C.baja_fecha > '20260801')
and (S.baja_fecha is null or S.baja_fecha > '20260801')
```

Caso validado en `PruebasCoreINFOMEDICAL`:

```sql
declare @vigen_desde datetime,
        @vigen_hasta datetime

exec dbo.afs_chequea_inhabilitacion_cuenta_subcta
  @prm_cuenta = 17,
  @prm_subcta = '175',
  @prm_fecha = '20260801',
  @prm_chequea_baja = 'S',
  @prm_vigen_desde = @vigen_desde output,
  @prm_vigen_hasta = @vigen_hasta output

select @vigen_desde,
       @vigen_hasta
```

Resultado validado:

```text
@vigen_desde = 2026-08-01 00:00:00
@vigen_hasta = NULL
```
user: en afi_SAC_interfaz_completo se llena el campo estado en #SUBCUENTAS
pasame un resumen de los posibles valores
assistant: Gabriel, en `afi_SAC_interfaz_completo`, para `#SUBCUENTAS.estado` los valores posibles son:

| Valor | Cuándo queda |
|---|---|
| `AL DIA` | Valor default al crear `#SUBCUENTAS`. Si no entra en ninguna regla posterior, queda así. |
| `EN MORA` | Cuando la subcuenta tiene inhabilitación vigente y el motivo tiene `HAB_INHAB_MOTIVOS.uso_morosidad = 'S'`. |
| `DEPURADO` | Solo dentro de `IF @prm_estado = 'A'`, cuando la subcuenta tiene inhabilitación y el motivo tiene `HAB_INHAB_MOTIVOS.uso_depuracion = 'S'`. |
| `CASTIGADO` | Solo dentro de `IF @prm_estado = 'A'`, cuando la subcuenta inhabilitada tiene comprobante de deuda mala (`compro_tipo = @con_compro_tipo_deuda_mala`). |

Orden de pisado:
`AL DIA` → puede pasar a `EN MORA` → puede pasar a `DEPURADO` → puede pasar a `CASTIGADO`.

Evidencia:
- Default `AL DIA`: [afi_SAC_interfaz_completo.sql](/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk/backend/procedures/afi_SAC_interfaz_completo.sql:106)
- Seteo `EN MORA`: [afi_SAC_interfaz_completo.sql](/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk/backend/procedures/afi_SAC_interfaz_completo.sql:456)
- Seteo `DEPURADO`: [afi_SAC_interfaz_completo.sql](/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk/backend/procedures/afi_SAC_interfaz_completo.sql:601)
- Seteo `CASTIGADO`: [afi_SAC_interfaz_completo.sql](/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk/backend/procedures/afi_SAC_interfaz_completo.sql:623)
user: armame un resumen de lo que cambiamos en el redmine 30222
quiero que menciones especialmente el punto en el cual decidimos modificar el sp afu_importacion_cobranzas_procesa, cuando en el analisis original habias propuesto un monton de sps
el otro punto que hicimos fue el script R30222_traslado_recaudos_onca_rollback, describi brevemente y funcionalmente
assistant: Gabriel, resumen para Redmine `30222`:

Se corrigió el circuito de importación de recaudos para evitar que los pagos o saldos excedentes se apliquen sobre contratos/subcuentas `ONCA`.

El punto importante de decisión fue que el análisis inicial identificaba varios SP potencialmente involucrados: `afu_imp_cob_x_contrato`, `afu_imp_cob_x_documento`, `afu_procesa_cobranza_item`, entre otros. Al revisar mejor el flujo, decidimos no tocar todos esos SP, porque el punto común correcto era `afu_importacion_cobranzas_procesa`. Ahí se arma la tabla común de candidatos `#COB_CONTRA_SUBCTAS_A_CANCELAR`, antes de completar datos y procesar recibos. Centralizar la exclusión ahí evita duplicar reglas y cubre tanto candidatos por contrato como por subcuenta.

Cambio aplicado:
`afu_importacion_cobranzas_procesa` excluye candidatos `ONCA`, validando `AFI_PLANES.plan_codi = 'ONCA'` y `SUBCTA_PLANES.plan_codi = 'ONCA'`. Si un ítem queda únicamente con candidatos ONCA, queda como no identificado para impedir que el recaudo se aplique incorrectamente.

Commit:
`r37566 | Redmine 30222 - Excluye ONCA en importacion de recaudos`

Además se armó el script:
`/mnt/Agentes/gpulka/R30222_traslado_recaudos_onca_rollback.sql`

Funcionalmente, ese script replica el flujo de la ventana de anulación/traslado de recaudos para corregir casos ya aplicados a ONCA. Carga los casos del Excel, filtra los ítems ONCA con `saldo_recibo <> 0`, busca destinos no ONCA del mismo contratante, anula el recaudo original con motivo de división de pago y genera los recibos de traslado hacia contratos válidos no ONCA. Está envuelto en `BEGIN TRAN` y termina con `ROLLBACK TRAN`, por lo que sirve para validar el impacto sin dejar cambios permanentes en la base.
user: sobre los cambios que hicimos en redmine 30222
en gpulka version trunk
hace update de working copy
en el script de la tabla planes agrega un campo que se llame es_onca char(1), con valor default N
en data_modification, en PLANES.sql agrega un update del campo a valor S para el registro plan_codi = 'ONCA'
modifica el sp donde hicimos los cambios afu_importacion_cobranzas_procesa, para que tenga en cuenta esa marca, asi evitamos hardcodear pon plan_codi ONCA
comitea
assistant: Gabriel, hecho y comiteado en `gpulka-Coomeva-trunk`.

Commit SVN:
`r37580 | Redmine 30222 - Usa marca es_onca para excluir ONCA`

Cambios:
- `backend/tables/PLANES.sql`: agregué `es_onca CHAR(1) DEFAULT 'N' NOT NULL`.
- `backend/data_modifications/PLANES.sql`: agregué `UPDATE PLANES SET es_onca = 'S' WHERE plan_codi = 'ONCA'`.
- `backend/procedures/afu_importacion_cobranzas_procesa.sql`: reemplacé el hardcode de `plan_codi = 'ONCA'` por validación contra `PLANES.es_onca = 'S'`.

Validaciones:
- `svn update` ejecutado antes del cambio, quedó en `r37579`.
- `SET PARSEONLY ON`: OK para los tres scripts.
- Encoding ASCII, sin BOM, CRLF preservado.
- Sin `RTRIM` ni `;` agregados.
- Working copy final limpia, salvo externos SVN `X` habituales.
user: Pre-compaction memory flush. Store durable memories only in memory/2026-08-14.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-14.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-14-HHMM.md); always use the canonical 2026-08-14.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 14th, 2026 - 5:34 PM (America/Buenos_Aires)
Reference UTC: 2026-08-14 20:34 UTC
user: fijate si tenes bajada una rama gpulka swiss o algo asi
assistant: Sí, Gabriel. Está bajada:

`gpulka-swiss-temp`

Ruta local:
`/home/luispicone/.openclaw/workspace/svn/gpulka-swiss-temp`

Remoto:
`https://srv-infosvn.infomedical.com/svn/Coomeva/branches/swiss-temp`
user: respecto al redmine 30234
veo que pusiste unos analisis
entiendo que sugeris crear un script para actualizar la mega cuenta de afi_clase
y actualizar la mega cuenta de la plantilla de facturacion
confirmame si es asi el cambio propuesto
fijate en la ventana wp_cuentas, si en la rama gpulka-swiss-temp que tenes bajada, hay una validacion con respecto al cambio de mega cuenta, que en gpulka version trunk de coomeva no tenemos... si es asi y es un cambio simple aplicala en gpulka version trunk , comitea y avisame de que se trata
user: respecto al redmine 30234
veo que pusiste unos analisis
entiendo que sugeris crear un script para actualizar la mega cuenta de afi_clase
y actualizar la mega cuenta de la plantilla de facturacion
confirmame si es asi el cambio propuesto
fijate en la ventana wp_cuentas, si en la rama gpulka-swiss-temp que tenes bajada, hay una validacion con respecto al cambio de mega cuenta, que en gpulka version trunk de coomeva no tenemos... si es asi y es un cambio simple aplicala en gpulka version trunk , comitea y avisame de que se trata
user: respecto al redmine 30234
veo que pusiste unos analisis
entiendo que sugeris crear un script para actualizar la mega cuenta de afi_clase
y actualizar la mega cuenta de la plantilla de facturacion
confirmame si es asi el cambio propuesto
fijate en la ventana wp_cuentas, si en la rama gpulka-swiss-temp que tenes bajada, hay una validacion con respecto al cambio de mega cuenta, que en gpulka version trunk de coomeva no tenemos... si es asi y es un cambio simple aplicala en gpulka version trunk , comitea y avisame de que se trata
