# Memory 2026-07-29

## Redmine 30141 / SARLAFT traslado existente
- Conversacion con Sebastian por Google Chat sobre Redmine 30141 y pruebas SARLAFT de traslado a contrato existente.
- Repositorio SVN usado: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Archivo central modificado varias veces: `Redmines/29362/fase2_ejecucion_sarlaft_traslado_existente.sql`.
- Commits SVN realizados:
  - `r37451`: `Redmine 30141 corrige rollback ejemplo SARLAFT traslado existente`. Corrigio `CATCH` para evitar error SQL Server 3930 cuando `XACT_ABORT ON` dejaba transaccion no confirmable; hace `ROLLBACK` antes de limpiar temporal.
  - `r37452`: `Redmine 30141 ajusta datos ejemplo SARLAFT traslado existente`. Quito `fecha_aplicacion` del XML y reemplazo asesor hardcodeado `1234` por seleccion de asesor vigente desde `dbo.Asesor`.
  - `r37453`: `Redmine 30141 deja ejemplo SARLAFT persistente para batch`. Quito rollback de exito para que la solicitud quede persistida y pueda procesarla el batch; queda rollback defensivo solo en `CATCH`.
  - `r37458`: `Redmine 30141 ajusta caso valido ejemplo SARLAFT traslado existente`. Cambio el caso base por datos del 29362: destino `contra=200`, origen `anter_contra=1002073`, `anter_inte=02`, documento `1193455906`, parentesco `H`, sin anexos, fecha inicialmente dinamica, id externo `301410049`.
  - `r37459`: `Redmine 30141 fija fecha caso SARLAFT traslado existente`. Fijo `@fecha_inicio_servicio = '2026-06-01T00:00:00'` para evitar que el afiliado origen no quede vigente por fecha dinamica.
  - `r37460`: `Redmine 30141 selecciona caso vigente ejemplo SARLAFT`. El script carga varios casos finales del 29362, selecciona automaticamente el primer origen vigente al `2026-06-01`, genera `id_soli_externa` dentro del rango `301410000-301419999` tomando maximo existente en `SOLI_AUTO_CONTRATO` / `GMS_CAB` + 1, y arma el XML SARLAFT.
- Estado funcional aprendido: el script es de traslado a contrato existente (`soli_moti=48`, dispatcher `dbo.afi_insertar_soli_auto`, derivacion `dbo.afi_insertar_soli_auto_traslado_existente`). Traslado a contrato nuevo es motivo `47`.
- Para que aparezca en Afilia, no alcanza con ejecutar el SQL del ejemplo: debe procesarlo el batch (`afu_genera_soli_auto_app` / flujo GMS) y crear registro en `SOLICITUDES`. Revisar `SOLI_AUTO_CONTRATO`, `GMS_CAB`, `GMS_ERR`, `SOLICITUDES`.
- Ultimo problema reportado por Sebastian al ejecutar batch: errores de definicion para secuencia/contrato destino `59550`, motivos `8087` (responsable pagador o titular no activo en cooperativa) y `8090` (profesion inexistente o dada de baja), con id externo `301410049`. Esto no parece SARLAFT sino datos invalidos del contrato destino elegido.
- Ultima accion antes de compactar: quedo CAMBIO LOCAL NO COMITEADO en `Redmines/29362/fase2_ejecucion_sarlaft_traslado_existente.sql`: se excluyo `59550`, se agrego prioridad explicita por `orden`, ahora intenta primero `999584`, despues `1000050`, luego `76704`, manteniendo seleccion automatica solo si el origen esta vigente. Validado ASCII sin BOM, LF puro, sin `fecha_aplicacion`, sin `;` y sin `RTRIM`. Falta que Sebastian lo pruebe o pida commit.
