# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Sebastian Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Redmine 29362 - asesor y caso BB Gestante

- Solicitud o contexto: Sebastian probo un XML del Redmine `29362` con `soli_moti=48` y recibio error porque `asesor_gestion=1234` no existia o estaba dado de baja. Luego el batch rechazo el caso BB Gestante con `8079` y `8222`.
- Respuesta o acciones del agente: Se valido en `InstallQA_89` que el asesor `20` estaba aprobado y activo. Luego se analizo el proceso `174` y se detecto que `gmsdef_val_datos_ident_inte` ya contemplaba la excepcion, pero `gmsdef_val_traslado_web` seguia rechazando BB Gestante con regla simple. Tambien se explico que `8222` era correcto para el documento BG usado porque el afiliado ya tenia edad `1` y `DOCU_TIPOS.BG` permitia edad `0-0`.
- Resultado: Se corrigio `gmsdef_val_traslado_web.sql` y se commiteo SVN `r37530` con mensaje `29362: ajustar validacion BB Gestante traslado web`. Se entrego un XML de ejemplo con BG vigente `1 / 828612 / 03`, documento `BG 10142660891`, y se recordo que para futuros XML no debe enviarse `fecha_aplicacion`; solo `fecha_inicio_servicio`.
- Estado: Resuelto

### Tema 2: Proceso 179 y rechazo 8222

- Solicitud o contexto: Sebastian informo que el proceso `179` rechazo con `8222` para `Id externo 1024` y pidio explicar la causa.
- Respuesta o acciones del agente: Se reviso `InstallQA_89` y se ubico el caso como `SOLI_AUTO_CONTRATO.id_proceso=176`, `GMS_CAB=295862`, estado `E`. El integrante destino era `05`, documento `BG 10473972601`, origen `1 / 1004685 / 03`, con `naci_fecha=2025-08-06` y edad actual `1`.
- Resultado: Se explico que el rechazo era correcto porque BG solo permite edad `0`; el fix de `8079` estaba bien porque ese rechazo ya no aparecia. Se recomendo usar el BG `10142660891` del origen `1 / 828612 / 03`, con edad actual `0`.
- Estado: Resuelto

### Tema 3: Redmine 29363 - contexto y casos 18/19

- Solicitud o contexto: Sebastian pidio ponerse en contexto del Redmine `29363` de traslado automatico a contrato nuevo (`soli_moti=47`) y luego analizar si los casos 18 y 19 eran cambios o aclaraciones.
- Respuesta o acciones del agente: Se resumio el flujo y antecedentes del Redmine: implementacion base `r37128`, ajustes de fechas `r37442/r37444`, facturacion `r37490`, casos 9/10/11 `r37491/r37494`, y casos 13/16/17 `r37510`. Para caso 18 se detecto que el parser ya tomaba datos colectivos desde `SUBCTA_CONTRATO`/`SUBCTA_DOMICILIOS`, pero `afs_soli_auto_obligatorios` seguia exigiendo domicilio/email `tipo_reg=C` desde XML. Para caso 19 se detecto que el automatico aceptaba `cod_camp_comercial`, aunque la ventana manual no lo permite.
- Resultado: Se concluyo que ambos eran arreglos: ajustar obligatoriedad para colectivos y rechazar/blindar campañas en `soli_moti=47`.
- Estado: Resuelto

### Tema 4: Commit casos 18/19 y correccion de compilacion

- Solicitud o contexto: Sebastian pidio aplicar los arreglos de casos 18/19, commitear y enviar ejemplos de rechazos; luego reporto un error de compilacion en `afs_soli_auto_obligatorios` cerca de `BEGIN`.
- Respuesta o acciones del agente: Se modificaron `afs_soli_auto_obligatorios.sql`, `afi_insertar_soli_auto_traslado_nuevo.sql`, `afu_genera_soli_auto_app.sql` y `gms_genera_solicitudes.sql`. Se agrego rechazo de campaña en cabecera/integrante y validaciones de facturacion individual; para colectivos se dejaron de exigir datos de facturacion/contactos de contrato desde XML. Luego se corrigio un parentesis faltante en el `IF EXISTS` de emails.
- Resultado: Se commiteo SVN `r37542` con mensaje `29363: ajustar casos 18 y 19 traslado nuevo` y luego `r37544` con mensaje `29363: corregir sintaxis obligatorios traslado nuevo`. Se entregaron ejemplos de rechazo por campaña y por falta de `emi_modo` en individual. El estado SVN quedo limpio salvo externos `X`.
- Estado: Resuelto

### Tema 5: Caso 20 - responsable pagador obligatorio

- Solicitud o contexto: Sebastian presento el caso 20 y pidio primero determinar si era arreglo o explicacion; luego pidio aplicar el cambio, commitear y enviar un ejemplo con rechazo especifico.
- Respuesta o acciones del agente: Se verifico que el XML incompleto sin `<responsable_pagador>` avanzaba aunque el flujo final de traslado nuevo esperaba esos datos. Se revisaron `afs_soli_auto_obligatorios.sql`, `afi_insertar_soli_auto_traslado_nuevo.sql`, `afu_genera_soli_auto_app.sql` y `gms_genera_solicitudes.sql`. Tambien se reviso un proceso en QA con `SOLI_AUTO_RESP_PAGADOR=0` y `GMSDEF_RESP_PAGADOR=0`.
- Resultado: Se concluyo que era arreglo. Se modifico `afs_soli_auto_obligatorios.sql` para rechazar `soli_moti=47` con cuenta destino individual cuando falta `<responsable_pagador>` o cuando el nodo viene sin datos minimos. Se commiteo SVN `r37545` con mensaje `29363: validar responsable pagador traslado nuevo`. Se entrego un XML de ejemplo que rechaza con `El XML debe informar responsable_pagador para traslado a contrato nuevo`.
- Estado: Resuelto
