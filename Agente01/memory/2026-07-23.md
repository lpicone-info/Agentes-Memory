
## 2026-07-23 11:13 ART - Redmine 30137 y traslado solicitudes automáticas

- Usuario humano identificado en conversación como Sebastián; usuario Redmine usado: `srmontenegro`.
- Redmine `https://redmine.infomedical.com.ar/issues/30137`: defecto en reporte `Facturación / Reportes => Pronto Pago`, archivo `ReportePPContratosConFC`.
- SP/DataWindow identificados:
  - DataWindow `exe_afilmed/src/d_afs_reporte_1_pronto_pago.srd` llama `dbo.afs_reporte_pronto_pago`.
  - Para reporte Contratos con FC usa `@prm_tipo_reporte = 1` y deriva a `dbo.afs_reporte_1_pronto_pago`.
  - Batch relacionado: `exe_afilmed/src/wp_main_super_batch.srw`.
- Corrección aplicada y commiteada en SVN working copy `svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`:
  - Archivo: `backend/procedures/afs_reporte_1_pronto_pago.sql`.
  - Commit SVN revisión `37410`.
  - Mensaje: `Redmine 30137 - Corrige reporte pronto pago contratos con FC`.
  - No se compiló ni ejecutó en base por pedido explícito del usuario.
- Detalle técnico de la corrección Redmine 30137:
  - `ValorAgrupador` y `ValorTotal`: se removió filtro incorrecto `CA3.compro_tipo_agrupador = 'FC'`, porque PP contrato agrupa contra PP agrupador, no contra FC.
  - `SaldoFactura`: al buscar DA/DS desde FC agrupadora ahora filtra por `Co.contra = C.contra` para no tomar el DA de otro contrato.
  - Se conservó FC agrupadora para no romper campos `Factura`, fechas de factura y saldo.
  - Validación contra `PruebasCoreINFOMEDICAL`: contrato `1127561` devuelve `SaldoFactura = 84095`; contrato `1128231` devuelve `SaldoFactura = 207220`; ambos con `ValorAgrupador = 4672815`, `ValorTotal = 291315`.
- Tema posterior: solicitudes automáticas/traslados con `afi_insertar_soli_auto`.
  - XML probado por Sebastián usa `soli_moti="48"`, `prepaga="1"`, `contra="144617"`, `fecha_aplicacion="2026-07-01T00:00:00"`, `fecha_inicio_servicio="2026-07-01T00:00:00"`, `asesor_gestion="400"`, integrantes docs `43798380` y `1000746784` desde contrato anterior `668449`.
  - Error reportado: `El codigo de asesor no existe, o se encuentra dado de baja` en `afi_insertar_soli_auto`, línea 103.
  - Diagnóstico realizado: en `InstallQA_89`, asesor `400` tiene `baja_fecha = 2025-11-18` y `dbo.afn_estado_asesor(400) = 0`.
  - `afs_soli_auto_obligatorios` valida para `soli_moti = 48`: `ASE.IdAsesor = @var_asesor_gestion`, `(ASE.baja_fecha IS NULL OR ASE.baja_fecha > @var_fecha_aplicacion)` y `dbo.afn_estado_asesor(ASE.IdAsesor) <> 0`.
  - Con `fecha_aplicacion = 2026-07-01`, asesor `400` no está vigente; por eso falla.
  - Observación importante: si se quita `fecha_aplicacion`, el SP compilado revisado debería insertarla desde `fecha_inicio_servicio` para `soli_moti 48`; como esa fecha también es `2026-07-01`, debería fallar igual en el mismo flujo/base. Si pasa sin `fecha_aplicacion`, probablemente no corre contra el mismo flujo/datos o algo cambió entre pruebas.
