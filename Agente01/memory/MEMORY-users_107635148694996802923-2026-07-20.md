# Memoria curada diaria

- Fecha: 2026-07-20
- Usuario: Savina Varela
- id_googlechat: users/107635148694996802923
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/xeq2hyaaaae

## Resumen de la actividad

### Tema 1: RM29363 - XML de prueba para traslado a contrato nuevo

- Solicitud o contexto: Savina pidió un ejemplo completo de XML para probar `Version104 - Solicitudes Automaticas - RM29363`, con un ejemplo genérico y otro con datos reales de `InstallQA_89`.
- Respuesta o acciones del agente: Se generó el archivo `/mnt/Agentes/svarela/RM29363_XML_solicitudes_automaticas_traslado_contrato_nuevo.sql`, con XML genérico para `soli_moti = 47`, XML con datos reales de `InstallQA_89` y wrapper SQL con `EXEC dbo.afi_insertar_soli_auto` comentado.
- Resultado: Se validó que existían `dbo.afi_insertar_soli_auto` y `dbo.afi_insertar_soli_auto_traslado_nuevo`, que `id_soli_externa = 293630302` no existía en `SOLI_AUTO_CONTRATO` ni `GMS_CAB`, que ambos XML compilaban como tipo `XML`, y que el archivo quedó UTF-8 sin BOM y LF.
- Estado: Resuelto

### Tema 2: redacción funcional macro para traslado a contrato nuevo

- Solicitud o contexto: Savina compartió un texto funcional de traslado a contrato existente y pidió uno similar, menos técnico y de extensión breve, para documentar traslado a contrato nuevo.
- Respuesta o acciones del agente: Se redactó un párrafo funcional macro explicando que el proceso recibe información de la APP de Coomeva para generar solicitudes automáticas de traslado desde un contrato origen hacia un contrato nuevo destino, registrando solicitud, contrato, integrantes, contactos, facturación y anexos.
- Resultado: Quedó una base textual funcional para documentación, sin entrar aún en detalles técnicos.
- Estado: Resuelto

### Tema 3: contactos, domicilio y email de facturación en XML y tablas finales

- Solicitud o contexto: Savina consultó dónde se declara el domicilio de facturación en el XML, dónde lo guarda el proceso manual y dónde queda registrado al impactar. También preguntó por `tipo_reg`, email de facturación y el check `Enviar factura`.
- Respuesta o acciones del agente: Se explicó que el domicilio de facturación del contrato se informa en `<contactos>` con `tipo_reg="C"`, primero en `SOLI_AUTO_DOMICILIOS`, luego en `GMSTXT_DOMICILIOS` / `GMSDEF_DOMICILIOS`, después en `SOLI_DOMICILIOS_CONTRA` y finalmente en `AFI_DOMICILIOS_CONTRA`. Para emails, se indicó que el email de facturación del contrato usa `<email tipo_reg="C" ... envia_factura="S|N" />`, pasando por `SOLI_AUTO_EMAILS`, `GMSTXT_EMAILS` / `GMSDEF_EMAILS`, `SOLI_EMAILS_CONTRA` y `AFI_EMAILS_CONTRA.envia_factura`.
- Resultado: Se definieron los significados funcionales de `tipo_reg`: `C` contrato, `R` responsable pagador, `E` empleado, `I` integrante. Se aclaró que `envia_factura` debe venir informado como `S` o `N` para el email de contrato.
- Estado: Resuelto

### Tema 4: estructura documental de tags XML

- Solicitud o contexto: Savina pidió una estructura de títulos/tags para XML de traslado a contrato nuevo y luego pidió enumeración, definición y formato ejemplo.
- Respuesta o acciones del agente: Se armó la estructura macro con `<solicitud>`, `<facturacion>`, `<contactos>`, `<domicilio>`, `<email>`, `<telefono>`, `<responsable_pagador>`, `<empleado>`, `<integrantes>`, `<integrante>`, `<anexos>` y `<anexo>`.
- Resultado: Se aclaró que `<integrantes>` es un título principal o contenedor, mientras que `<integrante>` es un subtítulo o registro repetible. También se aplicó el mismo criterio a `<anexos>` y `<anexo>`.
- Estado: Resuelto

### Tema 5: obligatoriedad de contactos y fallback de Registro Unico

- Solicitud o contexto: Savina preguntó si era obligatorio completar los cuatro tipos de contacto y qué ocurría si no enviaba contactos de integrante.
- Respuesta o acciones del agente: Se explicó que no es obligatorio informar todos los `tipo_reg`. Para traslado a contrato nuevo, el mínimo funcional indicado fue domicilio y email del contrato con `tipo_reg="C"`. Se indicó que si un tag no aplica, directamente no debe enviarse vacío.
- Resultado: Se aclaró que para `soli_moti = 47` los contactos se toman desde el XML clasificados por `tipo_reg`; si no se informa un domicilio `I` para el integrante, no se genera domicilio específico para ese integrante desde Registro Unico en este desarrollo.
- Estado: Resuelto

### Tema 6: verificación de código sobre contactos de integrantes en traslado a contrato nuevo

- Solicitud o contexto: Savina planteó que la solicitud automática impactada debería agregar domicilio y email del integrante desde el contrato anterior registrado, y pidió verificarlo en código PowerBuilder o procedimientos.
- Respuesta o acciones del agente: Se revisó el código programado y se concluyó que, para traslado a contrato nuevo, el proceso no toma automáticamente contactos del integrante desde el contrato anterior si no vienen en el XML.
- Resultado: La conclusión funcional fue que los contactos específicos de integrante deben venir informados en el XML si se quieren registrar para el integrante destino; el contrato sí requiere sus contactos mínimos propios.
- Estado: Resuelto

### Tema 7: Habeas Data, Enviar Factura y validaciones de inclusión

- Solicitud o contexto: Savina consultó sobre Habeas Data, Enviar Factura y fechas de aplicación/inicio de servicio en inclusiones.
- Respuesta o acciones del agente: Se explicó que Habeas Data va como dato principal obligatorio `S/N` en la carga masiva, y que Enviar Factura se maneja en emails de facturación `tipo_reg = C` con `envia_factura = S/N`. Sobre inclusiones, se indicó que la fecha de inicio de servicio no tiene que ser la misma fecha exacta que la fecha de aplicación, pero debe ser coherente con el período de aplicación.
- Resultado: Se estableció el criterio práctico: para aplicación `07/2026`, la fecha de inicio de servicio debería estar dentro de julio 2026; una fecha como `10/06/2026` no corresponde para ese período.
- Estado: Resuelto

### Tema 8: tablas y scripts de verificación por proceso

- Solicitud o contexto: Savina pidió un listado de tablas para verificar solicitudes automáticas de traslado a contrato nuevo, y luego pidió un script con consultas parametrizadas por `soli`, contrato, documento, `id_proceso` o `id_soli_externa`.
- Respuesta o acciones del agente: Se listaron tablas de interfaz `SOLI_AUTO_*`, tablas `GMS`, `GMSTXT`, `GMSDEF`, tablas de solicitud `SOLI_*` y tablas finales `AFI_*`. Luego se creó `/mnt/Agentes/svarela/RM29363_selects_verificacion_traslado_contrato_nuevo.sql`.
- Resultado: El script fue ajustado para trabajar por contexto de proceso, resolviendo `id_proceso`, `id_soli_externa`, solicitud generada, `cab_id`, contrato nuevo y datos relacionados, evitando traer histórico abierto. Se validó que no tuviera `RTRIM` ni finalizadores de punto y coma.
- Estado: Resuelto

### Tema 9: prueba de generación de contrato nuevo

- Solicitud o contexto: Savina pidió identificar el contrato nuevo generado por `id_soli_externa = 5000`. Luego pidió un contrato origen alternativo que pudiera procesarse correctamente y el XML completo.
- Respuesta o acciones del agente: Se consultó `InstallQA_89`: `id_soli_externa = 5000` no generó contrato nuevo porque `SOLI_AUTO_CONTRATO.soli` quedó `NULL` y `GMS_CAB` quedó en estado `E` con errores. Se propuso como candidato el contrato origen `1036112`, integrante `02`, documento `CC 43759566`, persona `SOL MUNERA`, validando vigencia, titular remanente, contactos en Registro Unico y ausencia de cambio de titularidad futuro. Se generó un XML completo con `id_soli_externa = 5001`.
- Resultado: Savina preguntó por el proceso `125`; se confirmó que generó solicitud `4677103` y contrato nuevo `1073910`, con `GMS_CAB 295770` en estado `B`, `puede_procesarse = S`, sin errores ni warnings. El integrante trasladado fue `CC 43759566 - SOL MUNERA`, origen `1036112 / inte 02`, destino `1073910 / inte 01`.
- Estado: Resuelto

### Tema 10: observaciones en cambio de cobertura de solicitudes automáticas

- Solicitud o contexto: Savina pidió ubicar el texto de observaciones para cambio de cobertura automática y corrigió que no se refería a `wp_cambio_cobertura_masiva`, sino al cambio de cobertura de solicitudes automáticas.
- Respuesta o acciones del agente: Se identificó el circuito `afi_insertar_soli_auto_cambio_cober.sql`, `soli_moti = 6`, `afu_genera_soli_auto_app.sql` y `gms_genera_solicitudes.sql`.
- Resultado: Se indicó que el texto insertado en `SOLI_OBSERVACIONES` para `origen_solicitud = APP` es `Solicitud generada automaticamente a partir del Proceso de Generacion Masiva de Solicitudes de Traslados`, aunque sea cambio de cobertura, porque el código no distingue el motivo y usa un texto genérico de traslados.
- Estado: Resuelto

### Tema 11: campo Novedad en traslado a contrato nuevo

- Solicitud o contexto: Savina preguntó por qué en la ventana `Datos de Usuario` el campo `Novedad` quedó vacío en una solicitud automática de traslado a contrato nuevo, y aclaró que en la ventana manual sí se completa automáticamente.
- Respuesta o acciones del agente: Primero se indicó que el mapeo automático observado no contemplaba `soli_moti = 47`; luego se corrigió el análisis al verificar que la ventana manual de traslado a contrato nuevo usa `wp_alta_total`, donde para `soli_moti = 47` el campo se carga como `Traslado` y queda protegido.
- Resultado: La conclusión corregida fue que, si en una solicitud automática de traslado a contrato nuevo el campo `Novedad` queda vacío al abrir la ventana, el problema no es el comportamiento manual normal, sino cómo la generación automática grabó `SOLI_AFILIADOS.novedad` / `novedad_sistema`, o que no copió el default de la ventana manual.
- Estado: En curso
