# Memoria curada diaria

- Fecha: 2026-07-24
- Usuario: Sebastián Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Persistencia inicial y análisis de Redmine 30137

- Solicitud o contexto: Sebastián pidió persistir información en memoria persistente y luego avanzar con Redmine 30137.
- Respuesta o acciones del agente: Se ejecutó el bootstrap operativo, se cargaron reglas, repositorios, perfiles, datos de usuario y preferencias de Sebastián. Se registró la información inicial en memoria operativa del workspace. Luego se consultó Redmine 30137 con la credencial registrada de `srmontenegro`, se revisó el estado del issue y se verificó la revisión SVN `37410`.
- Resultado: Se identificó que Redmine 30137 estaba en estado `En Pruebas`, asignado a Belkis, con caso original probado OK. Se confirmó que la revisión SVN `37410` había impactado `backend/procedures/afs_reporte_1_pronto_pago.sql`. Quedó pendiente leer el “CASO 2” porque el documento de Google devolvió `401 Unauthorized`.
- Estado: En curso

### Tema 2: Análisis del CASO 2 del Word y ajuste de `TotalRecaudo`

- Solicitud o contexto: Sebastián indicó que necesitaba revisar el CASO 2 y envió un documento Word relacionado con Redmine 30137.
- Respuesta o acciones del agente: Se ubicó el `.docx` recibido en `/home/luispicone/.openclaw/media/inbound/`, se extrajo su contenido con `pandoc`, se revisó el procedimiento `backend/procedures/afs_reporte_1_pronto_pago.sql` y se analizaron los datos en `PruebasCoreINFOMEDICAL`. Se aplicó un cambio local en el SP para calcular `TotalRecaudo` desde `COMPRO_IMPOR_LOTES.total_impor_importe`.
- Resultado: Se concluyó inicialmente que el SP estaba usando `COMPRO_IMPORTACION.valor` filtrado por contrato/documento, mientras que el Word indicaba tomar el total del lote. Se validó con casos puntuales: para `RC-146633483` ambas fórmulas daban `21.807.975`, y para `RC-148223933` la fórmula anterior daba `3.758.265` mientras el total del lote daba `84.008.454`. El parse SQL dio `PARSEONLY_OK` y se preservaron encoding UTF-8 con BOM y finales CRLF.
- Estado: En curso

### Tema 3: Commit SVN del ajuste de Redmine 30137

- Solicitud o contexto: Sebastián pidió no compilar y commitear directamente el cambio aplicado.
- Respuesta o acciones del agente: Se revisó el diff y el estado SVN del archivo `backend/procedures/afs_reporte_1_pronto_pago.sql`, se validó nuevamente encoding/BOM/CRLF y se ejecutó commit SVN con la credencial registrada de `srmontenegro`.
- Resultado: Se realizó el commit SVN `r37418` con mensaje `Redmine 30137 - Ajusta total recaudo desde lote importado`. El commit fue confirmado con `svn log -r 37418`; el SP quedó sin cambios pendientes. No se compiló en base.
- Estado: Resuelto

### Tema 4: Reversión del ajuste por corrección funcional

- Solicitud o contexto: Sebastián informó que había que revertir el cambio porque el analista funcional estaba equivocado.
- Respuesta o acciones del agente: Se recuperó el contexto de memoria sobre Redmine 30137 y la revisión `r37418`, se verificó el estado del archivo, se aplicó `svn merge -c -37418` sobre `backend/procedures/afs_reporte_1_pronto_pago.sql`, se revisó el diff, se validó parse SQL y se commiteó la reversa.
- Resultado: Se realizó la reversión SVN `r37429` con mensaje `Redmine 30137 - Revierte ajuste total recaudo`. El cambio de `r37418` quedó revertido y `TotalRecaudo` volvió al cálculo anterior. No se compiló en base. Se validó `PARSEONLY_OK`, SP sin cambios pendientes, UTF-8 con BOM y CRLF preservados.
- Estado: Resuelto

### Tema 5: Diagnóstico de por qué `TotalRecaudo` muestra cero

- Solicitud o contexto: Sebastián preguntó por qué en esos reportes `TotalRecaudo` estaba mostrando `0`.
- Respuesta o acciones del agente: Se revisó el SP, se analizaron condiciones de join/filtro y se ejecutaron consultas en `PruebasCoreINFOMEDICAL` para contrastar comprobantes, contratantes, documentos e importaciones.
- Resultado: Se explicó que la fórmula actual exige coincidencia por `compro_nro`, `compro_tipo`, `contra`, `cuenta`, `subcta`, `docu_nro` y también `docu_tipo` salvo que `COMPRO_IMPORTACION.docu_tipo` venga `NULL`. Cuando no hay match, el `SUM` queda `NULL` y el `ISNULL(..., 0)` muestra `0`. Como evidencia, para `RC-148223933` contrato `334855`, el reporte usa `PA 657692487` y la importación tiene `CC 657692487`, por lo que no matchea por tipo de documento. Para `RC-146633483` aplicado contra `DA-148451429`, el RC/importación viene con `CC 29067859`, pero la fila del reporte usa contratante `CC 38958631`, por lo que no matchea por número de documento.
- Estado: Informativo

### Tema 6: Uso de `Útiles` en una entrega

- Solicitud o contexto: Sebastián preguntó cuándo corresponde usar `Útiles` en una entrega.
- Respuesta o acciones del agente: Se revisó el repositorio SVN `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas` buscando referencias a `utiles` y se identificó que `Útiles` corresponde a un ejecutable/proyecto PowerBuilder separado dentro de `exe_presmed`.
- Resultado: Se explicó que `Útiles` debe usarse cuando el cambio impacta objetos del proyecto `exe_presmed/utiles`, como `utiles.pbt`, `utiles.pbl`, `utiles.pbg`, `utiles_launcher.exe`, objetos asociados a `utiles.sra`, `utiles.srj`, `m_mainutil.srm`, `wf_mainutil.srw`, DataWindows o ventanas incluidas en `utiles.pbg`, o cuando haya que regenerar/entregar `utiles.pbl` o el ejecutable de `Útiles`. También se aclaró que no corresponde para cambios solo en `backend/procedures/*.sql`, objetos de `exe_afilmed/src`, pantallas/reportes propios de Afilmed o cambios de base sin modificar `exe_presmed/utiles`. Para Redmine 30137 no aplicaba `Útiles`.
- Estado: Informativo

### Tema 7: Cierre breve

- Solicitud o contexto: Sebastián agradeció la respuesta.
- Respuesta o acciones del agente: Se respondió con un cierre breve.
- Resultado: Interacción finalizada sin tareas pendientes nuevas.
- Estado: Resuelto
