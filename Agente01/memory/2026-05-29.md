# 2026-05-29

## GMS traslado web/XML - SVN r37009
- User and agent agreed that PowerBuilder window validations for Alta Parcial traslado must be replicated in SQL because the new flow enters from web/XML, not PowerBuilder.
- Validation ownership decision: keep business validation in `gmsdef_val_traslado`; move large/no-PowerBuilder web/XML-specific checks into a helper SP called `gmsdef_val_traslado_web`, invoked from `gmsdef_val_traslado` inside the existing Fase 3/GMS_ERR circuit. Do not run these validations after `gms_validar_todo` externally.
- Recommended call chain documented in chat: `gms_validar_todo` -> `gmstxt_validar` / `gmsdef_validar` -> `gmsdef_val_alta` -> `gmsdef_val_traslado` -> `gmsdef_val_traslado_web`.
- Implementation completed and committed to SVN as `r37009 - 29362 Separa validaciones web de traslado GMSDEF`.
- Files changed in that commit: `backend/procedures/gmsdef_val_traslado.sql` now delegates to `dbo.gmsdef_val_traslado_web`; new `backend/procedures/gmsdef_val_traslado_web.sql` contains the web/XML traslado validation logic that had been in `gmsdef_val_traslado`.
- Validations run before reporting completion: `svn status`, CRLF/no BOM/ASCII checks, no semicolon/RTRIM check, CREATE/DROP/GO review, and SVN log confirmation for revision 37009.
- Important caveat reported to user: SPs were not executed against the database; changes were only versioned for the user to test in their own circuit. Existing SVN externals marked `X` were unrelated and left untouched.
