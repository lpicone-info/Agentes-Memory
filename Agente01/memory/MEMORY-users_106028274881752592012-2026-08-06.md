# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Gabriel Pulka
- id_googlechat: users/106028274881752592012
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/_rsuhyaaaae

## Resumen de la actividad

### Tema 1: Script de pruebas para afi_cambio_cober_cooperativa

- Solicitud o contexto: Gabriel pidio armar un script SQL con rollback en `InstallQA_89` para probar `afi_cambio_cober_cooperativa` con varios tipos de solicitud, incluyendo el filtro por `fecha_aplicacion_cambio` y la retrocompatibilidad con fecha nula.
- Respuesta o acciones del agente: Se preparo `/mnt/Agentes/gpulka/afi_cambio_cober_cooperativa_casos_rollback.sql` con casos de bloqueo, fecha nula, solicitudes `1/47/48`, `2`, `6`, `22`, y una fila informativa para `7` por falta de candidato real consistente.
- Resultado: El script quedo validado sin `RTRIM`, sin `;`, con finales `LF` y sin errores detectados por `sqlcmd`. Se explico que el caso `7_CAMBIO_TITULAR` no era una ejecucion fallida sino cobertura faltante por no existir candidato real consistente en `InstallQA_89`.
- Estado: Resuelto

### Tema 2: Diagnostico de demora del proceso de comisiones 07/2026

- Solicitud o contexto: Gabriel pidio analizar por que el proceso de comisiones de `pruebascoreinfomedical` para 07/2026 demoraba mucho mas de lo historico y no terminaba, cuidando no interferir con el `spid 129` activo.
- Respuesta o acciones del agente: Se revisaron DMVs y plan activo con consultas de lectura. Se detecto que el cuello inicial estaba en `afi_procesa_comisiones_valores_cabecera`, dentro del `INSERT INTO #COM_RECAUDOS`, sin bloqueo y con espera CPU-bound (`SOS_SCHEDULER_YIELD`). El plan estimaba muy pocas filas, tenia muchisimas lecturas logicas y mostraba missing index sobre `COMISIONES_MOVIMIENTOS_RECAUDOS(compro_nro)`.
- Resultado: Se concluyo que no parecia un dato puntual raro, sino una combinacion de volumen historico acumulado, estadisticas desactualizadas y falta de indice para el anti-join por `compro_nro`. Se recomendo no tocar estadisticas ni indices durante la corrida activa.
- Estado: Resuelto

### Tema 3: NOT EXISTS versus LEFT JOIN

- Solicitud o contexto: Gabriel consulto si reemplazar el `NOT EXISTS` por `LEFT JOIN ... IS NULL` mejoraria el rendimiento.
- Respuesta o acciones del agente: Se explico que SQL Server normalmente transforma ambas formas en un anti-join y que el problema no era la sintaxis sino la ausencia de indice por `REC.compro_nro`. Tambien se advirtio que un `LEFT JOIN` mal armado podia producir falsos positivos.
- Resultado: Se recomendo atacar el cuello con el indice permanente `idx_com_mov_recaudos_compro_nro` o con una temporal precalculada e indexada para comprobantes de otros periodos.
- Estado: Resuelto

### Tema 4: Commit de indices para comisiones

- Solicitud o contexto: Gabriel pidio actualizar la working copy `gpulka-Coomeva-trunk`, agregar el indice permanente en el script de tabla y el indice temporal en el SP, y commitear.
- Respuesta o acciones del agente: Se actualizo la working copy a revision `37530`, se agrego `idx_com_mov_recaudos_compro_nro` sobre `COMISIONES_MOVIMIENTOS_RECAUDOS (compro_nro, id_cabecera)` y `idx_com_recaudos_cod_unico` sobre `#COM_RECAUDOS (cod_unico_comprobante)` en `afi_procesa_comisiones_valores_cabecera.sql`.
- Resultado: Se commiteo SVN `r37531` con mensaje `Optimiza indices para proceso de comisiones`. Se valido estado limpio, solo externos `X`, sin cambios contra BD desde el agente.
- Estado: Resuelto

### Tema 5: Seguimiento posterior de performance

- Solicitud o contexto: Gabriel informo que recompilaron y volvieron a ejecutar el proceso en `pruebascoreinfomedical` con `spid 129`, y luego que termino en 23 minutos frente a mas de 3 horas previas.
- Respuesta o acciones del agente: Se monitoreo en vivo el `spid 129`, verificando que el cuello de `#COM_RECAUDOS` habia sido superado y que el proceso luego estaba en `afi_procesa_comisiones_valores_complementarios`, armando `#COMPRO_CON_COMI`.
- Resultado: Se propuso como mejora futura revisar un indice temporal sobre `#COMPRO (cuenta, subcta, contra)` incluyendo `saldo`. Se redacto una nota breve para Redmine con los cambios, revision `37531` y resultado de performance.
- Estado: Resuelto
