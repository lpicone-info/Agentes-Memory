# Memory - 2026-06-02

## Redmine 28090 - Cierre operativo previo a volver al 29362

Estado verificado:
- Repo/contexto usado: `coomeva-afilmed-trunk-srmontenegro`.
- Se reviso el estado Git del repo del 28090.
- Resultado: no hay cambios pendientes, no hay diff y no quedo nada para commitear en ese repo.

Conocimiento funcional persistido:
- El criterio rector del tema 28090 es evitar perdida de datos.
- Cualquier fix, migracion, ajuste SQL o proceso automatico relacionado debe priorizar preservacion del dato origen.
- Ante inconsistencias, el comportamiento esperado es conservar el dato original, registrar o exponer la inconsistencia y evitar sobrescrituras, borrados o equivalencias asumidas sin evidencia.
- Para procesos con transformacion de datos, aplicar staging/intermedio, validaciones cruzadas, auditoria, transacciones cuando corresponda y rollback definido antes de operaciones destructivas.
- El analisis previo del 28090 queda vigente: revisar especialmente el hardcode `C.ente = 1` en el reporte tipo 0 y la inconsistencia de tratamiento de `fecha_hasta` entre reportes.

Siguiente foco indicado por el usuario:
- Volver al Redmine 29362.
- Estado observado al volver: existe `Redmines/29362/CONOCIMIENTO_OPERATIVO_29362.md` como archivo no versionado en la working copy SVN del 29362.

## 2026-06-02 14:36 UTC - Flush operativo 29362

- Contexto activo: Redmine/SVN `29362`.
- Objetivo inmediato del usuario: cerrar subfase 3 moviendo los puntos 9 y 10 desde `gmsdef_val_traslado_web` hacia `gmsdef_val_traslado`.
- Pedido textual reiterado: "saca esos 2 puntos desde gmsdef_val_traslado_web y hacelos en gmsdef_val_traslado asi cerramos la subfase3".
- Punto 9: Aceptacion funcional del traslado. En ventana `wf_valida_traslado` llama `ilib_alta.f_alta_valida_traslado_aceptada`; SP real `dbo.afs_val_traslado_aceptada`. En GMS no esta llamado actualmente desde `gmsdef_validar`. Debe agregarse en `gmsdef_val_traslado` con temp table equivalente. Para `soli_moti = 48` permite baja total del contrato origen; para `47` rechaza si el contrato origen queda sin poblacion activa.
- Punto 10: Saldos, pagos anticipados, acuerdos, ajustes no facturados y conceptos facturados a futuro. En ventana es logica PowerBuilder dentro de `wf_valida_traslado`, usando `nvo_cuenta`, `nvo_contrato`, `nvo_subcta_contrato` y metodos `of_get_saldo`, `of_get_acuerdo_pago_pendiente`, `of_acuerdo_pago_pendiente`, `of_get_ajustes_no_facturados`, `of_get_conceptos_facturados_a_futuro`, `of_get_ultimo_contrato_de_subcta`. No se observo SP unico equivalente en GMSDEF. Accion esperada: incorporarlo en SQL para GMS, preferentemente en `gmsdef_val_traslado`, reutilizando consultas/SP existentes si se identifican en los NVO; si no hay SP reutilizable, crear SP auxiliar especifico para validacion financiera de origen.
- Razon tecnica ya explicada al usuario: estos puntos no corresponden a `gmsdef_val_traslado_web` porque no son validaciones web/XML directas; dependen de contexto funcional/financiero del contrato origen. Deben vivir en `gmsdef_val_traslado`, que es el orquestador funcional.
- Estado previo relevante: se habia creado y comiteado en SVN `r37026` con mensaje `29362 Agrega diagnostico fase 3 subfase 4`, incluyendo `Redmines/29362/CONOCIMIENTO_OPERATIVO_29362.md` y `Redmines/29362/fase1-2-3-4_ejecucion_diagnostico_rollback.sql`.
- Antes del flush hubo al menos un `apply_patch` iniciado para esta tarea, pero falta verificar estado real con `svn status`/diff al retomar. No asumir que quedo completo.
- Criterio rector persistente desde 28090 y aplicable: cero perdida de datos; no borrar/sobrescribir origen sin validacion completa; preferir staging/temp tables, auditoria, validaciones cruzadas, rollback e idempotencia.

## 2026-06-02 15:21 UTC - Flush antes de compactacion, tarea 29362

Contexto inmediato a conservar:
- El usuario pidio continuar con el objetivo: tomar la traza de "Aceptacion funcional del traslado" retirada de `gmsdef_val_traslado_web` e incorporar los dos puntos pendientes en `gmsdef_val_traslado`.
- Instruccion operativa explicita y reiterada del usuario: no usar busquedas globales complejas, no usar pipes ni regex con acentos para esta verificacion.
- Secuencia solicitada por el usuario antes de editar:
  1. Listar manualmente los archivos relacionados.
  2. Abrir solo los archivos que correspondan a `gmsdef_val_traslado` y `gmsdef_val_traslado_web`.
  3. Comparar ambos archivos manualmente.
  4. Buscar manualmente la logica de "Aceptacion funcional del traslado".
  5. Recien despues aplicar en `gmsdef_val_traslado` los dos puntos pendientes ya existentes o identificados en `gmsdef_val_traslado_web`.
- En un pedido anterior dentro de la misma tarea, el usuario tambien pidio busquedas simples y separadas por estos terminos, sin combinarlas: `gmsdef_val_traslado`, `gmsdef_val_traslado_web`, `Aceptacion funcional`, `traslado`.
- Debe indicarse al usuario antes de avanzar: que archivos se encontraron, si ya hubo modificaciones, y que falta incorporar en `gmsdef_val_traslado`.
- No asumir estado final por historial truncado: al retomar hay que verificar estado actual del codigo y diff/status antes de tocar nada.

## 2026-06-02 16:02 UTC - Redmine 28090 commit SVN pronto pago

Contexto funcional reportado por el usuario:
- En `afs_reporte_3_pronto_pago`, para la factura de pronto pago `FC XSCMP07922957` con `cod_unico_comprobante=148552181`, estaban facturados los contratos `1132329` y `999111`.
- El contrato `1132329` no tenia otras facturas; el contrato `999111` si.
- El valor mostrado en `OtrasFacturasOF = FC-149151562` correspondia al contrato `999111`, pero se estaba pegando indebidamente a la linea del contrato `1132329`.
- El `ValorOF = 334320.00` provenia como fallback del total de la factura OF cuando no se encontraba detalle DA/DS para el contrato equivocado.

Causa tecnica identificada:
- `#OTRAS_FC` ya filtraba por contrato al armar las otras facturas, pero luego la relacion contra `#PP_DS_BASE` se hacia solo por FC/subcuenta, sin exigir contrato.
- En facturas compartidas por mas de un contrato, eso permitia cruzar una OF de otro contrato.

Correccion aplicada:
- En `afs_reporte_3_pronto_pago.sql`, el dedupe de `#OTRAS_FC` ahora particiona tambien por `PP_contrato`.
- Los indices de `#OTRAS_FC` incorporan `PP_contrato`.
- `#PP_DS_BASE` ahora conserva una fila por contrato dentro de la misma FC/subcuenta.
- La union final entre OF y base exige `CR.contra = OFC.PP_contrato`, contemplando ambos `NULL`.

Commit realizado:
- Working copy SVN: `~/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`
- Ruta commiteada: `Version104-Afilmed-SolicitudesAutomaticas/Redmines/28090/afs_reporte_3_pronto_pago.sql`
- Revision SVN: `r37028`
- Autor SVN observado: `srmontenegro@INFOMEDICAL`
- Fecha commit: `2026-06-02 15:41:08 UTC`
- Mensaje: `Redmine 28090 corrige relacion contrato OF pronto pago`
- El commit incluyo solo ese archivo.

Validaciones ejecutadas:
- `svn diff` del archivo destino mostro solo la correccion esperada de contrato/OtrasFacturasOF.
- `svn status` del archivo commiteado quedo limpio despues del commit.
- Encoding del archivo destino: `Non-ISO extended-ASCII text`.
- BOM: ausente; primeros bytes `49 46 20`.
- Finales de linea: `LF` puro, sin `CRLF` ni `CR`.
- No se ejecuto prueba en SQL Server desde el entorno.

Estado residual importante:
- En la working copy SVN sigue un cambio no relacionado en `backend/procedures/gmsdef_val_traslado_web.sql`, correspondiente al trabajo previo del Redmine 29362; no fue incluido en `r37028`.
- En el repo Git `coomeva-afilmed-trunk-srmontenegro` quedo modificado localmente `backend/procedures/afs_reporte_3_pronto_pago.sql` con la misma correccion usada como origen para actualizar el archivo SVN de `Redmines/28090`.
# Memory - 2026-06-02

## Redmine 28090 - Cierre operativo previo a volver al 29362

Estado verificado:
- Repo/contexto usado: `coomeva-afilmed-trunk-srmontenegro`.
- Se reviso el estado Git del repo del 28090.
- Resultado: no hay cambios pendientes, no hay diff y no quedo nada para commitear en ese repo.

Conocimiento funcional persistido:
- El criterio rector del tema 28090 es evitar perdida de datos.
- Cualquier fix, migracion, ajuste SQL o proceso automatico relacionado debe priorizar preservacion del dato origen.
- Ante inconsistencias, el comportamiento esperado es conservar el dato original, registrar o exponer la inconsistencia y evitar sobrescrituras, borrados o equivalencias asumidas sin evidencia.
- Para procesos con transformacion de datos, aplicar staging/intermedio, validaciones cruzadas, auditoria, transacciones cuando corresponda y rollback definido antes de operaciones destructivas.
- El analisis previo del 28090 queda vigente: revisar especialmente el hardcode `C.ente = 1` en el reporte tipo 0 y la inconsistencia de tratamiento de `fecha_hasta` entre reportes.

Siguiente foco indicado por el usuario:
- Volver al Redmine 29362.
- Estado observado al volver: existe `Redmines/29362/CONOCIMIENTO_OPERATIVO_29362.md` como archivo no versionado en la working copy SVN del 29362.

## 2026-06-02 14:36 UTC - Flush operativo 29362

- Contexto activo: Redmine/SVN `29362`.
- Objetivo inmediato del usuario: cerrar subfase 3 moviendo los puntos 9 y 10 desde `gmsdef_val_traslado_web` hacia `gmsdef_val_traslado`.
- Pedido textual reiterado: "saca esos 2 puntos desde gmsdef_val_traslado_web y hacelos en gmsdef_val_traslado asi cerramos la subfase3".
- Punto 9: Aceptacion funcional del traslado. En ventana `wf_valida_traslado` llama `ilib_alta.f_alta_valida_traslado_aceptada`; SP real `dbo.afs_val_traslado_aceptada`. En GMS no esta llamado actualmente desde `gmsdef_validar`. Debe agregarse en `gmsdef_val_traslado` con temp table equivalente. Para `soli_moti = 48` permite baja total del contrato origen; para `47` rechaza si el contrato origen queda sin poblacion activa.
- Punto 10: Saldos, pagos anticipados, acuerdos, ajustes no facturados y conceptos facturados a futuro. En ventana es logica PowerBuilder dentro de `wf_valida_traslado`, usando `nvo_cuenta`, `nvo_contrato`, `nvo_subcta_contrato` y metodos `of_get_saldo`, `of_get_acuerdo_pago_pendiente`, `of_acuerdo_pago_pendiente`, `of_get_ajustes_no_facturados`, `of_get_conceptos_facturados_a_futuro`, `of_get_ultimo_contrato_de_subcta`. No se observo SP unico equivalente en GMSDEF. Accion esperada: incorporarlo en SQL para GMS, preferentemente en `gmsdef_val_traslado`, reutilizando consultas/SP existentes si se identifican en los NVO; si no hay SP reutilizable, crear SP auxiliar especifico para validacion financiera de origen.
- Razon tecnica ya explicada al usuario: estos puntos no corresponden a `gmsdef_val_traslado_web` porque no son validaciones web/XML directas; dependen de contexto funcional/financiero del contrato origen. Deben vivir en `gmsdef_val_traslado`, que es el orquestador funcional.
- Estado previo relevante: se habia creado y comiteado en SVN `r37026` con mensaje `29362 Agrega diagnostico fase 3 subfase 4`, incluyendo `Redmines/29362/CONOCIMIENTO_OPERATIVO_29362.md` y `Redmines/29362/fase1-2-3-4_ejecucion_diagnostico_rollback.sql`.
- Antes del flush hubo al menos un `apply_patch` iniciado para esta tarea, pero falta verificar estado real con `svn status`/diff al retomar. No asumir que quedo completo.
- Criterio rector persistente desde 28090 y aplicable: cero perdida de datos; no borrar/sobrescribir origen sin validacion completa; preferir staging/temp tables, auditoria, validaciones cruzadas, rollback e idempotencia.

## 2026-06-02 15:21 UTC - Flush antes de compactacion, tarea 29362

Contexto inmediato a conservar:
- El usuario pidio continuar con el objetivo: tomar la traza de "Aceptacion funcional del traslado" retirada de `gmsdef_val_traslado_web` e incorporar los dos puntos pendientes en `gmsdef_val_traslado`.
- Instruccion operativa explicita y reiterada del usuario: no usar busquedas globales complejas, no usar pipes ni regex con acentos para esta verificacion.
- Secuencia solicitada por el usuario antes de editar:
  1. Listar manualmente los archivos relacionados.
  2. Abrir solo los archivos que correspondan a `gmsdef_val_traslado` y `gmsdef_val_traslado_web`.
  3. Comparar ambos archivos manualmente.
  4. Buscar manualmente la logica de "Aceptacion funcional del traslado".
  5. Recien despues aplicar en `gmsdef_val_traslado` los dos puntos pendientes ya existentes o identificados en `gmsdef_val_traslado_web`.
- En un pedido anterior dentro de la misma tarea, el usuario tambien pidio busquedas simples y separadas por estos terminos, sin combinarlas: `gmsdef_val_traslado`, `gmsdef_val_traslado_web`, `Aceptacion funcional`, `traslado`.
- Debe indicarse al usuario antes de avanzar: que archivos se encontraron, si ya hubo modificaciones, y que falta incorporar en `gmsdef_val_traslado`.
- No asumir estado final por historial truncado: al retomar hay que verificar estado actual del codigo y diff/status antes de tocar nada.

## 2026-06-02 16:02 UTC - Redmine 28090 commit SVN pronto pago

Contexto funcional reportado por el usuario:
- En `afs_reporte_3_pronto_pago`, para la factura de pronto pago `FC XSCMP07922957` con `cod_unico_comprobante=148552181`, estaban facturados los contratos `1132329` y `999111`.
- El contrato `1132329` no tenia otras facturas; el contrato `999111` si.
- El valor mostrado en `OtrasFacturasOF = FC-149151562` correspondia al contrato `999111`, pero se estaba pegando indebidamente a la linea del contrato `1132329`.
- El `ValorOF = 334320.00` provenia como fallback del total de la factura OF cuando no se encontraba detalle DA/DS para el contrato equivocado.

Causa tecnica identificada:
- `#OTRAS_FC` ya filtraba por contrato al armar las otras facturas, pero luego la relacion contra `#PP_DS_BASE` se hacia solo por FC/subcuenta, sin exigir contrato.
- En facturas compartidas por mas de un contrato, eso permitia cruzar una OF de otro contrato.

Correccion aplicada:
- En `afs_reporte_3_pronto_pago.sql`, el dedupe de `#OTRAS_FC` ahora particiona tambien por `PP_contrato`.
- Los indices de `#OTRAS_FC` incorporan `PP_contrato`.
- `#PP_DS_BASE` ahora conserva una fila por contrato dentro de la misma FC/subcuenta.
- La union final entre OF y base exige `CR.contra = OFC.PP_contrato`, contemplando ambos `NULL`.

Commit realizado:
- Working copy SVN: `~/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`
- Ruta commiteada: `Version104-Afilmed-SolicitudesAutomaticas/Redmines/28090/afs_reporte_3_pronto_pago.sql`
- Revision SVN: `r37028`
- Autor SVN observado: `srmontenegro@INFOMEDICAL`
- Fecha commit: `2026-06-02 15:41:08 UTC`
- Mensaje: `Redmine 28090 corrige relacion contrato OF pronto pago`
- El commit incluyo solo ese archivo.

Validaciones ejecutadas:
- `svn diff` del archivo destino mostro solo la correccion esperada de contrato/OtrasFacturasOF.
- `svn status` del archivo commiteado quedo limpio despues del commit.
- Encoding del archivo destino: `Non-ISO extended-ASCII text`.
- BOM: ausente; primeros bytes `49 46 20`.
- Finales de linea: `LF` puro, sin `CRLF` ni `CR`.
- No se ejecuto prueba en SQL Server desde el entorno.

Estado residual importante:
- En la working copy SVN sigue un cambio no relacionado en `backend/procedures/gmsdef_val_traslado_web.sql`, correspondiente al trabajo previo del Redmine 29362; no fue incluido en `r37028`.
- En el repo Git `coomeva-afilmed-trunk-srmontenegro` quedo modificado localmente `backend/procedures/afs_reporte_3_pronto_pago.sql` con la misma correccion usada como origen para actualizar el archivo SVN de `Redmines/28090`.

## 2026-06-02 17:53 UTC - Redmine 29361 / gpulka version 104 - Solicitudes APP

Contexto activo:
- Usuario trabajo sobre `29361`, `gpulka`, `Version104-Afilmed-SolicitudesAutomaticas`.
- Objetivo funcional: excluir del proceso de facturacion existente las solicitudes cuyo circuito venga de `SOLICITUDES.origen_solicitud = 'APP'`, sin afectar el circuito especifico `AUFACT`.

Commits SVN realizados:
- `r37029 | gpulka@INFOMEDICAL`: agrego filtro APP inicialmente en tres SP.
- `r37030 | 29361 Revertir filtro APP en SP sin uso`: se reviso que `afs_contratos_pago_anual_selec` no aparecia invocado desde la working copy, solo su propia definicion; se revirtio unicamente ese cambio.
- `r37031 | 29361 Simplificar filtro origen APP en facturacion`: se quito `ISNULL` del filtro porque el campo `SOLICITUDES.origen_solicitud` fue creado como `CHAR(3) NOT NULL DEFAULT 'AFI'`.

Estado funcional vigente despues de `r37031`:
- Filtro APP queda aplicado solo en:
  - `backend/procedures/afu_contra_a_verif_min_solicitud.sql`
  - `backend/procedures/afu_proce_fecha_soli.sql`
- Forma final del filtro: `SOL.origen_solicitud <> 'APP'`.
- `backend/procedures/afs_contratos_pago_anual_selec.sql` quedo sin el cambio APP por considerarse SP sin uso desde la working copy.

Prueba del usuario en `InstallQA_89`:
- Inserto XML con `id_soli_externa="987123"`, `soli_moti="6"`, `prepaga="1"`, `contra="1002073"`, `fecha_aplicacion="2026-05-01T00:00:00"`, destino cuenta/subcuenta/plan/tarifa, integrantes `01` y `02`.
- Ejecuto: `EXEC dbo.afi_insertar_soli_auto @var_data_xml = @xml_entrada;`.
- Luego ejecuto batch de solicitudes y vio en log:
  - `Generando cabeceras GMS para solicitudes automaticas APP.`
  - `Error al cerrar afu_genera_soli_auto_app: Procedure has not been executed or has no results`
  - se desconecto y repitio el mismo error.

Diagnostico ya comunicado:
- El SP `afu_genera_soli_auto_app` si ejecuto correctamente; el error es falso negativo del PowerBuilder al cerrar un cursor/procedure que no devuelve resultset.
- En `InstallQA_89`, para `id_soli_externa=987123` se verifico:
  - `SOLI_AUTO_CONTRATO.proce_fecha` informado.
  - `GMS_CAB` generado con `cab_id=295275`, `soli_moti=6`, `transaccion=CC`, `origen_solicitud=APP`.
  - `GMSTXT_CLASE_PLAN` generado.
  - `CAMBIOS_MASIVOS_AGENDA` generado para tipo `AU`.
  - Todavia no habia fila en `SOLICITUDES` porque el batch hizo `CONTINUE` despues del falso error y no proceso la agenda.

Causa tecnica del falso error:
- En `exe_afilmed/src/wp_main_solicitudes.srw`, el batch ejecutaba `afu_genera_soli_auto_app` con `DECLARE ... PROCEDURE` y luego hacia `CLOSE genera_auto`.
- Como `afu_genera_soli_auto_app` tiene `SET NOCOUNT ON` y no devuelve resultset, PowerBuilder marca el `CLOSE` con: `Procedure has not been executed or has no results`.

Cambio local pendiente de commit:
- Archivo modificado localmente: `exe_afilmed/src/wp_main_solicitudes.srw`.
- Correccion aplicada localmente: reemplazar la ejecucion `DECLARE ... PROCEDURE` + `CLOSE genera_auto` por `EXECUTE IMMEDIATE "EXEC dbo.afu_genera_soli_auto_app" USING SQLCA`.
- Se elimino el `CLOSE` asociado.
- Validado localmente: diff correcto, BOM UTF-8 preservado, CRLF preservado, estado SVN con solo ese archivo modificado mas externos `X` normales.
- No se commiteo porque el pedido del usuario fue diagnosticar la ejecucion, no commit explicito.

Al retomar:
- Verificar `svn status` y diff antes de tocar nada.
- Si el usuario pide seguir/corregir/comitear, el cambio relevante a commitear es `exe_afilmed/src/wp_main_solicitudes.srw` con mensaje sugerido: `29361 Corrige ejecucion SP solicitudes APP sin resultset`.

## 2026-06-02 19:49 UTC - Cierre operativo Redmine 29362 Fase 3

Contexto:
- El usuario pidio cerrar la Fase 3 del Redmine 29362 para traslado a contrato existente.
- Objetivo funcional: sacar de `gmsdef_val_traslado_web` las referencias funcionales que no eran web/XML y llevarlas a `gmsdef_val_traslado`.
- Working copy SVN: `~/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.

Commits SVN realizados:
- `r37032 | 29362 Retira validacion funcional traslado de web`
  - Archivo: `backend/procedures/gmsdef_val_traslado_web.sql`.
  - Cambio: retiro de la traza de `Aceptacion funcional del traslado` del SP web/XML.
- `r37034 | 29362 Agrega aceptacion funcional traslado`
  - Archivo: `backend/procedures/gmsdef_val_traslado.sql`.
  - Cambio: agrega temp table `#INTEGRANTES_ANTERIORES` desde `GMSDEF_INTEGRANTES.anter_prepa`, `anter_contra`, `anter_inte` y ejecuta `dbo.afs_val_traslado_aceptada`.
  - Comportamiento esperado: para `soli_moti = 48` permite baja total del contrato origen; para `47` rechaza si el contrato origen queda sin poblacion activa, segun SP legacy.
- `r37036 | 29362 Agrega validacion financiera origen traslado`
  - Archivo: `backend/procedures/gmsdef_val_traslado.sql`.
  - Cambio: agrega validacion de saldos, pagos anticipados, acuerdos, ajustes no facturados y conceptos facturados a futuro del origen.
  - Reutiliza `cls_contrato` y `cls_subcta_contrato`.
  - Para cuentas colectivas valida a nivel subcuenta solo si el contrato origen es ultimo contrato de subcuenta, siguiendo logica observada en `wp_alta_parcial`.
  - Inserta rechazo en `GMS_ERR` con `recha_motivo = 9`.
- `r37038 | 29362 Actualiza diagnostico cierre fase 3`
  - Archivo: `Redmines/29362/fase1-2-3-4_ejecucion_diagnostico_rollback.sql`.
  - Cambio: agrega controles explicitos:
    - `FASE_3_CONTROL_ACEPTACION_FUNCIONAL_TRASLADO`
    - `FASE_3_ACEPTACION_FUNCIONAL_TRASLADO_GMS_ERR`
    - `FASE_3_CONTROL_FINANCIERA_ORIGEN_TRASLADO`
    - `FASE_3_FINANCIERA_ORIGEN_TRASLADO_GMS_ERR`

Validaciones ejecutadas:
- Antes de cada commit se valido `svn status` y diff puntual.
- `gmsdef_val_traslado.sql`: ASCII, sin BOM, finales CRLF puros, sin `RTRIM` y sin `;`.
- `gmsdef_val_traslado_web.sql`: quedo limpio luego de `r37032`.
- `fase1-2-3-4_ejecucion_diagnostico_rollback.sql`: mantiene LF puro, sin BOM, sin `RTRIM` y sin `;`.
- No se ejecuto parse SQL Server porque en el entorno no estan disponibles `sqlcmd` ni `tsql`.

Estado final verificado:
- `gmsdef_val_traslado.sql`, `gmsdef_val_traslado_web.sql` y `Redmines/29362/fase1-2-3-4_ejecucion_diagnostico_rollback.sql` quedaron sin cambios pendientes tras sus commits.
- `svn status` solo muestra externos `X` normales en PBLs:
  - `exe_InfoMailer/pbls/impresion.pbl`
  - `exe_presmed/entidades.pbl`
  - `exe_presmed/ficha_afiliado.pbl`

Punto para retomar manana:
- Si se continua con 29362, ejecutar o pedir ejecucion del script `Redmines/29362/fase1-2-3-4_ejecucion_diagnostico_rollback.sql` contra una base donde esten desplegados los SPs actualizados.
- Revisar especialmente los bloques nuevos de Fase 3 para confirmar que el caso de prueba no queda rechazado por aceptacion funcional ni validacion financiera del origen.
