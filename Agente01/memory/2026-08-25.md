# 2026-08-25

## Conversacion directa Google Chat - Sebastian

### Redmine 29364 - CT APP saldo pendiente muestra comprobante incorrecto
- Se analizo reprueba: era arreglo, no explicacion. La validacion de saldo pendiente era correcta, pero el mensaje informaba el comprobante individual `DA XSCMP07597571` en vez del agrupador `FC`.
- Cambio realizado y comiteado en SVN: `r37652 | Refs #29364 muestra FC agrupadora en saldo CT APP`.
- Archivo ajustado: `backend/procedures/gmsdef_validar.sql`.
- Objetivo del cambio: en validacion de saldos de CT APP, si el comprobante vencido tiene agrupador `FC`, mostrar `FC <cod_unico_comprobante>` en el mensaje, no el `DA/DS` individual.
- Caso de prueba usado: contrato `117329`; `DA XSCMP07597571` asociado a agrupador `FC XSCMP06941664`, cuyo `cod_unico_comprobante` esperado es `144152628`.
- Validaciones hechas: cambio aplicado en `InstallQA_89`, encoding `ISO-8859`, finales `CRLF`, sin `RTRIM` ni `;` agregados. Working copy limpia salvo externos `X` preexistentes en PBLs.
- El mensaje que Sebastian volvio a ver con `DA XSCMP07597571` correspondia a un error historico ya persistido en `GMS_ERR` antes del commit (`id_proceso = 210`, `id_soli_externa = 2008`, `fecha_desde = 24/08/2026 18:47:21`). El arreglo solo afecta errores nuevos.
- Para validar se le dio XML con `id_soli_externa="2009"` para ejecutar `dbo.afi_insertar_soli_auto`, contrato `117329`, `soli_moti="7"`, `prepaga="1"`, `fecha_aplicacion="2026-08-01"`, `inte_nuevo_titular="01"`, `baja_titular="N"`, `baja_moti="10"`, `paren_titular_anterior="C"`, `genera_resp_pagador="N"`, con parentescos 01 P/P/T, 02 T/T/C, 03 C/C/H. Luego correr batch y validar trazabilidad APP por `id_soli_externa = 2009`.

### Redmine 30276 - Consulta detallada de beneficiarios lenta
- Sebastian pidio ponerse en contexto y luego encontrar cuello de botella.
- Contexto del Redmine: Defecto `COOMEVA - Soporte`, producto `Afilmed`, incidente externo `11915`, estado En Desarrollo, asignado a Sebastian. Tema: Solicitudes / Reportes / Consulta Detallada de Beneficiarios.
- Cliente reporta que el reporte detallado de beneficiarios queda procesando mucho tiempo y no entrega resultado. Necesitan obtener, para contratos activos de plan familiar: numero de contrato, valor mensual a pagar, cantidad de cuotas vencidas.
- Prueba de Belkis en `PruebasCoreINFOMEDICAL`, siguiendo adjunto `PAso a paso.docx`: boton Buscar inicio 20:10 y termino 20:57, demorando 47 minutos.
- Ventana principal identificada: `wp_cons_detallada_beneficiario`.
- DataWindow/SP principal: `afs_cons_detallada_beneficiario`.
- Analisis funcional/tecnico previo: aunque el usuario seleccione pocas columnas, el SP trae/calcula todo y recien al exportar descarta columnas. El SP arma muchos datos que no se necesitan para este caso.
- Universo estimado con filtros del adjunto: aprox. `80.741` contratos y `119.016` integrantes activos.
- Cuello de botella medido en `PruebasCoreINFOMEDICAL`: dentro de `afs_cons_detallada_beneficiario`, llamada a `afs_get_valores_cuota_varios`.
- Statement pesado dentro del helper: `INSERT INTO #COMPROBANTES_X_CONTRATO`.
- Evidencia medida con filtros del adjunto: armado inicial del universo aprox. `15s`; contratos filtrados antes de cuotas `267.553`; afiliados activos finales `80.741` contratos / `119.016` integrantes; helper de cuotas empezo con `267.553` contratos; a los `126s` seguia corriendo en `INSERT INTO #COMPROBANTES_X_CONTRATO`; llevaba `17.702.651` lecturas logicas; se cancelo a los `154s` para no cargar QA.
- Conclusion tecnica comunicada: el cuello no esta en abrir la pantalla ni en exportar columnas; esta en calcular `valor_cuota` y `cuotas_vencidas` mediante `afs_get_valores_cuota_varios` para un universo de `267.553` contratos, aunque la salida util activa queda mucho menor.

### Redmine 30276 - Ajuste SVN r37653 y segundo ajuste local pendiente
- Sebastian pregunto que hacer luego de identificar el cuello. Se recomendo no empezar por indices, sino achicar el universo antes de llamar a `afs_get_valores_cuota_varios`: aplicar filtros finales antes, poblar `#CUO_VARIOS_CONTRATOS` solo con contratos que quedaron en `#AFILIADOS`, y luego recalcular/medir.
- Sebastian pidio hacer el arreglo y comitearlo. Se modificaron `backend/procedures/afs_cons_detallada_beneficiario.sql` y `backend/procedures/afs_get_valores_cuota_varios.sql`.
- Commit SVN realizado: `r37653 | Refs #30276 optimiza consulta detallada beneficiarios`.
- Rama/URL del commit: `https://srv-infosvn.infomedical.com/svn/Coomeva/trunk`. Working copy usada: `bmora-Coomeva-trunk`.
- Cambios de `r37653`: el SP principal manda al calculo de cuotas solo los contratos realmente presentes en `#AFILIADOS`; el helper reemplaza el `MAX(factu_perio)` correlacionado por comprobante por una tabla temporal agrupada/indexada.
- Validacion de `r37653`: helper aislado aprox. `1m01s` con `80.741` contratos, contra escenario anterior que seguia corriendo a mas de `154s`. Encoding y finales preservados: CRLF, sin BOM agregado, sin `RTRIM` ni `;`. SVN sin cambios propios pendientes, solo externos `X` preexistentes.
- Sebastian inicio prueba funcional a las `12:42`. Se advirtio que la base de prueba debia tener aplicados los scripts de `r37653` en `afs_cons_detallada_beneficiario` y `afs_get_valores_cuota_varios`.
- A los 12 minutos de prueba se reviso SQL Server: sesion `153`, host `INFONBOOK047`, programa `Afilmed`, estado `suspended`, wait `ASYNC_NETWORK_IO`, bloqueos `0`, tiempo SQL aprox. `12m35s`. Conclusion: ya no estaba en calculo pesado de cuotas; estaba en el `SELECT` final devolviendo filas a Afilmed, apuntando a cliente/red/DataWindow consumiendo muchas filas.
- Luego Sebastian informo que la aplicacion se cerro sola. Se verifico que la sesion `153` quedo `sleeping`, sin request activo. Conclusion comunicada: SQL Server ya no ejecutaba la consulta; el problema restante apunta a Afilmed/DataWindow/cliente por volumen de filas o memoria al consumir el resultado. Prueba aprox. `12:42` a `13:02`, unos 20 minutos, terminada con cierre de aplicacion.
- Sebastian indico que habia que ajustar algo mas. Se preparo un segundo ajuste local, aun NO comiteado al momento del flush: si se seleccionan solo `contrato`, `valor_cuota` y `nro_cuotas_vencidas`, la ventana usa un DataWindow liviano nuevo `d_cons_detallada_beneficiario_basico`; el SP `afs_cons_detallada_beneficiario` acepta `@prm_reporte_basico = 'S'` y en ese modo devuelve solo esas tres columnas; tambien se corrigieron descripciones invertidas del selector, donde `Valor Cuota` y `Cantidad de cuotas vencidas` estaban cruzadas.
- Validacion del segundo ajuste local: encoding/BOM/CRLF revisados; no se agrego `;` ni `RTRIM`. Se le pidio confirmacion explicita a Sebastian para comitear: respuesta esperada si quiere avanzar, `comitealo`.
# 2026-08-25

## Conversacion directa Google Chat - Sebastian

### Redmine 29364 - CT APP saldo pendiente muestra comprobante incorrecto
- Se analizo reprueba: era arreglo, no explicacion. La validacion de saldo pendiente era correcta, pero el mensaje informaba el comprobante individual `DA XSCMP07597571` en vez del agrupador `FC`.
- Cambio realizado y comiteado en SVN: `r37652 | Refs #29364 muestra FC agrupadora en saldo CT APP`.
- Archivo ajustado: `backend/procedures/gmsdef_validar.sql`.
- Objetivo del cambio: en validacion de saldos de CT APP, si el comprobante vencido tiene agrupador `FC`, mostrar `FC <cod_unico_comprobante>` en el mensaje, no el `DA/DS` individual.
- Caso de prueba usado: contrato `117329`; `DA XSCMP07597571` asociado a agrupador `FC XSCMP06941664`, cuyo `cod_unico_comprobante` esperado es `144152628`.
- Validaciones hechas: cambio aplicado en `InstallQA_89`, encoding `ISO-8859`, finales `CRLF`, sin `RTRIM` ni `;` agregados. Working copy limpia salvo externos `X` preexistentes en PBLs.
- El mensaje que Sebastian volvio a ver con `DA XSCMP07597571` correspondia a un error historico ya persistido en `GMS_ERR` antes del commit (`id_proceso = 210`, `id_soli_externa = 2008`, `fecha_desde = 24/08/2026 18:47:21`). El arreglo solo afecta errores nuevos.
- Para validar se le dio XML con `id_soli_externa="2009"` para ejecutar `dbo.afi_insertar_soli_auto`, contrato `117329`, `soli_moti="7"`, `prepaga="1"`, `fecha_aplicacion="2026-08-01"`, `inte_nuevo_titular="01"`, `baja_titular="N"`, `baja_moti="10"`, `paren_titular_anterior="C"`, `genera_resp_pagador="N"`, con parentescos 01 P/P/T, 02 T/T/C, 03 C/C/H. Luego correr batch y validar trazabilidad APP por `id_soli_externa = 2009`.

### Redmine 30276 - Consulta detallada de beneficiarios lenta
- Sebastian pidio ponerse en contexto y luego encontrar cuello de botella.
- Contexto del Redmine: Defecto `COOMEVA - Soporte`, producto `Afilmed`, incidente externo `11915`, estado En Desarrollo, asignado a Sebastian. Tema: Solicitudes / Reportes / Consulta Detallada de Beneficiarios.
- Cliente reporta que el reporte detallado de beneficiarios queda procesando mucho tiempo y no entrega resultado. Necesitan obtener, para contratos activos de plan familiar: numero de contrato, valor mensual a pagar, cantidad de cuotas vencidas.
- Prueba de Belkis en `PruebasCoreINFOMEDICAL`, siguiendo adjunto `PAso a paso.docx`: boton Buscar inicio 20:10 y termino 20:57, demorando 47 minutos.
- Ventana principal identificada: `wp_cons_detallada_beneficiario`.
- DataWindow/SP principal: `afs_cons_detallada_beneficiario`.
- Analisis funcional/tecnico previo: aunque el usuario seleccione pocas columnas, el SP trae/calcula todo y recien al exportar descarta columnas. El SP arma muchos datos que no se necesitan para este caso.
- Universo estimado con filtros del adjunto: aprox. `80.741` contratos y `119.016` integrantes activos.
- Cuello de botella medido en `PruebasCoreINFOMEDICAL`: dentro de `afs_cons_detallada_beneficiario`, llamada a `afs_get_valores_cuota_varios`.
- Statement pesado dentro del helper: `INSERT INTO #COMPROBANTES_X_CONTRATO`.
- Evidencia medida con filtros del adjunto: armado inicial del universo aprox. `15s`; contratos filtrados antes de cuotas `267.553`; afiliados activos finales `80.741` contratos / `119.016` integrantes; helper de cuotas empezo con `267.553` contratos; a los `126s` seguia corriendo en `INSERT INTO #COMPROBANTES_X_CONTRATO`; llevaba `17.702.651` lecturas logicas; se cancelo a los `154s` para no cargar QA.
- Conclusion tecnica comunicada: el cuello no esta en abrir la pantalla ni en exportar columnas; esta en calcular `valor_cuota` y `cuotas_vencidas` mediante `afs_get_valores_cuota_varios` para un universo de `267.553` contratos, aunque la salida util activa queda mucho menor.

### Redmine 30276 - Ajuste SVN r37653 y segundo ajuste local pendiente
- Sebastian pregunto que hacer luego de identificar el cuello. Se recomendo no empezar por indices, sino achicar el universo antes de llamar a `afs_get_valores_cuota_varios`: aplicar filtros finales antes, poblar `#CUO_VARIOS_CONTRATOS` solo con contratos que quedaron en `#AFILIADOS`, y luego recalcular/medir.
- Sebastian pidio hacer el arreglo y comitearlo. Se modificaron `backend/procedures/afs_cons_detallada_beneficiario.sql` y `backend/procedures/afs_get_valores_cuota_varios.sql`.
- Commit SVN realizado: `r37653 | Refs #30276 optimiza consulta detallada beneficiarios`.
- Rama/URL del commit: `https://srv-infosvn.infomedical.com/svn/Coomeva/trunk`. Working copy usada: `bmora-Coomeva-trunk`.
- Cambios de `r37653`: el SP principal manda al calculo de cuotas solo los contratos realmente presentes en `#AFILIADOS`; el helper reemplaza el `MAX(factu_perio)` correlacionado por comprobante por una tabla temporal agrupada/indexada.
- Validacion de `r37653`: helper aislado aprox. `1m01s` con `80.741` contratos, contra escenario anterior que seguia corriendo a mas de `154s`. Encoding y finales preservados: CRLF, sin BOM agregado, sin `RTRIM` ni `;`. SVN sin cambios propios pendientes, solo externos `X` preexistentes.
- Sebastian inicio prueba funcional a las `12:42`. Se advirtio que la base de prueba debia tener aplicados los scripts de `r37653` en `afs_cons_detallada_beneficiario` y `afs_get_valores_cuota_varios`.
- A los 12 minutos de prueba se reviso SQL Server: sesion `153`, host `INFONBOOK047`, programa `Afilmed`, estado `suspended`, wait `ASYNC_NETWORK_IO`, bloqueos `0`, tiempo SQL aprox. `12m35s`. Conclusion: ya no estaba en calculo pesado de cuotas; estaba en el `SELECT` final devolviendo filas a Afilmed, apuntando a cliente/red/DataWindow consumiendo muchas filas.
- Luego Sebastian informo que la aplicacion se cerro sola. Se verifico que la sesion `153` quedo `sleeping`, sin request activo. Conclusion comunicada: SQL Server ya no ejecutaba la consulta; el problema restante apunta a Afilmed/DataWindow/cliente por volumen de filas o memoria al consumir el resultado. Prueba aprox. `12:42` a `13:02`, unos 20 minutos, terminada con cierre de aplicacion.
- Sebastian indico que habia que ajustar algo mas. Se preparo un segundo ajuste local, aun NO comiteado al momento del flush: si se seleccionan solo `contrato`, `valor_cuota` y `nro_cuotas_vencidas`, la ventana usa un DataWindow liviano nuevo `d_cons_detallada_beneficiario_basico`; el SP `afs_cons_detallada_beneficiario` acepta `@prm_reporte_basico = 'S'` y en ese modo devuelve solo esas tres columnas; tambien se corrigieron descripciones invertidas del selector, donde `Valor Cuota` y `Cantidad de cuotas vencidas` estaban cruzadas.
- Validacion del segundo ajuste local: encoding/BOM/CRLF revisados; no se agrego `;` ni `RTRIM`. Se le pidio confirmacion explicita a Sebastian para comitear: respuesta esperada si quiere avanzar, `comitealo`.

### Redmine 30276 - Segundo ajuste local descartado
- Sebastian pregunto por el alcance del segundo ajuste local. Se explico que el reporte actual ejecuta el DataWindow completo, el SP devuelve mas de 90 columnas, Afilmed carga todo en memoria y recien al exportar copia a otro DataStore y borra columnas no seleccionadas.
- Se reconocio que una DW fija de 3 columnas no era solucion mantenible si el usuario podia seleccionar otras combinaciones entre mas de 90 opciones; se habia tomado como hipotesis tecnica, no como alcance pedido por cliente.
- Sebastian marco correctamente que eso implicaba decidir cosas no pedidas por el cliente. Se admitio el desvio de alcance: el segundo cambio no debia comitearse ni tratarse como arreglo final.
- Sebastian luego aclaro que el cierre anterior probablemente fue por 15 minutos de inactividad, y que volvio a ejecutar el reporte con `r37653` y funciono. Resultado funcional: bajo de 47 minutos a aprox. 20 minutos y entrego resultado.
- A pedido de Sebastian, se descarto completo el segundo cambio local. Quedo conservado solo el commit `r37653` en trunk.
- Tras descarte: no queda `d_cons_detallada_beneficiario_basico`, no queda parametro `@prm_reporte_basico`, no quedan cambios locales propios pendientes. `svn status` solo muestra externos `X` preexistentes.

### Redmine 30238 - Contexto inicial
- Sebastian pidio ponerse en contexto del Redmine `30238`.
- Contexto recuperado: Defecto, proyecto `COOMEVA - Soporte`, version `TRUNK`, estado actual `En Pruebas`, asignado a Belkis. Incidente externo/Mantis `11904`.
- Problema reportado en `Solicitudes / Inclusiones`, con `Renuncia de Antiguedad` marcada: el beneficiario debia quedar como `Venta Nueva`, pero el sistema lo dejo como `Reactivacion` y luego le otorgo antiguedad.
- Datos del caso reproducido: solicitud cliente `5634038`; reproduccion en `InstallQA_89` solicitud `4677183`; contrato `1022510`; integrante `02`; documento `CC 6335078`.
- Regla esperada: si aplica Reactivacion u Operacion Retorno y se marca `Renuncia Antiguedad`, debe pasar a `Venta Nueva`.
- Excepcion por Redmine `29058`: si el asesor gestion tiene `Plan Regresa` activo, puede mantenerse como `Reactivacion`.
- En este caso, el asesor no tendria Plan Regresa activo, por eso no deberia aplicar la excepcion.
- Relacion directa con Redmine `29058`.
- El 24/08 ya se habia informado ajuste: ventana `wp_alta_parcial` para que si `renuncia_antiguedad = S` y el asesor vigente no es Plan Regresa, fuerce `VENTA_NUEVA` en los puntos donde se recalcula la novedad por documento y por meses permanencia.
- Adjuntos relevantes mencionados: `1203803.docx` y `Analisis Solicitud Inclusion 4677183 - R30238 Mantis 11904.docx`.
- Se informo a Sebastian que para revisar codigo o base habia que confirmar repo y profile a usar.

## 2026-08-25 17:38 America/Argentina/Buenos_Aires - Solicitudes automáticas / certificados

- Contexto confirmado para trabajo de solicitudes automáticas: repo/rama `svarela-Version104-Afilmed-SolicitudesAutomaticas`, BD `InstallQA_89`.
- Contexto anterior de trunk: repo `svarela-Coomeva-trunk` en `/home/luispicone/.openclaw/workspace/svn/svarela-Coomeva-trunk`, BD profile/base `PruebasCoreINFOMEDICAL`.
- Caso XML `id_soli_externa=90010`, `soli_moti=22`, contrato `1-1000050`, documento `CC-23776800`: el proceso sí se insertó con `id_proceso=212` en `SOLI_AUTO_CONTRATO`; `SOLI_AUTO_RESP_PAGADOR` tiene 1 registro para ese proceso. No se mostró el id porque `afi_insertar_soli_auto_cambio_resp_pagador` finaliza con `RETURN 0` y no hace `SELECT @var_id_proceso AS id_proceso`, a diferencia de otros SP como cambio de titularidad.
- En `InstallQA_89`, error de rechazo `cab_id=295941`, etapa `2`, `tableid=203`, `rowid=208263`, `tipo_error=E`, `recha_motivo=9308`, info `Contrato: 1-1000050, Documento: CC-23776800`: motivo 9308 = "Existe otro cambio de responsable pagador para el mismo contrato y vigencia." Para `id_proceso=212` / `id_soli_externa=90010` / `soli_moti=22`, fecha aplicación-vigencia `01/07/2026`, el rechazo ocurre porque ya existe responsable pagador vigente desde `01/07/2026` para contrato `1000050`: `CC 21303510 - LONDOÑO MARINA`. También hay solicitudes previas con `CC 23776800 - GOMEZ JOSE` en solicitudes `4677063` y `4677064`.
- Caso certificado de preexistencia en `PruebasCoreINFOMEDICAL`, contrato `1204937`: el apellido/nombre del contratante sale en blanco porque el certificado usa `dw_cabecera_solicitud.fecha_aplicacion` como fecha de búsqueda, no la fecha inicio de servicio. En `wp_alta_parcial.srw` línea aprox. 4450: `ls_apli_fecha = STRING(dw_cabecera_solicitud.GetItemDateTime(1,'fecha_aplicacion'),'yyyy/mm/dd')`; luego llama a `afs_datos_impre_certificado`. Con `@prm_fecha_busca='2026-08-25'`, `dbo.afn_inte_tit(1,'1204937','2026-08-25')` devuelve vacío; con `2026-09-01` devuelve integrante `01` y el SP devuelve contratante `MORENO SILVA, ANGEL SANTIAGO`. El contrato/titular tiene vigencia desde `01/09/2026` y `AFI_RESP_PAGADOR` no registra responsable pagador para ese contrato.


### Redmine 30238 - Caso 3 y commit r37662 en rama Version104
- Sebastian paso el Caso 3 del Redmine `30238`. Lectura funcional: es distinto al caso base porque el asesor gestion si es `Plan Regresa`.
- En Caso 3 esta bien que la `Novedad Sistema` se mantenga como `Reactivacion` aunque `Renuncia Antiguedad = S`, por la excepcion de Plan Regresa.
- El sistema reconocia parcialmente la renuncia dejando `Meses de Permanencia = 0`, pero al impactar dejaba en Consulta General de Usuarios una `Fecha de Antiguedad` vieja (`04/03/2022`) en lugar de la fecha de aplicacion de solicitud (`01/08/2026`).
- Se respondio que era `arreglo`, no explicacion: no habia que forzar `Venta Nueva` en este caso, sino corregir el calculo/asignacion de fecha de antiguedad al impactar cuando hay renuncia.
- Sebastian indico trabajar en la rama `Version104-Afilmed-SolicitudesAutomaticas`, donde estaba lo ultimo de solicitudes automaticas.
- Cambio realizado y comiteado en SVN: `r37662 | Refs #30238 ajusta antiguedad por renuncia`.
- Archivo impactado: `exe_afilmed/src/wp_alta_parcial.srw`.
- Objetivo del cambio: cuando `renuncia_antiguedad = 'S'`, la fecha de antiguedad queda con la fecha de ingreso/aplicacion incluso si por Plan Regresa la novedad permanece como `REACTIVACION`; tambien se sincroniza esa fecha en `idw_asoc_lista`, usado al grabar la solicitud.
- Validacion informada: working copy limpia salvo externos SVN normales; archivo conserva UTF-8 con BOM y CRLF, sin mezcla de finales de linea.

### Redmine 28684 - Contexto SARLAFT migracion y commits r37663/r37664
- Sebastian pidio contexto del Redmine `28684`: Tarea `COOMEVA - Soporte`, version `TRUNK`, asunto `Solicitudes => Sarlaft migracion`, estado `En Desarrollo`, asignada a Sebastian, incidente externo `10975`.
- Requerimiento: poblar datos faltantes SARLAFT para usuarios migrados de CEM a Afilmed: profesion, ocupacion, origen de fondo, PEP, ciudad/departamento/pais de nacimiento.
- Tablas/campos relevantes: `AFI_SARLAFT` para pais/depto/municipio nacimiento, `es_pep`, origen de fondos posiblemente `declaracion_bs_fondos`; `AFILIADOS` para `profesion` y `ocupacion`.
- Reglas del ticket: si el dato ya existe en Afilmed, respetarlo; Coomeva aclaro actualizar para todos los contratos del documento sin importar estado vigente/activo/retirado/inhabilitado/castigado; `vigen_desde` debe poblarse con fecha de ejecucion del cargue.
- Historial de entregas: scripts `afu_insert_excel.sql`, `afu_sarlaft_migracion.sql`, `ejecucion.sql`; ultima entrega registrada 21/08/2026 con `backend2.zip`, ejecutar `21082006_afu_insert_excel.sql`, `21082006_afu_sarlaft_migracion.sql`, `21082006_ejecucion.sql`. Savina reabrio pidiendo revisar `#CASO 4`.
- Sebastian planteo que aparentemente no se actualizaba tabla `AFILIADOS`, aunque en version anterior si se hacia, posiblemente no trasladado al hacer reingenieria.
- Se verifico que un script anterior contenia bloque `-- 10. UPDATE SOBRE TABLA OFICIAL AFILIADOS`, armaba `#excel_afiliados_afiliados_update` y actualizaba `AFILIADOS.profesion`, `AFILIADOS.ocupacion`, `modi_fecha`. El `paquete.zip` de mayo tambien traia esa porcion.
- Se confirmo que si la version reingenierizada no actualizaba `AFILIADOS`, probablemente se migro la logica de `AFI_SARLAFT` pero quedo afuera esa seccion.
- Sebastian pidio arreglar y comitear en `Version104-Afilmed-SolicitudesAutomaticas`. Primer commit realizado: `r37663 | Refs #28684 agrega update afiliados sarlaft`, archivo `Redmines/28684/2 - afu_sarlaft_migracion.sql`.
- Luego Sebastian pregunto si la base usada era el ultimo archivo entregado. Se verifico que no: el commit `r37663` habia salido de un archivo anterior de 615 lineas, mientras el ultimo entregado `2 - 21082006_afu_sarlaft_migracion.sql` tenia 1795 lineas, usaba `@modo_preview`, manejaba `AFI_SARLAFT_PREVIEW` y no tenia el bloque de `AFILIADOS`.
- Sebastian pidio rehacer sobre la base correcta y de paso eliminar lineas vacias y dejar separadores por bloque bien indentados.
- Se corrigio sobre la base del ultimo archivo `21082006_afu_sarlaft_migracion` y se comiteo en `Version104-Afilmed-SolicitudesAutomaticas`: `r37664 | Refs #28684 actualiza afiliados en migracion sarlaft`.
- Archivo impactado final: `Redmines/28684/2 - afu_sarlaft_migracion.sql`.
- Validacion final informada: sin lineas vacias reales; separadores por bloque mantenidos; sin `;`; sin `RTRIM`; sin CTE `WITH` sueltas; ASCII, sin BOM, CRLF; working copy limpia para ese archivo.
- El bloque nuevo de actualizacion de `AFILIADOS` quedo en modo real cuando `@modo_preview = 0`; en preview solo informa candidatos.
