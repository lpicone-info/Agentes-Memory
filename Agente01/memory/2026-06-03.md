# Durable Memory - 2026-06-03

## Alias operativo - carpeta Gabriel
- When the user says "guarda en gabriel", "lee de gabriel", or equivalent phrases, interpret "gabriel" as the folder `/mnt/Agentes/Gabriel/` and perform the requested file action there, after validating the path exists.

## Facturacion automatica APP/AU - gpulka Version104
- User debugged automatic billing APP/AU for solicitud `4676708`, contrato `1-1002073`, InstallQA_89. Log on 2026-06-03 showed reversal succeeded and billing later reported success after user forced PB return value.
- Investigation found reversal OK: lote reverso `197828`, prefactura reverso `1119937`, 3 `DA` reversed with 3 `CA`. Prefactura `1119938` remained `NF`, had `hora_inicio_factu` but `hora_fin_factu = NULL`, no `COMPROBANTES` or `PREFAC_COMPROBANTES`.
- Root cause concluded: `lnvo_factu.f_facturacion_facturar(...)` reached `EXECUTE afs_facturar_facturar`; SQL Server warning `Null value is eliminated by an aggregate or other SET operation` was surfaced to PowerBuilder as `SQLCODE = -1`, aborting before comprobantes were generated. Forcing return to `1` only masked the error and committed inconsistent state.
- Similar calls elsewhere use the same signature/parameters; difference was data/path for APP/AU after reversal. Backend pattern already exists in `afs_facturar_verificar.sql`: `SET ANSI_WARNINGS OFF --Para que en el batch no muestre los warnings`.
- User requested fix. Initially I mistakenly committed in Git mirror `/home/luispicone/.openclaw/workspace/Coomeva-Afilmed-Version104-Afilmed-SolicitudesAutomaticas`, commit `d8f3e7bb Evita warnings SQL en facturacion batch`. User corrected that target is SVN.
- Correct SVN working copy: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`. Committed `backend/procedures/afs_facturar_facturar.sql` adding `SET ANSI_WARNINGS OFF` immediately after `SET NOCOUNT ON`. SVN revision `r37045`, message `Evita warnings SQL en facturacion batch`. File preserved ISO-8859 and CRLF. This is the valid change for user.
- User compiled the SP. To preserve case, advised not to rerun full APP/AU because reversal already happened and a second run could fail with no data to reverse. Current live pending prefactura identified as `1119939`, with 4 active `PREFACTURAS` rows, total neto `30.700`, contrato `1-1002073`, no `gene_fecha`, no comprobantes.
- User decided to manually facturar in Afilmed months May/June/July and rerun full process, needing only cleanup of request traces so there is something to reverse. Provided a SQL cleanup script with `ROLLBACK` default, deleting only `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_CONTRATO`, `GMS_INTEGRANTES`, `GMS_CONTRATOS`, `GMS_CAB` for `soli=4676708`, `cab_id=295276`, `id_proceso=5`, `id_soli_externa=987123`, `prepaga=1`, `contra='1002073'`, `prefac_cab=1119938`, `lote_reverso=197828`. Explicitly did not delete prefacturas/comprobantes/reversal tables.
# Durable Memory - 2026-06-03

## Facturacion automatica APP/AU - gpulka Version104
- User debugged automatic billing APP/AU for solicitud `4676708`, contrato `1-1002073`, InstallQA_89. Log on 2026-06-03 showed reversal succeeded and billing later reported success after user forced PB return value.
- Investigation found reversal OK: lote reverso `197828`, prefactura reverso `1119937`, 3 `DA` reversed with 3 `CA`. Prefactura `1119938` remained `NF`, had `hora_inicio_factu` but `hora_fin_factu = NULL`, no `COMPROBANTES` or `PREFAC_COMPROBANTES`.
- Root cause concluded: `lnvo_factu.f_facturacion_facturar(...)` reached `EXECUTE afs_facturar_facturar`; SQL Server warning `Null value is eliminated by an aggregate or other SET operation` was surfaced to PowerBuilder as `SQLCODE = -1`, aborting before comprobantes were generated. Forcing return to `1` only masked the error and committed inconsistent state.
- Similar calls elsewhere use the same signature/parameters; difference was data/path for APP/AU after reversal. Backend pattern already exists in `afs_facturar_verificar.sql`: `SET ANSI_WARNINGS OFF --Para que en el batch no muestre los warnings`.
- User requested fix. Initially I mistakenly committed in Git mirror `/home/luispicone/.openclaw/workspace/Coomeva-Afilmed-Version104-Afilmed-SolicitudesAutomaticas`, commit `d8f3e7bb Evita warnings SQL en facturacion batch`. User corrected that target is SVN.
- Correct SVN working copy: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`. Committed `backend/procedures/afs_facturar_facturar.sql` adding `SET ANSI_WARNINGS OFF` immediately after `SET NOCOUNT ON`. SVN revision `r37045`, message `Evita warnings SQL en facturacion batch`. File preserved ISO-8859 and CRLF. This is the valid change for user.
- User compiled the SP. To preserve case, advised not to rerun full APP/AU because reversal already happened and a second run could fail with no data to reverse. Current live pending prefactura identified as `1119939`, with 4 active `PREFACTURAS` rows, total neto `30.700`, contrato `1-1002073`, no `gene_fecha`, no comprobantes.
- User decided to manually facturar in Afilmed months May/June/July and rerun full process, needing only cleanup of request traces so there is something to reverse. Provided a SQL cleanup script with `ROLLBACK` default, deleting only `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_CONTRATO`, `GMS_INTEGRANTES`, `GMS_CONTRATOS`, `GMS_CAB` for `soli=4676708`, `cab_id=295276`, `id_proceso=5`, `id_soli_externa=987123`, `prepaga=1`, `contra='1002073'`, `prefac_cab=1119938`, `lote_reverso=197828`. Explicitly did not delete prefacturas/comprobantes/reversal tables.

## Traslado web - srmontenegro Version104
- User identified as `srmontenegro`; use SVN working copy `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas` for this line of work, not gpulka.
- SSMS showed inconsistent line endings opening `backend/procedures/gmsdef_val_traslado_web.sql`. Two copies existed; after user confirmed srmontenegro, normalized only the srmontenegro SVN file to Windows CRLF. SVN commit `r37043`, message `Normaliza finales de linea de gmsdef_val_traslado_web`. Verified content SQL unchanged by canonical hash, ASCII/no BOM, CRLF consistent, file clean after commit.
- Compile errors in same SP were due to references to non-existent columns on `GMSDEF_INTEGRANTES`: `plan_codi`, `tari`, `tari_adi`. Fixed validation to get destination plan/tariff from `GMS_CONTRATOS` (`plan_codi_afi`, `tari_afi`) and additional plan/tariff from `GMSDEF_ADICIONALES` (`plan_codi_adi`, `tari_adi`). SVN commit `r37044`, message `Corrige validacion de cobertura destino web`. Validated with SQL Server `NOEXEC`: OK, no invalid-column errors. File remained ASCII and CRLF.
- User later said execution with subfase 4 did not arrive/happen and asked to find an example that does all steps. Found a valid rollback-tested XML case that completes full flow: destination prepaga `1`, contrato `999146`, fecha_aplicacion/inicio `2026-06-01`, integrante destino `01`, documento `CC 1010244111`; origin prepaga `1`, contrato `999936`, integrante `01`; anexos plan `PREFM`, tarifa `AFPREFMA`. XML: `<solicitud id_soli_externa="29362101" soli_moti="48" prepaga="1" contra="999146" fecha_aplicacion="2026-06-01" fecha_inicio_servicio="2026-06-01" asesor_gestion="2"><integrantes><integrante inte="01" docu_tipo="CC" docu_nro="1010244111" anter_prepa="1" anter_contra="999936" anter_inte="01" paren_destino="T" /></integrantes><anexos><anexo inte="01" plan_anexo="PREFM" tari_anexo="AFPREFMA" /></anexos></solicitud>`.
- Rollback validation for the example: `afi_insertar_soli_auto` OK, `afu_genera_soli_auto_app` OK, `gms_validar_todo` OK with `GMS_ERR = 0`, and `gms_genera_solicitudes` OK generating solicitud `4676711` within rollback.
- Differences from failing screenshot/case: document was `101024411` but real origin affiliate is `1010244111`; using `inte="02"` for a titular caused `gms_genera_solicitudes` to search origin for destination titular `inte="01"` and not find it. With `inte="01"` the full flow completed.
# Durable Memory - 2026-06-03

## Alias operativo - carpeta Gabriel
- When the user says "guarda en gabriel", "lee de gabriel", or equivalent phrases, interpret "gabriel" as the folder `/mnt/Agentes/Gabriel/` and perform the requested file action there, after validating the path exists.

## Facturacion automatica APP/AU - gpulka Version104
- User debugged automatic billing APP/AU for solicitud `4676708`, contrato `1-1002073`, InstallQA_89. Log on 2026-06-03 showed reversal succeeded and billing later reported success after user forced PB return value.
- Investigation found reversal OK: lote reverso `197828`, prefactura reverso `1119937`, 3 `DA` reversed with 3 `CA`. Prefactura `1119938` remained `NF`, had `hora_inicio_factu` but `hora_fin_factu = NULL`, no `COMPROBANTES` or `PREFAC_COMPROBANTES`.
- Root cause concluded: `lnvo_factu.f_facturacion_facturar(...)` reached `EXECUTE afs_facturar_facturar`; SQL Server warning `Null value is eliminated by an aggregate or other SET operation` was surfaced to PowerBuilder as `SQLCODE = -1`, aborting before comprobantes were generated. Forcing return to `1` only masked the error and committed inconsistent state.
- Similar calls elsewhere use the same signature/parameters; difference was data/path for APP/AU after reversal. Backend pattern already exists in `afs_facturar_verificar.sql`: `SET ANSI_WARNINGS OFF --Para que en el batch no muestre los warnings`.
- User requested fix. Initially I mistakenly committed in Git mirror `/home/luispicone/.openclaw/workspace/Coomeva-Afilmed-Version104-Afilmed-SolicitudesAutomaticas`, commit `d8f3e7bb Evita warnings SQL en facturacion batch`. User corrected that target is SVN.
- Correct SVN working copy: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`. Committed `backend/procedures/afs_facturar_facturar.sql` adding `SET ANSI_WARNINGS OFF` immediately after `SET NOCOUNT ON`. SVN revision `r37045`, message `Evita warnings SQL en facturacion batch`. File preserved ISO-8859 and CRLF. This is the valid change for user.
- User compiled the SP. To preserve case, advised not to rerun full APP/AU because reversal already happened and a second run could fail with no data to reverse. Current live pending prefactura identified as `1119939`, with 4 active `PREFACTURAS` rows, total neto `30.700`, contrato `1-1002073`, no `gene_fecha`, no comprobantes.
- User decided to manually facturar in Afilmed months May/June/July and rerun full process, needing only cleanup of request traces so there is something to reverse. Provided a SQL cleanup script with `ROLLBACK` default, deleting only `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_CONTRATO`, `GMS_INTEGRANTES`, `GMS_CONTRATOS`, `GMS_CAB` for `soli=4676708`, `cab_id=295276`, `id_proceso=5`, `id_soli_externa=987123`, `prepaga=1`, `contra='1002073'`, `prefac_cab=1119938`, `lote_reverso=197828`. Explicitly did not delete prefacturas/comprobantes/reversal tables.
# Durable Memory - 2026-06-03

## Facturacion automatica APP/AU - gpulka Version104
- User debugged automatic billing APP/AU for solicitud `4676708`, contrato `1-1002073`, InstallQA_89. Log on 2026-06-03 showed reversal succeeded and billing later reported success after user forced PB return value.
- Investigation found reversal OK: lote reverso `197828`, prefactura reverso `1119937`, 3 `DA` reversed with 3 `CA`. Prefactura `1119938` remained `NF`, had `hora_inicio_factu` but `hora_fin_factu = NULL`, no `COMPROBANTES` or `PREFAC_COMPROBANTES`.
- Root cause concluded: `lnvo_factu.f_facturacion_facturar(...)` reached `EXECUTE afs_facturar_facturar`; SQL Server warning `Null value is eliminated by an aggregate or other SET operation` was surfaced to PowerBuilder as `SQLCODE = -1`, aborting before comprobantes were generated. Forcing return to `1` only masked the error and committed inconsistent state.
- Similar calls elsewhere use the same signature/parameters; difference was data/path for APP/AU after reversal. Backend pattern already exists in `afs_facturar_verificar.sql`: `SET ANSI_WARNINGS OFF --Para que en el batch no muestre los warnings`.
- User requested fix. Initially I mistakenly committed in Git mirror `/home/luispicone/.openclaw/workspace/Coomeva-Afilmed-Version104-Afilmed-SolicitudesAutomaticas`, commit `d8f3e7bb Evita warnings SQL en facturacion batch`. User corrected that target is SVN.
- Correct SVN working copy: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`. Committed `backend/procedures/afs_facturar_facturar.sql` adding `SET ANSI_WARNINGS OFF` immediately after `SET NOCOUNT ON`. SVN revision `r37045`, message `Evita warnings SQL en facturacion batch`. File preserved ISO-8859 and CRLF. This is the valid change for user.
- User compiled the SP. To preserve case, advised not to rerun full APP/AU because reversal already happened and a second run could fail with no data to reverse. Current live pending prefactura identified as `1119939`, with 4 active `PREFACTURAS` rows, total neto `30.700`, contrato `1-1002073`, no `gene_fecha`, no comprobantes.
- User decided to manually facturar in Afilmed months May/June/July and rerun full process, needing only cleanup of request traces so there is something to reverse. Provided a SQL cleanup script with `ROLLBACK` default, deleting only `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_CONTRATO`, `GMS_INTEGRANTES`, `GMS_CONTRATOS`, `GMS_CAB` for `soli=4676708`, `cab_id=295276`, `id_proceso=5`, `id_soli_externa=987123`, `prepaga=1`, `contra='1002073'`, `prefac_cab=1119938`, `lote_reverso=197828`. Explicitly did not delete prefacturas/comprobantes/reversal tables.

## Traslado web - srmontenegro Version104
- User identified as `srmontenegro`; use SVN working copy `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas` for this line of work, not gpulka.
- SSMS showed inconsistent line endings opening `backend/procedures/gmsdef_val_traslado_web.sql`. Two copies existed; after user confirmed srmontenegro, normalized only the srmontenegro SVN file to Windows CRLF. SVN commit `r37043`, message `Normaliza finales de linea de gmsdef_val_traslado_web`. Verified content SQL unchanged by canonical hash, ASCII/no BOM, CRLF consistent, file clean after commit.
- Compile errors in same SP were due to references to non-existent columns on `GMSDEF_INTEGRANTES`: `plan_codi`, `tari`, `tari_adi`. Fixed validation to get destination plan/tariff from `GMS_CONTRATOS` (`plan_codi_afi`, `tari_afi`) and additional plan/tariff from `GMSDEF_ADICIONALES` (`plan_codi_adi`, `tari_adi`). SVN commit `r37044`, message `Corrige validacion de cobertura destino web`. Validated with SQL Server `NOEXEC`: OK, no invalid-column errors. File remained ASCII and CRLF.
- User later said execution with subfase 4 did not arrive/happen and asked to find an example that does all steps. Found a valid rollback-tested XML case that completes full flow: destination prepaga `1`, contrato `999146`, fecha_aplicacion/inicio `2026-06-01`, integrante destino `01`, documento `CC 1010244111`; origin prepaga `1`, contrato `999936`, integrante `01`; anexos plan `PREFM`, tarifa `AFPREFMA`. XML: `<solicitud id_soli_externa="29362101" soli_moti="48" prepaga="1" contra="999146" fecha_aplicacion="2026-06-01" fecha_inicio_servicio="2026-06-01" asesor_gestion="2"><integrantes><integrante inte="01" docu_tipo="CC" docu_nro="1010244111" anter_prepa="1" anter_contra="999936" anter_inte="01" paren_destino="T" /></integrantes><anexos><anexo inte="01" plan_anexo="PREFM" tari_anexo="AFPREFMA" /></anexos></solicitud>`.
- Rollback validation for the example: `afi_insertar_soli_auto` OK, `afu_genera_soli_auto_app` OK, `gms_validar_todo` OK with `GMS_ERR = 0`, and `gms_genera_solicitudes` OK generating solicitud `4676711` within rollback.
- Differences from failing screenshot/case: document was `101024411` but real origin affiliate is `1010244111`; using `inte="02"` for a titular caused `gms_genera_solicitudes` to search origin for destination titular `inte="01"` and not find it. With `inte="01"` the full flow completed.

## GPulka Version104 - Redmine 29361 AUFACT filtros APP
- Working copy for this line: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- User asked to update WC for Redmine `29361`, branch `gpulka`, Version104, and verify that solicitudes excluded from normal billing by `origen_solicitud = 'APP'` were still included in another circuit.
- SVN update left WC at revision `37046` with no local versioned changes, only externals.
- Reviewed evidence: normal billing excludes `APP` through filters like `SOL.origen_solicitud <> 'APP'`; AUFACT includes APP through `afs_soli_auto_app_fact_pendientes`, `afs_proceso_agenda_superbatch`, `wp_main_super_batch`, and `afu_soli_auto_app_fact_traza`.
- Initial AUFACT pending detection had `SOL.origen_solicitud = 'APP'`, `SOL.soli_moti = 6`, `prefac_cab IS NULL`, `baja_fecha IS NULL`, `SOLI_AUTO_CONTRATO.proce_fecha_fact IS NULL`.
- User warned there will be several more `soli_moti` values and suggested removing the `soli_moti = 6` filter; agreed and changed AUFACT pending detection to rely on `SOL.origen_solicitud = 'APP'` plus economic/status marks, independent of `soli_moti`.
- Committed SVN revision `37047`, message `29361 - ampliar pendientes AUFACT por origen APP`.
- Files committed in r37047: `backend/procedures/afs_soli_auto_app_fact_pendientes.sql`, `backend/procedures/afs_proceso_agenda_superbatch.sql`, `Redmines/29361/29361.md`.
- Validation after r37047: target files clean; CRLF preserved; no BOM; remaining `soli_moti = 6` references in markdown are only for current change-of-coverage mapping, not AUFACT billing filter.

## In-progress request before compaction - Gabriel SQL for wf_proceso_facturacion_app_au
- Current user request repeated: "guardame en la carpeta de gabriel un archivo .sql para ejecutar todo lo que hacemos en wf_proceso_facturacion_app_au, con rollback, preparamelo con las variables seteadas para el contrato 1002073".
- Interpret "carpeta de gabriel" as `/mnt/Agentes/Gabriel/`.
- Need create/save a `.sql` file there, likely in context of gpulka Version104 and contract `1002073`, to execute the same operations performed by `wf_proceso_facturacion_app_au`, with variables pre-set and wrapped so default is rollback.
- Important: complete the actual file creation after compaction if not already done. Need inspect/read `wf_proceso_facturacion_app_au` in `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas` and reproduce its relevant SP/procedure call sequence with rollback. Avoid committing unless user asks.

## Traslado web APP - Redmine 29362 subfases 4 a 7
- Working copy for this work: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`. Keep using SVN, not Git mirror.
- User reported `gms_genera_solicitudes` invocation issue in `Redmines/29362/fase1-2-3-4_ejecucion_diagnostico_rollback.sql`. Fixed line by changing the call to positional parameters: `EXEC dbo.gms_genera_solicitudes @cab_id, @usuari_generacion, 'N'`. Committed SVN revision `r37048`, message `29362 Corrige invocacion GMS en diagnostico`.
- Subfase 5 objective: impact partial alta into destination contract for traslado APP, explicitly excluding certificates, medical audit, and health-state controls because those are handled before XML submission. Implemented in `backend/procedures/gms_genera_solicitudes.sql` and `backend/procedures/afu_completa_datos_audit_para_traslado.sql`, with documented start/end blocks. Added integrated diagnostic rollback script `Redmines/29362/fase1-2-3-4-5_ejecucion_diagnostico_rollback.sql` and updated `Redmines/29362/traslado a contrato existente fase 3.md` plus `Redmines/29362/29362.md`. Validation: `NOEXEC` OK; rollback OK; destination `AFILIADOS`, `AFI_GRAVADOS`, `AFI_HISTO_PAREN`, `AFI_ADICIONALES`, `SOLI_PREEXISTENCIAS`, and destination `AFI_PREEXISTENCIAS` expected records present; automatic audit and certificate counts 0; `@@TRANCOUNT = 0`. Committed SVN revision `r37049`, message `29362 Completa subfase 5 traslado APP`.
- Subfase 6 objective: retiro/baja del contrato original after destination impact. Documented and validated changes in `backend/procedures/gms_genera_solicitudes.sql`, `backend/procedures/afi_genera_solis_aut_traslados.sql`, created `Redmines/29362/fase1-2-3-4-5-6_ejecucion_diagnostico_rollback.sql`, and updated docs `Redmines/29362/traslado a contrato existente fase 3.md` and `Redmines/29362/29362.md`. Validation: `NOEXEC` OK; integrated rollback OK; generated origin request with `soli_moti = 3` for total baja; baja total expected `S`; origin requests generated `1`; origin affiliates baja `1`; rollback executed and `@@TRANCOUNT = 0`. Committed SVN revision `r37050`, message `29362 Documenta y valida fase 6 traslado APP`.
- Subfase 7 was completed as final consistency validation only; no functional `INSERT` was added. Created `Redmines/29362/fase1-2-3-4-5-6-7_ejecucion_diagnostico_rollback.sql`, updated `Redmines/29362/traslado a contrato existente fase 3.md` and `Redmines/29362/29362.md`. The script adds `INICIO FASE 7` / `FIN FASE 7` controls for `SOLI_AUTO_CONTRATO.soli`, `GMS_CONTRATOS.soli`, `SOLICITUDES`, `GMSTXT_INTEGRANTES`/`GMSDEF_INTEGRANTES` `anter_*`, `SOLI_AFILIADOS` `anter_*`, destination affiliate active, origin affiliate retired, derived origin request generated, no automatic audits/certificates, and `GMS_ERR = 0`. Validation: SQL `NOEXEC_OK`; integrated rollback OK; Fase 7 controls all expected counts `1`, audit/certificate `0`, `ROLLBACK_EJECUTADO`, `@@TRANCOUNT = 0`. Committed SVN revision `r37054`, message `29362 Agrega validacion final traslado APP`. After commit, local status clean except SVN externals `X`.
