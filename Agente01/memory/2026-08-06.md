
## 2026-08-06 14:36 ART - Gabriel / comisiones y homologacion

- Usuario Gabriel trabaja con bases SQL Server `InstallQA_89` y `pruebascoreinfomedical`; prefiere analisis tecnico directo, cauteloso con procesos activos y transacciones con rollback para pruebas destructivas.
- Se armo script SQL en `/mnt/Agentes/gpulka/afi_cambio_cober_cooperativa_casos_rollback.sql` para probar `afi_cambio_cober_cooperativa` con rollback en `InstallQA_89`. Casos incluidos: bloqueo por `fecha_aplicacion_cambio = 2026-06-01`, retrocompatibilidad con fecha NULL, solicitudes `1/47/48`, `2`, `6`, `22`, y fila informativa `7_CAMBIO_TITULAR` sin candidato real consistente.
- Resultado de ejecucion reportado por Gabriel: casos 1 a 6 dieron `OK`. El ultimo `SIN_CANDIDATO_REAL / 7_CAMBIO_TITULAR` no fue prueba ejecutada ni error; es aviso de cobertura faltante porque no se encontro en `InstallQA_89` un caso con solicitud motivo 7 y `SOLI_AFILIADOS` compatible (`codigo_unico_persona = TMP.id_contratante`, `paren = 'T'`, `paren_real_ant = 'T'`) mas el resto de condiciones.
- En revision de `afi_procesa_comisiones_valores_cabecera`, el `INSERT INTO #COM_RECAUDOS` es optimizable: `#COM_RECAUDOS` solo tenia indices por `compro_tipo` y `compro_tipo_novedad`, pero luego se usa por `(ente, compro_tipo, compro_nro)` y `cod_unico_comprobante`. Se sugirio probar indices temporales `#COM_RECAUDOS(ente, compro_tipo, compro_nro)` y `#COM_RECAUDOS(cod_unico_comprobante)`.
- Tambien se detecto posible indice permanente de alto impacto: `CREATE INDEX idx_com_mov_recaudos_compro_nro ON COMISIONES_MOVIMIENTOS_RECAUDOS (compro_nro, id_cabecera)`, porque el anti-join historico compara `COM.cod_unico_comprobante = REC.compro_nro` y la tabla no tenia indice por `compro_nro`.
- Sobre lentitud del periodo 07/2026 en `pruebascoreinfomedical`, el proceso de comisiones estaba corriendo en `spid 129`; se reviso con cuidado sin tocar stats ni crear indices para no interferir. Observado: sin bloqueo (`blocking_session_id = 0`, `wait_type = NULL`, `last_wait_type = SOS_SCHEDULER_YIELD`), dentro de `INSERT INTO #COM_RECAUDOS`, con lecturas logicas aprox. `1.898.167.337` y CPU aprox. `9.270.375 ms`.
- Plan activo del `spid 129`: estimacion muy baja (~35 filas) para el insert y missing index sobre `COMISIONES_MOVIMIENTOS_RECAUDOS(compro_nro)`; habia `Clustered Index Scan` sobre `COMISIONES_MOVIMIENTOS_RECAUDOS` (~9.865.573 filas) dentro del `NOT EXISTS` contra `REC.compro_nro`.
- Estadisticas vistas en `COMISIONES_MOVIMIENTOS_RECAUDOS`: `last_updated = 2026-06-21`, `modification_counter ~= 645.087`. Volumen de cabeceras no explicaba por si solo la demora: 07/2026 = 180.533, 06/2026 = 179.410, 07/2025 = 171.056. Comprobantes financieros 07/2026 altos: tipo A 170.793, tipo E 10.301.
- Hipotesis comunicada: no parece dato puntual raro ni lockeo; mas probable es volumen acumulado + estadisticas desactualizadas + falta de indice por `compro_nro`, causando mala estimacion y scans repetidos en el anti-join. Recomendacion para fuera de la corrida activa: `UPDATE STATISTICS COMISIONES_MOVIMIENTOS_RECAUDOS` y evaluar el indice `(compro_nro, id_cabecera)`.

## 2026-08-06 15:57 ART - Savina / Afilmed ProntoPago y portal de pagos

- Usuario Savina consulta sobre cliente Coomeva y flujo de ProntoPago desde Afilmed hacia portal propio de pagos. Contexto funcional aclarado: hoy Afilmed genera ProntoPago, luego FC, luego Facturacion Electronica, y alguien envia la FC al cliente. Nuevo pedido: cliente Coomeva entraria a un portal propio para visualizar/pagar lo generado por Afilmed.
- Criterio funcional comunicado: Afilmed debe seguir siendo responsable de generar la deuda/ProntoPago/FC; el portal deberia visualizar o recibir esa deuda y devolver estado de pago a Afilmed. No conviene plantear que el portal genere el ProntoPago.
- En repo SVN trunk `svarela-Coomeva-trunk`, se analizo proceso de link/pagos electronicos. Procedimiento `backend/procedures/afs_link_pagos.sql` recibe `@prm_docu_nro` y devuelve comprobantes/deudas vigentes y pagables para un documento: tipo, numero unico, emision, vencimiento, programa, contrato/subcuenta, total liquidado y saldo.
- `afs_link_pagos.sql` valida existencia del documento como contratante/responsable/cuenta/primera cuota; si no existe devuelve error "El contratante no existe o esta retirado". Distingue colectivo por `CUENTAS.docu_nro` e individual por `codigo_unico_persona`. Filtra comprobantes no reversados, vencimiento vigente, saldo > 0, debito/credito positivo, tipos `DA`, `DS`, `FC`, `EC`, `AP`, `RD`; en individual tambien contempla `PP` agrupado.
- No se encontro pantalla PowerBuilder que invoque `afs_link_pagos`; parece procedimiento backend consumible por portal/servicio externo. En facturacion electronica se embebe link `https://oficinavirtualmp.coomeva.com.co/Oficina-virtual/#/payment/credentials?numid=` concatenado con documento del contratante, en `backend/procedures/afu_fact_elect_actualiza_contenido.sql` asignado a `ENC_18`.
- Entrada de pagos a Afilmed: `backend/procedures/afi_pe_compro_importacion.sql` inserta respuesta en `PE_COMPRO_IMPORTACION` con sitio, fecha, moneda, valor, comprobante/documento, forma de pago, autorizacion, transaccion, referencia y CUS. Batch `afu_procesa_pagos_electronicos` procesa pendientes y genera lotes en `COMPRO_IMPOR_LOTES` / `COMPRO_IMPORTACION`; invocado desde `exe_afilmed/src/wp_main_super_batch.srw` como `IMPORTACION_RECAUDOS_PE`.
- Conclusion tecnica comunicada: flujo actual parece ser pull/consulta por portal: Afilmed factura e incluye link; portal consulta deuda por documento probablemente via `afs_link_pagos`; al pagar, portal informa a Afilmed via `afi_pe_compro_importacion`; batch procesa recaudacion. No se vio evidencia de push activo de ProntoPago desde Afilmed al portal.
- Pregunta puntual resuelta: si un contratante tiene 2 contratos en 2 sucursales comercializables distintas, el trunk deberia generar 2 PP, no uno. Razon: `afi_fac_compro_a_generar.sql` agrupa PP individual por contratante pero incluye `suc_comercializable` en el `GROUP BY` junto con cuenta, id_contratante, subcta_tipo, forma de pago y otros datos. Misma sucursal podria agrupar en 1 PP; distinta sucursal separa en 2 PP.

## 2026-08-06 17:18 ART - Savina / V104 solicitudes automaticas traslado contrato nuevo

- Savina pidio actualizar rama/perfil SVN `svarela-Version104-Afilmed-SolicitudesAutomaticas` con ultimos cambios de V104/desarrollo de Sebastian para probar XML de solicitudes automaticas. Working copy en `/home/luispicone/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas` quedo en revision SVN `37540`; externals actualizados tambien a r37540. Branch ya contenia ultimo cambio propio de Sebastian `srmontenegro@INFOMEDICAL`, rev `37530`. `svn status` final limpio salvo externos `X`.
- Para `soli_moti="47"` en V104 actualizada, el XML de traslado a contrato nuevo debe informar solo `fecha_inicio_servicio`; no deben enviarse `fecha_aplicacion` ni `fecha_radicacion`. El wrapper `afi_insertar_soli_auto` calcula `fecha_aplicacion` desde `fecha_inicio_servicio` y usa `GETDATE()` para radicacion. Tambien conviene omitir `ingre_fecha` del integrante para que se complete automaticamente.
- En pruebas de `emi_modo` y `emi_grupo`, se determino que para validar autocompletado no se deben enviar esos atributos en `<facturacion>`. Si se mandan vacios (`emi_modo=""` / `emi_grupo=""`) el parser intenta convertirlos a `TINYINT`. Facturacion minima usada: `<facturacion vto_grupo="2" pago_forma="E" />`; banco, sucursal, tipo cuenta, cuenta, tarjeta, titular y vencimiento no son obligatorios para ese XML. `pago_forma` puede defaultar a `E`, pero se dejo explicitado para prueba.
- Error observado con XML inicial: `No existe afiliado origen vigente para uno o mas integrantes del traslado a contrato nuevo` en `afi_insertar_soli_auto` linea 108. Causa funcional: `soli_moti="47"` no es alta totalmente nueva; es traslado a contrato nuevo y exige afiliado origen vigente en `AFILIADOS` mediante `anter_prepa`, `anter_contra`, `anter_inte`, `docu_tipo`, `docu_nro`. Datos totalmente nuevos siempre fallan en ese flujo.
- Caso validado en base `InstallQA_89` con `BEGIN TRAN / ROLLBACK`: XML con `id_soli_externa="293631201"`, responsable/integrante `CC 8103024`, `ALVAREZ CASTAÑO JUAN DAVID`, origen `anter_prepa="1"`, `anter_contra="1000048"`, `anter_inte="03"`, paso correctamente y devolvio `id_proceso = 177`; rollback aplicado, no se grabo. Validaciones: afiliado origen vigente, sin baja, no usado en `SOLI_AUTO_INTEGRANTES`, id externa libre.
- Ese XML generado luego creo contrato `1073920` cuando Savina lo ejecuto. Se verifico en `InstallQA_89` que el contrato `1073920` quedo con `cod_camp_comercial = 134` (`COMBO 2 PLUS - DENTAL FAMILIAR`).
- Sobre seleccion de campania en ventana manual T. Contrato Nuevo: en `wp_alta_total.srw`, cuando `ii_soli_moti = 47`, los controles de campania se ocultan (`campania_t`, `cb_campania`, `camp_comercial_deno`, `cod_camp_comercial`, `cb_validar_campania`). Conclusion comunicada: en T. Contrato Nuevo manual no se selecciona campania desde la ventana; en flujo automatico/XML si se puede informar con atributo `cod_camp_comercial="134"` y se impacta en contrato.
## 2026-08-06 14:36 ART - Gabriel / comisiones y homologacion

- Usuario Gabriel trabaja con bases SQL Server `InstallQA_89` y `pruebascoreinfomedical`; prefiere analisis tecnico directo, cauteloso con procesos activos y transacciones con rollback para pruebas destructivas.
- Se armo script SQL en `/mnt/Agentes/gpulka/afi_cambio_cober_cooperativa_casos_rollback.sql` para probar `afi_cambio_cober_cooperativa` con rollback en `InstallQA_89`. Casos incluidos: bloqueo por `fecha_aplicacion_cambio = 2026-06-01`, retrocompatibilidad con fecha NULL, solicitudes `1/47/48`, `2`, `6`, `22`, y fila informativa `7_CAMBIO_TITULAR` sin candidato real consistente.
- Resultado de ejecucion reportado por Gabriel: casos 1 a 6 dieron `OK`. El ultimo `SIN_CANDIDATO_REAL / 7_CAMBIO_TITULAR` no fue prueba ejecutada ni error; es aviso de cobertura faltante porque no se encontro en `InstallQA_89` un caso con solicitud motivo 7 y `SOLI_AFILIADOS` compatible (`codigo_unico_persona = TMP.id_contratante`, `paren = 'T'`, `paren_real_ant = 'T'`) mas el resto de condiciones.
- En revision de `afi_procesa_comisiones_valores_cabecera`, el `INSERT INTO #COM_RECAUDOS` es optimizable: `#COM_RECAUDOS` solo tenia indices por `compro_tipo` y `compro_tipo_novedad`, pero luego se usa por `(ente, compro_tipo, compro_nro)` y `cod_unico_comprobante`. Se sugirio probar indices temporales `#COM_RECAUDOS(ente, compro_tipo, compro_nro)` y `#COM_RECAUDOS(cod_unico_comprobante)`.
- Tambien se detecto posible indice permanente de alto impacto: `CREATE INDEX idx_com_mov_recaudos_compro_nro ON COMISIONES_MOVIMIENTOS_RECAUDOS (compro_nro, id_cabecera)`, porque el anti-join historico compara `COM.cod_unico_comprobante = REC.compro_nro` y la tabla no tenia indice por `compro_nro`.
- Sobre lentitud del periodo 07/2026 en `pruebascoreinfomedical`, el proceso de comisiones estaba corriendo en `spid 129`; se reviso con cuidado sin tocar stats ni crear indices para no interferir. Observado: sin bloqueo (`blocking_session_id = 0`, `wait_type = NULL`, `last_wait_type = SOS_SCHEDULER_YIELD`), dentro de `INSERT INTO #COM_RECAUDOS`, con lecturas logicas aprox. `1.898.167.337` y CPU aprox. `9.270.375 ms`.
- Plan activo del `spid 129`: estimacion muy baja (~35 filas) para el insert y missing index sobre `COMISIONES_MOVIMIENTOS_RECAUDOS(compro_nro)`; habia `Clustered Index Scan` sobre `COMISIONES_MOVIMIENTOS_RECAUDOS` (~9.865.573 filas) dentro del `NOT EXISTS` contra `REC.compro_nro`.
- Estadisticas vistas en `COMISIONES_MOVIMIENTOS_RECAUDOS`: `last_updated = 2026-06-21`, `modification_counter ~= 645.087`. Volumen de cabeceras no explicaba por si solo la demora: 07/2026 = 180.533, 06/2026 = 179.410, 07/2025 = 171.056. Comprobantes financieros 07/2026 altos: tipo A 170.793, tipo E 10.301.
- Hipotesis comunicada: no parece dato puntual raro ni lockeo; mas probable es volumen acumulado + estadisticas desactualizadas + falta de indice por `compro_nro`, causando mala estimacion y scans repetidos en el anti-join. Recomendacion para fuera de la corrida activa: `UPDATE STATISTICS COMISIONES_MOVIMIENTOS_RECAUDOS` y evaluar el indice `(compro_nro, id_cabecera)`.

## 2026-08-06 15:57 ART - Savina / Afilmed ProntoPago y portal de pagos

- Usuario Savina consulta sobre cliente Coomeva y flujo de ProntoPago desde Afilmed hacia portal propio de pagos. Contexto funcional aclarado: hoy Afilmed genera ProntoPago, luego FC, luego Facturacion Electronica, y alguien envia la FC al cliente. Nuevo pedido: cliente Coomeva entraria a un portal propio para visualizar/pagar lo generado por Afilmed.
- Criterio funcional comunicado: Afilmed debe seguir siendo responsable de generar la deuda/ProntoPago/FC; el portal deberia visualizar o recibir esa deuda y devolver estado de pago a Afilmed. No conviene plantear que el portal genere el ProntoPago.
- En repo SVN trunk `svarela-Coomeva-trunk`, se analizo proceso de link/pagos electronicos. Procedimiento `backend/procedures/afs_link_pagos.sql` recibe `@prm_docu_nro` y devuelve comprobantes/deudas vigentes y pagables para un documento: tipo, numero unico, emision, vencimiento, programa, contrato/subcuenta, total liquidado y saldo.
- `afs_link_pagos.sql` valida existencia del documento como contratante/responsable/cuenta/primera cuota; si no existe devuelve error "El contratante no existe o esta retirado". Distingue colectivo por `CUENTAS.docu_nro` e individual por `codigo_unico_persona`. Filtra comprobantes no reversados, vencimiento vigente, saldo > 0, debito/credito positivo, tipos `DA`, `DS`, `FC`, `EC`, `AP`, `RD`; en individual tambien contempla `PP` agrupado.
- No se encontro pantalla PowerBuilder que invoque `afs_link_pagos`; parece procedimiento backend consumible por portal/servicio externo. En facturacion electronica se embebe link `https://oficinavirtualmp.coomeva.com.co/Oficina-virtual/#/payment/credentials?numid=` concatenado con documento del contratante, en `backend/procedures/afu_fact_elect_actualiza_contenido.sql` asignado a `ENC_18`.
- Entrada de pagos a Afilmed: `backend/procedures/afi_pe_compro_importacion.sql` inserta respuesta en `PE_COMPRO_IMPORTACION` con sitio, fecha, moneda, valor, comprobante/documento, forma de pago, autorizacion, transaccion, referencia y CUS. Batch `afu_procesa_pagos_electronicos` procesa pendientes y genera lotes en `COMPRO_IMPOR_LOTES` / `COMPRO_IMPORTACION`; invocado desde `exe_afilmed/src/wp_main_super_batch.srw` como `IMPORTACION_RECAUDOS_PE`.
- Conclusion tecnica comunicada: flujo actual parece ser pull/consulta por portal: Afilmed factura e incluye link; portal consulta deuda por documento probablemente via `afs_link_pagos`; al pagar, portal informa a Afilmed via `afi_pe_compro_importacion`; batch procesa recaudacion. No se vio evidencia de push activo de ProntoPago desde Afilmed al portal.
- Pregunta puntual resuelta: si un contratante tiene 2 contratos en 2 sucursales comercializables distintas, el trunk deberia generar 2 PP, no uno. Razon: `afi_fac_compro_a_generar.sql` agrupa PP individual por contratante pero incluye `suc_comercializable` en el `GROUP BY` junto con cuenta, id_contratante, subcta_tipo, forma de pago y otros datos. Misma sucursal podria agrupar en 1 PP; distinta sucursal separa en 2 PP.

## 2026-08-06 17:18 ART - Savina / V104 solicitudes automaticas traslado contrato nuevo

- Savina pidio actualizar rama/perfil SVN `svarela-Version104-Afilmed-SolicitudesAutomaticas` con ultimos cambios de V104/desarrollo de Sebastian para probar XML de solicitudes automaticas. Working copy en `/home/luispicone/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas` quedo en revision SVN `37540`; externals actualizados tambien a r37540. Branch ya contenia ultimo cambio propio de Sebastian `srmontenegro@INFOMEDICAL`, rev `37530`. `svn status` final limpio salvo externos `X`.
- Para `soli_moti="47"` en V104 actualizada, el XML de traslado a contrato nuevo debe informar solo `fecha_inicio_servicio`; no deben enviarse `fecha_aplicacion` ni `fecha_radicacion`. El wrapper `afi_insertar_soli_auto` calcula `fecha_aplicacion` desde `fecha_inicio_servicio` y usa `GETDATE()` para radicacion. Tambien conviene omitir `ingre_fecha` del integrante para que se complete automaticamente.
- En pruebas de `emi_modo` y `emi_grupo`, se determino que para validar autocompletado no se deben enviar esos atributos en `<facturacion>`. Si se mandan vacios (`emi_modo=""` / `emi_grupo=""`) el parser intenta convertirlos a `TINYINT`. Facturacion minima usada: `<facturacion vto_grupo="2" pago_forma="E" />`; banco, sucursal, tipo cuenta, cuenta, tarjeta, titular y vencimiento no son obligatorios para ese XML. `pago_forma` puede defaultar a `E`, pero se dejo explicitado para prueba.
- Error observado con XML inicial: `No existe afiliado origen vigente para uno o mas integrantes del traslado a contrato nuevo` en `afi_insertar_soli_auto` linea 108. Causa funcional: `soli_moti="47"` no es alta totalmente nueva; es traslado a contrato nuevo y exige afiliado origen vigente en `AFILIADOS` mediante `anter_prepa`, `anter_contra`, `anter_inte`, `docu_tipo`, `docu_nro`. Datos totalmente nuevos siempre fallan en ese flujo.
- Caso validado en base `InstallQA_89` con `BEGIN TRAN / ROLLBACK`: XML con `id_soli_externa="293631201"`, responsable/integrante `CC 8103024`, `ALVAREZ CASTAÑO JUAN DAVID`, origen `anter_prepa="1"`, `anter_contra="1000048"`, `anter_inte="03"`, paso correctamente y devolvio `id_proceso = 177`; rollback aplicado, no se grabo. Validaciones: afiliado origen vigente, sin baja, no usado en `SOLI_AUTO_INTEGRANTES`, id externa libre.
- Ese XML generado luego creo contrato `1073920` cuando Savina lo ejecuto. Se verifico en `InstallQA_89` que el contrato `1073920` quedo con `cod_camp_comercial = 134` (`COMBO 2 PLUS - DENTAL FAMILIAR`).
- Sobre seleccion de campania en ventana manual T. Contrato Nuevo: en `wp_alta_total.srw`, cuando `ii_soli_moti = 47`, los controles de campania se ocultan (`campania_t`, `cb_campania`, `camp_comercial_deno`, `cod_camp_comercial`, `cb_validar_campania`). Conclusion comunicada: en T. Contrato Nuevo manual no se selecciona campania desde la ventana; en flujo automatico/XML si se puede informar con atributo `cod_camp_comercial="134"` y se impacta en contrato.

## 2026-08-06 17:45 ART - Sebastian / Redmine 29362 y 29363 solicitudes automaticas

- Usuario Sebastian trabaja sobre Redmine `29362` y `29363` en Afilmed/Solicitudes Automaticas, usando SQL Server `InstallQA_89` y rama SVN de V104/desarrollo. Prefiere respuesta tecnica directa y ejemplos SQL/XML listos para ejecutar.
- Redmine `29362`: traslado con anexo, `soli_moti="48"`. En XML de ejemplos no debe enviarse `fecha_aplicacion`; usar `fecha_inicio_servicio` y `asesor_gestion="20"` porque el asesor `1234` esta dado de baja. El asesor `20` valido: `Aprobado = 1`, `baja_fecha = NULL`, razon social `PREPAGADA COOMEVA MEDICINA`.
- Para `29362`, se corrigio validacion faltante de BB Gestante/BB Nacido en `backend/procedures/gmsdef_val_traslado_web.sql` y se commiteo en SVN `r37530` con mensaje `29362: ajustar validacion BB Gestante traslado web`. Motivo: el fix previo en `gmsdef_val_datos_ident_inte` evitaba `8079`, pero el batch tambien ejecutaba `gmsdef_val_traslado_web`, que seguia rechazando con `WHEN PAR.bb_gestante = 'S' THEN 8079`.
- Para probar `29362` con BG que impacte, no usar origen `1 / 1004685 / 03` con `docu_tipo="BG"`, `docu_nro="10473972601"`: nace `2025-08-06`, edad actual `1`, y `DOCU_TIPOS.BG` permite edad `0-0`, por eso da rechazo `8222`. Candidato valido comunicado: `docu_tipo="BG"`, `docu_nro="10142660891"`, `anter_prepa="1"`, `anter_contra="828612"`, `anter_inte="03"`, `paren_destino="0"`; nace `2025-08-25`, edad actual `0`, vigente, sin duplicado en destino `244512`.
- Proceso reportado por Sebastian como `179` con Id externo `1024` mostro solo `8222` para destino `244512`, inte `05`; se explico que el arreglo de `8079` estaba funcionando y el error restante era dato de prueba por BG con edad `1`.
- Redmine `29363`: traslado automatico a contrato nuevo, `soli_moti="47"`. Contexto: XML APP -> `SOLI_AUTO_*` -> GMS/GMSDEF -> `SOLICITUDES` -> impacto como alta total -> crea contrato destino definitivo -> retiro/baja de integrante origen -> AUFACT cuando corresponde. Implementacion base en `r37128`; fixes ya registrados en `r37442`, `r37444`, `r37490`, `r37491`, `r37494`, `r37510`.
- Para `29363`, el XML no debe informar `fecha_radicacion` ni `fecha_aplicacion`; solo `fecha_inicio_servicio`. Caso 13 requiere telefono de integrante `tipo_reg = I` para no caer en `8066`. Para validar autocompletado de facturacion no enviar `emi_modo` ni `emi_grupo`, ni siquiera vacios.
- Analisis solicitado por Sebastian para casos `18` y `19` de `29363`: ambos son arreglos, no meras aclaraciones.
- Caso 18 de `29363`: parte ya cubierta por parser de `47` cuando la cuenta es colectiva. `afi_insertar_soli_auto_traslado_nuevo` detecta `es_colectivo = S`, toma `vto_grupo`, `emi_modo`, `emi_grupo` desde `SUBCTA_CONTRATO`, ignora domicilio/email `tipo_reg=C` del XML e inserta domicilio desde `SUBCTA_DOMICILIOS`. Para `cuenta=56 / subcta=2242`, se valido que es colectiva y tiene configuracion vigente `vto_grupo=2`, `emi_modo=0`, `emi_grupo=2`, `factu=E`, `factu_def=E`.
- Arreglo pendiente caso 18: `afs_soli_auto_obligatorios` corre antes del parser y hoy exige `domicilio tipo_reg="C"` y `email tipo_reg="C"` en el XML, lo cual contradice colectiva porque esos datos deben salir de configuracion. Tambien conviene reforzar individual: para cuenta individual el XML debe traer datos de facturacion requeridos, en especial corte/modalidad, y el sistema calcula `emi_grupo`.
- Caso 19 de `29363`: arreglo claro. El parser actual lee `cod_camp_comercial` desde `/solicitud/@cod_camp_comercial` y desde cada integrante; `afu_genera_soli_auto_app` lo pasa a GMS y se impacta en contrato. Evidencia en `InstallQA_89`: `id_soli_externa=293631201` termino con `SOLI_AUTO_CONTRATO.cod_camp_comercial = 134`, `SOLI_AFILIADOS.cod_camp_comercial = 134`, y `AFI_CAMPANIAS` para contrato `1 / 1073920` con campania `134`.
- Arreglos pendientes caso 19: para `soli_moti=47`, rechazar XML si viene `/solicitud/@cod_camp_comercial` o si algun `/solicitud/integrantes/integrante/@cod_camp_comercial`; como blindaje adicional, no propagar campania para `soli_moti=47` en generacion/impacto, porque la ventana manual de T. Contrato Nuevo no permite campanias.

## 2026-08-06 18:07 ART - Savina / V104 edad minima y Datos adicionales

- Savina esta probando V104 `svarela-Version104-Afilmed-SolicitudesAutomaticas` en `InstallQA_89` para `soli_moti=47` traslado a contrato nuevo, con foco en validacion de edad minima cuando plan origen y destino son iguales.
- Caso historico `id_proceso=156`: habia generado errores de edad (`8086` edad del beneficiario no aplica para programa y `8142` no cumple edad minima/tope del programa principal) en condicion clave plan origen/destino iguales `OROF / FOACON`.
- Se armo y probo con `ROLLBACK` XML equivalente usando `id_soli_externa=5025`, origen `1 / 244512 / 02`, documento `CC 42753948`, integrante `MEJIA HENAO CLARA ROSA`, plan destino `OROF`, tarifa `FOACON`, asesor `106`, sucursal `11001`. Resultado de prueba: `id_proceso=179`, `cab_id=295865`, `canti_errores=0`, `puede_procesarse=S`, sin `GMS_ERR`, rollback OK.
- Luego Savina ejecuto/genero contrato con ese caso; al revisar `id_proceso=179` se vio que genero contrato destino `1073921`, solicitud `4677152`, `GMS_CAB=295867`, estado GMS `B`, errores `0`, pero sin responsable pagador en todo el flujo: sin registros en `SOLI_AUTO_RESP_PAGADOR`, `GMSTXT_RESP_PAGADOR`, `GMSDEF_RESP_PAGADOR`, `SOLI_RESP_PAGADOR` ni `AFI_RESP_PAGADOR` para contrato `1073921`.
- Causa comunicada: el XML usado para `id_proceso=179` no traia nodo `<responsable_pagador>`, por eso no carga la solapa/pestania Datos adicionales. Savina indico que el responsable pagador debe ser obligatorio porque sin eso no se carga Datos adicionales.
- El origen usado en `id_proceso=179` ya quedo retirado: origen `1 / 244512 / 02`, `baja_fecha=2026-07-01`, `realbaja_fecha=2026-06-30`; no reutilizarlo.
- Se probo en rollback otro caso equivalente con responsable pagador incluido: origen `1 / 244526 / 02`, documento `CC 88308248`, titular/responsable `CHOCONTA VARGAS LUIS ALVEIRO`, plan origen/destino `OROF / FOACON`. Resultado rollback: `id_proceso=180`, `cab_id=295868`, errores `0`, `puede_procesarse=S`, con `GMSTXT_RESP_PAGADOR=1` y `GMSDEF_RESP_PAGADOR=1`.
- Bloque responsable pagador valido para ese caso: `docu_tipo="CC" docu_nro="88308248" ape="CHOCONTA" ape2="VARGAS" nombre="LUIS" nombre2="ALVEIRO" naci_fecha="1977-10-24" sexo="M" nacio="CO" civil_esta="C" estrato_social="0" profesion="0" ocupacion="0" cargo="0" rango_salarial="0" red_coomeva="N"`; debe ir antes de `<integrantes>`.
- Ultima pregunta de Savina antes de flush: quiere saber donde se graban los datos de la solapa Datos adicionales de la ventana Traslados a Contrato existente, porque responsable pagador deberia ser obligatorio. La investigacion estaba en curso; no se habia entregado respuesta final todavia.

## 2026-08-06 18:33 ART - Sebastian / Redmine 29362 y 29363 solicitudes automaticas

- Usuario Sebastian trabaja en rama SVN `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, base habitual `InstallQA_89`, sobre solicitudes automaticas Afilmed.
- Redmine 29362 (`soli_moti=48`, traslado con anexo): se detecto que ejemplo con `asesor_gestion=1234` fallaba porque asesor estaba dado de baja; asesor valido usado: `20` (`Aprobado=1`, `baja_fecha=NULL`). Para proximos XML del 29362 no incluir `fecha_aplicacion`; solo `fecha_inicio_servicio`.
- Redmine 29362 caso 9: fix inicial de BB Gestante estaba incompleto porque `gmsdef_val_traslado_web` seguia rechazando `8079` con regla simple `WHEN PAR.bb_gestante = 'S'`. Se corrigio y commiteo `r37530 - 29362: ajustar validacion BB Gestante traslado web` en `backend/procedures/gmsdef_val_traslado_web.sql`.
- Para pruebas BG del 29362, evitar origen `1/1004685/03` con documento `BG 10473972601` porque nacio `2025-08-06`, edad actual `1`, y `DOCU_TIPOS.BG` permite edad `0-0`; por eso rechazo correcto `8222`. Candidato BG valido usado: `docu_tipo="BG" docu_nro="10142660891" anter_prepa="1" anter_contra="828612" anter_inte="03" paren_destino="0"`, naci_fecha `2025-08-25`, edad actual `0`.
- Redmine 29363 contexto: traslado automatico a contrato nuevo, `soli_moti=47`. XML APP -> `SOLI_AUTO_*` -> GMS/GMSDEF -> `SOLICITUDES` -> impacto como alta total -> creacion contrato destino definitivo -> retiro/baja origen -> AUFACT si corresponde. No enviar `fecha_radicacion` ni `fecha_aplicacion`; wrapper calcula aplicacion desde `fecha_inicio_servicio` y radicacion con `GETDATE()`.
- Redmine 29363 commits previos relevantes: `r37128` base traslado nuevo; `r37442`/`r37444` fecha radicacion; `r37490` caso 12 facturacion; `r37491`/`r37494` casos 9/10/11; `r37510` casos 13/16/17.
- Redmine 29363 caso 18 y 19: se corrigieron y commitearon en `r37542 - 29363: ajustar casos 18 y 19 traslado nuevo`. Archivos: `backend/procedures/afs_soli_auto_obligatorios.sql`, `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql`, `backend/procedures/afu_genera_soli_auto_app.sql`, `backend/procedures/gms_genera_solicitudes.sql`.
- Caso 18: para contrato destino colectivo, `tipo_reg="C"` de domicilio/email y datos de facturacion ya no son obligatorios desde XML; se toman de `SUBCTA_CONTRATO` / `SUBCTA_DOMICILIOS`. Para individual, si falta `emi_modo`, nuevo rechazo esperado: `El XML no informa emi_modo`.
- Caso 19: para `soli_moti="47"` se rechaza cualquier `cod_camp_comercial`, en cabecera o integrante, con mensaje esperado `El XML no debe informar campania comercial para traslado a contrato nuevo`; ademas se blindo GMS para no validar/impactar campanias en motivo 47. La ventana manual T. Contrato Nuevo no permite campaña.
- Luego una compilacion fallo en `afs_soli_auto_obligatorios`, linea 589: `Incorrect syntax near the keyword 'BEGIN'`. Causa: faltaba un parentesis en `IF EXISTS` de emails. Corregido y commiteado `r37544 - 29363: corregir sintaxis obligatorios traslado nuevo`. Diff de una linea, SVN limpio salvo externos `X`.
- Redmine 29363 caso 20 analizado antes de compaction: es arreglo, no explicacion. XML incompleto sin `<responsable_pagador>` fue aceptado y dejo solicitud con solapa/datos vacios. Evidencia: `afs_soli_auto_obligatorios.sql` exige `<responsable_pagador>` solo para `soli_moti=22`, no para `47`; `afi_insertar_soli_auto_traslado_nuevo.sql` no inserta en `SOLI_AUTO_RESP_PAGADOR` si falta el nodo; `afu_genera_soli_auto_app.sql` arma responsable para `22` o `7`, no para `47`; `gms_genera_solicitudes.sql` si contempla responsable para `47`. Proceso `179` en QA tenia `SOLI_AUTO_RESP_PAGADOR=0` y `GMSDEF_RESP_PAGADOR=0`. Correccion pendiente: validar entrada de `soli_moti=47`, al menos rechazando cuando falte `<responsable_pagador>` en traslado a contrato nuevo individual, y revisar obligatorios de contactos `tipo_reg="R"` o derivacion desde Registro Unico.
