# Memoria curada diaria

- Fecha: 2026-07-27
- Usuario: Luis Picone
- id_googlechat: users/103530042020124588192
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/pjjhtyaaaae

## Resumen de la actividad

### Tema 1: Preferencias personales cargadas

- Solicitud o contexto: Luis inició conversación y luego preguntó qué indicaban sus preferencias.
- Respuesta o acciones del agente: Se ejecutó el bootstrap operativo, se localizaron sus preferencias desde `USUARIOS.md` y se leyó `/mnt/Agentes/lpicone/preferencias/preferencias.md`.
- Resultado: Se informó que para Redmine debe usarse la credencial `lpicone`, que `guardalo en mi carpeta` apunta a `/mnt/Agentes/lpicone`, y que `tareas` se interpreta como `tareas_nemesys` del repo Git salvo indicación distinta.
- Estado: Informativo

### Tema 2: Menús de pantallas PowerBuilder en `bmora-Coomeva-trunk`

- Solicitud o contexto: Luis pidió identificar desde qué menú se abre `wp_alta_total`. Como había varios repositorios registrados, se pidió confirmación de working copy; luego eligió `bmora-Coomeva-trunk`.
- Respuesta o acciones del agente: Se listaron los repositorios registrados y se buscó `wp_alta_total` en `/home/luispicone/.openclaw/workspace/svn/bmora-Coomeva-trunk`, revisando `exe_afilmed/src/m_afilia.srm`, `wp_abm_solicitudes.srw` y `wp_consulta_afiliados.srw`.
- Resultado: Se informó que `wp_alta_total` se abre directamente desde `Solicitudes > Venta Nueva` y `Solicitudes > Traslado a Contrato Nuevo`; también indirectamente desde `Solicitudes > Seguimiento de Solicitudes` y `Solicitudes > Consulta General de Usuarios` mediante doble click sobre solicitudes con motivo/tipo `1` o `47`. Se aclaró que `Solicitudes > Procesos Masivos > Venta Nueva Masiva` abre `wp_alta_total_masiva`, no `wp_alta_total`.
- Estado: Resuelto

### Tema 3: Menú del ABM de Asesores

- Solicitud o contexto: Luis pidió identificar desde qué menú se abre el ABM de Asesores, usando el mismo repositorio `bmora-Coomeva-trunk`.
- Respuesta o acciones del agente: Se buscó `asesor` y variantes en `exe_afilmed/src/m_afilia.srm` y en fuentes relacionadas.
- Resultado: Se informó que el ABM de Asesores se abre desde `Configuración > Afiliaciones > Asesores > Asesores`, y que ese ítem abre la ventana `wp_abm_asesores_asesor`.
- Estado: Resuelto
