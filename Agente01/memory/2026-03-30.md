- Proyecto 29361 consolidado en repo `/home/luispicone/.openclaw/workspace/pb2022-coomeva-afilmed` bajo nueva convención documental: `Proyectos/29361/29361.md`.
- El flujo APP queda dividido en dos fases: `afi_insertar_soli_auto` registra interfaz (`SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_ANEXOS`) y `afu_genera_soli_auto_app` traslada pendientes a GMS (`GMS_CAB`, `GMSTXT_CLASE_PLAN`, `GMSTXT_ADICIONALES`).
- `afu_genera_soli_auto_app` ya pasa `@prm_id_soli_externa` a `gms_cab_in` y además ahora debe pasar `@prm_origen_solicitud = 'APP'`.
- Se definió para 29361 el nuevo campo funcional `origen_solicitud` con formato `CHAR(3)` y default `AFI`.
- El valor específico para el flujo APP/autogestión es `APP`.
- `gms_cab_in` debe aceptar `@prm_origen_solicitud CHAR(3) = 'AFI'` para no romper llamadas existentes y persistirlo en `GMS_CAB.origen_solicitud`.
- `GMS_CAB` incorpora `origen_solicitud CHAR(3) NOT NULL DEFAULT 'AFI'`.
- `gms_genera_solicitudes` debe leer `GMS_CAB.id_soli_externa` y `GMS_CAB.origen_solicitud` para pasarlos a `afu_solicitudes`.
- `afu_solicitudes` debe aceptar `@origen_solicitud CHAR(3) = 'AFI'` y persistirlo en `SOLICITUDES.origen_solicitud`.
- `SOLICITUDES` incorpora `origen_solicitud CHAR(3) NOT NULL DEFAULT 'AFI'`.
- Resultado esperado del circuito completo 29361: la solicitud generada desde APP termina en `SOLICITUDES` con `nro_formulario = id_soli_externa` y `origen_solicitud = 'APP'`.
- El documento funcional `Proyectos/29361/29361.md` debe mantenerse sin sección de histórico de cambios y describiendo directamente el estado vigente del proceso.
