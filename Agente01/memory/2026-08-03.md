
## 2026-08-03 12:42 ART - Pre-compaction memory flush

- Usuario: Sebastian (Google Chat directo). Prefiere comunicacion tecnica directa y acciones concretas.
- Trabajo SVN realizado en repo con procedimientos backend:
  - Commit `r37490`: `Redmine 29363 ajusta facturacion SOLI_AUTO caso 12`.
    - Archivos: `backend/procedures/gmstxt_ponerdatosdefault.sql`, `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql`.
    - Cambio conceptual: para APP / SOLI_AUTO / `soli_moti = 47`, `gmstxt_ponerdatosdefault` conserva valores informados en `GMSTXT_MULTI` para facturacion (`emi_modo`, `emi_grupo`, `vto_grupo`) y solo usa default de `SUBCTA_CONTRATO` si el dato viene nulo/comodin. En `afi_insertar_soli_auto_traslado_nuevo` se valida coherencia temprana `fecha_inicio_servicio` / `fecha_aplicacion` / `vto_grupo` (ej.: `vto_grupo=2 -> 2026-07-01`, `vto_grupo=3 -> 2026-07-06`). No se pudo compilar por falta de `sqlcmd`; se valido diff, SVN limpio, ASCII y CRLF.
  - Commit `r37491`: arreglo de numeracion de integrante al trasladar a contrato existente.
    - Archivo: `backend/procedures/afu_genera_soli_auto_app.sql`.
    - Cambio conceptual: en `soli_moti = 48`, `@var_int_base_inte` ahora calcula `MAX(inte)` historico del contrato destino sin filtrar por `baja_fecha`; si existe `inte 2` dado de baja, el nuevo trasladado entra como `inte 3`. Se valido diff minimo, SVN limpio, ASCII/CRLF, sin `;` ni `RTRIM`.
- Documento `caso_9_10_11`: estado informado al usuario:
  - Caso 10 solucionado por `r37491`.
  - Caso 9 pendiente: el batch valida tambien el `inte 01` titular existente del contrato destino, porque `afu_genera_soli_auto_app.sql` inserta en `GMSTXT_INTEGRANTES` al titular vigente usando `dbo.afn_inte_tit(...)`; luego `gmsdef_val_datos_ident_inte.sql` dispara validacion de edad/plan `8086` sobre un integrante que no vino en el XML. Cambio propuesto: para APP/SOLI_AUTO/48 quitar la insercion del titular destino en `GMSTXT_INTEGRANTES`; el titular debe ser solo contexto desde `AFILIADOS`, no fila de novedad. No tocar validacion global `8086`.
  - Caso 11 pendiente: hoy `afu_genera_soli_auto_app.sql` genera anexos solo desde `SOLI_AUTO_ANEXOS`. Cambio propuesto: antes del cursor `cur_anexos`, insertar automaticamente anexos obligatorios para `soli_moti = 48` segun configuracion `SUBCTA_PLAN_ADI` / `SUBCTA_PLAN_ADI_PAREN`, usando cuenta/subcta/plan del contrato destino, `paren_destino` del integrante trasladado, `obliga = 'S'`, evitando duplicar anexos ya enviados por XML en `SOLI_AUTO_ANEXOS`; luego el cursor actual seguiria generando `GMSTXT_ADICIONALES`.
