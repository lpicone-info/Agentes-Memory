
## 2026-08-26 12:19 ART - Flush pre-compaction

- Usuario en Google Chat directo: Gabriel.
- SVN `gpulka-Coomeva-trunk`: se corrigio Redmine 30271 en `backend/procedures/afi_procesa_comisiones_valores_complementarios.sql`, commit `r37667 | gpulka@INFOMEDICAL | Redmine 30271 - Ajusta mora por facturacion empresa`. Cambio clave: descartar hipotesis inicial de `NULL = NULL` por `contra`; separar matching por tipo funcional. Afiliado: `factu_destino='A'`, `aux_tipo='C'`, `prepaga + contra`. Empresa/subcuenta: `factu_destino='E'`, `aux_tipo='S'`, `cuenta + subcta`, sin usar `contra`. Tambien se cambio calculo historico para usar `CMC.fecha_hasta` y filtrar `C.fecha_vto <= CMC.fecha_hasta`. No hubo validacion contra SQL Server por falta de `sqlcmd`/`tsql`/`sqsh` en entorno local.
- Despues del commit Redmine 30271, Gabriel ejecuto en `PruebasCoreINFOMEDICAL` una transaccion con `exec afi_procesa_comisiones_valores_complementarios '20260601'` sobre cuenta `3263` y pregunto si `dias_en_mora` no deberia quedar distinto de NULL. Se empezo a revisar, pero la conversacion derivo a un resumen; dejar como punto posible de seguimiento si vuelve a consultarlo.
- SVN `gpulka-Version104-Afilmed-SolicitudesAutomaticas`: working copy actualizada a `r37670` sin conflictos. Quedaron no versionados previos en `Redmines/29361` y `Redmines/29362`; externos SVN marcados como `X`.
- En `gpulka-Version104-Afilmed-SolicitudesAutomaticas` se corrigio y comiteo Redmine 29365: `r37672 | gpulka@INFOMEDICAL | Redmine 29365 - Ajusta obligatorios RP SOLI_AUTO`. Archivos incluidos: `afs_soli_auto_obligatorios.sql`, `afi_insertar_soli_auto_cambio_resp_pagador.sql`, `w_clase_soli.srw`.
- Detalle Redmine 29365: para `soli_moti = 22`, tag `<parentescos>` obligatorio y cada `<parentesco>` completo (`inte`, `paren_actual`, `paren_real_actual`, `paren_nuevo`); `moti_ret_resp_pag` obligatorio cuando `finalizar_resp_pagador='S'` o `baja_resp_pagador='S'`, numerico positivo, solo permitido en tag `<solicitud>` y rechazado dentro de `<responsable_pagador>`; `profesion`, `ocupacion`, `cargo` obligatorios si no se completan desde Registro Unico.
- Detalle UI Redmine 29365: en `w_clase_soli.srw`, no buscar circuito vigente si la solicitud ya esta impactada (`soli_estado = 99`), evitando que la solicitud `4677201` en `InstallQA_89` muestre mensaje de circuito inexistente y fuerce radio en rechazada al abrir.
- Validaciones Redmine 29365: `PARSEONLY OK` en `InstallQA_89` para ambos SQL; se verifico que solicitud `4677201` tenia `SOLI_AUTO_PARENTESCOS` vacio y `profesion/ocupacion/cargo` vacios tanto en intermedia como en Registro Unico. Encoding preservado: SQL ASCII CRLF, PowerBuilder UTF-8 BOM CRLF.

## 2026-08-26 16:15 ART - Flush pre-compaction

- Usuario en Google Chat directo: Sebastian. Prefiere trato directo como "Max"/"Maxxuel". Instruccion operativa explicita: cuando Sebastian reporte errores de compilacion SQL, no compilar contra base desde el agente; corregir script local y commitear solo cuando lo pida explicitamente.
- Redmine 30238 / Mantis 11904 / Afilmed Solicitudes: cambios fueron hechos por error en la rama `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas` y Sebastian pidio reversar esa rama incorrecta y subir al trunk bajo usuario `srmontenegro`, no bajo `bmora`.
- Revisiones SVN conocidas del Redmine 30238 en la rama incorrecta: `r37624`, `r37626`, `r37627`, `r37628`, `r37629`, `r37630`, `r37631`, `r37632`, `r37633`, `r37662`, `r37665`. Archivos afectados conocidos: `exe_afilmed/src/wp_alta_parcial.srw` y `exe_afilmed/src/wp_alta_total.srw`.
- Estado pendiente Redmine 30238: antes de ejecutar la reversa se pidio confirmacion explicita del alcance de esas revisiones por ser accion destructiva. Tambien falta obtener/crear working copy de trunk con usuario `srmontenegro`: URL `https://srv-infosvn.infomedical.com/svn/Coomeva/trunk`, carpeta propuesta `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk`. No usar working copies de `bmora`, `gpulka` o `svarela` para este pedido.
- Redmine 30238 contexto funcional: escenarios 5-8 sobre validaciones Venta Nueva/Reactivacion, asesor vigente Plan Regresa o no, check renuncia antiguedad marcado o no. Se habia concluido que `wp_alta_parcial.srw` cubria los escenarios y que `wp_alta_total.srw` necesitaba ajuste equivalente en `wf_fecha_antiguedad` para `renuncia_antiguedad = 'S'`. Ese ajuste se comiteo equivocadamente en rama como `r37665 | Refs #30238 ajusta antiguedad por renuncia en alta total`.
- Redmine 28684 / SARLAFT migracion: archivo local en rama `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`: `Redmines/28684/2 - afu_sarlaft_migracion.sql`.
- Redmine 28684 commits realizados en la rama: `r37666 | Refs #28684 evita TRIM en migracion sarlaft` reemplazo `TRIM(...)` por `LTRIM(...)`, sin `RTRIM` ni `;`; `r37668 | Refs #28684 incluye afiliados no vigentes en migracion sarlaft` quito `AND a.baja_fecha IS NULL` de `#contratos_excel_afiliados`; `r37669 | Refs #28684 actualiza afiliados por documento excel` cambio `#afiliados_update` para salir desde `#excel_rud + #excel_documento + AFILIADOS`, actualizando profesion/ocupacion por documento/CUPS del Excel independiente de decision SARLAFT.
- Redmine 28684 diagnostico informado por Sebastian despues de ejecutar SP: aparecian `INSERTAR AFI_HISTO_PAREN_TI_TI_ULTIMO 1981`, varios `NO_INSERTAR`, `OK`, y `REVISAR_MANUAL SIN_FUENTE_PERSONA 131`; para `codigo_unico_persona = 437290` seguian con `ocupacion/profesion NULL` los contratos `104258/01` y `156589/24` antes de la correccion `r37669`.
