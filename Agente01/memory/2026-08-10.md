# Memoria 2026-08-10

## Redmine 30111 - Auditoria medica en solicitudes automaticas
- Usuario: Sebastian / srmontenegro.
- Contexto recuperado: Redmine `30111` corresponde a "Registrar Auditoria Medica de ingreso en Solicitudes", producto Afilmed, incidente externo `11503`, asignado a Sebastian y en estado En Desarrollo.
- Revision relevante: SVN `r37498`, autor `srmontenegro@INFOMEDICAL`, fecha `2026-08-03 17:15:09 -0300`, mensaje `Redmine 30111 - auditoria medica en solicitudes automaticas`.
- Cambios principales de `r37498`: tablas staging `SOLI_AUTO_AUDITORIA_MEDICA` y `SOLI_AUTO_PREEXISTENCIAS`; campos `SOLI_AUTO_CONTRATO.resultado_auditoria` y `SOLI_AUTO_CONTRATO.soli_recha`; parseo XML de auditoria medica para motivos `6`, `47`, `48`; validaciones en `afs_soli_auto_obligatorios`; helper `afu_soli_auto_app_auditoria_medica`; integracion en `gms_genera_solicitudes`.
- Analisis realizado: con `r37498` compilada/desplegada hay base tecnica para probar. Para resultado auditoria `A`, continua impacto normal. Para resultado `R`, graba seguimiento rechazado y evita impacto AFI si `afu_soli_seguimiento` devuelve estado circuito distinto de `99`; primer caso obligatorio a validar es rechazo sin impacto AFI.
- Prueba solicitada: ejemplo para contrato nuevo / traslado a contrato nuevo, motivo `47`, con auditoria medica aceptada y procesamiento completo.
- Archivo entregado al usuario: `/mnt/Agentes/srmontenegro/redmine_30111_traslado_nuevo_auditoria.sql`.
- Estado final del archivo: Sebastian pidio que no tenga consultas antes/despues; quedo reducido a definicion del XML, XML completo hardcodeado y ejecucion de `dbo.afi_insertar_soli_auto`, simulando el proceso en tiempo real.
- Ajustes del XML de ejemplo: `soli_moti="47"`, auditoria medica `resultado="A"`; no informar `cod_camp_comercial` para motivo `47`; se quitaron `fecha_aplicacion` y `fecha_radicacion` porque el dispatcher actual las rechaza y calcula `fecha_aplicacion` desde `fecha_inicio_servicio`.
- Validaciones del archivo reportadas: ASCII, sin BOM, finales LF, sin `;`, sin `RTRIM`. No se ejecuto contra base porque no se indico profile.
