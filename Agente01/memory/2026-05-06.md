# 2026-05-06

## Redmine 29361 / Traslado a Contrato Nuevo (`soli_moti = 47`)

- Gabriel pidió conservar como memoria persistente el análisis sobre `Traslado a Contrato Nuevo` (`soli_moti = 47`) para reutilizarlo luego también con Sebastián.
- Contexto de trabajo: repo SVN `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`, Redmine `29361`, mecánica APP → tablas de interfaz → GMS → `SOLICITUDES`, y comparación con `wp_alta_total` invocada con parámetro `47`.
- Aclaración importante de Gabriel: nadie planteó `47` como “Cambio de cobertura con otro motivo”; se sabe que hay muchas diferencias funcionales y técnicas que faltan y precisamente deben programarse. El antecedente `29361` solo debe tomarse como canal/técnica de entrada APP e integración GMS, no como equivalencia funcional.
- Conclusión técnica: `soli_moti = 47` requiere un flujo específico de Traslado a Contrato Nuevo sobre la mecánica de 29361, con programación nueva en varias capas.
- Diferencias obligatorias identificadas para `47`: ampliar XML, ampliar o crear tablas de interfaz, generar integrantes completos, persistir datos de contrato anterior por integrante, manejar responsable pagador, facturación/formas de pago, domicilios/contactos si el flujo lo requiere, poblar estructuras GMS adicionales y adaptar `gms_genera_solicitudes` para procesar `47`.
- Evidencia de `wp_alta_total`: el parámetro `Message.StringParm` define `ii_soli_moti`; con `47`, la ventana graba cabecera en `SOLICITUDES`, integrantes en `SOLI_AFILIADOS`, clase/planes, formas de pago, titulares, domicilios/contactos, anexos y completa auditoría/preexistencias para traslado.
- Campos críticos observados en `wp_alta_total` para traslado: por integrante se usan `anter_prepa`, `anter_contra`, `anter_inte`, `anter_plan`; también se validan datos previos contra `wf_valida_traslado()` y `f_alta_valida_traslado_aceptada`.
- Estado de las tablas de Redmine 29361: `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_ANEXOS` son suficientes para Cambio de cobertura/anexos, pero no para `47`. `SOLI_AUTO_INTEGRANTES` solo guarda `id_proceso`, `prepaga`, `contra`, `inte`, por lo que no alcanza para generar `SOLI_AFILIADOS` de traslado.
- Desfase detectado: `afu_genera_soli_auto_app.sql` espera leer `id_soli_externa` y `soli_moti` desde `SOLI_AUTO_CONTRATO`, pero el script de tabla inspeccionado no los define todavía.
- Punto GMS crítico: `gms_genera_solicitudes` no procesa actualmente `soli_moti = 47` en la lógica ejecutable analizada. Además, las estructuras `GMSTXT_INTEGRANTES`/`GMSDEF_INTEGRANTES` vistas solo contemplan `anter_prepa`, no el set completo `anter_contra`, `anter_inte`, `anter_plan` requerido para traslado.
- Recomendación persistida: diseñar `47` como flujo propio, reutilizando la arquitectura técnica de 29361 solo como patrón de ingreso automático APP y pipeline GMS. No resolver agregando ciegamente `47` a ramas existentes de `gms_genera_solicitudes` sin mapear todas las tablas finales que `wp_alta_total` genera.

## Redmine 29362 / Traslado a Contrato Existente (`soli_moti = 48`)

- SebastiánRMontenegro pidió cerrar el día dejando persistido el estado del Redmine `29362` para continuar mañana y para que la sesión de Gabi pueda tomar el puente automático.
- Repo SVN trabajado para Sebastián: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Objetivo de `29362`: extender el flujo de solicitudes automáticas de `29361` para `SOLI_MOTI = 48` (`Traslado a Contrato Existente`), tomando como referencia `wp_alta_parcial` abierta con parámetro `48`.
- Se encontró documentación de Sebastián en SVN: `Redmines/29362/HANDOFF_GABI.md` y `Redmines/29362/29362.md`.
- Se había creado y commiteado inicialmente `Redmines/29362/29362.md` en SVN r36805 con análisis técnico-funcional de Mantis 11504.
- Luego se agregó y commiteó handoff específico para Gabi en SVN r36808: `Redmines/29362/HANDOFF_GABI.md`, junto con ampliación de `Redmines/29362/29362.md`. Commit: `Redmine 29362 - Handoff datos traslado automatico`.
- Conclusión central para `29362`: el XML/interfaz actual de `29361` no alcanza completo para `wp_alta_parcial` con `soli_moti = 48`. Sirve como base para cabecera, cobertura/contrato destino, anexos y trazabilidad `APP`, pero falta información de traslado por integrante y auditoría.
- Criterio acordado para el puente de Gabi: no parametrizar todo. Recibir parametrizado lo que define la intención funcional y buscar en tablas lo derivable por Afilmed.
- Para `48`, deben recibirse parametrizados al menos: `id_soli_externa`, `soli_moti = 48`, `prepaga`, contrato destino (`contra` o `contra_destino`), `fecha_aplicacion`/`fecha_inicio_servicio`, estado final aprobado/negado, `requiere_auditoria`; por integrante: `anter_prepa`, `anter_contra`, `anter_inte`, `inte_destino` o confirmar que `inte` es destino, `paren_destino`, `paren_real_destino`; si hay auditoría: `estado_auditoria`, `nivel_riesgo`, `observacion_auditoria`, y eventualmente indicador de no generar carta de negación.
- Para `48`, pueden derivarse desde tablas: datos personales/documento/sexo/fecha nacimiento/edad desde `AFILIADOS`, parentesco origen, `anter_plan_codi` desde `AFI_PLANES` vigente a `fecha_aplicacion`, preexistencias desde `AFI_PREEXISTENCIAS`, FUM/fecha probable de parto desde origen, línea de negocio, grupo de plan destino, anexos obligatorios y datos para `afs_val_traslado_aceptada`.
- Recomendación técnica: `anter_plan_codi` no necesita venir en XML si `anter_prepa + anter_contra + anter_inte` identifican inequívocamente el origen; si viene, usarlo solo como control y validarlo contra base.
- Extensión mínima sugerida de `SOLI_AUTO_CONTRATO`: confirmar semántica de `contra` como destino o agregar `contra_destino`, agregar/usar `fecha_inicio_servicio`, `estado_traslado`, `requiere_auditoria`, `usuario_generico` si no sale de configuración.
- Extensión mínima sugerida de `SOLI_AUTO_INTEGRANTES`: `inte_destino` o confirmar semántica de `inte`, `anter_prepa`, `anter_contra`, `anter_inte`, `paren_destino`, `paren_real_destino`, `requiere_auditoria` si aplica por integrante, `estado_auditoria`, `nivel_riesgo`, `observacion_auditoria`, `copia_preexistencias` si se necesita registrar/forzar decisión.
- `SOLI_AUTO_ANEXOS` actual alcanza para anexos informados si `inte` representa integrante destino; los anexos obligatorios deben resolverse automáticamente.
- Objetos revisados clave para `29362`: `exe_afilmed/src/wp_alta_parcial.srw`, `exe_afilmed/src/d_soli_afiliados_edi_ap.srd`, `backend/procedures/afu_genera_soli_auto_app.sql`, `backend/procedures/gms_genera_solicitudes.sql`, `backend/procedures/afu_completa_datos_audit_para_traslado.sql`, `backend/procedures/afs_busca_ult_contra_misma_linea.sql`, `backend/procedures/afs_val_traslado_aceptada.sql`.
- Integración recomendada para mañana/Gabi: ampliar XML de `afi_insertar_soli_auto`, persistir cabecera/integrantes/anexos en `SOLI_AUTO_*` ampliadas, hacer que `afu_genera_soli_auto_app` para `soli_moti = 48` genere también datos de integrantes hacia GMS/SOLI equivalentes y no solo clase plan/anexos, y luego de generar `SOLICITUDES` ejecutar proceso específico de traslado automático para auditoría/preexistencias, aceptar/rechazar, impactar y facturar según Mantis 11504.

## DB / `cups_2024` / importación COBIS

- Se conectó correctamente a `cups_2024` usando el mismo servidor del perfil `PruebasCoreINFOMEDICAL`: `PR-CDPWIN36\PRCDPSQL11`. No guardar ni exponer credenciales.
- Se reprodujo el error `Error converting data type int to smallint.` ejecutando con transacción y rollback: `EXEC dbo.afs_cobis_importacion_archivo_saldos_cobis 'SaldosAsociados_2026-04-30 09-13-18.txt'`.
- La causa del error COBIS fue identificada en el encabezado del archivo: `ERROR_85297`; ese valor se parsea hacia `@var_cant_error = 85297`, que excede el máximo de `SMALLINT` (`32767`).
- Fix requerido: cambiar `dbo.ASOCIADOS_LOTES_ESTADOS.cant_registro_error` de `SMALLINT` a `INTEGER` y también cambiar el parámetro `@prm_cant_registro_error` de `dbo.afi_asociados_lotes_estados` de `SMALLINT` a `INTEGER`.
- Cambiar solo el SP/parámetro no alcanza si la columna de tabla sigue siendo `SMALLINT`.

## DB / `InstallQA_89` / solicitud `4676684` / anexos obligatorios

- Investigación en `InstallQA_89` sobre solicitud `4676684`: `SOLICITUDES.soli=4676684`, `soli_moti=5`, `prepaga=1`, `contra=1003935`, `fecha_recep=2026-05-06`, `apli_fecha=2026-05-01`, `origen_solicitud=AFI`.
- Para `4676684`, `SOLI_AFILIADOS` tiene un solo integrante: `item=1`, `inte=01`, documento `RC 1205975565`, `paren=T`, `paren_real=7`, sin baja. `SOLI_ADICIONALES` no tiene filas.
- `AFI_ADICIONALES` para `prepaga=1`, `contra=1003935`, `inte=01` muestra anexo `OPLB/BGAOPLB`: fila anterior `vigen_desde=2024-12-01`, `vigen_hasta=2025-01-31`, y fila nueva `vigen_desde=2025-02-01`, `vigen_hasta=NULL`; ambas modificadas/creadas alrededor de `2026-05-06 16:51:14`.
- La fecha `2025-02-01` del anexo obligatorio no sale de `SOLICITUDES.apli_fecha` (`2026-05-01`) sino de `AFI_HISTO_PAREN.vigen_desde` del integrante, cuyo historial relevante incluye `2024-12-01 paren_real=0`, `2025-02-01 paren_real=2`, `2026-05-01 paren_real=7`.
- Configuración corroborada: en `SUBCTA_PLAN_ADI_PAREN`, para `cuenta=1`, `subcta=ASMP`, `plan_codi=OPLA`, `plan_codi_adi=OPLB`, los parentescos `0..8` están configurados con `obliga='S'`.
- SPs relevantes para reconstrucción de anexos obligatorios: `dbo.afi_adic_cambTit` arma períodos a partir de cobertura y `AFI_HISTO_PAREN`, y llama a `dbo.afi_calcula_planes_adi`; este último consulta `subcta_plan_adi_paren` para determinar anexos obligatorios (`obliga='S'`).
- Regla de cálculo hallada en `afi_adic_cambTit`: `@vigen_desde = CASE WHEN @vigen_desde_cober >= @vigen_desde_paren THEN @vigen_desde_cober ELSE @vigen_desde_paren END`; es decir, vigencia del anexo = mayor fecha entre vigencia de cobertura y vigencia de parentesco.
- Conclusión persistida para `4676684`: el anexo obligatorio `OPLB` queda recreado con `vigen_desde=2025-02-01` porque el período efectivo se calcula con el histórico de parentesco real (`AFI_HISTO_PAREN`) y la cobertura, no con la fecha de aplicación de la solicitud actual.

## Repositorios / estado operativo

- SVN checkout principal usado: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`, actualizado a revisión `36804`.
- Se actualizó `REPOSITORIOS.md` con la entrada del repo SVN principal.
- SVN de Sebastián consultado: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- No hacer commit/push salvo pedido explícito.
- Para ejecuciones con impacto en DB, mantener criterio de Gabriel: usar `BEGIN TRAN` / `ROLLBACK` cuando corresponda.
