# Memoria curada diaria

- Fecha: 2026-07-29
- Usuario: Sebastián Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Corrección del ejemplo SARLAFT para traslado a contrato existente

- Solicitud o contexto: Sebastián retomó el Redmine `30141` y reportó que `fase2_ejecucion_sarlaft_traslado_existente.sql` fallaba con SQL Server `3930`: la transacción no podía confirmarse ni escribir al log, y propuso revisar si hacía falta otro ejemplo para probar SARLAFT.
- Respuesta o acciones del agente: Se corrigió el `CATCH` del script para evitar `DROP TABLE #salida` antes del `ROLLBACK` cuando `SET XACT_ABORT ON` dejaba la transacción no confirmable. Se guardaron `ERROR_NUMBER()` y `ERROR_MESSAGE()`, se aplicó `ROLLBACK` según `XACT_STATE()` y se limpió la temporal después.
- Resultado: Se validó encoding ASCII, sin BOM, finales LF, sin agregar `;` ni `RTRIM`. Por pedido explícito de Sebastián se comiteó el cambio en SVN como `r37451`, mensaje `Redmine 30141 corrige rollback ejemplo SARLAFT traslado existente`.
- Estado: Resuelto

### Tema 2: Ajustes sucesivos de datos del ejemplo de traslado existente

- Solicitud o contexto: Sebastián probó el ejemplo y reportó errores sucesivos: el XML no debía informar `fecha_aplicacion`; el asesor `1234` no existía o estaba de baja; necesitaba un script persistente, sin rollback exitoso, para probar el circuito completo con batch; luego el batch no mostraba solicitud en Afilia y aparecían múltiples errores funcionales.
- Respuesta o acciones del agente: Se quitó `fecha_aplicacion` del XML, se reemplazó el asesor hardcodeado por la selección de un asesor vigente, se dejó el script sin rollback de éxito y se explicó que el SQL solo carga `SOLI_AUTO_*`; la solicitud aparece en Afilia recién cuando el batch genera `GMS_CAB`, valida GMS y crea `SOLICITUDES`. Se realizaron múltiples ajustes de datos: destino, origen, parentesco, anexos, fecha de inicio, generación de `id_soli_externa` y selección de caso vigente.
- Resultado: Se realizaron commits SVN solicitados por Sebastián: `r37452` (`Redmine 30141 ajusta datos ejemplo SARLAFT traslado existente`), `r37453` (`Redmine 30141 deja ejemplo SARLAFT persistente para batch`), `r37458` (`Redmine 30141 ajusta caso valido ejemplo SARLAFT traslado existente`), `r37459` (`Redmine 30141 fija fecha caso SARLAFT traslado existente`), `r37460` (`Redmine 30141 selecciona caso vigente ejemplo SARLAFT`), `r37461` (`Redmine 30141 simplifica ejemplo SARLAFT traslado existente`), `r37462` (`Redmine 30141 ajusta ejemplo simple SARLAFT con caso validado`) y `r37463` (`Redmine 30141 usa caso validado sin errores DEF para SARLAFT`). El caso que finalmente validó sin errores DEF usó destino `1005326`, integrante destino `02`, documento `CC 14638533`, origen `968807 / 02`, parentesco `H` e `id_soli_externa = 301410102`.
- Estado: Resuelto

### Tema 3: Ubicación funcional de datos SARLAFT en Afilia

- Solicitud o contexto: Tras lograr que el ejemplo funcionara, Sebastián preguntó en qué pantalla de Afilia debía verificar que se completaron los datos SARLAFT y luego cómo llegar a `wp_cons_gral_contratante`.
- Respuesta o acciones del agente: Se explicó que antes de impactar la solicitud los datos SARLAFT quedan asociados a la solicitud en `SOLI_SARLAFT` y `SOLI_SARLAFT_AUTORIZA_CONTACTO`; después de impactarla deben verse en Afilia en la consulta del contratante, solapa SARLAFT. Se identificó la pantalla técnica `wp_cons_gral_contratante.srw`, que usa `afs_datos_sarlaft_contratante`, y el menú `Solicitudes > Consulta General de Contratante`.
- Resultado: Quedó indicado que para el ejemplo validado debía buscar el contratante o contrato destino `1005326`; si la solicitud no estaba impactada, el dato debía revisarse como dato de solicitud y no como maestro final del contratante.
- Estado: Informativo

### Tema 4: Cambio de alcance funcional de SARLAFT a solo traslado a contrato nuevo

- Solicitud o contexto: Sebastián aclaró que SARLAFT no debía aplicarse a traslado a contrato existente porque el titular no se modifica y esa ventana no tiene botón SARLAFT; pidió dejar el proceso solo para traslado a contrato nuevo.
- Respuesta o acciones del agente: Se modificó el alcance: `afi_insertar_soli_auto_traslado_existente.sql` pasó a rechazar XML con nodos `sarlaft` o `sarlaft_autoriza_contacto`; `afi_insertar_soli_auto_traslado_nuevo.sql` quedó con persistencia de `SOLI_AUTO_SARLAFT` y autorizaciones; `gms_genera_solicitudes.sql` quedó condicionado para pasar a `SOLI_SARLAFT` solo en solicitudes APP con `soli_moti = 47`. El ejemplo de traslado existente quedó sin nodos SARLAFT.
- Resultado: Por pedido de Sebastián se armó un ejemplo correcto para traslado a contrato nuevo en `Redmines/29362/fase2_ejecucion_sarlaft_traslado_nuevo.sql`. Se validó en `InstallQA_89` con rollback que ejecutaba `soli_moti = 47`, insertaba `SOLI_AUTO_SARLAFT = 1`, insertaba 2 autorizaciones SARLAFT y compilaban con rollback los 3 SPs tocados. Se comiteó en SVN como `r37464`, mensaje `Redmine 30141 aplica SARLAFT solo traslado nuevo`.
- Estado: Resuelto

### Tema 5: Corrección del ejemplo SARLAFT de traslado nuevo por profesión de baja

- Solicitud o contexto: Sebastián reportó que el batch del ejemplo de traslado nuevo devolvía errores DEF motivo `8090`: "La profesión debe existir o no debe estar dada de baja", asociado al externo `301410201`.
- Respuesta o acciones del agente: Se revisaron tablas y datos de profesiones vigentes en `InstallQA_89`. Se identificó que `profesion = 665` estaba dada de baja. Se actualizó el ejemplo de traslado nuevo cambiando la profesión a `874`, vigente, y también se cambió `id_soli_externa` a `301410202` porque `301410201` ya había sido usado por la prueba.
- Resultado: Se validó en `InstallQA_89` con rollback ejecutando el script nuevo, `afu_genera_soli_auto_app` y `gms_validar_todo`; el resultado fue `errores = 0`. Por pedido operativo ya dado, se comiteó en SVN como `r37465`, mensaje `Redmine 30141 corrige ejemplo SARLAFT traslado nuevo`. El `svn status` final quedó limpio salvo externos normales de PBLs.
- Estado: Resuelto
