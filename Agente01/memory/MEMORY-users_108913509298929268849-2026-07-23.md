# Memoria curada diaria

- Fecha: 2026-07-23
- Usuario: Sebastián Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Redmine 30137 - diagnóstico del reporte Pronto Pago

- Solicitud o contexto: Sebastián pidió ponerse en contexto del Redmine `30137` y confirmar comprensión del problema. Luego preguntó qué SP llama el reporte.
- Respuesta o acciones del agente: Se usó la credencial Redmine registrada para `srmontenegro`, se consultó el issue y se revisaron adjuntos y código en `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Resultado: Se identificó que el defecto correspondía al reporte `ReportePPContratosConFC`, con columnas `Valor Agrupador` y `Valor Total` vacías y `Saldo Factura` incorrecto para contratos `1127561` y `1128231`. Se determinó que el DataWindow `d_afs_reporte_1_pronto_pago.srd` llama a `dbo.afs_reporte_pronto_pago`, que para `@prm_tipo_reporte = 1` deriva a `dbo.afs_reporte_1_pronto_pago`.
- Estado: Resuelto

### Tema 2: Redmine 30137 - corrección y commit SVN

- Solicitud o contexto: Sebastián pidió solucionar el problema del reporte y luego solicitó commit sin compilar en base.
- Respuesta o acciones del agente: Se modificó `backend/procedures/afs_reporte_1_pronto_pago.sql` en la working copy SVN `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Resultado: Se corrigieron `ValorAgrupador` y `ValorTotal` quitando el filtro incorrecto `CA3.compro_tipo_agrupador = 'FC'`, porque los PP contrato agrupan contra un PP agrupador. También se corrigió `SaldoFactura` filtrando el DA/DS por `Co.contra = C.contra` para evitar tomar el saldo de otro contrato. Se validó contra `PruebasCoreINFOMEDICAL`: contrato `1127561` con saldo `84.095`, contrato `1128231` con saldo `207.220`, y valores agrupador/total esperados. No se compiló el SP por pedido explícito del usuario. Se realizó commit SVN revisión `37410` con mensaje `Redmine 30137 - Corrige reporte pronto pago contratos con FC`.
- Estado: Resuelto

### Tema 3: traslados 47/48 - validación de `fecha_aplicacion` y asesor dado de baja

- Solicitud o contexto: Sebastián retomó traslados y reportó que una solicitud XML con `soli_moti=48`, `fecha_aplicacion=2026-07-01` y `asesor_gestion=400` fallaba con `El codigo de asesor no existe, o se encuentra dado de baja`; indicó que sin `fecha_aplicacion` pasaba y pidió probarlo en `InstallQA_89`.
- Respuesta o acciones del agente: Se revisó el flujo de `afi_insertar_soli_auto` y la validación en `afs_soli_auto_obligatorios`. Luego se ejecutaron pruebas en `InstallQA_89` con transacción y `ROLLBACK`, usando ids externos libres para evitar dejar solicitudes insertadas.
- Resultado: Se confirmó que el asesor `400` estaba dado de baja desde `2025-11-18` y que la validación compara vigencia contra la fecha de aplicación. En la base `InstallQA_89`, tanto con `fecha_aplicacion` como sin `fecha_aplicacion`, la solicitud falló con el mismo mensaje, porque el dispatcher vuelve a completar la fecha desde `fecha_inicio_servicio` para traslados `47/48`.
- Estado: Resuelto

### Tema 4: traslados 47/48 - rechazo explícito de `fecha_aplicacion`

- Solicitud o contexto: Sebastián planteó que si `fecha_aplicacion` ya no debe enviarse para traslados, el SP debería informarlo explícitamente, y agregó que cualquier dato no recibido también debe informarse.
- Respuesta o acciones del agente: Se modificó `backend/procedures/afi_insertar_soli_auto.sql` para que en traslados `47/48`, si el XML trae `fecha_aplicacion`, devuelva el mensaje `El XML no debe informar fecha_aplicacion para traslados; se calcula desde fecha_inicio_servicio` y deje de pisar silenciosamente el valor recibido. También se revisó que `afs_soli_auto_obligatorios` ya tenía mensajes específicos `El XML no informa ...` para múltiples datos obligatorios.
- Resultado: Se informó el cambio local sin compilar ni commitear inicialmente. Luego Sebastián pidió commit y se realizó commit SVN revisión `37411` con mensaje `Ajusta validacion de fecha_aplicacion en traslados automaticos`.
- Estado: Resuelto

### Tema 5: corrección de finales de línea en `afi_insertar_soli_auto.sql`

- Solicitud o contexto: Sebastián informó que algo había quedado mal cerrado al abrir el archivo modificado.
- Respuesta o acciones del agente: Se diagnosticó que el problema no era un `BEGIN/END`, sino mezcla de finales de línea en `afi_insertar_soli_auto.sql`.
- Resultado: Se normalizó el archivo a finales Windows completos, manteniendo encoding ASCII sin BOM. Luego Sebastián pidió commit y se realizó commit SVN revisión `37412` con mensaje `Normaliza finales de linea en afi_insertar_soli_auto`.
- Estado: Resuelto

### Tema 6: SARLAFT en traslados APP 47/48

- Solicitud o contexto: Sebastián indicó que para traslado a contrato existente `48` y traslado a contrato nuevo `47` faltaba contemplar la sección o botón SARLAFT, y pidió analizar los datos nuevos necesarios. Luego solicitó un documento técnico y commit.
- Respuesta o acciones del agente: Se revisaron tablas, DataWindows y procedimientos relacionados con SARLAFT, incluyendo `SOLI_SARLAFT`, `AFI_SARLAFT`, `afu_afi_sarlaft`, `afs_datos_sarlaft_contratante`, `afs_cambio_datos_sarlaft`, `afi_impacta_alta_total` y el flujo de `gms_genera_solicitudes`.
- Resultado: Se propuso recibir opcionalmente los datos de `SOLI_SARLAFT`: `fecha_exp_id`, `pais_nac`, `depto_nac`, `municipio_nac`, `egresos`, `otros_ingresos`, `activos`, `pasivos`, `detalle_otros_ing`, `es_pep`, `cargo_pep`, `fecha_inicio_pep`, `fecha_fin_pep`, `representa_ong`, `organizacion`, `fecha_inicio_org`, `fecha_fin_org`, `vinculo_pep`, `nombre_vinculo_pep`, `cargo_vinculo_pep`, `paren_vinculo_pep`, `transacciona_me`, `transaccion`, `otra_transaccion`, `ctacte_me`, `cta_cte`, `declaracion_bs_fondos`, `autorizacion_1`, `autorizacion_2`, `autorizacion_3` y `habilita`; con default explícito `habilita = 'N'`. También se mencionó la posible colección opcional `SOLI_SARLAFT_AUTORIZA_CONTACTO` con `autoriza_contacto_nro_pregunta`, `respuesta_boolean`, `respuesta_decimal` y `observacion`.
- Resultado: Se creó y commiteó `Redmines/29362/DOCUMENTO_TECNICO_SARLAFT_TRASLADOS_APP_47_48.md` con revisión SVN `37413` y mensaje `Documenta cambios SARLAFT en traslados APP`. El documento incluye XML propuesto, datos opcionales, default `habilita = 'N'`, tablas nuevas sugeridas, SPs a modificar, validaciones y pruebas para `47/48`.
- Estado: Resuelto
