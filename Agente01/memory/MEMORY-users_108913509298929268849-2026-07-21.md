# Memoria curada diaria

- Fecha: 2026-07-21
- Usuario: Sebastian Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Registro Unico, domicilios y numeracion de integrantes en traslado existente

- Solicitud o contexto: Sebastian analizo un traslado a contrato existente con integrantes origen `02` y `03`, documentos `CC 71656954` y `TI 1017928396`, donde Registro Unico tenia domicilios vigentes completos pero el log marcaba errores de domicilio.
- Respuesta o acciones del agente: Se explico primero que la consulta directa a `REGISTRO_UNICO_DOMICILIOS` traia historicos y que debia quedarse con `ROW_NUMBER()` por mayor `vigencia`. Luego se valido que el SP `afu_soli_auto_app_contactos_gmstxt.sql` ya tomaba un unico domicilio vigente. Con los datos de `GMSDEF_INTEGRANTES` y `GMSDEF_DOMICILIOS` se detecto un desfasaje: para `cab_id=295769` los integrantes estaban en `01`, `06`, `07`, pero los domicilios en `04`, `05`.
- Resultado: Se concluyo que no faltaban domicilios reales, sino que contactos e integrantes usaban numeraciones destino inconsistentes.
- Estado: Resuelto

### Tema 2: Fix de numeracion de integrantes y validacion de contactos APP

- Solicitud o contexto: Sebastian pidio solucionar el calculo de integrantes destino. Aclaro el criterio funcional: si el contrato destino tiene 4 integrantes vigentes y se trasladan los origen `02` y `03`, deben quedar como destino `05` y `06`.
- Respuesta o acciones del agente: Se corrigio el analisis y se aplico el cambio en `backend/procedures/afu_genera_soli_auto_app.sql`: para `soli_moti=48`, la base de numeracion de integrantes sale solo de `AFILIADOS` vigente del contrato destino a `fecha_aplicacion`, no de `GMSTXT_INTEGRANTES`, `GMSDEF_INTEGRANTES` ni `SOLI_AFILIADOS`. Luego, al persistir errores para el `Inte 01`, se corrigio `backend/procedures/gmsdef_val_traslado_web.sql` para que las validaciones `29301`, `29302`, `29303` en `soli_moti=48` apliquen solo a integrantes con origen informado (`anter_prepa`, `anter_contra`, `anter_inte`).
- Resultado: Commits SVN `r37380` y `r37381`, con validacion de diff acotado, encoding y finales de linea.
- Estado: Resuelto

### Tema 3: Fechas de aplicacion e inicio de servicio

- Solicitud o contexto: Sebastian provo solicitudes nuevas con `id_soli_externa=1010/1011` y reviso criterios de fechas para traslados. Luego transmitio que para traslado a contrato existente estaba bien igualar `fecha_aplicacion` y `fecha_inicio_servicio`, pero para traslado a contrato nuevo la fecha del XML solo debia servir para periodo/proporcionalidad y el sistema debia calcular la fecha real de inicio.
- Respuesta o acciones del agente: Se aplicaron cambios para distinguir `soli_moti=48` y `soli_moti=47`. Para traslado nuevo, en `afi_insertar_soli_auto.sql` la `fecha_aplicacion` queda como primer dia del mes de `fecha_inicio_servicio`, y en `afi_insertar_soli_auto_traslado_nuevo.sql` la `fecha_inicio_servicio` real se calcula con `VTO_GRUPOS.vto_dia`/fin de mes, replicando el criterio de `f_calcula_fecha_inicio_servicio`.
- Resultado: Commit SVN `r37383` con mensaje `Calcula fecha de inicio en traslado nuevo APP`. Quedo pendiente para Sebastian compilar en base y probar una nueva solicitud `soli_moti=47`.
- Estado: Resuelto

### Tema 4: Observaciones de solicitudes automaticas de traslado

- Solicitud o contexto: Sebastian pidio que el campo observaciones indicara que la solicitud fue generada automaticamente a partir del proceso de Generacion Masiva de Solicitudes de Traslado, concatenando el numero de solicitud Afilmed, para ambos traslados y sus retiros correspondientes.
- Respuesta o acciones del agente: Se actualizo `gms_genera_solicitudes.sql` para la observacion de la solicitud principal APP en traslados `47` y `48`, y `afi_genera_solis_aut_traslados.sql` para las solicitudes automaticas de retiro/cambio generadas desde el traslado. Se uso `CHAR(176)` para representar el simbolo de grado sin alterar encoding.
- Resultado: Commit SVN `r37385`, estado SVN limpio para los archivos.
- Estado: Resuelto

### Tema 5: Logs y CSV del batch de solicitudes

- Solicitud o contexto: Sebastian pregunto por que el batch de solicitudes a veces genera `.log` y `.csv`, y otras veces solo `.log`, aun con solicitudes impactadas o sin errores.
- Respuesta o acciones del agente: Se explico que el `.log` se abre siempre al procesar un `cab_id`, mientras el `.csv` se genera solo dentro de `wf_genera_solicitudes` si existe destino de correo y DataObject de exportacion para la transaccion. En `wp_main_solicitudes.srw`, la transaccion `AP` no esta mapeada, por eso procesos impactados pueden terminar sin CSV con el mensaje `No existe DataObject de exportacion para la transaccion: AP`.
- Resultado: Sebastian valido una respuesta funcional: `AP` es Alta Persona, no tiene DataWindow definida, no es critico para estos procesos y se dejara como mejora posterior para liberar exportacion de casos faltantes.
- Estado: Resuelto

### Tema 6: Caso 4, anexos obligatorios por parentesco real

- Solicitud o contexto: Sebastian documento el caso 4: en traslado APP a contrato nuevo, con `paren_destino=T` y `paren_real_destino=T`, la ventana mostraba `OPLB` obligatorio aunque la configuracion no lo obliga para titular y el XML no enviaba anexos.
- Respuesta o acciones del agente: Se modifico `backend/procedures/gms_adicionales_generar.sql` para que la generacion de anexos/adicionales obligatorios evalue `SUBCTA_PLAN_ADI_PAREN.paren` contra `GMSDEF_INTEGRANTES.paren_real` cuando exista, con fallback a `GMS_INTEGRANTES.paren`.
- Resultado: Commit SVN `r37386`, validado con diff acotado, ASCII, CRLF y sin cambios pendientes.
- Estado: Resuelto

### Tema 7: Caso 5, validacion de responsable pagador

- Solicitud o contexto: Sebastian planteo que si existe un responsable pagador con documento distinto al titular, no debe existir un titular `T/T`; y si el RP tiene el mismo documento que el titular, entonces si debe ser `T/T`. Tambien pidio evitar dos responsables pagadores vigentes.
- Respuesta o acciones del agente: Se modifico `gmsdef_val_alta_total.sql` para levantar `8230` si hay RP distinto y el titular queda `T/T`, y `8231` si el RP es el mismo documento que el titular pero el titular no queda `T/T`. Se modifico `gmsdef_val_resp_pagador.sql` para validar que no haya mas de un `GMSDEF_RESP_PAGADOR` activo para el mismo contrato/sec_contra, usando `9308`.
- Resultado: Commit SVN `r37387`, con update previo sin conflictos y estado final limpio.
- Estado: Resuelto

### Tema 8: Caso 6, habeas data

- Solicitud o contexto: Sebastian indico que si el XML viene sin `habeas_data` debe tomarse el default tildado, pero si viene `habeas_data="N"` debe verse destildado.
- Respuesta o acciones del agente: Se ajusto `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql` para mapear `habeas_data` vacio/no informado a `S`, `N` a `N`, y `S` a `S`, manteniendo la validacion en `GMSTXT_MULTI`.
- Resultado: Commit SVN `r37388`, diff de una sola linea, ASCII, CRLF y estado limpio.
- Estado: Resuelto

### Tema 9: Caso 7, anexo XML no obligatorio

- Solicitud o contexto: Sebastian presento un caso donde el XML si trae un anexo para el integrante `01`, pero el anexo no es obligatorio por configuracion. El anexo debe cargarse/mostrarse porque fue informado, pero quedar como no obligatorio.
- Respuesta o acciones del agente: Se modifico `backend/procedures/gms_adicionales_generar.sql` para que los anexos informados por XML sincronicen `GMSDEF_ADICIONALES.obliga` tanto en `S` como en `N`. Antes solo se actualizaba la DEF cuando el anexo resultaba obligatorio.
- Resultado: Commit SVN `r37389`, validado con estado limpio, `us-ascii`, CRLF, sin `;` ni `RTRIM`.
- Estado: Resuelto

### Tema 10: Caso 8, origen afiliado en facturacion

- Solicitud o contexto: Sebastian planteo que el proceso `131` genero contrato `1073911` desde origen `1036462`, ambos debian ser Afiliado, pero el automatico dejo la facturacion como Cuenta. El criterio funcional era usar Cuenta solo para cuenta colectiva, y Afiliado para cuenta individual.
- Respuesta o acciones del agente: Se encontro que el automatico impactaba `AFI_TITULARES` usando `afi_afi_titulares`, que decide por `es_directo`, mientras la ventana manual usa `afu_afi_titulares`, que decide por cuenta individual/acuerdo corporativo. Se aplico el cambio en `backend/procedures/gms_impacta_soli_pf.sql`.
- Resultado: Commit SVN `r37390`, mensaje `Respeta origen afiliado en traslado nuevo APP`, validado con diff acotado, ASCII, sin BOM, CRLF, sin `;` ni `RTRIM`.
- Estado: Resuelto

### Tema 11: Caso 9, visualizacion de parentesco real

- Solicitud o contexto: Sebastian pidio que en la ventana el campo Parentesco Real no muestre `T` ni `Titular` cuando `paren_real=T`; debe verse grisado y vacio. Solo debe mostrar dato cuando el parentesco real es distinto de titular, por ejemplo cuando hay responsable pagador distinto al titular.
- Respuesta o acciones del agente: Se ajusto `exe_afilmed/src/wp_alta_parcial.srw` para que si `paren_real=T`, el campo quede grisado y vacio visualmente; si `paren_real` es distinto de titular, queda visible/habilitado en amarillo.
- Resultado: Commit SVN `r37391`, mensaje `Ajusta visualizacion de parentesco real APP`, con `UTF-8` con BOM y CRLF preservados, y estado final sin cambios pendientes.
- Estado: Resuelto
