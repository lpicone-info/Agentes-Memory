- En este entorno, al editar archivos `.sql` usados también desde SSMS, puede aparecer el aviso de "archivo modificado externamente" porque el archivo fue cambiado en disco fuera del editor de SSMS; no implica por sí mismo un problema de saltos de línea.
- Riesgo operativo importante para este repo legacy: varios `.sql` son sensibles a encoding esperado por SSMS y entorno Windows/SQL Server; si se guardan como UTF-8 cuando el entorno espera ANSI/Windows-1252, los acentos se degradan y aparecen textos como `Identificaci�n`.
- Regla práctica para futuras ediciones en este repo: evitar conversiones de encoding en archivos `.sql` y preservar la codificación compatible con SSMS, típicamente ANSI/Windows-1252 en este proyecto.
