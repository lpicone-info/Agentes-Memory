
## 2026-07-13 15:53 UTC - gpulka v104 / Redmine 29365

- Usuario pidió ponerse en contexto de la rama `gpulka` versión 104, actualizar working copy, revisar `soli_auto` para cambio de responsable pagador comparando con la ventana desde `afilmed`, especialmente solicitudes automáticas generadas además de la principal, aplicar cambios y commitear.
- Working copy SVN actualizada a `r37311` antes del análisis.
- Se revisó el circuito `soli_auto` para `soli_moti = 22` contra `wp_cambio_resp_pagador`.
- Cambio implementado en `backend/procedures/gms_genera_solicitudes.sql`: para `soli_moti = 22` el impacto ahora pasa por `afi_impacta_cambio_resp_pagador`, igual que la ventana, en lugar de impactar solo `afu_afi_resp_pagador`.
- Se evitó doble impacto de responsable pagador en el bloque genérico final.
- Se agregó generación de solicitud automática derivada de cambio de datos cuando hay parentescos informados.
- Se creó `backend/procedures/afi_genera_soli_auto_cambio_datos_parentescos.sql`: SP que genera e impacta la solicitud automática `soli_moti = 5` derivada de la RP, replicando comportamiento de la ventana cuando hay cambios de parentesco.
- Validación ejecutada: `SET PARSEONLY ON` contra `InstallQA_89`, resultado OK.
- Se verificó encoding/EOL, preservando CRLF en los archivos modificados.
- Commit SVN realizado: `r37312 | Redmine 29365 - completa impacto RP soli auto`.
- Estado final informado: working copy sin cambios versionados pendientes; quedaron no versionados dos `.docx` existentes en `Redmines/29361` y `Redmines/29362`, no incluidos.
## 2026-07-13 15:53 UTC - gpulka v104 / Redmine 29365

- Usuario pidió ponerse en contexto de la rama `gpulka` versión 104, actualizar working copy, revisar `soli_auto` para cambio de responsable pagador comparando con la ventana desde `afilmed`, especialmente solicitudes automáticas generadas además de la principal, aplicar cambios y commitear.
- Working copy SVN actualizada a `r37311` antes del análisis.
- Se revisó el circuito `soli_auto` para `soli_moti = 22` contra `wp_cambio_resp_pagador`.
- Cambio implementado en `backend/procedures/gms_genera_solicitudes.sql`: para `soli_moti = 22` el impacto ahora pasa por `afi_impacta_cambio_resp_pagador`, igual que la ventana, en lugar de impactar solo `afu_afi_resp_pagador`.
- Se evitó doble impacto de responsable pagador en el bloque genérico final.
- Se agregó generación de solicitud automática derivada de cambio de datos cuando hay parentescos informados.
- Se creó `backend/procedures/afi_genera_soli_auto_cambio_datos_parentescos.sql`: SP que genera e impacta la solicitud automática `soli_moti = 5` derivada de la RP, replicando comportamiento de la ventana cuando hay cambios de parentesco.
- Validación ejecutada: `SET PARSEONLY ON` contra `InstallQA_89`, resultado OK.
- Se verificó encoding/EOL, preservando CRLF en los archivos modificados.
- Commit SVN realizado: `r37312 | Redmine 29365 - completa impacto RP soli auto`.
- Estado final informado: working copy sin cambios versionados pendientes; quedaron no versionados dos `.docx` existentes en `Redmines/29361` y `Redmines/29362`, no incluidos.

## 2026-07-13 16:26 UTC - Redmine 29364 / cambio titularidad soli auto

- Conversación con Sebastián sobre pruebas de `backend/procedures/gms_genera_solicitudes.sql` y scripts en `Redmines/29364`.
- Se detectó error al abrir SP en SSMS por finales de línea mixtos en `gms_genera_solicitudes.sql`; se normalizó a CRLF sin cambiar contenido lógico, encoding ISO-8859-1 sin BOM. Commit SVN `r37308 | Redmine 29364 - normaliza SP y agrega caso limpio` junto con `prueba_completa_sin_rollback_29364_ejemplo_4.sql`.
- Primer caso probado tenía residuos: `id_soli_externa=293649003`, contrato `298320`, múltiples titulares vigentes. Se descartó y se preparó caso limpio `id_soli_externa=293649004`, contrato `1000100`, titular actual `01`, nuevo titular `02` Graciela Balmaceda.
- Se creó script mínimo XML + ejecución de `dbo.afi_insertar_soli_auto`: `Redmines/29364/prueba_xml_sp_principal_29364_ejemplo_5.sql`. Commit SVN `r37309 | Redmine 29364 - agrega script minimo XML`.
- El superbatch fallaba con `Error al validar el titular vigente del contrato`. Causa corregida: para motivo `7` no se grababa correctamente `SOLI_PARENTESCOS`, el impacto forzaba `T` desde `SOLI_AFILIADOS`, y la validación contaba históricos abiertos en vez del último parentesco vigente por integrante.
- Archivos corregidos y commiteados en SVN `r37310 | Redmine 29364 - corrige impacto cambio titularidad`: `backend/procedures/gms_genera_solicitudes.sql` y `backend/procedures/afi_impacta_soli_parentescos.sql`. Validación con rollback en `InstallQA_89` usando `293649006`: generó solicitud `4677071`, `SOLI_PARENTESCOS` quedó `01 C`, `02 T`, titular vigente final único.
- Sebastián ejecutó luego con `id_soli_externa=293649006`; superbatch completó y generó `Solicitud Afilmed: 4677072`. Si la fecha de aplicación era futura (`2026-08-01`), Consulta General seguía mostrando el titular anterior hasta esa vigencia; al probar con `2026-07-01` confirmó cambio visible.
- Se analizó baja del titular anterior y responsable pagador. Hallazgo: cambio de titularidad motivo `7` impactaba parentesco/baja, pero el bloque de `AFI_RESP_PAGADOR` estaba solo para motivos `1`, `22`, `47`; por lo tanto si el titular anterior era responsable pagador podía quedar RP apuntando al afiliado dado de baja.
- Se creó y commiteó ejemplo de baja titular: `Redmines/29364/prueba_xml_sp_principal_29364_ejemplo_6_baja_titular.sql`, SVN `r37311 | Redmine 29364 - agrega ejemplo baja titular`.
- Usuario pidió revisar documento/análisis original y Power: cuando están marcados baja titular y generar responsable pagador, el XML debe exigir bloque nuevo `<responsable_pagador>`. Se descartaron contrato/integrantes manipulados usados en ejemplos anteriores.
- Cambios finales commiteados en SVN `r37315 | Redmine 29364 - agrega RP en cambio titularidad`:
  - `afi_insertar_soli_auto_cambio_titularidad.sql`: si `genera_resp_pagador="S"`, exige nodo `<responsable_pagador>` y guarda datos de RP.
  - `afu_genera_soli_auto_app.sql`: genera responsable pagador también para motivo `7` cuando corresponde y marca anulado al titular anterior si `baja_titular="S"`.
  - `gms_genera_solicitudes.sql`: baja titular anterior marcado, genera `SOLI_RESP_PAGADOR` para CT con RP e impacta `afi_impacta_cambio_resp_pagador` con origen `7`.
  - Ejemplo titularidad sola `ejemplo_7`, contrato limpio `1000063`.
  - Ejemplo titularidad + responsable pagador `ejemplo_8`, contrato limpio `1000066`.
- Validación final reportada: contratos e IDs nuevos consultados en `InstallQA_89`, XML bien formado, `PARSEONLY OK` en los 3 SP, sin conflictos SVN, working copy limpia salvo externos `X` habituales.

## 2026-07-13 16:26 UTC - Redmine 29364 / cierre posterior responsable pagador CT

- Después del commit `r37315`, Sebastián corrigió el criterio funcional: en `wp_cambio_tit`, cuando se marca baja de titular y generar responsable pagador, no se informa un responsable nuevo desde XML. El único caso válido es que el titular anterior quede como nuevo responsable pagador; si no se marca el check, solo se da de baja el titular anterior y el responsable pagador sigue como estaba.
- Se volvió a revisar `wp_cambio_tit` y se confirmó la interpretación: Power pregunta si el titular actual sigue siendo responsable pagador y usa los datos del titular anterior, no un bloque externo de datos.
- Se hizo un commit intermedio `r37318 | Redmine 29364 - completa CT con RP y ejemplos`, incorporando `afi_genera_soli_auto_cambio_datos_parentescos` en el flujo de CT con RP y ejemplos consultados.
- Corrección final commiteada en SVN `r37320 | Redmine 29364 - ajusta RP en cambio titular`:
  - `backend/procedures/afi_insertar_soli_auto_cambio_titularidad.sql`: ya no exige ni consume nodo `<responsable_pagador>` para motivo `7`.
  - `backend/procedures/afu_genera_soli_auto_app.sql`: si `genera_resp_pagador = S`, toma al titular anterior vigente y lo genera como responsable pagador.
  - `Redmines/29364/prueba_xml_sp_principal_29364_ejemplo_8_titularidad_resp_pagador.sql`: se quitó el bloque `<responsable_pagador>` y quedó solo el flag.
- Validación final reportada: encoding/EOL controlado, sin `RTRIM`, sin `;`, `PARSEONLY OK`, y los tres archivos quedaron sin cambios pendientes.
- Criterio vigente para retomar 29364: no agregar campos nuevos de RP en XML para CT. La semántica correcta es `baja_titular="S"` y opcionalmente `genera_resp_pagador="S"` para mantener al titular anterior como responsable pagador.

## 2026-07-13 20:47 UTC - Redmine 29362 / traslado APP anexos

- Sebastián reportó ejecución de traslado a contrato existente (`soli_moti = 48`) con error del batch de solicitudes: `Cannot insert the value NULL into column 'item', table 'InstallQA_89.dbo.SOLI_ADICIONALES'`.
- Diagnóstico: el XML enviado asociaba el anexo por `inte="04"`, pero en traslado a contrato existente el `inte` destino no debe venir desde XML; el backend lo calcula. En el caso real, `SOLI_AUTO_INTEGRANTES.inte` quedó como `01`, `SOLI_AUTO_ANEXOS.inte` como `04`, luego `GMSDEF_ADICIONALES.inte` quedó `NULL` y terminó rompiendo técnicamente al insertar `SOLI_ADICIONALES.item`.
- Criterio funcional consolidado: un anexo de integrante debe asociarse por origen (`anter_prepa`, `anter_contra`, `anter_inte`). Si el plan/tarifa destino no permite ese anexo, el proceso debe rechazar funcionalmente la solicitud y no explotar con error técnico.
- Cambios commiteados en SVN `r37322 | Redmine 29362 - valida anexos de traslado APP`:
  - `backend/procedures/afi_insertar_soli_auto_traslado_existente.sql`: para anexos prioriza asociación por origen antes de `inte`.
  - `backend/procedures/afu_genera_soli_auto_app.sql`: para motivo `48`, si el anexo quedó mal asociado y hay un único integrante, resuelve el destino por ese único integrante; también se normalizó EOL de ese archivo a CRLF por venir mixto.
  - `backend/procedures/gmsdef_val_traslado_web.sql`: agrega validación funcional para anexo sin integrante destino resoluble o anexo no habilitado para plan/tarifa destino, registrando `GMS_ERR` con mensaje concreto.
  - `Redmines/29362/ejemplo_xml_traslado_app_anexo_valido_29362.sql`: ejemplo mínimo con anexo válido.
- Ejemplo validado contra `InstallQA_89`: `id_soli_externa=293629205` libre, destino `contra=999146`, origen `401596/01`, documento `CC 1010244111`, anexo habilitado `PREFM / AFPREFMA`.
- Validaciones ejecutadas antes del commit: `PARSEONLY OK` para los 3 SP y el ejemplo, sin `RTRIM`, sin `;`, encoding/EOL controlados. Estado final: working copy limpia salvo externos `X` habituales.
- Respuesta funcional dada a Sebastián: si el anexo no corresponde al plan/tarifa destino, con `r37322` el proceso debe rechazar por validación GMS en lugar de fallar por `NULL` en `SOLI_ADICIONALES.item`.

## 2026-07-13 20:47 UTC - Redmine 29364 / facturación CT con o sin RP

- Sebastián pidió retomar 29364 para revisar cambio de titularidad (`soli_moti = 7`) con posible cambio de responsable pagador, y asegurar que si la vigencia impactada es anterior el batch de facturación haga reversión y refacturación con nuevos datos/condiciones.
- Revisión del circuito AUFACT:
  - `wp_main_super_batch.srw` consume `dbo.afs_soli_auto_app_fact_pendientes`.
  - Para cada fila, si `requiere_reversion = 'S'`, llama `dbo.afu_soli_auto_app_fact_rev_incluir` con `@prm_prepaga` y `@prm_contra` de la fila.
  - Luego factura nuevamente desde `periodo_desde` hasta `periodo_hasta`, con comentario especial para `soli_moti = 7`: `Facturacion por Novedad de Cambio de Titularidad`.
- Conclusión técnica: no hizo falta modificar lógica productiva. El circuito ya cubre motivo `7` tanto con `genera_resp_pagador = 'N'` como con `genera_resp_pagador = 'S'`, porque la normalización económica se dispara por solicitud APP, contrato y período, no por el flag aislado.
- Validación en `InstallQA_89` con transacción rollback:
  - Caso simulado sobre contrato `1073909`, aplicación `2026-07-01`, titular `01`, nuevo titular `02`, con `genera_resp_pagador='S'`.
  - `afs_soli_auto_app_fact_pendientes` devolvió `requiere_reversion='S'`, `requiere_facturacion='S'`, `factu_destino='A'`, `cantidad_comprobantes=1`, período `2026-07-01`.
  - `afu_soli_auto_app_fact_rev_incluir` cargó 1 comprobante a reversar, prefactura base `1120128`, comprobante `FC-XSCMP06959198`, `cod_unico_comprobante=144237937`.
- Se agregó y commiteó script de validación reproducible en SVN `r37323 | Redmine 29364 - agrega validacion facturacion CT`: `Redmines/29364/validacion_facturacion_ct_rollback_29364.sql`.
- Validaciones del script: ejecución real contra QA con `ROLLBACK`, `PARSEONLY OK`, sin `RTRIM`, sin `;`, LF uniforme.
