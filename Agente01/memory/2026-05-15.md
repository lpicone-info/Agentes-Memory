# Memory - 2026-05-15

## Redmine 29194 - Afilmed CC automático Asociado/Familiar
- Interlocutora en Discord DM identificada por metadata como Belkis / usuario `bmora_info`; no exponer credenciales ni claves.
- Se registró y usó credencial Redmine local para usuario `bmora` sin exponer API key.
- Se leyó Redmine `29194`; requerimiento: extender la lógica de cambio automático de tarifa/cobertura Asociado → Familiar a programas/contratos antiguos con eventos calificantes desde `01/08/2024`.
- Documento funcional relevante identificado: `A-04-DEF Facturacion 6 - CC Automatico Asoc a Fam.docx` en documentación Afilmed. El punto 4.1 describe el proceso automático durante prefacturación: detectar contratos asociados, validar estado cooperativa, homologar programa/tarifa, generar solicitud automática de cambio de cobertura y eventualmente revertir Familiar → Asociado.

## Repositorios SVN usados
- Checkout documentación SVN registrado en `REPOSITORIOS.md`:
  - Nombre: `bmora-ProyectoCoomeva-Afilmed-01.Documentacion`
  - URL: `https://reposrv01.infomedical.com:18553/svn/ProyectoCoomeva/Afilmed/01.Documentacion`
  - Local: `~/.openclaw/workspace/svn/bmora-ProyectoCoomeva-Afilmed-01.Documentacion`
  - Usuario SVN: `bmora`; usar siempre credenciales/config aislados registrados.
- Checkout código SVN registrado en `REPOSITORIOS.md`:
  - Nombre: `bmora-Coomeva-trunk`
  - URL: `https://srv-infosvn.infomedical.com/svn/Coomeva/trunk`
  - Local: `~/.openclaw/workspace/svn/bmora-Coomeva-trunk`
  - Usuario SVN: `bmora`; usar siempre credenciales/config aislados registrados.
- Working copy previa `bmora-Coomeva-trunk-exe_afilmed` fue eliminada mediante skill `svn-delete` con confirmaciones y retirada de `REPOSITORIOS.md`.

## Análisis DEF vs sistema - resultado
- Se comparó el DEF punto 4.1 contra el código actual de `bmora-Coomeva-trunk`.
- Implementación principal encontrada:
  - `backend/procedures/afs_facturar_verificar.sql` ejecuta `afi_cambio_cober_cooperativa` durante la verificación/prefacturación para tipos `PI`, `PP`, `MA`, `PC`, `CO`.
  - `backend/procedures/afi_cambio_cober_cooperativa.sql` centraliza la selección de contratos, validación de estado cooperativa, homologación y generación GMS.
  - Usa parámetro `ASOCIADOS.habilita_cambio_asoc_fam` para habilitar/deshabilitar el proceso.
  - Parte de `VERIF_CONTRA_PREFACTURA`, busca planes con `PLANES.cambio_asoc_fam = 'S'`, y para reversa busca `AFI_PLANES.cambio_asoc_fam = 'S'`.
  - Cruza `ASOCIADOS_COOPERATIVA` / `ASOCIADOS_COOPERATIVA_ESTADOS` y evalúa `cambia_estado_fact`.
  - Usa `ASOCIADOS_HOMOLOGACION_CONFIG` para programa/tarifa destino en Asociado → Familiar.
  - Registra falta de homologación en `VERIF_AFIL_ASOCIADOS_ERROR`.
  - Genera registros `GMSTXT_CLASE_PLAN`, valida y llama a `gms_genera_solicitudes` con tipo `CC_COOP`.
  - `gmstxt_pasar_a_def`, `gms_genera_solicitudes` y `afu_afi_planes` propagan `cambio_asoc_fam`, `cod_estado`, `baja_fecha` y `soli` hacia `SOLI_PLANES`/`AFI_PLANES`.
  - Para `CC_COOP`, el flujo omite finalización de campañas y congelamientos vía flags hacia `afu_impacta_cambio_cobertura`.
- Conclusión: el sistema implementa sustancialmente el flujo principal del DEF 4.1.
- Diferencia crítica detectada: el DEF indica excluir casos donde la vigencia del plan coincide con la vigencia de la solicitud a generar; en `afi_cambio_cober_cooperativa.sql` esa lógica existe como `DELETE #CONTRATOS_ASOC WHERE periodo = vigencia_planes AND ori = 'A'`, pero está comentada, por lo que actualmente NO se aplica.
- Para el alcance específico de Redmine 29194, no se observó lógica explícita para eventos desde `01/08/2024`; el flujo actual depende de prefacturación, marcas/configuración vigente y datos de `VERIF_CONTRA_PREFACTURA`.

## Documento generado y commit
- Se creó carpeta en documentación SVN: `Req y Doc. Complementaria/Facturacion/29194`.
- Se generó Word: `Req y Doc. Complementaria/Facturacion/29194/Analisis_DEF_vs_Sistema_R29194_CC_Automatico_Asoc_Fam.docx`.
- El Word contiene el análisis DEF vs sistema, correspondencias, diferencias, evidencia por archivos/líneas y recomendaciones.
- Se validó estructura DOCX y contenido interno.
- Se hizo commit SVN SOLO del documento/carpeta nueva, sin cambios de código:
  - Revisión SVN: `3008`
  - Autor SVN: `bmora@INFOMEDICAL`
  - Mensaje: `R29194 - Agrega analisis DEF vs sistema CC automatico Asoc a Fam`
  - URL archivo: `https://reposrv01.infomedical.com:18553/svn/ProyectoCoomeva/Afilmed/01.Documentacion/Req%20y%20Doc.%20Complementaria/Facturacion/29194/Analisis_DEF_vs_Sistema_R29194_CC_Automatico_Asoc_Fam.docx`

## Base de datos / reglas recordadas
- Perfil DB usado durante la jornada: `InstallQA_89` en `172.16.10.124:28000`, DB `InstallQA_89`.
- Se consultó `dbo.COMPRO_TIPOS` contando registros vigentes con `baja_fecha IS NULL`; resultado informado: 23 tipos de comprobante vigentes.
- Regla persistente aplicada: si una tabla tiene `baja_fecha`, por defecto contar/analizar vigentes con `baja_fecha IS NULL`.

## Pendientes sugeridos para próxima sesión
- Si se continúa Redmine 29194, confirmar funcionalmente si debe reactivarse/implementar la exclusión por misma vigencia que hoy está comentada.
- Definir con precisión la fuente técnica de “eventos desde 01/08/2024” y qué programas/contratos antiguos entran al alcance.
- Revisar parametrización `EMI_MODOS.meses` si hay dudas sobre equivalencia prepago/pospago del DEF.

## Redmine 29362 - Traslado a Contrato Existente (`soli_moti = 48`) / cierre de jornada
- Interlocutor actual desde WebChat Control UI pidió: “por hoy terminamos guarda todo lo que vimos hoy en tu memoria persistente”. No asumir nombre propio en sesiones futuras salvo metadata confiable.
- Repo SVN trabajado: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Working copy actualizada durante la jornada a revisión `36873`.
- Cambio implementado y commiteado en `backend/procedures/afu_genera_soli_auto_app.sql`: reemplazo del `INSERT INTO dbo.GMSTXT_INTEGRANTES` directo por cursor que llama `dbo.GMSTXT_INTEGRANTES_ins` por integrante, porque el SP encapsula la inserción de un integrante por ejecución.
- Validaciones del cambio SQL ejecutadas antes de commit: no quedó `INSERT INTO dbo.GMSTXT_INTEGRANTES` directo, los parámetros del SP coinciden, archivo en UTF-8 sin BOM, CRLF preservado, y no se agregó `RTRIM`.
- Commit SVN realizado con autorización explícita del usuario (“hace comit”): revisión `36874`, mensaje `29362: usar GMSTXT_INTEGRANTES_ins en solicitud automatica app`.
- Después del commit, `svn status` solo mostraba externos `X` en `exe_InfoMailer/pbls/impresion.pbl`, `exe_presmed/entidades.pbl` y `exe_presmed/ficha_afiliado.pbl`.

### Análisis `wp_alta_parcial` / popup de contratos para traslado existente
- Objetivo analizado: al cargar documento en `wp_alta_parcial` para traslado a contrato existente, explicar por qué la ventana muestra solo algunos contratos donde aparece el beneficiario.
- En `exe_afilmed/src/wp_alta_parcial.srw`, el caso `il_soli_moti = 48` configura la pantalla como `Traslado a Contrato Existente`, usa `d_soli_afiliados_edi_ap`, fija `ls_filtro = "es_traslado = 'S'"` y parentescos con `es_titular = 'N' AND IsNull(baja_fecha)`.
- Al cambiar el documento, para `il_soli_moti <> 2` se ejecuta `ilib_alta.f_alta_afiliaciones_anteriores(ls_docu_tipo, ls_docu_nro, ll_soli, il_soli_moti, ls_linea_negocio, ll_prepaga, ls_contrato)`. Si devuelve `-1`, abre `wf_consu_integrante(row)`; si no, informa `El beneficiario NO aplica para traslado` y limpia el documento.
- `wf_consu_integrante` arma `str_consulta` con `habilita_seleccion = TRUE`, `soli_moti = il_soli_moti`, `linea_negocio` tomada de `invo_plan.of_get_plan_grupo().of_get_linea_negocio().of_get_linea_negocio()`, `prepaga = dw_cabecera.entidad` y `contra = dw_cabecera.contrato`, y abre `OPENWITHPARM(wr_datos_de_integrantes, s_consu)`.
- La ventana/popup usada es `exe_afilmed/src/wr_datos_de_integrantes.srw`; la grilla relevante es `tabpage_2.dw_afiliado_anterior`, con DataWindow `d_busca_afi_inte`.
- `exe_afilmed/src/d_busca_afi_inte.srd` ejecuta `dbo.afs_d_busca_afi_inte` con argumentos `ape`, `nombre`, `naci_fecha`, `docu_tipo`, `docu_nro`, `linea_negocio`, `prepaga`, `contra`.
- En `wr_datos_de_integrantes.srw`, `ue_postopen` recupera `idw_afi_anterior.retrieve(...)` y, para `soli_moti = 47 OR 48`, aplica filtro PowerBuilder `isnull(realbaja_fecha)`. Por lo tanto, en traslado existente solo quedan afiliaciones activas según `realbaja_fecha IS NULL`.
- La prevalidación `nvo_alta_total.sru::f_alta_afiliaciones_anteriores` usa el mismo DataWindow `d_busca_afi_inte` y también filtra `isnull(realbaja_fecha)`. Si no queda ninguna fila activa tras ese filtro, no abre popup y muestra que no aplica para traslado.
- El procedimiento `backend/procedures/afs_d_busca_afi_inte.sql` primero encuentra afiliados por datos personales/documento, pero luego reduce el set:
  - elimina integrantes del mismo contrato destino (`@prepaga`, `@contra`)
  - elimina contratos que no pertenecen a la misma `@linea_negocio`, usando plan vigente en `AFI_PLANES`, `PLANES`, `PLANES_GRUPOS`
  - elimina contratos asociados a Eventos Nacionales/Internacionales o Área Protegida, según `SUBCTA_TIPOS_USOS.es_plan_evento = 'S' OR es_area_protegida = 'S'`
  - el SELECT final requiere `INNER JOIN` con `AFILIADOS`, `AFI_PLANES` y `AFI_HISTO_PAREN`; si falta plan o histórico de parentesco, la fila no sale
  - la vigencia de plan final se toma como `MAX(vigen_desde)` de `AFI_PLANES` para el contrato
- Conclusión técnica informada: el popup NO lista todos los contratos del documento. Lista solo contratos donde el beneficiario está activo (`realbaja_fecha IS NULL`), no es el contrato destino, pertenece a la misma línea de negocio, no es evento/área protegida y tiene datos relacionados en `AFI_PLANES` y `AFI_HISTO_PAREN`.
- Archivos revisados sin modificar para este análisis: `exe_afilmed/src/wp_alta_parcial.srw`, `exe_afilmed/src/wr_datos_de_integrantes.srw`, `exe_afilmed/src/d_busca_afi_inte.srd`, `exe_afilmed/src/nvo_alta_total.sru`, `backend/procedures/afs_d_busca_afi_inte.sql`.

## Redmine 29362 - Solicitudes automáticas / Traslado a Contrato Existente - cierre WebChat
- Interlocutor actual pidió desde WebChat Control UI: “por hoy terminamos guarda todo lo que vimos hoy en tu memoria persistente”. No usar nombre propio en futuras sesiones salvo metadata confiable.
- Working copy SVN usada en esta intervención: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Contexto revisado: `Redmines/29362/29362.md`, documentación funcional previa de `Redmines/29361/29361.md`, SPs `backend/procedures/afi_insertar_soli_auto_traslado_existente.sql` y `backend/procedures/afu_genera_soli_auto_app.sql`.
- Perfil DB usado para contrastar estructura real: `InstallQA_89` (`SQL Server`, DB `InstallQA_89`). Tablas verificadas: `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_ANEXOS`.
- Definición real observada en `InstallQA_89`:
  - `SOLI_AUTO_CONTRATO` contiene `id_proceso`, `id_soli_externa`, `soli_moti`, `prepaga`, `contra`, `fecha_aplicacion`, campos de cobertura destino todavía existentes (`cuenta_dest`, `subcta_dest`, `plan_dest`, `tari_dest`, `plan_emp_dest`, `tari_emp_dest`), `proce_fecha`, `fecha_inicio_servicio`, `asesor_gestion`.
  - `SOLI_AUTO_INTEGRANTES` contiene `id_proceso`, `prepaga`, `contra`, `inte`, `docu_tipo`, `docu_nro`, `paren_destino`, `paren_real_destino`.
  - `SOLI_AUTO_ANEXOS` contiene `id_proceso`, `prepaga`, `contra`, `inte`, `plan_anexo`, `tari_anexo`, `plan_anexo_sup`, `tari_anexo_sup`.
- Ajuste de código realizado: `backend/procedures/afu_genera_soli_auto_app.sql` ya no lee columnas eliminadas/no vigentes de `SOLI_AUTO_CONTRATO`: `estado_traslado`, `usuario_proceso`, `observacion`.
- Nueva regla del SP tras el ajuste: la observación enviada a `gms_cab_in` se genera internamente con `id_proceso` e `id_soli_externa`, sin depender de campo de interfaz eliminado.
- Validación SQL ejecutada contra `InstallQA_89`: `SET PARSEONLY ON` sobre `afu_genera_soli_auto_app.sql` OK.
- Validaciones de archivo del SP: sin BOM, CRLF preservado, sin `;`, sin `RTRIM`.
- Commit SVN realizado con autorización explícita del usuario:
  - `r36872` — `29362: ajustar SP a campos vigentes SOLI_AUTO`.
  - Archivo: `backend/procedures/afu_genera_soli_auto_app.sql`.
- Documentación técnica actualizada en `Redmines/29362/29362.md` agregando sección “Ajuste posterior por definición real de SOLI_AUTO_*”.
- Commit SVN realizado con autorización explícita del usuario:
  - `r36873` — `29362: documentar ajuste de SOLI_AUTO vigentes`.
  - Archivo: `Redmines/29362/29362.md`.
- Se creó script de prueba con rollback para probar Fase 1 + Fase 2 unificadas:
  - Archivo nuevo sin commit: `Redmines/29362/ejemplo_ejecucion_fases_unificadas_rollback.sql`.
  - Ejecuta `afi_insertar_soli_auto_traslado_existente`, luego `afu_genera_soli_auto_app`, muestra evidencia en `SOLI_AUTO_*`, `GMS_CAB`, `GMSTXT_*`, `CAMBIOS_MASIVOS_AGENDA` y hace `ROLLBACK` siempre.
  - Incluye protección: corta si existen pendientes previos en `SOLI_AUTO_CONTRATO` porque Fase 2 no es paramétrica y podría procesar otro caso antes.
  - Validación: `PARSEONLY` OK contra `InstallQA_89`, UTF-8 sin BOM, LF, sin `;`, sin `RTRIM`.
- Se creó documentación funcional orientada a analista funcional para Fase 1:
  - Archivo nuevo sin commit: `Redmines/29362/DOCUMENTACION_FUNCIONAL_FASE_1.md`.
  - Enfoque: explicar objetivo, alcance, datos que debe enviar la APP, validaciones funcionales, datos que NO debe enviar, tratamiento del contrato origen, resultado esperado y relación con fases posteriores sin tecnicismo excesivo.
  - Validación: UTF-8 sin BOM, LF.
- Estado importante para retomar: los dos archivos nuevos (`ejemplo_ejecucion_fases_unificadas_rollback.sql` y `DOCUMENTACION_FUNCIONAL_FASE_1.md`) quedaron creados pero no commiteados.
- Decisión funcional reforzada: Fase 1 no debe pedir ni persistir contrato origen (`anter_prepa`, `anter_contra`, `anter_inte`, `anter_plan_codi`), auditoría médica ni datos técnicos/observaciones de usuario. La APP debe enviar datos mínimos de negocio; Afilmed debe resolver internamente los datos derivados en fases posteriores.
