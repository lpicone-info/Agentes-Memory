# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Savina Varela
- id_googlechat: users/107635148694996802923
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/xeq2hyaaaae

## Resumen de la actividad

### Tema 1: Logica de ProntoPago y link de pagos en Afilmed

- Solicitud o contexto: Savina pidio ordenar la logica funcional para que Afilmed genere ProntoPago y lo publique o integre con un portal propio de Coomeva, y luego pidio revisar en trunk el proceso de link de pagos.
- Respuesta o acciones del agente: Se explico que Afilmed debe seguir generando la deuda/FC/facturacion electronica y publicar la informacion al portal, mientras el portal deberia devolver el estado del pago. En `svarela-Coomeva-trunk` se reviso `afs_link_pagos`, `afu_fact_elect_actualiza_contenido` y `afi_pe_compro_importacion`.
- Resultado: Se concluyo que `afs_link_pagos` busca comprobantes/deudas vigentes y pagables por documento, no genera comprobantes nuevos; que no se encontro pantalla PowerBuilder invocandolo; que la factura electronica embebe un link a Oficina Virtual; y que la importacion de pagos entra por `PE_COMPRO_IMPORTACION` y luego se procesa por batch.
- Estado: Resuelto

### Tema 2: Cantidad de PP por contratante con sucursales distintas

- Solicitud o contexto: Savina pregunto si un contratante con dos contratos en dos sucursales distintas genera uno o dos PP.
- Respuesta o acciones del agente: Se reviso la agrupacion en `afi_fac_compro_a_generar.sql`.
- Resultado: Se explico que genera dos PP si difiere la `suc_comercializable`, porque el agrupador final incluye ese campo; con misma sucursal podria agrupar en un solo PP si no cambian otros criterios.
- Estado: Resuelto

### Tema 3: Actualizacion de rama V104 y XML de traslado a contrato nuevo

- Solicitud o contexto: Savina pidio actualizar su rama `svarela-Version104-Afilmed-SolicitudesAutomaticas` con lo ultimo de V104 y luego con lo ultimo programado por Sebastian, para probar XML de `soli_moti=47` con campos obligatorios y no obligatorios.
- Respuesta o acciones del agente: Se actualizaron working copies a revisiones `37536` y luego `37540`, con estado limpio salvo externos `X`. Se armaron XML de prueba y se corrigio el criterio: para la version vigente no deben enviarse `fecha_aplicacion` ni `fecha_radicacion`; solo `fecha_inicio_servicio`, porque el proceso calcula las otras fechas.
- Resultado: Se explico tambien que `emi_modo` y `emi_grupo` no deben enviarse vacios; para probar autocompletado conviene omitirlos. Al intentar datos nuevos, se aclaro que `soli_moti=47` requiere un afiliado origen vigente, por lo que no puede usarse una persona totalmente nueva sin origen.
- Estado: Resuelto

### Tema 4: Busqueda de caso valido para traslado automatico

- Solicitud o contexto: Savina necesitaba un XML que no fallara por `No existe afiliado origen vigente`, con integrantes existentes, no dados de baja ni ya trasladados, y un `id_soli_externa` nuevo.
- Respuesta o acciones del agente: Se busco en `InstallQA_89` un origen vigente y se ejecuto `afi_insertar_soli_auto` dentro de `BEGIN TRAN / ROLLBACK`.
- Resultado: Se encontro y valido un caso con `id_soli_externa=293631201`, `id_proceso=177`, sin grabacion persistente. Luego se observo que el contrato generado `1073920` habia impactado campaña `134`, lo que llevo a detectar diferencia con la ventana manual.
- Estado: Resuelto

### Tema 5: Campania en traslado a contrato nuevo

- Solicitud o contexto: Savina pregunto si en la ventana manual de Traslado a Contrato Nuevo se podia seleccionar campaña, luego de ver campaña asociada al contrato `1073920`.
- Respuesta o acciones del agente: Se reviso `wp_alta_total.srw` y se verifico que para `ii_soli_moti = 47` los controles de campaña se ocultan.
- Resultado: Se concluyo que la ventana manual no permite seleccionar campaña, mientras que el flujo automatico XML si la estaba aceptando e impactando. Esto quedo registrado como diferencia funcional relevante para corregir.
- Estado: Resuelto

### Tema 6: Reprueba de edad minima por plan origen/destino

- Solicitud o contexto: Savina pidio un XML con la misma logica del `id_proceso=156`, donde se habia probado la validacion de edad minima cuando plan origen y destino eran iguales.
- Respuesta o acciones del agente: Se armo y probo en `InstallQA_89` un XML con `id_soli_externa=5025`, dentro de rollback, para validar que ya no aparecieran los rechazos de edad `8086` y `8142`.
- Resultado: La prueba genero `id_proceso=179`, `cab_id=295865`, `canti_errores=0`, `puede_procesarse=S` y sin registros en `GMS_ERR`. Despues se detecto que el XML no incluia responsable pagador, por lo que no cargaba la solapa Datos adicionales.
- Estado: Resuelto

### Tema 7: Responsable pagador y Datos adicionales en traslado nuevo

- Solicitud o contexto: Savina detecto que sin responsable pagador no se cargaba la pestaña Datos adicionales, pidio revisar el contrato generado por `id_proceso=179` y saber donde se graban esos datos en la ventana manual de Traslado a Contrato Nuevo.
- Respuesta o acciones del agente: Se reviso el proceso `179`: genero contrato `1073921`, solicitud `4677152`, `GMS_CAB=295867`, estado `B`, sin errores, pero sin datos en `SOLI_AUTO_RESP_PAGADOR`, `GMSTXT_RESP_PAGADOR`, `GMSDEF_RESP_PAGADOR`, `SOLI_RESP_PAGADOR` ni `AFI_RESP_PAGADOR`. Tambien se analizo la ventana `wp_alta_total`.
- Resultado: Se explico que la ventana manual toma datos desde `idw_resp_pagador`, llama a `wf_grabar_soli_resp_pagador`, graba en `SOLI_RESP_PAGADOR` y tablas de domicilio/email/telefono, y al impacto pasa a `AFI_RESP_PAGADOR`. Para el flujo XML se graba primero en `SOLI_AUTO_RESP_PAGADOR` y luego via GMS/SOLI/AFI. Se concluyo que en automatico `soli_moti=47` faltaba hacer obligatorio el nodo `<responsable_pagador>`.
- Estado: Resuelto

### Tema 8: Diferencias entre proceso automatico y ventana manual

- Solicitud o contexto: Savina pidio listar diferencias actuales entre traslado a contrato nuevo automatico y manual, porque las pruebas revelaban diferencias reiteradas en campañas, edad, facturacion y otras validaciones.
- Respuesta o acciones del agente: Se preparo un listado de diferencias confirmadas: campañas, responsable pagador, contactos del responsable, facturacion (`emi_grupo`, `emi_modo`, `vto_grupo`), fechas, edad por grupo de plan, datos del integrante, EPS/obra social, preexistencias/auditoria, programas anexos/adicionales y reglas que la ventana ejecuta de forma distinta al XML.
- Resultado: Quedo identificado que ambos circuitos no eran equivalentes y que las diferencias debian tratarse como matriz de pruebas/correcciones para cerrar el Redmine sin seguir encontrando casos aislados.
- Estado: En curso
