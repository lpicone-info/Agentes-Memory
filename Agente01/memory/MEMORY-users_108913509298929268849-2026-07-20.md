# Memoria curada diaria

- Fecha: 2026-07-20
- Usuario: Sebastián Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Redmine 29362 - contexto del caso 1006 y anexos obligatorios

- Solicitud o contexto: Sebastián pidió ponerse en contexto del Redmine `29362`, desarrollo de Solicitudes Automáticas para traslado a contrato existente `soli_moti = 48`, en el repo `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Respuesta o acciones del agente: Se reconstruyó el contexto del caso `1006`: `id_proceso = 110`, `GMS_CAB = 295729`, solicitud `4677084`, destino `1000431`, origen `145539`. Se observó que la solicitud salió OK pero `SOLI_ADICIONALES` quedó sin registros, a pesar de existir configuración de anexo obligatorio `TEACS / TEACS811035741`.
- Resultado: Se concluyó que el caso `1006` era evidencia del problema original antes del ajuste, porque corrió el `15/07` y el SP actual con el bloque `29362 CASO 6` estaba recreado en QA el `16/07 19:37`. Se indicó que el caso `1008` no validaba anexos si no generaba solicitud.
- Estado: Resuelto

### Tema 2: respuesta para QA sobre el caso 1006 y precisión de fechas

- Solicitud o contexto: Sebastián pidió qué responderle a QA y luego pidió seguridad sobre la afirmación de que el caso se había probado antes del fix.
- Respuesta o acciones del agente: Se propuso una respuesta que reconocía el caso `1006` como válido para evidenciar el problema original y aclaraba que fue ejecutado antes del ajuste identificado. Ante la respuesta de QA, se ajustó la recomendación para no discutir el viernes y separar casos: `1006` pre-fix, `1008` sin solicitud generada, y cualquier otro caso posterior requería identificador.
- Resultado: Quedó una respuesta más precisa: si existía otro caso posterior al ajuste que sí generó solicitud y no cargó anexos, debía analizarse como evidencia nueva con `id_proceso`, `GMS_CAB` o solicitud.
- Estado: Resuelto

### Tema 3: caso 1008 - rechazo en GMS y errores visibles

- Solicitud o contexto: Sebastián aclaró que el caso correcto era `1008` y luego preguntó si el rechazo debería informar errores al usuario.
- Respuesta o acciones del agente: Se revisó que el XML `1008` quedó en `id_proceso = 122`, `GMS_CAB = 295765`, estado `E`, `puede_procesarse = N`, sin solicitud generada. Se indicó que la información de errores existía en base y podía consultarse con `dbo.gms_listar_resultado_gen @prm_cab_id = 295765, @prm_tipo = 'E', @prm_etapa = 2`.
- Resultado: Se concluyó que el error no faltaba en base: faltaba confirmar si la capa APP/integración consultaba y mostraba el resultado posterior del proceso GMS, en lugar de quedarse solo con `XML OK` o `id_proceso generado`.
- Estado: Resuelto

### Tema 4: consulta para demostrar datos incompletos y causa del error de afiliado inexistente

- Solicitud o contexto: Sebastián pidió una consulta que demostrara qué datos estaban incompletos en los integrantes. Luego mostró resultados donde aparecía `Afiliado inexistente` para el documento `1247049144`, aunque él estaba verificando otro afiliado.
- Respuesta o acciones del agente: Se entregó una consulta sobre `GMS_CAB`, `GMSDEF_INTEGRANTES`, `GMSDEF_DOMICILIOS`, `GMSDEF_TELEFONOS`, `GMSDEF_EMAILS`, `GMS_ERR` y `RECHA_MOTIVOS`, mostrando origen completo y contactos por integrante. Al revisar la salida, se explicó que el error `Afiliado inexistente` correspondía al `inte 01`, documento `1247049144`, con `anter_prepa`, `anter_contra` y `anter_inte` nulos, no a los trasladados del XML.
- Resultado: Se detectó que el proceso agregaba internamente el titular vigente del contrato destino `1000431` al armar `GMSTXT/GMSDEF_INTEGRANTES`, y luego `gmsdef_val_traslado_web` lo validaba como si fuera un trasladado con origen obligatorio.
- Estado: Resuelto

### Tema 5: caso 1008 - bug de validación del titular destino técnico

- Solicitud o contexto: Sebastián mostró el XML del `1008`, que traía solo dos integrantes trasladados: `CC 71656954` desde origen `160053 / inte 02` y `TI 1017928396` desde origen `160053 / inte 03`. Preguntó por qué aparecía el DNI `1247049144`.
- Respuesta o acciones del agente: Se explicó que, para `soli_moti = 48`, `afu_genera_soli_auto_app` inserta internamente el titular vigente del contrato destino en `GMSTXT_INTEGRANTES`, tomando `AFI.inte = dbo.afn_inte_tit(...)` y dejando `anter_prepa`, `anter_contra`, `anter_inte` en `NULL`. Después `gmstxt_pasar_a_def` copia esa fila a `GMSDEF_INTEGRANTES`, y `gmsdef_val_traslado_web` validaba todas las filas `soli_moti IN (47,48)`.
- Resultado: Se diagnosticó un bug de validación: para traslado a contrato existente, las validaciones de origen completo debían aplicar solo a los integrantes trasladados, no al titular ya existente del contrato destino.
- Estado: Resuelto

### Tema 6: ajuste de `gmsdef_val_traslado_web` y commits SVN

- Solicitud o contexto: Sebastián pidió analizar qué SP modificar y recordó la regla funcional: en traslado a contrato existente no se puede enviar un integrante titular del contrato origen ni modificar el titular del contrato destino. Luego pidió aplicar cambios y commitearlos.
- Respuesta o acciones del agente: Se identificó como punto de ajuste principal `dbo.gmsdef_val_traslado_web`, con contexto en `afu_genera_soli_auto_app` y `gmstxt_pasar_a_def`. Se modificó `gmsdef_val_traslado_web.sql` para excluir la fila técnica del titular vigente destino cuando `soli_moti = 48`, `paren = titular` y no trae origen anterior, manteniendo activa la restricción contra trasladar titulares.
- Resultado: Se realizaron los commits SVN `r37378`, que excluyó del validador la fila técnica del titular vigente destino en traslado a contrato existente, y `r37379`, que agregó validación explícita para rechazar titular del contrato origen en `soli_moti = 48`. Se validaron diff SVN, commit, encoding `us-ascii`, ausencia de BOM y CRLF preservado.
- Estado: Resuelto

### Tema 7: validez del documento `TI` en traslado

- Solicitud o contexto: Sebastián preguntó si `docu_tipo = TI` era válido para traslado.
- Respuesta o acciones del agente: Se verificó que `TI` existe como `Tarjeta de Identidad` en `DOCU_TIPOS`, con `baja_fecha = NULL`.
- Resultado: Se concluyó que `TI` es válido para traslado si cumple las demás reglas: coincidencia con afiliado origen y compatibilidad con parentesco/edad. Para un integrante normal no titular, `TI` pasa si coincide con el origen.
- Estado: Resuelto

### Tema 8: diferencia entre `fecha_aplicacion` y `fecha_inicio_servicio`

- Solicitud o contexto: Sebastián preguntó la diferencia entre `fecha_aplicacion` y `fecha_inicio_servicio`, y si podían ser distintas.
- Respuesta o acciones del agente: Se explicó que `fecha_aplicacion` es la fecha efectiva administrativa del traslado y gobierna validaciones de vigencia, origen, destino, plan, tarifa, bajas y generación GMS. `fecha_inicio_servicio` es la fecha funcional de inicio de servicio/cobertura, guardada en `SOLI_AUTO_CONTRATO.fecha_inicio_servicio`.
- Resultado: Se concluyó que pueden ser distintas conceptualmente y el código de `soli_moti = 48` no exige igualdad, pero si se diferencian hay que asumir que las validaciones principales se gobiernan por `fecha_aplicacion`, no por `fecha_inicio_servicio`.
- Estado: Resuelto

### Tema 9: caso 1009 - origen real de contactos y datos faltantes

- Solicitud o contexto: Sebastián volvió al caso `1009`, donde el proceso devolvía errores de datos faltantes aunque él veía datos en `registro_unico`, `registro_unico_domicilios`, `registro_unico_telefonos` y `registro_unico_emails` para los códigos de persona `246923` y `303866`.
- Respuesta o acciones del agente: Se explicó que los errores finales `29301`, `29302` y `29303` no miran directamente `registro_unico*`, sino `GMSDEF_DOMICILIOS`, `GMSDEF_TELEFONOS` y `GMSDEF_EMAILS`. Registro Unico se usa antes como fallback en `afu_soli_auto_app_contactos_gmstxt`: arma `#MAP_INTEGRANTES` desde `SOLI_AUTO_INTEGRANTES`, matchea contra `AFILIADOS` por origen y documento, toma `AFILIADOS.codigo_unico_persona` y recién con ese código busca contactos vigentes en Registro Unico.
- Resultado: Se concluyó que si los datos existen en Registro Unico para `246923` y `303866`, pero `AFILIADOS.codigo_unico_persona` del afiliado origen no apunta a esos códigos, el proceso no los encuentra. También se observó que, después del ajuste anterior, el bloque de contactos todavía podía validar al titular destino técnico como si fuera trasladado, por lo que habría que aplicar el mismo criterio de exclusión para contactos obligatorios del titular destino técnico en `soli_moti = 48`.
- Estado: Pendiente
