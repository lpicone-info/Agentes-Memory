
## 2026-06-05 15:08 UTC - SVN APP/AU facturacion y reverso

- Contexto: trabajo sobre caso 1002073 en version 104. Se venia investigando por que proceso APP/AU fallaba al reversar mayo/junio y luego al facturar con DIAN, comparado contra ventana.
- Se actualizo working copy a r37062 y se hizo commit r37063 `Corrige reversion y prefijo APP AU` inicialmente con cambios en `backend/procedures/afi_compro_reverso_det.sql` y `backend/procedures/afi_fac_compro_a_generar.sql`.
- Conversacion posterior: el usuario no queria tocar SP comun de facturacion si el problema era que APP/AU no enviaba datos que ventana si manda. Se explico que ventana envia `punto_vta`, tipo debito y tipo credito a `f_facturacion_facturar`, mientras APP/AU mandaba nulos.
- Se corrigio enfoque DIAN: se revirtio el cambio en `afi_fac_compro_a_generar.sql`; se agrego `afs_soli_auto_app_fact_datos_facturacion.sql` para resolver defaults APP/AU (`punto_vta`, `compro_tipo_debito`, `compro_tipo_credito`); y se modifico `wp_main_super_batch.srw` para pasar esos valores a `f_facturacion_facturar` en vez de nulos. Commit SVN r37064 `Pasa datos facturacion APP AU`.
- Sobre reverso: se identifico diferencia con ventana. Ambos cargan mayo y junio en `#REV_COMPRO_A_REVERSAR`, pero ventana manual usa `afu_rev_compro_a_rev_incluir` y no carga `agrupador`, mientras APP/AU usa `afu_soli_auto_app_fact_rev_incluir` y carga `agrupador = COM.agrupador` (caso mayo/junio queda 0). La validacion de `afi_compro_reverso_det` aplica con `REV.agrupador = 0`, por eso APP/AU rechaza mayo al ver junio posterior; ventana no entra por `agrupador NULL`. El cambio en `afi_compro_reverso_det` corrige la regla para no bloquear periodo anterior si el posterior esta en el mismo lote temporal y no rechazado, con reevaluacion si el posterior falla.
- El usuario pidio verificar script de prueba en carpeta de Gabriel `secuencia_wf_proceso_facturacion_app_au_1002073.sql`. Se actualizo porque seguia facturando con datos nulos.
- Cambios al script de prueba de Gabriel: ahora ejecuta `afs_soli_auto_app_fact_datos_facturacion`, muestra diagnostico `DATOS_FACTURACION_APP_AU`, pasa `punto_vta`, `compro_tipo_debito`, `compro_tipo_credito` a facturacion, saco un `FETCH NEXT` ubicado despues de reversar y antes de facturar porque podia mezclar la solicitud siguiente, y agrego precondicion de compilacion para `afi_compro_reverso_det` y `afs_soli_auto_app_fact_datos_facturacion`.
- Validaciones reportadas: sintaxis SQL con `NOEXEC` OK para el script de Gabriel; para r37064 se valido que defaults devolvian `SCMP`, `FC`, `NC`, se preservaron CRLF/BOM, y `svn status` final limpio salvo externals habituales.
## 2026-06-05 15:08 UTC - SVN APP/AU facturacion y reverso

- Contexto: trabajo sobre caso 1002073 en version 104. Se venia investigando por que proceso APP/AU fallaba al reversar mayo/junio y luego al facturar con DIAN, comparado contra ventana.
- Se actualizo working copy a r37062 y se hizo commit r37063 `Corrige reversion y prefijo APP AU` inicialmente con cambios en `backend/procedures/afi_compro_reverso_det.sql` y `backend/procedures/afi_fac_compro_a_generar.sql`.
- Conversacion posterior: el usuario no queria tocar SP comun de facturacion si el problema era que APP/AU no enviaba datos que ventana si manda. Se explico que ventana envia `punto_vta`, tipo debito y tipo credito a `f_facturacion_facturar`, mientras APP/AU mandaba nulos.
- Se corrigio enfoque DIAN: se revirtio el cambio en `afi_fac_compro_a_generar.sql`; se agrego `afs_soli_auto_app_fact_datos_facturacion.sql` para resolver defaults APP/AU (`punto_vta`, `compro_tipo_debito`, `compro_tipo_credito`); y se modifico `wp_main_super_batch.srw` para pasar esos valores a `f_facturacion_facturar` en vez de nulos. Commit SVN r37064 `Pasa datos facturacion APP AU`.
- Sobre reverso: se identifico diferencia con ventana. Ambos cargan mayo y junio en `#REV_COMPRO_A_REVERSAR`, pero ventana manual usa `afu_rev_compro_a_rev_incluir` y no carga `agrupador`, mientras APP/AU usa `afu_soli_auto_app_fact_rev_incluir` y carga `agrupador = COM.agrupador` (caso mayo/junio queda 0). La validacion de `afi_compro_reverso_det` aplica con `REV.agrupador = 0`, por eso APP/AU rechaza mayo al ver junio posterior; ventana no entra por `agrupador NULL`. El cambio en `afi_compro_reverso_det` corrige la regla para no bloquear periodo anterior si el posterior esta en el mismo lote temporal y no rechazado, con reevaluacion si el posterior falla.
- El usuario pidio verificar script de prueba en carpeta de Gabriel `secuencia_wf_proceso_facturacion_app_au_1002073.sql`. Se actualizo porque seguia facturando con datos nulos.
- Cambios al script de prueba de Gabriel: ahora ejecuta `afs_soli_auto_app_fact_datos_facturacion`, muestra diagnostico `DATOS_FACTURACION_APP_AU`, pasa `punto_vta`, `compro_tipo_debito`, `compro_tipo_credito` a facturacion, saco un `FETCH NEXT` ubicado despues de reversar y antes de facturar porque podia mezclar la solicitud siguiente, y agrego precondicion de compilacion para `afi_compro_reverso_det` y `afs_soli_auto_app_fact_datos_facturacion`.
- Validaciones reportadas: sintaxis SQL con `NOEXEC` OK para el script de Gabriel; para r37064 se valido que defaults devolvian `SCMP`, `FC`, `NC`, se preservaron CRLF/BOM, y `svn status` final limpio salvo externals habituales.

## 2026-06-05 15:11 UTC - Primera cuota, SL/RC, PRP y facturacion masiva

- Conversacion funcional sobre generacion de primera cuota desde Venta Nueva en documentacion SVN de Facturacion.
- Documentos/evidencia usados previamente: `A-04-DEF Facturacion 3 - Primera Cuota y Cuota Inscrip_V14.docx`, `A-04-DEF Facturacion 3 - Primera Cuota y Cuota Inscrip - Cambios_V05.docx`, `P-04-DEF Facturacion 4 - Prefacturacion_V24.docx`, `A-04-DEF Facturacion 3 - Facturacion Masiva_V18.3.docx`, `A-04-DEF Facturacion 4 - Prefacturacion_V25.docx`, `AFILMED_PARAMS.sql`, `afi_fac_prefac_ajustes.sql`, y `_Scripts/CONSULTORIA/17. Pago de primera cuota.sql`.
- Regla documentada: primera cuota SL calcula tarifa, descuentos, adicional sexo/edad y descuentos por campania; excluye proporcionalidad, descuentos EPS, subsidios, recargos, bonificaciones, licencias y retenciones.
- Para corte 5, aplicacion/inicio servicio 21/05/2026: inicio normal del ciclo 06/05/2026, diferencia -15 dias, PRP esperado -50% de la cuota mensual. Concepto CUO queda cuota completa y PRP ajusta proporcionalidad cuando aplica.
- Caso RC: un RC del 19/05/2026 ingreso como recibo sin identificar; al impactar contrato el 21/05/2026 se identifico en `COMPRO_IMPORTACION_DET` en segunda secuencia/sub_item y quedo aplicado parcialmente contra PRP, dejando saldo PRP. Se explico que esto es aplicacion de recaudo/identificacion, no facturacion masiva.
- Interpretacion dada: al usar RC en vez de SL, no se genera SL; el contrato puede ingresar sin inhabilitacion inicial por primera cuota porque existe recaudo asociado. Si el RC parcial no cubre el proporcional, el saldo queda como cartera/deuda PRP.
- Sobre por que no entro en masiva del 02/06/2026: la masiva no busca saldos PRP abiertos para completarlos ni reabre un caso ya tratado por identificacion de RC + aplicacion PRP. La individual pudo forzar la facturacion del contrato/periodo para generar CUO del mes y tomar PRP adeudado.
- Sobre campania: si se genera SL desde boton `Generar Comp.` sin RC previo, el SL si debe tomar descuento por campania vigente/aplicable, pero no debe incluir PRP dentro del SL. No se encontro cambio posterior que indique que PRP se haya incluido en SL; `Prefacturacion_V25` modifica logica de liquidacion/tarifa de primera cuota SL para fin de anio/vencimiento diciembre, especialmente cortes 5/10, pero mantiene proporcionalidad como proceso separado.
- Matiz importante para continuar: aunque la ventana permita tildar proporcionalidad con SL o con RC, en SL el tilde no implica que PRP se incluya dentro del comprobante SL; con RC puede derivar en aplicacion/saldo PRP durante identificacion o por facturacion posterior segun datos existentes.


## 2026-06-05 16:44 UTC - Durable Work Notes

- After user reported SQL Server error `Msg 8154 ... procedimiento afi_compro_reverso_det ... The table '#REV_COMPRO_A_REVERSAR' is ambiguous`, fixed `afi_compro_reverso_det` by changing ambiguous `UPDATE #REV_COMPRO_A_REVERSAR` statements to `UPDATE REV` in both posterior-period validation blocks where the temp table is also referenced with aliases `REV` and `REV_POST`. Validated syntax with `NOEXEC`. Committed SVN revision `r37065`.
- Explained SVN revision `r37062` to user: it was not our APP/AU change; log message `29362 Ajusta consulta detalle traslado existente`; touched `afs_datos_afi_titular_soli.sql` and `afs_datos_titular_grupo_soli.sql`. Functional purpose: support `soli_moti = 48` traslado a contrato existente by falling back to the current titular in `AFILIADOS` when no titular is materialized in `SOLI_AFILIADOS` with `cambia_paren = 'T'`.
- User `gpulka` asked to update Redmine `29361` docs and generate functional documentation by phases from existing `29361.md` plus attached Drive draft. Updated `Redmines/29361/29361.md` with analyst-oriented phases: Fase 1 XML APP reception and `SOLI_AUTO_*` tables, Fase 2 GMS generation, Fase 3 AU batch, Fase 4 external-state consultation, Fase 5 APP/AU economic/factoring normalization including latest reversal and billing-data changes.
- Generated Word document in Gabriel folder named `29361_documentacion_funcional_solicitudes_app_au.docx`. Verified it is Word 2007+ format. Committed SVN revision `r37066` with message `29361 Actualiza documentacion funcional APP AU`. Working copy ended clean except habitual externals.

## 2026-06-05 18:50 UTC - APP/AU colectivos, prefacturacion e integrantes

- Para Redmine 29361 se agrego parametro `soli_auto.procesa_colectivos` en `backend/data_modifications/AFILMED_PARAMS.sql`, valor inicial `N`, para habilitar/deshabilitar procesamiento de contratos colectivos en circuito APP/`SOLI_AUTO`.
- En `afi_insertar_soli_auto` se implemento freno temprano antes de insertar en `SOLI_AUTO_*`: si el parametro esta en `N` y el contrato/cuenta corresponde a colectivo, se bloquea con `RAISERROR`. Para contratos existentes toma el contrato informado en XML y valida subcuenta vigente; para futuros casos sin contrato usa `cuenta_dest` o `cuenta` como fallback. Validado con SQL Server `SET NOEXEC ON`. Commit SVN `r37067` mensaje `29361 Parametriza colectivos soli auto APP`.
- Luego el usuario pidio update de working copy `gpulka-Version104-Afilmed-SolicitudesAutomaticas`; quedo actualizada a `r37070` antes de modificar.
- En `wp_pre_factu_config_batch` se incorporo proceso de facturacion APP/AU `AUFACT` en las dos DataWindows de configuracion batch, visible como `Facturacion Traslados APP/AU` (terminologia funcional para cliente: traslados). Tambien se agrego `AUFACT` en las dos DataWindows de mails para que el proceso aparezca correctamente al abrir la ventana de mail. Se reviso grabacion/validaciones especiales y no se detecto ajuste adicional necesario fuera de incluir el proceso en las listas/DW correspondientes.
- El usuario ya habia ajustado `afs_pre_factu_config_batch` para traer el proceso; se reviso ese cambio como base.
- Investigacion en `wp_consulta_afiliados` / `d_lista_solicitudes_grupo`: el campo integrantes para solicitudes de cambio de cobertura depende de `SOLI_AFILIADOS.inte_modif`. En `wp_cambio_clase` se marca `inte_modif = 'S'` para los integrantes incluidos en cambio de cobertura. Nuestro flujo APP estaba generando mal ese valor con `N`.
- Se ajusto `gms_genera_solicitudes.sql`: para `soli_moti = 6` (cambio de cobertura), todos los integrantes se insertan/marcan con `inte_modif = 'S'`, alineado con `wp_cambio_clase`, para que `d_lista_solicitudes_grupo` muestre integrantes tipo `01/02/...`.
- Validaciones: `gms_genera_solicitudes.sql` validado con SQL Server `SET NOEXEC ON`; DW revisadas por diff/grep, preservando encoding/BOM/CRLF; no se compilo en IDE PowerBuilder. Commit SVN `r37071` mensaje `29361 Ajusta AUFACT e integrantes cambio cobertura APP`. Working copy final limpia salvo externals habituales `X`.

## 2026-06-05 21:42 UTC - Superbatch/batchsolicitudes PBR y APP/AU CO multi-periodo

- Se actualizo el DOCX funcional `29361_documentacion_funcional_solicitudes_app_au.docx` en carpeta de Gabriel incorporando `AFILMED_PARAMS` `soli_auto.procesa_colectivos`, proceso `AUFACT` como `Facturacion Traslados`, configuracion `wp_pre_factu_config_batch`, mails, DataWindows batch/mails, regla `inte_modif = S` para cambio de cobertura, objetos y casos minimos. Se valido integridad DOCX con `unzip -t` y que no quedara texto `Facturacion APP/AU` salvo el titulo historico.
- Revision superbatch: target `exe_afilmed/src/superbatch.pbt` incluye `superbatch.pbl`, `entidades.pbl`, `batch_general.pbl`, `batch_objetos_compartidos.pbl`, `temptable.pbl`, `appeon_workarounds.pbl`. No hubo faltantes criticos para `AUFACT`; entradas de `exe_afilmed/pbls/pic/superbatch.pbr` apuntan a PBLs dentro del target. `d_proceso_agenda_superbatch` y `d_fac_config_batch_mail_vigentes` no estan en PBR pero si en `superbatch.pbg`/`superbatch.srj` como objetos estaticos de `wp_main_super_batch`.
- Para circuito `AUFACT`, DataObjects indirectos cubiertos por PBR: `d_proceso_temp_table`, `d_zz_script_tablas_temp`, `d_resumen_contratos_con_errores`, `d_contratos_con_errores`, `d_errores_facturacion`, `d_fac_resolucion_dian_vigente`, `d_fac_resolucion_dian_vigente_mail`.
- Revision `batchsolicitudes`: target `batchsolicitudes.pbt` no incluye `solicitudes_dw_mas.pbl`, pero `batchsolicitudes.pbr` tenia entradas a esa PBL para `d_baja_mas_solis_generadas`, `d_alta_mas_solis_generadas`, `d_cambio_cobertura_solis_generadas`. Esos DataObjects tambien existen en `batch_objetos_compartidos.pbl`, que si esta en target. Recomendacion dada: corregir PBR para apuntar a `batch_objetos_compartidos.pbl` y revisar/regenerar `batchsolicitudes.srj` porque conserva referencias viejas a `solicitudes_dw_mas.pbl`. No se hicieron cambios en esa revision.
- Error log `No existe DataObject de exportacion para la transaccion: INC`: se encontro en `wp_main_solicitudes.srw` el `CHOOSE CASE` que solo mapea `AT`, `BT`, `CC`. Traslado existente motivo `48` se mapea a `INCL`, pero variables/columnas `GMS_CAB.transaccion` son `VARCHAR(3)`, quedando truncado como `INC`. Conclusion dada: error por falta de mapeo de exportacion para `INC`, no por PBR directamente; si se agrega soporte, el DataObject debe estar en PBL incluida en target, ideal `batch_objetos_compartidos.pbl`.
- Usuario ejecuto por sistema facturacion APP/AU para contrato `1002073`, solicitud `4676721`. Log: reverso lote `197858`, prefactura `1119995`, proceso reporto OK. Analisis: no fue solo reverso; reverso mayo y junio, pero facturo de nuevo solo mayo. Prefactura quedo con `cuotas = 2` y rango mayo-junio, pero detalle/comprobantes generados solo mayo.
- Causa tecnica: facturacion individual contratante `CO` no propagaba `periodo_hasta`; aunque llegaban 2 cuotas, la verificacion armaba detalle como un solo periodo. Se corrigio `backend/procedures/afi_verif_contra_prefactura.sql` para que `CO` use el rango de cuotas en este flujo multi-periodo.
- Commit SVN realizado: `r37073 | 29361 Ajusta CO multi periodo traslados APP`. Validaciones: SQL `NOEXEC` OK, encoding ISO-8859-1 y CRLF preservados, working copy limpia salvo externals habituales. Importante operativo: caso ya ejecutado `4676721` no se repara automaticamente; junio quedo reversado y sin nueva facturacion, requiere correccion operativa/base despues de aplicar script.

## 2026-06-05 - Solicitudes APP/AU, AUFACT y analisis contactos
- SVN r37075 en gpulka-Version104-Afilmed-SolicitudesAutomaticas: ajuste quirurgico en exe_afilmed/src/wp_main_super_batch.srw para reiniciar uo_temptables_process despues de dropear temporales de verificacion y facturacion en cada periodo AUFACT. Mantiene SP compartidos de facturacion sin cambios y evita estado interno remanente entre meses.
- Estrategia de facturacion AUFACT vigente: afi_verif_contra_prefactura quedo con comportamiento historico; el multi-periodo para destino afiliado se aislo en wp_main_super_batch, facturando CO mes por mes, con una prefactura por periodo y la ultima prefactura registrada en la traza.
- Documento funcional actualizado: /mnt/Agentes/Gabriel/29361_documentacion_funcional_solicitudes_app_au.docx, agregando resguardo de procesamiento mensual y temporales.
- Documento nuevo creado: /mnt/Agentes/Gabriel/29361_analisis_traslado_app_contactos.docx. Analiza funcional y tecnicamente la incorporacion de domicilios, telefonos y mails para traslados APP a contrato existente, con prioridad XML sobre Registro Unico, fallback a Registro Unico y rechazo GMS_ERR si falta informacion.
- Hallazgos para futuro desarrollo contactos: wp_alta_parcial recupera Registro Unico via nvo_registro_unico_persona; afu_genera_soli_auto_app hoy solo genera clase plan, integrantes y adicionales para APP; hay que agregar interfaz SOLI_AUTO_DOMICILIOS/TELEFONOS/EMAILS o equivalente, poblar GMSTXT_DOMICILIOS/TELEFONOS/EMAILS y revisar validadores gmsdef_val_telefonos/gmsdef_val_mails para alcance INCL sin afectar alta total.
