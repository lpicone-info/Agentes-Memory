
## 2026-09-02 10:25 ART - Redmine #29364

- Usuario retomó caso #29364: consulta por solicitud automática `Cambio de datos generales beneficiarios` `4677229`, generada junto a `Cambio de Titularidad` `4677228`, contrato `117329`, donde al consultar mostraba `El contrato ingresado no existe`.
- Diagnóstico realizado en `InstallQA_89` y working copy `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`: el contrato y la solicitud existían; la pantalla fallaba porque `d_datos_titular_grupo_soli`/`afs_datos_titular_grupo_soli.sql` busca `SOLI_AFILIADOS` con `sa.soli = @soli AND sa.cambia_paren = 'T'`.
- En caso defectuoso `4677229`, `SOLI_AFILIADOS` tenía `inte 03`, `paren = T`, `cambia_paren = H`, `paren_real = T`, `paren_real_ant = H`; por eso no recuperaba titular. En caso testigo correcto `4371745`, `cambia_paren = T`.
- Causa raíz ubicada en `backend/procedures/afi_genera_soli_auto_cambio_datos_parentescos.sql`: al generar la solicitud automática hija se calculaba `@paren = 'T'`, pero se enviaba `@cambia_paren = @csr_paren` (parentesco anterior).
- Se modificó `backend/procedures/afi_genera_soli_auto_cambio_datos_parentescos.sql` para que cuando el `paren_nuevo` sea titular se grabe `cambia_paren = 'T'` en la automática hija de datos generales, dejando la solicitud consultable por la pantalla.
- Commit SVN `r37726`: `Refs #29364 corrige titular en solicitud automatica de datos generales`.
- Usuario pidió compilar en la 89, pero luego indicó `no, ya lo hago yo`; no se ejecutó compilación ni cambio de datos en base desde el agente.
- Usuario reportó error de compilación SQL Server: `Mensaje 156... Incorrect syntax near the keyword 'CASE'`, procedimiento `afi_genera_soli_auto_cambio_datos_parentescos`, línea 361.
- Corrección aplicada: SQL Server no aceptaba el `CASE` directo dentro del parámetro del `EXECUTE`; se reemplazó por variable previa `@var_cambia_paren_soli` y luego se pasó esa variable al `EXECUTE`.
- Commit SVN `r37727`: `Refs #29364 corrige sintaxis de cambio parentesco automatico`.
- Validaciones comunicadas al usuario: archivo ASCII, sin BOM, CRLF homogéneo, sin `;` ni `RTRIM`, working copy limpia para archivos propios con solo externos `X` habituales.

## 2026-09-02 10:42 ART - Reporte Datos Contacto Activacion DA / GMS FP

- Usuario consulto en trunk `svarela-Coomeva-trunk` y BD `PruebasCoreINFOMEDICAL` sobre `NOVEDADES_DA_IMPOR_TEMP` y `GMS_CAB`.
- Se relevo `GMS_CAB`: cabecera/lote del proceso GMS de generacion masiva de solicitudes. Campos clave: `id`, `soli_moti`, `transaccion`, `estado`, `nombre_archivo`, `canti_errores`, `canti_warnings`, `puede_procesarse`, `fecha_desde`, `fecha_hasta`, `tipo_archivo`, `cantidad_a_procesar`, `apli_fecha`, `canti_errores_impor`, `genera_credencial`, `linea_negocio`, `ultim_expor_fecha`. En BD habia 329.749 registros. Transacciones observadas: `AT`, `BT`, `CC`, `FP`, `CA`.
- Se analizo `afs_reporte_datos_contacto_activacion_da.sql`: el reporte toma origen `NOV` desde `NOVEDADES_DA_IMPOR_TEMP` y origen `GMS` desde `GMS_CAB` + `GMSDEF_MULTI`, filtrando `C.transaccion = 'FP'`, `C.fecha_hasta IS NOT NULL`, `C.estado = 'B'` y `M.contra <> ''`.
- Conclusion funcional comunicada: una solicitud de cambio de forma de pago aparece en el reporte solo si quedo en el circuito GMS/importacion masiva (`GMS_CAB/GMSDEF_MULTI` FP). Si fue solicitud manual/directa que solo grabo `SOLICITUDES`/`SOLI_PAGO_FORMAS`, el reporte no la levanta.
- No se encontro EF dentro del trunk para ese comportamiento; la especificacion de hecho esta en el SP del reporte y objetos PB: `wp_reporte_datos_contacto_activacion_da.srw`, `d_reporte_datos_contacto_activacion_da.srd`, `de_reporte_datos_contacto_activacion_da.srd`.
- Se busco el Redmine de desarrollo del reporte: RM `#25132`, titulo `Solicitudes / Reportes / Nueva Funcionalidad --> Reporte Datos de Contacto Activacion al Debito Automatico`, proyecto `COOMEVA - Desarrollos Facturable`, estado `Cerrado`, version `QA77 - ENTREGA 10/2024`, creado `21/11/2023`, cerrado `19/11/2024`.
- Primeros commits SVN del reporte: `r34268` y `r34271` del `25/10/2024`, desarrollador `gpulka`. SP tuvo commits `r34271`, `r34289`, `r34291`, `r34327`, `r34335`, `r34341`, `r34343`, todos contra `#25132`.
- RM `#25132` adjunta `7184_RQ Reporte.Datos.contacto.Activacion.Debitos.Automaticos.docx` y referencia DEF `A-04-DEF Reportes y Consultas E2 - Reportes Solicitudes_V12.1.docx`, punto `4.17`.
- El adjunto especifica dos circuitos: Banco Occidente por `Solicitudes/Novedades debito automatico/Importacion de Novedades` y Bancolombia por `Solicitudes/Procesos Masivos/Cambio de Forma de Pago Masivo-Novedades DA`, alineado con origen `NOV` y `GMS` del SP.

## 2026-09-02 10:54 ART - Batch Solicitudes Automaticas Version 104

- Usuario Gabriel consulto en rama `gpulka version 104` si el batch de solicitudes puede cerrar el proceso luego de ejecutar procesos pendientes; indico que si ocurre, no deberia.
- Working copy analizada: `svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Evidencia: `exe_afilmed/src/d_procesos_soli_batch.srd` trae pendientes desde `afs_cambios_masivo_agenda_proc`; el SP `backend/procedures/afs_cambios_masivo_agenda_proc.sql` solo devuelve procesos con `fecha_inicio_real IS NULL` y `fecha_fin_real IS NULL`.
- En `exe_afilmed/src/wp_main_solicitudes.srw`, para tipo `AU`, al finalizar actualiza estado del proceso a `E` si no proceso nada, `O` si proceso con errores, o `R` si termino OK; luego graba `fecha_fin_real`.
- `backend/procedures/afu_cambios_masivos_agenda.sql` persiste esa `fecha_fin_real`, por lo que el proceso deja de estar pendiente aunque haya quedado como `E` u `O`.
- Conclusion comunicada: el cierre observado parece ser del proceso agendado, no del ejecutable batch; no se vio cierre normal de la aplicacion al terminar, salvo errores generales/conexion en `exe_afilmed/src/w_clase_main_batch.srw`.

## 2026-09-02 10:59 ART - Batch Solicitudes Automaticas APP log referencia

- Usuario Gabriel aclaro que el caso reportado era posible cierre de la aplicacion batch, no solo cierre del proceso agendado. Señalo unico `HALT CLOSE` visible en `w_clase_main_batch.srw` dentro de `IF NOT gf_connect(ls_mensaje) THEN`, que deberia mostrar mensaje previo.
- Usuario aporto log del 01/09/2026 17:10 con generacion AU correcta hasta exportacion de resumen y luego linea: `No se pudo recuperar referencia de solicitud APP. Error: Mismatch between retrieve columns and fetch columns`.
- Diagnostico: el error salia de `wf_loguear_soli_auto_app` en `exe_afilmed/src/wp_main_solicitudes.srw`, por `DECLARE/FETCH` embebido contra `afs_soli_auto_app_log_resumen`; PowerBuilder esperaba 3 columnas y recibia resultset no coincidente. El retorno no cerraba la aplicacion; solo afectaba el log de referencia APP.
- Correccion aplicada en `wp_main_solicitudes.srw`: reemplazar cursor SQL embebido por `nvo_proceso_agenda.of_procedure`, construyendo datastore segun resultset real y liberando objetos creados.
- Commit SVN `r37728`: `Corrige log de referencia APP en batch solicitudes`.
- Validado: `svn status` limpio para archivo modificado; formato UTF-8 con BOM y CRLF preservado. No se pudo compilar PowerBuilder desde el entorno.

## 2026-09-02 11:20 ART - Validacion baja responsable pagador

- Usuario pidio para solicitud de cambio de responsable pagador y empleado: si `baja_resp_pagador = S`, `moti_ret_resp_pag` debe ser obligatorio; si `baja_resp_pagador = N`, `moti_ret_resp_pag` no debe informarse y debe estar vacio; evitar guardar `baja_resp_pagador = N` con motivo.
- Working copy: `svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Cambios aplicados:
  - `backend/procedures/afs_soli_auto_obligatorios.sql`: rechaza XML con `baja_resp_pagador = N` y `moti_ret_resp_pag` informado.
  - `backend/procedures/afi_insertar_soli_auto_cambio_resp_pagador.sql`: evita guardar `moti_ret_resp_pag` cuando no corresponde y trata atributo vacio como `NULL`.
  - `backend/procedures/afu_genera_soli_auto_app.sql`: control defensivo para no procesar solicitud ya persistida con `baja_resp_pagador <> S` y motivo cargado.
- Commit SVN `r37729`: `Valida motivo de baja de responsable pagador`.
- Validado: diff acotado, encoding ASCII y CRLF preservados. No se ejecuto prueba contra base porque no habia profile indicado. Quedaron pendientes no relacionados en working copy: `Redmines/29361`, `Redmines/29362` y externos SVN.

## 2026-09-02 12:30 ART - Errores GMS APP sin fecha fin

- Usuario consulto mensaje: `Generacion GMS iniciada sin fecha de fin. No se reprocesa con el mismo id_soli_externa. Generar nuevamente con otro numero. Existe solicitud APP vigente 4677207 con id_soli_externa=49001011`.
- Diagnostico inicial: el mensaje se dispara si `GMS_CAB.fecha_desde IS NOT NULL` y `GMS_CAB.fecha_hasta IS NULL`; no por `GMS_ERR` directamente. La parte de solicitud APP vigente aparece si ya hay otra solicitud APP vigente para el mismo contrato/motivo con otro `id_soli_externa`.
- Usuario indico que habia muchos casos y uno tenia `GMS_ERR` para `cab_id = 295978`; se concluyo que no era situacion excepcional sino un falso "sin cierre" por orden de ejecucion.
- Causa raiz: en `backend/procedures/gms_genera_solicitudes.sql`, cuando `@nro_error <> 0`, el procedimiento hacia `RAISERROR` y `RETURN` antes de actualizar `GMS_CAB.fecha_hasta = GETDATE()`. Por eso podia existir `GMS_ERR` y aun asi quedar `fecha_hasta = NULL`.
- Correcciones aplicadas:
  - `backend/procedures/gms_genera_solicitudes.sql`: ante `@nro_error <> 0`, ahora cierra `GMS_CAB.fecha_hasta` antes del `RAISERROR/RETURN`.
  - `backend/procedures/afs_soli_auto_app_log_resumen.sql`: si el `cab_id` tiene errores GMS, informa `Generacion GMS con errores...` en lugar del generico `iniciada sin fecha de fin`.
  - `backend/procedures/afs_soli_auto_app_trazabilidad.sql`: prioriza `GMS_ERR`/`canti_errores` como `ERROR_GENERACION_SOLICITUD` antes de clasificar como `GENERACION_GMS_INICIADA_SIN_FIN`.
- Commit SVN `r37731`: `Corrige cierre y log de errores GMS APP`.
- Validado: diff, estado SVN limpio para esos archivos, encoding/EOL preservados (`gms_genera_solicitudes.sql` ISO-8859 CRLF; otros dos ASCII CRLF). No se pudo correr `PARSEONLY` por ausencia de cliente `sqlcmd/tsql`.

## 2026-09-02 12:53 ART - Responsable pagador empleado existente error 9307

- Usuario Gabriel indico que el caso que daba error `9307` estaba mal validado: corresponde al circuito admitido por `wp_cambio_resp_pagador` para modificar datos de un responsable pagador/empleado existente, habilitado por cambios del dia anterior.
- Working copy: `svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Diagnostico: `backend/procedures/gmsdef_val_resp_pagador.sql` trataba como "responsable pagador nuevo ya vigente" al mismo registro vigente que el circuito necesita modificar.
- Correccion aplicada: ajuste en `gmsdef_val_resp_pagador.sql` para que el error `9307` solo rechace otro registro vigente distinto, excluyendo el mismo `vigen_desde`.
- Commit SVN `r37732`: correccion de validacion 9307 para responsable pagador existente.
- Validado: `svn diff` final sin diferencias luego del commit; encoding preservado `ISO-8859 text`, finales `CRLF`, sin agregar `RTRIM` ni `;`; quedaron solo no versionados/externals previos no relacionados.

## 2026-09-02 15:26 ART - Reporte Datos Contacto Activacion DA documento

- Usuario Savina pidio un documento `.docx` consolidando el analisis de hoy y ayer del reporte `Reporte Datos de Contacto Activacion al Debito Automatico`, incluyendo EF, Mantis, RM, conclusiones funcionales y cuestiones tecnicas.
- Se genero y verifico Word en `/mnt/Agentes/svarela/Reporte Datos de Contacto Activación al Débito Automático - análisis funcional.docx`.
- Contenido validado: EF `A-04-DEF Reportes y Consultas E2 - Reportes Solicitudes_V12.1.docx` punto `4.17`, RM `#25132`, Mantis `7184`, origen `NOV` desde `NOVEDADES_DA_IMPOR_TEMP`, origen `GMS` desde `GMS_CAB/GMSDEF_MULTI`, tilde `Sin Exportar`, datos de commits SVN y conclusiones.
- Preferencia durable indicada por Savina: cuando pida generar un documento, guardarlo por defecto en `/mnt/Agentes/svarela`, salvo ruta explicita distinta.

## 2026-09-02 15:26 ART - Solicitudes Automaticas V104 InstallQA_89 pruebas RP y auditoria

- Usuario consulto errores `9307` y `9308` en Solicitudes Automaticas V104 / BD `InstallQA_89`.
- `9307`: `El responsable pagador nuevo ya se encuentra vigente para el contrato.` Se dispara en `backend/procedures/gmsdef_val_resp_pagador.sql` cuando el proceso interpreta que se intenta agregar/cambiar a un RP ya vigente. En catalogo `RECHA_MOTIVOS`: grupo `AMA`, tipo/carac `E`, `sube_igual = N`.
- `9308`: `Existe otro cambio de responsable pagador para el mismo contrato y vigencia.` Se dispara cuando ya existe otro cambio de RP para mismo contrato/documento/vigencia, anterior no errado o duplicado dentro del mismo lote.
- Para prueba de rechazo por auditoria medica se sugirio XML basado en solicitud externa nueva `900106626`, motivo `47`, con origen vigente `anter_prepa=1`, `anter_contra=5550`, `anter_inte=02`, documento `CC 1114460164`, y auditoria `resultado=R`, `soli_recha=3`, integrante `estado=NEG`, `nivel_riesgo=2`, `icd=E119`.
- La solicitud externa `900106626` genero `SOLICITUDES.soli = 4677242`, quedo `estado = R`, sin contrato destino (`SOLICITUDES.contra` y `SOLI_AFILIADOS.contra` vacios). En `GMS_CAB`: `id = 295980`, `transaccion = AT`, `estado = B`, `id_proceso = 248`.
- Para cambio de datos de RP sin cambiar persona se intento caso contrato `1021408`, RP vigente `CC 32240879`, cambiando `estrato_social` de `3` a `4`, con contactos actuales y fecha `2026-08-01`; dio `9307`, confirmando que el XML/proceso lo interpretaba como alta/cambio de RP vigente.
- Luego se pidio un caso nuevo distinto para probar modificacion de datos del RP vigente sin cambiar RP, con contratos relacionados y sin problemas de facturacion/vigencia.
- Caso armado y validado con `afs_soli_auto_obligatorios` en `InstallQA_89`: contrato `1018008`, RP vigente `CC 41575559 - MARIA TERESA VELASQUEZ DE RESTREPO`, vigencia RP `2025-02-01`, codigo unico persona `178532`, misma persona como RP en contratos relacionados `309037`, `309268`, `311095`, `311096`, `380291`, `380713`, `1018008`.
- Controles del caso `1018008`: `posible_9307 = 0`, `posible_9308 = 0`; cambio de dato propuesto `estrato_social` de `0` a `1`; contactos tomados de Registro Unico vigente; id externo libre `49001028`.
- XML final recomendado para `1018008`: `soli_moti=22`, `prepaga=1`, `contra=1018008`, `fecha_aplicacion=2025-02-01`, `baja_resp_pagador=N`; `<responsable_pagador>` mismo `CC 41575559`, `estrato_social=1`; contactos `R` actuales; parentesco `inte=01`, `paren_actual=T`, `paren_real_actual=M`, `paren_nuevo=M`. Nota: se uso `paren_nuevo=M` porque con `<responsable_pagador>` informado el procedimiento no permite `paren_nuevo=T`.

## 2026-09-02 17:56 ART - Solicitudes Automaticas RP y forma de pago DA

- Usuario reporto en prueba de cambio de datos de RP el error: `Generacion GMS con errores. La Fecha de Aplicacion no debe ser superior o inferior a la cantidad de meses permitidos para la retroactividad o a futuro del Tipo de Solicitud.`
- Diagnostico: para `soli_moti = 22` la fecha `2025-02-01` quedaba fuera de la ventana activa permitida; rango relevado: desde `2026-04-01` hasta `2026-11-01`.
- Se armo caso nuevo validado en `InstallQA_89` para cambio de datos de RP sin cambiar persona: contrato `101306`, RP vigente `CC 43739013 - VALERIA SALDARRIAGA DE LA TORRE`, fecha aplicacion `2026-08-01`, `id_soli_externa = 49001030`, cambio `estrato_social` de `6` a `5`, contactos de Registro Unico vigente.
- El RP `CC 43739013` tambien figura vigente en contratos relacionados `101306`, `1039716`, `1073918`, `124797`, `995827`, `996211`; controles `9307 = 0` y `9308 = 0`; validacion XML `VALIDACION_OK`.
- XML recomendado para contrato `101306`: `soli_moti=22`, `prepaga=1`, `contra=101306`, `fecha_aplicacion=2026-08-01`, `baja_resp_pagador=N`, responsable `codigo_unico_persona=202932`; parentescos informados para integrantes `02 C`, `03 H`, `04 H`, `05 P`, `06 E`, `07 M`.
- Usuario consulto por que se genero solicitud `4677249` si solo se habia probado RP. Diagnostico en base: `4677249` fue solicitud hija `soli_moti=5` generada desde solicitud RP `4677248`; observacion: `Solicitud generada a partir de la solicitud de Cambio de Responsable Pagador Nro: 4677248`.
- Causa de la hija `4677249`: el XML informo cambio de parentesco/titularidad en `<parentescos>` (`paren_actual=T` pero `paren_nuevo=P/M` segun normalizacion), por eso el sistema marco `SOLI_AFILIADOS.cambia_paren=T` y genero `Cambio de datos generales beneficiarios`. Conclusion: para probar solo cambio de datos del RP sin derivada, no modificar parentesco/titularidad; mandar solo parentescos realmente iguales o evitar informar titular con cambio.
- Usuario consulto en trunk `PruebasCoreINFOMEDICAL` si al activar debito automatico el proceso de informacion de formas de pago migra automaticamente a `Pago Recurrente`.
- Diagnostico: en `afu_impacta_impor_novedades_da` se toman novedades aprobadas de `NOVEDADES_DA_IMPOR_TEMP`, se genera solicitud automatica motivo `11` y se graba `SOLI_PAGO_FORMAS` con `dbo.afn_traer_parametro('Afilmed','pago_forma_efectivo','E')`.
- En `PruebasCoreINFOMEDICAL` ese parametro vale `E`, y `PAGO_FORMAS.E` es `EFECTIVO`. `PAGO_FORMAS.RE` existe y esta activo como `PAGO RECURRENTE`, pero ese proceso no lo usa.
- Conclusion comunicada: con configuracion actual, la activacion DA por este proceso migra a `E - EFECTIVO`, no a `RE - PAGO RECURRENTE`; si funcionalmente debe migrar a Pago Recurrente, falta parametrizacion/cambio de configuracion o ajuste del procedimiento.
