
## 2026-07-08 15:25 UTC - Durable Work Notes

- User Gabriel asked about who inserts `PROCESO_AGENDA` with `tipo_proc_id = 'SIMASO'` in gpulka trunk. Findings: `SIMASO` is `SIMULADOR_ASOCIADOS` in `exe_afilmed/src/nvo_proceso_agenda.sru`; insert is performed by SP `dbo.afu_proceso_agenda` called from `nvo_proceso_agenda.of_grabar()`. In closing billing periods, `wp_cierre_factu_periodos.srw` event `ue_grabar` creates a `prefac_cab` via `f_get_proximo_prefac_cab(... tipo_proceso='S', tipo_facturacion='SA')` and then schedules `SIMASO` using `Afilmed/SimuladorAsociados.HoraAgenda`.

- Transaction issue identified in `wp_cierre_factu_periodos.srw`: previous code could insert into `PREFACTURAS_CAB` but fail before/while inserting into `PROCESO_AGENDA`, because no transaction was opened before `ue_grabar`; rollback/commit calls existed but `SQLCA.Autocommit` could remain true. Cause path: `f_get_proximo_prefac_cab()` -> `afs_fac_proximo_numero_verif` inserts into `PREFACTURAS_CAB`; later `lnvo_proceso_agenda.of_grabar()` calls `afu_proceso_agenda`.

- User requested SVN update, fix, and commit in `/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk`. Working copy was updated to r37290. Fix made in `exe_afilmed/src/wp_cierre_factu_periodos.srw`: open transaction before executing `ue_grabar()`, keep final commit, add defensive rollback if `ue_grabar()` returns `-1` without closing transaction. Committed to SVN as `r37291 | gpulka@INFOMEDICAL | Corrige transaccion en cierre de periodos de facturacion`. Validation reported: `svn status` clean except externals `X`; file UTF-8 BOM and CRLF preserved; no PowerBuilder compile executed.

- User asked to review `REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` from Gabriel folder executed in `InstallQA_89`, then batch solicitudes apparently did nothing. Finding: script did insert data, but batch skipped it because `SOLI_AUTO_CONTRATO.proce_fecha` was already set. In `InstallQA_89`, record found: `SOLI_AUTO_CONTRATO.id_proceso=91`, `id_soli_externa=202607071`, `soli_moti=22`, `prepaga=1`, `contra=1000050`, `soli=NULL`, `prefac_cab=NULL`, `proce_fecha=2026-07-07 16:17:55.633`. Matching `SOLI_AUTO_RESP_PAGADOR` exists for `id_proceso=91`. `afu_genera_soli_auto_app` processes only rows where `SOLI_AUTO_CONTRATO.proce_fecha IS NULL`, so it skipped this row. No `GMS_CAB` exists for `id_soli_externa=202607071`; trace status was `PENDIENTE_GENERACION_GMS`. Script comment said example `id_soli_externa=29365001`, but actual XML used `202607071`. No corrective `UPDATE` was executed in DB.


## 2026-07-08 16:45 UTC - Flush

- Usuario Gabriel trabaja sobre `gpulka` SVN y suele pedir cambios directos con commit. Mantener cuidado extremo con EOL/encoding: revisar y preservar CRLF/BOM cuando corresponda antes de commitear. Gabriel ya reclamó por saltos de línea; validar explícitamente `loneLF`/CRLF en archivos tocados.
- En `gpulka-Coomeva-trunk` se corrigió `wp_cierre_factu_periodos.srw` y se commiteó `r37291` (`Corrige transaccion en cierre de periodos de facturacion`): abrir transacción antes de `ue_grabar()` para que `PREFACTURAS_CAB` y `PROCESO_AGENDA` queden dentro del mismo flujo transaccional.
- En rama `gpulka-Version104-Afilmed-SolicitudesAutomaticas` se investigó `REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` ejecutado en `InstallQA_89`. Caso de prueba: `id_proceso=91`, `id_soli_externa=202607071`, `soli_moti=22`, `prepaga=1`, `contra=1000050`, `soli=NULL`, `prefac_cab=NULL`, `proce_fecha=2026-07-07 16:17:55.633`; existe `SOLI_AUTO_RESP_PAGADOR`; no existe `GMS_CAB`, `CAMBIOS_MASIVOS_AGENDA` ni `SOLICITUDES`. Trazabilidad: `PENDIENTE_GENERACION_GMS`.
- Corrección en `afu_genera_soli_auto_app.sql` commiteada `r37292` (`Corrige control de generacion GMS en solicitudes automaticas`): selector sin depender solo de `proce_fecha IS NULL`, corta con error si falla `gms_cab_id`, valida existencia de `GMS_CAB` y agenda antes de marcar `proce_fecha`.
- Gabriel aclaró que el cliente denomina todo el circuito como "traslados". Evitar llamar visible/logs "APP", "solicitud APP", "solicitud automática APP", "traslado APP"; solo conservar `APP` en nombres técnicos/constantes como `origen_solicitud = 'APP'`. Se ajustaron mensajes en `wp_main_solicitudes.srw` y procedures; commit `r37293` (`Ajusta mensajes del circuito de traslados`). Luego se normalizaron EOL por reclamo; commit `r37294` (`Normaliza saltos de linea en mensajes de traslados`).
- Última tarea antes del flush: se actualizó `/mnt/Agentes/Gabriel/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` para correr por fuera la fase del batch de solicitudes. Ahora hace preflight de dependencia (`dbo.gmsTXT_RESP_PAGADOR_ins` debe tener `@moti_ret_resp_pag`), inserta XML solo si no existe, ejecuta `dbo.afu_genera_soli_auto_app`, busca `cab_id`, ejecuta `dbo.gms_genera_solicitudes` si hay `GMS_CAB`, y muestra selects de `SOLI_AUTO`, `GMS_CAB`, `CAMBIOS_MASIVOS_AGENDA`, `GMS_ERR`, `SOLICITUDES` y trazabilidad. Validado con `PARSEONLY` OK. Mantiene LF porque el script ya estaba LF.
- Diagnóstico actual de por qué no hace nada en `InstallQA_89`: no es `proce_fecha`. El caso entra y falla en generación GMS con `Procedure or function gmsTXT_RESP_PAGADOR_ins has too many arguments specified`. En `InstallQA_89`, `afu_genera_soli_auto_app` está actualizado al `2026-07-08`, pero `gmsTXT_RESP_PAGADOR_ins` sigue viejo (`modify_date = 2018-01-04`), sin parámetro `@moti_ret_resp_pag`. Las tablas `GMSTXT_RESP_PAGADOR` y `GMSDEF_RESP_PAGADOR` sí tienen columna `moti_ret_resp_pag`. Debe actualizarse el procedure `gmsTXT_RESP_PAGADOR_ins` o instalar dependencia equivalente antes de probar.

## 2026-07-08 18:34 UTC - Traslados / solicitud automatica RP Redmine 29365

- Usuario reporto ejecucion de `/mnt/Agentes/Gabriel/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` en `InstallQA_89`: `id_soli_externa=202607071`, `id_proceso=91`, estado trazabilidad `PENDIENTE_GENERACION_SOLICITUD`, `soli=NULL`, `soli_moti=22 Cambio de Responsable Pagador`, `contra=1000050`.
- Diagnostico en `InstallQA_89`: ya no era `proce_fecha` ni el SP viejo; `gmsTXT_RESP_PAGADOR_ins` ya tenia el parametro esperado. El caso tenia `GMS_CAB.cab_id=295704` sin errores y 2 filas en `GMSTXT_RESP_PAGADOR`, pero `GMS_CONTRATOS=0`, `GMSDEF_CLASE_PLAN=0`, `GMSDEF_RESP_PAGADOR=0`; `CAMBIOS_MASIVOS_AGENDA` estaba en estado `P` sin inicio/fin real.
- Conclusion tecnica: `afu_genera_soli_auto_app` genero cabecera/TXT y marco el proceso, pero no se habia corrido la fase de validacion TXT/DEF que puebla las tablas logicas. Llamar directo a `gms_genera_solicitudes` no genera solicitud si no hay DEF.
- Se actualizo `/mnt/Agentes/Gabriel/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` para correr fase completa: `afu_genera_soli_auto_app`, luego `gmstxt_validar @prm_cab_id`, luego `gmsdef_validar @prm_cab_id, @prm_sinxml='N', @prm_etapa=2`, y finalmente `gms_genera_solicitudes`. Se agregaron selects intermedios de `GMS_CONTRATOS`, `GMSDEF_CLASE_PLAN`, `GMSDEF_RESP_PAGADOR`, `GMS_ERR`, agenda y solicitud.
- Validacion realizada: `PARSEONLY` OK del script actualizado. No se ejecuto completo para no volver a tocar el caso sin confirmacion.

## 2026-07-08 18:34 UTC - Revision transacciones similares a wp_cierre_factu_periodos

- En trunk `gpulka-Coomeva-trunk`, se buscaron casos Afilmed similares al bug corregido en `wp_cierre_factu_periodos`: flujos con `EVENT ue_grabar()` y `of_commit_transaction()`/rollback sin `of_begin_transaction()` visible.
- Se descartaron ventanas que heredan de `w_clase_abm` o `w_clase_soli` porque esos ancestros si abren transaccion.
- Candidatos fuertes encontrados en familia `w_clase_caja`, cuyo ancestro `w_clase_caja.srw` no abre transaccion: `w_clase_caja_abm.srw`, `w_clase_caja_contra.srw`, `wp_caja_cheques_rechazados.srw`, `wp_cierre_caja.srw`, `wp_egresos_caja.srw`, `wp_fondo_fijo_detalle.srw`, `wp_fondo_fijo_lineas.srw`, `wp_retiro_valores.srw`, `wp_reverso.srw`.
- No se modifico nada en esa revision; trunk quedo limpio salvo externos.
## 2026-07-08 15:25 UTC - Durable Work Notes

- User Gabriel asked about who inserts `PROCESO_AGENDA` with `tipo_proc_id = 'SIMASO'` in gpulka trunk. Findings: `SIMASO` is `SIMULADOR_ASOCIADOS` in `exe_afilmed/src/nvo_proceso_agenda.sru`; insert is performed by SP `dbo.afu_proceso_agenda` called from `nvo_proceso_agenda.of_grabar()`. In closing billing periods, `wp_cierre_factu_periodos.srw` event `ue_grabar` creates a `prefac_cab` via `f_get_proximo_prefac_cab(... tipo_proceso='S', tipo_facturacion='SA')` and then schedules `SIMASO` using `Afilmed/SimuladorAsociados.HoraAgenda`.

- Transaction issue identified in `wp_cierre_factu_periodos.srw`: previous code could insert into `PREFACTURAS_CAB` but fail before/while inserting into `PROCESO_AGENDA`, because no transaction was opened before `ue_grabar`; rollback/commit calls existed but `SQLCA.Autocommit` could remain true. Cause path: `f_get_proximo_prefac_cab()` -> `afs_fac_proximo_numero_verif` inserts into `PREFACTURAS_CAB`; later `lnvo_proceso_agenda.of_grabar()` calls `afu_proceso_agenda`.

- User requested SVN update, fix, and commit in `/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk`. Working copy was updated to r37290. Fix made in `exe_afilmed/src/wp_cierre_factu_periodos.srw`: open transaction before executing `ue_grabar()`, keep final commit, add defensive rollback if `ue_grabar()` returns `-1` without closing transaction. Committed to SVN as `r37291 | gpulka@INFOMEDICAL | Corrige transaccion en cierre de periodos de facturacion`. Validation reported: `svn status` clean except externals `X`; file UTF-8 BOM and CRLF preserved; no PowerBuilder compile executed.

- User asked to review `REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` from Gabriel folder executed in `InstallQA_89`, then batch solicitudes apparently did nothing. Finding: script did insert data, but batch skipped it because `SOLI_AUTO_CONTRATO.proce_fecha` was already set. In `InstallQA_89`, record found: `SOLI_AUTO_CONTRATO.id_proceso=91`, `id_soli_externa=202607071`, `soli_moti=22`, `prepaga=1`, `contra=1000050`, `soli=NULL`, `prefac_cab=NULL`, `proce_fecha=2026-07-07 16:17:55.633`. Matching `SOLI_AUTO_RESP_PAGADOR` exists for `id_proceso=91`. `afu_genera_soli_auto_app` processes only rows where `SOLI_AUTO_CONTRATO.proce_fecha IS NULL`, so it skipped this row. No `GMS_CAB` exists for `id_soli_externa=202607071`; trace status was `PENDIENTE_GENERACION_GMS`. Script comment said example `id_soli_externa=29365001`, but actual XML used `202607071`. No corrective `UPDATE` was executed in DB.


## 2026-07-08 16:45 UTC - Flush

- Usuario Gabriel trabaja sobre `gpulka` SVN y suele pedir cambios directos con commit. Mantener cuidado extremo con EOL/encoding: revisar y preservar CRLF/BOM cuando corresponda antes de commitear. Gabriel ya reclamó por saltos de línea; validar explícitamente `loneLF`/CRLF en archivos tocados.
- En `gpulka-Coomeva-trunk` se corrigió `wp_cierre_factu_periodos.srw` y se commiteó `r37291` (`Corrige transaccion en cierre de periodos de facturacion`): abrir transacción antes de `ue_grabar()` para que `PREFACTURAS_CAB` y `PROCESO_AGENDA` queden dentro del mismo flujo transaccional.
- En rama `gpulka-Version104-Afilmed-SolicitudesAutomaticas` se investigó `REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` ejecutado en `InstallQA_89`. Caso de prueba: `id_proceso=91`, `id_soli_externa=202607071`, `soli_moti=22`, `prepaga=1`, `contra=1000050`, `soli=NULL`, `prefac_cab=NULL`, `proce_fecha=2026-07-07 16:17:55.633`; existe `SOLI_AUTO_RESP_PAGADOR`; no existe `GMS_CAB`, `CAMBIOS_MASIVOS_AGENDA` ni `SOLICITUDES`. Trazabilidad: `PENDIENTE_GENERACION_GMS`.
- Corrección en `afu_genera_soli_auto_app.sql` commiteada `r37292` (`Corrige control de generacion GMS en solicitudes automaticas`): selector sin depender solo de `proce_fecha IS NULL`, corta con error si falla `gms_cab_id`, valida existencia de `GMS_CAB` y agenda antes de marcar `proce_fecha`.
- Gabriel aclaró que el cliente denomina todo el circuito como "traslados". Evitar llamar visible/logs "APP", "solicitud APP", "solicitud automática APP", "traslado APP"; solo conservar `APP` en nombres técnicos/constantes como `origen_solicitud = 'APP'`. Se ajustaron mensajes en `wp_main_solicitudes.srw` y procedures; commit `r37293` (`Ajusta mensajes del circuito de traslados`). Luego se normalizaron EOL por reclamo; commit `r37294` (`Normaliza saltos de linea en mensajes de traslados`).
- Última tarea antes del flush: se actualizó `/mnt/Agentes/Gabriel/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` para correr por fuera la fase del batch de solicitudes. Ahora hace preflight de dependencia (`dbo.gmsTXT_RESP_PAGADOR_ins` debe tener `@moti_ret_resp_pag`), inserta XML solo si no existe, ejecuta `dbo.afu_genera_soli_auto_app`, busca `cab_id`, ejecuta `dbo.gms_genera_solicitudes` si hay `GMS_CAB`, y muestra selects de `SOLI_AUTO`, `GMS_CAB`, `CAMBIOS_MASIVOS_AGENDA`, `GMS_ERR`, `SOLICITUDES` y trazabilidad. Validado con `PARSEONLY` OK. Mantiene LF porque el script ya estaba LF.
- Diagnóstico actual de por qué no hace nada en `InstallQA_89`: no es `proce_fecha`. El caso entra y falla en generación GMS con `Procedure or function gmsTXT_RESP_PAGADOR_ins has too many arguments specified`. En `InstallQA_89`, `afu_genera_soli_auto_app` está actualizado al `2026-07-08`, pero `gmsTXT_RESP_PAGADOR_ins` sigue viejo (`modify_date = 2018-01-04`), sin parámetro `@moti_ret_resp_pag`. Las tablas `GMSTXT_RESP_PAGADOR` y `GMSDEF_RESP_PAGADOR` sí tienen columna `moti_ret_resp_pag`. Debe actualizarse el procedure `gmsTXT_RESP_PAGADOR_ins` o instalar dependencia equivalente antes de probar.

## 2026-07-08 18:34 UTC - Traslados / solicitud automatica RP Redmine 29365

- Usuario reporto ejecucion de `/mnt/Agentes/Gabriel/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` en `InstallQA_89`: `id_soli_externa=202607071`, `id_proceso=91`, estado trazabilidad `PENDIENTE_GENERACION_SOLICITUD`, `soli=NULL`, `soli_moti=22 Cambio de Responsable Pagador`, `contra=1000050`.
- Diagnostico en `InstallQA_89`: ya no era `proce_fecha` ni el SP viejo; `gmsTXT_RESP_PAGADOR_ins` ya tenia el parametro esperado. El caso tenia `GMS_CAB.cab_id=295704` sin errores y 2 filas en `GMSTXT_RESP_PAGADOR`, pero `GMS_CONTRATOS=0`, `GMSDEF_CLASE_PLAN=0`, `GMSDEF_RESP_PAGADOR=0`; `CAMBIOS_MASIVOS_AGENDA` estaba en estado `P` sin inicio/fin real.
- Conclusion tecnica: `afu_genera_soli_auto_app` genero cabecera/TXT y marco el proceso, pero no se habia corrido la fase de validacion TXT/DEF que puebla las tablas logicas. Llamar directo a `gms_genera_solicitudes` no genera solicitud si no hay DEF.
- Se actualizo `/mnt/Agentes/Gabriel/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql` para correr fase completa: `afu_genera_soli_auto_app`, luego `gmstxt_validar @prm_cab_id`, luego `gmsdef_validar @prm_cab_id, @prm_sinxml='N', @prm_etapa=2`, y finalmente `gms_genera_solicitudes`. Se agregaron selects intermedios de `GMS_CONTRATOS`, `GMSDEF_CLASE_PLAN`, `GMSDEF_RESP_PAGADOR`, `GMS_ERR`, agenda y solicitud.
- Validacion realizada: `PARSEONLY` OK del script actualizado. No se ejecuto completo para no volver a tocar el caso sin confirmacion.

## 2026-07-08 18:34 UTC - Revision transacciones similares a wp_cierre_factu_periodos

- En trunk `gpulka-Coomeva-trunk`, se buscaron casos Afilmed similares al bug corregido en `wp_cierre_factu_periodos`: flujos con `EVENT ue_grabar()` y `of_commit_transaction()`/rollback sin `of_begin_transaction()` visible.
- Se descartaron ventanas que heredan de `w_clase_abm` o `w_clase_soli` porque esos ancestros si abren transaccion.
- Candidatos fuertes encontrados en familia `w_clase_caja`, cuyo ancestro `w_clase_caja.srw` no abre transaccion: `w_clase_caja_abm.srw`, `w_clase_caja_contra.srw`, `wp_caja_cheques_rechazados.srw`, `wp_cierre_caja.srw`, `wp_egresos_caja.srw`, `wp_fondo_fijo_detalle.srw`, `wp_fondo_fijo_lineas.srw`, `wp_retiro_valores.srw`, `wp_reverso.srw`.
- No se modifico nada en esa revision; trunk quedo limpio salvo externos.

## 2026-07-08 19:18 UTC - Savina / traslado a contrato existente con auditoria

- Usuario Savina preguntó en `svarela-Coomeva-trunk` si una solicitud de traslado a contrato existente o nuevo puede quedar rechazada o pasar por auditoría. Se analizó sin modificar código. Evidencia: en `backend/data_modifications/SOLI_MOTIVOS.sql`, motivos `47 = Traslado a Contrato Nuevo` y `48 = Traslado a Contrato Existente` tienen `audita='S'`, `audita_programa='S'`, `precarga='S'`; `afu_completa_datos_audit_para_traslado.sql` inserta en `SOLI_ESTADO_AUDITORIA`; `wp_alta_total.srw` y `wp_alta_parcial.srw` pueden pasar solicitud a condición `R` si no quedan integrantes aceptados/impactables.
- Savina pidió un caso ejecutable hoy en `InstallQA_89` para generar una solicitud de `Traslado a Contrato Existente` que deba pasar por auditoría. Caso entregado: fecha aplicación `08/07/2026`, motivo `48`, novedad `4 - TRASLADO`, origen contrato `99930`, integrante `02`, afiliada `ZULEMA RAMOS`, documento `CC 43731062`, edad `55`, programa actual `TRADICIONAL COLECTIVO`, grupo `TRD`; destino contrato existente `99973`, programa destino `ORO FAMILIAR`, grupo `ORO`.
- Justificación del caso: regla médica vigente para traslado (`soli_moti=48`, novedad `4`) aplica a mujer en grupos `TRD/ORO` con `edad_mayor_a=54`; la afiliada tiene 55 años. Además hay pase de programa `TRD -> ORO` y la regla tiene `auditar_pase_programa='S'`. Se validó que el documento no está activo en el contrato destino y no tiene solicitudes abiertas; solo solicitudes históricas finalizadas. No se modificaron datos en BD.

## 2026-07-08 19:47 UTC - Durable memory flush

- Gabriel pidió analizar el caso de traslados/RP para `contra=1000050` en `InstallQA_89`: el batch generó 2 solicitudes que finalizaron RP vigente y crearon RP nuevo, ambas impactadas, algo que por ventana no debería pasar por consistencia de vigencia/duplicación.
- Se investigó en `gpulka-Version104-Afilmed-SolicitudesAutomaticas` y se comiteó `r37300`.
- Hallazgo principal: `gms_genera_solicitudes.sql` llamaba siempre a `afu_soli_resp_pagador` con `@prm_item = 1`; al haber dos filas de RP en una solicitud, la segunda pisaba la primera en `SOLI_RESP_PAGADOR`, quedando solo el nuevo RP y perdiéndose la finalización.
- Cambios de `r37300`:
  - `backend/procedures/gms_genera_solicitudes.sql`: genera `item` incremental para RP y ordena primero finalización/baja y luego alta del nuevo RP.
  - `backend/procedures/gmsdef_val_resp_pagador.sql`: agrega validaciones GMS para consistencia de responsable pagador.
  - `backend/inserts/recha_motivos.sql`: agrega motivos de rechazo `9306`, `9307`, `9308`.
- Validaciones nuevas:
  - `9306`: el RP a finalizar debe existir vigente en `AFI_RESP_PAGADOR`.
  - `9307`: el nuevo RP no puede estar ya vigente para el contrato.
  - `9308`: no puede existir otro cambio previo GMS para el mismo contrato/documento/vigencia.
- Se validó con `SET PARSEONLY ON` contra `InstallQA_89` para los tres scripts y se probaron las condiciones contra las cabeceras del caso; detectan duplicado previo y RP nuevo ya vigente.
- Estado reportado: SVN limpio para esos archivos luego del commit.
