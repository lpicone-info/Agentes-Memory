
## 2026-09-04 15:04 ART - Redmine 30282 y 30314

### Redmine 30282 - PB22 / SQL Server 2022 / SQL Native Client 11
- Contexto de laboratorio: Windows Server 2025 + SQL Server 2022 + PowerBuilder 2022 32 bits.
- Se intento usar MSOLEDBSQL 19 x86, MSOLEDBSQL 18 x86 y MSOLEDBSQL 18.7.5 x86, pero fallaron en Windows Server 2025.
- SQL Native Client 11 x86 si instalo, PB22 lo detecto y conecto correctamente mediante `SNC SQL Native Client(OLE DB)` / provider `SQLNCLI11`.
- Datos validados de conexion de laboratorio: servidor `localhost`, base `afilmed_lab`, usuario `usr_afilmed_lab`, password `AfilmedLab2026!`, provider `SQLNCLI11`.
- Se creo y valido en SSMS el SP `dbo.sp_prueba_conexion_afilmed`, con `EXEC dbo.sp_prueba_conexion_afilmed` devolviendo `resultado = 1` en `afilmed_lab`.
- Archivo trabajado: `/mnt/Agentes/srmontenegro/redmine_30282_crear_sp_prueba_conexion_afilmed.sql`. Se ajusto para reglas SQL locales: ASCII, LF, sin BOM visible, sin `;` ni `RTRIM`.
- Archivo trabajado: `/mnt/Agentes/srmontenegro/w_main_pruebas.srw`. Se hicieron variantes para diagnostico de conexion y luego se dejo alineado al comportamiento real de Afilmed.
- Se probo que llamar al SP con nombre completo `afilmed_lab.dbo.sp_prueba_conexion_afilmed` funcionaba, pero Sebastian aclaro que en Afilmed no se antepone la DB en las llamadas y cambiar todo seria muy costoso.
- Decision tecnica: no modificar masivamente llamadas a SPs. La solucion correcta es que la conexion/SQLCA entre posicionada directamente en `afilmed_lab`.
- Ajuste final indicado en el export: llamada estilo Afilmed `EXEC dbo.sp_prueba_conexion_afilmed` y conexion reforzada con `SQLCA.Database = "afilmed_lab"` y `SQLCA.DBParm = "Provider='SQLNCLI11',Database='afilmed_lab'"`.
- El cliente decidio continuar la migracion usando SQL Native Client 11. Esto cambia el analisis, implementacion y tiempos: se descarta POC ODBC como camino principal, no hay redisenio masivo de capa de conexion, y el alcance se concentra en replicar configuracion SNC validada, posicionar correctamente la base y probar procesos criticos. Riesgo principal: SQL Native Client 11 es legacy/deprecado.

### Redmine 30314 - Solicitudes / Certificado Otro Si Preexistencia
- Caso: Redmine `30314`, proyecto `COOMEVA - Desarrollos Facturable`, tipo Tarea, estado En Analisis, version TRUNK, asunto `Solicitudes => Certificado Otro Si Preexistencia`.
- Alcance funcional: modificar la generacion del Certificado Anexo Otro Si Preexistencias para solicitudes de Inclusion cuando el contrato tenga fecha de inicio de vigencia posterior a la fecha de emision del certificado.
- Regla funcional: `CONTRATO.FECHA_INICIO_VIGENCIA > FECHA_EMISION_CERTIFICADO`.
- En ese caso el certificado debe mostrar datos del contratante: apellidos y nombres, identificacion del contratante.
- Relacionado con Redmine `30283`.
- Documento funcional relacionado trabajado con Belkis: `A-04-DEF Solicitudes_Cambios en Impresion de Certificados_V04.docx`, punto sugerido cercano `4.1.5.1.4.2 Certificado de Preexistencias`.
- Flujo afectado: `Solicitudes / Inclusiones / Boton Imprimir Certificados / Imprimir Preexistencias`.
- Analisis tecnico: no parece cambiar habilitacion de boton ni circuito de Auditoria Medica; impacto principal esta en los datos que alimentan la salida impresa/generacion del certificado.
- Objeto principal detectado para modificar: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk/backend/procedures/afs_soli_preexistencias_impre.sql`. Este SP arma el dataset del certificado y hoy devuelve `contratante`, `docu_contratante`, `ingre_fecha`, datos del afiliado, preexistencia y auditor.
- DataWindow del certificado: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk/exe_afilmed/src/d_impresion_preexistencia.srd`. Ya contiene los campos `contratante` y `docu_contratante` y visualmente muestra encabezados `Apellido(s) y Nombre(s) del Contratante` y `Cedula`; en principio no requiere modificacion salvo pedido visual del cliente.
- Objetos relacionados revisados pero no necesariamente modificables: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk/exe_afilmed/src/wr_soli_imprime_certificado_preexistencia.srw`, `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk/exe_afilmed/src/d_soli_imprime_preexistencias.srd`, `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk/exe_afilmed/src/d_soli_imprimir_certificados.srd`, `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Coomeva-trunk/backend/procedures/afs_soli_imprimir_certificados.sql`.
- Implementacion estimada: modificar `afs_soli_preexistencias_impre`, agregar condicion para solicitud de Inclusion (`soli_moti = 2`), evaluar fecha inicio vigencia del contrato posterior a fecha emision, y en ese caso forzar `contratante`/`docu_contratante` desde el responsable pagador/contratante correcto, manteniendo las mismas columnas de salida para no tocar el SRD.
- Punto pendiente de confirmacion antes de codificar: mapear exactamente `CONTRATO.FECHA_INICIO_VIGENCIA`, porque no se vio una tabla literal `CONTRATO`; posibles fuentes a verificar segun modelo Afilmed: `SOLICITUDES.apli_fecha`, `SOLI_AFILIADOS.ingre_fecha` o `SUBCTA_CONTRATO.vigen_desde`.

## 2026-09-04 16:28 ART - Redmine / gpulka durable context

- User Gabriel worked on Redmine 30282 in `/mnt/Agentes/gpulka`, using `gpulka` trunk context. Main document maintained as `REDMINE_30282_ANALISIS_MIGRACION_SQLSERVER2022_MSOLEDBSQL_COOMEVA.md` and regenerated Word `REDMINE_30282_ANALISIS_MIGRACION_SQLSERVER2022_MSOLEDBSQL_COOMEVA.docx`.
- Redmine 30282 scope evolved: initial analysis for PB22 + SQL Server 2022 + MSOLEDBSQL/ODBC risks was updated after customer decision to continue with `PB22 32 bits + SQL Server 2022 + SQL Native Client 11 x86` (`SQLNCLI11`), same access style currently used. Document now states no massive migration to MSOLEDBSQL/ODBC is required; focus is connection configuration, database positioning, critical process QA, and support risk because SQL Native Client 11 is legacy/deprecated.
- Redmine 30282 findings added earlier: Afilmed has 1,896 `.srd`, 1,582 SP DataWindows, 6 direct SQL/PBSELECT DataWindows, 434 PowerScript files with `DECLARE PROCEDURE FOR`, 8 `EXECUTE IMMEDIATE`, 8 BLOB-related. Presmed has 2,244 `.srd`, 859 SP DataWindows, 848 direct SQL/PBSELECT DataWindows, 380 PowerScript files with `DECLARE PROCEDURE FOR`, 59 cursors, 12 `EXECUTE IMMEDIATE`, 9 BLOB-related. `exe_presmed/Source Infomedical/d_instituciones.srd` contains legacy outer join `*=` and is a key SQL Server 2022 risk.
- Redmine 30282 document includes risks analogous to Redmine 28311/28331: date conversion failures (`SQLSTATE 22007`, `Conversion failed when converting date and/or time from character string`) around DataWindows, cast/convert, DateTime params, dateformat, text dates; truncation failures (`SQLSTATE 22001`, `String or binary data would be truncated`) especially Presmed internaciones and `pru_sanat_a_liquidar`; `image` type is supported but deprecated and must be tested for `SELECTBLOB`/`UPDATEBLOB` with SQLNCLI11.
- Redmine 30282 current effort estimates after SQLNCLI11 decision: minimum remediation with directed QA `7 a 11 jornadas`; additional expanded remediation `8 a 14 jornadas`. Previous MSOLEDBSQL/ODBC-driven estimates should not be treated as current.
- For Redmine 30319, user asked for a SQL file in Gabriel/gpulka folder, no before/after selects, direct updates only, no variables. Created `/mnt/Agentes/gpulka/REDMINE_30319_AJUSTE_CODIGO_UNICO.sql`: a single direct `UPDATE` on `dbo.AFILIADOS` changing contract `392545`, integrante `02`, documento `TI 1107868804`, from `codigo_unico_persona = 574778` to `385161`. Validated no `SELECT`, no `DECLARE`, no variables, no `RTRIM`, no semicolon; ASCII, no BOM, LF. Update was not executed and SVN was not modified.

## 2026-09-04 18:06 ART - Savina / Coomeva trunk consultas funcionales

- Sender/context: Savina consulto en Google Chat directo sobre trunk y base `PruebasCoreINFOMEDICAL`.
- Preferencia operativa inferida: cuando pide "trunk bd pruebascoreinfomediccal/PruebasCoreINFOMEDICAL" espera analisis combinado de codigo SVN trunk y consultas read-only a BD de pruebas, con respuesta funcional y evidencia tecnica breve.
- Consulta 1: campo `afi_planes.vigen_cober`.
  - Conclusion entregada: funcionalmente es fecha de vigencia de cobertura del plan, distinta de `vigen_desde` que el codigo trata como vigencia/base de facturacion.
  - En altas normales no se carga desde ventanas porque el sistema la completa igual a `vigen_desde`; toma sentido en cambios de cobertura con carencia, donde puede quedar posterior.
  - Evidencia de codigo: `backend/tables/AFI_PLANES.sql` poblo `vigen_cober` con `vigen_desde` y luego lo dejo `NOT NULL`; `backend/procedures/afu_afi_planes.sql` inserta `vigen_cober = @vigen_desde` y `tiempo_caren = 0`; `backend/procedures/prs_planes_princi_anexo.sql` documenta que `@prm_cobertura = N` toma `vigen_desde` y `@prm_cobertura = S` toma `vigen_cober`; `backend/procedures/afu_impacta_cambio_cobertura.sql` actualiza `vigen_cober = DATEADD(MM, tiempo_caren, vigen_desde)` cuando hay carencia.
  - Datos observados en `PruebasCoreINFOMEDICAL`: `AFI_PLANES` tenia `1.340.721` registros; ninguno con `vigen_cober` vacio; `1.340.690` con `vigen_cober = vigen_desde`; solo `31` distintos; vigentes `504.378`; vigentes con diferencia `15`; diferencias mayormente de 6 meses, consistente con carencia.
- Consulta 2: cambio de cobertura y fecha de aplicacion con corte.
  - Conclusion entregada: en trunk el cambio de cobertura no recalcula automaticamente la fecha de aplicacion por corte en la ventana; la fecha entra como `apli_perio` y se usa como vigencia base del tramite.
  - Circuito manual: `wp_cambio_cobertura_masiva` pide `apli_perio`, valida solo obligatoriedad, busca cuenta/subcuenta/plan/tarifa vigentes, guarda en `CAMBIOS_MASIVOS_AGENDA.fecha_aplic`, luego en `GMS_CAB.apli_fecha`, y al generar solicitud por motivo `6` pasa a `SOLICITUDES.apli_fecha`, `SOLI_CLASE.vigen_desde` y `SOLI_PLANES.vigen_desde`.
  - El corte se usa como contexto vigente a `GMS_CAB.apli_fecha`: `gms_genera_solicitudes` toma `vto_grupo`, `emi_modo`, `emi_grupo` desde `SUBCTA_CONTRATO` o `AFI_TITULARES` vigente a esa fecha; no desplaza la aplicacion al mes siguiente.
  - Circuito automatico `afi_cambio_cober_cooperativa`: toma `MAX(factu_perio)` de `FACTU_PERIODOS` con `baja_fecha IS NULL` y `fecha_cierre IS NULL` y usa ese valor como `GMS_CAB.apli_fecha`.
  - Dato de pruebas informado: periodo abierto maximo en ese momento `2026-10-01`; registros recientes `CC_COOP` observados con aplicacion `2026-09-01`, consistente con periodo abierto del momento de generacion.
  - Configuracion batch `CC` en pruebas: tipo `CC`, vigencia `2018-06-01`, horario permitido `12:00` a `12:10`, `max_cantidad_grupos = 999999`; no define corte funcional.
- No se modificaron archivos ni datos durante estas consultas; solo busquedas en `svarela-Coomeva-trunk` y consultas read-only en `PruebasCoreINFOMEDICAL`.
