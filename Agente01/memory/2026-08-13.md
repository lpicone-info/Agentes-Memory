
## 2026-08-13 12:27 ART - Redmine 30222 / recaudos ONCA

- Usuario activo: Gabriel, conversacion directa por Google Chat.
- Pedido trabajado: revisar Redmine 30222 y Excel adjunto `recaudos_onca_PruebasCoreINFOMEDICAL_2026-08-12.xlsx` para corregir recaudos aplicados a contratos ONCA. Regla funcional: procesar solo filas con `saldo_recibo <> 0`.
- Repositorio confirmado por Gabriel para basarse en la ventana PowerBuilder: `gpulka-Coomeva-trunk`.
- Ventana funcional de referencia: `wp_anulacion_recaudos`. Secuencia manual informada por Gabriel: buscar lote/item, seleccionar motivo de novedad division de pago, boton trasladar, seleccionar contratante usando `docu_tipo` y `docu_nro`, mostrar contratos del contratante, distribuir RC entre contratos no ONCA y aceptar.
- Caso testigo manual: lote `308818`, item `479`. Gabriel lo traslado previamente en `PruebasCoreINFOMEDICAL`; al revisar quedo sin ONCA activo y con subitems nuevos `7` y `8`.
- Script creado: `/mnt/Agentes/gpulka/R30222_traslado_recaudos_onca_rollback.sql`.
- Script generado con `BEGIN TRAN` y cierre `ROLLBACK TRAN`; crea `#recaudos_onca`, inserta 592 filas del Excel, filtra ONCA con `saldo_recibo <> 0`, resuelve `docu_tipo` desde `COMPRO_IMPORTACION` o `REGISTRO_UNICO_DOCUMENTO` si viene vacio, y distribuye el valor del item en partes iguales entre contratos destino no ONCA.
- SPs usados para replicar la ventana: `afs_compro_impor_anula_valida`, `afu_anular_recaudo` con motivo `5` / `01- DIVISION DE PAGO`, y `afu_genera_recibo_traslado` para cada contrato destino no ONCA.
- Validaciones reportadas al usuario: Redmine 30222 leido con adjunto correcto; parse SQL OK con `SET PARSEONLY ON`; archivo ASCII sin BOM y LF; sin `RTRIM`; sin finalizadores `;`.
- Diagnostico reportado: 592 filas Excel; 211 filas ONCA con `saldo_recibo <> 0`; 210 lote/item distintos a evaluar; 209 items con ONCA activo actual; 186 items activos con destino no ONCA; 23 items activos sin destino no ONCA; `308818/479` no queda activo para reprocesar.

## 2026-08-13 19:55 ART - Redmine 29364 / cambio titularidad automatico

- Usuario activo: Sebastian, conversacion directa por Google Chat.
- Contexto cargado: Redmine `29364 - Implementar proceso automatico de grabacion de cambio de titularidad`.
- Rama/repositorio de trabajo: `Version104-Afilmed-SolicitudesAutomaticas`, working copy `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Base/profile usado para pruebas: `InstallQA_89`.
- Dominio funcional: APP cambio de titularidad Afilmed, motivo `soli_moti = 7`, transaccion GMS `CT`.
- Cierre previo relevante `r37561`: `meses_cober` ya no debe venir desde XML; se calcula del lado SQL/Afilmed tomando la logica funcional de la ventana.
- Caso 1 analizado y resuelto: el XML no debe aceptar `fecha_radicacion` ni `contrato_fisico`; Afilmed completa `fecha_radicacion` con `GETDATE()` y `contrato_fisico` con `id_soli_externa`.
- Caso 1 tambien ajustado: `baja_moti` queda obligatorio para cambio de titularidad tanto con `baja_titular = S` como con `baja_titular = N`; si viene vacio/NULL debe rechazarse temprano.
- Commit realizado: SVN `r37569`, mensaje `29364: ajusta validaciones caso 1 cambio titularidad`.
- Archivos principales modificados en ese commit: `backend/procedures/afi_insertar_soli_auto_cambio_titularidad.sql`, `backend/procedures/afs_soli_auto_obligatorios.sql` y documentacion/scripts bajo `Redmines/29364`.
- Validaciones reportadas: `SET PARSEONLY ON` OK contra `InstallQA_89` para procedimientos y scripts SQL modificados; XML de ejemplos revisado para que no queden `baja_titular="N"` sin `baja_moti`; encoding/BOM/EOL preservados; working copy limpia salvo externos SVN `X` habituales.
- Resumen funcional comunicado a Sebastian: la APP solo envia los datos necesarios; Afilmed completa internamente fecha de radicacion y contrato fisico, y rechaza pedidos si falta el motivo del cambio o si se informan campos que ya no corresponden.
