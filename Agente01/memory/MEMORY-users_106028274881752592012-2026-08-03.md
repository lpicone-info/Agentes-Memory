# Memoria curada diaria

- Fecha: 2026-08-03
- Usuario: Gabriel Pulka
- id_googlechat: users/106028274881752592012
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/_rsuhyaaaae

## Resumen de la actividad

### Tema 1: Version104 - objetivo de cambios en wp_cambio_tit y wp_alta_parcial

- Solicitud o contexto: Gabriel pidió ubicarse en la working copy `gpulka-Version104-Afilmed-SolicitudesAutomaticas`, actualizarla y explicar el objetivo de los cambios en `wp_cambio_tit.srw` y `wp_alta_parcial.srw`.
- Respuesta o acciones del agente: Se actualizó la working copy a `r37489`, se revisó el diff entre `r37363` y `r37489`, `svn info`, `svn status` y `svn log`.
- Resultado: Se explicó que `wp_cambio_tit.srw` corrige la visualización del motivo real de baja en cambios de titularidad APP/Redmine 29364, evitando que parezca un cambio de parentesco. También se explicó que `wp_alta_parcial.srw` ajusta `wf_obliga_paren_real` para revisar y completar correctamente el parentesco real cuando un integrante pasa a titular.
- Estado: Resuelto.

### Tema 2: Error QUOTED_IDENTIFIER en PruebasCorePrestadores

- Solicitud o contexto: Gabriel reportó que una ejecución de `dbo.afi_insertar_soli_auto` con XML de `soli_moti = 6` fallaba en `PruebasCorePrestadores` con error por `QUOTED_IDENTIFIER`.
- Respuesta o acciones del agente: Se revisó la configuración de la sesión y de los procedimientos involucrados. Se identificó que `dbo.afs_soli_auto_obligatorios` estaba creado con `uses_quoted_identifier = False` y usaba métodos XML como `.exist`, `.value` y `.nodes`.
- Resultado: Se determinó que la causa no era el XML, sino el setting capturado al crear/alterar `afs_soli_auto_obligatorios`. A pedido de Gabriel se modificó `backend/procedures/afs_soli_auto_obligatorios.sql` y se hicieron dos commits: `r37492` para agregar `SET ANSI_NULLS ON` y `SET QUOTED_IDENTIFIER ON` al script, y `r37493` para mover esos `SET` dentro del procedimiento después del `BEGIN`, según la indicación posterior del usuario. Se validó `PARSEONLY OK`, encoding ASCII sin BOM, CRLF preservado y estado SVN limpio.
- Estado: Resuelto.

### Tema 3: Checkout SVN de Version107-Afilmed-FacturacionTarifas

- Solicitud o contexto: Gabriel pidió ejecutar la skill `svn-checkout` para una rama SVN de Version107.
- Respuesta o acciones del agente: Se solicitó URL, usuario y contraseña SVN; luego se realizó el checkout de `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version107-Afilmed-FacturacionTarifas` en `~/.openclaw/workspace/svn/gpulka-Version107-Afilmed-FacturacionTarifas`.
- Resultado: La working copy quedó registrada en `REPOSITORIOS.md`, con usuario SVN asociado `gpulka`, config SVN local y archivo de credenciales aislado. Se validó `svn info`, `svn update`, `svn status` y credencial aislada. No se guardó ni se expuso la contraseña en la memoria.
- Estado: Resuelto.

### Tema 4: Redmine 29194 - análisis técnico Mantis 9100

- Solicitud o contexto: Gabriel pidió trabajar sobre `gpulka-Version107-Afilmed-FacturacionTarifas`, revisar Redmine 29194 y el documento ubicado en `/mnt/Agentes/gpulka/29194`, identificando lo nuevo asociado a "Mantis 9100", y preparar un DOCX con análisis técnico detallado de cambios PowerBuilder y base de datos.
- Respuesta o acciones del agente: Se revisó la rama, el documento de entrada y el código relacionado. Se generó el archivo `/mnt/Agentes/gpulka/29194/REDMINE_29194_ANALISIS_TECNICO_MANTIS_9100.docx` con alcance nuevo, separación respecto de funcionalidad existente, objetos PowerBuilder/BD impactados, snippets sugeridos, criterios de aceptación, riesgos y plan de validación.
- Resultado: El DOCX fue validado con 83 párrafos, 2 tablas y 17 secciones. No se modificó código de la working copy; el `svn status` quedó solo con externals esperados.
- Estado: Resuelto.

### Tema 5: Corrección del DOCX de Redmine 29194

- Solicitud o contexto: Gabriel observó inconsistencias en el DOCX: referencias a `PruebasCorePrestadores` y dudas sobre el SP `afs_origeniados_homologacion_config`, indicando que la base de pruebas debía asumirse como `InstallQA_89`.
- Respuesta o acciones del agente: Se verificó el repositorio y la base `InstallQA_89`. Se confirmó que el archivo del repo es `backend/procedures/afs_asociados_homologacion_config.sql`, pero el procedimiento real que crea y consumen los DataWindows `de_homologacion_prog_tari_asoc.srd` y `d_homologacion_prog_tari_asoc.srd` es `dbo.afs_origeniados_homologacion_config`.
- Resultado: Como el DOCX original estaba abierto/ocupado, se generó `/mnt/Agentes/gpulka/29194/REDMINE_29194_ANALISIS_TECNICO_MANTIS_9100_REV1.docx`. Se eliminaron referencias a `PruebasCorePrestadores`, quedó `InstallQA_89` como base de pruebas y se explicitó la diferencia entre nombre de archivo y nombre real del SP.
- Estado: Resuelto.
