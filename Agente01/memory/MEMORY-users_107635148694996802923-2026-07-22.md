# Memoria curada diaria

- Fecha: 2026-07-22
- Usuario: Savina Varela
- id_googlechat: users/107635148694996802923
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/xeq2hyaaaae

## Resumen de la actividad

### Tema 1: Inicio de sesión y carga de preferencias

- Solicitud o contexto: Savina saludó al agente.
- Respuesta o acciones del agente: Se cargaron reglas, repositorios, perfiles de base de datos, la entrada de usuario y las preferencias de trabajo de Savina.
- Resultado: El agente confirmó disponibilidad para continuar.
- Estado: Informativo

### Tema 2: Prueba XML de traslado a contrato nuevo en Version104 e InstallQA_89

- Solicitud o contexto: Savina pidió probar un XML en la rama `svarela-Version104-Afilmed-SolicitudesAutomaticas` con base `InstallQA_89`, orientado a solicitudes de traslado a contrato nuevo (`soli_moti=47`), sin informar en el XML contactos de integrante ni de responsable pagador para verificar fallback desde Registro Unico.
- Respuesta o acciones del agente: Se validaron referencias de la rama SVN y del profile `InstallQA_89`. Se preparó un XML de prueba con `id_soli_externa=293630401`, contrato origen `1000142`, integrante `02`, responsable pagador distinto al titular, solo contactos obligatorios de facturación del contrato y sin anexos.
- Resultado: El XML se validó como XML válido y el identificador estaba libre antes de la ejecución. Luego, al consultar `id_proceso=133`, se confirmó que se grabó en `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES`, `SOLI_AUTO_RESP_PAGADOR`, `SOLI_AUTO_DOMICILIOS` y `SOLI_AUTO_EMAILS`, llegó a `GMS_CAB id=295778`, pero quedó con estado `E`, `puede_procesarse=N` y 7 errores por contactos faltantes de responsable pagador e integrante. También se indicó que `SOLI_AUTO_TELEFONOS` y `SOLI_AUTO_ANEXOS` no tenían registros.
- Estado: Pendiente

### Tema 3: Facturación, contactos y datos de cuenta/subcuenta en traslados automáticos

- Solicitud o contexto: Savina consultó qué datos se deben cargar para alta masiva de venta nueva, y si en traslados automáticos a contrato nuevo de colectivos se toman datos de contrato, cuenta o subcuenta.
- Respuesta o acciones del agente: Se revisó la lógica de solicitudes automáticas y de la ventana manual de traslado a contrato nuevo. Se explicó que, en el flujo automático `soli_moti=47`, los datos de facturación se leen del XML hacia `SOLI_AUTO_CONTRATO` y luego a `GMSTXT_MULTI` y `GMSDEF_MULTI`; no se observó derivación completa desde `SUBCTA_CONTRATO` diferenciada para cuentas colectivas o individuales.
- Resultado: Se indicó que para evitar errores actuales conviene informar en el XML facturación, forma de pago, domicilio/email/telefono de responsable pagador y contactos obligatorios. Para la ventana manual se identificó que los datos colectivos se obtienen por `nvo_subcta_contrato.of_inicializar`, `d_nvo_subcta_contrato` y `cls_subcta_contrato`, consultando principalmente `SUBCTA_CONTRATO`, `SUBCUENTAS`, `SUBCTA_TIPOS`, `SUBCTA_TIPOS_USOS`, `ENVIO_CREDEN` y `VTO_GRUPOS`.
- Estado: Informativo

### Tema 4: Prueba de traslado entre colectivos sin modificar datos de facturación colectiva

- Solicitud o contexto: Savina pidió un caso de traslado entre colectivos con responsable pagador distinto al titular para verificar que no se modifiquen datos de facturación, domicilios, emails, telefonos, `emi_modo` ni otros datos de cuentas/subcuentas colectivas, y que no se refacture.
- Respuesta o acciones del agente: Se consultó la base y se preparó un reporte previo con datos de origen y destino colectivos. Se propuso `id_soli_externa=293630501`, origen cuenta `1874`, subcta `5727`, contrato `1000146`, integrante origen `02`, destino cuenta `39`, subcta `128`, y responsable pagador distinto.
- Resultado: No se ejecutó carga ni batch. Se entregó un XML válido y un reporte previo de `CUENTAS`, `SUBCUENTAS`, `SUBCTA_CONTRATO`, `SUBCTA_DOMICILIOS` y `CUENTA_DOMICILIOS` para usar como comparación posterior.
- Estado: Pendiente

### Tema 5: Informe de errores GMS para cab_id 295779

- Solicitud o contexto: Savina pidió un informe de errores de `cab_id=295779`.
- Respuesta o acciones del agente: Se consultaron datos de `GMS_CAB` y `GMS_ERR`.
- Resultado: Se informó que correspondía a `id_proceso=134`, `id_soli_externa=5010`, `soli_moti=47`, estado `E`, `puede_procesarse=N`, 6 errores y 1 warning. Los errores relevantes incluyeron asesor inexistente o dado de baja, parentesco real inconsistente, datos obligatorios de empleado para cuenta facturable a empresa, cuenta no configurada en campaña y ausencia de integrante aplicable a campaña. Se destacó que el integrante quedó `paren_destino=T` y `paren_real_destino=H`.
- Estado: Informativo

### Tema 6: Reglas de parentesco real con titular y responsable pagador

- Solicitud o contexto: Savina preguntó por qué el parentesco real debía ser titular, si aplicaba a colectivo o individual, y si era nuevo, especialmente cuando el responsable pagador es distinto al titular.
- Respuesta o acciones del agente: Inicialmente se explicó que para Alta Total, si el integrante entra como titular, GMS exige `paren_destino=T` y `paren_real_destino=T`, y que aplicaba tanto a individuales como colectivos. Luego, al contrastar con una nueva prueba, se ajustó la conclusión: cuando el responsable pagador es distinto al titular, GMS rechazó `T/T` con error 8230.
- Resultado: La conclusión consolidada quedó condicionada por escenario: si titular y responsable/empleado son la misma persona, GMS espera `T/T`; si el responsable pagador es distinto al titular, GMS espera que el `paren_real_destino` del integrante titular no sea `T`.
- Estado: En curso

### Tema 7: Caso equivalente a soli_externa 293630402 y error cab_id 295781

- Solicitud o contexto: Savina pidió repetir una prueba equivalente a `soli_externa=293630402` para verificar `paren_destino=T` y `paren_real_destino=T`, con integrante titular y responsable pagador distinto.
- Respuesta o acciones del agente: Se indicó que `293630402` ya había generado solicitud y dado de baja el origen `1000142 / 02`, por lo que se propuso un caso equivalente activo con `id_soli_externa=293630403`, destino `2 / FSMP`, plan/tarifa `OPLF / FPLUNQ`, integrante Daniela Marin Villacorte como titular y responsable pagador Jose Manuel Aristizabal Gonzalez.
- Resultado: Savina ejecutó y reportó `cab_id=295781`. El agente informó que quedó con `id_proceso=136`, `estado=E`, `puede_procesarse=N`, 1 error: `8230 - El parentesco real debe ser distinto a Titular`, confirmando que para responsable pagador distinto no correspondía `T/T`.
- Estado: Informativo

### Tema 8: Caso titular igual a responsable pagador

- Solicitud o contexto: Savina pidió generar un caso donde titular y responsable pagador sean la misma persona para comprobar que `T/T` permita generar correctamente el contrato.
- Respuesta o acciones del agente: Se preparó un XML con `id_soli_externa=293630404`, usando el origen activo `996737 / 02`, integrante y responsable pagador Daniela Marin Villacorte, `paren_destino=T` y `paren_real_destino=T`.
- Resultado: Se validó el XML como `XML_OK`. Quedó como caso de prueba para ejecución y verificación posterior.
- Estado: Pendiente

### Tema 9: Habeas data en solicitud automatica y tabla final

- Solicitud o contexto: Savina preguntó por qué para `id_soli_externa=293630404` el XML tenía `habeas_data=N` pero la ventana mostraba la solapa de datos adicionales tildada, y en qué tabla se graba ese dato.
- Respuesta o acciones del agente: Se consultó el flujo de datos y la base. Se indicó que el valor viajó como `N` en `SOLI_AUTO_CONTRATO`, `GMSTXT_MULTI` y `GMSDEF_MULTI`, y que no se generó fila en `SOLI_HAB_DATA_EXCEDENTE` para la solicitud `4677113`.
- Resultado: Se explicó que si `GMSDEF_MULTI.habeas_data='S'`, el proceso inserta en `SOLI_HAB_DATA_EXCEDENTE` usando el valor configurado en `AFILMED_PARAMS` para `habeas_data`; si viene `N`, no inserta fila. Para que quede aceptado debe enviarse `habeas_data="S"` en el XML.
- Estado: Informativo

### Tema 10: Reprueba de anexos obligatorios tipo soli_externa 5001

- Solicitud o contexto: Savina pidió un caso igual a `soli_externa=5001` para reprobar que un anexo no se agregue como obligatorio cuando la configuración para cuenta `2`, subcta `FSMP`, plan `OPLF`, fecha `2026-07-01`, paren real titular, tiene `obliga=N`.
- Respuesta o acciones del agente: Se armó un XML con `id_soli_externa=293631001`, titular Angel Vicente Mendoza Montesino, responsable pagador misma persona, destino `2 / FSMP`, plan/tarifa `OPLF / FPLUNQ`, anexo `OPLB / BGFOPLB` informado explícitamente y contactos completos de contrato, responsable e integrante.
- Resultado: No se ejecutó carga ni batch. Se validó que `id_soli_externa=293631001` estaba libre, el XML era válido, `afs_soli_auto_obligatorios` respondía `SP_OK`, y que la configuración vigente de `SUBCTA_PLAN_ADI_PAREN` para `paren=T` tenía `obliga=N`. La expectativa documentada fue que el anexo no quede grabado como obligatorio del contrato.
- Estado: Pendiente
