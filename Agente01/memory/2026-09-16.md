# 2026-09-16

## Redmine 30394 - Bancos / Debito automatico Afilmed
- Conversacion con Belkis por Google Chat. Usuario pidio usar credencial Redmine `bmora` para leer issue `30394`.
- Redmine `30394`: proyecto `COOMEVA - Soporte`, tracker `Defecto`, estado `En Analisis`, prioridad `Media`, autor/asignado Belkis Josefina Garcia Mora, asunto `Configuracion => Gestion Bancos`, producto `Afilmed`, incidente externo/NC `12010`, adjunto `exportacion de debitos.txt`.
- Pedido del cliente: revisar/corregir bancos parametrizados usados en `Solicitudes > Novedades de debito automatico > Exportacion de novedades` y `Cambio de forma de pago > Bancos`; casos `RappiPay` figura `1811` y debe ser `811`; `Banco Falabella` duplicado, conservar codigo correcto `062`; revisar catalogo de debitos, corregir codigos inconsistentes y depurar duplicados.
- Repositorios solicitados originalmente: `bmora-Coomeva-trunk`, `bmora-ProyectoCoomeva-Afilmed-01.Documentacion-Especificaciones-Funcionales-Afilmed`, y `bmora-ProyectoCoomeva-Afilmed-01.Documentacion-Req y Doc. Complementaria`. El tercero no valido/no existe en `REPOSITORIOS.md` ni en `svn/`; Belkis autorizo trabajar solo con los dos validados.
- Resultado tecnico entregado: el menu `Configuracion > Facturacion y Cuentas Corrientes` existe en `exe_afilmed/src/m_afilia.srm`; contiene `Bancos` -> `wp_bancos`, `Ente recaudador` -> `wp_ente_recaudador`, `Formas de Pago o Cobro` -> `wp_pago_formas`.
- ABM `Bancos`: usa tabla `BANCOS`; columna `banco` es clave primaria (`backend/tables/BANCOS.sql`). Procedure `afu_bancos` solo actualiza `deno_bco`, `baja_fecha`, `modi_fecha`; no cambia codigo `banco`. Procedure `afd_bancos` hace baja logica por `baja_fecha`. Procedure `afs_cons_bancos` valida relaciones vigentes en `SOLI_PAGO_FORMAS` y `AFI_PAGO_FORMAS` antes de bajar.
- Conclusion funcional: `Banco Falabella` incorrecto puede inactivarse desde ABM si no bloquean relaciones; `RappiPay 1811 -> 811` no se corrige editando codigo, porque el codigo es clave/no editable por update; via ABM habria que crear `811`, reasociar donde corresponda e inactivar `1811` si queda sin relaciones. La revision contra catalogo puede hacerse manualmente por ABM, pero no se encontro comparador automatico.
- Asociacion bancos/formas validas para ente recaudador: desde `Ente recaudador > Formas de Pago`, graba en `ENTE_REC_PAGO_FORMAS` (`exe_afilmed/src/wr_ente_rec_pago_formas.srw`).
- Origen del selector `Ente Recaudador` en ventana `Exportacion de Novedades`: `wp_exportacion_novedades_da.srw`, DataWindow `de_exportacion_novedades_da.srd`, llama `afs_ente_recaudador @accion = 3`; procedure real `backend/procedures/afs_ente_recaudador_ac3.sql`; tabla fuente `ENTE_RECAUDADOR`; filtros `baja_fecha IS NULL` y `enti_tipo_imp IS NOT NULL`. ABM mantenedor: `Configuracion > Facturacion y Cuentas Corrientes > Ente recaudador`, ventana `wp_ente_recaudador`, tabla `ENTE_RECAUDADOR`.

## Redmine 30345 - Venta nueva colectiva / APP a GMS
- Conversacion con Sebastian por Google Chat. Usuario pidio continuar Redmine `30345`.
- Trabajo previo recuperado y confirmado: paso 3 del Redmine `30345` implementado sobre SQL legacy. Se modifico `procedures/afu_genera_soli_auto_app.sql` para tomar `SOLI_AUTO_ADICIONALES` cuando `soli_moti = 1` y cargarlo en `GMSTXT_ADICIONALES`, permitiendo que GMS complete el circuito normal de adicionales.
- Tambien se modifico `procedures/gms_genera_solicitudes.sql`: para origen `APP` y `soli_moti = 1`, vuelca desde `SOLI_AUTO_*` hacia `SOLI_*` datos especificos como certificados, gravados, habeas data excedente, estado de salud, medico, impresion de certificados, notas y observaciones.
- Validaciones informadas al usuario: diff acotado a esos 2 archivos, encoding/BOM/finales de linea preservados, sin `RTRIM`, sin finalizadores `;`, sin commit. No se compilo contra DB porque no se habia indicado profile.
- Usuario reporto que la prueba `/mnt/Agentes/srmontenegro/redmine_30345_venta_nueva_303450013_colectivo_ok_exec.sql` fallaba al compilar con `Mensaje 50000... procedimiento afi_insertar_soli_auto... El XML no debe informar responsable_pagador para venta nueva colectiva`.
- Correccion aplicada en la prueba colectiva: en `/mnt/Agentes/srmontenegro/redmine_30345_venta_nueva_303450013_colectivo_ok_exec.sql` se reemplazo `<responsable_pagador>` por `<empleado>`, se agregaron `codigo_nomina`, `centro_costo`, `sucursal_empresa` y `oficina_empresa`, y los contactos del empleado pasaron de `tipo_reg="R"` a `tipo_reg="E"`.
- Validaciones de la prueba corregida: XML embebido parsea OK, ya no queda `responsable_pagador`, ya no queda `tipo_reg="R"`, archivo ASCII sin BOM y LF uniforme.

- En la prueba colectiva, con `InstallQA_89`, la combinacion inicial `56/2242/OPLC/APLU890300625` para fecha de aplicacion `2026-10-01` fallo por motivos DEF `5044`, `5045`, `5160`, `5163` (cuenta/subcuenta/programa/tarifa baja, inhabilitada o no vigente). Se reemplazo por `cuenta=56`, `subcta=1028`, `plan=OP02`, `tari=APLU890300625`, tambien para `plan_emp_dest`/`tari_emp_dest`; se valido vigente en base.
- La misma prueba fallo luego con motivo `8101`: la sucursal comercializable del XML era `05001`, pero para subcuenta `56/1028` en `InstallQA_89` correspondia `08001` (`partido_comercializable`). Se actualizo `suc_comercializable="08001"` en `redmine_30345_venta_nueva_303450013_colectivo_ok_exec.sql`.
- El batch informaba `No se pudo recuperar referencia de solicitud APP. Error: Mismatch between retrieve columns and fetch columns`. La causa estaba en `exe_afilmed/src/wp_main_solicitudes.srw`, funcion `wf_loguear_soli_auto_app`: uso de `nvo_proceso_agenda.of_procedure` con DataStore dinamico/SyntaxFromSQL para el SP `dbo.afs_soli_auto_app_log_resumen`. Se reemplazo por cursor SQL embebido con FETCH explicito de las 3 columnas reales: `id_soli_externa`, `soli`, `mensaje_app`.
- Se valido contra `InstallQA_89` que el SP devuelve exactamente esas 3 columnas; se preservo UTF-8 con BOM y CRLF del `.srw`; no se compilo PowerBuilder localmente.
- El cambio fue committeado en SVN revision `r37822`, autor `srmontenegro@INFOMEDICAL`, mensaje `Redmine 30345 - Completar venta nueva APP y corregir log de referencia`. Archivos incluidos: `backend/procedures/afu_genera_soli_auto_app.sql`, `backend/procedures/gms_genera_solicitudes.sql`, `exe_afilmed/src/wp_main_solicitudes.srw`. Working copy sin cambios propios pendientes; solo externos SVN marcados con X.
- Ante la pregunta de por que se cambiaron los procedimientos: no fueron por el `Mismatch`; corresponden a la funcionalidad faltante del Redmine 30345 para completar venta nueva APP. El `.srw` fue el arreglo del log del batch.
- Para una venta nueva de asociado familiar, no se identifico otro `id_grupo` del XML: se mantiene `id_grupo="1"` como identificador externo y se usa la cobertura familiar validada en `InstallQA_89`: `cuenta_dest="2"`, `subcta_dest="FSMP"`, `plan_dest="OPLF"`, `tari_dest="FPLUNQ"`, `plan_emp_dest="OPLF"`, `tari_emp_dest="FPLUNQ"`. La base describe `OPLF` como `ORO PLUS FAMILIAR`, con `cambio_asoc_fam=S`, `plan_grupo=OPL` y `subcta_tipo=F`.


## Investigación Afilmed: auditorías y datos de profesión/ocupación
- Conversación con Savina sobre base `PruebasCoreINFOMEDICAL`, SVN trunk y documentación EF de `ProyectoCoomeva/Afilmed/01.Documentacion/Especificaciones Funcionales Afilmed`.
- En las EF se encontraron definiciones funcionales de estados de auditoría, principalmente en `A-04-DEF Solicitudes E2 - Precarga_V09.docx`, además de `A-04-DEF Solicitudes_Cambios en Impresión de Certificados_V05.docx`, `A-04-DEF Solicitudes E1 - 2_V20.docx` y `A-04-DEF Solicitudes E2 - Cambios Reglas de Auditoria_V07.docx`. Las EF describen estados, pero no una tabla explícita con los códigos `ASI`, `AEA`, `ACP`, `CON`, `EMA`, `EMI`.
- Equivalencias funcionales identificadas: `ASI` = Aceptado sin Auditoría; `AEA` = Aceptado en/con Auditoría Médica de Ingreso; `ACP` = Aceptado con Preexistencias; `CON` = Condicionado; `EMA` = Exámenes Médicos Adicionales; `EMI` = Examen Médico de Ingreso. También aparece Negado en la descripción funcional. Los códigos existen en `SOLI_ESTADO_AUDITORIA.estado`.
- Profesión y ocupación en ventanas manuales: no se encontró validación general que las marque obligatorias en `wp_alta_total.srw`, `wp_alta_parcial.srw`, `wp_cambio_datos_identificacion.srw` ni `wp_cambio_resp_pagador.srw`. En solicitudes automáticas la base las exige para traslado a contrato nuevo (`soli_moti = 47`), cambio de responsable pagador (`22`) y cambio de datos de empleado (`49`); regla en `backend/procedures/afs_soli_auto_obligatorios.sql`, referencias aproximadas líneas 607-656 y 1288-1311.
- Para modificar datos: beneficiario existente en `Solicitudes > Cambio de Datos Generales Beneficiarios`; altas/inclusiones en la grilla de integrantes de Venta Nueva o Alta Parcial; responsable pagador en Cambio de Responsable Pagador. En `wp_cambio_datos_identificacion.srw` y `wp_cambio_resp_pagador.srw` `ocupacion` aparece comentada en la configuración de combos, posible causa de que no cargue como lista.
- Catálogos: tabla `PROFESIONES` y tabla `OCUPACION`; el mantenimiento de profesiones aparece como opción `Profesiones` en menú de afiliaciones. Los datos asignados al afiliado se almacenan en `AFILIADOS.profesion` y `AFILIADOS.ocupacion`.
- Solapa `Datos Varios` de `Consulta General de Usuarios`: ventana `exe_afilmed/src/wp_consulta_afiliados.srw`, tab `tp_varios`, DataWindow `d_consulta_afis_varios`; recupera con `tab_consulta_afiliados.tp_varios.dw_varios.retrieve(ll_prepaga, ls_contra)`. Consulta mediante `backend/procedures/afs_consulta_afis_varios.sql`, procedimiento `dbo.afs_consulta_afis_varios`.
- Origen exacto en esa consulta: `AFILIADOS.profesion` y `AFILIADOS.ocupacion`; se traducen mediante `LEFT JOIN PROFESIONES PR ON A.profesion = PR.profesion` y `LEFT JOIN OCUPACION OCU ON OCU.ocupacion = A.ocupacion`; muestra `PR.deno` como `profesion_deno` y `OCU.deno` como `ocu_deno`. Referencias aproximadas en el procedure: líneas 25-31 y 74-81. No toma esos textos desde `REGISTRO_UNICO`.


## Cierre parcial Redmine 30345
- El usuario confirmó que la prueba de venta nueva colectiva con XML `id_soli_externa=303450015`, `soli_moti=1`, cobertura `56/1028/OP02/APLU890300625`, `suc_comercializable=08001` generó contrato correctamente y quedó validada como exitosa.
- Se concluyó que las variantes probadas hasta ese momento son: venta nueva individual (exitosa) y venta nueva colectiva (exitosa). La variante familiar todavía quedaba pendiente de ejecución.
- Se generó el archivo `/mnt/Agentes/srmontenegro/redmine_30345_venta_nueva_asociado_familiar.sql` para una venta nueva de asociado con plan familiar: `soli_moti=1`, cobertura `2/FSMP/OPLF/FPLUNQ`, ID externa/contrato `303450016`, `responsable_pagador` incluido, sin nodo `empleado`, sucursal `05001`. El XML fue validado y el ID se verificó libre en `SOLI_AUTO_CONTRATO`, `GMS_CAB` y `SOLICITUDES`.
- El SQL de venta nueva familiar no fue ejecutado automáticamente; quedó preparado para correrlo en `InstallQA_89` y luego probar el batch corregido de `wp_main_solicitudes.srw` incluido en SVN `r37822`. La expectativa es que ya no aparezca `Mismatch between retrieve columns and fetch columns`, pero aún faltaba evidencia de ejecución posterior.
- El cambio del batch ya estaba commiteado en SVN revision `r37822` junto con los dos procedimientos funcionales del Redmine 30345.
