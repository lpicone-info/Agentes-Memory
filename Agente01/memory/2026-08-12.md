# Memoria 2026-08-12

## Redmine 30222 - COOMEVA/Afilmed importacion de recaudos ONCA
- Usuario: Belkis. Conversacion por Google Chat directo.
- Belkis pidio leer Redmine `30222` con su credencial de Redmine y luego validar el defecto en repositorios `bmora-ProyectoCoomeva-Afilmed-01.Documentacion` y `bmora-Coomeva-trunk`, ademas del profile de BD `PruebasCoreINFOMEDICAL`.
- Redmine `30222`: Defecto de proyecto `COOMEVA - Soporte`, estado `En Analisis`, prioridad media, asignado a Belkis. Asunto: `Cuentas Corrientes / Importacion de Recaudos => Cargue de pagos`. Cliente reporta que Afilmed distribuye automaticamente saldos a favor o pagos superiores al valor facturado entre contratos activos del usuario, incluyendo contratos `ONCA`. Regla esperada: los pagos no deben aplicarse a contratos ONCA porque afecta conciliacion bancaria y contable. Producto `Afilmed`, origen `Cliente`, incidente externo/NC `11900`, version `TRUNK`, fecha inicio `2026-08-12`, vencimiento `2026-08-14`, adjunto `ONCA.xlsx`, sin journals.
- Conclusion tecnica comunicada: reclamo consistente. La logica actual de importacion/distribucion no tiene exclusion explicita para `ONCA`, `plan_codi = ONCA` o `subcta = 56001`; por eso puede tratar ONCA como contrato/subcuenta activa normal.
- Evidencia en `bmora-Coomeva-trunk`: `backend/procedures/afu_importacion_cobranzas_procesa.sql` identifica recaudos por codigo unico, contrato o documento alrededor de linea 183; hay bloque antiguo comentado para excluir linea `CEM` alrededor de linea 207, pero no equivalente ONCA. `backend/procedures/afu_imp_cob_x_contrato.sql` agrega contrato a cancelar sin validar plan ONCA alrededor de linea 30. `backend/procedures/afu_imp_cob_x_documento.sql` agrega contratos/subcuentas candidatas sin exclusion ONCA alrededor de linea 889. `backend/procedures/afu_procesa_cobranza_item.sql` distribuye por saldo vencido/corriente entre candidatos alrededor de linea 648 y genera recibo/guarda en `COMPRO_IMPORTACION_DET` alrededor de linea 1265.
- Evidencia documental: en repo `bmora-ProyectoCoomeva-Afilmed-01.Documentacion`, documento `A-04-DEF Ctas Ctes 1 - Importacion de Cobranzas_V17_Cambio.docx` describe que el proceso determina contratos/subcuentas donde aplicar el recaudo y puede generar mas de un recibo por recaudo. No se encontro exclusion funcional escrita para ONCA en documentos revisados.
- Evidencia Excel Redmine: casos lote `310016`, item `84`, documento `CC 93388403`, linea aplicada a `subcta 56001`; y lote `310510`, item `19`, documento `CC 1130653931`, linea aplicada a `subcta 56001`.
- Evidencia BD `PruebasCoreINFOMEDICAL`: los lotes del Excel no existian aun, consistente con que la BD no tenia ultimos pagos de agosto. Pero se encontraron recaudos cargados a `subcta = 56001` en `COMPRO_IMPORTACION_DET`, incluso agosto: `308818 / 479 / sub_item 5`, recibo `RC XSCMP10088526`, alta `2026-08-04`, contrato `410955`; `309292 / 14 / sub_item 1`, recibo `RC XSCMP10087039`, alta `2026-08-04`, contrato `1200360`.
- Cruce de contratos con planes: ambos figuran con `cuenta = 56`, `subcta = 56001`, `plan_codi = ONCA`, `plan_grupo = ONCO`, `plan_grupo_deno = ONCOLOGICO ASOCIADOS`.
- Validaciones hechas: lectura de repos registrados, busqueda acotada en fuentes PowerBuilder/SQL, extraccion temporal del Excel del Redmine, conexion a `PruebasCoreINFOMEDICAL`, consultas de lotes del Excel y consultas de `COMPRO_IMPORTACION_DET`, `COMPROBANTES`, `SUBCUENTAS`, `AFI_CLASE`, `AFI_PLANES`, `PLANES`, `PLANES_GRUPOS`. No se modificaron archivos ni datos.


## Redmine 29364 - Solicitudes Automaticas / Cambio de titularidad
- Usuario: Sebastian. Conversacion por Google Chat directo.
- Contexto: Redmine `29364` sobre cambio de titularidad en Afilmed/Solicitudes Automaticas. Profile usado para pruebas/busquedas: `InstallQA_89`.
- Se buscaron contratos vigentes con mas de un integrante para reproducir sin inventar integrantes. Caso util validado para XML: contrato `1040786`, prepaga `1`, integrantes vigentes `01 - T - VELASQUEZ FELIPE` y `02 - C - PAREDES NATHALY`; ejemplo de cambio: `inte_nuevo_titular="02"`, `baja_titular="N"`, `paren_titular_anterior="C"`, parentescos `01 -> C`, `02 -> T`. Caso descartado: contrato `1073909`, porque en `AFI_HISTO_PAREN` el titular vigente por fecha de aplicacion era `02`, aunque otra vista/DW parecia mostrar `01`.
- Regla funcional aclarada para casos 2 y 3 del documento enviado: son explicacion, no arreglo. Siempre que el XML informa `inte_nuevo_titular`, ese mismo integrante debe venir en `parentescos` con `paren_nuevo = T`, independientemente de `baja_titular`.
- Cambio implementado en repo `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`: `meses_cobertura` deja de ser parametro valido de recepcion XML. Si el XML lo informa, se rechaza con mensaje `El XML no debe informar meses_cobertura para cambio de titularidad`.
- En el mismo repo, `SOLICITUDES.meses_cober` se calcula en `gms_genera_solicitudes` copiando/adaptando la logica de la ventana: baja de titular por motivo fallecimiento y calculo desde planes/seguros vigentes del titular anterior.
- Se actualizo documentacion y ejemplos del Redmine `29364` para eliminar `meses_cobertura` del XML.
- Validaciones realizadas antes de responder: `PARSEONLY` SQL contra `InstallQA_89` OK, preservacion de encoding/BOM/EOL, busqueda residual de `meses_cobertura` quedando solo en la validacion que lo rechaza, sin nuevos `RTRIM` ni `;` agregados por el diff.
- Estado final comunicado: archivos impactados 10 entre backend SQL y documentos/scripts de `Redmines/29364`; no se hizo commit ni despliegue de procedimientos en base.


## Version 104 - Solicitudes Automaticas / Responsable pagador y empleado
- Usuario: Savina. Conversacion por Google Chat directo.
- Rama revisada: `svarela-Version104-Afilmed-SolicitudesAutomaticas`. BD/profile mencionado y validado: `InstallQA_89`.
- Consulta funcional: en XML de solicitudes automaticas version 104 para `soli_moti = 47`, que pasa si se cargan datos tanto en nodo/titulo `responsable_pagador` como en `empleado`; cual toma para generar la solicitud y si se graban ambos.
- Conclusion comunicada: no hay eleccion excluyente entre ambos nodos. Si el XML trae `responsable_pagador` y `empleado`, se graban y procesan como conceptos distintos.
- Evidencia de codigo: `afi_insertar_soli_auto_traslado_nuevo.sql` lee `/solicitud/responsable_pagador` e inserta en `SOLI_AUTO_RESP_PAGADOR`; luego lee `/solicitud/empleado` e inserta en `SOLI_AUTO_EMPLEADO`.
- Evidencia de generacion: `afu_genera_soli_auto_app.sql` toma `SOLI_AUTO_RESP_PAGADOR` para `GMSTXT_RESP_PAGADOR` mediante `gmsTXT_RESP_PAGADOR_ins`; y toma `SOLI_AUTO_EMPLEADO` para generar `GMSTXT_INTEGRANTES` con `inte = '00'` y `legajo = 'E'`.
- Respuesta tecnica dada: el responsable pagador representa el responsable pagador de la solicitud/contrato; el empleado no reemplaza al responsable pagador, sale por circuito de integrantes como empleado.
- Se preparo un XML base de prueba con ambos nodos cargados y documentos/nombres claramente distintos: responsable pagador `docu_nro="990001001"` con nombres `RESPPAGADOR_*_TEST`; empleado `docu_nro="990002002"` con nombres `EMPLEADO_*_TEST`. Se indico reemplazar valores `CAMBIAR_*` por datos validos y usar `id_soli_externa` unico.
- Validacion esperada del caso: `SOLI_AUTO_RESP_PAGADOR` debe contener documento `990001001`; `SOLI_AUTO_EMPLEADO` debe contener documento `990002002`; luego en GMS, `GMSTXT_RESP_PAGADOR` debe reflejar el responsable y `GMSTXT_INTEGRANTES` debe reflejar el empleado con `legajo = 'E'`.
- No se ejecuto el XML contra la base; solo se hizo analisis de codigo/objetos y se entrego XML de prueba.


## Redmine 29194 - Cambio cobertura cooperativa / solicitudes impactadas
- Usuario: Gabriel. Conversacion por Google Chat directo.
- Contexto pedido: ponerse en contexto de cambios para Redmine `29194`, rama `gpulka` version `107`, contrato de prueba `1056593`, base/profile `InstallQA_89`.
- Repositorio trabajado: `svn/gpulka-Version107-Afilmed-FacturacionTarifas`.
- Analisis inicial del caso: prefacturacion `prefac_cab = 1120199`, periodo `05/2025`, usuario `gpulka`; `VERIF_CONTRA_PREFACTURA` quedo `sin_errores = S` y `con_soli_aut = N`; sin registros en `VERIF_AFIL_ERRORES`; `GMS_CAB` creo cabecera automatica con `nombre_archivo = 1120199`, pero no inserto contratos en `GMSTXT_CLASE_PLAN`.
- Solicitudes relevantes del contrato `1056593`: automatica previa `4550722`, asociada a `prefac_cab = 1088193`, cambio `PREFA/APREFTE -> PREFA/FPREFTE`, `cambio_asoc_fam = S`; pruebas nuevas `4677172` cambio `PREFA/FPREFTE -> PREFA/APREFTE` y `4677173` cambio `PREFA/APREFTE -> ASOA/AASOCQ`.
- Diagnostico comunicado: al quedar cobertura vigente desde `2025-05-01` en `ASOA/AASOCQ`, y `PLANES.ASOA` tener `cambio_asoc_fam = N`, el contrato no calificaba para `afi_cambio_cober_cooperativa`; comportamiento consistente con datos y homologacion configurada `ASMP / PREFA / APREFTE -> FPREFTE`.
- Pedido posterior de Gabriel: en `afi_cambio_cober_cooperativa`, los `EXISTS` por solicitudes generadas deben considerar solo solicitudes impactadas con `SOL.estado = 'F'`; como estaba, podia considerar solicitudes no impactadas.
- Cambio implementado en `backend/procedures/afi_cambio_cober_cooperativa.sql`: se agrego filtro `SOL.estado = 'F'` en los cinco `EXISTS` que evaluan solicitudes para calcular/elegir `fecha_aplicacion_cambio`, para que solo habiliten solicitudes impactadas.
- Validaciones realizadas: `PARSEONLY OK` contra `InstallQA_89`; se preservo encoding UTF-8 sin BOM y finales de linea CRLF; `svn diff` quedo limpio despues del commit.
- Commit SVN realizado: revision `r37562`, mensaje `Redmine 29194 - Filtra solicitudes impactadas en cambio asoc fam`.


## Redmine 29363 - Solicitudes Automaticas / Traslado a contrato nuevo / Caso 21
- Usuario: Sebastian. Conversacion por Google Chat directo. Rama de trabajo/contexto: `Version104-Afilmed-SolicitudesAutomaticas`, repo usado en turnos previos `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`. Profile de BD usado para pruebas/busquedas: `InstallQA_89`.
- Redmine `29363`: Traslado a Contrato Nuevo automatico desde APP, `soli_moti = 47`. Flujo base recordado: `afi_insertar_soli_auto` -> `afi_insertar_soli_auto_traslado_nuevo` -> `afu_genera_soli_auto_app` -> `gms_validar_todo` -> `gms_genera_solicitudes` -> `afi_impacta_alta_total`; para motivo 47 luego dispara `afi_genera_solis_aut_traslados`.
- Caso 21 analizado: cuenta destino `39` en QA figura como colectiva (`es_colectivo = S`). En cuenta colectiva debe venir solo nodo `<empleado>`; en cuenta individual debe venir solo `<responsable_pagador>`. No deben venir ambos nodos juntos.
- Diagnostico comunicado: es arreglo de codigo. El proceso permitia responsable_pagador y empleado juntos, y ademas el empleado podia terminar pasando a GMS como `GMSTXT_INTEGRANTES` con `inte = 00`, `legajo = E`, disparando validaciones de integrante real como origen incompleto (`anter_prepa`, `anter_contra`, `anter_inte`) y faltantes de domicilio/telefono/email. Eso no corresponde: empleado no es integrante trasladado.
- Cambios pedidos por Sebastian: aplicar cambios, crear documento funcional para persona no tecnica, crear ejemplos de distintos rechazos y un ejemplo que permita el traslado, y al terminar comitear.
- Cambios que se debian aplicar segun analisis: en `afs_soli_auto_obligatorios`, validar exclusividad entre `<responsable_pagador>` y `<empleado>`; para cuenta colectiva exigir empleado y rechazar responsable pagador; para cuenta individual exigir responsable pagador y rechazar empleado; validar obligatorios de empleado y contactos `tipo_reg = E` con fallback a Registro Unico si corresponde. En `afi_insertar_soli_auto_traslado_nuevo`, mantener carga separada solo con XML ya validado. En `afu_genera_soli_auto_app`, revisar pasaje del empleado para que no dispare validaciones de integrante trasladado. En `gmsdef_val_datos_ident_inte`, las validaciones de origen traslado deben aplicar solo a integrantes reales (`legajo = I`), no a empleado (`legajo = E`). En `afu_soli_auto_app_contactos_gmstxt`, revisar mapeo de contactos `tipo_reg = E` para empleado.
- Estado al momento del flush: el asistente ya habia empezado a aplicar cambios y editar documentos/scripts con multiples `apply_patch` y validaciones por terminal, pero no habia enviado aun respuesta final ni consta en la conversacion compendiada que el commit SVN estuviera completado. Al retomar, verificar `svn status`, `svn diff`, parse/validaciones y si ya existe revision antes de comitear o responder.
# Memoria 2026-08-12

## Redmine 30222 - COOMEVA/Afilmed importacion de recaudos ONCA
- Usuario: Belkis. Conversacion por Google Chat directo.
- Belkis pidio leer Redmine `30222` con su credencial de Redmine y luego validar el defecto en repositorios `bmora-ProyectoCoomeva-Afilmed-01.Documentacion` y `bmora-Coomeva-trunk`, ademas del profile de BD `PruebasCoreINFOMEDICAL`.
- Redmine `30222`: Defecto de proyecto `COOMEVA - Soporte`, estado `En Analisis`, prioridad media, asignado a Belkis. Asunto: `Cuentas Corrientes / Importacion de Recaudos => Cargue de pagos`. Cliente reporta que Afilmed distribuye automaticamente saldos a favor o pagos superiores al valor facturado entre contratos activos del usuario, incluyendo contratos `ONCA`. Regla esperada: los pagos no deben aplicarse a contratos ONCA porque afecta conciliacion bancaria y contable. Producto `Afilmed`, origen `Cliente`, incidente externo/NC `11900`, version `TRUNK`, fecha inicio `2026-08-12`, vencimiento `2026-08-14`, adjunto `ONCA.xlsx`, sin journals.
- Conclusion tecnica comunicada: reclamo consistente. La logica actual de importacion/distribucion no tiene exclusion explicita para `ONCA`, `plan_codi = ONCA` o `subcta = 56001`; por eso puede tratar ONCA como contrato/subcuenta activa normal.
- Evidencia en `bmora-Coomeva-trunk`: `backend/procedures/afu_importacion_cobranzas_procesa.sql` identifica recaudos por codigo unico, contrato o documento alrededor de linea 183; hay bloque antiguo comentado para excluir linea `CEM` alrededor de linea 207, pero no equivalente ONCA. `backend/procedures/afu_imp_cob_x_contrato.sql` agrega contrato a cancelar sin validar plan ONCA alrededor de linea 30. `backend/procedures/afu_imp_cob_x_documento.sql` agrega contratos/subcuentas candidatas sin exclusion ONCA alrededor de linea 889. `backend/procedures/afu_procesa_cobranza_item.sql` distribuye por saldo vencido/corriente entre candidatos alrededor de linea 648 y genera recibo/guarda en `COMPRO_IMPORTACION_DET` alrededor de linea 1265.
- Evidencia documental: en repo `bmora-ProyectoCoomeva-Afilmed-01.Documentacion`, documento `A-04-DEF Ctas Ctes 1 - Importacion de Cobranzas_V17_Cambio.docx` describe que el proceso determina contratos/subcuentas donde aplicar el recaudo y puede generar mas de un recibo por recaudo. No se encontro exclusion funcional escrita para ONCA en documentos revisados.
- Evidencia Excel Redmine: casos lote `310016`, item `84`, documento `CC 93388403`, linea aplicada a `subcta 56001`; y lote `310510`, item `19`, documento `CC 1130653931`, linea aplicada a `subcta 56001`.
- Evidencia BD `PruebasCoreINFOMEDICAL`: los lotes del Excel no existian aun, consistente con que la BD no tenia ultimos pagos de agosto. Pero se encontraron recaudos cargados a `subcta = 56001` en `COMPRO_IMPORTACION_DET`, incluso agosto: `308818 / 479 / sub_item 5`, recibo `RC XSCMP10088526`, alta `2026-08-04`, contrato `410955`; `309292 / 14 / sub_item 1`, recibo `RC XSCMP10087039`, alta `2026-08-04`, contrato `1200360`.
- Cruce de contratos con planes: ambos figuran con `cuenta = 56`, `subcta = 56001`, `plan_codi = ONCA`, `plan_grupo = ONCO`, `plan_grupo_deno = ONCOLOGICO ASOCIADOS`.
- Validaciones hechas: lectura de repos registrados, busqueda acotada en fuentes PowerBuilder/SQL, extraccion temporal del Excel del Redmine, conexion a `PruebasCoreINFOMEDICAL`, consultas de lotes del Excel y consultas de `COMPRO_IMPORTACION_DET`, `COMPROBANTES`, `SUBCUENTAS`, `AFI_CLASE`, `AFI_PLANES`, `PLANES`, `PLANES_GRUPOS`. No se modificaron archivos ni datos.


## Redmine 29364 - Solicitudes Automaticas / Cambio de titularidad
- Usuario: Sebastian. Conversacion por Google Chat directo.
- Contexto: Redmine `29364` sobre cambio de titularidad en Afilmed/Solicitudes Automaticas. Profile usado para pruebas/busquedas: `InstallQA_89`.
- Se buscaron contratos vigentes con mas de un integrante para reproducir sin inventar integrantes. Caso util validado para XML: contrato `1040786`, prepaga `1`, integrantes vigentes `01 - T - VELASQUEZ FELIPE` y `02 - C - PAREDES NATHALY`; ejemplo de cambio: `inte_nuevo_titular="02"`, `baja_titular="N"`, `paren_titular_anterior="C"`, parentescos `01 -> C`, `02 -> T`. Caso descartado: contrato `1073909`, porque en `AFI_HISTO_PAREN` el titular vigente por fecha de aplicacion era `02`, aunque otra vista/DW parecia mostrar `01`.
- Regla funcional aclarada para casos 2 y 3 del documento enviado: son explicacion, no arreglo. Siempre que el XML informa `inte_nuevo_titular`, ese mismo integrante debe venir en `parentescos` con `paren_nuevo = T`, independientemente de `baja_titular`.
- Cambio implementado en repo `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`: `meses_cobertura` deja de ser parametro valido de recepcion XML. Si el XML lo informa, se rechaza con mensaje `El XML no debe informar meses_cobertura para cambio de titularidad`.
- En el mismo repo, `SOLICITUDES.meses_cober` se calcula en `gms_genera_solicitudes` copiando/adaptando la logica de la ventana: baja de titular por motivo fallecimiento y calculo desde planes/seguros vigentes del titular anterior.
- Se actualizo documentacion y ejemplos del Redmine `29364` para eliminar `meses_cobertura` del XML.
- Validaciones realizadas antes de responder: `PARSEONLY` SQL contra `InstallQA_89` OK, preservacion de encoding/BOM/EOL, busqueda residual de `meses_cobertura` quedando solo en la validacion que lo rechaza, sin nuevos `RTRIM` ni `;` agregados por el diff.
- Estado final comunicado: archivos impactados 10 entre backend SQL y documentos/scripts de `Redmines/29364`; no se hizo commit ni despliegue de procedimientos en base.


## Version 104 - Solicitudes Automaticas / Responsable pagador y empleado
- Usuario: Savina. Conversacion por Google Chat directo.
- Rama revisada: `svarela-Version104-Afilmed-SolicitudesAutomaticas`. BD/profile mencionado y validado: `InstallQA_89`.
- Consulta funcional: en XML de solicitudes automaticas version 104 para `soli_moti = 47`, que pasa si se cargan datos tanto en nodo/titulo `responsable_pagador` como en `empleado`; cual toma para generar la solicitud y si se graban ambos.
- Conclusion comunicada: no hay eleccion excluyente entre ambos nodos. Si el XML trae `responsable_pagador` y `empleado`, se graban y procesan como conceptos distintos.
- Evidencia de codigo: `afi_insertar_soli_auto_traslado_nuevo.sql` lee `/solicitud/responsable_pagador` e inserta en `SOLI_AUTO_RESP_PAGADOR`; luego lee `/solicitud/empleado` e inserta en `SOLI_AUTO_EMPLEADO`.
- Evidencia de generacion: `afu_genera_soli_auto_app.sql` toma `SOLI_AUTO_RESP_PAGADOR` para `GMSTXT_RESP_PAGADOR` mediante `gmsTXT_RESP_PAGADOR_ins`; y toma `SOLI_AUTO_EMPLEADO` para generar `GMSTXT_INTEGRANTES` con `inte = '00'` y `legajo = 'E'`.
- Respuesta tecnica dada: el responsable pagador representa el responsable pagador de la solicitud/contrato; el empleado no reemplaza al responsable pagador, sale por circuito de integrantes como empleado.
- Se preparo un XML base de prueba con ambos nodos cargados y documentos/nombres claramente distintos: responsable pagador `docu_nro="990001001"` con nombres `RESPPAGADOR_*_TEST`; empleado `docu_nro="990002002"` con nombres `EMPLEADO_*_TEST`. Se indico reemplazar valores `CAMBIAR_*` por datos validos y usar `id_soli_externa` unico.
- Validacion esperada del caso: `SOLI_AUTO_RESP_PAGADOR` debe contener documento `990001001`; `SOLI_AUTO_EMPLEADO` debe contener documento `990002002`; luego en GMS, `GMSTXT_RESP_PAGADOR` debe reflejar el responsable y `GMSTXT_INTEGRANTES` debe reflejar el empleado con `legajo = 'E'`.
- No se ejecuto el XML contra la base; solo se hizo analisis de codigo/objetos y se entrego XML de prueba.


## Redmine 29194 - Cambio cobertura cooperativa / solicitudes impactadas
- Usuario: Gabriel. Conversacion por Google Chat directo.
- Contexto pedido: ponerse en contexto de cambios para Redmine `29194`, rama `gpulka` version `107`, contrato de prueba `1056593`, base/profile `InstallQA_89`.
- Repositorio trabajado: `svn/gpulka-Version107-Afilmed-FacturacionTarifas`.
- Analisis inicial del caso: prefacturacion `prefac_cab = 1120199`, periodo `05/2025`, usuario `gpulka`; `VERIF_CONTRA_PREFACTURA` quedo `sin_errores = S` y `con_soli_aut = N`; sin registros en `VERIF_AFIL_ERRORES`; `GMS_CAB` creo cabecera automatica con `nombre_archivo = 1120199`, pero no inserto contratos en `GMSTXT_CLASE_PLAN`.
- Solicitudes relevantes del contrato `1056593`: automatica previa `4550722`, asociada a `prefac_cab = 1088193`, cambio `PREFA/APREFTE -> PREFA/FPREFTE`, `cambio_asoc_fam = S`; pruebas nuevas `4677172` cambio `PREFA/FPREFTE -> PREFA/APREFTE` y `4677173` cambio `PREFA/APREFTE -> ASOA/AASOCQ`.
- Diagnostico comunicado: al quedar cobertura vigente desde `2025-05-01` en `ASOA/AASOCQ`, y `PLANES.ASOA` tener `cambio_asoc_fam = N`, el contrato no calificaba para `afi_cambio_cober_cooperativa`; comportamiento consistente con datos y homologacion configurada `ASMP / PREFA / APREFTE -> FPREFTE`.
- Pedido posterior de Gabriel: en `afi_cambio_cober_cooperativa`, los `EXISTS` por solicitudes generadas deben considerar solo solicitudes impactadas con `SOL.estado = 'F'`; como estaba, podia considerar solicitudes no impactadas.
- Cambio implementado en `backend/procedures/afi_cambio_cober_cooperativa.sql`: se agrego filtro `SOL.estado = 'F'` en los cinco `EXISTS` que evaluan solicitudes para calcular/elegir `fecha_aplicacion_cambio`, para que solo habiliten solicitudes impactadas.
- Validaciones realizadas: `PARSEONLY OK` contra `InstallQA_89`; se preservo encoding UTF-8 sin BOM y finales de linea CRLF; `svn diff` quedo limpio despues del commit.
- Commit SVN realizado: revision `r37562`, mensaje `Redmine 29194 - Filtra solicitudes impactadas en cambio asoc fam`.


## Redmine 29363 - Solicitudes Automaticas / Traslado a contrato nuevo / Caso 21
- Usuario: Sebastian. Conversacion por Google Chat directo. Rama de trabajo/contexto: `Version104-Afilmed-SolicitudesAutomaticas`, repo usado en turnos previos `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`. Profile de BD usado para pruebas/busquedas: `InstallQA_89`.
- Redmine `29363`: Traslado a Contrato Nuevo automatico desde APP, `soli_moti = 47`. Flujo base recordado: `afi_insertar_soli_auto` -> `afi_insertar_soli_auto_traslado_nuevo` -> `afu_genera_soli_auto_app` -> `gms_validar_todo` -> `gms_genera_solicitudes` -> `afi_impacta_alta_total`; para motivo 47 luego dispara `afi_genera_solis_aut_traslados`.
- Caso 21 analizado: cuenta destino `39` en QA figura como colectiva (`es_colectivo = S`). En cuenta colectiva debe venir solo nodo `<empleado>`; en cuenta individual debe venir solo `<responsable_pagador>`. No deben venir ambos nodos juntos.
- Diagnostico comunicado: es arreglo de codigo. El proceso permitia responsable_pagador y empleado juntos, y ademas el empleado podia terminar pasando a GMS como `GMSTXT_INTEGRANTES` con `inte = 00`, `legajo = E`, disparando validaciones de integrante real como origen incompleto (`anter_prepa`, `anter_contra`, `anter_inte`) y faltantes de domicilio/telefono/email. Eso no corresponde: empleado no es integrante trasladado.
- Cambios pedidos por Sebastian: aplicar cambios, crear documento funcional para persona no tecnica, crear ejemplos de distintos rechazos y un ejemplo que permita el traslado, y al terminar comitear.
- Cambios que se debian aplicar segun analisis: en `afs_soli_auto_obligatorios`, validar exclusividad entre `<responsable_pagador>` y `<empleado>`; para cuenta colectiva exigir empleado y rechazar responsable pagador; para cuenta individual exigir responsable pagador y rechazar empleado; validar obligatorios de empleado y contactos `tipo_reg = E` con fallback a Registro Unico si corresponde. En `afi_insertar_soli_auto_traslado_nuevo`, mantener carga separada solo con XML ya validado. En `afu_genera_soli_auto_app`, revisar pasaje del empleado para que no dispare validaciones de integrante trasladado. En `gmsdef_val_datos_ident_inte`, las validaciones de origen traslado deben aplicar solo a integrantes reales (`legajo = I`), no a empleado (`legajo = E`). En `afu_soli_auto_app_contactos_gmstxt`, revisar mapeo de contactos `tipo_reg = E` para empleado.
- Estado al momento del flush: el asistente ya habia empezado a aplicar cambios y editar documentos/scripts con multiples `apply_patch` y validaciones por terminal, pero no habia enviado aun respuesta final ni consta en la conversacion compendiada que el commit SVN estuviera completado. Al retomar, verificar `svn status`, `svn diff`, parse/validaciones y si ya existe revision antes de comitear o responder.


## Redmine 29363 - Caso 21 / actualizacion post-respuesta
- Usuario: Sebastian. Conversacion por Google Chat directo.
- Respuesta final ya enviada despues del analisis del caso 21: se comunico que se aplico el ajuste faltante en `backend/procedures/afu_genera_soli_auto_app.sql`, alrededor de la linea 480.
- Cambio informado: para `soli_moti = 47`, el empleado ahora sale con `inte = NULL` y `legajo = 'E'`, en vez de `inte = '00'`. La intencion es mantener el puente legacy para que `gms_genera_solicitudes` lo grabe como `SOLI_RESP_PAGADOR` tipo `E`, evitando que quede identificado como integrante `00`.
- Coordinacion indicada con cambios locales ya presentes: `afs_soli_auto_obligatorios.sql` valida exclusividad `responsable_pagador`/`empleado`, reglas cuenta colectiva/individual y obligatoriedad de datos/contactos de empleado; `afi_insertar_soli_auto_traslado_nuevo.sql` repite barrera antes de insertar; `afu_soli_auto_app_contactos_gmstxt.sql` agrega contactos tipo `E` y fallback desde Registro Unico; `gmsdef_val_traslado_web.sql` acota validaciones de origen a `legajo = 'I'`; `gms_genera_solicitudes.sql` conserva cursor de empleado para generar `SOLI_RESP_PAGADOR` tipo `E`.
- Validacion comunicada: no se agregaron `RTRIM` ni finalizadores `;`, se corrigio/preservo CRLF en el archivo tocado. No se pudo ejecutar parseo contra SQL Server porque el entorno no tenia cliente SQL disponible.
- Estado comunicado: no se hizo commit. Al retomar, revisar `svn status` y `svn diff` antes de cualquier commit o nueva modificacion, porque habia cambios locales previos en varios procedimientos.
