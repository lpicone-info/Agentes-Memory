# Memoria curada diaria

- Fecha: 2026-07-22
- Usuario: Sebastián Rafael Montenegro
- id_googlechat: users/108913509298929268849
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/iro2hyaaaae

## Resumen de la actividad

### Tema 1: Error de compilación PowerBuilder por str_asesor.plan_regresa

- Solicitud o contexto: Sebastián reportó que al importar `wp_alta_parcial` recibía errores `C0019: Incompatible property plan_regresa for type str_asesor` en `dw_datos_generales.itemchanged`.
- Respuesta o acciones del agente: Se revisó la rama `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, las fuentes `exe_afilmed/src/wp_alta_parcial.srw`, `exe_afilmed/src/str_asesor.srs`, `exe_afilmed/src/nvo_alta_total.sru`, los PBG y el contenido visible de las PBL compiladas.
- Resultado: Se determinó que la fuente `str_asesor.srs` sí tenía el campo `plan_regresa`, pero la `empresas.pbl` local compilada contenía una versión vieja de la estructura. Se indicó importar primero `str_asesor.srs` en `empresas.pbl`, luego `nvo_alta_total.sru` en `clases_nvuo.pbl`, y después `wp_alta_parcial.srw` en `solicitudes.pbl`, con regeneración posterior.
- Estado: Resuelto

### Tema 2: Ubicación de exports PowerBuilder y problema SCC con pbls/src

- Solicitud o contexto: Sebastián preguntó si podía comitearse `str_asesor.srs` porque no la veía, y luego reportó un error SCC al intentar operar con `wp_cambio_tit.srw` desde una ruta bajo `pbls`.
- Respuesta o acciones del agente: Se validó que `str_asesor.srs` ya estaba versionada en `exe_afilmed/src/str_asesor.srs` y había sido incorporada previamente. También se revisó que `wp_cambio_tit.srw` existe en `exe_afilmed/src`, no en `exe_afilmed/pbls`.
- Resultado: Se explicó que en esa rama los exports `.srw/.srs/.srd` están en `src`, mientras que `pbls` contiene PBL/PBG. El error de SCC venía de que PowerBuilder intentaba hacer check-in sobre `pbls\wp_cambio_tit.srw`, ruta que no corresponde. Se indicó importar desde `src` y commitear los exports de `src`, no archivos inexistentes bajo `pbls`.
- Estado: Informativo

### Tema 3: Acompañamiento de importación y regeneración en PowerBuilder

- Solicitud o contexto: Sebastián hizo `get latest version`, importó `str_asesor`, observó un tilde rojo y consultó si era necesario importar `wp_alta_parcial` y si el estado era normal.
- Respuesta o acciones del agente: Se explicó que `get latest version` actualiza archivos en disco pero no necesariamente el contenido dentro de las PBL. Se recomendó importar y regenerar en orden: `str_asesor.srs`, `nvo_alta_total.sru`, `wp_alta_parcial.srw`, y luego regenerar la PBL u objetos afectados.
- Resultado: Se indicó que el tilde rojo puede ser normal al quedar el objeto modificado o checked out localmente luego de importar/regenerar. La validación real era que desapareciera el error `C0019` durante deploy o regenerate.
- Estado: Resuelto

### Tema 4: Caso 10 de contactos obligatorios en traslado a contrato nuevo

- Solicitud o contexto: Sebastián retomó el caso 10 basado en `id_soli_externa=293630401`, `id_proceso=133`, traslado a contrato nuevo (`soli_moti=47`), donde el XML omitía contactos de integrante y responsable pagador esperando que se tomaran desde Registro Unico.
- Respuesta o acciones del agente: Se diagnosticó que el procedimiento `dbo.afu_soli_auto_app_contactos_gmstxt` tenía un `RETURN` temprano para `soli_moti=47` después de copiar solo contactos del XML. Como el XML solo traía `tipo_reg=C`, nunca completaba contactos `R` del responsable pagador ni `I` del integrante desde `REGISTRO_UNICO_*`.
- Resultado: Se modificó localmente `backend/procedures/afu_soli_auto_app_contactos_gmstxt.sql` para mantener contactos del XML, completar faltantes `R` desde `SOLI_AUTO_RESP_PAGADOR` y Registro Unico, completar faltantes `I` desde `SOLI_AUTO_INTEGRANTES` y Registro Unico, y no tocar facturación `C`. Se validó contra `InstallQA_89` que el proceso 133 efectivamente estaba en la condición diagnosticada; se ejecutaron validaciones `PARSEONLY` y `NOEXEC`, con ASCII, sin BOM y CRLF preservado.
- Estado: Resuelto

### Tema 5: Commit del fix de contactos Registro Unico

- Solicitud o contexto: Sebastián pidió comitear el fix del caso 10.
- Respuesta o acciones del agente: Se validó el estado SVN acotado al archivo modificado y se realizó el commit.
- Resultado: Quedó comiteado en SVN revisión `r37398`, archivo `backend/procedures/afu_soli_auto_app_contactos_gmstxt.sql`, mensaje `Redmine 29363 - Completa contactos RU en traslado moti 47`. Se informó que no se desplegó en base de datos.
- Estado: Resuelto

### Tema 6: Asesor inexistente o inactivo en traslados automáticos 47 y 48

- Solicitud o contexto: Sebastián reportó que en traslado a contrato existente (`soli_moti=48`) con asesor inexistente o inactivo el proceso no validaba y permitía impactar. El caso informado ya estaba impactado en contrato `1000431`, `id_proceso=130`, con XML que usaba `asesor_gestion=400`. Pidió revisar si aplicaba también para traslado a contrato nuevo (`soli_moti=47`).
- Respuesta o acciones del agente: Se revisó el caso en `InstallQA_89` y se modificaron dos scripts: `backend/procedures/afs_soli_auto_obligatorios.sql` para validar `asesor_gestion` en `soli_moti` 47 y 48 antes de insertar la solicitud automática, y `backend/procedures/gmsdef_val_traslado_web.sql` para validar en GMS sobre `GMSDEF_CLASE_PLAN`.
- Resultado: La nueva regla genera error `2016` si el asesor no existe, si tiene `baja_fecha` vigente contra la fecha de aplicación, o si `afn_estado_asesor` lo da inactivo. Se validó que el caso impactado `id_proceso=130 / cab_id=295775` con asesor `400` quedaba alcanzado como `RECHAZA`, y que un asesor activo de control daba `ACEPTA`. Las validaciones `PARSEONLY` y `NOEXEC` dieron OK en ambos scripts, con ASCII, sin BOM y CRLF preservado. No se hizo deploy en base.
- Estado: Resuelto

### Tema 7: Commit del fix de asesor vigente en traslados APP

- Solicitud o contexto: Sebastián pidió comitear la validación de asesor vigente.
- Respuesta o acciones del agente: Se validó el estado SVN de los dos archivos modificados y se realizó el commit.
- Resultado: Quedó comiteado en SVN revisión `r37401`, mensaje `Redmine 29363 - Valida asesor vigente en traslados APP`, archivos `backend/procedures/afs_soli_auto_obligatorios.sql` y `backend/procedures/gmsdef_val_traslado_web.sql`. Se informó que el log de la revisión quedó con usuario `srmontenegro@INFOMEDICAL`.
- Estado: Resuelto
