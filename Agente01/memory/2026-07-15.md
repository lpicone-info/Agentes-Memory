
## 2026-07-15 15:10 UTC - Redmine 29365 / gpulka Version104 / SOLI_AUTO responsable pagador

Contexto durable:
- Usuario: Gabriel. Rama/working copy SVN: `gpulka-Version104-Afilmed-SolicitudesAutomaticas`, rama `gpulka version 104`.
- Se trabajo sobre Redmine 29365, circuito de cambio/finalizacion/baja/alta de responsable pagador via solicitudes automaticas (`soli_moti=22`).
- Documentacion/ejemplo externo en carpeta de Gabriel: `/mnt/Agentes/gpulka/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql`. El documento funcional original a contrastar era `REDMINE_29365_ANALISIS_FUNCIONAL_TECNICO_CAMBIO_RESP_PAGADOR_SOLI_AUTO.docx` en la carpeta de Gabriel.

Commits SVN realizados en la sesion:
- `r37330`: quita del XML de entrada campos tecnicos/internos para RP. `vigen_hasta_resp_pagador` ya no se pide; se calcula como `fecha_aplicacion - 1` cuando corresponde finalizar/bajar. `item`, `vigen_desde`, `vigen_hasta` de `<responsable_pagador>` tampoco se piden; se determinan internamente. Archivos: `afi_insertar_soli_auto_cambio_resp_pagador.sql`, `afs_soli_auto_obligatorios.sql`. Tambien se actualizo `/mnt/Agentes/gpulka/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql`.
- `r37331`: corrige caso donde finalizar/bajar RP vigente con el mismo documento informado en `<responsable_pagador>` era interpretado tambien como alta nueva y disparaba error 9307 `El responsable pagador nuevo ya se encuentra vigente para el contrato.` Archivo: `afu_genera_soli_auto_app.sql`. Logica: si finaliza/baja vigente y el documento del nodo coincide con el vigente, no genera fila de alta nueva.
- `r37332`: completa circuitos RP SOLI_AUTO. Permite: finalizar vigente, bajar vigente, finalizar/bajar vigente + crear nuevo, crear RP para contrato sin RP, nuevo RP existente en Registro Unico trayendo datos de RU, nuevo RP inexistente creando persona/datos desde XML. Archivos: `afs_soli_auto_obligatorios.sql`, `afu_soli_auto_app_contactos_gmstxt.sql`, `gms_genera_solicitudes.sql`, `gmsdef_val_resp_pagador.sql`. SQL de pruebas actualizado con variable `@caso` en `/mnt/Agentes/gpulka/REDMINE_29365_EJEMPLO_XML_EXEC_SOLI_AUTO_RP.sql`.
- `r37333`: corrige generacion de parentescos para RP SOLI_AUTO. Problema observado en InstallQA_89: solicitud 4677090 generada sin filas en `SOLI_PARENTESCOS`, contrato quedaba sin titular; `SOLI_AFILIADOS.inte_modif` quedaba `N` cuando en solicitudes comparables va `S`. Causa: para `soli_moti=22`, si XML traia `paren_actual=P` y `paren_nuevo=P`, filtro lo consideraba sin cambio. Cambio: para motivo 22, integrante guardado en `SOLI_AFILIADOS` queda con `inte_modif='S'`; se inserta `SOLI_PARENTESCOS` para el integrante presente en `SOLI_AFILIADOS` usando `paren_real` como parentesco a impactar; logica separada de cambio de titularidad (`soli_moti=7`). Archivo: `gms_genera_solicitudes.sql`. Validacion realizada: `PARSEONLY OK` sobre el procedure; con datos de InstallQA_89 para `id_proceso=112`/soli 4677090 habria insertado `inte=01`, `paren=P`.

Casos funcionales que deben mantenerse alineados con la ventana:
- Finalizar responsable pagador vigente sin alta nueva.
- Dar de baja responsable pagador vigente sin alta nueva.
- Finalizar vigente y crear responsable pagador nuevo.
- Dar de baja vigente y crear responsable pagador nuevo.
- Crear responsable pagador para contrato sin responsable pagador vigente.
- Responsable pagador nuevo existente en `registro_unico`: con el documento se deben traer datos desde RU.
- Responsable pagador nuevo inexistente en `registro_unico`: la persona se debe crear desde datos/contactos del XML.

Reglas de XML acordadas:
- No pedir `vigen_hasta_resp_pagador`; calcular internamente desde `fecha_aplicacion - 1` para cierre/baja del RP vigente.
- No pedir `item`, `vigen_desde` ni `vigen_hasta` en `<responsable_pagador>`; son datos internos o derivados.
- Para alta nueva, `vigen_desde` debe ser `fecha_aplicacion` cuando corresponde.
- Evitar pedir campos que en la ventana se calculan automaticamente porque inducen errores desde apps externas.

Datos de prueba relevantes:
- Entorno mencionado por usuario: `InstallQA_89`.
- XML usado para prueba de finalizacion sin nuevo RP: `soli_moti="22"`, `prepaga="1"`, `contra="1024546"`, `fecha_aplicacion="2026-05-01"`, `finalizar_resp_pagador="S"`, `baja_resp_pagador="N"`, `moti_ret_resp_pag="1"`, con `<parentescos>` para `inte="01"` y `inte="02"` ambos `paren_actual="P"`/`paren_nuevo="P"`, sin nodo `<responsable_pagador>`.
- Solicitud observada: `4677090`; `id_proceso=112`. Consulta de referencia del usuario: `select inte_modif,* from soli_afiliados where soli = 99807` para comparar que `inte_modif` suele ir `S`.

Pendientes/advertencias:
- No se ejecuto batch real desde el entorno del agente; varias validaciones SQL fueron por parse/diff y razonamiento contra datos consultados o informados.
- Al repetir pruebas con mismo `id_soli_externa`, limpiar datos o usar id nuevo para evitar remanentes en `SOLI_AUTO`, `GMS_CAB/GMSDEF` o solicitud generada.

## 2026-07-15 15:17 UTC - GPulka v104 Redmine 29365 RP SOLI_AUTO
- Tras r37333, quedó una duda funcional importante: al finalizar/dar de baja responsable pagador vigente sin crear uno nuevo, el contrato puede quedar sin titular si el XML no informa parentesco nuevo `T`.
- Revisión de comportamiento de ventana: el botón de parentescos obliga a que, cuando se finaliza/baja RP sin reemplazo, exactamente un integrante quede como Titular (`paren_nuevo = T`) y valida que no haya más de un titular.
- Si hay nuevo responsable pagador activo, la ventana aplica la regla inversa: no permite que integrantes queden como Titular, porque el nuevo contratante ocupa ese rol.
- El XML de prueba con `<parentesco inte="01" paren_actual="P" paren_nuevo="P" />` no representa el flujo de ventana para finalización sin reemplazo. Para alinear SOLI_AUTO, hay que asegurar exactamente un titular en ese circuito, ya sea exigiendo `paren_nuevo="T"` en XML o infiriéndolo internamente desde el titular vigente del contrato.

## 2026-07-15 15:55 UTC - Redmine 29364 / SolicitudesAutomaticas

- Conversacion con Sebastian sobre repo SVN `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas` en `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Caso funcional: cambio de titularidad CT motivo 7 para contrato `1000996`, ejemplo `Redmines/29364/prueba_completa_sin_rollback_29364_ejemplo_2.sql`.
- Primer batch fallo con warning SQLSTATE 01000 por `PRINT 'Solo aplica cuando el cambio de titularidad implica un cambio de contratante'` en `backend/procedures/afu_congela_descongela_edad.sql`. Se hizo fix minimo quitando solo ese `PRINT` y dejando el `RETURN`; commit SVN `r37334`, mensaje `29364 Evita warning en cambio titularidad sin contratante`.
- Luego se actualizo el ejemplo 2 cambiando `id_soli_externa` de `293649002` a `293649102`; commit SVN `r37335`, mensaje `29364 Actualiza id externo ejemplo cambio titularidad`.
- Tras ejecutar XML y batch, el log indico solicitud APP `293649102` -> Solicitud Afilmed `4677092`. En base se verifico que la solicitud si existe: `4677092`, `nro_formulario = 293649102`, contrato `1000996`, motivo `7`, estado `F`.
- El contrato parecia igual porque el XML usa `fecha_aplicacion="2026-08-01"`; en `AFI_HISTO_PAREN` quedo cambio futuro: integrante `01` pasa a `C` desde `01/08/2026`, integrante `02` pasa a `T` desde `01/08/2026`.
- Problema real detectado: la solicitud no aparecia en grilla porque `SOLI_AFILIADOS` quedo incompleto para `4677092`; solo grabo integrante `01` con `cambia_paren = C`, y la grilla para motivo 7 busca un registro con `cambia_paren = T`.
- Se dejo fix LOCAL, aun no commiteado ni aplicado en base, en `backend/procedures/gms_genera_solicitudes.sql` alrededor de linea 1140: evitar que el proceso borre de la temporal al nuevo titular informado por GMS antes de grabar `SOLI_AFILIADOS`.
- Validaciones reportadas del fix local: cambio acotado a `gms_genera_solicitudes.sql`, encoding `iso-8859-1`, sin BOM, finales `CRLF`, sin agregar `;` ni `RTRIM`, SP no aplicado en base, cambio no commiteado.

## 2026-07-15 16:08 UTC - Savina Version104 / Documento XML traslados SOLI_AUTO

Contexto durable:
- Usuario: Savina. Working copy principal para trabajo: `svarela-Version104-Afilmed-SolicitudesAutomaticas`, ruta `~/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas`.
- Savina pidio trabajar en Version104 sobre definiciones de solicitudes automaticas de traslados a contrato existente y traslados a contrato nuevo.
- Objetivo solicitado: documento funcional Word para cliente con variables XML de entrada, definiciones funcionales de cada tag/variable, tipos de datos, validaciones realizadas durante levantamiento del XML, errores devueltos en etapa XML hasta llegar a tablas GMS, XML con datos reales y XML generico.

Documentos generados en carpeta de Savina:
- `/mnt/Agentes/svarela/Definicion XML solicitudes automaticas traslados Version104.docx` fue la primera version.
- `/mnt/Agentes/svarela/Definicion XML solicitudes automaticas traslados Version104.md` quedo como fuente Markdown de respaldo y fue actualizado luego.
- Tras correccion funcional, se genero `/mnt/Agentes/svarela/Definicion XML solicitudes automaticas traslados Version104 - corregido.docx` porque el DOCX original estaba bloqueado/en uso.

Correccion importante acordada:
- Para traslado a contrato existente (`soli_moti=48`), el XML SI debe enviar `contra` en la cabecera, representando el contrato destino existente.
- El informe inicial decia incorrectamente que el XML no debia contener contrato para ese caso; fue corregido en el Markdown fuente y en el DOCX corregido.
- Traslado a contrato nuevo corresponde a `soli_moti=47`.

Validaciones/evidencias realizadas sobre el documento:
- Se relevaron procedures y evidencias Redmine `29362/29363` para sostener las definiciones.
- El documento cubre etapa XML hasta tablas `SOLI_AUTO_*`.
- DOCX generado con `pandoc`; se verifico tipo `Microsoft Word 2007+`.
- Markdown verificado sin BOM y con finales LF.
- En la version corregida se valido que el DOCX contiene la aclaracion explicita sobre `contra` para contrato existente.

Update SVN solicitado por Savina:
- Se ejecuto `svn update` sobre 9 working copies SVN registradas.
- Resultado: 8 repos quedaron en revision `37338`; `bmora-ProyectoCoomeva-Afilmed-01.Documentacion` quedo en revision `3146`.
- No quedaron conflictos SVN.
- La entrada `gabriel` no se actualizo porque es tipo `MNT`, no SVN.
- Advertencia observada: `gpulka-Version104-Afilmed-SolicitudesAutomaticas` tenia cambios locales no externos: 2 archivos `.docx` no versionados y `backend/procedures/gmsdef_val_resp_pagador.sql` modificado. No se tocaron ni revirtieron.
