# 2026-06-04

## Redmine 29362 - SVN trabajo realizado
- Repo/working copy principal usada: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Se documento el proceso tecnico por subfases para Redmine 29362 y se actualizo el grafico visual completo. Commit SVN: `r37055 - 29362 Documenta subfases y actualiza grafico proceso`.
- Archivos agregados en `Redmines/29362`: `DOCUMENTACION_TECNICA_SUBFASES_29362.md` y `proceso_completo_29362.html`.
- Validaciones de ese trabajo: `html-validate` OK, encoding `us-ascii`, sin BOM, finales LF, `svn log -r 37055` confirmado; quedaron solo externos `X` preexistentes fuera de alcance.

## Redmine 29362 - Validacion parentesco titular
- Pedido posterior: agregar validacion en `backend/procedures/gmsdef_val_traslado_web.sql` para que el parentesco destino no pueda ser titular, porque en ventana `wp_alta_parcial` no se puede elegir parentesco titular.
- Cambio aplicado: validacion rechaza `PARENTESCOS.es_titular = 'S'` con motivo `5107`, alineado con `soli_moti = 48`.
- Se agrego nuevo caso de ejemplo en `Redmines/29362/fase1-2-3-4-5-6-7_ejecucion_diagnostico_rollback.sql` con `id_soli_externa = 29362002`, destino `200`, origen `1002073/02`, DNI `1193455906`, integrante destino `04`, `paren_destino = H`, sin anexos.
- Commit SVN: `r37057 - 29362 Valida parentesco destino no titular`.
- Validaciones: `PARSEONLY OK` en SQL Server para SP y script, sin `RTRIM` ni sentencias terminadas en `;`, encoding `us-ascii`, finales preservados (SP CRLF, script LF), `svn status` sin cambios propios pendientes excepto externos `X` preexistentes.
- Nota: no se compilo el SP en base porque el script contiene `DROP PROCEDURE`/`CREATE PROCEDURE`; se indico que requiere confirmacion reforzada para ejecucion sobre DB.
