# Session: 2026-07-20 20:05:54 UTC

- **Session Key**: agent:main:googlechat:direct:spaces/iro2hyaaaae
- **Session ID**: 4a8ed387-fe01-498b-a7fc-36078c0b2fbe
- **Source**: googlechat

## Conversation Summary

assistant: Sí, Sebastián. Sigo acá.

Me quedé con la conclusión del `1008`: no generó solicitud porque quedó rechazado en `GMS_CAB 295765`, pero sí preparó los anexos obligatorios en GMS para los integrantes trasladados.
user: mmm, pero eso no deberia dar un mensaje de error por algun lugar asi el usuario sabe porque no se proceso?
assistant: Sí, Sebastián. **Debería informar error**, y de hecho la información existe en base.

Para el `1008`, el resultado estándar sale por:

```sql
EXEC dbo.gms_listar_resultado_gen
  @prm_cab_id = 295765,
  @prm_tipo = 'E',
  @prm_etapa = 2
```

Ese SP devuelve `estado = E`, `puede_procesarse = N`, `canti_errores = 10` y mensajes como:

- `Falta domicilio de contacto para traslado APP`
- `Falta telefono de contacto para traslado APP`
- `Falta email de contacto para traslado APP`
- `El estado civil debe existir o no debe estar dado de baja`
- `La profesión debe existir o no debe estar dada de baja`
- `El tipo de documento elegido no está permitido para la edad del Integrante`

Entonces el diagnóstico es:

```text
El XML fue recibido correctamente y por eso se generó el id_proceso 122, pero eso no significa que la solicitud haya sido procesada.

El proceso quedó rechazado en GMS_CAB 295765. Los errores están registrados en GMS_ERR y pueden consultarse con gms_listar_resultado_gen.

Si desde APP o desde la integración solo se informa "XML OK / id_proceso generado", falta exponer al usuario el resultado posterior del proceso GMS. Debería mostrarse el estado rechazado y el detalle de errores para que el usuario sepa por qué no se generó la solicitud.
```

O sea: **no falta el error en la base; falta confirmar si la capa APP/integración lo está consultando y mostrando.**
user: a ver, dame una consulta que demuestre que los integrantes tienen datos incompletos
assistant: Sebastián, usá esta consulta sobre QA. Te muestra integrante por integrante qué dato falta y el error registrado:

```sql
DECLARE @cab_id NUMERIC(18,0)

SET @cab_id = 295765

SELECT
    CAB.id_proceso,
    CAB.id_soli_externa,
    CAB.estado,
    CAB.puede_procesarse,
    CAB.canti_errores,
    ING.rowid,
    ING.inte,
    ING.docu_tipo,
    ING.docu_nro,
    ING.paren,
    ING.anter_prepa,
    ING.anter_contra,
    ING.anter_inte,
    origen_completo =
        CASE
            WHEN ING.anter_prepa IS NULL
              OR ING.anter_contra IS NULL
              OR ING.anter_inte IS NULL
            THEN 'NO'
            ELSE 'SI'
        END,
    tiene_domicilio =
        CASE
            WHEN EXISTS (
                SELECT 1
                FROM GMSDEF_DOMICILIOS DOM
                WHERE DOM.cab_id = ING.cab_id
                  AND DOM.soli_moti = ING.soli_moti
                  AND DOM.transaccion = ING.transaccion
                  AND DOM.prepaga = ING.prepaga
                  AND DOM.sec_contra = ING.sec_contra
                  AND DOM.inte = ING.inte
                  AND DOM.domi_tipo = 'I'
            )
            THEN 'SI'
            ELSE 'NO'
        END,
    tiene_telefono =
        CASE
            WHEN EXISTS (
                SELECT 1
                FROM GMSDEF_TELEFONOS TEL
                WHERE TEL.cab_id = ING.cab_id
                  AND TEL.soli_moti = ING.soli_moti
                  AND TEL.transaccion = ING.transaccion
                  AND TEL.prepaga = ING.prepaga
                  AND TEL.sec_contra = ING.sec_contra
                  AND TEL.inte = ING.inte
                  AND TEL.domi_tipo = 'I'
            )
            THEN 'SI'
            ELSE 'NO'
        END,
    tiene_email =
        CASE
            WHEN EXISTS (
                SELECT 1
                FROM GMSDEF_EMAILS EMA
                WHERE EMA.cab_id = ING.cab_id
                  AND EMA.soli_moti = ING.soli_moti
                  AND EMA.transaccion = ING.transaccion
                  AND EMA.prepaga = ING.prepaga
                  AND EMA.sec_contra = ING.sec_contra
                  AND EMA.inte = ING.inte
            )
            THEN 'SI'
            ELSE 'NO'
        END,
    ERR.recha_motivo,
    RCH.deno AS error_descripcion,
    ERR.info_adicional
FROM GMS_CAB CAB
INNER JOIN GMSDEF_INTEGRANTES ING
    ON ING.cab_id = CAB.id
LEFT JOIN GMS_ERR ERR
    ON ERR.cab_id = ING.cab_id
   AND ERR.rowid = ING.rowid
   AND ERR.tableid = 204
LEFT JOIN RECHA_MOTIVOS RCH
    ON RCH.recha = ERR.recha_motivo
WHERE CAB.id = @cab_id
ORDER BY ING.inte, ERR.recha_motivo
```

Para el caso `1008`, el `@cab_id` es `295765`. Esa salida te va a mostrar, por ejemplo, los `NO` en contacto/origen y los motivos `29301`, `29302`, `29303`, etc.
user: mira el resultado

122 1008 E N 10 2321518 01 RC 1247049144 T NULL NULL NULL NO NO NO NO 1 Afiliado inexistente Traslado sin origen completo anter_prepa, anter_contra, anter_inte
122 1008 E N 10 2321518 01 RC 1247049144 T NULL NULL NULL NO NO NO NO 5107 El parentesco no puede ser Titular NULL
122 1008 E N 10 2321518 01 RC 1247049144 T NULL NULL NULL NO NO NO NO 5127 El integrante no debe existir para el contrato. NULL
122 1008 E N 10 2321518 01 RC 1247049144 T NULL NULL NULL NO NO NO NO 29301 Falta domicilio de contacto para traslado APP Debe informar domicilio en XML para el integrante o cargar domicilio vigente en Registro Unico
122 1008 E N 10 2321518 01 RC 1247049144 T NULL NULL NULL NO NO NO NO 29302 Falta telefono de contacto para traslado APP Debe informar telefono en XML para el integrante o cargar telefono vigente en Registro Unico
122 1008 E N 10 2321518 01 RC 1247049144 T NULL NULL NULL NO NO NO NO 29303 Falta email de contacto para traslado APP Debe informar email en XML para el integrante o cargar email vigente en Registro Unico
122 1008 E N 10 2321519 04 CC 71656954 F 1 160053 02 SI SI SI SI 5114 El estado civil debe existir o no debe estar dado de baja. NULL
122 1008 E N 10 2321519 04 CC 71656954 F 1 160053 02 SI SI SI SI 8086 La edad del beneficiario no aplica para el programa al que está ingresando. NULL
122 1008 E N 10 2321519 04 CC 71656954 F 1 160053 02 SI SI SI SI 8090 La profesión debe existir o no debe estar dada de baja. NULL
122 1008 E N 10 2321520 05 TI 1017928396 F 1 160053 03 SI SI SI SI 8222 El tipo de documento elegido no está permitido para la edad del Integrante NULL

pero el primer error dice que el afiliado no existe y yo si veo que existe, porque sale ese error?
assistant: Sebastián, ese primer error **no corresponde al afiliado que estás consultando en la imagen**.

En tu resultado:

```text
rowid 2321518 | inte 01 | docu_nro 1247049144 | paren T | anter_prepa NULL | anter_contra NULL | anter_inte NULL
error 1 Afiliado inexistente
info: Traslado sin origen completo anter_prepa, anter_contra, anter_inte
```

Pero vos consultaste:

```sql
select * from afiliados where docu_nro = '1017928396'
```

Ese documento corresponde al integrante `05`, que en el resultado sí tiene origen completo:

```text
inte 05 | docu_nro 1017928396 | anter_prepa 1 | anter_contra 160053 | anter_inte 03
```

Entonces el mensaje “Afiliado inexistente” está saliendo por el **integrante 01 / titular / docu_nro 1247049144**, no por el `1017928396`.

El motivo real no es que no exista en `AFILIADOS` por documento, sino que para ese registro el proceso de traslado espera origen completo y viene así:

```text
anter_prepa  = NULL
anter_contra = NULL
anter_inte   = NULL
```

Por eso el detalle dice:

```text
Traslado sin origen completo anter_prepa, anter_contra, anter_inte
```

El texto “Afiliado inexistente” es genérico y confunde. Para este caso, la causa técnica concreta es: **está validando como traslado a un integrante titular sin datos de origen**.

Consulta rápida para comprobarlo:

```sql
SELECT *
FROM afiliados
WHERE docu_nro IN ('1247049144', '1017928396')
ORDER BY docu_nro, contra, inte
```

Mi sospecha: el `1008` está mezclando el titular existente del contrato destino con los integrantes trasladados. Habría que revisar por qué `GMSDEF_INTEGRANTES` incluye/valida al `inte 01` titular como si necesitara origen de traslado.
user: el traslado son 2 integrantes, mira el xml

DECLARE @xml XML





SET @xml = '


<solicitud


 id_soli_externa="1008"


 soli_moti="48"


 prepaga="1"


 contra="1000431"


 fecha_aplicacion="2026-07-01T00:00:00"


 fecha_inicio_servicio="2026-07-01T00:00:00"


 asesor_gestion="400">


 <integrantes>


 <integrante


 docu_tipo="CC"


 docu_nro="71656954"


 anter_prepa="1"


 anter_contra="160053"


 anter_inte="02"


 paren_destino="F"
/>


 <integrante


 docu_tipo="TI"


 docu_nro="1017928396"


 anter_prepa="1"


 anter_contra="160053"


 anter_inte="03"


 paren_destino="F"
/>


 </integrantes>


 </solicitud>'





EXEC dbo.afi_insertar_soli_auto


 @var_data_xml = @xml

porque el resultado que me pasaste muestra el dni 1247049144 si nada que ver!
assistant: Sí, Sebastián: tenés razón. El XML trae **solo 2 integrantes**.

El DNI `1247049144` aparece porque el proceso, para `soli_moti = 48`, agrega internamente el **titular vigente del contrato destino `1000431`** a `GMSTXT/GMSDEF_INTEGRANTES` antes de agregar los trasladados. Eso está en `afu_genera_soli_auto_app`: busca el titular con `dbo.afn_inte_tit(...)`, le pone `anter_prepa = NULL`, `anter_contra = NULL`, `anter_inte = NULL`, y lo inserta con `GMSTXT_INTEGRANTES_ins`.

Después la validación `gmsdef_val_traslado_web` está mal acotada: valida **todas** las filas de `GMSDEF_INTEGRANTES` con `soli_moti IN (47,48)` y exige origen anterior. Entonces alcanza también al titular destino, que no viene del XML y no debería necesitar `anter_*`.

Conclusión: el error del DNI `1247049144` no viene del XML. Es un efecto interno del armado del grupo destino, y parece un bug de validación: para traslado a contrato existente, la validación de “origen completo” debería aplicar solo a los integrantes trasladados, no al titular ya existente del contrato destino.
user: volviste?
assistant: Sí, Sebastián. Volví y sigo en contexto del caso `1008`.

El punto activo es: el XML trae 2 trasladados, pero el proceso agrega internamente el titular destino `01 / 1247049144`; después la validación de traslado le exige origen anterior a ese titular y genera un error que no corresponde.
user: hola Max,Estas en contexto del caso 1008.?
assistant: Sí, Sebastián. Estoy en contexto.

Caso `1008`, Redmine `29362`, traslado a contrato existente `soli_moti = 48`.

Punto clave: el XML trae 2 integrantes trasladados, pero el proceso agrega internamente el titular vigente del contrato destino `1000431` (`inte 01`, DNI `1247049144`) al armar GMS. Después `gmsdef_val_traslado_web` valida todas las filas `soli_moti IN (47,48)` y le exige `anter_prepa/anter_contra/anter_inte` también a ese titular destino, por eso aparece el error “Afiliado inexistente / Traslado sin origen completo” sobre un DNI que no estaba en el XML.
user: hola
assistant: Hola Sebastián, sigo disponible. ¿En qué avanzamos?
