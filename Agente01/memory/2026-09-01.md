
## 2026-09-01 13:26 ART - Redmine 29364 / Cambio de Titularidad

- Usuario: Sebastián trabaja sobre Redmine `29364`, cambio de titularidad automático desde APP (`soli_moti = 7`) en working copy SVN `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Se creó previamente en `/mnt/Agentes/srmontenegro/` una app/ventana de prueba PowerBuilder: `app_pruebas.sra` con `open(w_main_pruebas)` y `w_main_pruebas.srw` con botones para probar `w_infocrypt_test` y `w_chilkat95_test`. El usuario no podía cambiar la app porque se llama `migracion`; se le indicó usar en el evento `open` de la app:

```powerscript
SetPointer(hourglass!)

open(w_main_pruebas)
```

- Problema DLL resuelto: `infocrypt.dll` requería también `infolibeay32.dll`; ambas DLL son 32 bits. Se dejó `infolibeay32.dll` en `/mnt/Agentes/srmontenegro/`. Instrucción dada: copiar `infocrypt.dll` e `infolibeay32.dll` juntas al directorio donde corre el `.exe` y ejecutar/compilar PB en 32 bits. El usuario confirmó que funcionó.
- Caso 5 del Redmine 29364: se concluyó que era arreglo, no explicación. El XML venía inconsistente con `baja_titular="S"` y `paren_titular_anterior="T"`. Si el titular anterior se da de baja, `paren_titular_anterior` no debe informarse ni persistirse; solo aplica cuando `baja_titular="N"`. Error visible en GMS: `Error al validar el titular vigente del contrato.`
- Arreglo implementado en la working copy del 29364:
  - `backend/procedures/afs_soli_auto_obligatorios.sql`: rechaza XML con `baja_titular = 'S'` y `paren_titular_anterior` informado. Mensaje: `El XML no debe informar paren_titular_anterior cuando baja_titular = S`.
  - `backend/procedures/afi_insertar_soli_auto_cambio_titularidad.sql`: blindaje defensivo para grabar `paren_titular_anterior` solo si `baja_titular = 'N'`.
  - `backend/procedures/gms_cab_in.sql`: se corrigió causa de cabeceras `APP` pisadas a `AFI`. Antes `@prm_origen_solicitud CHAR(3) = 'AFI'`; ahora default `NULL`, en update preserva el origen existente y en insert legacy sin origen sigue grabando `AFI`.
- Validaciones realizadas antes de compacción: `PARSEONLY OK` de los 3 procedimientos contra `InstallQA_89`; prueba lógica del caso 5 ahora `RECHAZA`; prueba lógica de blindaje: update sin origen conserva `APP`, insert legacy sin origen genera `AFI`; `svn status` solo muestra esos 3 archivos modificados más externos habituales.
- No se aplicaron SPs en base y no se hizo commit. Registro QA `id_soli_externa=2016` ya quedó contaminado; para reprobar tras aplicar los SPs conviene usar otro `id_soli_externa`.

## 2026-09-01 14:58 ART - Savina / PruebasCoreINFOMEDICAL, SVN V104 y documentos de validaciones

- Conversacion directa por Google Chat con Savina. Contexto de trabajo usado: BD `PruebasCoreINFOMEDICAL` y repos SVN de Afilmed V104/SolicitudesAutomaticas.
- Para recaudador `525` / archivo plano importado, se analizo la linea `02603108050097410 017855024 000000005000000000.00270080484 800194297 CARTERA COLECTIVA ABIERTA ALIANZA BANCO DE OCCIDENTE 104701601`.
  - Coincidencia hallada en Afilmed: lote `283859`, item `3`, referencia `OCCI 5025 10032026`, recaudador interno `24` = `5025 BANCO OCC. COLECTIVO MANUAL`.
  - `5025` = sitio de pago / Banco de Occidente Colectivo; fecha del item `2026-03-10`; valor importado `5.000.000.000,00`; `800194297` quedo en `COMPRO_IMPORTACION.docu_nro`; descripcion del colectivo no queda como campo propio; `104701601` no fue encontrado como `cod_unico_comprobante`; `017855024` y `270080484` son datos del extracto/banco pero no quedaron en campos principales del item.
  - Resultado del item: detalle en `COMPRO_IMPORTACION_DET` con `compro_tipo = RC`, `compro_nro = XSCMP09283509`, `cod_unico_comprobante = 150010010`, origen `S`, motivo sin identificar `9106`.
- Verificacion de cuenta `800194297 CARTERA COLECTIVA ABIERTA ALIANZA BANCO DE OCCIDENTE` en `PruebasCoreINFOMEDICAL`:
  - No existe como cuenta maestra en `CUENTAS` por `cuenta`, `docu_nro` ni razon social.
  - Tampoco aparece en `AFI_RESP_PAGADOR`, `AFILIADOS` ni tablas de aportes revisadas.
  - Si existe como dato importado historico en `COMPRO_IMPORTACION.docu_nro`, incluyendo registros con sitio de pago `5025`; eso no confirma alta en maestro.
- Actualizaciones SVN V104 realizadas:
  - Working copy Gabriel Pulka: `gpulka-Version104-Afilmed-SolicitudesAutomaticas`, actualizada a revision `37717`; quedaron 2 `.docx` no versionados en `Redmines/29361` y `Redmines/29362`, no modificados.
  - Working copy Sebastian Montenegro: `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, actualizada a revision `37717`; se actualizaron archivos de `exe_afilmed/src` y procedures de `backend/procedures`; sin conflictos.
  - Working copy de Savina V104: actualizada a revision `37717`; se actualizaron procedures de `backend/procedures` y `exe_afilmed/src/wp_main_solicitudes.srw`; sin conflictos; quedaron solo `X` normales de externals SVN en PBL.
- Documento solicitado y generado para comparacion de validaciones de Cambio Datos de Empleado:
  - Archivo final: `/mnt/Agentes/svarela/comparacion de validaciones de Cambio Datos de Empleado.docx`.
  - Alcance: comparacion entre codigo actual trunk de ventana manual de cambio datos de empleado y version Solicitudes Automaticas para `soli_moti=49`.
  - Incluye matriz comparativa con validaciones cubiertas por automatica y brechas reales/parciales.
  - Principales faltantes detectados: bloqueo por falsedad/retiro bloqueante, comparacion contra `codigo_unico_persona`, licencia vigente del titular y cobertura parcial de contratos relacionados.
  - Validacion: archivo Word 2007+ correcto, ZIP interno sin errores y contenido legible. No se modifico codigo ni repositorios para ese entregable.
- Analisis de trunk: ventana `wp_reporte_datos_contacto_activacion_da` / reporte Datos de Contacto Activacion al Debito Automatico:
  - El tilde `Sin Exportar` filtra el reporte; cuando esta tildado, el DataWindow `de_reporte_datos_contacto_activacion_da.srd` pasa `@prm_sin_exportar` al SP `afs_reporte_datos_contacto_activacion_da` y trae solo registros con `ultim_expor_fecha IS NULL`.
  - Si ya estan exportados: con `Sin Exportar` tildado no aparecen; sin tilde aparecen con `F. Exportacion`.
  - La ventana permite reexportar registros ya exportados si el usuario los selecciona; al exportar vuelve a actualizar `ultim_expor_fecha`.
  - Exporta luego de pedir carpeta con `GetFolder('Seleccione el directorio de Exportacion')`; genera Excel `Reporte_DA_Activación_Datos_Contacto_ddmmyyyy.xls` en esa carpeta.
  - Marca exportacion en `NOVEDADES_DA_IMPOR_TEMP.ultim_expor_fecha` cuando origen `NOV`, y en `GMS_CAB.ultim_expor_fecha` cuando origen `GMS`.

## 2026-09-01 16:06 ART - Gabriel / Redmine 29365 Solicitudes Automaticas APP V104

- Conversacion directa por Google Chat con Gabriel. Rama/working copy usada: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas` sobre Version104 / Redmine `29365`.
- Caso investigado inicialmente: `id_soli_externa = 2016` en BD `InstallQA_89`. Se determino que `afu_genera_soli_auto_app` crea cabecera GMS con origen `APP`, pero ante error el flujo PB actualizaba `GMS_CAB` via `iuo_procesos_masivos.of_ins_upd_gms_cab` sin pasar origen, cayendo en default `AFI` de `gms_cab_in`. Ademas existia una solicitud vieja `SOLICITUDES.nro_formulario=2016` del anio 2020 con origen `AFI`, generando posible confusion visual. En la working copy de Gabriel no se modifico `gms_cab_in` en esta secuencia; el fix de origen habia quedado registrado aparte en la memoria de Sebastian.
- Documento de brechas de validaciones de Cambio Datos de Empleado (`soli_moti=49`) analizado. Se confirmo a Gabriel que los puntos agregados son equivalentes a validaciones existentes en la ventana, no validaciones extra inventadas.
- Commit SVN realizado: `r37719`, mensaje `Redmine 29365 - Completa validaciones empleado APP`.
  - `backend/procedures/afs_soli_auto_obligatorios.sql`: para `soli_moti = 49` se agregaron validaciones faltantes de bloqueo por retiro/falsedad, comparacion explicita contra Registro Unico (`codigo_unico_persona`) y licencia vigente del titular a la fecha de aplicacion.
  - `backend/procedures/afi_insertar_soli_auto_cambio_resp_pagador.sql`: el parser APP persiste `codigo_unico_persona` y `codigo_unico_persona_nuevo` en staging.
  - `backend/procedures/gms_genera_solicitudes.sql`: contratos relacionados ya no quedan restringidos al caso `RUD.codigo_unico_persona IS NULL`.
  - Validacion informada: parse SQL contra `InstallQA_89` con `SET PARSEONLY ON`, sin ejecutar `DROP/CREATE`; EOL/encoding revisados.
- Log posterior de AU mostraba loop cada minuto con `SQLSTATE = 42000` / `Error al validar el titular vigente del contrato.` y rollback completo. Gabriel marco correctamente que errores funcionales de validacion con evidencia en `GMS_ERR` no deben rollbackearse ni dejar procesos con fecha inicio sin fecha fin.
- Correccion implementada y commiteada: `r37720`, mensaje `Redmine 29365 - Cierra AU con errores funcionales`.
  - `backend/procedures/gmsdef_validar.sql`: se agrego validacion previa para origen APP + `soli_moti = 7`, controlando que el cambio deje exactamente un titular vigente. Para el caso visto (`cab_id 295963`, contrato `117329`) el predicado arrojaba `titulares_previstos = 2`, por lo que ahora debe entrar por `GMS_ERR` antes de impactar y no explotar como error tecnico posterior.
  - `exe_afilmed/src/wp_main_solicitudes.srw`: se separo error tecnico de error funcional. Si AU encuentra errores en `GMS_ERR`, no rollbackea la transaccion completa; cierra el proceso con estado/fecha fin y deja la evidencia funcional.
  - Validaciones realizadas: parse SQL completo de `gmsdef_validar.sql` contra `InstallQA_89`; prueba del predicado del caso real; encoding/EOL preservados (SRW UTF-8 BOM + CRLF; SQL ISO-8859-1 + CRLF); no se modificaron datos de `InstallQA_89`, solo consultas y pruebas dentro de transaccion con rollback.
- Estado local reportado tras commits: pendientes ajenos/no versionados eran principalmente `.docx` en carpetas Redmines y externals SVN habituales; no revertirlos.

## 2026-09-01 17:10 ART - Savina / Reporte DA y XML cambio Responsable Pagador

- Conversacion directa por Google Chat con Savina, trabajando sobre BD `PruebasCoreINFOMEDICAL` / `infomedicalpruebascore` y trunk/V104 Afilmed.
- Reporte `wp_reporte_datos_contacto_activacion_da` / SP `dbo.afs_reporte_datos_contacto_activacion_da`:
  - El DataWindow `d_reporte_datos_contacto_activacion_da` ejecuta el SP `afs_reporte_datos_contacto_activacion_da`.
  - Origen 1: `NOVEDADES_DA_IMPOR_TEMP` alias `NOV`, vinculado a `SOLICITUDES` por `NOV.soli = S.soli`; usa `NOV.lote` como proceso, `NOV.fecha` como fecha proceso, `NOV.ultim_expor_fecha` como fecha exportacion, `NOV.docu_nro_contratante`, `NOV.cod_aprobacion_negacion`, `NOV.desc_aprobacion_negacion`.
  - Origen 2: `GMS_CAB C` + `GMSDEF_MULTI M`; usa `C.id`, `C.fecha_desde`, `C.ultim_expor_fecha`; filtra `C.transaccion = 'FP'`, `C.estado = 'B'`, `C.fecha_hasta IS NOT NULL`, `M.contra <> ''`.
  - Cruces relevantes: `CAJA_HOMOLOGACION` para sitio de pago; `afn_filtro_sitio_pago('S','L',@prm_identificador)` para filtro de sitios elegidos; `REGISTRO_UNICO`, `REGISTRO_UNICO_DOCUMENTO`, `REGISTRO_UNICO_TELEFONOS`, `REGISTRO_UNICO_EMAILS`, `AFI_EMAILS_CONTRA`, `AFILIADOS`, `AFI_DATOS_CONTRATO_GRAL`, `ASESOR`, `AsesorPlan`, `CanalVentaCat`.
  - Filtro NOV informado: `NOV.fecha BETWEEN @prm_fecha_desde AND @prm_fecha_hasta`, `(NOV.ultim_expor_fecha IS NULL OR @prm_sin_exportar = 'N')`, `NOV.soli > 0`.
  - Filtro GMS informado: `CONVERT(DATE, C.fecha_desde) BETWEEN @prm_fecha_desde AND @prm_fecha_hasta`, `C.transaccion='FP'`, `C.estado='B'`, `C.fecha_hasta IS NOT NULL`, `(C.ultim_expor_fecha IS NULL OR @prm_sin_exportar='N')`, `M.contra <> ''`.
- Consulta de casos para periodo `2026-08-01` a `2026-08-31`, todos los sitios de pago, en `PruebasCoreINFOMEDICAL`:
  - Con tilde `Sin Exportar`: `0` registros.
  - Sin tilde: `0` registros.
  - Ya exportados: `0` registros.
  - Separado por origen: `NOV / NOVEDADES_DA_IMPOR_TEMP = 0`; `GMS / GMS_CAB + GMSDEF_MULTI = 0`.
  - Causa: no hay datos base en agosto 2026 que cumplan el reporte; no es efecto del tilde. `NOVEDADES_DA_IMPOR_TEMP` con fecha agosto 2026 = 0; `GMS_CAB` transaccion `FP` con fecha agosto 2026 = 0; `GMS_CAB FP` finalizado estado `B` agosto 2026 = 0.
  - Para que aparezca un caso NOV: debe existir `NOVEDADES_DA_IMPOR_TEMP.fecha` en rango, `soli > 0`, `ente_rec` homologado en `CAJA_HOMOLOGACION`, y si `Sin Exportar` esta tildado `ultim_expor_fecha IS NULL`.
  - Para que aparezca un caso GMS: debe existir `GMS_CAB.transaccion='FP'`, `estado='B'`, `fecha_hasta IS NOT NULL`, `fecha_desde` en rango, detalle `GMSDEF_MULTI.contra <> ''`, `ente_rec` homologado, y si `Sin Exportar` esta tildado `GMS_CAB.ultim_expor_fecha IS NULL`.
  - Caso testigo fuera de agosto: `GMS_CAB.id = 317209`, fecha `2026-01-13`, estado `B`, sitio `0778 BANCOLOMBIA DA`, contratos `4135`, `4183`, `977939`.
- XML solicitado para cambio de Responsable Pagador `soli_moti=22` del contrato `1000137` con RP documento `CC 1066180493`:
  - Se informo que en `PruebasCoreINFOMEDICAL` ese documento ya figura como responsable pagador vigente del contrato, por lo que sirve como prueba tecnica pero no cambia a persona distinta.
  - Datos usados para el RP: `ape=PALACIOS`, `ape2=AGRESOTT`, `nombre=HAROL`, `nombre2=JAVIER`, `naci_fecha=1991-01-20`, `sexo=M`, `civil_esta=S`, `nacio=CO`, `estrato_social=4`, `profesion=874`, `ocupacion=0`, `cargo=0`, `rango_salarial=0`, `red_coomeva=N`, `iva=G`, `es_asociado=N`.
  - XML entregado: `<solicitud id_soli_externa="221000137" soli_moti="22" prepaga="1" contra="1000137" fecha_aplicacion="2026-09-01" fecha_radicacion="2026-09-01" baja_resp_pagador="S" moti_ret_resp_pag="1">` con nodo `<responsable_pagador .../>` y parentescos para integrantes `01`, `02`, `03`, `05`; `paren_nuevo="F"` para todos. `moti_ret_resp_pag="1"` = voluntario, `paren_nuevo="F"` = familiar a cargo.

## 2026-09-01 17:39 ART - Gabriel / Redmine 29365 RP APP, relacionados y edicion de RP vigente

- Conversacion directa por Google Chat con Gabriel. Working copy usada: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`, Redmine `29365`, Version104 / Solicitudes Automaticas APP.
- Gabriel pidio revisar nuevamente la ventana `wp_cambio_resp_pagador` por la logica de contratos relacionados obligatorios: caso manual permitido donde entra con un contrato, modifica un nombre del responsable pagador activo, e intenta grabar/impactar; la ventana determina que debe seleccionar contratos relacionados.
- Revision de la ventana: la obligatoriedad de seleccionar relacionados no depende solo de que existan relacionados, sino de que el responsable/empleado afectado tenga `codigo_unico_persona_nuevo IS NULL`. Si hay `codigo_unico_persona_nuevo`, la ventana solo advierte cuando no se selecciona ninguno; si es nulo, obliga a seleccionar todos.
- Commit SVN realizado: `r37721`, mensaje `Redmine 29365 - Ajusta relacionados obligatorios APP`.
  - Archivo modificado: `backend/procedures/gms_genera_solicitudes.sql`.
  - Cambio: la autocarga automatica de `SOLI_CONTRATOS_RELACIONADOS` con `modifica = 'S'` ahora se ejecuta solo para filas activas de `SOLI_AUTO_RESP_PAGADOR` con `codigo_unico_persona_nuevo IS NULL`, replicando el caso obligatorio de la ventana. Antes insertaba relacionados ante responsable activo generado, pero no copiaba exactamente ese filtro.
  - Validacion informada: `PARSEONLY` completo contra `InstallQA_89`, columnas requeridas existentes en `SOLI_AUTO_RESP_PAGADOR`, encoding/EOL preservados ISO-8859-1 sin BOM + CRLF, archivo versionado limpio.
- Luego Gabriel probo XML de cambio Responsable Pagador `soli_moti=22`, `contra=1000137`, `id_soli_externa=221000137`, `baja_resp_pagador='N'`, documento RP `CC 1066180493`, modificando nombres (`HAROLLLLLL`, `JAVIERRRRR`). El SP `afi_insertar_soli_auto` devolvia error incorrecto: `El XML debe informar baja_resp_pagador = S para agregar un nuevo responsable pagador`.
- Analisis del caso: no era alta de nuevo RP ni baja/alta; el documento informado corresponde al mismo responsable pagador vigente del contrato. En `InstallQA_89`, `CC 1066180493` resuelve a `codigo_unico_persona=1000590` y el RP vigente del contrato tiene ese mismo codigo. La ventana permite modificar datos del RP/empleado vigente sin dar de baja el actual.
- Commit SVN realizado: `r37724`, mensaje `Redmine 29365 - Permite editar RP vigente en APP`.
  - `backend/procedures/afi_insertar_soli_auto_cambio_resp_pagador.sql`: el parser/insert permite el caso `baja_resp_pagador='N'` cuando el documento XML resuelve al mismo `codigo_unico_persona` del `AFI_RESP_PAGADOR` vigente; si el XML no trae `codigo_unico_persona`, lo infiere desde `REGISTRO_UNICO_DOCUMENTO`, igual que luego hace la generacion GMS.
  - `backend/procedures/afs_soli_auto_obligatorios.sql`: la validacion que exigia `baja_resp_pagador='S'` ya no se dispara cuando el RP informado es la misma persona vigente; solo bloquea cuando realmente intenta agregar/cambiar a otro responsable sin baja del actual.
  - Validaciones informadas: condicion del caso `contra=1000137` da `condicion_requiere_baja = 0`; ambos procedures con `PARSEONLY OK` contra `InstallQA_89`.
- Estado local reportado tras `r37724`: solo pendientes ajenos al cambio, principalmente `.docx` no versionados en `Redmines/29361` y `29362`, mas externals SVN `X`; no revertir ni limpiar esos archivos salvo pedido explicito.
