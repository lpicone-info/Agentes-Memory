- La lógica de `afu_sarlaft_migracion.sql` quedó alineada con el resumen funcional del PM para SARLAFT: contratos con registro vigente en `AFI_SARLAFT` solo completan campos obligatorios faltantes sin pisar existentes; contratos con registro dado de baja en `AFI_SARLAFT` no se tocan; contratos sin registro en `AFI_SARLAFT` se insertan en la preview.
- Se incorporó `AFILIADOS_PREVIEW` como copia persistente temporal de control, con columna `accion`, para revisar cambios de `profesion` y `ocupacion` sin impactar `AFILIADOS` productivo.
- En `AFILIADOS_PREVIEW`, solo se trabajan registros alcanzados por el Excel que ya existen en `AFILIADOS`; si `profesion` y/o `ocupacion` cambian y el código existe en maestra, `accion='actualizado'`; si falta código en `PROFESIONES` o `OCUPACION`, `accion` queda como `no actualizado: ...` con detalle del código faltante.
- Se corrigió una falsa invalidación de maestras: `PROFESIONES.profesion` y `OCUPACION.ocupacion` están guardadas como `char(3)` con espacios, no con ceros a la izquierda; la validación ahora compara por valor numérico lógico y no por strings padded como `008`.
- Regla final acordada para `AFILIADOS_PREVIEW`: no conservar filas con `accion='nada'`; la preview debe quedar solo con registros `actualizado` o `no actualizado: ...`.
- Para facilitar control visual, el `SELECT` final de `AFILIADOS_PREVIEW` quedó ordenado para mostrar primero las excepciones `no actualizado`, luego `actualizado`; `AFI_SARLAFT_PREVIEW` se ordena por `prepaga, contra, vigen_desde`.
- Toda la trazabilidad funcional y técnica de R28684 quedó documentada en `coomeva-afilmed-trunk-srmontenegro/Redmines/28684/28684.md`, con commits y pushes realizados durante la jornada.
