# Memoria 2026-09-14

## Redmine 30345 - AFE Coomeva / Venta nueva
- Humano en Google Chat directo trabajado como Sebastián en la conversación.
- Redmine `30345`: COOMEVA, asunto `XML => Solicitudes de venta Nueva`, producto `Afilmed`, origen `Cliente`, incidente externo `7005`, vinculado al Redmine padre/contextual `30284` sobre generación de solicitudes automáticas AFE.
- Fuente funcional revisada: video `Socializar Requerimiento Afiliacion Electronica-20260825_101017-Grabación de la reunión 1.mp4` en carpeta compartida. Conclusión: para venta nueva AFE radica, pasa por auditoría médica y envía a Afilmed datos de venta + resultado de auditoría + resultado final si impacta o rechaza. Afilmed no debe recalcular si corresponde auditoría por estado de salud/IMC/edad; debe registrar lo informado por AFE.
- Diagnóstico técnico encontrado: validación previa de `afs_soli_auto_obligatorios.sql` restringía auditoría médica a `soli_moti IN (6,47,48)`, heredada de Redmine `30111`, pero venta nueva usa `soli_moti = 1` y también requiere auditoría.
- Cambio aplicado y comiteado en SVN repo `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`: commit `r37802`, autor `srmontenegro@INFOMEDICAL`, mensaje `Redmine 30345 - habilitar auditoria medica en venta nueva`.
- Archivos modificados en `r37802`: `backend/procedures/afs_soli_auto_obligatorios.sql` y `backend/procedures/gms_genera_solicitudes.sql`.
- Cambio funcional de `r37802`: auditoría médica admite `soli_moti = 1` además de `6,47,48`; validación/asociación de integrantes contempla venta nueva; batch `gms_genera_solicitudes` ejecuta `afu_soli_auto_app_auditoria_medica` también para venta nueva.
- Validaciones hechas al commit: diff revisado, estado SVN limpio para los dos archivos, encoding preservado (`afs_soli_auto_obligatorios.sql` us-ascii, `gms_genera_solicitudes.sql` iso-8859-1), sin BOM, CRLF preservado, no se introdujo `RTRIM` ni `;` en líneas modificadas. No se ejecutó compilación/prueba DB porque no se indicó profile.

## Error posterior de XML / auditoría médica
- Luego del commit, el humano reportó error SQL Server: `Mensaje 50000... afi_insertar_soli_auto, línea 117: Existen integrantes de auditoria medica con estado inexistente o dado de baja`.
- Análisis informado: ya no parecía falla de SP sino dato inválido del XML. El ejemplo tenía `<integrante ... estado="APR" nivel_riesgo="1" ...>`, pero `APR` no existe en la parametría `ESTADOS_INTEGRANTES` versionada.
- Códigos vigentes encontrados/informados: `ASI` aceptado sin auditoría médica de ingreso; `AEA` aceptado en auditoría médica de ingreso; `ACP` aceptado con preexistencias; `NEG` negado/rechazado; `CON`, `EMA`, `EMI` otros estados de auditoría/examen.
- Corrección sugerida para caso aceptado sin riesgo: usar `<auditoria_medica resultado="A"><integrante inte="01" docu_tipo="CC" docu_nro="1000000001" estado="AEA" nivel_riesgo="1" observacion="Sin riesgo" /></auditoria_medica>`.
- También se recomendó quitar el bloque de preexistencias de ejemplo con `icd="Z00000"` porque no aparecía como ICD válido. Para probar aceptado con preexistencias: usar `estado="ACP"` y un ICD real vigente.

## Flujo siguiente SOLI_AUTO -> GMS / Batch
- El XML corregido fue compartido en `/mnt/Agentes/srmontenegro/ejemplo_xml_venta_nueva_30345_corregido.sql`; se informó que quedó listo para compilar, ASCII, sin BOM, CRLF, sin `APR` ni `Z00000`. El humano confirmó: "ahi paso! y lo inserto".
- Secuencia funcional acordada: `afi_insertar_soli_auto` inserta en `SOLI_AUTO_*`; luego `afu_genera_soli_auto_app` debe generar `GMS_CAB` y tablas `GMSTXT_*`, crear agenda `CAMBIOS_MASIVOS_AGENDA` con `tipo_proceso = 'AU'`; finalmente SuperBatch `AU` ejecuta `gms_genera_solicitudes` y genera la solicitud definitiva.
- No se considera necesario agregar tablas nuevas: los datos se distribuyen entre `GMS_CAB` y las tablas `GMSTXT_*`; auditoría/preexistencias permanecen en `SOLI_AUTO_*` y se aplican al generar la solicitud.
- Se modificó y comiteó `backend/procedures/afu_genera_soli_auto_app.sql` en SVN repo `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, commit `r37803`, mensaje `Redmine 30345 - habilitar generacion GMS para venta nueva`.
- Cambio informado de `r37803`: `soli_moti = 1` se mapea a Alta Total mediante `gms_traer_cod_transaccion`; venta nueva no exige contrato previo en `AFI_CLASE`; para otros motivos se conserva validación de origen/contrato vigente; se mantienen rutinas de generación `GMS_CAB`/`GMSTXT_*`.
- Se entregó script de prueba que toma `id_soli_externa = 303450001`, valida que exista una única solicitud pendiente y que no haya otros pendientes, ejecuta `afu_genera_soli_auto_app`, y consulta `GMS_CAB`, `GMSTXT_*` y `CAMBIOS_MASIVOS_AGENDA`. El script no ejecuta SuperBatch ni crea aún la fila final en `SOLICITUDES`.
- Riesgos pendientes: probar compilación y ejecución real contra una base/profile autorizado; confirmar nombres/columnas exactas de tablas GMS y que la agenda quede lista para SuperBatch; validar auditoría médica y resultado final en la generación definitiva.


## 2026-09-14 17:04 ART - Redmine 30345 avance GMS y prueba funcional

- Contexto activo con Sebastián sobre Redmine 30345, Venta Nueva AFE Coomeva. Se pausó el avance de código para entregar ejemplos de prueba funcional.
- Se preparó primero XML puro funcional completo en `/mnt/Agentes/srmontenegro/redmine_30345_xml_venta_nueva_funcional_completo.xml`, pero Sebastián aclaró que necesitaba SQL ejecutable con XML embebido.
- Se dejó SQL ejecutable con XML embebido en `/mnt/Agentes/srmontenegro/redmine_30345_xml_venta_nueva_funcional_completo_ejecutar.sql`. Validado estáticamente: XML parsea OK, llama a `dbo.afi_insertar_soli_auto`, sin transacciones, sin `SELECT *`, sin `;`, sin `RTRIM`.
- Para evitar validaciones opcionales, se generó otro ejemplo mínimo en `/mnt/Agentes/srmontenegro/redmine_30345_xml_venta_nueva_minimo_validaciones_ejecutar.sql`. Contiene cabecera venta nueva, facturación, contactos obligatorios, responsable pagador, dos integrantes y auditoría médica aceptada `AEA`. No incluye anexos, adicionales, certificados, SARLAFT ni preexistencias.
- Sebastián ejecutó el ejemplo mínimo en `InstallQA_89` y obtuvo `id_proceso = 255`; luego ejecutó `EXEC dbo.afu_genera_soli_auto_app`.
- La trazabilidad recomendada fue `EXEC dbo.afs_soli_auto_app_trazabilidad 303450002`, usando `id_soli_externa` y no `id_proceso`.
- Resultado informado por Sebastián: `303450002 S GENERACION_GMS_INICIADA_SIN_FIN Generacion GMS iniciada sin fecha de fin. No se reprocesa con el mismo id_soli_externa. Generar nuevamente con otro numero. 255 ... soli_moti 1 Venta Nueva ...`.
- Interpretación dada: XML -> `SOLI_AUTO_*` y `afu_genera_soli_auto_app` ya engancharon con GMS; ahora corresponde revisar/ejecutar proceso batch `AU` de solicitudes, que termina invocando `gms_genera_solicitudes`.
- Consultas recomendadas para el próximo paso:
  - `SELECT * FROM dbo.GMS_CAB WHERE id_proceso = 255 AND origen_solicitud = 'APP'`
  - con el `cab_id`: `SELECT * FROM dbo.CAMBIOS_MASIVOS_AGENDA WHERE cab_id = <cab_id> AND tipo_proceso = 'AU'`
  - errores: `SELECT * FROM dbo.GMS_ERR WHERE cab_id = <cab_id>`
  - validar resultado final: `SELECT soli FROM dbo.SOLI_AUTO_CONTRATO WHERE id_proceso = 255`
- Criterio actual: si hay agenda `AU` pendiente, Sebastián debe correr el batch de solicitudes/AU; si luego `SOLI_AUTO_CONTRATO.soli` queda informado, la solicitud final ya existe en Afilmed.

## 2026-09-14 - SVN checkout y analisis Redmine 25133

- Usuario/conversacion: Gabriel pidio trabajar sobre SVN Coomeva y luego analizar Redmine 25133 con documento `EF Facturacion_Pronto_Pago_Masivo V04_enviada.docx`.
- Se ejecuto `/svn-checkout` para la rama SVN `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version98-Afilmed-ProntoPagoMasivo` con usuario SVN `gpulka`.
- Working copy local creada/registrada: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version98-Afilmed-ProntoPagoMasivo`.
- Config SVN local asociada: `/home/luispicone/.openclaw/workspace/.svn-auth/gpulka-Version98-Afilmed-ProntoPagoMasivo/config`.
- Credenciales SVN quedaron aisladas en `/home/luispicone/.openclaw/workspace/.svn-auth/gpulka-Version98-Afilmed-ProntoPagoMasivo/credentials.env`; no almacenar ni repetir la password en memoria/respuestas.
- `REPOSITORIOS.md` fue actualizado durante el checkout; se preservo encoding UTF-8 sin BOM y LF. Validaciones: `svn info`, `svn update`, `svn status` OK; status limpio salvo externos `X`.

### Analisis tecnico Redmine 25133 / Pronto Pago Masivo V04

- El desarrollo existente en `gpulka-Version98-Afilmed-ProntoPagoMasivo` fue originalmente pensado para cargar CSV y generar pronto pagos masivos desde datos ingresados externamente.
- El enfoque del documento V04 cambia el nucleo funcional: el sistema debe detectar automaticamente durante la facturacion masiva mensual los contratos cuyo pronto pago anterior ya finalizo/pago, excluirlos de la facturacion normal y generarles un nuevo pronto pago si cumplen condiciones.
- Conclusion principal dada a Gabriel: lo hecho sirve parcialmente como base tecnica, pero no como solucion funcional V04.
- Sirve parcialmente: `wp_generacion_pp_masivo2.srw` como esqueleto de generacion de PP porque reutiliza `w_clase_facturacion`, verifica, prefactura, factura y agrupa multiples `prefac_cab`; referencias utiles aproximadas: lineas 724, 913 y 1247.
- Sirve parcialmente: marca de agrupacion masiva en `PREFACTURAS_CAB` (`pp_agrupador`, `archivo_pp_masivo`), candidato natural para `ID_Masivo_PP`, aunque debe definirse si se renombra, duplica o conserva.
- Sirve parcialmente: tablas/configuracion `PRONTOPAGO_CONFIG` y `PRONTOPAGO_CONFIG_CUOTAS`, y SP `afu_fac_descuento_pronto_pago` que inserta concepto `DPP`.
- Sirve parcialmente: control anti doble facturacion en `afu_contratos_a_verificar.sql`, que excluye contratos con PP vigente/saldo no vencido.
- No sirve como nucleo V04: `PRONTO_PAGO_MASIVO_TEMP`, porque representa el contrato CSV viejo (contratante, contrato, periodo, incluir cuota, forma de pago, cuotas, vencimiento, mail) y en V04 esos datos deben derivarse del PP anterior y del batch mensual.
- No sirve como flujo principal: importacion/homologacion CSV de `wp_generacion_pp_masivo2.srw` y `afs_valida_pronto_pago_masivo`, ya que validan datos cargados y no detectan candidatos automaticos.
- Falta implementar tabla `PRONTOPAGO_LINKS_PAGO`; `afs_link_pagos.sql` existente es consulta general de links/deuda y no persistencia V04.

### Diseno recomendado para programar

1. Crear modelo de proceso masivo: cabecera/detalle candidatos/errores, trazabilidad de `ID_Masivo_PP`, links de pago y auditoria.
2. Crear SP de deteccion automatica desde PP anterior pagado/finalizado, integrado al contexto de plantilla/corte/facturacion masiva mensual.
3. Excluir candidatos del flujo mensual `MA` despues de llenar `#FAC_VERIF_CONTRA_PREFACTURA`, dejando trazabilidad de motivo, PP anterior origen, periodo anterior y proceso mensual origen.
4. Agregar ejecucion `FACPPM` al superbatch: hoy aparece en configuracion, pero falta constante/case en `nvo_proceso_agenda` y `wp_main_super_batch`.
5. Generar nuevos PP tipo `PI`, asociados al mismo `ID_Masivo_PP`, con periodo actual, incluir cuota periodo `S`, cantidad de cuotas y forma de pago heredadas del ultimo PP pagado, y vencimiento calculado igual que la facturacion masiva del corte.
6. Para contratante con varios contratos: si el PP anterior incluia varios contratos, regenerar nuevo PP solo si todos vuelven a calificar; si falta uno, rechazar grupo completo y loguear motivo.
7. Implementar PDF, espera/insercion de link en `PRONTOPAGO_LINKS_PAGO`, mail automatico con PDF adjunto y auditoria de envio.
8. Implementar reversa por numero de proceso masivo con regla V04: si algun PP del proceso esta pagado o vencido, no se reversa ninguno.
9. Ampliar reporte de pronto pago con fecha envio, correo destino y numero de proceso masivo.

### Riesgos detectados

- Diferenciar formalmente tipo de facturacion `PRONTO_PAGO_MASIVO = 'PM'` en `uo_factu` vs tipo de proceso agenda `FACPPM`.
- Revisar secuencia `SQC_PREFACTURAS_CAB_PP_AGRUPADOR`: el script reinicia en `max + 1` y el SP hace `NEXT VALUE` mas `+ 1`, lo que puede provocar salto conceptual.
- Definir ubicacion oficial de `ID_Masivo_PP`: documento habla de `PREFACTURAS.ID_Masivo_PP`, desarrollo existente usa `PREFACTURAS_CAB.pp_agrupador`; tecnicamente conviene cabecera por trazabilidad de cada PP individual.
- Documento V04 relevante fue extraido a `/tmp/redmine-25133/EF_Facturacion_Pronto_Pago_Masivo_V04.md`; CSV viejo revisado en `/tmp/redmine-25133/pp_masivo.csv`.

## 2026-09-14 18:22 ART - Redmine 30345 integracion prebatch y errores AU

- Sebastián pidió integrar el prebatch para que al ejecutar `afi_insertar_soli_auto` quede generado `GMS_CAB`/`GMSTXT_*` y la agenda `AU`, sin tener que ejecutar manualmente `afu_genera_soli_auto_app`.
- Se modificaron localmente y luego se comitearon en SVN `r37809`, mensaje `Redmine 30345 - integrar venta nueva con GMS`, estos SP del repo `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`:
  - `backend/procedures/afi_insertar_soli_auto.sql`
  - `backend/procedures/afi_insertar_soli_auto_venta_nueva.sql`
  - `backend/procedures/afu_genera_soli_auto_app.sql`
  - `backend/procedures/afu_soli_auto_app_contactos_gmstxt.sql`
- Cambio de `r37809`: venta nueva (`soli_moti = 1`) ejecuta el armado GMS/prebatch desde el ingreso XML; contactos pasan como domicilio principal para evitar validacion `8118`; venta nueva manda `contra = NULL` hacia GMS para no tratarla como contrato existente; agenda `AU` queda lista para batch.
- Se generaron casos de prueba en carpeta compartida `/mnt/Agentes/srmontenegro/`:
  - `redmine_30345_venta_nueva_impacto_303450003.sql`: primer caso integrado, fallo por `8140` fecha inicio servicio/corte.
  - `redmine_30345_venta_nueva_impacto_303450004_corte_ok.sql`: intento con fecha calculada desde `VTO_GRUPOS`, pero el usuario pidio evitar consultas y parametrizacion externa; ademas hubo XML truncado/error parse `unexpected end of input`.
  - `redmine_30345_venta_nueva_303450005_crudo_ejecutar.sql`: XML crudo, un solo `EXEC`, fecha `2026-10-11`, fallo por `8141` corte/primer dia de mes.
  - `redmine_30345_venta_nueva_303450006_crudo_inicio_mes.sql`: XML crudo, un solo `EXEC`, `id_soli_externa = 303450006`, `fecha_aplicacion = 2026-10-01`, `fecha_inicio_servicio = 2026-10-01`; logro pasar validaciones DEF y entro a generacion.
- Batch AU con caso `303450006` avanzo hasta generacion real: log mostro `Transacción real GMS: AT - Motivo: 1` y luego fallo SQL Server por `Cannot insert the value NULL into column 'ori', table 'InstallQA_89.dbo.SOLI_CLASE'; column does not allow nulls. INSERT fails.`
- Diagnostico dado: para venta nueva se estaba generando `GMSTXT_CLASE_PLAN`/`GMSDEF_CLASE_PLAN` con `ori = NULL`; al impactar en `SOLI_CLASE.ori` falla porque es obligatorio.
- Correccion local aplicada pero AUN NO COMITEADA: en `backend/procedures/afu_genera_soli_auto_app.sql`, para `soli_moti = 1` ahora determina `ori` desde `SUBCTA_CONTRATO` vigente de `cuenta_dest/subcta_dest`; si no puede determinar `ori`, corta antes con mensaje claro en lugar de dejar llegar `NULL` al batch.
- Estado pendiente inmediato: compilar `dbo.afu_genera_soli_auto_app` con esa correccion, generar nuevo caso con nuevo `id_soli_externa`, correr XML crudo + batch AU y validar si impacta en `SOLICITUDES`. Si Sebastián pide commit, comitear primero la correccion local de `afu_genera_soli_auto_app.sql`.
- Error PB `Mismatch between retrieve columns and fetch columns` aparece al intentar recuperar referencia/log de solicitud APP, pero hasta ahora no fue el bloqueo funcional principal; los bloqueos reales fueron `8140`, `8141` y luego `SOLI_CLASE.ori NULL`.
