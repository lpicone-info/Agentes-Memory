# Session: 2026-05-27 22:25 UTC

- **Session Key**: agent:main:discord:direct:1504832198258331822
- **Source**: discord
- **Contexto**: trabajo con usuario Discord `bmora_info` sobre R29154 / Mantis 11367, SP `afs_consulta_integrantes_contrato` y documentación funcional Coomeva/Afilmed.

## Conversation Summary

Se revisó el DEF `A-04-DEF SPs de Consultas y Validaciones por Contrato_V05.docx`, punto `4.1. SP: Consulta de Integrantes de un Contrato`, contra el SP compilado `afs_consulta_integrantes_contrato` en el profile `PruebasCoreINFOMEDICAL`.

Hallazgo principal: para el contrato `1005124`, período `05/2026`, existen dos comprobantes/prefacturaciones vigentes no reversadas en el mismo período: `prefac_cab 1345710` para integrantes `01`, `02`, `03` y `prefac_cab 1358226` para integrante `04`. El SP compilado tomaba un único `@v_ult_prefac` global por contrato, por lo que usaba `1358226` para todos los integrantes. Resultado observado: integrantes `01`, `02`, `03` en `0.00` y `04` con valor.

Diagnóstico: la causa raíz es que el SP interpreta "último comprobante facturado al contrato" como un único `prefac_cab` global para todo el contrato. Ese criterio no cubre casos de inclusiones posteriores en el mismo período.

Recomendación funcional/técnica propuesta: tomar el último período facturado vigente/no reversado del contrato y, dentro de ese período, determinar para cada integrante el último `prefac_cab` vigente/no reversado que tenga detalle en `PREFACTURAS` para ese integrante. Mantener el cálculo neto con `impor * debe_haber` para preservar el comportamiento observado en conceptos con descuentos o movimientos negativos.

Valores esperados bajo el criterio recomendado para contrato `1005124`: `01 = 342700.00`, `02 = 538200.00`, `03 = 1054500.00`, `04 = 205600.00`, todos con `valor_tarifa_anexo = 0.00`.

Se generó un documento Word con el análisis completo en el repositorio `bmora-ProyectoCoomeva-Afilmed-01.Documentacion`, carpeta relativa `Req y Doc. Complementaria/_Scripts/R29154 - Mantis 11367 - SP Consulta de Integrantes de un Contrato`, archivo `Analisis_SP_Consulta_Integrantes_Contrato_tarifas_2026-05-27.docx`. Se validó como DOCX legible y se commiteó en SVN revisión `3044` con mensaje `Agrega analisis tarifas SP consulta integrantes contrato`.

Luego se compararon dos documentos funcionales:

1. `Req y Doc. Complementaria/_Scripts/R29154 - Mantis 11367 - SP Consulta de Integrantes de un Contrato/A-04-DEF SPs de Consultas y Validaciones por Contrato_V05.1_entregada.docx`
2. `A-04-DEF SPs de Consultas y Validaciones por Contrato_V05.docx`

El archivo `V05.1_entregada` existía en SVN remoto pero no en la working copy local, por lo que se leyó con `svn cat` a temporal de análisis sin ejecutar `svn update`.

Diferencias detectadas: `V05.1_entregada` agrega historial versión `5.1` del `23.04.2026` por `Svarela`, indicando actualización del punto `4.4` Mantis `11539`. En `4.1` no hay cambios funcionales; solo resaltados/marcados sobre referencias a `PREFACTURAS`, fórmulas y ejemplos. En `4.3` se separan mensajes de error: baja pasa a `El Contrato o usuario no está activo`, inexistencia pasa a `El Contrato o usuario no existe`; además se elimina la frase de múltiples anexos separados por coma en `Anexo_cobertura = SI`.

En `4.4` hay cambios funcionales relevantes para `SP: consulta contratos en AFILMED`: el estado transitorio cambia de `cuenta = 24690 y subcta = 29690` a `cuenta = 24690` para todas las subcuentas / `subcta = 'todas'`; se agrega estado `Retirado` cuando el parámetro de estado contrato es `N`; `Tipo plan` y `Programa` agregan criterio de último dato anterior para estado `Activo, transitorio`; `Cuenta` aplica a todos los estados; `Saldo en mora` pasa a requerir saldos pendientes y vencidos; y en parámetros de salida se ajustan `Estado contrato`, `NIT` y `Nombre cuenta`.

Conclusión de comparación: `V05.1_entregada` no modifica funcionalmente el punto `4.1` que motivó el análisis de tarifas, pero sí trae cambios relevantes para `4.4` y ajustes puntuales en `4.3`.
