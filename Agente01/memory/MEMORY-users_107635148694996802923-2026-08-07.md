# Memoria curada diaria

- Fecha: 2026-08-07
- Usuario: Savina Varela
- id_googlechat: users/107635148694996802923
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/xeq2hyaaaae

## Resumen de la actividad

### Tema 1: Inicio de sesion y carga de contexto operativo

- Solicitud o contexto: Savina saludo al agente.
- Respuesta o acciones del agente: Se ejecuto el bootstrap operativo: lectura de `REGLAS.md` en bloques, `DB_PROFILE.md`, `REPOSITORIOS.md`, `USUARIOS.md` y preferencias de Savina.
- Resultado: El agente respondio que las preferencias de Savina estaban cargadas y quedaba disponible para trabajar.
- Estado: Informativo

### Tema 2: Validacion de responsable pagador obligatorio para traslado a contrato nuevo

- Solicitud o contexto: Savina pidio generar un caso igual al `id=179`, pero sin responsable pagador, para probar el arreglo de obligatoriedad cuando el XML no trae datos de responsable pagador.
- Respuesta o acciones del agente: El agente pidio el profile de base de datos y la entidad del identificador. Savina indico Version104 en `InstallQA_89`. Se consulto memoria previa, se valido conectividad y se ejecuto prueba contra `InstallQA_89` con `dbo.afi_insertar_soli_auto`, usando variantes sin nodo `<responsable_pagador>` y con nodo vacio.
- Resultado: La prueba sin nodo fue rechazada con el mensaje `El XML debe informar responsable_pagador para traslado a contrato nuevo`. La prueba con nodo vacio fue rechazada con el mensaje `El XML informa responsable_pagador sin datos obligatorios de traslado a contrato nuevo`. Los `id_soli_externa` de prueba `900104179` y `900104180` quedaron con 0 registros en `SOLI_AUTO_CONTRATO`.
- Estado: Resuelto

### Tema 3: Caso documentable de responsable pagador ausente

- Solicitud o contexto: Savina pidio preparar un caso para documentar la validacion anterior.
- Respuesta o acciones del agente: Se redacto el caso funcional con ambiente `InstallQA_89`, Version104, SP `dbo.afi_insertar_soli_auto`, caso base `id_proceso = 179`, `id_soli_externa = 5025`, `soli_moti = 47`, objetivo, XML de prueba y evidencia.
- Resultado: Quedo documentado que para `soli_moti = 47` el XML debe informar responsable pagador; si el nodo no existe, el proceso corta antes de generar el caso en `SOLI_AUTO`.
- Estado: Resuelto

### Tema 4: Validacion de campaña comercial no permitida en traslado a contrato nuevo

- Solicitud o contexto: Savina pidio replicar el caso de `soli_externa 293631201`, donde se agregaba campaña comercial, para probar el arreglo del XML porque traslado a contrato nuevo no permite informar campañas.
- Respuesta o acciones del agente: Se consulto memoria previa y se ejecuto la validacion en `InstallQA_89` con `dbo.afi_insertar_soli_auto`. Se preparo una prueba negativa con `cod_camp_comercial="134"` en cabecera y una prueba positiva con XML corregido sin `cod_camp_comercial`.
- Resultado: El XML con campaña fue rechazado con el mensaje `El XML no debe informar campania comercial para traslado a contrato nuevo`. El XML corregido, usando `id_soli_externa = 900104185`, origen `1 / 244526 / 02`, documento `CC 88308248`, destino `cuenta_dest = 2`, `subcta_dest = FSMP`, `plan_dest = OROF`, `tari_dest = FOACON`, paso la validacion de insercion con rollback y devolvio `id_proceso = 180`, `cod_camp_comercial = NULL`, `emi_modo = 0`.
- Estado: Resuelto

### Tema 5: Prueba de autocompletado de datos de facturacion y forma de pago en colectivos

- Solicitud o contexto: Savina pidio probar que, en colectivos, los datos de facturacion y forma de pago no se tomen desde el XML, sino que se autocompleten desde las tablas correspondientes y se graben correctamente en tablas `SOLI` y `AFI`, sin modificar datos de subcuentas.
- Respuesta o acciones del agente: Se consultaron tablas de contrato y subcuenta en `InstallQA_89`. Se identifico una subcuenta de destino `cuenta = 56`, `subcta = 2242` con valores esperados `emi_grupo = 2`, `emi_modo = 0`, `vto_grupo = 2`, `factu = E`, `factu_def = E`, y domicilio esperado de subcuenta. Se ejecuto prueba con rollback usando `id_soli_externa = 900104186` y un XML que contenia datos de contacto distintos para verificar que el proceso tomara datos de tablas.
- Resultado: La insercion dejo en `SOLI_AUTO_CONTRATO` los valores esperados desde la subcuenta: `emi_grupo = 2`, `emi_modo = 0`, `vto_grupo = 2`, `pago_forma = E`. Los contactos tipo `C` en `SOLI_AUTO_DOMICILIOS` y `SOLI_AUTO_EMAILS` se poblaron desde datos de subcuenta, no desde los contactos del XML. El batch quedo con cabecera `GMS` en estado `B`, 0 errores, 0 warnings y `puede_procesarse = S`. Se verifico que `SUBCTA_CONTRATO` y `SUBCTA_DOMICILIOS` quedaron sin cambios despues del rollback, y que `id_soli_externa = 900104186` quedo con 0 registros despues del rollback.
- Errores o interrupciones: Hubo un segundo intento de prueba cuyo resultado no quedo registrado porque el turno se cerro por reinicio del gateway con `missing_tool_result`; no hubo respuesta final al usuario para ese pedido dentro de la sesion revisada.
- Estado: En curso
