# 2026-05-11

## Redmine 29362 / Traslado a Contrato Existente (`soli_moti = 48`) - avance SP inicial

- Interlocutor en Discord DM identificado por metadata confiable del mensaje como `SebastianRMontenegro`.
- Sebastián confirmó que el caso corresponde a `soli_moti = 48` y que el proceso es independiente de 29361; 29361 debe tomarse solo como base técnica del SP/XML, sin repetir su lógica funcional completa.
- Criterio acordado: partir del SP de 29361 `afi_insertar_soli_auto` y extenderlo para el XML completo de `Traslado a Contrato Existente`, usando los nombres comunes ya existentes en 29361: raíz `<solicitud>`, atributos `id_soli_externa`, `soli_moti`, `prepaga`, `contra`, `fecha_aplicacion`, `cuenta_dest`, `subcta_dest`, `plan_dest`, `tari_dest`, `plan_emp_dest`, `tari_emp_dest`; integrantes con `inte`; anexos con `inte`, `plan_anexo`, `tari_anexo`, `plan_anexo_sup`, `tari_anexo_sup`.
- Campos nuevos propuestos para 29362: cabecera `fecha_inicio_servicio`, `estado_traslado`, `requiere_auditoria`, `asesor_gestion`, `usuario_proceso`, `observacion`; integrantes `docu_tipo`, `docu_nro`, `anter_prepa`, `anter_contra`, `anter_inte`, `anter_plan_codi`, `paren_destino`, `paren_real_destino`, `copia_preexistencias`, `no_genera_carta_negacion`, `estado_auditoria`, `nivel_riesgo`, `observacion_auditoria`.
- Se creó el archivo SVN: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas/Redmines/29362/afi_insertar_soli_auto_traslado_existente.sql`.
- El SP creado se llama `dbo.afi_insertar_soli_auto_traslado_existente` y recibe `@var_data_xml XML`; valida `soli_moti = 48`, `id_soli_externa`, existencia de integrantes, datos obligatorios de traslado, auditoría si `requiere_auditoria = 'S'`, y anexos con `inte`, `plan_anexo`, `tari_anexo`.
- El SP inserta en `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES` y `SOLI_AUTO_ANEXOS`, devuelve `SELECT @var_id_proceso AS id_proceso`, usa `SET NOCOUNT ON`, `SET XACT_ABORT ON`, `BEGIN TRY/BEGIN CATCH`, transacción y rollback ante error.
- Validaciones antes de subir: archivo creado sin `;` y sin `RTRIM`. Encoding reportado por `file -bi` como `text/plain; charset=us-ascii`.
- Se ejecutó `svn add` y `svn commit` usando usuario SVN `srmontenegro` provisto por Sebastián. No exponer ni registrar la contraseña en respuestas o documentación.
- Commit SVN realizado correctamente: `r36830`, mensaje `29362: agregar SP inicial traslado contrato existente`.
- La explicación paso a paso del SP quedó en curso. Ya se explicaron, en lenguaje simple:
  1. `IF OBJECT_ID('dbo.afi_insertar_soli_auto_traslado_existente') IS NOT NULL`: verifica si el SP existe.
  2. `DROP PROCEDURE dbo.afi_insertar_soli_auto_traslado_existente`: borra el SP previo para recrearlo limpio.
  3. `GO`: separador de lote antes del `CREATE PROCEDURE`.
  4. `CREATE PROCEDURE dbo.afi_insertar_soli_auto_traslado_existente`: crea el SP específico para traslado existente.
  5. `(@var_data_xml XML)`: parámetro único que recibe el XML completo.
  6. Explicación humana de la tarea: recibe pedido web, lo guarda en bandeja/interfaz, otro proceso lo convierte en solicitud real e impacta el traslado.
  7. Conceptos explicados: Afilmed = sistema principal de afiliados/contratos/solicitudes; traslado = mover afiliado de contrato origen a contrato destino; solicitud real = registro operativo formal en Afilmed, especialmente `SOLICITUDES`.
  8. `AS BEGIN`: inicio del cuerpo/receta del SP.
  9. `SET NOCOUNT ON`: evita mensajes de filas afectadas.
  10. `SET XACT_ABORT ON`: ante error en transacción cancela todo para no dejar carga a medias.
  11. `DECLARE ...`: variables temporales internas del SP.
  12. `BEGIN TRY`: zona protegida donde se intenta ejecutar el trabajo principal.
  13. Primer bloque `SELECT @var_... = @var_data_xml.value(...)`: lee atributos del XML y los guarda en variables SQL.
  14. Lectura de datos de contrato destino: `fecha_aplicacion`, `fecha_inicio_servicio`, `cuenta_dest`, `subcta_dest`, `plan_dest`, `tari_dest`, `plan_emp_dest`, `tari_emp_dest`.
  15. Lectura de datos de control: `estado_traslado`, `requiere_auditoria`, `asesor_gestion`, `usuario_proceso`, `observacion`.
  16. Validación `IF @var_soli_moti <> 48`: barrera para que el SP solo procese traslado existente.
  17. Validación `IF @var_id_soli_externa IS NULL`: exige trazabilidad externa.
  18. Último punto explicado antes de cortar: `IF @var_data_xml.exist('/solicitud/integrantes/integrante') = 0`: valida que el XML tenga al menos un integrante a trasladar.
- Punto exacto para continuar mañana: seguir explicando el próximo bloque del SP:
  `IF @var_data_xml.exist('/solicitud/integrantes/integrante[not(@inte) or not(@docu_tipo) or not(@docu_nro) or not(@anter_prepa) or not(@anter_contra) or not(@anter_inte) or not(@paren_destino)]') = 1 ...`.

## Cierre jornada Gabriel / Redmine 29361 y entorno gpulka

- Interlocutor de Discord DM identificado por metadata confiable como `gpulka_info` / Gabriel Pulka.
- Se retomó contexto del Redmine `29361`: APP de autogestión Coomeva, solicitudes automáticas de cambio de cobertura, Fase 1 `dbo.afi_insertar_soli_auto` y Fase 2 `dbo.afu_genera_soli_auto_app`.
- Se revisó documentación local del proyecto en `/home/luispicone/.openclaw/workspace/Coomeva-Afilmed-Version104-Afilmed-SolicitudesAutomaticas/Redmines/29361/29361.md` y `CRONOGRAMA.md`.
- Gabriel pidió documentación funcional de Fase 2 orientada a analista funcional/cliente, con alcance solo hasta la ejecución de `afu_genera_soli_auto_app`. Criterio ajustado: evitar excesos técnicos, no listar campos/tablas como detalle técnico innecesario, hablar de lo que hace el proceso y no enfatizar lo que no hace.
- Versión funcional final propuesta para Fase 2: el proceso toma solicitudes APP pendientes, valida datos mínimos, evita duplicaciones, determina la operación GMS según motivo, incorpora cobertura destino/anexos, agenda el caso como `AU`, conserva trazabilidad `id_proceso`/`id_soli_externa`, identifica origen `APP` y marca la entrada como procesada.
- Gabriel pidió un ejemplo de ejecución que no cargue manualmente `SOLI_AUTO_*`. Se preparó ejemplo combinado: `BEGIN TRANSACTION`, XML de entrada, llamada a `dbo.afi_insertar_soli_auto`, obtención de `@id_proceso`, verificaciones de interfaz, ejecución de `dbo.afu_genera_soli_auto_app`, obtención de `@cab_id`, verificaciones GMS y `ROLLBACK TRANSACTION`. En el ejemplo se corrigió `soli_moti` a `6` porque mapea a cambio de cobertura / `CC`.
- Se registró credencial Redmine local para usuario `gpulka` mediante skill `redmine-signup`; archivo: `/home/luispicone/.openclaw/secrets/redmine/gpulka.env`, permisos `600`. No exponer ni copiar la API key.
- Se guardó endpoint Redmine local: `/home/luispicone/.openclaw/secrets/redmine/endpoint.env`, endpoint `https://redmine.infomedical.com.ar/`, permisos `600`.
- Se consultó Redmine `29361` con usuario `gpulka`: proyecto `COOMEVA - Desarrollos Facturable`, tracker `Tarea`, estado `En Análisis`, prioridad `Media`, asunto `Implementar proceso automático grabación cambio de cobertura`, autor/asignado Savina Varela, incidente externo `11503`, actualizado el `2026-05-11T18:58:47Z`.
- En Redmine `29361` quedó detectada la última nota de Gabriel con documentación funcional Fase 2 y ejemplo de ejecución combinado Fase 1 + Fase 2.
- Gabriel pidió buscar en Redmine Coomeva un incidente con script adjunto para identificar contratos retirados por motivo distinto de `Depuración de saldos`. Hallazgos principales:
  - `#29675 - DEPURACION DE SALDOS ==> Script de datos`, proyecto `COOMEVA - Soporte`, estado `Desarrollo en Progreso`, descripción coincide casi literal con la búsqueda. Adjuntos SQL: `Contratos_Retirados_con_Saldo_individuales (2).sql` y `R29675M11669.sql`.
  - Antecedente base más claro: `#23999 - SCRIPT =>> Listado de Contratos Retirados con saldo`, cerrado, adjunto `Contratos_Retirados_con_Saldo_individuales.sql`.
  - Relacionados: `#24059` con `24059.sql`; `#27671` con `R27671M10113.sql` y `R27671M10113_2.sql`.
- Se verificó estado pendiente de commit en `gpulka Version104`: Git `/home/luispicone/.openclaw/workspace/Coomeva-Afilmed-Version104-Afilmed-SolicitudesAutomaticas` limpio; SVN `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas` sin cambios locales, solo externals `X`.
- Gabriel pidió usar skill `svn-delete` para eliminar la working copy SVN `gpulka-Version104-Afilmed-SolicitudesAutomaticas`. Se siguió el flujo con dos confirmaciones exactas. Se eliminó `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas` y se removió su entrada de `REPOSITORIOS.md`. Validaciones OK.
- Gabriel pidió usar skill `svn-checkout` para volver a conectar la rama SVN `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version104-Afilmed-SolicitudesAutomaticas` con usuario SVN `gpulka`. No exponer ni registrar la contraseña en respuestas/memoria.
- El checkout SVN nuevo quedó en `~/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`, revisión `36831`, con externals `X` en `exe_InfoMailer/pbls/impresion.pbl`, `exe_presmed/entidades.pbl`, `exe_presmed/ficha_afiliado.pbl`.
- Se creó credencial SVN aislada para esa working copy: `~/.openclaw/workspace/.svn-auth/gpulka-Version104-Afilmed-SolicitudesAutomaticas/credentials.env`, config `~/.openclaw/workspace/.svn-auth/gpulka-Version104-Afilmed-SolicitudesAutomaticas/config`, permisos validados. Futuras operaciones SVN deben usar esos datos registrados y `--config-dir`; no usar cache global `~/.subversion/auth`.
- Se actualizó `REPOSITORIOS.md` agregando nuevamente la entrada SVN `gpulka-Version104-Afilmed-SolicitudesAutomaticas` con usuario SVN asociado `gpulka`, config local aislado y archivo de credenciales SVN.
- Gabriel pidió registrar tiempo dedicado en Redmine `29361`: se cargaron `8` horas para el día `2026-05-11`, actividad `Desarrollo`, comentario `desarrollo`, usando usuario `gpulka`. Time entry creado: `384878`.
- Cierre indicado por Gabriel: “Por hoy terminamos, guarda todo lo visto en memoria persistente”.
