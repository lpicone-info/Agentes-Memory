# Memoria 2026-08-04

## Caso XML traslado a contrato nuevo - Savina
- Contexto: Google Chat directo con Savina. Solicito caso XML completo de traslado a contrato nuevo para version EXE 104, BD `InstallQA_89`.
- Repositorio usado: `svarela-Version104-Afilmed-SolicitudesAutomaticas`, actualizado a SVN rev. `37499` para usar la ultima version disponible de version 104.
- Caso origen validado en `InstallQA_89`: contrato `1-211971`, vigente, con `subcta_dest=ASMP`, `plan_dest=OROA`, `tari_dest=AOANCN`, `plan_emp_dest=OROA`, `tari_emp_dest=AOANCN`.
- Contacto RU validado: `codigo_unico_persona=95821`, documento `CC 25166806`, integrante GLORIA ESPERANZA ALZATE SANCHEZ. Como los contactos `I` y `R` existen vigentes en Registro Unico, no se deben enviar como contactos nuevos en XML; el proceso debe completar desde RU. Se incluyo solo facturacion/domicilio/email necesarios segun el caso.
- XML propuesto para `dbo.afi_insertar_soli_auto`: `id_soli_externa=900104047`, `soli_moti=47`, `prepaga=1`, `id_grupo=1`, `fecha_inicio_servicio=2026-08-01`, `cuenta_dest=1`, `subcta_dest=ASMP`, `plan_dest=OROA`, `tari_dest=AOANCN`, `plan_emp_dest=OROA`, `tari_emp_dest=AOANCN`, `asesor_gestion=20`, `suc_comercializable=11001`, `habeas_data=S`, `vip=N`; facturacion con `emi_grupo=2`, `emi_modo=0`, `vto_grupo=2`, `pago_forma=E`; integrante `inte=01`, `docu_tipo=CC`, `docu_nro=25166806`, `anter_prepa=1`, `anter_contra=211971`, `anter_inte=01`, parentescos `T`.

## Diagnostico error cobertura destino
- Savina consulto por `id_proceso=158` y `cab_id` en errores `295819`, error: "Cobertura destino sin plan o tarifa vigente".
- Hallazgo: `cab_id=295819` no corresponde a `id_proceso=158`, sino a `id_proceso=157`, `id_soli_externa=5016`, documento `CC 88308248`, `cuenta_dest=2`, `subcta_dest=ASMP`, `plan_dest=OROA`, `tari_dest=AOANCN`, `fecha_aplicacion=2026-07-01`.
- `id_proceso=158` corresponde al caso documento `CC 25166806`, `cuenta_dest=1`, `fecha_aplicacion=2026-08-01`, `plan_dest=OROA`, `tari_dest=AOANCN`.
- Causa tecnica del error en `cab_id=295819`: `gmsdef_val_traslado_web` valida cobertura contra `SUBCTA_PLAN_TARI` usando `GMS_CONTRATOS.cuenta/subcta/plan/tari`. Para el cab `295819` busca `cuenta=2`, `subcta=ASMP`, `plan=OROA`, `tari=AOANCN`, `fecha=2026-07-01`; esa combinacion no existe en `SUBCTA_PLAN_TARI` para `cuenta=2`. La combinacion `ASMP / OROA / AOANCN` si existe vigente para `cuenta=1`.
- Conclusion comunicada: el XML/caso que genero `cab_id=295819` esta mal por `cuenta_dest=2`; para ese plan/tarifa debe ir `cuenta_dest="1"`.

## Continuidad XML traslado ASMP / OROA / AOANCN - Savina
- Error `8047`: asociado al `cab_id=295820`. Sale de `gmsdef_val_resp_pagador` y significa que el responsable pagador quedo sin `nombre`. En `GMSDEF_RESP_PAGADOR` para `cab_id=295820` estaban vacios `nombre`, `ape`, `naci_fecha`, `sexo`. Causa: se habia enviado `<responsable_pagador>` solo con documento esperando que complete desde RU, pero esa etapa no completa datos personales del responsable. Correccion: enviar datos personales completos del responsable pagador en XML; los contactos `I/R` si pueden omitirse cuando existen vigentes en Registro Unico.
- Error origen sin poblacion activa y saldos por pagar en `cab_id=295821`: el caso anterior trasladaba un integrante que dejaba el contrato origen sin poblacion activa y/o con deuda. Se busco un caso mejor: contrato origen `1-101306`, cobertura `ASMP / OROA / AOANCN`, 7 activos, trasladando integrante `02` el origen conserva poblacion; `cls_contrato` al `2026-08-01` con saldo 0, debitos 0, creditos 0, sin ajustes ni acuerdo pendiente. Integrante sugerido: `CC 70569878`, pero luego dio riesgo por cooperativa.
- Error `8087`: mensaje `El responsable pagador o Titular del contrato no se encuentra activo en la COOPERATIVA.` Para `ASMP`, la subcuenta exige asociado (`es_asociado=S`), por lo que responsable pagador o titular destino debe existir en `ASOCIADOS_COOPERATIVA`. `CC 70569878` no figuraba; `CC 43739013` si figuraba.
- XML alternativo armado con titular/responsable en cooperativa: `id_soli_externa=900104050`, origen `anter_contra=101306`, `anter_inte=01`, titular/responsable `CC 43739013`, `SALDARRIAGA DE LA TORRE VALERIA`, con `cuenta_dest=1`, `ASMP / OROA / AOANCN`, fecha `2026-08-01`. Se validaron contactos RU vigentes para `CC 43739013`, por eso no agregar contactos `I/R`.

## Contrato recomendado para traslado sin deuda y sin mover titular origen
- Savina pidio buscar un contrato con titular/titular, sin deuda, con mas de un integrante para trasladar sin deuda.
- Candidato validado en `InstallQA_89`: contrato origen `1-254659`.
- Validaciones: cobertura vigente `ASMP / OROA / AOANCN / OROA / AOANCN`; integrantes activos `5`; titular/titular vigente `inte 01`, `CC 16208018`, `GOMEZ VELASQUEZ RAMIRO`; deuda por `cls_contrato` `0.00`; saldos vencidos `0.00`; acuerdo de pago `N`; ajustes no facturados `N`; conceptos futuros `N`; forma de pago actual `E` efectivo.
- Integrante recomendado para trasladar sin tocar titular origen: `inte 02`, `CC 38439475`, `SALGUERO RIVADENEIRA MARIA LILIANA`. Esta vigente, no es titular en origen (`paren C / paren_real C`) y figura en Cooperativa, por lo que puede quedar como titular del contrato destino y evitar `8087`.

## Errores cobertura destino por cuenta incorrecta
- Para `cab_id=295824`, error `Cobertura destino sin plan o tarifa vigente`: el registro quedo con `cuenta_dest=2`, `subcta_dest=ASMP`, `plan_dest=OROA`, `tari_dest=AOANCN`. Esa combinacion no existe vigente para cuenta 2; existe para cuenta 1. Correccion: usar `cuenta_dest="1"`.
- Errores relacionados en `cab_id=295824`, etapa `208`, ref `2188432`: motivos `5144`, `5145`, `5046`, `5045`, repetidos. Significados comunicados: `5144` subcuenta debe existir/no estar dada de baja en solapa contrato; `5145` subcuenta debe estar asociada a prepaga; `5046` cuenta/subcuenta sin contrato vigente para fecha de aplicacion; `5045` cuenta/subcta/programa/tarifa dada de baja, inhabilitada o no vigente. Causa real comun: `ASMP / OROA / AOANCN` cargado con `cuenta=2`; corregir a `cuenta=1`.

## Especificacion traslado a contrato nuevo - titular y responsable
- Se reviso especificacion Redmine `29363` y procedimiento `afi_genera_solis_aut_traslados`.
- Regla funcional documentada: no se debe trasladar el titular/titular del contrato origen. El origen debe conservar al titular original vigente; el integrante trasladado puede ser beneficiario/conyuge/hijo/etc. En destino ese integrante puede quedar como titular con `paren_destino="T"` y `paren_real_destino="T"`.
- Contrato destino: titular lo determina el XML; debe haber un solo integrante destino con `paren_destino=T` y `paren_real_destino=T`. Responsable pagador destino se informa en `<responsable_pagador>` o empleado si aplica.
- Contrato origen: se da de baja solo el integrante trasladado; el titular original queda vigente; el responsable pagador vigente del origen no se redefine desde el XML destino.
- Rama tecnica si se traslada igualmente el titular origen: el codigo busca titular actual con `afn_inte_tit`; si el trasladado es titular, genera solicitud automatica de cambio de titularidad. Nuevo titular origen: integrante activo de mayor edad que no sea el titular; si empata, menor `inte`. Si no existe `AFI_RESP_PAGADOR` vigente para origen, genera solicitud automatica de cambio de responsable pagador; si ya existe, no recalcula.

## Ventana sitios/centros de pago
- Savina pregunto donde guarda configuracion la ventana sitios de pago. Respuesta: tabla `CAJA_HOMOLOGACION`.
- Ventana `wp_centros_cobranza` graba llamando a `afu_centros_cobranza`, que hace `UPDATE/INSERT` sobre `CAJA_HOMOLOGACION`.
- Campos principales: `sitio_pago`, `ftp_pendientes`, `ftp_procesados`, `ftp_error`, `uso_local`.
- Para consultar/listar: `afs_centros_cobranza`, tambien lee `CAJA_HOMOLOGACION`. `FILTRO_SITIO_PAGO` existe, pero parece ser para filtros temporales/de usuario en otras ventanas/reportes, no para la configuracion real.

## Validacion edad tope por programa padre
- Savina pidio explicar regla: si el programa a nivel de plan padre de la solicitud es igual al programa del contrato donde esta activo el beneficiario, no se valida edad minima ni tope; si es distinto, se bloquea con `Al menos un integrante no cumple con la edad mínima o tope del Programa principal`.
- Explicacion dada: si beneficiario ya esta activo en el mismo programa padre al destino, se preserva continuidad y no se reevalua edad como venta nueva. Si cambia a otro programa padre, se valida edad contra topes del programa destino. No mirar solo `plan_dest`/`tari_dest`; comparar programa/grupo padre (`plan_grupo` del programa).

## Validacion edad 8142 - traslado automatico vs manual
- Savina consulto por `id_proceso=156`, error `8142`: `Al menos un integrante no cumple con la edad minima o tope del Programa principal`.
- Datos del caso en `InstallQA_89`: beneficiaria `CC 42753948`, `MEJIA CLARA`, origen contrato `1-244512`, `inte 02`, nacimiento `1960-11-08`, edad a `2026-07-01`: 65 anios. Destino `FSMP / OROF / FOACON`; plan destino `OROF`; `plan_grupo` destino `ORO`; tope edad `OROF` 0 a 59.
- Origen tambien estaba en plan `OROF`, `plan_grupo=ORO`. Funcionalmente deberia aplicar excepcion de misma familia/programa padre y no validar edad tope.
- Diagnostico comunicado: el flujo automatico de traslado a contrato nuevo (`soli_moti=47`) ingresa a GMS como transaccion tecnica `AT`/alta total. En esa rama la validacion SQL revisa edad contra plan destino sin comparar `plan_grupo` origen vs destino, por eso dispara `8142` y tambien puede aparecer `8086` (`La edad del beneficiario no aplica para el programa al que esta ingresando`).
- Comparacion con ventana manual: en `wp_alta_total.srw` rev. version 104, la validacion manual si respeta la regla. Si `ii_soli_moti = 1` fuerza validacion; si no, compara `plan_grupo_destino` contra `plan_grupo_anterior` del integrante y solo valida edad minima/maxima cuando cambia el grupo de plan. Referencia revisada: `exe_afilmed/src/wp_alta_total.srw` alrededor de linea `11147`.
- Conclusion funcional: hay brecha entre especificacion y automatica. La manual de traslado a contrato nuevo respeta `mismo plan padre => no validar edad`; la automatica usada por solicitudes de traslado nuevo no la respeta para `AT`. Para pasar sin modificar codigo, usar beneficiario dentro del tope de edad del plan destino o un plan destino cuyo tope permita la edad; para corregir funcionalmente, alinear la validacion automatica con la regla de `plan_grupo`.

## 2026-08-04 15:17 ART - Redmine 29194 / Mantis 9100

- Usuario: Gabriel.
- Contexto de trabajo: documento `/mnt/Agentes/gpulka/29194/REDMINE_29194_ANALISIS_TECNICO_MANTIS_9100_REV1.docx`, contrastado con funcional `A-04-DEF Facturacion 6 - CC Automatico Asoc a Fam_V7.docx`.
- Preferencia/criterio funcional indicado por Gabriel: los campos nuevos en scripts/SELECT/listas se agregan siempre al final, no intercalados por ubicacion sugerida.
- Correccion importante: no inventar tablas `CONTRATOS` ni `AFI_CONTRATOS`; Gabriel indico que esas tablas no existen. Para contrato nuevo/traslados revisar `SOLI_AFILIADOS.ingre_fecha` como fecha de inicio del servicio del integrante; aparentemente en venta nueva es igual para todos los integrantes y en traslados deberia tomarse igual.
- Cambio de cobertura solo programa: basta con que cambie `plan_codi`; no condicionar por tarifa ni por `tari_ant`.
- Responsable pagador en Mantis 9100 quedo revisado con solicitud 22 + `SOLI_RESP_PAGADOR.vigen_desde`, confirmando vigencia en `AFI_RESP_PAGADOR`; Gabriel habia pedido revisar porque le hacia ruido.
- Documento actualizado: `fecha_aplicacion_cambio` indicado al final del SELECT/listas; eliminada hipotesis `CONTRATOS/AFI_CONTRATOS`; contrato nuevo/traslados contra `SOLI_AFILIADOS.ingre_fecha`; cobertura por `plan_codi_ant <> plan_codi`; verificada integridad DOCX y busqueda de frases problematicas.

## Redmine 29362 - ejemplo soli 48 con auditoria para circuito de exito

- Usuario: Sebastian.
- Contexto: Google Chat directo. Sebastian pidio armar un ejemplo de traslado con auditoria que complete circuito de exito, primero para `soli_moti = 48`, usando profile `InstallQA_89`.
- Base consultada: `InstallQA_89`, solo lectura. No se ejecutaron inserts ni procesos.
- Contexto tecnico recuperado: `soli_moti = 48` corresponde a traslado a contrato existente. Flujo esperado: `SOLI_AUTO_*` -> GMS -> `SOLICITUDES` / `SOLI_AFILIADOS` -> impacto destino -> retiro origen. Antecedentes relevantes del Redmine `29362`: commits `r36863`, `r36865`, `r36869`; luego ajustes `r37378` y `r37379` sobre `gmsdef_val_traslado_web` para excluir titular tecnico destino y rechazar traslado de titular origen.
- Hallazgo actual: aunque en una decision anterior se habia excluido auditoria del puente automatico, la base actual `InstallQA_89` tiene `dbo.afi_insertar_soli_auto_traslado_existente` con lectura de nodo `/solicitud/auditoria_medica` e insercion en `SOLI_AUTO_AUDITORIA_MEDICA`.
- Reglas vigentes: existen reglas en `REGLAS_AUDITORIA_MEDICA` para `soli_moti = 48`, `novedad = 4`, con `edad_mayor_a = 54`, por plan grupo y genero. Para circuito exitoso con auditoria, usar estado `AEA`, confirmado en `ESTADOS_INTEGRANTES` como aceptado en auditoria medica, `impacta = S`, `aceptado = S`.
- Caso recomendado para ejemplo:
  - Destino: `prepaga = 1`, `contra = 1005326`, estado contrato `I`, `vigen_hasta = NULL`, plan vigente `AESEF`, grupo plan `AESE`, 13 integrantes vigentes.
  - Origen: `anter_prepa = 1`, `anter_contra = 244512`, `anter_inte = 02`, `docu_tipo = CC`, `docu_nro = 42753948`, afiliada `MEJIA CLARA`, sexo `F`, edad 65, plan vigente `OROF`, grupo plan `ORO`, `baja_fecha = NULL`.
  - Validacion funcional: origen vigente, documento coincide, dispara regla de auditoria por grupo `ORO`, genero `F`, edad mayor a 54, y no se encontro duplicidad vigente del documento en contrato destino `1005326`.
- XML/SQL entregado como ejemplo minimo: ejecutar `dbo.afi_insertar_soli_auto_traslado_existente` con raiz `<solicitud id_soli_externa="480442451" soli_moti="48" prepaga="1" contra="1005326" fecha_aplicacion="2026-08-01" fecha_inicio_servicio="2026-08-01" asesor_gestion="1234">`, un integrante con origen `244512/02`, `paren_destino="H"`, y bloque `<auditoria_medica resultado="A">` con integrante `estado="AEA"`, `nivel_riesgo="1"` y observacion de aceptacion.
- Evidencia de circuito exitoso real relacionada: solicitud `4677118`, `soli_moti = 48`, destino `1005326`, estado `F`, con integrante trasladado desde `968807/02`.

## Redmine 29362 - analisis casos 9, 10 y 11 repruebas 1

- Usuario: Sebastian. Contexto: revision de casos de prueba para traslado automatico a contrato existente `soli_moti = 48` en `InstallQA_89`, focalizado en `CASO 9` seccion `PARA REVISAR -> REPRUEBAS 1`, `CASO 10` seccion `REVISAR -> REPRUEBAS 1` y `CASO 11`.
- Caso 9: requiere fix, no solo explicacion. El rechazo `8079` aparece porque `gmsdef_val_datos_ident_inte` bloquea cualquier `paren_destino = 0` al encontrar `PARENTESCOS.bb_gestante = S`, sin considerar si el afiliado origen ya era BB Gestante. Evidencia: origen `prepaga=1`, `contra=1004685`, `inte=03` tiene parentesco vigente `0 / BB Gestante`. Criterio funcional comunicado: si el traslado conserva el parentesco del origen, no se esta convirtiendo a BB Gestante; la automatica deberia contemplar `anter_prepa`, `anter_contra`, `anter_inte` y permitirlo cuando el origen ya era BB Gestante o BB nacido, segun la ventana/regla funcional aplicable.
- Caso 10: requiere fix. La reprueba vigente `id_proceso=164` ya no reproduce el problema inicial de reutilizacion de `inte=02`: en `GMSDEF_INTEGRANTES` quedaron correctamente `03` para `TI 1046726035` y `04` para `RC 1236888201`. El problema actual es contactos: el SP que completa contactos calcula el proximo integrante usando solo afiliados activos del contrato destino, por eso genera contactos para `02/03`; la generacion de integrantes usa maximo historico y crea realmente `03/04`. Resultado: el integrante real `04` queda sin domicilio, telefono y email, disparando errores `29301`, `29302`, `29303`.
- Caso 11: por ahora parece explicacion/revalidacion, no fix directo. Para `id_proceso=164`, aunque `SOLI_AUTO_ANEXOS` muestra el anexo con `inte=02`, en GMS ya quedo mapeado correctamente a `inte=04`: `GMSDEF_ADICIONALES cab_id=295826`, `inte=04`, `plan_codi_adi=OROB`, `tari_adi=BGFORBB`, `obliga=S`. Ese `inte=04` corresponde al integrante con `paren=2`; el integrante con `paren=H` no recibio el anexo. El caso queda bloqueado antes por los errores de contacto del Caso 10; revalidar Caso 11 despues del fix de contactos.
- Conclusion comunicada: arreglar Caso 9 y Caso 10. Caso 11 explicarlo y revalidarlo luego de corregir contactos.

## 2026-08-04 21:00 ART - Redmine 29362 cierre casos 9 y 10

- Usuario: Sebastian. Se descarto Caso 11 por el momento y se pidio arreglar Caso 9 y Caso 10 del Redmine `29362`, traslado automatico a contrato existente `soli_moti = 48`, y commitear.
- Repositorio/working copy: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Commit realizado en SVN: `r37509`, mensaje `29362: ajustar casos 9 y 10 traslado existente`.
- Caso 9 corregido en `backend/procedures/gmsdef_val_datos_ident_inte.sql`: se permite BB Gestante en traslado motivo `48` cuando el integrante origen vigente ya era BB Gestante o BB Nacido, usando `anter_prepa`, `anter_contra`, `anter_inte` como contexto del origen.
- Caso 10 corregido en `backend/procedures/afu_soli_auto_app_contactos_gmstxt.sql`: el mapeo de contactos se alinea con la numeracion real del integrante destino, tomando el maximo historico del contrato destino y no solo integrantes activos.
- Validaciones comunicadas: Caso 9 queda `permite_motivo_48 = S` y `rechaza_8079_con_fix = N`; Caso 10 pasa de mapeo `02/03` a `03/04`, alineado con `GMSDEF_INTEGRANTES`; parseo SQL OK contra `InstallQA_89`; sin nuevos `;` ni `RTRIM`; encoding/EOL preservado sin BOM y CRLF consistente; `svn status` limpio para los archivos commiteados.

## 2026-08-04 21:00 ART - Redmine 29363 contexto y analisis casos 13, 16 y 17

- Usuario: Sebastian. Se pidio ponerse en contexto del Redmine `29363` y luego analizar 3 casos para definir si son explicaciones o arreglos.
- Redmine `29363`: circuito de traslado automatico a contrato nuevo, `soli_moti = 47`. Flujo esperado: XML APP -> `SOLI_AUTO_*` -> GMS/GMSDEF -> `SOLICITUDES` -> impacto alta total -> creacion contrato destino -> retiro/baja del integrante en origen -> AUFACT con reversa/refacturacion si corresponde.
- Puntos tecnicos cargados: no se debe trasladar titular/titular del contrato origen; el trasladado puede quedar titular del contrato destino; cada integrante debe traer `anter_prepa`, `anter_contra`, `anter_inte`; `fecha_radicacion` no debe venir desde XML para traslados, se toma fecha de proceso, cubierto por commits previos `r37442` y `r37444`; existen implementaciones locales para `SOLI_AUTO_RESP_PAGADOR`, `SOLI_AUTO_EMPLEADO`, contactos `tipo_reg C/R/E/I`, parser `afi_insertar_soli_auto_traslado_nuevo`, generacion GMS para `47` y ajustes en `gms_genera_solicitudes`.
- Documentos base revisados: `Redmines/29363/29363.md`, `Redmines/29363/REDMINE_29363_TRASLADO_CONTRATO_NUEVO_HANDOFF.md`, `Redmines/29363/REDMINE_29363_DOCUMENTACION_TECNICA.md`. Prueba historica recomendada: `Redmines/29363/ejecucion_real_soli_auto_traslado_nuevo_contrato_808859.sql`.
- Caso 13: mixto. Error `8066` telefono solapa integrante obligatorio es explicacion/correccion de XML, porque el XML trae contacto contrato `tipo_reg=C` pero no telefono integrante `tipo_reg=I inte=01`; GMS lo exige salvo decision funcional de completar desde RU/origen. Errores `8086`/`8142` por edad requieren fix: en `InstallQA_89`, `id_proceso=156`, origen `244512/02`, destino `OROF`, ambos `plan_grupo=ORO`, edad 65; si plan padre origen y destino son iguales no deberia validar tope de edad, pero la automatica lo trata como alta total pura y dispara edad.
- Caso 16: requiere fix. Evidencia: `SOLI_AUTO_SARLAFT` 1, `SOLI_AUTO_SARLAFT_AUTORIZA_CONTACTO` 6, `SOLI_SARLAFT` 1, `SOLI_SARLAFT_AUTORIZA_CONTACTO` 6, pero `AFI_SARLAFT` 0 y `AFI_SARLAFT_AUTORIZA_CONTACTO` 0 para contrato `1073918`. La solicitud `4677130` queda con SARLAFT en `SOLI_*`, pero no impacta en `AFI_*`; probable problema de orden/impacto en `gms_genera_solicitudes`.
- Caso 17: requiere fix. La ventana manual al elegir `vto_grupo` calcula/actualiza el `emi_grupo` asociado. En automatico hoy se acepta lo que venga en XML. Ejemplo `id_proceso=169`: quedo `vto_grupo=2` y `emi_grupo=1`, cuando en base `vto_grupo=2` corresponde a `emi_grupo=2`. Criterio recomendado: para `soli_moti=47`, XML informa `vto_grupo` y el sistema resuelve `emi_grupo` desde `VTO_GRUPOS/EMI_GRUPOS`; preferencia tecnica comunicada: pisar/calcular para replicar la ventana, salvo que se decida validacion bloqueante.
