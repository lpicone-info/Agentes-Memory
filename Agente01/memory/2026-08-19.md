# Memoria durable - 2026-08-19

## Redmine 29363 - Afilmed
- Usuario interlocutor identificado en contexto como Sebastian.
- Ticket Redmine `29363`: COOMEVA / Afilmed, asunto "Implementar proceso de grabacion automatica traslado a contrato nuevo".
- Caso 2 analizado: XML con `auditoria_medica resultado="R"` y `soli_recha="3"` debia generar solicitud rechazada (`SOLICITUDES.estado = R`) y seguimiento de rechazo, sin impactar tablas `AFI_XXX`; se clasifico como arreglo, no explicacion.
- Repositorio SVN confirmado por el usuario para intervenir: `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Se hizo `svn update` antes de modificar la working copy, quedando en revision previa `37589`.
- Arreglo comiteado en SVN revision `r37590`, mensaje: `Refs #29363 ajusta rechazo por auditoria medica APP`.
- Archivos modificados en el commit: `backend/procedures/afu_genera_soli_auto_app.sql` y `Redmines/29363/caso_2_rechazo_auditoria_medica.sql`.
- Cambio funcional: `afu_genera_soli_auto_app.sql` ahora toma `resultado_auditoria` y `soli_recha` desde `SOLI_AUTO_CONTRATO`; si `resultado_auditoria = 'R'`, genera la GMS con estado `R` y valida que exista motivo de rechazo.
- Ejemplo de comprobacion creado: `Redmines/29363/caso_2_rechazo_auditoria_medica.sql`, usando el XML del caso 2 y `ROLLBACK TRANSACTION`.
- Validaciones reportadas: diff revisado, encoding/BOM/EOL ASCII sin BOM y CRLF, regla SQL sin `;` ni `RTRIM`, post-commit `r37590` contiene solo los dos archivos esperados.
- No se ejecuto el ejemplo contra base porque no se indico profile y no habia cliente SQL disponible en el entorno.

## Redmine 29363 - continuacion caso 2
- Se genero script persistente sin rollback para comprobar por pantalla: `Redmines/29363/caso_2_rechazo_auditoria_medica_sin_rollback.sql`, con XML directo en `@var_data_xml` y no calculado.
- Script sin rollback comiteado en SVN revision `r37591`, mensaje: `Refs #29363 agrega caso 2 sin rollback`.
- Al probar, el usuario recibio error por `id_soli_externa=900106619` ya existente; se indico cambiar tanto variable como XML a otro numero libre, por ejemplo `6620`, validando previamente en `SOLI_AUTO_CONTRATO`, `GMS_CAB` y `SOLICITUDES`.
- Nueva prueba del usuario produjo errores `sector_ori NULL` en `SOLI_SEGUIMIENTO` y `contra NULL` en `SOLI_AUTO_CONTRATO`.
- Diagnostico: el script usaba `@var_usuari = 'srmontenegro'` sin sector en `ZZ_USUARI`; y en rechazo por auditoria medica no se genera contrato definitivo, pero `gms_genera_solicitudes` intentaba actualizar `SOLI_AUTO_CONTRATO.contra = @contra` con NULL.
- Correccion comiteada en SVN revision `r37592`, mensaje: `Refs #29363 ajusta caso 2 rechazado APP`.
- Archivos del commit `r37592`: `backend/procedures/gms_genera_solicitudes.sql` y `Redmines/29363/caso_2_rechazo_auditoria_medica_sin_rollback.sql`.
- Para validar `r37592`: compilar `gms_genera_solicitudes.sql`, ejecutar el script sin rollback actualizado, que usa `id_soli_externa/nro_formulario = 900106620`, y buscar en Afilmed por `nro_formulario = 900106620`.
- Resultado esperado confirmado por el usuario: salio bien.

## Redmine 29364 - cambio titularidad APP
- Se retomo el ticket `29364` sobre cambio de titularidad automatico APP en la misma rama SVN `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Contexto conocido: `soli_moti = 7`, transaccion GMS `CT`, carpeta `Redmines/29364`, commits previos `r37246` a `r37249`, objetos clave `afi_insertar_soli_auto_cambio_titularidad`, `afu_genera_soli_auto_app`, `gms_genera_solicitudes`, `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_PARENTESCOS`.
- Usuario pidio revisar solicitud `4677191` en ventana: en Datos del cambio no aparecia Motivo; deberia mostrar `Nuevo Parentesco`. Ademas el check `Baja al ex Titular` aparecia marcado aunque el XML tenia `baja_titular="N"`.
- Se hizo `svn update` en la rama antes de modificar; working copy estaba actualizada a `r37592`.
- Analisis: problema de pantalla en `exe_afilmed/src/wp_cambio_tit.srw`; para solicitudes APP ya generadas la ventana reconstruia Datos del cambio desde la solicitud impactada y no desde la traza original `SOLI_AUTO_CONTRATO`.
- Para solicitud `4677191` se confirmo que los datos origen estaban bien en QA: `baja_titular = N`, `paren_titular_anterior = C`; `baja_moti = 1` en QA figura como `SITUACION ECONOMICA`.
- Se ajusto `wp_cambio_tit.srw` para que, cuando sea cambio de titularidad APP sin baja del ex titular, muestre visualmente `Nuevo Parentesco`, deje desmarcado `Baja al ex Titular`, y cargue el nuevo parentesco desde `paren_titular_anterior`.
- Archivo modificado aun NO comiteado al momento de esta memoria: `exe_afilmed/src/wp_cambio_tit.srw` alrededor de linea 284.
- Validaciones hechas sobre el cambio 29364: solo quedo modificado `exe_afilmed/src/wp_cambio_tit.srw`, sin conflictos, sin `RTRIM` ni `;` nuevos, encoding preservado UTF-8 con BOM y CRLF puro.
- Instruccion de prueba dada al usuario: recompilar la PBL/objeto `wp_cambio_tit` y abrir solicitud `4677191`; en Datos del cambio deberia verse motivo `Nuevo Parentesco` y check `Baja al ex Titular` desmarcado.
