# Memoria 2026-07-22

## Contexto de trabajo con Savina
- Conversacion por Google Chat directa. El humano identificado en contexto previo como Savina.
- Savina trabaja sobre pruebas de solicitudes automaticas XML en rama SVN `svarela-Version104-Afilmed-SolicitudesAutomaticas`, revision `37338`, usando BD/profile `InstallQA_89`.

## Prueba XML traslado a contrato nuevo
- Caso solicitado: traslado automatico a contrato nuevo (`soli_moti=47`) para probar que el sistema complete datos desde Registro Unico cuando el XML omite domicilio, telefono y email del integrante a trasladar y del responsable pagador.
- XML generado previamente con `id_soli_externa=293630401`, cuenta destino `2`, subcuenta `FSMP`, plan/tarifa `OPLF`/`FPLUNQ`, contrato origen `1-1000142-02` como integrante titular destino, y responsable pagador distinto `CC 19443106`.
- En el XML se dejaron solo contactos obligatorios de facturacion de contrato (`tipo_reg="C"` domicilio/email), sin telefonos, sin anexos y sin contactos `R` ni datos de contacto del integrante.
- Validacion previa del XML: `TRY_CAST(... AS XML) = OK`; `id_soli_externa=293630401` estaba libre antes de procesar.

## Resultado proceso id_proceso 133
- Para `id_proceso=133` se grabo `SOLI_AUTO_CONTRATO` con `id_soli_externa=293630401`, `soli_moti=47`, `prepaga=1`, `contra=1`, `proce_fecha=2026-07-22 09:53:18`, `soli=NULL`.
- Se grabo `SOLI_AUTO_INTEGRANTES`: 1 integrante `CC 66711108`, origen `1-1000142-02`, destino `inte=01`, `paren_destino=T`, `paren_real=H`.
- Se grabo `SOLI_AUTO_RESP_PAGADOR`: 1 responsable `CC 19443106`, Jorge Hernan Gomez Escobar.
- Se grabo `SOLI_AUTO_DOMICILIOS`: 1 domicilio `tipo_reg=C` de facturacion de contrato.
- Se grabo `SOLI_AUTO_EMAILS`: 1 email `tipo_reg=C` de facturacion de contrato.
- Se grabo `SOLI_AUTO_TELEFONOS`: 0; `SOLI_AUTO_ANEXOS`: 0.
- En GMS: `GMS_CAB id=295778`, `estado=E`, `canti_errores=7`, `puede_procesarse=N`.
- Errores `GMS_ERR`: `8149` mail responsable pagador solapa contrato obligatorio; `8151` telefono responsable pagador solapa contrato obligatorio; `8122` domicilio responsable pagador obligatorio; `8133` email responsable pagador obligatorio; `29301` domicilio integrante debe venir en XML o RU vigente; `29302` telefono integrante debe venir en XML o RU vigente; `29303` email integrante debe venir en XML o RU vigente.
- Se verifico que integrante `CC 66711108` y responsable `CC 19443106` tienen documento activo y domicilio/telefono/email vigentes en Registro Unico. Observacion tecnica: los datos de RU no se copiaron al armado GMS; en `GMSTXT/GMSDEF` quedaron solo domicilio/email de facturacion contrato.

## Alta masiva venta nueva / datos requeridos en XML
- Para alta masiva de venta nueva, con validacion vigente, conviene incluir en XML datos de facturacion y forma de pago: `emi_grupo`, `emi_modo`, `vto_grupo`, `pago_forma`, `titular`, `banco`, `banco_suc`, `tipo_cta`, `cta`, `tarje`, `venci_fecha` cuando aplique.
- Domicilio de facturacion de contrato debe venir como contacto `tipo_reg="C"` con `tipo_via`, `calle`, `nro`, `pais`, `loca`, `provin`, `partido`, `barrio`, `postal_codi`, `complemento` opcional.
- Email de facturacion de contrato debe venir como contacto `tipo_reg="C"` con `email_tipo`, `email`, `envia_factura`.
- Para pasar validaciones actuales tambien deben incluirse contactos del responsable pagador: domicilio/email/telefono `tipo_reg="R"`.

## Traslado a contrato nuevo colectivo: manual vs automatico
- Duda funcional de Savina: en altas de contratos colectivos de venta nueva, los datos de facturacion no son del contrato sino de cuenta/subcuenta; verificar si eso esta contemplado en traslados automaticos a contrato nuevo.
- Lectura tecnica: en traslado automatico a contrato nuevo (`soli_moti=47`) esta contemplado parcialmente. Toma `cuenta_dest` y `subcta_dest` del XML y arma `GMSTXT_CLASE_PLAN`; como contrato destino no existe, `contra_gms=NULL`.
- Para facturacion/forma de pago en automatico C.N., el flujo lee esos datos desde XML e inserta en `GMSTXT_MULTI`: `emi_modo`, `emi_grupo`, `vto_grupo`, `pago_forma`, `cta`, `tarje`, `titular`, `banco`, `banco_suc`, `tipo_cta`. No se vio que derive completamente desde `SUBCTA_CONTRATO` para `soli_moti=47`.
- En traslado a C.N manual por ventana, logica ubicada en `wp_cambio_clase.srw`. Para colectivos, la ventana detecta cuenta colectiva con `nvo_cuenta.of_get_colectivo()`, oculta corte/vto_grupo y no lo toma manualmente.
- Camino manual: `nvo_subcta_contrato.of_inicializar(cuenta, subcta, fecha_aplicacion)` carga DW `d_nvo_subcta_contrato`, que ejecuta `cls_subcta_contrato`.
- Tablas manuales principales para facturacion de cuenta/subcuenta colectiva: `SUBCTA_CONTRATO` (`vto_grupo`, `emi_modo`, `emi_grupo`, `factu`, `factu_def`, `creden_envio`, `subcta_tipo`, `partido_comercializable`), `SUBCUENTAS`, `SUBCTA_TIPOS`, `SUBCTA_TIPOS_USOS`, `ENVIO_CREDEN`, `VTO_GRUPOS`.
- Conclusion comunicada: en manual colectivo, facturacion sale de cuenta/subcuenta, principalmente `SUBCTA_CONTRATO` y tablas asociadas; en automatico C.N. habria que ajustar si el requerimiento es que tome esos datos desde cuenta/subcuenta.
