- User preference/rule: use a generic workspace-level `REGLAS.md` file at `/home/luispicone/.openclaw/workspace/REGLAS.md` as a pre-work rules source; do not keep this file inside individual repositories.
- Recorded rule #1 in `REGLAS.md`: when searching repositories for combined phrases (e.g. `CREATE PROCEDURE`), always account for variants with multiple spaces/tabs/other equivalent separators rather than assuming a single space.
- Recorded rule #2 in `REGLAS.md`: for each requirement worked, create a `.md` file inside the repo's `Requerimientos/` folder using the requirement number provided by Luis.
- For repo `pb2022-coomeva-afilmed`, requirement tracking file created: `/home/luispicone/.openclaw/workspace/pb2022-coomeva-afilmed/Requerimientos/29361.md` for requirement 29361, tied to document `A-04-DEF SPs de Consultas y Validaciones por Contrato_V03.pdf`; it should capture all analysis, decisions, changes, validations, and traceability for that requirement.
- Repository/session fact: cloned GitHub repo `git@github.com:lpicone-info/pb2022-coomeva-afilmed.git` into `/home/luispicone/.openclaw/workspace/pb2022-coomeva-afilmed` using a newly generated SSH key (`~/.ssh/id_ed25519` / `.pub`) after user added the public key to GitHub.
- UI/session fix performed earlier: changed OpenClaw session display metadata so `agent:main:main` shows as `main` instead of `heartbeat` by editing `/home/luispicone/.openclaw/agents/main/sessions/sessions.json` origin label/from/to.
- OpenClaw config lesson from user interaction: in `/home/luispicone/.openclaw/openclaw.json`, `tools.elevated.allowFrom.<provider>` expects an array, not a boolean. Corrected `tools.elevated.allowFrom.webchat` to `["*"]` after an invalid boolean caused `openclaw gateway restart` to fail.
