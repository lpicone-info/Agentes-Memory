
## 2026-08-18 - Redmine 29364 / cambio de titularidad

- Usuario en Google Chat directo: Sebastian. Prefiere trato directo y tecnico; me llama Max/Maxxuel.
- Contexto recordado: Redmine 29364, caso 4, corregido con commit SVN `r37571` (`29364: conserva motivo en cambio parentesco titular`). El cambio fue en `backend/procedures/gms_genera_solicitudes.sql`; para cambio de titularidad con `baja_titular = N`, conserva `baja_moti` en la fila del titular anterior de `SOLI_AFILIADOS`, permitiendo que la ventana reconstruya bien "Datos del cambio".
- Caso viejo `id_proceso = 197` en `InstallQA_89`: genero solicitud `4677179` antes del commit `r37571`, por eso quedo contaminado con `SOLI_AFILIADOS.baja_moti = NULL`. No sirve como evidencia del fix. Estado observado: `SOLI_AUTO_CONTRATO.soli = 4677179`, `baja_titular = N`, `baja_moti = 1`, `paren_titular_anterior = C`; `GMS_CAB` sin errores y `puede_procesarse = S`; `SOLICITUDES.estado = F`, `proce_fecha` vacia.
- Se dejo ejemplo SQL en carpeta compartida A: `/mnt/Agentes/srmontenegro/redmine_29364_ejemplo_limpio_datos_del_cambio.sql`. Primer ejemplo uso contrato `964835`, `id_soli_externa = 293649206`, pero al ejecutarlo genero `id_proceso = 199` y fue rechazado por fecha de aplicacion fuera de retroactividad: `GMS_CAB = 295928`, estado `E`, `puede_procesarse = N`, error `5024 - La Fecha de Aplicacion no debe ser superior o inferior...`. No genero solicitud Afilmed.
- Se dejo version corregida sin ejecutar/compilar en `/mnt/Agentes/srmontenegro/redmine_29364_ejemplo_limpio_datos_del_cambio_v2.sql`: `id_soli_externa = 293649207`, `fecha_aplicacion = 2026-08-01`, contrato `964835`, escenario cambio titularidad sin baja del ex titular: integrante `01` de titular a conyuge, integrante `02` de conyuge a titular, `baja_titular = N`, `baja_moti = 1`. Archivo validado ASCII, sin BOM, LF, sin `;` y sin `RTRIM`; no se ejecuto por pedido del usuario.
- Antes del batch, el caso solo se ve como solicitud APP pendiente en `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_PARENTESCOS` y trazabilidad `afs_soli_auto_app_trazabilidad @prm_id_soli_externa = 293649207, @prm_debug = 'S'`; no aparece en `Solicitudes -> Seguimiento de solicitudes` porque aun no existe registro en `SOLICITUDES`.
- Luego el usuario ejecuto batch y creo `id_proceso = 200` en `InstallQA_89`: `id_soli_externa = 293649207`, solicitud generada `4677187`, `GMS_CAB = 295929`, estado `B`, `puede_procesarse = S`, `canti_errores = 0`.
- Verificacion de `id_proceso = 200`: `SOLI_AFILIADOS` quedo correcto para "Datos del cambio": inte `01` con `paren = C`, `cambia_paren = C`, `baja_moti = 1`; inte `02` con `paren = T`; inte `03` con `paren = H`. La ventana debe mostrar baja al ex titular desmarcada, motivo `1`, nuevo parentesco `Conyuge`.
- En la UI PowerBuilder, "Datos del cambio" no es boton; es un recuadro dentro de la ventana de Cambio de Titularidad. Se llega desde `Solicitudes -> Seguimiento de solicitudes`, abrir la solicitud generada y entrar a la pantalla de Cambio de Titularidad. El boton `Parentescos` abre detalle, no ese bloque.
- Hallazgo adicional: tras impacto, `AFILIADOS.paren` puede seguir con valores anteriores (`01 = T`, `02 = C`, `03 = H`); el cambio se refleja en `AFI_HISTO_PAREN` con vigencia desde `2026-08-01` (`01 = C`, `02 = T`, `03 = H`). Codigo revisado: `afu_impacta_cambio_titutar` llama a `afi_impacta_soli_parentescos`, que actualiza/inserta en `AFI_HISTO_PAREN`; no actualiza `AFILIADOS.paren`. Si negocio espera actualizar `AFILIADOS.paren`, es otra correccion fuera del fix `r37571`.

## 2026-08-18 15:16 ART - Google Chat directo con Savina

- Savina trabaja sobre `version104`, BD `InstallQA_89`, repositorio SVN local `svarela-Version104-Afilmed-SolicitudesAutomaticas` en `/home/luispicone/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas`.
- Tema analizado: solicitudes automaticas, `soli_moti = 47`, tag XML `<facturacion>` para cuentas individuales y colectivas.
- Hallazgo funcional/codigo: tipo de cuenta destino se determina por `CUENTAS -> CUENTA_TIPOS -> CUENTA_TIPOS_USO.es_colectivo`.
- Para cuenta individual/no colectiva: debe venir `responsable_pagador`; exige datos directos de facturacion (`vto_grupo`, `emi_modo`), domicilio `tipo_reg="C"` y email `tipo_reg="C"`.
- Para cuenta colectiva: debe venir `empleado`; el codigo toma `vto_grupo`, `emi_modo` y `emi_grupo` desde configuracion vigente de la subcuenta destino.
- `responsable_pagador` y `empleado` son excluyentes; no pueden venir juntos.
- Tag comun obligatorio para ambos tipos: `<facturacion ... />`; evidencia principal en `backend/procedures/afs_soli_auto_obligatorios.sql` cerca linea 522 y `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql` cerca linea 115.
- Validacion por `pago_forma`: `afi_insertar_soli_auto_traslado_nuevo` lee/defaulta XML y luego la validacion fuerte esta en `gmsdef_val_pago_formas`.
- Si no viene `pago_forma`, default `E`.
- Para cuenta colectiva, `afi_insertar_soli_auto_traslado_nuevo` fuerza `pago_forma = 'E'` y limpia `banco`, `banco_suc`, `tipo_cta`, `cta`, `tarje`, `titular`, `venci_fecha`.
- Para individual/no colectiva, los datos del XML pasan a GMS y se validan despues.
- Matriz de obligatoriedad: `pago_forma = E` no exige banco/sucursal/tipo cuenta/cuenta/tarjeta/titular; `pago_forma = T` exige `tarje`, `cta` numerica y `titular`; si tarjeta es debito, `tipo_cta` debe ser `CA` o `CC`; `venci_fecha` no es obligatoria pero si viene debe estar entre `1950-01-01` y `2050-12-31`; `pago_forma = N` o formas con `PAGO_FORMAS.tipo_cobro = 'B'` exige `banco`, `banco_suc`, `tipo_cta` en `CA`/`CC` y `cta` numerica.
- Evidencia adicional: `backend/procedures/afi_insertar_soli_auto_traslado_nuevo.sql` cerca lineas 141 y 156; `backend/procedures/gmsdef_val_pago_formas.sql` cerca linea 700.

## 2026-08-18 15:45 ART - Google Chat directo con Gabriel

- Usuario: Gabriel, trato directo tecnico.
- Tema: Redmine 30234 en SVN `gpulka-Coomeva-trunk` (`/home/luispicone/.openclaw/workspace/svn/gpulka-Coomeva-trunk`). Pedido original: script de datos y validacion para no permitir modificar `mega_cuenta` de una cuenta ya guardada en `wp_cuentas`.
- Se hizo `svn update` previo y se modifico `exe_afilmed/src/wp_cuentas.srw`.
- Commit SVN `r37581`: `Redmine 30234 - Bloquea cambio de megacuenta guardada`. Agrego validacion en `itemchanged` para columna `mega_cuenta` cuando la fila no es nueva, y en `buttonclicked` para boton `cb_mega_cuenta`, con mensaje `No se permite modificar la megacuenta de una cuenta ya guardada`.
- Gabriel reporto que en `itemchanged` salia el error pero dejaba cambiar igual. Se corrigio moviendo la validacion antes del `accepttext()` y agregando `SetText(...)` para restaurar visualmente el valor anterior.
- Commit SVN `r37582`: `Redmine 30234 - Corrige rechazo de cambio de megacuenta`.
- Working copy quedo limpia luego de commits, solo externos SVN `X`. Archivo `wp_cuentas.srw` preservado como UTF-8 con BOM y CRLF.
- Script para Redmine 30234 dejado en `/mnt/Agentes/gpulka/R30234_corrige_megacuenta_afi_clase_rollback.sql`. Gabriel pidio simplificarlo a un par de `UPDATE`, sin variables y sin selects antes/despues. Version final: `UPDATE PREFACT_FILTRO_MEGA_CUENTA` para pasar plantilla `33` de megacuenta `1023` a `85`, y `UPDATE AFI_CLASE` para los 5 contratos puntuales. Quedo con `ROLLBACK TRAN`, ASCII, LF, sin `DECLARE`, sin `SELECT`, sin `RTRIM` y sin `;`.
- Tema posterior: Gabriel pidio buscar un Redmine similar a `30237` donde se hubiera generado script para anular un tipo de comprobante `CA`.
- Hallazgo: Redmine mas similar encontrado fue `27290 - Cuenta corriente / administracion cartera ==> DA reversion duplicada`. El ticket pedia script para baja tecnica de comprobantes duplicados; involucraba comprobantes `CA XSCMP00433936` (`cod_unico_comprobante 139665496`) y `CA XSCMP00433937` (`cod_unico_comprobante 139665497`). Nota final: `Se envia script para dar de baja tecnica el CA duplicado`. Ruta indicada en Redmine: `/CoomevaDB/Entregas Appeon/00_AFILMED/00_PRODUCTIVO/2024.11.01`, archivo `backend14.zip`.
- Otros cercanos pero menos exactos: `27343` baja tecnica por recaudo negativo (`RC/RZ`, no `CA`), y `28125` por aplicacion de recaudo anulado (`RC/RZ`, no baja/anulacion de `CA`). No se encontro el SQL local del `27290` por numero de Redmine, `cod_unico` o `compro_nro`.
