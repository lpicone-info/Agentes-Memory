# 2026-08-27

## Google Chat direct - Gabriel

- User greeted; agent loaded preferences and addressed him as Gabriel.
- Work context: SVN branch/working copy `gpulka version 104`, local path used previously: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Task completed and committed in SVN r37683: added validation in `backend/procedures/afs_soli_auto_obligatorios.sql` so automatic requests require a valid active `prepaga/contra` in `AFI_CLASE` for all `soli_moti <> 47`. `soli_moti = 47` remains exempt because traslado a contrato nuevo generates a new contract. Validated in `InstallQA_89` that process `215` had invalid `contra = 1-1000050`; valid contract was `1000050`. Read-only tests: invalid `1-1000050` blocks for `soli_moti = 22`, valid `1000050` allows, `47` allows by exception. SQL NOEXEC compile passed. Encoding preserved ASCII/CRLF. Did not deploy DROP/CREATE to DB.
- Second task completed and committed in SVN r37684: incorporated cambio de datos de empleado XML handling alongside cambio de responsable pagador. `soli_moti 22` remains responsible payer change with XML node `responsable_pagador`, type `R`, and does not accept employee-only fields such as `codigo_nomina`, `centro_costo`, `sucursal_empresa`, `oficina_empresa`. `soli_moti 49` added for employee data change with XML node `empleado`, type `E`, and employee labor fields required. Added validations matching `wp_cambio_resp_pagador`: `22` must not use cuenta colectiva; `49` requires cuenta colectiva. Corrected GMS/final flow to generate and save employee contacts as type `E`.
- For r37684, investigation found `wp_cambio_resp_pagador` uses `IF(tipo='E',1,0)` in DW fields because the same window supports cambio de responsable pagador and cambio de datos de empleado; some field ultimately persists differently in `afi_resp_pagador` depending on whether it is responsible payer (`R`) or employee (`E`). Validated process `215` defect showed labor fields stored under type `R`. NOEXEC compilation passed for six modified procedures in `InstallQA_89`. CRLF and encoding preserved; no RTRIM or semicolon style churn. Post-commit had no versioned pending changes, only unversioned docx and prior externals.
# 2026-08-27

## Google Chat direct - Gabriel

- User greeted; agent loaded preferences and addressed him as Gabriel.
- Work context: SVN branch/working copy `gpulka version 104`, local path used previously: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Task completed and committed in SVN r37683: added validation in `backend/procedures/afs_soli_auto_obligatorios.sql` so automatic requests require a valid active `prepaga/contra` in `AFI_CLASE` for all `soli_moti <> 47`. `soli_moti = 47` remains exempt because traslado a contrato nuevo generates a new contract. Validated in `InstallQA_89` that process `215` had invalid `contra = 1-1000050`; valid contract was `1000050`. Read-only tests: invalid `1-1000050` blocks for `soli_moti = 22`, valid `1000050` allows, `47` allows by exception. SQL NOEXEC compile passed. Encoding preserved ASCII/CRLF. Did not deploy DROP/CREATE to DB.
- Second task completed and committed in SVN r37684: incorporated cambio de datos de empleado XML handling alongside cambio de responsable pagador. `soli_moti 22` remains responsible payer change with XML node `responsable_pagador`, type `R`, and does not accept employee-only fields such as `codigo_nomina`, `centro_costo`, `sucursal_empresa`, `oficina_empresa`. `soli_moti 49` added for employee data change with XML node `empleado`, type `E`, and employee labor fields required. Added validations matching `wp_cambio_resp_pagador`: `22` must not use cuenta colectiva; `49` requires cuenta colectiva. Corrected GMS/final flow to generate and save employee contacts as type `E`.
- For r37684, investigation found `wp_cambio_resp_pagador` uses `IF(tipo='E',1,0)` in DW fields because the same window supports cambio de responsable pagador and cambio de datos de empleado; some field ultimately persists differently in `afi_resp_pagador` depending on whether it is responsible payer (`R`) or employee (`E`). Validated process `215` defect showed labor fields stored under type `R`. NOEXEC compilation passed for six modified procedures in `InstallQA_89`. CRLF and encoding preserved; no RTRIM or semicolon style churn. Post-commit had no versioned pending changes, only unversioned docx and prior externals.
- Follow-up task completed and committed in SVN r37685: removed/blocked `contrato_fisico` from automatic XML for cambio de responsable pagador / empleado (`soli_moti 22/49`) because `SOLICITUDES.nro_formulario` is already updated with `id_soli_externa`. Process `215` in `InstallQA_89` was confirmed as `soli_moti=22`, `contra=1-1000050`, `contrato_fisico=92200001`, with no generated `soli` yet.
- Files changed in r37685: `backend/procedures/afi_insertar_soli_auto_cambio_resp_pagador.sql` rejects `contrato_fisico` in XML for `22/49` and stops saving it to `SOLI_AUTO_CONTRATO`; `backend/procedures/afs_soli_auto_obligatorios.sql` adds the same early validation. Commit message: `No acepta contrato fisico en cambio resp pagador automatico`.
- For `soli_moti=49` economic flow, reviewed reversion/facturacion and left it unchanged: cuenta colectiva coverage already exists in `backend/procedures/afs_soli_auto_app_fact_pendientes.sql`, where collective accounts set `requiere_reversion='N'` and `requiere_facturacion='N'`. Validated NOEXEC compile for the two changed procedures. Working copy had no versioned pending changes after commit; only previous unversioned `.docx` and SVN externals `X` remained.

- Follow-up task completed and committed in SVN r37689: refined validation errors for cambio de responsable pagador automatico. Replaced generic message `El XML de cambio de responsable pagador no debe informar datos laborales` with field-specific validations/messages for `codigo_nomina`, `centro_costo`, `sucursal_empresa`, `oficina_empresa` in `backend/procedures/afi_insertar_soli_auto_cambio_resp_pagador.sql` and `backend/procedures/afs_soli_auto_obligatorios.sql`. Validated encoding/EOL, absence of new semicolons/RTRIM, and SQL NOEXEC compile in `InstallQA_89`. Working copy clean for versioned files after commit.
- Task completed and committed in SVN r37690: changed `backend/procedures/afu_genera_soli_auto_app.sql` so processing errors no longer use `RAISERROR/RETURN` that stops other automatic requests. New controlled error path records the failure in `GMS_ERR` with `recha_motivo = 5241`, marks `GMS_CAB` as `estado='E'`, `puede_procesarse='N'`, `canti_errores=1`, updates `SOLI_AUTO_CONTRATO.proce_fecha` so the process is not picked again, clears local transaction/cursors, and continues with the next process. Selector now excludes rows with `proce_fecha` already informed. Commit message: `Registra errores controlados en solicitudes automaticas APP`. Validated SQL NOEXEC compile in `InstallQA_89`, confirmed no remaining `RAISERROR` in procedure, no semicolon/RTRIM churn, ASCII without BOM and CRLF preserved.
