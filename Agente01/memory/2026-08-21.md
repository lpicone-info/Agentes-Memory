
## 2026-08-21 12:18 ART - R30238 / Mantis 11904 / Solicitud 4677183

- Usuario: Sebastian. Conversacion Google Chat directa.
- Contexto funcional: En Afilmed > Solicitudes > Inclusiones, solicitud `4677183`, integrante `02`, habia inconsistencia `renuncia_antiguedad = S` + `novedad_sistema = REACTIVACION` con asesor gestion `1761` cuyo `PlanRegresa = 0`; esperado: `VENTA_NUEVA`.
- Analisis: el SP `afu_soli_afiliados` no fue tocado; solo persiste/recalcula segun `novedad_sistema` recibido. El origen esta en PowerBuilder, principalmente `exe_afilmed/src/wp_alta_parcial.srw`, con logica equivalente en `wp_alta_total.srw`.
- Working copy SVN usada: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Commits SVN realizados:
  - `r37624` `Refs #30238 sincroniza Plan Regresa en inclusiones`: sincroniza `ii_plan_regresa` con `ilib_alta.f_busca_asesor(...)` antes de decidir por Plan Regresa en `wp_alta_parcial.srw` y `wp_alta_total.srw`.
  - `r37626` `Refs #30238 fuerza Venta Nueva al renunciar antiguedad`: fuerza `VENTA_NUEVA` cuando renuncia antiguedad = S y asesor real no es Plan Regresa; tambien en validacion previa a grabar.
  - `r37627` `Refs #30238 evita recalculo interactivo al renunciar antiguedad`: cambio en `wp_alta_total.srw` para evitar `POST event itemchanged(...novedad...)` que disparaba validacion ONCA.
  - `r37628` `Refs #30238 evita validacion ONCA al renunciar antiguedad parcial`: cambio equivalente en `wp_alta_parcial.srw` para evitar que validacion ONCA bloquee el cambio.
  - `r37629` `Refs #30238 acepta cambio de novedad al renunciar antiguedad parcial`: en `wp_alta_parcial.srw`, se cambio seteo directo por `This.SetItem(...)` y `RETURN 1` por `RETURN 0` para aceptar el itemchanged.
  - `r37630` `Refs #30238 interpreta valor check renuncia antiguedad parcial`: en `wp_alta_parcial.srw`, se ajusto condicion del checkbox a `IF data = 'S' OR Right(Trim(data), 1) = 'S' THEN` porque el DataWindow podia entregar valor compuesto; esto explicaba que solo cambiara `Mes permanencia` a 10 y `novedad_sistema` siguiera en Reactivacion.
- Ultimo estado conocido: despues de `r37630` aun no se recibio validacion del usuario. Si vuelve a fallar, revisar evento real/columna DataWindow de `renuncia_antiguedad` en `wp_alta_parcial.srw`, orden de eventos al cargar solicitud existente, y si hay otro recalculo posterior que vuelve a dejar `novedad_sistema` en Reactivacion.
- Validaciones reportadas en cada commit: UTF-8 con BOM preservado, CRLF puro, estado local limpio salvo externos SVN `X` preexistentes. SP modificado: ninguno.

## 2026-08-21 13:30 ART - Continuacion R30238 y contexto R29364

- R30238 / Mantis 11904 / Solicitud `4677183`: despues de `r37630`, Sebastian confirmo que el problema seguia hasta hacer correctamente Get Latest/import/regenerate en PowerBuilder. Punto operativo importante: los cambios en `.srw` SVN no impactan la ventana si la PBL no fue actualizada/importada/regenerada.
- Commits adicionales R30238:
  - `r37631` `Refs #30238 normaliza renuncia antiguedad post evento parcial`: en `exe_afilmed/src/wp_alta_parcial.srw`, se agrego `wf_aplica_renuncia_antiguedad` ejecutada con `POST` luego del click/itemchanged del checkbox; relee `renuncia_antiguedad`, toma `PlanRegresa` real del asesor vigente y fuerza `novedad_sistema = VENTA_NUEVA` en grilla visible y buffer compartido cuando no corresponde Plan Regresa.
  - `r37632` `Refs #30238 respeta renuncia al recalcular parcial`: en `wp_alta_parcial.srw`, se detecto que el recalculo de `Mes Permanencia` pisaba `novedad_sistema` y lo dejaba en Reactivacion. Se ajustaron puntos de recalculo por documento y meses permanencia para respetar `renuncia_antiguedad = S` + asesor no Plan Regresa, forzando `VENTA_NUEVA`; tambien se evito reingresar en validacion ONCA.
  - `r37633` `Refs #30238 normaliza renuncia al cargar parcial`: en `wp_alta_parcial.srw`, se agrego normalizacion despues del `retrieve(il_solicitud)` de integrantes para que al abrir una solicitud ya cargada con `renuncia_antiguedad = S` no quede visualmente con meses/novedad viejos hasta hacer click.
- Resultado funcional R30238 confirmado parcialmente por Sebastian: tras Get Latest, al volver a hacer click en `Renuncia Antiguedad`, pasa a `Meses = 0` y `Novedad Sistema = Venta Nueva`. Luego se agrego `r37633` para cubrir la carga inicial. Explicacion dada: `novedad` y `novedad_sistema` son campos distintos; `novedad` es la novedad operativa/seleccionada del beneficiario, `novedad_sistema` es la clasificacion calculada por reglas y era el foco del bug.
- R29364 / Cambio de Titularidad automatico APP: contexto retomado. Contrato testigo `109087`; solicitud previa de referencia `4677191`. Flujo: `soli_moti = 7`, transaccion GMS `CT`, staging `SOLI_AUTO_CONTRATO`, parentescos `SOLI_AUTO_PARENTESCOS`, SP receptor `afi_insertar_soli_auto_cambio_titularidad`, generacion/impacto `afu_genera_soli_auto_app` y `gms_genera_solicitudes`, pantalla `exe_afilmed/src/wp_cambio_tit.srw`.
- Antecedente R29364: `r37593` `Refs #29364 corrige visualizacion datos cambio titularidad APP`, habia reconstruido visualmente datos del cambio para solicitudes APP ya generadas, pero introdujo/permitio mostrar `Nuevo Parentesco` como si fuera `Motivo`.
- Hallazgo R29364 actual: Sebastian noto que en contrato `109087` el motivo mostraba `Nuevo Parentesco`, pero al abrir la ventana vacia esa opcion no existe en el combo. Conclusion: `Nuevo Parentesco` no es motivo real; corresponde al campo de parentesco nuevo, no a `baja_moti`.
- Commit R29364 realizado:
  - `r37634` `Refs #29364 corrige motivo visual cambio titularidad APP`: en `exe_afilmed/src/wp_cambio_tit.srw`, para cambio titularidad APP con `baja_titular = N`, deja `Motivo` vacio y carga el valor real en `Nuevo parentesco`; si `baja_titular = S`, mantiene el motivo de baja real.
- Estado/validaciones reportadas: working copy `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas` limpia salvo externos SVN `X` habituales; UTF-8 BOM y CRLF preservados. Recordatorio a Sebastian: hacer Get Latest/importar `wp_cambio_tit.srw` en la PBL y regenerar el objeto.

## 2026-08-21 13:55 ART - Solicitudes automaticas V104 / traslado a contrato existente

- Conversacion directa Google Chat con Savina.
- Contexto de trabajo identificado por la usuaria: version 104 `instalLqa_89` / `InstallQA_89`, solicitudes automaticas, traslado a contrato existente (`soli_moti=48`).
- Repositorio usado en la sesion: `svarela-Version104-Afilmed-SolicitudesAutomaticas`.
- Documentacion SVN involucrada: `/home/luispicone/.openclaw/workspace/svn/bmora-ProyectoCoomeva-Afilmed-01.Documentacion/A-04-DEF Solicitudes 1 - Cambio en Traslados y Asesores Masivos.docx`.
- Se habia agregado por error un ejemplo XML en `Traslado a Contrato Existente > Salidas > Ejemplos`; la usuaria indico que ese documento NO debe modificarse ni grabar nada nuevo ahi. Se restauro el DOCX a estado previo y SVN ya no lo marcaba como modificado.
- Para responder ejemplos XML, no modificar ese DOCX. Entregar el XML por chat salvo que Savina indique otra ubicacion.
- Hallazgo tecnico comunicado: en Version104 y en `InstallQA_89`, `afi_insertar_soli_auto_traslado_existente` aun lee/usa `fecha_aplicacion`; no se encontro evidencia local de una modificacion posterior que lo quite. Savina indica que `fecha_aplicacion="2026-08-01"` NO debe agregarse al XML y pide revisar contra la ultima version desarrollada.
- Ultimo XML generico propuesto para traslado a contrato existente contenia: raiz `solicitud` con `id_soli_externa`, `soli_moti="48"`, `prepaga`, `contra`, `fecha_aplicacion`, `fecha_inicio_servicio`, `asesor_gestion`; nodos `integrantes/integrante`, `anexos/anexo`, `contactos/domicilio`, `contactos/telefono`, `contactos/email`.
- Correcciones ya identificadas para el ejemplo: usar tag `telefono` sin tilde; atributos en minuscula `pais`, `tel_cod_pais`, `tipo_telefono`; anexos/contactos vinculados preferentemente por `anter_prepa`, `anter_contra`, `anter_inte`; no incluir `auditoria_medica`, `preexistencias`, `sarlaft`, ni `sarlaft_autoriza_contacto` para traslado a contrato existente segun la revision previa.
- Pendiente si se retoma: verificar en fuentes Version104 si existe cambio posterior que elimina `fecha_aplicacion` del XML de traslado existente y ajustar respuesta a la indicacion funcional de Savina.
