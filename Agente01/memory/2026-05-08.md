# Memoria durable - 2026-05-08

## Proyecto SVN / Redmine 29361 - AU solicitudes automáticas APP
- Working copy usada: `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Rama SVN: `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version104-Afilmed-SolicitudesAutomaticas`.
- Usuario SVN requerido/usado para commits: `gpulka`.
- Objetivo funcional acordado: `AU` es solo tipo de agenda/configuración batch; la transacción funcional real debe salir de `GMS_CAB.transaccion` para el circuito GMS. No hardcodear `CC` como transacción funcional global de `AU`.
- Decisión táctica posterior de Luis: por ahora usar `CASE` prolijo en `afu_genera_soli_auto_app` por `soli_moti`, con `RAISERROR` si no hay mapeo. Actualmente mapeo confirmado: `soli_moti = 6 -> CC`.
- Duplicación/re-agendado: Luis indicó que el batch es único punto de ejecución, pero pidió control adicional: el `SELECT TOP 1` de `afu_genera_soli_auto_app` debe excluir solicitudes cuyo `id_soli_externa` ya exista en `GMS_CAB`; también se aplicó criterio al `WHILE`.
- `SOLI_AUTO_CONTRATO`: Luis no quiere columnas nuevas definidas dos veces. Última instrucción aplicada: `id_soli_externa` y `soli_moti` deben estar en el `CREATE TABLE`, sin `ALTER TABLE` para esas columnas. Mantener `ALTER` solo si corresponde para columnas preexistentes como `proce_fecha`.
- Commits realizados durante este tramo:
  - `r36823` — `29361: integrar solicitudes automaticas APP al batch AU`.
  - `r36824` — `29361: evitar duplicar GMS por solicitud externa APP`.
  - `r36825` — normalización intermedia no deseada por Luis.
  - `r36826` — `29361: definir columnas APP en create de SOLI_AUTO_CONTRATO`.
- Revisión importante sobre `wp_main_solicitudes.srw`: Luis preguntó por bloques `IF ls_tipo_proceso = 'BT' THEN SQLCA.AutoCommit = TRUE`. Se explicó que el tratamiento no se eliminó, sino que pasó a depender de `ls_tipo_proceso_real` obtenido desde `GMS_CAB`; esto es necesario para que si agenda `AU` contiene transacción real `BT`, se use el manejo transaccional especial de BT. Revisado como seguro porque `wf_get_tipo_proceso_real` es consulta/fetch antes de activar AutoCommit.
- Estado reportado tras últimos commits: `svn status` limpio salvo externals `X`.
