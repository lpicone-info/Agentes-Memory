# Memoria curada diaria

- Fecha: 2026-07-29
- Usuario: Belkis Josefina Garcia Mora
- id_googlechat: users/101923937101998608405
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/nsoxdyaaaae

## Resumen de la actividad

### Tema 1: Diagnóstico de afiliado origen vigente en traslado a contrato nuevo

- Solicitud o contexto: Belkis consultó, usando el profile `InstallQA_89`, por el error del procedimiento `afi_insertar_soli_auto`: "No existe afiliado origen vigente para uno o mas integrantes del traslado a contrato nuevo".
- Respuesta o acciones del agente: Se revisó la conexión disponible a SQL Server, se consultaron procedimientos relacionados (`afi_insertar_soli_auto`, `afi_insertar_soli_auto_traslado_nuevo`, `afs_soli_auto_obligatorios`) y se explicó que para `soli_moti = 47` la validación se realiza contra `AFILIADOS` usando los datos de origen del XML: `anter_prepa`, `anter_contra`, `anter_inte`, `docu_tipo`, `docu_nro` y vigencia según `baja_fecha`.
- Resultado: Se concluyó que no era un error técnico del SP sino un rechazo funcional por datos: uno o más integrantes no tenían coincidencia vigente en `AFILIADOS`. También se indicó que para traslados el XML no debe informar `fecha_aplicacion`; el procedimiento la calcula desde `fecha_inicio_servicio`.
- Estado: Resuelto

### Tema 2: Estado `ERROR_GENERACION_SOLICITUD` en trazabilidad de solicitud automática

- Solicitud o contexto: Luego de corregir datos y ejecutar el XML, Belkis informó `id_proceso = 146` e indicó que `afs_soli_auto_app_trazabilidad @prm_id_soli_externa = '5012'` devolvía `estado_general = ERROR_GENERACION_SOLICITUD` con errores por fecha/corte, campaña comercial e integrantes consecutivos.
- Respuesta o acciones del agente: Se consultaron datos de `SOLI_AUTO_CONTRATO`, `SOLI_AUTO_INTEGRANTES`, `GMS_CAB`, tablas GMS y procedimientos de validación. Se identificó que `GMS_CAB` quedó con `estado = E`, `puede_procesarse = N` y errores reales `8141`, `8162` y `8221`.
- Resultado: Se explicó que el error de fecha/corte se debía a que en `SOLI_AUTO_CONTRATO` figuraban `emi_grupo = 3` y `vto_grupo = 3`, pero en `GMSTXT_MULTI` y `GMSDEF_MULTI` quedaron `emi_grupo = 2` y `vto_grupo = 2`; que la campaña comercial quedó con valor `0`, tratado como campaña informada pero inexistente; y que los integrantes destino no cumplían la secuencia esperada. Ante la duda sobre si provenía de la primera ejecución fallida, se explicó que ese primer rechazo ocurría antes de insertar `SOLI_AUTO_CONTRATO` y antes de generar GMS, por lo que no debería haber dejado estructuras GMS.
- Estado: Resuelto

### Tema 3: Hipótesis sobre falta de actualización posterior de tablas GMS

- Solicitud o contexto: Belkis preguntó si GMS pudo haber quedado con datos viejos por la primera ejecución fallida o porque una segunda ejecución no actualizó las tablas GMS.
- Respuesta o acciones del agente: Se revisó la cronología operativa visible en base: `SOLI_AUTO_CONTRATO` con `id_proceso = 146`, `fecha_radicacion`, `proce_fecha`, `emi_grupo = 3`, `vto_grupo = 3`, `cod_camp_comercial = 0`; y `GMS_CAB` con `cab_id = 295798` y fecha de generación posterior. También se indicó que no había triggers relevantes sobre `SOLI_AUTO_CONTRATO`, `GMSTXT_MULTI`, `GMSTXT_CLASE_PLAN`, `GMSDEF_MULTI` ni `GMSDEF_CLASE_PLAN`.
- Resultado: La hipótesis más consistente fue que el proceso `146` generó `GMS_CAB = 295798`, y luego el generador no volvió a tomar el mismo `id_soli_externa = 5012` por el filtro `NOT EXISTS` sobre `GMS_CAB`. Quedó explicado que, una vez generado GMS, una corrección posterior sobre la solicitud automática no necesariamente regenera las tablas GMS para el mismo externo.
- Estado: Resuelto
