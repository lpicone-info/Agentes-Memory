# Memoria durable - 2026-07-21

## Caso traslado APP Afilmed / solicitudes automaticas
- Usuario trabajo sobre solicitud automatica APP `soli_moti = 48`, contrato destino `prepaga=1`, `contra=1000431`, fecha aplicacion `2026-07-01T00:00:00`.
- XML de prueba traslado integrantes origen desde contrato `160053`: doc `CC 71656954` `anter_inte=02` y doc `TI 1017928396` `anter_inte=03`.
- Primer diagnostico: `REGISTRO_UNICO_DOMICILIOS` devolvia historicos; consulta correcta usa `ROW_NUMBER() OVER (PARTITION BY codigo_unico_persona ORDER BY vigencia DESC)` y `rn=1`. Para CUP `246923` corresponde vigencia `2024-08-01`; para CUP `306866` corresponde `2021-09-01`.
- Se verifico que el problema no era falta de domicilio en Registro Unico sino desfasaje entre integrantes generados y contactos/validacion.
- Proceso externo `id_soli_externa=1009` correspondia a `id_proceso=124`; no confundir `id_soli_externa` con `SOLI_AUTO_CONTRATO.id_proceso`.
- Para proceso 124, mapeo origen correcto: XML `01 -> anter_inte 02 -> codigo_unico_persona 246923`; XML `02 -> anter_inte 03 -> codigo_unico_persona 306866`.
- Hallazgo con `cab_id=295769`: `GMSDEF_INTEGRANTES` tenia `01,06,07`, pero `GMSDEF_DOMICILIOS` tenia contactos en `04,05`; causa: calculos distintos de `inte_destino` entre generacion de integrantes y contactos.
- Regla funcional acordada: para traslado `soli_moti=48`, si contrato destino tiene N integrantes reales vigentes en `AFILIADOS`, los trasladados deben quedar como `N+1`, `N+2`, etc. Ejemplo: destino con 4 integrantes + traslado origen `02/03` => destino `05/06`.
- Cambio aplicado y commiteado en SVN `r37380`: `backend/procedures/afu_genera_soli_auto_app.sql`. Para `soli_moti=48`, base `@var_int_base_inte` ahora sale solo de `AFILIADOS` vigente del contrato destino a `fecha_aplicacion`; se dejo de contaminar con `GMSTXT_INTEGRANTES`, `GMSDEF_INTEGRANTES`, `SOLI_AFILIADOS`.
- Luego persistia error de contactos para `Inte 01`. Diagnostico: `01` es titular/referencia del contrato destino, no integrante trasladado del XML, pero validacion APP exigia domicilio/telefono/email para todos los `GMSDEF_INTEGRANTES`.
- Cambio aplicado y commiteado en SVN `r37381`: `backend/procedures/gmsdef_val_traslado_web.sql`. Validaciones `29301`, `29302`, `29303` para `soli_moti=48` ahora aplican solo a integrantes con origen informado: `anter_prepa`, `anter_contra`, `anter_inte` no nulos.
- Tras compilar y ejecutar nueva solicitud `id_soli_externa=1011`, errores de contacto `29301/29302/29303` desaparecieron. Quedaron solo errores DEF para `Inte 04`: `5114` estado civil, `8086` edad/programa, `8090` profesion.
- Datos de `GMSDEF_INTEGRANTES` para `Inte 04`: doc `CC 71656954`, nacimiento `1965-02-09`, `civil_esta='C'`, `profesion='0'`, `paren='F'`, `paren_real='F'`, origen `1/160053/02`. Aparecen varias filas por distintas ejecuciones/cab_id (`295765`, `295771`, `295772`), no duplicidad dentro de la misma ejecucion.
- Tabla de profesiones: `PROFESIONES` con campos `profesion char(3)`, `deno`, `alta_fecha`, `modi_fecha`, `baja_fecha`. Registro `profesion='0'` existe como `No definido` pero tiene `baja_fecha=2024-03-06 19:57:33.940`; por eso `8090` es correcto.
- Estado civil se valida contra `CIVIL_ESTADOS`, condicion de vigencia: `baja_fecha IS NULL OR baja_fecha > GMSDEF_INTEGRANTES.apli_fecha`. Consultar `select * from CIVIL_ESTADOS where civil_esta = 'C'`.
- Edad `8086` se calcula con `dbo.afn_edad(TBL.naci_fecha, GMC.apli_fecha)` y se valida contra `PLANES.tope_minimo` / `PLANES.tope_edad`, via `GMSDEF_CLASE_PLAN.plan_codi = PLANES.plan_codi`.
# Memoria durable - 2026-07-21

## Caso traslado APP Afilmed / solicitudes automaticas
- Usuario trabajo sobre solicitud automatica APP `soli_moti = 48`, contrato destino `prepaga=1`, `contra=1000431`, fecha aplicacion `2026-07-01T00:00:00`.
- XML de prueba traslado integrantes origen desde contrato `160053`: doc `CC 71656954` `anter_inte=02` y doc `TI 1017928396` `anter_inte=03`.
- Primer diagnostico: `REGISTRO_UNICO_DOMICILIOS` devolvia historicos; consulta correcta usa `ROW_NUMBER() OVER (PARTITION BY codigo_unico_persona ORDER BY vigencia DESC)` y `rn=1`. Para CUP `246923` corresponde vigencia `2024-08-01`; para CUP `306866` corresponde `2021-09-01`.
- Se verifico que el problema no era falta de domicilio en Registro Unico sino desfasaje entre integrantes generados y contactos/validacion.
- Proceso externo `id_soli_externa=1009` correspondia a `id_proceso=124`; no confundir `id_soli_externa` con `SOLI_AUTO_CONTRATO.id_proceso`.
- Para proceso 124, mapeo origen correcto: XML `01 -> anter_inte 02 -> codigo_unico_persona 246923`; XML `02 -> anter_inte 03 -> codigo_unico_persona 306866`.
- Hallazgo con `cab_id=295769`: `GMSDEF_INTEGRANTES` tenia `01,06,07`, pero `GMSDEF_DOMICILIOS` tenia contactos en `04,05`; causa: calculos distintos de `inte_destino` entre generacion de integrantes y contactos.
- Regla funcional acordada: para traslado `soli_moti=48`, si contrato destino tiene N integrantes reales vigentes en `AFILIADOS`, los trasladados deben quedar como `N+1`, `N+2`, etc. Ejemplo: destino con 4 integrantes + traslado origen `02/03` => destino `05/06`.
- Cambio aplicado y commiteado en SVN `r37380`: `backend/procedures/afu_genera_soli_auto_app.sql`. Para `soli_moti=48`, base `@var_int_base_inte` ahora sale solo de `AFILIADOS` vigente del contrato destino a `fecha_aplicacion`; se dejo de contaminar con `GMSTXT_INTEGRANTES`, `GMSDEF_INTEGRANTES`, `SOLI_AFILIADOS`.
- Luego persistia error de contactos para `Inte 01`. Diagnostico: `01` es titular/referencia del contrato destino, no integrante trasladado del XML, pero validacion APP exigia domicilio/telefono/email para todos los `GMSDEF_INTEGRANTES`.
- Cambio aplicado y commiteado en SVN `r37381`: `backend/procedures/gmsdef_val_traslado_web.sql`. Validaciones `29301`, `29302`, `29303` para `soli_moti=48` ahora aplican solo a integrantes con origen informado: `anter_prepa`, `anter_contra`, `anter_inte` no nulos.
- Tras compilar y ejecutar nueva solicitud `id_soli_externa=1011`, errores de contacto `29301/29302/29303` desaparecieron. Quedaron solo errores DEF para `Inte 04`: `5114` estado civil, `8086` edad/programa, `8090` profesion.
- Datos de `GMSDEF_INTEGRANTES` para `Inte 04`: doc `CC 71656954`, nacimiento `1965-02-09`, `civil_esta='C'`, `profesion='0'`, `paren='F'`, `paren_real='F'`, origen `1/160053/02`. Aparecen varias filas por distintas ejecuciones/cab_id (`295765`, `295771`, `295772`), no duplicidad dentro de la misma ejecucion.
- Tabla de profesiones: `PROFESIONES` con campos `profesion char(3)`, `deno`, `alta_fecha`, `modi_fecha`, `baja_fecha`. Registro `profesion='0'` existe como `No definido` pero tiene `baja_fecha=2024-03-06 19:57:33.940`; por eso `8090` es correcto.
- Estado civil se valida contra `CIVIL_ESTADOS`, condicion de vigencia: `baja_fecha IS NULL OR baja_fecha > GMSDEF_INTEGRANTES.apli_fecha`. Consultar `select * from CIVIL_ESTADOS where civil_esta = 'C'`.
- Edad `8086` se calcula con `dbo.afn_edad(TBL.naci_fecha, GMC.apli_fecha)` y se valida contra `PLANES.tope_minimo` / `PLANES.tope_edad`, via `GMSDEF_CLASE_PLAN.plan_codi = PLANES.plan_codi`.

## Caso anexos OPLB en traslado nuevo contrato Solis Automaticas
- Ambiente/caso investigado: `InstallQA_89`, solicitud `SOLICITUDES.soli=4677103`, `soli_moti=47`, contrato nuevo `contra=1073910`, `estado=F`, `origen_solicitud=APP`, `id_proceso=125`, `id_soli_externa=5001`.
- Usuario reporto que el XML no envio datos en tag `anexos`, pero en boton Anexos de ventana de traslados aparecian datos OPLB; en Consulta General de Socio, pestaña Anexo, no aparecia nada.
- Validacion inicial: para `id_proceso=125` no hay filas en `SOLI_AUTO_ANEXOS`, tampoco en `GMSTXT_ADICIONALES` ni `GMSDEF_ADICIONALES`; la solicitud no tiene `SOLI_ADICIONALES` y el contrato `1073910` no tiene `AFI_ADICIONALES`. Por eso Consulta General no muestra anexos persistidos.
- Datos de plan del caso: `cuenta=2`, `subcta=FSMP`, `plan=OPLF`, fecha `2026-07-01`. El SP `afs_plan_adi_x_paren` puede devolver `OPLB - ANEXO BEBE GESTANTE PLUS` como configurado obligatorio para ciertos parentescos bebe, pero no para titular.
- Correccion importante del usuario: el nuevo contrato tiene titular y parentesco real titular; no corresponde tratar OPLB como obligatorio para todos los parentescos reales.
- Evidencia fina: `SOLI_AUTO_INTEGRANTES` para `id_proceso=125` tiene `paren_destino='T'` y `paren_real_destino='T'`; `SOLI_AFILIADOS` de solicitud `4677103` tiene `paren='T'`, `paren_real='T'`; `AFI_HISTO_PAREN` de contrato `1073910` tiene `paren='T'`, `paren_real='T'`.
- Configuracion `subcta_plan_adi_paren` para `cuenta 2 / FSMP / OPLF / OPLB`: fila `paren='T'` existe con `obliga='N'`; parentescos `0..8` figuran con `obliga='S'`. Ejecutar `afs_plan_adi_x_paren` con `@paren='T'` devuelve 0 filas; con `@paren='7'` devuelve OPLB, pero `7` es Bebe Titular, no Titular.
- Conclusion durable: si la ventana de traslados mostro/marco OPLB como obligatorio para este titular, no corresponde al estado final del caso. El anexo no fue enviado ni impactado; lo mostrado parece recalculo/propuesta en memoria por estado transitorio o residual del datastore de anexos antes de quedar firme `paren_real='T'`, o falta de limpieza/recalculo correcto al cambiar/confirmar parentesco.

## Caso traslado APP a contrato nuevo - proceso 131 y validaciones
- Usuario pidio un caso XML para probar traslado a contrato nuevo (`soli_moti=47`) donde la trasladada tenga mas de un contrato activo, se abra la ventanita manual de seleccion de contrato, responsable pagador exista en Registro Unico, responsable sea distinto al titular, y todos tengan datos de Registro Unico.
- Caso propuesto en `InstallQA_89`: trasladada/titular nuevo `CC 30303058 - MARCELA RIVERA TRINCHERI`. Tiene mas de un contrato activo: `1036462/02`, `1038437/02`, `144062/02`, `92806/02`. En el XML se eligio origen `prepaga=1`, `anter_contra=1036462`, `anter_inte=02`.
- Responsable pagador distinto al titular: `CC 1033810191 - LORENA SANCHEZ FONSECA`, existe en Registro Unico con domicilio/telefono/email. Ambos titulares/integrantes tienen datos RU. `id_soli_externa=293630901` estaba libre y `afs_soli_auto_obligatorios` devolvio 0 en la validacion previa.
- XML original incluia `cod_camp_comercial="134"`; esto genera campania aunque no haya tag separado de campanias. Si no se quiere campania, quitar ese atributo de `<solicitud>`. No es obligatorio: si falta, queda `NULL` y no deberia generar campania.
- Para contactos completos, se armo tag `<contactos>` con registros `tipo_reg="C"` para contrato, `tipo_reg="R"` para responsable pagador, y `tipo_reg="I" inte="01"` para integrante. Esto elimino errores de contacto.
- Proceso `128` / `id_soli_externa=5002` no genero contrato. `SOLI_AUTO_CONTRATO`: `contra=1`, `soli=NULL`, `proce_fecha=2026-07-21 11:57:29.487`. `GMS_CAB.id=295773`, `estado='E'`, `puede_procesarse='N'`, `canti_errores=6`. Errores: `8147` mail integrante obligatorio, `5114` estado civil invalido/baja, `8090` profesion invalida/baja, `29301` falta domicilio contacto traslado APP, `29302` falta telefono contacto traslado APP, `29303` falta email contacto traslado APP.
- Proceso `5003` / `GMS_CAB=295774` luego de agregar contactos quedo solo con error `8090` en `GMS_ERR` (`etapa=2`, `tableid=204`, `rowid=2321535`). Motivo: integrante `CC 30303058`, `inte=01`, Marcela Rivera Trincheri, venia con `profesion='0'`; en `PROFESIONES`, codigo `0` existe pero esta dado de baja (`baja_fecha=2024-03-06 19:57:33.940`). Solucion indicada: usar profesion vigente, por ejemplo `profesion="875"` (`Contaduria - Gestion contable`).
- Usuario pregunto por proceso `1331`; se verifico que no existia en `SOLI_AUTO_CONTRATO` ni en `GMS_CAB`. El proceso correcto reciente era `131` / `id_soli_externa=5004`.
- Proceso `131` genero correctamente: `GMS_CAB=295776`, `estado='B'`, `puede_procesarse='S'`, errores `0`, solicitud `4677109`, contrato nuevo `1073911`, motivo `47`, fecha aplicacion/inicio servicio `2026-06-01`, cuenta/subcuenta destino `2/FSMP`, plan/tarifa destino `OPLF/FPLUNQ`.
- Datos usados en proceso `131`: titular/integrante trasladado `CC 30303058`, `MARCELA RIVERA TRINCHERI`, `inte nuevo=01`, `paren_destino='T'`, `paren_real_destino='T'`, nacimiento `1967-01-02`, sexo `F`, civil `C`, profesion `875`, obra social `129`, antiguedad `2025-02-01`, origen `1/1036462/02`, `codigo_unico_persona=115467`.
- Responsable pagador proceso `131`: `CC 1033810191`, `LORENA SANCHEZ FONSECA`, nacimiento `1998-09-19`, sexo `F`, civil `S`, profesion `875`, ocupacion `1`, cargo `0`, rango salarial `8`, estrato `3`, `codigo_unico_persona=1044664`, vigente desde `2026-06-01`.
- Anexo generado proceso `131`: integrante `01`, `plan_anexo=OPLB`, `tari_anexo=BGFOPLB`, `condicion_plan='N'`, vigente desde `2026-06-01`.
- Contactos generados en solicitud proceso `131`: domicilio `KR 15 BA 10A-3`, barrio `01001`, localidad `05001`; telefono `3218856679`; email `pruebasgisprestador@gmail.com`.
- `habeas_data="N"` en XML debe verse destildado en la ventana. Regla esperada: `S` = tildado, `N` = no tildado. Para proceso `131`, `SOLI_AUTO_CONTRATO.habeas_data='N'`.
- En Consulta General, pestania Campanias mostro campania porque el XML tenia `cod_camp_comercial="134"` en cabecera. Impactos validados: `SOLI_AUTO_CONTRATO.cod_camp_comercial=134`, `SOLI_AFILIADOS.cod_camp_comercial=134`, `AFI_CAMPANIAS.cod_camp_comercial=134` para contrato `1073911`, `inte=01`, vigente desde `2026-06-01`, solicitud origen `4677109`, motivo `47`.
- Hallazgo bug pestania Grupos de Facturacion para proceso `131`: contrato nuevo `1073911` destino `cuenta=2/subcta=FSMP`, `CUENTAS.cuenta_tipo='I'` (individual), pero quedo `SOLI_CLASE.ori=1` y `AFI_CLASE.ori=1`, mostrando origen Cuenta como si fuera colectiva. Esto no viene de Consulta General ni ventana manual; backend lo arrastra del contrato origen `1036462`.
- Codigo origen del bug: en `afu_genera_soli_auto_app.sql`, para `soli_moti=47`, selecciona `@var_ori = AC.ori` desde `AFI_CLASE` del contrato anterior usando `SAI.anter_prepa/anter_contra`. En el caso origen `1036462`, `AFI_CLASE.ori=1`, `cuenta=1`, `subcta=ACEMN`; luego pasa por `GMSTXT_CLASE_PLAN`, `GMSDEF_CLASE_PLAN`, `SOLI_CLASE`, `AFI_CLASE`.
- Conclusion funcional del bug: para traslado a contrato nuevo deberia calcular `ori` segun cuenta destino individual, no copiarlo del contrato origen. Revisar `afu_genera_soli_auto_app.sql`.
- Hallazgo adicional de facturacion: `gms_genera_solicitudes` graba `SOLI_TITULARES` con `@emi_grupo = @vto_grupo` (comentario `R23195`), por eso en proceso `131` termino `SOLI_TITULARES.vto_grupo=2`, `emi_modo=0`, `emi_grupo=2`, aunque en `SOLI_AUTO_CONTRATO` venia `emi_grupo=1`, `emi_modo=1`, `vto_grupo=2`.

## Continuacion proceso 131 - facturacion/origen y visual parentesco real
- Usuario aclaro que el contrato origen del automatico `1036462` tambien es individual y deberia ser origen Afiliado; por lo tanto, si el contrato destino `1073911` tambien es individual, no corresponde que la pestania Grupos de Facturacion muestre origen Cuenta.
- Correccion conceptual: `AFI_CLASE.ori` no es necesariamente el mismo concepto visual de origen `Cuenta/Afiliado` en Grupos de Facturacion; puede referir a origen comercial/tipo `Directo`. No confundir ambos campos al diagnosticar.
- Para proceso `131`, la evidencia fuerte de pisada esta en facturacion: `SOLI_AUTO_CONTRATO` tenia `emi_modo=1`, `emi_grupo=1`, `vto_grupo=2`, pero `SOLI_TITULARES` quedo `emi_modo=0`, `emi_grupo=2`, `vto_grupo=2`.
- Regla explicada al usuario: `SOLI_AUTO_CONTRATO` y `SOLI_TITULARES` no son tablas espejo completas, pero los campos de facturacion (`emi_modo`, `emi_grupo`, `vto_grupo`) deberian quedar coherentes si el XML/automatico trae valores validos y no hay regla funcional explicita para modificarlos.
- Punto de codigo localizado: `backend/procedures/gms_genera_solicitudes.sql` alrededor de la llamada que graba `SOLI_TITULARES` fuerza `@emi_grupo = @vto_grupo` (comentario `R23195`). Esto explica que el automatico termine como si fuera Cuenta cuando la prueba esperaba Afiliado.
- Sobre ventana manual de traslado a contrato nuevo: segun lo visto, no parece ser el origen del problema; la ventana manual recalcula/guarda datos de facturacion segun seleccion de cuenta/subcuenta destino, mientras que el automatico recalcula/pisa en backend.
- Usuario pregunto por visualizacion de `parentesco_real` en cabeceras de ventanas/botones `Sarlaft`, `Imprime certificados`, `Anexos`, `Domicilios` dentro de traslado a contrato nuevo. Dato en base correcto: `paren_real='T'` es el codigo tecnico de titular.
- Criterio indicado: funcionalmente `T` esta bien grabado, pero en UI deberia mostrar descripcion `Titular` si el resto del sistema lo hace asi. En alta individual se encontro conversion explicita tipo `parentesco_real = LookUpDisplay(paren_real)` en `exe_afilmed/src/wp_alta_total.srw` alrededor de linea 6110, y DataWindow con `datacolumn=paren`, `displaycolumn=deno`. Documentar como bug visual si esas ventanas muestran codigo `T` en vez de descripcion `Titular`.

## Continuacion traslados APP - fechas XML, observaciones y batch
- Requerimiento fecha XML: para traslados APP debe venir solo `fecha_inicio_servicio` en el XML; `fecha_aplicacion` se elimina del XML y se deriva internamente.
- Cambio aplicado y commiteado en SVN `r37382`: `backend/procedures/afi_insertar_soli_auto.sql`. Para `soli_moti` `47` y `48`, el SP de entrada toma `fecha_inicio_servicio` del XML y fuerza internamente `fecha_aplicacion` antes de obligatorios y derivacion. Si falta `fecha_inicio_servicio`, corta con error claro.
- Matiz funcional posterior: para traslado a contrato existente (`soli_moti=48`) esta bien que `fecha_aplicacion = fecha_inicio_servicio`. Para traslado a contrato nuevo (`soli_moti=47`), la fecha del XML solo sirve para determinar periodo de aplicacion/proporcionalidad; la fecha real de inicio de servicio la calcula el sistema segun periodo, grupo de vencimiento y proporcionalidad.
- Cambio aplicado y commiteado en SVN `r37383`: `afi_insertar_soli_auto.sql` y `afi_insertar_soli_auto_traslado_nuevo.sql`. Para `soli_moti=47`, `fecha_aplicacion` se normaliza al primer dia del mes de `fecha_inicio_servicio`, y `fecha_inicio_servicio` real se calcula con `VTO_GRUPOS.vto_dia` / `fin_mes`, replicando logica de `f_calcula_fecha_inicio_servicio`. Para `48` se conserva el comportamiento anterior.
- Requerimiento observaciones: en ambos traslados y sus correspondientes retiros, campo observaciones debe decir: `Solicitud generada automaticamente a partir del proceso de Generacion Masiva de Solicitudes de Traslado N° <nro_solicitud_afilmed>`.
- Cambio aplicado y commiteado en SVN `r37385`: `gms_genera_solicitudes.sql` y `afi_genera_solis_aut_traslados.sql`. Se actualizo observacion de solicitud principal APP para traslados `47/48` y de solicitudes automaticas de retiro/cambio generadas desde el traslado. Se uso `CHAR(176)` para el simbolo `°` sin cambiar encoding.
- Pregunta usuario sobre `batchdesolicitudes`: a veces crea `.log` y `.csv`, otras solo `.log`. Diagnostico en `exe_afilmed/src/wp_main_solicitudes.srw`: el `.log` se crea siempre que empieza a procesar un `cab_id` (`<tipo>_<cab_id>_RESUMEN.log`). El `.csv` solo se genera dentro de `wf_genera_solicitudes` si llego a generar solicitudes, existe `destino_arch_correo`, y hay DataObject de exportacion para la transaccion. Si falla validacion, proceso `AU` ya tiene errores, horario/directorio invalido o no hay DataObject, queda solo `.log`. El mail adjunta `.csv` solo si el archivo existe.
