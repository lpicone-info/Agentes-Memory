- Se consolidó memoria operativa persistente de arranque en `/home/luispicone/.openclaw/workspace/BOOTSTRAP.md` con identidad de Maxxuel, método de trabajo, objetivos, datos de Luis, reglas persistentes, repo recordado y lecciones operativas.
- Se creó `/home/luispicone/.openclaw/workspace/REGLAS.md` como fuente persistente de reglas del workspace; incluye reglas sobre búsquedas con separadores variables, archivo `Requerimientos/<nro>.md`, verificación de memoria/rutas antes de afirmar que algo no existe y lectura de perfiles DB al iniciar.
- Se corrigió la UI de sesiones de OpenClaw para que la sesión principal `agent:main:main` muestre `main` en vez de `heartbeat`, editando `/home/luispicone/.openclaw/agents/main/sessions/sessions.json` en `origin.label`, `origin.from` y `origin.to`.
- Se confirmó nuevamente la lección operativa: la raíz del workspace `/home/luispicone/.openclaw/workspace` no debe asumirse como repo correcto; el repo de trabajo recordado es `/home/luispicone/.openclaw/workspace/pb2022-coomeva-afilmed`.
- Se verificó que `pb2022-coomeva-afilmed` tiene remoto `git@github.com:lpicone-info/pb2022-coomeva-afilmed.git`, rama `main`, y se ejecutó `git pull --ff-only`; resultado: `Already up to date`.
- Se confirmó que la instalación de OpenClaw en `/home/luispicone/.npm-global/lib/node_modules/openclaw` no es un repo git, por lo que no corresponde hacer `git pull` ahí.
- Se ratificó la lección de configuración OpenClaw: en `/home/luispicone/.openclaw/openclaw.json`, `tools.elevated.allowFrom.webchat` usa array (`["*"]`) y no boolean.
- Se investigó el problema de aprobaciones repetidas y se confirmó por documentación que para omitir aprobaciones de `exec` se requiere sesión en `elevated full`; finalmente la sesión quedó en estado `Elevated FULL — exec runs on host with auto-approval`.
- Se intentó reiniciar el gateway con `openclaw gateway restart`, pero el comando devolvió exit code 1; no quedó resuelto en esta sesión.
- Se creó `/home/luispicone/.openclaw/workspace/DB_PROFILE.md` para centralizar perfiles de conexión de bases de datos y se agregó a BOOTSTRAP/REGLAS la instrucción de leerlo en cada inicio de gateway o nueva sesión.
- Se registró el profile `InstallQA_89` con estos datos: motor SQL Server, host `172.16.10.124`, puerto `28000`, base `InstallQA_89`, usuario `sa`, password `Imedical77-`.
- Se validó conectividad TCP al SQL Server del profile `InstallQA_89` con respuesta OK sobre `172.16.10.124:28000`.
- Se instaló `pymssql` con `python3 -m pip install --user --break-system-packages pymssql` para poder abrir sesiones reales contra SQL Server desde este entorno.
- Se validó autenticación real a SQL Server del profile `InstallQA_89`; conexión exitosa a servidor `SQLCOOMEVA01` y base `InstallQA_89`.
- Se ejecutó el SP `afs_consulta_integrantes_contrato` para contrato `177796`; primer intento falló porque el parámetro correcto es `@prm_contrato` (no `@as_nu_contrato`).
- Se obtuvo correctamente la salida del SP `afs_consulta_integrantes_contrato @prm_contrato = 177796`, con 3 integrantes: GUIDO PARRA (Titular), BEATRIZ VERGARA (Conyuge) y ENRIQUE PARRA (Fliar a cargo), todos bajo tarifa `AOANCN - AOANCN ORO ASOCIADO NUEVAS COBERTURAS`; GUIDO tiene `otros_contratos = 533914`.
- Se mostró la salida anterior en formato grilla para uso conversacional.
- Ante la pregunta por la tarifa del contrato `1001895`, primero se respondió en base al SP `afs_consulta_integrantes_contrato`, pero Luis marcó correctamente que eso no alcanzaba y que había que armar la consulta con la lógica adecuada para tarifa actual del contrato.
- Se exploró metadata de base (`sys.tables`, `sys.procedures`, `sys.views`) y se inspeccionó `sp_helptext` de `afs_calc_tarifa_completo` y `afs_cons_detallada_contrato` para inferir la lógica correcta de vigencia tarifaria.
- Se identificó en `afs_cons_detallada_contrato` que para contratos se toma `AFI_PLANES` con `MAX(vigen_desde) <= hoy` y luego se cruza con `TARIFAS` y `TARIFAS_VIGENTE` para descripción y vigencia de tarifa.
- Se resolvió correctamente la tarifa actual del contrato `1001895` mediante consulta propia sobre `AFI_PLANES` + `TARIFAS` + `TARIFAS_VIGENTE`.
- Resultado correcto para contrato `1001895`: tarifa actual código `APLNNQ`, descripción `APLNNQ ASOCIADO PLUS`, vigencia del contrato/tarifa en `AFI_PLANES` = `2024-11-01`, última renovación de tarifa en `TARIFAS_VIGENTE` = `2025-01-01`.
- Quedó acordado como criterio operativo que para preguntas de negocio sobre datos o vigencias no alcanza con repetir la salida de un SP cualquiera; hay que reconstruir la lógica de negocio adecuada desde backend/base cuando corresponda.
- Quedó acordado que la memoria de trabajo diaria debe guardarse en archivos por fecha, por ejemplo `memory/2026-03-29.md`, para no contaminar una memoria principal única.
