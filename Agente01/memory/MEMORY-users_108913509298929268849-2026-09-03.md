# Memoria de conversación — Sebastián Rafael Montenegro

## Datos

- **Fecha:** 03/09/2026
- **Usuario:** Sebastián Rafael Montenegro
- **ID Google Chat:** `users/108913509298929268849`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 76
- **Inicio:** 11:23:20
- **Fin:** 18:33:40

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/iro2hyaaaae`

## Archivos revisados

- `b083064e-269f-432f-99a1-8f50b425bd80.jsonl`

---

## Transcripción completa

### [03/09/2026 11:23:20] Sebastián Rafael Montenegro

hola max, ponete en contexto del redmine 30282

---

### [03/09/2026 11:24:41] Agente

Hola Sebastián, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [03/09/2026 11:25:20] Sebastián Rafael Montenegro

ponete en contexto del redmine 30282

---

### [03/09/2026 11:27:07] Agente

Sebastián, contexto recuperado del Redmine `30282`.

Estado vigente:
- Asunto: `Validación técnica y soporte para migración a SQL Server 2022 (compatibilidad 2014) y Windows Server 2025`
- Proyecto: `COOMEVA - Soporte`
- Estado: `En Desarrollo`
- Asignado a: `Gabriel Pulka`
- Versión: `TRUNK`
- Incidente externo: `11920`
- Última actualización: `02/09/2026`
- Adjunto clave: `REDMINE_30282_ANALISIS_MIGRACION_SQLSERVER2022_MSOLEDBSQL_COOMEVA.docx`

Punto central del análisis ya documentado: hay riesgos reales al pasar de SQL Native Client a `MSOLEDBSQL`, principalmente por INIs, hardcodes en Presmed, seteos condicionados a `SNC`, runtime PowerClient 32 bits, linked servers Oracle y Ole Automation en SQL Server.

Tickets relacionados:
- `27629`: Migración PB9 a PB22, cerrado.
- `28065`: Instalación de SQL Native Client, cerrado.

Quedo en contexto. Para análisis técnico nuevo sobre código o base necesito que me indiques explícitamente el repo y/o profile a usar.

---

### [03/09/2026 11:31:27] Sebastián Rafael Montenegro

ok, mira.. mientras no tengamos el servidor definitivo, tenemos que hacer un analisis de todos los problemas y complicaciones que podemos tener en afilmed, presmed, batchdesolicitues y batchdefacturacion
mientras preparan el servidor, 2 semanas mas, tenemos que estimar complicaciones, yo en mi pc personal, instale en una maquina virtual, un windows server 25 con sql server 22 y power builder 22, no podemos llevar la base de datos ahi porque es muy pesada, pero podemos y necesito que simulemos los errores que podemos tener, dentro de esa vm, entendes?

---

### [03/09/2026 11:31:57] Agente

Sí, Sebastián, entiendo.

El objetivo no es probar la migración completa de la base todavía, sino usar tu VM como laboratorio controlado para anticipar fallas de compatibilidad en:

- `Afilmed`
- `Presmed`
- `batchsolicitudes`
- `batchfacturacion`

La estrategia correcta sería simular por capas:

1. **Conectividad PB22 contra SQL Server 2022**
   Probar drivers, `DBMS`, `DBParm`, `MSOLEDBSQL`, 32 bits, cadena de conexión, login y seteos de sesión.

2. **Errores de runtime sin base pesada**
   Armar bases mínimas o mocks con tablas/procedures chicas para forzar los caminos críticos: login, SQLCA, DataWindows, llamadas a SP, fechas, blobs, adjuntos, FTP, COM/OLE, batches.

3. **Componentes externos**
   Validar Chilkat, MSXML, ADODB, Excel automation, filesystem, `sp_OA*`, linked server Oracle simulado o al menos detección de dependencia.

4. **Batches**
   Ejecutarlos en modo acotado o con datos mínimos para detectar fallas de driver, permisos, rutas, DLLs, INIs, configuración y SQL incompatible.

5. **Matriz de riesgos**
   Documentar para Redmine qué problema puede aparecer, en qué módulo, cómo simularlo, impacto, evidencia y acción preventiva.

La VM sirve aunque no tengamos la base completa. No vamos a validar datos productivos; vamos a validar comportamiento técnico, dependencias, runtime, driver y puntos de quiebre previsibles.

Para avanzar ordenado, el próximo paso es que me confirmes el repo exacto sobre el que querés que armemos la matriz y los escenarios de simulación.

---

### [03/09/2026 11:39:33] Sebastián Rafael Montenegro

no hay rama, vamos a usar nuestra carpeta compartida

---

### [03/09/2026 11:39:57] Agente

Perfecto, Sebastián. Carpeta compartida validada: `/mnt/Agentes/srmontenegro`.

Entonces el enfoque queda así: sin rama SVN, sin tocar repositorio, y usando esa carpeta como espacio de trabajo para dejar matriz, scripts de simulación, checklist de pruebas y evidencias para la VM con Windows Server 2025, SQL Server 2022 y PB22.

Para el Redmine `30282`, voy a trabajar el análisis como laboratorio de compatibilidad, no como desarrollo sobre rama.

---

### [03/09/2026 11:41:47] Sebastián Rafael Montenegro

exacto

---

### [03/09/2026 11:41:54] Agente

Entendido, Sebastián.

Contexto fijado para el `30282`: trabajo en `/mnt/Agentes/srmontenegro`, enfoque de laboratorio/simulación sobre tu VM, sin rama SVN y sin intervención sobre repositorio.

---

### [03/09/2026 11:55:51] Sebastián Rafael Montenegro

ok, con que empezamos entonces?

---

### [03/09/2026 11:56:03] Sebastián Rafael Montenegro

ok, con que empezamos entonces?

---

### [03/09/2026 11:57:15] Sebastián Rafael Montenegro

ok, con que empezamos entonces?

---

### [03/09/2026 11:57:26] Sebastián Rafael Montenegro

ok, con que empezamos entonces?

---

### [03/09/2026 12:24:57] Sebastián Rafael Montenegro

estas?

---

### [03/09/2026 12:25:15] Agente

Sí, Sebastián, estoy.

Empezamos por lo más crítico y rápido de validar en tu VM: **conectividad PB22 contra SQL Server 2022 con `MSOLEDBSQL` 32 bits**.

Primer bloque de trabajo:
1. Validar instalación del driver `MSOLEDBSQL` x86.
2. Armar un `INI` mínimo estilo Afilmed/Presmed.
3. Crear una base chica de prueba en SQL Server 2022.
4. Probar conexión desde PB22.
5. Validar `SET DATEFORMAT`, `ANSI_WARNINGS`, `CONCAT_NULL_YIELDS_NULL`.
6. Documentar errores reales y configuración funcional.

Ese es el punto base. Si eso falla, todo lo demás falla arriba.

---

### [03/09/2026 12:27:39] Sebastián Rafael Montenegro

ok, vamos con ese bloque, el msoledbsql lo tengo que descargar?

---

### [03/09/2026 12:27:57] Agente

Sí, Sebastián. Tenés que instalarlo aparte en la VM.

SQL Server 2022 no trae `SQL Native Client` como estándar, y para PB22 necesitamos probar con **Microsoft OLE DB Driver for SQL Server**, provider:

`MSOLEDBSQL`

Clave para tu caso: como Afilmed/PowerClient está en **32 bits**, instalá sí o sí el **MSOLEDBSQL x86**. Si además vas a probar herramientas o servicios 64 bits, instalá también el **x64**.

Orden recomendado:

1. Descargar desde Microsoft: **Microsoft OLE DB Driver for SQL Server**.
2. Instalar primero `x64` si la VM es 64 bits.
3. Instalar también `x86`.
4. Reiniciar PowerBuilder si estaba abierto.
5. Probar un perfil PB22 usando `Provider='MSOLEDBSQL'`.

Primer punto de riesgo: con drivers modernos puede aparecer error por cifrado/certificado. Para la primera prueba controlada conviene preparar la conexión con `TrustServerCertificate=True` o configuración equivalente, así separamos problema de driver de problema de certificado.

---

### [03/09/2026 12:30:45] Sebastián Rafael Montenegro

ok, pero cual es la version que usa afilmed de Microsoft OLE DB Driver for SQL Server?

---

### [03/09/2026 12:31:25] Agente

Sebastián, Afilmed **no usa hoy una versión de Microsoft OLE DB Driver for SQL Server**.

Lo que usa actualmente en los INIs es:

`DBMS=SNC SQL Native Client(OLE DB)`

Eso corresponde a **SQL Server Native Client**, no a `MSOLEDBSQL`.

Entonces para la VM no tenemos que buscar “la versión que usa Afilmed”, porque Afilmed todavía no está configurado para ese driver. Tenemos que definir y probar el reemplazo.

Para PB22 + app 32 bits, el candidato técnico es:

`Microsoft OLE DB Driver for SQL Server 19.x - x86`

Provider:

`MSOLEDBSQL`

Punto importante: Driver 19 tiende a exigir cifrado/certificado por defecto. Para la primera simulación conviene probar explícitamente con `TrustServerCertificate=True`, así no mezclamos error de compatibilidad PB con error de certificado.

---

### [03/09/2026 12:41:16] Sebastián Rafael Montenegro

he redistributable installer for Microsoft OLE DB Driver for SQL Server installs the client components required during run time to take advantage of newer SQL Server features.
Microsoft OLE DB Driver 19.4.2 for SQL Server is the latest general availability (GA) version. The Microsoft OLE DB Driver 19 for SQL Server can install side by side with Microsoft OLE DB Driver 18 for SQL Server.
 Download Microsoft OLE DB Driver 19 for SQL Server (x64 and Arm64)
 Download Microsoft OLE DB Driver 19 for SQL Server (x86)
 me salen esas opciones

---

### [03/09/2026 12:41:27] Agente

Sebastián, para Afilmed/PB22 necesitamos bajar e instalar esta opción:

`Download Microsoft OLE DB Driver 19 for SQL Server (x86)`

Motivo: Afilmed/PowerClient trabaja como **32 bits**, y un ejecutable 32 bits solo ve providers OLE DB 32 bits.

La opción `x64 and Arm64` sirve para componentes 64 bits del servidor o herramientas 64 bits, pero no alcanza para Afilmed si el ejecutable es 32 bits.

Instalación recomendada en tu VM:
1. Instalar `x86` sí o sí.
2. Instalar también `x64` solo si vas a probar servicios/herramientas 64 bits o conectividad desde SQL tools.
3. Después verificamos que exista el provider `MSOLEDBSQL` 32 bits.

Versión: `19.4.2` está bien para la prueba. Es justamente la GA actual que tenemos que validar.

---

### [03/09/2026 12:54:55] Sebastián Rafael Montenegro

me dio ese error, existe windows server de 32 bits?

---

### [03/09/2026 12:55:00] Sebastián Rafael Montenegro

me dio ese error, existe windows server de 32 bits?

---

### [03/09/2026 12:55:54] Agente

No, Sebastián: **Windows Server moderno no existe en 32 bits**. Windows Server 2025 es solo **64 bits**.

Pero eso no es el problema. Una app PB/PowerClient de **32 bits puede correr en Windows Server 64 bits** usando componentes x86. Lo que necesitamos es que el **provider OLE DB x86** se instale en ese Windows.

Ese error indica otra cosa: el instalador `Microsoft OLE DB Driver 19.4.2 x86` probablemente **no soporta Windows Server 2025** todavía, o ese paquete puntual bloquea por matriz de compatibilidad.

Acción recomendada:

1. Instalá primero `Microsoft OLE DB Driver 19 x64` para validar si el driver 19 soporta tu Server 2025.
2. Para PB22/Afilmed 32 bits, probá instalar **Microsoft OLE DB Driver 18 x86**.
3. Si el 18 x86 instala, seguimos la simulación con `MSOLEDBSQL` sobre driver 18.
4. Dejamos documentado en Redmine 30282 como riesgo real: **MSOLEDBSQL 19 x86 falla en Windows Server 2025 por SO no soportado**.

Conclusión técnica: no busques Windows Server 32 bits. El laboratorio correcto sigue siendo Server 2025 x64, pero puede que para runtime PB32 tengamos que usar `MSOLEDBSQL 18 x86` o esperar una versión 19 compatible con Server 2025.

---

### [03/09/2026 13:02:58] Sebastián Rafael Montenegro

me sale esa advertencia, continuo?

---

### [03/09/2026 13:03:02] Sebastián Rafael Montenegro

me sale esa advertencia, continuo?

---

### [03/09/2026 13:03:12] Agente

Sí, Sebastián, **continuá con `Yes`**.

Esa advertencia no es error: detectó una versión menor instalada del **Microsoft OLE DB Driver for SQL Server** y te pregunta si querés actualizarla. En la VM de laboratorio conviene actualizar, porque justamente queremos probar con el driver moderno.

Después de instalar, verificá dos cosas:

1. Que termine sin error.
2. Si este instalador era `x64`, después intentá instalar también el `x86`, porque PB/Afilmed 32 bits necesita ver el provider 32 bits.

Si el `x86` vuelve a fallar por “not supported on this operating system”, lo documentamos como riesgo del laboratorio con Windows Server 2025 y probamos con `MSOLEDBSQL 18 x86`.

---

### [03/09/2026 16:44:34] Sebastián Rafael Montenegro

despues de instalar el x64 sin error, el instalador del x86 sigue dando el mismo resultado

---

### [03/09/2026 16:45:18] Agente

Sí, Sebastián. Entonces queda confirmado:

`MSOLEDBSQL 19 x64` instala correctamente en Windows Server 2025, pero `MSOLEDBSQL 19 x86` sigue fallando.

Eso es un **riesgo real para Afilmed/Presmed si el runtime PB22 se ejecuta en 32 bits**, porque el ejecutable 32 bits no puede usar el provider x64.

Próximo paso: probá descargar e instalar **Microsoft OLE DB Driver 18 for SQL Server x86** en la VM. Si ese instala bien, seguimos el laboratorio con `MSOLEDBSQL` versión 18 x86 y documentamos que la versión 19 x86 no es viable hoy en Server 2025.

Para el informe del Redmine `30282`, este hallazgo va como:

**Problema detectado:** incompatibilidad o bloqueo de instalación de `MSOLEDBSQL 19 x86` en Windows Server 2025.  
**Impacto:** aplicaciones PowerBuilder 32 bits no pueden validar conectividad con driver 19.  
**Mitigación tentativa:** probar `MSOLEDBSQL 18 x86` o definir runtime 64 bits si la arquitectura completa lo permite.

---

### [03/09/2026 16:48:24] Sebastián Rafael Montenegro

la version 18.0.2, de x86 da el mismo error

---

### [03/09/2026 16:48:45] Agente

Bien, Sebastián. Entonces ya tenemos un segundo hallazgo importante:

`MSOLEDBSQL 18.0.2 x86` también falla en Windows Server 2025.

Eso indica que el problema no es solo la versión 19: **la instalación del componente x86 del driver OLE DB está bloqueada en esa VM/OS**.

Conclusión técnica inmediata:

- `MSOLEDBSQL x64`: instala.
- `MSOLEDBSQL 19 x86`: falla.
- `MSOLEDBSQL 18.0.2 x86`: falla.
- Para Afilmed/Presmed PB22 32 bits, esto es crítico, porque necesitan provider x86.

Próximo diagnóstico antes de cerrar el punto:

1. Confirmar el texto exacto del error del instalador x86.
2. Probar una versión más nueva de la rama 18, si Microsoft ofrece una posterior a `18.0.2`.
3. Si sigue fallando, documentamos como riesgo fuerte: **Windows Server 2025 no es viable todavía para validar runtime PB22 32 bits con MSOLEDBSQL x86 en esta VM**.

Plan B técnico para seguir avanzando:
- Usar otra VM con **Windows Server 2022 x64** o **Windows 10/11 x64** para probar PB22 32 bits + `MSOLEDBSQL x86`.
- Mantener Windows Server 2025 para pruebas 64 bits, SQL Server 2022 y configuración servidor.
- Separar el informe en dos frentes: compatibilidad del servidor y compatibilidad del cliente PB32.

Este punto ya va directo a la matriz de riesgos del Redmine `30282`.

---

### [03/09/2026 16:50:36] Sebastián Rafael Montenegro

version 18.7.5 tambien da error

---

### [03/09/2026 16:50:48] Agente

Confirmado, Sebastián: `MSOLEDBSQL 18.7.5 x86` también falla por **sistema operativo no soportado** en Windows Server 2025.

Esto ya no parece un problema de versión puntual. Queda como hallazgo fuerte para Redmine `30282`:

**Windows Server 2025 no permite instalar Microsoft OLE DB Driver for SQL Server x86**, al menos en las versiones probadas:
- `19.4.2 x86`: falla
- `18.0.2 x86`: falla
- `18.7.5 x86`: falla
- `19.x x64`: instala correctamente

Impacto directo: si Afilmed/Presmed/batches corren como **PowerBuilder 32 bits**, no van a poder usar `Provider=MSOLEDBSQL` en esa VM con Server 2025, porque necesitan el provider x86.

Próximo paso técnico: no sigamos bajando versiones al azar. Hay que separar laboratorio:

1. **VM Windows Server 2025**: validar SQL Server 2022, configuración servidor, jobs, linked servers, permisos, OLE Automation, componentes 64 bits.
2. **VM Windows Server 2022 o Windows 10/11 x64**: validar PB22 32 bits + `MSOLEDBSQL x86`.

Este hallazgo ya es material para la matriz de riesgos.

---

### [03/09/2026 16:53:08] Sebastián Rafael Montenegro

y no hay otro driver por ejemplo SNC SQL
Native Client(OLE DB)

---

### [03/09/2026 16:53:17] Agente

Sí, existe: **SQL Server Native Client OLE DB**, el que hoy usan los INI como:

`DBMS=SNC SQL Native Client(OLE DB)`

Pero tiene un problema clave: **SQL Server Native Client está discontinuado/deprecated**. Puede servir como plan de contingencia o comparación, pero no debería ser la estrategia definitiva para SQL Server 2022 / Windows Server 2025.

Para el laboratorio haría esto:

1. Probar si en la VM existe el provider x86 de Native Client.
2. Si no existe, intentar instalar **SQL Server Native Client 11.0 x86**.
3. Validar si PB22 conecta con `DBMS=SNC SQL Native Client(OLE DB)`.
4. Documentar el resultado como comparación contra `MSOLEDBSQL`.

Riesgo: aunque `SNC` funcione, Microsoft ya no lo evoluciona y puede quedar fuera de soporte en escenarios nuevos. Para Redmine `30282`, el hallazgo sería: **MSOLEDBSQL x86 no instala en Windows Server 2025; SNC podría ser workaround, pero no solución recomendada a futuro**.

Entonces sí: probemos `SNC SQL Native Client(OLE DB)` como alternativa de laboratorio.

---

### [03/09/2026 16:54:18] Sebastián Rafael Montenegro

donde puedo probar si en la VM existe el provider x86 de Native Client?

---

### [03/09/2026 16:54:56] Agente

Sebastián, podés verificarlo en la VM desde **ODBC/OLE DB de 32 bits** o desde el **registro de Windows**.

La forma más directa:

1. Abrí `Win + R`.
2. Ejecutá:

```text
C:\Windows\SysWOW64\odbcad32.exe
```

3. Entrá en la pestaña **Drivers**.
4. Buscá algo como:

```text
SQL Server Native Client 11.0
```

o

```text
SQL Native Client
```

Importante: `SysWOW64\odbcad32.exe` es el administrador **32 bits**. No uses el de `System32`, porque ese muestra drivers 64 bits.

También podés verificar por registro con CMD:

```bat
reg query "HKLM\SOFTWARE\WOW6432Node\Microsoft\Microsoft SQL Server Native Client 11.0"
```

Si existe, hay componente x86 instalado. Si no existe, PB32 no va a ver `SNC`.

Para OLE DB específicamente, también revisá:

```bat
reg query "HKCR\WOW6432Node\CLSID" /f "SQLNCLI11" /s
```

Si aparece `SQLNCLI11`, el provider OLE DB x86 de Native Client está registrado.

---

### [03/09/2026 17:20:37] Sebastián Rafael Montenegro

me salen esas opciones

---

### [03/09/2026 17:20:49] Agente

Sebastián, esas opciones son **ODBC 32 bits**, no OLE DB.

Lo que veo instalado en x86 es:

- `ODBC Driver 17 for SQL Server`
- `ODBC Driver 18 for SQL Server`
- `SQL Server` genérico viejo

No aparece:

- `SQL Server Native Client 11.0`
- `SQL Native Client`
- `SQLNCLI11`

Entonces, por esa pantalla, **no está instalado el Native Client x86** que necesitaría `DBMS=SNC SQL Native Client(OLE DB)`.

Para confirmarlo bien, ejecutá en CMD:

```bat
reg query "HKLM\SOFTWARE\WOW6432Node\Microsoft\Microsoft SQL Server Native Client 11.0"
```

y también:

```bat
reg query "HKCR\WOW6432Node\CLSID" /f "SQLNCLI11" /s
```

Si ambos no devuelven nada útil, PB32 no va a poder usar `SNC`.

Dato importante para la matriz: aunque tengas `ODBC Driver 18 x86`, eso no equivale a `MSOLEDBSQL x86`. Son drivers distintos. Para Afilmed/Presmed con configuración actual `SNC`, necesitamos OLE DB, no ODBC.

---

### [03/09/2026 17:24:28] Sebastián Rafael Montenegro

y si descargo Microsoft SQL Server Native Client 11.0 o no se puede?

---

### [03/09/2026 17:24:56] Agente

Sí, Sebastián, podés descargar e intentar instalar **Microsoft SQL Server Native Client 11.0 x86**.

Pero lo trataría como **prueba de contingencia**, no como solución definitiva, porque `SQL Native Client` está discontinuado. Para el laboratorio sirve mucho: nos permite confirmar si Afilmed/Presmed pueden seguir conectando con el esquema actual `DBMS=SNC SQL Native Client(OLE DB)`.

Buscá el instalador del **SQL Server 2012 Native Client**, archivo típico:

`sqlncli.msi`

Necesitás la variante **x86**. Después de instalar, validá:

```bat
reg query "HKLM\SOFTWARE\WOW6432Node\Microsoft\Microsoft SQL Server Native Client 11.0"
```

y:

```bat
reg query "HKCR\WOW6432Node\CLSID" /f "SQLNCLI11" /s
```

Si eso aparece, PB22 32 bits debería poder ver el provider `SQLNCLI11` / `SNC`.

Si también falla en Windows Server 2025, el hallazgo queda más fuerte: no solo falla `MSOLEDBSQL x86`, también queda comprometida la continuidad con `SNC` en runtime PB32.

---

### [03/09/2026 17:42:09] Sebastián Rafael Montenegro

ok, ya se instalo, como lo comprobamos?

---

### [03/09/2026 17:42:31] Agente

Bien. Comprobalo en la VM con estos pasos.

Primero abrí **CMD como administrador** y ejecutá:

```bat
reg query "HKLM\SOFTWARE\WOW6432Node\Microsoft\Microsoft SQL Server Native Client 11.0"
```

Si devuelve claves, el componente **x86** está instalado.

Después verificá el provider OLE DB:

```bat
reg query "HKCR\WOW6432Node\CLSID" /f "SQLNCLI11" /s
```

Si aparece `SQLNCLI11`, PB22 32 bits debería poder verlo.

Validación visual adicional:

```text
Win + R
C:\Windows\SysWOW64\odbcad32.exe
```

En pestaña **Drivers** debería aparecer:

```text
SQL Server Native Client 11.0
```

Si esas tres validaciones dan bien, el próximo paso es probar desde PowerBuilder con `DBMS=SNC SQL Native Client(OLE DB)` contra una base mínima en SQL Server 2022.

---

### [03/09/2026 17:46:01] Sebastián Rafael Montenegro

C:\Users\Administrador>bat
"bat" no se reconoce como un comando interno o externo,
programa o archivo por lotes ejecutable.

C:\Users\Administrador>reg query "HKLM\SOFTWARE\WOW6432Node\Microsoft\Microsoft SQL Server Native Client 11.0"

HKEY_LOCAL_MACHINE\SOFTWARE\WOW6432Node\Microsoft\Microsoft SQL Server Native Client 11.0\CurrentVersion

C:\Users\Administrador>

---

### [03/09/2026 17:46:09] Agente

Bien, Sebastián. Eso confirma que **SQL Server Native Client 11.0 x86 quedó instalado**.

Lo de `bat` no había que escribirlo; era solo el formato del bloque de comando. No pasa nada.

Ahora falta validar que esté registrado el **provider OLE DB x86**. Ejecutá este comando:

```bat
reg query "HKCR\WOW6432Node\CLSID" /f "SQLNCLI11" /s
```

Si devuelve entradas con `SQLNCLI11`, ya tenemos confirmado que PB22 32 bits debería poder usar:

```text
DBMS=SNC SQL Native Client(OLE DB)
```

Después de eso pasamos a la prueba real desde PowerBuilder contra una base mínima en SQL Server 2022.

---

### [03/09/2026 17:47:00] Sebastián Rafael Montenegro

C:\Users\Administrador>reg query "HKCR\WOW6432Node\CLSID" /f "SQLNCLI11" /s

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{397C2819-8272-4532-AD3A-FB5E43BEAA39}
 (Predeterminado) REG_SZ SQLNCLI11

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{397C2819-8272-4532-AD3A-FB5E43BEAA39}\ExtendedErrors\{CA99D701-E6E7-4db4-A5CC-81541C75188A}
 (Predeterminado) REG_SZ SQLNCLI11 Error Lookup

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{397C2819-8272-4532-AD3A-FB5E43BEAA39}\InprocServer32
 (Predeterminado) REG_SZ C:\WINDOWS\SysWOW64\sqlncli11.dll

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{397C2819-8272-4532-AD3A-FB5E43BEAA39}\ProgID
 (Predeterminado) REG_SZ SQLNCLI11.1

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{397C2819-8272-4532-AD3A-FB5E43BEAA39}\VersionIndependentProgID
 (Predeterminado) REG_SZ SQLNCLI11

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F612DD2-7E28-424f-A2FD-C2EECC314AA2}
 (Predeterminado) REG_SZ SQLNCLI11 Enumerator

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F612DD2-7E28-424f-A2FD-C2EECC314AA2}\InprocServer32
 (Predeterminado) REG_SZ C:\WINDOWS\SysWOW64\sqlncli11.dll

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F612DD2-7E28-424f-A2FD-C2EECC314AA2}\ProgID
 (Predeterminado) REG_SZ SQLNCLI11.Enumerator.1

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F612DD2-7E28-424f-A2FD-C2EECC314AA2}\VersionIndependentProgID
 (Predeterminado) REG_SZ SQLNCLI11.Enumerator

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F8A2489-F677-43bf-B991-0BBE263147C8}
 (Predeterminado) REG_SZ SQLNCLI11 Connection Page

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F8A2489-F677-43bf-B991-0BBE263147C8}\InprocServer32
 (Predeterminado) REG_SZ C:\WINDOWS\SysWOW64\sqlncli11.dll

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F8A2489-F677-43bf-B991-0BBE263147C8}\ProgID
 (Predeterminado) REG_SZ SQLNCLI11.ConnectionPage.1

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{8F8A2489-F677-43bf-B991-0BBE263147C8}\VersionIndependentProgID
 (Predeterminado) REG_SZ SQLNCLI11.ConnectionPage

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{CA99D701-E6E7-4db4-A5CC-81541C75188A}
 (Predeterminado) REG_SZ SQLNCLI11 Error Lookup

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{CA99D701-E6E7-4db4-A5CC-81541C75188A}\InprocServer32
 (Predeterminado) REG_SZ C:\WINDOWS\SysWOW64\sqlncli11.dll

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{CA99D701-E6E7-4db4-A5CC-81541C75188A}\ProgID
 (Predeterminado) REG_SZ SQLNCLI11.ErrorLookup.1

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{CA99D701-E6E7-4db4-A5CC-81541C75188A}\VersionIndependentProgID
 (Predeterminado) REG_SZ SQLNCLI11.ErrorLookup

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{D2E5582D-7771-4777-89A2-90C374777FDB}
 (Predeterminado) REG_SZ SQLNCLI11 Advanced Page

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{D2E5582D-7771-4777-89A2-90C374777FDB}\InprocServer32
 (Predeterminado) REG_SZ C:\WINDOWS\SysWOW64\sqlncli11.dll

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{D2E5582D-7771-4777-89A2-90C374777FDB}\ProgID
 (Predeterminado) REG_SZ SQLNCLI11.AdvancedPage.1

HKEY_CLASSES_ROOT\WOW6432Node\CLSID\{D2E5582D-7771-4777-89A2-90C374777FDB}\VersionIndependentProgID
 (Predeterminado) REG_SZ SQLNCLI11.AdvancedPage

Fin de la búsqueda: 21 coincidencias encontradas.

C:\Users\Administrador>

---

### [03/09/2026 17:47:10] Agente

Perfecto, Sebastián. Quedó comprobado:

- `SQL Server Native Client 11.0 x86` está instalado.
- El provider OLE DB x86 está registrado como `SQLNCLI11`.
- La DLL activa es `C:\WINDOWS\SysWOW64\sqlncli11.dll`.

Eso significa que PB22 32 bits debería poder usar el modo actual:

```text
DBMS=SNC SQL Native Client(OLE DB)
```

Próximo paso: hacer una prueba real desde PowerBuilder contra SQL Server 2022.

En PB22 creá o ajustá un Database Profile con estos datos base:

```text
DBMS = SNC SQL Native Client(OLE DB)
Server = localhost o nombre_instancia
Database = una base chica de prueba
User ID = usuario_sql
Password = clave
```

Si usa `SQLCA`, la prueba mínima debería terminar en:

```powerscript
CONNECT USING SQLCA;

IF SQLCA.SQLCode <> 0 THEN
   MessageBox("Error conexión", SQLCA.SQLErrText)
ELSE
   MessageBox("OK", "Conectó correctamente con SNC x86")
END IF
```

Resultado técnico hasta ahora para Redmine `30282`: `MSOLEDBSQL x86` falla en Windows Server 2025, pero `SQL Native Client 11.0 x86` sí instala y registra provider OLE DB. Esto nos permite seguir el laboratorio con el driver legacy actual.

---

### [03/09/2026 17:48:33] Sebastián Rafael Montenegro

me habias dejado una w_main_pruebas, porque no agregas un boton ahi para validar la conexion y me lo compartis?

---

### [03/09/2026 17:50:57] Agente

Listo, Sebastián. Modifiqué [w_main_pruebas.srw](/mnt/Agentes/srmontenegro/w_main_pruebas.srw:1) en la carpeta compartida.

Agregué el botón **`Validar conexion SQL`**. La prueba intenta conectar desde PB22 con:

```text
DBMS=SNC SQL Native Client(OLE DB)
Provider=SQLNCLI11
Server=.
Database=master
TrustedConnection=1
```

Si conecta, muestra OK y desconecta. Si falla, muestra `SQLCode`, `SQLDBCode` y `SQLErrText`, que nos sirve directo como evidencia para el Redmine `30282`.

No pude compilarlo desde acá porque no tengo el IDE PB22 de tu VM, pero el export quedó revisado completo. Probalo importando/actualizando la ventana en PB y ejecutando ese botón.

---

### [03/09/2026 17:55:54] Sebastián Rafael Montenegro

ok, no... tenemos que crearlo con los datos y/o crear un usuario si es necesario, vamos paso a paso, que datos necesitas?

---

### [03/09/2026 17:56:20] Agente

Sí. Vamos paso a paso.

Para crear la prueba real necesito estos datos de la VM:

1. **Nombre de instancia SQL Server**
   
   Puede ser:
   ```text
   .
   localhost
   .\SQLEXPRESS
   NOMBREVM\INSTANCIA
   ```

2. **Cómo vas a autenticar**
   
   Recomendado para simular Afilmed/Presmed: **usuario SQL**, no Windows.
   
   Necesito saber si SQL Server está en **Mixed Mode**. Si no lo sabés, lo vemos.

3. **Nombre de la base de prueba**
   
   Propongo:
   ```text
   afilmed_lab
   ```

4. **Usuario SQL a crear**
   
   Propongo:
   ```text
   usr_afilmed_lab
   ```

5. **Password**
   
   Definilo vos. Para laboratorio puede ser algo simple pero que cumpla política SQL Server, por ejemplo:
   ```text
   AfilmedLab2026!
   ```

6. **Puerto**
   
   Si es instancia default:
   ```text
   1433
   ```
   
   Si es instancia nombrada, puede no hacer falta al principio.

Con eso yo te preparo:

- script SQL para crear base + login + usuario + permisos mínimos;
- ajuste del botón PB para conectar contra esa base con ese usuario;
- mensaje de error detallado si falla.

Primer dato: ¿tu SQL Server en la VM es instancia default `localhost` o es algo tipo `.\SQLEXPRESS`?

---

### [03/09/2026 18:00:44] Sebastián Rafael Montenegro

actualmente eso me conecta, pero necesitamos usuario y contraseña calculo

---

### [03/09/2026 18:00:58] Agente

Sí, Sebastián. Ahora estás conectando con **Autenticación de Windows** usando:

`WIN-5FNHM57C1QS\Administrador`

Para simular Afilmed/Presmed conviene crear un **login SQL con usuario y contraseña**, porque las aplicaciones normalmente no deberían depender del usuario Windows de la VM.

Arranquemos así desde esa conexión que ya funciona. Abrí una query nueva en SSMS y ejecutá:

```sql
CREATE DATABASE afilmed_lab;
GO

CREATE LOGIN usr_afilmed_lab
WITH PASSWORD = '***',
CHECK_POLICY = OFF,
CHECK_EXPIRATION = OFF;
GO

USE afilmed_lab;
GO

CREATE USER usr_afilmed_lab FOR LOGIN usr_afilmed_lab;
GO

ALTER ROLE db_owner ADD MEMBER usr_afilmed_lab;
GO

CREATE TABLE dbo.prueba_conexion (
    id int IDENTITY(1,1) PRIMARY KEY,
    descripcion varchar(100) NOT NULL,
    fecha datetime NOT NULL DEFAULT GETDATE()
);
GO

INSERT INTO dbo.prueba_conexion (descripcion)
VALUES ('Conexion PB22 SNC SQL Native Client OK');
GO
```

Después probá conectar en SSMS cambiando:

- **Authentication:** `SQL Server Authentication`
- **Login:** `usr_afilmed_lab`
- **Password:** `AfilmedLab2026!`
- **Database:** `afilmed_lab`
- **Cifrar:** obligatorio
- **Certificado de servidor de confianza:** marcado

Si eso conecta, recién ahí ajusto la `w_main_pruebas` para que el botón pruebe exactamente contra:

```text
Server=localhost
Database=afilmed_lab
User=usr_afilmed_lab
Password=***
Provider=SQLNCLI11
```

Si al probar con usuario SQL te rechaza el login, el servidor está probablemente en **Windows Authentication only** y tenemos que habilitar **Mixed Mode**.

---

### [03/09/2026 18:03:58] Sebastián Rafael Montenegro

error

---

### [03/09/2026 18:04:08] Agente

Ese error `18456` confirma que el usuario existe o fue intentado, pero SQL Server no lo acepta para login SQL.

Primero revisemos si el servidor está en **Mixed Mode**.

En SSMS, conectado con Windows, ejecutá:

```sql
SELECT SERVERPROPERTY('IsIntegratedSecurityOnly') AS SoloWindows;
```

Resultado esperado:

```text
0 = Mixed Mode habilitado
1 = Solo autenticación Windows
```

Si devuelve `1`, hay que habilitar Mixed Mode:

1. Click derecho sobre el servidor `localhost`.
2. `Properties`.
3. `Security`.
4. Seleccionar `SQL Server and Windows Authentication mode`.
5. Aceptar.
6. Reiniciar el servicio de SQL Server.

Después ejecutá esto para asegurar login, password y permisos:

```sql
ALTER LOGIN usr_afilmed_lab ENABLE;
GO

ALTER LOGIN usr_afilmed_lab
WITH PASSWORD = '***',
CHECK_POLICY = OFF,
CHECK_EXPIRATION = OFF;
GO

USE afilmed_lab;
GO

ALTER USER usr_afilmed_lab WITH LOGIN = usr_afilmed_lab;
GO
```

Luego probá de nuevo con:

- Authentication: `SQL Server Authentication`
- User: `usr_afilmed_lab`
- Password: `AfilmedLab2026!`
- Database: `afilmed_lab`
- Certificado de servidor de confianza: marcado

Pasame el resultado del `SELECT SERVERPROPERTY...`; con eso sabemos si el problema es modo de autenticación o login/password.

---

### [03/09/2026 18:06:54] Sebastián Rafael Montenegro

respondio 1

---

### [03/09/2026 18:07:02] Agente

Perfecto, Sebastián. `1` significa que SQL Server está en **solo autenticación Windows**.

Hay que habilitar **Mixed Mode**:

1. En SSMS, click derecho sobre el servidor `localhost`.
2. `Properties`.
3. `Security`.
4. Marcar: `SQL Server and Windows Authentication mode`.
5. Aceptar.
6. Reiniciar el servicio de SQL Server.

Después del reinicio, volvé a ejecutar:

```sql
SELECT SERVERPROPERTY('IsIntegratedSecurityOnly') AS SoloWindows;
```

Tiene que responder `0`.

Cuando responda `0`, probamos de nuevo el login `usr_afilmed_lab` con contraseña.

---

### [03/09/2026 18:09:34] Sebastián Rafael Montenegro

listo, ya pude conectarme

---

### [03/09/2026 18:09:43] Agente

Bien, Sebastián. Ya tenemos validado el paso clave: **SQL Server acepta autenticación SQL**.

Ahora seguimos con la prueba PB. Necesito que confirmemos estos datos finales:

```text
Servidor: localhost
Base: afilmed_lab
Usuario: usr_afilmed_lab
Password: AfilmedLab2026!
Driver: SNC SQL Native Client(OLE DB)
Provider: SQLNCLI11
```

Si esos son correctos, el próximo paso es ajustar `w_main_pruebas` para que el botón conecte con usuario/clave, ejecute un `SELECT` simple sobre `dbo.prueba_conexion`, muestre el resultado y desconecte.

Con eso dejamos evidencia concreta para el Redmine `30282`: PB22 32 bits + Windows Server 2025 + SQL Server 2022 + `SQLNCLI11 x86` conecta correctamente como contingencia legacy.

---

### [03/09/2026 18:11:03] Sebastián Rafael Montenegro

si, esos sons los datos q me diste y que pude conectarme

---

### [03/09/2026 18:12:13] Agente

Listo, Sebastián. Actualicé [w_main_pruebas.srw](/mnt/Agentes/srmontenegro/w_main_pruebas.srw:147).

El botón `Validar conexion SQL` ahora conecta con:

```text
Servidor: localhost
Base: afilmed_lab
Usuario: usr_afilmed_lab
Provider: SQLNCLI11
DBMS: SNC SQL Native Client(OLE DB)
```

Además ejecuta:

```sql
SELECT descripcion, fecha
FROM dbo.prueba_conexion
WHERE id = 1
```

Si conecta y lee, muestra el detalle. Si conecta pero falla la consulta, muestra el error SQL. No pude compilar desde acá porque PB22 está en tu VM, pero el export quedó revisado.

---

### [03/09/2026 18:17:37] Sebastián Rafael Montenegro

al importar me sale esto, que hago?

---

### [03/09/2026 18:17:41] Sebastián Rafael Montenegro

al importar me sale esto, que hago?

---

### [03/09/2026 18:17:45] Sebastián Rafael Montenegro

al importar me sale esto, que hago?

---

### [03/09/2026 18:17:57] Agente

Sebastián, eso no es un error de importación. Es la ventana de **Database Profiles 32-bit** de PowerBuilder.

Qué hacer ahora:

1. Cerrá esa ventana con **Close**.
2. En el árbol de la izquierda verificá que esté `w_main_pruebas`.
3. Hacé **Run** de la aplicación o abrí la ventana `w_main_pruebas`.
4. Probá el botón **Validar conexion SQL**.

Dato importante: en la imagen ya se ve `SNC SQL Native Client`, así que PB22 32 bits está detectando el driver. Eso es bueno.

No hace falta crear un Database Profile para esta prueba, porque el botón conecta directo por código con `SQLCA`. Si al ejecutar el botón falla, pasame el mensaje exacto que muestra.

---

### [03/09/2026 18:20:02] Sebastián Rafael Montenegro

no deja importarla si hago close, ni regenerarla ni verla...

---

### [03/09/2026 18:21:29] Agente

Sebastián, corregido en la carpeta compartida: [w_main_pruebas.srw](/mnt/Agentes/srmontenegro/w_main_pruebas.srw:144).

El problema era este: PowerBuilder no te dejaba importar/regenerar porque el botón tenía un `SELECT ... FROM dbo.prueba_conexion` embebido. PB intenta validar ese SQL en compile-time y por eso pide **valid database sign-on**.

Ahora lo cambié a SQL dinámico con `PREPARE / OPEN DYNAMIC / FETCH`, así debería importar sin pedir conexión de base.

Hacé esto:

1. Cerrá esa ventana de Database Profiles.
2. Volvé a importar/reemplazar `w_main_pruebas.srw`.
3. Regenerá.
4. Ejecutá la ventana y probá **Validar conexion SQL**.

Si ahora tira otro error de compilación, pasame la línea exacta y lo ajusto.

---

### [03/09/2026 18:25:26] Sebastián Rafael Montenegro

mm vuelve a importar pidiendo db

---

### [03/09/2026 18:26:52] Agente

Corregido, Sebastián.

Actualicé `/mnt/Agentes/srmontenegro/w_main_pruebas.srw` para que el botón **no tenga ningún SQL embebido**. Ahora solo valida conexión con:

```text
SNC SQL Native Client(OLE DB)
localhost
afilmed_lab
usr_afilmed_lab
```

Probá importar de nuevo. Si ahora importa bien, regenerá y ejecutá el botón. Si vuelve a pedir DB, entonces el problema ya no viene del código del botón y revisamos el objeto completo/importador de PB.

---

### [03/09/2026 18:29:23] Sebastián Rafael Montenegro

seeeee

---

### [03/09/2026 18:29:32] Agente

Excelente, Sebastián. Quedó validado el primer punto crítico:

`PB22 32 bits + Windows Server 2025 + SQL Server 2022 + SQL Native Client 11 x86` conecta correctamente contra `afilmed_lab`.

Hallazgo para Redmine `30282`:

- `MSOLEDBSQL x86`: no pudimos instalarlo en Server 2025.
- `SQLNCLI11 x86`: instaló, PB22 lo detectó y conectó correctamente.
- Esto habilita un camino de contingencia con `SNC SQL Native Client(OLE DB)`, aunque queda marcado como driver legacy/discontinuado.

Siguiente paso: agregamos una segunda prueba al botón o un botón nuevo para ejecutar un `SELECT` real contra `dbo.prueba_conexion`. Ahí validamos no solo conexión, sino lectura SQL básica desde PB.

---

### [03/09/2026 18:31:16] Sebastián Rafael Montenegro

bueno, pero espera, porque en afilmed no hay sql embebido, es todo por llamadas a sp

---

### [03/09/2026 18:31:46] Agente

Correcto, Sebastián. Entonces no tenemos que validar `SELECT` embebido como caso principal.

Para Afilmed la prueba realista tiene que ser:

1. PB22 conecta con `SQLCA`.
2. Ejecuta una llamada a stored procedure.
3. El SP devuelve resultado o código de salida.
4. PB captura `SQLCode`, `SQLDBCode`, `SQLErrText` y el valor devuelto.

El próximo botón debería probar algo estilo:

```sql
EXEC dbo.sp_prueba_conexion_afilmed
```

Primero creamos en `afilmed_lab` un SP mínimo. Después ajusto `w_main_pruebas` para llamarlo desde PB sin SQL embebido validable en importación, usando SQL dinámico o `DECLARE PROCEDURE`.

Objetivo técnico: simular el patrón real de Afilmed, no una consulta directa a tabla.

---

### [03/09/2026 18:32:28] Sebastián Rafael Montenegro

exacto

---

### [03/09/2026 18:33:40] Agente

Exacto. Ya lo ajusté.

Dejé dos archivos en la carpeta compartida:

- [w_main_pruebas.srw](/mnt/Agentes/srmontenegro/w_main_pruebas.srw:145): el botón ahora conecta y llama a `EXEC dbo.sp_prueba_conexion_afilmed`.
- [redmine_30282_crear_sp_prueba_conexion_afilmed.sql](/mnt/Agentes/srmontenegro/redmine_30282_crear_sp_prueba_conexion_afilmed.sql:1): script para crear el SP y dar permiso al usuario `usr_afilmed_lab`.

Primero ejecutá el `.sql` en SSMS conectado a `afilmed_lab`. Después importá/regenerá la ventana y probá el botón.

La prueba ahora representa mejor Afilmed: PB no consulta tablas directo, solo valida conexión + ejecución de stored procedure.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
