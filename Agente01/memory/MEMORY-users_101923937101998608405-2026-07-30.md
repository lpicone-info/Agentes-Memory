# Memoria curada diaria

- Fecha: 2026-07-30
- Usuario: Belkis Josefina Garcia Mora
- id_googlechat: users/101923937101998608405
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/nsoxdyaaaae

## Resumen de la actividad

### Tema 1: Continuidad del diagnostico de traslado a contrato nuevo

- Solicitud o contexto: Belkis retomo el analisis del dia anterior sobre una solicitud automatica de traslado a contrato nuevo en `InstallQA_89`. El caso giraba alrededor de `id_soli_externa=5012`, `id_proceso=146` y el procedimiento `afi_insertar_soli_auto`.
- Respuesta o acciones del agente: Se cargo el contexto operativo y la memoria previa del 2026-07-29. Se recordo que el primer error de afiliado origen vigente ocurre antes de insertar en `SOLI_AUTO_CONTRATO` y antes de generar GMS, por lo que no explicaba por si solo la existencia de `GMS_CAB=295798`.
- Resultado: Quedo establecido como punto de continuidad revisar por que el proceso 146 llego a GMS con valores distintos a los esperados en facturacion, campana e integrantes.
- Estado: En curso

### Tema 2: Analisis del XML enviado para `id_soli_externa=5012`

- Solicitud o contexto: Belkis envio el XML real usado para la solicitud `5012`, con `soli_moti=47`, `cuenta_dest=2`, `subcta_dest=FSMP`, `cod_camp_comercial=""`, facturacion `emi_grupo=3` y `vto_grupo=3`, e integrantes destino informados como `inte="01"` e `inte="04"` con origen `anter_inte="03"` y `anter_inte="04"`.
- Respuesta o acciones del agente: Se revisaron en modo solo lectura objetos SQL de `InstallQA_89`, incluyendo `afi_insertar_soli_auto`, `afi_insertar_soli_auto_traslado_nuevo`, `afs_soli_auto_obligatorios`, `afu_genera_soli_auto_app`, `afs_soli_auto_app_trazabilidad`, tablas GMS y tablas `SOLI_AUTO`. Se validaron datos actuales del proceso 146, definiciones de procedimientos y comportamiento de conversion XML a `INT`.
- Resultado: Se concluyo que `cod_camp_comercial=""` se convertia en `0` al leerse directo como `INT`, que GMS recibia integrantes destino `01` y `04` cuando para alta o traslado a contrato nuevo esperaba consecutivos `01` y `02`, y que los valores `emi_grupo=2` y `vto_grupo=2` en GMS no salian del XML informado si `SOLI_AUTO_CONTRATO` tenia `3/3` al momento de generar GMS. La diferencia de facturacion apuntaba a que GMS se genero cuando esos datos estaban nulos o distintos, o que `SOLI_AUTO_CONTRATO` fue corregida despues. Se confirmaron errores GMS asociados a `cab_id=295798`: `8141`, `8162` y `8221`.
- Estado: Resuelto
