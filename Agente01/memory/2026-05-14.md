
## Redmine 29362 / Traslado a Contrato Existente (`soli_moti = 48`) - cierre de jornada

- Interlocutor actual en Discord DM identificado por metadata confiable como `SebastianRMontenegro`; pidió persistir estado para continuar mañana.
- Repo SVN trabajado: `/home/luispicone/.openclaw/workspace/svn/srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`.
- Fase actual: **Fase 2** del flujo automático, puente `SOLI_AUTO_*` -> GMS -> solicitud Afilmed.
- Commits realizados y relevantes:
  - `r36863` — `29362: integrar traslado existente en generacion APP`.
    - Modificó `backend/procedures/afu_genera_soli_auto_app.sql` y `backend/procedures/gms_genera_solicitudes.sql`.
    - Agregó soporte `soli_moti = 48` con transacción `INCL`.
    - Genera `GMSTXT_INTEGRANTES` desde `SOLI_AUTO_INTEGRANTES`.
    - `gms_genera_solicitudes` incluye `48` para generar integrantes/SOLI y deriva `anter_prepa`, `anter_contra`, `anter_inte`, `anter_plan` antes de `afu_soli_afiliados`.
  - `r36865` — `29362: documentar fase GMS traslado existente`.
    - Actualizó `Redmines/29362/29362.md` con Fase 2, flujo resultante, límites y riesgos.
  - `r36869` — `29362: excluir auditoria del puente automatico`.
    - Modificó `backend/procedures/afu_genera_soli_auto_app.sql` y `Redmines/29362/29362.md`.
    - Eliminó del puente automático la variable/lectura `requiere_auditoria`.
    - Documentó que auditoría médica queda fuera de alcance.
- Estado remoto observado durante el cierre:
  - `backend/procedures/afi_insertar_soli_auto_traslado_existente.sql` ya estaba actualizado en remoto (`r36856`) sin campos de auditoría; al actualizar hubo conflicto, se aceptó remoto porque ya contenía la versión alineada sin auditoría.
  - `svn status` quedó limpio para `afi_insertar_soli_auto_traslado_existente.sql`, `afu_genera_soli_auto_app.sql` y `Redmines/29362/29362.md`.
- Validaciones hechas:
  - Grep en SPs activos de 29362 no encontró referencias a `requiere_auditoria`, `estado_auditoria`, `nivel_riesgo`, `observacion_auditoria`, `copia_preexistencias` ni `no_genera_carta`.
  - No se agregaron `;` ni `RTRIM` en SQL.
  - Encoding/BOM/EOL preservados en los archivos editados.
- Decisión funcional confirmada: **no se implementarán procesos de auditoría médica** para este alcance. El XML/SPs no deben pedir, validar ni persistir decisiones de auditoría. Si alguna tabla conserva columnas históricas de auditoría, quedan fuera del contrato funcional del flujo.
- Pendiente recomendado para continuar:
  1. Probar Fase 2 con datos reales o controlados: insertar XML de `soli_moti = 48`, ejecutar `afu_genera_soli_auto_app`, luego validar GMS y generación de `SOLICITUDES` / `SOLI_AFILIADOS`.
  2. Revisar si `afu_genera_soli_auto_app` todavía debe derivar cobertura destino desde contrato destino si Fase 1 ya no recibe `cuenta_dest/subcta_dest/plan_dest/tari_dest/plan_emp_dest/tari_emp_dest` según la documentación vigente.
  3. Validar ambigüedad de afiliado origen: si `docu_tipo + docu_nro` devuelve más de una afiliación vigente, el flujo hoy debe cortar por ambigüedad.
  4. Continuar con Fase 3 sin auditoría: impacto del traslado, novedades derivadas origen/destino y preexistencias técnicas solo si el impacto las requiere.
  5. Continuar con Fase 4: reversión, reliquidación, mail/salida de resultados si sigue en alcance.
