
## 2026-08-31 12:37 ART - Trabajo rama gpulka version 104
- Usuario Gabriel pidio investigar en installqa89 el caso `exec afs_soli_auto_app_trazabilidad 4900106`, que quedo iniciado sin finalizar, revisar transaccion/log y commitear cambios.
- Diagnostico reportado: `GMS_CAB 295948` tenia `fecha_desde` cargada y `fecha_hasta = NULL`; agenda `AU` en estado `P`; sin `GMS_ERR`; sin solicitud generada para `id_soli_externa = 4900106`.
- Se detecto solicitud APP vigente posterior para el mismo contrato/motivo: `soli 4677204` con `id_soli_externa = 4900109`; el mensaje esperado del log debe indicar que corresponde volver a generar con otro numero.
- Cambios realizados y commiteados en SVN `r37706`: `backend/procedures/afs_soli_auto_app_trazabilidad.sql` agrega estado `GENERACION_GMS_INICIADA_SIN_FIN` y `mensaje_generacion` descriptivo; `backend/procedures/afs_soli_auto_app_log_resumen.sql` devuelve `mensaje_app`; `exe_afilmed/src/wp_main_solicitudes.srw` escribe ese mensaje en el `.log` del proceso.
- Validaciones reportadas: conexion OK a `InstallQA_89`; consultas read-only sobre `SOLI_AUTO_CONTRATO`, `GMS_CAB`, `CAMBIOS_MASIVOS_AGENDA`, `GMS_ERR`, `SOLICITUDES`; `PARSEONLY` OK para ambos SPs; encoding/BOM/CRLF preservados; commit SVN confirmado `r37706`.

## 2026-08-31 13:26 ART - Redmine 30282 Chilkat 9.5 y Redmine 29364 saldo CT APP
- Usuario Sebastian retomo Redmine `30282`: en su PC personal monto VM nueva con Windows Server 2025, SQL Server 2022, PowerBuilder 2022 y Chilkat 9.5 comprado. Objetivo: probar compatibilidad de la libreria con una porcion aislada de Afilmed sin DB ni tablas.
- Se identifico que Afilmed usa Chilkat en `exe_afilmed/src/nvo_contabilizacion.sru`, funcion `of_gestiona_ftp`, tomando host/usuario/password/puerto/cifrado desde DB (`d_lista_config_ftp_vigente`) y usando COM `Chilkat_9_5_0.Ftp2` / `Chilkat_9_5_0.SFtp`.
- Se genero ventana PowerBuilder autonoma importable para prueba Chilkat: `/mnt/Agentes/srmontenegro/w_chilkat95_test.srw`. La ventana permite cargar parametros por pantalla y prueba `UnlockComponent`, conexion FTP/FTPS explicito/SFTP, directorio remoto y subida de archivo. Sin licencia ni credenciales embebidas, UTF-8 BOM, CRLF, sin modificar SVN.
- Usuario pidio contexto Redmine `29364`: cambio de titularidad automatico desde APP, `soli_moti = 7`, rama SVN `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, carpeta entregables `Redmines/29364`, commits previos `r37246` a `r37249`.
- Analisis caso Redmine `29364`: solicitud `id_soli_externa = 2012`, contrato `117329`, `soli_moti = 7`, rechazaba por `El titular nuevo no se encuentra activo en la COOPERATIVA` y `El contrato posee saldos por pagar`; comprobante con saldo `FC XSCMP06959240`, cod unico agrupador `144238061`, vencimiento `05/03/2027`, saldo `29942430.00`. El rechazo era correcto pero faltaba detalle porque `gmsdef_validar.sql` buscaba detalle solo si `COM.fecha_vto < @var_CTAPPApliFecha`.
- Se aplico y comiteo fix Redmine `29364` en SVN `r37707`, mensaje `Refs #29364 informa comprobante en saldo por pagar CT APP`, archivo `backend/procedures/gmsdef_validar.sql`. Cambio: para armar detalle del rechazo por saldo por pagar, busca primer comprobante con `saldo > 0` sin exigir vencimiento anterior a fecha de aplicacion. Encoding ISO-8859 y CRLF preservados; working copy limpia salvo externos `X` habituales.
- Aclaracion funcional dada: fix aplica a futuro; solicitudes ya procesadas con registros en `GMS_ERR` no se corrigen solas salvo reproceso/regeneracion o actualizacion explicita del error grabado.
- Para prueba en `InstallQA_89` se dejo solicitud generada con mensaje nuevo: `id_soli_externa = 293649301`, `id_proceso = 223`, `cab_id = 295954`, contrato `117329`, `soli_moti = 7`. Mensaje visible en `GMS_ERR` y trazabilidad: `El contrato posee saldos por pagar. Comprobante: FC 144238061    , vencimiento: 05/03/2027, valor: 29942430.00`. Consulta sugerida: `EXEC dbo.afs_soli_auto_app_trazabilidad @prm_id_soli_externa = 293649301, @prm_debug = 'S'`.
## 2026-08-31 12:37 ART - Trabajo rama gpulka version 104
- Usuario Gabriel pidio investigar en installqa89 el caso `exec afs_soli_auto_app_trazabilidad 4900106`, que quedo iniciado sin finalizar, revisar transaccion/log y commitear cambios.
- Diagnostico reportado: `GMS_CAB 295948` tenia `fecha_desde` cargada y `fecha_hasta = NULL`; agenda `AU` en estado `P`; sin `GMS_ERR`; sin solicitud generada para `id_soli_externa = 4900106`.
- Se detecto solicitud APP vigente posterior para el mismo contrato/motivo: `soli 4677204` con `id_soli_externa = 4900109`; el mensaje esperado del log debe indicar que corresponde volver a generar con otro numero.
- Cambios realizados y commiteados en SVN `r37706`: `backend/procedures/afs_soli_auto_app_trazabilidad.sql` agrega estado `GENERACION_GMS_INICIADA_SIN_FIN` y `mensaje_generacion` descriptivo; `backend/procedures/afs_soli_auto_app_log_resumen.sql` devuelve `mensaje_app`; `exe_afilmed/src/wp_main_solicitudes.srw` escribe ese mensaje en el `.log` del proceso.
- Validaciones reportadas: conexion OK a `InstallQA_89`; consultas read-only sobre `SOLI_AUTO_CONTRATO`, `GMS_CAB`, `CAMBIOS_MASIVOS_AGENDA`, `GMS_ERR`, `SOLICITUDES`; `PARSEONLY` OK para ambos SPs; encoding/BOM/CRLF preservados; commit SVN confirmado `r37706`.

## 2026-08-31 13:26 ART - Redmine 30282 Chilkat 9.5 y Redmine 29364 saldo CT APP
- Usuario Sebastian retomo Redmine `30282`: en su PC personal monto VM nueva con Windows Server 2025, SQL Server 2022, PowerBuilder 2022 y Chilkat 9.5 comprado. Objetivo: probar compatibilidad de la libreria con una porcion aislada de Afilmed sin DB ni tablas.
- Se identifico que Afilmed usa Chilkat en `exe_afilmed/src/nvo_contabilizacion.sru`, funcion `of_gestiona_ftp`, tomando host/usuario/password/puerto/cifrado desde DB (`d_lista_config_ftp_vigente`) y usando COM `Chilkat_9_5_0.Ftp2` / `Chilkat_9_5_0.SFtp`.
- Se genero ventana PowerBuilder autonoma importable para prueba Chilkat: `/mnt/Agentes/srmontenegro/w_chilkat95_test.srw`. La ventana permite cargar parametros por pantalla y prueba `UnlockComponent`, conexion FTP/FTPS explicito/SFTP, directorio remoto y subida de archivo. Sin licencia ni credenciales embebidas, UTF-8 BOM, CRLF, sin modificar SVN.
- Usuario pidio contexto Redmine `29364`: cambio de titularidad automatico desde APP, `soli_moti = 7`, rama SVN `srmontenegro-Version104-Afilmed-SolicitudesAutomaticas`, carpeta entregables `Redmines/29364`, commits previos `r37246` a `r37249`.
- Analisis caso Redmine `29364`: solicitud `id_soli_externa = 2012`, contrato `117329`, `soli_moti = 7`, rechazaba por `El titular nuevo no se encuentra activo en la COOPERATIVA` y `El contrato posee saldos por pagar`; comprobante con saldo `FC XSCMP06959240`, cod unico agrupador `144238061`, vencimiento `05/03/2027`, saldo `29942430.00`. El rechazo era correcto pero faltaba detalle porque `gmsdef_validar.sql` buscaba detalle solo si `COM.fecha_vto < @var_CTAPPApliFecha`.
- Se aplico y comiteo fix Redmine `29364` en SVN `r37707`, mensaje `Refs #29364 informa comprobante en saldo por pagar CT APP`, archivo `backend/procedures/gmsdef_validar.sql`. Cambio: para armar detalle del rechazo por saldo por pagar, busca primer comprobante con `saldo > 0` sin exigir vencimiento anterior a fecha de aplicacion. Encoding ISO-8859 y CRLF preservados; working copy limpia salvo externos `X` habituales.
- Aclaracion funcional dada: fix aplica a futuro; solicitudes ya procesadas con registros en `GMS_ERR` no se corrigen solas salvo reproceso/regeneracion o actualizacion explicita del error grabado.
- Para prueba en `InstallQA_89` se dejo solicitud generada con mensaje nuevo: `id_soli_externa = 293649301`, `id_proceso = 223`, `cab_id = 295954`, contrato `117329`, `soli_moti = 7`. Mensaje visible en `GMS_ERR` y trazabilidad: `El contrato posee saldos por pagar. Comprobante: FC 144238061    , vencimiento: 05/03/2027, valor: 29942430.00`. Consulta sugerida: `EXEC dbo.afs_soli_auto_app_trazabilidad @prm_id_soli_externa = 293649301, @prm_debug = 'S'`.

## 2026-08-31 14:27 ART - Soporte Savina Version104 / InstallQA_89
- Savina pidio revisar que `Version104` estuviera actualizada con lo ultimo desarrollado. Se actualizo working copy SVN `/home/luispicone/.openclaw/workspace/svn/svarela-Version104-Afilmed-SolicitudesAutomaticas` a revision `37705`; `svn update` y `svn status -u` sin pendientes remotos. No se hicieron modificaciones locales ni commit.
- Savina consulto solicitud `id=295948`, transaccion `RP`, motivo `49`, `SOLI_AUTO_APP`, `id_proceso=219`, `id_soli_externa=4900106` en profile `InstallQA_89`. Diagnostico: si genero insumo GMS (`GMS_CAB.id=295948`, `estado=B`, `puede_procesarse=S`, `canti_errores=0`) y detalles en `GMSTXT/GMSDEF_CLASE_PLAN` e `INTEGRANTES`, empleado `CC 14695651 LLANOS PAULINO`; sin errores en `GMS_ERR`, `GMS_TEMP_ERRORES`, `DETALLE_ERRORES_GMS_TMP`.
- Para `id_soli_externa=4900106`, no existe solicitud final en `SOLICITUDES` por `nro_formulario=4900106`; `SOLI_AUTO_CONTRATO.soli` sigue `NULL`. La agenda `CAMBIOS_MASIVOS_AGENDA.cab_id=295948` quedo `estado=P`, `fecha_inicio_real=2026-08-31 10:31:17.350`, `fecha_fin_real=NULL`.
- Tras correr el batch, se explico que no vuelve a tomar `cab_id=295948` porque el selector de pendientes exige `fecha_inicio_real IS NULL`, `fecha_fin_real IS NULL`, `baja_fecha IS NULL`; como tiene inicio real y no fin, quedo en estado intermedio iniciado/no finalizado. Reprocesarlo requiere intervencion explicita con `UPDATE` sobre `CAMBIOS_MASIVOS_AGENDA` y confirmacion previa del SQL exacto.
- Savina pidio contratos con saldo por pagar y saldo futuro. En `InstallQA_89` se informaron 5 contratos con comprobantes de afiliado (`aplica=1`, `factu_destino='A'`, `saldo>0`, `baja_fecha IS NULL`, `factu_perio >= '20260801'`): `1073889` saldo `48126120.00` periodo `2030-01-01`; `1073892` saldo `30685830.00` periodo `2030-01-01`; `117329` saldo `29942430.00` periodo `2026-08-01`; `1036112` saldo `25841039.00` periodo `2026-08-01`; `1073888` saldo `21240240.00` periodo `2030-01-01`.
- Savina pidio otros 5 contratos con saldo vencido y responsable pagador vigente al `2026-08-31`. En `InstallQA_89` se informaron: contrato `1021408` saldo vencido `1.00`, vto `2025-05-23`, resp pagador `CC 32240879`; `1029134` saldo `1.00`, vto `2025-05-30`, resp `CC 8280849`; `636125` saldo `2.00`, vto `2025-04-25`, resp `CC 8031221`; `374229` saldo `2.00`, vto `2025-05-23`, resp `CC 22429267`; `996351` saldo `2.00`, vto `2025-05-30`, resp `CC 16935039`. Criterio: saldo positivo vencido, aplica afiliado, sin baja ni reverso, `AFI_RESP_PAGADOR` vigente.
- Savina pidio un beneficiario asociado a cooperativa vigente. En `InstallQA_89` se informo beneficiario: prepaga `1`, contrato `10`, integrante `01`, documento `CE 160345`, nombre `GONZALEZ, ELSA`, ingreso afiliado `1989-04-01`, cooperativa `estado=A`, cod estado `10`, denominacion `Activo Normal`, ingreso cooperativa `1988-08-01`. Criterio: `AFILIADOS.baja_fecha IS NULL`, `ASOCIADOS_COOPERATIVA.estado='A'`, estado cooperativa vigente y denominacion `Activo%`.

## 2026-08-31 16:04 ART - Savina RP automatico XML e informe validaciones
- Savina trabajo sobre cambio de responsable pagador automatico para contrato `636125`, nuevo RP `CC 1000001498`, nuevo parentesco `H`, usando profile/contexto `InstallQA_89` y rama `svarela-Version104-Afilmed-SolicitudesAutomaticas` revision base `37705`.
- Para `CC 1000001498` se confirmo que ya existe en Registro Unico y esta asociado a cooperativa activo normal, corte `5`; datos personales usados: `VALDERRAMA DAZA JAVIER FELIPE`, nacimiento `2001-06-08`, sexo `M`, civil `S`, estrato `4`, nacionalidad `CO`, iva `G`, asociado `S`.
- Se indico XML de solicitud automatica `soli_moti="22"`, `prepaga="1"`, `contra="636125"`, `fecha_aplicacion="2026-09-01"`, con `responsable_pagador` autocerrado y parentesco de integrante `01` de `T` a `H`. No corresponde enviar `fecha_radicacion`; el SP usa `GETDATE()` si no viene.
- Codigos sugeridos para campos obligatorios faltantes: `profesion="951"` (Bachillerato), `ocupacion="1"` (Empleado), `cargo="187"` (Auxiliares administrativos y afines).
- Se aclaro que si `finalizar_resp_pagador="N"` y `baja_resp_pagador="N"`, la automatica puede generar solicitud igual si viene `<responsable_pagador>`, pero no cierra el RP vigente anterior y puede permitir solapamiento. Para reemplazo real de RP deberia usarse `finalizar_resp_pagador="S"`, `baja_resp_pagador="N"`, `moti_ret_resp_pag` informado.
- Savina reporto errores SQL Server `XML parsing ... illegal qualified name character`; se corrigio el formato explicando que `<responsable_pagador ... />` debe ir autocerrado y se entrego una version XML de una sola linea para evitar caracteres/saltos pegados.
- Savina pidio informe comparativo entre validaciones manuales por ventana de aplicacion y validaciones automaticas XML para cambio de responsable pagador. Se genero primero markdown y luego Word en `/mnt/Agentes/svarela/comparacion de validaciones de RP.docx`; el `.docx` fue validado como `Microsoft Word 2007+`, paquete valido, aprox. 16 KB.
- El informe compara `wp_cambio_resp_pagador.srw` contra procedures automaticos como `afi_insertar_soli_auto_cambio_resp_pagador.sql` y `afs_soli_auto_obligatorios.sql`. Gaps principales documentados: automatica permite RP nuevo sin finalizar/bajar RP vigente, faltan validaciones de edad, contactos, contratos relacionados, parentescos, cooperativa/corte, saldos/deuda futura y mensajes de error equivalentes a la ventana manual.


## 2026-08-31 16:13 ART - gpulka Version104 / Solicitudes Automaticas

- Usuario Gabriel trabaja sobre rama SVN `gpulka version 104`, working copy principal observada en `/home/luispicone/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Caso investigado en `InstallQA_89`: `exec afs_soli_auto_app_trazabilidad 4900106`. Diagnostico: `GMS_CAB 295948` quedo con `fecha_desde` cargada y `fecha_hasta = NULL`; agenda `AU` en estado `P`; sin `GMS_ERR`; sin solicitud generada para ese `id_soli_externa`. Existe solicitud APP vigente posterior para mismo contrato/motivo: `soli 4677204`, `id_soli_externa = 4900109`.
- Commit SVN `r37706` (`backend/procedures/afs_soli_auto_app_trazabilidad.sql`, `backend/procedures/afs_soli_auto_app_log_resumen.sql`, `exe_afilmed/src/wp_main_solicitudes.srw`): agrega trazabilidad/log descriptivo para estado `GENERACION_GMS_INICIADA_SIN_FIN` y `mensaje_generacion`/`mensaje_app`, indicando que se debe regenerar con otro numero cuando queda iniciado sin fin.
- Analisis transaccional informado: `afu_genera_soli_auto_app.sql` tiene `BEGIN TRANSACTION`/`COMMIT` y `ROLLBACK` en `CATCH` para armar GMS/agenda/marcar `SOLI_AUTO_CONTRATO.proce_fecha`; la generacion real la maneja PB en `wp_main_solicitudes.srw`. Problema detectado: `fecha_inicio_real` podia quedar fuera de un cierre garantizado si el batch/app se interrumpia antes de `fecha_fin_real`.
- Commit SVN `r37708` (`exe_afilmed/src/wp_main_solicitudes.srw`): corrige rollback completo en generacion AU. Para procesos `AU`, abre transaccion antes de grabar `fecha_inicio_real`; incluye en el mismo bloque `fecha_inicio_real`, validacion, generacion de solicitudes, actualizacion de estado y `fecha_fin_real`; ante falla hace `ROLLBACK` completo para que el caso pueda retomarse. `wf_genera_solicitudes` respeta transaccion externa y no hace `COMMIT`/`ROLLBACK` propio si ya hay una abierta.
- Consulta sobre `recha 7996` en `gmsdef_validar.sql`: aplica al circuito `soli_moti = 7` (Cambio de Titularidad) cuando `origen_solicitud = 'APP'`, `puede_procesarse = 'S'`, `SOLI_AUTO_CONTRATO.genera_resp_pagador = 'N'`, no hay `AFI_RESP_PAGADOR` vigente, y el contrato individual/acuerdo corporativo tiene saldo > 0. No aplica a traslados `47/48` ni a responsable pagador/empleado `22/49` desde ese bloque.
- Caso investigado en `InstallQA_89`: `id_soli_externa = 49001011`, `soli_moti = 22`, `finalizar_resp_pagador = N`, `baja_resp_pagador = N`, `moti_ret_resp_pag = 1`; genero un nuevo responsable pagador sin bajar el anterior, dejando 2 responsables vigentes para contrato `1021408`.
- Commit SVN `r37712` (`afs_soli_auto_obligatorios.sql`, `afi_insertar_soli_auto_cambio_resp_pagador.sql`, `afu_genera_soli_auto_app.sql`): valida baja de responsable pagador en soli_auto. Se elimina/ignora `finalizar_resp_pagador` del XML para responsable pagador/empleado; se conserva `baja_resp_pagador` y `moti_ret_resp_pag`; `moti_ret_resp_pag` es obligatorio cuando `baja_resp_pagador = 'S'`; si se informa nuevo `responsable_pagador`, exige `baja_resp_pagador = 'S'`; SP defensivo deja error descriptivo para solicitudes viejas/invalidas indicando regenerar con `baja_resp_pagador = S` y `moti_ret_resp_pag`.
- Validaciones ejecutadas durante estos cambios: consultas read-only contra `InstallQA_89`; `PARSEONLY` OK para SQL modificados; preservacion de `UTF-8 BOM` y `CRLF` en archivos PB/SQL relevantes; `svn log` confirmo commits `r37706`, `r37708`, `r37712`.

## 2026-08-31 17:22 ART - Redmine 29364 formato mensaje y Redmine 30282 pruebas Chilkat
- Redmine `29364`: tras commit `r37707`, Sebastian detecto espacio improlijo antes de la coma en mensaje de saldo: `Comprobante: FC 144238061 , vencimiento...`. Diagnostico corregido: no era literal del texto sino padding por variable SQL `@var_CTAPPComproNro CHAR(13)` y conversiones `CONVERT(CHAR(13), ...)` en `backend/procedures/gmsdef_validar.sql`.
- Se aplico y comiteo SVN `r37713`, mensaje `Refs #29364 corrige formato comprobante saldo CT APP`, archivo `backend/procedures/gmsdef_validar.sql`. Cambio: `@var_CTAPPComproNro CHAR(13)` -> `VARCHAR(13)` y `CONVERT(CHAR(13), COMAGR.cod_unico_comprobante)` -> `CONVERT(VARCHAR(13), COMAGR.cod_unico_comprobante)`. Sebastian pidio no compilar DB; solo commit. Validaciones reportadas: `PARSEONLY` OK en `InstallQA_89`, encoding ISO-8859 y CRLF preservados, working copy limpia salvo externos SVN habituales.
- Para probar `r37713`, se dio SQL no ejecutado con XML `soli_moti=7`. Primer ejemplo con contrato `117329`, `id_soli_externa=293649315`, nuevo titular `03`, titular anterior `02` a `C`; al correrlo Sebastian obtuvo otro bloqueo: `El contrato posee conceptos facturados a periodos posteriores a la fecha de aplicacion`, por lo que no servia para validar el formato de comprobante.
- Se entrego segundo ejemplo no ejecutado para `29364`: `id_soli_externa=293649316`, contrato `109087`, `fecha_aplicacion=2026-08-01`, `inte_nuevo_titular=03`, `baja_titular=N`, parentescos `01 O->O`, `02 P->P`, `03 C->T`, `05 O->O`, `06 T->C`. Resultado esperado luego de compilar y correr batch: `Comprobante: FC 144088535, vencimiento: 30/05/2025`, sin espacio antes de la coma.
- Redmine `30282`: Sebastian probo ventana `/mnt/Agentes/srmontenegro/w_chilkat95_test.srw`. Error inicial `debe informar licencia de chilkat`; se indico licencia usada por Afilmed en `exe_afilmed/src/nvo_contabilizacion.sru`: `NCBUGU.CB1012020_TEvxsxsC9U1l`. Luego error `no existe C:\temp\chilkat_test.txt`; se indico crear archivo con `mkdir C:\temp` y `echo prueba chilkat 9.5 > C:\temp\chilkat_test.txt`.
- Redmine `30282`: Sebastian confirmo que la prueba funciono con cifrado `E` (FTPS explicito), validando `Chilkat_9_5_0.Ftp2` + `AuthTls` en la VM nueva. Datos usados venian de `InstallQA_89`, tipo proceso `IC`: host `servicios.infomedical.com.ar`, puerto `22543`, usuario `infoftp`, password de `afs_ftp_config_vigente('IC')`, directorio `/Coomeva_db/PruebasRecaudos/5025/PENDIENTES`, licencia Chilkat `NCBUGU.CB1012020_TEvxsxsC9U1l`.
- Redmine `30282`: siguiente prueba sugerida es SFTP con cifrado `S`, que usa `Chilkat_9_5_0.SFtp`. En `InstallQA_89` se encontraron configs SFTP vigentes en `FTP_CONFIG` para tipos `SE`, `SI`, `EA`, `IA`, todas con host `172.16.10.14`, puerto `22`, usuario `infomedical`, password `Noteladigo.2023`, licencia `NCBUGU.CB1012020_TEvxsxsC9U1l`. Para primera prueba SFTP se recomendo dejar directorio remoto, archivo local y archivo remoto vacios, validando solo COM, unlock, connect, authenticate e initialize.
