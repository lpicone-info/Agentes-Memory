
## 2026-06-12 14:23 UTC - InstallQA_89 / Solicitudes Automaticas

- Contexto de trabajo: repo SVN `~/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Se investigó en `InstallQA_89` la solicitud de traslado a contrato existente `4676852`: `SOLICITUDES.soli_moti = 48`, origen APP, contrato destino `298320`, integrante con `SOLI_AUTO_INTEGRANTES.docu_tipo = TI`, `paren_destino = F`, generado en `SOLI_AFILIADOS` con `docu_tipo = TI`, `paren_real = F`.
- Hallazgo de validación: el bloque local de `gmsdef_val_traslado_web.sql` que valida relación entre parentesco destino y tipo de documento no estaba aplicado en `InstallQA_89`; por eso permitió familiar a cargo con `docu_tipo = TI`.
- Hallazgo UI: el `docu_tipo` queda visualmente vacío al abrir por ventana porque `DOCU_TIPOS.TI` está dado de baja desde `2025-10-08 10:00:11.860`; `dddw_docu_tipos` usa `afs_docu_tipos` con `baja_fecha IS NULL`. El dato no se perdió en `SOLI_AFILIADOS`.
- Cambio realizado y commiteado por pedido del usuario: corrección de auditoría de facturación de traslados a contrato existente en `SOLI_AUTO_FACT_AUDIT`, caso ejemplo `select * from SOLI_AUTO_FACT_AUDIT where id_proceso = 42` en `InstallQA_89`.
- Commit SVN: revisión `r37115`, mensaje `Corrige auditoria de facturacion en traslado existente`.
- Archivos impactados en `r37115`: `backend/procedures/afu_soli_auto_fact_audit.sql` y `exe_afilmed/src/wp_main_super_batch.srw`.
- Corrección técnica: `wp_main_super_batch` ahora pasa `ls_contra` al SP de auditoría; `afu_soli_auto_fact_audit` audita el contrato procesado por cada fila económica del batch en vez de usar siempre `SOLICITUDES.contra`; para reversos con comprobantes `NC` donde `COMPROBANTES.contra` viene `NULL`, graba el contrato recibido por parámetro.
- Validaciones realizadas: caso real proceso `42`, simulación con destino `298320` y origen `301371`, parse SQL del SP OK, y preservación de encoding/BOM/finales de línea: SQL CRLF sin BOM, SRW UTF-8 con BOM y CRLF.
- Nota operativa: quedaron sin tocar cambios previos no relacionados en la working copy.
## 2026-06-12 14:23 UTC - InstallQA_89 / Solicitudes Automaticas

- Contexto de trabajo: repo SVN `~/.openclaw/workspace/svn/gpulka-Version104-Afilmed-SolicitudesAutomaticas`.
- Se investigó en `InstallQA_89` la solicitud de traslado a contrato existente `4676852`: `SOLICITUDES.soli_moti = 48`, origen APP, contrato destino `298320`, integrante con `SOLI_AUTO_INTEGRANTES.docu_tipo = TI`, `paren_destino = F`, generado en `SOLI_AFILIADOS` con `docu_tipo = TI`, `paren_real = F`.
- Hallazgo de validación: el bloque local de `gmsdef_val_traslado_web.sql` que valida relación entre parentesco destino y tipo de documento no estaba aplicado en `InstallQA_89`; por eso permitió familiar a cargo con `docu_tipo = TI`.
- Hallazgo UI: el `docu_tipo` queda visualmente vacío al abrir por ventana porque `DOCU_TIPOS.TI` está dado de baja desde `2025-10-08 10:00:11.860`; `dddw_docu_tipos` usa `afs_docu_tipos` con `baja_fecha IS NULL`. El dato no se perdió en `SOLI_AFILIADOS`.
- Cambio realizado y commiteado por pedido del usuario: corrección de auditoría de facturación de traslados a contrato existente en `SOLI_AUTO_FACT_AUDIT`, caso ejemplo `select * from SOLI_AUTO_FACT_AUDIT where id_proceso = 42` en `InstallQA_89`.
- Commit SVN: revisión `r37115`, mensaje `Corrige auditoria de facturacion en traslado existente`.
- Archivos impactados en `r37115`: `backend/procedures/afu_soli_auto_fact_audit.sql` y `exe_afilmed/src/wp_main_super_batch.srw`.
- Corrección técnica: `wp_main_super_batch` ahora pasa `ls_contra` al SP de auditoría; `afu_soli_auto_fact_audit` audita el contrato procesado por cada fila económica del batch en vez de usar siempre `SOLICITUDES.contra`; para reversos con comprobantes `NC` donde `COMPROBANTES.contra` viene `NULL`, graba el contrato recibido por parámetro.
- Validaciones realizadas: caso real proceso `42`, simulación con destino `298320` y origen `301371`, parse SQL del SP OK, y preservación de encoding/BOM/finales de línea: SQL CRLF sin BOM, SRW UTF-8 con BOM y CRLF.
- Nota operativa: quedaron sin tocar cambios previos no relacionados en la working copy.

## 2026-06-12 19:46 UTC - Gpulka Version104 / Redmines 29361, 29362, 29363

- Interlocutor pidió trabajar sobre branch/working copy SVN `gpulka-Version104-Afilmed-SolicitudesAutomaticas`, contexto Redmine 29363, usuario gpulka. Se hizo `svn update` y se corrigió documentación del traslado a contrato nuevo.
- Commit SVN realizado: `r37118 - Refs #29363 Ajusta documentacion traslado contrato nuevo`.
- Archivos impactados en `r37118`: `Redmines/29363/29363.md`, `Redmines/29363/REDMINE_29363_TRASLADO_CONTRATO_NUEVO_HANDOFF.md`, `Redmines/29363/REDMINE_29363_TRASLADO_CONTRATO_NUEVO_FUNCIONAL_TECNICO.docx`.
- Corrección funcional 29363: eliminar completamente del alcance operativo/XML/documentación los conceptos `sarlaft`, `auditoria medica`, `estado de salud`, `notas`, `cert de estudios`; el XML no debe pedir datos adicionales a los ya requeridos por venta nueva masiva usando el Excel `VN_CUENTA 12 CAMPANIA 12_OK.xls`.
- Validación 29363: búsqueda en Markdown y texto extraído del `.docx` sin remanentes de los conceptos eliminados; encoding/BOM/finales de línea preservados; commit limitado a los tres documentos de 29363.
- Cambio local ajeno detectado y no tocado: `backend/procedures/gmsdef_val_traslado_web.sql`.
- Hallazgo funcional para Redmine sobre `wp_cambio_datos_identificacion`: en la ventana no finalizaban anexos/adicionales no obligatorios porque la validación de cambio de parentesco real tomaba referencia de `paren_real_ant`; la corrección propuesta usa `paren_real` vigente y, si viene nulo o vacío, lo normaliza con `paren_anterior`. Comparado contra `gpulka trunk`, el código disponible conservaba la lógica anterior basada en `paren_real_ant`.
- Explicación funcional redactada: al detectar correctamente cambios de parentesco o parentesco real, se dispara la revisión/finalización de adicionales no obligatorios que ya no corresponden para la nueva condición del integrante.
- Investigación superbatch reversión/facturación con recaudos `compro_tipo = RC`: funcionalmente es normal que no genere `RZ` cuando el proceso no anula el recaudo sino que refactura comprobantes futuros; el flujo desaplica el `RC` del comprobante anterior y luego lo vuelve a aplicar contra la nueva facturación/saldo. `RZ` corresponde cuando se anula/reversa el `RC` como comprobante en sí.
- Código revisado para esa explicación: `wp_main_super_batch.srw` usa flujo general de reversión/facturación; `afi_compro_reverso_det` primero llama a `afu_desaplicar_compro_item`; la parametrización RC->RZ aplica cuando el RC es el comprobante incluido para anulación/reversión.
- Documentación solicitada para Redmines 29361 y 29362: se actualizaron/generaron dos `.docx`, uno por Redmine, con documentación funcional y técnica completa.
- Archivos `.docx` creados y quedaron sin versionar al cierre de la tarea: `Redmines/29361/REDMINE_29361_DOCUMENTACION_FUNCIONAL_TECNICA.docx` y `Redmines/29362/REDMINE_29362_DOCUMENTACION_FUNCIONAL_TECNICA.docx`.
- Validación 29361/29362: `svn update` de carpetas a r37120; ambos `.docx` son válidos; texto extraído con `pandoc` y revisadas secciones funcionales/técnicas principales; no se tocó ni commiteó el SQL local pendiente `backend/procedures/gmsdef_val_traslado_web.sql`.
