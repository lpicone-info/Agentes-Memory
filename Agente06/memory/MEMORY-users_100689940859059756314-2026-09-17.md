# Memoria de conversación — Emanuel Romano

## Datos

- **Fecha:** 17/09/2026
- **Usuario:** Emanuel Romano
- **ID Google Chat:** `users/100689940859059756314`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 10
- **Inicio:** 10:10:26
- **Fin:** 16:44:12

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/v9c28kaaaae`

## Archivos revisados

- `29826091-d1c4-4d4b-b277-bd1c01c09355.jsonl`

---

## Transcripción completa

### [17/09/2026 10:10:26] Emanuel Romano

hola

---

### [17/09/2026 10:10:55] Emanuel Romano

hola

---

### [17/09/2026 10:11:54] Agente

Hola Emanuel, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [17/09/2026 10:16:53] Emanuel Romano

ya apliqué las correcciones que ayer discutimos, la que implicaba lo siguiente sobre el redmine #30361:

DATETIME ldt_hoy

ldt_hoy = DateTime(Date(gd_hoy), Now())

ahora, quiero comprobar que estas modificaciones funcionan. me ayudarías a utilizar el aplicativo afilmed para probar si estas modificaciones dan resultado? básicamente, quiero grabar solicitudes y chequear luego en base de datos la tabla compro_cae. me darías ejemplos de cómo usar afilmed para grabar solicitudes? todo esto en modo solo lectura.

---

### [17/09/2026 10:18:23] Emanuel Romano

ya apliqué las correcciones que ayer discutimos, la que implicaba lo siguiente sobre el redmine #30361:

DATETIME ldt_hoy

ldt_hoy = DateTime(Date(gd_hoy), Now())

ahora, quiero comprobar que estas modificaciones funcionan. me ayudarías a utilizar el aplicativo afilmed para probar si estas modificaciones dan resultado? básicamente, quiero grabar solicitudes y chequear luego en base de datos la tabla compro_cae. me darías ejemplos de cómo usar afilmed para grabar solicitudes? todo esto en modo solo lectura.

---

### [17/09/2026 10:23:28] Agente

Emanuel, sí. En modo solo lectura de mi lado, lo correcto es que yo no grabe nada por vos. Para probarlo, la grabación la harías vos desde Afilmed y después validamos por `SELECT` en `compro_cae`.

El camino más directo para validar la corrección es este:

1. En Afilmed entrar a:
`Prefacturación > Procesos masivos > Facturación electrónica > Exportar comprobantes`

2. Usar un comprobante no exportado. Encontré este caso de ejemplo en `desa_prevencion_prod`, leído desde el mismo SP de la pantalla:
`ente = 1`, `punto = 0002`, período `03/2026`, comprobante `NC B000200360682`.

3. Antes de grabar, validar que todavía no tenga solicitud:

```sql
SELECT
    ente,
    compro_tipo,
    compro_nro,
    CONVERT(varchar(19), fecha_solicitud, 120) AS fecha_solicitud,
    CONVERT(varchar(19), alta_fecha, 120) AS alta_fecha,
    cae
FROM compro_cae
WHERE ente = 1
  AND compro_tipo = 'NC'
  AND compro_nro = 'B000200360682'
```

Hoy devuelve sin registro en `compro_cae` para ese comprobante.

4. En la pantalla `Exportar comprobantes`, filtrar:
`Entidad/ente = 1`, `punto = 0002`, período `03/2026`, incluir no exportados. Debería aparecer `NC B000200360682` con `Exportado = N`.

5. Ejecutar la exportación. Eso debería disparar `wp_compro_cae_solicitar.srw` y marcar el comprobante llamando a `afu_compro_cae_exportados`.

6. Después validar:

```sql
SELECT
    ente,
    compro_tipo,
    compro_nro,
    CONVERT(varchar(19), fecha_solicitud, 120) AS fecha_solicitud,
    CONVERT(varchar(19), alta_fecha, 120) AS alta_fecha,
    cae
FROM compro_cae
WHERE ente = 1
  AND compro_tipo = 'NC'
  AND compro_nro = 'B000200360682'
```

Resultado esperado con tu corrección: `fecha_solicitud` debe quedar con hora/minuto real del puesto, no `00:00:00`. `alta_fecha` puede diferir porque la carga el SQL Server con `GETDATE()`.

También podés probar los otros flujos, pero para empezar usaría `Exportar comprobantes` porque valida directamente el cambio de `@fecha_solicitud = :ldt_hoy`. Los otros caminos son:
`Obtener CAE`, `Importar CAE` y `Resolicitar CAE`, que pasan por `afu_compro_cae_grabar` y sirven para validar `@fecha_hoy = :ldt_hoy`.

Validación ejecutada por mí: solo lecturas de código, documentos del análisis local y consultas `SELECT`/metadata sobre `desa_prevencion_prod`. No ejecuté ninguna acción de grabación.

---

### [17/09/2026 12:43:34] Emanuel Romano

desde donde más se puede probar? porque no puedo acceder a dichas ventanas por medio de las opciones de menú. al parecer, estas ventanas están inhabilitadas para el usuario que uso o directamente están deshabilitadas. al iniciar sesión, las opciones de menú que sugeriste no están disponibles. algún otra forma de probar estos cambios? analiza esto en Afilmed en mromano-prevencion-trunk en modo lectura.

---

### [17/09/2026 13:12:31] Emanuel Romano

desde donde más se puede probar? porque no puedo acceder a dichas ventanas por medio de las opciones de menú. al parecer, estas ventanas están inhabilitadas para el usuario que uso o directamente están deshabilitadas. al iniciar sesión, las opciones de menú que sugeriste no están disponibles. algún otra forma de probar estos cambios? analiza esto en Afilmed en mromano-prevencion-trunk en modo lectura.

---

### [17/09/2026 16:42:03] Emanuel Romano

desde donde más se puede probar? porque no puedo acceder a dichas ventanas por medio de las opciones de menú. al parecer, estas ventanas están inhabilitadas para el usuario que uso o directamente están deshabilitadas. al iniciar sesión, las opciones de menú que sugeriste no están disponibles. algún otra forma de probar estos cambios? analiza esto en Afilmed en mromano-prevencion-trunk en modo lectura.

---

### [17/09/2026 16:44:12] Agente

Emanuel, sí: hay otras rutas, y la más útil no depende del submenú de `Facturación electrónica`.

**Ruta alternativa principal:** imprimir/reimprimir comprobantes que necesiten CAE.

En `mromano-Prevencion-trunk`, la pantalla de impresión llama a `uo_nv_factu.of_compro_imprime()`. Ese método verifica si al comprobante le falta CAE con `afs_compro_falta_cae`; si corresponde, crea `uo_nv_afip_wsfe_autrequest` y ejecuta `ue_solicitar_cae()`, que termina en `ue_grabar_cae()` y llama a `afu_compro_cae_grabar`.

Opciones donde se ve ese camino:

- `Prefacturación > Procesos masivos > Impresión de comprobantes`
  - abre `wp_fac_compro_impresion`
  - termina llamando `factu.of_compro_imprime(str)`

- `Prefacturación > Procesos masivos > Reimpresión de comprobantes`
  - también abre `wp_fac_compro_impresion`, pero con parámetro `'R'`

- `Facturación > Afiliados facturados por empresa`
  - abre `wp_compro_impresion_afi_espe`
  - también arma comprobantes y puede pasar por impresión

Además encontré otras pantallas que llaman al mismo `of_compro_imprime()`:
`wp_ver_ctacte`, `wp_ver_ctacte_empresas`, `wp_ver_facturas`, `wp_compros_no_impre`, `wp_soli_comprobantes`, `wp_factu_aporte_empresas`, `wp_listado_ni_aportes_fact`, `wp_prorroga_vto`, `wr_ajustes_gen_comprob`, `wr_ajustes_empre_gen_comprob`.

**Condiciones para que sirva como prueba real:**
El comprobante elegido debe necesitar CAE, es decir `afs_compro_falta_cae` debe devolver falta de CAE. Además debe estar activa la facturación electrónica y el modo WS:
`glob.gs_reglas[198] = 'S'` y `glob.of_valor_regla(220) = 'W'`.

**Otra ruta posible:** liquidación/desliquidación de expedientes.

En `wp_selexp_liq.srw` y `wp_desliquida_expedientes.srw` se llama a `rut.of_pedido_cae(str[])`. Ese método está en `uo_nv_liquida.sru` y también crea `uo_nv_afip_wsfe_autrequest`, por lo que valida el mismo cambio en `uo_nv_afip_wsfe_autrequest.sru`.

**Conclusión técnica:** si no podés entrar a `Exportar comprobantes`, `Obtener CAE`, `Importar CAE` o `Resolicitar CAE`, la alternativa más limpia para probar la corrección es intentar imprimir/reimprimir un comprobante electrónico que todavía no tenga CAE. Eso prueba el flujo corregido de `@fecha_hoy = :ldt_hoy` vía `afu_compro_cae_grabar`.

Validación realizada: solo lectura sobre `mromano-Prevencion-trunk`. No ejecuté cambios ni procesos de grabación.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
