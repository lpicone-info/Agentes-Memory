# Memoria 2026-07-28

- Interaccion con Marcelo por Google Chat directo.
- Marcelo solicito implementar, en base al analisis previo, una secuencia directa desde `afi_genera_compro_compro` para grabar en `AJU_INTERNOS_CUENTA` y `AJU_INTER_CUENTA_ITEMS`, sin quitar la logica existente.
- Condicion funcional clave: el nuevo circuito aplica cuando `SUBCTA_CONTRATOS.modo_fac = 'E'` para la cuenta/subcuenta del contrato de `AFI_CLASE`, ingresado por importacion desde `wp_impor_aportes_prepaga` del menu `Cuentas --> Aportes --> Importacion de Aportes`. Debe tener cuenta y subcuenta.
- Trabajo realizado en repo `mepaduan-britanico-2026.05`: se modifico `backend/procedures/afi_genera_compro_compro.sql` para pasar cuenta/subcta a `afi_genera_compro_aju_aportes` y no cortar con recha 351 cuando el circuito viene con `tipo_factu/mode E`; se modifico `backend/procedures/afi_genera_compro_aju_aportes.sql` para conservar la logica existente de `AJU_INTERNOS/AJU_INTER_ITEMS` y agregar rama condicionada por `modo_fac = 'E'`; se creo `backend/procedures/afi_genera_aju_internos_cta.sql` para insertar directo en `AJU_INTERNOS_CUENTA` y `AJU_INTER_CUENTA_ITEMS`.
- Validacion ejecutada: parseo SQL con `SET PARSEONLY ON` sobre los 3 SP sin errores reportados; validacion de encoding/BOM/finales de linea preservando ISO-8859-1 y CRLF en SP existentes; `svn status` mostro 2 SP modificados y el nuevo SP sin versionar. No se ejecuto `svn add` ni commit.
- Riesgo pendiente informado: falta prueba funcional con caso real importado desde `wp_impor_aportes_prepaga` con `modo_fac = 'E'`, cuenta y subcuenta vigentes, para confirmar que `tmp_aportes_ajustes` trae aportes esperados y que el comprobante queda asociado por `XCUENTAINTERNOS`.
