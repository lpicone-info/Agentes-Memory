# Memoria curada diaria

- Fecha: 2026-08-04
- Usuario: Marcelo E. Paduan
- id_googlechat: users/101970137366556255918
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/4_yjbyaaaae

## Resumen de la actividad

### Tema 1: Diagnóstico de período nulo en generación de lote de aportes

- Solicitud o contexto: Marcelo informó que sus pruebas volvían a insertar registros, creía que en el mismo lote, y luego indicó que el lote nuevo se generaba pero fallaba porque `apli_perio` parecía llegar `NULL` cuando no debería. Pidió rastrear si el período venía del archivo o de la ventana.
- Respuesta o acciones del agente: Se recuperó memoria previa del trabajo sobre `aju_inter_aportes`, `afi_genera_compro_aju_aportes_cta.sql` y `afs_ajus_x_comp.sql`. Se revisó estáticamente el repositorio `mepaduan-britanico-2026.05`, incluyendo la ventana PowerBuilder `wp_abm_afi_aportes_real.srw`, procedimientos relacionados con lotes de aportes y tablas `afi_aportes_real_lote`, `afi_aportes_recibidos_lote`, `afi_aportes_real` y `aju_cta_subcta`.
- Resultado: Se determinó que la ventana crea el lote con `afi_afi_aportes_real_lote`, pasando `@periodo = :ld_periodo` desde `dw_sel.object.periodo[1]`, y luego ejecuta `afu_aju_cta_subcta_aportes @lote = :ll_lote`. El punto sospechoso fuerte quedó en que `afu_aju_cta_subcta_aportes` lee `SELECT @cuenta = cuenta, @obser = descripcion, @periodo = periodo FROM AFI_APORTES_RECIBIDOS_LOTE WHERE lote = @lote`, mientras el lote nuevo se genera en `afi_aportes_real_lote`. Si para el lote no existe fila en `afi_aportes_recibidos_lote`, o existe con `periodo` nulo, `@periodo` queda `NULL` y se intenta insertar en `aju_cta_subcta.apli_perio`, que es `NOT NULL`.
- Estado: En curso

### Tema 2: Consultas sugeridas para confirmar causa

- Solicitud o contexto: Marcelo necesitaba orientar la causa del error antes de modificar código.
- Respuesta o acciones del agente: Se sugirió validar el lote nuevo comparando `afi_aportes_real_lote` y `afi_aportes_recibidos_lote`.
- Resultado: La validación propuesta fue consultar `lote`, `periodo`, `cuenta` y `descripcion` en ambas tablas para el lote nuevo. Si `afi_aportes_real_lote` tiene período y `afi_aportes_recibidos_lote` no tiene fila o tiene período nulo, esa sería la causa. El fix probable quedó planteado como ajustar `afu_aju_cta_subcta_aportes` para tomar el período desde la tabla correcta o hacer fallback a `afi_aportes_real_lote` cuando no exista en `afi_aportes_recibidos_lote`. No se tocó código; el rastreo fue estático.
- Estado: Pendiente
