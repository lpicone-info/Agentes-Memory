
## 2026-08-27 10:10 ART - Diagnóstico Redmine R30292 Prevención

- Usuario: Omar. Conversación por Google Chat directo.
- Tarea realizada antes de compactación: diagnóstico solo lectura del Redmine R30292, cliente Prevención, base obligatoria `prevencion_entrega`, contrato/afiliado `203119`. Restricciones explícitas cumplidas: no modificar datos, Redmine, SVN ni código.
- Conclusión entregada: `COMPORTAMIENTO ESPERADO` según código/configuración vigente; no se encontró evidencia de defecto para el caso analizado.
- Regla reconstruida: la facturación ejecuta `afi_aju_inter_aporte_real`; para contrato origen `R` y clase `D` aplica aportes reales. Si el afiliado está cubierto por aportes, `afi_genera_compro_compro` valida con `afs_valida_afi_plan_aportes` y puede llamar a `afi_genera_plan_cubierto_aportes`. La FC/ND puede quedar en cero con bonificación y aporte aplicado; el excedente de aportes genera NI y una DI espejo `PCA` para cancelar el excedente interno. La rama especial `afi_aju_inter_aporte_real_ni` con `apo_ni_fac = S` evalúa saldo: saldo 0 no genera NI y pasa a consumible; aporte <= saldo genera ajuste total; aporte > saldo limita al saldo y deja resto consumible. En contrato 203119 los aportes revisados tenían `apo_ni_fac = N`.
- Rol de `afi_plan_cubierto_por_aportes`: marca por contrato/vigencia/prepaga para considerarlo cubierto por aportes. `afs_valida_afi_plan_aportes` demuestra que la cobertura puede venir por `aporte_planes`, por `afi_plan_cubierto_por_aportes`, por tarifa cubierta o por subcuenta cubierta. Para 203119 aplica el registro específico en `afi_plan_cubierto_por_aportes` vigente desde `2026-07-01`, `cubierto = S`, sin baja.
- Situación contrato 203119: titular `ARISTIMUÑO VALLEJOS MORENA`; origen vigente `R`; clase vigente `D`; plan vigente desde `2026-07-01`: `ON DEMAND`; tarifa `833`; validación `afs_valida_afi_plan_aportes`: `2026-06-01 = N`, `2026-07-01 = S`, `2026-08-01 = S`. `ON DEMAND` no estaba en `aporte_planes`; tarifa `833` no tenía `cubierto = S`; `subcta_planes` para `ON DEMAND` tenía `cance_excedente = N`; activación real por `afi_plan_cubierto_por_aportes`.
- Cambio de plan: historial `2021-04-01 A1 JOVEN tarifa 34 solicitud 869491`; `2024-12-01 BASICO tarifa 01 solicitud 2432470`; `2026-07-01 ON DEMAND tarifa 833 solicitud 3542397`. Incidencia temporal: coincide con inicio de cobertura por aportes, pero no se demostró que el plan ON DEMAND por sí solo active la regla.
- Trazabilidad 08/2026: FC `B000206852486`; cuota `CUO 96.037,90`; bonificación `BEX -19.207,58`; aporte aplicado `AP -76.830,32`; IVA FC `0,00`; total FC `0,00`. Aporte real `10137506`: neto `104.346,85`, comisión `11.478,15`, facturable `92.868,70`, aplicado FC `76.830,32`, excedente `16.038,38`. NI `B000100465602`: `AP -16.038,38`, IVA 10,5% `-1.684,03`, total `-17.722,41`. DI `B000100290186`: `PCA 16.038,38`, IVA `1.684,03`, total `17.722,41`.
- Trazabilidad 07/2026: ND `B000200292661` total `0,00`; NI `B000100458153` total `-18.390,26`; DI `B000100288858` total `18.390,26`.
- Evidencia de código citada: `/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_generar_compro_masiva.sql` línea aprox. 900; `afi_generar_compro_individual.sql` línea aprox. 409; `afi_genera_compro_compro.sql` líneas aprox. 316, 320, 331; `afi_aju_inter_aporte_real_ni.sql` líneas aprox. 148, 195, 458; `afs_valida_afi_plan_aportes.sql` líneas aprox. 70, 83, 112, 136; `afu_afi_plan_cubierto_x_ap.sql` línea aprox. 28; PB `exe/Source Infomedical/wp_solici.srw` líneas aprox. 12208 y 16798.
- Objetos SQL relevantes con referencia a `afi_plan_cubierto_por_aportes`: `afd_afiliados`, `afi_aju_inter_aporte_real`, `afi_genera_plan_cubierto_aportes`, `afi_graba_plan_cubierto_aportes`, `afs_genera_plan_cubierto_aportes`, `afs_valida_afi_plan_aportes`, `afu_afi_plan_cubierto_x_ap`, `afu_ingre_vigencias`, `afu_soli_plan_cubierto_x_ap`.
- Tablas revisadas: `afiliados`, `afi_histo_origenes`, `afi_clase`, `afi_planes`, `afi_tarifas`, `afi_plan_cubierto_por_aportes`, `afi_deriv_aportes`, `aportes`, `aportes_ajustes`, `aju_internos`, `aju_inter_items`, `comprobantes`, `compro_items`, `compro_tipos`, `factu_conceptos`, `param_afi`, `aporte_planes`, `tarifas`, `subcta_planes`, `subcta_contratos`.
- Consultas importantes usadas/incluidas al usuario: SELECTs sobre contrato `203119` en `afiliados`, `afi_histo_origenes`, `afi_planes`, `afi_tarifas`, `afi_plan_cubierto_por_aportes`, ejecución de `afs_valida_afi_plan_aportes` para fechas 20260601/20260701/20260801, SELECT de `aportes`, SELECT de `comprobantes` + `compro_items` + `factu_conceptos`, SELECT de `aju_internos` + `aju_inter_items` + `aportes_ajustes`.
## 2026-08-27 10:10 ART - Diagnóstico Redmine R30292 Prevención

- Usuario: Omar. Conversación por Google Chat directo.
- Tarea realizada antes de compactación: diagnóstico solo lectura del Redmine R30292, cliente Prevención, base obligatoria `prevencion_entrega`, contrato/afiliado `203119`. Restricciones explícitas cumplidas: no modificar datos, Redmine, SVN ni código.
- Conclusión entregada: `COMPORTAMIENTO ESPERADO` según código/configuración vigente; no se encontró evidencia de defecto para el caso analizado.
- Regla reconstruida: la facturación ejecuta `afi_aju_inter_aporte_real`; para contrato origen `R` y clase `D` aplica aportes reales. Si el afiliado está cubierto por aportes, `afi_genera_compro_compro` valida con `afs_valida_afi_plan_aportes` y puede llamar a `afi_genera_plan_cubierto_aportes`. La FC/ND puede quedar en cero con bonificación y aporte aplicado; el excedente de aportes genera NI y una DI espejo `PCA` para cancelar el excedente interno. La rama especial `afi_aju_inter_aporte_real_ni` con `apo_ni_fac = S` evalúa saldo: saldo 0 no genera NI y pasa a consumible; aporte <= saldo genera ajuste total; aporte > saldo limita al saldo y deja resto consumible. En contrato 203119 los aportes revisados tenían `apo_ni_fac = N`.
- Rol de `afi_plan_cubierto_por_aportes`: marca por contrato/vigencia/prepaga para considerarlo cubierto por aportes. `afs_valida_afi_plan_aportes` demuestra que la cobertura puede venir por `aporte_planes`, por `afi_plan_cubierto_por_aportes`, por tarifa cubierta o por subcuenta cubierta. Para 203119 aplica el registro específico en `afi_plan_cubierto_por_aportes` vigente desde `2026-07-01`, `cubierto = S`, sin baja.
- Situación contrato 203119: titular `ARISTIMUÑO VALLEJOS MORENA`; origen vigente `R`; clase vigente `D`; plan vigente desde `2026-07-01`: `ON DEMAND`; tarifa `833`; validación `afs_valida_afi_plan_aportes`: `2026-06-01 = N`, `2026-07-01 = S`, `2026-08-01 = S`. `ON DEMAND` no estaba en `aporte_planes`; tarifa `833` no tenía `cubierto = S`; `subcta_planes` para `ON DEMAND` tenía `cance_excedente = N`; activación real por `afi_plan_cubierto_por_aportes`.
- Cambio de plan: historial `2021-04-01 A1 JOVEN tarifa 34 solicitud 869491`; `2024-12-01 BASICO tarifa 01 solicitud 2432470`; `2026-07-01 ON DEMAND tarifa 833 solicitud 3542397`. Incidencia temporal: coincide con inicio de cobertura por aportes, pero no se demostró que el plan ON DEMAND por sí solo active la regla.
- Trazabilidad 08/2026: FC `B000206852486`; cuota `CUO 96.037,90`; bonificación `BEX -19.207,58`; aporte aplicado `AP -76.830,32`; IVA FC `0,00`; total FC `0,00`. Aporte real `10137506`: neto `104.346,85`, comisión `11.478,15`, facturable `92.868,70`, aplicado FC `76.830,32`, excedente `16.038,38`. NI `B000100465602`: `AP -16.038,38`, IVA 10,5% `-1.684,03`, total `-17.722,41`. DI `B000100290186`: `PCA 16.038,38`, IVA `1.684,03`, total `17.722,41`.
- Trazabilidad 07/2026: ND `B000200292661` total `0,00`; NI `B000100458153` total `-18.390,26`; DI `B000100288858` total `18.390,26`.
- Evidencia de código citada: `/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_generar_compro_masiva.sql` línea aprox. 900; `afi_generar_compro_individual.sql` línea aprox. 409; `afi_genera_compro_compro.sql` líneas aprox. 316, 320, 331; `afi_aju_inter_aporte_real_ni.sql` líneas aprox. 148, 195, 458; `afs_valida_afi_plan_aportes.sql` líneas aprox. 70, 83, 112, 136; `afu_afi_plan_cubierto_x_ap.sql` línea aprox. 28; PB `exe/Source Infomedical/wp_solici.srw` líneas aprox. 12208 y 16798.
- Objetos SQL relevantes con referencia a `afi_plan_cubierto_por_aportes`: `afd_afiliados`, `afi_aju_inter_aporte_real`, `afi_genera_plan_cubierto_aportes`, `afi_graba_plan_cubierto_aportes`, `afs_genera_plan_cubierto_aportes`, `afs_valida_afi_plan_aportes`, `afu_afi_plan_cubierto_x_ap`, `afu_ingre_vigencias`, `afu_soli_plan_cubierto_x_ap`.
- Tablas revisadas: `afiliados`, `afi_histo_origenes`, `afi_clase`, `afi_planes`, `afi_tarifas`, `afi_plan_cubierto_por_aportes`, `afi_deriv_aportes`, `aportes`, `aportes_ajustes`, `aju_internos`, `aju_inter_items`, `comprobantes`, `compro_items`, `compro_tipos`, `factu_conceptos`, `param_afi`, `aporte_planes`, `tarifas`, `subcta_planes`, `subcta_contratos`.
- Consultas importantes usadas/incluidas al usuario: SELECTs sobre contrato `203119` en `afiliados`, `afi_histo_origenes`, `afi_planes`, `afi_tarifas`, `afi_plan_cubierto_por_aportes`, ejecución de `afs_valida_afi_plan_aportes` para fechas 20260601/20260701/20260801, SELECT de `aportes`, SELECT de `comprobantes` + `compro_items` + `factu_conceptos`, SELECT de `aju_internos` + `aju_inter_items` + `aportes_ajustes`.

## 2026-08-27 17:00 ART - Análisis PowerBuilder `wp_solici.srw` / aportes familiares

- Usuario: Emanuel. Conversación por Google Chat directo.
- Tarea: análisis solo lectura del evento `ue_consulta` de la ventana `wp_solici.srw` en rama SVN `mromano-britanico-2026.05`, sin modificar archivos. Solicitud analizada: `1380510`, `soli_moti = 13`; asociado/contra mencionado: `25070117`. Usuario autorizó consultar `prevencion_prod` si hacía falta, pero no se consultó BD porque el código explicó el comportamiento y el profile `prevencion_prod` no estaba registrado en `DB_PROFILE.md`.
- Conclusión entregada: en `ue_consulta`, para solicitud existente con `soli_moti = 13`, `idw_aportes2` NO se mezcla con `d_afi_aportes_soli`. El `Retrieve(ll_soli)` de `idw_aportes2` recupera solamente desde `d_soli_deriv_aportes_fami`.
- Flujo en `ue_consulta`: `idw_aportes2` apunta a `fami.t_aportes_fami.dw_aportes_fami`, DataObject `d_soli_deriv_aportes_fami`. Al consultar trae `soli_moti` desde `solicitudes`, dispara `ue_cambia_moti`, y luego hace los retrieves de la solicitud. Para `soli_moti = 13` entra al `CASE 1, 5, 10, 14, 15, 16, 17, 2, 13` y ejecuta `idw_aportes2.retrieve(ll_soli)`.
- Bloque descartado: el bloque "Busco Aportes Reales" que usa `d_afi_aportes_soli` está protegido por `IF li_soli_moti <> 13 AND IsNull(ll_soli) THEN`, por lo que para `soli_moti = 13` no entra.
- DataWindow `d_soli_deriv_aportes_fami`: consulta `soli_afiliados` + `soli_deriv_aportes`, filtrando por `soli = :ksoli`, `paren <> 'T'` y `soli_deriv_aportes.baja_fecha is null`. Para la solicitud `1380510`, si el select principal trae una sola fila, las otras filas visibles no vienen de este retrieve.
- Dónde ocurre la mezcla real: fuera del camino de consulta de una solicitud ya existente. Ocurre en `wf_afiliado`, disparado al cargar/validar afiliado desde `idw_titu.itemchanged` cuando el motivo es mayor a 1, incluido 13.
- Detalle de mezcla en `wf_afiliado`: primero llama `wf_busca_familiar(...)`; `wf_busca_familiar` resetea `idw_aportes2`, recupera `d_afi_reales`, y hace `idw_aportes2.of_append(ds_afi)` para crear filas base de familiares con datos de integrante, apellido, nombre, parentesco, etc. Luego `wf_afiliado` recupera `d_afi_aportes_soli`, que lee desde `afi_deriv_aportes` + `afiliados`; filtra `paren <> 'T'` y copia campo a campo sobre `idw_aportes2`, buscando por `inte`. Si hay más de un aporte para el mismo integrante, inserta una fila adicional.
- Interpretación funcional: las filas que coinciden con `select * from afi_deriv_aportes where contra = '25070117'` no provienen del `Retrieve(ll_soli)` de `d_soli_deriv_aportes_fami`; provienen del flujo de carga del afiliado real (`wf_afiliado`) que toma `d_afi_aportes_soli` y completa/agrega filas en `idw_aportes2`.
- Referencias citadas al usuario: `wp_solici.srw` aprox. líneas 531, 547, 729, 768, 772, 9703, 9724, 12966, 13095, 13104, 13111, 13115, 16792, 20160, 22622; `d_soli_deriv_aportes_fami.srd` aprox. línea 28; `d_afi_aportes_soli.srd` aprox. línea 21.

## 2026-08-27 21:22 ART - Continuación análisis `wp_solici.srw` / botón `pb_borrar` en derivación de aportes

- Usuario: Emanuel. Conversación por Google Chat directo. Trabajo solicitado y realizado en modo solo lectura, sin modificar archivos.
- Contexto: rama SVN `mromano-britanico-2026.05`, ventana PowerBuilder `exe/Source Infomedical/wp_solici.srw`, pestaña 4 `Derivación de Aportes`, DataWindow `idw_aportes2` / `d_soli_deriv_aportes_fami`, solicitud ejemplo `1380510`, motivo `soli_moti = 13`, asociado/contra `25070117`.
- Análisis de `pb_borrar`: `pb_borrar.clicked` llama `ue_eliminar`. En `ue_eliminar`, `fami.SelectedTab = 4` opera sobre `idw_aportes2`. Si la fila está `New!` o `NewModified!`, ejecuta `idw_aportes2.DeleteRow(li_fi)`. Si la fila ya existía, no la pasa al buffer `DELETE!`: setea `baja_fecha` en la fila (`1900-01-01` si `vigen_desde > apli_fecha`, si no `today()`). Si no queda otra fila del mismo `item`, inserta una fila base del familiar para mantener renglón visible.
- Guardado: al presionar Aceptar, `ue_grabar` llama `wf_soli_grabar_deriv_aportes(...)`. Para motivos `1, 13, 17, 10, 2`, recorre TODAS las filas de `idw_aportes2`; para `soli_moti = 13` llama a `wf_grabar_deriv_aportes` cuando la fila está `NewModified!` o `DataModified!`. No graba solo la fila seleccionada.
- `wf_grabar_deriv_aportes` envía `baja_fecha` a `afu_soli_deriv_aportes` como `@prm_baja_fecha`. El manejo por `DeletedCount()` de `ue_grabar` aplica a `idw_aportes` e `idw_aportes_emp`, no a `idw_aportes2`.
- Diagnóstico del error de clave duplicada: el SP `afu_soli_deriv_aportes` busca existencia con `soli + item + vigen_desde + obra_soc`, pero la PK real vigente de `soli_deriv_aportes` quedó como `soli + item + vigen_desde` según script R29323 `backend/data/script_separar_tablas.sql`. Si recibe una fila con misma `soli/item/vigen_desde` pero `obra_soc` distinta, no la encuentra por el `EXISTS`, intenta `INSERT` y choca contra la PK real.
- Datos de ejemplo citados: en `soli_deriv_aportes` para solicitud `1380510` se observó fila familiar `item=2`, `vigen_desde=2026-08-01`, `obra_soc=249`. En `afi_deriv_aportes` para contra `25070117`, integrante `01`, hay dos filas activas con igual vigencia: `obra_soc=249` y `obra_soc=655`. La `655` fue tratada por el usuario como inconsistencia de datos productiva.
- Recomendación funcional entregada: si el caso es inconsistencia productiva, no conviene que una fila de `afi_deriv_aportes` se mezcle en `idw_aportes2` como fila operable de solicitud. `idw_aportes2` cumple dos roles incompatibles: muestra padrón real (`afi_deriv_aportes`) y actúa como buffer transaccional de `soli_deriv_aportes`. Para solicitudes existentes `soli_moti=13`, se aconsejó poblarla solo con `d_soli_deriv_aportes_fami`; si se muestran aportes reales, hacerlo como consulta/otra DW o con marca de origen no grabable. Agregar defensa para que filas sin origen real en `soli_deriv_aportes` no ejecuten `afu_soli_deriv_aportes`.
- Lugar exacto de mezcla que explica por qué comentar `of_append` de `ue_consulta` no alcanza: el `of_append` directo en `ue_consulta` está protegido por `IF li_soli_moti <> 13 AND IsNull(ll_soli) THEN`, así que para solicitud existente `soli_moti=13` no debería ejecutar. La carga problemática ocurre principalmente en `wf_afiliado`, alrededor de líneas aprox. 13095-13160: recupera `d_afi_aportes_soli`, filtra `paren <> 'T'`, busca familiar por `inte`, pisa columnas de `idw_aportes2` con datos de `afi_deriv_aportes` (`obra_soc`, `vigen_desde`, etc.) y, si hay más de un aporte para el mismo `inte`, inserta otra fila en `idw_aportes2`.
- Detalle adicional: `d_afi_aportes_soli.srd` ordena solo por `inte DESC`, por lo que para el asociado `25070117` puede traer primero `obra_soc=655` y luego `249`. `wf_afiliado` pisa la fila original de solicitud con el primer aporte real que encuentra y luego inserta otra fila por el segundo aporte del mismo integrante. Así la grilla puede quedar visualmente con `655` y `249`, pero internamente varias filas quedan modificadas.
- Explicación al usuario sobre prueba donde borra cualquiera de las dos filas y siempre falla: Aceptar no envía únicamente la fila seleccionada; recorre toda `idw_aportes2` y manda todas las filas `NewModified!`/`DataModified!`. Aunque el usuario seleccione la fila `249`, la fila `655` puede haber quedado modificada por la contaminación de `wf_afiliado` y se intenta grabar igual, disparando el error de clave duplicada.
- Referencias citadas al usuario: `wp_solici.srw` aprox. líneas 19431 (`pb_borrar.clicked`), 17830-17852 (`ue_eliminar`, case pestaña 4), 19305/3317/3755-3762 (`Aceptar`/`ue_grabar`/recorrido de `idw_aportes2`), 14419-14519 (`wf_grabar_deriv_aportes` y llamada a SP), 13095-13160 (`wf_afiliado` carga desde `d_afi_aportes_soli`), 13104 filtro `paren<>'T'`, 13114 `insertrow`, 13125 y 13139 asignación de `obra_soc`; `d_afi_aportes_soli.srd` aprox. línea 46 (`ORDER BY afi_deriv_aportes.inte DESC`); `backend/procedures/afu_soli_deriv_aportes.sql` aprox. línea 61; `backend/data/script_separar_tablas.sql` aprox. línea 147.
