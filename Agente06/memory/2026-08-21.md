# Memoria 2026-08-21

## Redmine R30241 - Prevencion
- Omar solicito trabajar exclusivamente con Redmine R30241, cliente validado Prevencion, base `prevencion_entrega`, SVN `ogomez-Prevencion-trunk` (`Prevencion/trunk`), carpeta `/mnt/Agentes/ogomez/TICKETS/R30241`; sin modificar codigo, commits, base ni estado de Redmine.
- Se valido que el proyecto real del Redmine R30241 es `Soporte Prevencion Salud`, compatible con cliente Prevencion.
- Se genero documentacion: `/mnt/Agentes/ogomez/TICKETS/R30241/AnalisisDefecto.docx` e `/mnt/Agentes/ogomez/TICKETS/R30241/InformeInteraccionBettina.docx`.
- Resultado del analisis: defecto confirmado acotado a la BEX 07/2026 incluida indebidamente. El punto de "IVA ausente" no quedo confirmado porque el item `CUO 06/2026` tenia `iva_item` calculado y la FC tenia `iva_total`. La BEX 06/2026 quedo como expectativa pendiente de validacion funcional.
- Sobre existencia de BEX fuera de `afi_subsidios`: en `prevencion_entrega`, ademas de `afi_subsidios`, BEX aparece en `factu_conceptos` (`BEX = Bonificacion Exclusiva`, vigente, `concep_sub_espe = S`), `param_afi` (`factu_concep_sub_esp = BEX`), `subsi_tipos` (varios tipos apuntan a `factu_concep = BEX`; para contrato 251604 aplica `subsi_tipo = 350`), `prefacturas` (registros `factu_concep = BEX`) y `compro_items` (items `factu_concep = BEX`). No se encontraron BEX en `afi_bonificaciones` ni `subcta_bonificaciones` para el caso.
- Aclaracion importante: `afi_subsidios` no guarda el texto `BEX`, guarda `subsi_tipo`; para contrato 251604 el vinculo es `afi_subsidios.subsi_tipo = 350` -> `subsi_tipos.factu_concep = BEX`.
- Omar pregunto como llegaron los registros BEX a `prefacturas` si en `afi_subsidios` no veia registros para el contrato. Se determino que llegaron por el circuito de subsidios especiales en `afi_generar_prefactura_xafi`: cruza `prefacturas` con `afi_subsidios` y `subsi_tipos`, exige `st.sub_esp = 'S'`, toma `factu_concep` desde `subsi_tipos` y lo inserta en `prefacturas.factu_concep`.
- Para contrato `251604` se encontro registro vigente en `afi_subsidios`: `subsi_tipo 350`, `vigen_desde 01/08/2026`, `baja_fecha NULL`; en `subsi_tipos`: `350 | Bonificacion Exclusiva | sub_esp S | factu_concep BEX`.
- Punto fino detectado: para retroactivos junio/julio aparecen BEX aunque `subsi_dto` queda `0.00`, porque el SP tambien suma `@valor_ant` de prefacturas ya generadas previamente; pueden venir arrastradas por valores previos durante refacturacion/reversion, no solo por porcentaje actual.
