# Memoria 2026-08-10

- Usuario en Google Chat directo: Bettina. Prefiere respuestas técnicas directas, con foco en análisis funcional/técnico PowerBuilder, Redmine, SQL Server y presupuestación.
- Trabajo completado R30161: usando credencial `bwilvers` se consultó Redmine 30161, se descargaron adjuntos visibles, se contrastó contra repositorio `ogomez-Prevencion-trunk` y base/profile `prevencion_entrega`. Se generó `/mnt/Agentes/bwilvers/R30161.pdf`. Nota registrada: el adjunto solicitado `R30161 BORRADOR Especificación Funcional.pdf` no estaba expuesto en Redmine ni en `/mnt/Agentes/bwilvers`; el informe lo asentó.
- Trabajo completado R30212: usando credencial `bwilvers` se consultó Redmine 30212, se revisó imagen adjunta, se contrastó contra `ogomez-Prevencion-trunk` y `prevencion_entrega`. Se generó `/mnt/Agentes/bwilvers/R30212.pdf` de 9 páginas. Conclusión principal: no parece ser un problema de límite por monto; hipótesis más fuerte: el proceso no encuentra registros facturables en `tmp_ajustes_y_prefacturas` para el `tipo_factu` esperado y retorna sin `recha`, por eso la ventana muestra solo "Comprobante no generado".

- Conversacion con Marcelo en Google Chat directo sobre repo SVN `mromano-britanico-2026.07` y requerimiento "cancelacion de deuda con aportes". Se analizo `backend/procedures/afu_compro_impor_proce.sql`: procesa cobranzas importadas desde `compro_importacion`, genera comprobante tipo recibo/cobro, registra pago y aplica contra deuda mediante logica de `afu_aplicar_compro`/`afu_aplicar_compro_item`; sirve como patron crear credito + aplicar + registrar resultado, pero no cancela aportes.
- Criterio funcional acordado para nuevo SP de cancelacion de deuda con aportes: origen `aportes`, tomar aportes disponibles con `impor_facturable > 0` y `baja_fecha IS NULL`; no usar `impor_consumible` como disponible para este proceso. Al aplicar, mover importe desde `impor_facturable` hacia `impor_facturado`; marcar `proce_fecha` solo si queda sin saldo facturable. Pendiente confirmar si comision integra la ecuacion del neto.
- Diseño propuesto del nuevo SP: leer deuda vencida y vigente del afiliado/cliente, leer aportes facturables elegibles, generar NC con items (`item > 0`, no `pago_item > 0`), aplicar contra FC/deuda insertando aplicacion y ajustando saldos, y dejar trazabilidad aporte-NC para evitar reutilizacion. La similitud con cobranzas esta en el tramo de imputacion, no en origen/tipo de comprobante.
- Regla adicional incorporada por caso experimental: el proceso corre luego del vencimiento de comprobantes emitidos y antes de la facturacion siguiente. Ejemplo afiliado 123 grupo emision 3: 20/02 factura periodo 03/26 FC $500.000 sin aportes, 05/03 se importan aportes 12/25 disponibles desde 02/26 por $500.000, 15/03 vence FC sin pago, 20/03 proceso genera NC $500.000 y deja saldo 0, 25/03 factura periodo 04/26.
- Punto clave para ampliar documento/solucion: no alcanza con `impor_facturable > 0`; el nuevo SP debe aplicar las mismas reglas de uso de aportes que la facturacion normal. Referencia vista en `afi_genera_aportes_ajustes`: `apo.periodo_aporte <= @prm_factu_perio`, orden por `periodo_aporte ASC`, validacion de planes contra `APORTE_PLANES`, vigencia de plan del afiliado y diferencias aporte real/estimado.
- Se genero Word con analisis consolidado en `/mnt/Agentes/mepaduan/Britanico/Analisis-IA/Analisis_cancelacion_deuda_con_aportes.docx`. Luego se concluyo que el documento base cumple la idea central, pero debe ampliarse con reglas de deuda vencida y reglas normales de uso de aportes para cubrir el caso experimental.
