# Memoria de conversación — Omar Gomez

## Datos

- **Fecha:** 16/09/2026
- **Usuario:** Omar Gomez
- **ID Google Chat:** `users/108736509699389549831`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 19
- **Inicio:** 09:20:32
- **Fin:** 17:22:30

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/vaokdyaaaae`

## Archivos revisados

- `cdf770d9-43ae-4bca-8e97-aaa89ef550fc.jsonl`

---

## Transcripción completa

### [16/09/2026 09:20:32] Omar Gomez

buenas

---

### [16/09/2026 09:21:37] Agente

Hola Omar, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [16/09/2026 11:17:03] Omar Gomez

Leer y aplicar obligatoriamente las reglas definidas en:
/mnt/Agentes/ogomez/preferencias/preferencias.md

Número de Redmine:
R30389

Trabajar exclusivamente con el Redmine R30389.
Leer el Redmine R30389, obtener el nombre real del proyecto y validar que pertenece al cliente seleccionado.
Cliente seleccionado y validado:
Prevención
El proyecto detectado debe contener alguno de estos textos para considerarse valido:
Prevención, Prevencion
Si el proyecto detectado no coincide con el cliente seleccionado, detener el procedimiento, informar la diferencia, no utilizar base ni rama y solicitar correccion del cliente.

Proceso solicitado:
Análisis de Defecto
Ejecutar el procedimiento definido en:
/mnt/Agentes/ogomez/PROMPTS/02_ANALISIS_DEFECTO.md

Base de datos seleccionada:
Utilizar la base de datos definida para este cliente en preferencias.md.

Rama SVN seleccionada:
Utilizar la rama SVN definida para este cliente en preferencias.md.

Utilizar exclusivamente los recursos seleccionados para este analisis, salvo que el propio procedimiento indique expresamente que debe realizarse una comparacion adicional.
La carpeta de trabajo será:
/mnt/Agentes/ogomez/TICKETS/R30389

Guardar la documentación en:
/mnt/Agentes/ogomez/TICKETS/R30389

Si la carpeta no existe, crearla.
La ausencia de esta carpeta no constituye un error y no debe detener el proceso.
Si existen contexto.json, R16.docx, AnalisisDefecto.docx, AnalisisFuncional.docx u otra documentación previa en la carpeta del ticket, reutilizarlos cuando corresponda. Si no existen, continuar según el procedimiento seleccionado.
Todas las referencias a RXXXXX, <NUMERO_REDMINE> y <RUTA_TICKET> deberán resolverse utilizando R30389 y su carpeta correspondiente.

Observaciones adicionales de Omar:
Sin observaciones adicionales.

No modificar codigo fuente.
No realizar commits.
No modificar informacion de base de datos.
No cambiar el estado del Redmine salvo autorizacion expresa.

---

### [16/09/2026 11:26:54] Agente

Omar, terminé el análisis del Redmine `R30389` y dejé generado el documento:

`/mnt/Agentes/ogomez/TICKETS/R30389/AnalisisDefecto.docx`

Resultado del análisis: **DEFECTO NO CONFIRMADO**.

Punto central: el error reportado es consistente con que no existe numerador `FC/X` para ente `1`, punto `0001`, pero en la base seleccionada `prevencion_entrega` el contrato `471059` calcula actualmente letra `B`, no `X`. Por eso no cerré causa raíz como defecto de código ni recomendé modificación técnica.

Validaciones realizadas: Redmine leído, proyecto validado como Prevención, adjunto revisado, SVN/base de Prevención usados en solo lectura, código y SPs analizados. No modifiqué código, no hice commits, no modifiqué base y no cambié el estado del Redmine.

---

### [16/09/2026 15:49:06] Omar Gomez

Pre-compaction memory flush. Store durable memories only in memory/2026-09-16.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-16.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-16-HHMM.md); always use the canonical 2026-09-16.md filename. If nothing to store, reply with NO_REPLY.
Current time: Wednesday, September 16th, 2026 - 3:49 PM (America/Buenos_Aires)
Reference UTC: 2026-09-16 18:49 UTC

---

### [16/09/2026 15:49:54] Agente

NO_REPLY

---

### [16/09/2026 15:49:54] Omar Gomez

necesito que reveas el caso del redmine 30389 ya con la imagen que acompaño el error si se reproduce. En la base indicada anteriormente.

---

### [16/09/2026 16:00:19] Agente

Omar, reanalicé el caso con la imagen nueva y actualicé la documentación en:

`/mnt/Agentes/ogomez/TICKETS/R30389/AnalisisDefecto.docx`

También quedó actualizado el Markdown fuente:

`/mnt/Agentes/ogomez/TICKETS/R30389/AnalisisDefecto.md`

Conclusión corregida: **DEFECTO CONFIRMADO**.

El punto clave cambió: con la reproducción confirmada, el sistema efectivamente llega a pedir numeración `FC/X`, pero en `prevencion_entrega` el contrato `471059` resuelve letra fiscal `B` para el período/fecha del caso. Por eso la falta de numerador `FC/X` es el síntoma inmediato, no la solución.

Validé en solo lectura. No modifiqué código fuente, no hice commits, no modifiqué base y no cambié el estado del Redmine.

---

### [16/09/2026 16:14:05] Omar Gomez

Continuá el análisis del defecto R30389. No necesito nuevamente el análisis funcional ni la explicación de por qué FC/X es incorrecto. Eso ya está confirmado.
Necesito reproducir el error completamente por SQL, fuera de PowerBuilder.
Revisá wr_ajustes_gen_comprob.srw y uo_nv_factu.sru para determinar exactamente qué llamada realiza la aplicación al generar el comprobante del ajuste 1244827.
Revisá la firma completa de afi_generar_compro_individual y preparame un script SQL ejecutable con TODOS los parámetros reales correspondientes al caso:

prepaga = 1
contrato = 471059
ajuste = 1244827
período = 09/2026
punto = 0001
y cualquier otro parámetro que obtenga la aplicación.
El script debe ejecutar el mismo circuito que ejecuta el botón "Generar comprobante". Quiero poder ejecutarlo en SQL Server y reproducir el error FC/X.
IMPORTANTE: envolvé la prueba en BEGIN TRAN / ROLLBACK o indicame una forma segura de ejecutarla sin persistir el comprobante ni modificar definitivamente información.


Después descomponé la ejecución SP por SP:

afi_generar_compro_individual

→ afi_genera_compro

→ afi_genera_compro_compro

→ afu_obtener_nro_compro

→ afs_afi_es_gravado
Para cada salto indicame los valores concretos que se pasan para:

@prepaga, @contra, @cuenta, @subcta, @perio, @compro_tipo, @punto, @letra y cualquier otro parámetro que intervenga en determinar la condición fiscal.


Necesito que determines exactamente EN QUÉ SP y EN QUÉ LÍNEA/INSTRUCCIÓN se pierde o deja de resolverse la letra B.
Sabemos que ejecutando directamente afs_afi_es_gravado para prepaga 1 / contrato 471059 devuelve letra B. Por lo tanto, compará los parámetros de esa ejecución correcta contra los parámetros con los que se lo invoca desde el flujo completo.

Si no podés determinarlo solamente mediante análisis estático, preparame scripts SQL de diagnóstico que yo pueda ejecutar manualmente para cada nivel del flujo, mostrando los parámetros y resultados intermedios hasta encontrar exactamente dónde B pasa a NULL y posteriormente a X.

NO modifiques ningún SP, código PowerBuilder ni datos. NO hagas commits. Por ahora solo quiero:
SQL exacto para reproducir el error;
SQL de diagnóstico;
punto exacto donde se pierde la letra;
causa técnica;
objeto que posteriormente debería modificarse.

---

### [16/09/2026 16:27:53] Agente

Omar, continué el análisis y dejé los scripts en la carpeta del ticket:

- `/mnt/Agentes/ogomez/TICKETS/R30389/R30389_repro_error.sql`
- `/mnt/Agentes/ogomez/TICKETS/R30389/R30389_diagnostico_letra.sql`

Reproduje por SQL el error completo, fuera de PowerBuilder, con el mismo circuito del botón. El `EXEC dbo.afi_generar_compro_individual` con:

```sql
@prepaga = 1
@contra = '471059'
@cuenta = NULL
@subcta = NULL
@fecha = '20260910'
@factu_perio = '20260901'
@compro_tipo = 'NC'
@punto = '0001'
@punto_factura_elec = NULL
@aju = 1244827
@aju_cta = 0
@vto_grupo = NULL
@facturacion = NULL
@proceso_origen = 'FI'
```

reproduce:

```text
No se encontro el numero del comprobante para el ente :1 pto: 0001 compro_tipo: FC letra: X
```

Punto exacto donde se pierde la letra: no se pierde en el primer comprobante del ajuste. Ese primer tramo genera `NC/B` correctamente. El error aparece después, en el tramo de aportes internos llamado desde `afi_generar_compro_individual` mediante `afi_aju_inter_aporte_real`.

La pérdida concreta está en `afi_genera_compro`, bloque de generación por cuenta `tipo_factu = E`: filtra la temporal con `@cur_cuenta/@cur_subcta`, pero llama a `afi_genera_compro_compro` pasando `@cuenta = @cuenta`, `@subcta = @subcta` y `@contra = NULL`. En este caso esos `@cuenta/@subcta` originales llegan `NULL`, aunque la fila temporal activa tiene `cuenta = 253`, `subcta = 1`.

Líneas clave:
- `afi_genera_compro.sql`: llamada problemática a `afi_genera_compro_compro` en el bloque `tipo_factu = E`, con `@contra = NULL` y sin pasar `@cur_cuenta/@cur_subcta`.
- `afi_genera_compro_compro.sql`: llama a `afu_obtener_nro_compro` con esos valores.
- `afu_obtener_nro_compro.sql`: al no obtener letra desde `afs_afi_es_gravado`, aplica fallback `ISNULL(@letra, 'X')`.

Causa técnica: en el segundo comprobante derivado de aportes, el flujo deja de resolver fiscalidad por contrato `471059` y tampoco pasa correctamente la cuenta/subcuenta efectiva de la temporal. Por eso `afs_afi_es_gravado` no puede devolver `B`, `afu_obtener_nro_compro` cae a `X`, y termina buscando numerador `FC/X`.

Objeto que debería modificarse después: `afi_genera_compro`, en el bloque que procesa `tipo_factu = E`, para pasar a `afi_genera_compro_compro` los parámetros efectivos `@cur_cuenta/@cur_subcta` o conservar un dato fiscal válido equivalente. No modifiqué SPs, código PowerBuilder, datos ni hice commits.

---

### [16/09/2026 16:35:44] Omar Gomez

Con la causa raíz ya identificada para R30389, prepará una propuesta de modificación mínima sobre afi_genera_compro, exclusivamente en el bloque tipo_factu = E.
Quiero que evalúes reemplazar en la llamada a afi_genera_compro_compro los parámetros originales @cuenta/@subcta por los valores efectivos de la fila en proceso @cur_cuenta/@cur_subcta.
No modifiques todavía el SP ni hagas commit.
Mostrame:
el código actual exacto de la llamada;
el código propuesto;
por qué esa modificación corrige específicamente R30389;
qué otras operatorias utilizan ese mismo bloque y podrían verse afectadas;
si @cur_cuenta/@cur_subcta pueden ser NULL en algún escenario válido;
qué regresiones deberían probarse;
preparame además una versión completa del SP modificado para que yo pueda probarla manualmente, pero NO la ejecutes ni la guardes en base.

---

### [16/09/2026 16:44:26] Omar Gomez

Con la causa raíz ya identificada para R30389, prepará una propuesta de modificación mínima sobre afi_genera_compro, exclusivamente en el bloque tipo_factu = E.
Quiero que evalúes reemplazar en la llamada a afi_genera_compro_compro los parámetros originales @cuenta/@subcta por los valores efectivos de la fila en proceso @cur_cuenta/@cur_subcta.
No modifiques todavía el SP ni hagas commit.
Mostrame:
el código actual exacto de la llamada;
el código propuesto;
por qué esa modificación corrige específicamente R30389;
qué otras operatorias utilizan ese mismo bloque y podrían verse afectadas;
si @cur_cuenta/@cur_subcta pueden ser NULL en algún escenario válido;
qué regresiones deberían probarse;
preparame además una versión completa del SP modificado para que yo pueda probarla manualmente, pero NO la ejecutes ni la guardes en base.

---

### [16/09/2026 16:50:08] Agente

Omar, preparé la propuesta sin modificar el SP original, sin ejecutar nada en base y sin commit.

Dejé la versión completa modificada para prueba manual en:

[/mnt/Agentes/ogomez/TICKETS/R30389/afi_genera_compro_R30389_propuesta.sql](/mnt/Agentes/ogomez/TICKETS/R30389/afi_genera_compro_R30389_propuesta.sql)

El SP original revisado es:

[/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_genera_compro.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_genera_compro.sql:1186)

**Código Actual**
Bloque `@prm_modo_fac_def = 'A'`, cursor `cur_tmp_aju_pref_cta`, `tipo_factu = 'E'`:

```sql
EXEC    dbo.afi_genera_compro_compro
        @proceso_origen     = @proceso_origen,
        @ente               = @ente,
        @prepaga            = @prepaga,
        @cuenta             = @cuenta,
        @subcta             = @subcta,
        @contra             = NULL,--@contra,
        @fecha              = @fecha,
        @factu_perio        = @factu_perio,
        @factu_perio_real   = @factu_perio_real,
        @punto              = @punto,
        @punto_factura_elec = @punto_factura_elec,
        @compro_tipo        = @compro_tipo          OUTPUT,
        @compro_nro         = @compro_nro           OUTPUT,
        @emi_grupo          = @emi_grupo,
        @vto_grupo          = @vto_grupo,
        @debug              = @texto,
        @recha              = @recha                OUTPUT,
        @proceso_ok         = @proceso_ok           OUTPUT,
        @prm_ori            = @prm_ori,
        @prm_clase_afi      = @prm_clase_afi,
        @prm_cur_cuenta     = @cur_cuenta,
        @prm_cur_subcta     = @cur_subcta,
        @prm_cur_contra     = NULL,--@cur_contra,
        @prm_cur_tipo_factu = @cur_tipo_factu,
        @prm_apli_perio     = @cur_apli_perio,
        @prm_modulo_fac     = @facturacion
```

**Código Propuesto**
Cambio mínimo: solo reemplazar los parámetros de cuenta/subcuenta usados para generar el comprobante.

```sql
EXEC    dbo.afi_genera_compro_compro
        @proceso_origen     = @proceso_origen,
        @ente               = @ente,
        @prepaga            = @prepaga,
        @cuenta             = @cur_cuenta,
        @subcta             = @cur_subcta,
        @contra             = NULL,--@contra,
        @fecha              = @fecha,
        @factu_perio        = @factu_perio,
        @factu_perio_real   = @factu_perio_real,
        @punto              = @punto,
        @punto_factura_elec = @punto_factura_elec,
        @compro_tipo        = @compro_tipo          OUTPUT,
        @compro_nro         = @compro_nro           OUTPUT,
        @emi_grupo          = @emi_grupo,
        @vto_grupo          = @vto_grupo,
        @debug              = @texto,
        @recha              = @recha                OUTPUT,
        @proceso_ok         = @proceso_ok           OUTPUT,
        @prm_ori            = @prm_ori,
        @prm_clase_afi      = @prm_clase_afi,
        @prm_cur_cuenta     = @cur_cuenta,
        @prm_cur_subcta     = @cur_subcta,
        @prm_cur_contra     = NULL,--@cur_contra,
        @prm_cur_tipo_factu = @cur_tipo_factu,
        @prm_apli_perio     = @cur_apli_perio,
        @prm_modulo_fac     = @facturacion
```

**Por Qué Corrige R30389**
En R30389, el bloque procesa una fila efectiva de `tmp_ajustes_y_prefacturas` con `tipo_factu = E`, `tipo = XCUENTAINTERNOS`, `cuenta = 253`, `subcta = 1`, pero la llamada actual manda a `afi_genera_compro_compro` los parámetros generales `@cuenta/@subcta`, que en este circuito llegan sin la cuenta efectiva.

Ahí se pierde la condición fiscal correcta: el flujo posterior no puede resolver la letra `B` con la cuenta/subcuenta real de la fila, termina sin letra y después cae al fallback `X`, generando la búsqueda de numerador `FC/X`.

El cambio hace que el comprobante se genere usando la misma cuenta/subcuenta que ya seleccionó el cursor para la fila en proceso: `@cur_cuenta/@cur_subcta`.

**Operatorias Afectadas**
El cambio impacta únicamente este bloque de `afi_genera_compro`:

- `@prm_modo_fac_def = 'A'`
- cursor `cur_tmp_aju_pref_cta`
- filas `tipo_factu = 'E'`
- generación por cuenta/subcuenta efectiva dentro de facturación mixta/afiliado con componente empresa

Las operatorias concretas alcanzadas son las que cargan temporales de cuenta en `afi_genera_compro_ajustes`, especialmente:

- `XCUENTA`
- `XCUENTAINTERNOS`

No toca el bloque empresa directo `@prm_modo_fac_def = 'E'` anterior, ni la generación por afiliado `tipo_factu = A`.

**NULL En @cur_cuenta / @cur_subcta**
Para las filas válidas de `XCUENTA` y `XCUENTAINTERNOS`, no deberían ser `NULL`: se insertan desde `AJ.cuenta` y `AJ.subcta`, con joins a `subcuentas` y `subcta_contratos`.

El cursor permite técnicamente traer filas con `cuenta/subcta NULL` porque el filtro contempla `OR cuenta IS NULL`, pero en este bloque eso sería dato temporal mal formado o una operatoria distinta no esperada para facturación por cuenta. Si apareciera, conviene que falle de forma visible antes que generar `FC/X`.

**Regresiones A Probar**
- R30389: ajuste `1244827`, contrato `471059`, período `09/2026`, punto `0001`, validando que ya no intente `FC/X`.
- Ajustes internos por cuenta con `tipo = XCUENTAINTERNOS`.
- Ajustes por cuenta comunes con `tipo = XCUENTA`.
- Caso con más de una cuenta/subcuenta en la misma corrida para confirmar que cada iteración usa su cuenta efectiva.
- Caso con `@var_nc_x_periodo = 'S'` para NC separada por período.
- Facturación individual que mezcle componentes `A` y `E`.
- Control de que `afi_genera_compro_compro_items` siga tomando los mismos filtros `@prm_cur_cuenta/@prm_cur_subcta`.

Validé que la copia propuesta conserva el perfil técnico del archivo: ISO-8859-1, sin BOM y finales CRLF. El archivo original no fue modificado.

---

### [16/09/2026 16:59:18] Omar Gomez

Pre-compaction memory flush. Store durable memories only in memory/2026-09-16.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-16.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-16-HHMM.md); always use the canonical 2026-09-16.md filename. If nothing to store, reply with NO_REPLY.
Current time: Wednesday, September 16th, 2026 - 4:59 PM (America/Buenos_Aires)
Reference UTC: 2026-09-16 19:59 UTC

---

### [16/09/2026 17:00:59] Agente

NO_REPLY

---

### [16/09/2026 17:01:00] Omar Gomez

Antes de probar la propuesta de modificación necesito validar el origen y significado funcional de cuenta = 253 / subcta = 1.
El contrato 471059 está en afi_clase como clase D y tiene cuenta = NULL, subcta = NULL.
Vos identificaste que durante el segundo tramo existe una fila de tmp_ajustes_y_prefacturas:
tipo_factu = E

tipo = XCUENTAINTERNOS

cuenta = 253

subcta = 1
y mencionás que esos valores se insertan desde AJ.cuenta / AJ.subcta.
Necesito que traces esa fila hacia atrás y me demuestres exactamente de dónde salen 253/1.
Indicame:
qué objeto/SP inserta esa fila concreta en tmp_ajustes_y_prefacturas;
cuál es el AJ concreto que origina esa fila y de qué tabla proviene;
dame el SELECT de solo lectura para consultar ese registro y comprobar que contiene cuenta 253/subcta 1;
qué representa funcionalmente cuenta 253/subcta 1 y qué relación tiene con el ajuste 1244827, concepto APE y contrato 471059;
por qué un afiliado clase D con cuenta/subcta NULL termina generando una fila XCUENTAINTERNOS para 253/1;
verificá qué representan conceptualmente los parámetros @cuenta/@subcta y @prm_cur_cuenta/@prm_cur_subcta dentro de afi_genera_compro_compro. Quiero saber por qué actualmente existen ambos pares de parámetros y si tienen finalidades diferentes;
demostrá mediante código que reemplazar @cuenta/@subcta por @cur_cuenta/@cur_subcta es semánticamente correcto y no solamente que hace desaparecer el error de R30389.
Por ahora no modifiques ni ejecutes la propuesta.

---

### [16/09/2026 17:09:55] Agente

Omar, revisé la traza y tengo que corregir la hipótesis anterior: **no pude demostrar que `253/1` salga del ajuste `1244827` ni del contrato `471059`**. Con la evidencia actual, esa asociación no es válida.

**Evidencia**
En el botón, [wr_ajustes_gen_comprob.srw](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/exe/Source%20Infomedical/wr_ajustes_gen_comprob.srw:108) llama:

```powerscript
factu.of_gene_compro_individual(
    dw_1.object.prepaga[1],
    dw_1.object.contra[1],
    ll_null,
    ll_null,
    ls_punto,
    ls_punto_factura_elec,
    ld_perio,
    gd_hoy,
    li_null,
    str,
    ll_aju,
    0,
    ls_null
)
```

O sea: `cuenta = NULL`, `subcta = NULL`.

El ajuste real es:

```sql
SELECT A.aju, A.prepaga, A.contra, A.apli_perio, A.proce_fecha, A.compro_tipo, A.compro_nro, A.obser, A.debi_credi
FROM ajustes A
WHERE A.aju = 1244827

SELECT AI.aju, AI.item, AI.factu_concep, AI.valor, AI.alta_fecha, AI.baja_fecha
FROM aju_items AI
WHERE AI.aju = 1244827
```

Resultado validado:
- `ajustes.aju = 1244827`
- `prepaga = 1`
- `contra = 471059`
- `apli_perio = 09/2026`
- `factu_concep = APE`
- `valor = 177877.35`
- `debi_credi = -1`

El contrato:

```sql
SELECT AC.prepaga, AC.contra, AC.clase_afi, AC.cuenta, AC.subcta, AC.vigen_desde, AC.baja_fecha
FROM afi_clase AC
WHERE AC.prepaga = 1
AND AC.contra = '471059'
ORDER BY AC.vigen_desde DESC
```

Resultado validado:
- `clase_afi = D`
- `cuenta = NULL`
- `subcta = NULL`

**Sobre 253/1**
`253/1` existe y representa una cuenta/subcuenta de empresa:

```sql
SELECT C.*
FROM cuentas C
WHERE C.cuenta = 253

SELECT S.*
FROM subcuentas S
WHERE S.cuenta = 253
AND S.subcta = 1

SELECT SC.cuenta, SC.subcta, SC.vigen_desde, SC.modo_fac_def, SC.emi_grupo, SC.vto_grupo, SC.baja_fecha
FROM subcta_contratos SC
WHERE SC.cuenta = 253
AND SC.subcta = 1
ORDER BY SC.vigen_desde DESC
```

Resultado validado:
- cuenta `253`: cuenta tipo `E`
- subcuenta `1`
- `modo_fac_def = E`
- no hay relación vigente con contrato `471059`

Consulta directa:

```sql
SELECT AC.*
FROM afi_clase AC
WHERE AC.prepaga = 1
AND AC.contra = '471059'
AND AC.cuenta = 253
AND AC.subcta = 1
```

Resultado: **sin filas**.

**Objeto Que Inserta XCUENTAINTERNOS**
La fila `XCUENTAINTERNOS` en `tmp_ajustes_y_prefacturas` la inserta [afi_genera_compro_ajustes.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_genera_compro_ajustes.sql:788), leyendo desde:

- `aju_internos_cuenta`
- `aju_inter_cuenta_items`

El ajuste interno de cuenta se genera en circuitos de cuenta/empresa, por ejemplo [afi_aju_inter_aporte_cta_real.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_aju_inter_aporte_cta_real.sql:243), que inserta en `aju_internos_cuenta` con `@prm_cuenta/@prm_subcta`.

Pero para el botón del ajuste `1244827`, ese bloque no debería aplicar porque:
- la llamada entra con `@contra = 471059`
- `@cuenta/@subcta = NULL`
- el bloque de empresas de `afi_genera_compro_ajustes` está bajo `IF @contra IS NULL`
- y además filtra `AJ.cuenta = @cuenta` / `AJ.subcta = @subcta`

Validación con los mismos filtros: `0` filas para `XCUENTAINTERNOS` del botón.

**Parámetros En afi_genera_compro_compro**
En [afi_genera_compro_compro.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_genera_compro_compro.sql:52):

- `@cuenta/@subcta/@contra`: identifican el sujeto del comprobante. Se usan para numeración, condición fiscal, vencimientos, intereses, IIBB y grabación del encabezado en `afu_comprobantes`.
- `@prm_cur_cuenta/@prm_cur_subcta/@prm_cur_contra/@prm_cur_tipo_factu`: filtran las filas temporales en `tmp_ajustes_y_prefacturas` para calcular importes/items del comprobante actual.

Por código, no son equivalentes absolutos. `@prm_cur_*` son filtros de lote/iteración; `@cuenta/@subcta/@contra` son identidad fiscal/comercial del comprobante.

**Conclusión Técnica**
No queda demostrado que reemplazar `@cuenta/@subcta` por `@cur_cuenta/@cur_subcta` sea semánticamente correcto para R30389. Esa modificación puede ser razonable solo en un comprobante cuyo sujeto real sea la cuenta, pero **R30389 nace desde contrato `471059` clase `D` sin cuenta/subcuenta**.

Por lo tanto, dejo pausada la propuesta anterior. La causa de `FC/X` debe aislarse nuevamente en el flujo real del contrato, especialmente entre:

- `afi_genera_compro`
- `afi_genera_compro_compro`
- `afu_obtener_nro_compro`
- `afs_afi_es_gravado`

Validación ejecutada: solo `SELECT`, sin modificar SP, sin ejecutar la propuesta, sin commit y sin cambios de datos.

---

### [16/09/2026 17:12:46] Omar Gomez

Perfecto. Descartamos entonces completamente la hipótesis de 253/1 y la modificación propuesta anteriormente.
Volvamos a la reproducción SQL que ya confirmaste que genera exactamente:
No se encontro el numero del comprobante para el ente:1 pto:0001 compro_tipo:FC letra:X
No quiero por ahora una nueva propuesta de modificación. Quiero aislar definitivamente la causa.
Partiendo de ESA MISMA ejecución de afi_generar_compro_individual, determiná cuántos comprobantes intenta generar el proceso y seguí cada uno por separado.
Para cada llamada real a afi_genera_compro_compro indicame:
Orden de ejecución;
@prepaga;
@contra;
@cuenta;
@subcta;
@compro_tipo;
@prm_cur_contra;
@prm_cur_cuenta;
@prm_cur_subcta;
@prm_cur_tipo_factu;
importe;
origen/tipo de la fila temporal que está procesando.
Especialmente quiero comparar la llamada que genera correctamente NC/B contra la siguiente llamada que termina intentando generar FC/X.
Para la llamada que termina en FC/X, seguí luego los parámetros reales hasta afu_obtener_nro_compro y afs_afi_es_gravado.
Necesito que me indiques exactamente:
1. Qué comprobante intenta generar cuando aparece FC/X y de qué movimiento/registro surge.
2. Con qué @contra/@cuenta/@subcta entra ese comprobante a afi_genera_compro_compro.
3. Con qué parámetros exactos se ejecuta afs_afi_es_gravado en ESA llamada.
4. Por qué esa ejecución devuelve letra NULL mientras que ejecutada directamente para contrato 471059 devuelve B.
5. En qué instrucción concreta cambia, se pierde o deja de utilizarse @contra = 471059.
Si el análisis estático no alcanza, prepará una COPIA de los SP involucrados con trazas mediante PRINT o SELECT de diagnóstico para que pueda ejecutar la reproducción dentro de BEGIN TRAN / ROLLBACK. No modifiques los SP originales ni hagas commit.
No busques todavía una solución. El resultado que necesito primero es una traza del estilo:
Comprobante 1 → sujeto X → NC/B → OK
Comprobante 2 → origen X → sujeto X → FC → letra NULL → fallback X → ERROR
Hasta identificar exactamente por qué el segundo comprobante pierde o cambia su sujeto fiscal.

---

### [16/09/2026 17:22:30] Agente

Omar, queda aislado: **no se pierde `@contra = 471059`**. La llamada que falla entra con el contrato correcto. Lo que cambia es el **período usado para resolver condición fiscal**.

Dejé la traza ejecutable acá:

[/mnt/Agentes/ogomez/TICKETS/R30389/R30389_traza_comprobantes.sql](/mnt/Agentes/ogomez/TICKETS/R30389/R30389_traza_comprobantes.sql)

La ejecuté con `BEGIN TRAN / ROLLBACK`; no modifiqué SPs, datos ni código.

**Traza Confirmada**
El proceso intenta generar **3 comprobantes**, ordenados por `apli_perio` porque está activo `genera_nc_por_perio = S`.

```text
Comprobante 1
  origen: INTERNOS / COP / id 7617217
  apli_perio: 06/2026
  importe: 10604.00
  sujeto: prepaga 1, contra 471059, cuenta NULL, subcta NULL
  prm_cur_contra: 471059
  prm_cur_cuenta/subcta: NULL/NULL
  tipo_factu: A
  entra como NC, pero por importe positivo recalcula a FC
  afs_afi_es_gravado(1, 471059, NULL, NULL, fecha 2026-06-01) => letra NULL
  fallback en afu_obtener_nro_compro => X
  error: FC/X
```

```text
Comprobante 2
  origen: INTERNOS / COP / ids 7625416 + 7625417
  apli_perio: 07/2026
  importe: 15284.00
  sujeto: prepaga 1, contra 471059, cuenta NULL, subcta NULL
  intentaría FC
  afs_afi_es_gravado con fecha 2026-07-01 devuelve B
  no llega a ejecutarse porque el proceso corta antes en junio
```

```text
Comprobante 3
  origen: AJUSTES / APE / id 1244827
  apli_perio: 09/2026
  importe: -177877.35
  sujeto: prepaga 1, contra 471059, cuenta NULL, subcta NULL
  intentaría NC
  afs_afi_es_gravado con fecha 2026-09-01 devuelve B
  no llega a ejecutarse porque el proceso corta antes en junio
```

**Punto Exacto**
La instrucción clave está en [afi_genera_compro_compro.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afi_genera_compro_compro.sql:148):

```sql
IF @proceso_origen = 'FI' AND @compro_tipo = 'NC' AND @var_gene_nc = 'N' AND @prm_apli_perio IS NOT NULL
BEGIN
    SET @factu_perio      = @prm_apli_perio
    SET @factu_perio_real = @prm_apli_perio
END
```

Para el primer comprobante, `@prm_apli_perio = 2026-06-01`, entonces `afu_obtener_nro_compro` recibe `@perio = 2026-06-01`.

Luego [afu_obtener_nro_compro.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afu_obtener_nro_compro.sql:92) llama:

```sql
EXEC afs_afi_es_gravado
    @cuenta = NULL,
    @subcta = NULL,
    @prepaga = 1,
    @contra = '471059',
    @fecha = '2026-06-01',
    @compro_letra = @letra OUTPUT
```

Esa ejecución devuelve `NULL` porque para contrato `471059` no hay condición fiscal vigente en junio. En base se confirmó:

```text
afi_gravados:
  contra 471059 / inte 00 / iva A / vigen_desde 2026-07-01
  contra 471059 / inte 02 / iva A / vigen_desde 2026-07-01
```

Por eso:
- `afs_afi_es_gravado` en `2026-06-01` => `NULL`
- `afs_afi_es_gravado` en `2026-07-01` => `B`
- `afs_afi_es_gravado` en `2026-09-01` => `B`

**Causa Técnica**
El error `FC/X` no nace por pérdida del contrato ni por cuenta/subcuenta. Nace porque el primer comprobante real no es el ajuste APE de septiembre: es un interno `COP` de junio, positivo, que se transforma a `FC`, y el circuito usa junio como fecha fiscal. Para junio no existe vigencia fiscal del contrato, entonces la letra queda `NULL`; [afu_obtener_nro_compro.sql](/home/desarrollo-ia-06/.openclaw/workspace/svn/ogomez-Prevencion-trunk/backend/procedures/afu_obtener_nro_compro.sql:109) aplica fallback a `X` y luego falla al numerar `FC/X`.

Objeto involucrado para análisis posterior: `afi_genera_compro_compro`, por el cambio de `@factu_perio_real` a `@prm_apli_perio` en NC por período. No propuse solución todavía, según lo pedido.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
