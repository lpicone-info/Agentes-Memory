# 2026-07-13

## Emanuel - Preferencias, profiles DB y analisis visual PowerBuilder

- Interlocutor: Emanuel Romano. Usar `Emanuel` para dirigirse a el segun Regla 24.
- Se leyo el archivo de preferencias de Emanuel:
  - `/mnt/Agentes/Emanuel/preferencias/preferencias.md`
- Preferencias operativas recordadas:
  - Para acciones con Redmine, usar siempre la credencial/usuario `mromano`.
  - Cuando Emanuel diga `mi carpeta`, usar como ruta base `/mnt/Agentes/Emanuel`.
  - Repositorios principales de Emanuel:
    - `~/.openclaw/workspace/svn/mromano-Britanico-trunk`
    - `~/.openclaw/workspace/svn/mromano-Prevencion-trunk`
  - No consultar ni modificar repositorios de otros usuarios.
  - Referencias a `Hospital Britanico` o `britanico`: usar `mromano-Britanico-trunk`.
  - Referencias a `Prevencion Salud` o `prevencion`: usar `mromano-Prevencion-trunk`.
- Emanuel consulto que profiles de base de datos estaban disponibles.
  - Se respondieron los nombres:
    - `Test_Britanico`
    - `prevencion_entrega`
    - `Britanico_Prod`
    - `desa_britanico`
- Emanuel consulto que datos/credenciales se necesitan para registrar un nuevo profile de base de datos.
  - Se indico: nombre del profile, motor, host/IP, puerto, base de datos, usuario, password y notas opcionales.
  - Se aclaro que un alta efectiva debe ejecutarse con la skill `dbprofile-add`.
- Consulta PowerBuilder sobre `wp_selexp_sup_gn`, DataWindow `dw_pend`, evento `resize` y scrollbar horizontal:
  - Problema inicial: al abrir la ventana en tamano pequeno, la horizontal scrollbar era util porque la DataWindow tenia mas columnas que ancho visible.
  - Al maximizar, la DataWindow quedaba completa y la horizontal scrollbar ya no era necesaria.
  - El bug visual aparecia solo si Emanuel interactuaba antes con la scrollbar horizontal o pasaba el puntero por encima en Windows 11, activando la animacion del grabber/flechas.
  - Al maximizar despues de esa interaccion, quedaba un residuo visual de la scrollbar en la posicion antigua.
- Hipotesis y alternativas analizadas:
  - `dw_pend.HScrollBar = FALSE` y `dw_pend.Modify("DataWindow.HorizontalScrollBar=No")`.
  - Redibujado con `dw_pend.SetRedraw(FALSE/TRUE)` preservando buffer.
  - Evento posteado `ue_redibujar_dw_pend` para ejecutar el repintado despues del `resize`.
  - Variable de instancia para ocultar la scrollbar solo cuando la ventana creciera, manteniendola visible en tamano inicial pequeno.
  - Ocultar temporalmente `dw_pend` antes de redimensionar/mover controles y mostrarla al final del `resize`.
  - Como alternativa mas fuerte, invalidacion visual con API Windows `RedrawWindow`, pero no fue necesario aplicarla como solucion final.
- Resultado confirmado por Emanuel:
  - La solucion real aparecio al continuar el requerimiento funcional y editar la DataWindow aumentando el tamano de las columnas.
  - Despues de ajustar los anchos de columnas requeridos, el bug visual de la scrollbar horizontal desaparecio.
  - Conclusion practica: no fue necesario forzar redibujados ni usar API externa; el ajuste funcional de layout elimino la condicion que disparaba el residuo visual.
- Observacion operativa:
  - `openclaw.memory_search` esta pausado por indice de memoria con metadata faltante o incompatible.
  - La memoria se guardo directamente en `workspace/memory/2026-07-13.md`.
