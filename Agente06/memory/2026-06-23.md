# 2026-06-23

- Redmine 29823 / Azure 23977 / Britanico / Presmed: se analizo el problema de importacion de expediente por transacciones remotas con TXT de prevalidacion. Caso reproducido en `Britanico_Prod`, expediente `ofi=1`, `expe=175978`, transaccion externa `aufree=14815185`, prestacion `130104`, valores del archivo `77000`, `78000`, `79000`. Diagnostico: `pri_transac_conc` carga correctamente las lineas en `tmp_import_tr_errores`, pero `prs_valida_impor_tr_remotas` actualiza `AUTO_PRESTAC_ITEMS_REMOTAS.prestac_valor` con join por `transac_ext + prestac` sin distinguir ocurrencias repetidas; en produccion las tres filas `APIR.item` `14762806`, `14762807`, `14762808` quedaron con `79000`, y luego `pri_expe_transac_remotas` copio ese valor a `EXPE_ORDEN_ITEMS.ingre_valor` para el item de expediente `30628`.
- Solucion preparada para Redmine 29823: modificar `prs_valida_impor_tr_remotas` para validar cantidad de ocurrencias y hacer match uno a uno con `ROW_NUMBER()`. Se numera `TMP_IMPORT_TR_ERRORES` por `transac_ext + prestac` ordenado por `nro_registro`, y `AUTO_PRESTAC_ITEMS_REMOTAS` por `aufree + prestac` ordenado por `APIR.item`; el update aplica solo cuando coincide `rn`. Si la cantidad de ocurrencias no coincide, se deja error en `tmp_import_tr_errores` y no se actualiza esa prestacion/transaccion.
- Archivos generados/modificados Redmine 29823: working copy SVN modificada en `/home/desarrollo-ia-06/.openclaw/workspace/svn/wferreyra-2026.04-backend/procedures/prs_valida_impor_tr_remotas.sql` correspondiente a `https://srv-infosvn.infomedical.com/svn/Britanico/branches/2026.04/backend/procedures/prs_valida_impor_tr_remotas.sql`; estado SVN: archivo `M`, sin commit realizado. Copia compartida para Walter en `\\infomed03n\Agentes\WalterF\Britanico\prs_valida_impor_tr_remotas_modi.sql` (`/mnt/Agentes/WalterF/Britanico/prs_valida_impor_tr_remotas_modi.sql`). Tambien quedo copia interna en `/home/desarrollo-ia-06/.openclaw/workspace/WalterF/Britanico/Presmed/Tarea #29823/prs_valida_impor_tr_remotas.sql`.
- Validaciones Redmine 29823: el SP modificado conserva formato legacy `ISO-8859` con `CRLF`; se ejecuto validacion SQL con `SET PARSEONLY ON` contra `Britanico_Prod` sin errores; simulacion SELECT contra datos reales `aufree=14815185`, `prestac=130104` confirmo mapeo esperado: `APIR.item 14762806 -> 77000`, `14762807 -> 78000`, `14762808 -> 79000`. No se aplico el SP en base y no se hizo commit SVN.
