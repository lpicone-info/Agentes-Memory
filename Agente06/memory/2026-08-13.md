
## 2026-08-13 - afu/afs deuda vencida aportes
- Usuario: Marcelo. Contexto de trabajo en Google Chat directo con Homero/Agente06.
- Repo activo: `/mnt/Agentes/mepaduan/Britanico/Analisis-IA`.
- SP revisado/mejorado: `afs_afiliados_deuda_vencida.sql`.
  - Se optimizo `afi_clase` pasando la validacion a `EXISTS` para evitar duplicaciones.
  - Se agrego/uso concepto de contratos agrupados para no multiplicar aportes cuando un contrato tiene varias FC vencidas.
  - `#deuda` identifica facturas vencidas, `#contratos_deuda` reduce a contratos unicos, `#aportes` calcula aportes disponibles.
- SP nuevo/controlador: `afu_afiliados_deuda_vencida_generar.sql`.
  - Objetivo: consumir `tmp_afi_deuda_vencida` ya generada por `afs_afiliados_deuda_vencida`; no recalcula deuda.
  - Debe procesar primero FC/ND mas antiguas (`fecha_vto`) y consumir aportes del mas viejo al mas nuevo (`periodo_aporte`, `aporte`).
  - Debe controlar saldo vigente real de la FC/ND antes y despues de cada aplicacion.
  - Genera ajuste interno por aporte consumido, genera NC con `afi_genera_compro`, deja la NC impresa (`impre_fecha`), aplica con `afu_aplicar_creditos_pend`, actualiza `aportes`, registra `afi_aportes_ajustes`, y marca `tmp_afi_deuda_vencida` segun corresponda.
  - El `factu_concep` del item de ajuste/NC debe salir del aporte consumido (`A.factu_concep`); si viene NULL usa fallback `PARAM_AFI.factu_concep_ap`.
  - Se eliminaron temporales redundantes tipo `#deuda` dentro del generador; debe trabajar directo con `tmp_afi_deuda_vencida` filtrando por `spid`, `prepaga`, estado `P`, importe aplicable y contrato opcional.
  - El SP debe tener comentarios breves por fragmento explicando que hace cada bloque.
- Prueba autorizada y ejecutada en `desa_britanico` con `BEGIN TRAN` / `ROLLBACK`; se compilo el SP actualizado antes de probar.
  - Caso: contra `101130`, prepaga 1.
  - Deuda: `FC B006102169733`, factu_perio `2026-07-01`, vencimiento `2026-07-27`, saldo inicial `417817.67`.
  - Aporte disponible: aporte `58`, periodo `2026-07-01`, `impor_facturable 500.00`, `impor_facturado 0.00`, `factu_concep NULL`; uso fallback `API`.
  - `tmp_afi_deuda_vencida`: importe aplicable `500.00`, estado `P`.
  - Resultado dentro de transaccion: 1 contrato, 1 comprobante destino, 1 aporte consumido, 1 NC generada, total aplicado `500.00`.
  - Generado: `aju_inter 6043119`, `NC B006100026488`, aplicada a `FC B006102169733` por `500.00`.
  - `aju_inter_items`: item 1, `factu_concep API`, valor `500.00`.
  - NC: total `-500.00`, saldo `0.00`, impresa, item con `factu_concep API`, valor `-500.00`.
  - Aplicacion: `compro_apli 20343832`, credito aplicado `NC B006100026488`, importe `500.00`.
  - Saldo FC dentro de transaccion bajo a `417317.67`; aporte quedo `impor_facturable 0.00`, `impor_facturado 500.00`.
  - Rollback verificado: FC volvio a saldo `417817.67`, aporte volvio a `500.00/0.00`, NC/ajuste/aplicacion no persistieron.
- Riesgo tecnico pendiente: `afu_aplicar_creditos_pend` aplica creditos pendientes del comprobante destino; si existen otros creditos previos pendientes para la misma FC/ND podria aplicar mas que la NC recien generada. Validar si negocio lo acepta o si se necesita aplicacion especifica de la NC generada.

## 2026-08-13 - Preferencia SQL Marcelo
- Usuario: Marcelo (`users/101970137366556255918`).
- Preferencia persistente: al generar o corregir SP/scripts SQL para Marcelo, escribir siempre las relaciones con nomenclatura explicita `INNER JOIN`, `LEFT JOIN`, `RIGHT JOIN` o `JOIN` segun corresponda. Evitar joins implicitos en el `FROM` con tablas separadas por coma.
