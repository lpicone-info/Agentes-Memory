## 2026-09-18
- R30382: Validación de Desarrollo completada para proyecto `Soporte Prevencion Salud`, válido para cliente Prevención. Resultado: apto para QA con pruebas dirigidas. SVN r21335 modificó `prs_auto_expe.sql` y `prs_auto_expe_exists.sql`; no se modificaron código fuente, BD, estado de Redmine ni commits. Documentación en `/mnt/Agentes/ogomez/TICKETS/R30382/` (`RevisionDesarrollo.docx`, `RevisionDesarrollo.md`, `contexto.json`).
- R30389: análisis de regla 97 en `prevencion_entrega` y rama `ogomez-Prevencion-trunk`. Valor actual `N`; descripción: fechas de copagos en comprobantes, toma fecha de prestación y no prescripción. Evidencia trazada: la regla 97 no es consultada por los objetos PB/SP del circuito real que genera `aju_inter=7617217`; la generación depende de regla 51. Para expediente `1008662`, `expedientes.perio=01/06/2026`, `expe_ordenes.orden_fecha=30/06/2026`, `rea_fecha=31/07/2026`; trigger `tu_expedientes.sql` toma `I.perio` para `apli_perio`, mientras `rea_fecha` se usa en topes/vigencias/consumos, no para reemplazar período. `afi_genera_aju_internos.sql` copia el período recibido y tampoco consulta regla 97. Código demuestra `apli_perio=06/2026` con N y también con S en este circuito; no hay evidencia de 07/2026. No se encontró consumidor activo de `glob.gs_reglas[97]` ni `of_valor_regla(97)`; impacto sobre otras operatorias/comprobantes no queda demostrado con los recursos analizados. Documento: `/mnt/Agentes/ogomez/TICKETS/R30389/AnalisisRegla97_R30389.md`. No modificar regla, datos, SP, código, commits ni estado de Redmine.
- R30073: Se retomó el análisis solicitado por Emanuel sobre la ventana Afilmed `Afiliados > Solicitudes > Verificación de afiliados`, usando la working copy `mromano-Britanico-trunk` y consultas de solo lectura sobre `Britanico_Prod`. El flujo relevante pasa por `wp_chequeo`, DataWindow `d_afi_chequeo` y el procedimiento `afs_vali_reglas_afi`; el tope del titular se toma de `afi_tarifas.tope`, mientras que para familiares, incluido el cónyuge, se toma de `afi_edad_topes.tope`. La categoría `tari_cate='110'` corresponde a Matrimonio y no es el origen directo del tope.
- El defecto identificado para cónyuges estaba en `afs_valida_valor_recargo`: cuando `afs_puede_cambiar_tope` devolvía 0, se conservaba el tope persistido del cónyuge (por ejemplo 66 o 70), provocando un recargo superior aunque la regla impidiera generar el cambio de tope. En la definición compilada de producción se verificó un ajuste `R30073` que normaliza el tope efectivo a 65; para casos productivos analizados (contratos 529424 y 144720, integrante 02) el resultado efectivo fue 65.
- Parámetros vigentes verificados: `Max_Edad_Cambio_Tope=65`, `Max_Antig_Cambio_Tope=10`, inicio `2024-12-01`. La regla requerida es edad >= 65 y antigüedad >= 10. El código fuente y la definición compilada aún usan `@var_iEdad > @var_edad_lim` junto con antigüedad >=, por lo que personas de exactamente 65 años quedan fuera de la protección; se observaron cónyuges de 65 años con retorno 1 en `afs_puede_cambiar_tope`. Corrección recomendada: cambiar la comparación de edad a `>=`.
- Para garantizar el comportamiento se recomendó mantener la normalización del tope en `afs_valida_valor_recargo`, probar bordes 64/65/66 y 9/10/11 para titular y cónyuge, y validar Verificación de afiliados, Test de cuota y Prefacturación. También revisar procesos `afi_afi_edad_topes`, `afu_afi_edad_topes` y `pum_asignar_edades_tope_eg`, porque pueden volver a grabar topes familiares superiores sin aplicar la regla.
- Riesgo de trazabilidad: producción contiene el fix R30073, pero el archivo equivalente del trunk revisado no lo reflejaba. No se ejecutó `afs_vali_reglas_afi` por sus operaciones DELETE/INSERT sobre `afi_verif_errores`; no se modificaron base, repositorio, documentos ni cambios preexistentes.

- Diego / R30204: SVN branch `https://srv-infosvn.infomedical.com/svn/Britanico/branches/2026.09` was checked out with user `dgiuseppe` into `/home/desarrollo-ia-06/.openclaw/workspace/svn/dgiuseppe-2026.09`; validation showed `svn info` OK, `svn update` OK, clean `svn status`, revision `8341`, isolated SVN credential OK, and `REPOSITORIOS.md` updated. For Redmine 30204, work artifacts are to be placed under `/mnt/Agentes/dgiuseppe/redmine/30204` (user referred to `dgiuseppe\redmine\30204`). The PDF `R29651 Especificación Funcional - v2.pdf` was reviewed. Points 3.1 Plan excluido de morosidad, 3.2 Caja de afiliados, and 3.3 Reglas de morosidad were already implemented. Analysis for point 3.4 was produced at `/mnt/Agentes/dgiuseppe/redmine/30204/analisis_punto_3_4_morosidad.md`; conclusion: point 3.4 requires changes mainly in the analysis screen and `afi_morosidad_analisis.sql`, because the UI still contains `Sin Facturación` and `Sistema de Deriv. Apor.`, and the stored procedure still does not evaluate the new `C - Cuota vencida` condition nor use `cond_socio`, `cuotas_venc`, and `operador`. Branch remained clean; no code, DB, commits, or destructive operations performed.
