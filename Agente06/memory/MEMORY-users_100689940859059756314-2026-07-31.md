# Memoria curada diaria

- Fecha: 2026-07-31
- Usuario: Emanuel Romano
- id_googlechat: users/100689940859059756314
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/v9c28kaaaae

## Resumen de la actividad

### Tema 1: Inicio de conversacion y preferencias

- Solicitud o contexto: Emanuel inicio la conversacion con `hola`.
- Respuesta o acciones del agente: Se cargaron sus preferencias de trabajo.
- Resultado: El agente quedo disponible para trabajar con el contexto preferencial de Emanuel.
- Estado: Informativo

### Tema 2: Ubicacion de la tabla temp_compro_impresion

- Solicitud o contexto: Emanuel consulto por la tabla `temp_compro_impresion`, mencionada en el analisis de los requerimientos 30001 y 29797, porque no la encontraba entre los archivos SQL de definicion de tablas. Indico releer `/mnt/Agentes/mromano/PS-R30001-R29797/30001-29797-nuevo.docx` y consultar `desa_prevencion_prod` si era necesario.
- Respuesta o acciones del agente: Se reviso el repositorio `mromano-Prevencion-trunk`, el archivo de analisis y la base `desa_prevencion_prod`.
- Resultado: Se informo que no existe un `CREATE TABLE` para `dbo.temp_compro_impresion` en los scripts SQL del repo. La tabla existe como tabla fisica en `desa_prevencion_prod`, creada el 2013-02-18 y modificada el 2017-04-29, y esta relevada en `backend/tablas/tables-definitions.csv`, generado desde catalogo SQL Server por `extraccion-tables-definitions.sql`. La columna mencionada fue `dbo.temp_compro_impresion.nro char(13)`.
- Estado: Resuelto

### Tema 3: Actualizacion SVN y busqueda en mromano-prevencion-26.08.0

- Solicitud o contexto: Emanuel aclaro que el repositorio correcto para el desarrollo era `mromano-prevencion-26.08.0` y pidio ejecutar `svn update` y buscar alli la tabla `temp_compro_impresion`.
- Respuesta o acciones del agente: Se ejecuto `svn update` sobre `mromano-prevencion-26.08.0` y se busco la tabla en la rama actualizada.
- Resultado: La working copy quedo en revision `15560`. La busqueda confirmo que tampoco hay `CREATE TABLE` en archivos `.sql`; la definicion disponible esta en `backend/tablas/tables-definitions.csv` y tambien hay una mencion en `redmine/29797/29797.md`. No se modificaron archivos.
- Estado: Resuelto

### Tema 4: Listado de procedures candidatos por comprobante

- Solicitud o contexto: Emanuel pidio listar los procedures mencionados como candidatos de regresion por patrones vinculados a `@compro_nro CHAR(13)`, `@punto CHAR(4)`, `SUBSTRING(compro_nro,2,4)` o `SUBSTRING(compro_nro,6,8)`, y saber si habia procedures no incluidos en el analisis inicial.
- Respuesta o acciones del agente: Se contrasto el analisis inicial contra busquedas reales con `rg` sobre `backend/procedures` de `mromano-prevencion-26.08.0`.
- Resultado: Se informo que habia `115` archivos candidatos. Los mas relevantes no incluidos en la lista inicial fueron `afi_aju_internos.sql`, `afi_aju_internos_cuenta.sql`, `afi_genera_compro.sql` y `afs_busca_fecha_compro.sql`. Tambien se enumeraron otros adicionales por patron textual, aclarando que no todos tienen el mismo peso funcional.
- Estado: Resuelto

### Tema 5: Archivo auxiliar con listado de SP

- Solicitud o contexto: Emanuel pidio generar un `.txt` con el listado de procedures y guardarlo dentro de `/mnt/Agentes/mromano/PS-R30001-R29797` como `lista auxiliar de sp.txt`.
- Respuesta o acciones del agente: Se genero el archivo solicitado con los procedures candidatos.
- Resultado: Se creo `/mnt/Agentes/mromano/PS-R30001-R29797/lista auxiliar de sp.txt`. Se valido que contiene `115` procedures, formato `us-ascii`, sin BOM y finales de linea `LF`. No se modificaron archivos del repositorio.
- Estado: Resuelto

### Tema 6: Reanalisis basado solo en el PDF v3 correcto

- Solicitud o contexto: Emanuel informo que el analisis previo estaba incorrecto porque se habia tomado como base todos los adjuntos DOCX/PDF de Redmine, cuando el documento rector era `R29797 Documento de Especificacion Funcional v3.pdf`. Aclaro que no convenia tocar facturacion, incluyendo tablas como `puntos` y `ente_puntos`, y pidio un nuevo analisis usando solo ese PDF, con la rama 26.08.0 y la base `desa_prevencion_prod`, sin modificar archivos.
- Respuesta o acciones del agente: Se rehizo el analisis tomando unicamente el PDF v3 como fuente funcional. Se intento usar la rama de Britanico 26.08.0, pero se registro que no existia en SVN; el contraste tecnico se realizo contra `mromano-prevencion-26.08.0`, rama disponible y coherente con Prevencion.
- Resultado: Se genero `/mnt/Agentes/mromano/PS-R30001-R29797/analisis_PDF_v3.docx`. El nuevo enfoque excluyo modificar `puntos` y `entes_puntos`; dejo como impacto explicito `EXPE_COMPRO_EXTERNO_FARMA.compro_nro`, porque el PDF v3 la define como `char(14)` y en `desa_prevencion_prod` esta como `char(13)`.
- Validaciones: Se valido que el DOCX sea Word OpenXML valido, que `unzip -t` no reporte errores y que la extraccion de texto con `pandoc` sea correcta.
- Estado: Resuelto
