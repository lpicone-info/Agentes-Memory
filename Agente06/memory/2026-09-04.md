## 2026-09-04 09:52 ART - Redmine 29917 / Britanico

- Usuario Emanuel pidio analizar Redmine 29917 en repo SVN `mromano-Britanico-trunk`, ventana `wp_modexped` en `liqui03.pbl`, y DW `d_expe_hono_items_prac` en `liqui01.pbl`, usando base `Britanico_Prod` solo lectura.
- Caso investigado: expediente `177974`, prestador `241` REY Enrique. Problema: `expe_honorarios.lugar` quedo en `1`, aunque `prestad_lugares` muestra lugar `1` con `baja_fecha` y lugar `2` vigente.
- Conclusion comunicada: `wp_modexped` no asigna directamente `expe_honorarios.lugar`; alta llama a `pri_expedientes`, que inserta cabecera pero no recibe/graba lugar de honorarios.
- Flujo probable del error: `auto_remotas` -> `pri_expe_transac_remotas_hono` -> `pri_expe_honorarios`. En `pri_expe_transac_remotas_hono` se toma `@lugar = efe_lugar` desde `auto_remotas` y luego se pasa a `pri_expe_honorarios`; para el expediente 177974 las 4 transacciones en `auto_remotas` estaban con `efe_prestad = 241`, `efe_lugar = 1`, usuario `APIservices`, vinculadas a items 1 a 4.
- Evidencia de codigo citada: `pri_expe_transac_remotas_hono.sql` linea aprox. 155 asigna `@lugar = efe_lugar`; linea aprox. 339 llama a `pri_expe_honorarios` con ese `@lugar`.
- Se encontro origen anterior posible en `wp_auto_importac.srw`: `ll_efe_lugar = 1 // Se toma este como lugar default` (linea aprox. 213), y luego se pasa como `@efe_lugar` a `pru_autorizaciones` (linea aprox. 414).
- Para carga manual desde `wp_expe_hono`, se usa `dd_lugarxprestad`, que filtra `baja_fecha is null`; en `Britanico_Prod` con prestador 241 devuelve solo `lugar = 2`, por lo que ese flujo no explica el valor 1.
- `d_expe_hono_items_prac` no asigna lugar, solo muestra `expe_honorarios.lugar` cruzando con `prestad_lugares`; su select no filtra `prestad_lugares.baja_fecha is null`, por eso muestra domicilio/lugar dado de baja si el item ya quedo grabado con lugar 1.
- Archivos relevantes: `/home/desarrollo-ia-06/.openclaw/workspace/svn/mromano-Britanico-trunk/exe/Source Infomedical/wp_modexped.srw`, `wp_expe_hono.srw`, `wp_auto_importac.srw`, `d_expe_hono_items_prac.srd`, `dd_lugarxprestad.srd`, y backend procedures `pri_expedientes.sql`, `pri_expe_transac_remotas_hono.sql`.
