## Interacciones y hallazgos durables
- Interlocutor identificado explícitamente en conversaciones previas como Omar.
- Redmine 29886, relacionado con 28292, 27631, 27474 y 27629: la autorización ST 1835115 tenía una observación inválida `-1`. La causa identificada fue que `of_GetItemAny()` confunde texto vacío con error y devuelve `-1`; este valor puede propagarse por `istr_auto`, `dw_auto` e `ids_auto` hasta persistencia. Se recomendó corregir el circuito acotado de autorizaciones, sincronizar `istr_auto.obser`/`obser2`, actualizar solo al confirmar, no modificar al cancelar/cerrar y bloquear `-1` antes de guardar. Objetos principales: `wr_auto_observaciones`, `w_clase_auto`, `w_clase_auto_internacion` y DataWindows relacionados. No se modificó código.
- En Britanico_Prod, para buscar la autorización 1866273 creada con usuario nivel 1 desde SA nivel 2, el listado de Supervisión filtra por nivel exacto mediante `prs_autoxesta`. Debe buscarse en pendientes de nivel 1, o cambiar el filtro Nivel a 1/Todos, verificando rango de fechas y demás condiciones. No se pudo validar directamente en SQL Server por falta de cliente operativo en el entorno.
- Redmine 29651: se completó un relevamiento técnico-funcional orientado a presupuestación, centrado en `1_Solicitud de Requerimientos PSHB _Reglas de morosidad.docx`. Documento generado en `/home/desarrollo-ia-06/.openclaw/workspace/R16_Redmine_29651_Reglas_de_Morosidad.md`.
- Conclusiones Redmine 29651: requiere una nueva condición "Cuota vencida" y no se resuelve modificando parámetros actuales. La lógica vigente suma saldos vencidos y compara contra montos fijos, sin comparar saldo contra valor original de cuota. Impacto técnico clasificado Alto; las 8 horas inicialmente registradas no serían suficientes; quedaron documentadas 17 definiciones funcionales pendientes antes de presupuestar definitivamente. Se revisaron Redmine, documentos, capturas, código PowerBuilder, DataWindows, procedimientos y modelo de datos. SVN quedó limpio. No fue posible consultar directamente Britanico_Prod por falta de cliente SQL Server operativo.
