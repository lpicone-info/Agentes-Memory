# Memoria diaria - 2026-08-25

## Ticket R30279 - documentación de causa raíz
- Usuario: Omar.
- Solicitud: incorporar al contexto documental del ticket R30279 el resultado final del análisis, sin nuevo análisis y sin modificar código, base, SVN ni Redmine.
- Archivos actualizados: `/mnt/Agentes/ogomez/TICKETS/R30279/contexto.json`, `/mnt/Agentes/ogomez/TICKETS/R30279/AnalisisDefecto.md`, `/mnt/Agentes/ogomez/TICKETS/R30279/AnalisisDefecto.docx`.
- Causa raíz registrada: parametrización fiscal del concepto `AJ - Ajuste excedentes`, con `Tipo de IVA = 0` y `% sobre gravado = 0`; por eso el ajuste de `70.386,96` no se consideraba gravado y se combinaba con la cuota `CUO` gravada.
- Clasificación final: `PARAMETRIZACION / CONFIGURACION FUNCIONAL`, no defecto de código salvo especificación funcional en contrario.
- Antecedente incorporado: ante errores de Facturación Electrónica por combinación de conceptos gravados, exentos o no gravados, primero revisar composición del comprobante y parametrización fiscal del concepto que genera la diferencia antes de analizar código.
- Validaciones realizadas: JSON válido; Markdown ASCII sin BOM y LF; DOCX regenerado desde Markdown y verificado.

## Ticket R30203 - CasosPrueba.docx con checkboxes interactivos
- Solicitud: corrección puntual del generador DOCX para reemplazar los caracteres de texto `☐` por controles interactivos reales de Microsoft Word en estados `APROBADO`, `FALLIDO`, `BLOQUEADO`, manteniendo formato aprobado y sin modificar Markdown, JSON, casos ni TestMate.
- Archivo/procedimiento modificado: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md`.
- DOCX generado/verificado: `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx`.
- Backups generados: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md.bak_20260825_101135` y `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx.bak_20260825_101135`.
- Causa del comportamiento anterior: los estados estaban como caracteres Unicode de texto y no como controles de contenido.
- Implementación: controles Word/OpenXML `w:sdt` con `w14:checkbox`, estado inicial desmarcado.
- Exclusividad: no implementada; en DOCX estándar sin macros/VBA/DOCM no hay radio button robusto para garantizar exclusividad entre tres opciones. Limitación documentada: Word permite marcar más de una opción en un mismo paso/resultado.
- Validaciones realizadas: DOCX válido; `unzip -t` correcto; XML interno con `243` controles `w14:checkbox`; `62` filas de estado de pasos con controles; `19` filas de `RESULTADO DEL CASO` con controles; no quedan nodos de texto plano con la línea de estados; selección simulada persistió al reabrir XML; LibreOffice abrió/convirtió a PDF correctamente.

## Análisis morosidad contrato 553337 / ejecución 543180
- Solicitud: analizar por qué el contrato `553337` no aparece moroso con regla de morosidad `Cuota vencida`, usando SP `dbo.afi_morosidad_analisis`, rama `gomez-ProyectosEspeciales-EXES-BRITANICO-2026.09`, base `britanico prod`.
- Hallazgo clave 1: la ejecución informada no analiza el contrato `553337`, sino `543180`; el comprobante `FC A006100292477` de 07/2026 pertenece a `543180`, no a `553337`.
- Hallazgo clave 2: aun para `543180`, el SP recupera la regla con `cumple_condicion = 'C'`, `cuotas_venc = 1`, `operador = I`, `cond_socio = DES`, pero la evaluación final solo tiene bloques para `U`, `T`, `M`, `D`; no hay `INSERT INTO #tmp_salida` para `r.cumple_condicion = 'C'`.
- Bloque/condición excluyente: la regla `Cuota vencida` se recupera, pero nunca genera salida de moroso porque el SP no implementa el caso `C` en la generación de `#tmp_salida`.
- Datos para `543180`: origen `A`, clase `D`, ingresa a `#tmp_afi`; comprobantes vencidos abiertos que entran: `FC A006100292477` con fecha `01/07/2026`, vencimiento `10/07/2026`, saldo `371.666,96`; `FC A006100295978` con fecha `01/08/2026`, vencimiento `10/08/2026`, saldo `378.728,63`; fecha usada para mora `10/07/2026`; días mora al `25/08/2026`: `46`; saldo total evaluado `750.395,59`; cuotas FC vencidas abiertas `2`.
- Datos para `553337`: origen `W1`; la ejecución con origen `A` lo excluye en el armado de `#tmp_afi`; su comprobante 07/2026 real es `FC A006100292521`, no `A006100292477`; tiene `4` FC vencidas abiertas al `25/08/2026`.
- Lógica de vencimiento detectada: el SP incorpora comprobantes con `c.vto <= @fecha`, `c.cance = 'N'`, `c.impre_fecha IS NOT NULL`, `c.baja_fecha IS NULL`, `i.baja_fecha IS NULL`, `c.anu_fecha IS NULL`; luego calcula mora con `MIN(ISNULL(vto3, ISNULL(vto2, vto)))` y `DATEDIFF(dd, @min_fecha, @fecha)`.
- Conclusión: según la lógica actual, el comportamiento es coherente con el código, pero hay posible defecto/incompletitud funcional porque la condición configurada `Cuota vencida` (`C`) existe en reglas y se recupera, pero no está implementada en la salida final del SP. Además, para el caso real `553337`, la ejecución usada tenía parámetros inconsistentes: contrato `543180` y origen `A`.
# Memoria diaria - 2026-08-25

## Ticket R30279 - documentación de causa raíz
- Usuario: Omar.
- Solicitud: incorporar al contexto documental del ticket R30279 el resultado final del análisis, sin nuevo análisis y sin modificar código, base, SVN ni Redmine.
- Archivos actualizados: `/mnt/Agentes/ogomez/TICKETS/R30279/contexto.json`, `/mnt/Agentes/ogomez/TICKETS/R30279/AnalisisDefecto.md`, `/mnt/Agentes/ogomez/TICKETS/R30279/AnalisisDefecto.docx`.
- Causa raíz registrada: parametrización fiscal del concepto `AJ - Ajuste excedentes`, con `Tipo de IVA = 0` y `% sobre gravado = 0`; por eso el ajuste de `70.386,96` no se consideraba gravado y se combinaba con la cuota `CUO` gravada.
- Clasificación final: `PARAMETRIZACION / CONFIGURACION FUNCIONAL`, no defecto de código salvo especificación funcional en contrario.
- Antecedente incorporado: ante errores de Facturación Electrónica por combinación de conceptos gravados, exentos o no gravados, primero revisar composición del comprobante y parametrización fiscal del concepto que genera la diferencia antes de analizar código.
- Validaciones realizadas: JSON válido; Markdown ASCII sin BOM y LF; DOCX regenerado desde Markdown y verificado.

## Ticket R30203 - CasosPrueba.docx con checkboxes interactivos
- Solicitud: corrección puntual del generador DOCX para reemplazar los caracteres de texto `☐` por controles interactivos reales de Microsoft Word en estados `APROBADO`, `FALLIDO`, `BLOQUEADO`, manteniendo formato aprobado y sin modificar Markdown, JSON, casos ni TestMate.
- Archivo/procedimiento modificado: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md`.
- DOCX generado/verificado: `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx`.
- Backups generados: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md.bak_20260825_101135` y `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx.bak_20260825_101135`.
- Causa del comportamiento anterior: los estados estaban como caracteres Unicode de texto y no como controles de contenido.
- Implementación: controles Word/OpenXML `w:sdt` con `w14:checkbox`, estado inicial desmarcado.
- Exclusividad: no implementada; en DOCX estándar sin macros/VBA/DOCM no hay radio button robusto para garantizar exclusividad entre tres opciones. Limitación documentada: Word permite marcar más de una opción en un mismo paso/resultado.
- Validaciones realizadas: DOCX válido; `unzip -t` correcto; XML interno con `243` controles `w14:checkbox`; `62` filas de estado de pasos con controles; `19` filas de `RESULTADO DEL CASO` con controles; no quedan nodos de texto plano con la línea de estados; selección simulada persistió al reabrir XML; LibreOffice abrió/convirtió a PDF correctamente.

## Análisis morosidad contrato 553337 / ejecución 543180
- Solicitud: analizar por qué el contrato `553337` no aparece moroso con regla de morosidad `Cuota vencida`, usando SP `dbo.afi_morosidad_analisis`, rama `gomez-ProyectosEspeciales-EXES-BRITANICO-2026.09`, base `britanico prod`.
- Hallazgo clave 1: la ejecución informada no analiza el contrato `553337`, sino `543180`; el comprobante `FC A006100292477` de 07/2026 pertenece a `543180`, no a `553337`.
- Hallazgo clave 2: aun para `543180`, el SP recupera la regla con `cumple_condicion = 'C'`, `cuotas_venc = 1`, `operador = I`, `cond_socio = DES`, pero la evaluación final solo tiene bloques para `U`, `T`, `M`, `D`; no hay `INSERT INTO #tmp_salida` para `r.cumple_condicion = 'C'`.
- Bloque/condición excluyente: la regla `Cuota vencida` se recupera, pero nunca genera salida de moroso porque el SP no implementa el caso `C` en la generación de `#tmp_salida`.
- Datos para `543180`: origen `A`, clase `D`, ingresa a `#tmp_afi`; comprobantes vencidos abiertos que entran: `FC A006100292477` con fecha `01/07/2026`, vencimiento `10/07/2026`, saldo `371.666,96`; `FC A006100295978` con fecha `01/08/2026`, vencimiento `10/08/2026`, saldo `378.728,63`; fecha usada para mora `10/07/2026`; días mora al `25/08/2026`: `46`; saldo total evaluado `750.395,59`; cuotas FC vencidas abiertas `2`.
- Datos para `553337`: origen `W1`; la ejecución con origen `A` lo excluye en el armado de `#tmp_afi`; su comprobante 07/2026 real es `FC A006100292521`, no `A006100292477`; tiene `4` FC vencidas abiertas al `25/08/2026`.
- Lógica de vencimiento detectada: el SP incorpora comprobantes con `c.vto <= @fecha`, `c.cance = 'N'`, `c.impre_fecha IS NOT NULL`, `c.baja_fecha IS NULL`, `i.baja_fecha IS NULL`, `c.anu_fecha IS NULL`; luego calcula mora con `MIN(ISNULL(vto3, ISNULL(vto2, vto)))` y `DATEDIFF(dd, @min_fecha, @fecha)`.
- Conclusión: según la lógica actual, el comportamiento es coherente con el código, pero hay posible defecto/incompletitud funcional porque la condición configurada `Cuota vencida` (`C`) existe en reglas y se recupera, pero no está implementada en la salida final del SP. Además, para el caso real `553337`, la ejecución usada tenía parámetros inconsistentes: contrato `543180` y origen `A`.

## Ticket R30203 - regeneración de casos de prueba con alcance corregido
- Solicitud: regenerar `CasosPrueba.md`, `casos_prueba.json` y `CasosPrueba.docx` aplicando la versión actualizada de `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md`, sin reutilizar automáticamente CP previos y excluyendo el punto 4.
- Alcance exclusivo aplicado: `3.1 Plan excluido de morosidad`, `3.2 Caja de afiliados`, `3.3 Reglas de morosidad`.
- Matriz usada: 3.1 incluido para validar participación/exclusión del plan vía `planes.morosidad_analisis`; 3.2 incluido para validar cómo pagos/aplicaciones de caja impactan el saldo pendiente evaluado; 3.3 incluido para validar configuración, persistencia y evaluación de reglas de morosidad (`Cuota vencida`, `cuotas_venc`, `operador`, `cond_socio`, vigencia); punto 4 excluido por estar fuera de alcance (impresiones, SAP, padrón, consumidores remotos u otras salidas transversales).
- Archivos generados: `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.md`, `/mnt/Agentes/ogomez/TICKETS/R30203/casos_prueba.json`, `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx`.
- También se actualizó `/mnt/Agentes/ogomez/TICKETS/R30203/contexto.json` con trazabilidad de la regeneración.
- Backups creados con timestamp `20260825_135842`.
- Resultado final: 16 CP totales; 4 de 3.1, 4 de 3.2, 8 de 3.3; 0 CP de punto 4.
- Validaciones realizadas: JSON válido; CP correlativos; Markdown/JSON/DOCX consistentes; DOCX abre correctamente; DOCX mantiene controles reales `w:sdt` / `w14:checkbox`; no hay `punto_funcional = 4` ni casos derivados funcionalmente del punto 4.

## FlowMate - evolución del procedimiento 04_GENERAR_CASOS_PRUEBA
- Solicitud: leer completo `/mnt/Agentes/ogomez/MODIFICACION_FLOWMATE_CASOS_PRUEBA.txt`, confirmar lectura y luego aplicar exactamente las instrucciones; no regenerar R30203.
- Archivo de instrucciones leído completo antes de modificar. Cambios principales entendidos: incorporar regla de disponibilidad funcional/validaciones diferidas y usar cabecera de plantilla R-18 exclusivamente para `04_GENERAR_CASOS_PRUEBA.md` y generación de `CasosPrueba.docx`.
- Archivo modificado: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md`.
- Backup generado: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md.bak_20260825_174925`.
- Evolución incorporada: FlowMate ahora diferencia `alcance funcional`, `funcionalidad disponible`, `validación diferida` y `fuera de alcance` antes de generar casos de prueba.
- Validaciones diferidas: en Markdown se generan como sección independiente `VALIDACIONES DIFERIDAS`; en JSON como colección separada `validaciones_diferidas`; en Word como sección independiente después de los CP ejecutables. Las validaciones diferidas no quedan dentro de `casos` en `casos_prueba.json`.
- Responsable operativo de `CasosPrueba.docx`: procedimiento `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md`, sección FASE 5; la app FlowMate solo arma la instrucción desde `app.py` y no tiene clase generadora DOCX propia.
- Plantilla R-18 leída y mapeada: `/mnt/Agentes/ogomez/PLANTILLAS/R-18 PLANTILLA DE PRUEBAS.docx`; usar exclusivamente su cabecera real de tabla 2 filas x 4 columnas para `CasosPrueba.docx`.
- Campos R-18 mapeados: `APLICATIVO`, `PRESUPUESTO`, `DESCRIPCION`, `VERSION`; si falta un dato, dejar vacío o indicar `No informado`, sin inferir.
- Validaciones realizadas: UTF-8 sin BOM y CRLF preservados; marcadores del prompt validados; R-18 abre/convierte correctamente con LibreOffice; `python3 -m unittest` ejecutó 28 tests OK; `pytest` no estaba instalado.
- Confirmaciones: no se regeneró R30203; no se modificó Redmine, SVN, bases, TestMate ni código funcional de Británico.

## Ticket R30203 - regeneracion con validaciones diferidas y cabecera R-18
- Solicitud: ejecutar nuevamente GENERAR CASOS DE PRUEBA para Redmine `R30203` usando obligatoriamente `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md` actualizado.
- Alcance exclusivo aplicado: `3.1 Plan excluido de morosidad`, `3.2 Caja de afiliados`, `3.3 Reglas de morosidad`; punto 4 fuera de alcance.
- Regla clave aplicada: el proceso `Analisis de morosidad` todavia no esta desarrollado/disponible en la entrega, por lo que no debe usarse como paso ejecutable de CP; las verificaciones que dependan de ese proceso quedan como `VALIDACIONES DIFERIDAS`.
- Archivos regenerados: `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.md`, `/mnt/Agentes/ogomez/TICKETS/R30203/casos_prueba.json`, `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx`.
- Backups generados con timestamp `20260825_175701` antes de reemplazar archivos.
- CP ejecutables generados: 15 total; 4 para 3.1, 4 para 3.2, 7 para 3.3.
- Validaciones diferidas registradas: `VD01` 3.1 plan excluido no participa del analisis de morosidad; `VD02` 3.1 plan incluido participa del analisis de morosidad; `VD03` 3.2 pago parcial evita restriccion por cuota vencida; `VD04` 3.2 pago total evita restriccion por cuota vencida; `VD05` 3.3 comprobante totalmente impago restringe por regla Cuota vencida Igual; `VD06` 3.3 saldo menor, comprobante no valido, anulado o dado de baja no restringen.
- Confirmaciones entregadas: ningun CP ejecutable requiere ejecutar `Analisis de morosidad`; no existen CP del punto 4; MD, JSON y DOCX tienen correspondencia 15/15; `CasosPrueba.docx` usa la cabecera de la plantilla R-18; no se modifico Redmine, SVN, base de datos, TestMate ni codigo funcional.
