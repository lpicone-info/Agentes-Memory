# Memoria durable - 2026-08-24

## Redmine 30148 - Derivacion de aportes / baja obra social
- Usuario: Emanuel.
- Rama SVN usada: `mromano-britanico-2026.05`, actualizada previamente de revision 8292 a 8299; `svn status` final sin cambios locales tras update.
- Caso Redmine: `https://redmine.infomedical.com.ar/issues/30148`.
- Contexto funcional: Afilmed, target `afilia`, menu `Afiliados > Solicitudes > Solicitudes`, ventana `padron.pbl - wp_solici`, motivo `Cambio de deriv de aportes` (`soli_moti = 13`, deno `Cambio de Deriv. de Aportes`, `gen_verifica = "N"`).
- Caso de prueba: afiliado `493839`; solicitud detectada para la DW: `1380504`.
- Base usada: `desa_britanico`.
- Hallazgo clave: `d_soli_deriv_aportes_fami` recibe como parametro `ll_soli`, tomado de `dw_soli.object.soli[1]`; en el caso analizado vale `1380504`. El retrieve se hace en `wp_solici.srw` para motivo 13 dentro de la carga de familiares con `idw_aportes2.retrieve(ll_soli)`.
- Explicacion de por que la ventana muestra dos filas aunque el SELECT directo de la DW devuelve una: la pantalla recupera registros de `soli_deriv_aportes` para la solicitud y luego completa la grilla con datos vigentes de `afi_deriv_aportes` mediante `d_afi_aportes_soli`. Para afiliado `493839`, integrante `02`, existen dos derivaciones vigentes simultaneas, obras sociales `249` y `655`; eso genera la segunda fila visible y luego el error de clave duplicada al grabar.
- Comentario de analista previa en Redmine: si `deriv_aportes` posee registros vigentes, permitir solamente modificacion de `vigen_hasta`; si no estan vigentes, no permitir edicion.
- Fix aplicado en rama `mromano-britanico-2026.05`:
  - `wp_solici.srw`: se reactivo la proteccion de derivacion de aportes tambien para motivo `13`.
  - `wp_solici.srw`: en `pb_borrar`, si la fila esta protegida y pertenece a la solicitud, se completa `vigen_hasta`; si la fila viene solo desde afiliados, no se permite intentar grabarla y se muestra mensaje.
  - `d_soli_deriv_aportes_fami.srd`: `aportante_tipo` e `inte_titular` respetan la misma proteccion que obra social/sistema.
- Validaciones realizadas: Redmine API consultado, capturas revisadas, consultas contra `desa_britanico`, `svn diff` revisado, preservados UTF-8 con BOM y CRLF en archivos modificados. No se ejecuto commit ni compilacion PowerBuilder desde el entorno.

# Memoria durable - 2026-08-24

## Redmine 30148 - Derivacion de aportes / baja obra social
- Usuario: Emanuel.
- Rama SVN usada: `mromano-britanico-2026.05`, actualizada previamente de revision 8292 a 8299; `svn status` final sin cambios locales tras update.
- Caso Redmine: `https://redmine.infomedical.com.ar/issues/30148`.
- Contexto funcional: Afilmed, target `afilia`, menu `Afiliados > Solicitudes > Solicitudes`, ventana `padron.pbl - wp_solici`, motivo `Cambio de deriv de aportes` (`soli_moti = 13`, deno `Cambio de Deriv. de Aportes`, `gen_verifica = "N"`).
- Caso de prueba: afiliado `493839`; solicitud detectada para la DW: `1380504`.
- Base usada: `desa_britanico`.
- Hallazgo clave: `d_soli_deriv_aportes_fami` recibe como parametro `ll_soli`, tomado de `dw_soli.object.soli[1]`; en el caso analizado vale `1380504`. El retrieve se hace en `wp_solici.srw` para motivo 13 dentro de la carga de familiares con `idw_aportes2.retrieve(ll_soli)`.
- Explicacion de por que la ventana muestra dos filas aunque el SELECT directo de la DW devuelve una: la pantalla recupera registros de `soli_deriv_aportes` para la solicitud y luego completa la grilla con datos vigentes de `afi_deriv_aportes` mediante `d_afi_aportes_soli`. Para afiliado `493839`, integrante `02`, existen dos derivaciones vigentes simultaneas, obras sociales `249` y `655`; eso genera la segunda fila visible y luego el error de clave duplicada al grabar.
- Comentario de analista previa en Redmine: si `deriv_aportes` posee registros vigentes, permitir solamente modificacion de `vigen_hasta`; si no estan vigentes, no permitir edicion.
- Fix aplicado en rama `mromano-britanico-2026.05`:
  - `wp_solici.srw`: se reactivo la proteccion de derivacion de aportes tambien para motivo `13`.
  - `wp_solici.srw`: en `pb_borrar`, si la fila esta protegida y pertenece a la solicitud, se completa `vigen_hasta`; si la fila viene solo desde afiliados, no se permite intentar grabarla y se muestra mensaje.
  - `d_soli_deriv_aportes_fami.srd`: `aportante_tipo` e `inte_titular` respetan la misma proteccion que obra social/sistema.
- Validaciones realizadas: Redmine API consultado, capturas revisadas, consultas contra `desa_britanico`, `svn diff` revisado, preservados UTF-8 con BOM y CRLF en archivos modificados. No se ejecuto commit ni compilacion PowerBuilder desde el entorno.

## Omar - SVN EXES Britanico 2026.09
- Se registro/checkout la rama SVN `https://srv-infosvn.infomedical.com/svn/ProyectosEspeciales/EXES-BRITANICO/2026.09` para usuario `ogomez`.
- Working copy local: `~/.openclaw/workspace/svn/ogomez-ProyectosEspeciales-EXES-BRITANICO-2026.09`.
- Config SVN aislada: `~/.openclaw/workspace/.svn-auth/ogomez-ProyectosEspeciales-EXES-BRITANICO-2026.09/config`.
- Credenciales aisladas en: `~/.openclaw/workspace/.svn-auth/ogomez-ProyectosEspeciales-EXES-BRITANICO-2026.09/credentials.env`.
- Validaciones hechas: `svn info`, `svn update`, `svn status`, registro en `REPOSITORIOS.md`. No guardar ni repetir password en memoria.

## Omar - R30203 Casos de Prueba / FlowMate-TestMate
- Redmine trabajado: `R30203`; cliente validado: `Britanico`; proyecto Redmine detectado: `Presupuestos Britanico`; base seleccionada: `Britanico_Prod`; rama seleccionada: `EXES-BRITANICO/2026.09`.
- Carpeta de ticket: `/mnt/Agentes/ogomez/TICKETS/R30203`.
- Para generar casos se reutilizo informacion del ticket R30203 y documentacion de R29651, especialmente puntos 3.1, 3.2 y 3.3 indicados por Omar.
- Procedimiento evolucionado: `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md`.
- Nuevo estandar aprobado para intercambio futuro FlowMate-TestMate: generar `CasosPrueba.md` y `casos_prueba.json` con los mismos casos funcionales, codigos TestMate simples correlativos `CP01`, `CP02`, etc., campo separado `tipo` (`FELIZ`, `ERROR`, `BORDE`, `REGRESION`), `id_origen` opcional, `estado_ejecucion: PENDIENTE`, y pasos con `numero`, `validacion`, `resultado_esperado` obligatorio.
- Estructura JSON estandar: `version_formato`, `redmine`, `cliente`, `proyecto_redmine`, `base`, `rama`, `fecha_generacion`, `casos[]`, `validaciones_tecnicas[]`. Cada caso incluye `codigo`, `id_origen`, `tipo`, `titulo`, `objetivo`, `precondiciones`, `datos_prueba`, `prioridad`, `estado_ejecucion`, `pasos[]`.
- Las validaciones tecnicas automaticas de OpenClaw deben quedar separadas de los casos funcionales importables, en seccion independiente de Markdown y opcionalmente `validaciones_tecnicas` en JSON.
- Validaciones de consistencia obligatorias: misma cantidad de casos en MD/JSON, codigos unicos y correlativos, cada caso con titulo/objetivo, cada paso con numero/validacion/resultado esperado, sin pasos duplicados, JSON valido UTF-8.
- Resultado de prueba R30203 con nuevo formato: `19` casos, `62` pasos; tipos: `FELIZ: 5`, `ERROR: 4`, `BORDE: 5`, `REGRESION: 5`; codigos `CP01` a `CP19`; JSON valido; MD y JSON correspondientes.
- Archivos generados para R30203: `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.md` y `/mnt/Agentes/ogomez/TICKETS/R30203/casos_prueba.json`.
- Restricciones constantes del trabajo: no modificar codigo fuente, no commits, no Redmine, no SVN, no base de datos, no TestMate.

## Omar - R30203 CasosPrueba.docx QA manual
- Procedimiento `/mnt/Agentes/ogomez/PROMPTS/04_GENERAR_CASOS_PRUEBA.md` evolucionado nuevamente para generar obligatoriamente tres archivos: `CasosPrueba.md`, `casos_prueba.json`, `CasosPrueba.docx`.
- `CasosPrueba.docx` es entregable temporal para ejecucion manual del analista QA hasta que TestMate importe `casos_prueba.json`; no es fuente futura de importacion, la fuente futura sigue siendo `casos_prueba.json`.
- Word debe generarse desde la misma informacion del JSON, sin nuevo analisis, sin inventar/eliminar/modificar casos ni resultados esperados. MD, JSON y DOCX deben contener los mismos casos funcionales y la misma numeracion.
- Formato del Word aprobado: encabezado `CASOS DE PRUEBA` con Redmine, Cliente, Proyecto, Base, Rama y Fecha; casos `CP01...` con tipo, prioridad, objetivo, precondiciones, datos de prueba y estado `PENDIENTE`; cada paso con `PASO N`, `Que validar`, `Resultado esperado`, campos vacios para `Resultado obtenido`, estado con casillas `APROBADO/FALLIDO/BLOQUEADO`, `Observaciones` y `Evidencia`; cierre de cada caso con `RESULTADO DEL CASO` y campos vacios.
- Validaciones tecnicas `VT01`, `VT02`, etc. pueden incluirse al final del Word en seccion independiente `VALIDACIONES TECNICAS REALIZADAS POR FLOWMATE`, con codigo, titulo, objetivo, resultado y estado; no mezclarlas con CP.
- Metodo usado para generar Word: generacion directa con `python-docx` desde `/mnt/Agentes/ogomez/TICKETS/R30203/casos_prueba.json`.
- Archivo de prueba generado: `/mnt/Agentes/ogomez/TICKETS/R30203/CasosPrueba.docx`; contiene `19` casos y `62` pasos correspondientes a JSON/MD.
- Correccion de maquetacion aplicada al procedimiento: evitar titulos `PASO N` huerfanos al final de pagina. Los parrafos de titulo de paso deben usar `keep_with_next` y `keep_together` para quedar unidos al inicio de su tabla; la tabla puede continuar en pagina siguiente si es necesario, pero el titulo no debe quedar solo.
- Validacion de la correccion en R30203: `62` titulos `PASO N` con `keep_with_next` y `keep_together`; DOCX exportado a PDF y revisado en `39` paginas sin ningun `PASO N` huerfano. No se modifico contenido, casos, estructura logica, SVN, Redmine ni base.

## Omar - Busqueda Bettina R30279
- Omar pidio la iteracion con Bettina sobre Redmine `30279` y cualquier documento entregado, dejandolo en su carpeta de Tickets.
- Se reviso memoria, sesiones locales y carpetas de entregables; no se encontro interaccion con Bettina sobre `R30279` ni documento entregado asociado a ese Redmine.
- Hallazgo cercano: interaccion de Bettina del `21/08/2026` sobre `R30241`, por consulta de calculo del concepto `BEX` para `compro_tipo = 'FC'` y `compro_nro = 'B000206857897'`; existe `/mnt/Agentes/bwilvers/R30241.pdf`.
- Informe dejado para Omar en `/mnt/Agentes/ogomez/TICKETS/R30279/Busqueda_interaccion_Bettina_R30279.md`.
