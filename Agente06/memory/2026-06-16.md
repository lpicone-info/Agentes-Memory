## Interacciones y hallazgos durables

- Walter pidio analizar los Redmine 29820 y 29823, generar una guia tecnica paso a paso y casos de prueba, en un documento simple por cada Redmine, escrito de forma incremental de lo mas simple a lo mas complejo. Usuario Redmine usado: `wferreyra`.
- Documentos generados y ubicacion actual:
  - `/mnt/Agentes/WalterF/R29820_Guia_Tecnica_Maximizar_Supervision.md`
  - `/mnt/Agentes/WalterF/R29823_Guia_Tecnica_Transacciones_Remotas.md`
- Los documentos fueron creados en Markdown, UTF-8, sin BOM y con finales de linea LF. No se modifico codigo fuente ni se hizo commit/push.

### Redmine 29820 - Maximizar ventanas de supervision

- Titulo: `Maximizar ventana Expedientes a Supervisar - Ordenes (Azure 23975)`.
- Alcance funcional confirmado: maximizar y redimensionar ventanas de supervision de expedientes en Prestaciones / Liquidaciones para Honorarios, Ordenes y Sanatorios, evitando solapamiento de importes, grillas, botones, filtros, totales y paneles de acciones.
- Rama indicada en Redmine: `Britanico/branches/2026.04`.
- Repositorio operativo consultado: `wferreyra-2026.04-exe`.
- Objetos PowerBuilder principales:
  - `Source Infomedical/wp_selexp_sup_h.srw`
  - `Source Infomedical/wp_selexp_sup_os.srw`
  - `Source Infomedical/m_main.srm`
- Menu relevado:
  - Honorarios abre `wp_selexp_sup_h` con parametro `H`.
  - Ordenes abre `wp_selexp_sup_os` con parametro `O`.
  - Sanatorios abre `wp_selexp_sup_os` con parametro `S`.
- Estado actual relevado:
  - `wp_selexp_sup_h` tiene `maxbox = false` y `resizable = false`.
  - `wp_selexp_sup_os` tiene `maxbox = false` y `resizable = false`.
- Modificacion documental ya agregada al 29820: analisis del evento `resize`, revision del ancestro `w_clase` y codigo PowerScript sugerido para pegar en el evento `resize` de `wp_selexp_sup_h` y `wp_selexp_sup_os`.
- Publicacion en Redmine 29820: Walter pidio adjuntar `R29820_Guia_Tecnica_Maximizar_Supervision.md` como guia tecnica. Se adjunto por API al issue 29820 usando el usuario Redmine `wferreyra`. Attachment confirmado: `44291`, archivo `R29820_Guia_Tecnica_Maximizar_Supervision.md`. Nota agregada: `Se adjunta guia tecnica para el desarrollo del Redmine 29820.`
- Hallazgo tecnico del resize:
  - `wp_selexp_sup_h.srw` no tiene evento `resize` propio.
  - `wp_selexp_sup_os.srw` no tiene evento `resize` propio.
  - No se encontro `ue_resize` especifico para estas ventanas.
  - Ambas heredan de `w_clase`.
  - `w_clase.srw` tiene `resize` generico que usa `f_size_it` y `f_resize_it`.
  - `f_resize_it` no aplica el resize generico si la ventana tiene `Rectangle`, `Line`, `Oval` o `RoundRectangle`.
  - Estas ventanas tienen controles `rectangle`, por eso se documento que conviene implementar `resize` local especifico con `call super::resize` y acomodar controles manualmente.
- Patron PowerBuilder usado como referencia: eventos `resize` existentes que calculan ancho/alto con `THIS.WorkSpaceWidth()` y `THIS.WorkSpaceHeight()`, por ejemplo en `wp_cons_solis_cambio_baja.srw` y `wp_protec_boletines.srw`.
- Pendiente posible: seguir ajustando el documento del 29820 si Walter pide refinar el codigo propuesto, bajar el nivel de detalle, transformarlo a formato R-16, o convertir la guia en instrucciones exactas de implementacion.

### Redmine 29823 - Transacciones remotas con TXT

- Titulo: `Carga de expediente por transacciones Remotas (Azure 23977)`.
- Alcance funcional: corregir importacion de expedientes por transacciones remotas cuando se usa prevalidacion por TXT y una misma transaccion/prestacion aparece repetida con valores distintos.
- Caso principal relevado:
  - socio `92960/02`
  - transaccion remota `848021`
  - prestacion `130104`
  - valores esperados del TXT: `60000.00`, `30000.00`, `30000.00`
- Rama indicada en Redmine: `Britanico/branches/2026.04`.
- Repositorios operativos consultados:
  - `wferreyra-2026.04-exe`
  - `wferreyra-2026.04-backend`
- Objetos y procedimientos principales:
  - `Source Infomedical/wp_import_exp.srw`
  - `Source Infomedical/wp_impor_tran_remotas.srw`
  - `procedures/pri_transac_conc.sql`
  - `procedures/prs_valida_impor_tr_remotas.sql`
- Diagnostico documentado: la causa raiz esta en `prs_valida_impor_tr_remotas.sql`, porque el UPDATE sobre `AUTO_PRESTAC_ITEMS_REMOTAS` une por transaccion/prestacion/homologacion pero no distingue ocurrencias repetidas. La clave funcional necesaria es transaccion remota + prestacion + numero de ocurrencia.
- Solucion recomendada documentada: numerar ocurrencias del TXT por `transac_ext + prestac`, ordenadas por `nro_registro`; numerar items remotos por `AUT.aufree + prestacion resuelta`, ordenados por `APIR.item`; actualizar solo cuando coincida la ocurrencia; validar previamente diferencias de cantidad para evitar actualizaciones parciales.
- Pendiente posible: si Walter pide modificar el documento 29823, retomar desde esta guia y ampliar con SQL sugerido para `prs_valida_impor_tr_remotas.sql`, respetando reglas SQL del workspace: no usar `;` y no introducir `RTRIM`.

### Redmine 29823 - Trabajo retomado el 2026-06-17

- Walter reviso `/mnt/Agentes/WalterF/R29823_Guia_Tecnica_Transacciones_Remotas.md` y confirmo que el analisis era correcto.
- Se confirmo que la solucion base no requiere desarrollar objetos PowerBuilder. Los objetos `wp_import_exp.srw` y `wp_impor_tran_remotas.srw` ya ejecutan el flujo correcto.
- Se confirmo que el desarrollo corresponde a base de datos y que el objeto a modificar es `procedures/prs_valida_impor_tr_remotas.sql`.
- Se genero una copia modificada del SP en la carpeta de trabajo de Walter:
  - `/mnt/Agentes/WalterF/prs_valida_impor_tr_remotas.sql`
- No se modifico el archivo original del SVN:
  - `/home/desarrollo-ia-06/.openclaw/workspace/svn/wferreyra-2026.04-backend/procedures/prs_valida_impor_tr_remotas.sql`
- Cambio SQL propuesto:
  - Reemplazar el bloque `Setear los valores a las prestaciones`.
  - Numerar ocurrencias del TXT con `ROW_NUMBER()` particionando por `transac_ext, prestac` y ordenando por `nro_registro`.
  - Numerar ocurrencias remotas con `ROW_NUMBER()` particionando por `transac_ext, prestac` y ordenando por `APIR.item`.
  - Actualizar `AUTO_PRESTAC_ITEMS_REMOTAS` uniendo por transaccion remota, prestacion y numero de ocurrencia.
  - Agregar validacion previa de cantidad de ocurrencias entre TXT e items remotos, marcando error en `TMP_IMPORT_TR_ERRORES` si no coinciden para evitar actualizaciones parciales.
- Validaciones ejecutadas sobre `/mnt/Agentes/WalterF/prs_valida_impor_tr_remotas.sql`:
  - Encoding preservado como `iso-8859-1`.
  - Sin BOM.
  - Finales de linea `CRLF`.
  - Sin finalizadores `;`.
  - Sin uso de `RTRIM`.
  - Parseo SQL con `SET PARSEONLY ON` contra `Test_Britanico`, convirtiendo solo el stream de validacion desde ISO-8859-1 a UTF-8 para evitar error de charset por comentarios acentuados. Resultado: sin errores de sintaxis.
  - `svn status` del SP original: sin modificaciones.
- Horas consultadas en Redmine con usuario `wferreyra`:
  - Tarea `#27831`: estimadas `250`, imputadas `204`, disponibles `46`.
  - Tarea `#29826`: estimadas `0`, imputadas `74.5`, saldo `-74.5`.
  - Si se usan las `46` horas disponibles de `#27831` para compensar `#29826`, faltan `28.5` horas.

### Forma de retomar

- Pedido sugerido para recuperar contexto: `Retomemos los documentos de Redmine 29820 y 29823 en /mnt/Agentes/WalterF, especialmente la guia del resize del 29820 y la solucion SQL por ocurrencias del 29823`.


## Interacciones con Bettina - Redmine, repositorios y perfiles

- Bettina pidio listar repositorios cargados. Se informaron desde `REPOSITORIOS.md`:
  - `wferreyra-2026.04-exe` SVN: `~/.openclaw/workspace/svn/wferreyra-2026.04-exe`, remoto `https://srv-infosvn.infomedical.com/svn/Britanico/branches/2026.04/exe`
  - `wferreyra-2026.04-backend` SVN: `~/.openclaw/workspace/svn/wferreyra-2026.04-backend`, remoto `https://srv-infosvn.infomedical.com/svn/Britanico/branches/2026.04/backend`
  - `wferreyra-2025.05_Infomailer` SVN: `~/.openclaw/workspace/svn/wferreyra-2025.05_Infomailer`, remoto `https://srv-infosvn.infomedical.com/svn/Britanico/branches/2025.05_Infomailer`
  - `ogomez-Prevencion-trunk` SVN: `~/.openclaw/workspace/svn/ogomez-Prevencion-trunk`, remoto `https://srv-infosvn.infomedical.com/svn/Prevencion/trunk`
  - `ogomez-2026.03` SVN: `~/.openclaw/workspace/svn/ogomez-2026.03`, remoto `https://srv-infosvn.infomedical.com/svn/Britanico/branches/2026.03`
- Bettina pidio listar perfiles cargados. Se informaron desde `DB_PROFILE.md`:
  - `Nombre del Profile ESTE PROFILE ES DE EJEMPLO`
  - `Test_Britanico`
  - `prevencion_entrega`
  - `Britanico_Prod`

### Redmine 29922 - Britanico, analisis y adjunto

- Bettina pidio analizar Redmine `29922` usando credencial Redmine `bwilvers`, repositorio `ogomez-2026.03` y profile `Test_Britanico` si hacia falta.
- Se genero `/mnt/Agentes/Betu/R29922.pdf`.
- Conclusion principal: error por truncamiento en `prs_autoxafi`; asigna `'Mot: ' + b.moti` a `#final.ape2` y `#final.nombre2`, campos `VARCHAR(100)`. Los DataWindows `d_busauto` y `d_busauto_prn` esperan `char(100)`.
- Solucion recomendada: acotar con `SUBSTRING(...,1,100)` en ese `UPDATE`, manteniendo contrato actual y evitando tocar PowerBuilder.
- Validacion DB inicial no se pudo hacer por falta percibida de cliente SQL en ese momento.
- Luego Bettina pidio subir el PDF a Redmine. Se adjunto `R29922.pdf` al issue `29922`; attachment confirmado `id=44276`, filename `R29922.pdf`, tamano `8292 bytes`.
- En una regeneracion posterior se actualizo `/mnt/Agentes/Betu/R29922.pdf` como PDF valido de 4 paginas, incorporando estado Redmine `En Pruebas`, journals nuevos y referencia al SVN de incidentes indicada por Diego. Se mantuvo la causa raiz del truncamiento en `prs_autoxafi`.

### Redmine 29797 - Prevencion, relevamiento para presupuestacion

- Bettina pidio analizar Redmine `29797` usando credencial Redmine `bwilvers`, repositorio `ogomez-Prevencion-trunk` y profile `prevencion_entrega`.
- Objetivo del informe: relevamiento tecnico-funcional orientado a presupuestacion. Tareas obligatorias: leer Redmine completo, revisar `R29797 Documento de Especificación Funcional v2.docx` y `29797_prestaciones.pdf`, identificar proceso de negocio, investigar SVN, analizar logica actual, modelo de datos e impacto tecnico/funcional.
- Primer PDF generado: `/mnt/Agentes/Betu/R29797.pdf`, 6 paginas, con comprension del requerimiento, analisis tecnico, alcance, estimacion tecnica, horas y propuesta.
- Clasificacion incluida: `Alto`.
- Estimacion incluida: desarrollo `150 horas`, pruebas funcionales con documentacion `60 horas`, total `210 horas`.
- Primer informe uso snapshot versionado `backend/tablas/tables-definitions.csv` porque inicialmente se informo que no habia cliente SQL disponible.
- Bettina indico: `usa el driver de SQL que ya tenes instalado`.
- Se uso el driver instalado `libsybdb` / FreeTDS y se pudo conectar a `prevencion_entrega`.
- Validaciones DB directas realizadas:
  - DB real: `Prevencion_Entrega`
  - `puntos.punto`: `char(4)`
  - `entes_puntos.punto`: `char(4)`
  - `compro_cae_solicitud.compro_nro`: `char(13)`
  - `compro_impresion.nro`: `char(13)`
  - `temp_compro_impresion.nro`: `char(13)`
  - Letra `E` vigente en `compro_tipos_afip`: `0 registros`
- PDF actualizado con evidencia DB: `/mnt/Agentes/Betu/R29797_actualizado.pdf`.
- No se pudo reemplazar `/mnt/Agentes/Betu/R29797.pdf` porque el filesystem CIFS lo tenia bloqueado: `Device or resource busy`. No habia proceso local visible con `fuser`/`lsof`; probable lock remoto del share. Si Bettina retoma, recordar que el actualizado valido es `R29797_actualizado.pdf` y el path exacto `R29797.pdf` seguia bloqueado.

### Redmine 29946 - Britanico, problema visual en prestadores

- Bettina pidio analizar Redmine `29946` usando credencial Redmine `bwilvers`, repositorio `ogomez-2026.03` y profile `Test_Britanico` si hacia falta.
- Se genero `/mnt/Agentes/Betu/R29946.pdf`, PDF valido de 4 paginas.
- Conclusion principal: problema visual en `wp_presta_insti.srw` / `d_prestador_isss.srd`. Al abrir un prestador profesional con `tipo_mc <> 'N'`, la ventana muestra controles de medico de cabecera pero no oculta `inter_totales_agrupados`, ubicado en la misma linea. Esto produce el solapamiento observado en la captura.
- Solucion propuesta: ajustar la visibilidad inicial de `inter_totales_agrupados` para que replique la logica que ya existe cuando cambia el checkbox `esmc`.
