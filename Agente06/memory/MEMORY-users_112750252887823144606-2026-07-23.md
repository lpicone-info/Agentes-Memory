# Memoria curada diaria

- Fecha: 2026-07-23
- Usuario: Bettina Andion Wilvers
- id_googlechat: users/112750252887823144606
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/ihpyryaaaae

## Resumen de la actividad

### Tema 1: Ubicacion de pantalla Facturacion por afiliado en Prevencion

- Solicitud o contexto: Bettina pidio ubicar en `ogomez-Prevencion-trunk` la pantalla correspondiente a `AFILMED -> Facturacion -> Afiliados -> Facturacion por afiliados`.
- Respuesta o acciones del agente: Se relevo el menu PowerBuilder y la libreria declarativa.
- Resultado: Se identifico la pantalla `wp_ver_ctacte.srw` en `ogomez-Prevencion-trunk/exe/Source Infomedical`, abierta desde `m_afilia.srm` mediante `opensheetwithparm( wp_ver_ctacte, 'A', parentwindow, 1, layered! )`. La PBL asociada es `factu1.pbl` y el parametro de apertura es `A`.
- Estado: Resuelto

### Tema 2: Tablas actualizadas desde Facturacion por afiliado

- Solicitud o contexto: Bettina pidio determinar si las tablas mostradas en una imagen se actualizaban desde `wp_ver_ctacte`, cuales eran, en que momento y bajo que circunstancias.
- Respuesta o acciones del agente: Se reviso `wp_ver_ctacte` y la traza de stored procedures hacia `factu.of_gene_compro_individual` y `afi_generar_compro_individual`.
- Resultado: Se concluyo que desde `Facturacion por afiliado` se actualizan tablas del bloque de afiliados individuales solo al presionar `Generar` y cuando la generacion del comprobante sale OK. Las tablas involucradas son `APORTES_AJUSTES`, `AJU_INTERNOS` y `AJU_INTER_ITEMS`. Para cuentas, se indico que no corresponde a esa pantalla; `AJU_INTER_APORTES`, `AJU_INTER_CUENTA_ITEMS` y `AJU_INTERNOS_CUENTA` pertenecen a logica de cuenta/empresa, por ejemplo `wp_factu_aporte_empresas` y `afi_aju_inter_aporte_cta_real`.
- Estado: Resuelto

### Tema 3: Documento ajustesPREV.docx

- Solicitud o contexto: Bettina pidio agregar el analisis al documento `/mnt/Agentes/bwilvers/ajustesPREV.docx`.
- Respuesta o acciones del agente: Se valido la carpeta `/mnt/Agentes/bwilvers` y se constato que el archivo exacto no existia.
- Resultado: No se modifico ni creo archivo en el primer intento. Luego Bettina pidio generarlo, y el agente creo `/mnt/Agentes/bwilvers/ajustesPREV.docx` con el analisis. Se valido que era Word 2007+, con 28 parrafos y 2 tablas.
- Estado: Resuelto

### Tema 4: Momento de vinculacion entre ajustes y APORTES_AJUSTES

- Solicitud o contexto: Bettina pregunto en que momento pasa la informacion desde ajustes a `APORTES_AJUSTES`.
- Respuesta o acciones del agente: Se explico la secuencia de `afi_aju_inter_aporte_real`: detecta aportes reales facturables, ejecuta `afi_genera_aju_internos`, recibe `@out_aju_inter` y acto seguido inserta en `APORTES_AJUSTES`.
- Resultado: Se aclaro que no hay una copia posterior desde `AJU_INTERNOS` hacia `APORTES_AJUSTES`; el vinculo se registra inmediatamente despues de generar el ajuste interno. La condicion incluye `impor_facturable <> 0`, `tipo_aporte = 'R'`, periodo facturable, integrante y `apo_ni_fac = 'N'`. Luego se agrego esa explicacion al documento `/mnt/Agentes/bwilvers/ajustesPREV.docx` y se valido que siguiera siendo un `.docx` valido.
- Estado: Resuelto
