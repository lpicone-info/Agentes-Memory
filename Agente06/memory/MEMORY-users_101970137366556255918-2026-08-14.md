# Memoria de conversación — Marcelo E. Paduan

## Datos

- **Fecha:** 14/08/2026
- **Usuario:** Marcelo E. Paduan
- **ID Google Chat:** `users/101970137366556255918`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 28
- **Inicio:** 10:59:35
- **Fin:** 18:31:54

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/4_yjbyaaaae`

## Archivos revisados

- `3326b02c-8485-4bda-8205-583abf78826c.jsonl`

---

## Transcripción completa

### [14/08/2026 10:59:35] Marcelo E. Paduan

hola homero

---

### [14/08/2026 11:00:44] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [14/08/2026 11:04:10] Marcelo E. Paduan

necesito que carguemos a esos 4 contratos que veniamos trabajando en desa_britanico cargarle aportes... en forma aleatoria.. 3 aportes a cada uno de los contra... con 3 factu concept distitnos y por importes de 800 1500 2350 . algo asi para que me aparezcan los comprobantes con deudas vencidas y los aportes disponibles

---

### [14/08/2026 11:07:38] Agente

Marcelo, antes de ejecutar necesito tu confirmación explícita.

Voy a impactar en `desa_britanico` 12 aportes nuevos directos en `APORTES` vía `afi_aportes`, para estos contratos:

```text
564182: API 800,  SMT 1500, SAN 2350
544679: API 1500, SMT 2350, SAN 800
101130: API 2350, SMT 800,  SAN 1500
103471: API 800,  SMT 2350, SAN 1500
```

Total: `18600.00`.  
Lote propuesto: `20260814110407001`.  
Período aporte: `2026-07-01`.

Comando exacto que ejecutaría:

```sql
BEGIN TRAN

EXEC dbo.afi_aportes '20324208519', '20260701', '20324208519', 1, '564182', '01', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814110407001, 1, GETDATE(), NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '20324208519', '20260701', '20324208519', 1, '564182', '01', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814110407001, 2, GETDATE(), NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '20324208519', '20260701', '20324208519', 1, '564182', '01', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814110407001, 3, GETDATE(), NULL, 0, 'SAN', NULL

EXEC dbo.afi_aportes '20305571130', '20260701', '20305571130', 1, '544679', '01', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814110407001, 4, GETDATE(), NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '20305571130', '20260701', '20305571130', 1, '544679', '01', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814110407001, 5, GETDATE(), NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '20305571130', '20260701', '20305571130', 1, '544679', '01', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814110407001, 6, GETDATE(), NULL, 0, 'SAN', NULL

EXEC dbo.afi_aportes '23054207654', '20260701', '23054207654', 1, '101130', '02', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814110407001, 7, GETDATE(), NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '23054207654', '20260701', '23054207654', 1, '101130', '02', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814110407001, 8, GETDATE(), NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '23054207654', '20260701', '23054207654', 1, '101130', '02', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814110407001, 9, GETDATE(), NULL, 0, 'SAN', NULL

EXEC dbo.afi_aportes '27925680511', '20260701', '27925680511', 1, '103471', '02', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814110407001, 10, GETDATE(), NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '27925680511', '20260701', '27925680511', 1, '103471', '02', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814110407001, 11, GETDATE(), NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '27925680511', '20260701', '27925680511', 1, '103471', '02', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814110407001, 12, GETDATE(), NULL, 0, 'SAN', NULL

COMMIT
```

Confirmame textual si ejecuto este impacto.

---

### [14/08/2026 11:18:51] Marcelo E. Paduan

dale, ejecutalo

---

### [14/08/2026 11:21:34] Agente

Marcelo, impacto ejecutado en `desa_britanico`.

Quedó cargado el lote `20260814110407001` con `12` aportes disponibles, total `18600.00`:

```text
564182: 3 aportes, total 4650.00
544679: 3 aportes, total 4650.00
101130: 3 aportes, total 4650.00
103471: 3 aportes, total 4650.00
```

Conceptos usados: `API`, `SMT`, `SAN`.  
Importes usados por contrato: `800.00`, `1500.00`, `2350.00`.  
Todos quedaron con `impor_facturable > 0`, `impor_facturado = 0`, `baja_fecha = NULL`.

Validación funcional con `afs_afiliados_deuda_vencida`:

```text
544679  deuda 2441.83     aportes 4650.00
564182  deuda 619034.36   aportes 4650.00
101130  deuda 402567.67   aportes 4650.00
103471  deuda 1469082.95  aportes 4650.00
```

Nota técnica: hubo un primer intento sin inserciones por sintaxis con `GETDATE()` directo, y luego un intento parcial por contador interno desfasado. Lo completé en el mismo lote y la validación final quedó correcta con los 12 registros.

---

### [14/08/2026 11:23:19] Marcelo E. Paduan

listo ya los consumí... ahora te voy a pedir nuevamente lo mismo... cargar nuevos aportes a esos conttratos. para mantener el sistema de pruebas.

---

### [14/08/2026 11:25:51] Agente

Marcelo, validé con lectura `NOLOCK`:

```text
lote nuevo 20260814112316001: no existe
MAX(aporte): 73
contador aportes: 73
```

Para repetir la misma carga, ejecutaría estos 12 aportes nuevos:

```sql
DECLARE @fec SMALLDATETIME
SELECT @fec = GETDATE()

BEGIN TRAN

EXEC dbo.afi_aportes '20324208519', '20260701', '20324208519', 1, '564182', '01', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814112316001, 1, @fec, NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '20324208519', '20260701', '20324208519', 1, '564182', '01', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814112316001, 2, @fec, NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '20324208519', '20260701', '20324208519', 1, '564182', '01', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814112316001, 3, @fec, NULL, 0, 'SAN', NULL

EXEC dbo.afi_aportes '20305571130', '20260701', '20305571130', 1, '544679', '01', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814112316001, 4, @fec, NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '20305571130', '20260701', '20305571130', 1, '544679', '01', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814112316001, 5, @fec, NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '20305571130', '20260701', '20305571130', 1, '544679', '01', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814112316001, 6, @fec, NULL, 0, 'SAN', NULL

EXEC dbo.afi_aportes '23054207654', '20260701', '23054207654', 1, '101130', '02', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814112316001, 7, @fec, NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '23054207654', '20260701', '23054207654', 1, '101130', '02', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814112316001, 8, @fec, NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '23054207654', '20260701', '23054207654', 1, '101130', '02', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814112316001, 9, @fec, NULL, 0, 'SAN', NULL

EXEC dbo.afi_aportes '27925680511', '20260701', '27925680511', 1, '103471', '02', 'R', 800.00, 0, 800.00, 0, 'P', NULL, 20260814112316001, 10, @fec, NULL, 0, 'API', NULL
EXEC dbo.afi_aportes '27925680511', '20260701', '27925680511', 1, '103471', '02', 'R', 2350.00, 0, 2350.00, 0, 'P', NULL, 20260814112316001, 11, @fec, NULL, 0, 'SMT', NULL
EXEC dbo.afi_aportes '27925680511', '20260701', '27925680511', 1, '103471', '02', 'R', 1500.00, 0, 1500.00, 0, 'P', NULL, 20260814112316001, 12, @fec, NULL, 0, 'SAN', NULL

COMMIT
```

Confirmame textual y lo ejecuto.

---

### [14/08/2026 11:31:52] Marcelo E. Paduan

te confirmo el impacto

---

### [14/08/2026 11:34:35] Agente

Marcelo, impacto ejecutado y validado.

Quedó cargado el lote `20260814112316001`:

```text
12 aportes nuevos
total lote: 18600.00
3 aportes por contrato
4650.00 facturable por cada contrato
impor_facturado = 0.00
baja_fecha = NULL
```

Detalle funcional validado con `afs_afiliados_deuda_vencida`:

```text
101130  deuda 397917.67    aportes 4650.00
103471  deuda 1464432.95   aportes 4650.00
564182  deuda 614384.36    aportes 4650.00
```

El contrato `544679` tiene los 3 aportes cargados y disponibles en `APORTES` por `4650.00`, pero ya no apareció en el SP de deuda vencida después del consumo anterior. Ahí probablemente quedó sin deuda vencida calificable para ese filtro.

---

### [14/08/2026 13:37:01] Marcelo E. Paduan

bien... ahora de forma aleatoria quiero que agregues al menos un aporte del factuconcep API a 300 contratos con deuda vencida que aparezca en el sp afs_afiliados_deuda_vencida el cual muestra la deuda y los aportes disponibles para cancelar...
si necesitas fijate la logica del sp en la base desa_britanico y vas a ver como recupera los contatos con deuda vencida... a esos agregale aportes como hicimos anteriormente con los otros contratos y las pruebas fueron exitosas

---

### [14/08/2026 13:39:02] Agente

Marcelo, validé el universo del SP en `desa_britanico`:

```text
Contratos con deuda vencida bajo la lógica del SP: 2189
Contratos candidatos sin aportes facturables actuales: 2186
```

Propongo impactar `300` contratos aleatorios de ese universo, todos con `factu_concep = API`, período `2026-07-01`, lote `20260814133655001`, y un importe aleatorio por contrato entre `800`, `1500` y `2350`.

SQL exacto a ejecutar:

```sql
SET XACT_ABORT ON
SET NOCOUNT ON

DECLARE @lote NUMERIC(18)
DECLARE @fec SMALLDATETIME
DECLARE @prepaga SMALLINT
DECLARE @fecha_vto SMALLDATETIME
DECLARE @factu_perio SMALLDATETIME
DECLARE @emi_grupo TINYINT

SELECT @lote = 20260814133655001
SELECT @fec = GETDATE()
SELECT @prepaga = 1
SELECT @fecha_vto = '20260814'
SELECT @factu_perio = '20260701'
SELECT @emi_grupo = 14

CREATE TABLE #seleccion
(
  registro NUMERIC(9) IDENTITY(1,1),
  prepaga SMALLINT,
  contra CHAR(15),
  inte CHAR(2),
  cuil CHAR(11),
  valor DECIMAL(18,2)
)

INSERT INTO #seleccion
(
  prepaga,
  contra,
  inte,
  cuil,
  valor
)
SELECT TOP 300
  X.prepaga,
  X.contra,
  X.inte,
  X.cuil,
  CASE ABS(CHECKSUM(NEWID())) % 3
    WHEN 0 THEN 800.00
    WHEN 1 THEN 1500.00
    ELSE 2350.00
  END AS valor
FROM
(
  SELECT
    C.prepaga,
    C.contra,
    AF.inte,
    AF.cuil
  FROM comprobantes C WITH (NOLOCK)
  INNER JOIN compro_tipos CT WITH (NOLOCK)
    ON CT.compro_tipo = C.compro_tipo
  INNER JOIN afi_titulares AT WITH (NOLOCK)
    ON AT.prepaga = C.prepaga
    AND AT.contra = C.contra
    AND AT.baja_fecha IS NULL
  INNER JOIN afiliados AF WITH (NOLOCK)
    ON AF.prepaga = C.prepaga
    AND AF.contra = C.contra
    AND AF.paren = 'T'
    AND AF.baja_fecha IS NULL
  WHERE C.prepaga = @prepaga
    AND CT.debi_credi = 1
    AND C.baja_fecha IS NULL
    AND C.anu_fecha IS NULL
    AND ISNULL(C.cance, 'N') = 'N'
    AND C.impre_fecha IS NOT NULL
    AND C.contra IS NOT NULL
    AND C.saldo > 0
    AND ISNULL(ISNULL(C.vto3, C.vto2), C.vto) <= @fecha_vto
    AND C.factu_perio <= @factu_perio
    AND AT.emi_grupo = @emi_grupo
    AND AF.cuil IS NOT NULL
    AND AF.cuil <> '0'
    AND NOT EXISTS (
      SELECT 1
      FROM APORTES A WITH (NOLOCK)
      WHERE A.prepaga = C.prepaga
        AND A.contra = C.contra
        AND A.baja_fecha IS NULL
        AND ISNULL(A.impor_facturable, 0) > 0
        AND A.periodo_aporte <= @factu_perio
    )
    AND EXISTS (
      SELECT 1
      FROM afi_clase AC WITH (NOLOCK)
      WHERE AC.prepaga = C.prepaga
        AND AC.contra = C.contra
        AND AC.clase_afi = 'D'
        AND AC.vigen_desde = (
          SELECT MAX(AC2.vigen_desde)
          FROM afi_clase AC2 WITH (NOLOCK)
          WHERE AC2.prepaga = C.prepaga
            AND AC2.contra = C.contra
            AND AC2.clase_afi = 'D'
            AND AC2.vigen_desde <= @factu_perio
            AND ISNULL(AC2.baja_fecha, '20500101') >= @factu_perio
        )
    )
  GROUP BY
    C.prepaga,
    C.contra,
    AF.inte,
    AF.cuil
) X
ORDER BY NEWID()

BEGIN TRAN

DECLARE @registro NUMERIC(9)
DECLARE @contra CHAR(15)
DECLARE @inte CHAR(2)
DECLARE @cuil CHAR(11)
DECLARE @valor DECIMAL(18,2)

DECLARE cur_aportes CURSOR FOR
SELECT registro, contra, inte, cuil, valor
FROM #seleccion
ORDER BY registro

OPEN cur_aportes
FETCH cur_aportes INTO @registro, @contra, @inte, @cuil, @valor

WHILE @@fetch_status = 0
BEGIN
  EXEC dbo.afi_aportes @cuil, @factu_perio, @cuil, @prepaga, @contra, @inte, 'R', @valor, 0, @valor, 0, 'P', NULL, @lote, @registro, @fec, NULL, 0, 'API', NULL

  FETCH cur_aportes INTO @registro, @contra, @inte, @cuil, @valor
END

CLOSE cur_aportes
DEALLOCATE cur_aportes

COMMIT
```

Confirmame textual y lo ejecuto.

---

### [14/08/2026 14:05:33] Marcelo E. Paduan

dale.. ejecutalo y vemos como resulta

---

### [14/08/2026 14:06:29] Agente

Marcelo, impacto ejecutado correctamente en `desa_britanico`.

Quedó cargado el lote `20260814133655001`:

```text
registros insertados: 300
contratos impactados: 300
factu_concep: API
total facturable cargado: 494850.00
```

Distribución aleatoria de importes:

```text
API 800.00   -> 95 contratos
API 1500.00  -> 74 contratos
API 2350.00  -> 131 contratos
```

Validación funcional: ejecuté `afs_afiliados_deuda_vencida` con prepaga `1`, vencimiento `2026-08-14`, período `2026-07-01`, grupo `14`.

Resultado: el SP ahora devuelve `303` contratos con deuda vencida y aportes disponibles. Eso corresponde a los `300` nuevos más los `3` que ya seguían con aportes disponibles de las pruebas anteriores.

---

### [14/08/2026 15:39:56] Marcelo E. Paduan

al querer ejecutar lso 300 registros que preparamos... se trabó y arrojo un error. el evento ejecutar que procesa el sp

afu_afiliados_deuda_vencida_generar.sql
que puede ser? algo de la iteracion? o generacion de pk?

---

### [14/08/2026 15:44:51] Agente

Marcelo, por lo que validé, no parece ser PK ni iteración como causa principal.

La evidencia más fuerte es esta:

- El lote `20260814133655001` sigue intacto:
  - `300` aportes
  - `300` contratos
  - `impor_facturable = 494850.00`
  - `impor_facturado = 0.00`
  - sin relaciones en `APORTES_AJUSTES`

O sea: el proceso no llegó a consumir los aportes, o hizo rollback completo.

El popup muestra:

```text
SQLSTATE = 01000
... (afi_genera_compro) ..Fin Generacion de Aportes Estimados
```

`01000` en SQL Server/OLE DB es warning/mensaje informativo, no error de PK. Lo pude reproducir con rollback: `afi_genera_compro` termina con `return status = 0`, pero emite mensajes informativos de debug. Con el OLE DB Driver 19, PowerBuilder los está levantando como `SQLErrText`.

Causa probable: en `afu_afiliados_deuda_vencida_generar` se pasa:

```sql
@debug = @prm_debug
```

y el parámetro viene default `'N'`. Pero `afi_genera_compro` internamente parece evaluar `@debug IS NOT NULL`, entonces `'N'` igual activa mensajes. Eso dispara los `SQLSTATE 01000`.

También detecté algo secundario: `afs_afiliados_deuda_vencida` devuelve `303` contratos, pero cargó `428` filas en `tmp_afi_deuda_vencida`, porque hay contratos con varias FC vencidas. No rompe PK, pero hace que el generador itere varias deudas por contrato. Con un aporte por contrato, debería consumir una y luego omitir el resto, pero conviene ordenarlo.

Mi diagnóstico:

1. No es problema de PK.
2. No es la carga de los 300 aportes.
3. El bloqueo viene por warnings informativos de `afi_genera_compro` tratados como error por PowerBuilder/OLE DB.
4. Hay que ajustar el SP para llamar `afi_genera_compro` con `@debug = NULL`, o cambiar el default de `@prm_debug` a `NULL`.
5. Después conviene mejorar la granularidad contrato/deuda para que no multiplique filas en `tmp_afi_deuda_vencida`.

Además, ojo: la versión compilada en `desa_britanico` no coincide con el archivo actual de `/mnt/Agentes/mepaduan/Britanico/Analisis-IA/afu_afiliados_deuda_vencida_generar.sql`. En base quedó una versión anterior.

---

### [14/08/2026 16:14:09] Marcelo E. Paduan

tenes que analizarr la version compilada que es la ultima

---

### [14/08/2026 16:35:34] Agente

Marcelo, analicé la versión compilada real en `desa_britanico`.

Conclusión: no es PK ni iteración de los 300 aportes. El primer caso que rompe es este:

```text
Orden de proceso: 274
Contrato: 548012
Afiliada: DOMINAS, AGUSTINA GISELA
FC: B006102176592
Período: 2026-07
Aporte API: 2350.00
Rechazo interno: 1021
```

El rechazo `1021` significa:

```text
El total a aplicar en el item de credito es mayor al saldo disponible
```

Probé con `ROLLBACK`:

```text
Hasta N=273 procesa OK
N=274 falla
El contrato 548012 aislado también falla
```

La NC se genera bien:

```text
NC B006100026508
saldo: -2350.00
item API: -2350.00
proceso_ok = S
```

El problema aparece al aplicar la NC. La versión compilada de `afu_afiliados_deuda_vencida_generar` llama a:

```sql
afu_aplicar_creditos_pend
```

Ese SP a su vez llama a `afu_aplicar_compro` con `@compro_tipo = NULL`, o sea como crédito pendiente a cuenta. En ese camino, `afu_aplicar_compro` entra por CASO 4 y antes de aplicar la NC ejecuta lógica interna/intereses sobre la FC. Esa FC ya tiene aplicaciones internas contra sí misma (`DES`, `API`, `SUM`) cargadas desde `2026-06-26`, y ahí termina devolviendo `1021`.

Validación clave: cuando a la misma NC la aplico directo contra la FC destino usando `afu_aplicar_compro` con comprobante específico, funciona:

```text
recha = NULL
FC saldo: 368297.25 -> 365947.25
NC saldo: -2350.00 -> 0.00
```

Diagnóstico técnico: el generador no debería usar `afu_aplicar_creditos_pend` para este circuito, porque esa rutina barre créditos pendientes y dispara lógica general del contrato. Para este caso ya tenemos la FC destino exacta, entonces corresponde aplicar la NC generada directamente contra esa FC con `afu_aplicar_compro` en modo específico.

Siguiente paso recomendado: modificar `afu_afiliados_deuda_vencida_generar` reemplazando la llamada a `afu_aplicar_creditos_pend` por llamada directa a `afu_aplicar_compro` pasando `@csr_compro_tipo` y `@csr_compro_nro`.

---

### [14/08/2026 17:39:51] Marcelo E. Paduan

al pasarle null como parametro al debug. me devuelve esto. no se genera el erro anterior pero no puede hacer el proceso

---

### [14/08/2026 17:42:41] Agente

Marcelo, ya lo pude aislar.

Ese mensaje aparece porque al pasar `@debug = NULL` desaparecen los mensajes informativos de `afi_genera_compro`, pero la versión compilada de `afu_afiliados_deuda_vencida_generar` no devuelve ningún resultset final. En la compilada hace:

```sql
COMMIT TRANSACTION
SET NOCOUNT OFF
RETURN 0
```

pero nunca hace un:

```sql
SELECT ...
```

Entonces PowerBuilder deja de recibir el warning `01000`, pero tampoco recibe filas, y por eso muestra:

```text
Procedure has not been executed or has no results
```

Validé además que el lote de 300 no se consumió:

```text
aportes lote 20260814133655001: 300
impor_facturable: 494850.00
impor_facturado: 0.00
relaciones en APORTES_AJUSTES: 0
```

O sea: no quedó impacto parcial.

Hay dos correcciones necesarias:

1. El SP controlador debe devolver un resultado final explícito para PB, aunque sea resumen:
```sql
SELECT
  @var_contratos_proc AS contratos_procesados,
  @var_destinos_proc AS comprobantes_procesados,
  @var_aportes_proc AS aportes_consumidos,
  @var_nc_generadas AS nc_generadas,
  @var_total_aplicado AS total_aplicado
```

2. Sigue pendiente el problema anterior: la compilada todavía usa `afu_aplicar_creditos_pend`. Para este circuito conviene reemplazarlo por aplicación directa con `afu_aplicar_compro` contra la FC/ND destino, porque ya tenemos `@csr_compro_tipo` y `@csr_compro_nro`.

También detecté algo importante: la versión compilada en `desa_britanico` tiene firma de 4 parámetros:

```text
@prm_prepaga
@prm_factu_perio
@prm_emi_grupo
@prm_debug
```

pero el archivo actual de `Analisis-IA` tiene firma más nueva con `@prm_fecha_vto` y `@prm_contra`. Hay que alinear eso antes de volver a probar.

---

### [14/08/2026 17:52:07] Marcelo E. Paduan

ahora estan igualados los sp. en ambos repositorios, yo quite los parametros que no ibamos a necesitar mas.
solo alcanza con prepaga perio y emi grupo. y el debug va en N.
pero estoy obteniendo el error de "procedure has not been executed or has no result "
decime a que se debe ese error. si esta devolviendo algo mal o es por algun otro comprobante?

---

### [14/08/2026 17:54:35] Agente

Marcelo, el error no viene por otro comprobante ni por PK.

Validé la compilada en `desa_britanico`: el SP termina exitosamente así:

```sql
COMMIT TRANSACTION 
SET NOCOUNT OFF
RETURN 0
```

y en el bloque de error hace:

```sql
RAISERROR 99999 @var_msg
SET NOCOUNT OFF
RETURN -1
```

El problema es que en el camino exitoso no devuelve ningún `SELECT`.

O sea: el SP puede ejecutar bien, hacer `COMMIT`, devolver `RETURN 0`, pero para PowerBuilder eso no es un resultset. Si lo estás ejecutando desde una DataWindow o un flujo que espera filas/columnas, PB muestra:

```text
Procedure has not been executed or has no results
```

porque no recibió ninguna tabla de salida.

La causa concreta está acá: se crea `#resultado`, se insertan filas `OK` / `OMITIDO`, pero nunca se hace:

```sql
SELECT ...
FROM #resultado
```

ni tampoco un `SELECT` resumen con los contadores.

Corrección mínima recomendada, antes del `RETURN 0`:

```sql
COMMIT TRANSACTION

SELECT
  @var_contratos_proc AS contratos_procesados,
  @var_destinos_proc AS comprobantes_procesados,
  @var_aportes_proc AS aportes_consumidos,
  @var_nc_generadas AS nc_generadas,
  @var_total_aplicado AS total_aplicado

SET NOCOUNT OFF
RETURN 0
```

Si la pantalla necesita detalle, entonces en vez del resumen devolver:

```sql
SELECT
  prepaga,
  contra,
  compro_tipo_fc,
  compro_nro_fc,
  aporte,
  aju_inter,
  compro_tipo_nc,
  compro_nro_nc,
  importe,
  estado,
  mensaje
FROM #resultado
ORDER BY contra, compro_tipo_fc, compro_nro_fc, aporte
```

Pero no devuelvas ambos si la DataWindow espera una sola estructura.

Diagnóstico final: es un problema de contrato de salida del SP con PowerBuilder. El proceso no está devolviendo algo mal; directamente no está devolviendo resultset en éxito.

---

### [14/08/2026 18:25:23] Marcelo E. Paduan

ahi ejecute pb con debug y paso bien.

---

### [14/08/2026 18:25:31] Agente

Perfecto, Marcelo.

Entonces queda confirmado: el proceso SQL está funcionando. El mensaje anterior de PB era por contrato de salida/resultset, no por la lógica de generación/aplicación.

Con `debug` pudiste validar que el flujo pasa bien; ahora conviene dejar el SP con un `SELECT` final estable para que PB no interprete “sin resultados”. Mi recomendación sigue siendo devolver un único resultset resumen o detalle, según lo que espere la ventana.

---

### [14/08/2026 18:26:17] Marcelo E. Paduan

armame nuevamente los casos de pruebas para todos esos contratos pero excluyendo el contrato que te indique antes
generale aportes por $ 100 nomas a los que tiene deuda vencida. si queres utiliza otros 300 disttintos a los anteriores

---

### [14/08/2026 18:27:55] Agente

Marcelo, listo para armar el nuevo set.

Validé en `desa_britanico`:

```text
Contrato excluido: 548012
Lote anterior excluido: 20260814133655001
Lote nuevo propuesto: 20260814182730001
Candidatos disponibles distintos: 1889
Aportes a generar: 300
Importe por aporte: 100.00
factu_concep: API
Período aporte: 2026-07-01
Total a impactar: 30000.00
```

Nota: `afs_afiliados_deuda_vencida` en base todavía tiene `@prm_fecha_vto`; el generador sí está con `prepaga`, `perio`, `emi_grupo`, `debug`.

Para generar los casos usaría selección pseudoaleatoria de 300 contratos distintos, excluyendo:

```sql
C.contra <> '548012'
NOT EXISTS lote 20260814133655001
NOT EXISTS aportes facturables actuales
```

y cargaría un aporte API de `100.00` por contrato vía `afi_aportes`.

Necesito tu confirmación explícita para ejecutar el impacto en base.

---

### [14/08/2026 18:29:46] Marcelo E. Paduan

ejecuta en base por favor

---

### [14/08/2026 18:31:54] Agente

Marcelo, impacto ejecutado en `desa_britanico`.

Quedó cargado el lote `20260814182730001`:

```text
aportes insertados: 300
contratos impactados: 300
factu_concep: API
importe por aporte: 100.00
total facturable: 30000.00
impor_facturado: 0.00
```

Validaciones:

```text
contrato excluido 548012: 0 filas
contratos repetidos del lote anterior 20260814133655001: 0 filas
contratos del nuevo lote con deuda vencida: 300
```

Nota técnica: hubo un primer intento sin impacto por una consulta de validación final mal armada; confirmé que el lote seguía en `0` antes de re-ejecutar. El segundo impacto cerró correctamente.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
