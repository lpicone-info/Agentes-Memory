# Memoria curada diaria

- Fecha: 2026-07-22
- Usuario: Emanuel Romano
- id_googlechat: users/100689940859059756314
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/v9c28kaaaae

## Resumen de la actividad

### Tema 1: Diagnostico de DataWindow `d_factura_brit_pdf`

- Solicitud o contexto: Emanuel pidio analizar dos exports locales `d_factura_brit_pdf_original.srd` y `d_factura_brit_pdf_modificada.srd`, equivalentes al objeto real `d_factura_brit_pdf` usado en el target `superbatch`, para entender por que `lds_aux.object.qrcode[ll_row]` devolvia un valor incorrecto.
- Respuesta o acciones del agente: Se compararon los `.srd` y se reviso la definicion interna de columnas y controles. Se verifico que en el original `tipo_doc_impre`, `qrcode` y `qr_factura` conservaban la alineacion de `table_ordinal` y `control_id`, mientras que en el modificado el control visual `qrcode` habia quedado asociado al `id=66`, correspondiente a `tipo_doc_impre`.
- Resultado: La causa raiz fue una desalineacion interna del DataWindow modificado: el buffer declaraba `qrcode` como columna 67, pero el control `qrcode` estaba asociado al id equivocado. Esto explica que la notacion de puntos devolviera valores de otra columna. Tambien se observo que `tipo_doc_impre` habia pasado de columna visual a `compute`, y que el `.srd` modificado tenia datos embebidos de prueba.
- Recomendacion: Corregir el objeto en PowerBuilder borrando y recreando el control visual `qrcode` desde el listado de columnas para que el export vuelva a quedar con `column(... id=67 ... name=qrcode ...)`, evitando renombres manuales de propiedades.
- Estado: Resuelto

### Tema 2: Memoria persistente sobre diagnostico de IDs en DataWindow

- Solicitud o contexto: Emanuel pidio guardar como criterio general que, ante problemas de asignacion por notacion de puntos en DataWindows, conviene revisar los ids de columnas/controles dentro del source `.srd`.
- Respuesta o acciones del agente: Se ejecuto memoria bajo demanda para el usuario y se creo el archivo diario de memoria correspondiente.
- Resultado: Quedo registrado como criterio futuro revisar que los `id` de los controles `column(...)` coincidan con el ordinal real declarado en `table(...)`, porque una desalineacion puede hacer que la notacion de puntos devuelva valores de otra columna.
- Estado: Resuelto

### Tema 3: Consulta sobre `abs()` en expresion computada de DataWindow

- Solicitud o contexto: Emanuel consulto si la expresion `abs(if (isnull(deuda1),0,deuda1) + if (isnull(creditos),0,creditos))` devolvia `$26,382.17` en vez de `$-26,382.17` por usar `abs()`.
- Respuesta o acciones del agente: Se explico que `abs()` devuelve el valor absoluto y elimina el signo negativo.
- Resultado: La expresion correcta para preservar el signo es `if (isnull(deuda1),0,deuda1) + if (isnull(creditos),0,creditos)`. Se aclaro que, si el calculo ya devuelve negativo pero no se ve como esperado, debe revisarse el `format` del computed field.
- Estado: Resuelto
