- OpenClaw boot sequence requirements in this workspace: read BOOTSTRAP.md first if present, then BOOT.md and its ordered startup files; REGLAS.md must be read in small sequential chunks. User-facing bootstrap greeting should be 1–3 sentences, in persona, asking what to do next, without mentioning internal files/tools/steps.
- boot-md root cause and local fix applied in installed OpenClaw bundle under `/home/luispicone/.npm-global/lib/node_modules/openclaw/dist/bundled/boot-md/`: `HOOK.md` now registers `gateway:startup`, `command:new`, and `command:reset`; `handler.js` uses an inline command-event check for new/reset; boot runs must stay isolated and must not reuse the triggering session key, otherwise the hidden boot prompt leaks visibly into the user session.
- Current OpenClaw config facts worth remembering: `hooks.internal.enabled = true`; `agents.defaults.startupContext.enabled = true`; `applyOn = ["new","reset"]`. Apparent boot behavior in webchat can come from startupContext and is not proof that boot-md fired.
- Discord duplication investigation on 2026-04-28: no duplicate Discord channel configuration was found in `~/.openclaw/openclaw.json` (single `channels.discord` block) and only one gateway process was active. The same Discord `message_id` was ingested multiple times (examples observed 3x), correlated with agent failures showing `You have hit your ChatGPT usage limit...`, so the likely issue is retry/reingest deduplication after failures rather than duplicated Discord configuration.
