# 2026-07-15

## 13:25 UTC - Presmed Web 29809 prepaga changes
- User: Leandro. Context: Presmed Web shared copy for task/carpeta `29809` under `/mnt/Agentes/lcamaratta/29809`, mirrored from SVN working copy `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicialAgente`.
- Implemented and copied to shared: new frontend `ComboPrepaga.tsx` using `/combo/getPrepagas`; `ConsultaFiltros.tsx` now uses `ComboPrepaga` instead of raw numeric `FormInput` for entidad/prepaga in affiliate search.
- Backend: `backend/procedures/ars_afiliado.sql` final result now returns `prepagaDeno` from `PREPAGAS.abre_deno` using resolved `@prm_prepaga`; SQL file preserved as ISO-8859 + CRLF.
- Authorization load: frontend stores `prepagaDeno` in Formik/Redux and `AfiliadoIngreso.tsx` shows `Entidad` as disabled/read-only field to the left of `Contrato`.
- Validation already performed: `npm run build` in `presmed_web/frontend` OK; `cmp=0` between working copy and `/mnt/Agentes/lcamaratta/29809` for copied files; frontend files CRLF preserved; no commit and no `svn add` done. New `ComboPrepaga.tsx` remains unversioned in SVN; existing unrelated unversioned `.md` left intact.
