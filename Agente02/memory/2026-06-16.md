# Memoria 2026-06-16

## Incidente 26274 - PresmedWebInicial
- Usuario: Leandro. Repositorio SVN trabajado: `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicial`, working copy local usada: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial`.
- Se ejecutaron updates sucesivos: primero r83712 -> r83738, luego r83738 -> r83739, luego r83739 -> r83740.
- Revisiones asociadas al incidente 26274 identificadas/analisadas: r83715, r83717, r83738 y r83739/r83740 como ajustes posteriores del mismo flujo. r83736 y r83737 son RM 28221, no 26274.
- Cambios principales 26274: objetos `webpos` y SPs para soportar prestaciones odontologicas: `subtipo`, `listaOdonto`, pieza/cara/sector, carga automatica y detalle de impresion.
- Hallazgo inicial: `webpos/js/transac.js` armaba `listaOdonto` con separador `|` (`pieza|cara`), mientras `backend/procedures/pri_transac_webpos_total.sql` parseaba con `PARSENAME`, que requiere separador `.`. Impacto probable: pieza/cara mal parseadas. En r83739 se corrigio a `pieza.cara`; el hallazgo quedo resuelto.
- Riesgo detectado en r83739: `webpos/frames/frame_prestacion.php` consumia globalmente `$_SESSION['prestacionOdonto']` y podia activar layout odontologico en pantallas que no pasan `$odonto = true`. En r83740 se corrigio condicionando el layout a sesion odontologica y pantalla habilitada para odontologia.
- Sobre focus en carga de prestaciones: inicialmente se marco riesgo porque `prestacion.js` hace focus en `[name='pieza']` cuando `aplica` esta en `P/C/S`. Leandro aclaro regla de negocio: si la transaccion no es odontologica, `aplica` nunca puede ser `P`, `C` o `S`; viene `NULL`. Bajo esa regla, el cambio de focus es correcto y no se considera bug. Riesgo residual solo si un SP/servicio devuelve `P/C/S` fuera del contexto odontologico.
- Validaciones hechas: `svn info`, `svn status`, `svn update`, `svn log -v`, `svn diff -c`, revision estatica; `node --check` OK para `webpos/js/transac.js` y `webpos/js/prestacion_odonto.js`. No se pudo validar PHP porque `php` no esta instalado.

## 02A / `pri_transac_remotas`
- Para transacciones `02A`, `pri_transac_webpos_total.sql` llama a `pri_transac_remotas`, no a `prs_valida_of_auto_remotas`; no generan autorizacion nueva, pero quedan como transacciones remotas en `AUTO_REMOTAS` + `AUTO_PRESTAC_ITEMS_REMOTAS`.
- Usos posteriores identificados: armado/liquidacion de expedientes (`prs_arma_expe_transac.sql`), importacion al expediente (`pri_expe_transac_remotas_server.sql`, y odontologia en `pri_expe_odon_transac_remotas.sql`), control de topes/acumulados (`prs_topes_acumulados.sql`, `prs_cant_prestac_auto.sql`), consulta/impresion (`ars_transaccion_items.sql`, `ars_transaccion_items_impresion.sql`) y anulacion/baja (`prd_transac_remotas_auto.sql`, `aru_bajas_transac.sql`).
- Conclusion funcional: `02A` se usa luego como transaccion remota prestacional para liquidacion, expediente, consulta/impresion, topes y anulaciones. Puede quedar asociada a autorizacion existente por `sucur_rel/auto_rel`; sin `auto_rel`, igual participa como consumo web.

## Presmed - `PRESTAD_CATE_HABI`
- Pregunta investigada: desde que pantalla de Presmed se llena `SELECT * FROM PRESTAD_CATE_HABI` para prestador 1671.
- Respuesta: carga manual desde `Presmed > Prestadores > Asignacion de Categorias`.
- Implementacion PowerBuilder: ventana `wp_presta_insti_conve`, abierta desde menu `m_asignacioncategoria`; la pantalla titula `Asignacion de Categorias` y ejecuta SP `pru_prestad_cate_habi`.
- Evidencias locales: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-infomedical-trunk/exe/Source Infomedical/m_main.srm`, `/home/luispicone/.openclaw/workspace/svn/lcamaratta-infomedical-trunk/exe/Source Infomedical/wp_presta_insti_conve.srw`, `/home/luispicone/.openclaw/workspace/svn/lcamaratta-infomedical-trunk/backend/procedures/pru_prestad_cate_habi.sql`.
- Validacion en `STANDARD`: prestador `1671` es `SPINELLI Carolina`, tipo `P`, y actualmente no devuelve filas en `PRESTAD_CATE_HABI`.
- Otros caminos tecnicos no manuales: `wp_presta_insti` / Carga de Prestadores actualiza categorias solo para tipo circulo `C`; `wp_prestad_importac` / Importacion de Prestadores carga categoria por defecto para circulos importados.

## Ospecon_LC - 02A odontologica WebPos / prestador 1671
- Usuario: Leandro. Investigacion sobre rechazos sucesivos en `Ospecon_LC` para transacciones WebPos tipo `02A` odontologicas del prestador `1671`.
- Transaccion `25`: quedo rechazada con `recha = 20` (`Prestacion sin convenio para este plan`), `tipo_transac = 02A`, `plan_codi = BASE`, `efe_prestad = 1671`, `efe_lugar = 1`, items `550100` y `550501`, `conve = 10376`.
- Con datos actuales, validacion directa `prs_valida_of_convenio` para `550100` y `550501` contra convenio directo `10376` devuelve `erro = 0` y valores (`36000` y `17000` respectivamente). Plan `BASE` usa cartilla `1`; `carti_convenios` tiene `10376` para `carti = 1`, `prestad = 1671`, `lugar = 1`; `conve_prestaciones` y `conve_prestac_valores` estan cargados; `PRESTAD_CATE_HABI` tiene categorias `3` y `4` vigentes para el prestador.
- Transaccion `26`: reprodujo el rechazo 20. Origen real: en `prs_valida_of_auto_prestac`, como `auto_remotas.efe_circu = 3`, el SP busca el circulo asociado y reemplaza el prestador directo `1671` por el circulo `13`. Valida contra convenio de cartilla/categoria del circulo: `10208 - CIRCULO MEDICO DE BALCARCE - CAT C`, no contra el convenio directo `10376`. La solucion funcional fue cargar las prestaciones odontologicas `550100` y `550501` con conceptos `5` y `6` y valores en el convenio `10208`.
- Luego aparecio rechazo `186 - Excede cantidad de lineas`. Sale de `prs_valida_of_odon_tr`: toma `param_odon.bono_lineas`; si esta `NULL`, default `1`. La transaccion tenia 2 items. Tambien `bono_practi` estaba `NULL` y puede disparar `187 - Excede cantidad de practicas`. Pantalla Presmed para corregir: `Instalacion > Odontologia > Parametros de liquidacion`, ventana `wp_param_odon`; cargar `Bono Lineas >= 2` y `Bono Practi >= 2`.
- Luego aparecio rechazo `1418 - Tipo de trabajo inexistente o no homologado`. En transaccion `28`, ambos items quedaron con `tipo_trabajo = NULL`. La validacion en `prs_valida_of_odon_tr` exige que `ISNULL(@var_tipo_trabajo,'')` exista en `odon_tipo_trabajo`. En `Ospecon_LC`, homologados: `00 = Preexistencia`, `01 = Trabajo realizado`, `02 = Trabajo a realizar`.
- Diferencia entre canales: Auto Remotas Java para `02A` lee del XML por detalle `PiezaDental`, `CaraDental`, `TipoTrabajo`; llama a `pri_auto_prestac_items_remotas` y pasa `TipoTrabajo` como parametro 31, que se graba en `auto_prestac_items_remotas.tipo_trabajo`; luego ejecuta `pri_transac_remotas`. WebPos nuevo (`pri_transac_webpos_total`) solo arma/parsea `pieza.cara` en `@prm_lista_odonto`, no recibe ni pasa `tipo_trabajo`, por eso queda `NULL` y rechaza con 1418.
- Presmed clasico tampoco informa `tipo_trabajo` cuando hace autorizacion odontologica: en `uo_nv_valida_auto.sru` llama a `pri_auto_prestac_items_remotas` pasando pieza/cara pero deja `@prm_tipo_trabajo = NULL`; pantallas `w_clase_auto.srw` / `wp_auto_prestaciones.srw` validan odontologia para pieza/cara pero no tienen control de tipo de trabajo. Para WebPos 02A odontologica remota, la referencia correcta es Auto Remotas Java, no Presmed clasico.
- Conclusion funcional/codigo: para alinear WebPos con Auto Remotas Java, extender contrato WebPos + `pri_transac_webpos_total` para enviar `TipoTrabajo` por item odontologico y pasarlo a `pri_auto_prestac_items_remotas`. Para una prestacion ya realizada, default funcional probable: `01 - Trabajo realizado`. Atencion: `tipo_trabajo = '02'` queda excluido de armado/liquidacion en `prs_arma_expe_odon_transac`.
