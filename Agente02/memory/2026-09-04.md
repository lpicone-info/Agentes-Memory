
## 2026-09-04 - PresmedWebInicial / procesosbatch / autorizaciones

- Usuario en Google Chat directo identificado por contexto conversacional como Leandro.
- Repositorio usado: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial`.
- Consulta: procedimiento para migrar `procesosbatch` de 32 a 64 bits en PresmedWebInicial.
- Evidencia relevada:
  - Target PB: `exe/procesosbatch.pbt`.
  - Workspace PB: `exe/TRUNK.pbw`.
  - Proyecto build: `exe/Source Infomedical/procesosbatch.srj`.
  - EXE actual detectado como `PE32 executable, Intel i386`.
  - PBL principales: `procesosbatch.pbl`, `batch_general.pbl`, `infomailer_objetos_compartidos.pbl`, `procesos_batch_compartidos.pbl`.
- Conclusión sobre migración 64 bits de `procesosbatch`:
  - No parece costosa por dependencias nativas si el flujo real se limita a las DLL usadas por esta app.
  - Las DLL Windows (`user32.dll`, `shell32.dll`, eventualmente `kernel32.dll`) tienen equivalentes 64 bits del sistema.
  - Revisar prototypes WinAPI y tipos de handles/punteros (`LongPtr`) en PowerBuilder 2022 x64.
  - Validar provider SQL 64 bits porque `procesosbatch.ini` usa `DBMS=SNC SQL Native Client(OLE DB)`.
  - Compilar x64 en salida separada y probar arranque, conexión, systray, timer y procesos batch principales.
- Clasificación de DLLs revisadas:
  - Windows: `kernel32.dll`, `user32.dll`, `shell32.dll`, `ws2_32.dll`, `advapi32.dll`, `mpr.dll`, `gdi32.dll`, `winmm.dll`, `rpcrt4.dll`, `urlmon.dll`, `crypt32.dll`.
  - Propias/probables del producto: `InfoCrypt.dll`, `InfomedicalCrypDes.dll`, `infoview.dll`.
  - Terceros/runtime externo: `python27.dll`, `infolibeay32.dll`, `cl32.dll`, `Tszd.dll`, `PFCFLSRV.DLL`.
- Uso real para `procesosbatch` según búsqueda estática:
  - Confirmado: `user32.dll` en `procesosbatch.sra` para `FindWindowA`; `shell32.dll` en `w_clase_main_batch.srw` para `Shell_NotifyIcon`.
  - `IPS_TOOLS.DLL` declarada en `w_clase_main_batch.srw`, función `UpcPor(...)`, pero sin llamadas reales encontradas; Leandro indicó que se puede sacar.
  - `kernel32.dll`, `rpcrt4.dll`, `urlmon.dll`, `cl32.dll` aparecen en `n_smtp.sru`, pero no se encontró uso efectivo del flujo de mail en `procesosbatch`; envío comentado en `wp_main_procesos_batch.srw`.
  - No se encontró uso para esta aplicación de `InfoCrypt.dll`, `InfomedicalCrypDes.dll`, `Tszd.dll`, `python27.dll`, `infolibeay32.dll`, `infoview.dll`, `PFCFLSRV.DLL`.
- Consulta posterior: en Presmed legacy, autorizaciones de prestaciones, al cargar código de prestador, cómo se cargan lugares de atención.
- Respuesta técnica relevada:
  - En `w_clase_auto.srw`, `dw_efec.itemchanged`, caso `efe_prestad`, limpia `efe_lugar`, toma `LONG(data)` y para tipo auto `P` o `E` llama `ue_setefec(dw_efec, lstr_prestad, 'N', dw_auto.GetItemDateTime(1,'orden_fecha'), 'PR')`.
  - Dentro de `ue_setefec`, valida/recupera prestador con `val.of_prestador(...)`; si es válido setea `efe_prestad` y `efe_nombre`.
  - Luego obtiene child del dropdown: `a_dw.GetChild('efe_lugar', ldch_prestad_lugares)` y ejecuta `ldch_prestad_lugares.retrieve(astr_prestad.prestad, 'C')`.
  - DDDW usado: `dd_presxlug_mcc.srd`, que llama SP `dbo.prs_prestad_lugares @kprestad = :prestad, @cod_interno = :cod_interno`.
  - En `d_auto_cabe_pres.srd`, campo `efe_lugar` usa `dd_presxlug_mcc`, `displaycolumn=domi`, `datacolumn=lugar`.
  - Regla observada: si hay un solo lugar lo asigna automáticamente; si hay más de uno deja sin default y muestra aviso `El efector tiene más de un lugar de atención, seleccione uno.`; al consultar una autorización existente conserva el `efe_lugar` de `ids_auto`.

## 2026-09-04 - Presmed V25.08.001 / prestaciones y carga manual

- Usuario consultante en Google Chat directo identificado por contexto conversacional como César.
- Repositorio usado para análisis: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-V25.08.001`.
- Consulta 1: origen de valores convenidos para prestaciones de un prestador en Presmed.
- Conclusión: los valores convenidos no salen del maestro de prestaciones sino del circuito de convenios.
- Carga/mantenimiento: ventana PowerBuilder `exe/Source Infomedical/wr_conve_prestac_valores.srw`, DataWindow `d_plani_valor.srd`, tabla `backend/tables/conve_prestac_valores.sql`.
- Persistencia: `backend/procedures/pru_conve_prestac_valores.sql` resuelve `conve_prestac` desde `conve_prestaciones` e inserta/actualiza `conve_prestac_valores` con `valor`, `valor_uni`, porcentajes, multiplicadores, urgencias y plus.
- Consulta en prestaciones convenidas por prestador/plan: menú abre `wp_prestac_conv_prestad` desde `m_main.srm`; usa `prs_prestac_conv_prestad_det.sql`, que recupera convenios del prestador/plan y calcula el valor vía `prs_valida_of_valor_prestxc.sql`.
- Flujo resumido: `prestador/plan/lugar` -> `carti_convenios` / `prestad_circu_cate` -> `conve_prestaciones` -> `conve_prestac_valores` -> cálculo `prs_valida_of_valor_prestxc`.

- Consulta 2: carga manual de órdenes para liquidar; cómo un expediente de liquidación puede quedar en estado `R` en prestaciones.
- Conclusión: `R` es estado calculado por `prs_expe_estado`; si `expedientes.revi_fecha` o `inter_revi_fecha` no son nulas, devuelve `R` = Revisado.
- En carga manual de órdenes, `wp_expe_orde.srw` al cerrar ejecuta primero `pru_expedientes_carga`, que setea `carga_fecha` y limpia `revi_fecha`.
- Si no quedan revisiones pendientes (`li_pend = 0`), `wp_expe_orde.srw` llama `pru_expedientes_audit` con `@revi_fecha = gd_hoy`; ese SP actualiza `expedientes.revi_fecha`, provocando que `prs_expe_estado` devuelva `R`.
- Si quedan errores pendientes, se insertan en `expe_errores` con `esta = 'P'` y no se ejecuta la auditoría automática; queda como “A revisar” para listados, no como `R` interno.
- Caso de rechazo `93`: `prs_expe_orden_totcal.sql` puede insertar `recha = 93`, `obser = 'Dif.suma en orden'`, `esta = 'P'`, `vali = 'S'`, `revi_fecha = @alta` cuando el valor ingresado por orden supera la suma calculada de ítems.

- Consulta 3: en prestaciones V25.08.001, evento/función que controla campos obligatorios para alta de nueva fila en ventanas heredadas de `w_clase_abm_simple`, y qué modificar en `wp_cober_prestad_pago_copa`.
- Control genérico: botón `cb_alta::clicked` en `w_clase_abm_simple.srw` dispara `ue_novalidar`, setea `is_accion = 'alta'` y dispara `ue_agregar`.
- `w_clase_abm_simple.ue_agregar` hace `dwa_datos.AcceptText()`, luego `valida.of_tst_val(dwa_datos)`; si devuelve columna, enfoca esa columna, dispara `ItemChanged!` y sale sin grabar.
- El validador se inicializa en `ue_postopen`: `valida = create uo_nv_valida` y `valida.of_ini_val(dwa_datos)`.
- `uo_nv_valida.of_tst_val()` devuelve la primera columna actualizable que sigue no validada y con `tabsequence <> 0`.
- En `wp_cober_prestad_pago_copa`, la validación actual de `prestad` y `pago_copa` está en `ue_grabar`, demasiado tarde para el patrón base; además `ue_agregar` no captura correctamente el retorno de `ue_grabar` como control obligatorio.
- Recomendación: mover el control al patrón estándar `ue_novalidar` + `itemchanged` + `of_tst_val`; en `ue_novalidar` marcar no obligatorios `nombre`, `alta_fecha`, `modi_fecha`, `baja_fecha`; en `dwa_datos::itemchanged` usar `valida.of_true_val/of_false_val` para `prestad` y `pago_copa`; dejar en `ue_grabar` solo validación defensiva mínima.
