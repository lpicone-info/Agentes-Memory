# Memoria 2026-09-16

- Usuario: Leandro. Prefiere comunicación técnica directa, trazable y con cambios mínimos. Trabaja principalmente con repositorios SVN/Git bajo `/home/luispicone/.openclaw/workspace`.
- Repositorio activo frecuente: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial`.
- En PresmedWebInicial se actualizó SVN hasta revisión `84685` y se tomaron cambios remotos para `presmed_web/backend/dist` y `presmed_web/frontend/dist` tras doble confirmación. Quedó sin conflictos SVN. Persisten cambios/locales no versionados: `presmed_web/backend/.env`, `presmed_web/frontend/e2e`, `presmed_web/frontend/e2e-artifacts`, `presmed_web/frontend/playwright.real.config.ts`.
- Problema de deployment Presmed Web: el frontend espera `window.__PRESMED_CONFIG__`, no `window.__AFILMED_CONFIG__`. `frontend/public/config.js` ya fue corregido a `__PRESMED_CONFIG__`; un próximo `npm run build` debería copiarlo a `frontend/dist/config.js`. `vite.config.ts` quedó con `base: "./"`, por lo que `dist/index.html` debería cargar `./config.js`.
- Documento creado para capacitación HTML de WebPOS: `/mnt/Agentes/lcamaratta/webpos_html.md`. Debe tratar exclusivamente HTML usado en la app PHP `webpos`; no incluir PHP, JavaScript, jQuery, Bootstrap, React, JSX ni Formik, porque esos temas se documentarán aparte.
- Aclaración sobre WebPOS: está en carpeta `webpos` dentro del repositorio `lcamaratta-PresmedWebInicial`; es aplicación PHP tradicional, no React.
- Consulta PowerBuilder legacy Afilmed/Presmed: PB/DataWindow no referencia un TTF por ruta directa; puede distribuirse junto al EXE y cargarse en runtime con Windows API `AddFontResourceEx` usando `FR_PRIVATE`, luego usarlo por nombre de fuente. No requiere administrador si es carga privada y el usuario puede leer el `.ttf`; sí requiere admin si se instala globalmente en `C:\Windows\Fonts` o registro.
- Plan conversado para nueva sección Presmed Web `Prestaciones`: crear componente `PrestacionesIngreso` con ícono `Capsule`, array de filas con código prestación string, nomenclador numérico/select con denominación, denominación prestación, cantidad numérica, nivel, búsqueda por fila, eliminar fila, agregar fila y limpiar todas. Si se informa prestación, todos los datos de la fila son obligatorios; fila vacía no bloquea.
- Backend plan para `Prestaciones`: nueva carpeta `backend/src/controllers/prestacion`, método `retrievePrestaciones` invocable por código o denominación, siguiendo patrón de `diagnostico`. Definir endpoint tipo `/api/prestacion/retrievePrestaciones`, validar JWT y criterios. Pendiente confirmar stored procedure/campos exactos.
- Frontend plan para búsqueda de prestaciones: crear módulo similar a diagnóstico con filtros por código/denominación, tabla con código, denominación, nomenclador, denominación nomenclador y nivel, y selección que completa la fila. Integrar validación Yup/Formik en autorización.
