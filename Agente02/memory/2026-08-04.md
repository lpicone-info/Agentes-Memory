# 2026-08-04

## Redmine 29856 - analisis sin modificar repo
- Usuario solicitante en chat: Cesar. Pidio analizar Redmine/incidente 29856 con usuario `clnani`, usando repositorio SVN `lcamaratta-V25.08.001`, sin consultar base y sin modificar codigo.
- Resultado del analisis: el pedido busca permitir que ciertos prestadores cambien solo el criterio de cobro de copagos definidos por plan, de adelantado a al prestador, sin duplicar valores por prestador-plan-prestacion.
- Evidencia tecnica principal en `lcamaratta-V25.08.001`: `backend/procedures/prs_valida_of_copagos_row.SQL` centraliza calculo de copagos por prestacion. Prioridad observada: copago prestador, convenio, plan. `COBER_PRESTAD_COPAGOS` devuelve valor y `pago_copa`; `COBER_COPAGOS` tambien devuelve `pago_copa` para copago de plan.
- Webpos/impresion: `backend/procedures/ars_transaccion_items_impresion.sql` muestra copago segun `pago_copa`; remoto persiste `copa_valor` y `pago_copa` en `backend/procedures/pri_auto_prestac_items_remotas.sql`.
- Cambio sugerido: crear parametrizacion nueva por prestador-plan para indicar que ese prestador cobra al prestador los copagos del plan, sin definir valor por prestacion. Ajustar `prs_valida_of_copagos_row.SQL` para que si el copago encontrado es de origen plan (`PL`) y existe marca vigente por `prepaga`, `plan_codi`, `prestad`, `baja_fecha IS NULL`, conserve valor de `COBER_COPAGOS` y fuerce `@prm_pago_copa = 'P'`.
- Impacto: principalmente backend SQL. Posible nueva tabla/script, ajuste en `prs_valida_of_copagos_row.SQL`, posible ABM PowerBuilder solo si negocio necesita pantalla. Riesgo de alcance si se aplica demasiado amplio. Revisar tambien `backend/procedures/prs_config_copa_orden.sql` si el alcance incluye copagos por orden.
- Ventanas de configuracion de copagos encontradas en `V25.08.001`: `wp_prestad_plan_copa` (Copagos por Prestador - Plan - Prestacion/Orden), `wr_planes_copag` (Copagos del Plan), `wr_planes_copag_def` (Definicion de Grupo para fijar Copagos), `wr_planes_orden_copagos`, `wr_planes_orden_copagos_capi`, `wp_planes_orden_copagos_tipos`, `wp_prestad_exen_copa`, `wp_abm_afi_exen_copagos`, `wp_prestac_exclu_copa_tope`, `wr_conve_copagos`, `wp_bonos_copago`.
- Conclusion para Redmine 29856: pantalla mas directa actual es `wp_prestad_plan_copa`, pero hoy configura valor y criterio juntos. No se encontro ventana existente para configurar solo criterio de cobro por prestador-plan manteniendo valor del plan.
