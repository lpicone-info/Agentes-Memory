# Memoria 2026-08-06

## Contexto operativo
- Usuario trabaja con repositorio SVN `lcamaratta-V25.08.001` ubicado en `~/.openclaw/workspace/svn/lcamaratta-V25.08.001`.
- Para base `smc_prox_ver`, usar los mismos datos del profile `STANDARD` cambiando solo la base destino. Validacion previa devolvio base activa `SMC_Prox_Ver`.
- Profile `STANDARD`: SQL Server `172.16.10.108:1433`, base original `Ospecon_Prod`, usuario `sa`, password `IMed.2021`.

## Analisis durable: transaccion 6484587 / rechazo 668 WebPOS vs Presmed
- Consulta investigada: `SELECT ESTA, RECHA, efe_lugar, * FROM AUTO_REMOTAS WHERE TRANSAC = 6484587`.
- Caso: transaccion ejecutada desde WebPOS quedo aceptada aunque los `conve_prestac` 516839 y 516840 estan deshabilitados.
- SP directo `prs_valida_prest_deshab` con plan `CLASSIC1`, prepaga `1`, prestad `52`, lugar `1`, nomen `2`, prestac `140104`, fecha `20260805`, conce `5`, devuelve deshabilitada.
- Evidencia encontrada: transaccion `6484587` quedo `ESTA = A`, `RECHA = NULL`; item remoto `140104` quedo `esta = A`, `recha = NULL`, convenio `10347`.
- `prs_valida_prest_deshab` devuelve deshabilitado: con `@conce = 5` devuelve `1`; sin `@conce` devuelve `2`.
- `prs_valida_of_convenio` para `H` y `P` devuelve `erro = 668`.
- Regla `zz_sistema_reg.reg = 134` esta activa con `valor = S`.
- Flujo WebPOS para `02C`: `pri_transac_webpos_total` llama `prs_valida_of_auto_remotas` (linea aprox. 394); este llama `prs_valida_of_convenio_remoto` (linea aprox. 575).
- En `prs_valida_of_convenio_remoto`, la llamada a `prs_valida_of_convenio` puede obtener `@cur_error = 668`, pero ese error no se evalua ni se graba en el item remoto en ese tramo.
- Despues se ejecuta `prs_valida_of_auto_prestac`; en este repo no llama a `prs_valida_prest_deshab` como control separado.
- Motivo del aceptado WebPOS: cuando hay convenio valido (`@conve > 0`, en el caso `10347`), el flujo prioriza convenio encontrado y no transforma `@r_erro/@cur_error = 668` en rechazo del item. Por eso queda aceptada aunque la deshabilitacion exista.
- Rechazo `20` si se detecta en WebPOS porque es un caso de "sin convenio": en `prs_valida_of_auto_prestac`, si `@conve` queda nulo y `@r_erro > 0`, entra por la rama "SI NO TIENE CONVENIO, HAY QUE RECHAZARLO" y actualiza el item remoto con el rechazo. Lineas aproximadas: llamada a `prs_valida_of_convenio` cerca de 201, rama de no convenio cerca de 427, update cerca de 444.
- Correccion conceptual: `prs_valida_of_cobertura_remoto` no calcula convenio. La explicacion correcta del rechazo `20` pasa por `prs_valida_of_auto_prestac` y la ausencia de convenio.
- Presmed cliente muestra/rechaza antes porque en `w_clase_auto.srw` declara/ejecuta `dbo.prs_valida_prest_deshab` (linea aprox. 10354) y si `li_deshab >= 1` setea `dw_prestac.object.recha[a_linea] = 668`, `dw_prestac.object.esta[a_linea] = 'R'`, `RETURN FALSE` (linea aprox. 11345).
- Aunque Presmed luego vuelve a ejecutar la transaccion llamando `prs_valida_of_auto_remotas` para `02C`, llega con el item ya marcado `R/668` en `auto_prestac_items_remotas`; al final de `prs_valida_of_auto_remotas`, el estado se recalcula si existen items `R` o `P` (linea aprox. 1925: si existe item `esta = 'R'`, `@esta = 'R'`).
- WebPOS inserta el item inicialmente aceptado en `pri_transac_webpos_total` (linea aprox. 333) con `@esta = 'A'` y `@valida_item = 'S'`; depende de que `prs_valida_of_auto_remotas` lo rechace, cosa que no ocurre para `668` cuando hay convenio.
- Conclusion durable: no es que la prestacion este habilitada; el bug esta en la validacion remota/WebPOS, especialmente en `prs_valida_of_convenio_remoto` / `prs_valida_of_auto_prestac`, que no propaga o no prioriza el `668` devuelto por `prs_valida_of_convenio` cuando tambien encontro convenio.
