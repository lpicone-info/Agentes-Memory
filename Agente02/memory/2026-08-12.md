# Memoria 2026-08-12

- Usuario en Google Chat directo: Leandro. Agente: Camaron / Agente02.
- Repositorio SVN `lcamaratta-V25.08.001` actualizado correctamente; quedo en revision `84308`. Se actualizaron archivos de `exe`, `backend/procedures` y `webpos`. No se hicieron commits ni cambios manuales.
- Repositorio SVN `lcamaratta-PresmedWebInicial` actualizado contra `branches/PresmedWebInicial`; quedo en revision `84311`. Inicialmente hubo 4 conflictos de texto en `backend/procedures/cri_precarga_solicitud.sql`, `crs_consulta_precarga_deriv_aportes.sql`, `cru_genera_precarga.sql` y `cru_genera_precarga_tmp.sql`.
- A pedido de Leandro, en `lcamaratta-PresmedWebInicial` se resolvieron los conflictos tomando los cambios remotos de base de datos correspondientes al trabajo del `27848`. Verificacion final informada: no quedaban conflictos SVN ni modificaciones pendientes en `backend`; seguian modificados archivos locales de `afilmed_web/frontend`, no tocados.
- Analisis en rama `PresmedWebInicial`, app `ws_auto_remotas`: `EjecutarTransaccion` con XML tipo `04A` y `AnularTransaccion` terminan ejecutando `prd_transac_remotas_auto`, pero difieren en como arman la 04A y de donde toman la transaccion a anular. `EjecutarTransaccion` usa `Preautorizacion.codigoPreautorizacion` o `EncabezadoMensaje.NroReferenciaCancel`; `AnularTransaccion` usa `EncabezadoMensaje.Transaccion`.
- Impacto `ws_auto_remotas`: ambos caminos insertan nueva fila `AUTO_REMOTAS` con `tipo_transac = '04A'` y `anula_transac` apuntando a la transaccion objetivo. Luego `prd_transac_remotas_auto` da de baja la transaccion original y relacionados con `baja_fecha`: `auto_remotas`, `auto_prestac_items_remotas`, `transac_remotas`, `transac_remotas_items`, y `auto_produ_items_remotas` solo en caso sin `auto`. Si la original tiene autorizacion `02C` o internacion `02I`, ejecuta `prd_autorizaciones`.
- Diferencia relevante en items `ws_auto_remotas`: `EjecutarTransaccion` carga detalles contra la nueva `04A`; `AnularTransaccion` carga detalles contra la transaccion original. Sin embargo, `prd_transac_remotas_auto` no usa los items de la nueva `04A` como entrada funcional; solo da de baja items asociados a `@anula_transac`.
- Analisis implementacion `27848` derivacion de aportes en `PresmedWebInicial`: la UI limita visualmente la carga de derivaciones al titular (`values.paren == "T"` y `tipoAfi == "O"` en `CargaIntegrante.jsx`). Permite varias derivaciones y valida duplicado exacto por `obra social + CUIT + vigencia desde`; la tabla `PRECARGA_DERIV_APORTES` tiene PK con `id_precarga, item, obra_soc, cuit, vigen_aporte`.
- Riesgo detectado en `27848` derivacion de aportes: no se encontro validacion de solapamiento de vigencias para la misma `obra_soc + cuit`. Tampoco hay defensa fuerte en backend de "solo titular": `cru_genera_precarga_tmp.sql` toma nodos `/integrante/deriva` de cualquier integrante enviado. Restriccion fuerte actualmente en UI, no en SP.
