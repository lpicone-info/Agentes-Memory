# Memoria curada - 2026-08-18

- Usuario en Google Chat directo: Leandro.
- Working copy SVN usada para PresmedWebInicial: `lcamaratta-PresmedWebInicial`, rama `branches/PresmedWebInicial`.
- Se ejecutó `svn update` en `lcamaratta-PresmedWebInicial`: pasó de revisión `84311` a `84343`.
- Cambios remotos analizados entre `84311` y `84343`: Redmine `#27848` “Cambio de Derivación de Aportes”; agrega pantalla/ruta `precarga/aportes`, menú “Cambio Derivación de Aportes”, `tipoPrecarga = 13`, edición del titular para ese tipo, vigencias `vigenDesde`/`vigenHasta`, validación de vigencias superpuestas por obra social + CUIT, y cambio en `cri_precarga_solicitud_generar.sql` donde `vigen_hasta` toma `PDA.vigen_desde` en vez de `NULL`.
- Durante el update hubo 7 conflictos de texto en `CargaIntegrante.jsx`, `ResumenPrecarga.jsx`, `TabObraSocial.jsx`, `usePrecargaRetrieve.js`, `index.js`, `PrecargaRoutes.jsx`, `NavBar.jsx`. A pedido del usuario se resolvieron tomando remoto (`theirs-full`). Luego `svn status` no mostró conflictos y la working copy quedó en revisión `84343`.
- Quedaron pendientes locales no tocados en `lcamaratta-PresmedWebInicial`: `M ResumenPrecargaBotonera.jsx` y `? CambioDerivacionAportes.jsx`.
- Consulta legacy Presmed sobre importación de transacciones a expedientes: en `lcamaratta-PresmedWebInicial`, `02C` también se importa si cumple filtros. Evidencia: `d_expe_import_odon.srd` usa `prs_arma_expe_odon_transac`; ese SP excluye solo `02F` y `02J`, exige `TT.liquida = 'S'`, `AR.esta = 'A'`, `AR.expe IS NULL`, `AR.baja_fecha IS NULL`, `AR.aufree IS NOT NULL`, `AR.contra IS NOT NULL`, `AR.subtipo = 'O'`. `02C` no está excluida. `d_monitor_prestac.srd` permite explícitamente `02C` en `tipo_transac IN ('02A', '02C', '02L', '02D', '02I', '02F')`; `prs_valida_of_tipo_transac.sql` reconoce `02C`; `prs_valida_of_afiliado_remoto.sql` trata `02C` con `02A`, `02D`, `02L` para morosidad por prestación. En `pri_expe_odon_transac_remotas.sql`, `02C` cae por `ELSE` y usa `TR.sucur/TR.auto`.
- Consulta Presmed legacy sobre tipos de copago permitidos: `pago_copa` funcional en listas de carga tiene `A` = Paga por adelantado / Por adelantado, `P` = Paga al prestador / Al prestador, `C` = Debita en cuenta / Deb en cta. “Debita en cuenta” usa código `C`. Evidencia: `d_cober_orden_cop_vigencias.srd`, `d_copa_prestad_orden.srd`, y `ars_transaccion_items_impresion.sql` muestra leyenda `(debita en cuenta)` cuando `pago_copa = 'C'`. `X` aparece en algunos SPs como marca interna para restar copago, no como opción normal de parametrización.
