# Memory - 2026-06-05

## Durable Work Notes

- WSGenerales / WSafil SOAP resource leak analysis: confirmed DAOs in `WSGenerales` open JDBC connections with `ConectionUtils.getInstance().connect()` and many only closed `CallableStatement` via `closeStatament(cs)`, leaking connections. In `ws_autorizaciones_remotas`, many DAOs receive `Connection` from BOs and BOs close it, so DAO should not close externally owned connections.
- WSGenerales connection-close patch was prepared but not committed. Shared patch path: `/mnt/Agentes/Leandro/ws_generales_close_conexion.patch`. Local copy path: `/home/luispicone/.openclaw/workspace/entregables/ws_generales_close_conexion.patch`. Changes included safe close overloads in `src/java/utils/ConectionUtils.java`, DAO finally blocks closing `ResultSet`, statement, and locally opened connection in order, plus closing `cs2` in `pedidoDAO`. Validation reported: `svn status` showed 23 modified files, no commit; CRLF/ASCII preserved; no old `closeStatament(cs)` remained in modified DAOs; could not compile because `ant` and `javac` unavailable.
- WSGenerales endpoint publication finding: `getLugarAtencion` is SOAP (`@WebService`, `@WebMethod`), not REST. In the local trunk it exists as `src/java/service/getLugarAtencion.java` but is not registered in `web/WEB-INF/sun-jaxws.xml`, `web.xml`, or `nbproject/jax-ws.xml`. Context tested by user: `http://localhost:8080/WSafil/getDocuTipos` works, while `http://localhost:8080/WSafil/getLugarAtencion` does not. Conclusion: local trunk does not publish `getLugarAtencion`; client screenshot likely uses a different deployed WAR/context/config or only shows a configured attempted call.
- WSGenerales unpublished SOAP services count: 18 `@WebService` service classes found, 8 published and 10 unpublished. Unpublished: `getInfoDetalladaAfiliado`, `getTokenAfiliado`, `getPedidosAutorizacion`, `getPedidosAfiliado`, `getPedidosAutoPrestac`, `getLugarAtencion`, `getPrepagas`, `getPedidosGrabar`, `getPedidosConfig`, `getPedidosGrabarAttach`. Published: `Pedido`, `getDocuTipos`, `getParentescos`, `getObraSocial`, `getNacionalidades`, `getPlanes`, `getLocalidades`, `PrecargaSolicitudes`.
- DB/SP performance context: SP `ars_lugares_atencion` responds immediately when run directly outside Java. Prior index check found no missing suspected main index, but `PRESTAD_LUGARES_CIRCU` is a HEAP and estimated plan scanned it filtering `baja_fecha IS NULL`; current table approx. 5,746 rows, so not severe now. Remaining DB risk: `PRESTADORES.cuit` exists but is not composed with `tipo` and `baja_fecha`, causing lookup. Most likely service latency cause is Java resource handling rather than SP.
- Redmine 26274 WebPOS subtype/odontologia work: user asked to base all proposals on SVN repo `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicialAgente`, but not modify the local repository; only read repo objects and place proposed modified files in shared folder for Leandro. Target shared folder: `/mnt/Agentes/Leandro/redmine-26274-webpos-02C-subtipo`.
- Redmine 26274 requirement evolved: previous idea of a combo in `frame_encabezado` was replaced. The odontologia intent must live in `frame_prestacion`, next to the existing guardia checkbox. Add a checkbox to the right named/labelled `Odontologia`. When user checks it, run the same behavior previously triggered by choosing odontologia in the combo. When unchecked, remove odontologia-related headers and inputs.
- Redmine 26274 delivered files in shared folder without local repo changes: `webpos/frames/frame_encabezado.php` with old `Tipo de autorizacion` combo removed; `webpos/frames/frame_prestacion.php` with `Odontologia` checkbox added right of `Guardia`; `webpos/js/prestacion_odonto.js` where `esOdonto()` reads `chk_odontologia` and change event applies previous combo logic; `webpos/js/transac.js` where `subtipoTransac` sends `'O'` when odontologia checkbox is checked and `null` otherwise. Validation reported: `node --check` OK for `prestacion_odonto.js` and `transac.js`; CRLF/no BOM preserved; no combo/text `Tipo de autorizacion` left in shared frame files; `php -l` unavailable.
# Memory - 2026-06-05

## Durable Work Notes

- WSGenerales / WSafil SOAP resource leak analysis: confirmed DAOs in `WSGenerales` open JDBC connections with `ConectionUtils.getInstance().connect()` and many only closed `CallableStatement` via `closeStatament(cs)`, leaking connections. In `ws_autorizaciones_remotas`, many DAOs receive `Connection` from BOs and BOs close it, so DAO should not close externally owned connections.
- WSGenerales connection-close patch was prepared but not committed. Shared patch path: `/mnt/Agentes/Leandro/ws_generales_close_conexion.patch`. Local copy path: `/home/luispicone/.openclaw/workspace/entregables/ws_generales_close_conexion.patch`. Changes included safe close overloads in `src/java/utils/ConectionUtils.java`, DAO finally blocks closing `ResultSet`, statement, and locally opened connection in order, plus closing `cs2` in `pedidoDAO`. Validation reported: `svn status` showed 23 modified files, no commit; CRLF/ASCII preserved; no old `closeStatament(cs)` remained in modified DAOs; could not compile because `ant` and `javac` unavailable.
- WSGenerales endpoint publication finding: `getLugarAtencion` is SOAP (`@WebService`, `@WebMethod`), not REST. In the local trunk it exists as `src/java/service/getLugarAtencion.java` but is not registered in `web/WEB-INF/sun-jaxws.xml`, `web.xml`, or `nbproject/jax-ws.xml`. Context tested by user: `http://localhost:8080/WSafil/getDocuTipos` works, while `http://localhost:8080/WSafil/getLugarAtencion` does not. Conclusion: local trunk does not publish `getLugarAtencion`; client screenshot likely uses a different deployed WAR/context/config or only shows a configured attempted call.
- WSGenerales unpublished SOAP services count: 18 `@WebService` service classes found, 8 published and 10 unpublished. Unpublished: `getInfoDetalladaAfiliado`, `getTokenAfiliado`, `getPedidosAutorizacion`, `getPedidosAfiliado`, `getPedidosAutoPrestac`, `getLugarAtencion`, `getPrepagas`, `getPedidosGrabar`, `getPedidosConfig`, `getPedidosGrabarAttach`. Published: `Pedido`, `getDocuTipos`, `getParentescos`, `getObraSocial`, `getNacionalidades`, `getPlanes`, `getLocalidades`, `PrecargaSolicitudes`.
- DB/SP performance context: SP `ars_lugares_atencion` responds immediately when run directly outside Java. Prior index check found no missing suspected main index, but `PRESTAD_LUGARES_CIRCU` is a HEAP and estimated plan scanned it filtering `baja_fecha IS NULL`; current table approx. 5,746 rows, so not severe now. Remaining DB risk: `PRESTADORES.cuit` exists but is not composed with `tipo` and `baja_fecha`, causing lookup. Most likely service latency cause is Java resource handling rather than SP.
- Redmine 26274 WebPOS subtype/odontologia work: user asked to base all proposals on SVN repo `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicialAgente`, but not modify the local repository; only read repo objects and place proposed modified files in shared folder for Leandro. Target shared folder: `/mnt/Agentes/Leandro/redmine-26274-webpos-02C-subtipo`.
- Redmine 26274 requirement evolved: previous idea of a combo in `frame_encabezado` was replaced. The odontologia intent must live in `frame_prestacion`, next to the existing guardia checkbox. Add a checkbox to the right named/labelled `Odontologia`. When user checks it, run the same behavior previously triggered by choosing odontologia in the combo. When unchecked, remove odontologia-related headers and inputs.
- Redmine 26274 delivered files in shared folder without local repo changes: `webpos/frames/frame_encabezado.php` with old `Tipo de autorizacion` combo removed; `webpos/frames/frame_prestacion.php` with `Odontologia` checkbox added right of `Guardia`; `webpos/js/prestacion_odonto.js` where `esOdonto()` reads `chk_odontologia` and change event applies previous combo logic; `webpos/js/transac.js` where `subtipoTransac` sends `'O'` when odontologia checkbox is checked and `null` otherwise. Validation reported: `node --check` OK for `prestacion_odonto.js` and `transac.js`; CRLF/no BOM preserved; no combo/text `Tipo de autorizacion` left in shared frame files; `php -l` unavailable.

- Redmine 29735 was analyzed using Redmine user `clnani`. Initial answer overfocused on general/Portal behavior; corrected focus is Presmed supervision by level: `wp_lista_auto_pend` opens authorization editable via `iuo_auto.of_consultar_auto_editable(int(au.sucur), long(au.auto))`, but real item/state logic belongs in common ancestor/window `w_clase_auto.srw`.
- Redmine 29735 likely root cause: current SQL validations use `cant_prestac_auto_rem` against per-line quantity (`canti > cant_prestac_auto_rem`), while the reported issue is max number of authorization item lines controlled by `parametros.auto_vali_cant_items`. Distinguish max quantity per prestation/row from max lines per authorization.
- Redmine 29735 proposed solution for supervision: centralize control in `w_clase_auto.srw`, not only `wp_lista_auto_pend`. Existing add-item guard `IF dw_prestac.RowCount() = ii_param_max_items AND ii_param_max_items > 0 THEN` should use `>=`. Add a validation function (e.g. `wf_valida_cantidad_items()`) before `ue_setestado_auto(...)` and before update/save/revalidation so excess lines block save and prevent pending items from being recalculated into accepted. Functional rule: if `dw_prestac.RowCount() > ii_param_max_items`, do not save, do not recalculate states, do not change pending to accepted, show message and leave authorization unchanged.
- Redmine 29735 backend safeguard: add SQL validation counting active lines in `auto_prestac_items` by `sucur` + `auto` and in `auto_prestac_items_remotas` by `transac`, compare with `parametros.auto_vali_cant_items` when > 0, and reject/control if exceeded. SQL is mandatory barrier for Portal/other flows; PowerBuilder UI is prevention.
- Subedatos module identified in `infomedical-trunk`: own PB project/exe `subedatos.pbt` / `subedatos.exe`, main PBL `subedato.pbl`, menu `m_subedatos.srm`, main window `wf_mainsubedatos.srw`, app `subedatos.sra` / `subedatos_svn.sra`. Registered as `menu_id = m_subedatos`. Functional area: imports/homologations for afiliados, certificados, prestadores, productos, odontologia, boletines. Associated backend examples: `afi_importacion.sql`, `afi_import_batch.sql`, `afi_import_batch_isss.sql`, `afi_importacion_ext_upd.sql`, `afs_impor_revision.sql`, `afs_valida_importacion_certificados.sql`, `pru_homolog_presta_sube_datos.sql`. It is not in repo `autorizaciones_remotas`.
- Redmine 29018 analyzed around Afilmed notes imported from Subedatos. Evidence: imported notes show at bottom; manual sort by date works temporarily but reopens wrong; manually loaded Afilmed notes appear above; manual note editor hides first typed characters until cursor advances. Relevant files: `wp_afi_notas.srw`, `d_lista_notas.srd`, `de_afi_notas.srd`, import SP `afi_importar_notas.SQL`, table `afi_notas`.
- Redmine 29018 likely cause: `d_lista_notas.srd` SELECT orders by `afi_notas.baja_fecha ASC, afi_notas.contac_fecha DESC`, but DataWindow has `sort="nro_nota D "`, which can override visual order. Imported notes get sequential `nro_nota` during import, causing visual order by note number rather than contact date.
- Redmine 29018 proposed solution: in `d_lista_notas.srd`, set DataWindow sort to `baja_fecha A contac_fecha D nro_nota D` or remove the sort and rely on SQL; preferred explicit sort with `nro_nota` only as tie-breaker. In `wp_afi_notas.srw`, after `Retrieve`, force `dw_lista.SetSort("baja_fecha A, contac_fecha D, nro_nota D")` then `dw_lista.Sort()` so reopening preserves order. For typing visibility, adjust `de_afi_notas.srd` column `nota` to multiline/autovscroll behavior while keeping `edit.vscrollbar=yes`; current behavior appears one-line/horizontal scroll. Do not touch `afi_importar_notas.SQL` unless tests prove `fecha_nota` lacks expected time precision. Validation plan: import notes with distinct dates, open Afilmed notes, confirm newest first after reopen, and type long manual note verifying first characters are visible.

## Durable Work Notes - 17:37 UTC

- Redmine 26274 WebPOS odontologia controls evolved from free text inputs to closed selects in shared folder `/mnt/Agentes/Leandro/redmine-26274-webpos-02C-subtipo`, specifically `webpos/js/prestacion_odonto.js`. Initial combo validation reused/proposed local JS/PB-derived logic, but user correctly challenged that Presmed and SP behavior must be distinguished.
- Redmine 26274 validation source audit: Presmed PB validates odontologia in `wp_auto_prestaciones.srw`, `wp_expe_odon.srw`, and common logic `uo_nv_liqui_odon.sru`; Presmed sectors are `1..6`, pieces include supernumeraries `19/29/39/49`, and PB rejects `B+L` and `B+P`. However `prs_valida_of_odon_tr` is intended/commented for WP/Activia/remotas and current SP behavior differs.
- Redmine 26274 chain finding: Presmed can call `prs_valida_of_auto_remotas`, which calls `prs_valida_of_auto_items`; odontologia SP `prs_valida_of_odon_tr` is called conditionally when `@aufree IS NOT NULL`. WebPOS also has an explicit odontologia validation path when `subtipo = 'O'`, so the JS must avoid values the current SP will reject.
- Redmine 26274 final user decision: do not touch `prs_valida_of_odon_tr` because it was not requested. Any proposed SP file was removed from the shared delivery. Current shared change is JS-only and aligned to what current SP permits: valid pieces exclude supernumeraries `19/29/39/49`; valid sectors follow `homolog_sectores_odonto` (`1..8`); upper faces limited to `D, M, O, V`; lower faces limited to `D, M, O, V, L`.
- Redmine 26274 latest validation reported: `node --check` OK for `prestacion_odonto.js`; CRLF/no BOM preserved; `backend/procedures/prs_valida_of_odon_tr.sql` no longer exists in shared folder; local SVN working copy was not modified.
