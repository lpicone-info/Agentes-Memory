## 2026-08-21 13:39 ART - PresmedWebInicial / #27848

- Humano: Leandro.
- Repositorio trabajado: `lcamaratta-PresmedWebInicial`, actualizado por SVN a `r84395`.
- Cambios entrantes relevantes de `#27848`:
  - `r84393`: `backend/procedures/crs_planes.sql`
  - `r84395`: `backend/procedures/cri_precarga_solicitud_generar_deriv_aportes.sql`
- Analisis del SP `cri_precarga_solicitud_generar_deriv_aportes.sql`: el `CASO 6` agregado para motivo `13` copia derivaciones vigentes de integrantes del grupo familiar desde `AFI_DERIV_APORTES ADA_G`, solo si coinciden con la derivacion del titular en `PRECARGA_DERIV_APORTES` comparando `vigen_desde`, `obra_soc`, `cuit` y `vigen_hasta` con equivalencia `NULL = NULL`; incluye `NOT EXISTS` contra `SOLI_DERIV_APORTES` para evitar duplicados en la solicitud.
- Observacion funcional previa: habia una modificacion local pendiente en el `CASO 5` removiendo un `NOT EXISTS` contra `AFI_DERIV_APORTES`; con `CASO 6` incorporado, esa remocion parecia obsoleta y riesgosa porque `CASO 5` deberia seguir evitando insertar como nueva una derivacion ya vigente.
- Se documento propuesta de mejora de rendimiento en `/mnt/Agentes/lcamaratta/27848/mejora_rendimiento_cri_precarga_solicitud_generar_deriv_aportes.md`.
- Se genero SQL final de propuesta en `/mnt/Agentes/lcamaratta/27848/cri_precarga_solicitud_generar_deriv_aportes_optimizado.sql`.
- Criterios acordados para el SQL optimizado:
  - Unificar los 6 inserts del motivo `13` mediante `WITH ... UNION ALL` y un unico `INSERT INTO SOLI_DERIV_APORTES`.
  - Usar tablas temporales `#PSA`, `#PDA`, `#ADA` creadas al inicio del SP con `CREATE TABLE` y campos explicitos; no usar `SELECT INTO`.
  - El dato requerido de `PRECARGA_SOLICITUDES` es `apli_fecha`, incorporado en `#PSA`.
  - Reaplicar el uso de temporales tambien para motivos `1` y `2`.
  - No crear indices sobre temporales porque tendran pocos registros.
  - Cargar temporales filtrando registros vigentes: `PSA.baja_fecha IS NULL`, `PDA.baja_fecha IS NULL`, `ADA.baja_fecha IS NULL`.
  - Quitar `baja_fecha` de las temporales y adaptar el codigo posterior para no preguntar por `baja_fecha`, porque ya fue filtrado al cargar.
  - Al llenar `#ADA`, usar `INNER JOIN #PSA` para limitar a afiliados de la precarga.
  - Eliminar las temporales al final del SP con `DROP TABLE`.
- Estado de validacion informado del SQL optimizado: ASCII, CRLF, 714 lineas, sin `RTRIM`, sin `;`, parseado con `SET PARSEONLY ON` sin ejecutar `DROP/CREATE`.

