# Memoria 2026-09-03

- Interaccion con Leandro por Google Chat sobre repositorio SVN `lcamaratta-V25.08.001` y Redmine 29856.
- Se actualizo la working copy `lcamaratta-V25.08.001` desde revision local `84469` a `84510`; quedo sin cambios locales pendientes segun `svn status`.
- Redmine 29856: cambios principales en revisiones SVN `84484`, `84490`-`84495`, `84501`-`84503`. El resto del update correspondia a otros tickets (`30220`, `30265`, `29582`, etc.).
- Funcionalidad RM 29856: agrega tabla `COBER_PRESTAD_PAGO_COPA`, ABM PB `wp_cober_prestad_pago_copa`, menu en Prestadores, SPs `prs_cober_prestad_pago_copa`, `pru_cober_prestad_pago_copa`, `prs_obtener_copago_prestador`, ajuste de autorizaciones PB para reemplazar `pago_copa` calculado por plan/convenio si existe configuracion por prestador, y ajuste en `prs_valida_copago_remoto` para Portal/WS.
- Riesgos detectados RM 29856: `backend/procedures/pru_cober_prestad_pago_copa.sql` tiene `;` al final de `SET @prm_baja_fecha = GETDATE();`, contrario a Regla 25 local sobre SQL sin punto y coma; `exe/Source Infomedical/w_clase_auto.srw` centraliza lectura de criterio por prestador pero callers no evaluan retorno de error de `prs_obtener_copago_prestador`; no se valido contra base por falta de profile DB.
- Analisis solicitado: donde aplicar logica equivalente a `w_clase_auto` para que, luego de obtener `pago_copa`, se reemplace por lo definido en `COBER_PRESTAD_PAGO_COPA`.
- Conclusion tecnica: punto central recomendado en `backend/procedures/pru_expe_copagos.sql`, luego de armar/calcular `#copa` y antes del `IF @es_consul = 'S'` / `INSERT #altas`, aprox. linea 2047. Alli `#copa.pago_copa` ya esta calculado y aun no se grabo en `expe_copagos`.
- Ese punto central cubre cierres/reprocesos llamados desde: `wp_expe_orde.srw` aprox. linea 7959, `wp_expe_hono.srw` aprox. linea 2430, `wp_expe_a_rectificar.srw` aprox. linea 467, `wp_expe_a_revisar_orde.srw` aprox. linea 1054, `wp_expe_a_revisar_hono.srw` aprox. linea 882, `wp_expe_ajus.srw` aprox. linea 2562.
- Para carga manual con calculo visible en pantalla, conviene replicar la consulta en `exe/Source Infomedical/wp_expe_orde.srw::wf_auxi`, aprox. linea 5186, luego de `rut.of_get_cober_prestad(...)` o despues del `FETCH copa INTO :ls_dc,:ls_pago_copa,:le_copa`, y antes de evaluar `IF ls_pago_copa = 'P' THEN` / `IF ls_pago_copa <> 'P' OR lb_exento THEN`.
- Odontologia queda fuera de `pru_expe_copagos`; `uo_nv_liqui_odon.sru` solo llama `pru_expe_topes`. Si el alcance incluye expedientes odontologicos, revisar `wp_expe_odon.srw`: calculo por prestacion aprox. linea 1902, copago por orden lineas 2411-2415, grabacion directa `pru_expe_copa_direct` linea 4356, lectura `pago_copa_ord` linea 4575, grabacion detalle linea 4654, grabacion cabecera/orden linea 4878.
- Correccion posterior de Leandro: en la importacion de prestaciones de expediente si hay calculo de copagos cuando no esta asociado a una autorizacion.
- Se rastreo `wp_import_exp.srw`, evento `ue_importar_amb`: cuando la prestacion no esta asociada a autorizacion, no entra al bloque de validacion de autorizacion aprox. lineas 591-613, pero graba cabecera con `pri_expe_ordenes` aprox. linea 855, item con `pri_expe_orden_items` aprox. linea 907, conceptos temporales con `pri_tmp_expe_import_conceptos` aprox. linea 934, y luego recalcula valores/copagos con `pru_orden_a_liqui_expe` aprox. linea 1028.
- `backend/procedures/pru_orden_a_liqui_expe.sql` baja copagos previos y llama a `pru_expe_copagos` aprox. linea 792. Por eso la importacion ambulatoria de prestaciones sin autorizacion queda cubierta agregando la logica en `pru_expe_copagos.sql`; no conviene agregar funcion PB en `wp_import_exp.srw` para ese caso porque ahi no se obtiene `pago_copa` en pantalla, se delega al recalculo SQL posterior.

- Aclaracion posterior sobre `wp_expe_orde`: `d_expe_orde_item_cons` no trae `pago_copa`; trae `real_valor` y un campo oculto `copago`. Por eso no corresponde modificar `wp_expe_orde` solo por ese dataobject.
- En `wp_expe_orde`, `pago_copa` aparece como variable local `ls_pago_copa` dentro de `wf_auxi` aprox. linea 4844. Se carga desde `rut.of_get_cober_prestad` aprox. linea 5189 y, si no hay valor, desde `prs_copago` aprox. linea 5205.
- En `wp_expe_orde::wf_auxi`, la resta asociada a `ls_pago_copa = 'P'` afecta el valor auxiliar visible/calculado en pantalla (`auxi_uni`), aprox. lineas 5217 y 5235; no graba directamente `expe_copagos`.
- Conclusion corregida: para persistencia final en expedientes de ordenes, manda el cierre de `wp_expe_orde`, que llama `rut.of_expe_topes_copagos` aprox. linea 7959; esa rutina ejecuta `pru_expe_copagos` desde `uo_nv_liquida.sru` aprox. linea 971. Si la logica de `COBER_PRESTAD_PAGO_COPA` queda centralizada en `pru_expe_copagos`, no hace falta tocar `wp_expe_orde` para el grabado final; solo seria necesario si se busca consistencia visual/auxiliar en pantalla antes del cierre.
