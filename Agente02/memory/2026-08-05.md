
## 2026-08-05 13:48 ART - Conversacion Google Chat con Cesar sobre PB V25.08.001

- Cesar consulto en que librerias PB estan las ventanas relacionadas con coberturas/copagos en la rama/repo `V25.08.001`.
- Resultado relevante: no estan en una sola PBL. Principales referencias encontradas:
  - `planes3.pbl`: `wr_planes_cober_varios`, `wr_planes_inter_cober`, `wr_planes_orden_copagos`, `wr_planes_orden_copagos_capi`, `wp_planes_orden_copagos_tipos`, `wp_copa_aumentos_xpresta`.
  - `planes2.pbl`: `wr_planes_copag`, `wr_planes_copag_def`, `wr_asoc_cober_copa_grupos_rango_cantidad`.
  - `prestad2.pbl`: `wp_prestad_plan_copa`, `wp_prestad_exen_copa`, `wr_prestad_exen_copa_a_revisar`, `wr_prestad_plan_copa_err`, `wr_planes_prestad_orden_copagos`.
  - `padron3.pbl`: `wp_abm_afi_exen_copagos`, `wp_cons_afi_exen_copagos`, `wr_seguimiento_exen_copagos`.
  - `bonos.pbl`: `wp_bonos_copago`.
  - `liqui10.pbl`: `wp_conscopagoafi`.
  - `liqui12.pbl`: `wp_expor_copagos`.
  - `conve3.pbl`: `wr_conve_copagos`.
  - `farma2.pbl`: `wr_planes_cober_farma`, `wp_produ_cober_exclu`, `wr_flias_cober_exclu`.
  - `wp_estados_cobertura.srw` aparecio como fuente, pero no se encontro entrada asociada en `.pbg`; `dd_estados_cobertura.srd` aparece en `afinstal.pbl`.
- Cesar pregunto de donde salio `wp_moti_traslado`. Evidencia directa en `V25.08.001`:
  - `exe/Source Infomedical/ambulancias.pbg:34`: `"wp_moti_traslado.srw" "ambulancias.pbl"`.
  - `exe/Source Infomedical/m_main.srm:5798`: `opensheet(wp_moti_traslado, wf_mainpresta, 1, original!)`.
  - `exe/TRUNK.pbc:28782`: referencia compilada a `wp_moti_traslado.srw`.
  - Fuente existente: `exe/Source Infomedical/wp_moti_traslado.srw`.
  - Conclusion respondida: la ventana esta en `ambulancias.pbl`.
- Cesar aclaro que queria comparar el codigo propuesto para crear `wp_cober_prestad_pago_copa` contra un ABM de prestaciones, no contra `wp_moti_traslado`.
- Revision respondida:
  - La propuesta original basada en `wp_moti_traslado` / `w_clase_abm_simple` era funcionalmente valida como ABM simple, pero insuficiente para replicar el patron de prestaciones.
  - Patrones correctos a usar: `wp_prestacion` o `wp_prestacion_sincapi` en `nomenc.pbl` para ABM de prestaciones; y `wp_prestad_plan_copa` en `prestad2.pbl` como referencia especifica de negocio de copagos por prestador.
  - Cambios necesarios para `wp_cober_prestad_pago_copa`: validar `prestad` contra prestadores (`val.of_prestador(...)` y/o busqueda `wr_busprestad`), usar DDLB para `pago_copa` con valores `P = Paga al prestador`, `C = Debita en cuenta`, `A = Paga por adelantado`, manejar recuperacion de registros dados de baja si existe la combinacion, agregar `ResetUpdate`, control de cambios, estados de botones luego de grabar/limpiar, y seguir eventos/validaciones de prestaciones (`ue_previoupdate`, `ue_buscar`, `ue_consulta`).
  - Se indico que no se encontro `wp_cober_prestad_pago_copa` existente en la working copy y que existen estructuras cercanas como `cober_prestad_copagos` y `cober_prestad_copa_orden`.
