# Memoria persistente - 2026-06-12

- En esta instalación, cuando el usuario diga "carpeta compartida", debe interpretarse como `/mnt/Agentes/Leandro`.
- Para próximos checkouts SVN con usuario `lcamaratta`, existe credencial local aislada reutilizable desde working copies SVN previas. No mostrar ni registrar la contraseña en memoria general ni en `REPOSITORIOS.md`; usar archivos locales aislados por working copy.
- Working copy SVN relevante: `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicial` descargada en `~/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial`, usuario SVN `lcamaratta`.
- Revisión conocida luego del último update de `PresmedWebInicial`: `83710`. `svn status` estaba limpio.
- Desarrollo funcional actual: Redmine `#26274 Portal de Prestadores: Transaccionar autorizaciones odontologia` sobre rama SVN `PresmedWebInicial`.
- El usuario pidió no modificar directamente la working copy SVN para las correcciones actuales; tomar archivos desde la working copy y dejar copias modificadas en `/mnt/Agentes/Leandro/26274`.
- Archivos ya dejados en `/mnt/Agentes/Leandro/26274`: `webpos/js/auto_busqueda.js`, `webpos/js/transac_busqueda.js`, `webpos/js/prestacion.js`.
- Correcciones aplicadas en esas copias: en `auto_busqueda.js` y `transac_busqueda.js`, reemplazo de referencias DOM frágiles `children()[0/1/3]` sobre `#tablaPrestacionBody` por búsquedas por `name` usando `ultimo.find("[name='prestac']")`, `ultimo.find("[name='nomen']")`, `ultimo.find("[name='canti']")`; en `prestacion.js`, reemplazo de `$(fila).parent().parent().remove()` por `$(fila).closest(".row-container").remove()`.
- Contexto técnico: los cambios de base de datos para `@prm_lista_odonto` y `@prm_subtipo` todavía no fueron implementados/aplicados; el usuario confirmó que se harán próximamente. La pantalla `02C.php` luego se usará para odontología.
- Bug de `frame_prestacion.php` con HTML `</div>';` en caso `framePrestacion(false, true)` fue corregido por el usuario y actualizado en SVN revisión `83710`.
