# Memoria curada diaria

- Fecha: 2026-07-22
- Usuario: Cesar Luis Nani
- id_googlechat: users/104086917411623335141
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/xhkrbyaaaae

## Resumen de la actividad

### Tema 1: Prestaciones duplicadas en `wp_prestacion`

- Solicitud o contexto: Cesar pidio verificar si la ventana `wp_prestax` o equivalente controla que una prestacion no se repita en mas de un nomenclador.
- Respuesta o acciones del agente: Se reviso el codigo y se identifico que la ventana real parecia ser `exe/Source Infomedical/wp_prestacion.srw`. Se encontro que consulta `prs_prestaciones_nomenc` y bloquea cuando la prestacion activa pertenece a otro nomenclador, pero que existe una validacion defensiva en el evento `sqlpreview` del DataWindow interno `dwa_datos` antes del insert.
- Resultado: Se concluyo que el control existe parcialmente. En el flujo normal, si hay una prestacion activa en otro nomenclador se muestra mensaje y se corta; si solo hay duplicadas dadas de baja, se avisa y se habilita el alta mediante `wf_carga_vacia`. El mensaje `La prestacion esta siendo utilizado ya en otro nomenclador. Desea darla de alta?` esta en `sqlpreview` y probablemente solo aparece en caminos no habituales, cambios concurrentes o bypass del flujo de busqueda.
- Estado: Informativo

### Tema 2: Analisis de deadlocks en `TEMP_COPAGOS`

- Solicitud o contexto: Cesar pidio analizar un CSV con eventos `xml_deadlock_report` de SQL Server.
- Respuesta o acciones del agente: Se analizo el archivo completo y se cruzo con el codigo fuente del procedimiento `prs_valida_copago_remoto`.
- Resultado: Se identificaron 36 deadlocks entre las 08:05 y 09:04 hora Argentina del 2026-07-21, concentrados en `SMC_Prod.dbo.TEMP_COPAGOS`. La causa probable fue el uso de una tabla permanente como temporal por `spid`, en heap, sin PK ni indices, con operaciones concurrentes de borrado/lectura que tomaban locks `RID`. Se recomendo revisar la estrategia de tabla temporal o indexacion para reducir barridos y bloqueos.
- Estado: Informativo

### Tema 3: Codigos de error de `prs_valida_of_cobertura`

- Solicitud o contexto: Cesar pregunto el significado de los errores 9, 10, 11, 12, 15, 58 y 80 del procedimiento `prs_valida_of_cobertura`.
- Respuesta o acciones del agente: Se cruzaron comentarios y asignaciones de `@erro` en `prs_valida_of_cobertura.sql` y `prs_valida_of_cobertura_tope.sql` con el catalogo `SMC_Prod.dbo.recha_motivos`.
- Resultado: Se informo que 9 corresponde a cobertura inexistente, 10 a cobertura dada de baja, 11 a carencias, 12 a requiere autorizacion, 15 a prestador demandante solo de cartilla, 58 a fecha de prestacion anterior a autorizacion y 80 a autorizacion inexistente. Se aclaro que 58 esta en catalogo y comentario pero el seteo estaba comentado en el procedimiento, y que 15 se observa en la variante de cobertura/tope.
- Estado: Informativo

### Tema 4: Relacion entre numeros de error y `recha_motivos`

- Solicitud o contexto: Cesar pregunto como se sabia que los numeros de error correspondian al texto funcional de `recha_motivos`, y donde se encontraba esa relacion.
- Respuesta o acciones del agente: Se busco evidencia de uso en SQL y DataWindows. Se encontro una union entre transacciones remotas y `recha_motivos` por `recha`, y DataWindows que muestran `recha_motivos.deno` usando `recha_motivos.recha` como columna de dato.
- Resultado: Se explico que el numero de error se guarda o consulta como codigo `recha`, y el texto funcional sale de `recha_motivos.deno`. Se citaron como evidencia `prs_cons_transac_remotas_det.sql`, `dd_recha_motivos_aut.srd`, `d_auto_cabe_inter.srd` y la definicion de tabla `backend/tables/recha_motivos.sql`.
- Estado: Informativo

### Tema 5: Uso de `@cod_interno` en `prs_valida_of_cobertura`

- Solicitud o contexto: Cesar pregunto para que sirve `@cod_interno` en `prs_valida_of_cobertura`.
- Respuesta o acciones del agente: Se reviso la declaracion y los puntos donde el procedimiento evalua `@cod_interno = 'C'`.
- Resultado: Se concluyo que `@cod_interno` no cambia la logica de validacion, sino la forma de salida. Cuando vale `C`, el procedimiento devuelve un resultset con los datos calculados de cobertura y `@erro`; cuando no vale `C`, el llamador debe consumir los parametros `OUTPUT`.
- Estado: Informativo

### Tema 6: Proyecto `api` del trunk

- Solicitud o contexto: Cesar pregunto en que lenguaje esta el proyecto `api` y luego pidio los comandos para levantarlo.
- Respuesta o acciones del agente: Se reviso `api/package.json` y la estructura de archivos bajo `api/src`.
- Resultado: Se informo que el proyecto `api` esta en JavaScript sobre Node.js, usa Express y la dependencia `mssql`, y que sus fuentes principales son `.js`. Se indicaron los comandos `npm install`, `npm run dev` para ejecutar con `nodemon`, y `npm start` para ejecutar con `node src/index.js`.
- Estado: Informativo
