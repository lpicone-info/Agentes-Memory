# 2026-05-21

## Presmed Web / Redmine 29526
- Contexto operativo: repositorio SVN `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicialAgente`, working copy registrada como `lcamaratta-PresmedWebInicialAgente`. Usuario Redmine indicado por el humano: `lcamaratta`.
- Analisis del Redmine `29526`: el problema reportado sobre el campo `Token` en `Informar Prestacion` (`02A`) apunta a frontend. `auto_busqueda.js` ejecuta `bloquear_campos(true)` luego de recuperar una autorizacion, y `transac.js` bloquea los `input/select` de `#contenido` salvo `.no-block`. El `#token` generado por `frame_afiliado.php` queda deshabilitado.
- Criterio acordado: no usar clase `no-block` para `#token`, porque seria una excepcion permanente y demasiado amplia. La alternativa acordada es re-habilitar `#token` inmediatamente despues de `bloquear_campos(true)` dentro del flujo de `auto_busqueda.js`.
- Alcance funcional revisado: `auto_busqueda.js` se carga desde `02A`, `02D`, `02E`, `02Q`, `03A`, `05A`. `02E` y `03A` no renderizan token. `02A`, `02D`, `02Q`, `05A` renderizan token.
- Criterio pendiente/seguro para token: aplicar la re-habilitacion a las transacciones con token, pero exceptuar `05A` por ahora porque aunque muestra `#token`, el flujo backend actual no lo usa igual que las otras transacciones. Si se requiere incluir `05A`, conviene preguntar/confirmar codigo de transaccion y alcance funcional antes de cambiar backend.
- No se aplico cambio de codigo para el fix de token del Redmine `29526` en esta parte del trabajo; quedo como analisis y recomendacion.

## Presmed Web / Ajuste UI boton generacion
- Pedido del humano: que el boton de guardar quede al final abajo, fuera del area/card de datos de autorizacion, que diga `Generar`, y que se parezca al flujo de Afilmed Web para generacion de precarga.
- Cambio aplicado: se saco el boton del card `Datos de Autorizacion`, se ubico al final del formulario debajo del area de datos, se cambio el texto de `Guardar fechas` a `Generar`, y se uso estilo tipo boton `success` con margen `m-2`.
- Archivos impactados informados al humano: `AutorizacionDatosGral.tsx` y `AutorizacionCarga.tsx`.
- Validacion ejecutada: `npm run build` finalizo OK.
- Despliegue/preview disponible para prueba: `http://10.194.0.30:4173/` y `http://10.194.0.30:4174/`. Backend Node escuchando en puerto `5003`.
- No se hizo commit.
