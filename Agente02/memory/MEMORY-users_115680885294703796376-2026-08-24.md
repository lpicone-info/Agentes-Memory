# Memoria de conversación — Leandro Camaratta

## Datos

- **Fecha:** 24/08/2026
- **Usuario:** Leandro Camaratta
- **ID Google Chat:** `users/115680885294703796376`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 61
- **Inicio:** 11:22:49
- **Fin:** 16:41:54

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/qf8sryaaaae`

## Archivos revisados

- `d212601a-17a8-4109-9572-9b1d1f45f4a0.jsonl`

---

## Transcripción completa

### [24/08/2026 11:22:49] Leandro Camaratta

hola

---

### [24/08/2026 11:24:03] Agente

Hola Leandro, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [24/08/2026 11:24:52] Leandro Camaratta

Me esta dando este error en afilme legacy en la rama PresmedWebInicial con la base de datos Ospecon_LC. Por que? La semana pasada funcionaba

---

### [24/08/2026 11:31:07] Agente

Leandro, por el mensaje y por el código, la causa más probable no es `Ospecon_LC` ni un procedure: es carga de DLL nativa en el cliente legacy.

El error sale en [uo_nv_cryptlib.sru](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/uo_nv_cryptlib.sru:18), donde PowerBuilder declara:

`library "infocrypt.dll" ALIAS FOR "info_encriptar_texto;Ansi"`

Y se dispara durante el control de licencia de Afilmed, en [wf_main_afi.srw](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/wf_main_afi.srw:111). Ese control ejecuta un test de cifrado al iniciar, por eso cae aunque todavía no estés haciendo una operación funcional concreta.

Validé además:

- En la rama `PresmedWebInicial` la DLL existe: `exe/InfoCrypt.dll`.
- También existe su dependencia: `exe/infolibeay32.dll`.
- `InfoCrypt.dll` no cambió recientemente; viene desde SVN rev `58` de 2008.
- Los commits del 17 al 24/08 tocaron frontend y procedures de `#27848`, no tocaron el legacy PB ni esa DLL.
- `InfoCrypt.dll` es `PE32`, o sea 32 bits, y depende de `infolibeay32.dll`.

Conclusión operativa: si la semana pasada funcionaba y ahora no, lo más probable es que estés ejecutando `afilia.exe` desde una carpeta donde falta `InfoCrypt.dll` o falta `infolibeay32.dll`, o que cambió el runtime/entorno de ejecución y está intentando cargar una DLL 32-bit desde un proceso incompatible. Revisá que en la misma carpeta desde donde corre `afilia.exe` estén ambas:

`InfoCrypt.dll`  
`infolibeay32.dll`

Si estás corriendo desde PowerBuilder IDE, también puede pasar porque la búsqueda de DLL no apunta al `exe` de la working copy. En ese caso hay que ajustar el “Start in”/PATH o copiar ambas DLL al directorio efectivo de ejecución.

---

### [24/08/2026 11:45:05] Leandro Camaratta

esas 2 dlls estan presentes. Lo estoy corriendo desde powerbuilder

---

### [24/08/2026 11:48:02] Agente

Leandro, entonces el foco queda en PowerBuilder IDE, no en la base.

Validé que `InfoCrypt.dll`, `infolibeay32.dll` y `afilia.exe` son 32 bits. Si desde PB 2022 estás ejecutando/debuggeando en 64 bits, la DLL va a fallar aunque esté presente. Tenés que correr el target en 32-bit/x86.

Además hay otro punto probable: las DLL están en:

`/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe`

pero también existe target en:

`exe/Source Infomedical/afilia.pbt`

Si abriste ese `.pbt` desde `Source Infomedical`, PowerBuilder puede estar resolviendo `infocrypt.dll` desde esa carpeta, donde las DLL no están. Para probar rápido:

1. Abrí el workspace `exe/TRUNK.pbw` o el target `exe/afilia.pbt`, no el de `exe/Source Infomedical`.
2. Confirmá que el Run/Debug esté en 32-bit.
3. Si querés correr sí o sí desde `Source Infomedical`, agregá la carpeta `...\PresmedWebInicial\exe` al `PATH` antes de abrir PowerBuilder, o poné esas dos DLL también en el directorio efectivo desde donde corre el IDE.

Conclusión: si la semana pasada funcionaba, lo más probable es que cambió una de estas dos cosas: abriste el target desde otra ubicación, o PowerBuilder quedó ejecutando en 64-bit.

---

### [24/08/2026 12:11:46] Leandro Camaratta

Por que no me está trayendo el registro de derivación de aportes si existe en la tabla soli_deriv_aportes con esa soli en ospecon_lc ?

---

### [24/08/2026 12:15:30] Agente

Leandro, no lo trae porque el registro de `soli_deriv_aportes` existe, pero para `item = 2` no tiene su correspondiente registro en `soli_afiliados`.

Validé en `Ospecon_LC`:

```sql
soli_afiliados
136913 | item 1 | inte 00 | paren T | PERONGA CARLOS

soli_deriv_aportes
136913 | item 1 | JOIN OK
136913 | item 1 | JOIN OK
136913 | item 2 | SIN SOLI_AFILIADOS
```

El DataWindow de la grilla inferior (`d_soli_deriv_aportes_fami`) ejecuta `dbo.afs_soli_deriv_aportes_inte`, y ese SP hace:

```sql
INNER JOIN soli_deriv_aportes SD
  ON SD.soli = SA.soli
 AND SD.item = SA.item
...
WHERE SA.soli = @prm_soli
  AND SA.paren <> 'T'
```

Entonces:

- Los registros `item = 1` no entran en la grilla inferior porque son del titular (`paren = 'T'`).
- El registro `item = 2` tampoco entra porque no existe `SOLI_AFILIADOS` para `soli = 136913` + `item = 2`.
- Resultado del SP: `0 filas`.

La fila `PEREZ PEPITO` que ves en pantalla parece estar en memoria/en la UI, pero no está persistida en `soli_afiliados` para esa solicitud. Por eso la derivación de aportes queda huérfana y legacy no la puede asociar al familiar.

---

### [24/08/2026 12:18:09] Leandro Camaratta

Eso es porque desde afilmed web para esta nueva solicitud tipo 13 no estoy insertando en soli_afiliados. Hay otra tabla mas que deberia tener registro? Como podria agregar a todos los integrantes para este tipo de solicitud?

---

### [24/08/2026 12:25:16] Agente

Leandro, para este caso no falta “otra tabla” funcional para que aparezca la derivación. Falta que el integrante exista en `SOLI_AFILIADOS`.

Validé en `Ospecon_LC` para `soli = 136913`:

- `PRECARGA_SOLI_AFILIADOS`: tiene item 1 titular y item 2 familiar.
- `PRECARGA_DERIV_APORTES`: tiene derivación para item 2.
- `SOLI_DERIV_APORTES`: tiene derivación para item 2.
- `SOLI_AFILIADOS`: solo tiene item 1. No existe item 2.

La causa está en [cri_precarga_solicitud_generar.sql](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/backend/procedures/cri_precarga_solicitud_generar.sql:213):

```sql
WHERE SA.id_precarga = @prm_id_precarga
AND   SA.baja_fecha IS NULL
AND   (SA.editable = 'S' OR SA.paren = 'T')
```

En la precarga, `PEREZ PEPITO` está con `editable = 'N'`. Entonces el SP lo usa para calcular `SOLI_DERIV_APORTES`, porque la lógica de tipo 13 contempla grupo familiar, pero no lo inserta en `SOLI_AFILIADOS`. Ahí queda huérfano.

Para tipo 13, ajustaría ese filtro así:

```sql
AND (
      SA.editable = 'S'
   OR SA.paren = 'T'
   OR S.soli_moti = 13
)
```

Con eso, al generar una solicitud tipo 13, pasan todos los integrantes de `PRECARGA_SOLI_AFILIADOS` a `SOLI_AFILIADOS`, incluidos los no editables, y las derivaciones familiares tienen contra qué hacer join.

No tocaría `SOLI_DOMICILIOS`, `SOLI_MAILS`, `SOLI_CELULARES` ni `SOLI_OPCION` para resolver este síntoma. Para la grilla de derivación alcanza con consistencia entre `SOLI_AFILIADOS.item` y `SOLI_DERIV_APORTES.item`.

---

### [24/08/2026 12:34:40] Leandro Camaratta

Estoy cargando la solicitud 136915 pero para la vigencia de 2025 08 01 me está poniendo la ventana 2026 08 01 sin editar. No me está tomando la vigencia original de la solicitud cargada en afilmed web. Por que? Deberia entonces para las nuevas no dejar poner una vigencia anterior al aplica?

---

### [24/08/2026 12:37:08] Agente

Leandro, la causa es que la solicitud `136915` quedó generada con `apli_fecha = 2026-08-01`.

En `Ospecon_LC` vi esto:

```sql
PRECARGA_SOLICITUDES.apli_fecha = 2026-08-01
SOLICITUDES.apli_fecha          = 2026-08-01
```

Y en `PRECARGA_DERIV_APORTES` tenés una línea con `vigen_aporte = 2025-08-01`, pero cerrada con `vigen_hasta = 2026-01-01`. Entonces, para la aplicación `2026-08-01`, esa ventana ya no está vigente.

El origen está en `cru_genera_precarga_tmp`: carga la solicitud con el primer día del mes actual:

```sql
SET @var_primer_dia = dbo.afn_PrimerDiaDelMes(GETDATE())
...
apli_fecha = @var_primer_dia
```

Y la lógica de tipo 13 después busca derivaciones vigentes a la fecha de aplicación:

```sql
PDA_A.vigen_aporte <= PSA_T.apli_fecha
AND (PDA_A.vigen_hasta IS NULL OR PDA_A.vigen_hasta > PSA_T.apli_fecha)
```

Por eso te “corre” la ventana a `2026-08-01`: no está tomando una vigencia de aplicación cargada desde Afilmed Web; la calcula el SP con la fecha actual.

Sí: para **nuevas derivaciones** no debería dejar cargar `vigenDesde < apli_fecha`, salvo que también permitas que la solicitud tenga una `apli_fecha` retroactiva. Si no, generás historia retroactiva dentro de una solicitud que aplica en 2026, y la lógica termina usando la ventana vigente al aplica.

Criterio que aplicaría:

- Nueva derivación: `vigenDesde >= apli_fecha`.
- Modificación/cierre de una derivación existente: puede conservar `vigen_desde` anterior, porque representa una ventana histórica real.
- Si Afilmed Web debe respetar una vigencia de aplicación elegida por el usuario, hay que agregar `@prm_apli_fecha` a `cru_genera_precarga` / `cru_genera_precarga_tmp` y no usar siempre `GETDATE()`.

---

### [24/08/2026 12:39:30] Leandro Camaratta

No, tiene que seguir una linea parecida a afilmed legacy. Por lo que veo ahi no permite elegir la vigencia desde directamente. Estoy en lo cierto?

---

### [24/08/2026 12:40:57] Agente

Sí, estás en lo cierto.

En legacy, `vigen_desde` de derivación de aportes está visible pero protegido:

- Titular: [d_soli_deriv_aportes_titu.srd](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/d_soli_deriv_aportes_titu.srd:106) tiene `protect="1"` para `vigen_desde`.
- Familiar: [d_soli_deriv_aportes_fami.srd](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/d_soli_deriv_aportes_fami.srd:73) también tiene `protect="1"` para `vigen_desde`.

O sea: el usuario no elige libremente la vigencia desde en esa grilla. Legacy la muestra y la usa, pero no la edita directamente.

Entonces para Afilmed Web, si querés seguir una línea similar al legacy, no conviene permitir editar `vigenDesde` manualmente para nuevas derivaciones. La web debería calcularla/setearla desde la fecha de aplicación de la solicitud.

El problema actual es que en web estás mandando `vigenDesde` en cada derivación, pero `cru_genera_precarga_tmp` fija `apli_fecha` con `GETDATE()` primer día del mes. Eso deja dos fechas con lógicas distintas.

Criterio alineado a legacy:

- La solicitud define la `apli_fecha`.
- La derivación nueva toma `vigen_desde = apli_fecha`.
- `vigenDesde` no debería ser editable en web para tipo 13.
- Para registros existentes, se muestra su `vigen_desde` histórico, pero no se modifica como campo libre.

---

### [24/08/2026 12:42:58] Leandro Camaratta

Pre-compaction memory flush. Store durable memories only in memory/2026-08-24.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-24.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-24-HHMM.md); always use the canonical 2026-08-24.md filename. If nothing to store, reply with NO_REPLY.
Current time: Monday, August 24th, 2026 - 12:42 PM (America/Buenos_Aires)
Reference UTC: 2026-08-24 15:42 UTC

---

### [24/08/2026 12:43:48] Agente

NO_REPLY

---

### [24/08/2026 12:43:50] Leandro Camaratta

Pero si modifica una vigencia pasada, y le pone la vigencia hasta, como se crea en la solicitud en afilmed legacy? Por que aca si tenia una vigencia pasada en la solicitud, me la sobreescribio con el aplica?

---

### [24/08/2026 12:44:41] Agente

En legacy, si modificás una ventana pasada, no se “recrea” cambiando el `vigen_desde` al aplica.

El camino es este:

- `vigen_desde` está protegido en la grilla.
- La fila conserva su clave original: `soli + item + vigen_desde + obra_soc + cuit`.
- Si el usuario carga `vigen_hasta`, legacy llama a `afu_soli_deriv_aportes`.
- Ese SP hace `MERGE` por esa misma clave y actualiza `vigen_hasta`, sin reemplazar `vigen_desde` por `apli_fecha`.

O sea, legacy trata una ventana histórica como ventana histórica: le cambia datos/cierre, pero no le mueve el inicio.

En tu caso `136915`, por lo que vi en base, la fila 2025 no fue exactamente sobrescrita:

```text
item 1 | vigen_desde 2025-08-01 | vigen_hasta 2026-01-01
item 1 | vigen_desde 2026-08-01 | vigen_hasta NULL
item 2 | vigen_desde 2026-08-01 | vigen_hasta NULL
```

Lo que pasó es distinto: la solicitud web nació con `apli_fecha = 2026-08-01`, y además terminó generando una ventana nueva vigente a esa fecha. Para el familiar, el SP de web busca la derivación del titular vigente al `apli_fecha`:

```sql
PDA_A.vigen_aporte <= PSA_T.apli_fecha
AND (PDA_A.vigen_hasta IS NULL OR PDA_A.vigen_hasta > PSA_T.apli_fecha)
```

Como la ventana 2025 tiene `vigen_hasta = 2026-01-01`, no está vigente al `2026-08-01`. Entonces toma/propaga la ventana `2026-08-01`.

Entonces sí: para seguir legacy, en web habría que separar:

- **Modificar ventana existente:** no permitir cambiar `vigenDesde`; mantener el original y permitir solo `vigenHasta`/datos editables.
- **Crear ventana nueva:** no dejar elegir `vigenDesde`; setearla automáticamente con `apli_fecha`.
- **Familiares:** derivarlos desde la ventana vigente del titular al `apli_fecha`.

El error conceptual actual es que web deja que convivan una edición histórica y una creación nueva sin una regla clara de UI. Legacy lo evita porque `vigen_desde` no es un campo libre.

---

### [24/08/2026 12:52:14] Leandro Camaratta

Como hago para no permitir modificar la vigen_desde en afilmed web?

---

### [24/08/2026 12:52:58] Agente

En el código actual de `precarga_soli`, `vigenDesde` no está renderizado en [TabObraSocial.jsx](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/precarga_soli/frontend/src/precarga/components/tabs/TabObraSocial.jsx:20). La grilla solo muestra OS, CUIT, empresa y sueldo. Pero cuando editás una precarga, `retrieveDeriva` trae `vigenDesde` y queda dentro del objeto `deriva`.

Para bloquearlo bien, haría dos cosas:

1. **Frontend:** no agregar ningún input editable para `deriva.vigenDesde`. Si necesitás mostrarlo, mostrarlo como texto/readonly, no como campo Formik editable.

2. **Antes de guardar:** para derivaciones nuevas (`nuevo = 'S'`), no mandar `vigenDesde` desde la web. Que el SQL lo tome desde la aplicación de la solicitud. Para derivaciones existentes (`nuevo = 'N'`), sí conservar `vigenDesde` como dato técnico oculto, porque identifica la ventana que se está modificando.

Ejemplo en `createPrecarga`, antes del POST:

```js
const integrantesNormalizados = integrantes.map((inte) => ({
  ...inte,
  deriva: inte.deriva.map((d) => {
    if (d.nuevo === "S") {
      const { vigenDesde, ...rest } = d;
      return rest;
    }

    return d;
  }),
}));
```

Y en el body:

```js
integrantes: integrantesNormalizados,
```

Con eso:

- Si es una ventana existente, viaja su `vigenDesde` original para que el SP sepa cuál modificar.
- Si es una derivación nueva, la web no decide la vigencia.
- `cru_genera_precarga_tmp` termina usando su default actual: `@var_primer_dia`.

Después, si quieren que la vigencia nueva sea exactamente la `apli_fecha` elegida por Afilmed Web, ahí falta otro cambio: agregar `apliFecha` como campo de solicitud y pasarlo al SP. Pero para “no permitir modificar `vigen_desde`”, el bloqueo correcto es ese: oculto/readonly en UI y no enviarlo para altas nuevas.

---

### [24/08/2026 12:55:55] Leandro Camaratta

Pero lo unico que hay que hacer es <FormInput
 lg={2}
 md={6}
 name={deriva.${index}.vigenDesde}
 label="Vigencia Desde"
 type="date"
 disabled={true}
 />

en TabObraSocial

---

### [24/08/2026 12:56:10] Agente

Sí, para bloquearlo visualmente en Afilmed Web alcanza con agregar ese `FormInput` deshabilitado en `TabObraSocial`.

Solo cuidá la sintaxis del `name`, tiene que ir como template string:

```jsx
<FormInput
  lg={2}
  md={6}
  name={`deriva.${index}.vigenDesde`}
  label="Vigencia Desde"
  type="date"
  disabled={true}
/>
```

Con eso el usuario no lo puede modificar desde la pantalla.

Criterio: para registros existentes va a mostrar la vigencia que viene de `retrieveDeriva`; para registros nuevos, si no seteás `vigenDesde` al hacer `push`, quedará vacío y el SP va a usar el default actual (`@var_primer_dia`). Si querés que se vea desde el alta, agregalo también en el `push`, pero deshabilitado.

---

### [24/08/2026 12:59:56] Leandro Camaratta

Pero para los nuevos ya esta seteando la primer fecha del mes. No estas viendo el codigo

---

### [24/08/2026 13:00:28] Agente

Sí, tenés razón. Lo correcto para tu caso es más simple.

El código ya hace esto en SQL:

```sql
vigen_aporte = ISNULL(NULLIF(T.c.value('(vigenDesde)[1]', 'DATE'), '19000101'), @var_primer_dia)
```

O sea: si `vigenDesde` no viene en el XML, lo setea con el primer día del mes. Entonces no hace falta normalizar ni quitar nada en `createPrecarga`.

Para no permitir modificarlo en web, alcanza con agregar el campo deshabilitado en `TabObraSocial`:

```jsx
<FormInput
  lg={2}
  md={6}
  name={`deriva.${index}.vigenDesde`}
  label="Vigencia Desde"
  type="date"
  disabled={true}
/>
```

Eso respeta el flujo actual: existentes muestran su `vigenDesde`; nuevos no lo puede tocar el usuario y SQL completa con `@var_primer_dia`.

Mi respuesta anterior sobre limpiar el payload fue de más para este código.

---

### [24/08/2026 13:13:53] Leandro Camaratta

Cuando desde afilmed web tome un contrato con una derivación de aportes, le asigne una vigencia hasta y luego agregue otra vigencia desde con 01 08 2026 y con vigen hasta null, generando ambos registros para el integrante, luego al abrir la solicitud de afilmed legacy, me va a recuperar todo correctamente con esa vigencia original?

---

### [24/08/2026 13:16:30] Agente

Sí, si Afilmed Web genera estos dos registros en `SOLI_DERIV_APORTES` para el mismo `soli + item`:

```text
vigen_desde = vigencia original | vigen_hasta = 2026-08-01
vigen_desde = 2026-08-01       | vigen_hasta = NULL
```

legacy debería recuperarlos correctamente, manteniendo la vigencia original de la primera ventana.

La clave es que la fila vieja no se “sobrescriba”. Tiene que viajar con su `vigenDesde` original desde la web, aunque el input esté `disabled`, porque esa fecha forma parte de la clave:

```sql
soli + item + vigen_desde + obra_soc + cuit
```

En legacy, `afs_soli_deriv_aportes_inte` recupera las derivaciones por `SOLI_AFILIADOS + SOLI_DERIV_APORTES` y no reemplaza `vigen_desde` por el aplica. Entonces va a mostrar las ventanas existentes de la solicitud siempre que:

- exista el integrante en `SOLI_AFILIADOS`;
- ambas filas estén en `SOLI_DERIV_APORTES`;
- la fila vieja tenga `vigen_hasta`, no `baja_fecha` técnica;
- la fila nueva tenga `vigen_desde = 2026-08-01` y `vigen_hasta = NULL`.

El riesgo concreto sería que la web mande la fila vieja sin `vigenDesde`. Ahí `cru_genera_precarga_tmp` le pone default con `@var_primer_dia`, y ahí sí perderías la vigencia original. Por eso el campo puede estar deshabilitado visualmente, pero el valor debe seguir presente en el objeto/payload para registros existentes.

---

### [24/08/2026 13:22:32] Leandro Camaratta

Pero otra solicitud, la 136915, desde algún lado del código me cambio la vigen desde original de 2025 08 01 a 2026 08 01. Cómo puede ser?

---

### [24/08/2026 13:22:47] Leandro Camaratta

Pero otra solicitud, la 136915, desde algún lado del código me cambio la vigen desde original de 2025 08 01 a 2026 08 01. Cómo puede ser?

---

### [24/08/2026 13:26:11] Agente

Leandro, en `136915` no veo que haya cambiado la fila original del titular de `2025-08-01`. Esa fila sigue en `SOLI_DERIV_APORTES`:

```text
item 1 | vigen_desde 2025-08-01 | vigen_hasta 2026-01-01
item 1 | vigen_desde 2026-08-01 | vigen_hasta NULL
item 2 | vigen_desde 2026-08-01 | vigen_hasta NULL
```

Lo que sí pasó es que el familiar `item 2` quedó con `2026-08-01`, y eso viene desde la precarga: `PRECARGA_DERIV_APORTES` ya tiene `item 2 / vigen_aporte = 2026-08-01`.

Cómo puede pasar:

1. En [TabObraSocial.jsx](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/precarga_soli/frontend/src/precarga/components/tabs/TabObraSocial.jsx:119), el botón `Agregar` crea una derivación nueva sin `vigenDesde`.

2. En [cru_genera_precarga_tmp.sql](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/backend/procedures/cru_genera_precarga_tmp.sql:457), si `vigenDesde` no viene, SQL hace:

```sql
vigen_aporte = ISNULL(NULLIF(...vigenDesde...), @var_primer_dia)
```

Y para esta solicitud `@var_primer_dia` / `apli_fecha` quedó `2026-08-01`.

3. Para tipo `13`, [cri_precarga_solicitud_generar_deriv_aportes.sql](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/backend/procedures/cri_precarga_solicitud_generar_deriv_aportes.sql:523) busca la derivación del titular vigente al `apli_fecha`. Como la de `2025-08-01` tiene `vigen_hasta = 2026-01-01`, no está vigente al `2026-08-01`; entonces toma/genera la ventana `2026-08-01`.

Conclusión: no parece un `UPDATE` que cambió `2025-08-01` por `2026-08-01`. Parece que para el familiar se creó una nueva fila con default `2026-08-01` porque la web no conservó/envió la `vigenDesde` original para esa línea, o porque se agregó como nueva. Para evitarlo, la fila existente debe viajar con su `vigenDesde` original aunque el input esté deshabilitado; solo las filas realmente nuevas deberían tomar el primer día del mes/aplica.

---

### [24/08/2026 13:29:45] Leandro Camaratta

El dato está correcto en la base de datos. El problema ocurre cuando abro esa solicitud desde afilmed legacy. Me trae los registros del titular en la pantalla de solicitudes, pero en vez de ver esa vigencia original 2025 08 01 veo 2026 08 01. Debe hacer algo luego de ejecutar el retrieve del data window, porque Al ejecutarlo por afuera trae correcta la vigencia original pero después algo la cambia en el datawindow

---

### [24/08/2026 13:32:51] Agente

Sí, Leandro. Encontré el punto exacto.

Después del retrieve, legacy ejecuta `wf_cambiar_fechas(dw_varios.object.apli_fecha[1])`. En esa función, para motivo `13`, hace esto:

[wp_solici.srw](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/wp_solici.srw:15238)

```powerscript
//Aportes Titular
IF li_soli_moti=1 OR li_soli_moti=13 THEN
    IF idw_aportes.RowCount()>=1 THEN
        FOR l = 1 to idw_aportes.RowCount()
            IF idw_aportes.Object.proteger[l] <> 'S' THEN
                idw_aportes.object.vigen_desde[l] = a_fecha
            END IF
        NEXT
    END IF
END IF
```

Entonces pasa esto:

- El DataWindow recupera bien `vigen_desde = 2025-08-01`.
- Pero esa fila viene con `proteger = 'N'`.
- Como la solicitud `136915` tiene `apli_fecha = 2026-08-01`, `wf_cambiar_fechas()` pisa en memoria el `vigen_desde` y lo deja en `2026-08-01`.

Por qué viene `proteger = 'N'`: el DW calcula `proteger` con un `LEFT JOIN` contra `AFI_DERIV_APORTES` por la clave completa:

```sql
prepaga + contra + inte + vigen_desde + obra_soc + cuit
```

Para `136915`, en `AFI_DERIV_APORTES` ya no está la fila `2025-08-01`; están las de `2026-08-01`. Entonces el join no encuentra la histórica y marca `proteger = 'N'`.

Conclusión: no es la base ni el retrieve. La pantalla legacy pisa el valor después del retrieve por esta regla genérica de `wf_cambiar_fechas()` aplicada también a `soli_moti = 13`.

El fix debería ser acotar esa lógica. Para motivo `13`, no debería pisar todas las filas no protegidas; como mínimo debería respetar las filas recuperadas con `vigen_desde_ori` o modificar solo filas nuevas/sin vigencia. Ahí está el origen del comportamiento.

---

### [24/08/2026 13:36:03] Leandro Camaratta

Ese registro en realidad yo lo inserte por afuera. Si pensamos que un registro de derivación aportes existente, uno le agregue la vigencia hasta, entonces si lo encontraría en afi deriv aportes y el proteger sería S me imagino y no pasaría la fecha.

---

### [24/08/2026 13:36:16] Agente

Sí, correcto.

Si la ventana `2025-08-01` existiera realmente en `AFI_DERIV_APORTES` para:

```text
prepaga + contra + inte + vigen_desde + obra_soc + cuit
```

entonces el retrieve de `d_soli_deriv_aportes_titu` la matchearía con el `LEFT JOIN`, calcularía:

```sql
proteger = 'S'
```

y después `wf_cambiar_fechas()` no le pisaría el `vigen_desde`, porque solo pisa cuando:

```powerscript
IF idw_aportes.Object.proteger[l] <> 'S' THEN
    idw_aportes.object.vigen_desde[l] = a_fecha
END IF
```

Entonces el caso que probaste se comportó distinto porque lo insertaste por afuera solo en `SOLI_DERIV_APORTES`, pero no existía la contraparte en `AFI_DERIV_APORTES`. Para legacy eso no es “una derivación existente impactada”, sino una fila de solicitud no protegida, y por eso la normaliza al `apli_fecha`.

Criterio final:

- **Modificar derivación existente real:** viene de `AFI_DERIV_APORTES`, `proteger = S`, no pisa `vigen_desde`; permite cerrar con `vigen_hasta`.
- **Fila creada manualmente o nueva en solicitud:** no matchea `AFI_DERIV_APORTES`, `proteger = N`, legacy puede pisar `vigen_desde` con `apli_fecha` para motivo 13.

Para testear el flujo real, conviene partir de una derivación que exista en `AFI_DERIV_APORTES`, no solo insertada en `SOLI_DERIV_APORTES`.

---

### [24/08/2026 13:42:03] Leandro Camaratta

En afilmed legacy, en una solicitud de cambio de derivación de aportes, se permite cambiar la empresa y el sueldo de una derivación que ya existe en afi_deriv_aportes ?

---

### [24/08/2026 13:43:01] Agente

Sí y no, según el campo.

En legacy, para una derivación que existe en `AFI_DERIV_APORTES`, el DataWindow la trae con `proteger = 'S'`.

Con `proteger = 'S'`:

- `obra_soc`: protegida.
- `cuit`: protegido.
- `vigen_desde`: protegido.
- `legajo`: protegido.
- `tipo_aportante`: protegido.
- `vigen_hasta`: queda editable.
- `empresa`: no está protegida por `proteger`; queda editable para titular.
- `sueldo`: no está protegido por `proteger`; queda editable para titular.

La evidencia está en [d_soli_deriv_aportes_titu.srd](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/d_soli_deriv_aportes_titu.srd:107): `obra_soc/cuit/legajo` usan `protect="...proteger='S'..."`, pero `empresa` y `sueldo` solo se protegen si `tipo_aportante = 'G'`.

En grabación, `afu_soli_deriv_aportes` hace `MERGE` por:

```sql
soli + item + vigen_desde + obra_soc + cuit
```

y si matchea, actualiza `empresa`, `sueldo`, `vigen_hasta`, etc.

Entonces: legacy sí permite cambiar `empresa` y `sueldo` de una derivación existente en la solicitud, aunque la derivación esté protegida por existir en `AFI_DERIV_APORTES`. Lo que no permite cambiar libremente es la clave de la derivación: `vigen_desde`, `obra_soc`, `cuit`.

---

### [24/08/2026 13:45:08] Leandro Camaratta

Si cambia el sueldo o la empresa de titular con tipo aportante titular, los integrantes dependientes con tipo aportante G se les actualizan esos campos con los nuevos valores?

---

### [24/08/2026 13:45:39] Agente

Sí.

En `dw_aportes_titu`, cuando cambia una fila con `tipo_aportante = 'T'`, legacy busca en `idw_aportes2` los dependientes `tipo_aportante = 'G'` que tengan la misma clave:

```text
vigen_desde + obra_soc + cuit + inte_titular_aportante
```

y les propaga campos.

La rutina es `ue_verif_dependientes_aportantes` en [wp_solici.srw](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/exe/Source%20Infomedical/wp_solici.srw:34370). Ahí contempla explícitamente:

```powerscript
CASE 'empresa'
    idw_aportes2.object.empresa[ll_find] = THIS.object.empresa[al_row]

CASE 'sueldo'
    idw_aportes2.object.sueldo[ll_find] = THIS.object.sueldo[al_row]
```

Y el `itemchanged` dispara esa rutina para `empresa` y para `sueldo`.

Detalle: en `dw_aportes_titu`, `empresa` dispara directo junto con `vigen_desde`; `sueldo` tiene su propio `CASE 'sueldo'`, actualiza también sindicato si corresponde, y luego dispara `ue_verif_dependientes_aportantes`.

Entonces: si el titular es `tipo_aportante = 'T'` y el familiar depende de esa misma derivación como `tipo_aportante = 'G'`, legacy sí le actualiza empresa y sueldo en memoria al familiar.

---

### [24/08/2026 13:50:27] Leandro Camaratta

Al impactar la precarga en afilmed web creo que no estoy generando los registros para el integrante cuando se cambia la empresa o el sueldo, solo lo hago si cambia la vigencia hasta, puede ser?

---

### [24/08/2026 13:51:00] Agente

Sí, puede ser. De hecho, en el generador web la lógica de grupo familiar para motivo `13` está mirando casi solo `vigen_hasta`.

En [cri_precarga_solicitud_generar_deriv_aportes.sql](/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/backend/procedures/cri_precarga_solicitud_generar_deriv_aportes.sql:422):

- **Caso 4** actualiza/cierra derivaciones `G`, pero entra si falta la derivación del titular en precarga o si cambió `vigen_hasta`.
- **Caso 5** crea nuevas derivaciones `G`, pero también depende de que falte la ventana o cambie `vigen_hasta`.
- **Caso 6** mantiene iguales las derivaciones `G`, y compara `vigen_hasta`, pero toma `empresa` y `sueldo` desde `ADA_G`, no desde la precarga del titular.

La condición crítica es:

```sql
PDA_B.id_precarga IS NULL
OR ISNULL(PDA_B.vigen_hasta, '20500101') <> ISNULL(ADA_T.vigen_hasta, '20500101')
```

No compara:

```sql
PDA_T.empresa <> ADA_T.empresa
PDA_T.sueldo <> ADA_T.sueldo
```

Entonces si en Afilmed Web cambiás solo `empresa` o `sueldo` del titular, el titular sí entra por caso 2 y se genera con los nuevos valores, pero los integrantes dependientes `tipo_aportante = 'G'` pueden quedar con los valores viejos de `AFI_DERIV_APORTES`.

Para alinearlo con legacy, esa detección de cambio en titular debería incluir `empresa` y `sueldo`, y al generar los `G` debería tomar `empresa/sueldo` desde `PDA_T`, no desde `ADA_G`, cuando la derivación depende del titular modificado.

---

### [24/08/2026 13:53:03] Leandro Camaratta

Cómo sería este cambio?

---

### [24/08/2026 13:54:37] Agente

El cambio sería así en `cri_precarga_solicitud_generar_deriv_aportes.sql`.

En **Caso 4**, cuando se arma la fila `G`, no tomar siempre `empresa/sueldo` desde `ADA_G`. Si existe la derivación del titular en la precarga (`PDA_T`), usar los valores nuevos del titular:

```sql
empresa = CASE
            WHEN PDA_T.id_precarga IS NULL THEN ADA_G.empresa
            ELSE UPPER(PDA_T.empresa)
          END,

sueldo = CASE
           WHEN PDA_T.id_precarga IS NULL THEN ADA_G.sueldo
           ELSE ISNULL(PDA_T.sueldo, 0)
         END,
```

Y ampliar el `WHERE` del Caso 4. Hoy está así:

```sql
AND (
      PDA_T.id_precarga IS NULL
   OR ISNULL(PDA_T.vigen_hasta, '20500101') <> ISNULL(ADA_T.vigen_hasta, '20500101')
)
```

Debería quedar:

```sql
AND (
      PDA_T.id_precarga IS NULL
   OR ISNULL(PDA_T.vigen_hasta, '20500101') <> ISNULL(ADA_T.vigen_hasta, '20500101')
   OR ISNULL(UPPER(LTRIM(RTRIM(PDA_T.empresa))), '') <> ISNULL(UPPER(LTRIM(RTRIM(ADA_T.empresa))), '')
   OR ISNULL(PDA_T.sueldo, 0) <> ISNULL(ADA_T.sueldo, 0)
)
```

Después, en **Caso 5**, dentro del `EXISTS`, repetir el mismo criterio. Hoy solo dispara por falta de `PDA_B` o cambio de `vigen_hasta`:

```sql
AND (
      PDA_B.id_precarga IS NULL
   OR ISNULL(PDA_B.vigen_hasta, '20500101') <> ISNULL(ADA_T.vigen_hasta, '20500101')
)
```

Debería quedar:

```sql
AND (
      PDA_B.id_precarga IS NULL
   OR ISNULL(PDA_B.vigen_hasta, '20500101') <> ISNULL(ADA_T.vigen_hasta, '20500101')
   OR ISNULL(UPPER(LTRIM(RTRIM(PDA_B.empresa))), '') <> ISNULL(UPPER(LTRIM(RTRIM(ADA_T.empresa))), '')
   OR ISNULL(PDA_B.sueldo, 0) <> ISNULL(ADA_T.sueldo, 0)
)
```

Con eso, si en web cambia `empresa` o `sueldo` del titular tipo `T`, el generador detecta que la derivación titular cambió y genera también las filas de los dependientes `G`.

No tocaría `vigen_desde`, `obra_soc` ni `cuit`: siguen siendo la clave de la ventana. El cambio es solo propagar a los `G` los campos editables que legacy ya propaga en memoria.

---

### [24/08/2026 15:04:33] Leandro Camaratta

Y el caso 6?

---

### [24/08/2026 15:04:58] Agente

Sí, el **Caso 6 también hay que ajustarlo**.

Hoy Caso 6 dice “derivaciones iguales”, pero solo valida igualdad de `vigen_hasta`. Si cambiás `empresa` o `sueldo`, técnicamente ya no son iguales.

En el `INNER JOIN #PDA PDA_T` del Caso 6, después de esta condición:

```sql
AND (
      (PDA_T.vigen_hasta = ADA_G.vigen_hasta)
   OR (PDA_T.vigen_hasta IS NULL AND ADA_G.vigen_hasta IS NULL)
)
```

agregaría:

```sql
AND ISNULL(UPPER(LTRIM(RTRIM(PDA_T.empresa))), '') =
    ISNULL(UPPER(LTRIM(RTRIM(ADA_G.empresa))), '')

AND ISNULL(PDA_T.sueldo, 0) =
    ISNULL(ADA_G.sueldo, 0)
```

Quedaría conceptualmente:

```sql
AND (
      (PDA_T.vigen_hasta = ADA_G.vigen_hasta)
   OR (PDA_T.vigen_hasta IS NULL AND ADA_G.vigen_hasta IS NULL)
)
AND ISNULL(UPPER(LTRIM(RTRIM(PDA_T.empresa))), '') =
    ISNULL(UPPER(LTRIM(RTRIM(ADA_G.empresa))), '')
AND ISNULL(PDA_T.sueldo, 0) =
    ISNULL(ADA_G.sueldo, 0)
```

Importante: aunque no lo tocaras, probablemente Caso 4 ganaría por `ROW_NUMBER() ORDER BY caso`, porque Caso 4 viene antes que Caso 6. Pero dejar Caso 6 sin este filtro queda conceptualmente mal y puede generar filas candidatas contradictorias. Mejor que Caso 6 solo cubra verdaderamente “sin cambios”.

---

### [24/08/2026 15:40:26] Leandro Camaratta

Desde afilmed legacy es cierto que cuando se crea una solicitud de cambio de derivación de aportes y se agrega un registro no tiene que tener vigencia hasta sino sale una validación? Desde afilmed web con la precarga 88528, había dado de baja una derivación y dado de alta otra asignando una vigencia hasta. Cuando fui a afilmed legacy, al querer impactar me dio una validación de que tenía que tener una derivación vigente. Eso no estaba del todo bien informado, porque al mes actual si tenia una derivación vigente. Le quite la vigencia hasta al titular donde se actualizo la del integrante, y ahi me dejo impactar la solicitud

---

### [24/08/2026 15:42:39] Agente

Sí, es cierto para el flujo de legacy.

En solicitud `soli_moti = 13` (`Cambio de derivación de aportes`), legacy no se conforma con que exista una derivación “vigente al aplica” si tiene `vigen_hasta` futura. En las validaciones fuertes exige una derivación abierta, o sea:

```powerscript
vigen_desde <= apli_fecha
AND IsNull(vigen_hasta)
AND IsNull(baja_fecha)
```

Está en `wp_solici.srw`, caso `10, 13`, dentro de `wf_valida_deriv_aportes_general()`:

- Titular: busca `ISnull(vigen_hasta)` para considerar que queda derivación vigente.
- Familiares: también exige `ISnull(vigen_hasta)`.

Entonces lo que te pasó con la precarga `88528` tiene sentido:

- Cerraste una derivación vieja: correcto, esa lleva `vigen_hasta`.
- Agregaste una derivación nueva, pero también con `vigen_hasta`: para legacy eso no queda como derivación activa abierta.
- Al impactar, dispara: “Ningún integrante del grupo familiar puede quedar sin una derivación de aportes vigente...”
- Cuando le quitaste `vigen_hasta` al titular, legacy propagó al integrante y pasó la validación.

Conclusión funcional: en Afilmed Web, para una solicitud tipo `13`, si el usuario da de baja una derivación y agrega otra, la nueva derivación no debería permitir `vigenHasta`. La `vigenHasta` corresponde a la ventana que se cierra, no a la nueva ventana activa.

Regla alineada a legacy:

```text
Derivación existente que se cierra: vigenHasta obligatoria/opcional según acción.
Derivación nueva/reemplazo activo: vigenDesde = aplica, vigenHasta = NULL.
```

El mensaje de legacy está mal redactado para este caso, porque dice “vigente”, pero el código en realidad exige “vigente abierta sin fecha hasta”.

---

### [24/08/2026 15:45:48] Leandro Camaratta

Entonces en afilmed web para las derivaciones nuevas hay que bloquear la vigencia hasta. Solo dejarlo habilitado para los registros existentes

---

### [24/08/2026 15:46:18] Agente

Sí. En Afilmed Web la regla debería quedar:

```text
deriva.nuevo === "S"  -> vigenHasta bloqueada y valor NULL/vacío
deriva.nuevo === "N"  -> vigenHasta habilitada
```

En `TabObraSocial.jsx`, si agregás el campo, sería:

```jsx
<FormInput
  lg={2}
  md={6}
  name={`deriva.${index}.vigenHasta`}
  label="Vigencia Hasta"
  type="date"
  disabled={i.nuevo === "S"}
/>
```

Y al hacer `push` de una nueva derivación, no le pongas `vigenHasta` o ponelo vacío:

```js
push({
  cuit: "",
  os: 0,
  nombreEmpresa: "",
  sueldo: "",
  vigenHasta: "",
  nuevo: "S",
});
```

Además, para evitar que viaje un valor residual, antes de guardar conviene asegurar:

```js
if (d.nuevo === "S") {
  return {
    ...d,
    vigenHasta: "",
  };
}
```

Conceptualmente: la derivación nueva es la activa abierta. Si nace con `vigenHasta`, legacy después no la considera válida para impactar porque busca `IsNull(vigen_hasta)`.

---

### [24/08/2026 16:12:39] Leandro Camaratta

Hice la solicitud 136921 desde aflmed web cambiando también el regimen y el plan a ESP. En solicitudes está el nuevo plan, pero cuando edite esta solicitud desde afilmed legacy, me cambio el sueldo que había modificado desde afilmed web, pero no me cambio el plan, por que?

---

### [24/08/2026 16:17:16] Agente

Sí: el sueldo cambió porque va por `SOLI_DERIV_APORTES`. El plan no cambió porque la solicitud web quedó incompleta para la lógica de impacto de legacy.

En `136921` vi esto:

```text
SOLICITUDES.plan_codi      = ESP
SOLICITUDES.tari           = UNI
SOLICITUDES.plan_codi_ant  = vacío
SOLICITUDES.tari_ant       = vacío
```

Y el contrato sigue con:

```text
AFI_PLANES.plan_codi = BASE
```

El problema está en `cri_precarga_solicitud_generar.sql`: al generar la solicitud desde precarga, está insertando siempre:

```sql
plan_codi_ant = NULL
tari_ant      = NULL
```

Pero en impacto legacy (`afu_soli_abordar1.sql`), para motivo `13`, el cambio de plan solo se activa si existen plan/tarifa anterior y son distintos:

```sql
IF @plan_codi_ant IS NOT NULL AND @tari_ant IS NOT NULL
BEGIN
  IF ((@plan_codi <> @plan_codi_ant) OR (@tari <> @tari_ant))
    SET @var_cambio_plan_derapo = 'S'
END
```

Como `plan_codi_ant` y `tari_ant` están vacíos, legacy no considera que haya cambio de plan. Entonces impacta derivación/sueldo, pero no genera el cambio en `AFI_PLANES`.

La corrección para Afilmed Web es: cuando la precarga tipo `13` trae un plan nuevo, `cri_precarga_solicitud_generar.sql` tiene que grabar también el plan/tarifa anterior del contrato en `SOLICITUDES.plan_codi_ant` y `SOLICITUDES.tari_ant`.

Conceptualmente:

```sql
plan_codi     = plan nuevo web
tari          = tarifa nueva web
plan_codi_ant = plan vigente actual del contrato
tari_ant      = tarifa vigente actual del contrato
```

No alcanza con guardar `SOLICITUDES.plan_codi = ESP`; para legacy eso es solo el dato nuevo de la solicitud. Para que lo impacte como cambio de plan en motivo `13`, necesita el par anterior.

---

### [24/08/2026 16:21:22] Leandro Camaratta

Entonces tengo que conseguir de afi_tarifas los datos actuales. En cru_genera_precarga_temp ya se obtienen los datos de ahi, quizas pueda guardarlos en plan_ant y tari_ant

---

### [24/08/2026 16:22:26] Agente

Sí, ese es el camino correcto, pero hoy no tenés dónde guardarlo en `PRECARGA_SOLICITUDES`.

Actualmente `cru_genera_precarga_tmp` hace esto:

```sql
SELECT @var_tari =
  CASE
    WHEN @prm_soli_moti = 13 AND AT.plan_codi <> @prm_plan_codi THEN NULL
    ELSE AT.tari
  END
FROM AFI_TARIFAS AT
...
```

Ahí ya está leyendo la tarifa vigente actual del contrato. Lo que falta es conservar:

```sql
@var_plan_codi_ant = AT.plan_codi
@var_tari_ant      = AT.tari
```

Pero `PRECARGA_SOLICITUDES` hoy solo tiene:

```text
plan_codi
tari
```

No tiene `plan_codi_ant` ni `tari_ant`.

Entonces el cambio completo sería:

1. Agregar columnas a `PRECARGA_SOLICITUDES`:

```sql
ALTER TABLE PRECARGA_SOLICITUDES ADD
  plan_codi_ant CHAR(10) NULL,
  tari_ant CHAR(3) NULL;
```

2. En `cru_genera_precarga_tmp`, declarar variables:

```sql
@var_plan_codi_ant CHAR(10) = NULL,
@var_tari_ant      CHAR(3)  = NULL
```

3. En el `SELECT` desde `AFI_TARIFAS`:

```sql
SELECT
  @var_plan_codi_ant = AT.plan_codi,
  @var_tari_ant      = AT.tari,
  @var_tari          = CASE
                         WHEN @prm_soli_moti = 13 AND AT.plan_codi <> @prm_plan_codi THEN NULL
                         ELSE AT.tari
                       END
FROM AFI_TARIFAS AT
...
```

4. Insertar esos valores en `#PRECARGA_SOLICITUDES` y luego en `PRECARGA_SOLICITUDES`.

5. En `cri_precarga_solicitud_generar.sql`, cambiar esto:

```sql
plan_codi_ant = NULL,
tari_ant      = NULL
```

por:

```sql
plan_codi_ant = CASE
                  WHEN S.soli_moti = 13
                   AND ISNULL(S.plan_codi_ant, '') <> ''
                   AND S.plan_codi_ant <> S.plan_codi
                  THEN S.plan_codi_ant
                  ELSE NULL
                END,
tari_ant      = CASE
                  WHEN S.soli_moti = 13
                   AND ISNULL(S.plan_codi_ant, '') <> ''
                   AND S.plan_codi_ant <> S.plan_codi
                  THEN S.tari_ant
                  ELSE NULL
                END
```

Así, si web cambia de `BASE` a `ESP`, la solicitud queda:

```text
plan_codi     = ESP
tari          = UNI
plan_codi_ant = BASE
tari_ant      = tarifa vigente anterior
```

Y legacy ya puede activar `@var_cambio_plan_derapo = 'S'` al impactar.

---

### [24/08/2026 16:27:10] Leandro Camaratta

Hay un camino mucho mas sencillo. En cri_precarga_solicitud_generar, antes de insertar en solicitudes:

 SELECT @var_plan_ant = AT.plan_codi ,
 @var_tari_ant = AT.tari

 FROM PRECARGA_SOLICITUDES PS

 INNER JOIN
 AFI_TARIFAS AT
 ON AT.prepaga = PS.prepaga
 AND AT.contra = PS.contra

 WHERE PS.soli_moti = 13
 AND AT.vigen_desde = (
 SELECT MAX(vigen_desde)
 FROM AFI_TARIFAS
 WHERE baja_fecha IS NULL
 AND prepaga = AT.prepaga
 AND contra = AT.contra
 AND vigen_desde <= PS.apli_fecha
 )

Luego usar esas variables para insertar en plan_ant y tari_ant de solicitudes

---

### [24/08/2026 16:27:26] Agente

Sí, ese camino es mejor y más acotado. Evita tocar `PRECARGA_SOLICITUDES` y resuelve el dato justo donde se necesita: al generar `SOLICITUDES`.

Solo ajustaría dos detalles:

1. Filtrar por `PS.id_precarga = @prm_id_precarga`, para no leer cualquier precarga tipo 13.
2. Guardar `plan_codi_ant/tari_ant` solo si realmente hay cambio contra `S.plan_codi/S.tari`.

Quedaría así en `cri_precarga_solicitud_generar.sql`, antes del `INSERT INTO SOLICITUDES`:

```sql
DECLARE @var_plan_ant CHAR(10),
        @var_tari_ant CHAR(3);

SELECT
  @var_plan_ant = AT.plan_codi,
  @var_tari_ant = AT.tari
FROM PRECARGA_SOLICITUDES PS
INNER JOIN AFI_TARIFAS AT
  ON AT.prepaga = PS.prepaga
 AND AT.contra = PS.contra
WHERE PS.id_precarga = @prm_id_precarga
AND   PS.soli_moti = 13
AND   AT.baja_fecha IS NULL
AND   AT.vigen_desde = (
        SELECT MAX(AT2.vigen_desde)
        FROM AFI_TARIFAS AT2
        WHERE AT2.baja_fecha IS NULL
        AND   AT2.prepaga = AT.prepaga
        AND   AT2.contra = AT.contra
        AND   AT2.vigen_desde <= PS.apli_fecha
      );
```

Y en el `SELECT` del insert a `SOLICITUDES`, reemplazar:

```sql
plan_codi_ant = NULL,
tari_ant      = NULL,
```

por:

```sql
plan_codi_ant = CASE
                  WHEN S.soli_moti = 13
                   AND ISNULL(@var_plan_ant, '') <> ''
                   AND @var_plan_ant <> S.plan_codi
                  THEN @var_plan_ant
                  ELSE NULL
                END,
tari_ant      = CASE
                  WHEN S.soli_moti = 13
                   AND ISNULL(@var_plan_ant, '') <> ''
                   AND @var_plan_ant <> S.plan_codi
                  THEN @var_tari_ant
                  ELSE NULL
                END,
```

Ese cambio es suficiente para que una solicitud tipo 13 generada desde web quede igual de interpretable para legacy:

```text
SOLICITUDES.plan_codi     = nuevo plan
SOLICITUDES.tari          = nueva tarifa
SOLICITUDES.plan_codi_ant = plan vigente anterior
SOLICITUDES.tari_ant      = tarifa vigente anterior
```

Así `afu_soli_abordar1` detecta el cambio y aplica la rama de cambio de plan.

---

### [24/08/2026 16:29:01] Leandro Camaratta

Pero directamente conviene comparar con el anterior al select con afi_tarifas

---

### [24/08/2026 16:29:13] Agente

Sí, conviene. Más simple y más claro: resolvés el plan/tarifa anterior desde `AFI_TARIFAS`, pero solo lo cargás si es distinto del plan/tarifa de la precarga.

Ejemplo:

```sql
DECLARE @var_plan_ant CHAR(10),
        @var_tari_ant CHAR(3);

SELECT
  @var_plan_ant = CASE
                    WHEN AT.plan_codi <> PS.plan_codi
                      OR ISNULL(AT.tari, '') <> ISNULL(PS.tari, '')
                    THEN AT.plan_codi
                    ELSE NULL
                  END,
  @var_tari_ant = CASE
                    WHEN AT.plan_codi <> PS.plan_codi
                      OR ISNULL(AT.tari, '') <> ISNULL(PS.tari, '')
                    THEN AT.tari
                    ELSE NULL
                  END
FROM PRECARGA_SOLICITUDES PS
INNER JOIN AFI_TARIFAS AT
  ON AT.prepaga = PS.prepaga
 AND AT.contra = PS.contra
WHERE PS.id_precarga = @prm_id_precarga
AND   PS.soli_moti = 13
AND   AT.baja_fecha IS NULL
AND   AT.vigen_desde = (
        SELECT MAX(AT2.vigen_desde)
        FROM AFI_TARIFAS AT2
        WHERE AT2.baja_fecha IS NULL
        AND   AT2.prepaga = AT.prepaga
        AND   AT2.contra = AT.contra
        AND   AT2.vigen_desde <= PS.apli_fecha
      );
```

Después en el insert queda limpio:

```sql
plan_codi_ant = @var_plan_ant,
tari_ant      = @var_tari_ant,
```

Esto evita repetir el `CASE` dentro del `INSERT` y deja la regla en un solo lugar: “si lo vigente actual en `AFI_TARIFAS` difiere de lo pedido por la precarga, entonces eso vigente actual es el anterior”.

---

### [24/08/2026 16:30:27] Leandro Camaratta

Pondria el dato en el where, y solo por plan_codi porque desde afilmed web no se puede elegir la tarifa

---

### [24/08/2026 16:30:39] Agente

Sí, mejor todavía.

Si desde Afilmed Web no se elige tarifa, el disparador funcional es solo cambio de `plan_codi`. Entonces el `SELECT` puede traer anterior solo cuando el plan vigente actual difiere del plan pedido:

```sql
DECLARE @var_plan_ant CHAR(10),
        @var_tari_ant CHAR(3);

SELECT
  @var_plan_ant = AT.plan_codi,
  @var_tari_ant = AT.tari
FROM PRECARGA_SOLICITUDES PS
INNER JOIN AFI_TARIFAS AT
  ON AT.prepaga = PS.prepaga
 AND AT.contra = PS.contra
WHERE PS.id_precarga = @prm_id_precarga
AND   PS.soli_moti = 13
AND   AT.baja_fecha IS NULL
AND   AT.plan_codi <> PS.plan_codi
AND   AT.vigen_desde = (
        SELECT MAX(AT2.vigen_desde)
        FROM AFI_TARIFAS AT2
        WHERE AT2.baja_fecha IS NULL
        AND   AT2.prepaga = AT.prepaga
        AND   AT2.contra = AT.contra
        AND   AT2.vigen_desde <= PS.apli_fecha
      );
```

Y en el insert:

```sql
plan_codi_ant = @var_plan_ant,
tari_ant      = @var_tari_ant,
```

Con esto:

- Si el plan web es igual al vigente: no carga `plan_codi_ant/tari_ant`.
- Si el plan web es distinto: guarda el plan/tarifa vigente anterior.
- Legacy detecta cambio de plan para motivo `13`.

Criterio correcto: comparar solo `plan_codi`, porque `tari` en web es derivada/no editable.

---

### [24/08/2026 16:35:45] Leandro Camaratta

Cargue la solicitud 136922 desde afilmed web y cuando cargo la solictud en afilmed, no me deja ESP, me sigue dejando BASE y no puedo cambiarlo, por que?

---

### [24/08/2026 16:37:26] Agente

Porque ahora `136922` quedó en el esquema correcto para legacy: el plan nuevo está en la solicitud, pero el plan principal que muestra la cabecera queda deliberadamente en el plan anterior.

Validé en `Ospecon_LC`:

```text
SOLICITUDES.plan_codi     = ESP
SOLICITUDES.tari          = NULL
SOLICITUDES.plan_codi_ant = BASE
SOLICITUDES.tari_ant      = UNI
```

Y el contrato actual sigue:

```text
AFI_PLANES.plan_codi = BASE
```

En legacy, para `soli_moti = 13`, cuando existe `plan_codi_ant`, hace esto:

```powerscript
IF lib.of_vacio(ls_plan_ant) THEN
    dw_soli.object.plan_codi[1] = ls_plan
ELSE
    dw_soli.object.plan_codi[1] = ls_plan_ant
END IF
```

O sea: como ahora `plan_codi_ant = BASE`, legacy muestra `BASE` en `dw_soli.plan_codi`. Eso es intencional: la cabecera principal muestra el plan actual/anterior. El plan nuevo (`ESP`) lo carga en `dw_varios.plan_codi`.

Además, `dw_soli.plan_codi` está protegido para motivo 13:

```powerscript
CASE 13
    dw_soli.object.plan_codi.protect = '1'
```

Por eso no podés cambiarlo ahí.

El punto a revisar ahora es otro: `SOLICITUDES.tari` quedó `NULL`. Como web no permite elegir tarifa, cuando cambia a `ESP` tenés que resolver la tarifa nueva del plan y grabarla también. Si no, legacy queda con:

```text
plan nuevo = ESP
tarifa nueva = NULL
```

Entonces: que veas `BASE` en la cabecera legacy es esperable. Pero `tari` nuevo no debería quedar `NULL`; hay que completar `SOLICITUDES.tari` para el plan `ESP` al generar la solicitud.

---

### [24/08/2026 16:40:26] Leandro Camaratta

Pero no veo abajo los campos viendo el ESP y para que elija la tarifa

---

### [24/08/2026 16:41:54] Agente

Porque en `Ospecon_LC` está apagado el parámetro que hace visibles esos campos.

Legacy crea `dw_varios` como `de_afi_plan` para motivo 13, pero después muestra u oculta `plan_codi` y `tari` según:

```powerscript
is_plan_tari_depen_benef
```

Ese valor sale de:

```powerscript
lib.of_trae_param_gral(
  'Afilmed',
  'Solicitudes',
  'plan_depende_de_tipo_beneficiario',
  ...,
  is_plan_tari_depen_benef
)
```

Lo consulté en `Ospecon_LC` y devuelve:

```text
plan_depende_de_tipo_beneficiario = N
```

Con ese valor, legacy entra por esta rama:

```powerscript
ELSE
  dw_varios.object.plan_codi.visible = FALSE
  dw_varios.object.tari.visible      = FALSE
END IF
```

Por eso no ves abajo el `ESP` ni la tarifa para elegir.

Entonces hay dos temas separados:

1. La solicitud `136922` ya está bien grabada como cambio de plan:
```text
plan_codi     = ESP
plan_codi_ant = BASE
```

2. La pantalla legacy no muestra el bloque de plan/tarifa porque el parámetro funcional está en `N`.

Si querés que legacy permita ver/elegir plan y tarifa en ese flujo, tiene que estar activo `plan_depende_de_tipo_beneficiario = S`, o habría que cambiar la condición de pantalla para mostrar `dw_varios.plan_codi/tari` cuando `soli_moti = 13` y exista `plan_codi_ant`, aunque el parámetro general esté en `N`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
