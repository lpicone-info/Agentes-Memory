# Memory - 2026-06-01

## Redmine 28309 / SVN infomedical
- Usuario Redmine solicitado por regla local y confirmado por el usuario: `lcamaratta`.
- Repositorio SVN branch analizado inicialmente: `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicialAgente`, registrado/local como `lcamaratta-PresmedWebInicialAgente`.
- Checkout SVN trunk descargado luego con las mismas credenciales ya usadas para la branch: `https://srv-infosvn.infomedical.com/svn/infomedical/trunk`.
- Carpeta local trunk: `~/.openclaw/workspace/svn/lcamaratta-infomedical-trunk`.
- Usuario SVN asociado: `lcamaratta`. No almacenar ni revelar contraseñas.
- `svn info` del trunk validó revisión `83528`; `svn update` OK; `svn status` OK sin cambios locales al finalizar.
- `REPOSITORIOS.md` fue actualizado durante el checkout del trunk, preservando UTF-8 sin BOM y finales LF.

## Análisis funcional/técnico Redmine 28309 contra trunk
- El ticket pide una API relacionada con notas de afiliados en Afilmed. Aclaración posterior del usuario: el flujo correcto es inbound; un sistema externo llamaría nuestra API y esta llamaría un SP para insertar la nota.
- No hacer cambios locales para ese análisis; se verificó `svn status` limpio.
- En trunk ya existe carpeta raíz `api` con Express + `mssql`, rutas por módulo y respuestas del estilo `{ cod, msgErr }`.
- Tabla actual relevante: `backend/tables/afi_notas.sql`; no tiene campo `origen`.
- Cambio DB esperado: agregar `origen CHAR(1) DEFAULT 'P'` a `afi_notas`, valores `P` = Presmed y `E` = Externo. Recomendado `CHECK (origen IN ('P','E'))` y registros existentes como `P`.
- Crear SP nuevo para alta externa, por ejemplo `api_afi_notas_ins` o `aws_afi_notas_ins`, que inserte la nota con `origen = 'E'` y realice las validaciones pedidas.
- Validaciones sugeridas del SP: `prepaga` obligatoria, `contra` obligatoria, nota obligatoria/no vacía, existencia en `afi_titulares`, afiliado/titular vigente según reglas existentes, fecha contacto válida o default `GETDATE()`, y origen solo `E` para entrada externa.
- Recomendar que el SP inserte metadata y texto en una sola operación, a diferencia del flujo PowerBuilder actual que usa `afu_notas` y luego `updateblob`.
- En `api`, agregar ruta tipo `/api/afiliados/notas/insertar`, controller `mssql`, patrón actual `validateInputs`, `validateJWT`, `getMsgErr`, respuesta `{ cod, msgErr, nroNota }`. Autenticación técnica externa queda pendiente de definición si no aplica JWT actual.
- PowerBuilder relevante: `wp_afi_notas.srw`, `wr_cons_afi_notas.srw`, `d_lista_notas.srd`, `wr_ficha_afiliado.srw`.
- En PB, modificar ficha del afiliado y pantalla de notas para mostrar el nuevo campo origen. En `d_lista_notas.srd` conviene mostrar `Presmed` para `P` y `Externo` para `E`; `wp_afi_notas.srw`/`wr_cons_afi_notas.srw` deberían tomarlo por DataWindow. `wr_ficha_afiliado.srw` solo cuenta notas vía `wf_tiene_notas`; tocar solo si se requiere distinguir visualmente notas externas.
- Riesgo detectado: Redmine original parecía describir informar hacia otro sistema, pero la aclaración del usuario define endpoint inbound + SP transaccional, sin cola ni push saliente.
