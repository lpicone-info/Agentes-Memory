
## 2026-08-20 16:35 ART - presmedWebInicial / Redmine 27848

- Conversation with Leandro in Google Chat about SVN repo `lcamaratta-PresmedWebInicial` / `presmedWebInicial`, incident Redmine `#27848`.
- SVN repo path used: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial`.
- Updated repo several times; latest remembered SVN revision after update was `r84391`, working copy clean before later local edit.
- DB test profile used: same as `STANDARD` but database `Ospecon_LC` on `SQL2019TEST`.
- Development concerns derivacion de aportes and new motivo solicitud `13`, while preserving/regression testing motivos `1` alta and `2` incorporacion.
- Relevant procedures:
  - `backend/procedures/cri_precarga_solicitud_generar.sql`
  - `backend/procedures/cri_precarga_solicitud_generar_deriv_aportes.sql`
  - `backend/procedures/cru_genera_precarga_tmp.sql`
- `cru_genera_precarga_tmp`: issue found where contrato no nulo copied tarifa from `AFI_TARIFAS`; for motivo `13`, if plan from latest `AFI_TARIFAS` differs from parameter `@prm_plan_codi`, `tari` should be set `NULL`; if same plan, keep tarifa. Leandro later compiled this change in `Ospecon_LC` and SVN update took remote version.
- SQL test battery with rollback validated precargas:
  - Existing templates: `88508` motivo `1`, `88459` motivo `2`, `88509` motivo `13`.
  - Created persisted test precargas: `88512` motivo `1`, `88513` motivo `2`, `88514` motivo `13`; all generated solicitudes successfully under rollback.
  - Leandro created motivo `13` precargas `88518`, `88519`, `88520`; rollback tests generated solicitudes successfully: `88518 -> 136903` with 1 afiliado/4 derivaciones, `88519 -> 136904` with 1 afiliado/4 derivaciones, `88520 -> 136905` with 1 afiliado/7 derivaciones; no duplicate `SOLI_DERIV_APORTES`.
- Functional app test on `88518` generated real solicitud `136907` exposed problem in legacy Afilmed: it requested integrante data because dependents were not represented.
- Diagnosed `88518 / 136907`:
  - `PRECARGA_SOLI_AFILIADOS` had items `1`, `2`, `3`.
  - `SOLI_AFILIADOS` for `136907` had only item `1`.
  - `SOLI_DERIV_APORTES` for `136907` had only item `1`.
  - Dependents/items `2` and `3` missing from `SOLI_DERIV_APORTES`.
- Local code edit already applied before compaction: in `backend/procedures/cri_precarga_solicitud_generar_deriv_aportes.sql` around line `570`, removed the exclusion that prevented inserting grupo familiar derivaciones when corresponding rows already existed in `AFI_DERIV_APORTES`; kept check against `SOLI_DERIV_APORTES` to avoid duplicates in same solicitud.
- Validation of local edit before compaction:
  - Change scoped to `cri_precarga_solicitud_generar_deriv_aportes.sql` only.
  - Encoding reported `ASCII` and line endings `CRLF`.
  - `svn status` showed one modified file.
  - Not compiled yet in `Ospecon_LC` because procedure script performs `DROP/CREATE PROCEDURE`.
- Operational cautions:
  - SQL tests generally run inside transactions with `ROLLBACK`; sequences may still advance in SQL Server.
  - Do not delete or revert user/local changes without explicit confirmation.
