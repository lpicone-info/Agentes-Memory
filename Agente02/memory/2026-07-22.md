# 2026-07-22

- Conversacion con Leandro en Google Chat directo. Saludo inicial y disponibilidad como Camaron/Agente02.
- Analisis solicitado sobre WebPos, transacciones `02A` y `02D`, al referenciar una autorizacion en la carga. Contexto: al cargar referencia, PHP llama un SP para recuperar items de la autorizacion; se requiere devolver items con saldo, omitir items sin saldo y distinguir autorizacion inexistente de autorizacion sin saldo disponible.
- Hallazgos en rama SVN `lcamaratta-V25.08.001`: el flujo usa `ars_auto_detalle` desde `webpos/modelo/autorizacion.php`, invocado por `webpos/controlador/auto_busqueda.php`; no usa `prs_cons_auto_remotas` para esta carga. Validaciones principales estan en PHP: existencia, prestador, tipo de autorizacion, estado pendiente/rechazada, internacion/egreso/cirugia. El JS `webpos/js/auto_busqueda.js` carga la grilla usando `item.canti`.
- Control final de integridad ya existe en `backend/procedures/pri_transac_remotas.sql`: calcula cantidad autorizada, consumos remotos `02A`, `02Q`, `02D`, consumos en expedientes y saldo; si queda negativo rechaza con `709 - Sin saldo`. `02D` pasa por el mismo control via `pri_transac_remotas_dif`.
- Recomendacion dada: implementar el cambio principalmente en `backend/procedures/ars_auto_detalle.sql`, calculando campo `saldo` por item con la misma logica que `pri_transac_remotas` (`saldo = cantidad_original_auto - consumos_aceptados_por_sucur_rel_auto_rel - consumos_en_expediente`), incluyendo `02A`, `02D` y probablemente `02Q` por consistencia con backend. Para `02A`/`02D`, devolver solo items con `saldo > 0` y agregar `saldo` al recordset.
- Recomendacion de contrato: si la autorizacion existe pero no tiene prestaciones con saldo, no devolver recordset vacio; devolver una fila/cabecera con items nulos y campo `error = 'La autorizacion no posee prestaciones con saldo disponible'` para que PHP no lo interprete como autorizacion inexistente.
- Impacto minimo sugerido: tocar `backend/procedures/ars_auto_detalle.sql`, `webpos/modelo/autorizacion.php` para mapear `saldo` y `error`, y `webpos/js/auto_busqueda.js` para usar `item.saldo` en `02A`/`02D`. No tocar inicialmente `pri_transac_remotas`, porque debe quedar como segunda barrera por concurrencia o manipulacion del formulario.
- Estado: solo analisis estatico; no se hicieron cambios ni pruebas contra base.

- Refinamiento posterior del cambio WebPos `02A`/`02D`: Leandro prefiere no agregar campo nuevo `saldo`; quiere reutilizar `canti` para devolver directamente el saldo disponible y no necesita conservar la cantidad original del item. Se acordo que el cambio sigue siendo en `ars_auto_detalle` y que `pri_transac_remotas` queda como control final, aunque su logica es poco intuitiva.
- Aclaracion tecnica sobre `ars_auto_detalle`: actualmente no usa tabla temporal `#salida`, devuelve un `SELECT` directo. Para diferenciar autorizacion existente sin saldo de autorizacion inexistente, se concluyo que conviene incorporar `#salida`: calcular consumos una sola vez, insertar la salida real con `canti = saldo`, filtrar `canti > 0` para `02A`/`02D`, y si `@@ROWCOUNT = 0` pero existen items vigentes de la autorizacion, insertar una fila con campos nulos/parciales y `error = 'La autorizacion no posee prestaciones con saldo disponible'`. Evitar `@@ROWCOUNT` despues de un `SELECT` directo porque PHP recibiria primero un recordset vacio y no leeria un segundo resultado sin `sqlsrv_next_result()`.
- Documentacion solicitada del repo SVN `https://srv-infosvn.infomedical.com/svn/infomedical/trunk`: se genero un MD con servicios expuestos, explicacion funcional y parametros de `ws_generales` y `ws_auto_remotas`. Se valido contra `web.xml`, `sun-jaxws.xml`, clases `service`, `bo`, `daos`, entidades XML y `TransactionEnum`. El documento final fue dejado en el compartido: `/mnt/Agentes/lcamaratta/documentacion_servicios_ws_generales_ws_auto_remotas.md`. Se elimino de la working copy del trunk y `svn status` quedo limpio.
- Analisis sobre validacion al eliminar autorizaciones de Presmed si estan usadas en transacciones `02A` o `02D`: `prd_autorizaciones` es el punto de integridad backend, pero no conviene agregar `RAISERROR` sin revisar llamadores porque afecta `uo_nv_valida_auto.of_auto_baja`, `w_clase_auto.ue_grabar`, `prd_transac_remotas` y `prd_transac_remotas_auto` (podrian transformar el caso funcional en error generico `718`). Recomendacion practica inicial: ampliar `prs_auto_rel` de `tipo_transac = '02A'` a `tipo_transac IN ('02A','02D')` y llamarlo desde `w_clase_auto.srw`, evento `ue_eliminar`, antes de confirmar la baja para bloquear con mensaje claro en Presmed. Si se requiere garantia total, agregar tambien blindaje en `prd_autorizaciones` junto con ajuste de propagacion de errores en llamadores.
- Sobre `recha_motivos.carac`: es una clasificacion interna del motivo de rechazo, no longitud ni caracteres. Usos observados: `V` para motivos visibles/seleccionables por usuario (ej. farmacia `grupo='FAR' AND carac='V'`, ambulatorio `grupo='EXP' AND carac='V'`); `A` para internacion/auditoria (`grupo='UIN' AND carac='A'`); `E` para errores electronicos/de proceso. Ejemplo: `prs_farma_liq_productos` prioriza errores `V` sobre `E`. Para un rechazo funcional visible normalmente corresponde `carac = 'V'`.

- Conversacion con Cesar en Google Chat directo sobre ventana PowerBuilder `wp_prestax`; se verifico que en el repo no existe `wp_prestax.srw` y que la ventana funcional corresponde a `wp_prestacion.srw` en SVN trunk (`/home/luispicone/.openclaw/workspace/svn/lcamaratta-infomedical-trunk/exe/Source Infomedical/wp_prestacion.srw`).
- Hallazgos `wp_prestacion`: la ventana controla duplicidad de prestaciones entre nomencladores en `ue_buscar`. Si existe prestacion activa en otro nomenclador y no existe en el nomenclador actual, muestra `La prestacion pertenece a un nomenclador distinto...`, limpia `prestac1` y hace `RETURN`. Si existe activa en otro nomenclador y en el nomenclador actual esta dada de baja, muestra que no puede recuperarse porque esta activa en otro nomenclador y tambien retorna.
- Caso permitido en `wp_prestacion`: si las prestaciones duplicadas en otros nomencladores estan dadas de baja, muestra `Existen prestaciones con otro nomenclador duplicadas dadas de baja.` y llama `wf_carga_vacia(li_nomen, ls_prestac)`. En `wf_carga_vacia` se habilita `cb_alta.Enabled = TRUE`.
- Validacion defensiva final en `wp_prestacion`: el mensaje `La prestacion esta siendo utilizado ya en otro nomenclador. Desea darla de alta?` esta en el evento `sqlpreview` del DataWindow interno `dwa_datos`, dentro de `CHOOSE CASE is_accion`, `CASE "alta"`. En flujo normal probablemente no aparece porque el boton Alta se habilita solo cuando no hay prestacion activa en otro nomenclador; podria aparecer por bypass, desincronizacion o concurrencia.
- Analisis de archivo CSV `deadlock_xml---4bbb2d2d-6142-48c7-bb6c-3b14b5f5db2d.csv`: contiene 36 deadlocks entre 2026-07-21 08:05 y 09:04 hora Argentina. Todos se concentran en `SMC_Prod.dbo.TEMP_COPAGOS`, con locks `RID` sobre una tabla permanente usada como temporal por `spid`.
- Causa probable deadlocks `TEMP_COPAGOS`: `prs_valida_copago_remoto` ejecuta `DELETE temp_copagos WHERE spid = @@spid` (linea fuente revisada en `backend/procedures/prs_valida_copago_remoto.sql`, aprox. linea 28). En produccion se confirmo que `TEMP_COPAGOS` es HEAP, sin PK ni indices, con ~759 filas al momento de la consulta; por eso cada DELETE barre la tabla y en concurrencia genera ciclos de locks RID.
- Stack repetido de deadlock: `pri_transac_webpos_total -> prs_valida_of_auto_remotas -> prs_valida_of_convenio_remoto -> prs_valida_of_cobertura_remoto -> prs_valida_copago_remoto -> DELETE temp_copagos WHERE spid = @@spid`. Tambien hay ejecuciones directas JDBC contra `prs_valida_of_auto_remotas`. Victimas observadas: 23 desde `Microsoft SQL Server JDBC Driver`/`INFOMEDICALW` y 13 desde `apache2`/`ubuntu-webpos`.
- Recomendacion deadlock dada: agregar indice no cluster empezando por `spid`, por ejemplo `CREATE NONCLUSTERED INDEX ix_temp_copagos_spid_nomen_prestac ON dbo.TEMP_COPAGOS (spid, nomen, prestac)`, para que DELETE/SELECT/UPDATE trabajen por busqueda y no por barrido del heap.
- Consulta sobre errores `9,10,11,12,15,58,80` de `prs_valida_of_cobertura`: significados vigentes en `SMC_Prod.recha_motivos`: 9 `Cobertura inexistente`; 10 `Cobertura dada de baja`; 11 `No corresponde por carencias`; 12 `Requiere autorizacion`; 15 `El prestador demandante solo puede ser de cartilla`; 58 `Fecha prestacion anterior a autorizacion`; 80 `Autorizacion inexistente`.
- Disparadores revisados para errores en `prs_valida_of_cobertura`: 9 cuando no encuentra cobertura para prepaga/plan/nomen/prestac; 10 cuando la cobertura existe pero tiene baja anterior a fecha evaluada; 12 cuando `cober_auto = 'S'` y no se informo autorizacion; 80 cuando se informo autorizacion pero no existe activa en `AUTORIZACIONES` para sucur/auto; 11 por carencia; 58 existe en catalogo pero el seteo en `prs_valida_of_cobertura` esta comentado; 15 fue ubicado en `prs_valida_of_cobertura_tope.sql`.

- Refinamiento sobre validacion de baja de autorizaciones Presmed usadas por transacciones `02A`/`02D`: Leandro confirmo que `prd_autorizaciones` ya usa `RAISERROR` para asociaciones a expedientes, por lo que puede agregarse ahi una validacion similar sin impacto funcional critico. Si se usa el mismo `sqldbcode = 99999`, las pantallas principales mostraran el texto fijo existente `La autorizacion esta asociada a expedientes vigentes`, aceptable para Leandro.
- Si se requiere un codigo distinto para el caso `02A`/`02D`, por ejemplo `sqldbcode = 99998`, el objeto principal a modificar es `exe/Source Infomedical/uo_nv_valida_auto.sru`, funcion `of_auto_baja(...)`, donde hoy se captura `sqlca.sqldbcode = 99999`. Alli habria que agregar `ELSEIF sqlca.sqldbcode = 99998` y mostrar un texto especifico o `SQLCA.SQLErrText`.
- Revision de llamadores de `prd_autorizaciones`: las pantallas que pasan por `val_auto.of_auto_baja(...)` ya contemplan `99999`, incluyendo `w_clase_auto.srw`, `w_clase_auto_internacion.srw` y `wp_auto_cirugias.srw`. Llamadores directos como `wp_auto_importac.srw` y `wp_auto_expe_items.srw` no tratan especialmente `99999`; ante error muestran mensaje generico `prd_autorizaciones`.
- Repositorio SVN rama `V25.08.001`: se actualizo la working copy local apuntada a `https://srv-infosvn.infomedical.com/svn/infomedical/branches/V25.08.001`. Resultado informado: revision `84108`, sin conflictos. Quedaban modificaciones locales previas en `backend/data/R23586_recha_motivos.sql`, `backend/data/recha_motivos.sql` y `backend/procedures/prd_transac_remotas_auto.sql`.
- Solicitud de memoria persistente bajo demanda: se ejecuto guardado diario para lo visto hoy; resultado informado por la herramienta: archivo creado `/home/luispicone/.openclaw/workspace/memory/MEMORY-users_115680885294703796376-2026-07-22.md`, modo bajo-demanda, sin credenciales ni tokens expuestos. Nota operativa posterior: en pre-compaction flush usar siempre el archivo canonico `memory/2026-07-22.md` y no variantes timestamped ni por usuario.
