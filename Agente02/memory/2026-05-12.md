# Memory - 2026-05-12

## Redmine 29735 - WebPos tope items autorización
- Repo SVN: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicialAgente`, URL `https://srv-infosvn.infomedical.com/svn/infomedical/branches/PresmedWebInicialAgente`.
- Parámetro funcional identificado: `parametros.auto_vali_cant_items` (límite de líneas/items por autorización), mostrado en PowerBuilder vía `dw_param_auto.srd`.
- Redmine 29735: la validación existía en alta Presmed pero faltaba en Supervisión modificación y Portal/WebPos.
- Para WebPos se acordó que el control de cantidad debe quedar sólo en JS al agregar filas/items, no en PHP ni stored procedure ni al generar transacción.
- Criterio final aplicado: exponer `auto_vali_cant_items` desde `prs_parametros_autorem.sql`, leerlo en `webpos/modelo/parametro.php`, guardarlo en sesión desde `webpos/controlador/parametro_general.php`, renderizarlo como `<input type="hidden" id="auto_vali_cant_items" ...>` en `webpos/frames/frame_prestacion.php`, y validar en `webpos/js/prestacion.js`.
- El botón `#agregarPrestac` debe llamar `agregar_prestac(true)` para mostrar mensaje. F4 también llama `agregar_prestac(true)`. Las llamadas automáticas a `agregar_prestac()` no muestran mensaje; sólo no insertan la fila si está en el tope.
- No modificar para este criterio: `webpos/js/transac.js`, `webpos/controlador/transaccionar.php`, `webpos/modelo/transaccion.php`, `backend/procedures/pri_transac_webpos_total.sql`.
- Commit SVN realizado: revisión `r83308`, usuario `lcamaratta@INFOMEDICAL`, mensaje `R29735 - Controlar tope de items en WebPos`.
- Validación reportada: `node --check webpos/js/prestacion.js` OK; `svn status` limpio; `php -l` no disponible.
- Importante: para este repo usar sólo usuario SVN `lcamaratta@INFOMEDICAL`, nunca `lpicone`. Credenciales SVN indicadas por usuario en `~/.openclaw/workspace/.svn-auth/lcamaratta-PresmedWebInicialAgente/credentials.env`.

## Redmine 29738 - Importación Desempleo asigna tipo beneficiario dado de baja
- Se leyó y analizó Redmine #29738 y adjuntos: TXT `DY4LC15.SYS8.M2605.P10540.txt` y documento `R29738.doc` extraído parcialmente.
- Caso: importación de archivo Desempleo asigna al afiliado un tipo de beneficiario/régimen dado de baja y no configurado.
- Flujo principal identificado: `exe/Source Infomedical/wp_importar_aportes_desempleo.srw`; al importar llama `afi_afi_deriv_apor_imp_x_lote` con `@prm_jubi_desem = 'D'`.
- Punto sospechoso principal: `backend/procedures/afi_afi_deriv_apor_imp_x_lote.sql`, bloque DESEMPLEO. Cuando hay más de un régimen activo filtra `AR.baja_fecha IS NULL` y `AR.defecto = 'S'`, pero en el `ELSE` no filtra `AR.baja_fecha IS NULL`: `WHERE AAR.es_desempleo = 'S'`.
- Diagnóstico probable: esa rama puede tomar un `AFI_REGIMEN` histórico/dado de baja, coincidiendo con el defecto reportado.
- Cambio probable sugerido: en rama `ELSE` de DESEMPLEO agregar `AND AR.baja_fecha IS NULL`; revisar si corresponde también priorizar/filtrar `AR.defecto = 'S'` de forma consistente. El mismo patrón existe en JUBILADOS y conviene revisar riesgo análogo.
- Otro punto inconsistente: `backend/procedures/afi_afi_deriv_apor_imp_x_lote_afi_opcion.sql` recibe `@prm_regimen CHAR(4)` pero recalcula régimen internamente según jubilado/desempleo; no parece causa directa porque filtra activos, pero es inconsistencia de diseño.

## Redmine 29538 - Consulta Detallada de Padrón duplica afiliados
- Incidencia documentada en adjunto `R29538.docx`: menú `Afilmed > Afiliados > Consultas > Detallada de Padrón`; base `SMC_PROD`; ejemplo contrato/integrante `99775/01`, incluyendo triplicado.
- SP principal: `backend/procedures/afs_detalle_afiliados.sql`. Ventana/DW relacionados: `exe/Source Infomedical/wp_cons_detalle_afiliados.srw`, `exe/Source Infomedical/d_afs_detalle_afiliados.srd`.
- Causa diagnosticada: join final a `AFI_OPCION` no incluía `inte` y tenía criterio de baja sospechoso, pudiendo traer opciones de otros integrantes y multiplicar filas.
- Fix implementado y commiteado: unir `AFI_OPCION` por clave completa `prepaga + contra + inte` y vigencia activa a fecha para evitar duplicados.
- Commit SVN realizado: revisión `r83312`, usuario `lcamaratta@INFOMEDICAL`, mensaje `R29538 - Evitar duplicados en detalle de padron`.
- Después del commit quedó un cambio local NO commiteado en `backend/procedures/afs_detalle_afiliados.sql`: reemplazo del join final a `AFI_OPCION` + subquery `MAX(vigen_desde)` por `OUTER APPLY TOP 1` ordenado por `vigen_desde DESC`. Pendiente decidir mantener, ajustar o revertir antes de cualquier commit.
- Análisis de puntos flojos del SP: monolítico con muchas responsabilidades; muchas actualizaciones sucesivas sobre `#DETALLE_AFILIADOS`; riesgo de duplicados en joins históricos 1:N; filtros de vigencia inconsistentes; funciones escalares por fila; cálculos agregados repetidos por contrato; temporales cargadas aunque filtros sean NULL; uso innecesario de `WHERE 1=1`/`DISTINCT`/`UNION` en algunos casos.
- Recomendación de optimización segura para `afs_detalle_afiliados.sql`: priorizar joins 1:1 (`AFI_OPCION`, `AFI_RESP_PAGADOR`), índices en temporales `(prepaga, contra, inte)` y `(prepaga, contra)`, revisar `SET @mostrar_domi = 'S'`, cambiar subqueries `MAX(vigen_desde)` a `OUTER APPLY TOP 1`, precalcular importes/datos repetidos por contrato, y no cargar filtros temporales cuando no se usan.

## PowerBuilder - Exportación de débitos
- Usuario pidió optimizar la ventana PowerBuilder de exportación de débitos.
- Archivo modificado localmente: `exe/Source Infomedical/wp_export_debitos.srw`.
- Primer cambio aplicado localmente NO commiteado: en botón Buscar se eliminó la recuperación redundante de `dw_list_prn.Retrieve(...)`; antes el SP/listado se ejecutaba para detalle `D`, totales `T` y totales para impresión `T` otra vez. Nueva lógica: `dw_list_prn` se carga lazy en `ue_imprimir` sólo si el usuario imprime y está vacío. Se agregaron `SetRedraw(FALSE/TRUE)` alrededor de retrieves grandes.
- Cambio posterior pedido por usuario: implementar generación del contenido del archivo desde SQL y que PowerBuilder sólo escriba el archivo.
- Nuevos procedimientos agregados localmente y `svn add` realizados:
  - `backend/procedures/afs_export_debitos_contenido.sql`: genera contenido CAB/DET/TOT en SQL, llama a `afs_compro_exportacion` para datos D/T, lee `homolog_formatos`, arma líneas con CRLF y devuelve `archivo`, `contenido`, `reg_cont_det`. Mantiene efecto de `alta_codigo` registrando cuentas Visa faltantes mediante `afu_cuentas_visa`.
  - `backend/procedures/afi_compro_exportacion_spid.sql`: registra en lote los comprobantes exportados desde `exportacion_debitos` para el `@@SPID`, evitando un execute por comprobante.
- `wp_export_debitos.srw` ahora en `ue_exportar` llama `afs_export_debitos_contenido`, recibe contenido por archivo lógico (`CAB`, `DET`, `TOT`), convierte a `Blob(..., EncodingANSI!)` y escribe con `FileWriteEx` usando `FileOpen(..., StreamMode!, ..., EncodingANSI!)`.
- `wf_graba_comprobante` dejó de iterar `dw_list_det` y ahora llama una sola vez a `afi_compro_exportacion_spid`.
- Validación disponible: inspección/diff SVN y checks de marcadores/paréntesis. No se pudo compilar PowerBuilder ni ejecutar CREATE PROCEDURE en SQL Server desde este entorno.
- Riesgo técnico a validar en base/IDE PB: equivalencia exacta de máscaras/formatos especiales de `homolog_formatos`, porque la lógica de formateo fue migrada desde PowerBuilder a T-SQL y puede requerir ajuste fino por ente recaudador.

## Estado actual de working copy
- Repo: `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicialAgente`.
- Cambios locales NO commiteados conocidos:
  - `M backend/procedures/afs_detalle_afiliados.sql` (optimización OUTER APPLY post r83312, no relacionada con exportación débitos).
  - `A backend/procedures/afs_export_debitos_contenido.sql`.
  - `A backend/procedures/afi_compro_exportacion_spid.sql`.
  - `M exe/Source Infomedical/wp_export_debitos.srw`.
- No hacer `svn update`, commit ni otros cambios de repo salvo pedido explícito. Para este repo usar sólo SVN user `lcamaratta@INFOMEDICAL`, nunca `lpicone`.
