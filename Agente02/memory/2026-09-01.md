
## 2026-09-01 16:44 ART - PresmedWebInicial expediente import analysis

- User (Leandro) asked in Google Chat about `PresmedWebInicial`, window `wp_import_exp`, tracing where `real_valor` is populated during expediente import and how to change `pago_copa` logic.
- Findings already communicated:
  - `wp_import_exp.srw` triggers recalculation with `EXECUTE recalcu; -- pru_orden_a_liqui_expe` near line 1021.
  - `real_valor` is populated in `backend/procedures/pru_orden_a_liqui_expe.sql`: it calls `prs_orden_a_liquidar`, receives `@r_real_valor OUTPUT`, then updates `EXPE_ORDEN_ITEMS` setting `real_valor = @r_real_valor` and `conve_valor = @r_real_valor` around line 556.
  - `pru_orden_a_liqui_expe.sql` later calls `pru_expe_copagos.sql`, where `pago_copa` is calculated and persisted into `expe_copagos` from `#copa.pago_copa` around line 2142.
  - Table `backend/tables/cober_prestad_copagos.sql` contains relevant columns `prestad`, `pago_copa`, `baja_fecha`, `vigen_desde`.
  - Current `pru_expe_copagos.sql` uses `@var_prestad` from `expedientes.prestad`, not the order efector.
- Functional distinction communicated:
  - `expedientes.prestad` = expediente header/main prestador, loaded through `pri_expedientes`.
  - `expe_ordenes.efe_prestad` = efector prestador for a specific order/practice, loaded through `pri_expe_ordenes` from import row data.
  - For the requested `pago_copa` override, the effective check should be against `expe_ordenes.efe_prestad` when an active `cober_prestad_copagos` row exists for that efector/cobertura.
- Odontology import clarification communicated:
  - Odontology uses `wp_import_exp_odon.srw`, `pri_expe_odon.sql`, `expe_odon`, and `pru_odon_a_liquidar.sql`; it does not use `expe_ordenes.efe_prestad` or `pru_expe_copagos` in the same way.
  - Odontology equivalent efector field is `expe_odon.efe_prestad`.
  - `wp_import_exp_odon.srw` passes `@efe_prestad = :ll_efe_prestad` to `pri_expe_odon`; `expe_odon_items.real_valor` is initially inserted as null and later calculated in `pru_odon_a_liquidar.sql` through `prs_valida_of_convenio`, then updated into `expe_odon_items.real_valor`.
  - If applying a similar `pago_copa` override for odontology, analyze/change odontological flow (`pru_odon_a_liquidar` or PB `uo_nv_liqui_odon.of_copagos...`) using `expe_odon.efe_prestad`, not `pru_expe_copagos`.
