# 2026-07-08

## AutoRem / ws_auto_remotas incident analysis
- User: Cesar Luis Nani via Google Chat direct.
- Context: analyzing logs and code for `ws_auto_remotas` under `/home/luispicone/.openclaw/workspace/svn/lcamaratta-PresmedWebInicial/ws_auto_remotas`.
- Initial log showed JNDI/JDBC datasource `jdbc/AutoRemWebAplication` failing because SQL Server port was configured as literal `PORT`, then as unreachable `172.16.10.108:1443`; local profile indicated likely port `1433`.
- Later logs showed datasource initialized but Tomcat JDBC pool exhaustion: `PoolExhaustedException: Timeout: Pool empty... [size:40; busy:40; idle:0; lastwait:30000]`. Representative direct counts from `log_prueba_12.50`: `ConectionUtils` pool exhaustion errors 319, `PoolExhaustedException` occurrences 419, timestamped ERROR lines 580. Another cursor-related log had cursor errors 0, pool exhaustion direct errors 154, `PoolExhaustedException` occurrences 186, total ERROR lines 255.
- JMeter CSV `resultados_conpool_lean3` summary: 150 samples, 150 HTTP 200, no JMeter errors, total duration about 349.67s, avg throughput about 0.429 req/s, max `allThreads` 23, computed max concurrent in-flight requests 23, p95/p99/max elapsed about 90.1s. Recommendation given: if each request holds 1 JDBC connection, minimum `maxActive=23`, operational recommendation `maxActive=35`, conservative `40`; if pool of 40 still exhausts, likely leaks/long SQL/multiple connections per request.
- Excel analysis using column `alta_fecha`: 6823 rows, all valid, 988 distinct minutes. Ranking file created previously at `/home/luispicone/.openclaw/workspace/ranking_alta_fecha_por_minuto.csv`. Top minute was `2025-11-03 09:44 | 23`.

## Code review findings for connection leaks
- Main likely leak: `src/java/com/autorem/entities/Preautorizacion.java`, method `parce()`, around line 84: opens `conn = ConectionUtils.getInstance().connect();` and never closes it. Proposed fix: add `finally { ConectionUtils.getInstance().close(conn); }`.
- `src/java/com/autorem/bo/PrepagaBO.java`, around line 35/47: closes with raw `conn.close()` in `finally`; when `connect()` fails, `conn` is null and this matches logged `PrepagaBO - NO FUE POSIBLE CERRAR LA CONEXION:: java.lang.NullPointerException`. Proposed fix: replace with `ConectionUtils.getInstance().close(conn);` in both methods.
- `src/java/com/autorem/daos/SucursalDAO.java`, around line 55: opens `cs2` and `cs`, but only closes `cs`; proposed fix: close both `cs` and `cs2` in finally and check resultsets if present.
- `src/java/com/autorem/daos/consumoFarmaciaDAO.java`: multiple methods open connections without closing them, including approximate lines 41 `getConsumoFarmacia()`, 117 `grabarItems()`, 155 `valida_transaccion()`, 191 `cargar_salida()`, 380 `getAnulaConsumoFarmacia()`, 422 `cargar_salida_anulacion()`. They typically close statements but not `Connection`.
- BO methods with unsafe close inside try instead of finally, risk leak on DAO exception: `AfiliadoBO.java` lines ~36 and ~48; `PrestacionBO.java` lines ~40, 52, 64, 76, 88; `UserBO.java` lines ~43, 105, 129.
- `AutorizacionesDAO.java` around line 137: secondary `cs2`/`rs2` for `ars_param_general` did not show explicit close in reviewed context.
- Priority communicated: 1) `Preautorizacion.parce()`, 2) `consumoFarmaciaDAO` connection ownership, 3) BO methods closing inside try, 4) secondary Statement/ResultSet closure.
