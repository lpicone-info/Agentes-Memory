
## 2026-07-21 17:42 ART - Presmed Web / V25.08.001

- Contexto cargado desde `memory/2026-07-20.md`: Redmine 29809, Presmed Web, situacion terapeutica en autorizaciones tipo `P`; decision funcional de usar `histo` como clave contractual y `situ` solo como contexto/display.
- Presmed Web `lcamaratta-PresmedWebInicial`: se reviso `auto2` autorizacion asociada. Se confirmo que `auto2` no es obligatorio; si se informa, la validacion numerica entero mayor a 0 ya esta en Yup. Backend `validateAutoAsociada` debe responder `ok` para ejecucion tecnica y `existe` para existencia funcional; inexistencia no debe cerrar la app.
- Para `auto2`, se dejo/acepto criterio final: frontend puede usar `useApiRequest` y feedback anticipado en blur, pero no duplicar validacion funcional en submit porque las validaciones finales iran concentradas en SP al guardar.
- Se copiaron cambios de Presmed Web al compartido `/mnt/Agentes/lcamaratta/29809/presmed_web` y se validaron byte a byte con `cmp` en 7 archivos relacionados con `validateAutoAsociada`, hooks/frontend y combo de situacion terapeutica.
- Situacion terapeutica: el combo no marcaba tilde verde porque usaba `Form.Select` directo y no el wrapper `FormSelect` conectado a Formik. Se pidio usar un componente de combo como el resto, recibiendo array de situaciones como parametro, sin extender `FormSelect` con `handleChange`; usar el blur existente.
- Luego de actualizar `lcamaratta-PresmedWebInicial` a r84086, se resolvieron conflictos tomando version remota integrada. El remoto ya traia `validateAutoAsociada` con `ok/existe`, `useApiRequest`, combo versionado `ComboSituHistoriaAfiliado` y `onSubmit` sin validacion duplicada de `auto2`. Se retiro la validacion local redundante entero/positivo del blur y quedo sin cambios versionados pendientes.
- Se actualizo la rama SVN `lcamaratta-V25.08.001` a r84086; `svn status` quedo limpio inicialmente.
- WebPos transaccion `04A` al eliminar/anular: se relevo que el flujo WebPos valida numero de transaccion, entero, existencia, tipos anulables `02A/02C/02D/02L/02I`, pertenencia al prestador/centro, estado aceptado `A`, confirmacion en UI, y finalmente llama a `pri_transac_webpos_total`, que para `04A` llama a `prd_transac_remotas_auto`.
- En `prd_transac_remotas_auto.sql` se implemento validacion para rechazar una `04A` si la transaccion a anular tiene autorizacion (`@auto`) y existe otra transaccion vigente/aceptada `02A` o `02D` relacionada a esa misma autorizacion por `sucur_rel/auto_rel`, excluyendo la misma `@anula_transac`. Rechazo asignado: `1663`, mensaje: `La autorizacion esta relacionada a otra transaccion.`
- Tambien se agrego el rechazo `1663` en `backend/data/recha_motivos.sql` y `backend/data/R23586_recha_motivos.sql`. Quedaron modificados solo esos 3 archivos en `lcamaratta-V25.08.001`; no se ejecuto el SP contra base, solo validacion estatica/diff.
- Consulta posterior: si el metodo `ejecutarTransaccion` de `ws_auto_remotas` ejecuta una `04A`, segun la rama revisada no usa `prd_transac_remotas_auto`; el flujo legacy `uo_auto_remotas.sru` declara `del_trans` contra `dbo.prd_transac_remotas` y en caso `"4A"` ejecuta `prd_transac_remotas`. Por lo tanto el cambio actual cubre WebPos, pero no necesariamente `ws_auto_remotas`/legacy. Para cubrir ese flujo hay que replicar la misma validacion tambien en `backend/procedures/prd_transac_remotas.sql` con el rechazo `1663`.
