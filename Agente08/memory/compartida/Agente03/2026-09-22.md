# Memoria durable - 2026-09-22

- Interaccion en Google Chat directo con Federico. El agente se presento/cargo como Stan (Agente03), arquitecto tecnico para PowerBuilder 2022 y MS SQL.
- Flujo trabajado para Redmine 30352, usuario `facevedo`.
- Primero se recibio un pedido inconsistente para ticket `30252` con documento/ruta `R30352`; se detuvo ejecucion y se pidio correccion por inconsistencia y ruta de salida inexistente.
- Luego se genero informe bug `\\infomed03n\Agentes\facevedo\R30352\ticket_30352_02_bug.md` usando prompt SVN `branches/prompts_std/01_bug.md`, Redmine `30352`, documento `\\infomed03n\Agentes\facevedo\R30352\R30352_Requerimiento_mio.doc`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R30244` revision `84665`, base `SMC_Prox_Ver`.
- Resultado del informe bug 30352: `Defecto de codigo`, estado `CON OBSERVACIONES` porque falta caso concreto de reproduccion con expediente/autorizacion/efector real.
- Hallazgo funcional 30352: el flujo de internacion/sanatorio de circulo sigue validando que efector y lugar pertenezcan al circulo mediante `wp_expe_sana.srw` y SPs asociados; esa validacion bloquea permitir efectores no relacionados al circulo manteniendo lugar/categoria correctos para valorizar por convenio del circulo.
- Luego se genero especificacion tecnica `\\infomed03n\Agentes\facevedo\R30352\ticket_30352_04_spec.md` usando prompt SVN `branches/prompts_std/03_spec.md`, ticket `30352`, base `MutualRioIV_Prod_Test`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`, informe analisis previo `ticket_30352_02_bug.md`.
- Resumen de la spec 30352: objeto a tocar `wp_expe_sana.srw`; scripts SQL `0`; complejidad `Media`; estado `CON OBSERVACIONES`; sin decisiones de lider pendientes; definicion abierta no bloqueante por falta de expediente/autorizacion real para prueba; sin desvios funcionales, documentando que la spec se hizo contra `trunk` y el analisis venia de `R30244`.

## 2026-09-22 13:12 ART - Contexto durable

- Interlocutor en Google Chat directo: Walter.
- Walter consulto por impresion de facturas para cliente MonteGrande; MonteGrande se identifica con `contra_forma = 'T'`.
- Rama productiva verificada: SVN working copy `wguise-Infomedical-trunk`, URL `trunk`, bajo `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk`.
- En PowerBuilder, `uo_nv_factu.sru` selecciona `d_factura_montegrande.srd` para `contra_forma = 'T'`.
- `d_factura_montegrande.srd` ejecuta `dbo.afs_compro_impresion_cmmg`, toma `COMPRO_IMPRESION.barra` y lo imprime visualmente con `gf_interleaved_2de5(barra)`.
- Para MonteGrande (`T`), el armado de `barra` sale de `afi_compro_impresion.sql`: cuando `@var_contra_forma IN ('M','T','E')`, llama a `dbo.afn_get_medimas_codibarra(...)`; MonteGrande reutiliza logica Medimas.
- Condiciones detectadas: si `@reg_cod_barra = 'N'`, queda `barra = comprobantes.codi_barra`; si `ENTE_RECAUDADOR.tipo_barra = 'NG'`, no se genera codigo de barras.
- Tablas/campos base para el numero de barra:
  - `PREPAGAS`: `prepaga`, `ente`, `barra_ente_rec`.
  - `ENTES`: `cpagofacil` como primeros 4 digitos, ajustado con `RIGHT('0000' + LEFT(ISNULL(cpagofacil,''),4),4)`.
  - `ENTE_RECAUDADOR`: `tipo_barra` cuando `barra_ente_rec > 0`.
  - `COMPROBANTES`: `compro_nro`, `saldo`, `total`, `interes2`, `iva_interes2`, `ivan_interes2`, `vto`, `vto2`, `prepaga`, `contra`, `cuenta`, `subcta`.
  - `COMPRO_TIPOS`: `debi_credi`; solo genera barra para comprobantes de debito (`debi_credi = 1`).
- Para MonteGrande, importes usados:
  - `importe`: si `saldo > 0`, usa `saldo`; si no, usa `total`.
  - `importe2`: si `saldo <= 0`, usa `0`; si no, usa `saldo + interes2 + iva_interes2 + ivan_interes2`.
  - `vto1 = COMPROBANTES.vto`; `vto2 = COMPROBANTES.vto2`.
- Deuda para barra: se calcula con `afs_ctacte_deuda` sobre comprobantes adeudados por contrato (`prepaga`, `contra`) o subcuenta (`cuenta`, `subcta`); `deuda1` a `vto1`, `deuda2` a `vto2`; luego se usa deuda neta `deuda - creditos`.
- Firma de armado final: `dbo.afn_get_medimas_codibarra(cpagofacil, compro_nro, importe, importe2, vto1, vto2, deuda_neta1, deuda_neta2)`.
- Formato final del numero: `cpagofacil` 4 + `(importe + deuda_neta1) * 100` 11 + fecha vto1 AA/juliano 5 + ASCII letra `compro_nro` 2 + numero `compro_nro` sin letra 12 + moneda fija `0` + diferencia segundo vencimiento vs primero 11 + dias entre vencimientos 2 + digito verificador 1 + digito verificador 2.
- `gf_interleaved_2de5.srf` transforma el numero para impresion Interleaved 2 of 5: si largo impar antepone `0`, agrupa de a dos digitos, convierte cada par al caracter de fuente y envuelve con `{` y `}`.
