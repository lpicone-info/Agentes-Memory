
## 2026-06-02 18:13 UTC - Pre-compaction flush

- User works through Discord DM, Spanish. Owner/operator likely Luis Picone but do not address by name unless runtime explicitly identifies sender. User `wguise` uses Redmine/SVN and expects concise technical Spanish.
- SVN working copy in use: `/home/luispicone/.openclaw/workspace/svn/wguise-trunk_Agente03`, branch URL `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/trunk_Agente03`.
- Completed earlier: fixed `wp_main_procesos_batch.srw` so `FETCH valida_importa` runs only if `EXECUTE valida_importa` succeeded; committed SVN r83554 (`Corrige control de fetch en importacion DDJJ`). Validated clean WC, UTF-8 BOM, CRLF, svn log.
- Redmine 29588 / duplicate CUIL analysis: user reports `afs_afi_aportes_soli` still duplicates for contract `20-26742087-5`. Local branch SP already uses `OUTER APPLY TOP 1` on `cuentas` by `cuit`, prioritizing active account (`baja_fecha IS NULL`), latest `alta_fecha`, latest `cuenta`, so `cuentas` should no longer multiply rows. Suspected remaining duplication source: `afiliados` join lacks vigencia filtering, or duplicate/vigente rows already in `afi_deriv_aportes`. Candidate fix in `afs_afi_aportes_soli`: add to join with `afiliados` `AND (a.baja_fecha IS NULL OR a.baja_fecha >= @prm_fecha)` after validating data.
- Pending local uncommitted change from Redmine 29588 analysis: `exe/Source Infomedical/d_lst_afi_deriv_aportes.srd` changed from direct `LEFT OUTER JOIN cuentas c2 ON c2.cuit = ada.cuit` to `OUTER APPLY TOP 1`, prioritizing active account, latest `alta_fecha`, latest `cuenta`. This was not committed.
- DB profile handling: user attempted to share SQL Server credentials over Discord for `Quimicos_Prod` (`172.16.10.108`, port `1433`, database `Quimicos_Prod`, user `sa`, password exposed in chat). Do not repeat the password. Refused to save credentials from Discord; instructed profile must be created via UI/WebChat or console. Existing `SMC_PROD_TEST` profile noted; avoid confusing it with `Quimicos_Prod`.
- Redmine 29847: used user `wguise` Redmine credentials, downloaded/read attached `R29847.docx`, analyzed branch. Wrote analysis document at `/home/luispicone/.openclaw/workspace/svn/wguise-trunk_Agente03/redmines/29847/29847.md`. Main marked error: `prs_auto_x_pedido.sql` lists authorizations by pedido without filtering `baja_fecha IS NULL`, so rejected historical/non-current auth can appear even when current auth is accepted. Also documented risk in `afn_estado_pedido.sql`: for WEB pedidos, rejection can override accepted state if active `A` and `R` authorizations coexist. Reviewed `ars_pedidos_app`, `ars_pedidos_auto`, `afn_estado_pedido`, `prs_auto_x_pedido`, `prs_auto_pedidos`, `ars_pedidos_auto_prestac`, `d_pedido_auto.srd`. No commit made.
