# Memoria durable - 2026-08-11

## Trabajo con Cristian / RM30192 / wp_expe_orde
- Usuario frecuente: Cristian. Prefiere comunicación directa y técnica en español.
- Se trabajó sobre `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/wp_expe_orde.srw`.
- Cambio confirmado OK por Cristian: problema de refresh visual post-guardado del combo `Lugar` en `dw_det` de `wp_expe_orde`. La pantalla guardaba bien en DB, pero luego de guardar no refrescaba con el prestador efector hasta cerrar y reabrir.
- Causa detectada: después de `ue_restaurar`, la pantalla limpiaba `efe_prestad`, `efe_nombre`, `efe_cate` y `lugar`; luego llamaba a `wf_lugares('A')` sin haber restaurado el efector en `dw_det`, por lo que el child del combo `Lugar` se armaba sin el prestador efector correcto.
- Solución aplicada: reactivar la llamada existente a `wf_set_datos(...)` antes de `wf_lugares('A')`, para reponer fecha, demandantes, efector y categoría desde `str_i`, y recién después refrescar el combo `Lugar`.
- Ubicación del cambio informado a Cristian: `wp_expe_orde.srw`, líneas actuales aproximadas `7841` a `7844`, justo después de `TRIGGEREVENT('ue_restaurar')` y antes de `wf_lugares('A')`.
- Código reactivado:
```powerscript
wf_set_datos(str_i.fecha, li_nacpr_ori, ls_matri_ori, str_i.ori_prestad,&
					str_i.ori_nombre, li_nacpr_aso, ls_matri_aso, str_i.aso_prestad, &
					str_i.aso_nombre, li_nacpr_efe, ls_matri_efe, str_i.efe_nombre, &
					str_i.efe_prestad, str_i.efe_cate, ls_atrib)
```
- Se validó en ese momento: `svn diff` solo cambiaba ese bloque; encoding UTF-8 con BOM preservado; CRLF preservado; estado SVN solo modificado `exe/Source Infomedical/wp_expe_orde.srw`; no se hizo commit.
- Cristian pidió guardar el objeto en `/mnt/Agentes/cratto/RM30192/Consultas Particulares/Consulta1`; se copió `wp_expe_orde.srw` allí y se validó que destino quedó idéntico al origen, conservando UTF-8 con BOM + CRLF.

## Análisis funcional de `wp_expe_orde` y lugares / círculo
- En `wp_expe_orde.srw`, el prestador del expediente se lee desde `prestadores.tipo` al abrir la ventana:
```sql
SELECT ape_razon+', '+nombre_abre , tipo, bole_canti
  INTO :is_nom, :is_tipo_pre, :li_bole_canti
  FROM prestadores
 WHERE prestad = :exp.prestad
```
- Interpretación usada en pantalla: `is_tipo_pre = 'C'` significa prestador de círculo; NO círculo si `is_tipo_pre <> 'C'`.
- En `ACTIVA_Desarrollo`, para expediente `127885` con prestador `3480`, mensaje “Prestador sin lugar de atención cargado”. Datos relevados:
  - `ofi = 1`, `tipo = S`, `prestad = 3480`, `ambu_total = 50000`.
  - Prestador `3480` tiene `tipo = C`, o sea círculo.
  - `3480` tiene lugar directo vigente en `prestad_lugares`: lugar `1`, domicilio `AV. RIVADAVIA 481`, `baja_fecha = NULL`.
  - Pero para círculo la pantalla no usa ese camino directo: `wf_lugares('A')` usa el DW `dd_lugarxprestad_2`, que ejecuta `dbo.prs_lugares_x_prestador_circulo`.
  - Llamado equivalente devuelve 0 filas:
```sql
EXEC dbo.prs_lugares_x_prestador_circulo
  @prm_prestad_circulo = 3480,
  @prm_prestad = NULL,
  @prm_lugar = NULL
```
  - `prestad_lugares_circu` para `prestad = 3480`: 0 filas.
  - `prestad_lugares_circu` para `prestad2 = 3480`: 0 filas.
  - `prestad_circu_coord` relacionado con `3480`: 0 filas.
- Conclusión dada: no falta lugar directo; falta configuración vigente de relaciones de círculo usadas por `prs_lugares_x_prestador_circulo`.
- Caso comparable válido en `ACTIVA_Desarrollo`: prestador círculo `3178` - `CIRCULO MEDICO TRENQUE LAUQUEN`.
  - Lugar devuelto: `AVELLANEDA Nº 145 (1)`.
  - `prestad_lugares`: 1 lugar vigente.
  - `prestad_lugares_circu`: 157 relaciones vigentes.
  - Expedientes ejemplo: `127687`, `127675`, `127688`, `126548`, `126751`, `126750`, `125954`, `125956`, todos con `ofi = 1`, tipo `O`, prestad `3178` y lugar `AVELLANEDA Nº 145 (1)`.

## Tipos de expedientes explicados
- `expedientes.tipo = 'O'`: Órdenes. Carga de prestaciones por orden ambulatoria: prácticas, consultas, bonos, cirugías ambulatorias, etc. Pantalla relacionada: `wp_expe_orde`.
- `expedientes.tipo = 'S'`: Sanatorio.
  - Parte ambulatoria cuando `exp.tipo = 'S' AND exp.ambu_total <> 0`.
  - Parte internación para la carga sanatorial/internación.
  - Código relevante:
```powerscript
IF (exp.tipo = 'O') OR (exp.tipo = 'S' AND exp.ambu_total <> 0) THEN
```
- Odontología: circuito odontológico. En `wp_expe_odon.srw`, `i_expe.tipo = 'D'` para odontología valorizada.
- También aparece `E` como odontología no valorizada en módulos de selección/supervisión.
- Resumen transmitido: `O = Órdenes`, `S = Sanatorio`, `D = Odontología valorizada`, `E = Odontología no valorizada`.

## Autorizaciones sin efector
- En autorizaciones existe validación:
```powerscript
IF is_obliga_efe = 'S' THEN
    IF lib.of_vacio(dw_efec.GetItemNumber(1,'efe_prestad')) THEN
        mbox(THIS,'Ingrese Datos del Efector',1,0)
```
- Referencia informada: `w_clase_auto.srw`, líneas aproximadas `2806` a `2815`.
- Comentario histórico visto:
```powerscript
// Se debe permitir generar la autorización sin prestador para las de prestaciones.
```
- En `ACTIVA_Desarrollo`, `parametros.obliga_efe = S`, por lo que no permite cargar autorización sin efector en los circuitos alcanzados por esa validación.
- Para habilitar carga sin efector se indicó cambiar:
```sql
UPDATE parametros
SET obliga_efe = 'N'
```
- No se ejecutó el `UPDATE` sin confirmación explícita.

## Profile de base ACTIVA_Desarrollo
- Cristian pidió crear profile en `DB_PROFILE.md`:
  - Profile: `ACTIVA_Desarrollo`
  - Motor: `MS SQL`
  - Host/IP: `172.16.10.108`
  - Base: `ACTIVA_Desarrollo`
  - Usuario: `sa`
  - Password: `IMed.2021`
  - Puerto: `1433`
  - Notas: `N/A`
- Se registró correctamente en `/home/luispicone/.openclaw/workspace/DB_PROFILE.md`.
- Se validó conexión autenticada y `SELECT DB_NAME()` con estado `OK`.
- Archivo conservó UTF-8 sin BOM + LF.

## Ajuste propuesto pendiente: seleccionar lugar de autorización en `wf_lugares('A')`
- Requerimiento funcional de Cristian: al agregar una autorización al expediente, el combo `Lugar` se completa con datos del efector y debe respetar la regla: “Carga de autorización con efector igual al del expediente debe admitirse y los datos de efector y lugar se deben tomar de la autorización”. El sistema ya toma efector/lugares, falta dejar seleccionado el lugar de la autorización.
- Primer punto detectado: en `ItemChanged` de columna `AUTO`, bloque aprox. líneas `885` a `889` pisa el lugar con el primer lugar del combo:
```powerscript
IF idch_lug.RowCount() > 0 THEN
	IF dw_det.Rowcount() > 0 THEN
		dw_det.object.lugar[1] = idch_lug.getitemnumber( 1, "lugar")
	END IF
END IF
```
- Se sugirió reemplazar por lógica que use `lds_auto.object.efe_lugar[1]` si existe en el child `idch_lug`; si no, caer al primer lugar.
- Luego Cristian observó que en `ue_agregar`, al final, dispara `wf_lugares('A')`, y en ese método existe un bloque que para prestador círculo/órdenes/sanatorio ambulatorio deja lugar en null o primer lugar según reglas. Se consideró más correcto ajustar ahí.
- En `ue_agregar`, antes de `ue_restaurar`, se conserva el lugar de autorización en variable global:
```powerscript
ii_lugar = str_i.lugar
```
- Referencia informada: `wp_expe_orde.srw`, línea actual aproximada `7829`.
- Luego se llama a `wf_set_datos(...)` y `wf_lugares('A')` en líneas aprox. `7841` a `7849`.
- Bloque propuesto para reemplazar dentro de `wf_lugares('A')`, líneas actuales aproximadas `3874` a `3881`:
```powerscript
IF li_lugs = 1 THEN
	dw_det.Object.lugar[1] = ids_lugar.object.lugar[1]
ELSE
	IF lib.of_vacio(dw_det.Object.lugar[1]) OR cambioEfector = TRUE THEN //R28808 - Simon
		dw_det.Object.lugar[1] = ll_null
		mbox('El efector tiene más de un lugar de atención, seleccione uno.')
		RETURN li_lugs
	END IF
END IF
```
- Reemplazo sugerido:
```powerscript
IF li_lugs = 1 THEN
	dw_det.Object.lugar[1] = ids_lugar.object.lugar[1]
ELSE
	IF NOT lib.of_vacio(ii_lugar) AND idch_lug.Find("lugar = " + String(ii_lugar), 1, idch_lug.RowCount()) > 0 THEN
		dw_det.Object.lugar[1] = ii_lugar
	ELSEIF lib.of_vacio(dw_det.Object.lugar[1]) OR cambioEfector = TRUE THEN //R28808 - Simon
		dw_det.Object.lugar[1] = ll_null
		mbox('El efector tiene más de un lugar de atención, seleccione uno.')
		RETURN li_lugs
	END IF
END IF
```
- Motivo: en el flujo de `ue_agregar`, `ii_lugar` conserva `str_i.lugar`, que viene de la autorización. Si ese lugar existe dentro del combo cargado (`idch_lug`), se selecciona directamente. Si no existe, se mantiene comportamiento anterior: lugar vacío y obliga a seleccionar.

## RM30192 - Validaciones y documentos generados
- Se ejecutó revisión de validación para Redmine `30192` según prompt SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/05_validar.md`, revisión SVN `84294`, rama `trunk`, base `ACTIVA_Desarrollo`, documento funcional `/mnt/Agentes/cratto/RM30192/R30192.docx` con foco en resumen funcional desde página 8.
- Informe generado en `/mnt/Agentes/cratto/RM30192/pruebas/ticket_30192_validar.md`.
- Veredicto informado: `Rechazado - requiere correcciones y nueva revisión`.
- Punto principal: r84294 cubría parcialmente el parámetro `liquida_efector_diferente`, pero no garantizaba correctamente que, al aceptar autorización con efector diferente y parámetro `S`, el `Lugar` y los valores se tomen del prestador del expediente como pide el resumen funcional. También quedaron observaciones sobre Sanatorio y Odontología.

## RM30192 - Cambio Validación Funcional 1
- Cristian pidió revisar el informe `ticket_30192_validar.md`, apartado `Validación Funcional`, punto `1 Aplicar en órdenes - Parcial`, y armar documento con cambios para cumplirlo.
- Documento generado: `/mnt/Agentes/cratto/RM30192/pruebas/Cambios_ValFuncional1.md`.
- Contenido del documento: cambios propuestos para completar el punto `Validación Funcional 1 - Aplicar en Órdenes - Parcial`, enfocados en `wp_expe_orde.srw`: ordenar validación de efector diferente, recalcular combo `Lugar` con el prestador correcto, respetar `efe_lugar` cuando corresponde y evitar doble validación histórica del parámetro.
- Luego Cristian pidió exportar el objeto modificado en `/mnt/Agentes/cratto/RM30192/pruebas/CambioValfuncional1`.
- Se modificó/exportó `wp_expe_orde.srw` en:
  - Working copy: `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/wp_expe_orde.srw`
  - Exportado: `/mnt/Agentes/cratto/RM30192/pruebas/CambioValfuncional1/wp_expe_orde.srw`
- Se validó que el exportado fuera idéntico al objeto de working copy y conservara `UTF-8 con BOM + CRLF`. No se compiló PowerBuilder.

## RM30192 - Cambio Validación Funcional 9
- Cristian pidió revisar el punto `9 - Con parametro S y Aceptar, lugar y valores desde prestador del expediente`, indicado como no cumple, armar documento y exportar objetos modificados.
- Documento generado: `/mnt/Agentes/cratto/RM30192/pruebas/CambioValFuncional9/Cambios_ValFuncional9.md`.
- Objetos exportados en `/mnt/Agentes/cratto/RM30192/pruebas/CambioValFuncional9/`:
  - `wp_expe_orde.srw`
  - `wp_expe_sana.srw`
- También quedaron modificados en working copy SVN los objetos correspondientes dentro de `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk/exe/Source Infomedical/`.
- Se validó que ambos exportados fueran idénticos a la working copy y conservaran `UTF-8 con BOM + CRLF`. No se compiló PowerBuilder.

## Sanatorio / Internación - circuito de carga
- Cristian preguntó desde qué pantalla se carga un expediente de sanatorios y qué tipo/opción de autorización inicia el circuito.
- Respuesta dada: para `Sanatorio / Internación`, el circuito arranca con autorización tipo `I = Internaciones`.
- Opción de menú informada: `Autorizaciones > Autorizaciones > Internaciones`.
- Pantalla de autorización: `wp_auto_internaciones`.
- Luego el expediente sanatorial se carga en `wp_expe_sana`.
- En la selección de expedientes, para `expedientes.tipo = 'S'`, la pantalla decide:
```powerscript
case 'S'
   if rb_inter.checked then
      openwithparm(wp_expe_sana,lstr)
   else
      openwithparm(wp_expe_orde,lstr)
   end if
```
- Interpretación transmitida: `Sanatorio + Internación` usa `wp_expe_sana`; `Sanatorio + Ambulatoria` usa `wp_expe_orde`, aunque el expediente sea tipo `S`.
