
## 2026-07-15 - Infomedical facturacion/exportacion/importador

- Usuario consulto en Google Chat sobre rama productiva `Infomedical/trunk`, working copy principal en `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk`, revision local observada `83888`.
- `afs_export_recaudo_cab.sql`: `tipo_contrato` se define como `char(1)` en `#TMP_RECAUDOS`; para cabeceras se carga desde `AFI_CLASE.clase_afi` con `CASE afc.clase_afi WHEN 'D' THEN 'D' WHEN 'E' THEN 'E' ELSE NULL END`; para detalles copia `tmp.tipo_contrato`; el `SELECT` final lo devuelve. PowerBuilder `d_export_recaudo.srd` recibe la columna y `wp_export_recaudos_ente.srw` la exporta cruda si el formato la incluye; si es NULL se escribe string vacio. Formato `ER`: posicion 28, longitud 1, tipo S en `homolog_formatos_23044.SQL`.
- Facturacion con alta futura: el proceso masivo llama `afi_generar_prefacturas` desde `afj_job_facturacion_procesar.sql`. `afi_generar_prefacturas.sql` busca `AFI_CLASE` vigente con `vigen_desde <= @factu_perio` para directos y empresa, calcula `mes_ingreso` con la menor `AFI_CLASE.vigen_desde` hasta el periodo, y descarta si `mes_ingreso > factu_perio`. `afi_prefac_prefacturar.sql` tambien busca clase vigente por `@var_apli_perio`. Conclusion dada: por codigo no deberia facturar periodos sin clase vigente; revisar si la comparacion del usuario es contra `COMPROBANTES.fecha` en vez de `COMPRO_ITEMS.apli_perio` / `PREFACTURAS.apli_perio`, o si los comprobantes se generaron antes de corregir `AFI_CLASE`.
- `afs_valida_valor_cuota_import`: en la rama productiva se encontro un solo consumidor aplicativo: ventana PowerBuilder `w_importar.srw` del aplicativo separado de importacion. Declara `DECLARE prs_tarifa PROCEDURE FOR dbo.afs_valida_valor_cuota_import`; se ejecuta desde evento `cb_import.clicked`, solo para titulares activos, bloque "Buscamos la tarifa del titular", con `EXECUTE`/`FETCH` alrededor de la linea 601. Tambien aparece compilado en `exe/import.pbl` y `exe/import.pbd`; no se encontraron otros consumidores en SQL, fuente PB, PBL/PBD u otros archivos.
- Apertura de `w_importar`: se abre desde el ejecutable/proyecto `import.exe` definido por `impor.srj`; aplicacion PB `import.sra` en evento `open` hace `Open(w_login_imp)` y, si no cancela, `Open(w_importar)`. `w_importar.srw` esta incluido en `import.pbg`. No se encontro abierto desde menu del aplicativo principal.

## 2026-07-15 18:00 UTC - Autorizaciones V25.08.001 pago_copa

- Usuario reporto en Google Chat bug en rama `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`, working copy local usada `/home/luispicone/.openclaw/workspace/svn/wguise-V25.08.001`: al editar el estado de una autorizacion ya guardada, `AUTORIZACIONES.pago_copa` queda NULL; si luego cambia la cantidad, el copago vuelve correcto.
- Analisis inicial: `pru_autorizaciones.sql` tiene parametro `@pago_copa` default NULL y, en update de autorizacion existente, hace `pago_copa = @pago_copa`; por eso cualquier flujo que llame cabecera sin informar `@pago_copa` pisa el valor anterior. Casos detectados: `uo_nv_valida_auto.sru` declara/llama `pru_autorizaciones` y puede mandar `pago_copa` desde `ads_auto`; `wp_auto_importac.srw` llama cabecera sin pasar `@pago_copa`; `prs_valida_of_auto_remotas.sql` tambien puede pisar con valor nulo desde remotas o limpiar por guardia/copagos_guardia=N. En `w_clase_auto.srw` cambios de grilla pueden limpiar `pago_copa` de item al editar `excedente`/`nomen`.
- Secuencia especifica confirmada: en `w_clase_auto.srw`, al editar columna `esta`, entra por `dw_prestac.itemchanged` caso `esta`; setea `is_cambia_estado = 'S'`, llama calculo/validacion de copagos, pero bajo condicion RM21363 en consulta con cambio de estado y copago existente se desactiva recalculo (`lb_recalc = FALSE`). Asi puede no repoblar `dw_copa_orden.pago_copa`; luego al guardar `uo_nv_valida_auto.of_auto_remotas()` toma `pago_copa` desde `ads_auto` y se lo pasa a `pru_autorizaciones`, que actualiza cabecera con NULL.
- Comparacion con productiva `trunk` (`/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk`, revision local observada `83888`) contra `V25.08.001` (`83966`): `pru_autorizaciones.sql` no explica la diferencia porque actualiza igual en ambas. La diferencia funcional esta en `w_clase_auto.srw`: trunk tiene flujo de cambio de estado mas simple y al cambiar `esta` recalcula copago y ejecuta `wf_actualizar_copagos()`; `V25.08.001` incorpora cambios R28243/R29786, tipos nuevos `M` y `T`, `ib_modo_consulta` y `wf_ingresa_modo_consulta()`, lo que deja mas veces el cambio de estado en `is_cambia_estado='S' AND is_consulta='S'` sin repoblar `dw_copa_orden.pago_copa`.
- Diagnostico comunicado: productiva no falla porque no tiene el nuevo flujo de modo consulta/editable que evita recomponer `pago_copa` al editar solo estado. Ajuste recomendado para proxima tarea: preservar el `pago_copa` anterior o repoblar `dw_copa_orden.pago_copa` antes de copiar a `lds_auto`/`ads_auto` cuando solo se edita estado, evitando que `pru_autorizaciones` reciba `@pago_copa = NULL`.

## 2026-07-15 18:11 UTC - Autorizaciones V25.08.001 efe_prestad/lugar

- Usuario retomo tarea en rama `V25.08.001` sobre modulo de autorizaciones: ya no arrojaba validacion cuando la autorizacion no tenia `efe_prestad`, pero faltaba corregir que al ingresar un numero de autorizacion el sistema intentaba cambiar el combo/campo `lugar` al lugar del `efe_prestad` de la autorizacion. Si la autorizacion no tiene `efe_prestad`, debe conservar el lugar ya cargado desde el prestador del expediente.
- Working copy usada: `/home/luispicone/.openclaw/workspace/svn/wguise-V25.08.001`.
- Cambio aplicado en `exe/Source Infomedical/w_clase_auto.srw`: en la carga de autorizacion tipo `P/E/M/T`, ahora solo ejecuta `ue_setefec(...)` si `ids_auto.efe_prestad` tiene valor. Si no tiene `efe_prestad`, no intenta recalcular efector ni `efe_lugar`, preservando el lugar existente del expediente.
- Cambio aplicado en `exe/Source Infomedical/w_clase_auto_internacion.srw`: se reforzo el mismo criterio usando `lib.of_vacio(...)` en lugar de solo `isnull(...)`, cubriendo valores nulos/vacios/cero.
- Validacion realizada: revision de `svn diff`; el cambio quedo acotado a esas guardas. No se compilo PowerBuilder desde el entorno. Se informo que habia modificaciones previas no propias en la working copy: `prd_autorizaciones.sql`, `prs_valida_cate_circu.sql` y `wp_sube_prestador2.srw`.
