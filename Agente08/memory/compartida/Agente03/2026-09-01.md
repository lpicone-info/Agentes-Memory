# Memoria durable - 2026-09-01

## Ticket Redmine 30305 - conta_cuentas_expe / ambu_inter
- Walter solicitó generar el informe `X:\wguise\R30305\ticket_30305_01_bug.md` usando prompt SVN `branches/prompts_std/01_bug.md`, ticket Redmine `30305`, usuario `wguise`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`, documentos fuente `X:\wguise\R30305\MAFALDA analisis_ospecon_test_asientos_expedientes_11_12_20260831.docx` y `X:\wguise\R30305\R30305.docx`.
- El informe fue generado y luego actualizado. Validaciones reportadas: archivo ASCII, LF, sin BOM; primero 282 líneas, luego 286 líneas tras ajuste.
- Hallazgo principal inicial: `pfn_calcular_conta_cuentas` no interpreta `ambu_inter = NULL` como `Ambos`, porque filtra por `c.ambu_inter = @prm_ambu_inter OR c.ambu_inter = 'T'`; con `NULL` no matchea.
- Otro hallazgo: `prs_valida_conta_cuentas` puede bloquear altas/modificaciones por validaciones parciales demasiado amplias antes de validar la clave funcional completa.
- Walter aclaró que el combo `Ambito` se alimenta directo de `conta_cuentas_expe.ambu_inter` en el DataObject `d_abm_conta_cuentas_expe`; el campo tiene edit `dropdownlistbox` con valores `Ambos = T`, `Ambulatorio = A`, `Internacion = I`. Si en tabla está `NULL`, no muestra nada. Para que el combo muestre Ambos naturalmente debe estar persistido `T`.
- Se actualizó el informe para dejar explícito que tocar solo cálculo/validación no corrige la visualización del combo si sigue guardado `NULL`; para corregir visualización debe persistirse `ambu_inter = 'T'` o maquillar visualmente el histórico.
- Luego Walter pidió verificar si el aplicativo opera con `conta_cuentas_expe.ambu_inter` cuando tiene `NULL`, para decidir entre seguir guardando `NULL` y ajustar visual, o empezar a guardar `T`.
- Revisión de rama `V25.08.001`: `d_abm_conta_cuentas_expe.srd` recupera directo `conta_cuentas_expe.ambu_inter` y el combo solo conoce `T/A/I`, por eso `NULL` queda vacío.
- `prs_d_lista_conta_cuentas_expe.sql` en grilla sí interpreta `NULL` como Ambos con `CASE`: `A -> Ambulatorio`, `I -> Internacion`, `ELSE -> Ambos`. La lista maquilla `NULL` como Ambos.
- `pri_conta_cuentas.sql` y `pru_conta_cuentas.sql` graban lo recibido en `@prm_ambu_inter`.
- `wp_abm_conta_cuentas_expe.srw` inicializa `ambu_inter = 'T'` en alta/limpieza y valida que no esté vacío antes de alta/modificación. Desde pantalla actual debería tender a guardar `T`, no `NULL`.
- Script de tabla `conta_cuentas_expe.sql` define/agrega `ambu_inter CHAR(1) NULL`, por lo cual la base permite históricos o cargas externas con `NULL`.
- Criterio técnico recomendado a Walter: no conviene ajustar solo visual manteniendo `NULL`, porque afecta cálculo contable. Consolidar `T` como valor real persistido para Ambos: normalizar históricos `NULL -> T` con validación previa, asegurar nuevas altas con `T`, ajustar detalle visual defensivamente para históricos, y revisar `pfn_calcular_conta_cuentas` si se decide tolerar bases no normalizadas.

## Cristian - Redmine 30003 - DataWindow auditoria transiciones comentarios
- Cristian retomó RM30003 con objetos locales: `/mnt/Agentes/cratto/RM30003/d_auditoria_transiciones.srd`, `/mnt/Agentes/cratto/RM30003/pri_audit_auto_transiciones.sql`, `/mnt/Agentes/cratto/RM30003/prs_audit_auto_transiciones.sql`. Indicó explícitamente no tocar ramas SVN.
- Requerimiento: columna `comentarios` de la DW debe soportar 1000 caracteres, unificar criterio con SP ya modificado para devolver 1000, autoestirar celda hacia abajo y permitir lectura correcta.
- Cambio aplicado solo en `/mnt/Agentes/cratto/RM30003/d_auditoria_transiciones.srd`: columna `comentarios` con `edit.limit=1000`, `edit.autohscroll=no`, mantiene `height.autosize=yes`, `detail height.autosize=yes`, `edit.displayonly=yes`; luego se cambió `tabsequence=10` a `tabsequence=32766` para evitar entrada en modo edición/foco al hacer click.
- No se modificaron `pri_audit_auto_transiciones.sql`, `prs_audit_auto_transiciones.sql`, base de datos ni ramas SVN.
- Validaciones realizadas: SRD preservado como `UTF-16 LE con BOM` y finales `CRLF`; delta limitado a propiedades esperadas. Caso testigo en `Ospecon_Desarrollo`: `select comentario from AUDIT_AUTO_TRANSICIONES where id_auto_transicion = 2631`; comentario real tiene 1016 caracteres y SP devuelve `VARCHAR(1000)`, truncando a 1000.
- Sobre mayúsculas: en el SRD no hay `edit.case=upper`, está `edit.case=any`; SP no aplica `UPPER`; el dato en base vuelve con minúsculas y acentos. El síntoma al hacer click se atribuyó a entrada en edición/foco, no al dato ni a la DW como definición.
- Se explicó a Cristian que `Send(Handle(THIS),256,9,LONG(0,0)); RETURN 1` envía `WM_KEYDOWN` con `VK_TAB` al control actual para simular Tab y consumir el evento. Para `comentarios` se recomendó no activarlo porque es una columna de lectura de texto largo y puede disparar navegación/eventos heredados innecesarios.

## Cristian - Redmine 26993 - cambio codigo de barras desestimado
- RM26993 fue retomado para revisar diferencia entre Montegrande y otros clientes en código de barras Medimas.
- Estado técnico analizado: en Montegrande, para el caso testigo, `impor1=43000`, `deuda1=0`, `creditos=32000`, `deuda_neta=-32000`; la función anterior calculaba barra como `impor1 + deuda_neta = 11000`, mientras el importe visible ya venía neto. El cambio propuesto era usar solo `importe` cuando `deuda_neta <= 0`, y `importe + deuda_neta` cuando `deuda_neta > 0`.
- Comparación con otros clientes: en Celer el monto visible y la barra usan criterio alineado (`impor1 + deuda1 - creditos`), por eso no se reproduce la doble resta de crédito.
- Cristian decidió desestimar el cambio de código de barras. Instrucción vigente: no tocar barras ni SVN para RM26993; el archivo local queda solo como antecedente no vigente.

## Cristian - Redmine 29840 - prueba en smc_desarrollo
- RM29840 retomado. Objetos propuestos existentes: `/mnt/Agentes/cratto/RM29840/ObjetosModificados/wp_selexp_cir.srw`, `/mnt/Agentes/cratto/RM29840/ObjetosModificados/uo_nv_liquida.sru`, `/mnt/Agentes/cratto/RM29840/ObjetosModificados/pru_expe_reabrir.sql`; informes en `/mnt/Agentes/cratto/RM29840/Informes/`. Prompt 04 quedó en modo `PROPONER`; no tocar ramas SVN ni base salvo pedido explícito.
- Cristian pidió una base de desarrollo versión 25.08 para probar. Se revisaron profiles de desarrollo: `Ospecon_Desarrollo` no sirve para RM29840 porque no tiene expedientes sanatorio útiles (`tipo S`) y `prs_d_expe_a_reabrir` no devuelve filas; `ACTIVA_Desarrollo` tiene sanatorios pero no candidatos completos; `smc_desarrollo` sí sirve.
- Candidato recomendado para prueba en `smc_desarrollo`: expediente `ofi=2`, `expe=24032`, `tipo='S'`, `prestad=2408`, `ambu_total=725168.00`, `inter_total=82666348.44`, `carga_fecha=28/02/2025`, `inter_carga_fecha=28/02/2025`, `cierre_admin_fecha=28/02/2025`, `revi_fecha=28/02/2025`, `inter_revi_fecha=28/02/2025`, `super_fecha=NULL`, `liqui_fecha=NULL`, `expor_admin='S'`.
- Otros candidatos en `smc_desarrollo`: `ofi 2 - expe 22670`, `ofi 1 - expe 25736`, `ofi 2 - expe 17973`, `ofi 1 - expe 4958`.
- Resumen de prueba indicado: validar independencia entre parte ambulatoria e internación. Prueba 1: buscar expediente 24032, seleccionar `Internacion` y ejecutar reapertura. Esperado por base: `carga_fecha` conserva valor; `inter_carga_fecha`, `cierre_admin_fecha`, `revi_fecha` quedan `NULL`; `super_fecha` y `liqui_fecha` siguen `NULL`; `expor_admin` queda `NULL`. Revisar también `audi_fecha` e `inter_audi_fecha`.
- Consulta de control sugerida para expediente: `SELECT ofi, expe, tipo, carga_fecha, inter_carga_fecha, cierre_admin_fecha, revi_fecha, inter_revi_fecha, super_fecha, liqui_fecha, expor_admin, audi_fecha, inter_audi_fecha FROM expedientes WHERE ofi = 2 AND expe = 24032`.
- Auditoría sugerida: `SELECT TOP 20 ofi, expe, fecha_hora, activi, usuario, ambu_inter FROM expe_auditoria WHERE ofi = 2 AND expe = 24032 ORDER BY fecha_hora DESC`. Esperado al reabrir internación: aparecen `Reabre Inter` y `Reapertura`; no debe reabrir ambulatorio dejando `carga_fecha` en `NULL`.
- Prueba 2/ambulatorio: requiere restaurar el expediente a cerrado o usar otro candidato equivalente. Al reabrir `Ambulatorio`, esperado: `carga_fecha=NULL`, `inter_carga_fecha` conserva valor, y no debe duplicar `Reapertura` por una sola acción ambulatoria.
