
## 2026-08-27 12:54 ART - Pre-compaction flush

- Usuario en Google Chat directo: Cristian. El agente se presenta/funciona como Stan (Agente03), perfil arquitecto técnico PowerBuilder 2022 / MS SQL, comunicación directa y precisa.
- DB_PROFILE usado recientemente: `Activa_Desarrollo`. Consultas previas solicitadas por Cristian:
  - Afiliado activo con cuenta corriente 2026: COFFEN, IVANA MARIA, contrato `000332`, integrante `00`, documento `DU 12277875`, CUIL `23122778754`, comprobante `FC B000300003696`, periodo `01/08/2026`, importe/saldo `14936.72`. Criterio: `afiliados.baja_fecha IS NULL`, `comprobantes.baja_fecha IS NULL`, `factu_perio` dentro de 2026.
  - Afiliado activo con cuenta corriente a favor: ALMADA, MONICA BEATRIZ, contrato `006087`, integrante `00`, documento `DU 13906810`, CUIL `27139068101`, comprobante `RC B000100000417`, periodo `01/03/2026`, saldo `-398.49`. Criterio: saldo negativo en 2026.
- Caso RM26993: Cristian pidio aplicar en distintos objetos los criterios utilizados en Sbsalud, sin modificar ramas SVN. Importante mantener trabajo solo sobre carpetas de objetos bajo `/mnt/Agentes/cratto/RM26993/...`.
- Caso Montegrande finalizado sobre:
  - `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/dbo.afs_compro_impresion_cmmg.sql`
  - `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd`
  Cambios informados: SP usa `dbo.afn_numeros_a_letras(c.total)` para `impor_letras`/`impor_letra`; devuelve `total`, `grava`, `exen` desde `COMPROBANTES`; elimina salida/referencia a `saldo`; DW agrega `total`, `grava`, `exen`; `subtotal_a = grava + exen`; `total_factura_a/b = total`; vencimientos 1 y 2 A/B usan `abs(total - creditos)`; `barra` se oculta cuando `abs(total - creditos) = 0`. Validado: SQL `ISO-8859`, sin BOM, CRLF puro, sin `;`; SRD `UTF-16 LE BOM`, CRLF UTF-16; `svn status` sin modificaciones en copias SVN consultadas; no se compilo PowerBuilder.
- Caso Mutual finalizado sobre:
  - `/mnt/Agentes/cratto/RM26993/ObjetosMutual/afs_compro_impresion_mutual.sql`
  - `/mnt/Agentes/cratto/RM26993/ObjetosMutual/d_factura_mutual.srd`
  Cambios informados: SP usa `dbo.afn_numeros_a_letras(c.total)` para `impor_letras`/`impor_letra`; devuelve `total`, `grava`, `exen`; DW agrega `total`, `grava`, `exen`; `total_factura_a/b = total`; `subtotal_a = grava + exen`; vencimientos A/B de vto1, vto2 y vto3 usan `abs(total - creditos)`; `barra`, `barra_128` y `barra_numeros` se ocultan si `abs(total - creditos) = 0`. Validado: SQL `ASCII`, sin BOM, CRLF puro, sin `;`; SRD `UTF-16 LE BOM`, CRLF UTF-16; `svn status` sin modificaciones en copias SVN consultadas; no se compilo PowerBuilder.
- En Mutual hubo una ruta errada inicial (`d_factura_montegrande.srd`) que no existia; se corrigio a `/mnt/Agentes/cratto/RM26993/ObjetosMutual/d_factura_mutual.srd` tras confirmacion.
