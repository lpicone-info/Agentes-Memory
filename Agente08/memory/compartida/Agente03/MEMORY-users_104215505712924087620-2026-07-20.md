# Memoria curada diaria

- Fecha: 2026-07-20
- Usuario: Federico Acevedo
- id_googlechat: users/104215505712924087620
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/l2edokaaaae

## Resumen de la actividad

### Tema 1: Alta del profile Unosalud_prod_test

- Solicitud o contexto: Federico pidió registrar el profile `Unosalud_prod_test` para MS SQL Server con host `172.16.10.108`, puerto `1433`, base `Unosalud_prod_test` y usuario `sa`.
- Respuesta o acciones del agente: Se detectó que faltaba el campo obligatorio `Notas`, se solicitó ese dato y luego se ejecutó la skill de alta controlada de profile. No se conserva la contraseña en esta memoria.
- Resultado: El profile `Unosalud_prod_test` fue registrado en `DB_PROFILE.md`, con conexión autenticada validada mediante `SELECT DB_NAME()`, sin duplicado previo, TCP OK, formato preservado sin BOM y finales LF.
- Estado: Resuelto

### Tema 2: Informe de bug Redmine 30074

- Solicitud o contexto: Federico pidió generar `ticket_30074_01_bug.md` usando prompt SVN `01_bug.md`, ticket Redmine `30074`, usuario `facevedo`, base `Unosalud_prod_test`, rama trunk y documento fuente `R30074.doc`, limitado al caso #1 y caso #3.
- Respuesta o acciones del agente: Se analizó el documento fuente solo en los casos indicados y se usó Redmine como contexto complementario.
- Resultado: Se generó `/mnt/Agentes/facevedo/R30074/ticket_30074_01_bug.md`. El caso #1 se confirmó como error funcional en el rango de fechas de búsqueda de autorizaciones: julio no devolvía filas, pero desde junio aparecía la autorización `885580` del `03/06/2026`. El caso #3 se confirmó como error lógico en `wp_presta_insti.srw`: la opción de lugar propio se agregaba para tipos `P` y `E`, pero no para `I`, aunque la validación contemplaba instituciones.
- Estado: Resuelto

### Tema 3: Análisis de ROW_NUMBER en prs_autoxafi.sql

- Solicitud o contexto: Federico pidió analizar el stored procedure `prs_autoxafi.sql`, especialmente el cálculo del campo `inicial_asoc`, porque el `ORDER BY` por fecha, sucursal y autorización no contemplaba hora/minuto/segundo y necesitaba obtener el valor solo en el primer registro agrupado por fecha, sucursal y autorización.
- Respuesta o acciones del agente: Se explicó que el `ROW_NUMBER()` era no determinístico porque particionaba y ordenaba por los mismos campos. Se propuso agregar un criterio de desempate estable dentro del grupo, por ejemplo una columna `IDENTITY` temporal `orden_inicial_asoc` en `#FINAL`, y usarla en el `ORDER BY` del `ROW_NUMBER()`.
- Resultado: Federico recibió una propuesta técnica para que solo el primer registro del grupo conserve `Inicial` o `Asoc.` y los siguientes queden vacíos, con orden estable.
- Estado: Resuelto

### Tema 4: Especificación técnica para Redmine 30074

- Solicitud o contexto: Federico pidió generar `ticket_30074_03_spec1.md` usando prompt SVN `03_spec_01.md`, ticket Redmine `30074`, base `Unosalud_prod_test`, rama trunk y el informe de análisis previo.
- Respuesta o acciones del agente: Se generó la especificación técnica a partir del informe de bug.
- Resultado: Se creó `/mnt/Agentes/facevedo/R30074/ticket_30074_03_spec1.md`, con dos cambios PowerBuilder definidos: `wr_busauto_filtro_fechas.srw` para ampliar el rango de búsqueda de autorizaciones y `wp_presta_insti.srw` para incluir tipo `I` en la opción de lugar propio. No se propusieron cambios en stored procedures ni base de datos. Validación final: 291 líneas, UTF-8 sin BOM, finales LF y secciones obligatorias presentes.
- Estado: Resuelto

### Tema 5: Validación pendiente del alcance de rango de búsqueda

- Solicitud o contexto: En el informe de bug del ticket 30074 quedó una definición funcional abierta.
- Respuesta o acciones del agente: Se dejó explícito que antes de desarrollar debía confirmarse el alcance del nuevo rango.
- Resultado: Quedó pendiente confirmar si el nuevo rango del caso #1 debía aplicar a todos los usos de `wr_busauto_filtro_fechas` o solo al flujo de expedientes de órdenes.
- Estado: Pendiente
