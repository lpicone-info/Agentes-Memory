# Memoria de conversación — Cristian Iván Ratto

## Datos

- **Fecha:** 04/09/2026
- **Usuario:** Cristian Iván Ratto
- **ID Google Chat:** `users/112625344596118312340`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 52
- **Inicio:** 09:37:35
- **Fin:** 17:18:11

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/vmclokaaaae`

## Archivos revisados

- `92a32e2e-b6f4-4a7e-b0a8-45683d53a0dc.jsonl`

---

## Transcripción completa

### [04/09/2026 09:37:35] Cristian Iván Ratto

stan

---

### [04/09/2026 09:39:09] Agente

Hola Cristian, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [04/09/2026 09:39:54] Cristian Iván Ratto

Stan retomamos 29794

---

### [04/09/2026 09:42:24] Agente

Cristian, contexto recuperado del RM29794.

Estado actual validado:
- Trabajo solo en `/mnt/Agentes/cratto/RM29794/ObjetosModificados`
- No toqué ramas SVN
- Objetos foco:
  - `wp_export_comprobantes_ec_csv.srw`
  - `d_export_comprobantes_csv.srd`
  - `d_export_estado_cuenta_csv.srd`
- Los DataObjects ya están con una sola columna `linea`
- Ambos ejecutan los SP hijos con `@prm_modo`
- La ventana llama los `Retrieve(..., 1)` fijo
- La exportación escribe directamente el campo `linea`
- Los PB están en UTF-16 LE con CRLF

Pendiente que quedó de ayer: validar import/regenerate en PowerBuilder y corregir el error exacto si vuelve a aparecer.

---

### [04/09/2026 09:53:59] Cristian Iván Ratto

Stan tengo un error en esta linea:
ll_estado_cuenta = lds_estado_cuenta.Retrieve(ii_prepaga, ii_emi_grupo, id_factu_perio, 1)

El SP relacionado funciona OK si lo ejecuto por fuera con los parametros.
EXECUTE afs_generar_estado_cuenta_csv @prm_prepaga = 1,@prm_emi_grupo = 1,@prm_factu_perio = '20260801', @prm_modo = 1

Sin embargo, con mismos prametros desde powerbuilder en tiempo de ejecucion falla.
En tiempo de diseño conectado a premedic_desarrollo No falla.

---

### [04/09/2026 09:56:54] Agente

Cristian, hice un ajuste puntual para ese caso.

Causa probable: en runtime PowerBuilder estaba pasando `id_factu_perio` como `DATETIME`, mientras tu prueba manual funcional usa el período como texto `20260801`. En diseño puede tolerarlo, pero en runtime con `SQLCA` real puede cambiar la conversión.

Modifiqué:
- `/mnt/Agentes/cratto/RM29794/ObjetosModificados/exe/Source Infomedical/wp_export_comprobantes_ec_csv.srw`
- `/mnt/Agentes/cratto/RM29794/ObjetosModificados/exe/Source Infomedical/d_export_estado_cuenta_csv.srd`

Cambio aplicado:
```powerscript
ls_factu_perio = String(Date(id_factu_perio), "yyyymmdd")
ll_estado_cuenta = lds_estado_cuenta.Retrieve(ii_prepaga, ii_emi_grupo, ls_factu_perio, 1)
```

Y el DataObject `d_export_estado_cuenta_csv` ahora define:
```text
("prm_factu_perio", string)
```

Validé:
- Archivos siguen en UTF-16 LE con CRLF.
- `@prm_modo` sigue fijo en `1`.
- No toqué ramas SVN.
- No pude compilar/regenerar desde PowerBuilder en este entorno.

---

### [04/09/2026 10:05:44] Cristian Iván Ratto

Sigue fallando, el Retrieve retonar -1, sin embargo sqlcode = 0 y sqldbcode=0.
Deberia traer 16 filas.

---

### [04/09/2026 10:07:37] Cristian Iván Ratto

Sigue fallando, el Retrieve retonar -1, sin embargo sqlcode = 0 y sqldbcode=0.
Deberia traer 16 filas.

---

### [04/09/2026 10:09:20] Agente

Cristian, ajusté el caso más probable para `Retrieve = -1` con `SQLCode = 0`: metadata del resultset no aceptada por el DataStore en runtime.

Cambios locales:
- `afs_generar_estado_cuenta_csv.sql`: la columna `linea` ahora sale tipada explícitamente:
```sql
SELECT linea = CONVERT(varchar(4000), CONCAT(
```
- `d_export_estado_cuenta_csv.srd`: la columna `linea` ahora está definida como:
```text
type=char(4000)
```

Validé:
- El SP actual en `Premedic_Desarrollo` devuelve 16 filas con tus parámetros.
- Los archivos conservaron encoding y finales:
  - SQL: UTF-8 con CRLF.
  - SRD: UTF-16 LE con CRLF.
- No toqué ramas SVN.

Importante: para probar este ajuste en PowerBuilder, primero debe aplicarse el SQL local actualizado del SP en la base donde corre la aplicación; si no, runtime seguirá usando la versión anterior del procedimiento.

---

### [04/09/2026 10:16:24] Cristian Iván Ratto

Stan sigue fallando. Debe ser otra cosa, deja el SP como estaba.
Intenta buscar otro problema.

---

### [04/09/2026 10:24:43] Agente

Cristian, dejé el SP restaurado: `afs_generar_estado_cuenta_csv.sql` volvió a:

```sql
SELECT linea = CONCAT(
```

Y cambié el enfoque en runtime: el botón Exportar ya no usa `lds_estado_cuenta.Retrieve(...)`. Ahora ejecuta `afs_generar_estado_cuenta_csv` con `DECLARE ... PROCEDURE FOR`, hace `FETCH` de `linea` y escribe directo el archivo `EC_...csv`.

Archivo modificado:
- `/mnt/Agentes/cratto/RM29794/ObjetosModificados/exe/Source Infomedical/wp_export_comprobantes_ec_csv.srw`

Validé:
- No queda `lds_estado_cuenta.Retrieve`.
- `@prm_modo` sigue fijo en `1`.
- SP quedó como estaba.
- `.srw` sigue UTF-16 LE con CRLF.
- No toqué ramas SVN.

---

### [04/09/2026 10:36:21] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-09-04.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-04.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-04-HHMM.md); always use the canonical 2026-09-04.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, September 4th, 2026 - 10:36 AM (America/Buenos_Aires)
Reference UTC: 2026-09-04 13:36 UTC

---

### [04/09/2026 10:37:05] Agente

NO_REPLY

---

### [04/09/2026 10:37:06] Cristian Iván Ratto

Stan vuelve al cambio anterior

---

### [04/09/2026 10:38:16] Agente

Cristian, volví al cambio anterior.

Quedó restaurado:
- `wp_export_comprobantes_ec_csv.srw` vuelve a usar:
```powerscript
lds_estado_cuenta.DataObject = "d_export_estado_cuenta_csv"
lds_estado_cuenta.SetTransObject(SQLCA)
ll_estado_cuenta = lds_estado_cuenta.Retrieve(ii_prepaga, ii_emi_grupo, ls_factu_perio, 1)
```

También quedó:
- Quitada la función alternativa con `DECLARE PROCEDURE/FETCH`.
- `afs_generar_estado_cuenta_csv.sql` restaurado con `SELECT linea = CONCAT(`.
- Archivos conservan formato:
  - `.srw`: UTF-16 LE con CRLF.
  - `.sql`: UTF-8 con CRLF.
- No toqué ramas SVN.

---

### [04/09/2026 10:44:59] Cristian Iván Ratto

Stan te doy una pista del error.
SQLSTATE = 42000
Microsoft SQL Native Client
UPDATE failed because the following SET options have incorrect settings: 'ANSI_WARNINGS'. Verify that SET options are correct for use with indexed views and/or indexes on computed columns and/or filtered indexes and/or query notifications and/or XML data type methods and/or spatial index operations.

---

### [04/09/2026 10:46:05] Agente

Cristian, corregido en el SP local.

Agregué dentro del procedimiento:

```sql
SET ANSI_WARNINGS ON
```

Archivo:
`/mnt/Agentes/cratto/RM29794/ObjetosModificados/afs_generar_estado_cuenta_csv.sql`

Diagnóstico: el `SET ANSI_WARNINGS ON` que estaba antes del `CREATE PROCEDURE` no alcanza para runtime si la conexión de PowerBuilder llega con esa opción apagada. El SP necesitaba forzarlo dentro del `BEGIN`.

Validé:
- Quedó dentro del SP.
- No agregué `;` nuevo.
- Archivo sigue UTF-8 con CRLF.
- No toqué ramas SVN.

---

### [04/09/2026 11:05:18] Cristian Iván Ratto

Stan el cambio funciona OK.
Quiero saber si el cambio en el sp afs_generar_estado_cuenta_csv SET ANSI_WARNINGS ON, puede afectar al SP madre afs_generar_compros_y_ecs_csv del proceso batch.

---

### [04/09/2026 11:07:00] Agente

Cristian, el impacto sobre `afs_generar_compros_y_ecs_csv` debería ser positivo o neutro.

Evidencia:
- El SP madre no ejecuta directamente el hijo en la misma conexión.
- Arma un comando externo con `sqlcmd` vía `xp_cmdshell`.
- Para EC arma:
```sql
EXECUTE dbo.afs_generar_estado_cuenta_csv ...
```
- No le pasa `@prm_modo`, por lo tanto el hijo sigue usando el default `@prm_modo = 0`, que es la salida tabular original esperada por el batch.

Conclusión:
- `SET ANSI_WARNINGS ON` dentro de `afs_generar_estado_cuenta_csv` solo fuerza la opción durante la ejecución del SP hijo.
- No cambia la firma del SP.
- No cambia la salida del modo batch.
- No debería romper al SP madre.
- Si el batch venía ejecutando con `ANSI_WARNINGS OFF`, este cambio lo protege del mismo error que viste desde PowerBuilder.

Riesgo bajo: solo podría exponer errores reales que antes quedaban tolerados con `ANSI_WARNINGS OFF`, pero para este caso SQL Server ya está exigiendo `ON` por índices/vistas/columnas computadas/filtradas.

---

### [04/09/2026 11:09:10] Cristian Iván Ratto

Stan quiero saber si el orden de columnas de salida que se muestran mediante proceso batch, son iguales al orden de salida de los dos SP hijos con modo = 1.

---

### [04/09/2026 11:09:49] Agente

Cristian, sí: el orden de columnas es igual.

Verifiqué ambos SP hijos:

- `afs_generar_comprobantes_csv`
  - Batch usa `@prm_modo = 0` por default.
  - `@prm_modo = 1` concatena en el mismo orden del `SELECT` tabular del modo `0`.

- `afs_generar_estado_cuenta_csv`
  - Batch también usa `@prm_modo = 0` por default.
  - `@prm_modo = 1` concatena en el mismo orden del `SELECT` tabular del modo `0`.

Aclaración técnica: el SP madre `afs_generar_compros_y_ecs_csv` no pasa `@prm_modo`, por eso el batch sigue mostrando la salida tabular original. El modo `1` mantiene el orden lógico de esas columnas, pero la forma de salida cambia: en vez de columnas separadas por `sqlcmd`, devuelve una sola columna `linea` ya concatenada.

---

### [04/09/2026 11:11:13] Cristian Iván Ratto

Por ultimo Stan, quiero saber si las salidas de los SP hijos en modo = 1 es lo pedido en el redime

---

### [04/09/2026 11:14:40] Agente

Cristian, sí: las salidas de los dos SP hijos con `@prm_modo = 1` están alineadas con lo pedido para el Redmine 29794.

`afs_generar_comprobantes_csv` en modo 1 devuelve una sola columna `linea`, armada con los 34 campos del archivo de comprobantes en el mismo orden pedido en el informe: Descripción, letra, sucursal, número, fechas, contrato, importes, CAE, datos fiscales, ítems, percepciones, leyenda y transparencia fiscal. Separador: `;`. Eso corresponde al TXT pedido en [ticket_29794_02_nuevo.md](</mnt/Agentes/cratto/RM29794/Informes/ticket_29794_02_nuevo.md:152>).

`afs_generar_estado_cuenta_csv` en modo 1 también devuelve una sola columna `linea`, con los 35 campos del estado de cuenta en el mismo orden pedido: contrato, período, comprobante, fecha, titular, plan, composición, saldos, obra social, nro item, aportante, empresa, período aporte, concepto y valores. Separador: `;`. Eso corresponde al CSV pedido en [ticket_29794_02_nuevo.md](</mnt/Agentes/cratto/RM29794/Informes/ticket_29794_02_nuevo.md:212>).

La diferencia técnica es esta: el batch original usa los SP hijos en modo tabular y deja que `sqlcmd` aplique el separador con `-s`. Para PowerBuilder, el modo 1 ya trae la línea concatenada lista para escribir al archivo. Funcionalmente la salida es equivalente para el objetivo del Redmine: TXT de comprobantes y CSV de estado de cuenta, sin selección manual y respetando el orden de columnas pedido.

---

### [04/09/2026 11:22:06] Cristian Iván Ratto

Stan quiero saber porque estos SP disparados con mismos criterios devuelven diferente cantidad de filas.

EXECUTE dbo.afs_compro_impresion
@tipo_impre = 'R',
@prepaga = 1,
@plan_codi = null,
@emi_grupo = 1,
@factu_perio = '20260801',
@signo = null,
@cant_compro = null,
@compro_tipo = null,
@compro_dde = null,
@compro_hta = null,
@contra_dde = null,
@contra_hta = null,
@cpostal_dde = null,
@cpostal_hta = null,
@condicion = null,
@valor = null

EXECUTE afs_generar_comprobantes_csv
@prm_prepaga = 1,
@prm_emi_grupo = 1,
@prm_factu_perio = '20260801',
@prm_modo = 1

---

### [04/09/2026 11:30:06] Agente

Cristian, la diferencia viene por dos causas concretas.

Primero, no tienen la misma granularidad:

`afs_compro_impresion` devuelve una fila por comprobante. En tu caso dio `763` filas, todas `CI`/`DI`.

`afs_generar_comprobantes_csv` devuelve una fila por ítem de comprobante. En tu caso dio `1022` filas, porque repite los datos de cabecera por cada ítem, que es lo pedido para el TXT.

Segundo, hay 13 comprobantes que están en `afs_compro_impresion` pero no entran al CSV porque en `COMPROBANTES.contra` tienen `NULL`. El SP CSV los filtra explícitamente con:

`AND com.contra IS NOT NULL`

en [afs_generar_comprobantes_csv.sql](</mnt/Agentes/cratto/RM29794/ObjetosModificados/afs_generar_comprobantes_csv.sql:199>).

Los 13 faltantes son comprobantes `CI/DI` con contrato nulo. Verifiqué que no son por falta de ítems: tienen registros en `COMPRO_IMPRESION` y `COMPRO_ITEMS`, pero `contra = NULL`.

Conclusión: la diferencia esperable por ítems explica que el CSV tenga más filas que la grilla. Pero además hay una diferencia real de universo: `afs_compro_impresion` lista comprobantes con contrato nulo, mientras `afs_generar_comprobantes_csv` los excluye. Esa es la causa raíz de que no coincidan como conjunto de comprobantes.

---

### [04/09/2026 11:47:23] Cristian Iván Ratto

Stan retomemos Tarea #26993

---

### [04/09/2026 11:48:53] Agente

Cristian, retomado RM26993.

Contexto vigente:
- Caso: impresión de comprobantes y aplicación de saldos.
- Documento base: `/mnt/Agentes/cratto/RM26993/R26993_V2.docx`.
- Restricción activa: no tocar ramas SVN.
- Último estado importante: el cambio propuesto sobre `afn_get_medimas_codibarra` para Montegrande quedó desestimado; instrucción vigente: no tocar barras.
- Carpetas trabajadas: `ObjetosRoisa`, `ObjetosActivaSalud`, `ObjetosSBSalud`, `ObjetosSMC`, `ObjetosMontegrande`, `ObjetosMutual`, `ObjetosPremedic`, `ObjetosIgualdad`, `ObjetosOspicine(resto)`.

Quedo parado en RM26993. Decime con qué frente seguimos: cliente/objeto, bug puntual o validación que querés revisar.

---

### [04/09/2026 12:29:16] Cristian Iván Ratto

Stan tiene este perfil de conexion Ospana_Prod_test ?

---

### [04/09/2026 12:29:36] Agente

Cristian, no. En `DB_PROFILE.md` no figura ningún profile llamado `Ospana_Prod_test` ni variantes `Ospana/Ospaña`.

Profiles parecidos cargados: `Ospicine_Prod`, `Ospecon_Desarrollo`, `Ospecon_Prox_Ver`, `ospecon_prod`.

---

### [04/09/2026 12:31:05] Cristian Iván Ratto

Stan crea un perfil en DB_PROFILE.md.

Profile: OSPANA_Prod_Test
Motor: MS SQL
Host/IP: 172.16.10.115
Base de datos: OSPANA_Prod_Test
Usuario: sa
Password: IMed.2021
Puerto: 1433
Notas: N/A

---

### [04/09/2026 12:32:57] Agente

Profile registrado correctamente.

Profile: `OSPANA_Prod_Test`  
Motor: `MS SQL`  
Base de datos: `OSPANA_Prod_Test`  
Estado de conexión: `OK`  
Archivo: [DB_PROFILE.md](/home/luispicone/.openclaw/workspace/DB_PROFILE.md)

Validé conexión TCP, autenticación contra la base y `SELECT DB_NAME()`. También quedó una sola entrada del profile y se preservó UTF-8 sin BOM con finales LF.

---

### [04/09/2026 12:36:14] Cristian Iván Ratto

Stan Aplica el criterio Roisa a este nuevo cliente Ospaña
/mnt/Agentes/cratto/RM26993/ObjetosOspania/d_factura_ospania.srd

Luego verifica si estos errores documentados se solucionan:
/mnt/Agentes/cratto/RM26993/Bugs/R26993-Errores OSPANA.docx

Fijate si puedes verificar si los errores se han solucionado.
La base es: OSPANA_Prod_Test

recuerda no tocar ramas SVN.

---

### [04/09/2026 12:43:31] Agente

Cristian, aplicado sobre:

[d_factura_ospania.srd](</mnt/Agentes/cratto/RM26993/ObjetosOspania/d_factura_ospania.srd>)

Cambios hechos:
- Agregué al DataWindow las columnas `total`, `grava` y `exen`, que el SP `dbo.afs_compro_impresion_caseros` ya devuelve en `OSPANA_Prod_Test`.
- `SUBTOTAL` ahora toma `grava + exen`.
- `TOTAL FACTURA` ahora toma `total`.
- Vencimientos ahora calculan `total + deudaN - creditos`, con piso en `0` si el saldo a favor cubre todo.
- La barra de pago se oculta cuando el neto del primer vencimiento queda `<= 0`.

Verificación contra `OSPANA_Prod_Test`:
- Exento con saldo a favor mayor/igual: vencimiento queda `0` y barra oculta.
- Exento con saldo a favor menor: vencimiento queda por el neto.
- Exento en contra: suma deuda al vencimiento.
- Gravado en contra: suma deuda al vencimiento.
- Gravado con saldo a favor mayor/igual: vencimiento queda `0` y barra oculta.
- Gravado con saldo a favor menor: vencimiento queda por el neto y `SUBTOTAL/TOTAL FACTURA` dejan de depender de `impor1`.

Validaciones técnicas:
- No toqué ramas SVN; `svn status` no informó cambios.
- Archivo preservado en UTF-16 LE con BOM y CRLF.
- No pude compilar ni previsualizar en PowerBuilder desde este entorno.

---

### [04/09/2026 13:50:42] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-09-04.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-04.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-04-HHMM.md); always use the canonical 2026-09-04.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, September 4th, 2026 - 1:50 PM (America/Buenos_Aires)
Reference UTC: 2026-09-04 16:50 UTC

---

### [04/09/2026 13:51:33] Cristian Iván Ratto

Stan pasame las querys para probar por base de datos los casos reportados en el documento.
Seria una query contra comprobantes.
Otra query contra compro_impresion

---

### [04/09/2026 13:52:19] Agente

Cristian, usá estas dos queries en `OSPANA_Prod_Test`.

**1. Contra `COMPROBANTES`**
```sql
SELECT DISTINCT
       caso_reportado =
       CASE
          WHEN ISNULL(c.exen,0) > 0 AND ISNULL(c.grava,0) = 0 AND ISNULL(ci.creditos,0) >= ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '1 Exento a favor MAYOR'
          WHEN ISNULL(c.exen,0) > 0 AND ISNULL(c.grava,0) = 0 AND ISNULL(ci.creditos,0) > 0 AND ISNULL(ci.creditos,0) < ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '2 Exento a favor MENOR'
          WHEN ISNULL(c.exen,0) > 0 AND ISNULL(c.grava,0) = 0 AND ISNULL(ci.deuda1,0) > 0 AND ISNULL(ci.creditos,0) = 0
             THEN '3 Exento en contra'
          WHEN ISNULL(c.grava,0) > 0 AND ISNULL(ci.deuda1,0) > 0 AND ISNULL(ci.creditos,0) = 0
             THEN '4 Gravado saldo en CONTRA'
          WHEN ISNULL(c.grava,0) > 0 AND ISNULL(ci.creditos,0) >= ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '5 Gravado saldo a favor MAYOR'
          WHEN ISNULL(c.grava,0) > 0 AND ISNULL(ci.creditos,0) > 0 AND ISNULL(ci.creditos,0) < ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '6 Gravado saldo a favor MENOR'
       END,
       c.ente,
       c.compro_tipo,
       c.compro_nro,
       c.fecha,
       c.total,
       c.grava,
       c.exen,
       ci.impor1,
       ci.deuda1,
       ci.creditos,
       neto_vto1 = ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0),
       vto1_esperado = CASE WHEN ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0) <= 0 THEN 0 ELSE ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0) END,
       barra_visible_esperada = CASE WHEN ci.barra IS NULL OR ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0) <= 0 THEN 'NO' ELSE 'SI' END
FROM COMPROBANTES c
INNER JOIN COMPRO_IMPRESION ci
        ON ci.ente = c.ente
       AND ci.tipo = c.compro_tipo
       AND ci.nro = c.compro_nro
WHERE c.baja_fecha IS NULL
  AND ci.impre_fecha IS NOT NULL
  AND c.compro_tipo IN ('FC','ND')
  AND (
       ISNULL(ci.creditos,0) > 0
       OR ISNULL(ci.deuda1,0) > 0
       OR ISNULL(c.grava,0) > 0
  )
ORDER BY caso_reportado, c.compro_tipo, c.compro_nro
```

**2. Contra `COMPRO_IMPRESION`**
```sql
SELECT DISTINCT
       caso_reportado =
       CASE
          WHEN ISNULL(c.exen,0) > 0 AND ISNULL(c.grava,0) = 0 AND ISNULL(ci.creditos,0) >= ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '1 Exento a favor MAYOR'
          WHEN ISNULL(c.exen,0) > 0 AND ISNULL(c.grava,0) = 0 AND ISNULL(ci.creditos,0) > 0 AND ISNULL(ci.creditos,0) < ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '2 Exento a favor MENOR'
          WHEN ISNULL(c.exen,0) > 0 AND ISNULL(c.grava,0) = 0 AND ISNULL(ci.deuda1,0) > 0 AND ISNULL(ci.creditos,0) = 0
             THEN '3 Exento en contra'
          WHEN ISNULL(c.grava,0) > 0 AND ISNULL(ci.deuda1,0) > 0 AND ISNULL(ci.creditos,0) = 0
             THEN '4 Gravado saldo en CONTRA'
          WHEN ISNULL(c.grava,0) > 0 AND ISNULL(ci.creditos,0) >= ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '5 Gravado saldo a favor MAYOR'
          WHEN ISNULL(c.grava,0) > 0 AND ISNULL(ci.creditos,0) > 0 AND ISNULL(ci.creditos,0) < ISNULL(c.total,0) + ISNULL(ci.deuda1,0)
             THEN '6 Gravado saldo a favor MENOR'
       END,
       ci.ente,
       ci.tipo,
       ci.nro,
       ci.contra,
       ci.fecha,
       ci.vto1,
       ci.impor1,
       ci.deuda1,
       ci.creditos,
       ci.barra,
       c.total,
       c.grava,
       c.exen,
       subtotal_esperado = ISNULL(c.grava,0) + ISNULL(c.exen,0),
       total_factura_esperado = ISNULL(c.total,0),
       neto_vto1 = ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0),
       vto1_esperado = CASE WHEN ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0) <= 0 THEN 0 ELSE ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0) END,
       barra_visible_esperada = CASE WHEN ci.barra IS NULL OR ISNULL(c.total,0) + ISNULL(ci.deuda1,0) - ISNULL(ci.creditos,0) <= 0 THEN 'NO' ELSE 'SI' END
FROM COMPRO_IMPRESION ci
INNER JOIN COMPROBANTES c
        ON c.ente = ci.ente
       AND c.compro_tipo = ci.tipo
       AND c.compro_nro = ci.nro
WHERE c.baja_fecha IS NULL
  AND ci.impre_fecha IS NOT NULL
  AND ci.tipo IN ('FC','ND')
  AND (
       ISNULL(ci.creditos,0) > 0
       OR ISNULL(ci.deuda1,0) > 0
       OR ISNULL(c.grava,0) > 0
  )
ORDER BY caso_reportado, ci.tipo, ci.nro
```

La lectura clave es `vto1_esperado` y `barra_visible_esperada`: esos son los valores contra los que deberías comparar la impresión corregida.

---

### [04/09/2026 15:12:00] Cristian Iván Ratto

Stan
Caso medimas --> faltó agregar formula de de esconder codigo de barras
Te paso el objeto: /mnt/Agentes/cratto/RM26993/ObjetosMedimas/d_factura_solo_medimas.srd
Solo eso.

---

### [04/09/2026 15:13:37] Agente

Cristian, listo. Apliqué solo el ajuste pedido en:

[d_factura_solo_medimas.srd](</mnt/Agentes/cratto/RM26993/ObjetosMedimas/d_factura_solo_medimas.srd:269>)

La barra `barra_posta` ahora queda oculta cuando `saldo1 <= 0`, cuando es `NC`, o cuando `barra` viene nula. No toqué SVN. Validé que el archivo siga en UTF-16 LE con BOM y CRLF.

---

### [04/09/2026 15:43:10] Cristian Iván Ratto

Stan caso mutual
Como estaba antes y como esta ahora ?

---

### [04/09/2026 15:45:23] Agente

Cristian, en **Mutual** quedó así:

**Antes**
- `TOTAL FACTURA A/B`: usaba `abs(impor1)`.
- `SUBTOTAL A`: usaba `abs(impor1 - iibb_arba - iva_total)`.
- `SUBTOTAL B`: usaba `abs(impor1)`.
- Vencimientos:
  - A vto1: `abs(impor1 + deuda1)`
  - A vto2: `abs(impor2 + deuda2)`
  - B vto1: `abs(impor1)`
  - B vto2: `abs(impor2)`
  - vto3: `abs(impor3)`
- Código de barras: se mostraba si existía `barra` y empezaba con `0448`.
- `saldo_anterior2`: mostraba solo `deuda2`.

**Ahora**
- El SP `afs_compro_impresion_mutual.sql` trae desde `COMPROBANTES`: `total`, `grava`, `exen`.
- `impor_letras` / `impor_letra` ahora salen de `c.total`.
- `TOTAL FACTURA A/B`: usa `abs(total)`.
- `SUBTOTAL A`: usa `abs(grava + exen)`.
- `SUBTOTAL B`: quedó igual, `abs(impor1)`.
- Vencimientos A/B: ahora usan `abs(total - creditos)`.
- Código de barras: además de validar `barra` y `0448`, se oculta si `abs(total - creditos) = 0`.
- `saldo_anterior2`: pasó de `deuda2` a `deuda2 - creditos`.

Conclusión: Mutual quedó con criterio tipo **SBSalud**: fiscal separado por `total/grava/exen`, y vencimiento/barra mirando saldo final `total - creditos`. Ojo: no incluye `deudaN` en los vencimientos actuales; ese criterio es distinto al de Ospaña/Roisa que vimos después.

---

### [04/09/2026 16:05:46] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-09-04.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-04.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-04-HHMM.md); always use the canonical 2026-09-04.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, September 4th, 2026 - 4:05 PM (America/Buenos_Aires)
Reference UTC: 2026-09-04 19:05 UTC

---

### [04/09/2026 16:06:30] Agente

NO_REPLY

---

### [04/09/2026 16:06:31] Cristian Iván Ratto

Stan el caso Premedic
Que imprime en el código de barras? → Solo para saber, no es error

---

### [04/09/2026 16:08:36] Agente

Cristian, en **Premedic** el DataWindow imprime la barra desde campos que devuelve el SP `afs_compro_impresion_caseros.sql`: `a.barra` y `a.barra_afip`.

Para la **barra de pago**:

- Si `barra` empieza con `0448`, imprime con `gf_code_128(barra)` en el objeto `barra_128`.
- Además muestra el número legible debajo con el valor crudo de `barra` en `barra_numeros`.
- Si `barra` no empieza con `0448`, usa `gf_interleaved_2de5(barra)` en el objeto `barra`.

Condición actual importante: la barra se oculta si `abs(total - creditos) = 0`.

También hay una **barra AFIP** separada:
- Usa `barra_afip`
- Imprime `gf_interleaved_2de5(barra_afip)`
- Solo visible cuando `legal_inter = 'L'`
- No imprime si `tipo = 'RS'`

No ejecuté PB ni base; esto sale de leer el SRD y el SP actual de `ObjetosPremedic`.

---

### [04/09/2026 16:36:13] Cristian Iván Ratto

Stan
Agregamos un nuevo cliente: Femesa
Te paso los objetos:
/mnt/Agentes/cratto/RM26993/ObjetosFemesa/d_factura_solo_medimas.srd
/mnt/Agentes/cratto/RM26993/ObjetosFemesa/afs_compro_impresion_femesa.sql
Necesito que unicamente agreguemos al SP, grava y exen como salidas.
Luego en el objeto d_factura_solo_medimas usemos esos campos nuevos y la formula de subtotal sea: abs(if(isnull(grava),0,grava) + if(isnull(exen),0,exen))

---

### [04/09/2026 16:37:01] Agente

Cristian, el SP existe, pero el SRD con la ruta exacta que pasaste no existe:

`/mnt/Agentes/cratto/RM26993/ObjetosFemesa/d_factura_solo_medimas.srd`

En `ObjetosFemesa` está este objeto:

`/mnt/Agentes/cratto/RM26993/ObjetosFemesa/d_factura_femesa.srd`

Necesito que me confirmes si aplico el cambio sobre `d_factura_femesa.srd`. No modifiqué nada todavía.

---

### [04/09/2026 16:38:04] Cristian Iván Ratto

Stan
Agregamos un nuevo cliente: Femesa
Te paso los objetos:
/mnt/Agentes/cratto/RM26993/ObjetosFemesa/d_factura_femesa.srd
/mnt/Agentes/cratto/RM26993/ObjetosFemesa/afs_compro_impresion_femesa.sql
Necesito que unicamente agreguemos al SP, grava y exen como salidas.
Luego en el objeto d_factura_solo_medimas usemos esos campos nuevos y la formula de subtotal sea: abs(if(isnull(grava),0,grava) + if(isnull(exen),0,exen))

---

### [04/09/2026 16:48:42] Agente

Cristian, quedó aplicado en Femesa.

Impacté:
- `/mnt/Agentes/cratto/RM26993/ObjetosFemesa/afs_compro_impresion_femesa.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosFemesa/d_factura_femesa.srd`

Cambios:
- SP: agregué `grava = ISNULL(c.grava, 0)` y `exen = ISNULL(c.exen, 0)` como salidas en los dos `SELECT` principales.
- SP: en la rama `RS` agregué `LEFT JOIN comprobantes c` porque esa rama no tenía alias `c` disponible.
- SRD: agregué columnas `grava` y `exen`.
- SRD: `subtotal_a` quedó con:
  `abs(if(isnull(grava),0,grava) + if(isnull(exen),0,exen))`

Validé encoding/EOL:
- SQL sigue `ISO-8859` con `CRLF`.
- SRD sigue `UTF-16LE BOM` con `CRLF`.
- No toqué ramas SVN. Solo leí SVN para recuperar la copia base del SQL Femesa cuando la edición in-place sobre CIFS falló.

---

### [04/09/2026 17:13:09] Cristian Iván Ratto

Stan consulta.
el objeto d_factura como estaba antes y como esta ahora ?

---

### [04/09/2026 17:14:24] Agente

Cristian, en `d_factura_femesa.srd` estaba así:

**Antes**
- No tenía columnas `grava` ni `exen`.
- Después de `qr` venía directo el `procedure`.
- `subtotal_a` calculaba:
  `abs(impor1 - iibb - iva_total)`
- `subtotal_b` ya estaba y quedó:
  `abs(impor1)`

**Ahora**
- Se agregaron las columnas:
  `grava`
  `exen`
- Quedaron antes del `procedure`.
- `subtotal_a` ahora calcula:
  `abs(if(isnull(grava),0,grava) + if(isnull(exen),0,exen))`
- `subtotal_b` no se tocó.

En resumen: el único cambio funcional del objeto fue que el subtotal para comprobantes `A` o `B` deja de depender de `impor1 - iibb - iva_total` y pasa a tomar la base fiscal desde `COMPROBANTES.grava + COMPROBANTES.exen`.

---

### [04/09/2026 17:16:15] Cristian Iván Ratto

Stan consulta.
el objeto "d_factura" - que es generico- como estaba antes y como esta ahora ?
Ese el objeto usado cuando en el choose case de reglas de contra forma se escapa por un case else.

 CASE ELSE //OTROS
 ls_datastore = 'd_factura'
 ls_font = 'z3of9.ttf'

---

### [04/09/2026 17:18:11] Agente

Cristian, el `d_factura` genérico cambió bastante respecto del backup original.

Comparé:

- Actual: `/mnt/Agentes/cratto/RM26993/ObjetosOspicine(resto)/d_factura.srd`
- Original: `/mnt/Agentes/cratto/RM26993/Objetos_OriginalesDeImpresion (BKP)/d_factura.srd`

Antes el objeto genérico no tenía las columnas `total`, `grava` ni `exen`. Calculaba así:

```powerbuilder
subtotal_a = abs(impor1 - iibb_arba - iva_total)
total_factura_a = abs(impor1)
total_factura_b = abs(impor1)

vto1 = abs(impor1 + deuda1)
vto2 = abs(impor2 + deuda2)
vto3 = abs(impor3 + deuda3)
```

Ahora el objeto genérico tiene agregadas las columnas `total`, `grava` y `exen`, y quedó así:

```powerbuilder
subtotal_a = abs(if(isnull(grava),0,grava) + if(isnull(exen),0,exen))
subtotal_b = abs(impor1)

total_factura_a = abs(if(isnull(total),0,total))
total_factura_b = abs(if(isnull(total),0,total))

vto1 = abs(total + deuda1 - creditos)
vto2 = abs(total + deuda2 - creditos)
vto3 = abs(total + deuda3 - creditos)
```

Además, ahora el código de barras de pago se oculta cuando:

```powerbuilder
abs(total + deuda1 - creditos) = 0
```

Antes solo se mostraba/ocultaba según existiera `barra` y según empezara o no con `0448`.

Lo que no cambió: `saldo_anterior1`, `saldo_anterior2` y la barra AFIP (`barra_afip`) siguen con la misma lógica.

Conclusión: el `d_factura` genérico ya no se comporta igual que antes. Si cae por el `CASE ELSE`, ahora usa lógica tipo “Roisa/Medimas/Ospaña/Femesa” para subtotal, total, vencimientos y ocultamiento de barra. Eso puede impactar a cualquier cliente que todavía caiga por `CASE ELSE // OTROS`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
