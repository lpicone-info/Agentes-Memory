# Memoria 2026-08-05

- Usuario en Google Chat directo: Hugo.
- Stan ya cargo preferencias y se presento disponible.
- Trabajo activo R29398: analisis de performance de `wp_export_debitos` del trunk usando base `SMC_PROD_TEST`, foco en el caso con checkbox `aplica` tildado.
- Informe inicial entregado en `/mnt/Agentes/harboleas/R29398/informe_wp_export_debitos_aplica_performance.md`.
- Hallazgo principal del informe inicial: patron fila-a-fila en la ventana; recalcula varias veces `afs_compro_exportacion`, graba `compro_exportacion` con una llamada por comprobante y, con `aplica` tildado, `afu_compro_expor_aplica` procesa por cursor cada comprobante del lote.
- Pedido posterior de Hugo: empezar por `afu_compro_export_aplica` y dejar en la carpeta los cambios propuestos.
- Propuesta SQL entregada en `/mnt/Agentes/harboleas/R29398/archivos_propuestos/backend/procedures/afu_compro_expor_aplica.sql`.
- Resumen de cambios propuestos entregado en `/mnt/Agentes/harboleas/R29398/archivos_propuestos/backend/procedures/afu_compro_expor_aplica_cambios_propuestos.md`.
- Criterio de la propuesta: conservadora, mantiene `afu_aplicar_compro`, precarga lote en temporal indexada, precarga formas de pago del ente recaudador, evita lecturas repetidas contra `comprobantes` y resetea variables sensibles por iteracion.
- Validaciones informadas: no se toco trunk ni se ejecutaron cambios en base; se valido parseo SQL con `PARSEONLY`, se verifico ausencia de `RTRIM` y `;`, y el `.sql` quedo ASCII con CRLF como scripts propuestos.

## 2026-08-05 16:02 ART - Contexto tecnico durable

- Usuario: Walter. Trabaja sobre rama productiva SVN `wguise-Infomedical-trunk` en `/home/luispicone/.openclaw/workspace/svn/wguise-Infomedical-trunk`.
- Tema performance: se analizo `backend/procedures/afu_compro_expor_aplica.sql`. Hallazgos principales: cursor RBAR desde linea aprox. 70; por comprobante llama `afu_obtener_nro_compro`, `afs_dame_interes_compro`, inserta en `comprobantes`, `compro_pagos`, llama `afu_aplicar_compro`, inserta `compro_exportacion` y actualiza origen. Recomendaciones: unificar lecturas de `param_afi`; evitar `EXISTS` + `SELECT` por fila a `ente_rec_pago_formas`; ampliar cursor para traer datos de `comprobantes` y reemplazar `INSERT ... SELECT FROM comprobantes` por `VALUES`; revisar necesidad funcional del `ORDER BY O.fecha DESC`. Indices observados: `compro_exportacion` PK clustered `(lote, ente, compro_tipo, compro_nro)`, `comprobantes`/`compro_items` cubiertas por PK, `compro_aplicaciones` con indice por `(ente2, compro_tipo2, compro_nro2)`.
- Tema PowerBuilder `TriggerEvent('ue_aceptar')`: en `wp_export_debitos.srw` no se encontro ese trigger actualmente. Flujo: ejecuta `afu_compro_expor_aplica`, `COMMIT`, cierra archivos, limpia `dw_list` y `dw_list_det`, muestra "Exportacion terminada". Mover/refrescar al final puede mejorar consistencia visual, pero no performance del SP. Si se agrega refresh post exportacion, preferir `PostEvent('ue_aceptar')` al final exitoso despues de los `Reset()`, siempre que `ue_aceptar` solo consulte/refresque y no participe de la transaccion.
- Tema `d_factura` total factura: `exe/Source Infomedical/d_factura.srd` calcula etiqueta total factura como `abs(impor1 + deuda1)` (linea aprox. 210), consumiendo `compro_impresion` via `afs_compro_impresion_caseros`. `backend/procedures/afi_compro_impresion.sql` arma `impor1` desde `IMP.importe`; `importe = CASE WHEN @var_contra_forma IN (...) AND C.saldo > 0 THEN C.saldo ELSE C.total END` (linea aprox. 517) y luego graba `impor1 = IMP.importe` (linea aprox. 2518). Conclusion: para ciertos formatos/circuitos `impor1` representa importe a cobrar/saldo pendiente, no total original. No conviene tocar `impor1` globalmente porque alimenta total impreso, codigo de barras, importe en letras y otros procesos. Si Walter necesita mostrar `comprobantes.total`, agregar dato separado al SP de seleccion/impresion y cambiar solo esa etiqueta.
