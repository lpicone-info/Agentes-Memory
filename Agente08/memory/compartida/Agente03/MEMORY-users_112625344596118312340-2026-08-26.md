# Memoria de conversación — Cristian Iván Ratto

## Datos

- **Fecha:** 26/08/2026
- **Usuario:** Cristian Iván Ratto
- **ID Google Chat:** `users/112625344596118312340`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 37
- **Inicio:** 08:52:42
- **Fin:** 18:29:34

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/vmclokaaaae`

## Archivos revisados

- `d51b237d-4421-479a-bcb2-a527828c6e05.jsonl`

---

## Transcripción completa

### [26/08/2026 08:52:42] Cristian Iván Ratto

stan

---

### [26/08/2026 08:53:45] Agente

Hola Cristian, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [26/08/2026 15:36:03] Cristian Iván Ratto

Stan retomemos el caso roisa

---

### [26/08/2026 15:37:01] Agente

Cristian, contexto ROISA retomado.

Caso: `RM 26993`.

Estado que quedó registrado:

- Profile BD: `ROISA_Prod_Test`, validado OK.
- Rama usada previamente: `wguise-Infomedical-trunk`.
- Objeto trabajado/copiadado: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`.
- Para ROISA se definió que sí corresponde tocar SP para traer `grava` desde `comprobantes`.
- En el DW se agregaron `total` y `grava`.
- `total_factura_a` y `total_factura_b` quedaron tomando `total`.
- `subtotal_a` quedó tomando `grava`.
- Los vencimientos no se tocaron: siguen con `impor1 + deuda1` / `impor2 + deuda2`.
- No hubo `svn commit`.

También tengo presente tu preferencia vigente: no tocar ramas SVN.

Decime si retomamos por análisis, comparación antes/después, validación contra base, o armado de respuesta para Redmine.

---

### [26/08/2026 15:40:34] Cristian Iván Ratto

Recuerda los objetos de trabajo para roisa:
/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd
/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql
(no modifiques ramas svn)

Necesito que el los importes vencimientos sean total + (deuda - creditos)
El campo total que siga tomandolo del SP.
El campo subtotal que lo tome de grava que llega del SP.
Recuerda la formula para esconder la barra en caso del que el vencimiento sea 0.
Quita el campo Grava del SP, no es necesario.

---

### [26/08/2026 15:45:58] Cristian Iván Ratto

Recuerda los objetos de trabajo para roisa:
/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd
/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql
(no modifiques ramas svn)

Necesito que el SP contemple:
Quitar el campo saldo del SP, ya no necesitamos traerlo en el select.
El campo total que siga tomandolo del SP de la tabla comprobante.
El campo grava que siga tomandolo del SP de la tabla comprobante.
Debe traer el campo exen tambien de la tabla comprobante
En total son 3 campos que se traen, total, grava y exen.

En la DW para el componente roisa debemos hacer.
Necesito que el los importes vencimientos sean total + (deuda - creditos)
El campo subtotal debe ser grava + exen que llega del SP.
Recuerda la formula para esconder la barra en caso del que el vencimiento sea 0.

---

### [26/08/2026 15:48:48] Agente

Cristian, quedó aplicado sobre los dos objetos indicados, sin tocar ramas SVN.

Archivos impactados:
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`

Cambios realizados:
- SP: se quitó `saldo` de la salida y se reemplazó por `exen = ISNULL(c.exen, 0)`.
- SP: quedan saliendo `total`, `grava` y `exen` desde `COMPROBANTES`.
- DW: se reemplazó el campo `saldo` por `exen`.
- DW: `subtotal_a` ahora calcula `grava + exen`.
- DW: vencimientos A/B para vto1, vto2 y vto3 ahora calculan `total + deuda - creditos`.
- DW: barra, `barra_128` y `barra_numeros` ocultan cuando el primer vencimiento neto da `0`.

Validaciones:
- SRD preservado como UTF-8 con BOM y CRLF.
- SQL preservado como ISO-8859-1 sin BOM y CRLF.
- Confirmado que no queda `c.saldo`, `dbname="saldo"` ni `name=saldo`.
- Confirmado que `total`, `grava` y `exen` aparecen 2 veces en el SP, una por cada SELECT del `UNION ALL`.
- No ejecuté compilación PowerBuilder ni validación contra base.

---

### [26/08/2026 15:56:00] Cristian Iván Ratto

Pre-compaction memory flush. Store durable memories only in memory/2026-08-26.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-26.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-26-HHMM.md); always use the canonical 2026-08-26.md filename. If nothing to store, reply with NO_REPLY.
Current time: Wednesday, August 26th, 2026 - 3:55 PM (America/Buenos_Aires)
Reference UTC: 2026-08-26 18:55 UTC

---

### [26/08/2026 15:56:34] Agente

NO_REPLY

---

### [26/08/2026 15:56:34] Cristian Iván Ratto

Te pido un resumen del caso roisa como está en el trunk vs roisa ahora con los ultimos cambios.

---

### [26/08/2026 15:57:59] Agente

Cristian, resumen comparativo ROISA:

**Trunk**
SP `afs_compro_impresion_roisa.sql`:
- No trae `total`, `grava`, `exen` ni `saldo` desde `COMPROBANTES`.
- Sigue usando `impor1` para `impor_letras`.

DW `d_factura_roisa.srd`:
- No tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA`: `abs(impor1)`.
- `SUBTOTAL A/B`: `abs(impor1 - iibb_arba - iva_total)`.
- Vto1 A/B: `abs(impor1 + deuda1)`.
- Vto2 A/B: `abs(impor2 + deuda2)`.
- Vto3 A/B: `abs(impor3 + deuda3)`.
- Barra común: se oculta solo por tipo de barra `0448`, no por importe de vencimiento en cero.

**ROISA ahora**
SP `afs_compro_impresion_roisa.sql`:
- Trae desde `COMPROBANTES` estos 3 campos:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `exen = ISNULL(c.exen, 0)`
- Ya no trae `saldo`.
- `impor_letras` sigue igual que trunk: `dbo.afn_numeros_a_letras(impor1)`.

DW `d_factura_roisa.srd`:
- Tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL A/B`: `abs(if(isnull(grava),0,grava) + if(isnull(exen),0,exen))`.
- Vto1 A/B: `abs(total + deuda1 - creditos)`.
- Vto2 A/B: `abs(total + deuda2 - creditos)`.
- Vto3 A/B: `abs(total + deuda3 - creditos)`.
- Barra común, `barra_128` y `barra_numeros`: se ocultan cuando `abs(total + deuda1 - creditos) = 0`.

No modifiqué SVN; la comparación fue solo lectura contra trunk y los objetos actuales de tu carpeta.

---

### [26/08/2026 16:00:36] Cristian Iván Ratto

stan retomemos caso SBsalud

---

### [26/08/2026 16:01:21] Agente

Cristian, contexto SBSalud retomado.

Caso: `RM26993`.

Objetos de trabajo registrados:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Estado final que tengo en memoria:
- No tocar SVN salvo pedido explícito.
- SP devuelve `total`, `grava` y `saldo` desde `COMPROBANTES`.
- DW tiene columnas `total`, `grava` y `saldo`.
- `TOTAL FACTURA A/B` toma `total`.
- `SUBTOTAL` toma `grava`.
- Vencimientos calculan:
  - `Vto1 = abs(saldo + deuda1)`
  - `Vto2 = abs(saldo + deuda2)`
  - `Vto3 = abs(saldo + deuda3)`
- `barra`, `barra_128` y `barra_numeros` se ocultan si `abs(saldo + deuda1) = 0`.
- Encoding validado previamente: SQL `ISO-8859` + CRLF; SRD `UTF-16 LE BOM` + CRLF.

También quedó anotado que el tema de ocultar barras era funcionalmente discutible y debía confirmarse antes de consolidarlo.

---

### [26/08/2026 16:02:21] Cristian Iván Ratto

Recuerda los objetos de trabajo para SBSalud:
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql
(no modifiques ramas svn)

Necesito que el SP contemple:
Quitar el campo saldo del SP, ya no necesitamos traerlo en el select.
El campo total que siga tomandolo del SP de la tabla comprobante.
El campo grava que siga tomandolo del SP de la tabla comprobante.
Debe traer el campo exen tambien de la tabla comprobante
En total son 3 campos que se traen, total, grava y exen.

En la DW para el componente roisa debemos hacer.
Necesito que el los importes vencimientos sean total + (deuda - creditos)
El campo subtotal debe ser grava + exen que llega del SP.
Recuerda la formula para esconder la barra en caso del que el vencimiento sea 0

---

### [26/08/2026 16:04:53] Agente

Cristian, quedó aplicado en SBSalud sin tocar SVN.

Archivos impactados:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`

Cambios:
- SP: se quitó `saldo` de la salida.
- SP: ahora devuelve desde `COMPROBANTES` solo estos 3 campos: `total`, `grava`, `exen`.
- DW: se reemplazó la columna `saldo` por `exen`.
- DW: `subtotal_a` quedó como `grava + exen`.
- DW: vencimientos A/B para vto1, vto2 y vto3 quedaron como `total + deuda - creditos`.
- DW: `barra`, `barra_128` y `barra_numeros` se ocultan cuando el primer vencimiento neto da `0`.

Validaciones:
- SRD preservado como `UTF-16 LE BOM` con CRLF UTF-16.
- SQL preservado como `ISO-8859-1` con CRLF.
- Confirmado: no queda `c.saldo`, `saldo =`, ni `name=saldo dbname="saldo"`.
- Confirmado: `total`, `grava` y `exen` aparecen 2 veces en el SP.
- No ejecuté compilación PowerBuilder ni parse SQL porque no está disponible `sqlcmd` en el entorno.

---

### [26/08/2026 16:06:10] Cristian Iván Ratto

Te pido un resumen del caso SbSalud como está en el trunk vs SbSalud ahora con los ultimos cambios.

---

### [26/08/2026 16:06:52] Agente

Cristian, resumen comparativo SBSalud:

**Trunk**
SP `afs_compro_impresion_caseros.sql`:
- No trae `total`, `grava`, `exen` ni `saldo` desde `COMPROBANTES`.
- Mantiene `creditos`.
- `impor_letras` sigue desde `impor1`.

DW `d_factura_sbsalud.srd`:
- No tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(impor1)`.
- `SUBTOTAL A/B`: `abs(impor1 - iibb_arba - iva_total)`.
- Vto1 A/B: `abs(impor1)`.
- Vto2 A/B: `abs(impor2)`.
- Vto3 A/B: `abs(impor3)`.
- Barras: se muestran/ocultan solo por existencia de `barra` y tipo `0448`, no por vencimiento en cero.

**SBSalud ahora**
SP `afs_compro_impresion_caseros.sql`:
- Trae desde `COMPROBANTES`:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `exen = ISNULL(c.exen, 0)`
- No trae `saldo`.
- Mantiene `creditos`.
- `impor_letras` sigue desde `impor1`.

DW `d_factura_sbsalud.srd`:
- Tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(total)`.
- `SUBTOTAL A/B`: `abs(grava + exen)`.
- Vto1 A/B: `abs(total + deuda1 - creditos)`.
- Vto2 A/B: `abs(total + deuda2 - creditos)`.
- Vto3 A/B: `abs(total + deuda3 - creditos)`.
- `barra`, `barra_128` y `barra_numeros`: se ocultan cuando `abs(total + deuda1 - creditos) = 0`.

Comparación hecha en solo lectura contra trunk; no modifiqué SVN.

---

### [26/08/2026 16:34:08] Cristian Iván Ratto

Recuerda los objetos de trabajo para SBSalud:
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd
/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql
(no modifiques ramas svn)

Stan para el caso SbSalud te pido
Que los vencimientos sean impor1 como estaba en el trunk.
Y que en el SP cuando retorna las letras, use el campo total de tabla comprobante.

---

### [26/08/2026 16:35:30] Agente

Cristian, quedó aplicado en SBSalud sin tocar SVN.

Cambios:
- `d_factura_sbsalud.srd`: vencimientos restaurados como trunk:
  - Vto1 A/B: `abs(impor1)`
  - Vto2 A/B: `abs(impor2)`
  - Vto3 A/B: `abs(impor3)`
- `afs_compro_impresion_caseros.sql`: las letras ahora usan `c.total`:
  - `dbo.afn_numeros_a_letras(c.total)`

Se mantiene:
- SP devuelve `total`, `grava`, `exen`.
- DW mantiene subtotal como `grava + exen`.
- La condición de ocultar barras quedó como estaba después del cambio anterior: evalúa `total + deuda1 - creditos`.

Validaciones:
- SRD preservado `UTF-16 LE BOM` con CRLF UTF-16.
- SQL preservado `ISO-8859-1` con CRLF.
- SQL sin `;`.
- Confirmado que ya no quedan letras con `impor1`.

---

### [26/08/2026 16:40:36] Cristian Iván Ratto

Te pido un resumen del caso SbSalud como está en el trunk vs SbSalud ahora con los ultimos cambios.

---

### [26/08/2026 16:41:14] Agente

Cristian, resumen comparativo SBSalud actualizado:

**Trunk**
SP `afs_compro_impresion_caseros.sql`:
- No trae `total`, `grava`, `exen` ni `saldo` desde `COMPROBANTES`.
- Mantiene `creditos`.
- `impor_letras` usa `dbo.afn_numeros_a_letras(impor1)`.

DW `d_factura_sbsalud.srd`:
- No tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(impor1)`.
- `SUBTOTAL A/B`: `abs(impor1 - iibb_arba - iva_total)`.
- Vto1 A/B: `abs(impor1)`.
- Vto2 A/B: `abs(impor2)`.
- Vto3 A/B: `abs(impor3)`.
- Barras: se ocultan/muestran solo por existencia de `barra` y tipo `0448`.

**SBSalud ahora**
SP `afs_compro_impresion_caseros.sql`:
- Trae desde `COMPROBANTES`:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `exen = ISNULL(c.exen, 0)`
- No trae `saldo`.
- Mantiene `creditos`.
- `impor_letras` usa `dbo.afn_numeros_a_letras(c.total)`.

DW `d_factura_sbsalud.srd`:
- Tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(total)`.
- `SUBTOTAL A/B`: `abs(grava + exen)`.
- Vencimientos quedaron como trunk:
  - Vto1 A/B: `abs(impor1)`
  - Vto2 A/B: `abs(impor2)`
  - Vto3 A/B: `abs(impor3)`
- Barras: siguen con la condición ajustada, se ocultan si `abs(total + deuda1 - creditos) = 0`.

Comparación hecha en solo lectura; no modifiqué SVN.

---

### [26/08/2026 17:01:27] Cristian Iván Ratto

Para el caso SBSalud que los vencimientos sean total - creditos

---

### [26/08/2026 17:02:48] Agente

Cristian, quedó aplicado en SBSalud.

Archivo impactado:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Cambio:
- Vto1, Vto2 y Vto3 A/B ahora calculan:
  - `abs(total - creditos)`

También alineé las barras:
- `barra`, `barra_128` y `barra_numeros` se ocultan si `abs(total - creditos) = 0`.

Validaciones:
- SRD preservado como `UTF-16 LE BOM` con CRLF UTF-16.
- Confirmado que ya no quedan referencias a `deuda1`, `deuda2` ni `deuda3` en las fórmulas ajustadas.
- SP quedó sin cambios en este paso y mantiene letras con `c.total`, más `total`, `grava`, `exen`.

---

### [26/08/2026 17:28:41] Cristian Iván Ratto

Te pido un resumen del caso SbSalud como está en el trunk vs SbSalud ahora con los ultimos cambios.

---

### [26/08/2026 17:29:21] Agente

Cristian, resumen SBSalud actualizado:

**Trunk**
SP `afs_compro_impresion_caseros.sql`:
- No trae `total`, `grava`, `exen` ni `saldo` desde `COMPROBANTES`.
- Mantiene `creditos`.
- Las letras usan `dbo.afn_numeros_a_letras(impor1)`.

DW `d_factura_sbsalud.srd`:
- No tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(impor1)`.
- `SUBTOTAL A/B`: `abs(impor1 - iibb_arba - iva_total)`.
- Vto1 A/B: `abs(impor1)`.
- Vto2 A/B: `abs(impor2)`.
- Vto3 A/B: `abs(impor3)`.
- Barras: se muestran/ocultan solo por existencia de `barra` y tipo `0448`.

**SBSalud ahora**
SP `afs_compro_impresion_caseros.sql`:
- Trae desde `COMPROBANTES`:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `exen = ISNULL(c.exen, 0)`
- No trae `saldo`.
- Mantiene `creditos`.
- Las letras usan `dbo.afn_numeros_a_letras(c.total)`.

DW `d_factura_sbsalud.srd`:
- Tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(total)`.
- `SUBTOTAL A/B`: `abs(grava + exen)`.
- Vto1/Vto2/Vto3 A/B: `abs(total - creditos)`.
- `barra`, `barra_128` y `barra_numeros`: se ocultan si `abs(total - creditos) = 0`.

Comparación hecha en solo lectura; no modifiqué SVN.

---

### [26/08/2026 17:31:21] Cristian Iván Ratto

Caso SBsalud lo damos por cerrado verificado OK

---

### [26/08/2026 17:31:28] Agente

Perfecto Cristian, dejo registrado en contexto operativo:

Caso `SBSalud / RM26993`: cerrado, verificado OK.

---

### [26/08/2026 17:43:46] Cristian Iván Ratto

Stan Caso Roisa que el SP tome las letras del campo total

---

### [26/08/2026 17:44:18] Agente

Cristian, verificado en ROISA:

`/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`

El SP ya toma las letras desde `c.total` en ambas ramas del `UNION ALL`:

- `impor_letras = dbo.afn_numeros_a_letras(c.total)`
- `impor_letra = dbo.afn_numeros_a_letras(c.total)`

No hice cambios porque ya estaba aplicado. Validé que el archivo sigue `ISO-8859-1`, sin BOM, con CRLF y sin `;`.

---

### [26/08/2026 17:44:58] Cristian Iván Ratto

Te pido un resumen del caso Roisa como está en el trunk vs Roisa ahora con los ultimos cambios.

---

### [26/08/2026 17:45:37] Agente

Cristian, resumen ROISA actualizado:

**Trunk**
SP `afs_compro_impresion_roisa.sql`:
- No trae `total`, `grava`, `exen` ni `saldo` desde `COMPROBANTES`.
- Mantiene `creditos`.
- Las letras usan `dbo.afn_numeros_a_letras(impor1)`.

DW `d_factura_roisa.srd`:
- No tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(impor1)`.
- `SUBTOTAL A/B`: `abs(impor1 - iibb_arba - iva_total)`.
- Vto1 A/B: `abs(impor1 + deuda1)`.
- Vto2 A/B: `abs(impor2 + deuda2)`.
- Vto3 A/B: `abs(impor3 + deuda3)`.
- Barras: se muestran/ocultan solo por existencia de `barra` y tipo `0448`.

**ROISA ahora**
SP `afs_compro_impresion_roisa.sql`:
- Trae desde `COMPROBANTES`:
  - `total = ISNULL(c.total, 0)`
  - `grava = ISNULL(c.grava, 0)`
  - `exen = ISNULL(c.exen, 0)`
- No trae `saldo`.
- Mantiene `creditos`.
- Las letras usan `dbo.afn_numeros_a_letras(c.total)`.

DW `d_factura_roisa.srd`:
- Tiene columnas `total`, `grava`, `exen`.
- `TOTAL FACTURA A/B`: `abs(total)`.
- `SUBTOTAL A/B`: `abs(grava + exen)`.
- Vto1 A/B: `abs(total + deuda1 - creditos)`.
- Vto2 A/B: `abs(total + deuda2 - creditos)`.
- Vto3 A/B: `abs(total + deuda3 - creditos)`.
- `barra`, `barra_128` y `barra_numeros`: se ocultan si `abs(total + deuda1 - creditos) = 0`.

Comparación hecha en solo lectura; no modifiqué SVN.

---

### [26/08/2026 17:47:05] Cristian Iván Ratto

Damos por cerrado caso Roisa. Quedo OK

---

### [26/08/2026 17:47:16] Agente

Perfecto Cristian, dejo registrado en contexto operativo:

Caso `ROISA / RM26993`: cerrado, verificado OK.

---

### [26/08/2026 18:02:47] Cristian Iván Ratto

Retomemos caso SMC, objetos de trabajo:
/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd
/mnt/Agentes/cratto/RM26993/ObjetosSMC/afs_compro_impresion_caseros.sql
(no modifiques ramas svn)
Aplica los criterios utilizados en Sbsalud pero en SMC

---

### [26/08/2026 18:06:24] Agente

Cristian, quedó aplicado SMC con el criterio final de SBSalud.

Archivo impactado:
- [d_factura_caseros.srd](/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd)

Cambios:
- DW: reemplacé columna `saldo` por `exen`.
- DW: `subtotal_a` quedó como `grava + exen`.
- DW: vencimientos A/B vto1 y vto2 quedaron como `total - creditos`.
- DW: barra principal oculta cuando `abs(total - creditos) = 0`.

El SP [afs_compro_impresion_caseros.sql](/mnt/Agentes/cratto/RM26993/ObjetosSMC/afs_compro_impresion_caseros.sql) no requirió cambios: ya trae `total`, `grava`, `exen`, no trae `saldo`, y las letras usan `c.total`.

Validado:
- No quedan referencias a `if(isnull(saldo),0,saldo)` ni columna `saldo`.
- SRD preservado como `UTF-16 LE BOM`.
- La carpeta `ObjetosSMC` no es working copy SVN, no se tocaron ramas SVN.

---

### [26/08/2026 18:25:49] Cristian Iván Ratto

Retomemos caso Activa objetos de trabajo:
/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/afs_compro_impresion_caseros.sql
/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd
(no modifiques ramas svn)
Aplica los criterios utilizados en Roisa pero en Activa

---

### [26/08/2026 18:29:34] Agente

Cristian, quedó aplicado Activa con criterio Roisa.

Archivo impactado:
- [d_factura_activa.srd](/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd)

Cambios realizados:
- DW: reemplacé la columna `saldo` por `exen`.
- DW: `subtotal_a` y `subtotal_b` quedaron como `grava + exen`.
- DW: vencimientos vto1, vto2 y vto3 quedaron como `total + deudaN - creditos`.
- DW: la barra principal ahora se oculta cuando el primer vencimiento neto `total + deuda1 - creditos` da `0`.

El SP [afs_compro_impresion_caseros.sql](/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/afs_compro_impresion_caseros.sql) no requirió cambios: ya trae `total`, `grava`, `exen`, no trae `saldo`, y las letras usan `c.total`.

Validaciones:
- No quedan referencias a `if(isnull(saldo),0,saldo)` ni columna `name=saldo dbname="saldo"`.
- SRD preservado como `UTF-16 LE` con `CRLF`.
- SQL preservado como `ISO-8859` con `CRLF`.
- La carpeta `ObjetosActivaSalud` no es working copy SVN; no se tocaron ramas SVN.

Riesgo pendiente: no ejecuté validación en PowerBuilder ni prueba contra base.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
