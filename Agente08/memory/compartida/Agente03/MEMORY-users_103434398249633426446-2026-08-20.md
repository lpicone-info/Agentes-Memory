# Memoria de conversación — Marcelo Viciconti

## Datos

- **Fecha:** 20/08/2026
- **Usuario:** Marcelo Viciconti
- **ID Google Chat:** `users/103434398249633426446`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 2
- **Inicio:** 12:58:36
- **Fin:** 13:00:04

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/25hnskaaaae`

## Archivos revisados

- `56390155-381c-4055-a2d4-0d58b7c0d364.jsonl`

---

## Transcripción completa

### [20/08/2026 12:58:36] Marcelo Viciconti

El Job de Cierre de cajas corre el sp afj_Cierre_de_Cajas y está dando el siguiente Error:
Message
Executed as user: NT SERVICE\SQLSERVERAGENT. Invalid object name '#TEMP_MOVS'. [SQLSTATE 01000] (Message 0) Invalid object name '#TEMP_MOVS'. [SQLSTATE 42000] (Error 50000)Unable to open Step output file. The step failed

Esto essta ocurriendo con una migracion de servidor hardware instalando todo el soft de base de nuevo (WIndows 2022 server y SQL Server 2019). Alguna pista? Esto estaba andando hasta antes de la migracion.

---

### [20/08/2026 13:00:04] Agente

Hola Marcelo. Pista fuerte: revisaría primero **collation/case sensitivity del nuevo SQL Server**.

**Evidencia**
El error real es:

`Invalid object name '#TEMP_MOVS'`

Lo de:

`Unable to open Step output file`

parece otro problema aparte: el SQL Agent no puede escribir el archivo de salida del step, probablemente porque la ruta no existe en el nuevo servidor o `NT SERVICE\SQLSERVERAGENT` no tiene permisos.

**Hipótesis principal**
En la migración, el SQL Server nuevo puede haber quedado con una collation **case-sensitive**. Si dentro de `afj_Cierre_de_Cajas` la tabla temporal se crea como, por ejemplo:

`#Temp_Movs`

pero después se consulta como:

`#TEMP_MOVS`

en un servidor case-insensitive funcionaba, pero en uno case-sensitive falla con `Invalid object name`.

Validaría esto:

```sql
SELECT
  SERVERPROPERTY('Collation') AS server_collation,
  DATABASEPROPERTYEX(DB_NAME(), 'Collation') AS database_collation,
  DATABASEPROPERTYEX('tempdb', 'Collation') AS tempdb_collation
```

Y después buscaría en el SP todas las referencias a esa temporal:

```sql
SELECT OBJECT_DEFINITION(OBJECT_ID('dbo.afj_Cierre_de_Cajas'))
```

Puntualmente verificaría si el `CREATE TABLE` o `SELECT INTO` usa exactamente `#TEMP_MOVS` con la misma capitalización que todos los `INSERT`, `UPDATE`, `SELECT` y `DROP`.

**Recomendación**
1. Comparar la collation del servidor viejo contra el nuevo.
2. Revisar casing exacto de `#TEMP_MOVS` dentro del SP.
3. Corregir el nombre de la temporal para que sea idéntico en todo el procedimiento.
4. Corregir aparte la ruta/permisos del output file del Job SQL Agent.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
