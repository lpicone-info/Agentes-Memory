# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Walter Guise
- id_googlechat: users/112746999453197865753
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/hf8ohyaaaae

## Resumen de la actividad

### Tema 1: RM30157 - análisis de bug en listado global de autorizaciones

- Solicitud o contexto: Walter pidió ejecutar el prompt `01_bug.md` para el Redmine 30157, usando rama productiva, documento fuente `\infomed03n\Agentes\wguise\R30157\R30157.doc`, salida `ticket_30157_01_bug.md`.
- Respuesta o acciones del agente: se validó la rama productiva, se leyó el prompt desde SVN, se consultó Redmine 30157, se convirtió y revisó el documento `.doc`, y se inspeccionaron `wp_list_global_auto.srw`, `d_auto_count_x_fecha.srd`, `d_auto_prestaciones.srd` y los SP relacionados.
- Resultado: se generó `\infomed03n\Agentes\wguise\R30157	icket_30157_01_bug.md`. El diagnóstico inicial ubicó el problema en la solapa Prestaciones del listado global: el resumen filtraba correctamente por SQL, pero la solapa aplicaba un `SetFilter` local problemático sobre `d_auto_prestaciones`.
- Estado: Resuelto

### Tema 2: RM30157 - especificación técnica

- Solicitud o contexto: Walter pidió ejecutar el prompt `03_spec_01.md` usando como informe base `ticket_30157_01_bug.md`, con salida `ticket_30157_especificacion_tecnica.md`.
- Respuesta o acciones del agente: se leyó el prompt desde SVN, se revisó el informe de análisis, se validó la rama productiva y se consultó el ticket.
- Resultado: se generó `\infomed03n\Agentes\wguise\R30157	icket_30157_especificacion_tecnica.md`. La especificación quedó como documento de revisión, sin aplicar cambios al código. Inicialmente proponía impacto en `d_auto_prestaciones.srd` y `wp_list_global_auto.srw`, pero luego fue ajustada por las pruebas de Walter para limitar el alcance a `wp_list_global_auto.srw` y no tocar SP ni DataWindows.
- Estado: Resuelto

### Tema 3: RM30157 - investigación del error `Expecting TRUE/FALSE expression`

- Solicitud o contexto: Walter probó filtros en PowerBuilder y reportó que el `retrieve` seguía fallando al agregar condiciones como `prestaciones.tipo = 'P'`; luego aclaró que necesitaba filtrar por todos los campos y que `prestaciones.prestac like "250102%"` funcionaba aislado.
- Respuesta o acciones del agente: se explicó que `SetFilter()` usa el parser de expresiones de DataWindow y se analizaron posibles causas: alias SQL, columna `tipo`, comillas y orden de condiciones.
- Resultado: con las pruebas de Walter se consolidó que el comportamiento sensible era el orden del `LIKE`: en el filtro de DataWindow debía dejarse la condición `LIKE` al final de la cadena. Se corrigió el criterio de los entregables para reflejar que el fix debía rearmar `ls_filtro` con condiciones exactas primero y el `LIKE` al final.
- Estado: Resuelto

### Tema 4: revisión SVN 84263

- Solicitud o contexto: Walter pidió ejecutar el prompt `08_revision.md` sobre la revisión SVN `84263`, rama productiva, con salida `rev_45210_08_revision.md`.
- Respuesta o acciones del agente: se leyó el prompt, se obtuvo el log y diff SVN comparando `r84262` contra `r84263`, y se revisó el cambio en `backend/procedures/afs_detalle_afiliados.sql`.
- Resultado: se generó `\infomed03n\Agentes\wguise\R30157ev_45210_08_revision.md`. La revisión dejó dos hallazgos SQL: riesgo medio por `CROSS APPLY TOP 1` contra `AFI_DERIV_APORTES` por cada fila de `#DETALLE_AFILIADOS`, y riesgo bajo por uso de `YEAR/MONTH` sobre `ada.baja_fecha`. Sin base informada, esquema, índices, volumetría y plan quedaron marcados como no disponibles.
- Estado: Resuelto

### Tema 5: performance y alcance de `SET DATEFORMAT YMD`

- Solicitud o contexto: Walter consultó si había algo poco performante en la revisión y para qué servía `SET DATEFORMAT YMD`; luego preguntó si afectaba toda la base o solo la sesión del SP.
- Respuesta o acciones del agente: se explicó que el riesgo de performance estaba en el `CROSS APPLY` por fila y las funciones `YEAR/MONTH` sobre columna de fecha, no en `SET DATEFORMAT YMD`.
- Resultado: quedó aclarado que `SET DATEFORMAT YMD` afecta solo la sesión/conexión donde se ejecuta el SP; no modifica la base ni el servidor. Dentro del SP impacta conversiones de texto a fecha durante esa ejecución.
- Estado: Resuelto

### Tema 6: combo Centros de atención

- Solicitud o contexto: Walter preguntó qué condiciones necesitaba para que el combo de centros de atención trajera información.
- Respuesta o acciones del agente: se revisó el criterio de carga del DDDW `dd_puntos_atencion` asociado al campo `punto` del DW `d_puntos_atencion_filtro`, y el contexto de visibilidad en `wp_presta_insti`.
- Resultado: se informó que el combo trae `cod_punto_atencion` y `deno` desde `puntos_atencion` con `baja_fecha IS NULL`, sin filtrar por prestador, lugar, especialidad, subespecialidad, círculo ni prepaga; además, el campo se muestra solo cuando el prestador es tipo profesional (`is_tipo = 'P'`).
- Estado: Resuelto
