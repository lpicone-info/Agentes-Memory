
## Conversacion Google Chat - 2026-08-21 17:06 ART

- Usuario Facundo consulto analisis de rechazos en `SMC_Prox_Ver` para transacciones remotas. Contexto funcional relevante: para prestacion `180104` el orden esperado de prioridad de rechazos es `4` afiliado moroso, `32` prestador inhabilitado, `1254` prestacion no definida para edad, `11` carencias, `90` requiere nivel superior, `20` sin convenio para plan. Se concluyo que actualmente `20` puede tapar otros rechazos por flujo procedural, no por prioridad global.
- Transacciones `6514972`, `6514973`, `6514974`: evidencia previa indicada al usuario: `6514972` queda `recha=20` Prestacion sin convenio para este plan, `6514973` queda `recha=32` Prestador inhabilitado, `6514974` queda `recha=4` Afiliado actualmente moroso. Diagnostico: en `pri_transac_remotas` / validaciones remotas no siempre se ejecuta o propaga primero la morosidad.
- Transacciones `6514993` vs `6514994`, ambas `01C`: diferencia explicada por datos de items y origen. `6514993` tiene `aufree=77667000`, usuario `sa`, items con `nomen=1` (`420101`, `200201`), sin convenio (`conve NULL`, `conve_valor 0`) y responde `recha=20`. `6514994` tiene `aufree=-1` (portal prestadores), usuario `ffa_prest`, items con `nomen=2`, encuentra convenio `10347` y responde `recha=4`. Facundo confirmo que el error era `nomen=2`.
- Transaccion `6514999` es `02I`: se verifico que el afiliado es moroso (`afi_morosidad_estado` devuelve `9999 - AFILIADO MOROSO`) pero no queda `recha=4` porque `prs_valida_of_cobertura_remoto` tiene logica especifica: si `@r_erro=4` y `tipo_transac='02I'`, actualiza `auto_remotas.activoCondicional='S'` y limpia `@r_erro`. Resultado observado: `esta=A`, `recha=NULL`, `activoCondicional=S`, `auto=2049675`, `aufree=77667000`. No se encontro otra condicion especial distinta: baja NULL y sin registro vigente relevante en `AFI_ESTADOS_CONTRA`; el no rechazo lo causa la regla tecnica de `02I`.
- `activoCondicional='S'` fue explicado a Facundo como marca de afiliado activo condicional: para este caso deja pasar la autorizacion aunque detecto morosidad, manteniendo `esta=A` y `recha=NULL`.
- Transacciones `6515004` a `6515008`: consulta por que `6515005`, `6515006`, `6515007`, `6515008` no responden `recha=1253` como `6515004`. Respuesta: solo `6515004` (`01B`) pasa por bloque que valida sexo/edad de prestacion antes de convenio. `6515004` prestacion `2/200201`, afiliado sexo `M`, prestacion sexo `F`, queda `1253 - Prestacion no definida para el sexo del afiliado`. `6515005` (`01C`), `6515006` (`02A`) y `6515007` (`02C`) con misma prestacion terminan `recha=668 - Prestacion deshabilitada para el convenio del prestador`. `6515008` (`02Q`) quedo con otra prestacion/nomenclador (`1/110101`) y `recha=20`. Causa tecnica: `1253/1254` salen de `prs_prestacion_prop`; `01B` via `prs_valida_of_cobertura_remoto` llama esa validacion antes, pero `01C/02A/02C` exponen antes convenio `668`. En `prs_valida_of_convenio_remoto` hay antecedente `11036`: 'No valida la configuracion de edad en el webpos', por lo que ese camino no prioriza sexo/edad igual que `01B`.

## 2026-08-21 17:25 ART - Memoria operativa R29856 / V25.08.001

- Usuario referido como Pato en la conversacion directa de Google Chat.
- Repositorios disponibles informados previamente: `mpariso-Infomedical-trunk`, `mpariso-V25.08.001`, `Incidentes`, `Manuales-Proyecto-Estandar`, `ffarias-V25.08.001_PROX`.
- Trabajo sobre requerimiento `R29856` en repo `Incidentes`. Documento principal generado/actualizado en `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856.doc` con interpretacion funcional, alcance, impacto sistemico, objetos DB impactados, relacion objeto-funcionalidad, casos de prueba y pendientes.
- Para R29856 se tomo como base principal `R-09_29856_V2.doc` del repo `Incidentes`, por ser la version mas reciente/completa identificada.
- Hallazgos funcionales R29856: la modificacion apunta a copagos en transacciones remotas/WebPos, especialmente el tratamiento del criterio `Paga por adelantado`, con necesidad de parametrizar por prestador si se mantiene como adelantado o se transforma a `Paga al prestador`/`Debita en cuenta`.
- Parametro `Configo_Copa_Adelantado` en version `25.08.001`: definido en `backend/data/PARAMETROS_GENERALES.sql` con valores `A` mantiene Paga por adelantado, `P` cambia a Paga al prestador, `C` cambia a Debita en cuenta. Uso real en `prs_valida_copago_remoto`: si `pago_copa` o `pago_copa_orden` queda en `A`, lo reemplaza por el parametro; si no esta configurado, queda `A`. No calcula importe, solo cambia criterio/modo de cobro final.
- Alcance de `Configo_Copa_Adelantado`: flujo remoto con `aufree` informado, invocado desde `prs_valida_of_cobertura_remoto` y tambien desde `prs_valida_of_auto_remotas` cuando corresponde validar copagos, por ejemplo pendientes por morosidad.
- Ventana autorizaciones clasica V25.08.001 Ospecon: si no aparece valor en `Copago unit` pero si en `Coseguro p/ Orden` y `Total`, funcionalmente no falta copago; el importe esta como copago/coseguro de orden, no de item. Referencia de logica PowerBuilder: `exe/Source Infomedical/w_clase_auto.srw`, funcion `wf_valida_copagos_orden`, asigna `dw_copa_orden.object.copa_orden`, `copa_item`, `total`.
- Orden funcional de copagos identificado: para item/prestacion: 1) copago ya definido en autorizacion, 2) exenciones afiliado/situacion terapeutica/prestador-plan, 3) copago por prestador/cobertura cuando aplica `prs_valida_of_copagos_row` o `cober_prestad_copagos`, 4) copago por convenio, 5) copago por plan/cobertura. Para copago por orden: 1) especifico prestador/plan, 2) nomenclador/capitulo/subcapitulo con vigencia mas reciente, 3) copago por orden del plan, 4) exclusion por `prestac_copa_orden_exclu` anula copago por orden.
- Regla de convivencia: no siempre se aplica solo uno; si existe copago por orden y por item, `suma_reemp` decide. Si es `R`, el copago por orden reemplaza al de items; si suma, pueden coexistir copago por orden + copago por item.
- Objetos DB relacionados a R29856 documentados: nueva tabla de configuracion por prestador con nombre fisico pendiente, `parametros_generales`, `temp_copagos`, `auto_remotas`, `auto_prestac_items_remotas`, `prs_valida_copago_remoto`, `prs_valida_of_cobertura_remoto`, `prs_valida_of_auto_remotas`, `prs_valida_of_copagos`, `prs_valida_of_copagos_row`, tablas de copagos por plan/prestador/convenio/orden y objetos de exenciones.
- Ultimo estado del documento: el usuario pidio agregar dentro de cada item de funcionalidad relacionada la ventana y opcion de menu para acceder y probar. Se preparo una version con menus en `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856_con_menus.doc`, pero no se pudo reemplazar `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856.doc` porque estaba abierto/bloqueado; existia lock `~$R29856.doc` y el sistema devolvia `Device or resource busy`. Pendiente: cuando el usuario cierre `R29856.doc`, reemplazarlo por `R29856_con_menus.doc` si lo solicita.


## 2026-08-21 17:54 ART - R29856 / copagos en V25.08.001

- Usuario: Pato. Contexto de trabajo principal: repo SVN `mpariso-V25.08.001` y repo/carpeta de incidentes `Incidentes/RM29856`.
- Documento de análisis funcional R29856 generado y mantenido en `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856.doc`.
- Se agregó análisis de alcance funcional, impacto sistémico, casos de prueba, objetos de base de datos impactados, y relación entre objetos/funcionalidad.
- Hubo un intento de agregar rutas de ventana/opción de menú dentro de cada ítem de funcionalidad relacionada. `R29856.doc` estaba bloqueado/abierto y no pudo reemplazarse. Se dejó versión actualizada en `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856_con_menus.doc` con el agregado de “Ventana / opcion de menu para prueba”. Si Pato cierra el `.doc`, se puede reemplazar el original por esta versión.
- Interpretación funcional de `Configo_Copa_Adelantado`: parámetro de `parametros_generales` usado en transacciones remotas/WebPos por `prs_valida_copago_remoto`. Si el copago calculado queda como `A = Paga por adelantado`, lo mantiene como `A` o lo convierte a `P = Paga al prestador` o `C = Debita en cuenta`. No cambia importe, solo criterio final de cobro.
- Copagos en ventana clásica de autorizaciones: puede ser correcto que `Copago unit` no muestre importe si el valor está calculado como `Coseguro p/ Orden`. La pantalla totaliza copago por orden e ítems por separado. En `w_clase_auto.srw`, revisar `wf_valida_copagos_orden`, `wf_actualizar_copagos`, `wf_calcular_copago`.
- Prioridad funcional observada para copago por prestación/ítem: 1) copago ya definido en autorización, 2) exenciones de afiliado/situación terapéutica/prestador-plan, 3) copago por prestador/cobertura cuando aplica `prs_valida_of_copagos_row` o `cober_prestad_copagos`, 4) convenio, 5) plan/cobertura.
- Prioridad funcional observada para copago por orden: 1) copago por orden específico del prestador/plan, 2) configuración por nomenclador/capítulo/subcapítulo con vigencia más reciente, 3) copago por orden del plan, 4) exclusiones en `prestac_copa_orden_exclu` anulan copago por orden. `suma_reemp = R` reemplaza copagos de ítem; si suma, pueden convivir copago por orden + copago por ítem.
- Autorización automática desde pedido en V25.08.001: el pedido no calcula copagos directamente. En `wp_auto_pedidos.srw`, el botón Generar abre la ventana estándar de autorización (`wp_auto_prestaciones`, `wp_auto_cirugias`, `wp_auto_internaciones`, etc.) mediante `openwithparm(...)`. Luego, en el flujo normal de autorización, sí se calculan copagos si corresponde: `wf_calcular_copago()`, `wf_valida_copagos_orden()`, `wf_actualizar_copagos()`, y al grabar `ue_setcopagos(...)`. `pru_auto_pedido.sql` solo vincula pedido/autorización y no calcula copagos.


## 2026-08-21 18:03 ART - V25.08.001 / copagos en Honorarios y Odontologia no valorizado

- Usuario: Pato. Se respondio consulta funcional sobre aplicacion de copagos en liquidacion de Honorarios y Odontologia no valorizado en version `25.08.001`.
- Honorarios: el copago no se calcula al cargar cada renglon, sino al cerrar/revisar el expediente. La ventana ejecuta calculo de copagos/topes para tipo `H`; el objeto no visual llama primero a `pru_expe_copagos` y despues a `pru_expe_topes`.
- Orden funcional Honorarios identificado: 1) copago ya definido en autorizacion (`auto_prestac_items.copa_valor / pago_copa`), 2) copago por convenio (`conve_copagos`) si no habia copago previo, 3) copago por prestador/plan/prestacion (`cober_prestad_copagos`) con prioridad sobre plan, 4) copago por plan/cobertura (`cober_copagos`), 5) autorizacion con copago valor `0` funciona como anulacion del copago del item.
- Honorarios: en `pru_expe_copagos.sql` se observo que el copago por orden para tipo `H` fue eliminado/no aplica; el SP lo deja explicitamente fuera.
- Odontologia no valorizado: no pasa por `pru_expe_copagos`; ese SP indica que se saco la parte de Odontologia porque los copagos se calculan en la carga. En la carga/validacion odontologica, PowerBuilder arma cobertura/convenio/copago, llama logica de copago por renglon, y si `pago_copa = 'P'` y valor > 0 descuenta el copago del valor real a liquidar.
- Odontologia tambien puede manejar copago por orden: se recalcula en revision con `prs_valida_of_copa_orden` y se actualiza en `expe_copagos` con `pru_actu_copa_orden`.
- Objetos/ventanas Honorarios: `wp_expe_hono.srw` cierre/carga de Honorarios llama `of_expe_topes_copagos(..., 'H', ...)`; `wp_expe_a_revisar_hono.srw` revision de Honorarios recalcula copagos/topes ante debitos; `uo_nv_liquida.sru` ejecuta `pru_expe_copagos` y `pru_expe_topes`; `pru_expe_copagos.sql` genera copagos de expedientes no odontologicos e inserta en `expe_copagos`.
- Objetos/ventanas Odontologia: `wp_expe_odon.srw` carga de prestaciones odontologicas valida prestacion/convenio/copago; `uo_nv_liqui_odon.sru` arma estructura de copago y descuenta valor real cuando `pago_copa = 'P'`; `pru_expe_copa_direct.sql` persiste copago calculado desde PowerBuilder en `EXPE_COPAGOS`; `wr_expe_odon_item_rev.srw` revision de items odontologicos recalcula copago por orden tras debitos; `pru_actu_copa_orden.sql` actualiza/da de baja copago por orden odontologico en `EXPE_COPAGOS`; `prs_expe_odon_copa.sql` consulta/reporta copagos odontologicos por item y orden, filtrando `pago_copa = 'P'` y vigentes.
- Accesos de menu verificados: Honorarios `Liquidaciones > Supervisión para cierre > Honorarios` abre `wp_selexp_sup_h` con parametro `H`; Odontologia no valorizado `Liquidaciones > Supervisión para cierre > Odontología no valorizados` abre `wp_selexp_sup_h` con parametro `E`; Odontologia carga `Odontología > Expedientes > Carga de prestaciones`; Odontologia revision `Odontología > Expedientes > Revisión de errores`.
