# Memoria durable - 2026-09-15

- Usuario identificado como Pato en conversación directa Google Chat.
- Tema trabajado: Liquidaciones de `Ospecon_Prox_ver`, en working copy SVN `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001`.
- Diagnóstico sobre cambio visual de campo afiliado en `exe/Source Infomedical/wp_expe_orde.srw`: el evento `doubleclicked` del DataWindow alterna entre modo contrato/integrante y modo credencial. Doble click sobre `contra` o `inte` oculta esos campos, muestra `creden` y cambia el rótulo a `Cred`; doble click sobre `creden` vuelve a `Afil`. Referencias aproximadas vistas: líneas 11873, 11876 y 11917.
- Diagnóstico sobre copagos en la misma pantalla: puede ser correcto que no impacte como valor ingresado si `Ingr.valor` está en `(No)`, porque validación envía `ingre_valor` nulo. Referencia aproximada: `wp_expe_orde.srw` línea 7213.
- Lógica de copago relevada: busca copago por prestador/cobertura y luego general; solo carga copago al prestador si `pago_copa = 'P'`; si prestador está exento o el copago no es tipo prestador, fuerza el copago a cero; luego descuenta `le_copago` del valor convenido con `str_c.valor = str_c.valor - le_copago`. Referencias aproximadas: líneas 5186, 5224 y 7306 de `wp_expe_orde.srw`.
- Para continuar una revisión de copago en Ospecon, validar datos puntuales de configuración: que el copago de la prestación tenga `pago_copa = 'P'` y que el prestador no esté marcado como exento.
