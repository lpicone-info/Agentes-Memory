## 2026-09-10
- Conversacion en Google Chat directo con Pato.
- Contexto de tarea pendiente: completar `Matriz_Pruebas_RM29856_DATOS V2.xlsx` ubicado en `/mnt/Agentes/pbossa/Incidentes/RM29856/`, generando `Matriz_Pruebas_RM29856_DATOS V2 completo.xlsx` en la misma carpeta.
- El archivo contiene 25 casos de prueba de Prestaciones, Traslados, Materiales y Odontologia, con cruces de excepciones y prioridades de copago. Se debe completar cada fila con plan/prestador/prestacion/convenio vigentes, copagos en columnas correspondientes, maximo 1 o 2 registros por casuistica, y observaciones para casos no encontrados o parciales.
- Antes de ejecutar falta confirmar la base de datos objetivo, porque planes, prestadores, convenios, afiliados y copagos varian entre bases. Opciones previamente planteadas: `Ospecon_Prox_Ver`, `SMC_Prox_Ver` u otra base especifica.
- Hallazgos previos en `SMC_Prox_Ver`: prepaga `1 - O.S.E.P.J.A.N.A.`, prestador `3 - CIRCULO MEDICO DE DOLORES`, plan `BLANCO - BLANCO`, convenio vigente `15`, 7 afiliados vigentes; tambien se identifico plan `SENIOR 1 - ASI 300` con prestador `1 - CIRC.MED.GRAL.RODRIGUEZ`, convenio `12`, 27818 afiliados, y una prestacion sin copago aplicable. Estos datos son solo antecedentes y no reemplazan la confirmacion de la base para la matriz.
## 2026-09-10
- Conversacion en Google Chat directo con Pato.
- Contexto de tarea pendiente: completar `Matriz_Pruebas_RM29856_DATOS V2.xlsx` ubicado en `/mnt/Agentes/pbossa/Incidentes/RM29856/`, generando `Matriz_Pruebas_RM29856_DATOS V2 completo.xlsx` en la misma carpeta.
- El archivo contiene 25 casos de prueba de Prestaciones, Traslados, Materiales y Odontologia, con cruces de excepciones y prioridades de copago. Se debe completar cada fila con plan/prestador/prestacion/convenio vigentes, copagos en columnas correspondientes, maximo 1 o 2 registros por casuistica, y observaciones para casos no encontrados o parciales.
- Antes de ejecutar falta confirmar la base de datos objetivo, porque planes, prestadores, convenios, afiliados y copagos varian entre bases. Opciones previamente planteadas: `Ospecon_Prox_Ver`, `SMC_Prox_Ver` u otra base especifica.
- Hallazgos previos en `SMC_Prox_Ver`: prepaga `1 - O.S.E.P.J.A.N.A.`, prestador `3 - CIRCULO MEDICO DE DOLORES`, plan `BLANCO - BLANCO`, convenio vigente `15`, 7 afiliados vigentes; tambien se identifico plan `SENIOR 1 - ASI 300` con prestador `1 - CIRC.MED.GRAL.RODRIGUEZ`, convenio `12`, 27818 afiliados, y una prestacion sin copago aplicable. Estos datos son solo antecedentes y no reemplazan la confirmacion de la base para la matriz.

- Facundo consulto repositorios SVN registrados. Estaban: `mpariso-Infomedical-trunk`, `mpariso-V25.08.001`, `mpariso-Incidentes-Incidentes`, `mpariso-ManualesProyectoEstandar-ManualesProyectoEstandar`, `ffarias-V25.08.001_PROX`.
- Se elimino localmente la working copy SVN `ffarias-V25.08.001_PROX` y su entrada en `REPOSITORIOS.md`, previa doble confirmacion exacta. Ruta eliminada: `/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`. Remoto NO tocado: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001_PROX`. No se ejecuto `svn delete`, `svn remove`, `svn commit` ni operacion remota. Backup dejado: `REPOSITORIOS.md.bak-svn-delete-20260910-102928`.
- Investigacion TRUNK / base `Premedic_Prod_Test`, ventana PRESMED pedidos de autorizacion: combo `mensaje si la autorizacion es rechazada` deberia traer todos los mensajes activos de `PEDIDOS_MENSAJES_CONFIG`; hay 4 mensajes activos. Causa probable de combo vacio: cambio `r29281`, tabla `pedidos_mensajes_config.texto_msj varchar(MAX)` pero DDDW `dd_pedidos_msjs_config_texto.srd` declara `texto_msj char(200)` y se usa desde `d_abm_pedidos_auto.srd`. En base hay textos de 395 y 1203 caracteres. Recomendacion: en `dd_pedidos_msjs_config_texto` y `dd_pedidos_msjs_config`, castear/truncar para visualizacion: `CONVERT(varchar(200), pedidos_mensajes_config.texto_msj) AS texto_msj`.
- Investigacion version `25.08.001`, base `SMC_Prox_Ver`, transaccion remota `6555348` tipo `02I`: rechaza con `esta = R`, `recha = 25`, motivo `El sanatorio no tiene definida la prestacion para los convenios del plan`. Item `nomen = 2`, `prestac = 180107`, `conve = 10230`, prestacion `ECOGRAFIA CEREBRAL -CON MODO B Y A- Y/O TRANSCRANEANA`. Causa: convenio `10230 - IRURETA SUSANA` tiene la prestacion solo con `concep = 5` honorarios, falta `concep = 6` gastos. `prs_valida_of_convenio` con `ayu_espe = 'H'` OK; con `ayu_espe = 'P'` devuelve error 25. Remotas validan honorarios y gastos en `prs_valida_of_convenio_remoto.sql`; PRESMED local internacion usa `prs_valida_of_convenio_inter` y no rechaza porque encuentra honorarios para practica no quirurgica. Solucion de datos: agregar `2 / 180107` con `concep = 6` al convenio `10230` si corresponde funcionalmente.
- En `SMC_Prox_Ver` se busco convenio que solo tenga prestaciones vigentes con `concep = 6`. Ejemplo encontrado: convenio `10439 - DIAVERUM`, una relacion vigente: `nomen = 2`, `prestac = 270102`, `concep = 6`, `conve_prestac = 632207`. Query usado: agrupar `conve_prestaciones` vigentes por convenio, filtrar `HAVING SUM(CASE WHEN cp.concep <> 6 OR cp.concep IS NULL THEN 1 ELSE 0 END) = 0`, join con `convenios` vigente.
- Investigacion remotas `02I`, tag XML `<ArancelPrestacion></ArancelPrestacion>`: el tag se parsea en `DetalleProcedimientos.java`, se pasa a `pri_auto_prestac_items_remotas` y se guarda en `auto_prestac_items_remotas.aran_prestac`, pero no se usa para aceptar/rechazar la transaccion `02I`. No aparece usado en `pri_transac_remotas` para `02I`, ni en `prs_valida_of_convenio_remoto`, ni en `prs_valida_of_auto_prestac_int`. Si una `02I` ya llego a `auto_remotas` y transacciono, `ArancelPrestacion` no incidio en aceptacion/rechazo. Existe bug Java: `if(!StringUtils.getInstance().isNullOrEmpty(arancelPrestacionInteger)); this.arancelPrestacion = Integer.parseInt(arancelPrestacionInteger);` por el `;`, por lo que si viene exactamente vacio puede fallar en parseo antes de transaccionar, segun `XmlUtils.obtenerValor` devuelva `""` o `null`.


## 2026-09-10 15:33 America/Argentina/Buenos_Aires - RM29856 / Autorem contexto durable

- Usuario habitual: Pato. Trabaja con bases `Ospecon_Prox_Ver` y `SMC_Prox_Ver`, casos de copagos/prestadores/planes/convenios/afiliados para pruebas funcionales.
- Archivo de matriz RM29856 ubicado en `/mnt/Agentes/pbossa/Incidentes/RM29856/Matriz_Pruebas_RM29856_DATOS V2.xlsx`. Se genero el completo en la misma carpeta: `/mnt/Agentes/pbossa/Incidentes/RM29856/Matriz_Pruebas_RM29856_DATOS V2 completo.xlsx` sobre base `Ospecon_Prox_Ver`.
- Para RM29856: Pato pidio completar 25 filas con plan vigente, prestador vigente, prestacion vigente, convenio vigente, excepciones marcadas con cruces y copagos indicados. Se cargo 1-2 registros maximo por casuistica; si no se encontro completo se marco observacion/parcial.
- En explicacion de fila 1 de matriz RM29856: `Parcial` significo que no se encontro un unico caso que cumpla todas las cruces juntas. Datos mencionados: prestador `443`, plan `BASE`, excepcion c exacta prestador/plan vigente desde `01/05/2026`; prestacion `180112`, cobertura `62953`, copago plan/cobertura `$5.000`; afiliados vigentes BASE `273.613`; para excepcion a) afiliado `1000/00` con situacion `200` para prestacion `180112`; para b) afiliado `100507/00`; no habia mismo afiliado con a+b. No se identifico copago L para prestador `443`.
- Caso WS transacciones remotas Autorem consultado por Pato: XML `TipoTransaccion 02A`, afiliado `Entidad 1 / Contra 100261 / Inte 00`, prestador `729` CUIT `24066762714`, prestacion `190102` tipo `1`, fecha atencion `20251107`, diagnostico `R520`.
- Diagnostico dado: el WS devuelve `<MensajeDisplay>COMUNICARSE CON ATENCION AL SOCIO</MensajeDisplay>` por morosidad del afiliado a la fecha enviada en XML. En `Ospecon_Prox_Ver` se registraron transacciones `21033` y `21034`, estado `R`, rechazo `4`; `recha_motivos` indica `Afiliado actualmente moroso`. SP `prs_auto_rem_salida_activia` homologa la salida a `COMUNICARSE CON ATENCION AL SOCIO`.
- Validacion morosidad: `afi_morosidad_estado` dio `1` para `07/11/2025` y `NULL` para `10/09/2026`. Conclusion: portal no reproduce porque para `02A` usa fecha del dia (`webpos/controlador/transaccionar.php`: `$fecha = date('Ymd H:i:s');`), mientras el WS valida contra `FechaAtencion` del XML (`20251107`).
## 2026-09-10

- Usuario prefiere ser llamado Juana según el contexto previo; solicita explicaciones claras y sencillas, con análisis técnico preciso.
- Trabajos recientes: análisis sin cambios sobre Trunk/V25.03.006 en afilmed, módulo Facturación > Procesos masivos > Exportación de débitos; se determinó que `alta_codigo` en `homolog_formatos` (posición 80, longitud 1) se calcula en `exe/Source Infomedical/wp_export_debitos.srw`, función `wf_arma_registro_det`, consultando `afi_cuentas_visa` por prepaga/contrato/cuenta. Si existe, exporta espacio; si no existe, `E`, y luego registra mediante `afu_cuentas_visa`.
- Para Redmine #30218, rama V25.08.001 y base `mutualrioiv_prod_test`, se elaboró propuesta sin modificar código/base. Se recomendó validación centralizada previa a grabar/aprobar solicitudes en `wp_solici.srw`, detectando solicitudes vigentes futuras del mismo contrato; estados relevantes observados: `A` y `F`. Documento generado en `/mnt/Agentes/jvmendoza/Analisis/R30218/R30218_Propuesta_validacion_solicitudes.docx`.
- Criterio adicional sobre #30218: conviene una validación general para tipos de solicitud con `apli_fecha`, como alerta; especial relevancia en altas/bajas, cambios de plan/tarifa, titularidad, clase, forma de pago, datos impositivos, derivaciones, prórroga y prestador externo. Otros tipos pueden alertar con menor criticidad; decidir bloqueo según impacto.
- Para exportación de débitos, análisis previo concluyó que repetir filtros al día siguiente no garantiza grilla/totales idénticos porque puede intervenir `GETDATE()` y fecha de débito futura; no se hicieron cambios.
- Repositorios/working copies mencionadas: `mpariso-Infomedical-trunk`, `mpariso-V25.08.001`, `ffarias-V25.08.001_PROX`, `Incidentes`, `Manuales-Proyecto-Estandar`. Mantener cautela con legacy PowerBuilder 2022 y MS SQL; no revertir cambios ajenos.
