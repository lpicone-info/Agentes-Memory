# Memoria diaria - 2026-09-08

## Conversacion con Pato - copagos, autorizaciones y RM29856
- Usuario referido como Pato. Trabaja sobre base `Ospecon_Prox_Ver` y codigo legacy Infomedical/PowerBuilder 2022 + MS SQL.
- Tema central: copagos en autorizaciones, prestaciones, convenios, orden e items.
- Repositorio/contexto consultado: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001`.
- Hallazgo funcional: la prioridad de copagos esta mayormente en stored procedures SQL, no en una unica funcionalidad PowerBuilder. SP relevantes: `prs_valida_of_copagos`, `prs_valida_of_copagos_row`, `pru_expe_copagos`, `prs_copagos_orden`, `prs_copagos_orden_pres`, `prs_config_copa_orden`.
- En autorizacion clasica `w_clase_auto.srw`, PB orquesta/calcula visualmente: `wf_valida_copagos_orden()` llama SP de copago por orden y aplica `suma_reemp`; si viene `R`, blanquea/reemplaza copago de item. `wf_actualizar_copagos()` suma copagos de prestaciones con cantidad y copago por orden en memoria (`dw_copa_orden.total`).
- Definicion persistente para autorizaciones:
  - Copago por convenio/item: `auto_prestac_items.copa_valor`.
  - Convenio aplicado al item: `auto_prestac_items.conve`.
  - Valor convenido de la prestacion: `auto_prestac_items.conve_valor`.
  - Copago por orden: `autorizaciones.copa_valor` y `autorizaciones.pago_copa`.
  - No hay campo fisico unico para la suma final del copago por orden + copago por convenio en autorizaciones; se reconstruye como `autorizaciones.copa_valor + SUM(auto_prestac_items.copa_valor * auto_prestac_items.canti)` y/o se ve en el DW.
- Para expediente, el registro real de copagos queda en `expe_copagos`; pero Pato aclaro que su caso era autorizacion, no expediente.
- Caso concreto trabajado: prestador 241, convenio 10301, copago configurado 1260 en `conve_copagos.valor`. Al crear autorizacion, corresponde grabar 1260 en `auto_prestac_items.copa_valor`, con `auto_prestac_items.conve = 10301`. No corresponde a `autorizaciones.copa_valor` salvo que ademas haya copago por orden.
- Tabla de configuracion de copago por convenio: `conve_copagos`; aplica si antes se resuelve convenio valido en `conve_prestaciones`/`carti_convenios`/`convenios`.
- Tablas de configuracion de copago por orden: `cober_prestad_copa_orden` y `cober_orden_copagos`; campo clave `suma_reemp` con `S` para sumar y `R` para reemplazar.
- Exenciones de copago por afiliado en `Ospecon_Prox_Ver`: tabla `afi_exen_copagos`. Para que el calculo considere exenciones, revisar regla `zz_sistema_reg.reg = 7`.
- Se entregaron queries para: copagos vigentes por orden/prestacion para prestadores activos de odontologia, materiales y traslados; detectar convenio/copago vigente; verificar `suma_reemp`; identificar exenciones multiples vigentes en `afi_exen_copagos`; identificar prestaciones plan BASE con distintos copagos vigentes via `coberturas` + `cober_copagos`.
- Portal de prestadores: login valida contra `ZZ_USUARI` mediante SP `ars_usuario_pass_valido`. Mensaje "Usuario o contraseña incorrecta" puede indicar usuario inexistente con `tipo = 'W'`, password no coincidente en `ZZ_USUARI.pwd_encript`, usuario con `baja_fecha`, bloqueo en `ZZ_INTENTOS`, o contexto/base incorrecta. Usuario visto en ejemplo: `a_uOspecPrV`.
- RM29856: Pato pidio actuar como QA sobre criterios de aceptacion de la EF seccion 5, minima cantidad de casos con cobertura 100%, separados por transacciones. Se genero archivo Excel: `/mnt/Agentes/pbossa/Incidentes/RM29856/Casos de Prueba/Matriz de Casos de Prueba Transacciones Remotas.xlsx`. Incluye hojas `Matriz`, `Cobertura`, `Reglas consideradas`. Validacion informada: 49 casos, 137 criterios, 0 sin cubrir, LibreOffice abrio/convirtio sin errores.

## Conversacion con Maru - RM27385 y transacciones remotas en expedientes
- Usuario referido como Maru (`mpariso`). Prefiere que se armen criterios/casos de prueba segun sus preferencias locales y con validacion sobre desarrollo ya realizado cuando corresponda.
- RM27385: pidio criterios de aceptacion tomando como referencia la rama `trunk` porque el desarrollo ya estaba hecho. Se revisaron EF, requerimiento, Redmine y diff SVN de integracion.
- Se genero el documento `/mnt/Agentes/mpariso/Incidentes/RM27385/RM27385_Criterios de aceptacion.docx` con 17 casos de prueba en Gherkin.
- Alcance tecnico RM27385 observado: cambios en `wp_solici.srw`, alta del parametro general `Permite editar el Banco de Acreditacion` en `parametros_generales.sql` y compilado `padron.pbd`.
- Supuestos documentados para RM27385: base de testing equivalente a Mutual Rio IV, bancos cargados con codigos compatibles con BCRA y parametro general disponible para probar valores `N` y `S`.
- Validaciones realizadas RM27385: SVN update de `trunk`, `Incidentes` y `Manuales-Proyecto-Estandar`; consulta Redmine RM27385 con usuario `mpariso`; revision de EF/documentacion/diff; validacion interna del `.docx` como ZIP, lectura con `python-docx` y conversion headless con LibreOffice OK.
- Consulta posterior de Maru: en rama `V25.08.001`, identificar en que tablas relacionadas a expedientes se graban transacciones remotas por campo `transac`.
- Rama revisada: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-V25.08.001`, actualizada a revision `84575`.
- Hallazgo principal: las transacciones remotas se relacionan con expedientes principalmente en `AUTO_REMOTAS`; no se graban en las tablas generales de expediente mediante un campo `transac`.
- Relacion principal: `AUTO_REMOTAS.transac` identifica la transaccion remota y `AUTO_REMOTAS.ofi`, `AUTO_REMOTAS.expe`, `AUTO_REMOTAS.item` apuntan al expediente/item generado o asociado.
- Evidencia `AUTO_REMOTAS`: definicion en `backend/tables/auto_remotas.sql`; `pru_transac_remotas.sql` actualiza `AUTO_REMOTAS` seteando `ofi` y `expe` por `transac`; `pru_auto_remotas_expe_transac.sql` actualiza `AUTO_REMOTAS` con `ofi`, `expe` e `item` por `transac`.
- Tablas `expe_*` con relacion directa por campo de transaccion encontradas solo en farmacia: `expe_farma_recetas.transac` y `expe_farma_rece_items.item_transac`.
- `expe_farma_recetas.transac`: definido en `backend/tables/expe_farma_recetas.sql`; grabado por `backend/procedures/pri_expe_farma_recetas.sql`, recibe `@prm_transac` y lo inserta.
- `expe_farma_rece_items.item_transac`: definido en `backend/tables/expe_farma_rece_items.sql`; grabacion/update e insert en `backend/procedures/pri_expe_farma_rece_items.sql`.
- Flujo general: `pri_expe_transac_remotas_server` toma `AUTO_REMOTAS.transac`, arma temporales y genera `EXPE_ORDENES`, `EXPE_ORDEN_ITEMS`, actualiza `EXPEDIENTES` y `AUTO_REMOTAS.item`; no se observo que `EXPE_ORDENES` ni `EXPE_ORDEN_ITEMS` guarden campo `transac`, la trazabilidad queda en `AUTO_REMOTAS`.
