# Memoria 2026-09-02

- Conversacion directa Google Chat con Maru.
- Preferencia/identidad operativa cargada: responder como Bender / Agente07, tono tecnico, directo, analitico, enfocado en PowerBuilder 2022 y MS SQL.
- Consulta RM30265 / Portal del Prestador rama 25.08.001: para transaccion 02A, si se ingresa una autorizacion inexistente el portal muestra "Autorizacion inexistente"; si existe pero no corresponde al prestador del usuario logueado muestra "La autorizacion no pertenece al prestador". Validado en `webpos/controlador/auto_busqueda.php`, `webpos/js/auto_busqueda.js` y SP `backend/procedures/ars_auto_detalle.sql`; Redmine RM30265 impacta esos archivos.
- BBDD `Ospecon_Prox_Ver`: se buscaron prestadores con lugar de atencion propio con convenio propio y uno o mas lugares por circulo con convenio del circulo para categoria/lugar. Con datos vigentes se encontraron solo 2 casos: prestador 728 CAÑERO VELASCO (lugar propio 2, convenio 10378 PREUBA P728 - LABORATORIO; lugar circulo 1; circulo 722 CEMAPS; categoria 4; convenio circulo 10325 CEMAPS) y prestador 3739 DIEZ (lugar propio 1, convenio 10354 DR DIEZ JOSE DAVID; lugar circulo 2; circulo 722 CEMAPS; categoria 4; convenio circulo 10325 CEMAPS). Si se exige que el mismo lugar tenga convenio propio y relacion al circulo, queda solo prestador 728, lugar 1, circulo 722, categoria 4.
- Presmed rama 25.08.001, menu `Convenios > Consultas > Resumen por convenios por prestador`: abre ventana `wp_resupresconv`; solapa "Lugares de atencion por cartilla" usa DataWindow `d_resupresconv_carti_lugar` y recupera con `tab_1.tp_3.dw_lug.Retrieve(ll_prestad)`.
- Consulta de `d_resupresconv_carti_lugar`: primer SELECT desde `carti_convenios`, `prestad_lugares`, `convenios`, `servi_tipos` por prestador y lugar, con `carti_convenios.baja_fecha IS NULL`; UNION con SELECT desde `PRESTAD_CATE_HABI`, `prestad_lugares`, `convenios`, `servi_tipos` por categoria habilitada, filtrando bajas nulas en `PRESTAD_CATE_HABI`, `prestad_lugares`, `convenios`; ordena por cartilla/categoria y convenio.
- Columnas visibles en solapa "Lugares de atencion por cartilla": `Carti/Cate` muestra `carti_convenios.carti` o `convenios.cate` convertido a texto; `Convenio` muestra codigo de convenio; descripcion muestra `convenios.deno`; `Tipo Serv.` muestra `servi_tipos.deno`; `Especialidad` muestra `convenios.espe` via dropdown `dd_especialidad`; `Lugar de atencion` muestra `prestad_lugares.domi`; localidad muestra `prestad_lugares.loca` via dropdown `dd_localidades`; tipo de lugar muestra `prestad_lugares.lugar_tipo` entre parentesis (`C = Consultorio`, `I = Institucion`). `convenios.subespe` se recupera pero no se vio columna visible asociada.
- Manuales Presmed: en `Manuales/Presmed/5. Convenios/Convenios - Consultas.doc`, para `Convenios > Consultas > Resumen por convenio por prestador`, indica que se registra el prestador, con o sin check en normas, y muestra convenio con cartillas, prestaciones desde/hasta con ultimos valores cargados en convenio; en lugares de atencion muestra los domicilios registrados. No se encontro especificacion mas detallada de columnas/reglas para esa solapa. Referencia cercana de otra consulta "Resumen por convenio": Lugares de atencion muestra nro prestador, razon social, cartilla, tipo de servicio, especialidad, lugar de atencion, localidad y tipo de lugar.

- Conversacion directa Google Chat con Facundo.
- R28246 / `SMC_Prox_Ver`: transaccion `6535234` tipo `01C` responde cabecera `auto_remotas.recha=733`, pero el item conserva `auto_prestac_items_remotas.recha=18`; comparada con `6535238` tipo `02C`, que queda cabecera e item con `recha=18`. Datos iguales relevantes: `contra=29888777`, `inte=00`, `plan_codi=MASTER 10`, `efe_tipo=C`, `efe_circu=NULL`, `efe_prestad=NULL`, `ori_prestad=138`, `aufree=88061688`, fecha `2026-09-02`.
- Causa R28246: en `01C`, `pri_transac_webpos_total` deriva a `prs_valida_of_convenio_remoto`; primero se detecta rechazo `18` por prestador/cobertura, pero el SP solo corta para error `20`. Como viene `18`, continua a `prs_valida_of_auto_prestac`; con cabecera `efe_tipo='C'` y `efe_circu=NULL` dispara `733 - Circulo sin categorias habilitadas` y pisa el rechazo de cabecera. El esperado funcional sigue siendo `18`.
- Origen de `efe_tipo=C` en R28246: no sale del XML, viene del WS `ws_auto_remotas/src/java/com/autorem/configuration/webmed.properties` (`efe_tipo = C`), leido en `Transaction.java` (`properties.getProperty("efe_tipo")`) e insertado desde `PrestacionDAO.java` como parametro 19 hacia `pri_auto_remotas_webpos @efe_tipo`, `pri_auto_remotas @efe_tipo` y `AUTO_REMOTAS.efe_tipo`.
- XML de R28246: `<EsCentro>N</EsCentro>` carga `auto_remotas.es_centro=N`; `<CuitPrestador>9999999999</CuitPrestador>` carga `auto_remotas.cuit_prestad=9999999999`; `<CodigoPrestador></CodigoPrestador>` deja `efe_prestad=NULL`. En base, CUIT `9999999999` no existe en `prestadores`; `prs_valida_of_prestador_remoto` intenta resolverlo por CUIT, no lo encuentra y corresponde `18 - Prestador inexistente`. No limpia `efe_tipo`, por eso queda `C` en cabecera aunque el prestador no exista.
- `SMC_Prox_Ver` version `25.08`, autorizacion `2049796`: sistema deja prestacion `420101` en pendiente por `recha=90`, aunque la prestacion no posee condicion activa configurada para nivel. Validado: autorizacion `1/2049796`, cabecera estado `P`, recha `90`; item nomen `2`, prestacion `420101`, estado `P`, recha `90`, nivel item `0`.
- Configuracion para `420101`: en `prestac_auto_niveles` existe `2 / 420101 / nivel 2` con `baja_fecha = 2023-03-01`, o sea baja/inactiva. Para cobertura: `prepaga=2`, `plan=SENIOR 1`, `nomen=2`, `prestac=420101`, `auto=N`, `auto_inter=S`, `baja_fecha=NULL`.
- Causa autorizacion `2049796`: en `prs_valida_of_auto_remotas`, la definicion activa tiene comentado el `IF` que deberia acotar el rechazo `90` por nivel (`@nivel_usuari < @nivel_prestac AND @var_cober_auto = 'S'`), pero quedo activo el cuerpo con `SELECT @r_erro = 90` y el `UPDATE auto_prestac_items_remotas ... recha = @r_erro ... AND recha IS NULL`. Por eso calcula `@nivel_prestac=0` pero igual pisa el item con `recha=90`; es bug del SP, no configuracion vigente de `420101`.
