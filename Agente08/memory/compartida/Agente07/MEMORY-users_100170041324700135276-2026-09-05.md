# Memoria de conversación — Pablo Raul Fernandez

## Datos

- **Fecha:** 05/09/2026
- **Usuario:** Pablo Raul Fernandez
- **ID Google Chat:** `users/100170041324700135276`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 12
- **Inicio:** 17:04:39
- **Fin:** 20:04:53

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/45efmkaaaae`

## Archivos revisados

- `ef6efc0b-70f7-43de-808c-236561a30501.jsonl`

---

## Transcripción completa

### [05/09/2026 17:04:39] Pablo Raul Fernandez

Buen dia, estoy queriendo ingresar a ospecon_prod_test desde presmed, y me arroja la version de licencia que adjunto, como lo soluciono?

---

### [05/09/2026 17:10:22] Agente

Hola Pablo, el error viene del control de licencia de Presmed: al iniciar valida `zz_sistema.parametro1` en `Ospecon_Prod_Test`, y la licencia vigente quedó vencida al `05/01/2026`.

Ya encontré el script de renovación preparado para OSPECON, actualizado el `19/08/2026`:

```sql
update zz_sistema
set 	parametro1 = 'iIBTyxOc9RXRMhD1A3f9Z0YzdAlhcrkOH4/qe/6egNTCP7GYeuqECCgEUv2LTEPC6kwl5EPX9k11DY/kPPWXPRYRw1ul2sT/ ',
	parametro2 = null
```

No lo ejecuté porque es un `UPDATE` en base y requiere confirmación explícita. Confirmame que querés que lo aplique sobre `Ospecon_Prod_Test`.

---

### [05/09/2026 18:16:58] Pablo Raul Fernandez

necesito me busques datos para mostrar en esta pantalla que adjunto, para la base de ospecon_prod_test. Necesito sabes la tabla tambien donde figuran los pedidos, y si esta vacia. Ademas quiero saber en que base puedo encontrar datos para poder manipular la pantalla, que sean prod_test. En caso que no encuentres, mostrame como generar los pedidos.

---

### [05/09/2026 18:24:16] Agente

Sí.

En `Ospecon_Prod_Test` la pantalla está vacía para esta consulta:

- `PEDIDOS = 0`
- `PEDIDOS_CONFIG = 0`
- `PEDIDOS_TIPO = 0`
- `AUTORIZACIONES = 11`, pero `0` tienen `nro_pedido`

La grilla de [wp_auto_pedidos.srw](</home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/exe/Source%20Infomedical/wp_auto_pedidos.srw:471>) se alimenta desde `prs_auto_pedidos`, que arranca por `PEDIDOS` y cruza con `PEDIDOS_CONFIG`, `PEDIDOS_TIPO` y `AUTORIZACIONES` ([prs_auto_pedidos.sql](</home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/prs_auto_pedidos.sql:272>)).

Para ver datos reales en `prod_test`, las bases más útiles que encontré son:

- `Premedic_Prod_Test` `PEDIDOS=1.215.534`
- `SMC_Prod_Test` `PEDIDOS=71.262`
- `Celer_Prod_Test` `PEDIDOS=136.204`

Si querés generar pedidos, el alta real está en [aru_grabar_pedido.sql](</home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/aru_grabar_pedido.sql:113>), y si el origen es `APP` además dispara la creación de autorización ([aru_grabar_pedido.sql](</home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/aru_grabar_pedido.sql:171>)).

Patrón mínimo:

```sql
EXEC dbo.pri_pedidos_tipos @prm_codigo = 'P', @prm_deno = 'Prestaciones'

EXEC dbo.pri_pedidos_config
  @prm_auto_tipo = 'P',
  @prm_fecha_necesidad_oblig = 'N',
  @prm_cant_adjuntos = 1,
  @prm_dias_min = 0,
  @prm_baja_fecha = NULL,
  @prm_modi_fecha = NULL,
  @prm_prestador_sugerido = 'N',
  @prm_fecha_orden = 'N',
  @prm_dias_max_orden = 0,
  @prm_leyenda = NULL,
  @prm_requiere_material = 'N',
  @prm_genera_auto = 'S',
  @prm_nomen = NULL,
  @prm_prestac = NULL,
  @prm_msj_recha = NULL,
  @prm_sucur = NULL,
  @prm_todos_planes = 'N'

EXEC dbo.aru_grabar_pedido
  @prm_Contrato = '12345',
  @prm_Inte = '00',
  @prm_Tipo = 'P',
  @prm_Observacion = 'TEST',
  @prm_Operador = 1,
  @prm_origen = 'WEB',
  @prm_prepaga = 1
```

Para `Ospecon_Prod_Test`, hoy la respuesta corta es: no hay pedidos cargados para esa pantalla; para manipularla con datos, conviene usar una `prod_test` poblada como `Premedic_Prod_Test`, `SMC_Prod_Test` o `Celer_Prod_Test`.

---

### [05/09/2026 19:29:09] Pablo Raul Fernandez

Quiero la query para cambiar la fecha de vigencia del domiclio de un afiliado, ademas de que me digas las tablas que involucra, para poder hacer cambios por base en los datos del afiliado.

---

### [05/09/2026 19:31:50] Agente

Pablo, para cambiar solo la fecha de vigencia del domicilio del afiliado, la tabla principal es:

- `AFI_DOMICILIOS`: domicilio general del afiliado
- `AFI_DOMICILIOS_INTE`: domicilio específico del integrante, si existe; la ficha lo prioriza antes que `AFI_DOMICILIOS`
- Referenciales: `DOMI_TIPOS`, `LOCALIDADES`, `PROVINCIAS`, `BARRIOS`
- Tabla base del afiliado: `AFILIADOS`

Query controlada para `Ospecon_Prod_Test`:

```sql
USE Ospecon_Prod_Test

DECLARE @prepaga SMALLINT
DECLARE @contra CHAR(15)
DECLARE @inte CHAR(2)
DECLARE @domi_tipo CHAR(1)
DECLARE @vigen_desde_actual SMALLDATETIME
DECLARE @vigen_desde_nueva SMALLDATETIME

SET @prepaga = 1
SET @contra = 'CONTRATO'
SET @inte = '00'
SET @domi_tipo = 'P'
SET @vigen_desde_actual = '20260101'
SET @vigen_desde_nueva = '20260901'

SELECT 'AFI_DOMICILIOS_INTE' AS tabla, *
FROM AFI_DOMICILIOS_INTE
WHERE prepaga = @prepaga
AND contra = @contra
AND inte = @inte
AND domi_tipo = @domi_tipo

SELECT 'AFI_DOMICILIOS' AS tabla, *
FROM AFI_DOMICILIOS
WHERE prepaga = @prepaga
AND contra = @contra
AND inte = @inte
AND domi_tipo = @domi_tipo

BEGIN TRAN

IF EXISTS (
    SELECT 1
    FROM AFI_DOMICILIOS_INTE
    WHERE prepaga = @prepaga
    AND contra = @contra
    AND inte = @inte
    AND domi_tipo = @domi_tipo
    AND vigen_desde = @vigen_desde_actual
)
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM AFI_DOMICILIOS_INTE
        WHERE prepaga = @prepaga
        AND contra = @contra
        AND inte = @inte
        AND domi_tipo = @domi_tipo
        AND vigen_desde = @vigen_desde_nueva
    )
    BEGIN
        UPDATE AFI_DOMICILIOS_INTE
        SET vigen_desde = @vigen_desde_nueva,
            modi_fecha = GETDATE()
        WHERE prepaga = @prepaga
        AND contra = @contra
        AND inte = @inte
        AND domi_tipo = @domi_tipo
        AND vigen_desde = @vigen_desde_actual
    END
END
ELSE
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM AFI_DOMICILIOS
        WHERE prepaga = @prepaga
        AND contra = @contra
        AND inte = @inte
        AND domi_tipo = @domi_tipo
        AND vigen_desde = @vigen_desde_nueva
    )
    BEGIN
        UPDATE AFI_DOMICILIOS
        SET vigen_desde = @vigen_desde_nueva
        WHERE prepaga = @prepaga
        AND contra = @contra
        AND inte = @inte
        AND domi_tipo = @domi_tipo
        AND vigen_desde = @vigen_desde_actual
    END
END

SELECT @@ROWCOUNT AS filas_afectadas

ROLLBACK TRAN
-- Cambiar ROLLBACK TRAN por COMMIT TRAN cuando valides el resultado
```

La clave primaria en ambas tablas es: `prepaga`, `contra`, `inte`, `domi_tipo`, `vigen_desde`. Por eso antes del `UPDATE` valido que no exista ya un domicilio con la nueva vigencia, para evitar choque de clave.

---

### [05/09/2026 19:40:16] Pablo Raul Fernandez

La query la quiero especificada en premedic_prod_test, para el contrato 27087806 y el afiliado 01, y la fecha de vigencia del domicilio del integrante, para el dia 10/10/2026

---

### [05/09/2026 19:40:42] Agente

Pablo, para `Premedic_Prod_Test` la tabla principal es `AFI_DOMICILIOS_INTE`. Para ubicar el registro también cruzás con `AFILIADOS`; y para validación de domicilio intervienen `DOMI_TIPOS`, `LOCALIDADES`, `PROVINCIAS` y `BARRIOS`.

```sql
USE Premedic_Prod_Test

DECLARE @prepaga SMALLINT
DECLARE @contra CHAR(15)
DECLARE @inte CHAR(2)
DECLARE @domi_tipo CHAR(1)
DECLARE @vigen_desde_nueva SMALLDATETIME

SET @prepaga = 1
SET @contra = '27087806'
SET @inte = '01'
SET @domi_tipo = 'P'
SET @vigen_desde_nueva = '20261010'

SELECT *
FROM AFI_DOMICILIOS_INTE
WHERE prepaga = @prepaga
AND contra = @contra
AND inte = @inte
AND domi_tipo = @domi_tipo

BEGIN TRAN

UPDATE AFI_DOMICILIOS_INTE
SET vigen_desde = @vigen_desde_nueva,
    modi_fecha = GETDATE()
WHERE prepaga = @prepaga
AND contra = @contra
AND inte = @inte
AND domi_tipo = @domi_tipo

SELECT @@ROWCOUNT AS filas_afectadas

ROLLBACK TRAN
-- Cambiar por COMMIT TRAN cuando valides el registro
```

Si el domicilio no está en `AFI_DOMICILIOS_INTE`, entonces está en `AFI_DOMICILIOS` y la misma lógica aplica ahí.

---

### [05/09/2026 19:49:49] Pablo Raul Fernandez

observo que no cambio la fecha desde del domicilio del afiliado, aca te paso la imagen que lo demuestra. Esa fecha es la que quiero cambiar

---

### [05/09/2026 19:51:50] Agente

Tenés razón: esa fecha es `vigen_desde`, y para el integrante 01 la tabla correcta es `AFI_DOMICILIOS_INTE`, no `AFI_DOMICILIOS`. Lo confirma el SP [`aws_ficha_afiliado_domi.sql`](</home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/backend/procedures/aws_ficha_afiliado_domi.sql>) y el esquema de [`afi_domicilios.sql`](</home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/backend/tables/afi_domicilios.sql>).

```sql
USE Premedic_Prod_Test
GO

BEGIN TRAN

SELECT *
FROM AFI_DOMICILIOS_INTE
WHERE prepaga = 1
  AND contra = '27087806'
  AND inte = '01'

UPDATE AFI_DOMICILIOS_INTE
SET vigen_desde = '20261010',
    modi_fecha   = GETDATE()
WHERE prepaga = 1
  AND contra = '27087806'
  AND inte = '01'
  AND vigen_desde = '20200101'   -- valor actual que se ve en la pantalla

SELECT @@ROWCOUNT AS filas_afectadas

ROLLBACK TRAN
-- Cambiar por COMMIT TRAN cuando valides que el registro es el correcto
```

Tablas que intervienen para esa pantalla:
`AFI_DOMICILIOS_INTE`, `AFI_HISTO_PAREN`, `LOCALIDADES`, `PROVINCIAS`, `BARRIOS` y `DOMI_TIPOS`.

Si querés, te dejo ahora la versión con validación para no chocar con una `vigen_desde` ya existente.

---

### [05/09/2026 19:55:35] Pablo Raul Fernandez

Necesito un afiliado de la base premedic_prod_test, cuya fecha desde del domicilio sea mayor a la fecha de hoy, y tenga pedidos de autorizacion cargadas.

---

### [05/09/2026 20:04:53] Agente

Pablo, encontré un caso válido en `Premedic_Prod_Test`:

- Afiliada: `MISHELLE SOFIA QUIÑONES PAREDES`
- Contrato: `96335052`
- Integrante: `00`
- Domicilio desde: `01/10/2026`
- Autorizaciones con `nro_pedido`: `6`
- Pedidos asociados existentes: `6`

Tablas usadas: `afiliados`, `AFI_DOMICILIOS`, `autorizaciones`, `pedidos`

Dato importante: `AFI_DOMICILIOS_INTE` no devolvió filas con `vigen_desde` futura en esta base, pero `AFI_DOMICILIOS` sí.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
