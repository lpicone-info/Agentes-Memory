## 2026-08-11 12:07 ART

- Usuario identificado en conversación previa como Maru.
- Se investigó el WS `auto_remotas` en ramas `V25.08.001` y `trunk` para transacciones de anulación `04A`.
- Para `AnularTransaccion`: ambas ramas usan el mismo formato funcional de XML en el parámetro SOAP `mensajeXml`, con raíz `<Mensaje>`. Tags obligatorios identificados: `<Mensaje>`, `<EncabezadoMensaje>`, `<TipoTransaccion>04A</TipoTransaccion>`, `<Transaccion>` con la transacción original a anular, y `<EncabezadoAtencion>`. Se recomendó incluir además `<InicioTrx>`, `<Software><NombreSoftware>WebPostInfomedical</NombreSoftware></Software>`, datos de `<Prestador>`, `<Preautorizacion>` y `<Credencial>` para evitar defaults o validaciones colaterales. `trunk` tiene mejoras internas defensivas de parseo/logging, sin cambio de contrato XML.
- Para `EjecutarTransaccion` generando `04A`: ambas ramas ejecutan `prd_transac_remotas_auto` y no se detectó diferencia de contrato XML. Tags obligatorios identificados: `<Mensaje>`, `<EncabezadoMensaje>`, `<TipoTransaccion>04A</TipoTransaccion>`, `<EncabezadoAtencion>`, `<Preautorizacion>`, `<CodigoPreautorizacion>` con la transacción a anular. Alternativa aceptada por código: `<NroReferenciaCancel>` si `<CodigoPreautorizacion>` viene vacío. Recomendación técnica dada: enviar siempre `<CodigoPreautorizacion>`, porque en `V25.08.001` el parser de `Preautorizacion` hace `.trim()` sin validar nulo y puede registrar error de parseo si falta el tag; en `trunk` está corregido, sin cambiar formato funcional.
## 2026-08-11 12:07 ART

- Usuario identificado en conversación previa como Maru.
- Se investigó el WS `auto_remotas` en ramas `V25.08.001` y `trunk` para transacciones de anulación `04A`.
- Para `AnularTransaccion`: ambas ramas usan el mismo formato funcional de XML en el parámetro SOAP `mensajeXml`, con raíz `<Mensaje>`. Tags obligatorios identificados: `<Mensaje>`, `<EncabezadoMensaje>`, `<TipoTransaccion>04A</TipoTransaccion>`, `<Transaccion>` con la transacción original a anular, y `<EncabezadoAtencion>`. Se recomendó incluir además `<InicioTrx>`, `<Software><NombreSoftware>WebPostInfomedical</NombreSoftware></Software>`, datos de `<Prestador>`, `<Preautorizacion>` y `<Credencial>` para evitar defaults o validaciones colaterales. `trunk` tiene mejoras internas defensivas de parseo/logging, sin cambio de contrato XML.
- Para `EjecutarTransaccion` generando `04A`: ambas ramas ejecutan `prd_transac_remotas_auto` y no se detectó diferencia de contrato XML. Tags obligatorios identificados: `<Mensaje>`, `<EncabezadoMensaje>`, `<TipoTransaccion>04A</TipoTransaccion>`, `<EncabezadoAtencion>`, `<Preautorizacion>`, `<CodigoPreautorizacion>` con la transacción a anular. Alternativa aceptada por código: `<NroReferenciaCancel>` si `<CodigoPreautorizacion>` viene vacío. Recomendación técnica dada: enviar siempre `<CodigoPreautorizacion>`, porque en `V25.08.001` el parser de `Preautorizacion` hace `.trim()` sin validar nulo y puede registrar error de parseo si falta el tag; en `trunk` está corregido, sin cambiar formato funcional.

## 2026-08-11 17:41 ART

- En `trunk`, el SP `prs_valida_of_cobertura` puede devolver rechazo `12` ("Requiere autorización") asignando `@erro = 12` cuando `@cober_auto = 'S'`, `@auto IS NULL` y `@var_exclu_autoriz = 'N'`. Archivo relevado: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/prs_valida_of_cobertura.sql`.
- En `trunk`, la ventana Presmed "Prestaciones a Refacturar" está en `Presmed > Autorizaciones > Consultas > Prestaciones a Refacturar`. Opción `m_prestacionesarefacturar` bajo `m_consultas5`/`m_autorizaciones`; abre `wp_auto_prestac_refac_cons`. Visibilidad condicionada por parámetro general `Presmed / Autorizaciones / refacturar_prestaciones`.
- Se generó la EF para RM27864 en `/mnt/Agentes/mpariso/Incidentes/RM27864/R-01_R27864_Documento_Especificacion_Funcional.docx`, tomando plantilla `/mnt/Agentes/mpariso/Plantillas/R-01_Documento_Especificacion_Funcional.docx`, requerimiento local/Redmine RM27864, EF modelo RM28244, trunk, manuales y preferencias de criterios de aceptación. Funcionalidad documentada: consultar prestaciones/productos/provisiones marcados para refacturación, registrar refacturados y reversar refacturaciones. SPs incluidos: `prs_lst_prestac_refac_items`, `pru_auto_prestac_refac_cons`, `prs_auto_prestac_refac`, `pru_auto_prestac_refac`, `pru_auto_domi_segui_refac`, `pru_auto_produ_refac`, `prs_valida_auto_prestac_refac`.
- En rama `V25.08.001` rev. SVN `84293`, para Presmed > Autorizaciones de Prestaciones, el prestador efector se carga en `dw_efec` objeto `de_ingre_auto_efec_prestac.srd`; el campo `Circ/Categ` es `efe_circu` y usa DDDW `dd_prestad_categ_x_circulo`.
- Flujo de carga de `Circ/Categ` en `V25.08.001`: al modificar `efe_prestad`, `ItemChanged` de `w_clase_auto.srw` invoca `ue_setefec(...)`; este valida prestador/lugares y llama `wf_set_circu_efec(...)`; esa función resetea el child DDDW de `efe_circu` y ejecuta `dd_prestad_categ_x_circulo`, cuyo retrieve usa el SP `prs_prestad_categ_x_circulo`. Si devuelve filas, habilita el combo; si no, queda protegido/deshabilitado.
- Determinación de prestador de círculo en `V25.08.001`: la lógica principal está en `prs_prestad_categ_x_circulo.sql`. Si `prestadores.tipo = 'C'`, se trata como prestador círculo y devuelve categorías desde `prestad_cate_habi`. Si no es tipo `C`, busca relación del efector con círculo mediante `prestad_circu_cate` (`pcc.prestad2 = @prestad`), validando lugar, vigencia, baja, relación en `prestad_lugares_circu` y servicios activos. Puede preseleccionar categoría con `prs_circu_cate_vig_auto`.
