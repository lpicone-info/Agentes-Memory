# Memoria 2026-08-25

- Usuario: Juana. Conversacion directa Google Chat. Prefiere trato directo y tecnico.
- Contexto de trabajo: Afilmed, rama trunk, base solicitada `Premedic_Prod` (no se ejecuto conexion porque en `DB_PROFILE.md` no habia profile exacto `Premedic_Prod`; aparecia `Premedic_Prod_Test`).
- Analisis facturacion Afilmed: en trunk se identifico que la ejecucion de facturacion queda registrada en `PROCESO_FACTURACION` con campos `inicio` y `fin`; la programacion nueva queda en `PROGRAMACION_FACTURACION`; cambios de `parametros_generales` se auditan en `param_generales_audit`.
- Claves consultadas para parametros generales: sistema `Afilmed`, modulo `Facturacion`, claves `aplica_auto_n_cred` y `aplicacion_auto_cred`. Conclusion: se puede determinar historia solo si hay registros en `param_generales_audit`; `parametros_generales` muestra solo valor vigente.
- Querys entregadas: ultimas ejecuciones en `PROCESO_FACTURACION`; ultimas programaciones en `PROGRAMACION_FACTURACION` con `tipo_proceso = 'FACTU'`; valores vigentes en `parametros_generales`; auditoria en `param_generales_audit`; cruce de auditoria contra ultima facturacion con ventana desde 7 dias antes de `inicio` hasta 1 dia despues de `fin`.
- Analisis aplicacion manual de creditos/debitos Afilmed: se encontro que `COMPRO_APLICACIONES` no guarda un indicador explicito de origen manual, ventana, usuario o modulo.
- Evidencia de codigo para aplicacion manual: ventana `wp_compro_aplica_manual` corresponde a modulo de ingreso/aplicacion manual; llama a `afi_cobranza_manual`; en etapa `APLICA COMPROBANTE` llama a `afu_aplicar_compro` con `@prm_proceso_llamador_fact = 'IM'`; la insercion final ocurre en `COMPRO_APLICACIONES` desde `afu_aplicar_compro_item`.
- Conclusion aplicacion manual: solo se puede inferir con sustento revisando el credito aplicado y coherencia entre `COMPRO_APLICACIONES`, `COMPROBANTES` y eventualmente `COMPRO_PAGOS`; indicios tipicos: credito con `proceso_facturacion = 0`, `impre_usu` cargado y fechas coherentes. No hay evidencia de indicador directo e inequivoco en `COMPRO_APLICACIONES` que diga que salio de Cuentas Corrientes / Afiliados / Aplicacion manual.
# Memoria 2026-08-25

- Usuario: Juana. Conversacion directa Google Chat. Prefiere trato directo y tecnico.
- Contexto de trabajo: Afilmed, rama trunk, base solicitada `Premedic_Prod` (no se ejecuto conexion porque en `DB_PROFILE.md` no habia profile exacto `Premedic_Prod`; aparecia `Premedic_Prod_Test`).
- Analisis facturacion Afilmed: en trunk se identifico que la ejecucion de facturacion queda registrada en `PROCESO_FACTURACION` con campos `inicio` y `fin`; la programacion nueva queda en `PROGRAMACION_FACTURACION`; cambios de `parametros_generales` se auditan en `param_generales_audit`.
- Claves consultadas para parametros generales: sistema `Afilmed`, modulo `Facturacion`, claves `aplica_auto_n_cred` y `aplicacion_auto_cred`. Conclusion: se puede determinar historia solo si hay registros en `param_generales_audit`; `parametros_generales` muestra solo valor vigente.
- Querys entregadas: ultimas ejecuciones en `PROCESO_FACTURACION`; ultimas programaciones en `PROGRAMACION_FACTURACION` con `tipo_proceso = 'FACTU'`; valores vigentes en `parametros_generales`; auditoria en `param_generales_audit`; cruce de auditoria contra ultima facturacion con ventana desde 7 dias antes de `inicio` hasta 1 dia despues de `fin`.
- Analisis aplicacion manual de creditos/debitos Afilmed: se encontro que `COMPRO_APLICACIONES` no guarda un indicador explicito de origen manual, ventana, usuario o modulo.
- Evidencia de codigo para aplicacion manual: ventana `wp_compro_aplica_manual` corresponde a modulo de ingreso/aplicacion manual; llama a `afi_cobranza_manual`; en etapa `APLICA COMPROBANTE` llama a `afu_aplicar_compro` con `@prm_proceso_llamador_fact = 'IM'`; la insercion final ocurre en `COMPRO_APLICACIONES` desde `afu_aplicar_compro_item`.
- Conclusion aplicacion manual: solo se puede inferir con sustento revisando el credito aplicado y coherencia entre `COMPRO_APLICACIONES`, `COMPROBANTES` y eventualmente `COMPRO_PAGOS`; indicios tipicos: credito con `proceso_facturacion = 0`, `impre_usu` cargado y fechas coherentes. No hay evidencia de indicador directo e inequivoco en `COMPRO_APLICACIONES` que diga que salio de Cuentas Corrientes / Afiliados / Aplicacion manual.

## RM29856 / Pato

- Usuario tratado como Pato en la conversacion directa. Trabaja con incidentes bajo `/mnt/Agentes/pbossa/Incidentes` y prefiere documentos de criterios de aceptacion en formato similar al usado en `/mnt/Agentes/pbossa/Incidentes/RM29985/Criterios de aceptación IA.docx`.
- RM29856: se trabajo en `/mnt/Agentes/pbossa/Incidentes/RM29856`. La EF tomada como fuente fue `/mnt/Agentes/pbossa/Incidentes/RM29856/R-01_29856_Documento_Especificacion_Funcional.doc`, porque no existia carpeta `EF` dentro del incidente y Pato confirmo usar ese archivo de raiz.
- Se genero `/mnt/Agentes/pbossa/Incidentes/RM29856/Impactos SP.xlsx` con hojas `Autorizaciones`, `Liquidaciones`, `WebPos Portal` y `WS Remotas`; fue regenerado con LibreOffice porque el primer archivo dio error al abrir.
- Se genero `/mnt/Agentes/pbossa/Incidentes/RM29856/Criterios de aceptacion IA.docx` con 27 escenarios Gherkin para RM29856. Luego se rehizo como `/mnt/Agentes/pbossa/Incidentes/RM29856/criterios de Aceptacion IA v2.doc`, tomando el modelo de RM29985, con 30 escenarios y estructura simple: datos del requerimiento, alcance funcional, precondiciones generales, criterios de aceptacion y observaciones.
- Se genero `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856 Criterios de Aceptacion Autorizaciones.doc`, acotado a autorizaciones de prestaciones, traslados, odontologia y materiales; contiene 68 escenarios Gherkin separados por tipo de autorizacion, incluyendo copagos por convenio, prestador/plan/prestacion, plan/prestacion, plan/orden, suma/reemplaza, exenciones y prestaciones excluidas de copago por orden.
- Se genero version nueva `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856 Criterios de Aceptacion AutorizacionesV3.doc`, manteniendo 68 escenarios, con doble espacio visual entre criterios y texto en español revisado con acentos y redaccion corregida. Validacion indicada: abre/exporta con LibreOffice y contiene los 68 escenarios esperados.
- Analisis funcional RM29856: el cambio no recalcula importes de copago. Primero se calcula el copago normal respetando convenio, plan, prestador/plan/prestacion, copago por orden y exenciones; al final, si el prestador efector tiene configuracion vigente en `PRESTAD_COPA_EXCE`, se reemplaza solo el tipo final de copago (`pago_copa` o `pago_copa_orden`) por `C` o `P` segun configuracion.
- SP `pru_auto_prestac_items`: funcionalmente persiste los items de autorizacion en `auto_prestac_items`; para RM29856 debe grabar el valor final ya resuelto de `pago_copa`, sin decidir nuevamente ni recalcular. Riesgo: que sobrescriba con un valor anterior o distinto al visto al aceptar.
- SP `prs_prestac_copa_orden_exclu`: valida si una prestacion esta excluida del copago por orden; para RM29856 mantiene prioridad como regla de exclusion. Si la prestacion esta excluida, no debe aplicarse el nuevo comportamiento para forzar `pago_copa_orden`.
- Autorizacion automatica desde pedido: funcionalmente transforma pedido en autorizacion real usando datos de afiliado, prestador efector, practica/prestacion, cantidades y datos administrativos; valida cobertura, reglas de autorizacion y copagos como una carga manual, sin recarga total por usuario.
- Para version `25.08.001` de Ospecon se reviso rama/local `V25.08.001`: la generacion automatica desde pedido esta centralizada en `uo_nv_valida_auto.sru`, funcion `of_generar_auto`, que llama al SP `prs_valida_of_auto_remotas`. Conclusion comunicada: para esta funcionalidad puntual no llevaria cambio en codigo de ventana; el impacto deberia ir en base de datos/SP. Solo habria cambio de ventana si se requiere mostrar o administrar la nueva configuracion `PRESTAD_COPA_EXCE` en un ABM de tipo de copago por prestador.
- SPs/flujo RM29856 mencionados como relevantes: `prs_valida_of_copagos_row`, `prs_valida_of_copagos`, `prs_copagos_orden`, `prs_copagos_orden_pres`, `pru_auto_prestac_items`, `prs_valida_of_auto_remotas`, `pri_transac_webpos_total`, `pri_auto_prestac_items_remotas`, `prs_valida_of_cobertura_remoto`, `prs_valida_of_auto_remotas`, `prs_valida_copago_remoto`, `pri_transac_remotas`, `pri_transac_remotas_dif`, `pri_transac_remotas_lab`, `pru_expe_copagos`, `pru_expe_copagos_x_tope`, `pru_expe_copa_direct`, `pru_actu_copa_orden`, `prs_config_copa_orden`, `prs_prestac_copa_orden_exclu`.
# Memoria 2026-08-25

- Usuario: Juana. Conversacion directa Google Chat. Prefiere trato directo y tecnico.
- Contexto de trabajo: Afilmed, rama trunk, base solicitada `Premedic_Prod` (no se ejecuto conexion porque en `DB_PROFILE.md` no habia profile exacto `Premedic_Prod`; aparecia `Premedic_Prod_Test`).
- Analisis facturacion Afilmed: en trunk se identifico que la ejecucion de facturacion queda registrada en `PROCESO_FACTURACION` con campos `inicio` y `fin`; la programacion nueva queda en `PROGRAMACION_FACTURACION`; cambios de `parametros_generales` se auditan en `param_generales_audit`.
- Claves consultadas para parametros generales: sistema `Afilmed`, modulo `Facturacion`, claves `aplica_auto_n_cred` y `aplicacion_auto_cred`. Conclusion: se puede determinar historia solo si hay registros en `param_generales_audit`; `parametros_generales` muestra solo valor vigente.
- Querys entregadas: ultimas ejecuciones en `PROCESO_FACTURACION`; ultimas programaciones en `PROGRAMACION_FACTURACION` con `tipo_proceso = 'FACTU'`; valores vigentes en `parametros_generales`; auditoria en `param_generales_audit`; cruce de auditoria contra ultima facturacion con ventana desde 7 dias antes de `inicio` hasta 1 dia despues de `fin`.
- Analisis aplicacion manual de creditos/debitos Afilmed: se encontro que `COMPRO_APLICACIONES` no guarda un indicador explicito de origen manual, ventana, usuario o modulo.
- Evidencia de codigo para aplicacion manual: ventana `wp_compro_aplica_manual` corresponde a modulo de ingreso/aplicacion manual; llama a `afi_cobranza_manual`; en etapa `APLICA COMPROBANTE` llama a `afu_aplicar_compro` con `@prm_proceso_llamador_fact = 'IM'`; la insercion final ocurre en `COMPRO_APLICACIONES` desde `afu_aplicar_compro_item`.
- Conclusion aplicacion manual: solo se puede inferir con sustento revisando el credito aplicado y coherencia entre `COMPRO_APLICACIONES`, `COMPROBANTES` y eventualmente `COMPRO_PAGOS`; indicios tipicos: credito con `proceso_facturacion = 0`, `impre_usu` cargado y fechas coherentes. No hay evidencia de indicador directo e inequivoco en `COMPRO_APLICACIONES` que diga que salio de Cuentas Corrientes / Afiliados / Aplicacion manual.
# Memoria 2026-08-25

- Usuario: Juana. Conversacion directa Google Chat. Prefiere trato directo y tecnico.
- Contexto de trabajo: Afilmed, rama trunk, base solicitada `Premedic_Prod` (no se ejecuto conexion porque en `DB_PROFILE.md` no habia profile exacto `Premedic_Prod`; aparecia `Premedic_Prod_Test`).
- Analisis facturacion Afilmed: en trunk se identifico que la ejecucion de facturacion queda registrada en `PROCESO_FACTURACION` con campos `inicio` y `fin`; la programacion nueva queda en `PROGRAMACION_FACTURACION`; cambios de `parametros_generales` se auditan en `param_generales_audit`.
- Claves consultadas para parametros generales: sistema `Afilmed`, modulo `Facturacion`, claves `aplica_auto_n_cred` y `aplicacion_auto_cred`. Conclusion: se puede determinar historia solo si hay registros en `param_generales_audit`; `parametros_generales` muestra solo valor vigente.
- Querys entregadas: ultimas ejecuciones en `PROCESO_FACTURACION`; ultimas programaciones en `PROGRAMACION_FACTURACION` con `tipo_proceso = 'FACTU'`; valores vigentes en `parametros_generales`; auditoria en `param_generales_audit`; cruce de auditoria contra ultima facturacion con ventana desde 7 dias antes de `inicio` hasta 1 dia despues de `fin`.
- Analisis aplicacion manual de creditos/debitos Afilmed: se encontro que `COMPRO_APLICACIONES` no guarda un indicador explicito de origen manual, ventana, usuario o modulo.
- Evidencia de codigo para aplicacion manual: ventana `wp_compro_aplica_manual` corresponde a modulo de ingreso/aplicacion manual; llama a `afi_cobranza_manual`; en etapa `APLICA COMPROBANTE` llama a `afu_aplicar_compro` con `@prm_proceso_llamador_fact = 'IM'`; la insercion final ocurre en `COMPRO_APLICACIONES` desde `afu_aplicar_compro_item`.
- Conclusion aplicacion manual: solo se puede inferir con sustento revisando el credito aplicado y coherencia entre `COMPRO_APLICACIONES`, `COMPROBANTES` y eventualmente `COMPRO_PAGOS`; indicios tipicos: credito con `proceso_facturacion = 0`, `impre_usu` cargado y fechas coherentes. No hay evidencia de indicador directo e inequivoco en `COMPRO_APLICACIONES` que diga que salio de Cuentas Corrientes / Afiliados / Aplicacion manual.

## RM29856 / Pato

- Usuario tratado como Pato en la conversacion directa. Trabaja con incidentes bajo `/mnt/Agentes/pbossa/Incidentes` y prefiere documentos de criterios de aceptacion en formato similar al usado en `/mnt/Agentes/pbossa/Incidentes/RM29985/Criterios de aceptación IA.docx`.
- RM29856: se trabajo en `/mnt/Agentes/pbossa/Incidentes/RM29856`. La EF tomada como fuente fue `/mnt/Agentes/pbossa/Incidentes/RM29856/R-01_29856_Documento_Especificacion_Funcional.doc`, porque no existia carpeta `EF` dentro del incidente y Pato confirmo usar ese archivo de raiz.
- Se genero `/mnt/Agentes/pbossa/Incidentes/RM29856/Impactos SP.xlsx` con hojas `Autorizaciones`, `Liquidaciones`, `WebPos Portal` y `WS Remotas`; fue regenerado con LibreOffice porque el primer archivo dio error al abrir.
- Se genero `/mnt/Agentes/pbossa/Incidentes/RM29856/Criterios de aceptacion IA.docx` con 27 escenarios Gherkin para RM29856. Luego se rehizo como `/mnt/Agentes/pbossa/Incidentes/RM29856/criterios de Aceptacion IA v2.doc`, tomando el modelo de RM29985, con 30 escenarios y estructura simple: datos del requerimiento, alcance funcional, precondiciones generales, criterios de aceptacion y observaciones.
- Se genero `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856 Criterios de Aceptacion Autorizaciones.doc`, acotado a autorizaciones de prestaciones, traslados, odontologia y materiales; contiene 68 escenarios Gherkin separados por tipo de autorizacion, incluyendo copagos por convenio, prestador/plan/prestacion, plan/prestacion, plan/orden, suma/reemplaza, exenciones y prestaciones excluidas de copago por orden.
- Se genero version nueva `/mnt/Agentes/pbossa/Incidentes/RM29856/R29856 Criterios de Aceptacion AutorizacionesV3.doc`, manteniendo 68 escenarios, con doble espacio visual entre criterios y texto en español revisado con acentos y redaccion corregida. Validacion indicada: abre/exporta con LibreOffice y contiene los 68 escenarios esperados.
- Analisis funcional RM29856: el cambio no recalcula importes de copago. Primero se calcula el copago normal respetando convenio, plan, prestador/plan/prestacion, copago por orden y exenciones; al final, si el prestador efector tiene configuracion vigente en `PRESTAD_COPA_EXCE`, se reemplaza solo el tipo final de copago (`pago_copa` o `pago_copa_orden`) por `C` o `P` segun configuracion.
- SP `pru_auto_prestac_items`: funcionalmente persiste los items de autorizacion en `auto_prestac_items`; para RM29856 debe grabar el valor final ya resuelto de `pago_copa`, sin decidir nuevamente ni recalcular. Riesgo: que sobrescriba con un valor anterior o distinto al visto al aceptar.
- SP `prs_prestac_copa_orden_exclu`: valida si una prestacion esta excluida del copago por orden; para RM29856 mantiene prioridad como regla de exclusion. Si la prestacion esta excluida, no debe aplicarse el nuevo comportamiento para forzar `pago_copa_orden`.
- Autorizacion automatica desde pedido: funcionalmente transforma pedido en autorizacion real usando datos de afiliado, prestador efector, practica/prestacion, cantidades y datos administrativos; valida cobertura, reglas de autorizacion y copagos como una carga manual, sin recarga total por usuario.
- Para version `25.08.001` de Ospecon se reviso rama/local `V25.08.001`: la generacion automatica desde pedido esta centralizada en `uo_nv_valida_auto.sru`, funcion `of_generar_auto`, que llama al SP `prs_valida_of_auto_remotas`. Conclusion comunicada: para esta funcionalidad puntual no llevaria cambio en codigo de ventana; el impacto deberia ir en base de datos/SP. Solo habria cambio de ventana si se requiere mostrar o administrar la nueva configuracion `PRESTAD_COPA_EXCE` en un ABM de tipo de copago por prestador.
- SPs/flujo RM29856 mencionados como relevantes: `prs_valida_of_copagos_row`, `prs_valida_of_copagos`, `prs_copagos_orden`, `prs_copagos_orden_pres`, `pru_auto_prestac_items`, `prs_valida_of_auto_remotas`, `pri_transac_webpos_total`, `pri_auto_prestac_items_remotas`, `prs_valida_of_cobertura_remoto`, `prs_valida_of_auto_remotas`, `prs_valida_copago_remoto`, `pri_transac_remotas`, `pri_transac_remotas_dif`, `pri_transac_remotas_lab`, `pru_expe_copagos`, `pru_expe_copagos_x_tope`, `pru_expe_copa_direct`, `pru_actu_copa_orden`, `prs_config_copa_orden`, `prs_prestac_copa_orden_exclu`.

## Facundo / 25.08 / SMC_Prox_Ver

- Usuario tratado como Facundo en Google Chat. Preferencia operativa solicitada: cuando diga `25.08` y la tarea requiera base de datos, usar por defecto el profile/base `SMC_Prox_Ver`.
- Portal de prestadores/WebPOS 25.08: URL registrada `http://10.194.0.15/standard/portal_prestadores/prox_ver/index.php`; usuario de prueba registrado `ffa_prest`.
- Para modificar base de datos, Facundo pregunto como pedirlo. Respuesta operativa dada: indicar explicitamente profile, sentencia exacta y confirmacion textual de querer modificar datos. Para cambios en base se debe frenar antes de ejecutar, mostrar comando exacto y pedir confirmacion; confirmar reforzado si es masivo, sin WHERE, ambiguo o destructivo.
- Prestador demandante valido para `plan_codi = SENIOR 2` en `SMC_Prox_Ver`: `prestad = 138`, tipo `P`, `IRURETA, SUSANA`, matricula provincial `A48269`, provincia `2 - BUENOS AIRES`, codigo XML provincia `B`, cartilla plan `SE`, lugar `1`, especialidades en cartilla `15` y `22`. XML sugerido: `<ProvinciaPrescriptor>B</ProvinciaPrescriptor>`, `<NroMatriculaPrescriptor>A48269</NroMatriculaPrescriptor>`, `<ApellidoPrescriptor>IRURETA, SUSANA</ApellidoPrescriptor>`.
- XML anterior de prestador/prescriptor recordado: para prestador ROLDAN, HUGO, matricula `A94783`, `ProvinciaPrescriptor` debia ser `B` porque Buenos Aires; con `A` lo tomaba como Salta y no resolvia `ori_prestad`.
- Error portal `prs_provincias expects parameter @pais`: en `SMC_Prox_Ver`, `EXEC prs_provincias @pais = '8'` funciona y devuelve provincias; en working copy versionada `25.08` la llamada en `webpos/modelo/matricula.php` tambien esta correcta. Conclusion comunicada: el portal desplegado probablemente no coincide con la working copy o entra por otro archivo que llama `prs_provincias` sin parametro.
- SP central del flujo remoto/WebPOS identificado: `prs_valida_of_auto_remotas`; el persistido final de autorizacion pasa por `pru_autorizaciones`, pero la decision de estado viene desde `prs_valida_of_auto_remotas`.
- Transaccion `6524981`: tipo `02I`, `aufree = 88061500`, cabecera `recha = 54`, estado `P`, efector `52`, item `5808742` prestacion `2/420101`, item `recha = 2` afiliado dado de baja, item estado `P`. Rechaza por validacion de cartilla del efector para `02I`: efector `52` no pertenece a cartilla `SE` del plan historico `SENIOR 2`; no rechaza por demandante `138`.
- Transaccion `6524982`: WebPOS/portal, `aufree = -1`, aceptada y sin item en `auto_prestac_items_remotas`; no comparable directo con `6524981`.
- Comparacion `6524981` vs `6524983`: mismo rechazo de item `2`, distinto estado por rama segun `aufree`. En `prs_valida_of_auto_remotas`, para error item `2` o `4`, con `audita_morosidad = S` y `aufree` informado queda `P` por logica especial; si `aufree IS NULL` aplica `auto_validacion_rechazos` (`recha 2 -> R`). `6524983`: `tipo_transac NULL`, `aufree NULL`, item `5808743`, prestacion `2/420101`, item `recha = 2`, item estado `R`, cabecera `recha = 2`, estado `R`.
- Comparacion `6524984` vs `6484614`: en `auto_remotas` son casi iguales funcionalmente: mismo afiliado, tipo `02I`, efector `52`, lugar `12`, `aufree = -1`, sin items, ambas aceptadas. Diferencias: `6484614 -> auto 2049548`, fecha `2026-08-07 12:04`, `ori_nombre FFFFF`, `ingre_icd I10`, `autorizaciones.esta_inter = R`; `6524984 -> auto 2049701`, fecha `2026-08-25 13:35`, `ori_nombre ffff`, `ingre_icd i10`, `autorizaciones.esta_inter = A`. Facundo indico que cambio el SP y ahora `6524984` queda `esta_inter = A`; se concluyo que el cambio impacto alta/actualizacion de `autorizaciones.esta_inter`.
- Historia de situacion terapeutica pendiente: se explico que implica historia cargada pero no autorizada/aprobada, con estado pendiente de revision, y puede afectar validaciones que dependen de situacion terapeutica vigente/autorizada.
- Transaccion `6524993`: tipo `02A`, `aufree = -1`, cabecera `recha = 4`, estado `R`, item `5808752`, item `recha = 4`, item estado `R`. Aunque `auto_validacion_rechazos.recha = 4` configura estado `P`, entra al flujo especial de morosidad remota en `prs_valida_of_auto_remotas` porque `aufree IS NOT NULL` y `audita_morosidad = S`; manda parametro general `Presmed / Autorizaciones / Transacciones Remotas (Afiliado Moroso)`, que en `SMC_Prox_Ver` esta en `R`.
- Respaldo/versionado de morosidad remota: `PARAMETROS_GENERALES.sql` historial `R16146` documenta poder determinar si autorizaciones rechazadas por morosidad quedan `P` o `R`; el parametro dice `Transacciones Remotas (WebPos - Otros), por afiliado moroso la autorizacion quedara P(Pendiente) o R(Rechazada)`. Default versionado `P`, pero en `SMC_Prox_Ver` esta `R`. Tambien se mencionaron `R17609` como script temporal insertando clave `Transacciones Remotas (Afiliado Moroso)` y `R28246` sobre circuitos/bandejas remotas 02C/02I, sin justificar especificamente morosidad `R`.
- Configuracion actual para rechazos remotos: `recha 4` (`Afiliado actualmente moroso`) tiene `auto_validacion_rechazos.esta = P`, pero por parametro general `Transacciones Remotas (Afiliado Moroso) = R` queda `R` en remotas con `aufree` informado y `audita_morosidad = S`. `recha 90` (`La autorizacion de esta prestacion depende de un nivel superior`) tiene `auto_validacion_rechazos.esta = P` y parametro general `Transacciones Remotas (Nivel de Autorizacion) = P`, por lo que queda `P`. En el SP la normalizacion final por `auto_validacion_rechazos` para `02C` con `aufree IS NOT NULL` excluye explicitamente `4` y `90`.
- Transaccion `6524997`: no entra al `recha 53` porque `53` no refiere al prestador sino a `El afiliado alcanzo el tope diario de reintegros por material descartable`. La transaccion es `02I`, tipo `I`, efector `53`, lugar `1`, plan `MASTER 10`, cartilla `16`, estado final `A`, `recha NULL`, auto `2049704`; no tiene items en tablas remotas ni finales, y el efector `53/1` esta asociado activamente a cartilla `16`, por lo que tampoco corresponde rechazo por cartilla `54`.
