# Memoria curada diaria

- Fecha: 2026-08-05
- Usuario: Pablo Raul Fernandez
- id_googlechat: users/100170041324700135276
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/45efmkaaaae

## Resumen de la actividad

### Tema 1: Prestador válido para transacción 01A en MutualRioIV_Prod_Test

- Solicitud o contexto: Pablo compartió un XML `01A` para `MutualRioIV_prod_test` que rechazaba por `Prestador Inexistente` y pidió buscar un prestador para obtener transacción aceptada.
- Respuesta o acciones del agente: Se validaron datos del afiliado `Entidad=1 / Contra=200533 / Inte=00`, plan `45FAMILIA`, cartilla `02`, y prestadores/lugares vigentes.
- Resultado: Se recomendó reemplazar el CUIT por `30566661663`, correspondiente al prestador `10 - CENTRO DE BIOQUIMICOS RIO CUARTO`, con lugar `1`, vigente y con antecedentes de `01A` aceptadas para ese plan.
- Estado: Resuelto

### Tema 2: Rechazo por código de seguridad inválido y token

- Solicitud o contexto: Pablo indicó que con el prestador sugerido ya no rechazaba por prestador, pero sí por `1449 - Código de Seguridad invalido`, y preguntó qué dato necesitaba. Luego consultó si podía parametrizar para no pedir token o dónde debía ir el token en el XML.
- Respuesta o acciones del agente: Se revisó la transacción `1092501`, parametrización de `tr_tran_tipos`, `prs_valido_token_tr`, tokens de `afi_token` y vigencia general.
- Resultado: Se explicó que `01A` usa y obliga token, validado contra `afi_token` por documento `DU 13426519`, con vigencia de 3 minutos. Para no solicitar token, se propuso parametrizar `prestad_exen_token` mediante `pru_prestad_no_token` para el prestador `10` y transacción `01A`, sin ejecutarlo por ser cambio de parametrización. Luego se buscó un prestador ya exceptuado.
- Estado: Resuelto

### Tema 3: Prestador exceptuado de token para 01A

- Solicitud o contexto: Pablo pidió un prestador que no solicitara token para evitar el rechazo por código de seguridad inválido.
- Respuesta o acciones del agente: Se buscó un prestador con excepción vigente para `01A`.
- Resultado: Se indicó usar CUIT `27371777984`, prestador `6289 - MARTINELLO`, lugar `1`, vigente, cartilla `02`, con `prestad_exen_token` para `01A` en `exceptuado = S` y una `01A` aceptada previa sin token.
- Estado: Resuelto

### Tema 4: Cobertura de prestaciones para transacción 02C

- Solicitud o contexto: Pablo compartió un XML `02C` que respondía `Prestacion sin Cobertura` para `MutualRioIV_Prod_Test` y pidió identificar una prestación con cobertura para el contrato/plan o, si no existía, otro contrato con cobertura.
- Respuesta o acciones del agente: Se analizaron el contrato `200533/00`, plan `45FAMILIA`, y los ítems del XML.
- Resultado: Se concluyó que no era necesario cambiar de contrato. La prestación `340101` tenía cobertura para el plan con `Nomenclador=2`, no con `Nomenclador=1`; el otro ítem `170101` con `Nomenclador=2` ya estaba cubierto. Se recomendó cambiar solo el nomenclador del item `340101` a `2`.
- Estado: Resuelto

### Tema 5: Estructura XML para transacción 04A en AutoRemotas

- Solicitud o contexto: Pablo pidió la estructura del XML para ejecutar la transacción `04A` por AutoRemotas en SoapUI.
- Respuesta o acciones del agente: Se explicó que `04A` corresponde a anulación de transacción, sin token, y que el dato clave es la transacción original a anular.
- Resultado: Se entregó una estructura XML mínima con `TipoTransaccion=04A`, datos de inicio, prestador y `TransaccionOriginal/NroTransaccion`. Se aclaró que el CUIT debe coincidir con el prestador de la transacción original, que no lleva `DetalleProcedimientos` y que puede no requerir beneficiario según endpoint/validación.
- Estado: Resuelto
