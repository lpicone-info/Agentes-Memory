# Memory - 2026-07-16

- User Francisco is investigating PowerBuilder 2022 / MS SQL legacy code in SVN trunk for Infomedical transfer/DDJJ import flows.
- Relevant repo path used in this session: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk`.
- User asked about rejection `1222`: "Existe mas de una cuenta vigente para el mismo C.U.I.T." for transfer import and CUIT `30559755482`.
- Initial finding: `afi_aportes_ddjj_lote` does not validate `1222`; it creates `AFI_APORTES_PRE_LOTE` and checks duplicate file by `prepaga + control_archivo + baja_fecha IS NULL`.
- DDJJ masivo path: `afi_valida_e_importa_ddjj` -> `afi_valida_e_importa_ddjj_masivo` -> `afs_valida_cuil_cuit_ddjj_auto_masivo`; `1222` condition is effectively `CAN.cantidad > 1 AND (CAN.cantidad - CAN.bajas) > 1`, grouping `CUENTAS` by `cuit` without filtering `cuenta_tipo`.
- Reproceso/correccion path: `afi_afu_cuenta_trans` applies `1222` when `@cur_count_empresa_vig > 1`.
- CUIT `30559755482` was found in `SMC_Prox_Ver` and `SMC_Prod_Test` associated to two vigente accounts: cuenta `1` tipo `O`, `baja_fecha NULL`; cuenta `753` tipo `E`, `baja_fecha NULL`. Under DDJJ masivo/reproceso logic this should trigger `1222`.
- Transfer batch path previously identified: `afi_importacion_transferencias_batch` -> `afs_importacion_transsferencias_valida` -> `afi_impacta_transferencias_batch`; `afs_importacion_transsferencias_valida` does not include `1222`, only cuenta validations like `1221` dada de baja and `1220` inexistente, and account validation runs only when `TRANS.cuitcont <> TRANS.cuitapo`.
- User then clarified focus: manual/window import object `wp_importar_trans`, not reproceso or masivo.
- For `wp_importar_trans`, sequence found: `cb_verif.clicked` in `wp_clase_imp_aportes.srw` -> `PARENT.EVENT ue_verificar()` -> `wp_importar_trans.ue_verificar` -> `ue_verificar_trans(FALSE)`; this checks duplicate file against `afi_aportes_real_lote`, validates affiliate with `wf_verif_cuil_sistema`, and validates contributor account with `wf_verif_cuit_contribuyente` only when `cuitcont <> cuitapo`.
- `wf_verif_cuil_sistema` in `wp_importar_trans` executes `afs_valida_cuil_ddjj_trans`, which delegates to SQL function `afn_valida_cuil_ddjj_trans`; that function only returns affiliate-related rejects such as `1223`, `1229`, `1311`, not duplicate-account `1222`.
- Import button sequence for `wp_importar_trans`: `cb_impor.clicked` in `wp_clase_imp_aportes.srw` -> `PARENT.EVENT ue_importar()` -> `wp_importar_trans.ue_importar` calls `afi_aportes_trans_lote`, then `afi_aportes_trans`, then `afu_aportes_real_lote`, then `afu_aportes_pre`, then `ue_verificar_trans(TRUE)` to record errors through `afi_aportes_trans_error`.
- In `wp_importar_trans`, `1222` is only contemplated in counters/CASE handling after affiliate validation, but no active validation sets it for contributor CUIT. `wf_verif_cuit_contribuyente` searches `MIN(cuenta)` for CUIT and returns true if any vigente account exists; it does not count multiple vigente accounts. For transfer type it only sets `1220` when no vigente or baja account is found.
- There is a `1222` validation in `wp_clase_imp_aportes.wf_verif_cuil` using `li_count > 1 AND li_count_vige > 1`, but `wp_importar_trans` does not call that function; it calls `wf_verif_cuil_sistema`.
- Current conclusion to preserve: manual transfer import via `wp_importar_trans` does not effectively validate rejection `1222`; it can display/grab the reject if already set, but no active path in that window sets it for multiple vigente accounts on contributor CUIT.
# Memory - 2026-07-16

- User Francisco is investigating PowerBuilder 2022 / MS SQL legacy code in SVN trunk for Infomedical transfer/DDJJ import flows.
- Relevant repo path used in this session: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk`.
- User asked about rejection `1222`: "Existe mas de una cuenta vigente para el mismo C.U.I.T." for transfer import and CUIT `30559755482`.
- Initial finding: `afi_aportes_ddjj_lote` does not validate `1222`; it creates `AFI_APORTES_PRE_LOTE` and checks duplicate file by `prepaga + control_archivo + baja_fecha IS NULL`.
- DDJJ masivo path: `afi_valida_e_importa_ddjj` -> `afi_valida_e_importa_ddjj_masivo` -> `afs_valida_cuil_cuit_ddjj_auto_masivo`; `1222` condition is effectively `CAN.cantidad > 1 AND (CAN.cantidad - CAN.bajas) > 1`, grouping `CUENTAS` by `cuit` without filtering `cuenta_tipo`.
- Reproceso/correccion path: `afi_afu_cuenta_trans` applies `1222` when `@cur_count_empresa_vig > 1`.
- CUIT `30559755482` was found in `SMC_Prox_Ver` and `SMC_Prod_Test` associated to two vigente accounts: cuenta `1` tipo `O`, `baja_fecha NULL`; cuenta `753` tipo `E`, `baja_fecha NULL`. Under DDJJ masivo/reproceso logic this should trigger `1222`.
- Transfer batch path previously identified: `afi_importacion_transferencias_batch` -> `afs_importacion_transsferencias_valida` -> `afi_impacta_transferencias_batch`; `afs_importacion_transsferencias_valida` does not include `1222`, only cuenta validations like `1221` dada de baja and `1220` inexistente, and account validation runs only when `TRANS.cuitcont <> TRANS.cuitapo`.
- User then clarified focus: manual/window import object `wp_importar_trans`, not reproceso or masivo.
- For `wp_importar_trans`, sequence found: `cb_verif.clicked` in `wp_clase_imp_aportes.srw` -> `PARENT.EVENT ue_verificar()` -> `wp_importar_trans.ue_verificar` -> `ue_verificar_trans(FALSE)`; this checks duplicate file against `afi_aportes_real_lote`, validates affiliate with `wf_verif_cuil_sistema`, and validates contributor account with `wf_verif_cuit_contribuyente` only when `cuitcont <> cuitapo`.
- `wf_verif_cuil_sistema` in `wp_importar_trans` executes `afs_valida_cuil_ddjj_trans`, which delegates to SQL function `afn_valida_cuil_ddjj_trans`; that function only returns affiliate-related rejects such as `1223`, `1229`, `1311`, not duplicate-account `1222`.
- Import button sequence for `wp_importar_trans`: `cb_impor.clicked` in `wp_clase_imp_aportes.srw` -> `PARENT.EVENT ue_importar()` -> `wp_importar_trans.ue_importar` calls `afi_aportes_trans_lote`, then `afi_aportes_trans`, then `afu_aportes_real_lote`, then `afu_aportes_pre`, then `ue_verificar_trans(TRUE)` to record errors through `afi_aportes_trans_error`.
- In `wp_importar_trans`, `1222` is only contemplated in counters/CASE handling after affiliate validation, but no active validation sets it for contributor CUIT. `wf_verif_cuit_contribuyente` searches `MIN(cuenta)` for CUIT and returns true if any vigente account exists; it does not count multiple vigente accounts. For transfer type it only sets `1220` when no vigente or baja account is found.
- There is a `1222` validation in `wp_clase_imp_aportes.wf_verif_cuil` using `li_count > 1 AND li_count_vige > 1`, but `wp_importar_trans` does not call that function; it calls `wf_verif_cuil_sistema`.
- Current conclusion to preserve: manual transfer import via `wp_importar_trans` does not effectively validate rejection `1222`; it can display/grab the reject if already set, but no active path in that window sets it for multiple vigente accounts on contributor CUIT.

- User Pato asked where remote transactions can be consulted/imported into an expediente. Answered functional path: `Autorizaciones > Consultas > Transacciones registradas`, with accesses `Por prestador`, `Por farmacia`, and `Por afiliado`; import path: `Carga de expedientes > Importación > Prestaciones de expediente`, choose option `Remotas`, opening `Importacion de transacciones`.
- Evidence for remote transaction import path came from repo files/objects: menu `m_main.srm`, windows `wp_import_exp.srw`, `wp_impor_tran_remotas.srw`, and consulta datawindows/SPs around `prs_cons_transac_remotas`.
- User then asked what the `Buscar` button does in `wp_impor_tran_remotas` (`Importacion de transacciones`) and which SQL objects it calls. Conclusion: `Buscar` does not import; it builds/populates the candidate transaction grid for later import.
- `Buscar` in `wp_impor_tran_remotas` takes `fecha`, `fecha_hasta`, `prepaga`, and expediente `prestad`, builds an `Origen` filter from selected origins, optionally asks for a `.TXT` when `Archivo de Transacciones` is checked, reads the full file, creates temp tables for process `IMPORT_TRAN`, retrieves `dw_det`, applies the origin filter in PowerBuilder, and enables `Importar`/`Imprimir` when rows are found. If no rows: shows `No hay registros para generar`.
- If a transaction file was used, `Buscar` shows processed/marked totals and, if some rows could not be validated, opens `wr_impor_tran_remotas_no_selec` (`Transacciones que no pudieron validarse`).
- SQL chain for `Buscar`: `uo_temptables_process.of_create_tables('IMPORT_TRAN')` -> datawindow `d_proceso_temp_table` -> `afs_proceso_tablas_temp`, using temp table `ARCHIVO_TRAN_NO_SELEC`; main retrieval datawindow `d_expe_import` calls `prs_arma_expe_transac_archivo_tran(@prestad, @fecha, @prm_fecha_hasta, @prm_prepaga, @prm_archivo)`; that SP internally executes `prs_arma_expe_transac`, which retrieves available remote transactions from `auto_remotas`; if file validation has not-selected rows, `prs_transac_archivo_tran_no_selec` feeds `wr_impor_tran_remotas_no_selec`.
- Relevant SQL tables/functions in that chain: `auto_remotas`, `prestadores`, `prestad_circu_cate`, `prestad_lugares_circu`, `tr_tran_tipos`, `afiliados`, `origenes`, `sucursales`, `prepagas`, `HOMOLOG_FORMATOS_CAB`, `HOMOLOG_FORMATOS`, `afn_parsear_sin_trim`, and temp `#ARCHIVO_TRAN_NO_SELEC`.
- Central query condition for remote transaction candidates: accepted transactions (`esta = 'A'`), not baja, no expediente associated (`expe IS NULL`), with `aufree` and contract informed, excluding transaction types `02F` and `02J`.

## 2026-07-16 20:13 UTC - Pre-compaction memory flush

- Usuario habitual: Maru. Comunicación preferida: directa, técnica, en español, con evidencias por archivo/línea cuando se analiza código legado.
- Rol/agente activo: Bender / Agente07, arquitecto de sistemas enfocado en PowerBuilder 2022, MS SQL y análisis de Redmine/incidentes. Priorizar trazabilidad, cautela sobre legacy y no modificar código salvo pedido explícito.
- Workspace principal usado hoy: `/home/desarrollo-ia-07/.openclaw/workspace`. Repo trunk local consultado: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk`. Incidentes consultados: `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Incidentes-Incidentes`.
- SVN update de trunk falló en varias respuestas por autenticación/certificado SSL no confiable; los análisis se hicieron sobre la working copy local disponible.

### Perfil BBDD registrado
- Se registró correctamente en `DB_PROFILE.md` el profile `Roisa_Prod_Test`.
- Motor: MS SQL. Host/IP: `172.16.10.115`. Puerto: `1433`. Base: `Roisa_Prod_Test`. Usuario: `sa`. Password informado por usuario: `IMed.2021`. Notas: `Roisa_Prod_Test`.
- Validación de conexión reportada: OK.

### RM26993 / impresión de comprobantes
- Diagnóstico entregado: la inconsistencia reportada no parece ser corrupción de datos puntual, sino inconsistencia de modelo/cálculo en `afi_compro_impresion`: se mezclan el total propio del comprobante y el importe a pagar al vencimiento neto de saldos/créditos.
- Evidencia clave en trunk: `afi_compro_impresion.sql` carga `importe` usando `C.saldo` solo para ciertos `contra_forma`, mientras para formato estándar usa `C.total`; luego persiste `impor1`, `deuda1` y `creditos` separados en `COMPRO_IMPRESION`.
- Para SOS / formato estándar, si saldo a favor cubre todo el comprobante, el crédito queda registrado pero `impor1` sigue mostrando total factura, por eso el primer vencimiento muestra el total en vez de 0.
- Para otros formatos, usar `C.saldo` puede acercar el vencimiento esperado, pero contamina subtotal/total factura porque `impor1` también alimenta datos visuales del comprobante.
- Antecedentes revisados: R29732 (problema relacionado en Estado de Cuenta), R28776 (logueo de facturación), R29996 (presentación PBD para no mostrar saldo de cuenta corriente). Ninguno corrige la raíz `impor1` / vencimiento.

### Transacciones remotas 02C / prestador efector
- Respuesta entregada: para transacción remota `02C` es obligatorio informar `EncabezadoMensaje/Prestador/CuitPrestador` en XML de entrada.
- Si no se envía o está vacío, el WS corta en Java antes de validar autorización y responde:
  - `DescripcionRtaGeneral`: `Falta especificar Cuit del Prestador`
  - `MensajeDisplay`: `Falta especificar Cuit del Prestador`
  - `MensajePrinter`: `Ocurrio un error en el sistema`
- Evidencia indicada: `XmlUtils.java`, `Prestador.java`, `AutorizacionesDAO.java`, `AutorizacionesBO.java`. Si el dato llega a backend pero no se resuelve, `prs_valida_of_prestador_remoto.sql` rechaza con `recha = 18`, motivo `Prestador inexistente`.

### Afilmed / MOROSIDAD_REGLAS.tipo_beneficiario
- Respuesta entregada: en alta de reglas de morosidad, si la entidad no es obra social, la pantalla oculta/no exige tipo beneficiario y fuerza `ls_tipo_benef = '-1'`.
- Ese valor se pasa a `afi_morosidad_reglas` como `@tipo_beneficiario` y se inserta en `MOROSIDAD_REGLAS.tipo_beneficiario`.
- Si la entidad sí es OS, la pantalla muestra y exige `tipo_benef`; `wf_es_os()` la considera OS si `dd_prepagas.rnos` está informado.
- Al recuperar reglas, `afs_morosidad_reglas` transforma `'-1'` en `NULL`, por eso visualmente puede parecer no completado.

### Afilmed / solicitudes de afiliación prepaga sin RNOS con derivación de aportes
- Respuesta entregada: para una prepaga pura sin RNOS, no surge como obligatorio cargar régimen de derivación con tipo beneficiario solo porque el afiliado tenga derivación de aportes.
- En `wp_solici.srw`, cuando `is_rnas` está vacío, se ocultan `regimen`, categoría, situación revista y solapa de régimen derivación familiar.
- Para motivo 13 “Cambio de Deriv. de Aportes”, solo habilita esos campos si hay RNOS o si es entidad mixta; si no, llama a `wf_campos_tipo_beneficiario('N','N',5)`.
- La solapa `SOLI_DERIV_APORTES` titular guarda datos de derivación (obra social, CUIT, empresa, sistema, deriva, tipo aportante, etc.) y no contiene `regimen` ni `tipo_beneficiario`; el régimen está en `SOLI_OPCION.regimen`.
- Parámetro relacionado: `Afilmed/Solicitudes/plan_depende_de_tipo_beneficiario`, default `N` en `parametros_generales.sql`.

### Afilmed / comprobante Roisa B-0064-00183674
- Pedido analizado usando trunk y BBDD `Roisa_Prod_Test`: cómo se construye el importe junto a la leyenda “A la fecha registra facturas por un saldo de $” para contrato `20004228545`, prepaga `1`, comprobante `B-0064-00183674`.
- Respuesta entregada: importe mostrado `$ 1.041.935,94`.
- En `d_factura_roisa.srd`, la leyenda usa `compute_31`, expresión `saldo_anterior1`; `saldo_anterior1 = deuda1 - creditos`.
- En `COMPRO_IMPRESION` para `FC / B006400183674`: `deuda1 = 1.041.935,94`, `creditos = 0,00`, por lo tanto impreso `1.041.935,94`.
- `afs_compro_impresion_roisa` toma `deuda1` y `creditos` desde `COMPRO_IMPRESION`.
- Reconstrucción base: deuda corresponde a factura anterior pendiente `FC / B006400179108`, saldo `957.086,99`, interés con IVA al primer vencimiento `84.848,95`, total deuda anterior `1.041.935,94`.
- Lógica base: `afi_compro_impresion` calcula `deuda1` llamando a `afs_ctacte_deuda`; `afs_ctacte_deuda` suma `saldo + interes_civa`.
