- Usuario identificado operativamente como Javier.
- Para análisis de tickets, la carpeta de entrega confirmada es `/mnt/Agentes/jdermit/Ticket`.
- Profile de base funcional confirmado para SMC_PROD: `Smc_prod`.
- `Caseros_prod` no estaba registrado en `DB_PROFILE.md` durante la verificación.
- En trunk, la opción más cercana a reportes por día estaba en `Liquidaciones > Consulta de expedientes > Altas y bajas de expedientes por dias`.
- Usuario identificado operativamente como Javier.
- Para análisis de tickets, la carpeta de entrega confirmada es `/mnt/Agentes/jdermit/Ticket`.
- Profile de base funcional confirmado para SMC_PROD: `Smc_prod`.
- `Caseros_prod` no estaba registrado en `DB_PROFILE.md` durante la verificación.
- En trunk, la opción más cercana a reportes por día estaba en `Liquidaciones > Consulta de expedientes > Altas y bajas de expedientes por dias`.
- En Consalud_prod, el tema de odontología/WebPOS quedó ligado a `zz_sistema_reg.reg = 12`, `param_odon.nomen` y al flujo que debe exponer `odonto_habilitado`.
- El warning `Undefined array key 'odonto_habilitado'` indica desalineación entre el portal desplegado y el backend que devuelve parámetros, no solo una falta de parametrización en SQL.
- TK 6193 quedó asociado a rechazo por exceso de líneas odontológicas y al parámetro `param_odon.bono_lineas`.
- Usuario identificado operativamente como Javier.
- Para análisis de tickets, la carpeta de entrega confirmada es `/mnt/Agentes/jdermit/Ticket`.
- Profile de base funcional confirmado para SMC_PROD: `Smc_prod`.
- `Caseros_prod` no estaba registrado en `DB_PROFILE.md` durante la verificación.
- En trunk, la opción más cercana a reportes por día estaba en `Liquidaciones > Consulta de expedientes > Altas y bajas de expedientes por dias`.
- Usuario identificado operativamente como Javier.
- Para análisis de tickets, la carpeta de entrega confirmada es `/mnt/Agentes/jdermit/Ticket`.
- Profile de base funcional confirmado para SMC_PROD: `Smc_prod`.
- `Caseros_prod` no estaba registrado en `DB_PROFILE.md` durante la verificación.
- En trunk, la opción más cercana a reportes por día estaba en `Liquidaciones > Consulta de expedientes > Altas y bajas de expedientes por dias`.
- En Consalud_prod, el tema de odontología/WebPOS quedó ligado a `zz_sistema_reg.reg = 12`, `param_odon.nomen` y al flujo que debe exponer `odonto_habilitado`.
- El warning `Undefined array key 'odonto_habilitado'` indica desalineación entre el portal desplegado y el backend que devuelve parámetros, no solo una falta de parametrización en SQL.
- TK 6193 quedó asociado a rechazo por exceso de líneas odontológicas y al parámetro `param_odon.bono_lineas`.
- TK 6194 analizado en `CONSALUD_PROD`; la entrega quedó en `/mnt/Agentes/jpinto/S6194/analisis_ticket_6194.docx`.
- Contexto operativo: prestador configurado `webprueba_6194` asociado al prestador código `105` (`MAS GERARDO`).
- No se pudo corroborar en base el doble otorgamiento exacto del prestador `105`; el cierre quedó sustentado en la parametrización y la lógica del código.
- En Consalud_prod, la habilitación de odontología en WebPOS/Portal quedó ligada a `zz_sistema_reg.reg = 12`, `param_odon.nomen` y al backend que expone `odonto_habilitado`; el warning `Undefined array key 'odonto_habilitado'` apunta a desalineación entre portal y backend.
- TK 6193 quedó asociado al rechazo por exceso de líneas odontológicas y al parámetro `param_odon.bono_lineas`.
- El caso de odontología en WebPOS sí aparece en flujos `02A/02C/02D`; `01A` no expone ese circuito.
- Se generaron documentos de análisis funcional/técnico en `A:\rcassandro\DTI` para `R30335`, `R30336` y `R30056` en formato resumen ejecutivo.
- TK 6197 se vinculó funcionalmente con el antecedente `R28198` / `TK 4355` sobre ampliación del valor permitido en reintegros, como mismo tema base pero con pedido actual separado.
- Persistió el contexto operativo de la conversación con la usuaria Rosana Cassandro y la carpeta de entrega usada en esta sesión: `A:\rcassandro\DTI`.
- TK 6199 quedó analizado y el documento se dejó en `/mnt/Agentes/jdermit/Ticket/analisis_ticket_6199.docx`.
- En TK 6199, la inconsistencia principal detectada fue un alcance mezclado: "todas las transacciones del portal y WS" versus "solo 02A y 02D".
- El profile `celer_prod` ya existe en `DB_PROFILE.md`; apunta a la base `celer_prod` y usa las mismas credenciales que `mutualrioiv_prod`.
- R28882 quedó asociado al antecedente funcional R26274: "Portal de Prestadores: Transaccionar autorizaciones odontologia"; no se encontró un Redmine propio con ese texto literal, pero ese es el fix más cercano y directo.
- El análisis de R29452 quedó orientado a probar expedientes con débitos vigentes en `expe_inter_debitos` (sin baja) pasando por Supervisión de expedientes para validar si emite o no el comprobante.
- Los documentos de análisis para R30335 y R30336 quedaron separados en archivos individuales en `A:\rcassandro\DTI`.
- En liquidaciones, el IVA se dispara por la condición impositiva del afiliado y la lógica del expediente; si el afiliado está gravado/consumidor final puede haber IVA aunque la prestación no tenga IVA en el nomenclador.
- En el expediente 125515, el item 1 (prestación 660546) calculó `iva_valor = 100.43` a partir de `ingre_valor = 956.51` con `iva_tasa = 10.50%`; el origen no fue el maestro de la prestación sino la rutina de cálculo del expediente.
- Para odontología, el tope configurado de "una cada 12 meses" se controla con `odon_prestac.meses = 12` y `odon_prestac.cant_max = 1`; la validación corre al cargar/aceptar la prestación, antes de liquidar y vuelve a revisarse al preparar la liquidación.
- En el portal del prestador, la validación odontológica se ejecuta antes de liquidar, por lo que una repetición dentro de la ventana de meses puede rechazarse en la carga y no recién en la liquidación.
- En `premedic_prod_test`, el SOAP devolvía "afiliado inexistente" porque estaba resolviendo contra la prepaga 2 por configuración por defecto, mientras la credencial existía en prepaga 1; faltaba informar el financiador/entidad correcto en el request o ajustar la configuración del WS.

## 2026-09-07 16:19 ART - Pre-compaction memory flush

- Usuario habitual identificado en la conversacion como Andre. Prefiere respuestas tecnicas directas y documentos de analisis en formato `.doc` que pueda abrir en Word. Cuando el documento se ve mal o incompleto, prefiere el formato anterior/limpio y completo.
- Repositorios SVN registrados actualizados previamente: `Manuales-Proyecto-Estandar` revision 71, `estandar-trunk` revision 84538, `Incidentes` revision 21146, `abiondi-V25.08.001` revision 84538. Se detecto que la ruta de configuracion registrada podia apuntar a una carpeta intermedia; para `svn update` usar el `config-dir` correcto de cada repositorio.
- Base/profile `igualdadsalud_prod` existe y esta registrada en `DB_PROFILE.md`.
- Analisis 02F con `sos_prod` y trunk: la anulacion de una `02F` se realiza con transaccion `04A` (`Anula Transaccion`). `04A` anula tanto `02F` como `02J`; `02J` es la variante diferida de farmacia cuando la fecha de la `02F` es menor a hoy. Evidencia clave: `webpos/js/transac.js` envia `tipoTransac = '04A'` y `anulaTransac = transac`; `backend/procedures/pri_transac_webpos_total.sql` ejecuta `prd_transac_remotas_auto` para `04A`; `backend/procedures/pri_transac_remotas_farm.sql` valida que la transaccion a anular sea `02F` o `02J`; `backend/procedures/pri_auto_remotas_farma.sql` guarda como `02J` si corresponde.
- Redmine 30058 (`/mnt/Agentes/abiondi/redmine/30058`), usando `igualdadsalud_prod` y trunk: se analizo lentitud en envio de mails de credenciales provisorias. Documento generado: `R30058_analisis_lentitud_credenciales_provisorias.doc` en esa carpeta. Conclusion: la lentitud ocurre antes de que Infomailer tome la cola; cuello en `wp_gene_crede_provi.srw`, principalmente en `saveAs` del PSR por cada fila y espera activa del archivo. `MAIL_ENVIO`/`MAIL_TO` se graban recien despues.
- Redmine 25487 (`/mnt/Agentes/abiondi/redmine/25487`), usando `activa_prod` y trunk: documento generado `analisis_R25487.doc`, luego reemplazado por formato mas limpio/completo. Conclusion: clasificacion `Error`. Problema en calculo del resumen del prestador. Ajuste indicado en `backend/procedures/prs_expe_debitos_resum.sql`: el total actual suma `debcal + inter_debcal + debito` y deja afuera `iva_deb`.
