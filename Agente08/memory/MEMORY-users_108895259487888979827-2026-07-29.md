# Memoria curada diaria

- Fecha: 2026-07-29
- Usuario: Jean González
- id_googlechat: users/108895259487888979827
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/1fptbkaaaae

## Resumen de la actividad

### Tema 1: Registro de credencial Redmine y preferencias de usuario

- Solicitud o contexto: Jean pidió registrar su usuario de Redmine y luego modificar su archivo de preferencias para usar el nombre `JEAN` y el usuario Redmine `jpinto`.
- Respuesta o acciones del agente: Se usó la skill de registro Redmine, se guardó la credencial localmente sin copiar el valor sensible en memoria y se modificó `/mnt/Agentes/jpinto/preferencias/preferencias.md`.
- Resultado: Se registró la credencial Redmine para `jpinto` en `/home/desarrollo-ia-08/.openclaw/secrets/redmine/jpinto.env` con permisos `600`. Luego se reemplazaron `<Nombre>` por `JEAN` y `<Usuario_Redmine>` por `jpinto`, validando ASCII, sin BOM y finales LF.
- Estado: Resuelto

### Tema 2: Consulta de repositorios y perfiles de base disponibles

- Solicitud o contexto: Jean preguntó qué repositorios y perfiles de base de datos estaban disponibles.
- Respuesta o acciones del agente: Se leyó `REPOSITORIOS.md` y `DB_PROFILE.md`.
- Resultado: Se listaron los repositorios SVN registrados (`Manuales-Proyecto-Estandar`, `estandar-trunk`, `Incidentes`, `abiondi-V25.08.001`) y perfiles de base con conexión OK. Más tarde se incluyeron también los perfiles agregados durante el día, `Premedic_prod` y `Sanar_prod`.
- Estado: Informativo

### Tema 3: Análisis InvGate 6014 en celer_prod

- Solicitud o contexto: Jean pidió analizar el ticket InvGate `6014`, guardarlo en su carpeta con nombre `S6014` y usar el perfil `celer_prod`. Luego aclaró que el comprobante indicado por el cliente no existía en esa base y que necesitaba identificar exactamente el parámetro a revisar.
- Respuesta o acciones del agente: Se generó y luego actualizó el Word `/mnt/Agentes/jpinto/S6014/analisis_ticket_6014.docx`.
- Resultado: Se indicó la limitación de que el comprobante no existía en `celer_prod`. Se reforzó que el parámetro a revisar es `DERIV_APORTES_SISTEMA_DET.meses_estimacion`, visible como “Meses de estim.” en el detalle del sistema de derivación de aportes para `RGA`; en `celer_prod` estaba en `2`. Jean confirmó con captura y luego informó que enviaría la respuesta al cliente.
- Estado: Resuelto

### Tema 4: Alta de profile Premedic_prod y consulta de versión

- Solicitud o contexto: Jean pidió agregar el profile `Premedic_prod` para la base `Premedic_prod` en `172.16.10.108`, reutilizando las credenciales de `medimas_prod`, y luego consultar la versión desde `zz_sistema`.
- Respuesta o acciones del agente: Se validó conexión efectiva y se actualizó `DB_PROFILE.md`.
- Resultado: Se registró `Premedic_prod` con estado OK; `SELECT DB_NAME()` devolvió `Premedic_Prod`. La versión leída en `zz_sistema.version` fue `25.03.003`.
- Estado: Resuelto

### Tema 5: Validación de token en Premedic_prod para auto_remotas 10275747

- Solicitud o contexto: Jean pidió analizar en `Premedic_prod` la transacción `auto_remotas.transac = 10275747`, tomar el token y verificar si estaba vigente en `afi_token` para el afiliado a la fecha/hora de la transacción.
- Respuesta o acciones del agente: Se cruzó `auto_remotas`, `afiliados` y `afi_token`.
- Resultado: Se clasificó como posible inconsistencia operativa/dato de autorización. La transacción correspondía a afiliada `FENOGLIETTO, MARIELA PAULA`, documento `28023665`, token grabado `198`, con hora real `2026-07-28 10:16:00`. Para ese documento había 40 tokens históricos, pero ningún registro con token `198`, ni dentro ni fuera del rango horario. Jean confirmó que la última consulta era la indicada para su respuesta.
- Estado: Resuelto

### Tema 6: Registro de tiempo dedicado en Redmine

- Solicitud o contexto: Jean consultó si era posible sumar tiempo dedicado en Redmine y luego pidió registrar 1 hora en el Redmine `2174`, comentario `prueba de ingreso de horas (JG)`, actividad `Gestión`.
- Respuesta o acciones del agente: Se validó el pedido y se registró el tiempo a nombre de Jean usando su credencial Redmine registrada.
- Resultado: Se registró `1.0` hora el `2026-07-29`, actividad `Gestión`, comentario indicado, con ID de registro `394205`.
- Estado: Resuelto

### Tema 7: Alta de profile Sanar_prod y consulta de versión

- Solicitud o contexto: Jean pidió agregar el profile `Sanar_prod` para la base `sanar_prod` en `172.16.10.108`, reutilizando credenciales de `medimas_prod`, y consultar la versión desde `zz_sistema`.
- Respuesta o acciones del agente: Se actualizó `DB_PROFILE.md`, se validó conexión efectiva y se leyó `zz_sistema`.
- Resultado: Se registró `Sanar_prod` con estado OK; `SELECT DB_NAME()` devolvió `SANAR_Prod`. La versión leída fue `25.03.004`.
- Estado: Resuelto

### Tema 8: Análisis InvGate 6011 en sanar_prod y corrección de uso de credenciales SVN

- Solicitud o contexto: Jean pidió analizar el ticket InvGate `6011`, guardarlo en `/mnt/Agentes/jpinto/S6011`, usar `sanar_prod`, considerar que la base podía no estar actualizada y analizar según versión `25.03.004` o `25.03.005`. Luego aclaró que el `svn update` debía usar las credenciales indicadas en `REPOSITORIOS.md`.
- Respuesta o acciones del agente: Al primer intento, el análisis quedó bloqueado por error de autenticación SVN porque se había intentado usar credencial/cache incorrecto. Tras la aclaración, se corrigió el criterio, se actualizó `estandar-trunk` con la configuración SVN registrada y se generó el análisis.
- Resultado: Se creó `/mnt/Agentes/jpinto/S6011/analisis_ticket_6011.docx`. La limitación documentada fue que en `sanar_prod` no estaban las autorizaciones del ejemplo `56468`, `56469` y `56472`; se validó la versión `25.03.004`, parametrización relacionada y código vigente en `estandar-trunk` actualizado a revisión `84188`. Se intentó enviar un aviso por Google Chat directo a Jean, pero la API respondió `403 PERMISSION_DENIED`; el mensaje quedó entregado en la conversación actual.
- Estado: Resuelto
