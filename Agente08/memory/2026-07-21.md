# 2026-07-21

## InvGate ticket 5968 analysis
- User Andre requested: "analiza el ticket 5968 usando la base roisa_prod y deja tu analisis en un documento en mi carpeta de invgate".
- Completed analysis document created at `/mnt/Agentes/abiondi/invgate/analisis_ticket_5968.docx`.
- Analysis used ticket data/comments, attached Word document, embedded screenshots/images, source references, and read-only database checks against `roisa_prod`.
- Important limitations recorded in the document and communicated to Andre: unable to inspect the `.mp4` video frame by frame or extract the `.7z` attachment because the environment lacked `ffmpeg/ffprobe` and 7z-capable tooling/libraries.
- Final response to Andre stated the document path and those limitations.

## Database profile mutualrioiv_prod
- Andre asked whether `mutualriov_prod` existed; no profile by that name was found in `DB_PROFILE.md`.
- Andre then provided credentials for `mutualrioiv_prod` and requested registration.
- Registered profile `mutualrioiv_prod` in `/home/desarrollo-ia-08/.openclaw/workspace/DB_PROFILE.md`.
- Connection validation succeeded (`Estado de conexión: OK`).
- Profile details: MS SQL, host `172.16.10.108`, port `1433`, database `mutualrioiv_prod`, user `info1`, notes `mutualrioiv_prod`.

## InvGate ticket 5944 analysis
- Andre requested: "analizao el ticket 5944 usando es base y deja tu analsiis en un documento en mi carpeta invgate" after registering `mutualrioiv_prod`.
- Completed analysis document created at `/mnt/Agentes/abiondi/invgate/analisis_ticket_5944.docx`.
- The `mutualrioiv_prod` SQL user allowed read access to data and metadata, but did not allow viewing productive stored procedure definitions.
- Code review for the ticket was done against the updated local SVN working copy `estandar-trunk`.
- Final response to Andre stated the document path and this limitation.

## Authorization attachment preview types, version 25.08.001
- Andre asked which authorization attachment file types can be uploaded for lateral preview in version `25.08.001`.
- Answer given: for the side preview in that authorization window, accepted formats are `PDF`, `JPG`, `JPEG`, `BMP`, `XML`, and `PNG`.
- Evidence cited: `/home/desarrollo-ia-08/.openclaw/workspace/svn/estandar-trunk/exe/Source Infomedical/wp_solici.srw` around line `27537`, where extension validation shows "Formato de archivo no compatible con vista previa." for unsupported formats.
- Important nuance: reusable viewer `/home/desarrollo-ia-08/.openclaw/workspace/svn/estandar-trunk/exe/Source Infomedical/uo_visor_archivos.sru` around line `80` also supports `DOC`, `DOCX`, `XLSX`, `TXT`, and `RTF`, but `wp_solici.srw` filters them out before the lateral preview in this window.

## Nicolas repository updates and Portal odontologia analysis
- Nicolas asked to update repositories. First request mentioned `incidentes` and `exe`; `incidentes` was validated as `abiondi-Incidentes-Incidentes`, but `exe` was not registered in `REPOSITORIOS.md` nor as a local SVN working copy, so no partial update was executed.
- Nicolas then asked to update `INCIDENTES` Redmine and `trunk`. Both were validated as SVN working copies with local config/credentials before update. `INCIDENTES` updated to revision `20355`; `trunk` was already current at revision `84089`.
- Nicolas asked whether Portal del Prestador Redmine `26274` included, in transaction `01A` (registracion de realizaciones), the ability to input odontological `pieza` and `cara` by window.
- Conclusion given: in current Portal source, `01A` does not include UI or request parameters for odontological pieza/cara/sector. Classified as `Nuevo desarrollo requerido` unless functionally confirmed that `01A` was in the accepted scope of Redmine `26274`, in which case it would be an omission.
- Evidence for `01A`: Redmine `26274` title was "Portal de Prestadores: Transaccionar autorizaciones odontologia" and document R26274 mentions pieza/seccion/cara for odontologia, explicitly referencing `02C` and `02D` but no direct mention of `01A`; `webpos/01A.php` only loads header, provider, and affiliate; `webpos/frames/frame_prestacion.php` exposes only codigo/nomenclador/descripcion/cantidad; `webpos/js/transac.js`, `webpos/controlador/transaccionar.php`, and `webpos/modelo/transaccion.php` send/receive only `listaNomen`, `listaPrestac`, and `listaCanti`; no functional `pieza`, `cara`, or `odont` logic was found in the `01A` web flow.
- Nicolas then asked whether Portal transactions `02A` and `02D` identify odontological prestations and allow entering pieza/cara/sector.
- Conclusion given: no. The Portal window for `02A`/`02D` does not visually identify prestations as odontological and does not allow entering pieza, cara, or sector.
- Evidence for `02A`/`02D`: `02A.php` and `02D.php` load `framePrestacion(true)`; `frame_prestacion.php` only provides codigo/nomenclador/descripcion/cantidad; `transac.js` only submits `listaNomen`, `listaPrestac`, `listaCanti`; `transaccionar.php` receives only those lists; `pri_transac_webpos_total.sql` has no pieza/cara/sector inputs; `pri_auto_prestac_items_remotas.sql` has `@prm_pieza` and `@prm_cara` and can map sector, but Portal does not send them, so defaults are `pieza = 0` and `cara = '0'`; `pri_transac_remotas.sql` validates odontological logic for `02A` and `02D` using values already stored in `AUTO_PRESTAC_ITEMS_REMOTAS`.
