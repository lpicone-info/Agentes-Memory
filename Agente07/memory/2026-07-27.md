
## 2026-07-27 17:33 ART - Conversacion tecnica con Pato

- Usuario identificado en conversacion como Pato. Prefiere respuestas tecnicas directas, con referencias a archivos/lineas cuando se analiza codigo.
- Contexto Afilmed Web, repo SVN `svn/mpariso-V25.08.001`: en Precarga, el flujo de alta/edicion desde `ResumenPrecarga` muestra confirmacion antes de ejecutar validaciones fuertes del backend. Luego llama `createPrecarga`, que ejecuta `cru_genera_precarga`; este SP valida con `crs_precarga_solicitud_validar`, hace rollback y devuelve errores si corresponde. Si corresponde generacion directa, despues se llama `createSolicitud`/`cru_genera_solicitud`.
- En Precarga Afilmed Web no todo el modulo maneja igual el flujo de errores: resumen final concentra errores de impacto en `ResumenPrecargaResultado`; `CargaIntegrante` usa `validateIntegrante`, errores de campo y advertencias tipo `W` con confirmacion; adjuntos/certificados/preexistencias usan mayormente Formik/Yup; consulta/aprobacion valida adjuntos antes de preguntar y genera solicitud despues; errores tecnicos de API pasan por `useApiRequest` con alerta global/logout.
- Pantallas principales de Precarga que comparten el resumen: `Afiliacion.jsx`, `Incorporacion.jsx`, `Personales.jsx`, `Domicilio.jsx`. Consulta/aprobacion usa otro flujo en `TablaFilaPrecarga.jsx`.
- Contexto Presmed productivo, repo `svn/mpariso-Infomedical-trunk`: en item de autorizacion, "cantidad a consumir" significa unidades que se intentan imputar ahora; "saldo" significa unidades disponibles restantes, no importe monetario. Logica ubicada en `backend/procedures/pri_transac_remotas.sql` alrededor de linea 829: `saldo = cant_autorizadas - cant_remotas - cant_expe`. En farmacia/productos se descuenta saldo al aceptar en `pri_transac_remotas_farm_items.sql` alrededor de linea 432.
