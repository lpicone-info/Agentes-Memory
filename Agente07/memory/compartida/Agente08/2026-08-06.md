# Memoria 2026-08-06

- Usuario: Javier. Prefiere respuestas tecnicas precisas, con evidencia y separando criterios cuando corrige una consulta.
- Consulta trabajada sobre `estandar-trunk` y base `Mutualrioiv_prod`: reportes que muestran expedientes e informan cantidad de items.
- Primera validacion: reportes que muestran `expedientes.item_nro` como columna visible `Items` o `# Cargadas`: `Expedientes pendientes de carga`, `Expedientes a revisar para liquidar`, `Expedientes a auditar para liquidar`, `Cierre Administrativo`, `Expedientes cerrados (ambulatorio)`, `Expedientes cerrados y supervisados pendientes de liquidacion`, `Expedientes a revisar (Odontologia)`, `Expedientes cerrados (Odontologia)`, `Expedientes por prestador` (odontologia, columna `# Cargadas`). Se excluyeron reportes con `item_nro` oculto o sin encabezado visible.
- Consulta ajustada de Javier: necesitaba reportes que muestren la cantidad de items de un expediente calculandola al momento de emitir el reporte, usando `estandar-trunk` y `Mutualrioiv_prod`.
- Resultado validado: un solo reporte cumple estrictamente: `Expedientes en curso`; menu `Liquidaciones > Consulta de expedientes en curso`; ventana `wp_consexpencurso`; DataWindow `d_expe_encurso`; procedure `prs_consexpencurso`.
- Evidencia del resultado ajustado: `d_expe_encurso` muestra columna `Items`, pero no toma directo `expedientes.item_nro`; para expedientes de farmacia (`tipo = 'F'`) el procedure `prs_consexpencurso` calcula el valor al recuperar el reporte con `COUNT(*)` sobre `expe_farma_recetas`, filtrando por `ofi`, `expe` y `baja_fecha IS NULL`. En `Mutualrioiv_prod` se confirmo existencia de `dbo.prs_consexpencurso`, `expedientes` y `expe_farma_recetas`.
# Memoria 2026-08-06

- Usuario: Javier. Prefiere respuestas tecnicas precisas, con evidencia y separando criterios cuando corrige una consulta.
- Consulta trabajada sobre `estandar-trunk` y base `Mutualrioiv_prod`: reportes que muestran expedientes e informan cantidad de items.
- Primera validacion: reportes que muestran `expedientes.item_nro` como columna visible `Items` o `# Cargadas`: `Expedientes pendientes de carga`, `Expedientes a revisar para liquidar`, `Expedientes a auditar para liquidar`, `Cierre Administrativo`, `Expedientes cerrados (ambulatorio)`, `Expedientes cerrados y supervisados pendientes de liquidacion`, `Expedientes a revisar (Odontologia)`, `Expedientes cerrados (Odontologia)`, `Expedientes por prestador` (odontologia, columna `# Cargadas`). Se excluyeron reportes con `item_nro` oculto o sin encabezado visible.
- Consulta ajustada de Javier: necesitaba reportes que muestren la cantidad de items de un expediente calculandola al momento de emitir el reporte, usando `estandar-trunk` y `Mutualrioiv_prod`.
- Resultado validado: un solo reporte cumple estrictamente: `Expedientes en curso`; menu `Liquidaciones > Consulta de expedientes en curso`; ventana `wp_consexpencurso`; DataWindow `d_expe_encurso`; procedure `prs_consexpencurso`.
- Evidencia del resultado ajustado: `d_expe_encurso` muestra columna `Items`, pero no toma directo `expedientes.item_nro`; para expedientes de farmacia (`tipo = 'F'`) el procedure `prs_consexpencurso` calcula el valor al recuperar el reporte con `COUNT(*)` sobre `expe_farma_recetas`, filtrando por `ofi`, `expe` y `baja_fecha IS NULL`. En `Mutualrioiv_prod` se confirmo existencia de `dbo.prs_consexpencurso`, `expedientes` y `expe_farma_recetas`.

- Usuario en Google Chat directo identificado como Andre. Prefiere trato directo y analisis tecnico con evidencia.
- El 2026-08-06 se actualizaron repositorios SVN registrados: `Manuales-Proyecto-Estandar` rev. 47, `estandar-trunk` rev. 84261, `Incidentes` rev. 20619, `abiondi-V25.08.001` rev. 84261.
- Se registro profile DB `sos_prod_test` en `DB_PROFILE.md`: MS SQL, host `172.16.10.108`, puerto `1433`, base `sos_prod_test`, usuario `info1`, password informado por usuario, notas `sos_prod_test`. Conexion validada OK.
- Analisis solicitado por Andre: ticket InvGate 6057 usando base `sos_prod_test` y rama `estandar-trunk`, dejando documento en carpeta del usuario. Documento generado: `/mnt/Agentes/abiondi/analisis_ticket_6057.docx`.
- Hallazgo del ticket 6057: en `sos_prod_test` existe el parametro `liquida_efector_diferente = S`, pero no se encontro el expediente `31350` ni la autorizacion `246461` en las tablas consultadas, por lo que no se pudo reproducir exactamente con esos IDs en esa base.
- Punto funcional clave del ticket 6057: para odontologia, al cargar una autorizacion, el sistema copia el efector de la autorizacion a `efe_prestad` del expediente (`wp_expe_odon.srw`, alrededor de linea 686). Sin embargo, para calcular valor convenido arma `lstr_val.expe_prestad = i_expe.prestad` (`wp_expe_odon.srw`, alrededor de linea 1854) y luego `of_calcula_valor_convenido` llama a `val.of_convenio(...)` con `a_str.expe_prestad` (`uo_nv_liqui_odon.sru`, alrededor de linea 879). Conclusion comunicada: si se permite liquidar autorizacion de otro efector, la autorizacion actua como respaldo/origen de prestaciones y puede informar efector actuante, pero la valorizacion usa el convenio/prestador del expediente, no el efector de la autorizacion.

- Usuario en Google Chat directo: Ro. Prefiere respuestas tecnicas concretas, orientadas a diferencia entre requerimiento, evidencia en base/codigo y accion recomendada.
- Analisis R30188 / OSPECON_CONTA: se reviso `R30188.docx`, captura/mensaje nuevo y expediente `60` en base `OSPECON_CONTA`. Clasificacion comunicada: Error / inconsistencia funcional.
- Hallazgos R30188: `expedientes.total = 36200`; suma vigente de `expe_orden_items.real_valor = 35000`; `expedientes.debito = 1200`; `expedientes.debi_moti = Copagos p/orden:1200.00`; `expe_copagos.valor = 1200`, `pago_copa = 'P'`. El sistema estaba tratando el copago por orden como leyenda/debito global de 1200, pero no bajaba el valor reconocido/capturado a 33800.
- Conclusion R30188 comunicada: el pedido no debe quedar como parche visual de resumen `Debito global + copago por orden = 2400`; funcionalmente Presmed debe descontar automaticamente el copago por orden al prestador cuando tenga forma de pago al prestador. Calculo esperado: comprobante 36200, convenido original 35000, copago por orden 1200, costo a reconocer 33800, debito global adicional 1200, total a descontar 2400, neto al prestador 33800.
- Recomendacion R30188: modificar el requerimiento para indicar descuento automatico del copago por orden del importe a reconocer/liquidar, sin decision manual del usuario en cierre; ajustar resumen como consecuencia del calculo correcto; validar impacto en cierre de expediente, resumen por prestador, generacion de debitos y asiento contable. Probable impacto tecnico: calculo/grabacion de expediente de ordenes y resumen `prs_expe_orde_resum` / `d_prs_expe_orden_resum`; no alcanza con tocar solo reporte.
- Analisis TK 6058 OSPANA/OSPAÑA: consulta de Ro sobre si parametro permite facturar con numero de pasaporte. Clasificacion comunicada: Configuracion.
- Hallazgo TK 6058: parametro `Afilmed / Facturacion Electronica / Tipo Doc 96 = S` en fuente estandar hace que para comprobantes AFIP tipo `6/7/8` (Factura/ND/NC B) y total mayor o igual al limite configurado, se envie a AFIP `DocTipo = 96` y `DocNro = 88888888888`.
- Conclusion TK 6058 comunicada: el parametro evita rechazos AFIP error `10015` cuando el sistema enviaba `DocTipo 80` como CUIT con numeros tipo `44444445` o `11111112` que no son CUIT validos. No factura con numero de pasaporte literal; factura como tipo doc. 96 con numero generico AFIP `88888888888`, que para el caso es lo esperado.
- Recomendacion TK 6058: cambiar `Tipo Doc 96` de `N` a `S`, cerrar y reabrir ventana/proceso de solicitud de CAE, reintentar comprobantes rechazados, y no modificar el tipo de documento del afiliado ni cargar esos numeros como CUIT. No se pudo confirmar valor actual en base porque `Ospana_Prod` no figuraba registrado en `DB_PROFILE.md`.

## 2026-08-06 11:46 ART - Memoria operativa

- Usuario frecuente: Vale / vesaravia. Prefiere respuestas técnicas directas para incidencias InvGate, con clasificación y conclusión accionable.
- Caso ROISA_PROD / Doctor Flex: se respondió que existe el reporte `Facturación > Facturación detallada por período/sucursal`, pero no permite aislar confiablemente el grupo de emisión DOCTOR FLEX porque el filtro de grupo está oculto o no se pasa al retrieve. Para consultar todo lo facturado de ese grupo corresponde base de datos filtrando `comprobantes.emi_grupo = 11`. En validación previa no había comprobantes ni impresiones con `emi_grupo = 11`, aunque sí cabecera en `prefacturas_cab` para julio 2026.
- Odontología / campo AUTO: clasificación Configuración. El campo `AUTO` se muestra/oculta con `glob.gb_odon_carg_auto`, cargado desde preferencia `setodon`, clave `carg_auto`. En el sistema se habilita desde `Odontología > Expedientes > Carga de prestaciones`, solapa `Opciones`, check `Autorizaciones`; también muestra `Suc`. Si aparece mensaje "El prestador de la orden no se corresponde con el expediente", el código limpia `auto` porque la autorización tiene efector/prestador distinto al del expediente.
- TK 6060 / SMS_PROD: contexto aportado por Vale: cliente sufrió virus y necesita reinstalar todo; aunque ya figure Portal del Prestador o WebPOS, el pedido debe interpretarse como reinstalación. Se aceptó continuar sin validación de base porque no había profile `sms_prod` en `DB_PROFILE.md`. Carpeta de entrega indicada: `/mnt/Agentes/vesaravia/`.
- Resultado TK 6060: generado Word `/mnt/Agentes/vesaravia/analisis_ticket_6060.docx`. Conclusión comunicada: `Configuración / soporte de reinstalación`, no error ni nuevo desarrollo. Se validó InvGate, comentarios y adjuntos; no tenía comentarios ni archivos/imágenes embebidas. Se actualizó `estandar-trunk` a revisión `84262` y se revisaron fuentes/manuales. Sin validación de base por indicación del usuario.

- Pendiente indicado por Ro: mantener a mano el análisis del documento `R30188` junto con la captura de ventana/mensaje nuevo. Ro lo volverá a pedir de forma clara; al retomarlo, usar la evidencia ya registrada de `OSPECON_CONTA`, expediente `60`, copago por orden `1200`, débito global `1200`, total a descontar `2400` y conclusión de que no alcanza con parche visual de resumen sino que debe descontarse automáticamente el copago por orden del importe reconocido/liquidado al prestador.

## 2026-08-06 13:17 ART - Memoria operativa

- Andre consulto el origen del parametro `liquida_efector_diferente`. Se respondio que el alta tecnica aparece en Redmine `23589`, archivo `backend/scripts_temporales/R23589_parametros_generales.sql`, revision SVN `75541`, log `Tarea #23589 - Ajuste proceso de Pre Facturacion`, creando `Presmed / Autorizaciones / liquida_efector_diferente`. Luego Andre aclaro correctamente que ese Redmine era de Afilmed, no Presmed; conclusion ajustada: `23589` es solo el origen tecnico del script/alta historica del parametro, pero funcionalmente no parece ser antecedente del incidente Presmed. Para el incidente actual tienen relacion directa los cambios recientes `30136` y defecto/reportado `30192`.
- Analisis solicitado por Andre sobre prorrogas de internacion, rama `25.08.001` (`abiondi-V25.08.001`) y base `ospecon_prod`: documento generado en `/mnt/Agentes/abiondi/analisis_prorrogas_internacion_circuito_bandejas.docx`. Conclusion: las prorrogas de internacion (`tipo R`, `prorro = S`) no pasan por circuito de bandejas en esa rama. Aunque en `ospecon_prod` exista circuito configurado para `R`, el codigo excluye explicitamente `R` al obtener/grabar `id_circuito`. Si asigna `sector_actual = Carga Inicial` y usa sectores codificados en `auto_salas`, pero eso corresponde a logica de dias/sectores de internacion, no al circuito de bandejas.
- Analisis solicitado por Andre sobre documento `/mnt/Agentes/abiondi/documentos/no pasa a alephoo.docx`, rama `25.08.001` y base `ospecon_prod`: se actualizo el mismo documento con el analisis. Autorizacion analizada: `512`. Conclusion principal: no se informa copago a Alephoo porque la cabecera de `AUTORIZACIONES` esta en estado `R` rechazada. Aunque los items esten aceptados y tengan copago adelantado, la validacion exige `AUTORIZACIONES.esta = 'A'` y `fecha_cierre` informada.
- En el analisis Alephoo se confirmo a Andre que no tiene que ver con que el efector sea un circulo ni con que no se haya indicado categoria en `Circ/Categ`: el SP de Alephoo no valida `efe_circu` ni categoria. Solo excluye si efector/lugar pertenece al CEMAP parametrizado; para efector `32` no se encontro relacion activa con CEMAP `722`.
- Condiciones documentadas para informar copago a Alephoo: autorizacion con cabecera aceptada (`AUTORIZACIONES.esta = 'A'`), `fecha_cierre` informada, items aceptados/con copago segun reglas del SP, no excluida por CEMAP parametrizado, y cumplir filtros propios del procedimiento de interfaz Alephoo revisado en rama `25.08.001`.

## 2026-08-06 13:51 ART - Memoria operativa

- Andre pidio profundizar el analisis del documento `/mnt/Agentes/abiondi/documentos/no pasa a alephoo.docx` para determinar si el estado `R` de cabecera de la autorizacion `512` se genero por no estar cargada la categoria en la autorizacion.
- Se actualizo el mismo documento `no pasa a alephoo.docx` con el nuevo analisis y se valido el DOCX con `unzip -t`.
- Conclusion comunicada: si, para la autorizacion `512` el estado general `R` de cabecera esta asociado a que el efector es un circulo y la autorizacion quedo sin categoria/circulo cargado (`efe_circu = NULL`). La validacion previa a Alephoo marca la remota vigente con `recha = 733` ("Circulo sin categorias habilitadas") y `esta = R`, y luego eso impacta en `AUTORIZACIONES`.
- Aclaracion importante comunicada: el prestador/efector `32` si tiene categorias activas (`A`, `B`, `C`); el problema no es falta de configuracion de categorias, sino que no quedo seleccionada/grabada la categoria en la autorizacion.

- Pendiente indicado por Ro: mantener a mano el tema TK `6064` / `OSPECON_Prod` sobre robots AFIP/ARCA y Alfabeta para volver a verlo. Hallazgos ya comunicados: clasificacion `Configuracion / entorno de ejecucion de robots`; AFIP/ARCA informado en ticket como funcionando; Alfabeta informado como fallando; ambos sin envio de mails. Se revisaron adjuntos InvGate: DOCX con captura de Alfabeta y properties AFIP/Alfabeta. Captura Alfabeta mostraba error de Chrome/Google APIs `DEPRECATED_ENDPOINT`, no un error funcional concluyente de Alfabeta. Fuentes `estandar-trunk` rev. `84265`: ambos robots usan `webdriver.Chrome`, no navegador predeterminado. Codigo de mail en ambos robots siempre ejecuta `server.login(...)`; servidor configurado `rsuocra.mail.protection.outlook.com:25` respondio SMTP y STARTTLS, pero no anuncio `AUTH`, por lo que el login no es compatible con ese servidor. Proximos puntos si se retoma: correr Alfabeta con `debug=1`, obtener error real del script, validar Chrome/ChromeDriver en servidor productivo y definir si mail se envia por relay sin login o por SMTP autenticado con configuracion compatible.

- Pendiente para mañana indicado por Ro: mantener el cierre del TK `6061` / `MUTUAL_Prod` afiliado `603326`. Punto actual del ticket: el cliente pregunta si una modificacion de otra indole pudo invalidar el estimado, porque Paola indica que solo cargo el estimado el `01/07` y no volvio a tocar ese item. Evidencia validada: solicitud `28857` (`Cambio de Deriv. de Aportes`) del `01/07/2026`, usuario `delucapao`, sueldo `1.833.430,20`, genero correctamente aporte estimado julio `126.140,00`; solicitud `29135` (`Incorporaciones`) del `20/07/2026`, usuario `ORONAR`, acciones `Alta Solicitud`, `Auditoria Medica`, `Aceptacion Item`, `Pasaje a Afiliados`, llevo derivacion con sueldo `0,01`; solicitudes `29151` (`Baja familiares`) y `29152` (`Incorporaciones`) del `21/07/2026`, usuario `ORONAR`, tambien arrastran sueldo `0,01`. Respuesta sugerida: una modificacion cualquiera no invalida el estimado ya generado, pero si la solicitud incluye/arrastra derivacion de aportes y se pasa a afiliados, puede actualizar el dato vigente; por eso julio quedo correcto y agosto tomo sueldo vigente `0,01`, sin generar aporte.
