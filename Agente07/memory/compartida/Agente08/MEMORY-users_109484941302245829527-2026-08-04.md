# Memoria curada diaria

- Fecha: 2026-08-04
- Usuario: Javier Dermit
- id_googlechat: users/109484941302245829527
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/rlilwkaaaae

## Resumen de la actividad

### Tema 1: Columna item_nro en exportación de reporte en MutualRIOIV_PROD

- Solicitud o contexto: El usuario pidió verificar qué dato contiene la columna `item_nro` al exportar a Excel un reporte, usando el profile `MutualRIOIV_PROD`.
- Respuesta o acciones del agente: Se identificó que el dato sale del procedimiento `dbo.prs_consexpliquidados` y se toma de `EXPEDIENTES.item_nro`. Se contrastó con expedientes y órdenes del rango visible en la imagen.
- Resultado: Se explicó que `item_nro` es el contador/número de ítems del expediente en cabecera. Para expedientes tipo orden, puede coincidir con la numeración de ítems activos, aunque con bajas puede representar la numeración alcanzada y no necesariamente la cantidad activa.
- Estado: Resuelto

### Tema 2: Cartel de validación de prestador/expediente y parámetros en MutualRioIV_Prod

- Solicitud o contexto: El usuario envió una imagen con un cartel y pidió verificar si existe un parámetro para evitar ese control.
- Respuesta o acciones del agente: Se localizó el mensaje en `wp_expe_orde.srw`, bloque `R28808`, y se verificó que la pantalla llama a `wf_valida_prestador`, que ejecuta `dbo.prs_valida_lugares_x_prestador_circulo`.
- Resultado: Se concluyó que no hay un parámetro que evite ese cartel específico. Existe `Presmed / Autorizaciones / liquida_efector_diferente = N`, pero se lee después de ese control y no lo evita. La salida funcional es corregir/configurar la relación prestador/lugar/círculo, o desarrollar una parametrización nueva.
- Estado: Resuelto

### Tema 3: Tablas SQL donde se registran débitos de expedientes en MutualRIOIV_PROD

- Solicitud o contexto: El usuario preguntó en qué tabla SQL se graban los débitos de expedientes, tomando el profile `MutualRIOIV_PROD`.
- Respuesta o acciones del agente: Se revisaron tablas y datos activos en la base.
- Resultado: Se informó que el detalle principal se registra en `dbo.expe_errores`, con campos como `ofi`, `expe`, `item`, `orden`, `recha`, `real_valor`, `esta`, `vali` y `baja_fecha`. Se indicó que los reportes de débitos al prestador filtran típicamente `vali = 'S'`, `esta = 'P'`, `baja_fecha IS NULL` y `real_valor <> 0`. También se mencionaron totales consolidados en `dbo.EXPEDIENTES` y tablas específicas sin registros activos relevantes.
- Estado: Resuelto
