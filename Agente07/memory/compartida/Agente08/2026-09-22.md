# 2026-09-22

## Conversacion con Ro - analisis odonto / tope en liquidaciones

- Usuario: Ro / rcassandro. Canal: Google Chat directo.
- Tema analizado: diferencia entre mensaje de tope/autorizaciones y mensaje mostrado en liquidaciones para odontologia.
- Conclusión inicial entregada: el mensaje `Prestacion excede la cantidad permitida` mostrado en liquidaciones no corresponde al tope. Corresponde al rechazo `249`, asociado a cantidad maxima permitida por prestacion odontologica.
- Evidencia usada:
  - En `IgualdadSalud_Prod`, expediente `2785` tenia rechazo `249` registrado en `expe_errores` para `ofi=1`, `expe=2785`, `item=1`, `orden=1`.
  - Prestacion cargada: `O0101`, cantidad `4`.
  - En `odon_prestac`, para `nomen=3`, `prestac=O0101`, `cant_max=1`.
  - En `prs_valida_of_odon.sql`, el rechazo `249` se genera cuando `@cant > @cant_max`.
  - Mensaje especifico de tope identificado: `recha 29` = `El afiliado alcanzo el tope y el mismo podra generar un copago al liquidar`.
  - No se encontro registro en `expe_topes` para ese expediente.
- Documento solicitado por Ro: revisar `mail nb odonto` en `A:\rcassandro\Analisis varios` y generar documento indicando si corresponde a error, motivo o comportamiento correcto.
- Documento generado: `/mnt/Agentes/rcassandro/Analisis varios/Analisis_mail_nb_odonto_tope_liquidacion.docx`.
- Conclusión del documento: no surge como error a ajustar. El `recha 29` esta tratado por codigo como aviso de tope/copago y no genera registro en `expe_errores` al pasar a liquidacion. Si liquidaciones debe mostrarlo explicitamente, corresponde a mejora funcional de visualizacion, no bug directo.
- Hipotesis adicional de Ro: posible diferencia por uso de letra `O` en autorizaciones/definicion de planes y sin `O` en liquidacion.
- Verificacion agregada al documento: no parece ser la causa. El codigo de odontologia contempla la normalizacion: usa la prestacion con `O` cuando viene de autorizacion o tabla de prestaciones, y para liquidacion la guarda/muestra sin la letra usando `mid(..., 2)`. Ejemplo: `O0102` -> `0102`.
- Matiz asentado: solo podria ser causa si existiera configuracion puntual mal grabada en base con una forma y el proceso buscara la otra, pero el codigo revisado no explica el caso principal por esa diferencia.

## Conversacion con Andre - Montegrande, OSPECON AYD e importacion ISSS

- Usuario: Andre / abiondi. Canal: Google Chat directo.
- Profile DB confirmado por Andre: `Montegrande_Prod` para servidor `172.16.10.120`; el usuario lo escribio como `montegrande_prod`.
- Caso Montegrande deuda informada no sumada al vencimiento 1:
  - Se encontro ejemplo en `Montegrande_Prod`: comprobante `FC A002000015227`, contrato `1007064`, fecha comprobante `01/08/2024`, periodo `09/2024`, vencimiento 1 `01/10/2024`.
  - `compro_impresion.deuda1 = 66070.70`, `impor1 = 2054.31`, `comprobantes.total = 2054.31`; los items suman `2054.31` (`ADI`, `IVA`, `IBB`).
  - En septiembre 2024 habia 2128 comprobantes con `deuda1 > 0` e `impor1 = total`. Clasificacion preliminar dada: configuracion/regla de facturacion a validar; para error hay que revisar rutina que arma `compro_impresion`.
- Planes Montegrande con facturacion de valor de cuota:
  - Interpretacion usada: concepto `CUO - Cuota` en `prefacturas`.
  - Planes listados: `A C/O`, `C.DOR.PLUS`, `C.PLATEADO`, `CLASS`, `FIRST`, `INTEGRAL`, `ORO`, `ORO 2.2`, `OS PMO`, `OSPM COORP`, `OSPM ORO`, `OSPM PLATA`, `OSPM-C`, `OSPM-D`, `OSPM-F`, `OSPMORO2.2`, `OSPM-P`, `OSPMPLA2.2`, `PAMA PLUS`, `PLATA`, `PLATA 2.2`, `PRODONTO`, `ProMedAm`, `PROMI 9 C`, `TOP 04`.
  - No se encontro configuracion activa en `plan_adic_factu_concep` para `CUO`; si registros reales en `prefacturas.factu_concep = 'CUO'`.
- Configuracion de codigo de barras Montegrande:
  - `entes.cpagofacil` nullable; para ente `1` estaba `NULL`.
  - Significado: codigo de empresa/convenio usado para armar barras Pago Facil / SIRO / Rapipago segun rutina. Si llega `NULL`, algunas rutinas lo convierten en `'0'` o `''` y completan con ceros.
  - Para elegir barra: `prepagas.barra_ente_rec = NULL` cae por defecto en `PF`; `barra_ente_rec = 0` no genera barra (`NG`); si apunta a `ENTE_RECAUDADOR`, usa `ENTE_RECAUDADOR.tipo_barra`.
  - Para Pago Facil: `entes.cpagofacil` debe contener codigo de empresa/servicio Pago Facil, normalmente 3 digitos.
  - Para SIRO cerrado: `ENTE_RECAUDADOR.tipo_barra = 'SC'` y `entes.cpagofacil` contiene codigo de empresa SIRO/Banco Roela de 4 digitos; el codigo documenta ejemplo `0447`. Se aclaro que `0448` podria ser SIRO si fue otorgado por Banco Roela/SIRO, pero el sistema no valida ese valor especifico.
- OSPECON AYD / rama `25.08.001` / importacion ISSS:
  - Pantalla/importacion de `Novedadessss.txt` guarda carga operativa en `afi_importaciones_isss` mediante SP `afi_cargar_reg_archivo_sss` y calcula operaciones con `afu_cod_oper_isss`.
  - Guarda detalle historico por lote en `isss_novedades_sssalud_items`, cargado al procesar mediante `afi_isss_novedades_sssalud`.
  - Cabecera/lote se guarda en `ISSS_NOVEDADES_SSSALUD_CAB` (campos principales: `lote`, `ref`, `periodo`, `alta_fecha`, `revisado`, `prepaga`, `tipo_importacion`, `hash`).
  - En `ospecon_ayd` se verifico lote `1`: `ref = JUNIO`, `periodo = 06/2026`, `alta_fecha = 12/08/2026 10:01`, `revisado = N`, `prepaga = 1`, `tipo_importacion = N`; `isss_lotes` existia pero vacia.
- Analisis solicitado por Andre: por que una baja de integrante procesada correctamente no genera solicitud y el afiliado sigue activo, usando `ospecon_ayd` y rama `25.08.001`.
  - Documentos generados en carpeta de Andre: `/mnt/Agentes/abiondi/analisis_ospecon_ayd_lote2_baja_integrante_101793_03.docx` y `/mnt/Agentes/abiondi/analisis_ospecon_ayd_lote2_baja_integrante_101793_03.md`.
  - Conclusion: `afd_afiliados_novedades_isss` busca afiliado por CUIL/documento/contrato y encuentra 2 filas para la misma persona (`101793/03` activo y `101793/04` historico dado de baja). Como el SP exige `@filas = 1`, no entra al bloque que crea la solicitud de baja, pero tampoco informa error. El proceso queda como terminado OK, `revisado = S`, `soli = NULL`, y el afiliado activo permanece activo.
