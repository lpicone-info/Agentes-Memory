# Memoria curada diaria

- Fecha: 2026-07-28
- Usuario: Atencion al Cliente
- id_googlechat: users/113373607595606911178
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/8rjymkaaaae

## Resumen de la actividad

### Tema 1: Inicio de sesion y carga de preferencias

- Solicitud o contexto: Atencion al Cliente saludo por Google Chat directo.
- Respuesta o acciones del agente: Se ejecuto el bootstrap operativo, se identifico el usuario como Andrea/Andre en `USUARIOS.md` y se leyo `/mnt/Agentes/abiondi/preferencias/preferencias.md`.
- Resultado: Se cargaron preferencias y se respondio el saludo inicial como Andre.
- Estado: Resuelto

### Tema 2: Actualizacion de repositorios

- Solicitud o contexto: Andre pidio actualizar todos sus repositorios.
- Respuesta o acciones del agente: Se validaron y actualizaron las working copies SVN registradas para su operatoria.
- Resultado: Se actualizaron `Manuales-Proyecto-Estandar`, `estandar-trunk`, `Incidentes` y `abiondi-V25.08.001` sin conflictos ni cambios locales pendientes. En memoria intermedia del dia constan revisiones: `Manuales-Proyecto-Estandar` rev. 29, `estandar-trunk` rev. 84159, `Incidentes` rev. 20443 y `abiondi-V25.08.001` rev. 84159.
- Estado: Resuelto

### Tema 3: Copago por orden en OSPECON, afiliado 2229/00, autorizacion 505

- Solicitud o contexto: Andre consulto en `ospecon_prod` y rama `25.08.001` donde se guarda el copago por orden y pidio analizar la autorizacion `505` del afiliado `2229/00`.
- Respuesta o acciones del agente: Se revisaron tablas `autorizaciones`, `auto_prestac_items`, el SP `ars_pedidos_auto_prestac`, el objeto PowerBuilder `w_clase_auto.srw`, procedimientos `prs_verif_esta_exento_copagos`, `prs_valor_copa_orden_xauto`, `prs_valida_of_copagos` y datos productivos en `ospecon_prod`.
- Resultado: Se dejo analisis en `/mnt/Agentes/abiondi/analisis_ospecon_copago_orden_auto_505_20260728.md` y `.docx`. La conclusion inicial fue que el valor de copago por orden se guarda en `autorizaciones.copa_valor` y el tipo en `autorizaciones.pago_copa`; en items, `copa_valor` y `pago_copa` corresponden al copago por prestacion. Luego se amplio el diagnostico: clasificacion `Error`; `prs_verif_esta_exento_copagos` devuelve correctamente la exencion del afiliado, pero `w_clase_auto.srw`, funcion `wf_valida_copagos_orden`, calcula copago por orden cuando el prestador no esta exento y hay prestacion sin bloquear por `istr_afi.exen_copago`. Tambien se observo que `prs_valor_copa_orden_xauto` no contempla exencion del afiliado y puede sostener el copago persistido.
- Estado: Resuelto

### Tema 4: Error en documento `mail ospecon`

- Solicitud o contexto: Andre indico que en su carpeta de documentos habia un archivo llamado `mail ospecon` con un error reproducido, usando `ospecon_prod` y rama `25.08.001`.
- Respuesta o acciones del agente: Se busco el documento en `/mnt/Agentes/abiondi/documentos`, se analizo la pantalla/listado global de autorizaciones y el flujo `wp_list_global_auto`.
- Resultado: Se genero `/mnt/Agentes/abiondi/analisis_mail_ospecon_global_autorizaciones_250102_20260728.md` y `.docx`. La clasificacion fue `Error`: el resumen calcula correctamente 6 autorizaciones con `250102 - KINESIOTERAPIA`, pero en la solapa `Prestaciones` de `wp_list_global_auto` la DataWindow recupera un universo mas amplio y aplica un `SetFilter` invalido con nombres tipo SQL (`prestaciones.prestac`, `prestaciones.tipo`), lo que genera `Expecting TRUE/FALSE expression` y deja visibles prestaciones/autorizaciones incorrectas como la autorizacion `504`.
- Estado: Resuelto

### Tema 5: Solicitudes OSPECON, titular `inte='0 '` vs `inte='00'`

- Solicitud o contexto: Andre pidio analizar por que en solicitudes se recuperaba `inte='0 '` con baja tecnica en lugar de `inte='00'` titular activo del grupo.
- Respuesta o acciones del agente: Se reviso `ospecon_prod`, rama `25.08.001`, el contrato `418768`, la solicitud `163366`, y procedimientos relacionados con titulares y abordaje de solicitudes.
- Resultado: Se genero `/mnt/Agentes/abiondi/analisis_ospecon_solicitud_inte_0_vs_00_418768_20260728.md` y `.docx`. La clasificacion fue `Error`: `afs_afi_datos_titu_soli` detecta que el contrato tiene algun afiliado vigente, pero despues trae titulares `paren='T'` sin filtrar `A.baja_fecha`, por lo que devuelve primero `inte='0 '` con `baja_fecha = 1900-01-01` y luego `inte='00'` activo. La pantalla toma el primer registro y provoca el error posterior.
- Estado: Resuelto

### Tema 6: Error 1016 en solicitud OSPECON 418768

- Solicitud o contexto: Andre mostro o consulto el error recibido por el cliente al hacer una solicitud para contrato `418768`.
- Respuesta o acciones del agente: Se reviso el flujo de `afu_afi_titulares` y `afu_soli_abordar1` contra `ospecon_prod` y rama `25.08.001`.
- Resultado: Se dejo analisis en `/mnt/Agentes/abiondi/analisis_ospecon_error_1016_solicitud_418768_20260728.md` y `.docx`. La clasificacion fue `Error`: `afu_afi_titulares` valida titulares por `prepaga`, `contra` y `paren='T'`, pero no filtra baja tecnica, vigencia ni respeta `@inte`; por eso encuentra dos titulares para el contrato (`inte='0 '` con baja tecnica e `inte='00'` activo), devuelve rechazo `1016` (`Hay mas de un registro del mismo afiliado con distintos datos`) y `afu_soli_abordar1` arma el mensaje para la solicitud `163366` con error `-99999`. El Markdown anterior fue actualizado, pero el `.docx` anterior estaba bloqueado/abierto y no pudo sobrescribirse.
- Estado: Resuelto
