# Memoria 2026-08-11

- Usuario Ro consulto por Afilmed Exportacion de Aportes en `Celer_Prod` vs `Medimas_Prod` para entidad mixta RNOS/RNEMP. Diagnostico: no es falta RNOS/RNEMP; en `Celer_Prod` el SP `afs_afi_aportes_export` para `PREPAGA MIXTA` periodo `06/2026` generaba filas sin `contra`, y `dbo.afi_apor_export_prepaga.contra` estaba `NOT NULL`. En `Medimas_Prod` esa columna permite `NULL`, por eso no falla. Parametros generales equivalentes. Recomendacion enviada: alinear estructura de `dbo.afi_apor_export_prepaga`; minimo `ALTER TABLE dbo.afi_apor_export_prepaga ALTER COLUMN contra CHAR(15) NULL;`, idealmente dejar solo `lote` y `registro` como obligatorios y campos de detalle `prepaga, contra, inte, valor, debi_credi, cuit, cuil, periodo, concepto` como NULL. No tocar `lote/registro` por PK.
- Texto DTI sugerido para Celer: corregir estructura en `Celer_Prod` de `dbo.afi_apor_export_prepaga`, porque exportacion puede generar registros sin contrato asociado; validar exportacion luego y revisar lotes parciales. Causa: diferencia estructural de base, no parametrizacion RNOS/RNEMP.
- Usuario consulto TK 6086 Presmed `MUTUAL_Prod`, topes grupo `6814`. Diagnostico: configuracion funcional inconsistente. Grupo `6814` plan `238XVOS`, tope 1 sesion, vigencia anio calendario, periodo 11 meses, regla misma prestacion, unidad `M`. `dbo.calendario_topes_mes` en `MUTUAL_Prod` solo tiene periodos activos de `2`, `3`, `4` y `6` meses; por eso 6 meses no falla y 10/11/12/18 meses devuelven rechazo/error `1365`: "No se encuentran configurado los meses de tope calendario". No es bug de autorizacion; si quieren control anual, configurar como 1 anio calendario, no 11/12 meses.
- Se verifico en fuente que no hay pantalla/ABM Presmed para cargar `calendario_topes_mes`; es parametrizacion tecnica por script. SP relevante: `prs_valida_of_rango_mes_calen`, busca `mes_desde/mes_hasta` por `meses = @prm_meses_canti`. Para 1 mes, el SP lo resuelve directamente con primer/ultimo dia del mes y no requiere tabla. Para 18 meses no corresponde con logica actual porque la tabla maneja meses 01-12 dentro del anio calendario; requeriria otro criterio o desarrollo.
- Se compararon clientes/profiles accesibles para `calendario_topes_mes`: `MUTUAL_Prod`, Medimas, Celer, Premedic, Quimicos, Ospecon, Sanar, Consalud, SMC, SOS, Activa, Roisa y Montegrande. Todos tenian solo `2`, `3`, `4`, `6` meses; no se encontraron clientes con periodos adicionales.
- Usuario consulto TK 6076, etapa 1 para integracion Debi: enviar deuda desde Infomedical hacia Debi y notificar resultado de cobranza. Respuesta sugerida: clasificacion nuevo desarrollo requerido / integracion nueva con reutilizacion parcial de Entes recaudadores. El circuito existente de Entes recaudadores + Exportacion de debitos + Importacion de cobranzas sirve si Debi opera con archivos planos ida/vuelta. Para avanzar pedir layout de envio de deuda, layout de devolucion de cobranzas, identificador unico de retorno, manejo de rechazados/parciales/anulados y confirmar si intercambio sera archivo o API. Si Debi requiere API/webhook/notificacion en linea, no alcanza parametrizacion: requiere interfaz/adaptador especifico. Etapa 1 debe limitarse a probar ciclo completo deuda enviada y resultado recibido/importado; etapa 2 queda fuera hasta validar ese ciclo.

- Usuario Jean consulto si en Presmed existe parametro para permitir crear prestadores con mismo CUIT en tabla `prestadores.CUIT`, validando `ROISA_PROD` (`roisa_prod`), manuales `Manuales-Proyecto-Estandar` e incidentes (`Incidentes`, equivalente a repositorio de Redmine). Clasificacion: Configuracion. Conclusion: no se encontro parametro vigente que habilite/bloquee CUIT duplicado. En `parametros_generales`, modulo `Presmed / Prestadores`, solo estaban `anter_prestad_oblig`, `valida_fechas`, `valores_en_consulta`, `vto_habilitacion_sanitaria`, `vto_poliza_resp_civil`. Indice `ik_prestadores_4` sobre `prestadores.cuit` es no unico. `ROISA_PROD` ya tenia CUIT duplicados: 1206 CUIT con mas de un prestador y 947 con mas de un prestador vigente. SPs relevantes: `prs_busca_cant_prestad_x_cuit` cuenta prestadores por CUIT; `prs_val_prestador_duplicado` devuelve `S` si CUIT ya duplicado y `N` si no. En `Incidentes`, script `R4752/.../SP_20181026_0178_pri_prestadores.sql` valida unicidad de matriculas, no unicidad de CUIT antes del insert. Recomendacion dada: no tratar como parametro configurable; la base permite duplicados estructuralmente pero la deteccion funcional depende de validaciones/procedimientos.
- Usuario Jean retomo ticket InvGate 6078, con analisis previo en `/mnt/Agentes/jpinto/S6078/S6078.docx`, y comento que en `SMC_PROD` no pudo replicar el error. Diagnostico actual: no reproducible con evidencia; el error original indicaba que `afu_cambio_vto` intento insertar auditoria en `comprobantes_audi` y fallo porque `comprobantes_audi.grava` no admite `NULL`. Validaciones: actualmente en `SMC_Prod` no hay comprobantes con `grava NULL`; con filtros de captura para `prepaga = 2`, `FC`, periodo `08/2026`, vto `10/08/2026`, vto2 `20/08/2026`, vto3 `NULL`, habia 1580 comprobantes y 0 con `grava NULL`; facturas visibles en captura tambien tenian `grava` informado. Se intento reproducir con un comprobante y con todos sin obtener error. Respuesta sugerida: informar que el incidente queda no reproducible, sin dato puntual para corregir en base; pedir al cliente repetir operacion y, si reaparece, informar comprobantes exactos seleccionados, nuevo vencimiento ingresado y captura del mensaje. Si se reproduce, derivar a revision tecnica de `afu_cambio_vto` porque falla en auditoria del cambio de vencimiento.
- Usuario Jean pidio analizar ticket InvGate 6090 y guardarlo en carpeta existente `/mnt/Agentes/jpinto/S6090`, usando profile `Roisa_Prod`/`roisa_prod`. Contexto aportado por usuario: en ese profile solo estan prestadores 389 y 38630 respecto de los reportados; sospecha que cliente igualo CUIT entre esos prestadores y necesita saber si se puede permitir mismo CUIT entre varios prestadores. Se uso skill `invgate-get`; inicialmente fallo `svn update` obligatorio de `estandar-trunk` por autenticacion SVN registrada como `lpicone`, luego el usuario pidio reintentar porque las credenciales correctas estaban registradas y el update avanzo.
- Resultado ticket 6090: se genero Word validado en `/mnt/Agentes/jpinto/S6090/analisis_ticket_6090.docx`, archivo integro e incluye dos capturas del ticket. Conclusion principal: no se encontro parametro para permitir CUIT duplicado en transaccion `02A`. La base permite CUIT repetidos estructuralmente, pero la transaccion `02A` rechaza por regla programada en `pri_transac_remotas` cuando el CUIT identifica a mas de un prestador vigente. La transaccion `01A` pasa por otro procedimiento y por eso puede comportarse distinto.

- Usuario Andre pidio usar repo/profile `ospecon_conta` en rama `25.08.001` y analizar documentos en `/mnt/Agentes/abiondi/redmine/30188` sobre ajuste en ventana de debitos de expedientes cuando existe copago por orden tipo "paga al prestador". Se genero documento Word en `/mnt/Agentes/abiondi/redmine/30188/Analisis_R30188_copago_orden_debitos_expedientes.docx`, validado como abrible. El documento incluye clasificacion, evidencia de base/codigo, diagnostico, consideracion para evitar neto negativo y tablas/campos del valor "capturado".
- Conclusiones Redmine 30188: el ajuste corresponde a nuevo desarrollo/revision funcional en debitos de expedientes. Hoy el debito global se guarda en `expedientes.debito`; el copago por orden paga al prestador se guarda separado en `expe_copagos.valor` con `pago_copa = 'P'`; el valor presentado/capturado del expediente esta reflejado en `expedientes.total`. Caso verificado expediente 60: `total = 36200`, `totcal = 35000`, `debito = 1200` y copago por orden vigente `1200`; por eso el total funcional esperado a descontar es `2400` y el neto esperado `33800`.
- Consideracion tecnica Redmine 30188: al descontar copago + debito se debe evitar que el neto quede negativo. Regla sugerida: calcular contra el importe base/capturado del expediente y acotar el descuento al maximo disponible o validar/rechazar cuando `debito + copago_paga_prestador > total/base`, dejando evidencia clara para auditoria.

- Usuario Javier saludo a Mafalda/Malfa y se cargo disponibilidad de trabajo en Google Chat directo.
- Usuario Javier consulto en `mutualrioiv_prod` por expedientes de odontologia donde el sistema antepone la letra `O` al codigo de prestacion antes de buscar en base. Clasificacion: Configuracion / datos maestros. Evidencia: nomenclador odontologico `nomen = 3` (`ODONTOLOGIA`), unica config en `param_odon` para `nomen = 3`, sin clave vigente en `parametros_generales` relacionada con agregar/prefijo `O`; en `odon_prestac` habia 45 prestaciones vigentes `O%`, pero en `prestaciones` esas mismas tenian `baja_fecha = 2026-07-01` y no habia prestaciones vigentes `O%`. Conclusion dada: no se encontro parametro vigente para apagar el agregado de `O`; comportamiento asociado a logica fija o datos maestros inconsistentes. Recomendacion: revisar/corregir `odon_prestac`, dejando vigentes codigos odontologicos sin `O` y dando de baja/reemplazando referencias `O%` que apuntan a prestaciones dadas de baja. No se ejecutaron cambios.
- Usuario Javier consulto en TRUNK por WSDL `http://localhost:8080/AutoRemWebApplication/TransactionServ`, transaccion `02C`, que SP registra la transaccion. Evidencia en `estandar-trunk`: `TransactionServ.EjecutarTransaccion` deriva a `AutorizacionesBO.ejecutarTransacciones`; `PrestacionDAO.precargarAutorizacionesRemotas` llama `pri_auto_remotas_webpos`; ese SP delega en `pri_auto_remotas`; `pri_auto_remotas` hace el `INSERT INTO AUTO_REMOTAS`. Para procesamiento especifico de `02C`, `TransactionEnum` define `prs_valida_of_auto_remotas(?,?,?)`, invocado dinamicamente desde `AutorizacionesDAO`. Respuesta corta dada: registro en `AUTO_REMOTAS` = `pri_auto_remotas`, invocado via `pri_auto_remotas_webpos`; procesamiento/validacion `02C` = `prs_valida_of_auto_remotas`.
- Usuario Javier consulto trabajando con WS de `AutoRemWebApplication` tipo transaccion `02C`, que tag completar para resolver error `Sin prestador demandante`. Clasificacion: Configuracion / integracion WS. Respuesta: completar bloque `<Prescriptor>` con `ProvinciaPrescriptor`, `NroMatriculaPrescriptor` y `ApellidoPrescriptor`; dato clave `NroMatriculaPrescriptor` acompañado por `ProvinciaPrescriptor`. Evidencia: parser `Prescriptor.java` lee esos tags; `AutorizacionesDAO` los pasa como datos del demandante; validacion `prs_validar_prestador_demandante` rechaza con `1386` cuando la prestacion requiere prescriptor libre y no tiene matricula nacional, matricula provincial ni nombre demandante. Ejemplo minimo dado: `<Prescriptor><ProvinciaPrescriptor>1</ProvinciaPrescriptor><NroMatriculaPrescriptor>12345</NroMatriculaPrescriptor><ApellidoPrescriptor>APELLIDO O RAZON</ApellidoPrescriptor></Prescriptor>`. Si provincia es Capital, se trata como matricula nacional; si es otra provincia, valida contra `matri_prov` y `matri_prov_provin`.

- Usuario Valeria analizo TK InvGate 6087 en `mutualrioiv_prod`, grupo/contrato `208118`. Clasificacion: Configuracion / inconsistencia operativa entre ficha vigente e historico. Se verifico que Walter David Toledo y Florencia Belen Toledo estaban involucrados; solicitudes aplicadas el `30/07/2026`: `29117` cambio de titularidad dejaba Florencia titular y Walter adherente; `29118` baja familiares con `realbaja_fecha = 01/08/2026`, motivo `JUBILACION`, para Walter; `29132` cambio de carencias/preexistencias conservaba referencia de baja; `29258` cambio de titularidad posterior volvio a dejar Walter como titular y Florencia adherente. En `afiliados` Walter podia figurar sin `baja_fecha`, pero en `afi_histo_ingresos` tenia baja `01/08/2026`; Presmed lo tomaba de baja al autorizar con fecha `11/08/2026`. Se aclaro que la solicitud `29285` no correspondia al contrato `208118` sino a `600553`; la que revirtio el grupo fue `29258`.
- Recomendacion TK 6087: no reafiliar a Walter como primera opcion, porque ya tiene baja historica desde `01/08/2026` y la maniobra de reafiliacion + cambio titularidad + baja agregaria movimientos historicos artificiales con posible impacto en auditoria, carencias, cuenta corriente, padrones y validaciones. Corresponde generar tarea a otro sector para correccion controlada de inconsistencia: dejar Florencia Belen Toledo como titular activa y Walter David Toledo dado de baja desde `01/08/2026`, alineando ficha vigente con historico y evitando operar por circuito funcional normal bloqueado. Se dejo Word en `/mnt/Agentes/vesaravia/analisis_tk6087_correccion_controlada.docx`, validado como DOCX abrible.
- Usuario Valeria pidio analizar TK InvGate 6091 por clientes reinstalando/reconfigurando tras incidente de virus/migracion a nube. Clasificacion: Configuracion / soporte de reinstalacion. Ticket pide WAR de precarga de solicitudes. En `estandar-trunk`, el componente no se llama precarga sino `WSafil.war`, dentro de modulo `ws_generales`, expone servicio `PrecargaSolicitudes` en `/PrecargaSolicitudes` y llama SP `ars_precarga_solicitud`; en `mutualrioiv_prod` existen objetos de precarga como `ars_precarga_solicitud`, `ari_precarga_solicitud`, `PRECARGA_SOLICITUDES`, `PRECARGA_SOLI_AFILIADOS`, `PRECARGA_ERRORES`, `PRECARGA_DEFAULTS`. Se aclaro que `precarga_soli` es Node/React y no genera WAR. Luego Valeria encontro ruta de version `25.03.006`, pero necesitaba `25.03.005`; se concluyo que no conviene indicar WAR de `25.03.006` para cliente en `25.03.005`. En local de `25.03.005` solo se encontro `EntregaSoloSMC_25.03.005/Ws/WS/WAR/AutoRemWebApplication.war`, no `WSafil.war`; en ramas SVN visibles no aparecia `V25.03.005`, si otras como `V25.03.001` y `V25.03.006_PROVI`. Respuesta sugerida: confirmar con tecnico cual WAR compatible corresponde para `25.03.005` o desde que entrega debe tomarse, sin mezclar versiones.
- Usuario Valeria retomo TK 6074 y 6075. Diagnostico: el problema no era que el contrato estuviera totalmente dado de baja en `afiliados`, sino que la clase vigente quedo cortada/dada de baja en `afi_clase`; el sistema al operar solicitudes valida clase vigente del contrato y al encontrar `baja_fecha` en `afi_clase` bloquea con mensaje tipo "el afiliado no se encuentra en el periodo de aplicacion". TK 6074: contrato/afiliado `20270939288`, en `afiliados` `baja_fecha = NULL`, pero en `afi_clase` `baja_fecha = 01/08/2026`, asociado a solicitud `7192`, motivo `Baja total`. TK 6075: contrato/afiliado `20278153224`, afiliados principales vigentes/sin baja en `afiliados`, pero `afi_clase` clase `D`, `vigen_desde = 01/07/2026`, `baja_fecha = 01/07/2026`, asociado a solicitud `6106`, motivo `Baja total`. Se genero Word en `/mnt/Agentes/vesaravia/analisis_tk6074_6075_afi_clase.docx`, validado como DOCX abrible, con pedido sugerido para otro sector de correccion controlada de `afi_clase`.
- Usuario Valeria pregunto si en Presmed hay parametro para repetir CUIT. Se respondio con antecedente validado en `ROISA_PROD`/`roisa_prod`: no se encontro parametro vigente para habilitar o bloquear CUIT duplicado en `parametros_generales` modulo `Presmed / Prestadores`; la base estructuralmente permite duplicados porque el indice sobre `prestadores.cuit` no es unico. Existen SPs que detectan duplicidad como `prs_busca_cant_prestad_x_cuit` y `prs_val_prestador_duplicado`; por lo tanto no tratarlo como parametrizacion general sino revisar validacion puntual de pantalla/proceso.
- Usuario Valeria comenzo consulta por situacion de CUIT repetido en base `roisa_prod`. Se hicieron varias validaciones de solo lectura aun sin respuesta final antes de la compactacion. Contexto probable: continuar desde la pregunta de parametro CUIT repetido en Presmed/ROISA y revisar validaciones/procedimientos concretos de la situacion aportada por la usuaria.
# Memoria 2026-08-11

- Usuario Ro consulto por Afilmed Exportacion de Aportes en `Celer_Prod` vs `Medimas_Prod` para entidad mixta RNOS/RNEMP. Diagnostico: no es falta RNOS/RNEMP; en `Celer_Prod` el SP `afs_afi_aportes_export` para `PREPAGA MIXTA` periodo `06/2026` generaba filas sin `contra`, y `dbo.afi_apor_export_prepaga.contra` estaba `NOT NULL`. En `Medimas_Prod` esa columna permite `NULL`, por eso no falla. Parametros generales equivalentes. Recomendacion enviada: alinear estructura de `dbo.afi_apor_export_prepaga`; minimo `ALTER TABLE dbo.afi_apor_export_prepaga ALTER COLUMN contra CHAR(15) NULL;`, idealmente dejar solo `lote` y `registro` como obligatorios y campos de detalle `prepaga, contra, inte, valor, debi_credi, cuit, cuil, periodo, concepto` como NULL. No tocar `lote/registro` por PK.
- Texto DTI sugerido para Celer: corregir estructura en `Celer_Prod` de `dbo.afi_apor_export_prepaga`, porque exportacion puede generar registros sin contrato asociado; validar exportacion luego y revisar lotes parciales. Causa: diferencia estructural de base, no parametrizacion RNOS/RNEMP.
- Usuario consulto TK 6086 Presmed `MUTUAL_Prod`, topes grupo `6814`. Diagnostico: configuracion funcional inconsistente. Grupo `6814` plan `238XVOS`, tope 1 sesion, vigencia anio calendario, periodo 11 meses, regla misma prestacion, unidad `M`. `dbo.calendario_topes_mes` en `MUTUAL_Prod` solo tiene periodos activos de `2`, `3`, `4` y `6` meses; por eso 6 meses no falla y 10/11/12/18 meses devuelven rechazo/error `1365`: "No se encuentran configurado los meses de tope calendario". No es bug de autorizacion; si quieren control anual, configurar como 1 anio calendario, no 11/12 meses.
- Se verifico en fuente que no hay pantalla/ABM Presmed para cargar `calendario_topes_mes`; es parametrizacion tecnica por script. SP relevante: `prs_valida_of_rango_mes_calen`, busca `mes_desde/mes_hasta` por `meses = @prm_meses_canti`. Para 1 mes, el SP lo resuelve directamente con primer/ultimo dia del mes y no requiere tabla. Para 18 meses no corresponde con logica actual porque la tabla maneja meses 01-12 dentro del anio calendario; requeriria otro criterio o desarrollo.
- Se compararon clientes/profiles accesibles para `calendario_topes_mes`: `MUTUAL_Prod`, Medimas, Celer, Premedic, Quimicos, Ospecon, Sanar, Consalud, SMC, SOS, Activa, Roisa y Montegrande. Todos tenian solo `2`, `3`, `4`, `6` meses; no se encontraron clientes con periodos adicionales.
- Usuario consulto TK 6076, etapa 1 para integracion Debi: enviar deuda desde Infomedical hacia Debi y notificar resultado de cobranza. Respuesta sugerida: clasificacion nuevo desarrollo requerido / integracion nueva con reutilizacion parcial de Entes recaudadores. El circuito existente de Entes recaudadores + Exportacion de debitos + Importacion de cobranzas sirve si Debi opera con archivos planos ida/vuelta. Para avanzar pedir layout de envio de deuda, layout de devolucion de cobranzas, identificador unico de retorno, manejo de rechazados/parciales/anulados y confirmar si intercambio sera archivo o API. Si Debi requiere API/webhook/notificacion en linea, no alcanza parametrizacion: requiere interfaz/adaptador especifico. Etapa 1 debe limitarse a probar ciclo completo deuda enviada y resultado recibido/importado; etapa 2 queda fuera hasta validar ese ciclo.

- Usuario Jean consulto si en Presmed existe parametro para permitir crear prestadores con mismo CUIT en tabla `prestadores.CUIT`, validando `ROISA_PROD` (`roisa_prod`), manuales `Manuales-Proyecto-Estandar` e incidentes (`Incidentes`, equivalente a repositorio de Redmine). Clasificacion: Configuracion. Conclusion: no se encontro parametro vigente que habilite/bloquee CUIT duplicado. En `parametros_generales`, modulo `Presmed / Prestadores`, solo estaban `anter_prestad_oblig`, `valida_fechas`, `valores_en_consulta`, `vto_habilitacion_sanitaria`, `vto_poliza_resp_civil`. Indice `ik_prestadores_4` sobre `prestadores.cuit` es no unico. `ROISA_PROD` ya tenia CUIT duplicados: 1206 CUIT con mas de un prestador y 947 con mas de un prestador vigente. SPs relevantes: `prs_busca_cant_prestad_x_cuit` cuenta prestadores por CUIT; `prs_val_prestador_duplicado` devuelve `S` si CUIT ya duplicado y `N` si no. En `Incidentes`, script `R4752/.../SP_20181026_0178_pri_prestadores.sql` valida unicidad de matriculas, no unicidad de CUIT antes del insert. Recomendacion dada: no tratar como parametro configurable; la base permite duplicados estructuralmente pero la deteccion funcional depende de validaciones/procedimientos.
- Usuario Jean retomo ticket InvGate 6078, con analisis previo en `/mnt/Agentes/jpinto/S6078/S6078.docx`, y comento que en `SMC_PROD` no pudo replicar el error. Diagnostico actual: no reproducible con evidencia; el error original indicaba que `afu_cambio_vto` intento insertar auditoria en `comprobantes_audi` y fallo porque `comprobantes_audi.grava` no admite `NULL`. Validaciones: actualmente en `SMC_Prod` no hay comprobantes con `grava NULL`; con filtros de captura para `prepaga = 2`, `FC`, periodo `08/2026`, vto `10/08/2026`, vto2 `20/08/2026`, vto3 `NULL`, habia 1580 comprobantes y 0 con `grava NULL`; facturas visibles en captura tambien tenian `grava` informado. Se intento reproducir con un comprobante y con todos sin obtener error. Respuesta sugerida: informar que el incidente queda no reproducible, sin dato puntual para corregir en base; pedir al cliente repetir operacion y, si reaparece, informar comprobantes exactos seleccionados, nuevo vencimiento ingresado y captura del mensaje. Si se reproduce, derivar a revision tecnica de `afu_cambio_vto` porque falla en auditoria del cambio de vencimiento.
- Usuario Jean pidio analizar ticket InvGate 6090 y guardarlo en carpeta existente `/mnt/Agentes/jpinto/S6090`, usando profile `Roisa_Prod`/`roisa_prod`. Contexto aportado por usuario: en ese profile solo estan prestadores 389 y 38630 respecto de los reportados; sospecha que cliente igualo CUIT entre esos prestadores y necesita saber si se puede permitir mismo CUIT entre varios prestadores. Se uso skill `invgate-get`; inicialmente fallo `svn update` obligatorio de `estandar-trunk` por autenticacion SVN registrada como `lpicone`, luego el usuario pidio reintentar porque las credenciales correctas estaban registradas y el update avanzo.
- Resultado ticket 6090: se genero Word validado en `/mnt/Agentes/jpinto/S6090/analisis_ticket_6090.docx`, archivo integro e incluye dos capturas del ticket. Conclusion principal: no se encontro parametro para permitir CUIT duplicado en transaccion `02A`. La base permite CUIT repetidos estructuralmente, pero la transaccion `02A` rechaza por regla programada en `pri_transac_remotas` cuando el CUIT identifica a mas de un prestador vigente. La transaccion `01A` pasa por otro procedimiento y por eso puede comportarse distinto.

- Usuario Andre pidio usar repo/profile `ospecon_conta` en rama `25.08.001` y analizar documentos en `/mnt/Agentes/abiondi/redmine/30188` sobre ajuste en ventana de debitos de expedientes cuando existe copago por orden tipo "paga al prestador". Se genero documento Word en `/mnt/Agentes/abiondi/redmine/30188/Analisis_R30188_copago_orden_debitos_expedientes.docx`, validado como abrible. El documento incluye clasificacion, evidencia de base/codigo, diagnostico, consideracion para evitar neto negativo y tablas/campos del valor "capturado".
- Conclusiones Redmine 30188: el ajuste corresponde a nuevo desarrollo/revision funcional en debitos de expedientes. Hoy el debito global se guarda en `expedientes.debito`; el copago por orden paga al prestador se guarda separado en `expe_copagos.valor` con `pago_copa = 'P'`; el valor presentado/capturado del expediente esta reflejado en `expedientes.total`. Caso verificado expediente 60: `total = 36200`, `totcal = 35000`, `debito = 1200` y copago por orden vigente `1200`; por eso el total funcional esperado a descontar es `2400` y el neto esperado `33800`.
- Consideracion tecnica Redmine 30188: al descontar copago + debito se debe evitar que el neto quede negativo. Regla sugerida: calcular contra el importe base/capturado del expediente y acotar el descuento al maximo disponible o validar/rechazar cuando `debito + copago_paga_prestador > total/base`, dejando evidencia clara para auditoria.

- Usuario Javier saludo a Mafalda/Malfa y se cargo disponibilidad de trabajo en Google Chat directo.
- Usuario Javier consulto en `mutualrioiv_prod` por expedientes de odontologia donde el sistema antepone la letra `O` al codigo de prestacion antes de buscar en base. Clasificacion: Configuracion / datos maestros. Evidencia: nomenclador odontologico `nomen = 3` (`ODONTOLOGIA`), unica config en `param_odon` para `nomen = 3`, sin clave vigente en `parametros_generales` relacionada con agregar/prefijo `O`; en `odon_prestac` habia 45 prestaciones vigentes `O%`, pero en `prestaciones` esas mismas tenian `baja_fecha = 2026-07-01` y no habia prestaciones vigentes `O%`. Conclusion dada: no se encontro parametro vigente para apagar el agregado de `O`; comportamiento asociado a logica fija o datos maestros inconsistentes. Recomendacion: revisar/corregir `odon_prestac`, dejando vigentes codigos odontologicos sin `O` y dando de baja/reemplazando referencias `O%` que apuntan a prestaciones dadas de baja. No se ejecutaron cambios.
- Usuario Javier consulto en TRUNK por WSDL `http://localhost:8080/AutoRemWebApplication/TransactionServ`, transaccion `02C`, que SP registra la transaccion. Evidencia en `estandar-trunk`: `TransactionServ.EjecutarTransaccion` deriva a `AutorizacionesBO.ejecutarTransacciones`; `PrestacionDAO.precargarAutorizacionesRemotas` llama `pri_auto_remotas_webpos`; ese SP delega en `pri_auto_remotas`; `pri_auto_remotas` hace el `INSERT INTO AUTO_REMOTAS`. Para procesamiento especifico de `02C`, `TransactionEnum` define `prs_valida_of_auto_remotas(?,?,?)`, invocado dinamicamente desde `AutorizacionesDAO`. Respuesta corta dada: registro en `AUTO_REMOTAS` = `pri_auto_remotas`, invocado via `pri_auto_remotas_webpos`; procesamiento/validacion `02C` = `prs_valida_of_auto_remotas`.
- Usuario Javier consulto trabajando con WS de `AutoRemWebApplication` tipo transaccion `02C`, que tag completar para resolver error `Sin prestador demandante`. Clasificacion: Configuracion / integracion WS. Respuesta: completar bloque `<Prescriptor>` con `ProvinciaPrescriptor`, `NroMatriculaPrescriptor` y `ApellidoPrescriptor`; dato clave `NroMatriculaPrescriptor` acompañado por `ProvinciaPrescriptor`. Evidencia: parser `Prescriptor.java` lee esos tags; `AutorizacionesDAO` los pasa como datos del demandante; validacion `prs_validar_prestador_demandante` rechaza con `1386` cuando la prestacion requiere prescriptor libre y no tiene matricula nacional, matricula provincial ni nombre demandante. Ejemplo minimo dado: `<Prescriptor><ProvinciaPrescriptor>1</ProvinciaPrescriptor><NroMatriculaPrescriptor>12345</NroMatriculaPrescriptor><ApellidoPrescriptor>APELLIDO O RAZON</ApellidoPrescriptor></Prescriptor>`. Si provincia es Capital, se trata como matricula nacional; si es otra provincia, valida contra `matri_prov` y `matri_prov_provin`.

- Usuario Valeria analizo TK InvGate 6087 en `mutualrioiv_prod`, grupo/contrato `208118`. Clasificacion: Configuracion / inconsistencia operativa entre ficha vigente e historico. Se verifico que Walter David Toledo y Florencia Belen Toledo estaban involucrados; solicitudes aplicadas el `30/07/2026`: `29117` cambio de titularidad dejaba Florencia titular y Walter adherente; `29118` baja familiares con `realbaja_fecha = 01/08/2026`, motivo `JUBILACION`, para Walter; `29132` cambio de carencias/preexistencias conservaba referencia de baja; `29258` cambio de titularidad posterior volvio a dejar Walter como titular y Florencia adherente. En `afiliados` Walter podia figurar sin `baja_fecha`, pero en `afi_histo_ingresos` tenia baja `01/08/2026`; Presmed lo tomaba de baja al autorizar con fecha `11/08/2026`. Se aclaro que la solicitud `29285` no correspondia al contrato `208118` sino a `600553`; la que revirtio el grupo fue `29258`.
- Recomendacion TK 6087: no reafiliar a Walter como primera opcion, porque ya tiene baja historica desde `01/08/2026` y la maniobra de reafiliacion + cambio titularidad + baja agregaria movimientos historicos artificiales con posible impacto en auditoria, carencias, cuenta corriente, padrones y validaciones. Corresponde generar tarea a otro sector para correccion controlada de inconsistencia: dejar Florencia Belen Toledo como titular activa y Walter David Toledo dado de baja desde `01/08/2026`, alineando ficha vigente con historico y evitando operar por circuito funcional normal bloqueado. Se dejo Word en `/mnt/Agentes/vesaravia/analisis_tk6087_correccion_controlada.docx`, validado como DOCX abrible.
- Usuario Valeria pidio analizar TK InvGate 6091 por clientes reinstalando/reconfigurando tras incidente de virus/migracion a nube. Clasificacion: Configuracion / soporte de reinstalacion. Ticket pide WAR de precarga de solicitudes. En `estandar-trunk`, el componente no se llama precarga sino `WSafil.war`, dentro de modulo `ws_generales`, expone servicio `PrecargaSolicitudes` en `/PrecargaSolicitudes` y llama SP `ars_precarga_solicitud`; en `mutualrioiv_prod` existen objetos de precarga como `ars_precarga_solicitud`, `ari_precarga_solicitud`, `PRECARGA_SOLICITUDES`, `PRECARGA_SOLI_AFILIADOS`, `PRECARGA_ERRORES`, `PRECARGA_DEFAULTS`. Se aclaro que `precarga_soli` es Node/React y no genera WAR. Luego Valeria encontro ruta de version `25.03.006`, pero necesitaba `25.03.005`; se concluyo que no conviene indicar WAR de `25.03.006` para cliente en `25.03.005`. En local de `25.03.005` solo se encontro `EntregaSoloSMC_25.03.005/Ws/WS/WAR/AutoRemWebApplication.war`, no `WSafil.war`; en ramas SVN visibles no aparecia `V25.03.005`, si otras como `V25.03.001` y `V25.03.006_PROVI`. Respuesta sugerida: confirmar con tecnico cual WAR compatible corresponde para `25.03.005` o desde que entrega debe tomarse, sin mezclar versiones.
- Usuario Valeria retomo TK 6074 y 6075. Diagnostico: el problema no era que el contrato estuviera totalmente dado de baja en `afiliados`, sino que la clase vigente quedo cortada/dada de baja en `afi_clase`; el sistema al operar solicitudes valida clase vigente del contrato y al encontrar `baja_fecha` en `afi_clase` bloquea con mensaje tipo "el afiliado no se encuentra en el periodo de aplicacion". TK 6074: contrato/afiliado `20270939288`, en `afiliados` `baja_fecha = NULL`, pero en `afi_clase` `baja_fecha = 01/08/2026`, asociado a solicitud `7192`, motivo `Baja total`. TK 6075: contrato/afiliado `20278153224`, afiliados principales vigentes/sin baja en `afiliados`, pero `afi_clase` clase `D`, `vigen_desde = 01/07/2026`, `baja_fecha = 01/07/2026`, asociado a solicitud `6106`, motivo `Baja total`. Se genero Word en `/mnt/Agentes/vesaravia/analisis_tk6074_6075_afi_clase.docx`, validado como DOCX abrible, con pedido sugerido para otro sector de correccion controlada de `afi_clase`.
- Usuario Valeria pregunto si en Presmed hay parametro para repetir CUIT. Se respondio con antecedente validado en `ROISA_PROD`/`roisa_prod`: no se encontro parametro vigente para habilitar o bloquear CUIT duplicado en `parametros_generales` modulo `Presmed / Prestadores`; la base estructuralmente permite duplicados porque el indice sobre `prestadores.cuit` no es unico. Existen SPs que detectan duplicidad como `prs_busca_cant_prestad_x_cuit` y `prs_val_prestador_duplicado`; por lo tanto no tratarlo como parametrizacion general sino revisar validacion puntual de pantalla/proceso.
- Usuario Valeria comenzo consulta por situacion de CUIT repetido en base `roisa_prod`. Se hicieron varias validaciones de solo lectura aun sin respuesta final antes de la compactacion. Contexto probable: continuar desde la pregunta de parametro CUIT repetido en Presmed/ROISA y revisar validaciones/procedimientos concretos de la situacion aportada por la usuaria.

- Usuario consulto usando profile `Roisa_Prod` y fuente `estandar-trunk` aplicativo Presmed/Prestaci si existe regla que permita o impida cargar mas de un prestador con el mismo CUIT. Clasificacion dada: Configuracion. Conclusion: la regla aplicativa relevante es `zz_sistema_reg.reg = 33`. En `ROISA_Prod` esta vigente con `valor = 'S'`, por lo que permite mas de un prestador activo con el mismo CUIT. Si estuviera en `N`, el aplicativo impediria duplicar CUIT contra prestadores vigentes y dentro del lote/importacion.
- Evidencia CUIT/regla 33: `d_sis_reg.srd` carga reglas vigentes desde `zz_sistema_reg` (`baja_fecha IS NULL`); `wp_sube_prestador.srw` activa `lb_cuit_unico` cuando `glob.gs_reglas[33] = 'N'` y valida `prestadores` vigentes por CUIT, marcando error si ya existe; tambien valida duplicados dentro del archivo/lote cuando regla 33 = `N`. `wp_prestad_importac.srw` comenta explicitamente que solo cuando regla 33 esta en `N` no permite duplicaciones del CUIT. En base, `prestadores.cuit` tiene indice no unico `ik_prestadores_4`, no hay unique constraint ni triggers sobre `prestadores`, y se observaron 948 CUIT con mas de un prestador vigente.
- Usuario pregunto si hay alguna relacion desde `zz_sistema_reg` hacia otro lado para entender el significado de la regla. Se reviso y se respondio que no hay relacion formal ni catalogo descriptivo: `zz_sistema_reg` solo tiene `reg`, `valor`, `alta_fecha`, `baja_fecha`; no tiene FKs desde/hacia esa tabla; `d_sis_reg` no hace join descriptivo; `zzu_sistema` solo inserta/actualiza `reg` y `valor`; `zz_sistema.reglas` conserva codificacion historica tipo `#33S` pero sin descripcion. El significado de regla 33 sale del uso en codigo. La pantalla `wp_sistema_reglas.srw` documenta manualmente algunas reglas, pero en la exportacion revisada llegaba solo hasta regla 24 y no documentaba la 33.
