# Memoria curada diaria

- Fecha: 2026-07-24
- Usuario: Javier Dermit
- id_googlechat: users/109484941302245829527
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/rlilwkaaaae

## Resumen de la actividad

### Tema 1: Ticket InvGate 5996 sobre impresion de autorizaciones y obra social de derivacion

- Solicitud o contexto: Javier pidio analizar el ticket `5996` y revisar Redmine del ultimo periodo para ubicar donde se habia modificado la impresion indicada. Luego aclaro que la busqueda debia acotarse a impresion de autorizaciones, no comprobantes, y al nombre de la obra social cargada en derivacion de aportes.
- Respuesta o acciones del agente: Se aplico la skill InvGate, se leyo el ticket y la captura `IMPRESION PRESMED.jpeg`, se reviso codigo vigente de impresion de autorizaciones y se buscaron antecedentes Redmine/SVN.
- Resultado: El ticket se clasifico como configuracion/comportamiento vigente. En `de_orden_afi_cabe2.srd` se imprime `obra_social_deno` debajo del logo y en `wr_auto_impresion.srw` se asigna desde la obra social de derivacion de aportes cuando la entidad trabaja con forma de contrato `C`. Tras una primera busqueda amplia, Javier pidio acotar; el antecedente correcto identificado fue Redmine `#22278`, revision SVN `r74400`, que ajusto `wr_auto_impresion.srw` para tomar la obra social vigente desde `afi_deriv_aportes` y asignarla a `au.obra_social_deno`.
- Estado: Resuelto

### Tema 2: Carga de preferencias y carpeta de tickets

- Solicitud o contexto: Javier pidio cargar sus preferencias antes de continuar con un analisis de ticket.
- Respuesta o acciones del agente: Se cargaron las preferencias disponibles para Javier.
- Resultado: Se resolvio que su carpeta de tickets es `/mnt/Agentes/jdermit/Ticket`, usada luego para guardar el documento del ticket `5999`.
- Estado: Resuelto

### Tema 3: Ticket InvGate 5999 en Premedic_Prod sobre subsidios y generacion de comprobante

- Solicitud o contexto: Javier pidio analizar el ticket `5999` usando `PREMEDIC_PROD`/`Premedic_Prod` y dejar un documento. Despues informo que habia configurado la agrupacion de conceptos de los planes `BASICO` y `PMO BASICO` para subsidios, pero que el comprobante seguia sin generarse.
- Respuesta o acciones del agente: Se leyo el ticket, capturas, codigo de `estandar-trunk` y datos en `Premedic_Prod`. Se genero `/mnt/Agentes/jdermit/Ticket/analisis_ticket_5999.docx`. Luego se reanalizo la situacion con la nueva configuracion.
- Resultado: El diagnostico inicial fue configuracion: prefacturas pendientes con concepto `SUB - Subsidio` para planes `BASICO` y `PMO BASICO` no tenian agrupamiento vigente en `PREFAC_AGRUPAMIENTO`. En el reanalisis se verifico que el rechazo original ya no aparecia, que existian agrupamientos activos para `SUB - Subsidio`, que habia 105 movimientos/prefacturas por total neto `1.685.267,03`, y que INMERCAR no tenia comprobante generado para `07/2026`. Se intento reproducir la etapa final dentro de transaccion con rollback, pero se corto por demora para no dejar transaccion abierta en produccion. Se concluyo que el problema restante ya no estaba en `PREFAC_AGRUPAMIENTO`, sino en una etapa posterior como numeracion, punto de venta, vencimientos/intereses, impacto del comprobante o impresion.
- Estado: En curso

### Tema 4: Busqueda de manuales sobre medico de cabecera

- Solicitud o contexto: Javier pidio revisar manuales y devolver nombres de documentos que hablaran de `medico de cabecera`.
- Respuesta o acciones del agente: Se busco en el repositorio de manuales con y sin tilde y se inspeccionaron documentos Office cuando fue necesario.
- Resultado: Se encontraron referencias en `Manuales/Afilmed/2. Afiliados/Afiliados - Consultas .docx` y `Manuales/Afilmed/2. Afiliados/Afiliados - Solicitudes.docx`. Tambien aparecieron coincidencias en archivos de reglas/no manuales: `Reglas_de_los_sistemas.xls` y `Motivos de rechazo.xls`.
- Estado: Resuelto

### Tema 5: Marca de medico de cabecera en prestadores

- Solicitud o contexto: Javier sospecho que faltaba una marca en la ficha del prestador y pidio confirmar si los prestadores tienen forma de identificarse como medico de cabecera.
- Respuesta o acciones del agente: Se revisaron fuentes de `estandar-trunk`, datawindows de prestador y procedimientos relacionados.
- Resultado: Se confirmo que existe la marca `esmc`, visible como checkbox `Es Medico de Cabecera`, con valores `S`/`N`, presente en `d_prestador.srd` y `d_prestador_isss.srd`. Tambien existe una regla/modulo `Tiene el modulo de medico de cabecera` que habilita la opcion de marcar prestadores y cargar capitas. La asignacion efectiva al afiliado se maneja por `afi_prestadores`, y `afs_dame_med_cab_xafi.sql` busca el medico vigente desde esa relacion.
- Estado: Resuelto

### Tema 6: Marca de medico de cabecera en planes

- Solicitud o contexto: Javier pregunto si en planes existe una marca similar para saber si un plan usa medico de cabecera.
- Respuesta o acciones del agente: Se revisaron fuentes y datawindows de planes en `estandar-trunk`.
- Resultado: Se confirmo que existe el campo `planes.mc`, mostrado como `Medico Cabecera` / `Es Plan Med Cabecera`, con valores `S` y `N`. Se encontro evidencia en `d_plan.srd`, `d_plan_abm.srd`, `d_plan_datos_grales.srd` y `wr_plan.srw`, donde se envia al procedimiento `pru_planes` como parametro `@mc`. La marca se muestra/habilita segun `glob.gs_reglas[52]`, correspondiente al modulo de medico de cabecera. Se concluyo que para usar medico de cabecera no alcanza con marcar el prestador: el plan debe tener `planes.mc = 'S'` y el afiliado debe tener asignacion vigente en `afi_prestadores`.
- Estado: Resuelto
