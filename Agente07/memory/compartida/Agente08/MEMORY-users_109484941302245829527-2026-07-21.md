# Memoria curada diaria

- Fecha: 2026-07-21
- Usuario: Javier Dermit
- id_googlechat: users/109484941302245829527
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/rlilwkaaaae

## Resumen de la actividad

### Tema 1: Alta de credencial Redmine y preferencias de Javier

- Solicitud o contexto: Javier saludo, pidio registrar su usuario de Redmine y luego pidio modificar su archivo de preferencias.
- Respuesta o acciones del agente: Se ejecuto el alta controlada de credencial Redmine para `jdermit` sin persistir ni exponer la API key. Luego se actualizo `/mnt/Agentes/jdermit/preferencias/preferencias.md` con nombre `Javier`, usuario Redmine `Jdermit` y carpeta de trabajo.
- Resultado: La credencial Redmine quedo registrada localmente con permisos restrictivos y el archivo de preferencias fue actualizado y validado en ASCII, sin BOM y con finales LF.
- Estado: Resuelto

### Tema 2: Preferencias para repositorios Manuales, Fuentes e Incidentes

- Solicitud o contexto: Javier pidio listar repositorios disponibles y agregar preferencias para interpretar palabras clave de trabajo.
- Respuesta o acciones del agente: Se listaron los repositorios registrados y se agregaron reglas de preferencia: `Manuales` usa `abiondi-ManualesProyectoEstandar-ManualesProyectoEstandar`, `Fuentes` usa `estandar-trunk` e `Incidentes` usa `abiondi-Incidentes-Incidentes`.
- Resultado: Las preferencias quedaron registradas en `/mnt/Agentes/jdermit/preferencias/preferencias.md` y el archivo fue validado.
- Estado: Resuelto

### Tema 3: Perfiles de base de datos disponibles

- Solicitud o contexto: Javier pidio listar los perfiles de base de datos registrados.
- Respuesta o acciones del agente: Se leyo `DB_PROFILE.md` y se listaron los perfiles disponibles.
- Resultado: Se informaron perfiles como `SMC_PROD_TEST`, `Quimicos_Prod`, `Premedic_Prod`, `Ospecon_Conta`, `Sos_prod`, `Ospecon_prod`, `unosalud_prod_test`, `ospicine_prod`, `medimas_prod`, `roisa_prod` y `mutualrioiv_prod`.
- Estado: Resuelto

### Tema 4: Analisis InvGate del ticket 5858

- Solicitud o contexto: Javier pidio analizar el ticket InvGate `5858`. Indico que el archivo debia quedar en su carpeta y que se continuara sin consultar base de datos.
- Respuesta o acciones del agente: Se uso la skill `invgate-get`, se proceso el ticket y se genero el documento en la carpeta de Javier.
- Resultado: Se genero `/mnt/Agentes/jdermit/analisis_ticket_5858.docx`. Se informo que no se consulto base de datos por indicacion expresa y que el ticket no incluia especificacion tecnica de Activia sobre el repositorio virtual.
- Estado: Resuelto

### Tema 5: Preferencia para carpeta de analisis de tickets

- Solicitud o contexto: Javier pidio agregar que cuando solicite analizar un ticket el archivo se arme dentro de la carpeta `Ticket`.
- Respuesta o acciones del agente: Se actualizo el archivo de preferencias con la ruta de salida para analisis de tickets.
- Resultado: Quedo registrada la carpeta `/mnt/Agentes/jdermit/Ticket` como destino para archivos de analisis de tickets y se valido el archivo de preferencias.
- Estado: Resuelto

### Tema 6: Configuracion de prestacion odontologica una vez por pieza usando Manuales

- Solicitud o contexto: Javier pidio explicar como configurar una prestacion de odontologia para que solo pueda ser realizada una vez por pieza usando Manuales.
- Respuesta o acciones del agente: Se reviso el repositorio de Manuales y se clasifico la consulta como `Configuracion`.
- Resultado: Se explico que la configuracion relacionada esta en `Configuracion > Odontologia > Parametros de Liquidacion de Prestaciones`, con `Aplica = Pieza`, `Cantidad maxima de prestaciones = 1`, definicion de `Meses` y, si corresponde, marca de practica odontologica. Se aclaro que con `Aplica = Pieza` el sistema obliga a informar pieza y aplica el control sobre esa unidad odontologica.
- Estado: Resuelto

### Tema 7: Configuracion de prestacion odontologica una vez por pieza usando Fuentes

- Solicitud o contexto: Javier pidio ampliar la explicacion usando Fuentes.
- Respuesta o acciones del agente: Se reviso `estandar-trunk`, incluyendo objetos y procedimientos de prestaciones odontologicas.
- Resultado: Se concluyo que para una prestacion odontologica una sola vez por pieza se configura `aplica = 'P'`, `cant_max = 1` y un valor en `meses`. Se indico que si modifica odontograma tambien debe seleccionarse el estado correspondiente, y que no se vio un flag especifico de "nunca repetir".
- Estado: Resuelto

### Tema 8: Ampliacion con base Sos_prod y rechazo 249

- Solicitud o contexto: Javier pidio ampliar el analisis usando la base `Sos_prod`.
- Respuesta o acciones del agente: Se consulto `Sos_prod` y se verificaron tablas de odontologia y motivos de rechazo.
- Resultado: Se identificaron prestaciones activas con `aplica = 'P'`, `cant_max = 1` y distintos valores de `meses`. Se detecto que el rechazo `249`, "Prestacion excede la cantidad permitida", esta configurado como tipo `A`, y que en `prs_valida_of_odon` solo corta la validacion si el motivo es tipo `E`.
- Estado: Resuelto

### Tema 9: Significado del tipo A en rechazo 249

- Solicitud o contexto: Javier pregunto que significa el tipo `A` en el rechazo `249`.
- Respuesta o acciones del agente: Se reviso el registro en `recha_motivos` y la logica de `prs_valida_of_odon`.
- Resultado: Se explico que tipo `A` funciona como advertencia o ajuste, no como error bloqueante. El sistema puede registrar el motivo `249`, pero no necesariamente invalida la prestacion salvo que el motivo se comporte como tipo `E`.
- Estado: Resuelto

### Tema 10: Regla que activa el modulo de odontologia

- Solicitud o contexto: Javier pregunto con que regla se activa el modulo de odontologia.
- Respuesta o acciones del agente: Se revisaron fuentes y `Sos_prod`, incluyendo referencias a `glob.gs_reglas[12]`, `prs_rei_tipos.sql` y `zz_sistema_reg`.
- Resultado: Se concluyo que el modulo de Odontologia se activa con la regla `12` en valor `S`. En `Sos_prod` la regla `12` estaba activa.
- Estado: Resuelto
