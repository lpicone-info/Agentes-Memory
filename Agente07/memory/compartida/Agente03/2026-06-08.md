# Durable Memory - 2026-06-08

## Working Context: wguise-trunk_Agente03
- Active legacy workspace analyzed repeatedly: `/home/luispicone/.openclaw/workspace/svn/wguise-trunk_Agente03`.
- Stack/context: PowerBuilder 2022 source under `exe/Source Infomedical` and MS SQL backend procedures under `backend/procedures`.

## AFIP / CAE Findings
- User asked about AFIP warning in `Resolicitar CAE`: error `No existen datos en nuestros registros para los parametros ingresados. (602)`.
- Technical conclusion: this comes from AFIP `FECompConsultar`, not from local validation. It means AFIP did not find a comprobante for the queried combination of CUIT/certificate, AFIP type, point of sale and comprobante number.
- Case referenced: solicitud `91066`, comprobante `FC A000201849118`; interpreted as `PtoVta 0002`, number `1849118`, type `1` (Factura A). Need compare against AFIP last authorized comprobante for `PtoVta 0002` + `CbteTipo 1` and verify correct ente/CUIT/cert/environment.

## Facturacion Mensual / Puntos de Venta Warning
- User asked about warning `No se han definido puntos de venta.`.
- Source identified: `exe/Source Infomedical/wp_fac_proceso_mensual.srw`, around line 147, window/process loads active points through DataWindow `dd_puntos_x_ente`.
- Cause: no rows returned for active `puntos`, before later ente-specific checks.
- Suggested SQL checks:
  - `SELECT * FROM puntos WHERE baja_fecha IS NULL`
  - `SELECT * FROM entes_puntos WHERE baja_fecha IS NULL`
- Functional menu/context: `Facturacion y cuentas corrientes > Puntos de venta` and `Comprobantes por ente` / point-to-ente association.

## Copagos Debita en Cuenta / Ajustes al Contrato
- User asked whether today the system generates an adjustment for copagos `Debita en cuenta` when liquidation creates copagos for an affiliate of class empresa whose company invoices in company mode.
- Conclusion: today it does not generate a facturable adjustment for the contract.
- Evidence:
  - Liquidation recalculates copagos in `backend/procedures/pru_orden_a_liqui_expe.sql`, around line 792, calling `pru_expe_copagos`.
  - `backend/procedures/pru_expe_copagos.sql`, around line 2264, inserts `expe_copagos` for orders/solicitudes with `pago_copa <> 'A'`, so `C` is stored.
  - Current `pago_copa = 'C'` circuit is export, not adjustment: `backend/procedures/prs_expor_copagos.sql`, around line 45 selects `expe_copagos.pago_copa = 'C'`; around line 207 it only marks `expor_fecha`.
  - Existing adjustment generation found for authorization/advanced copago: `backend/procedures/aru_auto_pedido.sql`, around line 288 calls `aru_generar_ajuste`; `backend/procedures/aru_generar_ajuste.sql`, around line 65 filters `temp_copagos.pago_copa = 'A'`.
- Recommended modification point: after `pru_expe_copagos` in `pru_orden_a_liqui_expe.sql`, around line 798, call a new backend procedure such as `pru_expe_copagos_generar_ajustes`.
- New procedure should take liquidated `expe_copagos` with `pago_copa = 'C'`, `baja_fecha IS NULL`, `valor > 0`; validate affiliate class empresa (`afi_clase.clase_afi = 'E'`) and account/subaccount company invoice mode (`subcuentas.modo_fac = 'E'` respecting validity/baja); generate `AJUSTES` via `afu_ajustes`; generate `AJU_ITEMS` via `afu_aju_items`, probably with `factu_concep = 'COP'`; and include traceability against expediente/copago to avoid duplicates during reliquidation/recalculation.
- Do not reuse `aru_generar_ajuste` directly without redesign because it is authorization-oriented: depends on `temp_copagos`, filters `pago_copa = 'A'`, uses authorization observations, and writes origin fields like `ori_sucur/ori_auto`.
- Open functional risk: decide whether this new adjustment replaces or coexists with `prs_expor_copagos` export for `pago_copa = 'C'` to prevent double treatment.

## Document Analysis: Consulta de Consumo / Costo por Entidades Externas
- User requested technical/functional analysis of attached requirement document.
- Requirement: in `Liquidaciones > Consulta de consumo > Costo por entidades externas`, split current `Valor` column into `Valor convenido` and `Valor ingresado`; keep remaining columns; add separated totals for both values.
- Main report/window: `exe/Source Infomedical/wp_consexter_subtot.srw`, around line 29, window `Subtotales por entidades externas`.
- Main DataWindow: `exe/Source Infomedical/d_expe_externas_subtot.srd`, around line 24, uses procedure `backend/procedures/prs_expe_externas_subtot.sql`.
- Current backend procedure returns single `valor`:
  - Honorarios: `eho.real_valor * eho.canti`
  - Ordenes: `eoi.real_valor * eoi.canti`
  - Internacion: `ein.total`
- Current DataWindow has visual column `Valor` around line 34 and sum around line 58.
- Proposed backend changes in `prs_expe_externas_subtot.sql`: add `valor_convenido` and `valor_ingresado` to temp/result; preserve `valor` only if needed for compatibility; for orders validate use of `expe_orden_items.real_valor`, `ingre_valor`, and possibly `conve_valor` to determine pure convenido vs ingresado; clarify honorarios/internacion equivalents.
- Proposed DataWindow changes in `d_expe_externas_subtot.srd`: add columns `valor_convenido`, `valor_ingresado`; change heading from `Valor` to `Valor convenido`; add `Valor ingresado`; adjust following columns (`Debito`, `Copago`, `Pago Copago`, `Plan`, `Item`, `Orden`); add totals `sum(valor_convenido for all)` and `sum(valor_ingresado for all)`.
- Related objects with similar/adjacent value logic:
  - `backend/procedures/prs_expe_orde_entidades.sql`, around line 1031, mixes entered/convened value into one output.
  - `exe/Source Infomedical/d_expe_entidades.srd`.
  - `backend/procedures/prs_expe_odon_entidades.sql`, around line 362, odontologia returns single `valor`.
  - `exe/Source Infomedical/d_expe_odon_entidades.srd`.
- Functional doubts raised: whether no-convenio should show null/blank or 0; whether change applies only to subtotals report or also order/odontology entity tabs; for honorarios/internacion whether current value should be `Valor ingresado` and convenido blank; whether report width can expand horizontally.
# Durable Memory - 2026-06-08

## Working Context: wguise-trunk_Agente03
- Active legacy workspace analyzed repeatedly: `/home/luispicone/.openclaw/workspace/svn/wguise-trunk_Agente03`.
- Stack/context: PowerBuilder 2022 source under `exe/Source Infomedical` and MS SQL backend procedures under `backend/procedures`.

## AFIP / CAE Findings
- User asked about AFIP warning in `Resolicitar CAE`: error `No existen datos en nuestros registros para los parametros ingresados. (602)`.
- Technical conclusion: this comes from AFIP `FECompConsultar`, not from local validation. It means AFIP did not find a comprobante for the queried combination of CUIT/certificate, AFIP type, point of sale and comprobante number.
- Case referenced: solicitud `91066`, comprobante `FC A000201849118`; interpreted as `PtoVta 0002`, number `1849118`, type `1` (Factura A). Need compare against AFIP last authorized comprobante for `PtoVta 0002` + `CbteTipo 1` and verify correct ente/CUIT/cert/environment.

## Facturacion Mensual / Puntos de Venta Warning
- User asked about warning `No se han definido puntos de venta.`.
- Source identified: `exe/Source Infomedical/wp_fac_proceso_mensual.srw`, around line 147, window/process loads active points through DataWindow `dd_puntos_x_ente`.
- Cause: no rows returned for active `puntos`, before later ente-specific checks.
- Suggested SQL checks:
  - `SELECT * FROM puntos WHERE baja_fecha IS NULL`
  - `SELECT * FROM entes_puntos WHERE baja_fecha IS NULL`
- Functional menu/context: `Facturacion y cuentas corrientes > Puntos de venta` and `Comprobantes por ente` / point-to-ente association.

## Copagos Debita en Cuenta / Ajustes al Contrato
- User asked whether today the system generates an adjustment for copagos `Debita en cuenta` when liquidation creates copagos for an affiliate of class empresa whose company invoices in company mode.
- Conclusion: today it does not generate a facturable adjustment for the contract.
- Evidence:
  - Liquidation recalculates copagos in `backend/procedures/pru_orden_a_liqui_expe.sql`, around line 792, calling `pru_expe_copagos`.
  - `backend/procedures/pru_expe_copagos.sql`, around line 2264, inserts `expe_copagos` for orders/solicitudes with `pago_copa <> 'A'`, so `C` is stored.
  - Current `pago_copa = 'C'` circuit is export, not adjustment: `backend/procedures/prs_expor_copagos.sql`, around line 45 selects `expe_copagos.pago_copa = 'C'`; around line 207 it only marks `expor_fecha`.
  - Existing adjustment generation found for authorization/advanced copago: `backend/procedures/aru_auto_pedido.sql`, around line 288 calls `aru_generar_ajuste`; `backend/procedures/aru_generar_ajuste.sql`, around line 65 filters `temp_copagos.pago_copa = 'A'`.
- Recommended modification point: after `pru_expe_copagos` in `pru_orden_a_liqui_expe.sql`, around line 798, call a new backend procedure such as `pru_expe_copagos_generar_ajustes`.
- New procedure should take liquidated `expe_copagos` with `pago_copa = 'C'`, `baja_fecha IS NULL`, `valor > 0`; validate affiliate class empresa (`afi_clase.clase_afi = 'E'`) and account/subaccount company invoice mode (`subcuentas.modo_fac = 'E'` respecting validity/baja); generate `AJUSTES` via `afu_ajustes`; generate `AJU_ITEMS` via `afu_aju_items`, probably with `factu_concep = 'COP'`; and include traceability against expediente/copago to avoid duplicates during reliquidation/recalculation.
- Do not reuse `aru_generar_ajuste` directly without redesign because it is authorization-oriented: depends on `temp_copagos`, filters `pago_copa = 'A'`, uses authorization observations, and writes origin fields like `ori_sucur/ori_auto`.
- Open functional risk: decide whether this new adjustment replaces or coexists with `prs_expor_copagos` export for `pago_copa = 'C'` to prevent double treatment.

## Document Analysis: Consulta de Consumo / Costo por Entidades Externas
- User requested technical/functional analysis of attached requirement document.
- Requirement: in `Liquidaciones > Consulta de consumo > Costo por entidades externas`, split current `Valor` column into `Valor convenido` and `Valor ingresado`; keep remaining columns; add separated totals for both values.
- Main report/window: `exe/Source Infomedical/wp_consexter_subtot.srw`, around line 29, window `Subtotales por entidades externas`.
- Main DataWindow: `exe/Source Infomedical/d_expe_externas_subtot.srd`, around line 24, uses procedure `backend/procedures/prs_expe_externas_subtot.sql`.
- Current backend procedure returns single `valor`:
  - Honorarios: `eho.real_valor * eho.canti`
  - Ordenes: `eoi.real_valor * eoi.canti`
  - Internacion: `ein.total`
- Current DataWindow has visual column `Valor` around line 34 and sum around line 58.
- Proposed backend changes in `prs_expe_externas_subtot.sql`: add `valor_convenido` and `valor_ingresado` to temp/result; preserve `valor` only if needed for compatibility; for orders validate use of `expe_orden_items.real_valor`, `ingre_valor`, and possibly `conve_valor` to determine pure convenido vs ingresado; clarify honorarios/internacion equivalents.
- Proposed DataWindow changes in `d_expe_externas_subtot.srd`: add columns `valor_convenido`, `valor_ingresado`; change heading from `Valor` to `Valor convenido`; add `Valor ingresado`; adjust following columns (`Debito`, `Copago`, `Pago Copago`, `Plan`, `Item`, `Orden`); add totals `sum(valor_convenido for all)` and `sum(valor_ingresado for all)`.
- Related objects with similar/adjacent value logic:
  - `backend/procedures/prs_expe_orde_entidades.sql`, around line 1031, mixes entered/convened value into one output.
  - `exe/Source Infomedical/d_expe_entidades.srd`.
  - `backend/procedures/prs_expe_odon_entidades.sql`, around line 362, odontologia returns single `valor`.
  - `exe/Source Infomedical/d_expe_odon_entidades.srd`.
- Functional doubts raised: whether no-convenio should show null/blank or 0; whether change applies only to subtotals report or also order/odontology entity tabs; for honorarios/internacion whether current value should be `Valor ingresado` and convenido blank; whether report width can expand horizontally.

## Document Analysis: R28888-09 / Bandejas de Supervisión de Autorizaciones
- User requested a technical/functional description of changes to develop for attached document `R28888-09`; no files were modified and working copy was reported clean.
- Requirement affects authorization supervision trays:
  - `Pendientes/Supervisadas por nivel`: when selecting `Entidad`, combo `Plan` must show only plans for that entity.
  - `Pendientes/Supervisadas por afiliado moroso`: same entity-dependent plan filtering.
  - Keep options `Entidad = Todas` and `Plan = Todos`.
  - Add a new grid column with authorization `Observación Interna`, showing at least the first 20 characters.
- Current behavior: result filtering already receives `prepaga` and `plan_codi`, but the plan combo is not conditioned by entity. It uses `dddw_planes`, which returns all active plans from all entities.
- Main PowerBuilder objects to modify:
  - `exe/Source Infomedical/wp_lista_auto_pend.srw`, around line 84: window for pending/supervised by level. Reload/filter child DW for `plan_codi` according to selected `prepaga`, and react when entity changes.
  - `exe/Source Infomedical/wp_lista_auto_moro_pend.srw`, around line 84: same behavior for morosity/affiliate tray.
  - `exe/Source Infomedical/de_auto_pend_nivel.srd`, around line 40: common filter DW; `plan_codi` currently uses `dddw_planes`.
  - `exe/Source Infomedical/dddw_planes.srd`, around line 15: currently returns all active plans and adds `Todos`.
  - `exe/Source Infomedical/dddw_planes_union_todos.srd`, around line 25: candidate to reuse/adapt because it already executes `afs_planes_union_todos` with `prepaga` parameter.
  - `exe/Source Infomedical/d_lista_auto_pend.srd` and `exe/Source Infomedical/d_lista_auto_moro_pend.srd`, around line 8 for moro DW: add new field and visible grid column.
- Backend objects to modify:
  - `backend/procedures/prs_autoxestaxnivel.sql`, around line 190: add internal observation column to SELECT.
  - `backend/procedures/prs_autoxesta_morosidad.sql`, around line 168: add internal observation column to SELECT.
- Internal observation field: confirmed as `autorizaciones.obser`; evidence from `exe/Source Infomedical/d_auto_observaciones.srd`, around line 73, labeled `Observaciones Internas`, and `exe/Source Infomedical/wr_auto_observaciones.srw`, around line 251, where it is saved through `pru_auto_obser`.
- Suggested design: implement a general parameter, likely under `Presmed / Autorizaciones`, to activate entity-based plan filtering. If enabled, on `prepaga` change reload `plan_codi` with plans for that entity plus `Todos`.
- Open decisions raised:
  - With `Entidad = Todas`, should `Plan` show all plans or only `Todos`?
  - Should the new parameter be active by default?
  - Should internal observation display exactly 20 chars or a column width capable of showing at least 20 chars and possibly more?

## Redmine 29406 / Inconsistencia Derivacion de Aportes DDJJ-TRANS
- User asked to reanalyze Redmine `29406`, where inconsistencies/errors were marked, implement changes in the working branch, commit them, and explain modified objects/resolved points.
- Workspace/branch: `/home/luispicone/.openclaw/workspace/svn/wguise-trunk_Agente03`.
- Changes were applied locally, but SVN commit could not be completed because authentication failed. Final SVN error reported: `Authentication failed`; cached user detected: `wguise`.
- Local modified backend procedures left pending commit:
  - `backend/procedures/afi_aportes_ddjj.sql`: calculates affiliate future `baja_fecha` and passes it to `afi_afi_deriv_aportes_ddjj_trans` through `@prm_baja_fecha`.
  - `backend/procedures/afi_afi_deriv_aportes_desde_lotes_ddjj_y_trans.sql`: in DDJJ and TRANS paths, loads `baja_fecha` into `#IMPOR_DATOS` when the affiliate baja is future relative to the period.
  - `backend/procedures/afi_afi_deriv_aportes_ddjj_trans.sql`: normalizes `baja_fecha` only when applicable to the period; does not copy historical bajas on reactivation; preserves future baja when CUIT/obra social changes or when aportante changes from `G` to `T`; avoids duplicate alta when an equivalent active derivation already exists, including `tipo_aportante` and same validity.
  - `backend/procedures/afi_afi_deriv_aportes_ddjj_trans_masivo.sql`: mirrors future baja handling; avoids duplicate equivalent active derivations; for multiple periods of the same affiliate/CUIT/obra social in one lot, compacts into a single alta using the first validity and the salary from the last reported period.
- Resolved points intended by the local changes:
  - Stops creating one derivation per period for the same CUIL/CUIT.
  - Avoids reactivating historical records when an equivalent active derivation already exists.
  - Preserves future affiliate baja in new/reactivated derivations.
  - Covers `tipo_aportante` transition from `G` to `T` without losing future baja.
  - Aligns manual, list, and massive import paths.
- Validation performed before the failed commit: reviewed `svn diff`, `svn status`, and preservation of encoding/CRLF. Four files remained locally modified and uncommitted due to SVN credentials.
