# Memoria de conversación — Marcelo Viciconti

## Datos

- **Fecha:** 26/08/2026
- **Usuario:** Marcelo Viciconti
- **ID Google Chat:** `users/103434398249633426446`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 8
- **Inicio:** 14:31:16
- **Fin:** 16:40:05

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/25hnskaaaae`

## Archivos revisados

- `6623afcc-73c8-4962-a5d1-e8df90ff2d78.jsonl`

---

## Transcripción completa

### [26/08/2026 14:31:16] Marcelo Viciconti

Ese query me devuelve entre otras cosas, el uso de los indices para un sp dado (o todos), pero los resultados no me convencen. Debe haber un error en la forma de acceso a las vistas de sys.dm_* porque veo que en lugares me dice un tablo o index scan cuando esta en verdad asignando un scalar.
Encuentre el error y corrijalo. Deje el resultante en mi carpeta compartida con Ud.

---

### [26/08/2026 14:37:55] Agente

Marcelo, listo. Dejé el SQL corregido en [dondeseutilizaunindice_corregido.sql](/mnt/Agentes/mviciconti/dondeseutilizaunindice_corregido.sql).

El error estaba en que el query usaba `sys.dm_exec_query_plan(qs.plan_handle)`, que trae el plan completo del SP/batch, pero mostraba `sql_fragment` por offsets de una sentencia puntual. Eso mezclaba operadores de otra sentencia, por ejemplo un `Index Scan`, con un fragmento que en realidad era una asignación scalar. Lo corregí usando `sys.dm_exec_text_query_plan(qs.plan_handle, qs.statement_start_offset, qs.statement_end_offset)` para analizar solo el plan de la sentencia correspondiente.

También filtré con `INNER JOIN` contra los operadores físicos esperados y `CROSS APPLY` al nodo `Object`, para no devolver operadores que no representan acceso real a tabla/índice.

Validaciones hechas: archivo en ISO-8859-1, sin BOM, CRLF, sin tabs, sin trailing spaces, sin `RTRIM`. No lo ejecuté contra una base porque no se indicó profile de conexión.

---

### [26/08/2026 14:49:47] Marcelo Viciconti

Encontré que el resultado a un mismo sp es tremendamente mas corto que el anterior. Puede garantizarme que es lo correcto? Por ejemplo en base de datos Premedic_Fac para el sp afi_afu_cuenta_trans

---

### [26/08/2026 14:51:31] Agente

Marcelo: no puedo garantizarlo todavía para `Premedic_Fac / afi_afu_cuenta_trans` sin ejecutarlo contra esa base. No tengo un profile registrado con nombre `Premedic_Fac`; en `DB_PROFILE.md` solo aparecen `Premedic_Prod`, `Premedic_Prox_Ver` y `Premedic_Prod_Test`.

Técnicamente, que el resultado sea mucho más corto es esperable y es una señal a favor, no en contra. El query anterior multiplicaba resultados porque tomaba el plan completo del SP con `sys.dm_exec_query_plan(qs.plan_handle)` para cada fila de `sys.dm_exec_query_stats`, que ya viene por sentencia. Entonces una sentencia scalar podía quedar mostrada junto con operadores físicos de otra sentencia del mismo SP.

La corrección correcta es usar:

```sql
sys.dm_exec_text_query_plan(
  qs.plan_handle,
  qs.statement_start_offset,
  qs.statement_end_offset
)
```

Eso limita el XML al plan de la sentencia real que corresponde al `sql_fragment`.

Conclusión: puedo garantizar que el error conceptual detectado era real y que el resultado corto es coherente con la corrección. No puedo garantizar el resultado funcional específico en `Premedic_Fac` hasta validarlo contra el profile correcto de esa base.

---

### [26/08/2026 15:03:13] Marcelo Viciconti

Agrego algo: Encontré que ha usado la funcion TRY_CONVERT(xml, ...). Aplicado a la base que le acabo de comentar, no funciona y la tuve que reemplazar por CONVERT(xml, ...). No dio error por suerte.

---

### [26/08/2026 15:03:21] Agente

Marcelo, correcto. En ese punto conviene dejar `CONVERT(xml, p.query_plan)`.

`TRY_CONVERT` puede fallar por compatibilidad de la base si `Premedic_Fac` está en un compatibility level viejo, aunque el motor soporte `sys.dm_exec_text_query_plan`. Si con `CONVERT(xml, ...)` no dio error, es la opción más compatible para ese entorno.

Entonces el núcleo correcto queda:

```sql
CROSS APPLY
sys.dm_exec_text_query_plan(qs.plan_handle,
                            qs.statement_start_offset,
                            qs.statement_end_offset) as p

CROSS APPLY
(
  SELECT query_plan = CONVERT(xml, p.query_plan)
) as p_xml
```

Y la garantía técnica se mantiene: lo importante no era `TRY_CONVERT`; lo importante era dejar de leer el plan completo con `sys.dm_exec_query_plan` y pasar a leer el plan de la sentencia con `sys.dm_exec_text_query_plan`.

---

### [26/08/2026 16:35:54] Marcelo Viciconti

La solucion funciona bastante bien. Pero me esta pasando algo: Le pongo como objeto a buscar afi_afu_soli_apo_rec pero me trae resultados de afu_soli_abordar1. Hay alguna razon para esto?
Si quiere probarlo en la base de datos Ospicine_Prod, puede usar las credenciales de las otras bases que posee para hacerlo. No precisa cambiar datos.

---

### [26/08/2026 16:40:05] Agente

Marcelo, sí: la razón era el filtro.

El SQL todavía tenía esta lógica:

```sql
AND t.text LIKE @var_ObjectName
```

Eso no filtra “el SP cuyo plan quiero ver”; filtra cualquier módulo cuyo texto contenga ese nombre. En `Ospicine_Prod` validé que `dbo.afu_soli_abordar1` entra por ese motivo: su definición contiene una llamada a `EXEC afi_afu_soli_apo_rec`.

Evidencia en `Ospicine_Prod`:

```text
Filtro por texto '%afi_afu_soli_apo_rec%':
dbo.afu_soli_abordar1 | 4 statements | 12 reads

Filtro por objeto exacto:
dbo.afi_afu_soli_apo_rec existe con object_id 1833226477
pero no tiene entradas actuales en sys.dm_exec_query_stats
```

Corregí el archivo en tu carpeta:

[dondeseutilizaunindice_corregido.sql](/mnt/Agentes/mviciconti/dondeseutilizaunindice_corregido.sql)

Cambio aplicado: ahora resuelve el objeto con `OBJECT_ID()` y filtra por `t.objectid`, no por aparición textual dentro de otros SPs. También reemplacé el `sp_name` parseado a mano por `OBJECT_SCHEMA_NAME(t.objectid, t.dbid) + '.' + OBJECT_NAME(t.objectid, t.dbid)`.

Validaciones: consulta read-only contra `Ospicine_Prod`, archivo final ISO-8859-1, CRLF, sin tabs, sin trailing spaces, sin `RTRIM`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
