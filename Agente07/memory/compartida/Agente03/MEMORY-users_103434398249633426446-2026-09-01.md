# Memoria de conversación — Marcelo Viciconti

## Datos

- **Fecha:** 01/09/2026
- **Usuario:** Marcelo Viciconti
- **ID Google Chat:** `users/103434398249633426446`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 6
- **Inicio:** 10:27:30
- **Fin:** 10:55:50

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/25hnskaaaae`

## Archivos revisados

- `18bfd820-3f5c-4d45-bfa0-1d3911024963.jsonl`

---

## Transcripción completa

### [01/09/2026 10:27:30] Marcelo Viciconti

Tengo el sp afs_soli_pendientes_cab que hace index scan sobre SOLICITUDES.pk_solicitudes porque en el WHERE tiene muchos OR.
Y ademas tiene varios UPDATE sobre la tabla #T que contiene en este caso mas de 22k registros.
QUe se le ocurre para hacerlo mejor performante? No se si es util que devuelva tantos registros cuando trabaja con las solicitudes.

---

### [01/09/2026 10:28:44] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [01/09/2026 10:38:25] Marcelo Viciconti

Tengo el sp afs_soli_pendientes_cab que hace index scan sobre SOLICITUDES.pk_solicitudes porque en el WHERE evalua los parametros con OR.
Ademas existen varios UPDATE sobre la tabla #T que contiene en este caso mas de 22k registros.
QUe se le ocurre para hacerlo mejor performante? No se si es util que devuelva tantos registros cuando trabaja con las solicitudes.
Usar la version del trunk. (Primero refresquelo)

---

### [01/09/2026 10:41:27] Agente

Marcelo, revisé trunk refrescado a r84483. El SP está en `backend/procedures/afs_soli_pendientes_cab.sql`.

**Evidencia**
El problema no es solo el `OR` de parámetros. Hay tres focos claros:

1. La primera carga a `#T1` trae todas las `SOLICITUDES` con `baja_fecha IS NULL` y `apli_fecha IS NULL`, sin aplicar fecha ni filtros de entidad/cuenta/sucursal/etc. Si eso hoy son 22k, el resto del SP trabaja innecesariamente sobre 22k filas.

2. La segunda carga usa filtros opcionales del tipo:
`(s.cuenta = @prm_cuenta OR @prm_cuenta IS NULL)`
Eso suele impedir planes selectivos estables. Con muchos parámetros opcionales, SQL Server termina eligiendo scan o planes promedio malos.

3. Después de cargar `#T1`, el SP hace muchos `UPDATE` contra la misma temporal:
contra/nombre, total, aceptadas, rechazadas, usuario, estado, confirma. Eso multiplica lecturas contra `SOLI_AFILIADOS` y `SOLI_AUDITORIA`.

**Hipótesis**
El mayor ahorro vendría de reducir filas antes de llenar `#T1`. Si funcionalmente no tiene sentido mostrar 22k solicitudes pendientes en una DataWindow, la mejor mejora no es de índice: es de contrato funcional. Obligar filtros mínimos, fecha acotada, entidad/cuenta o paginado cambia mucho más que ajustar índices.

**Recomendación técnica**
Yo lo encararía en este orden:

1. Corregir la carga inicial para que ambas ramas apliquen filtros equivalentes, o separar explícitamente “pendientes sin aplicar” de “aplicadas pendientes” si la primera rama tiene una lógica funcional distinta. Hoy la rama `apli_fecha IS NULL` parece demasiado abierta.

2. Reemplazar los `UPDATE` por una única carga set-based: insertar en `#T1` ya con los datos agregados desde derivados/CTE:
`SOLI_AFILIADOS` agrupado por `soli` para `total`, `ace`, `recha`, existencia de `X`, primer item y titular.
`SOLI_AUDITORIA` prefiltrado por `soli` para usuario.
Así se evita recorrer `#T1` 7 u 8 veces.

3. Agregar índice a `#T1` inmediatamente después de cargarla si se mantiene la temporal:
clustered/nonclustered por `soli`. Con 22k filas y varios joins posteriores, ayuda.

4. Para los filtros opcionales, usar `OPTION (RECOMPILE)` como mejora rápida o SQL dinámico parametrizado como mejora más sólida. `OPTION (RECOMPILE)` permite que SQL Server elimine predicados `@param IS NULL` en tiempo de compilación. Si el SP se ejecuta mucho y con combinaciones muy distintas, SQL dinámico con solo los filtros informados suele dar mejores seeks.

5. Revisar índice real de `SOLICITUDES`. En trunk solo veo `pk_solicitudes` por `soli` y `ndx_solicitudes_baja_apli_fecha` por `(baja_fecha, apli_fecha)` incluyendo `soli_moti`, `prepaga`, `plan_codi`. Para este SP probablemente falta un índice orientado a búsqueda por `baja_fecha`, `esta`, `fecha` y filtros frecuentes.

Mi diagnóstico: primero limitar el universo de solicitudes; segundo consolidar los `UPDATE`; tercero recién ajustar índices. Si la pantalla devuelve 22k filas, aunque el SQL mejore, PowerBuilder/DataWindow también va a seguir pagando costo de red, render y memoria.

---

### [01/09/2026 10:51:37] Marcelo Viciconti

Reescriba el sp con sus mejoras propuestas de codigo. Deje el sp en mi carpeta

---

### [01/09/2026 10:55:50] Agente

Marcelo, dejé la versión reescrita en [afs_soli_pendientes_cab_optimizado.sql](/mnt/Agentes/mviciconti/afs_soli_pendientes_cab_optimizado.sql).

Tomé trunk refrescado a r84483 y no modifiqué el SP original del repositorio. La versión propuesta reduce el universo antes de cargar `#T1`, elimina el `UNION`, agrega índice clustered sobre la temporal por `soli`, consolida los cálculos de `SOLI_AFILIADOS` en una sola pasada y deja `OPTION (RECOMPILE)` para mejorar el plan con parámetros opcionales.

Supuesto importante: apliqué los filtros de fecha/parámetros también a las solicitudes con `apli_fecha IS NULL`. Eso es lo que más debería bajar volumen, pero es un cambio funcional respecto del SP actual, que trae esas solicitudes casi sin filtrar.

Validaciones ejecutadas: archivo creado con CRLF, sin TABs, sin `RTRIM`, sin `TRIM()`, sin cambios locales sobre el SP original del trunk. No ejecuté compilación en base porque no indicaste profile de DB para instalar/probar el procedimiento.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
