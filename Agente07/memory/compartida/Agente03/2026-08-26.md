# Memoria 2026-08-26

## ROISA / RM 26993
- Usuario: Cristian.
- Objetos de trabajo exclusivos para ROISA: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd` y `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/afs_compro_impresion_roisa.sql`.
- Restriccion vigente del usuario: no modificar ramas SVN.
- Estado aplicado informado: cambios hechos solo sobre esos dos objetos, sin tocar ramas SVN.
- SP `afs_compro_impresion_roisa.sql`: debe traer desde `COMPROBANTES` exactamente los campos `total`, `grava` y `exen`; se quito `saldo` de la salida y se agrego/reemplazo por `exen = ISNULL(c.exen, 0)`.
- DW `d_factura_roisa.srd`: se reemplazo `saldo` por `exen`; `subtotal_a` calcula `grava + exen`.
- DW vencimientos ROISA: importes de vencimientos A/B para vto1, vto2 y vto3 calculan `total + deuda - creditos`.
- DW barra ROISA: `barra`, `barra_128` y `barra_numeros` se ocultan cuando el primer vencimiento neto da `0`, respetando la formula de ocultamiento solicitada.
- Validaciones informadas: SRD preservado como UTF-8 con BOM y CRLF; SQL preservado como ISO-8859-1 sin BOM y CRLF; confirmado que no queda `c.saldo`, `dbname="saldo"` ni `name=saldo`; confirmado que `total`, `grava` y `exen` aparecen dos veces en el SP, una por cada SELECT del `UNION ALL`.
- Pendiente/no ejecutado: compilacion PowerBuilder y validacion contra base.

## Walter / RM 29392
- Usuario interactuante: Walter (`wguise`). Trabaja con prompts SVN estandar y espera informes en rutas `X:\wguise\Rxxxxx\`.
- RM 29392: se genero `X:\wguise\R29392\ticket_29392_01_bug.md` usando prompt `prompts_std/01_bug.md`, documentos `RM29392 - Analisis Bender.docx` y `R29392.docx`, Redmine 29392 y rama SVN `V25.08.001`.
- Luego se valido contra base `mutualRioIV_Prod` / `mutualRioIV_prod` con consultas solo lectura.
- Defecto confirmado en `prs_consexpliquidados`: DataWindow muestra `totalapagae` como "Total a pagar"; el SP usa el total completo de la nota en `LIQUI_COMPROBANTES`, lo que falla cuando una misma nota agrupa varios expedientes.
- Evidencia DB relevante: DI `C600100001865` asociada a 9 expedientes tipo `S`; total nota `-2.300.353,21` coincide con suma de debitos propios. SP devuelve 68 registros para el rango; `Total a pagar` actual `138.749.547,85` vs control `total - debito` `157.228.260,53`, diferencia `-18.478.712,68`.
- Walter confirmo que las condiciones de correccion son las mismas que se aplican hoy: actualmente no hay filtro por tipo y no debe agregarse filtro por tipo para `totalapagae`.
- Walter confirmo que tambien debe contemplarse tipo `G` segun documento: corregir calculo de `debito` a `debito = deb.debito`, quitando la resta que hoy lo deja en cero para expedientes globales.
- Se genero especificacion tecnica `X:\wguise\R29392\ticket_29392_especificacion_tecnica.md` con 1 objeto a modificar: `backend/procedures/prs_consexpliquidados.sql`; sin cambios PowerBuilder.
- Cambio tecnico especificado RM 29392: `debito = deb.debito` y `totalapagae = ex.total + ex.iibb - deb.debito`; validar casos representativos `S`, `F` y `G`.

## Walter / RM 30107
- Se genero analisis `X:\wguise\R30107\ticket_30107_02_nuevo.md` con prompt `prompts_std/02_req_nuevo.md`, documento `X:\wguise\R30107\R-09_Documento de Requerimiento Detallado.doc`, rama SVN `V25.08.001`, sin base de datos.
- Requerimiento RM 30107: ordenar alfabeticamente los filtros `Sucursal`, `Situacion terapeutica` y `Tipo de autorizacion` en `Consulta de autorizaciones por sector`.
- Analisis identifica resolver preferentemente en DDW/procedimientos reutilizables que alimentan combos, sin modificar logica de busqueda.
- Walter confirmo que el orden alfabetico por descripcion es aceptable en otras ventanas que reutilicen `dd_situ_tera` y `dd_usuari_sucur_config`.
- Walter confirmo que las opciones generales `Todas` / `Todos` deben permanecer como primera opcion, independientemente del orden alfabetico del resto.
- Se genero especificacion tecnica `X:\wguise\R30107\ticket_30107_especificacion_tecnica.md` con 3 objetos a modificar: `dd_situ_tera.srd`, `prs_sucursal_x_usuario.sql` y `prs_tipo_auto_x_usuario.sql`; no aplica cambios todavia, documento de revision previo al prompt 04.

## Walter / RM 27864
- Se genero analisis de requerimiento nuevo `X:\wguise\R27864\ticket_27864_02_nuevo.md` usando prompt `prompts_std/02_req_nuevo.md`, documento `X:\wguise\R27864\R-01_R27864_Documento_Especificacion_Funcional.docx` y rama indicada como productiva; sin base de datos.
- RM 27864 impacta ventana `Prestaciones a Refacturar`: agregar columnas informativas en grilla, impresion y exportacion; corregir filtro de prestador para internacion/cirugia; recuperar expedientes odontologicos.
- Componentes centrales identificados: `prs_lst_prestac_refac_items`, `wp_auto_prestac_refac_cons` y `d_auto_prestac_refac_lst`.
- Estado RM 27864: listo para especificacion tecnica; sin definiciones funcionales bloqueantes, solo confirmar base objetivo si luego se requiere validacion con datos reales.
