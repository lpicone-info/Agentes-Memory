## 2026-08-14 14:33 ART - Pre-compaction flush

- Usuario Cristian/Crato preferencias vigentes: para Redmine usar credencial `cratto`; `mi carpeta` equivale a `/mnt/Agentes/cratto`; para codigo fuente, ramas SVN solo lectura; no modificar ramas SVN; no hacer commits SVN.
- RM30192 odontologia: objeto `/mnt/Agentes/cratto/RM30192/BugLugarPatriciaOdon/wp_expe_odon.srw`. Cambio programado y confirmado OK por Cristian. Se agrego `wf_cargar_lugares_expediente()` para restaurar combo/campo `lugar` al prestador del expediente cuando `ue_autorizaciones()` rechaza una autorizacion y limpia `auto` por mensaje "El prestador de la orden no se corresponde con el expediente". Validacion estatica: UTF-16 LE, CRLF, iconv OK; no compilado PB.
- RM30192 prestaciones: objeto `/mnt/Agentes/cratto/RM30192/BugLugarPatriciaPresta/wp_expe_orde.srw`. Cambio programado y confirmado OK por Cristian. En `ue_eliminar`, luego de eliminar practica de `dw_p` y `ue_restaurar`, se limpian explicitamente en `dw_det`: `nac_prov_efe`, `efe_matri_naci`, `efe_nombre`, `efe_prestad`, `efe_cate`, `ori_matri_naci`, `ori_nombre`, `ori_prestad`, `nac_prov_ori`. Validacion estatica: UTF-16 LE BOM, CRLF, iconv OK; no compilado PB.
- RM26993 contexto vigente: base previa `ROISA_Prod_Test`; no tocar ramas SVN; ruta previa Roisa `/mnt/Agentes/cratto/RM26993/ObjetosRoisa`; objetos modificados previamente `afs_compro_impresion_roisa.sql` y `d_factura_roisa.srd`; cambio anterior OK sobre calculo de `grava`, subtotal, total factura y vencimientos para ROISA.
- RM26993 Activa Salud: ruta de trabajo `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/`; objeto `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`; caso de prueba `B000300003628`. Se propuso y programo corregir vencimientos Vto1/Vto2 usando criterio confirmado de `d_factura_solo_medimas`: `imporN + saldo_anteriorN`. Cambios aplicados: `saldo_anterior2 = deuda2 - creditos`; `total_factura_a_vto1` y `total_factura_b_vto1` usan `impor1 + saldo_anterior1`; `total_factura_a_vto2` y `total_factura_b_vto2` usan `impor2 + saldo_anterior2`; Vto3 sin cambios porque no existe `saldo_anterior3`. Validacion estatica: UTF-16 LE BOM, CRLF, iconv OK; no compilado PB; no SVN ni commit.
