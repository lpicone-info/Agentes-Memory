# Memoria curada diaria

- Fecha: 2026-07-30
- Usuario: Marcelo Viciconti
- id_googlechat: users/103434398249633426446
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/25hnskaaaae

## Resumen de la actividad

### Tema 1: Diagnóstico de falla de MERGE por duplicación de clave en R30010

- Solicitud o contexto: Marcelo pidió revisar en su carpeta el script `r30010\r30010_Fix.sql`, leer la especificación `R-01_R30010.docx` y explicar por qué el `MERGE` fallaba por duplicación de clave. Luego precisó que se debía leer el punto `1.2.3 Actualización de datos` y dejar el script resultante con otro nombre en la misma carpeta.
- Respuesta o acciones del agente: Se cargaron las preferencias de Marcelo y se revisaron `/mnt/Agentes/mviciconti/r30010/r30010_Fix.sql` y `/mnt/Agentes/mviciconti/r30010/R-01_R30010.docx`. Se identificó que el origen `#TEMP_ADA_FINAL` podía generar más de una fila para la clave de `AFI_DERIV_APORTES`: `prepaga, contra, inte, vigen_desde, obra_soc, cuit`.
- Resultado: Se generó `/mnt/Agentes/mviciconti/r30010/r30010_Fix_corregido.sql`. La causa informada fue que el script armaba varias etapas con `UNION` y normalizaba `vigen_desde` en `#TEMP_ADA_NEW` aunque ya existiera el registro del primer día, provocando que el `MERGE` intentara actualizar o insertar más de una vez la misma clave destino. El script corregido eliminó el `MERGE` y separó la lógica en cierre de registros activos observados, reactivación del registro del día 1 si existía, e inserción de un solo registro por clave normalizada si no existía. No se ejecutaron cambios contra la base.
- Estado: Resuelto

### Tema 2: Solución simple desde especificación, sin usar el código provisto

- Solicitud o contexto: Marcelo pidió releer el DOCX y generar una solución nueva, sin usar el código provisto, lo más simple posible en dos o tres pasos.
- Respuesta o acciones del agente: Se releyó la especificación y se generó una solución nueva en `/mnt/Agentes/mviciconti/r30010/r30010_solucion_simple.sql`.
- Resultado: El script quedó organizado en tres pasos: cerrar registros activos observados poniendo `baja_fecha = vigen_desde`; reactivar el registro normalizado al día 1 si ya existía y estaba dado de baja; insertar el registro normalizado al día 1 cuando no existía. Se validó ASCII, finales CRLF, sin tabs y sin `MERGE`. No se ejecutaron cambios contra la base.
- Estado: Resuelto

### Tema 3: Corrección de interpretación de preferencias SQL de Marcelo

- Solicitud o contexto: Marcelo corrigió al agente indicando que sus preferencias eran usar `;` al final de las sentencias SQL y, si existía `TRIM`, reemplazarlo por `LTRIM(RTRIM())`.
- Respuesta o acciones del agente: Se aceptó la corrección de criterio y se ajustó `/mnt/Agentes/mviciconti/r30010/r30010_solucion_simple.sql`.
- Resultado: El script fue corregido para usar `;` al final de las sentencias. Se validó que no contenía `TRIM`, por lo que no había reemplazo por `LTRIM(RTRIM())` que aplicar. También quedó ASCII, CRLF, sin tabs y sin `MERGE`.
- Estado: Resuelto
