# Memoria curada diaria

- Fecha: 2026-08-04
- Usuario: Cristian Iván Ratto
- id_googlechat: users/112625344596118312340
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/vmclokaaaae

## Resumen de la actividad

### Tema 1: Alta y validación del profile ROISA_Prod_Test

- Solicitud o contexto: Cristian pidió analizar Redmine 26993 sobre problemas en `d_factura_roisa`, indicando inicialmente `BASE DE DATOS` vacía y luego el profile `ROISA_Prod_Test`, que no existía en `DB_PROFILE.md`.
- Respuesta o acciones del agente: Se detuvo el análisis hasta contar con un profile válido. Luego se solicitó la información obligatoria para registrar el profile MS SQL y, una vez completa, se registró `ROISA_Prod_Test`.
- Resultado: Profile `ROISA_Prod_Test` registrado y conexión validada OK en `DB_PROFILE.md`. No se guardó en memoria ningún valor secreto.
- Estado: Resuelto

### Tema 2: Corrección de ROISA para Redmine 26993

- Solicitud o contexto: Cristian pidió comparar `d_factura_roisa` contra otros objetos de impresión que calculan correctamente y corregir el problema donde el saldo a favor afectaba subtotal y TOTAL FACTURA. El ticket involucrado fue Redmine 26993, rama SVN productiva/trunk, usuario `cratto`, base `ROISA_Prod_Test`.
- Respuesta o acciones del agente: Se consultó Redmine 26993 y se revisó la rama productiva local. Se compararon patrones con `d_factura_femesa.srd`, `d_factura_ospania.srd`, `d_factura_celer.srd` y `d_factura_britanico.srd`. Se corrigió `d_factura_roisa.srd` para que `subtotal_b`, `subtotal_a`, `total_factura_a` y `total_factura_b` sumen `creditos` a `impor1`, mostrando el importe total real del comprobante. Se ajustó `afs_compro_impresion_roisa.sql` para que el importe en letras use `impor1 + creditos`.
- Resultado: Los casos reales validados en `ROISA_Prod_Test` cerraron con la suma de ítems: `B007000020517` y `B007000017622`. Los archivos corregidos se copiaron luego a `/mnt/Agentes/cratto/RM26993/ObjetosRoisa` tras corregir la ruta destino informada.
- Validaciones: Repo SVN validado en trunk revisión `84123`; `svn diff` revisado; SRD conservó UTF-8 con BOM + CRLF; SQL conservó ISO-8859-1 + CRLF; copias validadas byte a byte. No se ejecutó `svn commit`.
- Estado: Resuelto

### Tema 3: Revisión y corrección inicial de d_factura_caseros para Igualdad/SMC

- Solicitud o contexto: Cristian pidió aplicar correcciones similares a `d_factura_caseros`, dejando objetos en `/mnt/Agentes/cratto/RM26993/informe/ObjetosIgualdadSalud`. La carpeta no existía; luego indicó `/mnt/Agentes/cratto/RM26993/ObjetosIgualdadSalud`.
- Respuesta o acciones del agente: Se detuvo ante la primera ruta inexistente y se continuó con la ruta corregida. Se revisó `d_factura_caseros` contra patrones de `d_factura_ospania`, `d_factura_femesa` y la corrección aplicada en ROISA. Se modificó `d_factura_caseros.srd` para reconstruir subtotal y TOTAL FACTURA con `impor1 + creditos`, habilitar el código de barras solo si el neto de primer vencimiento queda mayor a cero y calcular el primer vencimiento como `impor1 + deuda1 - creditos` con piso cero. Se alineó el importe en letras en `afs_compro_impresion_caseros.sql`.
- Resultado: Se copiaron `d_factura_caseros.srd` y `afs_compro_impresion_caseros.sql` a `/mnt/Agentes/cratto/RM26993/ObjetosIgualdadSalud` y se validaron byte a byte.
- Validaciones: Encoding/BOM/EOL preservados; `svn diff` revisado; no se ejecutó `svn commit`; no se validó con datos reales de Igualdad porque no había profile indicado/registrado para esa base.
- Estado: Resuelto

### Tema 4: Interpretación de Histo 6 y Excel de análisis de comprobantes

- Solicitud o contexto: Cristian consultó si la Histo 6 de Redmine era una tarea o solo aclaraciones, y luego compartió un Excel para comprender el criterio funcional.
- Respuesta o acciones del agente: Se interpretó la Histo 6 como síntesis funcional con requerimiento general, no como simple aclaración. Se explicó que SUBTOTAL y TOTAL FACTURA no deben quedar afectados por saldo a favor; que el saldo a favor debe impactar en VENCIMIENTO; que el saldo deudor depende del formato/cliente; y que los parámetros `aplicación_auto_cred` y `aplica_auto_n_cred` deben respetarse. Del Excel se identificó la hoja `Revisión 07-2026` como matriz por cliente/formato.
- Resultado: Se aclaró que para Igualdad Salud la fila relevante indicaba `Contra_forma = I` y `Nombre Formato Comprobante = d_factura_igualdad`, por lo que el objeto correcto para Igualdad no era `d_factura_caseros`.
- Estado: Informativo

### Tema 5: Relación prepa_afi_reglas.contra_forma, uo_nv_factu y corrección final de d_factura_igualdad

- Solicitud o contexto: Cristian explicó que `prepa_afi_reglas` guarda `contra_forma`, que cada base puede tener letras o números distintos y que esa configuración alimenta el `choose case` del método `of_compro_imprime` en `uo_nv_factu`. Informó que la base usada como ejemplo estaba mal configurada, lo que explicaba el error. Luego pidió corregir efectivamente `d_factura_igualdad` según la fila 11 del Excel y guardar el objeto y su SP en `/mnt/Agentes/cratto/RM26993/ObjetosIgualdadSalud`, sobrescribiendo si era necesario.
- Respuesta o acciones del agente: Se registró que el problema dependía de la combinación `prepa_afi_reglas.contra_forma` -> `uo_nv_factu.of_compro_imprime` -> DataWindow elegido -> cálculo impreso. Se corrigió `d_factura_igualdad.srd` según fila 11 del Excel y se identificó que su SP asociado era `afs_compro_impresion_caseros.sql`, porque ese DataWindow llama a ese procedimiento.
- Resultado: Se copiaron y sobrescribieron en `/mnt/Agentes/cratto/RM26993/ObjetosIgualdadSalud` los archivos `d_factura_igualdad.srd` y `afs_compro_impresion_caseros.sql`.
- Validaciones: Copias validadas por SHA-256 contra los archivos corregidos del SVN local; encoding/BOM/EOL preservados. No se hizo commit ni validación contra base Igualdad por falta de profile registrado.
- Estado: Resuelto
