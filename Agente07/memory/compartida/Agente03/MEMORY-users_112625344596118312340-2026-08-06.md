# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Cristian Iván Ratto
- id_googlechat: users/112625344596118312340
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/vmclokaaaae

## Resumen de la actividad

### Tema 1: apertura de sesión y carga de preferencias

- Solicitud o contexto: Cristian abrió la conversación con un saludo.
- Respuesta o acciones del agente: se ejecutó el bootstrap operativo y se cargaron las preferencias de Cristian.
- Resultado: sesión disponible para trabajo operativo.
- Estado: Resuelto

### Tema 2: RM26762 - análisis de pedido y solución

- Solicitud o contexto: Cristian pidió identificar qué se solicitaba y cómo solucionar el Redmine 26762, usando trunk, sin base informada, y guardarlo en `/mnt/Agentes/cratto/RM26762/Informe`.
- Respuesta o acciones del agente: se consultó Redmine con usuario `cratto`, se extrajo y revisó el adjunto `.doc`, y se validó la rama SVN local sin modificarla.
- Resultado: se generó `/mnt/Agentes/cratto/RM26762/Informe/ticket_26762.md`. El informe concluyó que el alcance era ajustar el defecto del botón “Motivos de Alerta del Afiliado” en la consulta de autorizaciones desde supervisión; la situación terapéutica quedó fuera de alcance según el DTI. La solución recomendada fue modificar `w_clase_auto.srw`, función `wf_afi_motivo_alerta`, para usar `li_prepaga` y `ls_contra` leídos desde `dw_afi` al llamar a `of_afi_motivo_alerta`, manteniendo la regla de mostrar el botón solo si `ai_canti > 1`.
- Estado: Resuelto

### Tema 3: RM30157 - diagnóstico y archivo modificado fuera de working copy SVN

- Solicitud o contexto: Cristian reportó que la pantalla tiraba `Expecting TRUE/FALSE expression` al hacer click en la solapa Prestaciones y pidió encontrar el error; luego pidió fragmentos de código y guardar una pantalla modificada en `/mnt/Agentes/cratto/RM30157/CambioPrimerErr`.
- Respuesta o acciones del agente: se inspeccionó `wp_list_global_auto.srw` y se explicó inicialmente que el riesgo estaba en el armado de `ls_filtro` antes de `SetFilter()` para `idw_prestaciones`, por concatenar `and` y usar referencias de tipo SQL. Luego se preparó una copia modificada fuera de SVN.
- Resultado: se guardó `/mnt/Agentes/cratto/RM30157/CambioPrimerErr/wp_list_global_auto.srw`. La copia conservó UTF-8 con BOM y CRLF, no se tocó la working copy SVN. La investigación continuó porque Cristian detectó que el problema también afectaba Materiales y que la causa real no era solamente el prefijo de columna.
- Estado: En curso

### Tema 4: RM30157 - causa final del error en `SetFilter()`

- Solicitud o contexto: Cristian probó múltiples variantes del filtro y demostró que funcionaba cuando el `LIKE` quedaba al final, por ejemplo `prestaciones.tipo = 'P' AND nomen = 5 and prestaciones.prestac like '250102%'`, pero fallaba cuando el `LIKE` quedaba en el medio.
- Respuesta o acciones del agente: se analizaron alias, columna `tipo`, comillas y sintaxis del DataObject; luego se reconoció que, con la información y pruebas aportadas, la conclusión correcta era que el parser de `SetFilter()` quedaba sensible a la posición del `LIKE`.
- Resultado: se consolidó la regla de solución para este caso: en filtros de DataWindow con `SetFilter()`, armar primero condiciones exactas y dejar cualquier condición con `LIKE` al final de la expresión. Se reconoció que la causa debió cerrarse antes con la información ya provista por Cristian.
- Estado: Resuelto

### Tema 5: RM30192 - primer análisis desde rama de Incidentes

- Solicitud o contexto: Cristian cambió de tema y pidió ejecutar el análisis del Redmine 30192 con prompt informado como `02_nuevo.md`, rama `https://srv-infosvn.infomedical.com/svn/Incidentes/Incidentes/R30192`, salida `/mnt/Agentes/cratto/RM30192/ticket_30192.md`.
- Respuesta o acciones del agente: se verificó que `02_nuevo.md` no existía en `prompts_std` y se usó el equivalente disponible `02_req_nuevo.md`. Se tomó como fuente principal `R30192.docx` y como complemento Redmine 30192 con relaciones a RM21807, RM28808, RM30074 y RM30136.
- Resultado: se generó `/mnt/Agentes/cratto/RM30192/ticket_30192.md`. El resumen indicó que la validación nueva de correspondencia prestador/efector bloqueaba antes de respetar el parámetro `liquida_efector_diferente`, y recomendó ajustar PB para que expedientes de prestador normal con parámetro `S` permitan continuar con advertencia, revisando Ordenes, Odontología y Sanatorio. No se modificaron fuentes SVN.
- Estado: Resuelto

### Tema 6: perfiles de base y alta de `ACTIVA_Prod`

- Solicitud o contexto: Cristian pidió listar sus perfiles de base y saber si existía `ACTIVA_Prod`; luego pidió registrar ese profile en `DB_PROFILE.md`.
- Respuesta o acciones del agente: se revisó `DB_PROFILE.md`, se informó que `ACTIVA_Prod` no estaba registrado, se cargó la skill de alta de profile, se validó conectividad TCP y conexión real a la base con `SELECT DB_NAME()` usando la credencial provista, sin copiar secretos en memoria.
- Resultado: se agregó el profile `ACTIVA_Prod` en `/home/luispicone/.openclaw/workspace/DB_PROFILE.md`; quedó una sola entrada para ese profile y la conexión fue validada correctamente.
- Estado: Resuelto

### Tema 7: RM30192 - segundo análisis sobre trunk con base `ACTIVA_Prod`

- Solicitud o contexto: Cristian pidió un nuevo análisis del Redmine 30192 sobre trunk, usando base `ACTIVA_Prod`, con instrucción explícita de no tocar objetos de base de datos ni PowerBuilder, salida `/mnt/Agentes/cratto/RM30192/Analisis/ticket_30192.md`.
- Respuesta o acciones del agente: se consultó memoria previa, se leyó el prompt `02_req_nuevo.md`, se consultó Redmine 30192, se inspeccionaron fuentes en trunk y se hizo una consulta read-only a `ACTIVA_Prod` para confirmar el parámetro `liquida_efector_diferente = S`.
- Resultado: se generó `/mnt/Agentes/cratto/RM30192/Analisis/ticket_30192.md`. Se dejó asentado que Redmine no tenía adjuntos, por lo que el análisis formal se basó en ticket, comentarios, relaciones, trunk y consulta read-only de base. No se tocaron objetos PB ni BD; se observaron cambios previos ajenos en trunk, sin modificarlos.
- Estado: Resuelto
