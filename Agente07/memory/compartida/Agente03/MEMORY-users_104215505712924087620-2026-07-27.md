# Memoria curada diaria

- Fecha: 2026-07-27
- Usuario: Federico Acevedo
- id_googlechat: users/104215505712924087620
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/l2edokaaaae

## Resumen de la actividad

### Tema 1: Inicio de sesión y carga de preferencias

- Solicitud o contexto: Federico inició una consulta técnica sobre un script SQL.
- Respuesta o acciones del agente: Antes de responder la consulta, se ejecutó el bootstrap operativo, se revisaron los archivos rectores del workspace y se cargaron las preferencias asociadas a Federico.
- Resultado: El agente quedó disponible para trabajar con las preferencias cargadas.
- Estado: Informativo

### Tema 2: Script SQL para índice en AUTO_PRESTAC_ITEMS_REMOTAS

- Solicitud o contexto: Federico compartió un script que verificaba la existencia del índice `ik_auto_prestac_items_remotas`, lo eliminaba si existía y luego lo recreaba sobre `dbo.AUTO_PRESTAC_ITEMS_REMOTAS` con columnas `(transac, item, nomen, prestac)` e `INCLUDE (canti, esta, recha, pieza, cara)`.
- Respuesta o acciones del agente: Se indicó que `OBJECT_ID('AUTO_PRESTAC_ITEMS_REMOTAS')` depende del esquema por defecto, que es más seguro usar `OBJECT_ID('dbo.AUTO_PRESTAC_ITEMS_REMOTAS', 'U')`, que en SQL Server corresponde usar `DROP INDEX ik_auto_prestac_items_remotas ON dbo.AUTO_PRESTAC_ITEMS_REMOTAS`, y que borrar y recrear el índice es una acción destructiva sobre un objeto de base de datos.
- Resultado: Se entregó una versión ajustada del script de eliminación y creación, y se pidió precisar el problema concreto.
- Estado: Resuelto

### Tema 3: Evitar recreación repetida del índice

- Solicitud o contexto: Federico preguntó cómo hacer para que cada ejecución del script no vuelva a borrar y crear el índice.
- Respuesta o acciones del agente: Se recomendó invertir la condición y crear el índice solo cuando no exista, usando `IF NOT EXISTS` contra `sys.indexes` y `OBJECT_ID('dbo.AUTO_PRESTAC_ITEMS_REMOTAS', 'U')`.
- Resultado: Se entregó un script idempotente que crea `ik_auto_prestac_items_remotas` solo la primera vez. Se aclaró que esta solución evita la recreación por nombre, pero no valida si el índice existente tiene exactamente las mismas columnas o columnas incluidas.
- Estado: Resuelto
