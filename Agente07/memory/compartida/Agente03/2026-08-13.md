
## 2026-08-13 12:36 ART - RM26993 ROISA

- Usuario Cristian / `cratto` esta trabajando el ticket Redmine `26993` sobre ROISA, base `ROISA_Prod_Test`, documento `/mnt/Agentes/cratto/RM26993/Analisis1/R26993_V2.docx`.
- Restriccion indicada por el usuario: exportar objetos en `/mnt/Agentes/cratto/RM26993/ObjetosRoisa` y no tocar ninguna rama SVN.
- Objetos exportados/modificados para ROISA: `afs_compro_impresion_roisa.sql` y `d_factura_roisa.srd`.
- SP `afs_compro_impresion_roisa`: se agrego al final del resultset el campo `grava`, tomando `ISNULL(c.grava, 0)` desde `COMPROBANTES` para comprobantes normales. Para rama `RS` quedo `grava = 0` porque no une contra `COMPROBANTES`.
- `d_factura_roisa.srd`: se agrego columna `grava`; `subtotal_a` usa `abs(if(isnull(grava),0,grava))`; `total_factura_a` y `total_factura_b` usan `abs(grava + iva_total + iibb_arba + iibb_caba)` con controles `if(isnull(...),0,...)`.
- Los vencimientos de `d_factura_roisa` fueron restaurados a la logica original: `total_factura_a_vto1`, `total_factura_a_vto2`, `compute_17`, `total_factura_b_vto1`, `total_factura_b_vto2`, `compute_8` quedan como `abs(impor1 + deuda1)`, `abs(impor2 + deuda2)`, `abs(impor3 + deuda3)` segun corresponda, con formatos originales `[General]` / `[GENERAL]`.
- Caso testigo B validado: afiliado `200010399432`, comprobante `B006400183675`; `grava=671894.94`, `iva_total=70548.97`, `iibb=0.00`, total formula `742443.91`; usa `total_factura_b` y `subtotal_a`.
- Caso testigo A con saldo a favor: afiliado/contra `20350509012`, comprobante `A006400002508`, tipo `FC`, fecha `30/04/2026`, total `596913.71`, grava `540193.40`, IVA `56720.31`, IIBB `0.00`, creditos `577844.82`, vto1 `19068.89`, vto2 `25141.49`. El segundo vencimiento sale de `COMPRO_IMPRESION.impor2`; diferencia contra vto1 `6072.60` ya viene incorporada.
- Busqueda de otros tipos con saldo a favor: no se encontraron letras distintas de A/B en 2025/2026 con saldo a favor aplicado. Como otro tipo operativo se encontro `ND` con letra B: afiliado/contra `200029637638`, comprobante `B006400049200`, fecha `24/04/2026`, total/grava/IVA `1766792.96` / `1598907.66` / `167885.30`, creditos `1766792.96`. Tambien `DI`: comprobante `B000100006406`, afiliado/contra `27388320600`, fecha `29/12/2025`, total/credito `378751.53`.
- Referencias de uso del SP: busqueda previa indico que `afs_compro_impresion_roisa` aparece usado directamente solo por `d_factura_roisa.srd` en la propiedad `procedure`; el flujo funcional llega desde `uo_nv_factu.sru` seleccionando `ls_datastore = 'd_factura_roisa'` para ROISA.
- Validaciones hechas durante el trabajo: `COMPROBANTES.grava` existe como `decimal(14,2)`; SQL validado con `PARSEONLY`; encoding preservado SQL ISO-8859 CRLF sin BOM y SRD UTF-8 con BOM CRLF; SVN fuente sin modificaciones.


## 2026-08-13 15:12 ART - Hugo harboleas V25.08.001 / RM24514

- Usuario Hugo / `harboleas` trabajo sobre repositorio SVN local `wguise-V25.08.001` en `/home/luispicone/.openclaw/workspace/svn/wguise-V25.08.001`, rama `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`.
- Se indentó el SP `afu_cod_oper_isss.sql` sin modificar el repositorio; salida generada en `/mnt/Agentes/harboleas/afu_cod_oper_isss.sql`.
- Origen del SP indentado: `/home/luispicone/.openclaw/workspace/svn/wguise-V25.08.001/backend/procedures/afu_cod_oper_isss.sql`.
- Validaciones del SP indentado: cambios solo whitespace, encoding `ISO-8859-1`, finales `CRLF`, sin agregar `;`, SVN sin modificaciones locales.
- Para Redmine `24514`, se genero informe usando prompt SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/01_bug.md`, DB `ospecon_prox_ver` / `Ospecon_Prox_Ver`, documento fuente `/mnt/Agentes/harboleas/R24514/Pruebas de AFILIACIONES-INCORPORACION-CAMBIO DERIVAPORTE 20260805.docx` (tambien referenciado con doble espacio antes de fecha en respuesta), salida `/mnt/Agentes/harboleas/R24514/ticket_24514_01_bug.md`.
- Resumen técnico RM24514: el problema se explica por reglas de derivación distribuidas en varios SPs; `ME` procesa solo titulares; las altas copian una única derivación existente; los caminos revisados no completan consistentemente `tipo_aportante` / `inte_titular_aportante` para titular e integrantes.
- El informe RM24514 dejo puntos pendientes de validación funcional antes de desarrollar. Validaciones realizadas: Redmine consultado, DOCX extraído, rama SVN validada, DB consultada solo lectura, repo sin cambios locales.
