# Memoria curada diaria

- Fecha: 2026-08-04
- Usuario: Hugo Armando Arboleas
- id_googlechat: users/114449115098924951452
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/jilgmkaaaae

## Resumen de la actividad

### Tema 1: R29398 error XML en exportación de débitos

- Solicitud o contexto: Hugo informó que con la solución propuesta en su carpeta `harboleas/R29398` aparecía un error y luego preguntó cuál era la corrección. Envió una captura sin texto. Más tarde aclaró que el error se producía en el SP `afi_compro_exportacion`.
- Respuesta o acciones del agente: Se revisó la propuesta en `/mnt/Agentes/harboleas/R29398`. Primero se detectó que el XML generado por `dw_list_det.Describe("DataWindow.Data.XML")` se enviaba directo al SP y se agregó limpieza de `char(2)` y `encoding="UTF-8"` en `wp_export_debitos.srw`. Tras la captura, se corrigió la limpieza porque el XML real venía con `encoding="UTF-16LE"`, agregando también limpieza de `encoding="UTF-16LE"` y `encoding="UTF-16"`.
- Resultado: Se actualizaron `/mnt/Agentes/harboleas/R29398/archivos_propuestos/exe/Source Infomedical/wp_export_debitos.srw` y `/mnt/Agentes/harboleas/R29398/ticket_29398_especificacion_tecnica.md`. Se explicó que la causa era el error de SQL Server `XML parsing: unable to switch the encoding`.
- Validaciones: El `.srw` quedó en UTF-8 con BOM y CRLF uniforme. No se pudo validar compilación PowerBuilder desde el entorno.
- Estado: Resuelto

### Tema 2: Corrección real en afi_compro_exportacion.sql

- Solicitud o contexto: Hugo precisó que el error ocurría en el SP `afi_compro_exportacion`, no solo en PowerBuilder.
- Respuesta o acciones del agente: Se revisó `afi_compro_exportacion.sql` y se concluyó que si el parámetro llega declarado como `xml`, SQL Server intenta parsearlo antes de entrar al cuerpo del procedure; por eso la limpieza hecha en PowerBuilder podía no alcanzar. Se cambió `@detalle_xml` a `nvarchar(max)` y dentro del SP se agregó limpieza de `char(2)`, `encoding="UTF-16LE"`, `encoding="UTF-16"` y `encoding="UTF-8"`, para luego convertir a XML en una variable `@detalle_xml_parseado`.
- Resultado: Se actualizó `/mnt/Agentes/harboleas/R29398/archivos_propuestos/backend/procedures/afi_compro_exportacion.sql` y la documentación técnica `ticket_29398_especificacion_tecnica.md`. El `.nodes()` quedó planteado sobre `@detalle_xml_parseado`.
- Validaciones: El `.sql` quedó ASCII con CRLF y el `.srw` continuó UTF-8 con BOM y CRLF. Se indicó que para probar nuevamente primero debe ejecutarse el script actualizado del SP en la base y luego correr la exportación.
- Estado: Resuelto
