# Memoria durable - 2026-08-25

## Caso SBSalud / RM26993

Contexto de trabajo: Cristian retomó el caso SBSalud en `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/`. No tocar SVN salvo pedido explícito.

Archivos relevantes:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Secuencia y estado final:
- Primero se había aplicado un cambio amplio tipo Roisa, luego Cristian pidió volver SBSalud como estaba antes.
- Se restauró SBSalud al estado anterior/original: SRD igual al backup original de `Objetos_OriginalesDeImpresion (BKP)` y SQL igual a referencia SVN, sin `total`, `grava`, `saldo` ni `LEFT JOIN comprobantes c`.
- Luego Cristian pidió aplicar el criterio Roisa a SBSalud, pero sin tocar vencimientos.
- Se aplicó inicialmente solo la parte fiscal: el SP devuelve `total` y `grava` desde `comprobantes`; el DW incorpora columnas `total` y `grava`; `TOTAL FACTURA A/B` usa `abs(if(isnull(total),0,total))`; `SUBTOTAL` usa `abs(if(isnull(grava),0,grava))`; vencimientos seguían `abs(impor1)`, `abs(impor2)`, `abs(impor3)`; no se agregó `saldo` en esa etapa.
- Después Cristian pidió dos ajustes: que los vencimientos tomen del nuevo campo `saldo` del SP, y aplicar fórmula de visualización para que la barra no se vea cuando el vencimiento sea 0.
- Estado final SBSalud aplicado: el SP devuelve también `saldo = ISNULL(c.saldo, 0)`; el DW incorpora la columna `saldo`; vencimientos calculan `Vto1 = abs(saldo + deuda1)`, `Vto2 = abs(saldo + deuda2)`, `Vto3 = abs(saldo + deuda3)`; `barra`, `barra_128` y `barra_numeros` se ocultan si `abs(saldo + deuda1) = 0`.
- Se mantiene: `TOTAL FACTURA A/B` toma `total`; `SUBTOTAL` toma `grava`.
- Validaciones informadas en ese momento: SQL conserva `ISO-8859` + `CRLF`; SRD conserva `UTF-16 LE BOM` + `CRLF`; `SET PARSEONLY ON` OK; `SET NOEXEC ON` OK; SVN no tocado.

Consulta técnica respondida sobre código de barras SBSalud:
- Para SBSalud la barra se arma antes, en `dbo.afi_compro_impresion`.
- El SP de impresión `afs_compro_impresion_caseros` no calcula la barra; solo lee `COMPRO_IMPRESION.barra` y `COMPRO_IMPRESION.barra_afip`.
- El DataWindow solo dibuja con `gf_interleaved_2de5(barra)` y `gf_code_128(barra)`.
- SBSalud usa el criterio `contra_forma = 'K'`, definido como `@con_contra_forma_sb_Salud`.
- La generación entra por la misma rama que Caseros: `dbo.afn_get_caseros_codibarra(cpagofacil, compro_nro, importe, importe2, vto1, vto2)`.
- En la base revisada, SBSalud tiene `cpagofacil = 6246` y no tiene `barra_ente_rec`, por eso cae en tipo barra `PF`.
- El importe que se manda a la barra sale de `COMPROBANTES`: `importe = si C.saldo > 0 entonces C.saldo, sino C.total`; `importe2 = si C.saldo <= 0 entonces 0, sino C.saldo + intereses del 2do vencimiento`.
- Conclusión comunicada: en SBSalud la barra se genera con el saldo vigente del comprobante cuando hay saldo positivo; no toma el `TOTAL FACTURA` fiscal mostrado en el DW.

## Caso Roisa / RM26993

Cristian confirmó: `Caso Roisa OK Cerrado`.

Archivo relevante:
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Estado final Roisa:
- `Subtotal`: usa `COMPROBANTES.grava`.
- `Total factura A/B`: usa `grava + iva_total + iibb_arba + iibb_caba`.
- Vencimientos: quedaron con el criterio ya validado previamente.
- Código de barras: se oculta cuando el vencimiento queda en `0`.
- Fórmula de visualización aplicada en `barra`, `barra_128` y `barra_numeros`: condición basada en `abs(if(isnull(saldo),0,saldo) + if(isnull(deuda1),0,deuda1)) = 0`.
- No se tocó SP en ese ajuste de barras.
- SRD conservó `UTF-8 BOM` y `CRLF`.
- SVN no tocado.

## Actualizaciones RM26993 - 2026-08-25 15:19 ART

### SBSalud - criterio final corregido

Cristian corrigió el criterio de vencimientos para SBSalud: deben usar solo el campo `saldo`, sin sumar `deuda1`, `deuda2` ni `deuda3`.

Estado final aplicado en:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`

Resultado:
- El SP ya trae `total`, `grava` y `saldo` desde `COMPROBANTES`.
- `TOTAL FACTURA A/B`: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL`: `abs(if(isnull(grava),0,grava))`.
- `Vto1`, `Vto2`, `Vto3`: `abs(if(isnull(saldo),0,saldo))` / criterio `abs(saldo)`.
- `barra`, `barra_128` y `barra_numeros` se ocultan cuando `abs(saldo) = 0`.
- Se validó que no quedara `saldo + deuda` en fórmulas de vencimiento/barra.
- SRD conserva `UTF-16 LE BOM`; SQL conserva `ISO-8859`; SVN no tocado.

Consulta adicional SBSalud:
- Parámetro general relacionado con aplicación de créditos/saldos: `Afilmed / Facturacion / aplicacion_auto_cred`.
- En `SBSalud_Prod_Test`: `aplicacion_auto_cred = S`.
- Para la barra, `afi_compro_impresion` no lee directamente ese parámetro en la función de barra; la barra queda afectada porque el `importe` enviado a `afn_get_caseros_codibarra` ya se calcula con `C.saldo` cuando `C.saldo > 0`, si no usa `C.total`.
- Si `@reg_cod_barra = 'N'`, no regenera barra y copia `COMPROBANTES.codi_barra` existente.

Consulta afiliado SBSalud 5223:
- Si tiene saldo en contra de `167828,68`, para dejar la cuenta corriente en `0` se indicó que corresponde una nota de crédito por `167828,68`, aplicada contra las FC abiertas que componen el saldo.

### SMC - análisis y aplicación de criterio SBSalud

Consulta técnica SMC:
- En `SMC_Prod_Test`, SMC usa el mismo circuito base que SBSalud para barra: `dbo.afi_compro_impresion` genera y `dbo.afs_compro_impresion_caseros` lee `COMPRO_IMPRESION.barra`.
- SMC tiene `contra_forma = 'C'` y usa `dbo.afn_get_caseros_codibarra(cpagofacil, compro_nro, importe, importe2, vto1, vto2)`.
- `cpagofacil` observado: `1504`.
- Importe para barra: si `COMPROBANTES.saldo > 0` usa `saldo`; si `saldo <= 0` usa `total`.
- Ejemplos revisados: `B000100002586` total `331500.00`, saldo `111021.16`, barra con monto `111021.16`; `B000100002585` total `110500.00`, saldo `0.00`, barra con monto `110500.00`.
- Conclusión: se comporta como SBSalud cuando hay saldo positivo.

Aplicación SMC:
- Archivo impactado: `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`.
- No se modificó el SQL porque ya traía `total = ISNULL(c.total, 0)`, `grava = ISNULL(c.grava, 0)` y `saldo = ISNULL(c.saldo, 0)`.
- DW: agregadas columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA A/B` toma `total`.
- `SUBTOTAL` toma `grava`.
- `Vto1` y `Vto2` toman solo `saldo`, sin deuda: `abs(if(isnull(saldo),0,saldo))`.
- Barra de pago se oculta cuando `saldo = 0`.
- `barra_afip` no fue modificada.
- SQL ISO-8859 + CRLF sin cambios; SRD UTF-16 LE BOM + LF preservado; SVN no tocado.

### ActivaSalud - análisis y aplicación de criterio SBSalud/SMC

Carpeta/archivo de trabajo informado por Cristian:
- `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

Consulta técnica previa:
- El DW de ActivaSalud no calcula código de barras de pago; solo dibuja campo `barra` con `gf_interleaved_2de5(barra)` y lo muestra si `impor1 > 0`.
- En `Activa_Prod_Test`, `COMPRO_IMPRESION.barra` estaba siempre `NULL`: `total_registros = 10934`, `con_barra = 0`, `con_barra_afip = 10308`.
- Sí tiene `barra_afip`, fiscal/AFIP, generada con `afn_get_afip_codibarra(ente, compro_tipo, compro_nro, cae, cae_vto)`.
- Activa tiene `contra_forma = 'G'`, no entra en la rama Caseros/SBSalud/SMC que arma barra con importe/saldo.
- Criterio previo de Activa: total fiscal por `impor1`, vencimientos neteados con `creditos`, sin barra de pago efectiva.

Consulta de impacto previa:
- Para aplicar `total = total`, `subtotal = grava`, `vencimientos = saldo`, había que tocar también `afs_compro_impresion_caseros.sql` porque Activa no devolvía `total`, `grava`, `saldo` al DW.
- Riesgo informado: facturas exentas podrían mostrar `SUBTOTAL = 0` si `grava = 0`, por ejemplo comprobante con `total = 40000`, `exen = 40000`, `grava = 0`.
- Vencimientos solo con `saldo` dejan fuera deuda previa acumulada si existía.

Aplicación ActivaSalud:
- Archivos impactados:
  - `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/afs_compro_impresion_caseros.sql`
  - `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`
- SP ahora devuelve `total`, `grava` y `saldo` desde `COMPROBANTES`.
- DW incorporó columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA A/B`: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL`: `abs(if(isnull(grava),0,grava))`.
- `Vto1`, `Vto2`, `Vto3`: `abs(if(isnull(saldo),0,saldo))`.
- Barra de pago se oculta si el saldo/vencimiento es `0`.
- `barra_afip` no fue modificada.
- Validado: SQL ANSI/ISO-8859 sin BOM + CRLF; SRD UTF-16 LE BOM + CRLF; SQL `SET PARSEONLY ON` sin errores; SVN no tocado.
# Memoria durable - 2026-08-25

## Caso SBSalud / RM26993

Contexto de trabajo: Cristian retomó el caso SBSalud en `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/`. No tocar SVN salvo pedido explícito.

Archivos relevantes:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`

Secuencia y estado final:
- Primero se había aplicado un cambio amplio tipo Roisa, luego Cristian pidió volver SBSalud como estaba antes.
- Se restauró SBSalud al estado anterior/original: SRD igual al backup original de `Objetos_OriginalesDeImpresion (BKP)` y SQL igual a referencia SVN, sin `total`, `grava`, `saldo` ni `LEFT JOIN comprobantes c`.
- Luego Cristian pidió aplicar el criterio Roisa a SBSalud, pero sin tocar vencimientos.
- Se aplicó inicialmente solo la parte fiscal: el SP devuelve `total` y `grava` desde `comprobantes`; el DW incorpora columnas `total` y `grava`; `TOTAL FACTURA A/B` usa `abs(if(isnull(total),0,total))`; `SUBTOTAL` usa `abs(if(isnull(grava),0,grava))`; vencimientos seguían `abs(impor1)`, `abs(impor2)`, `abs(impor3)`; no se agregó `saldo` en esa etapa.
- Después Cristian pidió dos ajustes: que los vencimientos tomen del nuevo campo `saldo` del SP, y aplicar fórmula de visualización para que la barra no se vea cuando el vencimiento sea 0.
- Estado final SBSalud aplicado: el SP devuelve también `saldo = ISNULL(c.saldo, 0)`; el DW incorpora la columna `saldo`; vencimientos calculan `Vto1 = abs(saldo + deuda1)`, `Vto2 = abs(saldo + deuda2)`, `Vto3 = abs(saldo + deuda3)`; `barra`, `barra_128` y `barra_numeros` se ocultan si `abs(saldo + deuda1) = 0`.
- Se mantiene: `TOTAL FACTURA A/B` toma `total`; `SUBTOTAL` toma `grava`.
- Validaciones informadas en ese momento: SQL conserva `ISO-8859` + `CRLF`; SRD conserva `UTF-16 LE BOM` + `CRLF`; `SET PARSEONLY ON` OK; `SET NOEXEC ON` OK; SVN no tocado.

Consulta técnica respondida sobre código de barras SBSalud:
- Para SBSalud la barra se arma antes, en `dbo.afi_compro_impresion`.
- El SP de impresión `afs_compro_impresion_caseros` no calcula la barra; solo lee `COMPRO_IMPRESION.barra` y `COMPRO_IMPRESION.barra_afip`.
- El DataWindow solo dibuja con `gf_interleaved_2de5(barra)` y `gf_code_128(barra)`.
- SBSalud usa el criterio `contra_forma = 'K'`, definido como `@con_contra_forma_sb_Salud`.
- La generación entra por la misma rama que Caseros: `dbo.afn_get_caseros_codibarra(cpagofacil, compro_nro, importe, importe2, vto1, vto2)`.
- En la base revisada, SBSalud tiene `cpagofacil = 6246` y no tiene `barra_ente_rec`, por eso cae en tipo barra `PF`.
- El importe que se manda a la barra sale de `COMPROBANTES`: `importe = si C.saldo > 0 entonces C.saldo, sino C.total`; `importe2 = si C.saldo <= 0 entonces 0, sino C.saldo + intereses del 2do vencimiento`.
- Conclusión comunicada: en SBSalud la barra se genera con el saldo vigente del comprobante cuando hay saldo positivo; no toma el `TOTAL FACTURA` fiscal mostrado en el DW.

## Caso Roisa / RM26993

Cristian confirmó: `Caso Roisa OK Cerrado`.

Archivo relevante:
- `/mnt/Agentes/cratto/RM26993/ObjetosRoisa/d_factura_roisa.srd`

Estado final Roisa:
- `Subtotal`: usa `COMPROBANTES.grava`.
- `Total factura A/B`: usa `grava + iva_total + iibb_arba + iibb_caba`.
- Vencimientos: quedaron con el criterio ya validado previamente.
- Código de barras: se oculta cuando el vencimiento queda en `0`.
- Fórmula de visualización aplicada en `barra`, `barra_128` y `barra_numeros`: condición basada en `abs(if(isnull(saldo),0,saldo) + if(isnull(deuda1),0,deuda1)) = 0`.
- No se tocó SP en ese ajuste de barras.
- SRD conservó `UTF-8 BOM` y `CRLF`.
- SVN no tocado.

## Actualizaciones RM26993 - 2026-08-25 15:19 ART

### SBSalud - criterio final corregido

Cristian corrigió el criterio de vencimientos para SBSalud: deben usar solo el campo `saldo`, sin sumar `deuda1`, `deuda2` ni `deuda3`.

Estado final aplicado en:
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`
- `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/afs_compro_impresion_caseros.sql`

Resultado:
- El SP ya trae `total`, `grava` y `saldo` desde `COMPROBANTES`.
- `TOTAL FACTURA A/B`: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL`: `abs(if(isnull(grava),0,grava))`.
- `Vto1`, `Vto2`, `Vto3`: `abs(if(isnull(saldo),0,saldo))` / criterio `abs(saldo)`.
- `barra`, `barra_128` y `barra_numeros` se ocultan cuando `abs(saldo) = 0`.
- Se validó que no quedara `saldo + deuda` en fórmulas de vencimiento/barra.
- SRD conserva `UTF-16 LE BOM`; SQL conserva `ISO-8859`; SVN no tocado.

Consulta adicional SBSalud:
- Parámetro general relacionado con aplicación de créditos/saldos: `Afilmed / Facturacion / aplicacion_auto_cred`.
- En `SBSalud_Prod_Test`: `aplicacion_auto_cred = S`.
- Para la barra, `afi_compro_impresion` no lee directamente ese parámetro en la función de barra; la barra queda afectada porque el `importe` enviado a `afn_get_caseros_codibarra` ya se calcula con `C.saldo` cuando `C.saldo > 0`, si no usa `C.total`.
- Si `@reg_cod_barra = 'N'`, no regenera barra y copia `COMPROBANTES.codi_barra` existente.

Consulta afiliado SBSalud 5223:
- Si tiene saldo en contra de `167828,68`, para dejar la cuenta corriente en `0` se indicó que corresponde una nota de crédito por `167828,68`, aplicada contra las FC abiertas que componen el saldo.

### SMC - análisis y aplicación de criterio SBSalud

Consulta técnica SMC:
- En `SMC_Prod_Test`, SMC usa el mismo circuito base que SBSalud para barra: `dbo.afi_compro_impresion` genera y `dbo.afs_compro_impresion_caseros` lee `COMPRO_IMPRESION.barra`.
- SMC tiene `contra_forma = 'C'` y usa `dbo.afn_get_caseros_codibarra(cpagofacil, compro_nro, importe, importe2, vto1, vto2)`.
- `cpagofacil` observado: `1504`.
- Importe para barra: si `COMPROBANTES.saldo > 0` usa `saldo`; si `saldo <= 0` usa `total`.
- Ejemplos revisados: `B000100002586` total `331500.00`, saldo `111021.16`, barra con monto `111021.16`; `B000100002585` total `110500.00`, saldo `0.00`, barra con monto `110500.00`.
- Conclusión: se comporta como SBSalud cuando hay saldo positivo.

Aplicación SMC:
- Archivo impactado: `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`.
- No se modificó el SQL porque ya traía `total = ISNULL(c.total, 0)`, `grava = ISNULL(c.grava, 0)` y `saldo = ISNULL(c.saldo, 0)`.
- DW: agregadas columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA A/B` toma `total`.
- `SUBTOTAL` toma `grava`.
- `Vto1` y `Vto2` toman solo `saldo`, sin deuda: `abs(if(isnull(saldo),0,saldo))`.
- Barra de pago se oculta cuando `saldo = 0`.
- `barra_afip` no fue modificada.
- SQL ISO-8859 + CRLF sin cambios; SRD UTF-16 LE BOM + LF preservado; SVN no tocado.

### ActivaSalud - análisis y aplicación de criterio SBSalud/SMC

Carpeta/archivo de trabajo informado por Cristian:
- `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

Consulta técnica previa:
- El DW de ActivaSalud no calcula código de barras de pago; solo dibuja campo `barra` con `gf_interleaved_2de5(barra)` y lo muestra si `impor1 > 0`.
- En `Activa_Prod_Test`, `COMPRO_IMPRESION.barra` estaba siempre `NULL`: `total_registros = 10934`, `con_barra = 0`, `con_barra_afip = 10308`.
- Sí tiene `barra_afip`, fiscal/AFIP, generada con `afn_get_afip_codibarra(ente, compro_tipo, compro_nro, cae, cae_vto)`.
- Activa tiene `contra_forma = 'G'`, no entra en la rama Caseros/SBSalud/SMC que arma barra con importe/saldo.
- Criterio previo de Activa: total fiscal por `impor1`, vencimientos neteados con `creditos`, sin barra de pago efectiva.

Consulta de impacto previa:
- Para aplicar `total = total`, `subtotal = grava`, `vencimientos = saldo`, había que tocar también `afs_compro_impresion_caseros.sql` porque Activa no devolvía `total`, `grava`, `saldo` al DW.
- Riesgo informado: facturas exentas podrían mostrar `SUBTOTAL = 0` si `grava = 0`, por ejemplo comprobante con `total = 40000`, `exen = 40000`, `grava = 0`.
- Vencimientos solo con `saldo` dejan fuera deuda previa acumulada si existía.

Aplicación ActivaSalud:
- Archivos impactados:
  - `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/afs_compro_impresion_caseros.sql`
  - `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`
- SP ahora devuelve `total`, `grava` y `saldo` desde `COMPROBANTES`.
- DW incorporó columnas `total`, `grava`, `saldo`.
- `TOTAL FACTURA A/B`: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL`: `abs(if(isnull(grava),0,grava))`.
- `Vto1`, `Vto2`, `Vto3`: `abs(if(isnull(saldo),0,saldo))`.
- Barra de pago se oculta si el saldo/vencimiento es `0`.
- `barra_afip` no fue modificada.
- Validado: SQL ANSI/ISO-8859 sin BOM + CRLF; SRD UTF-16 LE BOM + CRLF; SQL `SET PARSEONLY ON` sin errores; SVN no tocado.

## Actualizaciones RM26993 - 2026-08-25 15:50 ART

### ActivaSalud - ajuste final de vencimientos y barra

Cristian pidió cambiar ActivaSalud para que los vencimientos sean `saldo + deuda`, manteniendo total/subtotal ya aplicados.

Archivo impactado:
- `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud/d_factura_activa.srd`

Estado final ActivaSalud:
- El SP ya había quedado devolviendo `total`, `grava` y `saldo` desde `COMPROBANTES`.
- `TOTAL FACTURA A/B`: `abs(if(isnull(total),0,total))`.
- `SUBTOTAL`: `abs(if(isnull(grava),0,grava))`.
- `Vto1 A/B`: `abs(saldo + deuda1)`.
- `Vto2 A/B`: `abs(saldo + deuda2)`.
- `Vto3 A/B`: `abs(saldo + deuda3)`.
- Barra de pago se oculta si el primer vencimiento da 0: `abs(saldo + deuda1) = 0`.
- `barra_afip` no fue modificada.
- Validado: SRD conserva `UTF-16 LE BOM` + `CRLF`; no se tocó SQL en este ajuste; SVN no tocado.

Motivo por el que la barra no se ve en ActivaSalud:
- El campo `barra` viene `NULL` desde origen, por eso no se veía ni antes ni después de la modificación visual.
- En `Activa_Prod_Test`: `COMPRO_IMPRESION total_registros = 10934`, `con_barra = 0`, `con_barra_afip = 10308`.
- Causa funcional: ActivaSalud tiene `contra_forma = 'G'`, que no entra en la rama que genera `barra` tipo Caseros/SBSalud/SMC. Además `barra_ente_rec` está `NULL`; cae como tipo `PF`, pero la generación PF no contempla `contra_forma = 'G'`.
- Lo que sí existe es `barra_afip`, fiscal/AFIP, no barra de pago.

### Montegrande - aplicación parcial en SRD

Cristian pidió aplicar en Montegrande el mismo criterio general:
- Total desde `total` del SP.
- Subtotal desde `grava` del SP.
- Vencimientos desde `saldo + deuda` del SP.
- Ocultar barra si el vencimiento es 0, si aplica.

Archivo impactado:
- `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd`

Cambios aplicados en el SRD:
- Agregadas columnas esperadas del SP: `total`, `grava`, `saldo`.
- `TOTAL FACTURA` A/B toma `abs(total)`.
- `SUBTOTAL` A/B toma `abs(grava)`.
- `Vto1` toma `abs(saldo + deuda1)`.
- `Vto2` toma `abs(saldo + deuda2)`.
- Barra de entes recaudadores se oculta cuando `abs(saldo + deuda1) = 0`.

Advertencia importante comunicada:
- El SP usado por Montegrande, `dbo.afs_compro_impresion_cmmg` en `Montegrande_Prod_Test`, hoy no expone `total`, `grava` ni `saldo`.
- El SRD quedó preparado, pero para que funcione en ejecución el SP también debe devolver esos tres campos.

Validación Montegrande:
- SRD mantuvo `UTF-16 LE BOM` y `CRLF`.
- No se tocó SVN ni base de datos.
