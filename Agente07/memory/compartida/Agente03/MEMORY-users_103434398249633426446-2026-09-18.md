# Memoria de conversación — Marcelo Viciconti

## Datos

- **Fecha:** 18/09/2026
- **Usuario:** Marcelo Viciconti
- **ID Google Chat:** `users/103434398249633426446`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 24
- **Inicio:** 14:35:12
- **Fin:** 17:48:29

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/25hnskaaaae`

## Archivos revisados

- `ff7ef681-5124-4540-82af-0813b902fcfe.jsonl`

---

## Transcripción completa

### [18/09/2026 14:35:12] Marcelo Viciconti

PROMPT SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/01_bug.md]
TICKET REDMINE : [30121]
USUARIO : [mviciconti]
BASE DE DATOS : [MutualRioIV_Desarrollo]
RAMA SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI]
DOCUMENTO ATENCION AL CLIENTE : []
DOCUMENTO ANALISIS Y DISENO : [\\infomed03n\mviciconti\R30121\R30121.doc]
RUTA DE SALIDA : [\\infomed03n\mviciconti\R30121\]
NOMBRE ARCHIVO : [ticket_30121_01_bug.md]
OBSERVACIONES : [Proponga la solucion definitiva sin hacer cambios.
perfil de base de datos el mismo de MutualRioIV_Prod]

---

### [18/09/2026 14:36:18] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [18/09/2026 14:36:32] Marcelo Viciconti

PROMPT SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/01_bug.md]
TICKET REDMINE : [30121]
USUARIO : [mviciconti]
BASE DE DATOS : [MutualRioIV_Desarrollo]
RAMA SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI]
DOCUMENTO ATENCION AL CLIENTE : []
DOCUMENTO ANALISIS Y DISENO : [\\infomed03n\mviciconti\R30121\R30121.doc]
RUTA DE SALIDA : [\\infomed03n\mviciconti\R30121\]
NOMBRE ARCHIVO : [ticket_30121_01_bug.md]
OBSERVACIONES : [Proponga la solucion definitiva sin hacer cambios.
perfil de base de datos el mismo de MutualRioIV_Prod]

---

### [18/09/2026 14:43:31] Agente

Marcelo, generé el informe solicitado sin hacer cambios en código ni base de datos.

Ubicación: `\\infomed03n\mviciconti\R30121\ticket_30121_01_bug.md`  
Ruta validada en el agente: `/mnt/Agentes/mviciconti/R30121/ticket_30121_01_bug.md`

Fuentes usadas: Redmine 30121, `R30121.doc`, rama SVN `V25.08.002_PROVI` refrescada a rev. 84721, prompt `01_bug` rev. 84565 y consultas de solo lectura sobre `MutualRioIV_Desarrollo`.

Desenlace: **Defecto de código**.

Resumen ejecutivo: la causa raíz es la asunción legacy de que reglas odontológicas siempre deben resolver `conve_prestac` con `concep = 5`. La solución actual de quitar el filtro y usar `TOP 1` no es definitiva porque no tiene orden determinístico y deja objetos relacionados con `concep = 5`. La solución definitiva debe alinear alta, baja, garantías asociadas y aplicación de garantías con un criterio único para resolver `conve_prestac`.

Puntos para validar antes de especificar: prioridad funcional cuando existen conceptos 5 y 6, unicidad esperada de regla por prestación/vigencia y ajuste de `pru_garan_odon_conve` para no quedar limitado a concepto 5.

---

### [18/09/2026 14:46:16] Marcelo Viciconti

PROMPT SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/01_bug.md]
TICKET REDMINE : [30231]
USUARIO : [mviciconti]
BASE DE DATOS : [Fiat_Prod]
RAMA SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI]
DOCUMENTO ATENCION AL CLIENTE : []
DOCUMENTO ANALISIS Y DISENO : [\\infomed03n\mviciconti\R30121\R-09_Documento de Requerimiento Detallado_V2.doc]
RUTA DE SALIDA : [\\infomed03n\mviciconti\R30231\]
NOMBRE ARCHIVO : [ticket_30231_01_bug.md]
OBSERVACIONES : [Proponga la solucion definitiva sin hacer cambios]

---

### [18/09/2026 14:47:09] Agente

Marcelo, no puedo avanzar todavía porque no validaron dos referencias obligatorias:

1. `BASE DE DATOS: Fiat_Prod` no existe como profile en `DB_PROFILE.md`.
2. El documento indicado `\\infomed03n\mviciconti\R30121\R-09_Documento de Requerimiento Detallado_V2.doc` no existe en la ruta montada equivalente.

Dato verificado: en la carpeta de salida `R30231` sí existe un archivo con ese nombre, pero necesito confirmación explícita para usarlo porque la ruta informada apunta a `R30121`.

No generé el informe ni hice cambios.

---

### [18/09/2026 14:48:12] Marcelo Viciconti

PROMPT SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/01_bug.md]
TICKET REDMINE : [30231]
USUARIO : [mviciconti]
BASE DE DATOS : [Fiat_Prod]
RAMA SVN : [https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.002_PROVI]
DOCUMENTO ATENCION AL CLIENTE : []
DOCUMENTO ANALISIS Y DISENO : [\\infomed03n\mviciconti\R30231\R-09_Documento de Requerimiento Detallado_V2.doc]
RUTA DE SALIDA : [\\infomed03n\mviciconti\R30231\]
NOMBRE ARCHIVO : [ticket_30231_01_bug.md]
OBSERVACIONES : [Proponga la solucion definitiva sin hacer cambios]

---

### [18/09/2026 14:48:37] Agente

Marcelo, el documento ya quedó validado en:

`\\infomed03n\mviciconti\R30231\R-09_Documento de Requerimiento Detallado_V2.doc`

Pero sigo bloqueado por la base: `Fiat_Prod` no existe en `DB_PROFILE.md`, ni como profile ni como base registrada. Por Regla 2/5 no puedo consultar una base usando supuestos.

Necesito que me indiques un profile registrado equivalente para `Fiat_Prod` o que se dé de alta el profile antes de continuar.

---

### [18/09/2026 14:50:40] Marcelo Viciconti

El perfil es el mismo que para Fiat_Desarrollo

---

### [18/09/2026 14:51:38] Agente

Marcelo, `Fiat_Desarrollo` tampoco existe en el `DB_PROFILE.md` actual de este workspace.

Validé además memoria compartida y hay antecedentes de `FIAT_Prod` en otro contexto/agente, pero no puedo usar eso como profile operativo registrado para esta sesión.

Quedo bloqueado hasta que se registre el profile o me pases un profile existente de `DB_PROFILE.md` para usar como equivalente.

---

### [18/09/2026 14:52:39] Marcelo Viciconti

Use la base de datos Fiat_Desarrollo. Ya le dije que el perfil es el mismo que para Fiat_Desarrollo. O no entiende?

---

### [18/09/2026 14:53:11] Agente

Marcelo, entiendo la instrucción. El bloqueo no es por no entender la base, es porque en este workspace no existe un profile registrado llamado `Fiat_Desarrollo`, y tampoco existe `Fiat_Prod`.

Para poder continuar necesito una de estas dos cosas:

1. Que me pases los datos completos del profile `Fiat_Desarrollo`:
```text
Motor:
Host/IP:
Puerto:
Base de datos: Fiat_Desarrollo
Usuario:
Password:
Notas:
```

2. O que me indiques un profile existente de `DB_PROFILE.md` para copiar como referencia técnica.

Con lo que está hoy registrado no puedo conectarme a `Fiat_Desarrollo` sin inventar host/credenciales.

---

### [18/09/2026 14:54:13] Marcelo Viciconti

Es el mismo perfil que SMC_Desarrollo, server, ip, usuario, password. todo

---

### [18/09/2026 14:59:14] Agente

Marcelo, generado el informe sin modificar código ni base.

Ubicación: `\\infomed03n\mviciconti\R30231\ticket_30231_01_bug.md`  
Ruta validada en el agente: `/mnt/Agentes/mviciconti/R30231/ticket_30231_01_bug.md`

Desenlace: **Defecto de código / desarrollo funcional faltante**.

Resumen ejecutivo: para FIAT (`contra_forma = B`), el proceso de novedades SSS debe priorizar el CUIT de derivaciones asociadas a cuentas con `mega_cuenta`; hoy `afi_isss_afiliados_busca_compara` no evalúa `cuentas.mega_cuenta` y selecciona por vigencia mínima/`TOP 1`. La solución definitiva debe incorporar esa prioridad solo para FIAT y mantener el comportamiento actual cuando no haya derivaciones con mega cuenta.

Validación: archivo creado, SVN sin cambios locales.

---

### [18/09/2026 15:04:33] Marcelo Viciconti

Pre-compaction memory flush. Store durable memories only in memory/2026-09-18.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-18.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-18-HHMM.md); always use the canonical 2026-09-18.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, September 18th, 2026 - 3:04 PM (America/Buenos_Aires)
Reference UTC: 2026-09-18 18:04 UTC

---

### [18/09/2026 15:05:38] Agente

NO_REPLY

---

### [18/09/2026 15:05:40] Marcelo Viciconti

Tengo este codigo adaptado, pero no estoy seguro que funcione como quiero.
puede corregirlo para que funcione con su especificacion?

DECLARE
 @prm_prepaga smallint = 1,
 @prm_fecha_hasta smalldatetime = GETDATE()
 DECLARE

 @var_contra_forma char(2);

 SELECT @var_contra_forma = contra_forma -- r30231
 FROM PREPA_AFI_REGLAS
 WHERE prepaga = @prm_prepaga;

 --SET @var_contra_forma = 'X';

 INSERT INTO ##TMP_RNAS_ACTUAL
 SELECT -- DISTINCT
 'NUEVO',
 afi.prepaga ,
 afi.contra ,
 afi.inte ,
 --ada.tipo_aportante,
 cuit_emp = IsNull(CASE WHEN IsNull(ada.tipo_aportante, 'T') = 'T' THEN (
 SELECT TOP 1
 ada5.cuit

 FROM AFI_DERIV_APORTES as ada5

 INNER JOIN -- r30231
 CUENTAS as cue
 ON cue.cuenta = ada5.obra_soc

 WHERE ada.prepaga = ada5.prepaga
 AND ada.contra = ada5.contra
 AND ada.inte = ada5.inte
 AND IsNull(ada5.tipo_aportante,'T') = 'T'
 AND ada5.vigen_desde < @prm_fecha_hasta
 AND (ada5.vigen_hasta IS NULL OR ada5.vigen_hasta >= @prm_fecha_hasta)
 AND (ada5.baja_fecha IS NULL OR ada5.baja_fecha >= @prm_fecha_hasta)
 AND ( -- r30231
 @var_contra_forma <> 'B'
 OR
 (
 @var_contra_forma = 'B'
 AND ada5.obra_soc = ada.obra_soc
 )
 )
 AND ada5.vigen_desde = (
 SELECT MIN(vigen_desde)
 FROM AFI_DERIV_APORTES as ada6
 WHERE ada6.prepaga = ada5.prepaga
 AND ada6.contra = ada5.contra
 AND ada6.inte = ada5.inte
 AND ada6.obra_soc = ada5.obra_soc -- r30231
 AND ada6.vigen_desde < @prm_fecha_hasta
 AND (ada6.vigen_hasta IS NULL OR ada6.vigen_hasta >= @prm_fecha_hasta)
 AND (ada6.baja_fecha IS NULL OR ada6.baja_fecha >= @prm_fecha_hasta)
 )
 ORDER -- r30231
 BY --CASE WHEN @var_contra_forma <> 'B' THEN (SELECT NULL) END,
 CASE WHEN @var_contra_forma = 'B' THEN ada5.vigen_desde END DESC,
 CASE WHEN @var_contra_forma = 'B' THEN ada5.obra_soc END ASC,
 CASE WHEN @var_contra_forma = 'B' THEN ada5.cuit END ASC
 )
 WHEN ada.tipo_aportante = 'G' THEN (SELECT TOP 1 cuit FROM afi_deriv_aportes ada3
 WHERE ada.prepaga = ada3.prepaga
 AND ada.contra = ada3.contra
 AND ada.inte_titular_aportante = ada3.inte
 AND ada3.tipo_aportante = 'T'
 AND ada3.vigen_desde < @prm_fecha_hasta
 AND (ada3.vigen_hasta IS NULL OR ada3.vigen_hasta >= @prm_fecha_hasta)
 AND (ada3.baja_fecha IS NULL OR ada3.baja_fecha >= @prm_fecha_hasta)
 AND ada3.vigen_desde = (SELECT min(vigen_desde) FROM afi_deriv_aportes ada4
 WHERE ada4.prepaga = ada3.prepaga
 and ada4.contra = ada3.contra
 and ada4.inte = ada3.inte
 and ada3.vigen_desde < @prm_fecha_hasta
 and (ada4.vigen_hasta IS NULL OR ada4.vigen_hasta >= @prm_fecha_hasta)
 and (ada4.baja_fecha IS NULL OR ada4.baja_fecha >= @prm_fecha_hasta)
 )
 )
 ELSE SPACE(13)
 END ,'0')

 FROM afiliados afi

 INNER JOIN afi_deriv_aportes ada
 ON afi.prepaga = ada.prepaga
 and afi.contra = ada.contra
 and afi.inte = ada.inte

 INNER JOIN afi_domicilios afd
 ON afi.prepaga = afd.prepaga
 and afi.contra = afd.contra
 and afi.inte = afd.inte

 LEFT OUTER JOIN afi_opcion afo
 ON afi.prepaga = afo.prepaga
 and afi.contra = afo.contra
 and afi.inte = afo.inte
 and (afo.baja_fecha IS NULL OR afo.baja_fecha >= @prm_fecha_hasta)
 AND afo.vigen_desde = (SELECT max(vigen_desde) FROM afi_opcion afo2
 WHERE afo2.prepaga = afi.prepaga
 and afo2.contra = afi.contra
 and afo2.inte = afi.inte
 and (afo2.baja_fecha IS NULL OR afo2.baja_fecha >= @prm_fecha_hasta)
 and afo2.vigen_desde < @prm_fecha_hasta --MP
 )

 LEFT OUTER JOIN localidades loc
 ON afd.loca = loc.loca
 AND loc.fecha_baja IS NULL

 WHERE afi.prepaga = @prm_prepaga
 AND ada.obra_soc = (SELECT cuenta FROM cuentas ctas
 WHERE ctas.rnos = (SELECT rnos FROM prepagas pre
 WHERE ada.prepaga = pre.prepaga
 )
 )
 AND ada.vigen_desde < @prm_fecha_hasta
 AND (ada.baja_fecha IS NULL OR ada.baja_fecha >= @prm_fecha_hasta)
 AND ada.vigen_desde = (SELECT min(vigen_desde) FROM afi_deriv_aportes ada2
 WHERE ada2.prepaga = ada.prepaga
 and ada2.contra = ada.contra
 and ada2.inte = ada.inte
 and ada2.vigen_desde < @prm_fecha_hasta
 and (ada2.baja_fecha IS NULL OR ada2.baja_fecha >= @prm_fecha_hasta)
 and (ada2.vigen_hasta IS NULL OR ada2.vigen_hasta >= @prm_fecha_hasta)
 )
 AND afd.vigen_desde = (SELECT max(vigen_desde) FROM afi_domicilios afd2
 WHERE afd2.prepaga = afi.prepaga
 and afd2.contra = afi.contra
 and afd2.inte = afi.inte
 and afd2.vigen_desde < @prm_fecha_hasta
 and (afd2.baja_fecha IS NULL OR afd2.baja_fecha >= @prm_fecha_hasta)
 )

 AND (afi.baja_fecha IS NULL OR afi.baja_fecha >= @prm_fecha_hasta)
 AND afi.ingre_fecha < @prm_fecha_hasta
GO

---

### [18/09/2026 15:06:20] Agente

Marcelo, sí: tu adaptación está cerca, pero tiene un problema central.

Este filtro rompe la especificación:

```sql
AND ada5.obra_soc = ada.obra_soc
```

Porque obliga a elegir dentro de la misma `obra_soc` que ya tomó el flujo original. Para FIAT justamente necesitás poder mirar todas las derivaciones vigentes del afiliado y priorizar la que tenga cuenta con `mega_cuenta`.

Usaría esta lógica para reemplazar el cálculo de `cuit_emp`:

```sql
cuit_emp = ISNULL(
  CASE
    WHEN ISNULL(ada.tipo_aportante, 'T') = 'T' THEN
    (
      SELECT TOP 1
        ada5.cuit
      FROM AFI_DERIV_APORTES ada5
      LEFT JOIN CUENTAS cue
        ON cue.cuenta = ada5.obra_soc
        AND cue.baja_fecha IS NULL
      WHERE ada5.prepaga = ada.prepaga
        AND ada5.contra = ada.contra
        AND ada5.inte = ada.inte
        AND ISNULL(ada5.tipo_aportante, 'T') = 'T'
        AND ada5.vigen_desde < @prm_fecha_hasta
        AND (ada5.vigen_hasta IS NULL OR ada5.vigen_hasta >= @prm_fecha_hasta)
        AND (ada5.baja_fecha IS NULL OR ada5.baja_fecha >= @prm_fecha_hasta)
      ORDER BY
        CASE
          WHEN @var_contra_forma = 'B' AND cue.mega_cuenta IS NOT NULL THEN 0
          ELSE 1
        END,
        CASE
          WHEN @var_contra_forma = 'B' AND cue.mega_cuenta IS NOT NULL THEN ada5.vigen_desde
          ELSE NULL
        END DESC,
        ada5.vigen_desde ASC,
        ada5.obra_soc ASC,
        ada5.cuit ASC
    )

    WHEN ada.tipo_aportante = 'G' THEN
    (
      SELECT TOP 1
        ada3.cuit
      FROM AFI_DERIV_APORTES ada3
      LEFT JOIN CUENTAS cue3
        ON cue3.cuenta = ada3.obra_soc
        AND cue3.baja_fecha IS NULL
      WHERE ada3.prepaga = ada.prepaga
        AND ada3.contra = ada.contra
        AND ada3.inte = ada.inte_titular_aportante
        AND ada3.tipo_aportante = 'T'
        AND ada3.vigen_desde < @prm_fecha_hasta
        AND (ada3.vigen_hasta IS NULL OR ada3.vigen_hasta >= @prm_fecha_hasta)
        AND (ada3.baja_fecha IS NULL OR ada3.baja_fecha >= @prm_fecha_hasta)
      ORDER BY
        CASE
          WHEN @var_contra_forma = 'B' AND cue3.mega_cuenta IS NOT NULL THEN 0
          ELSE 1
        END,
        CASE
          WHEN @var_contra_forma = 'B' AND cue3.mega_cuenta IS NOT NULL THEN ada3.vigen_desde
          ELSE NULL
        END DESC,
        ada3.vigen_desde ASC,
        ada3.obra_soc ASC,
        ada3.cuit ASC
    )

    ELSE SPACE(13)
  END,
  '0'
)
```

Criterio que cumple:

- Si `contra_forma = 'B'` y hay derivaciones con `mega_cuenta`, toma esas primero.
- Entre derivaciones con `mega_cuenta`, toma la de mayor `vigen_desde`.
- Si no hay `mega_cuenta`, mantiene un criterio compatible con el legacy: menor `vigen_desde`.
- Evita `INNER JOIN` contra `CUENTAS`, porque si no existe cuenta activa no debe perder la derivación.
- Agrega desempate estable por `obra_soc` y `cuit`.

No pondría el filtro `ada5.vigen_desde = MIN(...)` dentro de la subquery FIAT, porque impide elegir la derivación de mayor vigencia con mega cuenta.

---

### [18/09/2026 17:19:03] Marcelo Viciconti

Me parece que la parte de
"WHEN ada.tipo_aportante = 'G' THEN"
no hay que tocarla, segun el documento. Verifiquelo.

---

### [18/09/2026 17:20:39] Agente

Marcelo, verifiqué el documento y tu interpretación no coincide con su alcance completo.

El documento indica explícitamente:

- La regla aplica al proceso de determinación de novedades para FIAT (`contra_forma = B`).
- Debe priorizarse el CUIT de una cuenta con `mega_cuenta`.
- El cambio aplica a **“todos los integrantes involucrados en el proceso, que generen la novedad”**.

Por lo tanto, el bloque:

```sql
WHEN ada.tipo_aportante = 'G' THEN
```

también debe contemplarse.

En ese caso, el integrante `G` no busca su propio CUIT: busca el CUIT del titular aportante mediante:

```sql
ada.inte_titular_aportante = ada3.inte
```

Si ese titular aportante tiene varias derivaciones vigentes, la especificación exige aplicar la misma prioridad: elegir una derivación asociada a una cuenta activa con `mega_cuenta` y, entre ellas, la de mayor `vigen_desde`.

La modificación correcta sería:

- Mantener intacta la estructura funcional del bloque `G`.
- Agregar únicamente la consulta a `CUENTAS` y el criterio de ordenamiento por `mega_cuenta`.
- No aplicar esa prioridad cuando `contra_forma <> 'B'`.
- Mantener el comportamiento actual como fallback si el titular aportante no tiene ninguna derivación asociada a `mega_cuenta`.

Conclusión: **no corresponde dejar `G` completamente sin tocar**, porque eso incumpliría la frase “todos los integrantes involucrados”. Sí corresponde modificarlo de forma equivalente al caso `T`, sin alterar su relación funcional con `inte_titular_aportante`.

---

### [18/09/2026 17:33:32] Marcelo Viciconti

Al quitar la parte de ada5.vigen_desde = MIN(...), y como ha dejado el codigo, ya se contemplan todos los casos para 'B' y <> 'B' con el ORDER BY complejo?

---

### [18/09/2026 17:35:50] Agente

Sí, **la estrategia correcta es quitar el filtro `ada5.vigen_desde = MIN(...)` y resolver la selección mediante `TOP 1` + `ORDER BY`**. Así se contemplan ambos casos:

- **`contra_forma = 'B'`**:
  1. Primero se consideran derivaciones vinculadas a cuentas con `mega_cuenta IS NOT NULL`.
  2. Entre ellas se selecciona la de mayor `vigen_desde`.
  3. Si no existe una cuenta mega, se utiliza la mejor derivación disponible como respaldo.

- **`contra_forma <> 'B'`**:
  1. No se aplica prioridad por cuenta mega.
  2. `vigen_desde ASC` conserva el comportamiento anterior, seleccionando la fecha mínima.
  3. Los demás campos funcionan como desempate determinístico.

El `ORDER BY` debería seguir esta lógica:

```sql
ORDER BY
  CASE
    WHEN @var_contra_forma = 'B'
         AND cue.mega_cuenta IS NOT NULL THEN 0
    ELSE 1
  END,
  CASE
    WHEN @var_contra_forma = 'B'
         AND cue.mega_cuenta IS NOT NULL
      THEN ada5.vigen_desde
    ELSE NULL
  END DESC,
  ada5.vigen_desde ASC,
  ada5.obra_soc ASC,
  ada5.cuit ASC
```

Pero en el código compartido todavía quedan dos restricciones que deben eliminarse:

```sql
AND ada5.vigen_desde = (
  SELECT MIN(...)
)
```

y:

```sql
AND ada5.obra_soc = ada.obra_soc
```

La segunda impide comparar todas las derivaciones del afiliado y puede evitar que se encuentre la cuenta mega correcta.

Además, la misma selección debe aplicarse al bloque `tipo_aportante = 'G'`, utilizando los alias correspondientes (`ada3` y `cue3`), porque el documento indica que la regla alcanza a todos los miembros involucrados en la generación de novedades.

---

### [18/09/2026 17:46:56] Marcelo Viciconti

por favor , no conteste en modo potencial. diga lo que se hizo y que debe quedar asi.

---

### [18/09/2026 17:48:29] Agente

Marcelo, la solución definitiva quedó definida así:

- Se elimina `ada5.vigen_desde = MIN(...)`.
- Se elimina `ada5.obra_soc = ada.obra_soc`, porque impedía comparar todas las derivaciones.
- Se utiliza `TOP 1` con `ORDER BY`.
- Para `contra_forma = 'B'`, se prioriza `cuentas.mega_cuenta IS NOT NULL` y, dentro de esas cuentas, la mayor `vigen_desde`.
- Para `contra_forma <> 'B'`, `vigen_desde ASC` conserva la selección histórica de la fecha mínima.
- La misma lógica se aplica a los aportantes `T` y `G`, porque la especificación alcanza a todos los miembros involucrados.
- El vínculo con `CUENTAS` queda como `LEFT JOIN`, para mantener el comportamiento de respaldo cuando no existe cuenta mega.

Por lo tanto, **el bloque `G` debe quedar ajustado con la misma lógica**. No debe conservarse allí el filtro por `MIN`, porque dejaría incompleta la solución.

No se realizaron cambios en SVN ni en la base de datos; quedó definida la corrección que debe implementarse.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
