
## 2026-09-09 11:00 ART - Tarea Redmine 27498 / R27498

- Conversacion con Cristian sobre alcance de la propuesta para Redmine `27498` (`Liquidacion - Ingreso de prestaciones: Asignacion automatica de sucursal`). Usuario Redmine usado: `cratto`; base: `Ospecon_Desarrollo`; rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498`; salida local: `/mnt/Agentes/cratto/R27498`.
- La propuesta anterior tenia `13` archivos en `propuestos`: `8` objetos PB modificados, `1` objeto PB nuevo (`d_auto_sucursales.srd`), `2` scripts SQL, mas instrucciones y changelog.
- Cristian consulto por que se tocaron `w_clase_auto.srw` y `wp_expe_odon.srw`. Respuesta dada: `wp_expe_odon.srw` quedaba alineado por Odontologia/expedientes; `w_clase_auto.srw` fue una inferencia tecnica por flujo comun de autorizaciones, pero el documento original no lo nombraba ni pedia tocar pantallas generales de autorizaciones. Se reconocio que debio quedar como supuesto tecnico a confirmar.
- Cristian aclaro que el documento original hablaba de pantallas relacionadas a expedientes y no habia que tocar pantallas de autorizaciones. Criterio durable: para esta tarea, no asumir modificacion de `w_clase_auto.srw` sin confirmacion explicita del lider.
- Se regenero `/mnt/Agentes/cratto/R27498/ticket_27498_02_req_nuevo.md` usando prompt SVN `branches/prompts_std/02_req_nuevo.md`, Redmine `27498`, documento fuente `//infomed03n/cratto/R27498/R27498_Documento_Especificacion_Funcional.doc`, rama `R27498` y base `Ospecon_Desarrollo` solo metadata.
- Estado del informe regenerado: `REQUIERE DEFINICIONES`. Confirmaciones principales: `A1` confirmar si se autoriza tocar `w_clase_auto.srw`; `A2` confirmar si se autoriza tocar DataWindows `d_auto*`; `D2` definir si la logica comun va en NVO/helper o Window Functions locales en ventanas de expedientes.
- Validaciones reportadas: prompt SVN leido, Redmine consultado con `cratto`, documento fuente leido, rama `R27498` validada en solo lectura, base consultada solo metadata. Archivo final ASCII, sin BOM, LF, 413 lineas. No se modifico SVN ni base.

## 2026-09-09 11:00 ART - Tarea Redmine 27498 / R27498

- Conversacion con Cristian sobre alcance de la propuesta para Redmine `27498` (`Liquidacion - Ingreso de prestaciones: Asignacion automatica de sucursal`). Usuario Redmine usado: `cratto`; base: `Ospecon_Desarrollo`; rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498`; salida local: `/mnt/Agentes/cratto/R27498`.
- La propuesta anterior tenia `13` archivos en `propuestos`: `8` objetos PB modificados, `1` objeto PB nuevo (`d_auto_sucursales.srd`), `2` scripts SQL, mas instrucciones y changelog.
- Cristian consulto por que se tocaron `w_clase_auto.srw` y `wp_expe_odon.srw`. Respuesta dada: `wp_expe_odon.srw` quedaba alineado por Odontologia/expedientes; `w_clase_auto.srw` fue una inferencia tecnica por flujo comun de autorizaciones, pero el documento original no lo nombraba ni pedia tocar pantallas generales de autorizaciones. Se reconocio que debio quedar como supuesto tecnico a confirmar.
- Cristian aclaro que el documento original hablaba de pantallas relacionadas a expedientes y no habia que tocar pantallas de autorizaciones. Criterio durable: para esta tarea, no asumir modificacion de `w_clase_auto.srw` sin confirmacion explicita del lider.
- Se regenero `/mnt/Agentes/cratto/R27498/ticket_27498_02_req_nuevo.md` usando prompt SVN `branches/prompts_std/02_req_nuevo.md`, Redmine `27498`, documento fuente `//infomed03n/cratto/R27498/R27498_Documento_Especificacion_Funcional.doc`, rama `R27498` y base `Ospecon_Desarrollo` solo metadata.
- Estado del informe regenerado: `REQUIERE DEFINICIONES`. Confirmaciones principales: `A1` confirmar si se autoriza tocar `w_clase_auto.srw`; `A2` confirmar si se autoriza tocar DataWindows `d_auto*`; `D2` definir si la logica comun va en NVO/helper o Window Functions locales en ventanas de expedientes.
- Validaciones reportadas: prompt SVN leido, Redmine consultado con `cratto`, documento fuente leido, rama `R27498` validada en solo lectura, base consultada solo metadata. Archivo final ASCII, sin BOM, LF, 413 lineas. No se modifico SVN ni base.

## 2026-09-09 11:56 ART - Tarea Redmine 30342 / R30342

- Walter pidio generar informe de bug para Redmine `30342`, usuario `wguise`, base `mutualrioIV_prod`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`, documento `//infomed03n/wguise/R30342/R30342.docx`, salida `//infomed03n/wguise/R30342/ticket_30342_01_bug.md`.
- Observacion funcional: no exporta cuando en el combo de cuentas de debito esta seleccionada la opcion `mismo banco acred.`; en `idw_data.RowCount()` da `0`. Con otra opcion del combo exporta correctamente.
- Informe generado con desenlace `Defecto de codigo`. Causa raiz ubicada en `wp_export_debitos.srw`: al buscar para BBVA se aplica filtro temporal sobre `dw_list_det` por `recalcula = "S"`; si no hay filas recalculables, el filtro no se limpia. Luego, como el ente BBVA tiene `total_detalle = D`, `idw_data` apunta a `dw_list_det`; `RowCount()` queda en `0` y aparece `No hay registros para ser exportados`.
- Aclaracion tecnica posterior a Walter: el flag `recalcula = 'S'` lo pone el SP `afs_compro_exportacion`, no PowerBuilder. En `backend/procedures/afs_compro_exportacion.sql`, bloque BBVA, primero arma `#compro_recalculo` con `recalcula = 'N'`; luego, si `@var_min_vto < @fecha_debito`, hace `UPDATE #compro_recalculo` y pone `S` cuando `@fecha_debito > IsNull(Isnull(vto3, vto2), vto)`. Despues copia a `#comprobantes.recalcula` y el DataWindow detalle devuelve ese campo.
- Criterio durable para continuar R30342: que todos los comprobantes vengan con `recalcula = 'N'` es un escenario valido; el bug es que PB no restaura `dw_list_det` con `SetFilter('')` + `Filter()` despues del filtro temporal cuando no encuentra filas `S`.
- Punto pendiente antes de `03_spec`: confirmar si el alcance incluye solo restaurar filtro/redraw de `dw_list_det` o tambien corregir anomalia visual de doble barra/superposicion mencionada durante el analisis.

## 2026-09-09 15:55 ART - Tarea Redmine 28243 / R28243 Odontologia

- Conversacion con Federico sobre Redmine `28243`, seccion odontologia, ruta de trabajo `//infomed03n/Agentes/facevedo/R28243/seccion_odonto`.
- Primera entrega previa: documento `ticket_28243_1_05_dev.md` y archivos propuestos en `ARCHIVOS2/`. Diagnostico: `prs_valida_of_odon` devuelve rechazos propios de odontologia, pero `prs_valida_of_auto_remotas` no los recalculaba como rechazo priorizado de cabecera antes de grabar con `pru_autorizaciones`. La propuesta SQL tomaba rechazo vigente de cabecera mas rechazos de `auto_prestac_items_remotas`, ordenaba por grupo/orden y asignaba `@recha`/`@esta` antes del grabado.
- Validacion informada en esa entrega: en `SMC_Prox_Ver`, rechazos `183` y `249` tienen grupo/orden; `179`, `180`, `181` no. Parseo SQL con `SET PARSEONLY ON` sin errores. No se modifico SVN ni base.
- Federico luego detecto que en la propuesta basada en `ticket_28243_1_04_dev.md` faltaba la llamada explicita a `prs_valida_of_odon` dentro de `prs_valida_of_auto_remotas` para validar rechazos odontologicos y considerarlos al guardar la autorizacion.
- Segunda entrega generada: documento `//infomed03n/Agentes/facevedo/R28243/seccion_odonto/ticket_28243_1_06_dev.md` y archivos propuestos en `//infomed03n/Agentes/facevedo/R28243/seccion_odonto/ARCHIVOS3/`.
- Cambio principal de `ARCHIVOS3/backend/procedures/prs_valida_of_auto_remotas.sql`: ejecutar `dbo.prs_valida_of_odon` por cada item remoto odontologico antes de `pru_autorizaciones`; cargar los rechazos devueltos en `auto_prestac_items_remotas` respetando prioridad por grupo/orden; recalcular `@recha` y `@esta` de cabecera para que la autorizacion se guarde considerando esos rechazos.
- Validaciones reportadas para la segunda entrega: revisados `ticket_28243_1_04_dev.md`, `prs_valida_of_odon.sql` y `prs_valida_of_auto_remotas.sql`; sintaxis SQL validada con `SET PARSEONLY ON` sin errores; sin modificaciones en SVN ni ejecuciones de cambio en base.
## 2026-09-09 11:00 ART - Tarea Redmine 27498 / R27498

- Conversacion con Cristian sobre alcance de la propuesta para Redmine `27498` (`Liquidacion - Ingreso de prestaciones: Asignacion automatica de sucursal`). Usuario Redmine usado: `cratto`; base: `Ospecon_Desarrollo`; rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498`; salida local: `/mnt/Agentes/cratto/R27498`.
- La propuesta anterior tenia `13` archivos en `propuestos`: `8` objetos PB modificados, `1` objeto PB nuevo (`d_auto_sucursales.srd`), `2` scripts SQL, mas instrucciones y changelog.
- Cristian consulto por que se tocaron `w_clase_auto.srw` y `wp_expe_odon.srw`. Respuesta dada: `wp_expe_odon.srw` quedaba alineado por Odontologia/expedientes; `w_clase_auto.srw` fue una inferencia tecnica por flujo comun de autorizaciones, pero el documento original no lo nombraba ni pedia tocar pantallas generales de autorizaciones. Se reconocio que debio quedar como supuesto tecnico a confirmar.
- Cristian aclaro que el documento original hablaba de pantallas relacionadas a expedientes y no habia que tocar pantallas de autorizaciones. Criterio durable: para esta tarea, no asumir modificacion de `w_clase_auto.srw` sin confirmacion explicita del lider.
- Se regenero `/mnt/Agentes/cratto/R27498/ticket_27498_02_req_nuevo.md` usando prompt SVN `branches/prompts_std/02_req_nuevo.md`, Redmine `27498`, documento fuente `//infomed03n/cratto/R27498/R27498_Documento_Especificacion_Funcional.doc`, rama `R27498` y base `Ospecon_Desarrollo` solo metadata.
- Estado del informe regenerado: `REQUIERE DEFINICIONES`. Confirmaciones principales: `A1` confirmar si se autoriza tocar `w_clase_auto.srw`; `A2` confirmar si se autoriza tocar DataWindows `d_auto*`; `D2` definir si la logica comun va en NVO/helper o Window Functions locales en ventanas de expedientes.
- Validaciones reportadas: prompt SVN leido, Redmine consultado con `cratto`, documento fuente leido, rama `R27498` validada en solo lectura, base consultada solo metadata. Archivo final ASCII, sin BOM, LF, 413 lineas. No se modifico SVN ni base.

## 2026-09-09 11:00 ART - Tarea Redmine 27498 / R27498

- Conversacion con Cristian sobre alcance de la propuesta para Redmine `27498` (`Liquidacion - Ingreso de prestaciones: Asignacion automatica de sucursal`). Usuario Redmine usado: `cratto`; base: `Ospecon_Desarrollo`; rama SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/R27498`; salida local: `/mnt/Agentes/cratto/R27498`.
- La propuesta anterior tenia `13` archivos en `propuestos`: `8` objetos PB modificados, `1` objeto PB nuevo (`d_auto_sucursales.srd`), `2` scripts SQL, mas instrucciones y changelog.
- Cristian consulto por que se tocaron `w_clase_auto.srw` y `wp_expe_odon.srw`. Respuesta dada: `wp_expe_odon.srw` quedaba alineado por Odontologia/expedientes; `w_clase_auto.srw` fue una inferencia tecnica por flujo comun de autorizaciones, pero el documento original no lo nombraba ni pedia tocar pantallas generales de autorizaciones. Se reconocio que debio quedar como supuesto tecnico a confirmar.
- Cristian aclaro que el documento original hablaba de pantallas relacionadas a expedientes y no habia que tocar pantallas de autorizaciones. Criterio durable: para esta tarea, no asumir modificacion de `w_clase_auto.srw` sin confirmacion explicita del lider.
- Se regenero `/mnt/Agentes/cratto/R27498/ticket_27498_02_req_nuevo.md` usando prompt SVN `branches/prompts_std/02_req_nuevo.md`, Redmine `27498`, documento fuente `//infomed03n/cratto/R27498/R27498_Documento_Especificacion_Funcional.doc`, rama `R27498` y base `Ospecon_Desarrollo` solo metadata.
- Estado del informe regenerado: `REQUIERE DEFINICIONES`. Confirmaciones principales: `A1` confirmar si se autoriza tocar `w_clase_auto.srw`; `A2` confirmar si se autoriza tocar DataWindows `d_auto*`; `D2` definir si la logica comun va en NVO/helper o Window Functions locales en ventanas de expedientes.
- Validaciones reportadas: prompt SVN leido, Redmine consultado con `cratto`, documento fuente leido, rama `R27498` validada en solo lectura, base consultada solo metadata. Archivo final ASCII, sin BOM, LF, 413 lineas. No se modifico SVN ni base.

## 2026-09-09 11:56 ART - Tarea Redmine 30342 / R30342

- Walter pidio generar informe de bug para Redmine `30342`, usuario `wguise`, base `mutualrioIV_prod`, rama SVN `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk`, documento `//infomed03n/wguise/R30342/R30342.docx`, salida `//infomed03n/wguise/R30342/ticket_30342_01_bug.md`.
- Observacion funcional: no exporta cuando en el combo de cuentas de debito esta seleccionada la opcion `mismo banco acred.`; en `idw_data.RowCount()` da `0`. Con otra opcion del combo exporta correctamente.
- Informe generado con desenlace `Defecto de codigo`. Causa raiz ubicada en `wp_export_debitos.srw`: al buscar para BBVA se aplica filtro temporal sobre `dw_list_det` por `recalcula = "S"`; si no hay filas recalculables, el filtro no se limpia. Luego, como el ente BBVA tiene `total_detalle = D`, `idw_data` apunta a `dw_list_det`; `RowCount()` queda en `0` y aparece `No hay registros para ser exportados`.
- Aclaracion tecnica posterior a Walter: el flag `recalcula = 'S'` lo pone el SP `afs_compro_exportacion`, no PowerBuilder. En `backend/procedures/afs_compro_exportacion.sql`, bloque BBVA, primero arma `#compro_recalculo` con `recalcula = 'N'`; luego, si `@var_min_vto < @fecha_debito`, hace `UPDATE #compro_recalculo` y pone `S` cuando `@fecha_debito > IsNull(Isnull(vto3, vto2), vto)`. Despues copia a `#comprobantes.recalcula` y el DataWindow detalle devuelve ese campo.
- Criterio durable para continuar R30342: que todos los comprobantes vengan con `recalcula = 'N'` es un escenario valido; el bug es que PB no restaura `dw_list_det` con `SetFilter('')` + `Filter()` despues del filtro temporal cuando no encuentra filas `S`.
- Punto pendiente antes de `03_spec`: confirmar si el alcance incluye solo restaurar filtro/redraw de `dw_list_det` o tambien corregir anomalia visual de doble barra/superposicion mencionada durante el analisis.

## 2026-09-09 15:55 ART - Tarea Redmine 28243 / R28243 Odontologia

- Conversacion con Federico sobre Redmine `28243`, seccion odontologia, ruta de trabajo `//infomed03n/Agentes/facevedo/R28243/seccion_odonto`.
- Primera entrega previa: documento `ticket_28243_1_05_dev.md` y archivos propuestos en `ARCHIVOS2/`. Diagnostico: `prs_valida_of_odon` devuelve rechazos propios de odontologia, pero `prs_valida_of_auto_remotas` no los recalculaba como rechazo priorizado de cabecera antes de grabar con `pru_autorizaciones`. La propuesta SQL tomaba rechazo vigente de cabecera mas rechazos de `auto_prestac_items_remotas`, ordenaba por grupo/orden y asignaba `@recha`/`@esta` antes del grabado.
- Validacion informada en esa entrega: en `SMC_Prox_Ver`, rechazos `183` y `249` tienen grupo/orden; `179`, `180`, `181` no. Parseo SQL con `SET PARSEONLY ON` sin errores. No se modifico SVN ni base.
- Federico luego detecto que en la propuesta basada en `ticket_28243_1_04_dev.md` faltaba la llamada explicita a `prs_valida_of_odon` dentro de `prs_valida_of_auto_remotas` para validar rechazos odontologicos y considerarlos al guardar la autorizacion.
- Segunda entrega generada: documento `//infomed03n/Agentes/facevedo/R28243/seccion_odonto/ticket_28243_1_06_dev.md` y archivos propuestos en `//infomed03n/Agentes/facevedo/R28243/seccion_odonto/ARCHIVOS3/`.
- Cambio principal de `ARCHIVOS3/backend/procedures/prs_valida_of_auto_remotas.sql`: ejecutar `dbo.prs_valida_of_odon` por cada item remoto odontologico antes de `pru_autorizaciones`; cargar los rechazos devueltos en `auto_prestac_items_remotas` respetando prioridad por grupo/orden; recalcular `@recha` y `@esta` de cabecera para que la autorizacion se guarde considerando esos rechazos.
- Validaciones reportadas para la segunda entrega: revisados `ticket_28243_1_04_dev.md`, `prs_valida_of_odon.sql` y `prs_valida_of_auto_remotas.sql`; sintaxis SQL validada con `SET PARSEONLY ON` sin errores; sin modificaciones en SVN ni ejecuciones de cambio en base.

## 2026-09-09 18:04 ART - Tarea Redmine 27498 / R27498 continuacion

- Cristian respondio definiciones del informe `02_req_nuevo`: `D1` elegir nombre apropiado inexistente; `D3` modificar solo textos `Sucur` por `Sucursal` y `Auto` por `Autorizacion`; `A1: No` tocar `w_clase_auto.srw`; `A2: No` tocar DataWindows `d_auto*`.
- Se explico `D2`: la logica comun para resolver sucursal por autorizacion puede ir en NVO/helper reutilizable o repetirse como Window Functions locales. Cristian aprobo usar funcion/objeto reutilizable.
- Se actualizo `/mnt/Agentes/cratto/R27498/ticket_27498_02_req_nuevo.md` a estado `OK` con definiciones cerradas: `prs_auto_sucursales_x_auto` como SP nuevo validado inexistente; D2 con funcion reutilizable comun invocada desde ventanas de expedientes; D3 solo textos; A1 no tocar `w_clase_auto.srw`; A2 no tocar `d_auto*`. Validacion reportada: ASCII, sin BOM, LF, 426 lineas.
- Analisis funcional actual informado a Cristian: hoy la pantalla no completa sucursal desde autorizacion. Ordenes/Honorarios inicializan `sucur` con `glob.gi_sucur` y validan `sucur + auto`; Sanatorios si `sucur` viene nula asigna `glob.gi_sucur`; Odontologia corta con `Ingrese la sucursal.` si se ingresa auto sin sucursal.
- Cristian pidio revisar si el combo de sucursales se usa solo para autorizaciones o tambien para otra cosa, porque si fuera exclusivo podria cargarse con las sucursales de la auto. Advertencia durable: no modificar `dd_sucursales.srd` generico si es compartido; en la especificacion se eligio DDDW especifico para autorizaciones/expedientes.
- Se genero `/mnt/Agentes/cratto/R27498/ticket_27498_03_spec.md` desde prompt SVN `branches/prompts_std/03_spec.md`, usando informe `02_req_nuevo`. Estado `OK`; objetos PB nuevos `2`; objetos PB a modificar `9`; objeto PB solo a revisar en IDE `1` (`d_expe_hono_cons.srd`); scripts SQL `2`; complejidad media.
- Criterio tecnico de `03_spec`: no tocar `w_clase_auto.srw`, `d_auto*` ni `dd_sucursales.srd`; crear NVO reutilizable `uo_nv_auto_sucur_expe`, DDDW especifico `dd_auto_sucursales_x_auto` y SP `prs_auto_sucursales_x_auto`.
- Se genero `/mnt/Agentes/cratto/R27498/ticket_27498_04_dev.md` en modo `PROPONER`, con archivos propuestos en `/mnt/Agentes/cratto/R27498/propuestos/`. Resultado final: PB nuevos `2`, PB modificados `9`, scripts SQL `2` (alta + rollback), total `15` archivos en `propuestos`, estado `COMPLETADA CON OBSERVACIONES`.
- Observacion de `04_dev`: `d_expe_hono_cons.srd` queda para revision manual en IDE porque el export textual no contiene controles `sucur`/`auto`, DDDW `dd_sucursales` ni etiquetas aplicables.
- Validacion final reportada para `04_dev`: no se entregan `w_clase_auto.srw`, `d_auto*`, `d_auto_sucursales.srd` ni `dd_sucursales.srd`; no se toco SVN, no se ejecuto DDL/DML y no se hizo commit.
