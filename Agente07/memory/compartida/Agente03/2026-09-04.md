
## 2026-09-04 10:36 ART - RM29794 / Export comprobantes EC CSV

- Conversacion con Cristian por RM29794. Trabajo local restringido a `/mnt/Agentes/cratto/RM29794/ObjetosModificados`; no se tocaron ramas SVN.
- Objetos foco: `wp_export_comprobantes_ec_csv.srw`, `d_export_comprobantes_csv.srd`, `d_export_estado_cuenta_csv.srd`, y SP local `afs_generar_estado_cuenta_csv.sql`.
- Los DataObjects de exportacion CSV quedaron conceptualmente con una unica columna `linea`; los SP hijos reciben `@prm_modo`; la ventana llama modo fijo `1`.
- Incidente diagnosticado: `lds_estado_cuenta.Retrieve(ii_prepaga, ii_emi_grupo, id_factu_perio, 1)` fallaba en runtime con retorno `-1`, mientras `SQLCode = 0` y `SQLDBCode = 0`. El SP `afs_generar_estado_cuenta_csv` ejecutado manualmente devolvia 16 filas con `EXECUTE afs_generar_estado_cuenta_csv @prm_prepaga = 1, @prm_emi_grupo = 1, @prm_factu_perio = '20260801', @prm_modo = 1`. En diseno conectado a `premedic_desarrollo` no fallaba.
- Primer intento revertible: convertir `id_factu_perio` a string `yyyymmdd` y cambiar argumento del DataObject a string. No resolvio.
- Segundo intento revertido por pedido de Cristian: tipar explicitamente `linea` como `CONVERT(varchar(4000), CONCAT(...))` en `afs_generar_estado_cuenta_csv.sql` y `type=char(4000)` en `d_export_estado_cuenta_csv.srd`. Como siguio fallando, se restauro el SP local a `SELECT linea = CONCAT(`.
- Solucion local vigente al cierre: en `wp_export_comprobantes_ec_csv.srw`, el flujo de exportacion de estado de cuenta ya no usa `lds_estado_cuenta.Retrieve(...)`; ejecuta `afs_generar_estado_cuenta_csv` mediante `DECLARE ... PROCEDURE FOR`, hace `FETCH` de la columna `linea` y escribe directo el archivo `EC_...csv`.
- Validaciones informadas: no queda `lds_estado_cuenta.Retrieve`; `@prm_modo` sigue fijo en `1`; SP restaurado; `.srw` conserva UTF-16 LE con CRLF. No se pudo compilar/regenerar desde PowerBuilder en el entorno.

## 2026-09-04 13:21 ART - R30307 facturacion masiva / correlatividad fechas

- Interlocutor: Walter.
- Tema: Redmine 30307. Requerimiento: en facturacion masiva, cambiar la fecha del comprobante para que pueda ser el primer dia de `factu_perio` segun configuracion.
- Criterio acordado: no tocar `afs_busca_fecha_compro` como solucion global, porque tambien puede ser consumido por facturacion individual y otros flujos compartidos. El cambio del R30307 debe acotarse al punto de decision de la facturacion masiva, antes de entrar al flujo comun.
- Validacion sobre `afs_busca_fecha_compro`: no se ejecuta en todos los procesos que generan comprobantes. En rama `wguise-V25.08.001` se lo vio llamado directamente en `backend/procedures/afi_genera_compro_compro.sql` y `backend/procedures/afi_genera_compro_compro_nc.sql`.
- Flujo principal moderno: `afi_fac_compro_a_generar.sql` contiene logica equivalente de correlatividad. Arma `#MAXIMO_FECHA_LETRA_PUNTO`, busca maxima `COMPROBANTES.fecha` por `ente`, `compro_tipo`, letra y punto, y si hay una mayor que `@prm_fecha`, actualiza `#FAC_COMPRO_A_GENERAR.fecha` y `vto1_fecha`.
- Procesos cubiertos por el nucleo `afi_fac_compro_a_generar`: facturacion masiva mensual (`afi_generar_compro_masiva` -> `afi_genera_compro`), facturacion masiva por job/batch (`afj_job_facturacion_procesar` -> `afi_generar_compro_masiva`), facturacion individual afiliado/empresa-subcuenta (`afi_generar_compro_individual` -> `afi_genera_compro`), ajustes manuales y notas de debito por intereses desde caja/cobranza si pasan por `afi_gen_aju_*_y_facturar` -> `afi_genera_compro`, y solicitudes/pedidos con `aru_generar_comprobante` si derivan a `afi_generar_compro_individual`.
- Vencimientos: en `afu_fac_compro_a_generar_vtos_interes`, la fecha base es la mayor entre `CAG.fecha` y `CAG.factu_perio`; luego llama a `afs_dame_fechas_vto`. Si la correlatividad sube la fecha del comprobante a una fecha futura, los vencimientos tambien quedan arrastrados por esa fecha.
- Riesgo detectado en recibos/cobranzas: los recibos comunes se generan con fecha actual (`gd_hoy` / `GETDATE()` / `@fecha_pago` segun flujo). La busqueda de deuda puede incluir comprobantes a vencer/futuros cuando `afs_ctacte_deuda` recibe `@compro_avencer = 'S'`, porque usa `@fecha_hasta = '20501231'`. Tambien hay busqueda explicita de deuda desde hoy en adelante (`d_ctacte_inte_desde_hoy`) con `fecha > :as_fecha`, `impre_fecha is not null`, `saldo > 0`, `baja_fecha is null`.
- Riesgo funcional: hoy la aplicacion puede permitir aplicar un recibo contra una factura/ND con fecha futura. Si el recibo queda con fecha de hoy y el comprobante aplicado tiene fecha posterior, se produce inconsistencia temporal en cuenta corriente, reportes por fecha, imputaciones y controles de correlatividad.
- Procesos no cubiertos o a revisar especialmente: `afi_recibos.sql` graba `recibo_fecha = getdate()` e ignora el parametro `@fecha`; cobranza manual/diaria/multiple inserta directo en `COMPROBANTES` con `@fecha_pago` como `fecha` e `impre_fecha` (ejemplo: `afi_cobranza_manual.sql`); reversores como `afi_rev_comprobantes_mass` insertan comprobantes reversos con fecha del dia (`GETDATE()` normalizado) sin control visible de maxima fecha; solicitud/exportacion CAE (`afs_compro_cae_solicitud.sql`) no corrige fechas, solo toma comprobantes ya grabados pendientes de CAE, por lo que la correlatividad debe estar resuelta antes.
- Recomendacion tecnica para continuar R30307: ademas del cambio acotado en masiva, agregar proteccion especifica en flujos no cubiertos: recibos que aplican comprobantes futuros deben usar fecha = max(fecha actual/pago, maxima fecha de comprobantes aplicados); cobranza manual/importada/diaria/multiple que inserte recibos en `COMPROBANTES` debe aplicar el mismo criterio; reversion debe validar maxima fecha por ente/tipo/letra/punto antes de grabar; CAE no debe ser corrector, solo punto de validacion.

## 2026-09-04 16:05 ART - RM26993 / OSPANA, Medimas y Mutual

- Conversacion con Cristian retomando RM26993. Restriccion vigente: no tocar ramas SVN.
- Se verifico que no existia profile `Ospana_Prod_test` en `DB_PROFILE.md`. Luego se registro profile nuevo `OSPANA_Prod_Test`: motor MS SQL, host `172.16.10.115`, base `OSPANA_Prod_Test`, usuario `sa`, password `IMed.2021`, puerto `1433`, notas `N/A`. Validacion informada: conexion TCP, autenticacion y `SELECT DB_NAME()` OK; entrada unica; `DB_PROFILE.md` preservado UTF-8 sin BOM con LF.
- RM26993 OSPANA: Cristian pidio aplicar criterio Roisa a `/mnt/Agentes/cratto/RM26993/ObjetosOspania/d_factura_ospania.srd` y verificar errores de `/mnt/Agentes/cratto/RM26993/Bugs/R26993-Errores OSPANA.docx` contra base `OSPANA_Prod_Test`.
- Cambios OSPANA aplicados en `d_factura_ospania.srd`: se agregaron columnas `total`, `grava`, `exen` que ya devuelve `dbo.afs_compro_impresion_caseros`; `SUBTOTAL` toma `grava + exen`; `TOTAL FACTURA` toma `total`; vencimientos calculan `total + deudaN - creditos` con piso en `0`; barra de pago oculta cuando el neto del primer vencimiento queda `<= 0`.
- Verificacion OSPANA informada contra `OSPANA_Prod_Test`: casos exento saldo a favor mayor/igual, exento saldo a favor menor, exento en contra, gravado en contra, gravado saldo a favor mayor/igual y gravado saldo a favor menor quedaron alineados; no se pudo compilar/previsualizar en PowerBuilder; archivo preservado UTF-16 LE con BOM y CRLF.
- Se entregaron a Cristian dos queries de control para OSPANA: una partiendo de `COMPROBANTES` y otra de `COMPRO_IMPRESION`, ambas clasificando casos reportados por combinacion de `exen`, `grava`, `total`, `deuda1`, `creditos`, y calculando `vto1_esperado` y `barra_visible_esperada`.
- RM26993 Medimas: Cristian indico que faltaba formula para esconder codigo de barras en `/mnt/Agentes/cratto/RM26993/ObjetosMedimas/d_factura_solo_medimas.srd`, solo eso. Se ajusto `barra_posta` para ocultarse cuando `saldo1 <= 0`, cuando es `NC`, o cuando `barra` viene nula. No se toco SVN; archivo preservado UTF-16 LE con BOM y CRLF.
- RM26993 Mutual: ante consulta de Cristian se resumio estado. Antes: `TOTAL FACTURA A/B` usaba `abs(impor1)`; `SUBTOTAL A` usaba `abs(impor1 - iibb_arba - iva_total)`; `SUBTOTAL B` usaba `abs(impor1)`; vencimientos A usaban `abs(imporN + deudaN)`, B usaba `abs(imporN)`, vto3 `abs(impor3)`; barra visible si existia y empezaba con `0448`; `saldo_anterior2` mostraba `deuda2`.
- RM26993 Mutual ahora: `afs_compro_impresion_mutual.sql` trae `total`, `grava`, `exen` desde `COMPROBANTES`; `impor_letras`/`impor_letra` salen de `c.total`; `TOTAL FACTURA A/B` usa `abs(total)`; `SUBTOTAL A` usa `abs(grava + exen)`; `SUBTOTAL B` quedo `abs(impor1)`; vencimientos A/B usan `abs(total - creditos)`; barra ademas se oculta si `abs(total - creditos) = 0`; `saldo_anterior2` paso de `deuda2` a `deuda2 - creditos`. Se aclaro que Mutual quedo con criterio tipo SBSalud y no incluye `deudaN` en vencimientos, distinto a OSPANA/Roisa.

## 2026-09-04 18:58 ART - R30307 / fechas futuras y copagos revision 2

- R30307: la facturacion masiva entra por `afi_generar_compro_masiva` y termina en `afi_fac_compro_a_generar`, donde ya existe control de maxima fecha por `ente + compro_tipo + letra + punto`. `afs_busca_fecha_compro` solo aparece en los generadores legacy `afi_genera_compro_compro` y `afi_genera_compro_compro_nc`.
- R30307: los vencimientos se calculan en `afu_fac_compro_a_generar_vtos_interes` usando la mayor entre `CAG.fecha` y `CAG.factu_perio`; si la correlatividad sube la fecha a una futura, los vencimientos tambien quedan basados en esa fecha futura.
- R30307: se genero y dejo en el compartido la especificacion funcional en `X:\wguise\R30307\R30307_especificacion_funcional_fecha_comprobantes.md`.
- Copagos revision 2 en `wguise-V25.08.001`: la exencion existe para el prestador/plan consultado en `prestad_exen_copa`, pero el flujo de copago por orden no la aplica; `wf_valida_copagos_orden` deja pasar aun con exencion y `prs_copagos_orden` / `prs_copagos_orden_pres` no consultan `prestad_exen_copa`, por eso el copago sigue saliendo comun.
