# Memoria durable - 2026-08-20

## RM26993 - contexto general
- Humano: Cristian. Pide trabajar sobre Redmine 26993 sin filtrar memoria por sender_id cuando busca contexto historico del caso.
- Documento funcional actualizado: `/mnt/Agentes/cratto/RM26993/R26993_V2.docx`. Reglas ampliadas desde pagina 58 / Histo 6.
- Reglas funcionales aprendidas del documento actualizado:
  - Existen 12 formatos de comprobante mas el formato generico `d_factura`.
  - El analisis debe contemplar: visualizacion de saldo a favor; aplicacion de saldo a favor mayor o igual al importe del comprobante; aplicacion de saldo a favor menor al importe; aplicacion de saldo deudor al vencimiento.
  - La correccion aplica a todos los formatos de facturacion.
  - `SUBTOTAL` no debe ser afectado por saldo a favor ni saldo deudor.
  - `TOTAL FACTURA` debe ser `SUBTOTAL + IVA + IIBB`, cuando correspondan.
  - `VENCIMIENTO` debe reflejar la cuenta corriente: con saldo a favor aplicado, `TOTAL FACTURA - saldo a favor aplicado`; sin saldo, `TOTAL FACTURA`; con saldo deudor, segun formato/cliente, puede mantenerse igual al total o sumar saldo deudor.
  - La aplicacion de saldos depende de parametros `aplicacion_auto_cred` y `aplica_auto_n_cred`.
- Restriccion operativa de Cristian: no tocar ramas SVN; usar SVN solo lectura y exportar objetos a carpetas de trabajo.

## RM26993 - ROISA
- Carpeta final/trabajo: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa`.
- Objetos finales: `afs_compro_impresion_roisa.sql`, `d_factura_roisa.srd`.
- Regla especifica ROISA aprendida: incorporar `GRAVA` como campo adicional en el SP de impresion, obtenido desde `COMPROBANTES`; en `d_factura_roisa`, `SUBTOTAL` debe ser directamente `GRAVA`, sin calculo ni transformacion adicional.
- Estado anterior ROISA:
  - `Subtotal` se calculaba desde `impor1`: `abs(impor1 - iibb_arba - iva_total)`.
  - `Total factura A/B` se calculaba como `abs(impor1)`.
  - Problema: `impor1` es importe de impresion/vencimiento y podia venir afectado por creditos, saldo a favor o logica de vencimientos.
- Estado actual ROISA:
  - SP devuelve `grava = ISNULL(c.grava, 0)` desde `COMPROBANTES`; rama `RS` devuelve `grava = 0`.
  - `subtotal_a = abs(grava)`.
  - `total_factura_a = abs(grava + iva_total + iibb_arba + iibb_caba)`.
  - `total_factura_b = abs(grava + iva_total + iibb_arba + iibb_caba)`.
  - Vencimientos sin cambios: `total_factura_a_vto1 = abs(impor1 + deuda1)`, `total_factura_a_vto2 = abs(impor2 + deuda2)`, Vto3 A `compute_17 = abs(impor3 + deuda3)`, `total_factura_b_vto1 = abs(impor1 + deuda1)`, `total_factura_b_vto2 = abs(impor2 + deuda2)`, Vto3 B `compute_8 = abs(impor3 + deuda3)`.
- Casos testigo ROISA validados contra `ROISA_Prod_Test`:
  - `B006400183675`: `grava = 671894.94`, `iva_total = 70548.97`, `iibb = 0.00`, total esperado `742443.91`, OK.
  - `A006400002508`: `grava = 540193.40`, `iva_total = 56720.31`, `iibb = 0.00`, total esperado `596913.71`, OK.
- Idea fuerza para explicar: ROISA ahora muestra importe fiscal correcto en `Subtotal` y `Total Factura`; el saldo aplicado queda limitado a vencimientos.

## RM26993 - ActivaSalud
- Carpeta de trabajo: `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud`.
- Objeto: `d_factura_activa.srd`.
- SP asociado: `afs_compro_impresion_caseros.sql`.
- El SP `afs_compro_impresion_caseros` se usa en 8 DataWindows: `d_factura`, `d_factura_ospania`, `d_factura_igualdad`, `d_factura_sbsalud`, `d_factura_premedic`, `d_factura_caseros`, `d_factura_sin_deuda`, `d_factura_activa`.
- Hubo un primer intento con `exen/grava`, pero Cristian pidio revertirlo. Estado final: no tocar SP, subtotal ni total factura; revisar solo vencimientos.
- Estado anterior ActivaSalud:
  - `subtotal_a = abs(impor1 - iibb_arba - iva_total)`.
  - `total_factura_a = abs(impor1)`.
  - `total_factura_b = abs(impor1)`.
  - `saldo_anterior1 = deuda1 - creditos`.
  - `saldo_anterior2 = deuda2`.
  - `total_factura_a_vto1 = abs(impor1)`.
  - `total_factura_b_vto1 = abs(impor1 + deuda1)`.
  - `total_factura_a_vto2 = abs(impor2)`.
  - `total_factura_b_vto2 = abs(impor2 + deuda2)`.
- Estado actual ActivaSalud:
  - `Subtotal` y `Total Factura` siguen como trunk, sin cambios.
  - `saldo_anterior1 = deuda1 - creditos`.
  - `saldo_anterior2 = deuda2 - creditos`.
  - `total_factura_a_vto1 = abs(impor1 + saldo_anterior1)`.
  - `total_factura_b_vto1 = abs(impor1 + saldo_anterior1)`.
  - `total_factura_a_vto2 = abs(impor2 + saldo_anterior2)`.
  - `total_factura_b_vto2 = abs(impor2 + saldo_anterior2)`.
  - Vto3 sin cambio porque no existe `saldo_anterior3`: `compute_2` / Vto3 A `abs(impor3)`, `compute_8` / Vto3 B `abs(impor3 + deuda3)`.
- Caso testigo ActivaSalud validado contra `Activa_Prod_Test`:
  - `B000300003628`, afiliado `004063 - GIANA, ANDRES PEDRO`.
  - Datos: `total = 40000.00`, `impor1 = 40000.00`, `impor2 = 40000.00`, `deuda1 = 0.00`, `deuda2 = 0.00`, `creditos = 40000.00`.
  - Resultado: Vto1 `40000 + (0 - 40000) = 0.00`; Vto2 `40000 + (0 - 40000) = 0.00`.
- Idea fuerza: ActivaSalud mantiene subtotal y total como estaban; solo corrige vencimientos para que el saldo a favor aplicado reduzca correctamente el importe a pagar.

## RM26993 - Celer
- Carpeta de trabajo revisada: `/mnt/Agentes/cratto/RM26993/ObjetosCeler`.
- Objeto revisado: `d_factura_celer.srd`.
- Resultado: Celer sigue OK a nivel objeto; el exportado es equivalente al trunk, sin diferencias funcionales.
- SP asociado: `afs_compro_impresion_sel_full`.
- No hay SP exportado en la carpeta de ObjetosCeler, solo `.srd`.
- Logica correcta ya presente:
  - `saldo_anterior1 = deuda1 - creditos`.
  - `saldo_anterior2 = deuda2 - creditos`.
  - Vto1: `compute_5` y `compute_9` muestran `impor1 + saldo_anterior1`; si resultado <= 0 muestran `**,**`.
  - Vto2: `compute_6` y `compute_11` muestran `impor2 + saldo_anterior2`; si resultado <= 0 muestran `**,**`.
  - Saldo anterior: `compute_31` muestra `saldo_anterior1`, visible solo si hay saldo distinto de cero y no es nota de credito.
  - Total principal: `compute_24 = abs(impor1)`; no se cambio.
- Conclusion: Celer ya estaba alineado con la regla esperada para vencimientos, descontando creditos desde `saldo_anterior1/2` y mostrando importe exigible como `imporN + saldo_anteriorN`.

## RM26993 - Montegrande
- Carpeta/objeto de trabajo: `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd`.
- SP asociado: `afs_compro_impresion_cmmg`; no fue modificado.
- Base usada para pruebas: `Montegrande_Prod_Test`.
- Cambio aplicado en vencimientos:
  - `saldo_anterior1` ya estaba OK: `deuda1 - creditos`.
  - `saldo_anterior2` ya estaba OK: `deuda2 - creditos`.
  - Se ajustaron Vto1/Vto2 para no usar `abs(...)` sobre resultados negativos.
  - Ahora si el neto da menor o igual a cero, muestra `0`.
  - Tambien se ajusto el codigo de barras para que solo se imprima si `impor1 + saldo_anterior1 > 0`.
- Formulas finales Montegrande:
  - `Vto1 A/B = if(impor1 + saldo_anterior1 <= 0, 0, impor1 + saldo_anterior1)`.
  - `Vto2 A/B = if(impor2 + saldo_anterior2 <= 0, 0, impor2 + saldo_anterior2)`.
- Casos de prueba Montegrande:
  - A con saldo a favor: `A002000017456`, contra `12591`; `impor1 124847.49`, `deuda1 49725.00`, `creditos 206652.51`; Vto1 `0.00`, Vto2 `0.00`.
  - B con saldo a favor: `B002000293804`, contra `24458`; `impor1 78726.59`, `creditos 21273.41`; Vto1/Vto2 `57453.18`.
  - A con saldo en contra: `A002700000036`, contra `9891`; `impor1 304.35`, `deuda1 8855.83`, `creditos 0.00`; Vto1/Vto2 `9160.18`.
  - B con saldo en contra: `B003400007068`, contra `9999997`; `impor1 17250.00`, `deuda1 2001.73`, `creditos 0.00`; Vto1 `19251.73`, Vto2 `17250.00`.
- Validaciones realizadas: encoding preservado UTF-16 LE con BOM y CRLF; formulas finales verificadas en `.srd`; consultas read-only contra `Montegrande_Prod_Test`; SVN no tocado; no se compilo PowerBuilder.

## Perfiles de conexion DB informados a Cristian
- `SMC_PROD_TEST`
- `Quimicos_Prod`
- `Premedic_Prod`
- `SMC_PROX_VER`
- `Ospecon_Desarrollo`
- `Pensandosalud_Prod`
- `Unosalud_prod_test`
- `Consalud_Prod`
- `Ospicine_Prod`
- `Ospecon_Prox_Ver`
- `ROISA_Prod_Test`
- `ACTIVA_Prod`
- `Premedic_Prox_Ver`
- `ACTIVA_Desarrollo`
- `mutualRioIV_prod`
- `Activa_Prod_Test`
- `ospecon_prod`
- `Montegrande_Prod_Test`
# Memoria durable - 2026-08-20

## RM26993 - contexto general
- Humano: Cristian. Pide trabajar sobre Redmine 26993 sin filtrar memoria por sender_id cuando busca contexto historico del caso.
- Documento funcional actualizado: `/mnt/Agentes/cratto/RM26993/R26993_V2.docx`. Reglas ampliadas desde pagina 58 / Histo 6.
- Reglas funcionales aprendidas del documento actualizado:
  - Existen 12 formatos de comprobante mas el formato generico `d_factura`.
  - El analisis debe contemplar: visualizacion de saldo a favor; aplicacion de saldo a favor mayor o igual al importe del comprobante; aplicacion de saldo a favor menor al importe; aplicacion de saldo deudor al vencimiento.
  - La correccion aplica a todos los formatos de facturacion.
  - `SUBTOTAL` no debe ser afectado por saldo a favor ni saldo deudor.
  - `TOTAL FACTURA` debe ser `SUBTOTAL + IVA + IIBB`, cuando correspondan.
  - `VENCIMIENTO` debe reflejar la cuenta corriente: con saldo a favor aplicado, `TOTAL FACTURA - saldo a favor aplicado`; sin saldo, `TOTAL FACTURA`; con saldo deudor, segun formato/cliente, puede mantenerse igual al total o sumar saldo deudor.
  - La aplicacion de saldos depende de parametros `aplicacion_auto_cred` y `aplica_auto_n_cred`.
- Restriccion operativa de Cristian: no tocar ramas SVN; usar SVN solo lectura y exportar objetos a carpetas de trabajo.

## RM26993 - ROISA
- Carpeta final/trabajo: `/mnt/Agentes/cratto/RM26993/ObjetosRoisa`.
- Objetos finales: `afs_compro_impresion_roisa.sql`, `d_factura_roisa.srd`.
- Regla especifica ROISA aprendida: incorporar `GRAVA` como campo adicional en el SP de impresion, obtenido desde `COMPROBANTES`; en `d_factura_roisa`, `SUBTOTAL` debe ser directamente `GRAVA`, sin calculo ni transformacion adicional.
- Estado anterior ROISA:
  - `Subtotal` se calculaba desde `impor1`: `abs(impor1 - iibb_arba - iva_total)`.
  - `Total factura A/B` se calculaba como `abs(impor1)`.
  - Problema: `impor1` es importe de impresion/vencimiento y podia venir afectado por creditos, saldo a favor o logica de vencimientos.
- Estado actual ROISA:
  - SP devuelve `grava = ISNULL(c.grava, 0)` desde `COMPROBANTES`; rama `RS` devuelve `grava = 0`.
  - `subtotal_a = abs(grava)`.
  - `total_factura_a = abs(grava + iva_total + iibb_arba + iibb_caba)`.
  - `total_factura_b = abs(grava + iva_total + iibb_arba + iibb_caba)`.
  - Vencimientos sin cambios: `total_factura_a_vto1 = abs(impor1 + deuda1)`, `total_factura_a_vto2 = abs(impor2 + deuda2)`, Vto3 A `compute_17 = abs(impor3 + deuda3)`, `total_factura_b_vto1 = abs(impor1 + deuda1)`, `total_factura_b_vto2 = abs(impor2 + deuda2)`, Vto3 B `compute_8 = abs(impor3 + deuda3)`.
- Casos testigo ROISA validados contra `ROISA_Prod_Test`:
  - `B006400183675`: `grava = 671894.94`, `iva_total = 70548.97`, `iibb = 0.00`, total esperado `742443.91`, OK.
  - `A006400002508`: `grava = 540193.40`, `iva_total = 56720.31`, `iibb = 0.00`, total esperado `596913.71`, OK.
- Idea fuerza para explicar: ROISA ahora muestra importe fiscal correcto en `Subtotal` y `Total Factura`; el saldo aplicado queda limitado a vencimientos.

## RM26993 - ActivaSalud
- Carpeta de trabajo: `/mnt/Agentes/cratto/RM26993/ObjetosActivaSalud`.
- Objeto: `d_factura_activa.srd`.
- SP asociado: `afs_compro_impresion_caseros.sql`.
- El SP `afs_compro_impresion_caseros` se usa en 8 DataWindows: `d_factura`, `d_factura_ospania`, `d_factura_igualdad`, `d_factura_sbsalud`, `d_factura_premedic`, `d_factura_caseros`, `d_factura_sin_deuda`, `d_factura_activa`.
- Hubo un primer intento con `exen/grava`, pero Cristian pidio revertirlo. Estado final: no tocar SP, subtotal ni total factura; revisar solo vencimientos.
- Estado anterior ActivaSalud:
  - `subtotal_a = abs(impor1 - iibb_arba - iva_total)`.
  - `total_factura_a = abs(impor1)`.
  - `total_factura_b = abs(impor1)`.
  - `saldo_anterior1 = deuda1 - creditos`.
  - `saldo_anterior2 = deuda2`.
  - `total_factura_a_vto1 = abs(impor1)`.
  - `total_factura_b_vto1 = abs(impor1 + deuda1)`.
  - `total_factura_a_vto2 = abs(impor2)`.
  - `total_factura_b_vto2 = abs(impor2 + deuda2)`.
- Estado actual ActivaSalud:
  - `Subtotal` y `Total Factura` siguen como trunk, sin cambios.
  - `saldo_anterior1 = deuda1 - creditos`.
  - `saldo_anterior2 = deuda2 - creditos`.
  - `total_factura_a_vto1 = abs(impor1 + saldo_anterior1)`.
  - `total_factura_b_vto1 = abs(impor1 + saldo_anterior1)`.
  - `total_factura_a_vto2 = abs(impor2 + saldo_anterior2)`.
  - `total_factura_b_vto2 = abs(impor2 + saldo_anterior2)`.
  - Vto3 sin cambio porque no existe `saldo_anterior3`: `compute_2` / Vto3 A `abs(impor3)`, `compute_8` / Vto3 B `abs(impor3 + deuda3)`.
- Caso testigo ActivaSalud validado contra `Activa_Prod_Test`:
  - `B000300003628`, afiliado `004063 - GIANA, ANDRES PEDRO`.
  - Datos: `total = 40000.00`, `impor1 = 40000.00`, `impor2 = 40000.00`, `deuda1 = 0.00`, `deuda2 = 0.00`, `creditos = 40000.00`.
  - Resultado: Vto1 `40000 + (0 - 40000) = 0.00`; Vto2 `40000 + (0 - 40000) = 0.00`.
- Idea fuerza: ActivaSalud mantiene subtotal y total como estaban; solo corrige vencimientos para que el saldo a favor aplicado reduzca correctamente el importe a pagar.

## RM26993 - Celer
- Carpeta de trabajo revisada: `/mnt/Agentes/cratto/RM26993/ObjetosCeler`.
- Objeto revisado: `d_factura_celer.srd`.
- Resultado: Celer sigue OK a nivel objeto; el exportado es equivalente al trunk, sin diferencias funcionales.
- SP asociado: `afs_compro_impresion_sel_full`.
- No hay SP exportado en la carpeta de ObjetosCeler, solo `.srd`.
- Logica correcta ya presente:
  - `saldo_anterior1 = deuda1 - creditos`.
  - `saldo_anterior2 = deuda2 - creditos`.
  - Vto1: `compute_5` y `compute_9` muestran `impor1 + saldo_anterior1`; si resultado <= 0 muestran `**,**`.
  - Vto2: `compute_6` y `compute_11` muestran `impor2 + saldo_anterior2`; si resultado <= 0 muestran `**,**`.
  - Saldo anterior: `compute_31` muestra `saldo_anterior1`, visible solo si hay saldo distinto de cero y no es nota de credito.
  - Total principal: `compute_24 = abs(impor1)`; no se cambio.
- Conclusion: Celer ya estaba alineado con la regla esperada para vencimientos, descontando creditos desde `saldo_anterior1/2` y mostrando importe exigible como `imporN + saldo_anteriorN`.

## RM26993 - Montegrande
- Carpeta/objeto de trabajo: `/mnt/Agentes/cratto/RM26993/ObjetosMontegrande/d_factura_montegrande.srd`.
- SP asociado: `afs_compro_impresion_cmmg`; no fue modificado.
- Base usada para pruebas: `Montegrande_Prod_Test`.
- Cambio aplicado en vencimientos:
  - `saldo_anterior1` ya estaba OK: `deuda1 - creditos`.
  - `saldo_anterior2` ya estaba OK: `deuda2 - creditos`.
  - Se ajustaron Vto1/Vto2 para no usar `abs(...)` sobre resultados negativos.
  - Ahora si el neto da menor o igual a cero, muestra `0`.
  - Tambien se ajusto el codigo de barras para que solo se imprima si `impor1 + saldo_anterior1 > 0`.
- Formulas finales Montegrande:
  - `Vto1 A/B = if(impor1 + saldo_anterior1 <= 0, 0, impor1 + saldo_anterior1)`.
  - `Vto2 A/B = if(impor2 + saldo_anterior2 <= 0, 0, impor2 + saldo_anterior2)`.
- Casos de prueba Montegrande:
  - A con saldo a favor: `A002000017456`, contra `12591`; `impor1 124847.49`, `deuda1 49725.00`, `creditos 206652.51`; Vto1 `0.00`, Vto2 `0.00`.
  - B con saldo a favor: `B002000293804`, contra `24458`; `impor1 78726.59`, `creditos 21273.41`; Vto1/Vto2 `57453.18`.
  - A con saldo en contra: `A002700000036`, contra `9891`; `impor1 304.35`, `deuda1 8855.83`, `creditos 0.00`; Vto1/Vto2 `9160.18`.
  - B con saldo en contra: `B003400007068`, contra `9999997`; `impor1 17250.00`, `deuda1 2001.73`, `creditos 0.00`; Vto1 `19251.73`, Vto2 `17250.00`.
- Validaciones realizadas: encoding preservado UTF-16 LE con BOM y CRLF; formulas finales verificadas en `.srd`; consultas read-only contra `Montegrande_Prod_Test`; SVN no tocado; no se compilo PowerBuilder.

## Perfiles de conexion DB informados a Cristian
- `SMC_PROD_TEST`
- `Quimicos_Prod`
- `Premedic_Prod`
- `SMC_PROX_VER`
- `Ospecon_Desarrollo`
- `Pensandosalud_Prod`
- `Unosalud_prod_test`
- `Consalud_Prod`
- `Ospicine_Prod`
- `Ospecon_Prox_Ver`
- `ROISA_Prod_Test`
- `ACTIVA_Prod`
- `Premedic_Prox_Ver`
- `ACTIVA_Desarrollo`
- `mutualRioIV_prod`
- `Activa_Prod_Test`
- `ospecon_prod`
- `Montegrande_Prod_Test`

## Walter - consulta Redmine 28246 / Facundo Farias
- Walter pidio: "me podes pasar la iteracion que tuvo Facundo Farias con el Redmine 28246".
- Se encontro memoria compartida de Agente07 con tres bloques de trabajo de Facundo sobre R28246:
  - 2026-08-06: armado de casos de prueba para Redmine `28246` usando documentacion en `Incidentes/R28246`, especificacion `R-01_R28246_Documento_Especificacion_Funcional`, Excel `Casos a testear.xlsx` y evidencia previa. Resultado: 20 casos en `/mnt/Agentes/ffarias/R28246/Casos_Prueba_R28246.md` y `.csv`.
  - 2026-08-14: ejecucion Caso CP21 / Portal Prestadores desde `ffarias\R28246\CP\CasoCP21.xlsx`, fila real 5 `ID=CP4`, tipo `02A`; ejecuciones `6504883` y `6504886`, ambas `AUTO_REMOTAS.esta=R`, rechazo `12 - Requiere autorizacion`. Excel actualizado con `RESULTADO=NO OK`, `TRANSACCION=6504886`, `RECHAZO=12 - Requiere autorizacion`; luego se regenero el XLSX porque Facundo no podia abrirlo.
  - 2026-08-14: analisis tecnico posterior de rechazos: `6504887` tipo `01B` no validaba `1386` por exclusion en `prs_valida_of_cobertura_remoto`; `6504765` `02A` vs `6504766` `02C` diferian porque `02C` remapea estado usando `AUTO_VALIDACION_RECHAZOS`; `6504773` `02Q` primero validaba `668`; `6484621` `02A` quedaba `R` con `recha=NULL` por no propagar motivo `4`, mientras `6484624` `02C` si lo guardaba via `prs_valida_of_auto_remotas`.
- Tambien se encontro consolidacion del 2026-08-18: para `02A` no se obtiene `recha=90` por nivel superior si antes aplica `recha=12`; evidencia `6504886`; para `02C` si se obtiene `recha=90`, evidencia `6504878`.
- Respuesta dada a Walter incluyo fuentes: `memory/compartida/Agente07/MEMORY-users_110088613929347470679-2026-08-06.md`, `...-2026-08-14.md` y `memory/compartida/Agente07/2026-08-18.md`.

## Walter - R30046 macro estimacion generada
- Walter envio prompt estructurado para generar estimacion:
  - Prompt SVN: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/prompts_std/09_macro_estimacion.md`
  - Ticket Redmine: vacio
  - Usuario: `wguise`
  - Base de datos: vacio
  - Rama SVN: `rama productiva`
  - Documento fuente: `\\infomed03n\Agentes\wguise\R30046\R-09_30046.doc`
  - Ruta de salida: `\\infomed03n\Agentes\wguise\R30046\`
  - Nombre archivo: `ticket_30046_05_estimacion.md`
  - Observaciones: opcional/sin foco adicional.
- Se genero el archivo final: `\\infomed03n\Agentes\wguise\R30046\ticket_30046_05_estimacion.md`.
- Fuente principal usada: `R-09_30046.doc`.
- Rama SVN usada: rama productiva `wguise-Infomedical-trunk`, trunk rev. `84382`.
- Resumen ejecutivo informado: reporte de movimientos de cobranzas electronicas que consolida movimientos positivos y negativos de exportacion/reprocesos de recaudos por ente y recibos anulados no automaticos; impacto en Cobranzas / Cuentas Corrientes, PowerBuilder y SQL Server; sin impacto React/Node detectado; complejidad Media con incertidumbre funcional por definiciones pendientes.
- Macro-estimacion total informada: `10 a 17 dias/hombre`, confianza `Medio/Bajo`.
- Validaciones informadas: documento fuente y carpeta de salida validados; prompt SVN leido; rama productiva validada; Markdown generado con 128 lineas; ASCII sin BOM; LF; `pandoc` convirtio sin error.


## RM26993 - SMC
- Caso trabajado sobre `/mnt/Agentes/cratto/RM26993/ObjetosSMC/d_factura_caseros.srd`.
- Base usada: `SMC_Prod_Test` (profile existente `SMC_PROD_TEST`; se intento crear `SMC_Prod_Test` pero ya existia por comparacion case-insensitive).
- SP asociado: `afs_compro_impresion_caseros`; no fue modificado. SVN no tocado.
- Cambios aplicados en el DataWindow:
  - `saldo_anterior2 = if (isnull(deuda2),0,deuda2) - if (isnull(creditos),0,creditos)`.
  - Vto1 A/B calcula `impor1 + saldo_anterior1` con piso en `0`.
  - Vto2 A/B calcula `impor2 + saldo_anterior2` con piso en `0`.
  - Los 4 computes de vencimiento quedaron con formato `0.00`, para mostrar `$ 0.00` cuando corresponda.
  - Barra se ajusto para imprimir solo si `impor1 + saldo_anterior1 > 0`.
  - `SUBTOTAL` y `TOTAL FACTURA` no se modificaron.
- Casos prueba SMC:
  - A saldo a favor: `A000800081205`, contra `33696412`, total `57086.11`, creditos `1964.50`, deuda1/deuda2 `0.00`; Vto1 `53157.11`, Vto2 `54393.97`.
  - B saldo a favor: `B000900000029`, contra `17114`, total `69660.93`, creditos `8.91`, deuda1/deuda2 `0.00`; Vto1 `69643.11`, Vto2 `71952.37`.
  - A saldo en contra: `A000800081443`, contra `36434954`, total/impor1 `71999.28`, creditos `0.00`, deuda1 `966756.31`, deuda2 `976480.02`; Vto1 `1038755.59`, Vto2 `1049319.29`. Calculo Vto1 explicado a Cristian: `71999.28 + (966756.31 - 0.00) = 1038755.59`.
  - B saldo en contra: `B000900000028`, contra `17750`, total `8092.41`, creditos `0.00`, deuda1 `7492.98`, deuda2 `7741.37`; Vto1 `15585.39`, Vto2 `16102.04`.

## RM26993 - Mutual Rio IV
- Se creo profile `MutualRioIV_Prod_Test` en `DB_PROFILE.md`; conexion OK.
- Caso trabajado sobre `/mnt/Agentes/cratto/RM26993/ObjetosMutual/d_factura_mutual.srd`.
- Base usada: `MutualRioIV_Prod_Test`. SVN no tocado.
- Cambios aplicados:
  - `saldo_anterior2` ahora descuenta `creditos`.
  - Vto1, Vto2 y Vto3 calculan importe + deuda - creditos, con piso en `0`.
  - Vencimientos con formato `0.00`.
  - Barras de pago ocultas si el Vto1 exigible queda en cero.
  - `SUBTOTAL` y `TOTAL FACTURA` no fueron modificados.
- Validacion: encoding `UTF-16 LE BOM` y CRLF preservados.
- En `MutualRioIV_Prod_Test` no existen comprobantes `FC` letra A ni B en datos de impresion; solo letra C (`C: 1.483.692`, C con creditos `58.992`, C con deuda sin credito `162.744`). No fue posible validar los 4 escenarios A/B pedidos.
- Casos reales disponibles para rama no-A:
  - C/no-A saldo a favor: `C001600008878`, afiliado `PANIGATTI, ANA LUZ`, contra `403702`, creditos `1.00`, deuda1 `0.00`; Vto1 `0.00`, Vto2 `0.01`, Vto3 `0.02`.
  - C/no-A saldo en contra: `C001600008881`, afiliado `FUSCO, FABIO DANIEL`, contra `205824`, creditos `0.00`, deuda1 `977205.97`; Vto1 `1073455.97`, Vto2 `1083101.67`, Vto3 `1092747.37`.

## RM26993 - Premedic
- Se creo profile `Premedic_Prod_Test` en `DB_PROFILE.md`; conexion OK.
- Caso trabajado sobre `/mnt/Agentes/cratto/RM26993/ObjetosPremedic/d_factura_premedic.srd`.
- Base usada: `Premedic_Prod_Test`. SVN no tocado.
- Cambios aplicados:
  - `saldo_anterior2` ahora descuenta `creditos`.
  - Vto1, Vto2 y Vto3 calculan `importe + deuda - creditos`, con piso en `0`.
  - Vencimientos con formato `0.00`.
  - Barras de pago ocultas si el Vto1 exigible queda en cero.
  - `SUBTOTAL` y `TOTAL FACTURA` no se modificaron.
- Validacion: encoding `UTF-16 LE BOM` y CRLF preservados.
- En `Premedic_Prod_Test` no existen los 4 casos A/B pedidos en datos de impresion: `compro_impresion.creditos` tiene 0 registros con credito y `max_creditos = 0.00`; hay deuda pero no en comprobantes A/B; en `comprobantes` no hay `FC`; los A existentes son `DI` y `RC` sin registro asociado en `compro_impresion`; no aparecen comprobantes B.
- Caso real disponible para validar deuda en rama no-A: `CI C000100000003`, afiliado `Cardoso, Nancy E.`, contra `25390497`, `impor1 = -198.00`, `deuda1 = 17111.00`, `creditos = 0.00`, Vto1 esperado `16913.00`.

## RM26993 - SBSalud
- Se creo profile `SBSalud_Prod_Test` en `DB_PROFILE.md`; conexion OK.
- Caso trabajado sobre `/mnt/Agentes/cratto/RM26993/ObjetosSBSalud/d_factura_sbsalud.srd`.
- Base usada: `SBSalud_Prod_Test`. SVN no tocado.
- Cambios aplicados:
  - `saldo_anterior2` ahora descuenta `creditos`.
  - Vto1, Vto2 y Vto3 calculan `importe + deuda - creditos`, con piso en `0`.
  - Vencimientos con formato `0.00`.
  - Barras de pago ocultas si el Vto1 exigible queda en cero.
  - `SUBTOTAL` y `TOTAL FACTURA` no se modificaron.
- Validacion: encoding `UTF-16 LE BOM` y CRLF preservados.
- Casos prueba SBSalud:
  - A saldo a favor: `A000800007580`, afiliado `FERREIRO, FEDERICO EMANUEL`, contra `2526`, creditos `150.03`, deuda1 `0.00`; Vto1 `168581.94`, Vto2 `168897.19`, Vto3 `169212.43`.
  - B saldo a favor: `B000800451748`, afiliado `RAFANIELLO BORDIN, FRANCISCO ALAN`, contra `2589`, creditos `100338.00`, deuda1 `35148.43`; Vto1 `0.00`, Vto2 `0.00`, Vto3 `0.00`.
  - A saldo en contra: `A000800007581`, afiliado `GORDILLO, AMALIA LILIA`, contra `4003`, creditos `0.00`, deuda1 `3289213.04`; Vto1 `3836562.04`, Vto2 `3843566.13`, Vto3 `3850570.20`.
  - B saldo en contra: `B000800450346`, afiliado `CONTRERAS, HORTENSIA ADELINA`, contra `9852`, creditos `0.00`, deuda1 `321104.60`; Vto1 `482681.60`, Vto2 `483579.91`, Vto3 `484478.21`.

## RM26993 - punto funcional pendiente sobre barras de pago
- Cristian consulto por que se ocultaron barras de pago. Respuesta dada: se aplico por arrastre del criterio usado en Montegrande/SMC: si el Vto1 exigible queda en `0`, no dejar una barra activa por un importe que ya no corresponde pagar.
- Cambio conceptual aplicado en varios objetos: antes la barra dependia de `impor1 > 0`; ahora depende de `impor1 + saldo_anterior1 > 0`.
- Se aclaro que es un punto funcional discutible: si el criterio correcto es mostrar siempre la barra original aunque el vencimiento neto sea cero, conviene revertir solamente la condicion de barras y dejar intactos vencimientos, subtotal y total factura.
- Recomendacion vigente: confirmar regla con Cristian/cliente antes de consolidar barras en Mutual, Premedic y SBSalud; posiblemente tambien revisar Montegrande y SMC por el mismo criterio.
