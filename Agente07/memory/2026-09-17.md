# Memoria durable - 2026-09-17

## RM29856 / Pato / Expedientes, valorizacion y copagos
- Usuario: Pato. Tema de trabajo: incidente RM29856, importacion de cabeceras/detalles de expedientes, valorizacion de honorarios, copagos y excepciones en bases `SMC_Prox_Ver` y `Ospecon_Prox_Ver`.
- En `SMC_Prox_Ver`, para prestador `52 - Sanatorio Modelo de Caseros`, el convenio relacionado identificado fue `10347 - SANATORIO MODELO CASEROS`.
- Diagnostico de valorizacion inicial: la prestacion `660002` tenia valor para convenio `10053` bajo concepto `Gastos`, pero no bajo concepto `Honorarios`; por eso el test de honorarios indicaba prestacion sin valor aunque existia valor en otro concepto.
- Carpeta del incidente usada: `/mnt/Agentes/pbossa/Incidentes/RM29856/ARchivos de prueba`.
- Archivos de detalle existentes revisados: `Consumos 201806 - 20000 lineas-SMC.txt`, `Consumos-reducido.txt`, `PR52-expe4829-consumos-1-registro.txt`, `PR52-expe4829-consumos-15mil_reg.txt`, `PR52-expe4829-consumos-reducido.txt`, `PR52-expe4829-consumos.txt`. Son archivos de consumos/detalle, no cabeceras.
- Se genero cabecera de 1 registro: `/mnt/Agentes/pbossa/Incidentes/RM29856/ARchivos de prueba/PR52-expe4829-cabecera-1-registro.txt`, tipo expediente `H`, periodo `20190801`, fecha/recepcion `20190806`, prestador `52`, comprobante `FC B0000100004829`, total `1759.51`, largo `217`.
- Se genero detalle multi-registro: `/mnt/Agentes/pbossa/Incidentes/RM29856/ARchivos de prueba/PR52-expe4829-detalle-15-registros-prestador-52.txt`, con 15 registros, prestador 52, total `19891.95`.
- Se genero cabecera compatible de 15 registros: `/mnt/Agentes/pbossa/Incidentes/RM29856/ARchivos de prueba/PR52-expe4829-cabecera-15-registros.txt`, tipo `H`, comprobante `FC B0000100004830`, total `19891.95`, largo `217`.
- Al importar cabecera con grupo `001`, el sistema bloqueo porque para prestador 52, periodo `2019-08-01`, grupo `1`, ya habia expedientes liquidados (`liqui_fecha`). Se genero alternativa con grupo libre `002`: `/mnt/Agentes/pbossa/Incidentes/RM29856/ARchivos de prueba/PR52-expe4829-cabecera-15-registros-grupo-002.txt`.
- Configuracion de cabeceras: menu `Liquidacion -> Cabeceras de expedientes`; tabla tecnica `homolog_formatos` con `enti_tipo = 'XX'`, `enti_codi = -1`, `archi = 1`.
- Tipos de expediente y tablas: cabecera general `expedientes`, tipos en `expe_tipos`. `H` Honorarios -> `expe_honorarios`; `O` Ordenes -> `expe_ordenes` y `expe_orden_items`; `S` Sanatorios -> `expe_internaciones` y `expe_inter_prestaciones`; `F` Farmacias -> `expe_farmacias`, `expe_farma_recetas`, `expe_farma_rece_items`; `D` Odontologia y `E` Odon. No Val -> `expe_odon`, `expe_odon_items`; `C` Capitas -> `expe_capitas`; `G` Globales -> `expe_globales` / `expe_gravado_global`; `A` Ajustes -> `expe_ajustes`; `N` Fijos (Hon) con cabecera en `expedientes` y sin tabla `expe_fijos` especifica detectada.
- Tablas transversales de expedientes: `expe_copagos`, `expe_errores`, `expe_diagnosticos`, `expe_pagos`, `expe_pagados`, `expe_auditoria`, `EXPE_ATTACH`, `expe_comentarios`.
- Se genero/actualizo Excel `/mnt/Agentes/pbossa/Incidentes/RM29856/PrestacionesSMC.xlsx` con hojas `PlanesActivos`, `Prestaciones` y luego `SIN_COBER_PMO`.
- `PrestacionesSMC.xlsx`: `PlanesActivos` contenia 40 planes relacionados al convenio del prestador 52 con afiliados activos. `Prestaciones` contenia 25 registros de prueba con condicion, valor de convenio, tipo/valor de copago y excepcion detectada. `SIN_COBER_PMO` agrego 78 prestaciones del plan `3 / SIN COBER`, nomenclador `2 - PRACTICAS PMO`, convenio `10347`, prestador 52; tienen valor en convenio y cobertura para el plan, pero no se encontro copago vigente ni por plan (`cober_copagos`) ni por prestador/plan (`cober_prestad_copagos`), por eso se marco `Sin copago vigente`.
- Planes asociados al convenio 10347 con afiliados activos detectados: prepaga 1: `BLANCO`, `CLASSIC1`, `MASTER1`, `SIN COBERT`; prepaga 2: `ASI 100`, `BLANCO 1`, `BLANCO 10`, `CLASSIC 1`, `CLASSIC 10`, `CLASSIC 2`, `CLASSIC 20`, `CLASSIC B`, `CLASSIC E`, `EVOLUTION`, `EXCLU-20`, `EXCLUSIV 2`, `MASTER 1`, `MASTER 10`, `MASTER 2`, `MASTER 20`, `MASTER E`, `R27428`, `SENIO-PLUS`, `SENIOR 1`, `SENIOR 10`, `SENIOR 2`, `SENIOR 20`, `SENIOR 3`, `SENIOR B`, `SENIOR C`, `SENIOR E`, `SIN COBERT`; prepaga 3: `ASI RENAS`, `SIN COBER`, `SUPERADOR`, `XXXX`; prepaga 4: `GRAF/SC`, `GRAFICOS`, `S1`, `S2`.
- Ejemplos de prestaciones con valor/copago bajo convenio 10347 y prestador 52: `1000` PSA-T valor `545.76`, copago `P`/`Pe 24000.00`, plan `BLANCO 1`; `1010` beta HCG cuantitativa valor `242.56`, copago `P`/`Pe 8.00`, plan `AMBULATORI`; `1035` Colesterol HDL valor `517.42`, copago `P`/`Pe 8.00`; `1040` Colesterol LDL valor `689.82`, copago `P`/`Pe 8.00`; `1070` Hemoglobina glicosilada valor `375.00`, copago `P`/`Pe 24000.00`.
- Excepciones RM29856 detectadas para prestador 52: no habia configuracion vigente en `COBER_PRESTAD_PAGO_COPA`; si habia casos por `prestad_exen_copa`, por afiliado (`afi_exen_copagos`) y por prestacion excluida de copago por orden (`prestac_copa_orden_exclu`). Ejemplos usados: excepcion prestador/plan en `ASI 100` con prestaciones `1000` y `1070`; excepcion afiliado en `ASI 100` con `1000` y `115101`; excepcion prestacion copa orden con `BLANCO`/`1000` y `ASI 100`/`661000`.
- En `Ospecon_Prox_Ver`, al importar detalle de expediente, una pantalla mostro `1 filas revisadas, 0 errores, 1 avisos`; no era error bloqueante. Para expediente `25`: tipo `H`, prestador `216 - BASA SAN JUAN S.A.`, prepaga `1`, regla de bono `N`. El aviso mas probable diagnosticado fue `Sin afiliado (contrato o integrante inexistente)`, porque la importacion estaba con opciones de credencial/documento desmarcadas y busca por campos `contra` posicion `154` largo `15`, `inte` posicion `169` largo `2`; si no existe en `afiliados` para prepaga 1, avisa y puede reemplazar por `00`. Para verlo exacto, tildar `Ver tambien avisos`.
