# Memoria curada diaria

- Fecha: 2026-07-23
- Usuario: Francisco Valentin Miano
- id_googlechat: users/102716984057791327521
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/r_u-8kaaaae

## Resumen de la actividad

### Tema 1: Saludo y carga de preferencias

- Solicitud o contexto: Francisco saludó al agente.
- Respuesta o acciones del agente: El agente ejecutó bootstrap operativo y cargó sus preferencias.
- Resultado: Respondió que las preferencias estaban cargadas y quedó disponible para trabajar.
- Estado: Informativo

### Tema 2: Archivos de prueba DDJJ y transferencias para SMC_Prox_Ver

- Solicitud o contexto: Francisco pidió generar archivos de importación de DDJJ y transferencias usando plantillas de su carpeta de trabajo, base `SMC_Prox_Ver`, prepaga activa con RNOS, período enero 2026 y cuatro casos de validación sobre derivaciones de aportes.
- Respuesta o acciones del agente: Se conectó al profile `SMC_Prox_Ver`, detectó más de una prepaga activa con RNOS y pidió confirmación. Francisco indicó usar prepaga `1`.
- Resultado: Se generaron `Importacion_DDJJ_Enero_2026.txt` y `Importacion_Transferencias_Enero_2026.txt` en `/mnt/Agentes/fvmiano/Prueba de aportes PROX VER`, con prepaga `1`, RNOS `002204` y cuatro casos iniciales: titular activo con CUIT empresa ajeno, dependiente activo con CUIT ajeno, derivación finalizada y derivación dada de baja. Se validó ausencia de placeholders, formato de líneas, BOM, finales de línea y fechas de enero 2026.
- Estado: Resuelto

### Tema 3: Registro de expresiones normales en preferencias

- Solicitud o contexto: Francisco pidió registrar en su archivo de preferencias el significado operativo de expresiones como `afiliado activo`, `titular de aportes`, `dependiente`, `derivación fuera de vigencia`, `derivación dada de baja` y `afiliado dado de baja`.
- Respuesta o acciones del agente: Se leyó y modificó `/mnt/Agentes/fvmiano/preferencias/preferencias.md`, agregando una sección `Expresiones normales`.
- Resultado: Quedó registrado que `afiliado activo` implica `baja_fecha` nula o futura en `afiliados`; `titular de aportes` implica `afi_deriv_aportes.tipo_aportante = 'T'`; `dependiente` implica `tipo_aportante = 'G'`; `derivación fuera de vigencia` implica `vigen_hasta` menor a la fecha actual o informada; y bajas implican `baja_fecha` no nula anterior a la fecha actual o informada. Se validó UTF-8, sin BOM y LF.
- Estado: Resuelto

### Tema 4: Corrección del caso 3 en archivos SMC

- Solicitud o contexto: Francisco detectó que el caso 3 generado inicialmente estaba mal porque el afiliado tenía `tipo_aportante = 'G'` y no `T`; luego pidió otro reemplazo porque el nuevo caso tenía una derivación activa con el mismo CUIT.
- Respuesta o acciones del agente: Se buscaron nuevos afiliados en `SMC_Prox_Ver` que cumplieran las condiciones de derivación finalizada, `tipo_aportante = 'T'`, `baja_fecha = NULL` y sin derivación activa con el mismo CUIT.
- Resultado: Primero se reemplazó el caso 3 por CUIL `20164052932` y CUIT `33637617449`, pero luego se volvió a reemplazar por CUIL `20164577628` y CUIT `30707376321`, con derivación finalizada `vigen_hasta = 2025-01-31`, `baja_fecha = NULL` y sin derivación activa con el mismo CUIT. Se actualizó el caso en ambos archivos y se validó formato, ausencia de valores anteriores, BOM y finales de línea.
- Estado: Resuelto

### Tema 5: Consulta de profiles de base de datos disponibles

- Solicitud o contexto: Francisco pidió listar los perfiles de base de datos disponibles.
- Respuesta o acciones del agente: Se leyó `DB_PROFILE.md`.
- Resultado: Se informó la lista de profiles registrados, incluyendo ambientes `Prod_Test` y `Prox_Ver` para SMC, Celer, SANAR, Activa, Consalud, Medimas, MutualRioIV, Quimicos, Ospicine, SOS, Premedic, UnoSalud, IgualdadSalud, SBSalud, Fiat, Ospecon y PensandoSalud, además de `Quimicos_Prod` y `SMC_PROD_TEST`.
- Estado: Resuelto

### Tema 6: Archivos de prueba DDJJ y transferencias para Celer_Prox_Ver

- Solicitud o contexto: Francisco pidió generar nuevos archivos con datos de `Celer_Prox_Ver`, usando los mismos casos originales y la corrección del caso de derivación finalizada como titular sin derivación activa con el mismo CUIT.
- Respuesta o acciones del agente: Se conectó a `Celer_Prox_Ver`, buscó los casos necesarios y reutilizó las plantillas.
- Resultado: Se generaron `Importacion_DDJJ_Celer_Enero_2026.txt` y `Importacion_Transferencias_Celer_Enero_2026.txt` en `/mnt/Agentes/fvmiano/Prueba de aportes CELER PROX VER`, con prepaga `1`, RNOS `118804` y cuatro casos: CUIL `20143879802` con CUIT `27253806260`, CUIL `27173453855` con CUIT `27253806260`, CUIL `20388547589` con CUIT `30707179046` y CUIL `20176474522` con CUIT `30540228945`. Se validó ausencia de placeholders, longitud de líneas, BOM y finales de línea.
- Estado: Resuelto

### Tema 7: Archivo DDJJ para afiliado dado de baja con aporte vigente en afi_aportes_pre

- Solicitud o contexto: Francisco pidió generar solo un archivo DDJJ con un afiliado dado de baja que tuviera registros en `afi_aportes_pre` sin baja, informando el mismo CUIT y CUIL del afiliado.
- Respuesta o acciones del agente: Se buscó un caso válido en `Celer_Prox_Ver` y se generó el archivo en la carpeta de pruebas de Celer.
- Resultado: Se generó `/mnt/Agentes/fvmiano/Prueba de aportes CELER PROX VER/Importacion_DDJJ_Celer_Afiliado_Baja_Aporte_Pre.txt` con prepaga `1`, RNOS `118804`, CUIL/CUIT `20283716350`, afiliado con `baja_fecha = 2025-09-01` y aporte vigente en `afi_aportes_pre` sin `baja_fecha`. Se validó ausencia de placeholders, 3 líneas de 230 caracteres, LF y sin BOM.
- Estado: Resuelto

