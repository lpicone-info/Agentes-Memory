# Memoria 2026-08-14

## Portal prestadores / Caso CP21
- Usuario humano: Facundo Farias.
- Portal de prestadores validado: `http://10.194.0.15/standard/portal_prestadores/prox_ver`.
- Base utilizada: `smc_prox_ver` / `SMC_Prox_Ver`.
- Credencial portal usada en la sesion: usuario `ffa_prest`, clave `ffa_prest`.
- Forma de ejecucion usada: automatizacion HTTP contra el portal, manteniendo sesion/cookies y posteando a los mismos endpoints de WebPOS; no se genero la transaccion directo por stored procedure. Las validaciones JavaScript no se ejecutan automaticamente como en navegador; se leen/replican las relevantes, y la validacion fuerte observada viene del backend/base.

## Transacciones realizadas
- `01C - Verifica Convenio` con prestador/demandante `52`, lugar `1`, afiliado entidad/prepaga `1`, contra `270219892`, inte `00`, prestacion `nomen=2`, `prestac=180109`, cantidad `1`: aceptada. Transaccion `6504874`, estado `A`, sin rechazo. Prestacion: `180109 - ECOGRAFIA OFTALMOLOGICA UNI O BILATERAL`.
- Analisis nivel 01C: `ffa_prest` tiene `zz_usuari.nivel = 0`; prestacion `2/180109` tiene `prestac_auto_niveles.nivel = 2`; `parametros.valida_nivel_webpos = S`. Sin embargo, en `01C` el item quedo con `nivel = NULL` y el flujo acepto porque no comparo nivel de usuario vs nivel de prestacion. Causa tecnica localizada: `pri_transac_webpos_total` llama a `pri_auto_prestac_items_remotas` sin pasar `@nivel`; luego `prs_valida_of_convenio_remoto` valida convenio/cobertura pero no nivel.
- `02C` con mismos datos inicialmente rechazo por ICD invalido/faltante: transaccion `6504876`, auto `2049635`, estado `R`, rechazo `704 - ICD invalido`.
- `02C` con ICD `I10` rechazo antes por demandante: transaccion `6504877`, auto `2049636`, estado `R`, rechazo persistido `15 - El prestador demandante solo puede ser de cartilla`; JSON mostro codigo `22` con mismo texto.
- `02C` usando demandante codigo `52` validado por buscador del portal: transaccion `6504878`, auto `2049637`, estado `P`, motivo `La autorizacion de esta prestacion depende de un nivel superior`; `auto_remotas.recha = 90`, `auto_prestac_items.nivel = 2`, item `P`, usuario nivel `0`. JSON trajo `recha = 34` pero en base quedo `90`.

## Archivo CasoCP21.xlsx
- Ubicacion indicada por el usuario: carpeta personal `ffarias\R28246\CP`, archivo `CasoCP21.xlsx`.
- Estructura usada: columnas de datos `TRAN_TIPO`, `ORI_PRESTAD`, `EFE_PRESTAD`, `EFE_LUGAR`, `PREPAGA`, `CONTRA`, `INTE`, `INGRE_ICD`, `NOMEN`, `PRESTAC`, `CANTI`; columnas de salida `RESULTADO` (M), `TRANSACCION` (N), `RECHAZO` (O; encabezado observado con espacio final `RECHAZO ` pero tratado como `RECHAZO`).
- Fila `ID=CP4` corresponde a `TRAN_TIPO=02A` (el usuario corrigio que era fila 5). Datos corregidos verificados: `ORI_PRESTAD=52`, `EFE_PRESTAD=52`, `EFE_LUGAR=1`, `PREPAGA=1`, `CONTRA=270219892`, `INTE=00`, `INGRE_ICD=i10`, `NOMEN=2`, `PRESTAC=180109`, `CANTI=1`.
- Primera ejecucion `CP4` / `02A`: transaccion `6504883`, `AUTO_REMOTAS.esta = R`, rechazo `12 - Requiere autorizacion`, item `R`, `auto = NULL`; no se actualizo XLSX porque el criterio inicial solo escribia OK para A/P.
- Segunda ejecucion `CP4` / `02A` tras cambio de criterio: transaccion `6504886`, `AUTO_REMOTAS.esta = R`, rechazo `12 - Requiere autorizacion`. Se actualizo `CasoCP21.xlsx` en fila `CP4`: `RESULTADO = NO OK`, `TRANSACCION = 6504886`, `RECHAZO = 12 - Requiere autorizacion`. Se valido que el XLSX quedo estructuralmente correcto.

## Criterio acordado para futuras ejecuciones CP21
- Si `AUTO_REMOTAS.esta` queda `A` o `P`: escribir `RESULTADO = OK` y `TRANSACCION = <nro>`.
- Si `AUTO_REMOTAS.esta` queda `R`: escribir `RESULTADO = NO OK`, `TRANSACCION = <nro>`, y `RECHAZO = <recha> - <denominacion>`.
- Si no hubo rechazo: dejar `RECHAZO` en null.
# Memoria 2026-08-14

## Portal prestadores / Caso CP21
- Usuario humano: Facundo Farias.
- Portal de prestadores validado: `http://10.194.0.15/standard/portal_prestadores/prox_ver`.
- Base utilizada: `smc_prox_ver` / `SMC_Prox_Ver`.
- Credencial portal usada en la sesion: usuario `ffa_prest`, clave `ffa_prest`.
- Forma de ejecucion usada: automatizacion HTTP contra el portal, manteniendo sesion/cookies y posteando a los mismos endpoints de WebPOS; no se genero la transaccion directo por stored procedure. Las validaciones JavaScript no se ejecutan automaticamente como en navegador; se leen/replican las relevantes, y la validacion fuerte observada viene del backend/base.

## Transacciones realizadas
- `01C - Verifica Convenio` con prestador/demandante `52`, lugar `1`, afiliado entidad/prepaga `1`, contra `270219892`, inte `00`, prestacion `nomen=2`, `prestac=180109`, cantidad `1`: aceptada. Transaccion `6504874`, estado `A`, sin rechazo. Prestacion: `180109 - ECOGRAFIA OFTALMOLOGICA UNI O BILATERAL`.
- Analisis nivel 01C: `ffa_prest` tiene `zz_usuari.nivel = 0`; prestacion `2/180109` tiene `prestac_auto_niveles.nivel = 2`; `parametros.valida_nivel_webpos = S`. Sin embargo, en `01C` el item quedo con `nivel = NULL` y el flujo acepto porque no comparo nivel de usuario vs nivel de prestacion. Causa tecnica localizada: `pri_transac_webpos_total` llama a `pri_auto_prestac_items_remotas` sin pasar `@nivel`; luego `prs_valida_of_convenio_remoto` valida convenio/cobertura pero no nivel.
- `02C` con mismos datos inicialmente rechazo por ICD invalido/faltante: transaccion `6504876`, auto `2049635`, estado `R`, rechazo `704 - ICD invalido`.
- `02C` con ICD `I10` rechazo antes por demandante: transaccion `6504877`, auto `2049636`, estado `R`, rechazo persistido `15 - El prestador demandante solo puede ser de cartilla`; JSON mostro codigo `22` con mismo texto.
- `02C` usando demandante codigo `52` validado por buscador del portal: transaccion `6504878`, auto `2049637`, estado `P`, motivo `La autorizacion de esta prestacion depende de un nivel superior`; `auto_remotas.recha = 90`, `auto_prestac_items.nivel = 2`, item `P`, usuario nivel `0`. JSON trajo `recha = 34` pero en base quedo `90`.

## Archivo CasoCP21.xlsx
- Ubicacion indicada por el usuario: carpeta personal `ffarias\R28246\CP`, archivo `CasoCP21.xlsx`.
- Estructura usada: columnas de datos `TRAN_TIPO`, `ORI_PRESTAD`, `EFE_PRESTAD`, `EFE_LUGAR`, `PREPAGA`, `CONTRA`, `INTE`, `INGRE_ICD`, `NOMEN`, `PRESTAC`, `CANTI`; columnas de salida `RESULTADO` (M), `TRANSACCION` (N), `RECHAZO` (O; encabezado observado con espacio final `RECHAZO ` pero tratado como `RECHAZO`).
- Fila `ID=CP4` corresponde a `TRAN_TIPO=02A` (el usuario corrigio que era fila 5). Datos corregidos verificados: `ORI_PRESTAD=52`, `EFE_PRESTAD=52`, `EFE_LUGAR=1`, `PREPAGA=1`, `CONTRA=270219892`, `INTE=00`, `INGRE_ICD=i10`, `NOMEN=2`, `PRESTAC=180109`, `CANTI=1`.
- Primera ejecucion `CP4` / `02A`: transaccion `6504883`, `AUTO_REMOTAS.esta = R`, rechazo `12 - Requiere autorizacion`, item `R`, `auto = NULL`; no se actualizo XLSX porque el criterio inicial solo escribia OK para A/P.
- Segunda ejecucion `CP4` / `02A` tras cambio de criterio: transaccion `6504886`, `AUTO_REMOTAS.esta = R`, rechazo `12 - Requiere autorizacion`. Se actualizo `CasoCP21.xlsx` en fila `CP4`: `RESULTADO = NO OK`, `TRANSACCION = 6504886`, `RECHAZO = 12 - Requiere autorizacion`. Se valido que el XLSX quedo estructuralmente correcto.

## Criterio acordado para futuras ejecuciones CP21
- Si `AUTO_REMOTAS.esta` queda `A` o `P`: escribir `RESULTADO = OK` y `TRANSACCION = <nro>`.
- Si `AUTO_REMOTAS.esta` queda `R`: escribir `RESULTADO = NO OK`, `TRANSACCION = <nro>`, y `RECHAZO = <recha> - <denominacion>`.
- Si no hubo rechazo: dejar `RECHAZO` en null.

## Analisis 01A / auto_remotas para Analia
- Usuario humano: Analia. Contexto: pidio actualizar repositorio `trunk`, usar `smc_prod_test` para BBDD y repositorio `Trunk` para codigo fuente.
- SVN trunk actualizado de revision `84329` a `84335` en `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk`.
- Transacciones analizadas en `smc_prod_test`: `01A` nro `6854584` y `6854585`.
- Diferencias en `dbo.auto_remotas`: `transac`, `orden_fecha`, `fecha`, `alta_fecha`, `usuario`, `aufree`, `origen_inter`, `motivo_egreso`, `token`, `docu_nro`. El resto de columnas no difiere; otras tablas con columna `transac` no tenian registros para esas transacciones.
- Valores clave: `6854584` tiene `usuario=1ruiztest`, `aufree=-1`, `docu_nro=NULL`, `origen_inter=NULL`, `motivo_egreso=NULL`, `token=espacios`; `6854585` tiene `usuario=SA`, `aufree=0`, `docu_nro=8818400`, `origen_inter=0`, `motivo_egreso=0`, `token=NULL`.
- Conclusion tecnica: `6854584` entra como `WEBPOS` porque `aufree=-1`; flujo `webpos/modelo/transaccion.php` -> `pri_transac_webpos_total` -> `pri_auto_remotas_webpos` con `@aufree=-1` y `@prm_busca_creden='N'`, por eso no completa `docu_nro`. `6854585` entra como `ACTIVIA` porque `aufree=0`; `pri_auto_remotas_webpos` no saltea la logica de documento, toma credencial como documento y termina pasando `@prm_docu_nro=8818400` a `pri_auto_remotas`.
- Referencias usadas: `backend/function/afn_OrigenTransacRemota.sql`, `backend/procedures/pri_transac_webpos_total.sql`, `backend/procedures/pri_auto_remotas_webpos.sql`, `backend/procedures/pri_auto_remotas.sql`.

## Pruebas farmacia ws_auto_remotas / WebPos / SoapUI
- Usuario humano: Analia. Requerimiento: indicar base y datos para probar transacciones farmacia del `ws_auto_remotas`, incluyendo orden, sets de datos y parametros.
- Base recomendada para pruebas farmacia: `SOS_Prod_Test`. Motivo: contiene transacciones aceptadas reales para `01F`, `02F` y `02J`, farmacias vigentes, productos vigentes y normas activas. No se recomendo `SMC_Prod_Test` porque tenia normas de farmacia pero casos recientes rechazados y no se encontraron consumos aceptados con items.
- Transacciones/operativa: `01F` simulacion de consumo (`simula=1` en SoapUI); `02F` registro de consumo (`simula<>1` y `fecha_venta` actual); `02J` consumo diferido, derivado automaticamente cuando entra `02F` con `fecha_venta < hoy`; `04A` anulacion mediante operacion `Anular` usando autorizacion devuelta por un `02F` o `02J` aceptado.
- Parametros de `SOS_Prod_Test`: `farma_normas` configurada para prepagas `1` y `5`; `rece_dias=30`; `presen_dias=60`; `renglo_canti=2` (maximo 2 productos por transaccion). Para `02J`, evitar fecha de venta de 15 dias o mas de antiguedad porque rechaza con `122`. Para evitar rechazo `1436`, informar al menos un producto por `mov*_registro`, `mov*_troquel` o `mov*_codigbarra`.
- Set 1 farmacia: prepaga `1`, grupo_familiar `9329`, afiliado `02`, credencial historica `0000000932902`, cuit_farmacia `30669414532`, manda_farma `7`, farmacia `1 - FARMAVIDA CENTRAL`, medico_matricula `12345`, medico_nombre `MEDICO PRUEBA`, producto 1 `registro=5487`, `troquel=2680121`, `codigbarra=7792175005081`, `cantidad=1`, `tipo_trat=N`; producto 2 `registro=22806`, `troquel=4568731`, `codigbarra=7795345010540`, `cantidad=1`, `tipo_trat=N`.
- Set 2 farmacia: prepaga `5`, grupo_familiar `20837`, afiliado `00`, credencial historica `0000002083700`, cuit_farmacia `27305298668`, manda_farma `1`, farmacia `135 - FARMACIA SAN ISIDRO`, medico_matricula `12345`, medico_nombre `MEDICO PRUEBA`, producto 1 `registro=26200`, `troquel=4562291`, `codigbarra=7798069050076`, `cantidad=1`, `tipo_trat=N`; producto 2 `registro=14729`, `troquel=2622542`, `codigbarra=7798069050182`, `cantidad=1`, `tipo_trat=N`.
- Fechas sugeridas al momento del analisis: para `01F`, `simula=1`, `fecha_venta=14/08/2026`, `fecha_prescripcion=14/08/2026`; para `02F`, `simula=0`, ambas fechas `14/08/2026`; para `02J`, `simula=0`, ambas fechas `13/08/2026`.
- Orden sugerido: ejecutar `01F`; ejecutar `02F`; ejecutar `04A` sobre la autorizacion devuelta por `02F`; ejecutar `02J` con fecha de venta de ayer.
- SoapUI: usar operacion `guardar` para `01F/02F/02J` y operacion `Anular` para `04A`. En WebPos no se encontro pantalla especifica `01F/02F/02J`; para portal/prestadores la prueba valida es la que invoque el mismo contrato SOAP/backend.
- Referencias usadas: `ws_auto_remotas/src/java/com/autorem/service/RegistrarConsumoFarmacia.java`, `backend/procedures/pri_auto_remotas_farma.sql`, `backend/procedures/pri_transac_remotas_farm.sql`.
