## 2026-09-16
- Usuario identificado en el contexto como Any; trabaja con análisis funcional/técnico de PowerBuilder/MS SQL sobre la base `SMC_PROX_VER` y la versión 25.08.001.
- Caso Redmine/funcionalidad: `R28243`, tabla `AUTO_PRESTAC_RECHA_ACEPTADO`. Se documentaron reglas de altas, bajas lógicas/reinserción y aplicación de rechazos en `AUTORIZACIONES` y `AUTO_PRESTAC_ITEMS`.
- Autorización analizada: `2049928`; se documentó que aceptó rechazos `4`, `1253` y `1254` para transacción `6565469`, y que la revalidación dejó el rechazo `10 - Cobertura dada de baja` en `AUTORIZACIONES`/`AUTO_PRESTAC_ITEMS`.
- Archivos generados en `/mnt/Agentes/acruiz/R28243/`: `R28243_funcionalidad_auto_prestac_recha_aceptado.md`, `R28243_autorizacion_2049928_rechazos_simple_y_tecnico.md` y `R28243_sets_datos_casos_validaciones.md`.
- El último documento analizó `casos validaciones.txt` y contiene 10 casos con 3 sets de datos por caso (entidad, contrato, integrante, plan, edad, demandante, prestador efector, prestación, cantidad y observaciones), usando `SMC_PROX_VER`; se indicó que no se modificaron datos de la base ni repositorios.

- Usuario identificado en el contexto conversacional como Juana para consultas de verificación funcional/técnica sobre trunk, bases de prueba y repositorios SVN.
- Validación R30342 SIRO: en trunk, módulo `Facturación/Procesos Masivos/Exportación de débitos`, base `MutualRioIV_Prod_Test`, archivo del repo `Testing/R30342/SIRO-det_12720260915.txt`. Resultado informado: respeta la estructura SIRO para `prepaga=1`, `ente_rec=27`, `enti_tipo='W'`, `enti_codi=27`; 11415 líneas, 1 cabecera, 11413 detalles, 1 trailer, registros útiles de 280 caracteres, CRLF, trailer con cantidad 11413 y total `381543058922` centavos coincidente. Trunk en SVN rev. `84677`.
- Validación R30342 MASTERCARD: en trunk, módulo `Facturación/Procesos Masivos/Exportación de débitos`, base `SMC_Prod_Test`, archivo del repo `Testing/R30342/DA168D.TXT`. Resultado informado: respeta la estructura de MASTERCARD para `prepaga=2`, `ente_rec=35`, `enti_tipo='W'`, `enti_codi=35`; layout con cabecera `archi=1` y detalle `archi=2`, sin trailer; 399 líneas, 1 cabecera, 398 detalles, registros de 127 caracteres, CRLF, comercio `14198811`, cabecera tipo `1` fecha `160926`, detalle tipo `2`, cuotas `001`, plan `999`, frecuencia `01`, cantidad 398 y total `7805324624` centavos coincidentes. Trunk en SVN rev. `84677`.
- Consulta PowerBuilder en base `montegrande_prox_ver`: en `uo_nv_factu.of_compro_imprime`, cuando `contra_forma = 'T'`, el objeto asignado a `ls_datastore` es `d_factura_montegrande`; referencia usada: `svn/mpariso-Infomedical-trunk/exe/Source Infomedical/uo_nv_factu.sru:2057`. La alternativa `d_factura_cmmg_pdf` existe pero está comentada.
