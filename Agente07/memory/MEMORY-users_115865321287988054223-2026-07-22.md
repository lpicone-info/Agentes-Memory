# Memoria curada diaria

- Fecha: 2026-07-22
- Usuario: Patricia de Lourdes Bossa
- id_googlechat: users/115865321287988054223
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/jrvbkkaaaae

## Resumen de la actividad

### Tema 1: Saludo inicial y carga de contexto

- Solicitud o contexto: La usuaria inició la conversación con un saludo.
- Respuesta o acciones del agente: El agente ejecutó el bootstrap operativo, cargó reglas, repositorios, perfiles de base de datos y preferencias disponibles para la usuaria.
- Resultado: El agente respondió que las preferencias estaban cargadas y quedó disponible para trabajar.
- Estado: Informativo

### Tema 2: Configuración de adjuntos obligatorios para precarga en Afilmed Web

- Solicitud o contexto: La usuaria preguntó dónde se configura la obligatoriedad de adjuntos para una precarga en Afilmed Web.
- Respuesta o acciones del agente: Se revisó el backend de Afilmed Web y procedimientos relacionados en el repositorio SVN `mpariso-Infomedical-trunk`.
- Resultado: Se indicó que la validación se realiza con `crs_valida_precarga_adjuntos.sql`, llamado desde `afilmed_web/backend/src/controllers/precarga/validateAdjuntos.js`. La configuración se consulta primero en `ADJUNTOS_TIPOS_MODULOS_SOLICITUDES` por `soli_moti`, `modulo = 1` y `obligatorio = 'S'`; si no hay configuración por motivo, usa `ADJUNTOS_TIPOS_MODULOS` como configuración general del módulo Precarga.
- Estado: Resuelto

### Tema 3: Menú PowerBuilder para tipos de adjuntos

- Solicitud o contexto: La usuaria preguntó cuál es el menú de PowerBuilder asociado a la pantalla de configuración.
- Respuesta o acciones del agente: Se buscaron referencias en menús y ventanas PowerBuilder.
- Resultado: Se informó que el menú es `Configuración -> Tipos de Adjuntos`, que abre `wp_adjuntos_tipos`. Desde esa pantalla, el botón `SubModulo` abre `wp_modulos_adjuntos`, donde se configura el módulo Precarga y la obligatoriedad por tipo de adjunto. Se citaron referencias en `m_afilia.srm`, `wp_adjuntos_tipos.srw` y `m_main.srm`.
- Estado: Resuelto

### Tema 4: Uso de la tabla AFI_HISTO_PAREN

- Solicitud o contexto: La usuaria preguntó para qué se usa `AFI_HISTO_PAREN`.
- Respuesta o acciones del agente: Se analizó la finalidad funcional de la tabla dentro del dominio de afiliados.
- Resultado: Se explicó que `AFI_HISTO_PAREN` guarda el histórico de parentesco por integrante del contrato, con vigencia desde y baja. Se aclaró que el maestro de parentescos es `PARENTESCOS`. Se enumeraron usos funcionales: determinar parentesco vigente a una fecha, reconstruir grupo familiar histórico, validar integrantes, calcular tarifas o cuotas, validar cobertura/autorizaciones y alimentar consultas o reportes.
- Estado: Resuelto

### Tema 5: Consulta de configuración efectiva de adjuntos para probar una precarga

- Solicitud o contexto: La usuaria aclaró que no quería consultar una precarga existente, sino saber qué adjuntos debe cargar antes de hacer una precarga de prueba para que la validación no la frene por faltantes.
- Respuesta o acciones del agente: Se derivó la consulta desde la misma lógica de `crs_valida_precarga_adjuntos`, partiendo de `tipoPrecarga` como `soli_moti`, `tipoAfi` y `modulo = 1`.
- Resultado: Se indicó que debe consultar los adjuntos obligatorios filtrando por `soli_moti`, `tipo_afi`, `modulo = 1`, `obligatorio = 'S'` y vigencia (`baja_fecha IS NULL`), priorizando la configuración por motivo en `ADJUNTOS_TIPOS_MODULOS_SOLICITUDES` y usando la configuración general de `ADJUNTOS_TIPOS_MODULOS` cuando no exista configuración específica por motivo.
- Estado: Resuelto
