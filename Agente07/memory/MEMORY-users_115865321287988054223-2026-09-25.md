# Memoria de conversación — Patricia de Lourdes Bossa

## Datos

- **Fecha:** 25/09/2026
- **Usuario:** Patricia de Lourdes Bossa
- **ID Google Chat:** `users/115865321287988054223`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 40
- **Inicio:** 09:34:45
- **Fin:** 17:50:36

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/jrvbkkaaaae`

## Archivos revisados

- `chat.history` (`bc336b1f-ccb6-4363-a625-f450e70fe772`)

---

## Transcripción completa

### [25/09/2026 09:34:45] Patricia de Lourdes Bossa

hola

---

### [25/09/2026 09:34:54] Agente

Hola, Pato. Te leo.

---

### [25/09/2026 09:35:50] Patricia de Lourdes Bossa

sobre la base Mutual_prod_test dame algunos prestadores que no tengan lugar de atencion cargados, y despues otros que  no tengan lugar de atecion propio pero si qeu tengan de circulo

---

### [25/09/2026 09:35:59] Agente

Pato, en `DB_PROFILE.md` no existe `Mutual_prod_test`. ¿Confirmás que querés consultar el perfil **`MutualRioIV_Prod_Test`**?

---

### [25/09/2026 09:37:31] Patricia de Lourdes Bossa

si

---

### [25/09/2026 09:37:43] Agente

Pato, confirmado `MutualRioIV_Prod_Test`. Voy a separar los resultados con vigencia actual en dos grupos: prestadores sin ningún lugar y prestadores sin lugares propios pero con lugares vinculados a un círculo.

---

### [25/09/2026 09:38:06] Agente

El segundo grupo sí tiene resultados. Para el primero, la consulta no devolvió prestadores vigentes sin lugares activos; voy a confirmar si realmente son cero y separar, si existen, prestadores dados de baja o con todos sus lugares dados de baja para no mezclarlos con datos utilizables.

---

### [25/09/2026 09:38:59] Agente

Pato, en `MutualRioIV_Prod_Test`:

### Prestadores vigentes sin lugares de atención

No existen prestadores vigentes sin lugares activos.

- Prestadores vigentes: `7.804`
- Vigentes sin lugar activo: `0`
- Prestadores que nunca tuvieron lugares cargados: `0`

Hay `398` prestadores dados de baja cuyos lugares también están dados de baja, pero no sirven como datos vigentes de prueba.

### Vigentes sin lugar propio, pero con lugar de círculo

Encontré `5.737`. Algunos ejemplos:

| Prestador | Tipo | Nombre | Círculo | Nombre del círculo | Lugar | Domicilio |
|---:|:---:|---|---:|---|---:|---|
| 261 | P | FOGLINO | 65 | INSTITUTO MEDICO RIO CUARTO SA | 1 | HIPOLITO IRIGOYEN 1020 |
| 279 | P | CASTAÑARES | 65 | INSTITUTO MEDICO RIO CUARTO SA | 1 | Hipolito Yrigoyen 1020 |
| 284 | P | MIRANDA | 59 | POLICLINICO PRIVADO SAN LUCAS | 3 | MITRE 930 |
| 284 | P | MIRANDA | 215 | INSTITUTO PRIVADO NEONATOLOGIA Y PEDIATRIA SA | 2 | AV. ITALIA 1384 |
| 308 | P | MENDIA | 16 | Colegio de Psicólogos de la Prov. de Córdoba | 1 | Pringles 345 |
| 311 | P | MAYOL OLAYA | 59 | POLICLINICO PRIVADO SAN LUCAS | 2 | MITRE 930 |
| 334 | P | ESPINOZA | 19 | COLEGIO MEDICO LABOULAYE | 1 | SAN MARTIN 66 |
| 352 | P | MARANZANO OTTOGALLI | 8 | ASOC. DE ANESTESIOLOGIA CBA | 1 | BV. GRAL. ROCA 949 |

Para esta clasificación consideré como lugar propio todo lugar vigente que no tenga una relación vigente en `prestad_lugares_circu`.

---

### [25/09/2026 09:44:02] Patricia de Lourdes Bossa

dame una query que me brinde datos de prestadores  y sus lugares de atencion, indicando si esos lugares son de atencion de circulo o no

---

### [25/09/2026 09:44:18] Agente

Pato, voy a armarla con una fila por prestador y lugar, un indicador `Sí/No` y, cuando corresponda, el código y nombre de los círculos asociados. La ejecuto primero en `MutualRioIV_Prod_Test` para comprobar que no tenga errores.

---

### [25/09/2026 09:44:53] Agente

Pato, esta query fue ejecutada en `MutualRioIV_Prod_Test` y no devolvió errores.

Devuelve una fila por prestador y lugar, indicando si el lugar está relacionado con algún círculo y cuáles son esos círculos.

```sql
USE MutualRioIV_Prod_Test
GO

DECLARE @prestad INT

-- NULL devuelve todos los prestadores
-- Informar un código para consultar solo uno
SET @prestad = NULL

SELECT TOP (1000)
       p.prestad,
       p.tipo AS tipo_prestador,
       p.ape_razon AS prestador,
       pl.lugar,
       pl.domi AS domicilio,
       pl.loca AS localidad,
       pl.lugar_tipo,
       CASE
           WHEN circulos.circulos IS NULL THEN 'NO'
           ELSE 'SI'
       END AS es_lugar_de_circulo,
       circulos.circulos
FROM prestadores p
INNER JOIN prestad_lugares pl
        ON pl.prestad = p.prestad
       AND pl.baja_fecha IS NULL
OUTER APPLY
(
    SELECT STUFF
    (
        (
            SELECT ', '
                   + CONVERT(VARCHAR(20), plc.prestad)
                   + ' - '
                   + ISNULL(pc.ape_razon, '')
            FROM prestad_lugares_circu plc
            INNER JOIN prestadores pc
                    ON pc.prestad = plc.prestad
                   AND pc.tipo = 'C'
                   AND pc.baja_fecha IS NULL
            WHERE plc.prestad2 = p.prestad
              AND plc.lugar = pl.lugar
              AND plc.baja_fecha IS NULL
            ORDER BY plc.prestad
            FOR XML PATH(''), TYPE
        ).value('.', 'VARCHAR(MAX)'),
        1,
        2,
        ''
    ) AS circulos
) circulos
WHERE p.baja_fecha IS NULL
  AND
  (
      @prestad IS NULL
      OR p.prestad = @prestad
  )
ORDER BY
    p.prestad,
    pl.lugar
GO
```

Para consultar un prestador específico:

```sql
SET @prestad = 261
```

`es_lugar_de_circulo = 'SI'` significa que el lugar tiene una relación vigente en `prestad_lugares_circu`. La columna `circulos` muestra el código y nombre de los círculos relacionados.

---

### [25/09/2026 09:52:20] Patricia de Lourdes Bossa

y si algun prestador tiene de los dos tipos de lugares me los muestra?

---

### [25/09/2026 09:52:32] Agente

Sí, Pato. La consulta devuelve **una fila por cada lugar**, por lo que muestra ambos tipos para un mismo prestador.

Por ejemplo, para:

```sql
SET @prestad = 258
```

obtendrías conceptualmente:

| Prestador | Lugar | Domicilio | Es lugar de círculo | Círculo |
|---:|---:|---|:---:|---|
| 258 | 1 | KOWALK 17 | SI | 65 - INSTITUTO MEDICO RIO CUARTO SA |
| 258 | 2 | PRINGLES 30 | NO | NULL |
| 258 | 3 | BUENOS AIRES 542 | NO | NULL |

Así podés identificar prestadores que tienen simultáneamente lugares propios y lugares relacionados con círculos.

---

### [25/09/2026 12:18:30] Patricia de Lourdes Bossa

con que  valor valoriza  en esta liqui de prestaciones? si consulto el convenio veo otro valor. Decime si hay algo erroneo:

---

### [25/09/2026 12:18:48] Agente

Pato, voy a revisar el expediente `31367` en `MutualRioIV_Prod_Test`: identificaré el convenio realmente tomado por el ítem `180103`, la vigencia aplicada al `25/09/2026` y si el importe `70.965,71` proviene del convenio, nomenclador, presupuesto u otra regla de valorización.

---

### [25/09/2026 12:19:35] Agente

El renglón que muestra `70.965,71` todavía no está grabado: en base el expediente tiene solo las prestaciones `180301` y `180302`. Por eso voy a reconstruir la valorización con los datos visibles de pantalla —círculo `7203`, lugar `1`, plan `239VIPLUS`, categoría `1`, nomenclador `2`, prestación `180103` y fecha `25/09/2026`— y compararla con todas las vigencias del convenio.

---

### [25/09/2026 12:20:48] Agente

Ya encontré la composición exacta: el sistema no está tomando un único importe directo. Está calculando honorarios y gastos por unidades del nomenclador y sumándolos. Verifico además que el convenio seleccionado corresponde al círculo del expediente y no al efector `3340`.

---

### [25/09/2026 12:22:34] Agente

Pato, el valor **$70.965,71 está correctamente calculado**, pero detecté otro error en la carga.

### Cómo obtiene el valor convenido

El sistema toma el convenio `10024` de **MATER VITA SRL**, porque:

- Prestador del expediente: `7203 - MATER VITA SRL`, tipo círculo.
- Plan: `239VIPLUS`.
- Cartilla: `01`.
- Lugar: `1`.
- Categoría: `1 - ÚNICA`.
- Fecha de realización: `25/09/2026`.
- Prestación: nomenclador `2`, código `180103`.

La prestación tiene dos componentes:

| Componente | Unidades | Valor unitario vigente | Resultado |
|---|---:|---:|---:|
| Honorarios | 33 | $1.306,91 | $43.128,03 |
| Gastos | 71 | $392,08 | $27.837,68 |
| **Total convenido** | | | **$70.965,71** |

Por eso, si en la consulta del convenio ves `$1.306,91` o `$392,08`, estás viendo los **valores unitarios**, no el total calculado de la prestación.

### Lo que sí está erróneo

El efector cargado es:

- `3340 - MARTIN CABELLO`
- Su lugar vigente es `1 - Ituzangó 1070 / San Martín 1339`.

Pero la pantalla sigue mostrando:

- `1 - PRINGLES 30`, que pertenece al círculo `7203 - MATER VITA SRL`.

Además, el efector `3340` no tiene relación vigente de categoría ni de lugar con el círculo `7203`.

Según el RM30352, al cargar manualmente un efector no vinculado, el combo **Lugar** debería refrescarse con sus lugares propios. Eso no está ocurriendo.

**Conclusión:** la valorización de `$70.965,71` es correcta y corresponde al convenio del círculo. El defecto está en el combo **Lugar**, que conserva el domicilio del círculo en vez de mostrar el lugar del efector `3340`. Como ambos tienen lugar número `1`, la valorización encuentra igualmente el convenio `10024`, ocultando parcialmente el problema.

---

### [25/09/2026 12:26:00] Patricia de Lourdes Bossa

en este caso no es una carga manual , viene de una autorizacion

---

### [25/09/2026 12:26:11] Agente

Pato, tomo la corrección. Entonces debo validar el circuito de autorización: cuál es la autorización del afiliado `200068/02`, qué efector, lugar y categoría trae, y si la valorización está respetando esos datos. La identifico en base y ajusto el diagnóstico.

---

### [25/09/2026 12:26:59] Agente

Pato, tomo la corrección: si el ítem proviene de una autorización, tengo que seguir la trazabilidad **autorización → efector/lugar → convenio → valor**, no el circuito de carga manual. Recupero el contexto obligatorio y verifico esa asociación exacta.

---

### [25/09/2026 12:28:22] Agente

La base confirma un dato importante: el valor mostrado es correcto para el convenio `10024`, pero la fila que está en edición todavía no quedó asociada a ninguna autorización en el expediente. Estoy revisando si el programa pierde/limpia el número de autorización al trasladar sus datos, porque en la captura el campo **Autorización** está vacío.

---

### [25/09/2026 12:30:58] Agente

Correcto, Pato. Aunque la prestación venga de una autorización, el sistema **no copia el valor convenido de la autorización**.

El proceso:

1. Recupera de la autorización el nomenclador, prestación y cantidad.
2. Luego vuelve a valorizar mediante `val.of_convenio`.
3. Para buscar el convenio utiliza:
   - Prestador del expediente: `7203 - MATER VITA SRL`
   - Lugar: `1`
   - Categoría: `1`
   - Plan: `239VIPLUS`
   - Fecha: `25/09/2026`

Por eso selecciona el convenio `10024` y calcula:

```text
Honorarios: 33 × 1.306,91 = 43.128,03
Gastos:     71 ×   392,08 = 27.837,68
Total:                        70.965,71
```

El código confirma que de la autorización copia la prestación, pero no `conve` ni `conve_valor`: [wp_expe_orde.srw](</home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/exe/Source Infomedical/wp_expe_orde.srw:1045>). Después recalcula usando `exp.prestad`: [wp_expe_orde.srw](</home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/exe/Source Infomedical/wp_expe_orde.srw:5169>).

**Lo anómalo es la trazabilidad de la autorización:** en la captura el campo **Autorización** está vacío y el ítem todavía no está grabado. Además, en la base no existe una autorización para el afiliado `200068/02` con la prestación `180103` y tampoco existe una autorización cuyo efector sea `3340`.

Por lo tanto:

- El valor `70.965,71` está correctamente calculado según el convenio del expediente.
- La carga pudo haberse iniciado desde una autorización, pero actualmente perdió o no conserva su número.
- Para determinar por qué se perdió esa asociación necesito el número de autorización que ingresaste.

---

### [25/09/2026 12:42:06] Patricia de Lourdes Bossa

hola

---

### [25/09/2026 12:42:17] Agente

Hola, Pato. Completo la recuperación obligatoria posterior a la compactación y quedo lista para continuar.

---

### [25/09/2026 12:42:43] Agente

Hola, Pato. Te leo.

---

### [25/09/2026 12:44:50] Patricia de Lourdes Bossa

ayer te pedi una query de sus prestadores, conveios y sus prestaciones y sus vaslores.  genera la misma query pero donde se vea solo la ultima vigente y agregale  la valorizacion completa, tal como me explciaste arriba cone ste ejemplo, es decir en la query que se vea el total valorizado que luego se muestra en las liquidaciones , quiero ver eso mismo

---

### [25/09/2026 12:45:00] Agente

Pato, voy a recuperar la consulta anterior y ajustar el cálculo para que muestre únicamente la última vigencia aplicable y el total valorizado que usa la liquidación. Después la ejecutaré en `MutualRioIV_Prod_Test` para comprobar resultados y sintaxis.

---

### [25/09/2026 12:47:00] Agente

La valorización se compone por concepto, no por el importe unitario aislado. Estoy armando la salida con estas columnas: unidades, valor unitario vigente, subtotal por concepto y total de la prestación; para el ejemplo deberá resultar `43.128,03 + 27.837,68 = 70.965,71`.

---

### [25/09/2026 12:48:38] Agente

Pato, la query fue validada en `MutualRioIV_Prod_Test` con el convenio `10024`. Muestra únicamente la última vigencia aplicable y calcula el subtotal por concepto y el total valorizado de la prestación.

```sql
USE MutualRioIV_Prod_Test
GO

DECLARE @conve INT
DECLARE @fecha SMALLDATETIME
DECLARE @prestac CHAR(10)

SELECT @conve = 10024
SELECT @fecha = CONVERT(DATE, GETDATE())

-- NULL: devuelve todas las prestaciones del convenio
-- Informar un código para consultar solamente una prestación
SELECT @prestac = NULL

SELECT TOP (1000)
       x.prestad,
       x.prestador,
       x.tipo_prestador,
       x.lugar,
       x.lugar_atencion,
       x.carti,
       x.conve,
       x.convenio,
       x.ambu_inter,
       x.nomen,
       x.prestac,
       x.prestacion,
       x.concep,
       x.concepto,
       x.valor_vigente_desde,
       x.unidad,
       x.cantidad_unidades,
       x.valor_fijo,
       x.valor_unitario_convenio,
       x.valor_unidad_nomenclador,
       x.porcentaje_mas,
       x.porcentaje_menos,
       x.factor_multiplicador,
       x.plus_importe,
       x.valor_componente,
       SUM(ISNULL(x.valor_componente, 0)) OVER
       (
           PARTITION BY
               x.prestad,
               x.lugar,
               x.carti,
               x.conve,
               x.nomen,
               x.prestac
       ) AS total_valorizado
FROM
(
    SELECT
        pr.prestad,
        TRIM(pr.ape_razon) AS prestador,
        pr.tipo AS tipo_prestador,
        cc.lugar,
        TRIM(ISNULL(pl.nombre, pl.domi)) AS lugar_atencion,
        cc.carti,
        c.conve,
        TRIM(c.deno) AS convenio,
        c.ambu_inter,
        cp.nomen,
        TRIM(cp.prestac) AS prestac,
        TRIM(pe.exten_deno) AS prestacion,
        cp.concep,
        TRIM(co.deno) AS concepto,
        cpv.vigen_desde AS valor_vigente_desde,
        ar.uni AS unidad,
        ar.canti AS cantidad_unidades,
        cpv.valor AS valor_fijo,
        cpv.valor_uni AS valor_unitario_convenio,
        uv.valor AS valor_unidad_nomenclador,
        cpv.porce_mas AS porcentaje_mas,
        cpv.porce_menos AS porcentaje_menos,
        cpv.valor_multi AS factor_multiplicador,
        cpv.vier_plus AS plus_importe,
        calculo.valor_componente
    FROM carti_convenios cc
    INNER JOIN prestadores pr
            ON pr.prestad = cc.prestad
           AND pr.baja_fecha IS NULL
    LEFT JOIN prestad_lugares pl
           ON pl.prestad = cc.prestad
          AND pl.lugar = cc.lugar
          AND pl.baja_fecha IS NULL
    INNER JOIN convenios c
            ON c.conve = cc.conve
           AND c.baja_fecha IS NULL
    INNER JOIN conve_prestaciones cp
            ON cp.conve = c.conve
           AND cp.baja_fecha IS NULL
           AND
           (
               cp.fecha_hasta IS NULL
               OR cp.fecha_hasta >= @fecha
           )
    INNER JOIN prestaciones pe
            ON pe.nomen = cp.nomen
           AND pe.prestac = cp.prestac
           AND pe.baja_fecha IS NULL
    INNER JOIN conceptos co
            ON co.concep = cp.concep

    OUTER APPLY
    (
        SELECT TOP (1)
               v.vigen_desde,
               v.porce_mas,
               v.porce_menos,
               v.valor_multi,
               v.valor,
               v.valor_uni,
               v.vier_plus
        FROM conve_prestac_valores v
        WHERE v.conve_prestac = cp.conve_prestac
          AND v.vigen_desde <= @fecha
          AND v.baja_fecha IS NULL
        ORDER BY v.vigen_desde DESC
    ) cpv

    OUTER APPLY
    (
        SELECT TOP (1)
               a.uni,
               a.canti
        FROM aranceles a
        WHERE a.nomen = cp.nomen
          AND a.prestac = cp.prestac
          AND a.concep = cp.concep
          AND a.vigen_desde <= @fecha
          AND a.baja_fecha IS NULL
        ORDER BY a.vigen_desde DESC
    ) ar

    OUTER APPLY
    (
        SELECT TOP (1)
               u.valor
        FROM uni_valores u
        WHERE u.uni = ar.uni
          AND u.vigen_desde <= @fecha
          AND u.baja_fecha IS NULL
        ORDER BY u.vigen_desde DESC
    ) uv

    OUTER APPLY
    (
        SELECT
            CASE
                WHEN cpv.valor IS NOT NULL
                 AND cpv.valor > 0
                    THEN
                        CASE
                            WHEN cpv.valor = 0.01 THEN 0
                            ELSE cpv.valor
                        END
                WHEN cpv.valor_uni IS NOT NULL
                 AND cpv.valor_uni > 0
                    THEN CONVERT
                         (
                             DECIMAL(18,2),
                             cpv.valor_uni * ar.canti
                         )
                WHEN ar.uni = 'pe'
                    THEN CONVERT(DECIMAL(18,4), ar.canti)
                WHEN uv.valor IS NOT NULL
                    THEN CONVERT
                         (
                             DECIMAL(18,4),
                             uv.valor * ar.canti
                         )
            END AS valor_base
    ) base

    OUTER APPLY
    (
        SELECT
            CONVERT
            (
                DECIMAL(18,2),
                CASE
                    WHEN cpv.valor IS NOT NULL
                     AND cpv.valor > 0
                        THEN base.valor_base
                    WHEN cpv.valor_uni IS NOT NULL
                     AND cpv.valor_uni > 0
                        THEN base.valor_base
                    WHEN cpv.porce_mas IS NOT NULL
                     AND cpv.porce_mas > 0
                        THEN base.valor_base
                           * (1 + cpv.porce_mas / 100.00)
                    WHEN cpv.porce_menos IS NOT NULL
                     AND cpv.porce_menos > 0
                        THEN base.valor_base
                           * (1 - cpv.porce_menos / 100.00)
                    ELSE base.valor_base * cpv.valor_multi
                END
                + ISNULL(cpv.vier_plus, 0)
            ) AS valor_componente
    ) calculo

    WHERE cc.conve = @conve
      AND cc.baja_fecha IS NULL
      AND cpv.vigen_desde IS NOT NULL
) x
WHERE
(
    @prestac IS NULL
    OR x.prestac = TRIM(@prestac)
)
ORDER BY
    x.prestad,
    x.lugar,
    x.carti,
    x.nomen,
    x.prestac,
    x.concep
GO
```

Para comprobar el ejemplo:

```sql
SELECT @conve = 10024
SELECT @prestac = '180103'
```

Resultado validado:

```text
Honorarios: 33 × 1.306,91 = 43.128,03
Gastos:     71 ×   392,08 = 27.837,68
Total valorizado:            70.965,71
```

`valor_componente` muestra el subtotal de cada concepto y `total_valorizado` muestra el importe que se presenta como convenido en la liquidación estándar.

---

### [25/09/2026 13:05:52] Patricia de Lourdes Bossa

me da este error: Msg 102, Level 15, State 1, Line 153
Incorrect syntax near 'sql'.
Msg 102, Level 15, State 1, Line 203
Incorrect syntax near 'calculo'.

---

### [25/09/2026 13:06:15] Agente

Pato, el texto `Incorrect syntax near 'sql'` indica que se pegó en SQL Server una marca de formato Markdown (`sql` o las tres comillas invertidas). Además, voy a simplificar la consulta para eliminar el bloque que generó el segundo error y volver a ejecutarla completa.

---

### [25/09/2026 13:07:21] Agente

Pato, el error `Incorrect syntax near 'sql'` indica que se copió la palabra `sql` de la marca de formato. También simplifiqué el cálculo para evitar el error cerca de `calculo`.

Copiá únicamente el contenido interior del siguiente bloque. Fue ejecutado correctamente en `MutualRioIV_Prod_Test`.

```
USE MutualRioIV_Prod_Test
GO

WITH Parametros AS
(
    SELECT CONVERT(INT, 10024) AS conve,
           CONVERT(DATE, GETDATE()) AS fecha,
           CONVERT(VARCHAR(10), '180103') AS prestac
),
Datos AS
(
    SELECT pr.prestad,
           TRIM(pr.ape_razon) AS prestador,
           pr.tipo AS tipo_prestador,
           cc.lugar,
           TRIM(ISNULL(pl.nombre, pl.domi)) AS lugar_atencion,
           cc.carti,
           c.conve,
           TRIM(c.deno) AS convenio,
           c.ambu_inter,
           cp.nomen,
           TRIM(cp.prestac) AS prestac,
           TRIM(pe.exten_deno) AS prestacion,
           cp.concep,
           TRIM(co.deno) AS concepto,
           cpv.vigen_desde AS valor_vigente_desde,
           ar.uni AS unidad,
           ar.canti AS cantidad_unidades,
           cpv.valor AS valor_fijo,
           cpv.valor_uni AS valor_unitario_convenio,
           uv.valor AS valor_unidad_nomenclador,
           cpv.porce_mas AS porcentaje_mas,
           cpv.porce_menos AS porcentaje_menos,
           cpv.valor_multi AS factor_multiplicador,
           cpv.vier_plus AS plus_importe,
           CASE
               WHEN cpv.valor IS NOT NULL
                AND cpv.valor > 0
                   THEN
                       CASE
                           WHEN cpv.valor = 0.01 THEN 0
                           ELSE cpv.valor
                       END
               WHEN cpv.valor_uni IS NOT NULL
                AND cpv.valor_uni > 0
                   THEN CONVERT
                        (
                            DECIMAL(18,4),
                            cpv.valor_uni * ar.canti
                        )
               WHEN ar.uni = 'pe'
                   THEN CONVERT(DECIMAL(18,4), ar.canti)
               WHEN uv.valor IS NOT NULL
                   THEN CONVERT
                        (
                            DECIMAL(18,4),
                            uv.valor * ar.canti
                        )
           END AS valor_base
    FROM Parametros par
    INNER JOIN carti_convenios cc
            ON cc.conve = par.conve
           AND cc.baja_fecha IS NULL
    INNER JOIN prestadores pr
            ON pr.prestad = cc.prestad
           AND pr.baja_fecha IS NULL
    LEFT JOIN prestad_lugares pl
           ON pl.prestad = cc.prestad
          AND pl.lugar = cc.lugar
          AND pl.baja_fecha IS NULL
    INNER JOIN convenios c
            ON c.conve = cc.conve
           AND c.baja_fecha IS NULL
    INNER JOIN conve_prestaciones cp
            ON cp.conve = c.conve
           AND cp.baja_fecha IS NULL
           AND
           (
               cp.fecha_hasta IS NULL
               OR cp.fecha_hasta >= par.fecha
           )
    INNER JOIN prestaciones pe
            ON pe.nomen = cp.nomen
           AND pe.prestac = cp.prestac
           AND pe.baja_fecha IS NULL
    INNER JOIN conceptos co
            ON co.concep = cp.concep
    OUTER APPLY
    (
        SELECT TOP (1)
               v.vigen_desde,
               v.porce_mas,
               v.porce_menos,
               v.valor_multi,
               v.valor,
               v.valor_uni,
               v.vier_plus
        FROM conve_prestac_valores v
        WHERE v.conve_prestac = cp.conve_prestac
          AND v.vigen_desde <= par.fecha
          AND v.baja_fecha IS NULL
        ORDER BY v.vigen_desde DESC
    ) cpv
    OUTER APPLY
    (
        SELECT TOP (1)
               a.uni,
               a.canti
        FROM aranceles a
        WHERE a.nomen = cp.nomen
          AND a.prestac = cp.prestac
          AND a.concep = cp.concep
          AND a.vigen_desde <= par.fecha
          AND a.baja_fecha IS NULL
        ORDER BY a.vigen_desde DESC
    ) ar
    OUTER APPLY
    (
        SELECT TOP (1)
               u.valor
        FROM uni_valores u
        WHERE u.uni = ar.uni
          AND u.vigen_desde <= par.fecha
          AND u.baja_fecha IS NULL
        ORDER BY u.vigen_desde DESC
    ) uv
    WHERE cpv.vigen_desde IS NOT NULL
      AND
      (
          par.prestac IS NULL
          OR cp.prestac = par.prestac
      )
),
Componentes AS
(
    SELECT Datos.*,
           CONVERT
           (
               DECIMAL(18,2),
               CASE
                   WHEN valor_fijo IS NOT NULL
                    AND valor_fijo > 0
                       THEN valor_base
                   WHEN valor_unitario_convenio IS NOT NULL
                    AND valor_unitario_convenio > 0
                       THEN valor_base
                   WHEN porcentaje_mas IS NOT NULL
                    AND porcentaje_mas > 0
                       THEN valor_base
                          * (1 + porcentaje_mas / 100.00)
                   WHEN porcentaje_menos IS NOT NULL
                    AND porcentaje_menos > 0
                       THEN valor_base
                          * (1 - porcentaje_menos / 100.00)
                   ELSE valor_base * factor_multiplicador
               END
               + ISNULL(plus_importe, 0)
           ) AS valor_componente
    FROM Datos
)
SELECT TOP (1000)
       prestad,
       prestador,
       tipo_prestador,
       lugar,
       lugar_atencion,
       carti,
       conve,
       convenio,
       ambu_inter,
       nomen,
       prestac,
       prestacion,
       concep,
       concepto,
       valor_vigente_desde,
       unidad,
       cantidad_unidades,
       valor_fijo,
       valor_unitario_convenio,
       valor_unidad_nomenclador,
       porcentaje_mas,
       porcentaje_menos,
       factor_multiplicador,
       plus_importe,
       valor_componente,
       SUM(ISNULL(valor_componente, 0)) OVER
       (
           PARTITION BY
               prestad,
               lugar,
               carti,
               conve,
               nomen,
               prestac
       ) AS total_valorizado
FROM Componentes
ORDER BY
    prestad,
    lugar,
    carti,
    nomen,
    prestac,
    concep
GO
```

Los parámetros se modifican al comienzo:

```text
10024   → convenio
GETDATE() → fecha de valorización
180103  → prestación
```

Para devolver todas las prestaciones, reemplazá:

```text
CONVERT(VARCHAR(10), '180103') AS prestac
```

por:

```text
CONVERT(VARCHAR(10), NULL) AS prestac
```

Validación obtenida:

```text
Honorarios: 43.128,03
Gastos:     27.837,68
Total:      70.965,71
```

---

### [25/09/2026 13:20:48] Patricia de Lourdes Bossa

dejame  la query en un archivo en mi carpeta dentro de la carpeta del incidente RM30352: PrestacionesVlorizadas.txt, pero previamente agregale la columna de categoria del convenio

---

### [25/09/2026 13:20:59] Agente

Pato, voy a agregar el código y la descripción de la categoría del convenio, validar nuevamente la consulta en `MutualRioIV_Prod_Test` y guardar el archivo con el nombre exacto `PrestacionesVlorizadas.txt` dentro de `/mnt/Agentes/pbossa/Incidentes/RM30352`.

---

### [25/09/2026 13:21:54] Agente

La categoría del convenio `10024` quedó validada como `1 - UNICA`. La agregaré en las columnas `categoria_codigo` y `categoria`, manteniendo el cálculo validado del total valorizado.

---

### [25/09/2026 13:23:42] Agente

Pato, archivo generado y validado correctamente:

[PrestacionesVlorizadas.txt](</mnt/Agentes/pbossa/Incidentes/RM30352/PrestacionesVlorizadas.txt>)

Se agregaron:

- `categoria_codigo`
- `categoria`

La consulta fue ejecutada en `MutualRioIV_Prod_Test` y devolvió 1000 registros sin errores.

---

### [25/09/2026 17:49:46] Patricia de Lourdes Bossa

dame un prestador  de odontologia que no este asociado a circulo de la base MutualRioIV_Prod_Test y

---

### [25/09/2026 17:49:56] Agente

Pato, voy a buscar un prestador vigente de odontología que no tenga ninguna asociación vigente con círculos en `MutualRioIV_Prod_Test`.

---

### [25/09/2026 17:50:36] Agente

Pato, podés usar:

- **Prestador:** `205 - CROMID SA`
- **Tipo:** `I - Institución`
- **Servicio:** `9 - Odontología`
- **Especialidad:** `84 - Odontología General`
- **Lugar:** `1 - Constitución 840`
- **Estado:** vigente
- **Asociación con círculo:** ninguna vigente, ni por categoría ni por lugar de atención.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
