# Memoria 2026-09-01

## Liquidaciones - Copagos Prestaciones / Sanatorio / Odontologia
- Usuario: Pato.
- Contexto: Se reviso version `mpariso-V25.08.001` para determinar si el copago se calcula durante la carga en liquidacion de Prestaciones, comparando con Odontologia.
- Hallazgo corregido/importante: En Liquidacion de Prestaciones si se calcula/consulta copago durante la carga, pero principalmente para visualizacion y validacion del valor cargado. La persistencia definitiva en `expe_copagos` ocurre al `Cerrar Carga` mediante `rut.of_expe_topes_copagos(...)` / `pru_expe_copagos`.
- Evidencia funcional: El combo de `de_auxi_carga.srd` incluye opciones `Cop.prestad` y `Conv - Cop.Pr`. Al cambiarlo, `wp_expe_orde.srw` ejecuta `wf_auxi(0, False)`.
- Logica de carga: `wf_auxi` obtiene copago con `rut.of_get_cober_prestad(...)`; si no hay valor llama a `prs_copago`. Luego setea `dw_item.object.copago` y muestra copago solo o `valor convenio - copago`, segun el auxiliar seleccionado.
- Tipo de copago mostrado: copago al prestador aplicado a la prestacion, no cualquier tipo de copago. Solo se usa visualmente si `pago_copa = 'P'`.
- Calculo porcentual: Si el copago es porcentual sobre convenio (`PC`), calcula el importe sobre `l_cn.valor`.
- Prioridad evaluada para visualizacion en carga:
  1. Prestador / plan / prestacion mediante `prs_cober_prestad_copagos`.
  2. Si no hay, `prs_copago`, cuyo encabezado prioriza autorizacion con valor de copago, convenio y plan.
  3. Tambien revisa copago por orden con `wf_copa_orden_reemp`; si el copago por orden es de tipo reemplaza, anula el copago de prestacion para ese calculo auxiliar.
- Odontologia: En `V25.08.001`, Odontologia comun y no valorizada calculan/validan el copago normal durante carga/pre-guardado; al cierre pueden intervenir topes.
- Sanatorio ambulatorio / Prestaciones: La respuesta inicial habia dicho que era diferido; queda matizado/corregido para Prestaciones: durante la carga se calcula/visualiza y valida; al cierre se persiste definitivamente. Para Sanatorio ambulatorio queda pendiente revisar con el mismo nivel de evidencia si el usuario lo vuelve a pedir.

## Juana - Consultas Presmed / Montegrande 2026-09-01
- Usuario identificado en chat: Juana.
- Consulta DB `Montegrande_Prod_Test`, rama `trunk`: se buscaron 2 contratos activos de prepaga `1 - CENTRO MEDICO MONTE GRANDE`, excluyendo contrato `1000102`, con `afi_clase.clase_afi = 'D'` y comprobantes tipo B exentos. Resultado informado: contratos `1001353` (`FC B002100081144`, periodo 09/2026, exento 10000.00, gravado/IVA 0) y `1000557` (`FC B002100081140`, periodo 09/2026, exento 15000.00, gravado/IVA 0). Trunk estaba en SVN rev. `84483`.
- Consulta rama `25.08.001` en Presmed, pantalla carga de prestaciones a expedientes de ordenes: al perder foco campo autorizacion, `dw_clase.losefocus` hace `AcceptText()` y dispara `itemchanged`; toma sucursal de `dw_det.object.sucur[1]`, autorizacion de `THIS.object.AUTO[1]`, fecha realizacion de `THIS.object.rea_fecha[1]` y fecha orden de `THIS.object.orden_fecha[1]`.
- Busqueda cabecera autorizacion: DataWindow `d_auto` ejecuta `dbo.prs_auto_cabe @sucur, @auto, @auto_tipo`; en ese flujo `@auto_tipo` va `NULL`, por lo que la busqueda principal filtra por `a.sucur = @sucur`, `a.auto = @auto` y `(a.tipo = @auto_tipo OR @auto_tipo IS NULL)`. La baja no se descarta en el SP de cabecera, se valida luego en codigo.
- Busqueda prestaciones autorizadas: `ue_autoriz()` recupera `dw_aut` con DataObject `d_auto_prestac_aliqui`, que ejecuta `dbo.prs_auto_prestac_aliqui @sucur, @auto`; filtra `AUTO_PRESTAC_ITEMS` por sucursal, autorizacion, `baja_fecha IS NULL` y `esta = 'A'`, luego calcula pendientes descontando usos en expedientes/honorarios/odontologia.
- Mensajes post-perdida de foco en Ordenes informados: inexistente/no pertenece a sucursal; pendiente en supervision; rechazado general; prestador de orden no corresponde; complete fecha realizacion; afiliado sin derivacion de aportes vigente; autorizacion no pertenece al afiliado; prestador difiere; no se permite ingreso por prestador distinto; no tiene prestaciones detalladas; no es/es de cirugia; prestacion no vinculada a integracion; no quedan prestaciones pendientes; no corresponde a integracion; prestaciones con rechazos; observaciones prestaciones; autorizacion de prepaga externa/no es de entidad; error/no tiene credencial; dada de baja; inexistente verifique sucursal/numero; sin situacion terapeutica integracion; adherente voluntario sin recupero; certificado discapacidad no vigente para integracion tipo P.
- Luego Juana pidio lista ordenada de mensajes post-perdida de foco del campo autorizacion para expedientes de Ordenes, Honorarios (Practicas, Cirugias y Cons./Pract. c/demandante), Sanatorios, Odontologia y Odontologia no valorizado. Se respondio agrupado por tipo. Odontologia no valorizado usa la misma ventana `wp_expe_odon` y comparte la lista de Odontologia para ese evento.

## 2026-09-01 17:35 ART - Presmed autorizaciones en expedientes de ordenes

- Usuario: Maru.
- Se investigo en SVN/Presmed rama `25.08.001`, working copy `mpariso-V25.08.001`, actualizada a revision `84483`.
- Pantalla analizada: `wp_expe_orde.srw`, carga de prestaciones a expedientes de ordenes.
- Flujo al seleccionar sucursal e ingresar numero de autorizacion en `dw_det`: toma `dw_det.object.sucur[1]` y `dw_det.object.auto[1]`, ejecuta `lds_auto.Retrieve(li_sucur, ll_auto, ls_null)` contra DataWindow `d_auto`.
- `d_auto.srd` usa SP `dbo.prs_auto_cabe @sucur = :sucur, @auto = :auto, @auto_tipo = :auto_tipo`.
- Condiciones principales de `prs_auto_cabe`: `a.sucur = @sucur`, `a.auto = @auto`, `(a.tipo = @auto_tipo OR @auto_tipo IS NULL)`. Trae complementos con `LEFT JOIN` a `auto_remotas` (max transac), `origen_inter`, `afiliados`, `origenes`.
- Validadores relacionados: `uo_nv_valida_auto.of_autoriz` recupera `d_auto` y devuelve `0` OK, `80` inexistente, `87` baja; `uo_nv_valdatos.of_autoriz` consulta `autorizaciones WHERE sucur = :a_sucur AND auto = :a_auto` y distingue inexistente/baja.
- Prestaciones: `dw_aut.Retrieve(s_auto.sucur, s_auto.AUTO)` usa `d_auto_prestac_aliqui`, SP `dbo.prs_auto_prestac_aliqui @sucur = :sucur, @auto = :auto`, con condiciones `API.sucur = @sucur`, `API.auto = @auto`, `API.baja_fecha IS NULL`, `API.esta = 'A'`; descuenta prestaciones ya liquidadas en honorarios, ordenes y odontologia con `baja_fecha IS NULL`.
- Mensajes de validacion detectados: autorizacion inexistente/no pertenece a sucursal; inexistente verificar sucursal y numero; dada de baja; pendiente en supervision; estado general rechazado; complete fecha de realizacion; prestador de orden/autorizacion difiere del expediente; afiliado no coincide y pregunta si recupera; sin prestaciones detalladas; no es/es de cirugia; no vinculada a integracion; no corresponde a prestacion de integracion; sin situacion terapeutica de integracion; adherente voluntario sin recupero; sin derivacion de aportes vigente; sin certificado de discapacidad vigente; no es de esta entidad; no quedan prestaciones pendientes; cantidad mayor a autorizada; prestacion no incluida o sin saldo; ingreso mas prestaciones que autorizadas o que cantidad pendiente.
