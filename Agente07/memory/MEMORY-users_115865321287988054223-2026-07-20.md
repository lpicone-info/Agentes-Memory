# Memoria curada diaria

- Fecha: 2026-07-20
- Usuario: Patricia de Lourdes Bossa
- id_googlechat: users/115865321287988054223
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/jrvbkkaaaae

## Resumen de la actividad

### Tema 1: Saludo y carga de preferencias

- Solicitud o contexto: La usuaria saludó al agente.
- Respuesta o acciones del agente: El agente ejecutó bootstrap y cargó preferencias de Patricia.
- Resultado: Respondió que las preferencias estaban cargadas y quedó disponible.
- Estado: Informativo

### Tema 2: Relación entre afu_tarifas, afu_tari_familiar y afu_tari_capitas

- Solicitud o contexto: Pidió identificar qué ventana llama a `afu_tarifas` y qué relación tiene con `afu_tari_familiar` y `afu_tari_capitas`.
- Respuesta o acciones del agente: Se revisó `mpariso-V25.08.001`, incluyendo `wr_tarifa.srw`, `afu_tarifas.sql`, `wp_tari_familia.srw` y lógica de tarifas por cápita.
- Resultado: Se informó que `wr_tarifa`, ventana "Alta de tarifa", declara y ejecuta `afu_tarifas`; este SP graba/actualiza cabecera en `tarifas` y, para tarifa familiar, inicializa registros base en `tari_familiar` mediante `afu_tar_edad_tope`, no mediante `afu_tari_familiar`. `afu_tari_familiar` lo llama `wp_tari_familia` para grabar importes por estructura familiar; `afu_tari_capitas` pertenece al mantenimiento de importes por cápita.
- Estado: Resuelto

### Tema 3: SP relacionados con el parámetro general Edad Tope Calculada y facturación/prefacturación

- Solicitud o contexto: Pidió todos los SP que usan el parámetro general `Edad Tope Calculada` y se relacionan con prefacturación; luego pidió explicar cada uno desde la sección funcional, separar si pertenecen a facturación o prefacturación y aclarar desde qué funcionalidad se llaman.
- Respuesta o acciones del agente: Se buscaron usos del parámetro y llamados en `mpariso-V25.08.001`, revisando SPs y objetos PowerBuilder/DataWindows.
- Resultado: Se separó la relación directa con prefacturación: `afi_prefac_prefacturar` y `afs_prefac_cuota_categoria`. Se explicó que otros SP (`afs_valida_valor_cuota`, `afs_valida_valor_cuota_import`, `afs_valida_valor_descuento`, `afs_valida_valor_facargo`, `afs_valida_valor_recargo`, `ars_val_valor_cuo_precarga`) se relacionan con cálculo/validación de valores usados por facturación o funcionalidades auxiliares, no con el flujo directo de prefacturación mensual.
- Estado: Resuelto

### Tema 4: Clasificación de llamados desde prefacturación, facturación y test de cuota

- Solicitud o contexto: Pidió identificar por separado desde dónde se llaman los SP relacionados con `Edad Tope Calculada`, incluyendo pantalla de prefacturación, proceso de facturación y test de cuota.
- Respuesta o acciones del agente: Se rastrearon llamadas desde `wp_prefac_proceso_mensual`, `afi_generar_prefacturas`, `afi_prefac_prefacturar`, `wp_fac_proceso_mensual`, `uo_nv_factu`, `afi_generar_compro_masiva`, `afi_genera_compro`, `afi_compro_impresion` y `wp_test_cuota`.
- Resultado: Para prefacturación se listaron llamadas directas de pantalla: `afi_verifica_ddjj_coef`, `afi_generar_prefacturas` y `afs_facturacion_errores`; internamente `afi_generar_prefacturas` llama a `afi_prefac_prefacturar`, que llama a SPs `afs_prefac_*`, `afi_generar_dto_ripte`/`afi_generar_dto_ripte_no_acum` y `afs_prefac_diferencia_retroactivo`. Para facturación mensual se determinó que la cadena real llega a `afi_compro_impresion`, donde el SP vinculado al parámetro es `afs_valida_valor_descuento`; `afs_valida_valor_cuota` existe dentro de `uo_nv_factu.of_val_soli`, pero no en la cadena mensual directa. Para test de cuota se identificó llamada directa a `afs_valida_valor_cuota` y llamadas indirectas por DW/SP armador: `afs_arma_grupo_familiar` -> `afs_valida_valor_facargo` y `afs_arma_recargos` -> `afs_valida_valor_recargo`.
- Estado: Resuelto

### Tema 5: Funcionalidades auxiliares de cálculo de valores

- Solicitud o contexto: Preguntó desde qué funcionalidad se llaman `afs_valida_valor_recargo`, `afs_valida_valor_facargo`, `afs_valida_valor_cuota_import` y `ars_val_valor_cuo_precarga`.
- Respuesta o acciones del agente: Se corrigió el nombre escrito como `fas_valida_valor_recargo` a `afs_valida_valor_recargo` y se rastrearon llamados.
- Resultado: Se indicó que `afs_valida_valor_recargo` se llama desde `afs_arma_recargos`, usado en Test de cuota, exportación/listado de solicitudes pendientes y `afs_valida_valor`; `afs_valida_valor_facargo` se llama desde `afs_arma_grupo_familiar`/`afs_arm_grupo_fliar`, usado en Test de cuota, exportación de solicitudes pendientes, padrón SIRO y cálculo integral; `afs_valida_valor_cuota_import` se llama desde `w_importar`, funcionalidad de importación de débitos y créditos; y `ars_val_valor_cuo_precarga` pertenece a precarga/validación de solicitudes, fuera del proceso mensual directo.
- Estado: Resuelto

### Tema 6: Pre-compaction memory flush sin contenido durable adicional

- Solicitud o contexto: Llegó un pedido automático de memory flush para `memory/2026-07-20.md`.
- Respuesta o acciones del agente: El agente evaluó que no correspondía guardar contenido adicional en ese momento.
- Resultado: Respondió `NO_REPLY`.
- Estado: Informativo
