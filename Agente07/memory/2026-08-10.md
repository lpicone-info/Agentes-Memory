
## 2026-08-10 17:53 ART - Pre-compaction flush

- Usuario en Google Chat directo: Maru / mpariso. Prefiere documentos de criterios/casos de prueba en `/mnt/Agentes/mpariso/Incidentes/<RM>/Criterios de aceptación IA.docx` (carpeta correcta: `/mnt/Agentes/mpariso/Incidentes`, no `/mnt/Agentes/Mariela/Incidentes`).
- Para RM30097 se generó `/mnt/Agentes/mpariso/Incidentes/RM30097/Criterios de aceptación IA.docx` con 15 casos de prueba en Gherkin, usando documentación del incidente, `R30097.docx` y solución en `trunk` sobre `afs_detalle_afiliados.sql`. Validaciones: trunk rev. 84283, Incidentes rev. 20677, docx legible y contiene RM30097/casos hasta 15/Dado-Cuando-Entonces.
- Para RM28663 se generó `/mnt/Agentes/mpariso/Incidentes/RM28663/Criterios de aceptación IA.docx` con 25 casos de prueba en Gherkin, usando documentación, notas Redmine y solución `trunk`: endpoint Node, ruta, JWT, campos obligatorios, SP `aps_valida_carencias_topes`, orden autorización/carencias/topes y diferencias contrato funcional vs implementado. Validaciones: trunk rev. 84291, Incidentes rev. 20689, docx válido y contiene RM28663/endpoint `validarCarenciasTopes`/casos hasta 25/Dado-Cuando-Entonces.
- Análisis rama `25.08.001` sobre copagos: en `backend/procedures/pru_expe_copagos.sql`, si existen copago por convenio y por plan/prestación, prevalece el copago por convenio. Orden observado: copago específico ya cargado en autorización, luego convenio, luego plan/prestador/prestación, luego plan/prestación general. La inserción de plan/prestación usa `WHERE NOT EXISTS` contra `#copa` después de insertar convenio.
- Análisis rama `25.08.001` sobre botón "Marcar En Revisión" en `Presmed Autorizaciones/Consulta de autorizaciones por sector`: ventana `exe/Source Infomedical/wp_consulta_autorizaciones_por_sector.srw`, evento `ue_marca_fila`. `cb_marca.enabled = TRUE` solo si autorización seleccionada tiene `esta = 'P'` (Pendiente); cualquier otro estado lo deshabilita. `en_revision` no afecta habilitación, solo texto: `N` muestra "Marcar En Revisión", `S` muestra "Desmarcar En Revisión". DataWindow `d_lista_autorizaciones_x_sector` llama `prs_consulta_autorizaciones_x_sector`; `esta` sale de `au.esta`; `en_revision` se normaliza a `S` o `N`. Click persiste vía `pru_autorizaciones_campo_en_revision` actualizando `autorizaciones.en_revision` y `usuari_revision`.
