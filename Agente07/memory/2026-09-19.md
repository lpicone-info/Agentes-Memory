## 2026-09-19 - Copagos odontologia MutualRioIV_Prod_Test

- Usuario habitual: Pato. Conversacion directa por Google Chat. El agente se presenta/actua como Bender (Agente07), arquitecto de sistemas PowerBuilder 2022 y MS SQL.
- Diagnostico realizado sobre `mutualrioIV_prod_test` / `MutualRioIV_Prod_Test` para autorizacion `1-868360`: el item odontologico si guardo copago en `auto_prestac_items` para prestacion `101`, copago `2000.00`, `pago_copa = 'P'` (`Al prestador`). No se genero ajuste/recibo de copago: no habia registros en `ajustes` para `ori_auto = 868360` ni recibo asociado. La configuracion de cobertura `623099`, plan `72PLATINUM`, nomen `3`, prestacion `101`, define copago `2000.00` con `pago_copa = 'P'`. El codigo solo genera ajuste de copago para items con `pago_copa = 'A'` (`Adelantado`). En esa base `parametros.genera_ajuste_copago_auto` estaba `NULL` y la regla `320` estaba `N`, bloqueando generacion automatica al imprimir.
- Diagnostico realizado sobre expediente odontologico mostrado por usuario: `expe = 14898` existe, pero corresponde a expediente viejo del `03/11/2025`, ya liquidado/exportado, prestador `6415`, y no corresponde a captura 2026. No tiene registros en `expe_odon` ni `expe_odon_items`. Busqueda por afiliado `210897/00` tampoco mostro items odontologicos grabados. Expediente odontologico de recepcion `18/09/2026` encontrado en `expedientes`: `31360`, tambien sin detalle en `expe_odon_items`.
- Para prestacion `0104`, plan `72PLATINUM`, existe configuracion de cobertura `623100`, copago `2500.00`, `pago_copa = 'P'`. Causa probable del expediente: la pantalla aun no persistio el item odontologico. El codigo guarda copago con `pru_expe_copa_direct` recien despues de insertar/modificar item en `expe_odon_items`, y solo si el item queda como `NewModified` o `DataModified` y `copa_valor > 0`. En captura se observaba `Valor Uni. .00`, senal de fila no valorizada/calculada; por eso no llegaria a insertarse en `expe_copagos`.
