# Memoria durable - 2026-08-28

- Interlocutora: Juana. Conversacion directa por Google Chat. Prefiere trabajo tecnico concreto, con entregables en `A:\jvmendoza\Analisis\...` mapeado localmente como `/mnt/Agentes/jvmendoza/Analisis/...`.
- R27498 / RM27498: Se trabajo sobre trunk y bases `SMC_Prod_Test` y `Montegrande_Prod_Test`, modulo Presmed/Odon.
- Para R27498 se dejo el archivo SQL `/mnt/Agentes/jvmendoza/Analisis/R27498/consultas_odontologia_R27498.sql` con dos consultas: expedientes tipo Odontologia y Odon. No Val no liquidados ni dados de baja, y autorizaciones de odontologia no dadas de baja. Validado contra `SMC_Prod_Test`; ejecuto sin errores y no devolvio filas en ese momento.
- En pantalla Presmed > Odon. > Expedientes > Circuito de carga > Carga de expedientes de odontologia, para Expediente de ODONTOLOGIA, los campos origen son: Sucursal = tabla `expe_odon`, campo `sucur`; Autorizacion = tabla `expe_odon`, campo `auto`. Evidencia en ventana `wp_expe_odon`, datawindows `d_expe_odon_cab_fic` / `d_expe_odon_cab_pla`, procedures `prs_expe_odon` / `prs_expe_odon2`, ambos con `FROM expe_odon e` y asignaciones `e.sucur`, `e.auto`. Validado en `Montegrande_Prod_Test`: `expe_odon.sucur` smallint, `expe_odon.auto` int.
- Para RM27498, Juana pidio revisar y ampliar casos de prueba segun preferencias y EF del incidente. Primero no se encontraba la carpeta EF en Analisis; luego indico hacer update del SVN del repositorio del incidente. Se ejecuto `svn update` del repo `Incidentes`, quedando en revision `21003`, y aparecio la EF de R27498.
- Se actualizo `/mnt/Agentes/jvmendoza/Analisis/RM27498/Criterios de aceptacion IA.docx`: se agrego alcance de `Odon. / Expedientes / Circuito de carga`, se incorporaron Odontologia y Odon. no Val en datos minimos, y se ampliaron criterios de 15 a 20 escenarios. Incluye validacion especifica para que no pida "Ingrese la sucursal" antes de resolver la autorizacion contra todas las sucursales. Se valido que el `.docx` no estuviera corrupto y que quedaran escenarios nuevos de Odontologia, Odon. no Val y conservacion de validaciones existentes.

- Interlocutor: Facundo. Conversacion directa por Google Chat. Prefiere respuestas tecnicas concretas y accionables.
- Preferencia operativa indicada por Facundo: cuando mencione `25.08`, trabajar siempre sobre repositorio/ambiente `25.08` y base/profile `SMC_Prox_Ver`.
- Analisis transacciones en `SMC_Prox_Ver` / 25.08: `6525039` tiene cabecera `auto_remotas.recha = 54` porque es `tipo_transac = 02I` y entra a validacion de internacion/cartilla; `plan_codi` esta `NULL` y no hay plan vigente para contrato `103101/00` al `25/08/2026` (ultimo plan `SENIOR 2` dado de baja `01/02/2025`). Su item `5808795` queda con `recha = 2` porque el afiliado prepaga 2 contrato `103101/00` esta dado de baja desde `20/01/2025` en `afiliados` y `afi_histo_ingresos`.
- Comparacion transacciones: `6525042` responde `recha = 2` porque es `tipo_transac = 02C` / `tipo = P`, no entra a la validacion posterior especial de internacion `02I` que en `6525039` pisa cabecera con `54`; conserva el rechazo por afiliado dado de baja.
- Analisis transaccion `6525046`: queda `esta = R` aunque el rechazo `1255` en `auto_validacion_rechazos` esta configurado con `esta = P`. Confirmado que no ingresa a la validacion que toma estado desde `auto_validacion_rechazos`: el bloque de `prs_valida_of_auto_remotas` exige `R.aufree IS NULL`, pero la transaccion tiene `aufree = -1`; el otro bloque aplica solo a `tipo_transac = '02C' AND aufree IS NOT NULL`, y la transaccion es `02A`. Aplica `prs_valida_of_convenio_remoto` por `parametros.chk_conve_remotas = S` y `aufree IS NOT NULL`, que actualiza items con rechazo a `esta = 'R'`.
- RM30003: Facundo pidio conectar a Redmine R30003, usar el documento asociado y generar criterios de aceptacion/casos de prueba en Gherkin tomando como plantilla la EF del RM28243 seccion 7. Se genero `/mnt/Agentes/ffarias/Incidentes/RM30003/Criterios de aceptación IA.docx`. Fuentes usadas: RM30003 en Redmine, documento asociado `R30003.doc`, captura adjunta del RM y estructura de referencia de EF RM28243 seccion 7. Validado: Word 2007+ abre, `unzip -t` sin errores y extraccion de texto con 12 escenarios Gherkin.
