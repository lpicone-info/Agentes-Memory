# Memoria de conversación — Juana Virginia Mendoza

## Datos

- **Fecha:** 25/08/2026
- **Usuario:** Juana Virginia Mendoza
- **ID Google Chat:** `users/117659435859951903029`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 12
- **Inicio:** 08:48:37
- **Fin:** 12:47:00

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/5gqh-kaaaae`

## Archivos revisados

- `63e28f47-86de-46fc-bb30-904cc59a6600.jsonl`

---

## Transcripción completa

### [25/08/2026 08:48:37] Juana Virginia Mendoza

Buen dia

---

### [25/08/2026 08:50:02] Agente

Hola Juana, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [25/08/2026 09:57:23] Juana Virginia Mendoza

Bender en Afilmed, rama trunk Base Premedic_Prod, arma querys de SQL para determinar cuando fué ejecutado por última vez un proceso de facturación y si es posible determinar los cambios en parametros_generales específicamente las claves aplica_auto_n_cred y aplicacion_auto_cred

---

### [25/08/2026 10:00:20] Agente

Juana, armé las queries en base a trunk. Evidencia del código: la ejecución de facturación queda en `PROCESO_FACTURACION` con `inicio` y `fin`; la programación nueva queda en `PROGRAMACION_FACTURACION`; los cambios de `parametros_generales` se auditan en `param_generales_audit`.

No ejecuté conexión porque en `DB_PROFILE.md` no hay profile exacto `Premedic_Prod`; sí aparece `Premedic_Prod_Test`.

```sql
USE Premedic_Prod
GO

SELECT TOP 20
       pf.proceso_facturacion,
       pf.ente,
       pf.proceso_llamador_fact,
       llamador = plf.deno,
       pf.inicio,
       pf.fin,
       estado = CASE WHEN pf.fin IS NULL THEN 'EN_EJECUCION_O_SIN_CIERRE' ELSE 'FINALIZADO' END,
       duracion_minutos = DATEDIFF(MINUTE, pf.inicio, ISNULL(pf.fin, GETDATE()))
FROM   dbo.PROCESO_FACTURACION pf
       LEFT JOIN dbo.PROCESO_LLAMADOR_FACTURACION plf
       ON plf.proceso_llamador_fact = pf.proceso_llamador_fact
ORDER BY ISNULL(pf.fin, pf.inicio) DESC
GO
```

```sql
USE Premedic_Prod
GO

SELECT TOP 20
       id,
       tipo_proceso,
       prepaga,
       emi_grupo,
       factu_perio,
       fecha_programada,
       hora_programada,
       fecha_inicio_ejecucion,
       fecha_fin_ejecucion,
       fecha_ejecucion,
       errores,
       usuario_carga,
       usuario_modificacion
FROM   dbo.PROGRAMACION_FACTURACION
WHERE  fecha_baja IS NULL
AND    tipo_proceso = 'FACTU'
ORDER BY ISNULL(fecha_fin_ejecucion, ISNULL(fecha_ejecucion, fecha_inicio_ejecucion)) DESC
GO
```

```sql
USE Premedic_Prod
GO

SELECT sistema,
       modulo,
       clave,
       tipo_dato,
       valor,
       descripcion,
       baja_fecha
FROM   dbo.parametros_generales
WHERE  sistema = 'Afilmed'
AND    modulo = 'Facturacion'
AND    clave IN ('aplica_auto_n_cred', 'aplicacion_auto_cred')
AND    baja_fecha IS NULL
GO
```

```sql
USE Premedic_Prod
GO

SELECT fecha,
       usuario,
       sistema,
       modulo,
       clave,
       parametro,
       valor_anterior,
       valor_actual
FROM   dbo.param_generales_audit
WHERE  sistema = 'Afilmed'
AND    modulo = 'Facturacion'
AND    clave IN ('aplica_auto_n_cred', 'aplicacion_auto_cred')
ORDER BY fecha DESC
GO
```

Para cruzar cambios de parámetros contra la última facturación:

```sql
USE Premedic_Prod
GO

WITH ultima_facturacion AS
(
    SELECT TOP 1
           proceso_facturacion,
           inicio,
           fin
    FROM   dbo.PROCESO_FACTURACION
    ORDER BY ISNULL(fin, inicio) DESC
)
SELECT uf.proceso_facturacion,
       uf.inicio,
       uf.fin,
       aud.fecha,
       aud.usuario,
       aud.clave,
       aud.parametro,
       aud.valor_anterior,
       aud.valor_actual
FROM   ultima_facturacion uf
       INNER JOIN dbo.param_generales_audit aud
       ON aud.fecha >= DATEADD(DAY, -7, uf.inicio)
       AND aud.fecha <= DATEADD(DAY, 1, ISNULL(uf.fin, GETDATE()))
WHERE  aud.sistema = 'Afilmed'
AND    aud.modulo = 'Facturacion'
AND    aud.clave IN ('aplica_auto_n_cred', 'aplicacion_auto_cred')
ORDER BY aud.fecha DESC
GO
```

Conclusión técnica: sí es posible determinar cambios si `param_generales_audit` tiene registros para esas claves. Si esa tabla no tiene filas, desde `parametros_generales` solo se puede ver el valor vigente, no la historia del cambio.

---

### [25/08/2026 10:26:27] Juana Virginia Mendoza

Bender dime si se puede determinar si una aplicación de créditos y débitos fué ralizada a través del módulo de Afilmed cuentas corrientes/afiliados/aplicación manual

---

### [25/08/2026 10:28:57] Agente

Juana, sí se puede determinar parcialmente, pero con una salvedad importante: la tabla `COMPRO_APLICACIONES` no guarda un campo explícito “origen manual”, “ventana”, “usuario” o “módulo”.

Lo que sí encontré en trunk:

- La ventana `wp_compro_aplica_manual` corresponde al módulo “Ingreso manual de cobranza”.
- Esa ventana llama a `afi_cobranza_manual`.
- `afi_cobranza_manual`, en la etapa `APLICA COMPROBANTE`, llama a `afu_aplicar_compro` pasando `@prm_proceso_llamador_fact = 'IM'`.
- La aplicación final se inserta en `COMPRO_APLICACIONES` desde `afu_aplicar_compro_item`.
- `COMPRO_APLICACIONES` solo guarda comprobante débito, comprobante crédito, items, fecha, total, alta_fecha y baja_fecha.

Consulta base para ver la aplicación entre débito y crédito:

```sql
SELECT ca.compro_apli,
       ca.ente,
       ca.compro_tipo,
       ca.compro_nro,
       ca.item,
       ca.ente2,
       ca.compro_tipo2,
       ca.compro_nro2,
       ca.item2,
       ca.fecha,
       ca.total,
       ca.alta_fecha,
       ca.baja_fecha
FROM   dbo.COMPRO_APLICACIONES ca
WHERE  ca.ente = @ente
AND    ca.compro_tipo = @compro_tipo_debito
AND    ca.compro_nro = @compro_nro_debito
AND    ca.baja_fecha IS NULL
ORDER BY ca.alta_fecha DESC
```

Para inferir si vino del módulo manual, revisaría el comprobante crédito usado para aplicar:

```sql
SELECT ca.compro_apli,
       ca.alta_fecha AS fecha_aplicacion,
       ca.total AS importe_aplicado,
       deb.compro_tipo AS tipo_debito,
       deb.compro_nro AS nro_debito,
       cre.compro_tipo AS tipo_credito,
       cre.compro_nro AS nro_credito,
       cre.alta_fecha AS fecha_credito,
       cre.impre_fecha,
       cre.impre_usu,
       cre.proceso_facturacion,
       cre.proceso_origen,
       posible_origen =
           CASE
               WHEN cre.proceso_facturacion = 0
                    AND cre.impre_usu IS NOT NULL
                    THEN 'POSIBLE_INGRESO_MANUAL_COBRANZA'
               ELSE 'NO_DETERMINADO'
           END
FROM   dbo.COMPRO_APLICACIONES ca
       INNER JOIN dbo.COMPROBANTES deb
       ON deb.ente = ca.ente
       AND deb.compro_tipo = ca.compro_tipo
       AND deb.compro_nro = ca.compro_nro
       INNER JOIN dbo.COMPROBANTES cre
       ON cre.ente = ca.ente2
       AND cre.compro_tipo = ca.compro_tipo2
       AND cre.compro_nro = ca.compro_nro2
WHERE  ca.ente = @ente
AND    ca.compro_tipo = @compro_tipo_debito
AND    ca.compro_nro = @compro_nro_debito
AND    ca.baja_fecha IS NULL
ORDER BY ca.alta_fecha DESC
```

Conclusión: se puede inferir con buen sustento si la aplicación fue manual cuando el crédito aplicado fue generado por el flujo de cobranza manual, típicamente con `proceso_facturacion = 0`, `impre_usu` cargado y fechas coherentes entre `COMPROBANTES`, `COMPRO_PAGOS` y `COMPRO_APLICACIONES`. No veo evidencia de que quede grabado un indicador directo e inequívoco en `COMPRO_APLICACIONES` que diga “esto salió del menú Cuentas Corrientes / Afiliados / Aplicación manual”.

---

### [25/08/2026 12:13:33] Juana Virginia Mendoza

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 12:13 PM (America/Buenos_Aires)
Reference UTC: 2026-08-25 15:13 UTC

---

### [25/08/2026 12:14:15] Agente

NO_REPLY

---

### [25/08/2026 12:14:15] Juana Virginia Mendoza

Bender estoy analizando el redmine 30164 en el trunk base premedic_prod_test, reportan que hay contratos a los que se les cancelaron DI del período 04/2026 al 08/2026, por favor analiza, sin cambiar nada, que si se ejecutan los scripts de la carpeta solución del incidente, cuando se ejecuta Corre_tercero_cancela_CI y este llama al SP afu_cance_saldos es posible que haya tomado comprobantes DI de períodos mayores a 03/2026 ejemplo del contrato 94842728

---

### [25/08/2026 12:18:01] Agente

Juana, con la evidencia revisada, **no parece que el problema venga de `Corre_tercero_cancela_CI` tomando DI mayores a 03/2026**.

**Evidencia**
En `Corre_Primero_aplica.sql`, la carga inicial de `premedic_saldos` sí filtra comprobantes con:

[Corre_Primero_aplica.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Incidentes-Incidentes/R30164/Solucion/Corre_Primero_aplica.sql:125)

```sql
AND C.factu_perio <= @prm_periodo_hasta
```

y `@prm_periodo_hasta = '20260331'`.

En base `Premedic_Prod_Test`, validé que `premedic_saldos` no tiene períodos mayores a `20260331`:

```text
premedic_saldos_mayor_20260331 = 0
```

Para el contrato `94842728`, el tercer script generó DI de cancelación con:

```text
proceso_origen = CS
factu_perio = 31/03/2026
alta_fecha = 21/08/2026 23:11
cantidad = 47
```

Eso coincide con `Corre_tercero_cancela_CI` llamando a `afu_cance_saldos`, que a su vez llama a `afi_aju_inter_cancela` con `@proceso_origen = 'CS'` y `@factu_perio = '20260331'`.

**Hallazgo principal**
Lo que sí aparece para el contrato `94842728` es que a las `21/08/2026 20:40` se aplicaron CI antiguos contra DI de períodos posteriores:

```text
DI 05/2026, 06/2026, 07/2026 y 08/2026
aplicados contra CI de períodos 2020/2021
cantidad detectada = 32 aplicaciones
```

Eso no corresponde al tercer script, sino al flujo de `Corre_Primero_aplica.sql`, que llama a `afu_aplicar_compro` usando CI como crédito.

El punto sensible está en `afu_aplicar_compro`: en el caso de nota de crédito con saldo a favor, el SP busca débitos pendientes llamando a `afs_buscar_compro_a_aplicar` con `@prm_factu_perio = NULL`. En `afs_buscar_compro_a_aplicar`, cuando ese parámetro es `NULL`, la condición permite cualquier período:

[afs_buscar_compro_a_aplicar.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/afs_buscar_compro_a_aplicar.sql:158)

```sql
AND (COM.factu_perio <= @prm_factu_perio OR @prm_factu_perio IS NULL)
```

**Conclusión**
No: por la evidencia disponible, `Corre_tercero_cancela_CI` no tomó DI de 04/2026 a 08/2026. Ese script generó DI nuevos de cancelación con período `31/03/2026`.

Sí hay evidencia de que los DI de 05/2026 a 08/2026 del contrato `94842728` fueron cancelados antes, a las `20:40`, por aplicaciones de CI antiguos realizadas por el flujo de `Corre_Primero_aplica.sql` / `afu_aplicar_compro`, porque en ese caso el selector de débitos puede quedar sin límite de período.

Validación ejecutada: solo lectura sobre trunk, Redmine 30164 y `Premedic_Prod_Test`. No cambié archivos ni datos.

---

### [25/08/2026 12:45:56] Juana Virginia Mendoza

dime el bloque del afu_aplicar_compro donde puede ocurrir que tomó DI de 05/2026 a 08/2026

---

### [25/08/2026 12:47:00] Agente

Juana, el bloque puntual es el **CASO 4** de `afu_aplicar_compro`, en:

[afu_aplicar_compro.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/afu_aplicar_compro.sql:729)

```sql
-- CASO 4
-- Estamos aplicando un pago Mediante un Nota de Credito (@item > 0 => NC)
-- Pero a cuenta del afiliado o a comprobantes anteriores pendientes

IF @compro_tipo IS NULL AND @item > 0 
BEGIN 
    ...
    EXECUTE dbo.afs_buscar_compro_a_aplicar
        @prm_ente                 = @ente,
        @prm_prepaga              = @prepaga,
        @prm_contra               = @contra,
        @prm_inte                 = @inte,
        @prm_cuenta               = @cuenta,
        @prm_subcta               = @subcta,
        @prm_factu_perio          = NULL,
        @prm_compro_no_impre      = 'S',
        @prm_compro_tipo          = @compro_tipo OUTPUT,
        @prm_compro_nro           = @compro_nro  OUTPUT
```

El punto crítico es este parámetro:

```sql
@prm_factu_perio = NULL
```

Luego, dentro del `WHILE`, aplica el comprobante encontrado:

```sql
EXEC afu_aplicar_compro_ori
     @ente         = @ente,
     @compro_tipo  = @compro_tipo,
     @compro_nro   = @compro_nro,
     @compro_tipo2 = @compro_tipo2,
     @compro_nro2  = @compro_nro2,
     @tipo_pago    = 'notacred',
     @saldo_cre    = @saldo_cre OUTPUT,
     @recha        = @recha OUTPUT,
     @prm_compro_no_impre = @prm_compro_no_impre
```

Y vuelve a buscar otro débito también sin período límite:

[afu_aplicar_compro.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/afu_aplicar_compro.sql:788)

```sql
EXECUTE afs_buscar_compro_a_aplicar
    ...
    @prm_factu_perio = NULL,
    ...
```

La razón por la que puede tomar DI de `05/2026` a `08/2026` está en `afs_buscar_compro_a_aplicar`:

[afs_buscar_compro_a_aplicar.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk/backend/procedures/afs_buscar_compro_a_aplicar.sql:164)

```sql
AND (COM.factu_perio <= @prm_factu_perio OR @prm_factu_perio IS NULL)
```

Si `@prm_factu_perio` llega `NULL`, esa condición habilita comprobantes de cualquier período. Por eso este bloque puede haber seleccionado DI posteriores a `03/2026`, incluyendo `05/2026` a `08/2026`, si estaban pendientes y cumplían las demás condiciones del contrato/cuenta/subcuenta.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
