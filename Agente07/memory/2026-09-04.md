## 2026-09-04 - Memoria de trabajo

- Usuario: Francisco. Conversacion por Google Chat directo.
- Caso Redmine R29715: archivo `/mnt/Agentes/fvmiano/casos/R29715/DDJJ_TEST.txt` usado para importacion de DDJJ contra `PensandoSalud_Prod_Test`.
- Se reviso y luego se corrigio `DDJJ_TEST.txt`: RNOS `402202` respetando homologacion para entidad/prepaga 16. Quedo `402202` en header, `codsoc` de todos los detalles, `codosoc_inform` de todos los detalles y trailer. Validaciones reportadas: 8 lineas, largo fijo 230, encoding us-ascii, LF, y RNOS vigente en `prepagas` para entidad 16.
- Hallazgo inicial del archivo DDJJ: `52107909` no era RNOS sino importe (`remimpo`/`rem5`); el RNOS correcto era `402202`. Trailer original traia RNOS `104504` y cantidad `0000000009`, inconsistente con 6 detalles.
- En `Celer_Prod_Test` se analizo el circuito SQL de importacion/validacion DDJJ. Circuito de validacion temprana: `afi_valida_e_importa_ddjj_masivo` -> `afi_valida_e_importa_ddjj_masivo_prep` -> `afi_valida_e_importa_ddjj_masivo_vali` -> `afs_valida_cuil_cuit_ddjj_auto_masivo`.
- El rechazo `1225` se detecta en `afi_valida_e_importa_ddjj_masivo_vali`, no en `afi_aportes_ddjj_lista`. Condicion funcional: `tmp.contra IS NOT NULL`, cruce con `AFI_CLASE`, regimen determinado por `condcuil`, busqueda en `CUENTA_SUBCTA_SISTEMA` por cuenta/subcta/sistema; si `CSS.sistema IS NULL`, inserta `recha_moti = 1225`. `1226` es otro caso: cuenta nula con clase E o regimen sin sistema con clase D.
- Circuito que empieza en `afi_aportes_ddjj_lista`: `afi_aportes_ddjj_lista` -> `afi_aportes_ddjj` -> opcional `afi_afi_deriv_aportes_ddjj_trans`. No recalcula validacion completa, no contiene 1225, no usa `CUENTA_SUBCTA_SISTEMA`, y no llama a `afi_valida_e_importa_ddjj_masivo_vali`; para que llegue 1225 por ese camino debe venir ya calculado en `@prm_lista_recha_moti`.
- Hay un circuito posterior/correctivo no correlacionado con `afi_aportes_ddjj_lista`: `afi_afu_soli_ddjj` -> `afi_afu_cuenta_ddjj`, capaz de insertar o reactivar 1225 por cuenta/CUIL/lote.
- Repositorio SVN trunk ubicado en `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk`.
- En trunk, el batch de importacion DDJJ entra por `exe/Source Infomedical/wp_main_procesos_batch.srw`, caso `lnvo_proceso_agenda.IMPORTACION_DDJJ`. Llama a `wf_importacion_ddjj(...)` y dentro declara/ejecuta `afi_valida_e_importa_ddjj`.
- Primer SP operativo que desencadena importacion y validacion de errores desde el batch DDJJ: `afi_valida_e_importa_ddjj`. Antes solo hay preparacion: `afs_trae_rnos_prepaga`, `afs_contra_forma`, carga del archivo a `tmp_primer_paso_ddjj` mediante DataWindow `d_primer_paso_ddjj`, `UPDATE()` y `COMMIT`.
- Wrapper SQL en trunk: `backend/procedures/afi_valida_e_importa_ddjj.sql` llama a `afi_valida_e_importa_ddjj_masivo`; dentro del masivo se ejecuta `afi_valida_e_importa_ddjj_masivo_prep` y luego `afi_valida_e_importa_ddjj_masivo_vali`.

- Reanalisis R29715 para contrato `20315308667` y CUIT `30718854012` en `PensandoSalud_Prod_Test`: los rechazos `1225` y `1226` no aparecen como bloqueo directo de la generacion de derivaciones de aportes ni en el batch ni en `afi_aportes_ddjj_lista`.
- Hallazgo funcional dominante en `afi_aportes_pre` / `afi_aportes_real`: registros con `codigo_error = A`, `recha_moti = 1220` y texto `Cuenta inexistente.`; en `afi_aportes_pre_error` se concentran tambien `1225` y `1226` como errores asociados.
- No existe cuenta vigente para `30718854012` en `cuentas`, y no aparece derivacion vigente en `afi_deriv_aportes` para ese CUIT.
- El circuito batch y el de ventana no filtran por `1225`/`1226` como bloqueo de derivacion; el comportamiento observado apunta a una falla de version/correlacion del SP instalado o a un bloqueo previo por cuenta inexistente (`1220`), no a esos dos rechazos como causa unica.

- RM29856 en Presmed v25.08.001: el ajuste afecta el tipo final de copago en el recálculo posterior, no la habilitación de Revisar ni la mecánica base de Reabrir.
- En la ventana de revisión de expedientes, al cerrar sin pendientes se vuelve a ejecutar el cálculo de copagos/topes sobre `expe_copagos`; el mismo circuito se usa en el cierre de carga y en el cierre de revisión.
- `ls_estado = 'r'` se usa cuando el expediente tiene fecha de carga y no tiene fecha de revisión; en sanatorio internación se evalúan `inter_carga_fecha` e `inter_revi_fecha`.
- El botón Revisar se habilita cuando el estado calculado es `r`; no aplica igual para todos los tipos de expediente.
- Reabrir expediente limpia fechas del circuito y baja copagos previos no odontológicos, dejando el expediente listo para recarga y nuevo cierre.
- En la revisión, el combo `A cargo de` sigue ligado al estado del error/débito; RM29856 impacta el `pago_copa` final del recálculo vía `COBER_PRESTAD_PAGO_COPA`.
- El tipo de copago visible en Test de Valorización se muestra debajo de `Def.Copago` en el panel Cobertura como `Cop.desc.a`.
- Se generaron documentos en la carpeta `EF` del incidente RM29856: `Criterios de Aceptacion Test de Valorización_IA.doc`, `Criterios de Aceptacion Test de Valorización_IAv2.doc` y `Criterios de Aceptacion Revision de Expedientes_IA.doc`.
- Las secciones trabajadas de la EF fueron 6.1.4 y 6.1.5, usando además reglas de la sección 6 para el alcance de aceptación.

- En `Ospecon_Prox_Ver` (v25.08), el prestador `443` tiene exencion activa de copagos para el plan `BASE`; `prs_prestad_exen_copa` devuelve `S` al `04/09/2026`.
- En `Presmed` v25.08 / `SMC_Prox_Ver`, la validacion de rechazo `1386` no aplica para `01B` porque `prs_validar_prestador_demandante` solo contempla `02A`,`02C`,`02D`,`02L`,`02Q`; en `02Q` puede quedar bloqueada antes por `668` si el convenio no habilita la prestacion.
- En el circuito WebPOS, `01B` entra por `prs_valida_of_cobertura_remoto`, mientras que `02C` deriva por `prs_valida_of_auto_remotas`, que si puede llegar al rechazo `20`.
## 2026-09-04 - Memoria de trabajo

- Usuario: Francisco. Conversacion por Google Chat directo.
- Caso Redmine R29715: archivo `/mnt/Agentes/fvmiano/casos/R29715/DDJJ_TEST.txt` usado para importacion de DDJJ contra `PensandoSalud_Prod_Test`.
- Se reviso y luego se corrigio `DDJJ_TEST.txt`: RNOS `402202` respetando homologacion para entidad/prepaga 16. Quedo `402202` en header, `codsoc` de todos los detalles, `codosoc_inform` de todos los detalles y trailer. Validaciones reportadas: 8 lineas, largo fijo 230, encoding us-ascii, LF, y RNOS vigente en `prepagas` para entidad 16.
- Hallazgo inicial del archivo DDJJ: `52107909` no era RNOS sino importe (`remimpo`/`rem5`); el RNOS correcto era `402202`. Trailer original traia RNOS `104504` y cantidad `0000000009`, inconsistente con 6 detalles.
- En `Celer_Prod_Test` se analizo el circuito SQL de importacion/validacion DDJJ. Circuito de validacion temprana: `afi_valida_e_importa_ddjj_masivo` -> `afi_valida_e_importa_ddjj_masivo_prep` -> `afi_valida_e_importa_ddjj_masivo_vali` -> `afs_valida_cuil_cuit_ddjj_auto_masivo`.
- El rechazo `1225` se detecta en `afi_valida_e_importa_ddjj_masivo_vali`, no en `afi_aportes_ddjj_lista`. Condicion funcional: `tmp.contra IS NOT NULL`, cruce con `AFI_CLASE`, regimen determinado por `condcuil`, busqueda en `CUENTA_SUBCTA_SISTEMA` por cuenta/subcta/sistema; si `CSS.sistema IS NULL`, inserta `recha_moti = 1225`. `1226` es otro caso: cuenta nula con clase E o regimen sin sistema con clase D.
- Circuito que empieza en `afi_aportes_ddjj_lista`: `afi_aportes_ddjj_lista` -> `afi_aportes_ddjj` -> opcional `afi_afi_deriv_aportes_ddjj_trans`. No recalcula validacion completa, no contiene 1225, no usa `CUENTA_SUBCTA_SISTEMA`, y no llama a `afi_valida_e_importa_ddjj_masivo_vali`; para que llegue 1225 por ese camino debe venir ya calculado en `@prm_lista_recha_moti`.
- Hay un circuito posterior/correctivo no correlacionado con `afi_aportes_ddjj_lista`: `afi_afu_soli_ddjj` -> `afi_afu_cuenta_ddjj`, capaz de insertar o reactivar 1225 por cuenta/CUIL/lote.
- Repositorio SVN trunk ubicado en `/home/desarrollo-ia-07/.openclaw/workspace/svn/mpariso-Infomedical-trunk`.
- En trunk, el batch de importacion DDJJ entra por `exe/Source Infomedical/wp_main_procesos_batch.srw`, caso `lnvo_proceso_agenda.IMPORTACION_DDJJ`. Llama a `wf_importacion_ddjj(...)` y dentro declara/ejecuta `afi_valida_e_importa_ddjj`.
- Primer SP operativo que desencadena importacion y validacion de errores desde el batch DDJJ: `afi_valida_e_importa_ddjj`. Antes solo hay preparacion: `afs_trae_rnos_prepaga`, `afs_contra_forma`, carga del archivo a `tmp_primer_paso_ddjj` mediante DataWindow `d_primer_paso_ddjj`, `UPDATE()` y `COMMIT`.
- Wrapper SQL en trunk: `backend/procedures/afi_valida_e_importa_ddjj.sql` llama a `afi_valida_e_importa_ddjj_masivo`; dentro del masivo se ejecuta `afi_valida_e_importa_ddjj_masivo_prep` y luego `afi_valida_e_importa_ddjj_masivo_vali`.

- Reanalisis R29715 para contrato `20315308667` y CUIT `30718854012` en `PensandoSalud_Prod_Test`: los rechazos `1225` y `1226` no aparecen como bloqueo directo de la generacion de derivaciones de aportes ni en el batch ni en `afi_aportes_ddjj_lista`.
- Hallazgo funcional dominante en `afi_aportes_pre` / `afi_aportes_real`: registros con `codigo_error = A`, `recha_moti = 1220` y texto `Cuenta inexistente.`; en `afi_aportes_pre_error` se concentran tambien `1225` y `1226` como errores asociados.
- No existe cuenta vigente para `30718854012` en `cuentas`, y no aparece derivacion vigente en `afi_deriv_aportes` para ese CUIT.
- El circuito batch y el de ventana no filtran por `1225`/`1226` como bloqueo de derivacion; el comportamiento observado apunta a una falla de version/correlacion del SP instalado o a un bloqueo previo por cuenta inexistente (`1220`), no a esos dos rechazos como causa unica.

- RM29856 en Presmed v25.08.001: el ajuste afecta el tipo final de copago en el recálculo posterior, no la habilitación de Revisar ni la mecánica base de Reabrir.
- En la ventana de revisión de expedientes, al cerrar sin pendientes se vuelve a ejecutar el cálculo de copagos/topes sobre `expe_copagos`; el mismo circuito se usa en el cierre de carga y en el cierre de revisión.
- `ls_estado = 'r'` se usa cuando el expediente tiene fecha de carga y no tiene fecha de revisión; en sanatorio internación se evalúan `inter_carga_fecha` e `inter_revi_fecha`.
- El botón Revisar se habilita cuando el estado calculado es `r`; no aplica igual para todos los tipos de expediente.
- Reabrir expediente limpia fechas del circuito y baja copagos previos no odontológicos, dejando el expediente listo para recarga y nuevo cierre.
- En la revisión, el combo `A cargo de` sigue ligado al estado del error/débito; RM29856 impacta el `pago_copa` final del recálculo vía `COBER_PRESTAD_PAGO_COPA`.
- El tipo de copago visible en Test de Valorización se muestra debajo de `Def.Copago` en el panel Cobertura como `Cop.desc.a`.
- Se generaron documentos en la carpeta `EF` del incidente RM29856: `Criterios de Aceptacion Test de Valorización_IA.doc`, `Criterios de Aceptacion Test de Valorización_IAv2.doc` y `Criterios de Aceptacion Revision de Expedientes_IA.doc`.
- Las secciones trabajadas de la EF fueron 6.1.4 y 6.1.5, usando además reglas de la sección 6 para el alcance de aceptación.

- En `Ospecon_Prox_Ver` (v25.08), el prestador `443` tiene exencion activa de copagos para el plan `BASE`; `prs_prestad_exen_copa` devuelve `S` al `04/09/2026`.
- En `Presmed` v25.08 / `SMC_Prox_Ver`, la validacion de rechazo `1386` no aplica para `01B` porque `prs_validar_prestador_demandante` solo contempla `02A`,`02C`,`02D`,`02L`,`02Q`; en `02Q` puede quedar bloqueada antes por `668` si el convenio no habilita la prestacion.
- En el circuito WebPOS, `01B` entra por `prs_valida_of_cobertura_remoto`, mientras que `02C` deriva por `prs_valida_of_auto_remotas`, que si puede llegar al rechazo `20`.
- Para consultas SQL de convenios/prestadores, materiales y traslados se identifican por banderas de `nomencladores` (`materiales = 'S'`, `traslado = 'S'`); odontología se toma desde `param_odon.nomen`, y el plan se obtiene desde `coberturas.plan_codi` junto con `planes`.
