# Memoria durable - 2026-09-11

## RM29856 - Autorizaciones Presmed / Ospecon
- Usuario: Pato.
- Incidente trabajado: `/mnt/Agentes/pbossa/Incidentes/RM29856`.
- EF usada: carpeta `EF` del incidente RM29856, solo sección `Funcionalidad: Autorizaciones Presmed`.
- Instrucciones usadas: `Incidentes/RM29856/Intrucciones CP.docx`, desde sección 2 hasta el final.
- Resultado generado: `/mnt/Agentes/pbossa/Incidentes/RM29856/EF/ResultadoInstrucciones.xlsx`.
- Archivo auxiliar generado: `/mnt/Agentes/pbossa/Incidentes/RM29856/EF/DatosConfigurar.doc`.
- Copia previa antes de actualización: `/mnt/Agentes/pbossa/Incidentes/RM29856/EF/ResultadoInstrucciones - copia previa actualizacion.xlsx`.
- Profile DB usado: `Ospecon_Prox_Ver` / `Ospecon_prox_Ver`.
- Configuraciones faltantes ejecutadas en DB con autorización explícita del usuario, solo con `INSERT` condicionados para evitar duplicados, sin `UPDATE` ni `DELETE`:
  - `situ_historias`: situacion terapeutica `situ = 10`, afiliado `1 / 132787 / 00`, plan `BASE`, vigencia hasta `31/12/2050`, observacion `RM29856 QA copago prestador`.
  - `afi_exen_copagos`: afiliado `1 / 100213 / 02`, vigencia `11/09/2026` a `31/12/2050`, usuario `pbossa`.
  - `prestad_exen_copa`: prestador `704`, plan `BASE`, vigencia desde `11/09/2026`.
  - Prestacion exenta por situacion terapeutica `situ = 10`, `nomen = 5`, `prestac = 140103` ya existia vigente; no se duplico.
- Luego se actualizo `ResultadoInstrucciones.xlsx`:
  - Casos de situacion terapeutica, exencion individual y exencion por prestador/plan marcados como configurados.
  - Agregados datos concretos cargados en base.
  - Odontologia sigue pendiente porque falta seleccionar una practica odontologica ejecutable desde pantalla.
  - Agregada hoja `I_Config_Realizada` con evidencia de configuracion.
  - Validado que el Excel abre correctamente con LibreOffice.

## WS transacciones remotas - 02L transac 21081
- Consulta realizada en profile `ospecon_prox_ver` / `Ospecon_Prox_Ver`.
- Transaccion remota `auto_remotas.transac = 21081`:
  - `tipo_transac = 02L`.
  - socio `contra = 1001`, `inte = 01`.
  - estado `R`.
  - rechazo `710`.
  - `recha_motivos.710 = No existe solicitud para el socio`.
  - dato clave: `anula_transac = 0`.
- Causa explicada: para una `02L`, el procedimiento `pri_transac_remotas_lab` valida que `anula_transac` apunte a una transaccion previa `01A` vigente. Como vino `0`, no habia `01A` relacionada y el SP rechazo con `710`.
- Se buscaron transacciones `01A` para socio `1001/01` y no se encontraron; solo aparecian `02L 21081`, una `04A`, una `02A` y una `02D`.
- Tag XML informado para vincular una `02L` con una `01A`:
```xml
<Preautorizacion>
  <CodigoPreautorizacion>NUMERO_TRANSACCION_01A</CodigoPreautorizacion>
</Preautorizacion>
```
- Ese valor se graba en `auto_remotas.anula_transac`.
- Fallback de cabecera: `<NroReferenciaCancel>NUMERO_TRANSACCION_01A</NroReferenciaCancel>`, usado solo si `Preautorizacion/CodigoPreautorizacion` viene vacio.
# Memoria durable - 2026-09-11

## RM29856 - Autorizaciones Presmed / Ospecon
- Usuario: Pato.
- Incidente trabajado: `/mnt/Agentes/pbossa/Incidentes/RM29856`.
- EF usada: carpeta `EF` del incidente RM29856, solo sección `Funcionalidad: Autorizaciones Presmed`.
- Instrucciones usadas: `Incidentes/RM29856/Intrucciones CP.docx`, desde sección 2 hasta el final.
- Resultado generado: `/mnt/Agentes/pbossa/Incidentes/RM29856/EF/ResultadoInstrucciones.xlsx`.
- Archivo auxiliar generado: `/mnt/Agentes/pbossa/Incidentes/RM29856/EF/DatosConfigurar.doc`.
- Copia previa antes de actualización: `/mnt/Agentes/pbossa/Incidentes/RM29856/EF/ResultadoInstrucciones - copia previa actualizacion.xlsx`.
- Profile DB usado: `Ospecon_Prox_Ver` / `Ospecon_prox_Ver`.
- Configuraciones faltantes ejecutadas en DB con autorización explícita del usuario, solo con `INSERT` condicionados para evitar duplicados, sin `UPDATE` ni `DELETE`:
  - `situ_historias`: situacion terapeutica `situ = 10`, afiliado `1 / 132787 / 00`, plan `BASE`, vigencia hasta `31/12/2050`, observacion `RM29856 QA copago prestador`.
  - `afi_exen_copagos`: afiliado `1 / 100213 / 02`, vigencia `11/09/2026` a `31/12/2050`, usuario `pbossa`.
  - `prestad_exen_copa`: prestador `704`, plan `BASE`, vigencia desde `11/09/2026`.
  - Prestacion exenta por situacion terapeutica `situ = 10`, `nomen = 5`, `prestac = 140103` ya existia vigente; no se duplico.
- Luego se actualizo `ResultadoInstrucciones.xlsx`:
  - Casos de situacion terapeutica, exencion individual y exencion por prestador/plan marcados como configurados.
  - Agregados datos concretos cargados en base.
  - Odontologia sigue pendiente porque falta seleccionar una practica odontologica ejecutable desde pantalla.
  - Agregada hoja `I_Config_Realizada` con evidencia de configuracion.
  - Validado que el Excel abre correctamente con LibreOffice.

## WS transacciones remotas - 02L transac 21081
- Consulta realizada en profile `ospecon_prox_ver` / `Ospecon_Prox_Ver`.
- Transaccion remota `auto_remotas.transac = 21081`:
  - `tipo_transac = 02L`.
  - socio `contra = 1001`, `inte = 01`.
  - estado `R`.
  - rechazo `710`.
  - `recha_motivos.710 = No existe solicitud para el socio`.
  - dato clave: `anula_transac = 0`.
- Causa explicada: para una `02L`, el procedimiento `pri_transac_remotas_lab` valida que `anula_transac` apunte a una transaccion previa `01A` vigente. Como vino `0`, no habia `01A` relacionada y el SP rechazo con `710`.
- Se buscaron transacciones `01A` para socio `1001/01` y no se encontraron; solo aparecian `02L 21081`, una `04A`, una `02A` y una `02D`.
- Tag XML informado para vincular una `02L` con una `01A`:
```xml
<Preautorizacion>
  <CodigoPreautorizacion>NUMERO_TRANSACCION_01A</CodigoPreautorizacion>
</Preautorizacion>
```
- Ese valor se graba en `auto_remotas.anula_transac`.
- Fallback de cabecera: `<NroReferenciaCancel>NUMERO_TRANSACCION_01A</NroReferenciaCancel>`, usado solo si `Preautorizacion/CodigoPreautorizacion` viene vacio.

## R29863 - CP2 Presmed baja sin transaccion vinculada vigente
- Usuario: Any.
- Base usada: `SMC_Prox_Ver` / `smc_prox_ver`.
- Objetivo: identificar 5 datos para CP2 "Presmed - permite baja sin transaccion vinculada vigente".
- Precondicion validada: autorizacion vigente, sin `02A` o `02D` aceptadas y vigentes relacionadas por `sucur_rel`/`auto_rel`, sin expedientes vigentes en `prs_auto_expe`, sin otros bloqueos detectados.
- Evidencia tecnica del R29863: en `prd_autorizaciones` el bloqueo nuevo valida `AUTO_REMOTAS.tipo_transac IN ('02A','02D')`, `esta = 'A'`, `baja_fecha IS NULL`, vinculadas por `sucur_rel` y `auto_rel`.
- Set identificado:
  - Caso 1: `sucur=1`, `auto=2049855`, tipo `P - Prestaciones`, prepaga `2`, contra `106665`, inte `02`, fecha `09/09/2026`, valida hasta `08/12/2026`, prestacion `420101`, cantidad `1`.
  - Caso 2: `sucur=1`, `auto=2049866`, tipo `P - Prestaciones`, prepaga `2`, contra `106665`, inte `02`, fecha `09/09/2026`, valida hasta `08/12/2026`, prestacion `O0101`, cantidad `1`.
  - Caso 3: `sucur=1`, `auto=2049867`, tipo `P - Prestaciones`, prepaga `2`, contra `101750`, inte `03`, fecha `09/09/2026`, valida hasta `08/12/2026`, prestacion `O0101`, cantidad `1`.
  - Caso 4: `sucur=17`, `auto=2049844`, tipo `P - Prestaciones`, prepaga `2`, contra `108342`, inte `00`, fecha `01/09/2026`, valida hasta `01/10/2026`, prestacion `O0201`, cantidad `1`.
  - Caso 5: `sucur=17`, `auto=2049845`, tipo `P - Prestaciones`, prepaga `2`, contra `108342`, inte `00`, fecha `01/09/2026`, valida hasta `01/10/2026`, prestacion `O0201`, cantidad `1`.
- Riesgo informado: son datos vivos de prueba; luego de ejecutar la baja dejan de servir para repetir exactamente el CP sin restauracion o nuevo set.

## R28243 - Set de datos pruebas Seccion 8 Odontologia
- Usuario: Any.
- Pedido: analizar R28243 y solucion en Redmine, usar documento `R28243_Evidencia de Pruebas_Sección8 - Odontología.docx` en carpeta `\acruiz\R28243`, y generar `.md` con set de datos para pruebas con 4 contratos y 4 autorizaciones por cada caso planteado, usando `smc_prox_ver`.
- Redmine R28243 leido con usuario `acruiz`.
- Documento fuente leido desde `/mnt/Agentes/acruiz/R28243/R28243_Evidencia de Pruebas_Sección8 - Odontología.docx`.
- Repositorio `trunk` actualizado a revision `84649` durante el analisis.
- Consultas realizadas en `SMC_Prox_Ver` sobre `autorizaciones`, `auto_prestac_items`, `afiliados`, `AUTO_PRESTAC_RECHA_ACEPTADO` y `AUTO_AUDITORIA`.
- Archivo generado: `/mnt/Agentes/acruiz/R28243/R28243_set_datos_pruebas_seccion8_odontologia.md`.
- El archivo incluye alcance, criterios de aceptacion, datos minimos, supuestos de ejecucion, contratos base y set de autorizaciones por rechazo/caso funcional.
- Validacion final: archivo `us-ascii`, sin BOM y con finales `LF`.
- Riesgo documentado en el `.md`: para los rechazos `183` y `249`, la base devolvio menos de 4 autorizaciones vigentes disponibles con rechazo actual.
