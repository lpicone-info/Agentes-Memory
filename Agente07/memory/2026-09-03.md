
## 2026-09-03 12:40 ART - Redmine 29715 / 29249 derivacion de aportes

- Usuario: Francisco. Contexto de trabajo: caso Redmine 29715 relacionado con Redmine 29249, sobre derivacion no actualizada con nuevo CUIT de aportes en DDJJ y transferencias. Base usada: profile `PENSANDOSALUD_PROD_TEST` / `PensandoSalud_Prod_Test`.
- Archivos del caso: `/mnt/Agentes/fvmiano/casos/R29715/ddjj.csv` y `/mnt/Agentes/fvmiano/casos/R29715/transferencias.csv` contienen lotes importados con nuevo CUIT no actualizado en derivacion de aportes.
- Resultado relevamiento lotes: en DDJJ los rechazos bloqueantes detectados fueron `1225 - Cuenta Subcuenta sin sist. deriv. Regimen General.` y `1226 - Sin Sistema de Deriv. Aportes.`; en transferencias el rechazo bloqueante fue `1220 - Cuenta inexistente.`
- `1225` se detecta en `afi_valida_e_importa_ddjj_masivo_vali` validando `CUENTA_SUBCTA_SISTEMA` con cuenta, subcta y sistema vigentes. Si no existe fila vigente (`CSS.sistema IS NULL`) dispara el rechazo. `AFI_CLASE` aporta `subcta`; `AFI_REGIMEN` aporta `sistema` segun regimen derivado de `condcuil`.
- `1226` se valida en `afi_valida_e_importa_ddjj_masivo_vali` por dos condiciones: `(tmp.cuenta IS NULL AND cls.clase_afi = 'E')` o `(reg.sistema IS NULL AND cls.clase_afi = 'D')`. Bloquea luego en `afi_afu_cuenta_ddjj` porque baja `@var_bandera_sis = 0`.
- Origen de `tmp.cuenta`: en `afs_valida_cuil_cuit_ddjj_auto_masivo`, `##TMP_SEGUNDO_PASO_DDJJ.cuenta` se completa desde `CUENTAS` por `CUE.cuit = tmp.cuit`. Para los CUIT aportantes del caso `30698494642` y `30718854012` no existian filas en `CUENTAS`, por eso queda `NULL`.
- Origen de `cls.clase_afi`: en `afi_valida_e_importa_ddjj_masivo_vali`, desde `AFI_CLASE` por `prepaga`, `contra` y vigencia. En el caso relevado: `prepaga = 1`, `contra = 20315308667`, `clase_afi = D`, `subcta = NULL`.
- `AFI_REGIMEN.sistema`: en trunk el script base `backend/data/afi_regimen.sql` inserta regimenes con `sistema = NULL`. El SP que actualiza ese dato es `afi_afi_regimen`, que recibe `@sistema CHAR(3)` y hace `UPDATE AFI_REGIMEN SET sistema = @sistema WHERE regimen = @prm_regimen`.
- Pantalla Afilmed detectada para configurar `AFI_REGIMEN.sistema`: ventana `wp_beneficiario_titular_abm`, menu `Configuracion > Superintendencia de servicios de salud > Beneficiario titular`, DataWindow `d_afi_regimen`, campo visible `Sistema`, combo `dddw_deriv_aport_sistema` alimentado desde `DERIV_APORTES_SISTEMA` con `baja_fecha IS NULL`.
- Estado en PensandoSalud: `AFI_REGIMEN` tenia todos los regimenes `00` a `12` con `sistema = NULL`. Para lotes del caso, `condcuil` `01` o `05` calcula regimen `0`, que matchea `AFI_REGIMEN.regimen = '00'`; al estar `sistema = NULL` dispara `1226`.
- Configuracion funcional sugerida para corregir el caso: asignar sistema de derivacion al regimen `00 - RELACION DE DEPENDENCIA`, probablemente `RG - REGIMEN GENERAL` si corresponde al criterio funcional; verificar tambien que `DERIV_APORTES_SISTEMA` tenga un registro vigente y evaluar `defecto = 'S'` porque `afn_Sistema_Por_Defecto()` usado por `afi_afu_cuenta_ddjj` puede incidir en reproceso.
- No se encontro script temporal generico en trunk para completar `AFI_REGIMEN.sistema`; via estandar detectada es el ABM `Beneficiario titular`. Si no hay acceso visible/habilitado, alternativa tecnica: script puntual controlado sobre `AFI_REGIMEN` y posiblemente `DERIV_APORTES_SISTEMA.defecto`, con validacion funcional previa.
## 2026-09-03 12:40 ART - Redmine 29715 / 29249 derivacion de aportes

- Usuario: Francisco. Contexto de trabajo: caso Redmine 29715 relacionado con Redmine 29249, sobre derivacion no actualizada con nuevo CUIT de aportes en DDJJ y transferencias. Base usada: profile `PENSANDOSALUD_PROD_TEST` / `PensandoSalud_Prod_Test`.
- Archivos del caso: `/mnt/Agentes/fvmiano/casos/R29715/ddjj.csv` y `/mnt/Agentes/fvmiano/casos/R29715/transferencias.csv` contienen lotes importados con nuevo CUIT no actualizado en derivacion de aportes.
- Resultado relevamiento lotes: en DDJJ los rechazos bloqueantes detectados fueron `1225 - Cuenta Subcuenta sin sist. deriv. Regimen General.` y `1226 - Sin Sistema de Deriv. Aportes.`; en transferencias el rechazo bloqueante fue `1220 - Cuenta inexistente.`
- `1225` se detecta en `afi_valida_e_importa_ddjj_masivo_vali` validando `CUENTA_SUBCTA_SISTEMA` con cuenta, subcta y sistema vigentes. Si no existe fila vigente (`CSS.sistema IS NULL`) dispara el rechazo. `AFI_CLASE` aporta `subcta`; `AFI_REGIMEN` aporta `sistema` segun regimen derivado de `condcuil`.
- `1226` se valida en `afi_valida_e_importa_ddjj_masivo_vali` por dos condiciones: `(tmp.cuenta IS NULL AND cls.clase_afi = 'E')` o `(reg.sistema IS NULL AND cls.clase_afi = 'D')`. Bloquea luego en `afi_afu_cuenta_ddjj` porque baja `@var_bandera_sis = 0`.
- Origen de `tmp.cuenta`: en `afs_valida_cuil_cuit_ddjj_auto_masivo`, `##TMP_SEGUNDO_PASO_DDJJ.cuenta` se completa desde `CUENTAS` por `CUE.cuit = tmp.cuit`. Para los CUIT aportantes del caso `30698494642` y `30718854012` no existian filas en `CUENTAS`, por eso queda `NULL`.
- Origen de `cls.clase_afi`: en `afi_valida_e_importa_ddjj_masivo_vali`, desde `AFI_CLASE` por `prepaga`, `contra` y vigencia. En el caso relevado: `prepaga = 1`, `contra = 20315308667`, `clase_afi = D`, `subcta = NULL`.
- `AFI_REGIMEN.sistema`: en trunk el script base `backend/data/afi_regimen.sql` inserta regimenes con `sistema = NULL`. El SP que actualiza ese dato es `afi_afi_regimen`, que recibe `@sistema CHAR(3)` y hace `UPDATE AFI_REGIMEN SET sistema = @sistema WHERE regimen = @prm_regimen`.
- Pantalla Afilmed detectada para configurar `AFI_REGIMEN.sistema`: ventana `wp_beneficiario_titular_abm`, menu `Configuracion > Superintendencia de servicios de salud > Beneficiario titular`, DataWindow `d_afi_regimen`, campo visible `Sistema`, combo `dddw_deriv_aport_sistema` alimentado desde `DERIV_APORTES_SISTEMA` con `baja_fecha IS NULL`.
- Estado en PensandoSalud: `AFI_REGIMEN` tenia todos los regimenes `00` a `12` con `sistema = NULL`. Para lotes del caso, `condcuil` `01` o `05` calcula regimen `0`, que matchea `AFI_REGIMEN.regimen = '00'`; al estar `sistema = NULL` dispara `1226`.
- Configuracion funcional sugerida para corregir el caso: asignar sistema de derivacion al regimen `00 - RELACION DE DEPENDENCIA`, probablemente `RG - REGIMEN GENERAL` si corresponde al criterio funcional; verificar tambien que `DERIV_APORTES_SISTEMA` tenga un registro vigente y evaluar `defecto = 'S'` porque `afn_Sistema_Por_Defecto()` usado por `afi_afu_cuenta_ddjj` puede incidir en reproceso.
- No se encontro script temporal generico en trunk para completar `AFI_REGIMEN.sistema`; via estandar detectada es el ABM `Beneficiario titular`. Si no hay acceso visible/habilitado, alternativa tecnica: script puntual controlado sobre `AFI_REGIMEN` y posiblemente `DERIV_APORTES_SISTEMA.defecto`, con validacion funcional previa.

## 2026-09-03 15:02 ART - Facundo / V25.08.001 SMC_Prox_Ver remotas recha 90 y 02I

- Usuario: Facundo. Preferencia operativa declarada: para estos analisis usar siempre version `25.08.001` y base/profile `smc_prox_ver` (`SMC_Prox_Ver`). Rama SVN indicada: `https://srv-infosvn.infomedical.com/svn/Infomedical/branches/V25.08.001`. Working copy local usada previamente: `/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX`.
- Caso transacciones `6535271`, `6535272`, `6535273`: `6535272` es remota `tipo_transac=02I`, `sucur=1`, `auto=NULL`, `recha=2`, `esta=R`, `usuario=sa`, `aufree=88061688`, `sucur_rel=17`, `auto_rel=NULL`. Rechazo `2` corresponde a afiliado dado de baja; afiliado `prepaga=2`, `contra=103101`, `inte=00` tenia `baja_fecha=20/01/2025`.
- Comparativa WebPOS: `6535271` quedo `sucur=17`, `auto=2049804`, `recha=NULL`, `esta=A`, `usuario=ffa_prest`, `aufree=-1`. Existe `6535273`, dos minutos despues, mismos datos centrales, que si genero `sucur=1 auto=2049805`.
- Criterio funcional corregido por Facundo: una `02I` remota debe generar `sucur + auto` aunque rechace por `recha=2`; el problema de `6535272` no es el rechazo sino que quedo insertada/rechazada sin completar el circuito que genera la autorizacion.
- Flujo codigo WebPOS/remotas: `pri_auto_remotas` inserta en `AUTO_REMOTAS`; `pri_transac_webpos_total` luego ejecuta `prs_valida_of_auto_remotas` para `02I`; esa SP llama a `pru_autorizaciones` y completa `AUTO_REMOTAS.auto`. Referencia detectada: `/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/backend/procedures/pri_transac_webpos_total.sql:394`.
- Caso `6535288`: remota `tipo_transac=02C`, `sucur=17`, `auto=2049810`, `usuario=ffa_prest`, `aufree=-1`, `recha=20`, `esta=P`; item en `auto_prestac_items_remotas` con prestacion `2 / 150103`, `nivel=2`, `recha=20`, `esta=P`.
- Datos para `6535288`: usuario `ffa_prest` tiene nivel `0`; `prestac_auto_niveles` para `2 / 150103` exige nivel `2`; cobertura `MASTER 2 / 2 / 150103` tiene `auto='S'`; `parametros.valida_nivel_webpos = 'S'`; no hay excepcion en `PRESTAC_NO_AUTO_PRESTAD` para prestador `52`.
- Causa de que `6535288` no pase a `recha=90`: en `prs_valida_of_auto_remotas`, la actualizacion a `90` para `02C` incluye condicion `AND recha IS NULL`; como el item ya tenia `recha=20` (`Prestacion sin convenio para este plan`), la validacion de nivel no pisa el rechazo existente. Referencia detectada: `/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/backend/procedures/prs_valida_of_auto_remotas.sql:1193`.
- Pedido pendiente al momento de compactar: analizar en SVN `branches/V25.08.001` si para transacciones remotas no Presmed la validacion `recha=90` por nivel de usuario logueado SOLO aplica a `02C` y `02I`, y confirmar que el resto de validaciones no verifica nivel del usuario logueado. Ultima formulacion valida del usuario termina en punto; ignorar typo previo `logueado125`.
