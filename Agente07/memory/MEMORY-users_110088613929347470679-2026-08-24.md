# Memoria de conversación — Facundo Farias

## Datos

- **Fecha:** 24/08/2026
- **Usuario:** Facundo Farias
- **ID Google Chat:** `users/110088613929347470679`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 36
- **Inicio:** 08:57:11
- **Fin:** 16:40:03

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/9tgygkaaaae`

## Archivos revisados

- `f406528d-469d-4488-8e6a-07e1e6a5217d.jsonl`

---

## Transcripción completa

### [24/08/2026 08:57:11] Facundo Farias

hola

---

### [24/08/2026 08:58:35] Agente

Hola Facundo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [24/08/2026 09:53:09] Facundo Farias

Siguiendo los casos de transacciones mencionadas, necesito validar como obtener el recha=18 - Prestador inexistente

---

### [24/08/2026 09:53:55] Agente

Facundo, para validarlo necesito que me indiques el contexto operativo exacto:

- Repositorio o rama donde querés que revise la lógica.
- Profile de base de datos donde querés validar los datos.
- Si aplica, el XML o datos de la transacción que estás usando.

Sin eso no puedo confirmar con evidencia cómo forzar/obtener `recha=18 - Prestador inexistente`, porque depende de la lógica de validación y de los datos vigentes del ambiente.

---

### [24/08/2026 10:09:46] Facundo Farias

Siempre sobre la 25.08, base smc_prox_ver
Se puede usar webpos: http://10.194.0.15/standard/portal_prestadores/prox_ver/index.php
user ffa_prest password: ffa_prest
XML: http://127.0.0.1:8080/AutoRemWebAplication/TransactionServ?wsdl

<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ser="http://service.autorem.com/">
 <soapenv:Header/>
 <soapenv:Body>
 <ser:EjecutarTransaccion>
 <!--Optional:-->
 <mensajeXml>
 <![CDATA[
<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<Mensaje>
 <EncabezadoMensaje>
 <VersionMsj>1.0</VersionMsj>
 <NroReferencia>77667000</NroReferencia>
 <EsCentro>N</EsCentro>
 <TipoMsj>OL</TipoMsj>
 <TipoTransaccion>02Q</TipoTransaccion>
 <IdMsj>036747</IdMsj>
 <InicioTrx>
 <FechaTrx>20260820</FechaTrx>
 <HoraTrx>155800</HoraTrx>
 </InicioTrx>
 <Terminal>
 <TipoTerminal>PC</TipoTerminal>
 <NumeroTerminal></NumeroTerminal>
 </Terminal>
 <Validador>
 <NombreConcentrador>33707000509</NombreConcentrador>
 <NombreValidador>30699630450</NombreValidador>
 </Validador>
 <Financiador>
 <CodigoFinanciador></CodigoFinanciador>
 <CuitFinanciador></CuitFinanciador>
 </Financiador>

 <Prestador>
 <CuitPrestador>30519389297</CuitPrestador>
 <SucursalPrestador></SucursalPrestador>
 <CodigoPrestador>1</CodigoPrestador>
 </Prestador>


 </EncabezadoMensaje>
 <EncabezadoAtencion>
<Prescriptor>
 <ProvinciaPrescriptor></ProvinciaPrescriptor>
 <TipoPrescriptor></TipoPrescriptor>
 <NroMatriculaPrescriptor></NroMatriculaPrescriptor>
 <ApellidoPrescriptor>PAGPALTA</ApellidoPrescriptor>
 </Prescriptor>

 <Credencial>
 <NumeroCredencial></NumeroCredencial>
 <ModoIngreso>M</ModoIngreso>
 <CodigoSeguridad></CodigoSeguridad>
 </Credencial>

 <Beneficiario>
 <Entidad>2</Entidad>
 <Contra>38690954</Contra>
 <Inte>00</Inte>
 </Beneficiario>

 <Atencion>
 <FechaAtencion>20260820</FechaAtencion>
 </Atencion>
<Preautorizacion>
 <CodigoPreautorizacion></CodigoPreautorizacion>
 <Sucursal></Sucursal>
 </Preautorizacion>
 <Diagnostico>
 <CodDiagnostico>I15</CodDiagnostico>
 </Diagnostico>
 </EncabezadoAtencion>
 <DetalleProcedimientos>
 <NroItem>1</NroItem>
 <CodPrestacion>110101</CodPrestacion>
 <Nomenclador>1</Nomenclador>
 <TipoPrestacion>1</TipoPrestacion>
 <ArancelPrestacion>0</ArancelPrestacion>
 <CantidadSolicitada>1</CantidadSolicitada>
 </DetalleProcedimientos>

</Mensaje>
]]>


 </mensajeXml>
 </ser:EjecutarTransaccion>
 </soapenv:Body>
</soapenv:Envelope>

---

### [24/08/2026 10:13:02] Agente

Facundo, para obtener `recha=18 - Prestador inexistente` en `02Q` sobre `SMC_Prox_Ver`, tenés que enviar un prestador que no pueda resolverse contra `prestadores`.

Con tu XML actual no sale `18` porque el CUIT `30519389297` existe y corresponde al prestador `52`:

```text
prestad 52 | cuit 30519389297 | SANATORIO MODELO DE CASEROS
```

Además, en las transacciones `02Q` ya registradas con `aufree/referencia = 77667000`, quedó `efe_sana = 52`, o sea el prestador fue resuelto.

La forma más directa para forzar el `18` es cambiar el bloque `Prestador` así:

```xml
<Prestador>
  <CuitPrestador>30999999999</CuitPrestador>
  <SucursalPrestador></SucursalPrestador>
  <CodigoPrestador></CodigoPrestador>
</Prestador>
```

También usaría un `NroReferencia` nuevo para no mezclarlo con pruebas anteriores, por ejemplo:

```xml
<NroReferencia>77667018</NroReferencia>
```

Evidencia técnica: en `pri_transac_remotas_qui`, para `02Q`, si no encuentra prestador por `efe_prestad` o por `cuit_prestad`, ejecuta:

```sql
SELECT @error = 18 --Prestador inexistente

UPDATE auto_remotas
SET esta = 'R',
    recha = @error
WHERE transac = @transac
```

No pude ejecutar el SOAP desde este entorno porque `http://127.0.0.1:8080/...` no está escuchando acá, pero la validación por código y base confirma el criterio.

---

### [24/08/2026 10:29:29] Facundo Farias

Portal de prestadores necesito cargar un prestador existente para ingresar, o hay otra forma que veas de lograr el recha?

---

### [24/08/2026 10:30:24] Agente

Sí: para entrar al Portal necesitás un prestador existente, porque WebPOS toma el prestador desde la sesión (`$_SESSION['prestad']`) y lo manda como `efe_prestad`.

Para lograr `recha=18`, la vía más limpia no es WebPOS sino SOAP. En `02Q`, el rechazo 18 se da cuando `efe_prestad` viene nulo y el sistema intenta resolver por `cuit_prestad`, pero ese CUIT no existe en `prestadores`.

Usá esto:

```xml
<Prestador>
  <CuitPrestador>30999999999</CuitPrestador>
  <SucursalPrestador></SucursalPrestador>
  <CodigoPrestador></CodigoPrestador>
</Prestador>
```

Clave: dejá `CodigoPrestador` vacío. Si mandás un código de prestador inventado, el SP puede no caer en la condición `@var_efe_prestad IS NULL`, porque el valor ya viene cargado aunque no exista.

Conclusión: Portal/WebPOS requiere prestador existente para operar. Para este rechazo específico, usá SOAP con CUIT inexistente y sin código de prestador.

---

### [24/08/2026 11:01:03] Facundo Farias

las transac que uso son 01A,01AB,01C, 02A, 02C, 02D, 02Q,02I

---

### [24/08/2026 11:02:05] Agente

Facundo, para esas transacciones usá SOAP, no Portal, si querés forzar `recha=18`.

Validé en `SMC_Prox_Ver` que existen estas transacciones:

```text
01A, 01B, 01C, 02A, 02C, 02D, 02I, 02Q
```

`01AB` no existe como `tran_tipo`; entiendo que te referís a `01B`.

Criterio para todas: enviar prestador inexistente por CUIT y dejar vacío el código:

```xml
<Prestador>
  <CuitPrestador>30999999999</CuitPrestador>
  <SucursalPrestador></SucursalPrestador>
  <CodigoPrestador></CodigoPrestador>
</Prestador>
```

Esto aplica especialmente para:

```text
01A, 01B, 01C, 02A, 02C, 02D, 02I
```

Para `02Q` también aplica, como vimos antes, pero ahí el SP específico es `pri_transac_remotas_qui`.

Portal/WebPOS no te sirve para esta prueba porque exige prestador existente en sesión y manda ese prestador como `efe_prestad`. Con eso evitás justamente la condición que dispara `18`.

---

### [24/08/2026 12:12:16] Facundo Farias

01c responde recha 733, transac: 6524947, por que motivo?

wml:
<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ser="http://service.autorem.com/">
 <soapenv:Header/>
 <soapenv:Body>
 <ser:EjecutarTransaccion>
 <!--Optional:-->
 <mensajeXml>
 <![CDATA[
<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<Mensaje>
 <EncabezadoMensaje>
 <VersionMsj>1.0</VersionMsj>
 <NroReferencia>77667790</NroReferencia>
 <EsCentro>N</EsCentro>
 <TipoMsj>OL</TipoMsj>
 <TipoTransaccion>02A</TipoTransaccion>
 <IdMsj>036747</IdMsj>
 <InicioTrx>
 <FechaTrx>20260820</FechaTrx>
 <HoraTrx>155800</HoraTrx>
 </InicioTrx>
 <Terminal>
 <TipoTerminal>PC</TipoTerminal>
 <NumeroTerminal></NumeroTerminal>
 </Terminal>
 <Validador>
 <NombreConcentrador>33707000509</NombreConcentrador>
 <NombreValidador>30699630450</NombreValidador>
 </Validador>
 <Financiador>
 <CodigoFinanciador></CodigoFinanciador>
 <CuitFinanciador></CuitFinanciador>
 </Financiador>

 <Prestador>
 <CuitPrestador>30999999999</CuitPrestador>
 <SucursalPrestador></SucursalPrestador>
 <CodigoPrestador>1</CodigoPrestador>
 </Prestador>


 </EncabezadoMensaje>
 <EncabezadoAtencion>
<Prescriptor>
 <ProvinciaPrescriptor></ProvinciaPrescriptor>
 <TipoPrescriptor></TipoPrescriptor>
 <NroMatriculaPrescriptor></NroMatriculaPrescriptor>
 <ApellidoPrescriptor>PAGPALTA</ApellidoPrescriptor>
 </Prescriptor>

 <Credencial>
 <NumeroCredencial></NumeroCredencial>
 <ModoIngreso>M</ModoIngreso>
 <CodigoSeguridad></CodigoSeguridad>
 </Credencial>

 <Beneficiario>
 <Entidad>2</Entidad>
 <Contra>38690954</Contra>
 <Inte>00</Inte>
 </Beneficiario>

 <Atencion>
 <FechaAtencion>20260820</FechaAtencion>
 </Atencion>
<Preautorizacion>
 <CodigoPreautorizacion></CodigoPreautorizacion>
 <Sucursal></Sucursal>
 </Preautorizacion>
 <Diagnostico>
 <CodDiagnostico>I15</CodDiagnostico>
 </Diagnostico>
 </EncabezadoAtencion>
 <DetalleProcedimientos>
 <NroItem>1</NroItem>
 <CodPrestacion>110101</CodPrestacion>
 <Nomenclador>1</Nomenclador>
 <TipoPrestacion>1</TipoPrestacion>
 <ArancelPrestacion>0</ArancelPrestacion>
 <CantidadSolicitada>1</CantidadSolicitada>
 </DetalleProcedimientos>

</Mensaje>
]]>


 </mensajeXml>
 </ser:EjecutarTransaccion>
 </soapenv:Body>
</soapenv:Envelope>

---

### [24/08/2026 12:14:14] Agente

Facundo, el `733` sale por este motivo:

```text
733 = Círculo sin categorías habilitadas
```

En la transacción `6524947` quedó:

```text
tipo_transac = 01C
efe_tipo     = C
efe_circu    = NULL
efe_prestad  = NULL
cuit_prestad = 30999999999
recha cabecera = 733
recha item     = 18
```

La lógica que pisa la cabecera está en `prs_valida_of_auto_prestac`: si el efector queda como tipo círculo (`efe_tipo = 'C'`) y no tiene categoría (`efe_circu IS NULL`), setea `recha = 733`.

Además, con la misma referencia `77667790` se generaron varias transacciones:

```text
6524944  02Q  R  18
6524945  01A  R  18
6524946  01B  R  18
6524947  01C  R  733
```

El XML que pegaste tiene dos puntos a corregir para buscar `18` puro en `01C`:

```xml
<TipoTransaccion>02A</TipoTransaccion>
...
<CodigoPrestador>1</CodigoPrestador>
```

Para la prueba de prestador inexistente, usá:

```xml
<TipoTransaccion>01C</TipoTransaccion>
...
<Prestador>
  <CuitPrestador>30999999999</CuitPrestador>
  <SucursalPrestador></SucursalPrestador>
  <CodigoPrestador></CodigoPrestador>
</Prestador>
```

El `CodigoPrestador=1` mete ruido porque el prestador 1 existe y puede hacer que el flujo derive por validaciones de convenio/círculo en vez de quedarse solamente con prestador inexistente.

---

### [24/08/2026 12:20:33] Facundo Farias

Porque las transac si responden en 18 y 01C y 02A con 733

---

### [24/08/2026 12:21:11] Agente

Porque no todas cortan el flujo en el mismo punto.

Las que responden `18` se quedan en la validación inicial de prestador inexistente:

```text
18 = Prestador inexistente
```

En cambio `01C` y `02A` siguen hasta validación de convenio/prestación. Ahí el sistema ve estos valores:

```text
efe_tipo  = C
efe_circu = NULL
```

y pisa el rechazo con:

```text
733 = Círculo sin categorías habilitadas
```

La evidencia en tus transacciones con referencia `77667790`:

```text
6524944  02Q  recha 18   efe_tipo C  efe_circu NULL
6524945  01A  recha 18   efe_tipo C  efe_circu NULL
6524946  01B  recha 18   efe_tipo C  efe_circu NULL
6524947  01C  recha 733  efe_tipo C  efe_circu NULL
6524948  02A  recha 733  efe_tipo C  efe_circu NULL
6524949  01C  recha 733  efe_tipo C  efe_circu NULL
6524950  01A  recha 18   efe_tipo C  efe_circu NULL
```

El punto técnico está en `prs_valida_of_auto_prestac`: si `@var_efe_tipo = 'C'` y la categoría viene nula, setea `733`.

Conclusión: `01C` y `02A` no están fallando primero por inexistencia pura del prestador, sino por la derivación del flujo hacia validación de convenio/prestación con efector tipo círculo sin categoría.

---

### [24/08/2026 12:22:03] Facundo Farias

por que toman el flujo de circulo?

---

### [24/08/2026 12:22:39] Agente

Porque en el XML estás mandando:

```xml
<CodigoPrestador>1</CodigoPrestador>
```

Y en `SMC_Prox_Ver` el prestador `1` existe y es tipo círculo:

```text
prestad = 1
cuit    = 30517007680
tipo    = C
nombre  = CIRC.MED.GRAL.RODRIGUEZ
```

Entonces aunque el CUIT que mandás sea inexistente:

```xml
<CuitPrestador>30999999999</CuitPrestador>
```

la transacción queda marcada con:

```text
efe_tipo  = C
efe_circu = NULL
```

Después, `01C` y `02A` entran a validación de convenio/prestación. Ahí `prs_valida_of_auto_prestac` evalúa:

```sql
IF @var_efe_tipo = 'C' AND @var_cate IS NULL
```

y por eso devuelve:

```text
733 = Círculo sin categorías habilitadas
```

Para que no tome flujo de círculo, dejá vacío el código de prestador:

```xml
<Prestador>
  <CuitPrestador>30999999999</CuitPrestador>
  <SucursalPrestador></SucursalPrestador>
  <CodigoPrestador></CodigoPrestador>
</Prestador>
```

---

### [24/08/2026 12:23:11] Facundo Farias

tengo asi:
 <Prestador>
 <CuitPrestador>30999999999</CuitPrestador>
 <SucursalPrestador></SucursalPrestador>
 <CodigoPrestador></CodigoPrestador>
 </Prestador>

---

### [24/08/2026 12:24:08] Agente

Sí, con ese XML el `CodigoPrestador` ya no es el problema.

Lo que está pasando ahora es esto: aunque el código venga vacío, el alta de `auto_remotas` queda igual con:

```text
efe_tipo = C
efe_circu = NULL
efe_prestad = NULL
cuit_prestad = 30999999999
```

En `pri_auto_remotas`, cuando viene `cuit_prestad`, intenta resolverlo:

```sql
SELECT @var_prestad = prestad,
       @efe_tipo = tipo
FROM PRESTADORES
WHERE cuit = @var_cuit
AND baja_fecha IS NULL
```

Como `30999999999` no existe, no encuentra prestador, deja `efe_prestad = NULL`, pero no limpia `efe_tipo`. Ese `efe_tipo` ya viene como `C` desde el armado de la transacción remota.

Por eso:

```text
01A / 01B / 02Q cortan antes y quedan en 18
01C / 02A siguen a convenio/prestación y terminan en 733
```

El `733` no viene por el código de prestador, viene porque el flujo posterior ve:

```text
efe_tipo = C
efe_circu = NULL
```

y aplica:

```text
Círculo sin categorías habilitadas
```

Conclusión: tu XML ya está bien para probar prestador inexistente, pero para `01C` y `02A` el sistema pisa la cabecera con `733` por una validación posterior de círculo/categoría. El item sí quedó con `18`; la cabecera es la que se sobreescribe con `733`.

---

### [24/08/2026 12:25:03] Facundo Farias

como debería estar el codigo para que responda por 18

---

### [24/08/2026 12:25:31] Agente

Para que `01C` y `02A` respondan `18`, el código debería priorizar “prestador inexistente” antes de validar círculo/categoría.

Hoy llega así:

```text
efe_tipo = C
efe_circu = NULL
efe_prestad = NULL
cuit_prestad = 30999999999
```

y cae en:

```sql
IF @var_efe_tipo = 'C' AND @var_cate IS NULL
  SELECT @error = 733
```

La lógica correcta debería cortar antes con algo de este estilo en `prs_valida_of_auto_prestac`, antes del bloque que asigna `733`:

```sql
IF @var_efe_prestad IS NULL
AND ISNULL(@var_cuit_prestad, '') <> ''
AND NOT EXISTS (
  SELECT 1
  FROM prestadores
  WHERE cuit = @var_cuit_prestad
  AND baja_fecha IS NULL
)
BEGIN
  SELECT @error = 18

  UPDATE auto_remotas
  SET recha = @error,
      esta = 'R'
  WHERE transac = @transac

  UPDATE auto_prestac_items_remotas
  SET recha = @error,
      esta = 'R'
  WHERE transac = @transac
  AND baja_fecha IS NULL

  RETURN
END
```

Conceptualmente: si no se resolvió prestador por CUIT, no debería evaluar si es círculo sin categoría. Primero debe devolver `18 - Prestador inexistente`.

También sería sano revisar el origen de `efe_tipo = C`, porque si no hay prestador encontrado, ese campo no debería quedar informado como círculo.

---

### [24/08/2026 12:36:20] Facundo Farias

TRASNAC 6524960 RESPONDE RECHA 25 El sanatorio no tiene definida la prestacion para los convenios del plan

---

### [24/08/2026 12:37:45] Agente

Facundo, revisé `6524960` y no está respondiendo `25` desde la base ni desde el SP de salida.

Estado real de la transacción:

```text
transac      6524960
tipo_transac 02D
cabecera     recha 733
item         recha 18
prestac      420101
nomen        2
cuit         30999999999
efe_tipo     C
efe_circu    NULL
```

El SP de salida `prs_auto_rem_salida_activia @transac = 6524960` devuelve:

```text
aurest 31
auleyp Círculo sin categorías habilitadas
auinc1msg Círculo sin categorías habilitadas
```

O sea, para esa transacción el motivo efectivo es:

```text
733 = Círculo sin categorías habilitadas
```

El `25 = El sanatorio no tiene definida la prestación para los convenios del plan` existe, pero no es el rechazo que quedó en `6524960`. Ese `25` sale en validación de convenio cuando el flujo trata la prestación como sanatorio y no encuentra definición de convenio/plan para esa prestación. En este caso, antes queda tomado por el problema de círculo sin categoría, igual que veníamos viendo con `01C/02A/02D`.

---

### [24/08/2026 12:49:30] Facundo Farias

Pre-compaction memory flush. Store durable memories only in memory/2026-08-24.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-24.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-24-HHMM.md); always use the canonical 2026-08-24.md filename. If nothing to store, reply with NO_REPLY.
Current time: Monday, August 24th, 2026 - 12:49 PM (America/Buenos_Aires)
Reference UTC: 2026-08-24 15:49 UTC

---

### [24/08/2026 12:50:20] Agente

NO_REPLY

---

### [24/08/2026 12:50:20] Facundo Farias

Es la transac 6524958

---

### [24/08/2026 12:51:13] Agente

Correcto, `6524958` sí responde `25`.

La diferencia es que esa transacción es `02I`:

```text
transac      6524958
tipo_transac 02I
recha        25
item recha   25
plan         MASTER 10
prestac      420101
nomen        2
efe_tipo     C
efe_prestad  NULL
cuit         30999999999
```

En `02I` el flujo no queda cortado por `18`. Entra por `prs_valida_of_auto_remotas` y termina validando convenio de internación/sanatorio. Ahí, en `prs_valida_of_convenio`, cuando el contexto es sanatorio (`@sanatorio = 'T'`) y no encuentra la prestación definida para los convenios del plan, asigna:

```text
25 = El sanatorio no tiene definida la prestacion para los convenios del plan
```

Por eso:

```text
01A / 01B / 02Q -> cortan antes con 18
01C / 02A / 02D -> caen en círculo sin categoría: 733
02I -> cae en convenio de sanatorio/internación: 25
```

Para que `02I` devuelva `18`, el código debería validar prestador inexistente antes de entrar a la validación de convenio de internación, igual que veníamos planteando: si `cuit_prestad` no existe y `efe_prestad` queda `NULL`, cortar con `18` y no seguir al convenio.

---

### [24/08/2026 13:19:07] Facundo Farias

o sea 02I no toma que cuit_prestad no existe y efe_prestad queda NULL pero sigue validando al convenio.

---

### [24/08/2026 13:19:15] Agente

Exacto, Facundo.

En `02I` pasa esto:

```text
cuit_prestad = 30999999999
efe_prestad  = NULL
```

Eso representa prestador inexistente, pero el flujo no corta ahí con `18`. Sigue por `prs_valida_of_auto_remotas`, entra a validación de internación/sanatorio y termina en convenio. Como no encuentra la prestación definida para el plan/sanatorio, responde:

```text
25 = El sanatorio no tiene definida la prestacion para los convenios del plan
```

Entonces el problema de precedencia es ese: para `02I`, la validación de convenio está ganándole a la validación de prestador inexistente. Debería cortar antes con `18` cuando el CUIT no existe y `efe_prestad` queda `NULL`.

---

### [24/08/2026 14:50:53] Facundo Farias

TRANSAC 6524975, la prestac 340242 no rechaza por recha=1253 y 180110 no rechaza 90

---

### [24/08/2026 14:53:36] Agente

Facundo, para `6524975` quedó así:

```text
Cabecera 02C: esta P, recha 20

Items:
420101  A  recha NULL
180110  A  recha NULL
340242  P  recha 20
```

`340242` no queda con `1253` aunque la regla aplica. Validé directo:

```text
Afiliado sexo: M
Prestación 340242 sexo requerido: F
prs_prestacion_prop devuelve: 1253
```

Pero en el flujo real la prestación primero cae en:

```text
20 = Prestacion sin convenio para este plan
```

Y como `20` y `1253` tienen el mismo orden de rechazo:

```text
fn_recha_orden(20)   = 1
fn_recha_orden(1253) = 1
```

`prs_prestacion_prop` no pisa el `20` por `1253`. Por eso queda `20`.

Para `180110`, no rechaza `90` porque en cobertura del plan `SENIOR 10` está:

```text
prestac 180110
auto = N
```

Aunque el item terminó con `nivel = 2`, la condición para `90` exige que la prestación requiera autorización por cobertura. Como `auto = N`, no entra al rechazo:

```text
90 = La autorizacion de esta prestacion depende de un nivel superior
```

Resumen:

```text
340242 no da 1253 porque antes queda 20 y ambos tienen misma prioridad.
180110 no da 90 porque su cobertura tiene auto = N para SENIOR 10.
```

---

### [24/08/2026 14:57:29] Facundo Farias

OK genere 6524977 ahora queda el recha y sigue le recha 20 para 340242

---

### [24/08/2026 14:58:07] Agente

Sí, `6524977` mejoró una parte:

```text
180110 -> P / recha 90
```

Pero `340242` sigue igual:

```text
340242 -> P / recha 20
```

Motivo: `340242` primero cae en `20 = Prestacion sin convenio para este plan`. Después, cuando el flujo intenta validar propiedades de la prestación, ya entra con orden de rechazo previo `1`.

Validé la diferencia directa:

```text
prs_prestacion_prop con orden 0 -> 1253
prs_prestacion_prop con orden 1 -> 1254
```

O sea, `1253` solo aparece si todavía no hay rechazo previo de la misma prioridad. Como `20` ya quedó antes y tiene la misma prioridad/orden, no lo pisa por `1253`.

En la transacción real:

```text
Cabecera 6524977: P / 20
420101: A / NULL
180110: P / 90
340242: P / 20
```

Para que `340242` dé `1253`, necesitás evitar que antes caiga en `20`: tiene que existir cobertura/convenio válido para `340242` en `SENIOR 10`, así llega a la validación de sexo sin un rechazo previo que la bloquee.

---

### [24/08/2026 15:19:39] Facundo Farias

Queda flujo distinto a presmed para las mismas condiciones. Siempre 25.08. Podés simular lo mismo para una autorización de prestaciones ?

---

### [24/08/2026 15:27:00] Agente

Facundo, sí: lo simulé para autorización de prestaciones tipo `P`, sin grabar nada en base.

Resultado sobre `SMC_Prox_Ver` / 25.08, mismas condiciones:

```text
Afiliado: 2 / 344939918 / 00
Plan: SENIOR 10
Efector: prestad 52, lugar 1
Fecha: 20260824
Prestaciones: nomen 2
```

```text
340242 -> prs_valida_of_cobertura = 1253
180110 -> prs_valida_of_cobertura = 0, cobertura 51610, auto = S
```

Y validando convenio directo para `340242`:

```text
340242 H -> 20
340242 P -> 20
```

Conclusión: en Presmed autorización normal, `340242` debería cortar antes por `1253`, porque `wf_valida_prestacion` entra primero por validación de prestación/cobertura y si hay error hace `RETURN FALSE`. Recién después llega a convenio.

Referencias:
- [w_clase_auto.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/exe/Source%20Infomedical/w_clase_auto.srw:10793): llama `val.of_cobertura(...)`
- [w_clase_auto.srw](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/exe/Source%20Infomedical/w_clase_auto.srw:10798): si `li_error > 0`, setea `recha` y retorna
- [prs_valida_of_cobertura.sql](/home/desarrollo-ia-07/.openclaw/workspace/svn/ffarias-V25.08.001_PROX/backend/procedures/prs_valida_of_cobertura.sql:145): llama `prs_prestacion_prop`

Entonces la diferencia con remotas queda confirmada: remotas puede dejar `20` por convenio, pero Presmed para autorización de prestaciones debería priorizar `1253` para `340242`. Para `180110`, el `90` depende del nivel del usuario, porque la prestación tiene `prestac_auto_niveles.nivel = 2`.

---

### [24/08/2026 16:37:10] Facundo Farias

De las RANSAC>=6524964 resumi el estado y que rechazo tuvo cada item ya que esa información la deberá ver un desarrollador. El esperado debía ser recha 90 y 1253

---

### [24/08/2026 16:40:03] Agente

Facundo, resumen para desarrollo sobre `SMC_Prox_Ver`, transacciones `>= 6524964`.

**Cabeceras**
```text
6524964 02C P recha 20  usuario sa
6524965 02A R recha 20  usuario sa
6524966 01A A recha NULL usuario sa
6524967 01B R recha 1253 usuario sa
6524968 01C R recha 20  usuario sa
6524969 02D R recha 20  usuario sa
6524970 02I R recha 27  usuario sa
6524971 01A A recha NULL usuario ffa_prest, sin items vigentes consultados
6524972 01B R recha 1253 usuario ffa_prest
6524973 01C R recha 20  usuario ffa_prest
6524974 02A R recha 20  usuario ffa_prest
6524975 02C P recha 20  usuario ffa_prest
6524976 02D R recha 20  usuario ffa_prest
6524977 02C P recha 20  usuario ffa_prest
```

**Ítems relevantes**
```text
6524964 02C
420101 A NULL
180110 A NULL, nivel 2
340242 P 20

6524965 02A
420101 A NULL
180110 A NULL
340242 R 20

6524966 01A
420101 A NULL
180110 A NULL
340242 A NULL

6524967 01B
420101 A NULL
180110 A NULL
340242 R 1253

6524968 01C
420101 A NULL
180110 A NULL
340242 R 20

6524969 02D
420101 A NULL
180110 A NULL
340242 R 20

6524970 02I
420101 R 27
180110 R 27
340242 R 27

6524972 01B
420101 A NULL
180110 A NULL
340242 R 1253

6524973 01C
420101 A NULL
180110 A NULL
340242 R 20

6524974 02A
420101 A NULL
180110 A NULL
340242 R 20

6524975 02C
420101 A NULL
180110 A NULL, nivel 2
340242 P 20

6524976 02D
420101 A NULL
180110 A NULL
340242 R 20

6524977 02C
420101 A NULL
180110 P 90, nivel 2
340242 P 20
```

El esperado era `180110 -> 90` y `340242 -> 1253`. Solo se cumple parcialmente en `6524977`: `180110` queda `P/90`, pero `340242` sigue quedando `P/20` en lugar de `1253`. En `01B` sí aparece `340242 -> 1253`, concretamente en `6524967` y `6524972`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
