# 2026-06-09 - Memoria operativa

## Redmine 29886 - Analisis sin modificaciones

- El interlocutor pidio analizar Redmine `29886`, incluyendo contenido, imagenes y adjuntos si existian.
- Restricciones explicitas del interlocutor: si hacia falta revisar codigo, usar solamente la working copy SVN `dgiuseppe-2026.04`; si hacia falta consultar base de datos, usar solamente el profile/base `Test_Britanico` SQL Server; no modificar repositorios ni profiles/base; no revisar otros repositorios ni otras bases.
- Se aplico la preferencia operativa recordada para usuario OMAR: para Redmine se uso la credencial Redmine local registrada para `ogomez`. No exponer API keys ni secretos.
- Resultado funcional del ticket: reporta error al modificar una autorizacion de Situacion Terapeutica. Autorizacion `1835115`, producto indicado en evidencia `NODIS`, cambio de `% Dto`, mensaje: `El valor del campo observacion no es valido (-1). Por favor verifique.`
- Adjuntos relevados: una imagen PNG y un video MP4.
- Imagen PNG: confirma ventana `Situaciones terapeuticas`, autorizacion `1835115`, producto `NODIS`, `% Dto 50.00`, y mensaje por observacion `-1`.
- Video MP4: muestra que se consulta la autorizacion, se modifica el `% Dto` del segundo renglon de `50.00` a `60.00`, se presiona `Aceptar` y aparece la validacion.
- Evidencia tecnica en repo permitido `dgiuseppe-2026.04`:
  - `w_clase_auto.srw`, linea aproximada `3008`: el mensaje sale de la validacion de `dw_auto.object.obser[1] = '-1'`.
  - `wp_auto_situ_terap.srw`, linea aproximada `1049`: al cambiar `porce`, solo valida que no supere 100 y marca el item con estado `P`; no asigna `-1` a observacion.
  - `w_clase_auto.srw`, linea aproximada `916`: el guardado copia la observacion de cabecera hacia la transaccion remota y luego llama al circuito `of_auto_remotas` / `prs_valida_of_auto_remotas`.
- Evidencia en base permitida `Test_Britanico`:
  - La autorizacion `1835115` existe, pero no coincide con el caso del video.
  - Cabecera `obser` y `obser2`: vacias.
  - Item vigente observado: `LOSARTAN`, `% 70`.
  - Segundo item observado: dado de baja y producto `ROSUSTATIN`, no `NODIS`.
  - `NODIS` existe como producto, pero no estaba asociado a esa autorizacion en `Test_Britanico` al momento del analisis.
- Conclusion tecnica comunicada: el error no lo provoca directamente la edicion del `% Dto`; la edicion dispara el `Aceptar`, y alli se valida la observacion de cabecera. El `-1` ya viene cargado en el buffer/cabecera o se genera en el circuito previo de recuperacion/generacion en el ambiente donde se reprodujo el video.
- Limitacion declarada: con la base autorizada `Test_Britanico` no se puede reproducir exactamente porque los datos no coinciden con la evidencia adjunta.
- Estado: no se modifico repositorio, no se modifico base, y no se usaron repositorios ni profiles fuera de los indicados.
