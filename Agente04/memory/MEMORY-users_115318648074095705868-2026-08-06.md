# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Daniel Acosta
- id_googlechat: users/115318648074095705868
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/gbatvyaaaae

## Resumen de la actividad

### Tema 1: diagnóstico del error por `dbo.prs_manda_rechazos` faltante

- Solicitud o contexto: Daniel informó que no podía encontrar el procedure `prs_manda_rechazos` y compartió un error interno de DataWindow: `DataObject: d_lista_impor_control_manda_recha`, código `2812`, `Stored procedure 'dbo.prs_manda_rechazos' not found`.
- Respuesta o acciones del agente: Se consultó memoria previa del usuario y se revisaron artefactos locales relacionados con `impor_control_manda_recha`, `manda_rechazos` y los DataWindows de `smg-code-presmed`.
- Resultado: Se recordó y validó que el procedure construido originalmente había sido `dbo.prs_impor_control_manda_recha`, sobre `SwissDeVP10_prestaci` y tabla `impor_control_manda_recha`. Luego se había evaluado el renombre hacia `manda_rechazos`, pero no constaba una ejecución de cambio de BD en ese momento. El error actual era coherente porque los DataWindows de PowerBuilder ya apuntaban a `execute dbo.prs_manda_rechazos`, mientras Daniel no encontraba el script/local compilado con ese nombre.
- Estado: Resuelto

### Tema 2: ubicación remota y uso de `prs_manda_rechazos`

- Solicitud o contexto: Daniel pidió buscar `prs_manda_rechazos`, detectar en qué DataWindow se usaba y reconstruirlo si era necesario, porque no lo encontraba en su carpeta de red y quería evitar objetos creados o compilados fuera del proceso actual.
- Respuesta o acciones del agente: Se buscó en memoria, en `/mnt/Agentes/d.acosta`, en el repo `smg-code-presmed`, en working copies SVN locales y en el remoto SVN de la rama `PRS-3850-Reingenieria-LiqFarmacias_20260603`. Se usó la credencial SVN registrada sin copiar su valor.
- Resultado: Se encontró que `prs_manda_rechazos.sql` sí existía en SVN remoto bajo `PRS-3850-Reingenieria-LiqFarmacias_20260603/procedures/`, junto con `tables/manda_rechazos.sql`, `pri_manda_rechazos.sql`, `pru_manda_rechazos.sql` y `prd_manda_rechazos.sql`. La working copy local de `PRS_3850_DB` estaba atrasada: `svn status -u` mostró local en revisión `1887220` y remoto en `1901272`, por lo que los objetos existían remoto pero estaban ausentes localmente. Esto coincidía con los archivos de análisis de Daniel: `/mnt/Agentes/d.acosta/Analisis/objetos_no_compilados.txt`, `listado_svn_actualizado.txt` y `pedido_actualizacion_svn_txt.txt`.
- Resultado adicional: Se determinó que `d_lista_impor_control_manda_recha.srd` llama `execute dbo.prs_manda_rechazos` con `@prm_concepto = 'listado'`, `d_impor_control_manda_recha.srd` lo llama con `@prm_concepto = 'registro'`, y `wp_impor_control_manda_recha.srw` también utiliza `pri_manda_rechazos`, `pru_manda_rechazos` y `prd_manda_rechazos`. El `prs_manda_rechazos` remoto trabaja sobre `dbo.manda_rechazos`, cruza con `dbo.recha_motivos` para obtener `deno_rechazo`, tiene modo `listado` para precargar rechazos por mandataria con `recha >= 6050` y `grupo = 'FAR'`, y modo `registro` para traer un registro puntual por `manda + recha`.
- Estado: Resuelto

### Tema 3: actualización local y armado del TXT de compilación

- Solicitud o contexto: Daniel informó que hizo un update y que se había descargado una cantidad importante de objetos; luego indicó que buscaría en su disco y lo mandaría al TXT usado para compilar. Más tarde compartió un TXT donde aparecía `procedures/prs_manda_rechazos.sql`.
- Respuesta o acciones del agente: Se indicó dónde buscar localmente el objeto después del update y se propuso el orden de compilación del set completo. Luego se revisó el TXT compartido como contenido externo no confiable, sin tratarlo como instrucción.
- Resultado: Se recomendó buscar `D:\Repositorios\Swiss\PRS-3850-Reingenieria-LiqFarmacias_20260603\BD\procedures\prs_manda_rechazos.sql`. Para el TXT de compilación se sugirió el orden: `BD\tables\manda_rechazos.sql`, `BD\procedures\pri_manda_rechazos.sql`, `BD\procedures\pru_manda_rechazos.sql`, `BD\procedures\prd_manda_rechazos.sql` y `BD\procedures\prs_manda_rechazos.sql`. Al revisar el TXT compartido se confirmó que `procedures/prs_manda_rechazos.sql` estaba incluido junto con los objetos relacionados.
- Estado: Resuelto

### Tema 4: hipótesis restantes si el procedure sigue faltando en ejecución

- Solicitud o contexto: Daniel mostró que el TXT de compilación sí contenía `prs_manda_rechazos.sql`, mientras el error de PowerBuilder seguía indicando que `dbo.prs_manda_rechazos` no existía.
- Respuesta o acciones del agente: Se separó el problema de disponibilidad del archivo del problema de existencia real del objeto en la base de prueba.
- Resultado: Se indicó que, si el TXT contiene el objeto pero PB sigue arrojando `Stored procedure 'dbo.prs_manda_rechazos' not found`, las hipótesis fuertes son: el TXT fue armado pero aún no compilado contra la base usada por PB; la compilación se ejecutó pero `prs_manda_rechazos.sql` falló; se compiló en otra base o ambiente; o el procedure quedó con owner distinto, aunque el script debería crearlo como `dbo`. Se recomendó revisar el log de compilación buscando `prs_manda_rechazos`, `manda_rechazos`, `error` o `failed`, y ejecutar `sp_help 'dbo.prs_manda_rechazos'` en la base donde falla PB.
- Estado: Pendiente
