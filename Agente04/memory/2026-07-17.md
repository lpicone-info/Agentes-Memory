
## 2026-07-17 13:10 UTC - SwissDeVP10_prestaci / TAREA_29420_MODULO_FARMACIA

- Usuario: Julian (usar nombre si el contexto estructurado lo confirma). Conversacion en Google Chat directo.
- Repo usado en consultas: `/home/luispicone/.openclaw/workspace/git/smg-code-presmed`, rama local `TAREA_29420_MODULO_FARMACIA`.
- El formulario buscado como "Homologacion de Productos de farmacia" no aparece con ese titulo literal. El menu abre `wp_auto_farma`, titulo real: "Productos de farmacia para internacion domiciliaria".
- Tablas principales de `wp_auto_farma`: `auto_farma` (cabecera/maestro: `medides`, `deno`, `medi_descar`, `alta_fecha`, `baja_fecha`) y `auto_farma_valores` (valores/precios por producto y vigencia, vinculada por `medides`).
- Evidencia localizada: `ws_objects/prestaci.pbl.src/m_main.srm` abre `wp_auto_farma`; ventana en `ws_objects/autoriz4.pbl.src/wp_auto_farma.srw`; DataWindows `d_auto_farma.srd`, `d_auto_farma_valores.srd`, `d_lista_auto_farma.srd`.
- Alta en `wp_auto_farma`: boton heredado `cb_alta` setea `is_accion = "alta"` y dispara `ue_agregar` en `w_clase_abm.srw`; `ue_agregar` hace `dwa_datos.update()` y `commit`. El `sqlpreview` de `wp_auto_farma.srw` intercepta el alta y ejecuta `pri_auto_farma` para insertar en `auto_farma`, y recorre `dw_valores` ejecutando `pru_auto_farma_valores` para `auto_farma_valores` cuando `vigen_desde` tiene valor y `valor > 0`. `pru_auto_farma_valores` hace update y, si no encuentra fila, insert.
- Pantalla "Ingreso expediente", campo visible "Entidad": columna `expedientes.prepaga`. En `d_expedientes.srd` el label es "Entidad", el control se llama `prepaga` y mapea a `expedientes.prepaga`. Para farmacia tambien aplica en `d_expedientes_farma.srd`.
- Limitacion tecnica ya informada: no se valido directo contra DB `SwissDeVP10_prestaci` porque en el host no habia cliente `sqsh`/`isql`; conclusiones salieron del codigo PowerBuilder y scripts versionados.
