# Memoria curada diaria

- Fecha: 2026-08-05
- Usuario: Daniel Acosta
- id_googlechat: users/115318648074095705868
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/gbatvyaaaae

## Resumen de la actividad

### Tema 1: quitar SQL embebido en reporte de productos homologados

- Solicitud o contexto: Daniel pidió trabajar sobre la ventana `wp_rep_productos_homo.srw` de `smg-code-presmed` para quitar SQL embebido de DataWindows y ventana, centralizando lógica en el procedure `prs_produ_homolog_manda` con un parámetro de concepto.
- Respuesta o acciones del agente: Se modificaron objetos PowerBuilder del repositorio `smg-code-presmed` en la rama `TAREA_29420_MODULO_FARMACIA`. Se ajustaron DataWindows para llamar al procedure con conceptos y se agregó una función local de validación en la ventana.
- Resultado: Quedaron impactados `d_rep_productos_homo.srd`, `d_rep_productos_homo_imp.srd`, `d_rep_productos_homo_cab.srd` y `wp_rep_productos_homo.srw`. Los conceptos definidos fueron `listado`, `detalle` y `valida`. Se validó que no quedaran `retrieve="SELECT"` ni referencias directas a `FROM produ_homolog_manda` en los exports PB modificados, y `git diff --check` no reportó errores.
- Estado: Resuelto

### Tema 2: entrega de objetos en carpeta de Daniel

- Solicitud o contexto: Daniel indicó que los objetos debían dejarse en `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/wp_rep_productos_homo/` y luego avisó que había dejado el procedure `prs_produ_homolog_manda.SQL` en esa ubicación.
- Respuesta o acciones del agente: Se completó la carpeta de entrega con los objetos PB modificados y el procedure SQL evolucionado.
- Resultado: Se dejaron `prs_produ_homolog_manda.sql`, `wp_rep_productos_homo.srw`, `d_rep_productos_homo.srd`, `d_rep_productos_homo_cab.srd` y `d_rep_productos_homo_imp.srd`. El procedure quedó con `@prm_concepto VARCHAR(10)` y los conceptos `listado`, `detalle` y `valida`. Se validó SQL sin `;` y sin `RTRIM`, SQL ASCII con CRLF preservado, PB exports copiados idénticos a los modificados en repo y sin SQL embebido remanente para los casos tratados.
- Estado: Resuelto

### Tema 3: documentación de conocimiento sobre quitar SQL embebido

- Solicitud o contexto: Daniel pidió incorporar el arreglo como conocimiento en `LA_99_COMO_SE_HACE.TXT` para documentar el propósito de quitar SQL embebido.
- Respuesta o acciones del agente: Se agregó el conocimiento `[CONOCIMIENTO 000003] Quitar SQL Embebido de Ventanas y DataWindows Chicas` en `/mnt/Agentes/d.acosta/Recetas/LA_99_COMO_SE_HACE.TXT`.
- Resultado: Se documentó cómo detectar SQL embebido en DataWindows y ventanas, migrar DataWindows a stored procedures, evolucionar procedures con `@prm_concepto`, usar conceptos cortos como `listado`, `detalle` y `valida`, crear funciones `wf_...` representativas, preservar columnas, orden, aliases, layout, encoding y finales de línea, y el ejemplo concreto de `wp_rep_productos_homo` con `prs_produ_homolog_manda`.
- Estado: Resuelto

### Tema 4: criterio de formato para objetos entregables y control de calidad

- Solicitud o contexto: Daniel detectó que los objetos PB entregados tenían LF/UTF-8-BOM mientras el procedure estaba en CRLF/UTF-8, y explicó que los objetos para importar en PB22 deben quedar en formato Windows. Luego pidió registrar que todos los objetos entregables, para todos los proyectos y lenguajes, deben transformarse a `Windows (CRLF) | UTF-8`, y preguntó por un proceso Python de control de calidad.
- Respuesta o acciones del agente: Se reconoció el error de criterio de entrega: preservar formato era válido para el repo, pero no para objetos finales destinados a importar en PowerBuilder. Se agregó `[CONOCIMIENTO 000004] Formato Obligatorio de Objetos Entregables` y se creó un script `la99_control_calidad.py`.
- Resultado: El conocimiento quedó asentado en `/mnt/Agentes/d.acosta/Recetas/LA_99_COMO_SE_HACE.TXT`. El script `/mnt/Agentes/d.acosta/Recetas/la99_control_calidad.py` permite revisar una carpeta de objetos, con contexto y proyecto, y generar `_LA99_CONTROL_CALIDAD.txt`. Una prueba contra `wp_rep_productos_homo` revisó 5 archivos y detectó 8 hallazgos, principalmente los 4 objetos PB con LF y UTF-8-BOM.
- Estado: Resuelto

### Tema 5: modelo colaborativo de auditorías con varias IAs

- Solicitud o contexto: Daniel aclaró que él es el Programador Responsable y Auditor Final de todos los entregables, y describió un futuro flujo colaborativo donde La 99 es responsable de tarea, IA2 audita alcance y solución, IA3 ejecuta controles automáticos, e IA4 o Daniel realiza auditoría adicional o validación final.
- Respuesta o acciones del agente: Se validó el enfoque como una cadena de trabajo con responsabilidad, evidencia, control y revisión humana. Se propuso una nota más explícita para el control de calidad.
- Resultado: Quedó asentado el criterio de que el control automático no reemplaza la revisión funcional ni la auditoría final del Programador Responsable, sino que filtra objetos que deben volver a La 99 con el conocimiento exacto a aplicar.
- Estado: Informativo

### Tema 6: ajuste final del patrón técnico para SQL embebido

- Solicitud o contexto: Daniel repasó el caso concreto del SQL embebido en ventana contra `produ_homolog_manda`, definiendo que para quitar cada SQL embebido se usará un solo stored procedure con `@prm_concepto varchar(10) = 'listado'`, priorizando prefijos `prs_` para PRESMED y `afs_` para AFILMED. También detalló el patrón PowerBuilder en tres secciones: prototype, función booleana con `DECLARE PROCEDURE`/`EXECUTE`/`FETCH`/`CLOSE`, y reemplazo del bloque por `if wf_valida_homologacion(...) then`.
- Respuesta o acciones del agente: Se actualizó `[CONOCIMIENTO 000003]` a `VERSION=1.1` incorporando el patrón técnico exacto.
- Resultado: Quedó documentado que la función devuelve `TRUE` solo cuando el `FETCH` da `SQLCode = 0`, preservando el comportamiento original del bloque `if SQLCA.SQLCode = 0 then`; si no encuentra fila (`SQLCode 100`) devuelve `FALSE` sin tratarlo como error técnico. Daniel luego informó que todo quedó implementado correctamente.
- Estado: Resuelto
