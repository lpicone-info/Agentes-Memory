# Memoria curada diaria

- Fecha: 2026-08-04
- Usuario: Walter Ferreyra
- id_googlechat: users/112034177165901046099
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/jgr2byaaaae

## Resumen de la actividad

### Tema 1: Redmine 29801 y ventana PowerBuilder reutilizable

- Solicitud o contexto: Walter pidió trabajar sobre el Redmine `https://redmine.infomedical.com.ar/issues/29801` con usuario `wferreyra`. Primero solicitó una ventana similar a la de una imagen para copiar y crear una nueva; luego precisó que quería una ventana existente para hacer `Guardar Como` y modificar lo menos posible.
- Respuesta o acciones del agente: Se revisó el contexto operativo, repositorios y reglas antes de buscar objetos PowerBuilder en AFILMED. Se analizaron ventanas y DataWindows existentes para ubicar una base reutilizable con estructura ABM, filtros y patrón de selección aplicable al requerimiento.
- Resultado: Se recomendó como base principal la ventana `wp_config_recupero_regla_excep`, ubicada en `ws_objects/aumentos.pbl.src/wp_config_recupero_regla_excep.srw`, porque hereda de `w_clase_abm`, ya tiene botonera ABM, grilla/lista, DataStores de filtros y patrón de selección por plan/zona con estados `S`, `N` y `X`. Se propuso duplicarla como `wp_aumentos_etapas_segmentos` y duplicar los DataWindows `de_config_recupero_regla_excep` y `d_config_recupero_regla_excep` como `de_aumentos_etapas_segmentos` y `d_aumentos_etapas_segmentos`. Se sugirió ajustar el título a "Segmentos de Etapas de Aumentos", usar `is_tabla = 'AUMENTOS_ETAPAS_SEGMENTOS'`, conservar campos como `cod_camp`, `cod_etapa`, `lote`, `vigen_desde`, filtros de plan/zona, condición y porcentajes, y limpiar filtros/campos sobrantes de la ventana base. Se indicó `wp_campanias_aumentos_etapa` como referencia secundaria para contexto de campaña/etapa, no como base principal.
- Estado: Resuelto. No se modificaron archivos.
