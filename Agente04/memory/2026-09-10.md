# Memoria curada - 2026-09-10

## Pedido R30278 - Modo de cálculo en Mandataria
- Usuario conversando: Marcelo.
- Repo/carpeta de trabajo usada: `/mnt/Agentes/mepaduan/R30278`.
- Requerimiento funcional: crear estructura de tablas para guardar el modo de cálculo del importe a pagar a cada mandataria respecto de la aplicación de descuentos.
- Definición acordada: el autoincremental NO se maneja por base de datos; los códigos se asignan externamente.
- Convención acordada para descripción: usar campo `deno`, no `descripcion`.
- Se decidió mantener el nombre `cod_modo_calculo` porque es claro, consistente con la tabla `modo_calculo` y no es excesivo según convenciones existentes.
- Script generado: `/mnt/Agentes/mepaduan/R30278/modo_calculo.sql`.
  - Crea `dbo.modo_calculo`.
  - Campos: `cod_modo_calculo INT NOT NULL`, `deno VARCHAR(50) NOT NULL`, `alta_fecha DATETIME NOT NULL`, `modi_fecha DATETIME NULL`, `baja_fecha DATETIME NULL`, `usuario VARCHAR(30) NOT NULL`.
  - PK clustered sobre `cod_modo_calculo`.
  - Sin `IDENTITY`, sin `;`, sin `RTRIM`, ASCII y CRLF.
  - Incluye `LOCK ALLPAGES`, validación de creación y permisos a `prestaci_role`.
- Script generado: `/mnt/Agentes/mepaduan/R30278/manda_modo_calculo.sql`.
  - Crea tabla de relación `manda_modo_calculo`.
  - Campos: `manda INT NOT NULL`, `cod_modo_calculo INT NOT NULL`, fechas de alta/modificación/baja y `usuario VARCHAR(30) NOT NULL`.
  - PK compuesta no clustered.
  - FK hacia `mandatarias(manda)` y `modo_calculo(cod_modo_calculo)`.
  - Permisos a `prestaci_role`; validado ASCII, CRLF, sin `;`, sin `RTRIM`, sin `IDENTITY`.
- Para ABM de `modo_calculo`, se revisó ejemplo y se sugirió `wp_parametros_iva.srw` con `d_iva_tasa_farma.srd`, pero luego Marcelo informó que la tabla se cargará por DML, por lo que no hace falta ABM PowerBuilder por ahora.
- DML inicial sugerido para `modo_calculo`: un solo `INSERT` con `cod_modo_calculo = 1` y `deno = 'MODO CALCULO 1'`; `alta_fecha = GETDATE()`, `usuario = SUSER_NAME()`, fechas de modificación/baja en `NULL`.

## SVN
- Consulta respondida: usuario SVN registrado para Alberto Kostner.
- Usuario SVN informado: `infomedical.alberto.kostner`.
- Aclaración hecha: en los registros figura Kostner con `t`, no Kosner.
