# Memoria de conversación — Marcelo E. Paduan

## Datos

- **Fecha:** 02/09/2026
- **Usuario:** Marcelo E. Paduan
- **ID Google Chat:** `users/101970137366556255918`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 41
- **Inicio:** 09:21:42
- **Fin:** 18:44:09

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/yq3ndyaaaae`

## Archivos revisados

- `4cea330d-57f0-4118-a52e-1fa0adb2c2ed.jsonl`

---

## Transcripción completa

### [02/09/2026 09:21:42] Marcelo E. Paduan

99

---

### [02/09/2026 09:23:17] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [02/09/2026 10:26:22] Marcelo E. Paduan

en el repositorio de farmacia svn hay algun sp que valida mandataria con un sentencia similar a esta

select manda,
 razon,
 domi,
 loca,
 postal_codi,
 tele,
 obser,
 alta_fecha,
 baja_fecha,
 cuenta,
 cuit,
 iva
into :a_str.manda,
 :a_str.razon,
 :a_str.domi,
 :a_str.loca,
 :a_str.postal_codi,
 :a_str.tele,
 :a_str.obser,
 :a_str.alta_fecha,
 :a_str.baja_fecha,
 :a_str.cuenta,
 :a_str.cuit,
 :a_str.iva
from mandatarias
where manda = :a_manda;

---

### [02/09/2026 10:29:15] Agente

Marcelo, en el SVN de farmacia (`PRS_3850_DB`) no encontré un SP con la sentencia exacta tipo PowerBuilder:

`SELECT manda, razon, domi... INTO :a_str... FROM mandatarias WHERE manda = :a_manda`

Sí encontré SP relacionados:

1. [prs_valida_farma_expe.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/procedures/prs_valida_farma_expe.sql:57)  
   Es el más cercano a “validar mandataria” funcionalmente. Valida que la mandataria del expediente exista, esté vigente (`baja_fecha IS NULL`) y tenga `enti_tipo` configurado.

2. [prs_iva_mandataria.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/procedures/prs_iva_mandataria.sql:19)  
   Consulta `mandatarias` por `manda`, valida vigencia con `baja_fecha IS NULL` y devuelve si tiene `iva = '1'`.

3. [prr_auto_mandatarias_sgi.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/procedures/prr_auto_mandatarias_sgi.sql:18)  
   Es el más parecido por columnas: devuelve `manda`, `razon`, `domi`, `loca`, `postal_codi`, `tele`, `obser`, `alta_fecha`, `cuenta`, `cuit`, `iva`, etc. Pero no filtra por `manda`; filtra por `autorizaciones = @auto` y `baja_fecha IS NULL`.

Validación ejecutada: búsqueda en todo el SVN y luego acotada a `procedures` con patrones sobre `mandatarias`, `manda`, columnas equivalentes y nombres/comentarios de validación.

---

### [02/09/2026 10:39:18] Marcelo E. Paduan

ok gracias.
ahora revisame esto porfavor
estoy en wp_ingexped_farm y esta linea recupera una alicuota

rut.of_Tasa_Iva(this.object.fecha[1],100,ie_ivax,ie_retx)

y por otro lado en la ventana wp_ingexped_farm tengo la dw_det con d_expe_farm donde se realiza un calculo en base a esto , recuperar las tasas de iva
rut.of_Tasa_Iva(this.object.fecha[1],100,ie_ivax,ie_retx)y luego la funcion wf_itemchanged_valoresdonde se ejecuta
 dw_det.object.iva_ins[1] = dw_det.object.grava[1] * ie_ivax / 100 y en el caso de Farmacia me esta calculando con un 10.5 % la alicuota ie_ivax pero el usuari odice que ese calculo se tiene que hacer en base al 21 %.como hago o de donde recupero ese valor del 21 %, teniendo en cuenta que aca estoy usando una funcion que me trae el 10.5.tengo que encontrar la funcion similar que se utiliza en el sistema para recuperar esa alicuota

---

### [02/09/2026 10:39:27] Agente

Marcelo, para revisarlo necesito que me indiques en qué repositorio buscar la ventana `wp_ingexped_farm`.

Por la regla operativa no puedo buscar en repositorios de forma arbitraria si no está indicado el repo. Decime el nombre lógico, por ejemplo `smg-code-afilmed`, `smg-code-presmed` u otro de los registrados, y busco la función equivalente que recupere la alícuota del 21 %.

---

### [02/09/2026 10:59:13] Marcelo E. Paduan

ok luego vemo eso. ahora necesito
que me generes un archivo DML
para hacer el insert de 2 recha nuevos en recha_motivos
la deno tiene que ser la siguiente
'Mandataria inexistente'
'Mandataria dada de baja' con los codigos
6067 y 6068
toma el resto de los datos de lso campos de la siguiente linea
recha deno grupo tipo carac iso_recha iso_deno sube_igual nivel ambu_inter refacturable medico_adm conectividad informa_copago Excep_control aplica_cabecera resolucion_debito_externo conserva_conectividad conserva_debito combina_93 habilitado auto_anula manual
101 Farmacia inexistente FAR E E A S A 0 0 0 0 1 0 B

esa es la base y se reemplaza el recha y la deno.
el formato del DML por cada linea es esta una guia
IF (SELECT count(1) FROM recha_motivos WHERE recha = 6065) = 0
BEGIN
 INSERT INTO recha_motivos ( recha, deno, grupo, tipo, carac, habilitado )
 SELECT 6065, 'Precio del medicamento supera el maximo permitido por producto.', 'FAR', 'E', 'E', 'S'

 SET @modificados = @modificados + 1
END
ELSE
 UPDATE recha_motivos SET deno= 'Precio del medicamento supera el maximo permitido por producto.', tipo='E' WHERE recha = 6065

si queres enviame aca en el chat la linea del DML y yo las adjunto al archivo necesario y las compilo

---

### [02/09/2026 10:59:35] Agente

Marcelo, te dejo el DML tomando como base real el registro `recha = 101`, reemplazando solo `recha` y `deno`:

```sql
IF (SELECT count(1) FROM recha_motivos WHERE recha = 6067) = 0
BEGIN
    INSERT INTO recha_motivos
        ( recha, deno, grupo, tipo, carac, iso_recha, iso_deno, sube_igual, nivel, ambu_inter, refacturable, medico_adm, conectividad, informa_copago, Excep_control, aplica_cabecera, resolucion_debito_externo, conserva_conectividad, conserva_debito, combina_93, habilitado, auto_anula, manual )
    SELECT
        6067, 'Mandataria inexistente', grupo, tipo, carac, iso_recha, iso_deno, sube_igual, nivel, ambu_inter, refacturable, medico_adm, conectividad, informa_copago, Excep_control, aplica_cabecera, resolucion_debito_externo, conserva_conectividad, conserva_debito, combina_93, habilitado, auto_anula, manual
    FROM recha_motivos
    WHERE recha = 101

    SET @modificados = @modificados + 1
END
ELSE
    UPDATE recha_motivos
    SET deno = 'Mandataria inexistente'
    WHERE recha = 6067

IF (SELECT count(1) FROM recha_motivos WHERE recha = 6068) = 0
BEGIN
    INSERT INTO recha_motivos
        ( recha, deno, grupo, tipo, carac, iso_recha, iso_deno, sube_igual, nivel, ambu_inter, refacturable, medico_adm, conectividad, informa_copago, Excep_control, aplica_cabecera, resolucion_debito_externo, conserva_conectividad, conserva_debito, combina_93, habilitado, auto_anula, manual )
    SELECT
        6068, 'Mandataria dada de baja', grupo, tipo, carac, iso_recha, iso_deno, sube_igual, nivel, ambu_inter, refacturable, medico_adm, conectividad, informa_copago, Excep_control, aplica_cabecera, resolucion_debito_externo, conserva_conectividad, conserva_debito, combina_93, habilitado, auto_anula, manual
    FROM recha_motivos
    WHERE recha = 101

    SET @modificados = @modificados + 1
END
ELSE
    UPDATE recha_motivos
    SET deno = 'Mandataria dada de baja'
    WHERE recha = 6068
```

No generé archivo; solo te dejo las líneas para adjuntar donde corresponda.

---

### [02/09/2026 13:16:00] Marcelo E. Paduan

el repo para esto wp_ingexped_farm.
es
el de git farmacia.TAREA_29420_MODULO_FARMACIA
necesito que el calculo se haga con el 21 % y no con el 10.5
el calculo del iva partiendo del TOTAL GRAVADO

---

### [02/09/2026 13:19:44] Agente

Marcelo, dejé ajustado `wp_ingexped_farm.srw` en el repo `smg-code-presmed`, rama `TAREA_29420_MODULO_FARMACIA`.

Cambio aplicado en [wp_ingexped_farm.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui14.pbl.src/wp_ingexped_farm.srw:104):

```powerscript
DECIMAL{2} ie_ivax_farmacia = 21.00
```

Y reemplacé el cálculo automático:

```powerscript
dw_det.object.iva_ins[1] = dw_det.object.grava[1] * ie_ivax_farmacia / 100
```

También ajusté la validación que avisa “IVA no es X% del gravado” para que compare contra `21.00` y no contra `ie_ivax`.

Evidencia técnica: `rut.of_Tasa_Iva(...)` lee `iva_tasa.iva_tasa`; por eso seguía trayendo `10.5`. No encontré una función similar que devuelva directamente el 21% para este caso de farmacia, así que lo dejé encapsulado localmente en la ventana para no alterar la lógica general ni la percepción `ie_retx`.

Validaciones ejecutadas: búsqueda de usos, revisión del diff, preservación de UTF-8 con BOM y finales LF. No compilé PowerBuilder desde acá. No hice commit.

---

### [02/09/2026 13:28:10] Marcelo E. Paduan

NO NO.. no es eso lo que necesito... sino que esa variable debe recuperar en alguna lugar, objeto, funcion, sp, base de datos el 10.5 en base a parametros o condiciones que recibe... tengo que llegar a ese punto y ver si ademas de recuperar el 10.5,,, ver como recuperar el 21 ,
no es un dato que vaya a ir hardcodeado. no no
tengo que encontra y conocer el origen

---

### [02/09/2026 13:30:48] Agente

Marcelo, tenés razón. Ya revertí el hardcode que había puesto. No queda `21.00` agregado por mí en `wp_ingexped_farm.srw`.

El origen del `10.5` está acá:

[uo_nv_liquida.sru](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui08.pbl.src/uo_nv_liquida.sru:389)

```powerscript
select iva_tasa, reten_tasa
into :a_iva, :a_reten
from iva_tasa
where vigen_desde = (select max(b.vigen_desde)
from iva_tasa b
where b.vigen_desde < :a_fecha)
```

O sea: `rut.of_Tasa_Iva(...)` recupera `iva_tasa.iva_tasa` desde la tabla `prestaci.dbo.iva_tasa`.

La tabla tiene estos campos:

[iva_tasa.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/tables/iva_tasa.sql:3)

```sql
vigen_desde
iva_tasa
ivan_tasa
reten_tasa
```

Y el mantenimiento de parámetros muestra la semántica:

[d_iva_tasa_param.srd](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/instal.pbl.src/d_iva_tasa_param.srd:33)

- `iva_tasa`: “Iva ins”
- `ivan_tasa`: “Iva no ins”
- `reten_tasa`: “Reten”

El lugar similar que encontré para recuperar una alícuota más completa es:

[uo_nv_factu.sru](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui22.pbl.src/uo_nv_factu.sru:704)

Función:

```powerscript
of_alicuota_iva(ai_prepaga, as_contra, adt_fecha, ref adc_alicuota)
```

Esa función lee `iva_tasa` e `ivan_tasa`, y según condiciones de prepaga/contrato/IVA hace:

```powerscript
adc_alicuota = ldc_iva_tasa / 100
adc_alicuota = adc_alicuota + (ldc_ivan_tasa / 100)
```

Entonces el camino probable para llegar al `21` no es otra función simple “traeme 21”, sino revisar si para farmacia corresponde usar solo `iva_tasa` o `iva_tasa + ivan_tasa` bajo alguna condición funcional. Para confirmar el valor real en base habría que consultar `iva_tasa` vigente con el profile que corresponda.

---

### [02/09/2026 14:53:46] Marcelo E. Paduan

Pre-compaction memory flush. Store durable memories only in memory/2026-09-02.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-02.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-02-HHMM.md); always use the canonical 2026-09-02.md filename. If nothing to store, reply with NO_REPLY.
Current time: Wednesday, September 2nd, 2026 - 2:53 PM (America/Buenos_Aires)
Reference UTC: 2026-09-02 17:53 UTC

---

### [02/09/2026 14:54:31] Agente

NO_REPLY

---

### [02/09/2026 14:54:31] Marcelo E. Paduan

analiza esta ventana wp_modexped en el repo de farmacia y decime como hace el calculo del iva y de donde obtiene la alicuota cuando se carga el total gravbado

---

### [02/09/2026 14:56:18] Agente

Marcelo, en `wp_modexped` el cálculo se hace así:

Cuando cambia o se carga `grava`, ejecuta [wf_itemchanged_valores](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui03.pbl.src/wp_modexped.srw:2983). En el caso `grava` calcula:

```powerscript
dw_det.object.iva_ins[1] = dw_det.object.grava[1] * ie_ivax / 100
```

La variable `ie_ivax` no se calcula ahí. Ya viene cargada desde `rut.of_Tasa_Iva(...)`.

Se carga en estos puntos:

[wp_modexped.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui03.pbl.src/wp_modexped.srw:2852), cuando cambia la fecha del comprobante:

```powerscript
rut.of_Tasa_Iva(dw_det.object.fecha[1],100,ie_ivax,ie_retx)
```

[wp_modexped.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui03.pbl.src/wp_modexped.srw:3671), al abrir en alta usa `gd_hoy`.

[wp_modexped.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui03.pbl.src/wp_modexped.srw:4055), al cargar un expediente existente usa la fecha del comprobante o recepción según el DataWindow.

El origen real de la alícuota está en [uo_nv_liquida.sru](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui08.pbl.src/uo_nv_liquida.sru:389):

```powerscript
select iva_tasa, reten_tasa
into :a_iva, :a_reten
from iva_tasa
where vigen_desde = (select max(b.vigen_desde)
from iva_tasa b
where b.vigen_desde < :a_fecha)
```

Conclusión: cuando se carga el total gravado, `wp_modexped` toma `ie_ivax`, y `ie_ivax` viene de `prestaci.dbo.iva_tasa.iva_tasa`, buscando la vigencia máxima menor a la fecha del comprobante. No usa `ivan_tasa` ni otra condición para llegar a 21 en este flujo.

También vi que cuando los datos vienen de comprobante digital, primero carga `grava` y llama `wf_itemchanged_valores('grava')`; después, si AFIP trae IVA, pisa `iva_ins` con ese valor. Está en [wp_modexped.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui03.pbl.src/wp_modexped.srw:2338).

No modifiqué archivos en este análisis.

---

### [02/09/2026 15:23:09] Marcelo E. Paduan

lo que hace ese codigo no es lo que necesito. porque si ejecuto esa query / sp siempre me va a devolver 10.5
y yo necesito que el iva sea del 21 % y no lo veo coomo el ultimo vigente en la base de datos.
vos me podes decir si existe algun dato o parametro o algo de donde el ssitema recupera el 21 % para calculos de iva ya sea en la facturacion o en algun lugar del sistema donde este plasmado en una tabla de base de datos y no hardcode

---

### [02/09/2026 15:24:46] Agente

Marcelo, sí: en el sistema hay al menos dos orígenes de alícuota en tabla, no hardcodeados.

El que usa `wp_modexped` / `wp_ingexped_farm` es `iva_tasa.iva_tasa`. Por eso te devuelve 10.5:

```powerscript
rut.of_Tasa_Iva(fecha, 100, ie_ivax, ie_retx)
```

lee:

```sql
SELECT iva_tasa, reten_tasa
FROM iva_tasa
```

Pero para llegar al 21 existe esta lógica en facturación: [uo_nv_factu.sru](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui22.pbl.src/uo_nv_factu.sru:704), función `of_alicuota_iva`.

Esa función recupera:

```sql
SELECT iva_tasa, ivan_tasa
FROM iva_tasa
```

y calcula la alícuota así:

```powerscript
IF ls_es_grava = 'S' THEN
    adc_alicuota = ldc_iva_tasa / 100
END IF

IF ls_es_grava = 'S' AND ls_es_gravaN = 'S' THEN
    adc_alicuota = adc_alicuota + (ldc_ivan_tasa / 100)
END IF
```

Es decir: si `iva_tasa = 10.5` y `ivan_tasa = 10.5`, la alícuota final da `21`.

La condición para sumar `ivan_tasa` sale de:

- `parametros.ivan_inscrip`
- `ivas.es_grava`

La tabla `ivas` tiene el campo `es_grava`: [ivas.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/tables/ivas.sql:3).

También existe otra tabla de alícuota no hardcodeada para SAP:

[sap_liq_iva_alicuota.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/tables/sap_liq_iva_alicuota.sql:3)

```sql
sap_liq_iva_alicuota.alicuota
```

Se usa en SP de interfaz SAP como `pri_sap_provision_fac_def`, `pri_sap_creditos`, `pri_sap_nd_nc`, etc. Ejemplo: [pri_sap_provision_fac_def.sql](/home/luispicone/.openclaw/workspace/svn/Infomedical.lcamaratta-PRS-3850-Reingenieria-LiqFarmacias_20260603/procedures/pri_sap_provision_fac_def.sql:141).

Conclusión técnica: para el cálculo de farmacia, el origen más coherente para obtener 21 sin hardcodear parece ser replicar/adaptar la lógica de `uo_nv_factu.of_alicuota_iva`, usando `iva_tasa + ivan_tasa` cuando la condición IVA lo indique. La tabla SAP también tiene alícuota, pero está orientada a interfaces SAP, no al cálculo de carga/modificación del comprobante.

---

### [02/09/2026 15:30:33] Marcelo E. Paduan

no no, eso no me va a servir... capaz lo mas coherente sea agregar un parametro en algunas de las tablas existentes de parametros y ver si existen otras alicuotas ahi.
cuales son las tablas de parametros que consume o consulta el sistema?

---

### [02/09/2026 15:35:50] Agente

Marcelo, las tablas de parámetros que vi consumidas por el sistema son estas:

**Transversales / reales para configuración general**
- `parametros`: tabla histórica de una fila “ancha”. Se consulta mucho desde PB y SP. Tiene `iva_inscrip` e `ivan_inscrip`, pero no guarda la alícuota 21/10.5; guarda códigos de condición IVA.
- `parametros2`: tabla clave/valor (`clave`, `valor`, `valor_numerico`, `descripcion`, etc.). Se usa bastante para flags, fechas, leyendas y parámetros operativos. No encontré una clave vigente tipo alícuota IVA farmacia.
- `iva_tasa`: tabla vigente de tasas. Es la que usa `rut.of_Tasa_Iva(...)`. En Dev y QA tiene `21.00`, pero como registro viejo `Jan 1 1999`; la última vigente por fecha devuelve `10.50`, por eso tu ventana recibe 10.5.
- `ivas`: catálogo de condición IVA; define si grava (`es_grava`, `es_grava_expe`), pero no la tasa numérica.

**Específicas / candidatas pero con cuidado**
- `sap_liq_iva_alicuota`: tiene una sola alícuota usada por procesos SAP. En Dev y QA vale `10.50`, así que no te sirve para traer 21.
- `sap_imp_iva_config`: esta sí tiene alícuotas `10.50` y `21.00`, pero está atada a configuración de impuestos SAP (`id_imp`, `alicuota`, `computable`). Sirve como antecedente de que el sistema parametriza 21 en tabla, pero semánticamente es SAP, no carga manual de expediente farmacia.
- `tur_subd_cond_iva`: tiene clasificación `0%`, `21%`, `10.5%`, `EXENTO`, pero es para codificación/reportes, no para cálculo de importe.
- `redu_farm_param`: es específica de farmacia, pero parametriza reducción/descuento, no IVA.
- `liqui_param_auto_cab`, `liqui_param_auto_det`: parámetros de liquidación automática, no alícuotas generales.

Mi conclusión: para `wp_ingexped_farm`, hoy el origen correcto del 10.5 es `iva_tasa` vía `of_Tasa_Iva`. Si necesitás un 21 configurable sin hardcodear, lo más limpio sería agregar un parámetro nuevo, probablemente en `parametros2`, usando `valor` como decimal convertido a `DECIMAL`, porque `valor_numerico` es `INT` y no sirve bien para alícuotas con decimales como `10.50`.

No usaría `sap_liq_iva_alicuota` para esto: hoy también da 10.5 y además pertenece al circuito SAP.

---

### [02/09/2026 16:01:50] Marcelo E. Paduan

bueno armame una texto breve de propuesta indicando que no encontramos la alicuota 21 % en ningua tabla ni tampoco funcionalida que la utilice.
entonces armamos la propuesta de incorpara este paremtro
SELECT 'ALIC_IVA_FARMA', * FROM parametros2
con el valor = 21

---

### [02/09/2026 16:02:32] Agente

Marcelo, te dejo un texto breve:

> Luego del análisis del circuito actual de Farmacia, no se identificó una funcionalidad ni una parametrización vigente específica desde donde el sistema recupere la alícuota del 21% para calcular el IVA sobre el total gravado en `wp_ingexped_farm`.  
> Actualmente el cálculo utiliza la tasa recuperada por la lógica vigente, que devuelve 10.5%.  
> Se propone incorporar un nuevo parámetro en `parametros2`, con clave `ALIC_IVA_FARMA` y valor `21`, para que la alícuota utilizada por Farmacia quede configurable y no hardcodeada en el código.

Referencia propuesta:

```sql
SELECT 'ALIC_IVA_FARMA', * 
FROM parametros2
WHERE clave = 'ALIC_IVA_FARMA'
```

---

### [02/09/2026 16:28:27] Marcelo E. Paduan

esto pide el lider de proyecto


en esa ventana wp_parametros_iva, Parametros IVA en Líqui, tablas específicas
 agregar una solapa nueva para hacer referencia a una tabla nueva
 IVA_TASA_FARMA
con vigencia/iva/alta fecha/modi fecha/baja fecha/usuario
podes usar como base la tabla iva_tasa

te voy a dejar el objeto wp_parametros_iva en el repo mnt mepaduan 30278
consulta ahi la ventana y hacele las modificaciones pertinentes.
sotapa / tab / sp /datawindows / dataobjects
acordate que no usamos codigo embebido. hay que generar los sp
podes usar el pru_iva_tasa como ejemplo, calculo que este hace el insert y update
la solapa debe tener los 2 botones borrar y agregar / insertar con los mismos iconos que ya esta en la tab tp_2.

fijate con que podes avanzar.

---

### [02/09/2026 16:41:07] Agente

Marcelo, avancé con la estructura pedida.

En Git `smg-code-presmed`:
- Modifiqué `ws_objects/instal.pbl.src/wp_parametros_iva.srw`
  - Agregué solapa nueva `Tasa iva farmacia`
  - Agregué `dw_iva_farma`
  - Agregué botones borrar/agregar con los mismos iconos de `tp_2`
  - El guardado llama al SP `pru_iva_tasa_farma`
- Agregué `ws_objects/instal.pbl.src/d_iva_tasa_farma_param.srd`
  - Basada en `d_iva_tasa_param`
  - Apunta a `iva_tasa_farma`
- Modifiqué `ws_objects/prestaci.pbl.src/presta.srj`
  - Registré `d_iva_tasa_farma_param`

En SVN DDL `PRS_3850_DB`:
- Agregué `tables/iva_tasa_farma.sql`
  - Campos: `vigen_desde`, `iva_tasa`, `usuario`, `alta_fecha`, `modi_fecha`, `baja_fecha`
- Agregué `procedures/pru_iva_tasa_farma.sql`
  - Alta/update/baja lógica por `vigen_desde`
- Agregué `procedures/prs_consu_iva_tasa_farma_vigente.sql`
  - Consulta tasa vigente por fecha para usar luego desde cálculo sin SQL embebido

Validé:
- `git diff --check` sin errores
- SQL sin `;` final ni `RTRIM`
- PB/SRJ siguen UTF-8 con BOM
- SQL nuevos quedaron ASCII con CRLF
- No compilé PowerBuilder ni ejecuté SQL en base.

---

### [02/09/2026 16:42:14] Marcelo E. Paduan

Pre-compaction memory flush. Store durable memories only in memory/2026-09-02.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-02.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-02-HHMM.md); always use the canonical 2026-09-02.md filename. If nothing to store, reply with NO_REPLY.
Current time: Wednesday, September 2nd, 2026 - 4:42 PM (America/Buenos_Aires)
Reference UTC: 2026-09-02 19:42 UTC

---

### [02/09/2026 16:42:55] Agente

NO_REPLY

---

### [02/09/2026 16:42:56] Marcelo E. Paduan

quiero que me dejes ttodos los objetos en mi repo mnt mepaduan
:\mepaduan\R30278

---

### [02/09/2026 16:44:24] Agente

Listo Marcelo, dejé estos objetos en `/mnt/Agentes/mepaduan/R30278`:

- `wp_parametros_iva.srw`
- `d_iva_tasa_farma_param.srd`
- `iva_tasa_farma.sql`
- `pru_iva_tasa_farma.sql`
- `prs_consu_iva_tasa_farma_vigente.sql`

Validé que quedaron copiados, que los SQL no tienen `;` ni `RTRIM`, y que los encodings quedaron así:
- PB: UTF-8 con BOM
- SQL: ASCII con CRLF

---

### [02/09/2026 16:54:51] Marcelo E. Paduan

vamos hacer correcciones,
d_iva_tasa_farma_param que el nombre sea solo d_iva_tasa_farma
que lea a un sp prs_iva_tasa_farma (no embebido)

---

### [02/09/2026 17:02:29] Agente

Listo, Marcelo. Dejé corregido en `/mnt/Agentes/mepaduan/R30278`:

- [wp_parametros_iva.srw](/mnt/Agentes/mepaduan/R30278/wp_parametros_iva.srw:581): ahora la solapa farmacia referencia `DataObject="d_iva_tasa_farma"`.
- [d_iva_tasa_farma.srd](/mnt/Agentes/mepaduan/R30278/d_iva_tasa_farma.srd:1): quedó con el nombre final y recupera por `procedure="1 execute dbo.prs_iva_tasa_farma;1 "`, sin `SELECT` embebido.
- [prs_iva_tasa_farma.sql](/mnt/Agentes/mepaduan/R30278/prs_iva_tasa_farma.sql:1): creado como SP de lectura sobre `dbo.iva_tasa_farma`, devolviendo vigencia, tasa, fechas, usuario, `movim` y `ori_iva_tasa`.

Validé que no quede `d_iva_tasa_farma_param`, que el DW no tenga `retrieve=` embebido, y que el SQL nuevo no tenga `RTRIM` ni finalizadores `;`. También verifiqué encoding/EOL: `.srw/.srd` UTF-8 con BOM + LF, `.sql` ASCII + CRLF.

---

### [02/09/2026 17:15:18] Marcelo E. Paduan

Pre-compaction memory flush. Store durable memories only in memory/2026-09-02.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-09-02.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-09-02-HHMM.md); always use the canonical 2026-09-02.md filename. If nothing to store, reply with NO_REPLY.
Current time: Wednesday, September 2nd, 2026 - 5:15 PM (America/Buenos_Aires)
Reference UTC: 2026-09-02 20:15 UTC

---

### [02/09/2026 17:16:01] Agente

NO_REPLY

---

### [02/09/2026 17:16:02] Marcelo E. Paduan

bien ahora en el mismo repositorio hace un compare o merge o fusion de los 2 objetos , vos creaste o modificaste el wp_parametros_iva y yo volvi a subir el original wp_parametros_iva_ori
comparalos porqeu al querer importarlos se pincha el IDE y se cierra. algo debe haber quedado mal. fijate si encontras el problema y pisame el archivo wp_parametros_iva con la nueva version en el mismo repo

---

### [02/09/2026 17:21:33] Marcelo E. Paduan

bien ahora en el mismo repositorio hace un compare o merge o fusion de los 2 objetos , vos creaste o modificaste el wp_parametros_iva y yo volvi a subir el original wp_parametros_iva_ori
comparalos porqeu al querer importarlos se pincha el IDE y se cierra. algo debe haber quedado mal. fijate si encontras el problema y pisame el archivo wp_parametros_iva con la nueva version en el mismo repo

---

### [02/09/2026 17:26:15] Agente

Listo, Marcelo. Comparé contra `wp_parametros_iva_ori.srw` y pisé [wp_parametros_iva.srw](/mnt/Agentes/mepaduan/R30278/wp_parametros_iva.srw:1) con una versión fusionada desde el original.

Encontré 3 problemas probables del cierre del IDE:
- El original estaba en `UTF-16 LE con CRLF`, y la versión anterior había quedado en `UTF-8 con BOM`.
- Habían quedado finales de línea mezclados al insertar la solapa.
- En [d_iva_tasa_farma.srd](/mnt/Agentes/mepaduan/R30278/d_iva_tasa_farma.srd:1) lo rehice desde `d_iva_tasa_param` del mismo módulo, en `release 5`; antes estaba basado en un `release 9` y además detecté IDs duplicados en columnas visuales.

Validado ahora:
- `wp_parametros_iva.srw`: `UTF-16 LE` con `CRLF`, igual que `wp_parametros_iva_ori.srw`.
- `d_iva_tasa_farma.srd`: `release 5`, sin `retrieve=`, con `procedure="1 execute dbo.prs_iva_tasa_farma;1 "`.
- No queda referencia a `d_iva_tasa_farma_param`.
- Los IDs visuales del DW quedaron sin duplicados.

---

### [02/09/2026 18:38:39] Marcelo E. Paduan

me actualizas el objeto y lo subis al repo mnt mepaduan 30278

---

### [02/09/2026 18:39:39] Agente

Listo, Marcelo. Dejé actualizado en `/mnt/Agentes/mepaduan/R30278`:

- [wp_parametros_iva.srw](/mnt/Agentes/mepaduan/R30278/wp_parametros_iva.srw:1)
- [d_iva_tasa_farma.srd](/mnt/Agentes/mepaduan/R30278/d_iva_tasa_farma.srd:1)

Validé que `wp_parametros_iva.srw` quedó en `UTF-16 LE con CRLF`, igual que `wp_parametros_iva_ori.srw`, y que referencia `DataObject="d_iva_tasa_farma"`. También validé que `d_iva_tasa_farma.srd` está en `release 5`, sin `retrieve=` embebido y leyendo por `prs_iva_tasa_farma`.

La carpeta `/mnt/Agentes/mepaduan/R30278` no tiene `.git` ni `.svn`, así que lo dejé subido ahí como carpeta de entrega.

---

### [02/09/2026 18:41:19] Marcelo E. Paduan

no, quiero que me dejes el objeto exportado ahi en
actualizado en /mnt/Agentes/mepaduan/R30278
no lo estoy viendo .
lo quieroe con el nombre
wp_parametros_iva.srw

---

### [02/09/2026 18:42:02] Agente

Marcelo, está dejado exactamente acá:

`/mnt/Agentes/mepaduan/R30278/wp_parametros_iva.srw`

Lo actualicé con timestamp nuevo: `18:41:55`, tamaño `32826 bytes`, permisos `755`. También forcé `sync` para que refresque en la carpeta montada.

---

### [02/09/2026 18:43:38] Marcelo E. Paduan

bueno ahora si. y te voy a pedir esa correccion o ajuste que hiciste sobre el objeto por el cual pinchaba... quizas sea conveniente guardar el patron de correcion en las preferencias porque es la segunda vez que tenemos que hacer eso... l
podes?

---

### [02/09/2026 18:44:09] Agente

Sí, Marcelo. Tiene sentido guardarlo como preferencia porque ya se repitió el patrón: objeto PB exportado que pincha al importar por encoding/EOL, versión/release o IDs internos inconsistentes.

Te propongo agregar esta preferencia:

```md
4. Cuando Marcelo pida corregir, fusionar o generar objetos PowerBuilder exportados (`.srw`, `.srd`, `.sru`):
   - Comparar contra el objeto original/base cuando exista.
   - Preservar estrictamente encoding, BOM y finales de línea del objeto original.
   - Para `.srw`, si el original viene en UTF-16 LE con CRLF, mantener ese formato.
   - Para `.srd`, usar como base un DataWindow equivalente del mismo módulo/release cuando exista.
   - Validar antes de entregar que no queden finales de línea mezclados.
   - Validar consistencia de declaraciones `type`, `on`, `event`, `end type`, `end on`, `end event`.
   - Validar en DataWindows que no haya IDs duplicados o columnas visuales apuntando a IDs inexistentes.
   - Evitar regenerar objetos PB desde una base de distinto `release` salvo que Marcelo lo pida explícitamente.
```

Confirmame con un “sí, agregala” y la incorporo en tu archivo de preferencias.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
