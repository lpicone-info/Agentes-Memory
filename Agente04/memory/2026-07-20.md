
## 2026-07-20 - Google Chat direct context

- User addressed as Julián in this conversation.
- Active code context discussed: repository `SwissDeVP10_prestaciy`, local branch `TAREA_29420_MODULO_FARMACIA`.
- PowerBuilder screen `Control de Rechazos por Mandataria` maps to `wp_homolog_manda` / `d_homolog_manda_motivo`.
- For `Control de Rechazos por Mandataria`, the Alta action inserts a row in the DataWindow, but actual persistence happens on accept through stored procedure `prestaci.dbo.pru_homologacion`.
- Main table updated by that screen: `prestaci.dbo.homologacion` with `enti_tipo = 'R'`, `enti_codi = mandataria`, `nomen = 0`, `codi = codigo de rechazo`, `enti_id = error de la mandataria`.
- Lookup/main related tables for that screen: `prestaci.dbo.mandatarias`, `prestaci.dbo.recha_motivos`; related FK table `prestaci.dbo.homologacion_tipos`.
- Secondary SP for deletion/logical baja on that screen: `prestaci.dbo.prd_homologacion`.
- For Farm/Importaciones/importacion Masiva de Recetas: form `wp_import_drogueria_masivo` uses `Shift + Ctrl + doble clic` to `Open(wp_test_import)` and passes entity type with `wp_test_import.wf_enti_tipo(UPPER(is_enti_tipo))`.
- In checked source, no button with literal text `ABM Ctrl.Manda` was found in `wp_test_import`; no confirmed `Open(...)` from `wp_test_import` to an ABM control mandataria form. The related ABM found is `wp_homolog_manda`, opened from menu `m_main.srm` via `opensheet( wp_homolog_manda, ParentWindow, 1, Original! )`.
