
## 2026-09-14 12:07 ART - Memoria durable

- Conversacion directa Google Chat con Marcelo. El usuario saluda a Agente99 como "99".
- Consulta resuelta sobre `SwissDeVP10_prestaci`: la tabla `dbo.mandatarias_auditoria` existe y esta en uso. Tenia 101 registros; ultimo registro detectado: `05/08/2026 11:46`, actividad `Asigna Codigo SAP`, operador `sandraf`. Se observo actividad en abril, mayo, junio, julio y agosto de 2026. El SP `prs_abm_manda_auditoria` la referencia para consulta/inserciones relacionadas.
- Trabajo en repo/carpeta `/mnt/Agentes/mepaduan/r30278`: el usuario dejo `wp_farmacias` para analizar como registra cambios en `farma_auditoria` y pidio replicar la logica en `wp_mandatarias`.
- Analisis de `wp_farmacias`: `ue_grabar` guarda datos principales. En alta llama a `pri_2farmacias`; en modificacion llama a `pru_2farmacias`. La auditoria no se inserta directamente desde la ventana, sino desde esos SP hacia `farma_auditoria`. El boton `pb_auditoria.clicked` solo consulta/visualiza abriendo `wp_abm_farma_auditoria`, que usa `prs_abm_farma_auditoria`.
- SP identificados para farmacia: `pri_2farmacias` audita alta; `pru_2farmacias` audita modificaciones/baja con actividades como `Levanta baja`, `Baja`, `Cambia Baja`, `Cambia Cgo.Farm.Mdt`, cambios de coordenadas o `Cambia Datos`; `prs_abm_farma_auditoria` consulta auditoria. Ventanas auxiliares tambien auditan via `pri/pru/prd_conve_farmacias`, `pri/pru/prd_colegio_farmacias`, `pri/pru/prd_forma_pago_farmacias`, `pri/prd_farmacia_drogueria`.
- Implementacion realizada en `/mnt/Agentes/mepaduan/r30278/wp_mandatarias.srw`: se agrego auditoria en `ue_grabar` usando el SP existente `pri_mandataria_auditoria`. Alta: despues de `pri_mandatarias`, registra `Alta`. Modificacion: despues de `pru_mandatarias`, registra `Cambia Datos`. Baja: despues de `prd_mandatarias`, registra `Baja`. Si falla auditoria, hace `ROLLBACK` y corta el flujo.
- Validaciones realizadas en ese momento: preservado encoding `UTF-16 LE con BOM`, preservados finales `CRLF`, quedo una declaracion de `pri_mandataria_auditoria` y tres ejecuciones de auditoria (alta, modificacion y baja). No se modificaron scripts SQL ni se ejecutaron cambios en base.
