## Análisis wp_farma_liqui_err - expediente 6050789
- Se analizó la copia del objeto `wp_farma_liqui_err` en el repo `PRS-3850-Reingenieria-LiqFarmacias_20260603`, contra la base `prestaci` del servidor `SwissDeVP10`.
- La ventana recibe correctamente `ofi=1`, `expe=6050789` y `manda=367`; al abrir ejecuta el flujo de restauración. Las cuatro solapas no tienen SQL embebido propio: dos DataWindows se recuperan mediante `Retrieve` y las otras dos mediante `RowsCopy`.
- DataWindows/SP: `d_farma_liq_errores_recha` y `d_farma_liq_errores` usan `dbo.prs_farma_liq_errores`; `d_farma_liq_prod_recha` y `d_farma_liq_prod_far` usan `dbo.prs_farma_liq_productos`.
- Causa del vacío: ambos SP filtran por `c.ok = 'R' OR c.debi_farma != 0`. Para el expediente había un error vigente (`recha=6051`, farmacia `41366`, lote 3, receta 3, ítem 1), pero la receta asociada tenía `ok='N'` y `debi_farma=0.00`, por lo que queda excluida. Las ejecuciones directas de ambos SP devolvieron cero filas, también probando `deb_gen='N'`.
- Hallazgo secundario a revisar: los SP devuelven la columna `cant`, mientras algunos DataWindows declaran `canti`; no explica el vacío actual porque la consulta devuelve cero filas.
- No se modificaron archivos ni datos de la base.