# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Julián Castilla
- id_googlechat: users/103683452661762478402
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/snq5hyaaaae

## Resumen de la actividad

### Tema 1: disponibilidad de profiles, repositorios y rama PB

- Solicitud o contexto: Julián inició la conversación, pidió listar los profiles de base conectados, las ramas de código PB disponibles, si existía la rama `TAREA_29420_MODULO_FARMACIA`, y luego pidió listar los repositorios registrados.
- Respuesta o acciones del agente: Se completó el bootstrap operativo y se cargaron las preferencias de Julián. Se informó que los profiles disponibles eran `SwissQAP10_afilmed`, `SwissQAP10_prestaci`, `SwissDeVP10_afilmed` y `SwissDeVP10_prestaci`. Se validó `REPOSITORIOS.md`, la carpeta local `workspace/svn` y el remoto SVN de código PB.
- Resultado: Se informó que el único código PB conectado para Julián era `jcastilla-Infomedical-trunk`, tipo `SVN`, rama `trunk`, remoto `https://srv-infosvn.infomedical.com/svn/Infomedical/trunk/`. La rama `TAREA_29420_MODULO_FARMACIA` no estaba registrada, no existía como working copy local y no existía como path exacto en el remoto de ramas PB. También se listaron los repositorios Git y SVN disponibles, separando código PB de repositorios SQL/DDL.
- Estado: Resuelto

### Tema 2: administración del tiempo de advertencia por versión desactualizada en login

- Solicitud o contexto: Julián pidió, usando `SwissQAP10_prestaci` y `smg-code-presmed`, ubicar dónde se administra el tiempo de espera de 10 segundos que se anuncia cuando el usuario se loguea con versión desactualizada.
- Respuesta o acciones del agente: Se validaron las referencias operativas, se buscó el mensaje y el flujo de login en `smg-code-presmed`, y se revisaron los objetos de base del DDL de `prestaci`.
- Resultado: Se determinó que el mensaje sale desde `ws_objects/clases.pbl.src/w_frame_login.srw`, donde `w_frame_login.wf_habilita(...)` ejecuta `pru_cheq_usuario`. El stored procedure devuelve `li_segundos_mostrar_advertencia`, que se usa para calcular `ldt_fin_advertencia` y repetir el `MessageBox()` hasta que venza ese tiempo. El valor se administra en `prestaci..zz_modulos_version.cant_segundos_advertencia`, no en código PB. En `SwissQAP10_prestaci` el valor vivo estaba en `1`, con `permite_version_incorrecta = 1` y `version = 202607.3`; la versión hardcodeada en PB estaba en `uo_versiones_modulos.sru` como `202606.1`.
- Estado: Resuelto

### Tema 3: informe funcional del módulo Farmacia

- Solicitud o contexto: Julián pidió, usando `SwissDeVP10_prestaci` y `smg-code-presmed`, un informe funcional del menú `Farmacia`: carga de expediente, carga de recetas, configuración de lotes, auditorías y circuito hasta la liquidación.
- Respuesta o acciones del agente: Se revisó la navegación PB desde `m_main.srm`, los objetos principales del módulo y metadatos de tablas/SP en `SwissDeVP10_prestaci`. No se modificó código ni datos.
- Resultado: Se informó que el menú `Farmacia` abre funciones centrales como `Ingreso de expedientes` (`wp_ingexped_farm`), `Circuito de carga` (`wp_selexp_cir_farm`), `Modificación de expedientes` (`wp_selexp_ing_farm`), `Carga de recetas` (`wp_selexp_car_farm` / `wp_carga_receta`), `Auditoría de recetas` (`wp_selexp_aud_farm` / `wp_audit_receta`) y `Liquidación de expedientes` (`wp_selexp_liq_farm` / `wp_farma_liqui`). Se documentó que el expediente tipo `F` se registra en `expedientes`; los lotes por farmacia se administran en `wp_farma_lotes` y persisten en `expe_farmacias`; las recetas se cargan en `wp_carga_receta` y persisten en `expe_farma_recetas`, `expe_farma_rece_items` y `expe_farma_errores`; la auditoría se realiza en `wp_audit_receta`; y la liquidación definitiva se realiza desde `wp_farma_liqui`, previa validación con `prs_val_audi_farm_cierre`, ejecutando luego `pru_farma_expe_liquida` y `pru_auto_gestion_operacion_liq`.
- Resultado adicional: Se identificó el modelo de datos central: `expedientes -> expe_farmacias -> expe_farma_recetas -> expe_farma_rece_items / expe_farma_errores`, con clave operativa `ofi + expe + farma + lote + rece_nro`. El circuito resumido quedó como: ingreso de expediente, alta de cabecera tipo `F`, carga/configuración de lotes por farmacia, carga de recetas, validación electrónica, cierre del lote, auditoría, aprobación/rechazo/débitos, validación de cierre y liquidación definitiva con generación o consulta de orden de pago, débitos y reportes.
- Estado: Resuelto

### Tema 4: origen del nombre de mandataria en formulario de Farmacia

- Solicitud o contexto: Julián pidió, usando `SwissDeVP10_prestaci` y `smg-code-presmed`, identificar de dónde sale el nombre de la mandataria visible en un formulario.
- Respuesta o acciones del agente: Se validó el caso contra base y se siguió el flujo PB/SP desde el formulario hasta la consulta de datos del expediente.
- Resultado: Se determinó que el nombre `ACONCAGUA S.C.S.` sale de `prestaci.dbo.mandatarias.razon`, vinculado por `prestaci.dbo.expedientes.manda`. Para el expediente validado se informó `ofi = 1`, `expe = 5987261`, `tipo = F`, `perio = 07/2026`, `manda = 253` y `mandatarias.razon = ACONCAGUA S.C.S.`. En PB, `wp_selexp_cir_farm.srw` muestra `em_manda.text = uo_status.istr_expe.manda_deno`; `uo_farma_status.sru` carga el expediente mediante `io_val.of_expe(...)`; `uo_nv_valdatos.sru` ejecuta `prs_valida_of_expe`; y el SP asigna `@manda = a.manda` y `@manda_deno = b.razon` desde `expedientes` y `mandatarias`.
- Estado: Resuelto
