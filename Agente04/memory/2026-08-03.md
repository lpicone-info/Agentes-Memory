
## 2026-08-03 20:22 ART - Trabajo Afilmed / MASTER-2587 Aumentos Segmentados

- Usuario Walter trabaja en Google Chat directo sobre PowerBuilder 2022 / Afilmed.
- Contexto Git: rama solicitada `MASTER-2587-Aumentos-Segmentados_20260601`. Como no estaba accesible la carpeta Windows `A:\wferreyra\Swiss\MASTER-2587-Aumentos-Segmentados_20260601`, se acordó dejar entregables en carpeta accesible `/mnt/Agentes/wferreyra/` para que Walter los mueva.
- Objetos PowerBuilder generados/copias en `/mnt/Agentes/wferreyra/afinstal11.pbl.src/`:
  - `wp_abm_aumentos_zonas.srw`
  - `de_aumentos_zonas.srd`
  - `d_aumentos_zonas.srd`
- Base funcional PB: se copió/adaptó desde `wp_abm_zona_vencimiento.srw` y sus DataWindows, pero el nuevo objeto debe quedar como ABM de zonas de aumentos.
- Cambios PB aplicados:
  - Window renombrada a `wp_abm_aumentos_zonas`.
  - Título: `string title = "Zonas de aumentos"`.
  - Referencias de DataObject actualizadas a `de_aumentos_zonas` y `d_aumentos_zonas`.
  - Stored procedures referenciados reemplazados a `afs_aumentos_zonas`, `afi_aumentos_zonas`, `afu_aumentos_zonas`, `afd_aumentos_zonas`.
  - `is_tabla` corregido finalmente a `AUMENTOS_ZONAS` para que funcione histórico genérico de `w_clase_abm`.
  - Validación de solapamiento de vigencias agregada/reforzada en `ue_valida`: no valida contra/para registros con `baja_fecha`, y compara solo activos con `ISNULL(baja_fecha)`; mensaje: `Existen vigencias que se solapan para la zona ingresada`.
  - Se indicó que el botón Histórico no estaba vacío: depende del histórico genérico vía `is_tabla`; por eso la corrección de `is_tabla` era clave.
  - Validaciones hechas: sin referencias viejas a `wp_abm_zona_vencimiento`, `de_vencimiento_zona`, `d_vencimiento_zona`, `ZONAS_FACT` ni SPs anteriores; encoding UTF-8 con BOM y finales LF preservados.
- Objetos de base inicialmente generados en `/mnt/Agentes/wferreyra/base_de_datos_aumentos_zonas/`, pero Walter aclaró que fue incorrecto tomar `ZONAS_FACT.sql` como base porque `AUMENTOS_ZONAS` ya existe en la rama.
- Entregable DB rehecho y válido en `/mnt/Agentes/wferreyra/base_de_datos_aumentos_zonas_rehecho/procedures/`:
  - `afs_aumentos_zonas.sql`
  - `afi_aumentos_zonas.sql`
  - `afu_aumentos_zonas.sql`
  - `afd_aumentos_zonas.sql`
- DB rehecha: no generar tabla `AUMENTOS_ZONAS.sql`; la tabla existe en `tables` de la rama SVN. SPs contra `AUMENTOS_ZONAS`, usando columnas consumidas por ventana/DataWindows: `cod_zona`, `deno`, `alta_fecha`, `modi_fecha`, `baja_fecha`, `vigen_desde`, `vigen_hasta`.
- Validaciones DB rehecha: sin referencias a `ZONAS_FACT` ni SPs viejos; ASCII; CRLF; sin `;`; sin `RTRIM`; no se ejecutó nada contra base ni se hizo commit/push/SVN commit.
- SVN indicado por usuario para DB: `http://lv36papf-svncorp.swm.com.ar:8888/svn/swiss/mp/prod/base_de_datos/sybase/sybase-DB1/sybase-afilmed/DDL/branches/MASTER-2587-Aumentos-Segmentados`, usuario `wferreyra`, contraseña informada en chat. Checkout falló por autenticación en intento previo; no quedó working copy SVN ni credencial huérfana.
- Base objetivo mencionada por usuario: server `SwissDevP10`, db `Afilmed`. No ejecutar cambios en base salvo pedido explícito.
