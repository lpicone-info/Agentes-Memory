# Memoria durable - 2026-06-08

## Preferencias operativas OMAR
- Cuando el interlocutor indique que escribe desde usuario OMAR, los documentos generados o dejados por el agente deben ubicarse siempre en `/mnt/Agentes/OMAR`.
- Para pedidos de revisar, consultar o documentar Redmine desde usuario OMAR, usar la credencial Redmine registrada para `ogomez` en el almacen seguro local. No exponer API keys, rutas internas ni contenido de credenciales.
- Si OMAR indica un repositorio, base de datos o profile concreto, acotar la revision solo a esa referencia indicada. No ampliar busquedas ni validaciones a otros repositorios, bases o profiles salvo pedido explicito.

## Redmine 29877 - Prevencion incobrables
- Pedido: analizar Redmine 29877, contenido, imagenes y adjuntos; revisar solo repo `dgiuseppe-Prevencion-trunk` y DB/profile `prevencion_entrega`; no modificar repositorios ni base.
- Se genero informe en `/mnt/Agentes/OMAR/redmine_29877/analisis_redmine_29877.md` y se descargaron adjuntos en `/mnt/Agentes/OMAR/redmine_29877/attachments`.
- Conclusion principal: en la ventana PowerBuilder `wp_impor_incobrables` no hay filtro, checkbox ni comparacion directa contra `plan_codi = 'BASICO'`.
- La marca relevante para el tratamiento de plan basico/incobrables esta en SQL: `planes.permite_alta`.
- En `prevencion_entrega` se verifico que `BASICO` tiene `permite_alta = 'S'` y `Ospysa Bas` tiene `permite_alta = 'N'`.
- `Ospysa Bas` no queda interpretado actualmente como plan basico para la importacion de incobrables.
- La validacion relevante esta en `backend/procedures/afs_incobrables_tmp_valida.sql`: si el plan vigente tiene `PLA.permite_alta = 'N'`, exige que el afiliado este dado de baja; con `permite_alta = 'S'`, saltea esa validacion.
- Usos de `planes.permite_alta` en `dgiuseppe-Prevencion-trunk`: funcionalmente solo en `afs_incobrables_tmp_valida.sql`; aparece tambien como definicion/metadata en `backend/tables/planes.sql` y `backend/tablas/tables-definitions.csv`.
- Evidencia de alta: `planes` no fue creada para incobrables; se agrego el campo `planes.permite_alta` para el proceso. En `planes.sql` hay `ALTER TABLE dbo.planes ADD permite_alta CHAR(1) DEFAULT 'N' NOT NULL` y comentarios indicando que permite considerar incobrable aun sin baja.
- Para cerrar el pedido 29877, segun la evidencia revisada, alcanzaria con poner `planes.permite_alta = 'S'` para el plan solicitado `Ospysa Bas`, manteniendo el comportamiento dentro de incobrables. No conviene tocar `verif_impor_apo` porque corresponde a importacion de aportes/NI y puede tener efectos colaterales.
- No se hicieron modificaciones en repositorios ni en base de datos.

## Redmine 29846 - Nuevo presupuesto R16
- OMAR pidio generar nuevo presupuesto R16 del Redmine 29846 tomando como base un analisis previo ubicado en `/mnt/Agentes/OMAR`, revisando solamente `dgiuseppe-Prevencion-trunk` SVN.
- Respuestas del cliente incorporadas:
  - Para la regla `2 vencimiento = fecha de generacion del archivo + 30 dias`, debe aplicarse limite al ultimo dia del mes resultante/confirmado por ejemplos: si se genera el 31/05, limite 30/06; si se genera el 01/06, limite 30/06 tambien.
  - Si el archivo informa segundo vencimiento, mantener comportamiento actual sin calcular intereses automaticamente.
- Se genero nuevo R16 V3 en `/mnt/Agentes/OMAR`:
  - `/mnt/Agentes/OMAR/R-16 Redmine 29846 - Ajuste fechas vencimiento PMC Link V3.docx`
  - `/mnt/Agentes/OMAR/R-16 Redmine 29846 - Ajuste fechas vencimiento PMC Link V3.pdf`
  - `/mnt/Agentes/OMAR/R-16 Redmine 29846 - Ajuste fechas vencimiento PMC Link V3.md`
- Estimacion del presupuesto: 16 horas.
- Se valido el PDF extrayendo texto. Se cotejo tecnicamente solo contra `dgiuseppe-Prevencion-trunk`. No se modifico el repositorio.

## 2026-06-08 20:00 UTC - Analisis Redmine 29908 y 29816 para usuario OMAR

### Redmine 29908 - Prevencion / Resolicitar CAE

- El interlocutor solicito analizar el Redmine `29908` indicando restriccion explicita: revisar codigo solamente en el repositorio SVN `dgiuseppe-Prevencion-trunk` y consultar base solamente en `prevencion_entrega`; no modificar repositorios ni profiles/base.
- Se genero informe en `/mnt/Agentes/OMAR/redmine_29908/analisis_redmine_29908.md`.
- Ventana/ruta funcional confirmada por el interlocutor: `Afilmed > Procesos masivos > Facturacion > Facturacion electronica > Resolicitar CAE`.
- Ventana PowerBuilder asociada: `exe/Source Infomedical/wp_compro_cae_resolicitar_ws.srw`.
- Circuito relevado:
  - Grilla/listado: `d_compro_resolicitar_cae.srd`.
  - SP de busqueda: `afs_compro_resolicitar_cae`.
  - Servicio AFIP usado para resolicitar: `uo_nv_afip_fecompconsultar`.
  - Metodo AFIP: `FECompConsultar`.
- Diagnostico principal: el error AFIP `602` no parecia originarse en parametros mal armados por codigo. La resolicitud consultaba AFIP con datos coherentes (`CbteTipo=1`, `PtoVta=0002`, numeros como `1849118`, `1849119`), pero esos comprobantes estaban pendientes localmente sin evidencia de estar registrados/autorizados en AFIP.
- Datos clave observados:
  - Pendientes locales salian de `compro_cae_solicitud` con `volvio = N` y `grabo_cae = N`.
  - En `compro_cae` solo habia CAE para `A000201849105`, `A000201849106`, `A000201849107`.
  - La respuesta AFIP guardada para solicitud `91066` autorizo solo numeros `1849105`, `1849106`, `1849107`.
  - No aparecian en esa respuesta `1849118` ni `1849119`.
  - Para solicitud `91080` no se encontro XML de respuesta guardado.
- Conclusion operativa: `Resolicitar CAE` consulta comprobantes ya registrados en AFIP; no regenera una solicitud nueva de CAE. Si AFIP no tiene el comprobante, `FECompConsultar` devuelve `602`. Para esos casos queda definir si corresponde regenerar solicitud CAE, anular/limpiar pendientes inconsistentes o ajustar el proceso para diferenciar pendientes con respuesta AFIP previa de pendientes sin evidencia de envio.
- No se modifico repositorio ni base. El SVN quedo igual, con no versionados previos de `redmine/29797`.

### Redmine 29816 - Test_Britanico / contrato 530653

- El interlocutor solicito analizar el Redmine `29816`, usando como referencia un analisis previo existente en `/mnt/Agentes/OMAR/redmine_29816`, y explicar didacticamente por que para el contrato `530653` se genero una ND por `1.886,98` y no por `4.402,95`.
- Restricciones indicadas: consultar solamente la base SQL Server `Test_Britanico` si era necesario y usar solamente el perfil/repositorio SVN `dgiuseppe-2026.04` si hacia falta; no modificar repositorio ni profile/base.
- Respuesta entregada al interlocutor:
  - Factura involucrada: `FC B006102129711`.
  - Total factura: `626.903,17`.
  - Interes completo de segundo vencimiento: neto `3.984,57` + IVA `418,38` = `4.402,95`.
  - Segundo vencimiento cubria del `17/04/2026` al `24/04/2026`, es decir 7 dias.
  - El socio pago el `20/04/2026`, 3 dias despues del primer vencimiento, no al dia 7.
  - El sistema recalculo proporcionalmente hasta la fecha real de pago:
    - Interes neto proporcional: `3.984,57 / 7 * 3 = 1.707,67`.
    - IVA proporcional: `418,38 / 7 * 3 = 179,31`.
    - ND generada: `1.707,67 + 179,31 = 1.886,98`.
  - ND generada: `B006100417471`, con concepto `INT` por `1.707,67`, `IVA` por `179,31`, total `1.886,98`.
  - El importe `4.402,95` correspondia al interes completo si hubiera pagado al segundo vencimiento completo (`24/04/2026`). Como pago el `20/04/2026`, se cobraron solo 3 de 7 dias.
  - Tambien habia una aplicacion previa de `3.499,70` sobre la factura; el pago de `627.806,42` se distribuyo asi: factura pendiente luego del saldo previo `623.403,47`, ND proporcional `1.886,98`, excedente aplicado a factura siguiente `2.515,97`, total pagado `627.806,42`.
  - Diferencia entre esperado y generado: `4.402,95 - 1.886,98 = 2.515,97`; no se perdio, quedo aplicada a la factura siguiente `B006102145490`.
- Conclusion didactica: el talon mostraba el importe como si se pagara al segundo vencimiento completo, pero el proceso de ND recalculo el interes hasta el dia real de pago. Como pago el `20/04/2026` y no el `24/04/2026`, genero una ND proporcional por `1.886,98`.


## 2026-06-08 20:20 UTC - Redmine 29816 / Test_Britanico / SVN dgiuseppe-2026.04

- Contexto de trabajo: interlocutor OMAR pidio analizar Redmine 29816 usando solo base SQL Server `Test_Britanico` y, si hacia falta codigo, solo working copy SVN `dgiuseppe-2026.04`. No modificar repositorio ni profiles/base.
- Carpeta de referencia del analisis previo: `/mnt/Agentes/OMAR/redmine_29816`.
- Caso contrato `530653`:
  - La ND salio por `1.886,98` y no por `4.402,95` porque el sistema recalculo interes proporcional a la fecha real de pago.
  - FC `B006102129711`: total `626.903,17`, interes 2do vencimiento `3.984,57`, IVA interes `418,38`, total interes 2do vencimiento `4.402,95`.
  - Primer vencimiento `17/04/2026`, segundo vencimiento `24/04/2026`: tramo de 7 dias.
  - Pago real `20/04/2026`: 3 dias posteriores al primer vencimiento.
  - Cuenta verificada: `3.984,57 / 7 * 3 = 1.707,67`; `418,38 / 7 * 3 = 179,31`; total ND `1.886,98`.
  - ND generada `B006100417471`: item `INT` `1.707,67`, item `IVA` `179,31`, total `1.886,98`.
  - Diferencia `4.402,95 - 1.886,98 = 2.515,97`, observada como excedente aplicado a factura siguiente `B006102145490`.
- Regla de sistema asociada al calculo de fecha de interes:
  - En `Test_Britanico`, `zz_sistema_reg` regla `213 = N` y regla `217 = S`.
  - Regla `213`: si `N`, calcula a fecha real de pago; si `S`, lleva la fecha de calculo al vencimiento correspondiente cuando aplica.
  - Flujo tecnico: `afs_dame_interes_compro` llama a `afs_dame_fecha_calc_interes`; esta ultima consulta `zz_sistema_reg` regla `213`.
  - Con `213 = N`, para contrato `530653` se calcularon 3 dias; con `213 = S`, en este caso puntual la ND deberia haber sido por el interes completo del 2do vencimiento (`4.402,95`), no por el importe total del talon.
  - Regla `217` define desde donde empieza a contar interes: `S` desde primer vencimiento; `N` desde fecha/periodo del comprobante.
- Caso contrato `102256`:
  - Cambiar regla `213` a `S` no alcanza para corregirlo, porque el problema detectado no fue la fecha/dias de calculo sino la composicion de la ND.
  - FC `A006100285780` tenia `interes2 = 17.335,31` e `iva_interes2 = 1.820,21`; total esperado `19.155,52`.
  - ND `A006100061603` quedo por `17.335,31`, con `iva_total = 0` e `iva = B`.
  - No se encontro evidencia de cambio de condicion de IVA cercano a abril 2026 que explique la falta de IVA.
  - La FC figura gravada (`iva = 1`) y ya tenia IVA de interes cargado.
  - La condicion del responsable pagador figura como `B` desde 2008, mientras los gravados del afiliado estan en `1` desde anos anteriores; no se detecto cambio reciente asociado.
  - Diagnostico tecnico: en `backend/procedures/afu_aplica_interes.sql`, alrededor de la linea 236, el proceso calcula interes pero al generar la ND llama a `afi_aju_internos` pasando `@valor = @interes`; no pasa `@interes_civa` ni el IVA separado.
  - Conclusiones comparativas comunicadas: `530653` es un caso de fecha/dias de calculo y la regla `213 = S` ayudaria; `102256` es un caso donde se tomo el interes neto pero falto incorporar el IVA del interes como item/importe adicional.
- Seguridad/continuidad: no se modifico repositorio ni base durante este analisis. No exponer credenciales ni rutas internas de credenciales. Mantener la restriccion de OMAR: documentos en `/mnt/Agentes/OMAR`, Redmine con credencial `ogomez`, y limitar revisiones al repositorio/base/profile explicitamente indicado por el interlocutor.

---

## Fragmento migrado desde MEMORY.md - 20260625-211303

## 2026-06-08 - Preferencia operativa para usuario OMAR

- Cuando el interlocutor indique que escribe desde el usuario OMAR, los documentos generados o dejados por el agente deben ubicarse siempre en `/mnt/Agentes/OMAR`.
- La carpeta destino `/mnt/Agentes/OMAR` fue validada como existente el 2026-06-08.
- Cuando el interlocutor indique que escribe desde el usuario OMAR y solicite revisar, consultar o documentar un Redmine, usar siempre la credencial Redmine registrada para el usuario `ogomez`.
- La credencial Redmine de `ogomez` fue validada como registrada en el almacen seguro local el 2026-06-08, con archivo de credencial en permisos 600. No exponer API keys, rutas internas ni contenido del archivo de credenciales.
- Cuando el interlocutor indique que escribe desde el usuario OMAR y especifique un repositorio, base de datos o profile concreto, acotar la revision solo a esa referencia indicada. No ampliar busquedas ni validaciones a otros repositorios, bases o profiles salvo pedido explicito del interlocutor.
