# Memoria diaria

- Fecha: 2026-09-18

## Investigación: botón "flor" en Expedientes a liquidar

- Repositorio analizado: `smg-code-presmed`, working copy `/home/luispicone/.openclaw/workspace/git/EXT_MaPaduan-smg-code-presmed-medico-presmed-PRS-3850-Reingenieria-LiqFarmacias_20260603`.
- Contexto de base mencionado: `SwissDEvP10`, base `Prestaci`; no se ejecutaron operaciones contra la base.
- El ícono visual de flor corresponde a `liquida.bmp` y está definido como un control `Picture`, sin evento `Clicked`; no ejecuta el proceso por sí mismo.
- La acción real parte del botón `&Liquidar` (`cb_car`), que dispara `parent.triggerevent('ue_aceptar')` en `wp_selexp_liq_farm`.
- `ue_aceptar` obtiene oficina, expediente, período y tipo; valida que el período esté abierto. Para tipo `F` abre `wp_farma_liqui` para revisar el expediente; para tipo `A` liquida directamente mediante `ue_liquida`.
- En `uo_nv_liquida_farma.of_liquidar`, el flujo ejecuta en orden: `prs_val_audi_farm_cierre` para validar; `pru_farma_expe_liquida` como procedimiento principal de cierre/liquidación; y `pru_auto_gestion_operacion_liq` con tipo `F`, que devuelve indicadores `recuperable` y `facturable`.
- Para tipo `F`, el cierre final se realiza desde el botón `Cerrar liquidación` de `wp_farma_liqui`, que conduce al mismo flujo.
- La lista inicial de expedientes usa un `SELECT` directo sobre `expedientes`; no llama a un procedimiento almacenado.
- Referencias relevantes: `ws_objects/liqui14.pbl.src/wp_selexp_liq_farm.srw`, `ws_objects/liqui14.pbl.src/uo_nv_liquida_farma.sru`, `ws_objects/liqui19.pbl.src/wp_farma_liqui.srw`, `ws_objects/liqui14.pbl.src/d_expe_pend_liq_farma.srd`.
- Estado: investigación informativa resuelta; no hubo cambios de código.
