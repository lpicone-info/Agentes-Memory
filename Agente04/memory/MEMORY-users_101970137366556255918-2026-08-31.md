# Memoria de conversación — Marcelo E. Paduan

## Datos

- **Fecha:** 31/08/2026
- **Usuario:** Marcelo E. Paduan
- **ID Google Chat:** `users/101970137366556255918`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 16
- **Inicio:** 09:10:41
- **Fin:** 17:21:44

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/yq3ndyaaaae`

## Archivos revisados

- `264d98f7-ec99-4d39-acd3-d3443586923c.jsonl`

---

## Transcripción completa

### [31/08/2026 09:10:41] Marcelo E. Paduan

99

---

### [31/08/2026 09:11:49] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [31/08/2026 10:39:20] Marcelo E. Paduan

en el repo mnt mepaduan 30278 esta el sp pru_expe_farma_lote el cual tiene que ser amplicado para permitir modificar/update el resto de los datos de la tabla que se visualizan por pantalla.
tiene que tener esto campos
 ef.lote,
 ef.deno,
 ef.canti,
 ef.total,
 ef.presen_fecha,
 ef.rece_nro,
 ef.ok,
 ef.alta_fecha,
 ef.carga_fecha,
 ef.actu_fecha,
 ef.baja_fecha,
 ef.totcal,
 ef.rece_afi_valor,
 ef.debcal,
 ef.audi_afi_valor,
 ef.farma_dto,
 ef.debi_afi,
 ef.debi_farma,
 ef.debi_prepa,
 ef.audi_fecha,
 ef.afi_dto,
 ef.nro_caja
de expe_farmacias
modificame el sp con este pedido

---

### [31/08/2026 10:42:04] Agente

Marcelo, modifiqué el SP en [pru_expe_farma_lote.sql](/mnt/Agentes/mepaduan/R30278/pru_expe_farma_lote.sql).

Quedó ampliada la firma para recibir los campos faltantes de `expe_farmacias` y el `UPDATE` ahora actualiza también: `baja_fecha`, `totcal`, `rece_afi_valor`, `debcal`, `audi_afi_valor`, `farma_dto`, `debi_afi`, `debi_farma`, `debi_prepa` y `audi_fecha`. Mantuve la lógica existente de cambio de estado por `ok` y la auditoría.

Validaciones ejecutadas: archivo leído antes de editar, preservado en ASCII, sin BOM, finales de línea LF, sin `RTRIM` y sin `;`. No ejecuté el SP contra base de datos. La carpeta `/mnt/Agentes/mepaduan/R30278` no está versionada como Git ni SVN.

---

### [31/08/2026 11:01:27] Marcelo E. Paduan

ahora corregime este evento para adaptarlo a ese sp
string ls_deno
long li_canti,li_lote,li_canacu, li_farma
decimal{2} le_total,le_totacu
datetime ld_presen_fecha, ld_alta_fecha, ld_carga_fecha, ldt_periodo
long ll_rcount,ll,ll_dcount,ll_rc,ll_rd, ll_rece_nro
string ls_busca,ls_error,ls_ok, ls_nrocaja
decimal ld_afidto
integer li_ofi

dwItemStatus l_status

dw_lotes.AcceptText( )
ll_rcount = dw_lotes.RowCount()

//Cierre de Período - Liquidación
ldt_periodo = exp.perio
li_ofi = exp.ofi
if not wf_cierre_liq (ldt_periodo,li_ofi) then
 cb_liquidar.enabled = false
 cb_imp_cajas.enabled = false
 cb_limpiar.enabled = false
 cb_ace.enabled = false
 cb_lote.enabled = false
 return
else
end if

if dw_lotes.modifiedcount() + dw_lotes.deletedcount() = 0 then return

li_farma = dw_sel.object.farma[1]
for ll = 1 to ll_rcount
 if trim(dw_lotes.object.deno[ll])='' and &
 dw_lotes.object.canti[ll] < 1 and &
 dw_lotes.object.total[ll] < 1 then
 dw_lotes.object.lote[ll] = 0
 dw_lotes.deleteRow(ll)
 ll_rcount --
 end if
next

if li_farma <> ii_farma_calc then
 ii_farma_calc = li_farma
 select sum(canti),sum(total) into :ii_canti,:ie_total
 from expe_farmacias
 where ofi=:exp.ofi and expe=:exp.expe
 and (farma <> :li_farma)
 and baja_fecha is null;
 if sqlca.sqlcode <> 0 then mbox(this,'error del sistema',1,0)
end if

if isnull(ii_canti) then ii_canti = 0
if isnull(ie_total) then ie_total = 0
li_canacu = ii_canti
le_totacu = ie_total

for ll = 1 to ll_rcount
 if trim(dw_lotes.object.deno[ll])='' then
 mbox(this,'Complete denominación',1,0)
 dw_lotes.setcolumn('deno')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if dw_lotes.object.canti[ll] < 1 then
 mbox(this,'Complete cantidad',1,0)
 dw_lotes.setcolumn('canti')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if dw_lotes.object.total[ll] < 1 then
 mbox(this,'Complete total',1,0)
 dw_lotes.setcolumn('total')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if isnull(dw_lotes.object.presen_fecha[ll]) then
 mbox(this,'Complete fecha de presentación',1,0)
 dw_lotes.setcolumn('presen_fecha')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if dw_lotes.object.presen_fecha[ll] > exp.recep_fecha then
 mbox(this,'Fecha de presentación mayor a recepción',1,0)
 dw_lotes.setcolumn('presen_fecha')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if

 l_status = dw_lotes.GetItemStatus(ll,'canti', Primary!)
 li_canacu = li_canacu + dw_lotes.object.canti[ll]
 if l_status <> NotModified! then /* se modifico despues del retrieve */
 if li_canacu > exp.compro_canti then
 mbox(this,'ATENCION : Excede la cantidad de comprobantes definidos para el expediente ('+string(li_canacu)+'/'+string(exp.compro_canti)+')',1,0)
 dw_lotes.setcolumn('canti')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
// return
 end if
 end if

next

if mbox(this,'Acepta cambios ?',4,2) = 2 then
 triggerevent('ue_salir')
 return
end if

SetPointer(HourGlass!)

DECLARE ins_lote PROCEDURE FOR pri_expe_farma_lote
 @prm_ofi = :exp.ofi,
 @prm_expe = :exp.expe,
 @prm_farma= :li_farma,
 @prm_deno = :ls_deno,
 @prm_canti = :li_canti,
 @prm_total = :le_total,
 @prm_rece_nro = 0,
 @prm_ok = :ls_ok,
 @prm_alta_fecha =:gd_hoy,
 @prm_carga_fecha = null,
 @prm_actu_fecha = null,
 @prm_presen_fecha=:ld_presen_fecha,
 @prm_nro_caja = :ls_nrocaja,
 @prm_afi_dto = :ld_afidto,
 @prm_lote=null;

DECLARE mod_lote PROCEDURE FOR pru_expe_farma_lote
 @prm_ofi = :exp.ofi,
 @prm_expe = :exp.expe,
 @prm_farma = :li_farma,
 @prm_lote = :li_lote,
 @prm_deno = :ls_deno,
 @prm_canti = :li_canti,
 @prm_total = :le_total,
 @prm_rece_nro = :ll_rece_nro,
 @prm_ok = :ls_ok,
 @prm_alta_fecha = :ld_alta_fecha,
 @prm_carga_fecha = :ld_carga_fecha,
 @prm_actu_fecha = :gd_hoy,
 @prm_nro_caja = :ls_nrocaja,
 @prm_afi_dto = :ld_afidto,
 @prm_presen_fecha =:ld_presen_fecha;


ll_rcount = dw_lotes.RowCount()


IF ll_rcount > 0 THEN
 FOR ll = 1 TO ll_rcount

 l_status = dw_lotes.GetItemStatus(ll, 0, Primary!)

 IF l_status <> NotModified! THEN

 li_lote = dw_lotes.Object.lote[ll]
 li_farma = dw_lotes.Object.farma[ll]
 ls_deno = dw_lotes.Object.deno[ll]
 li_canti = dw_lotes.Object.canti[ll]
 le_total = dw_lotes.Object.total[ll]
 ld_presen_fecha = dw_lotes.Object.presen_fecha[ll]
 ll_rece_nro = dw_lotes.Object.rece_nro[ll]
 ls_ok = dw_lotes.Object.ok[ll]
 ld_alta_fecha = dw_lotes.Object.alta_fecha[ll]
 ld_carga_fecha = dw_lotes.Object.carga_fecha[ll]
 ld_afidto = dw_lotes.Object.afi_dto[ll]
 ls_nrocaja = dw_lotes.Object.nro_caja[ll]

 IF IsNull(li_lote) OR li_lote = 0 THEN
 EXECUTE ins_lote;

 IF SQLCA.SqlCode = -1 THEN
 Mbox("pri_expe_farma_lote", 2, 0)
 RETURN
 END IF

 FETCH ins_lote INTO :li_lote;
 CLOSE ins_lote;

 dw_lotes.Object.lote[ll] = li_lote
 ELSE
 EXECUTE mod_lote;

 IF SQLCA.SqlCode = -1 THEN
 Mbox("pru_expe_farma_lote", 2, 0)
 RETURN
 END IF
 END IF

 END IF

 NEXT
END IF

COMMIT;

cb_ace.enabled = true
dw_lotes.ResetUpdate()
f_reset_cambios()

this.Event ue_restaurar (0,0)

y los campso que agregamos en d_expe_farmacias
te dejo en el repo esa dw para que puedas consultar los campos nuevos agregados asi queda la traza con el sp y el codigo pb

---

### [31/08/2026 11:05:10] Agente

Marcelo, corregí el evento `ue_aceptar` en [wp_farma_lotes.srw](/mnt/Agentes/mepaduan/R30278/wp_farma_lotes.srw:271).

Cambios realizados:
- Agregué variables para los campos nuevos de `d_expe_farmacias`.
- Amplié el `DECLARE mod_lote PROCEDURE FOR pru_expe_farma_lote` con todos los parámetros nuevos del SP.
- Cargué desde `dw_lotes.Object` los valores de:
  `baja_fecha`, `totcal`, `rece_afi_valor`, `debcal`, `audi_afi_valor`, `farma_dto`, `debi_afi`, `debi_farma`, `debi_prepa`, `audi_fecha`.
- También tomé `li_farma` desde la fila de `dw_lotes`, para que la clave enviada al SP salga del registro modificado.

Validaciones:
- `wp_farma_lotes.srw` sigue en `UTF-16LE` con BOM.
- Conserva finales de línea `CRLF`.
- La lista de parámetros PB coincide contra la firma actual de `pru_expe_farma_lote.sql`.
- Verifiqué que `dw_lotes` usa `dataobject = "d_expe_farmacias"`.

No ejecuté compilación PowerBuilder ni prueba contra base.

---

### [31/08/2026 11:06:10] Marcelo E. Paduan

parame por aca todo el codigo del ue_aceptar... no en el objeto.

---

### [31/08/2026 11:07:13] Agente

Marcelo, acá va completo el código actual del `ue_aceptar`:

```powerscript
event ue_aceptar;call super::ue_aceptar;string ls_deno
long    li_canti,li_lote,li_canacu, li_farma
decimal{2} le_total,le_totacu
decimal{2} le_totcal, le_rece_afi_valor, le_debcal, le_audi_afi_valor
decimal{2} le_farma_dto, le_debi_afi, le_debi_farma, le_debi_prepa
datetime ld_presen_fecha, ld_alta_fecha, ld_carga_fecha, ldt_periodo
datetime ld_baja_fecha, ld_audi_fecha
long    ll_rcount,ll,ll_dcount,ll_rc,ll_rd, ll_rece_nro
string  ls_busca,ls_error,ls_ok, ls_nrocaja
decimal ld_afidto
integer li_ofi

dwItemStatus l_status

dw_lotes.AcceptText( )
ll_rcount = dw_lotes.RowCount()

//Cierre de Período - Liquidación
ldt_periodo 	= exp.perio
li_ofi			= exp.ofi
if not wf_cierre_liq (ldt_periodo,li_ofi) then
	cb_liquidar.enabled = false
	cb_imp_cajas.enabled = false
	cb_limpiar.enabled = false	
	cb_ace.enabled = false
	cb_lote.enabled = false
	return
else
end if	

if dw_lotes.modifiedcount() + dw_lotes.deletedcount() = 0 then return

li_farma = dw_sel.object.farma[1]
for ll = 1 to ll_rcount
	if trim(dw_lotes.object.deno[ll])='' and &
		dw_lotes.object.canti[ll] < 1  and &
		dw_lotes.object.total[ll] < 1  then
		dw_lotes.object.lote[ll] = 0
		dw_lotes.deleteRow(ll)
		ll_rcount --
	end if
next

if li_farma <> ii_farma_calc then
	ii_farma_calc = li_farma
	select sum(canti),sum(total) into :ii_canti,:ie_total
		from expe_farmacias 
		where ofi=:exp.ofi and expe=:exp.expe 
		  and (farma <> :li_farma)
		  and baja_fecha is null;
	if sqlca.sqlcode <> 0 then mbox(this,'error del sistema',1,0)
end if

if isnull(ii_canti) then ii_canti = 0
if isnull(ie_total) then ie_total = 0
li_canacu = ii_canti
le_totacu = ie_total

for ll = 1 to ll_rcount
	if trim(dw_lotes.object.deno[ll])='' then
		mbox(this,'Complete denominación',1,0)
		dw_lotes.setcolumn('deno')		
		dw_lotes.setrow(ll)
		dw_lotes.setfocus()		
		return
	end if
	if dw_lotes.object.canti[ll] < 1 then
		mbox(this,'Complete cantidad',1,0)
		dw_lotes.setcolumn('canti')		
		dw_lotes.setrow(ll)
		dw_lotes.setfocus()		
		return
	end if	
	if dw_lotes.object.total[ll] < 1 then
		mbox(this,'Complete total',1,0)
		dw_lotes.setcolumn('total')		
		dw_lotes.setrow(ll)
		dw_lotes.setfocus()		
		return
	end if
	if isnull(dw_lotes.object.presen_fecha[ll]) then
		mbox(this,'Complete fecha de presentación',1,0)
		dw_lotes.setcolumn('presen_fecha')		
		dw_lotes.setrow(ll)
		dw_lotes.setfocus()		
		return
	end if
	if dw_lotes.object.presen_fecha[ll] > exp.recep_fecha then
		mbox(this,'Fecha de presentación mayor a recepción',1,0)
		dw_lotes.setcolumn('presen_fecha')		
		dw_lotes.setrow(ll)
		dw_lotes.setfocus()		
		return
	end if
	
	l_status = dw_lotes.GetItemStatus(ll,'canti', Primary!)
	li_canacu = li_canacu + dw_lotes.object.canti[ll]
	if l_status <> NotModified! then	/* se modifico despues del retrieve */
		if li_canacu > exp.compro_canti then
			mbox(this,'ATENCION : Excede la cantidad de comprobantes definidos para el expediente ('+string(li_canacu)+'/'+string(exp.compro_canti)+')',1,0)
			dw_lotes.setcolumn('canti')		
			dw_lotes.setrow(ll)
			dw_lotes.setfocus()		
//			return
		end if
	end if
	
next

if mbox(this,'Acepta cambios ?',4,2) = 2 then
	triggerevent('ue_salir')
	return
end if

SetPointer(HourGlass!)

DECLARE ins_lote PROCEDURE FOR pri_expe_farma_lote
	@prm_ofi	= :exp.ofi,
	@prm_expe	= :exp.expe,
	@prm_farma= :li_farma,
	@prm_deno	= :ls_deno,
	@prm_canti = :li_canti,
	@prm_total = :le_total,
	@prm_rece_nro = 0,   
	@prm_ok = :ls_ok,   
	@prm_alta_fecha	 =:gd_hoy,
	@prm_carga_fecha = null,   
	@prm_actu_fecha = null,   
	@prm_presen_fecha=:ld_presen_fecha,
	@prm_nro_caja  = :ls_nrocaja,
	@prm_afi_dto   = :ld_afidto,
	@prm_lote=null;

DECLARE mod_lote PROCEDURE FOR pru_expe_farma_lote
	@prm_ofi	= :exp.ofi,
	@prm_expe	= :exp.expe,
	@prm_farma= :li_farma,
	@prm_lote	= :li_lote,
	@prm_deno	= :ls_deno,
	@prm_canti = :li_canti,
	@prm_total        = :le_total,
	@prm_rece_nro     = :ll_rece_nro,
	@prm_ok           = :ls_ok,   
	@prm_alta_fecha   = :ld_alta_fecha,   
	@prm_carga_fecha  = :ld_carga_fecha,   
	@prm_actu_fecha   = :gd_hoy,
	@prm_presen_fecha = :ld_presen_fecha,
	@prm_baja_fecha = :ld_baja_fecha,
	@prm_totcal = :le_totcal,
	@prm_rece_afi_valor = :le_rece_afi_valor,
	@prm_debcal = :le_debcal,
	@prm_audi_afi_valor = :le_audi_afi_valor,
	@prm_farma_dto = :le_farma_dto,
	@prm_debi_afi = :le_debi_afi,
	@prm_debi_farma = :le_debi_farma,
	@prm_debi_prepa = :le_debi_prepa,
	@prm_audi_fecha = :ld_audi_fecha,
	@prm_afi_dto      = :ld_afidto,
	@prm_nro_caja     = :ls_nrocaja;

DECLARE del_lote PROCEDURE FOR prd_expe_farma_lote
	@prm_ofi	= :exp.ofi,
	@prm_expe	= :exp.expe,
	@prm_farma= :li_farma,
	@prm_lote	= :li_lote ;


ll_rcount = dw_lotes.RowCount()
ll_dcount = dw_lotes.DeletedCount() 

/* trato los borrados */
if ll_dcount > 0 then
	for ll = 1 to ll_dcount
		li_lote = dw_lotes.GetItemNumber(ll,'lote',delete!,true)
		if li_lote > 0 then /* si 0 es una alta/baja junta,no hago nada */
			EXECUTE del_lote;
			IF SQLCA.SqlCode = -1 THEN
				Mbox ( "prd_expe_farma_lote", 2, 0 )
				Return 
			END IF
		end if
	next
end if

if ll_rcount > 0 then
	for ll = 1 to ll_rcount
		l_status = dw_lotes.GetItemStatus(ll,0, Primary!)
		if l_status <> NotModified! then	/* se modifico despues del retrieve */
			li_lote = dw_lotes.object.lote[ll]
			li_farma = dw_lotes.object.farma[ll]
			ls_deno = dw_lotes.object.deno[ll]
			li_canti= dw_lotes.object.canti[ll]
			le_total= dw_lotes.object.total[ll]
			ld_presen_fecha = dw_lotes.object.presen_fecha[ll]
			ll_rece_nro = dw_lotes.object.rece_nro[ll]
			ls_ok = dw_lotes.object.ok[ll]
			ld_alta_fecha = dw_lotes.object.alta_fecha[ll]
			ld_carga_fecha = dw_lotes.object.carga_fecha[ll]
			ld_baja_fecha = dw_lotes.object.baja_fecha[ll]
			le_totcal = dw_lotes.object.totcal[ll]
			le_rece_afi_valor = dw_lotes.object.rece_afi_valor[ll]
			le_debcal = dw_lotes.object.debcal[ll]
			le_audi_afi_valor = dw_lotes.object.audi_afi_valor[ll]
			le_farma_dto = dw_lotes.object.farma_dto[ll]
			le_debi_afi = dw_lotes.object.debi_afi[ll]
			le_debi_farma = dw_lotes.object.debi_farma[ll]
			le_debi_prepa = dw_lotes.object.debi_prepa[ll]
			ld_audi_fecha = dw_lotes.object.audi_fecha[ll]
			ld_afidto  = dw_lotes.Object.afi_dto[ll]
			ls_nrocaja = dw_lotes.Object.nro_caja[ll]
			
			if li_lote = 0 then /* alta */
				EXECUTE ins_lote;
				IF SQLCA.SqlCode = -1 THEN
					Mbox (  "pri_expe_farma_lote", 2, 0 )
					Return 
				else
					fetch ins_lote into :li_lote;
					close ins_lote;
				end if
				dw_lotes.object.lote[ll] = li_lote
			else
				EXECUTE mod_lote;
				IF SQLCA.SqlCode = -1 THEN
					Mbox ( "pru_expe_farma_lote", 2, 0 )
					Return 
				end if
			end if
		end if
	next
end if

COMMIT;

cb_ace.enabled = true
dw_lotes.ResetUpdate()
f_reset_cambios()

this.Event ue_restaurar (0,0)

end event
```

---

### [31/08/2026 13:21:04] Marcelo E. Paduan

al ejecutar el evento aceptar
error

---

### [31/08/2026 13:26:02] Agente

Marcelo, revisé el repo y probé contra `SwissDeVP10_prestaci`.

El SP vivo en DEV está bien creado y ejecuta sintácticamente correcto con todos los parámetros. En el repo también está bien esta parte:

```powerscript
@prm_alta_fecha   = :ld_alta_fecha,
@prm_carga_fecha  = :ld_carga_fecha,
```

Ese error:

```text
Incorrect syntax near '@prm_carga_fecha'
```

indica casi seguro que en el objeto PB que estás ejecutando quedó sin coma la línea anterior, así:

```powerscript
@prm_alta_fecha   = :ld_alta_fecha
@prm_carga_fecha  = :ld_carga_fecha,
```

La corrección puntual es agregar la coma después de `:ld_alta_fecha`.

También verifiqué que en `SwissQAP10_prestaci` no existe `pru_expe_farma_lote`; en DEV sí existe y está correcto. Si estás probando contra QA, primero hay que crear/actualizar el SP ahí.

---

### [31/08/2026 16:06:01] Marcelo E. Paduan

tengo un conflicto con PB en la rama Farmacia con el siguiente
 ---------- Source Control: Errors (15:49:13)
Pulling file: ws_objects/liqui14.pbl.src/wp_selexp_cir_farm.srw.Completed: 16, total: 16. Conflicted.
The file ws_objects/liqui14.pbl.src/wp_selexp_cir_farm.srw is in a conflicted state.
 ---------- Finished Errors (15:51:39)

y tengo que resolverlo y no se como. esto esta con GIT. como hacemos?

---

### [31/08/2026 16:08:45] Agente

Marcelo, ya lo resolví en el repo Git local.

El repo correcto es:

`/home/luispicone/.openclaw/workspace/git/smg-code-presmed`

Rama:

`TAREA_29420_MODULO_FARMACIA`

Hice un `fast-forward` contra `origin/TAREA_29420_MODULO_FARMACIA`. El archivo conflictuado quedó actualizado:

`ws_objects/liqui14.pbl.src/wp_selexp_cir_farm.srw`

Validé:

`git diff --name-only --diff-filter=U` no devuelve nada, o sea no quedan conflictos Git.

El estado actual sólo muestra cambios locales previos no relacionados:

`M ws_objects/liqui16.pbl.src/d_rep_productos_homo_imp.srd`  
`?? docs/`

Para PB: cerrá/reabrí o refrescá el workspace desde source control. Si PB sigue marcando conflicto, ya no viene de Git sino del estado interno/cache de PowerBuilder sobre ese objeto; en ese caso conviene importar/reconstruir `wp_selexp_cir_farm.srw` desde el source actualizado.

---

### [31/08/2026 16:53:07] Marcelo E. Paduan

te podes fijar en el mismo repositorio si hay alguna ventana de carga de datos donde el usuario ingrese la mandataria por codigo y / o por descripcion para ingresar datos
actualmente el campo manda de la d_expedientes_farma_cotiz es un DDW y el usuario quiere que esos datos no sean DDW para el ingreso sino que se puede ingresar el campo manda y la deno en campos separados que se puede buscar por ambos campos el resultado. y ademas si no encuentra correspondencia univoca que se abra una ventana pop up para elegir de una busqueda de mandatarias la deseada por el usuario
si existe un objeto ventana / dw con esta estructura. pasamela y la investigo

---

### [31/08/2026 16:56:36] Agente

Marcelo, revisé el repo `smg-code-presmed` en la rama `TAREA_29420_MODULO_FARMACIA`.

No encontré una ventana específica tipo `wr_busmanda` ya armada para seleccionar mandatarias como popup por código/descripción. Lo más parecido para copiar estructura es esto:

1. Patrón completo de búsqueda código/descripción + popup:
[wp_farma_lotes.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui14.pbl.src/wp_farma_lotes.srw:498)

Usa:
- DW de ingreso: [de_farma_lotes.srd](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui15.pbl.src/de_farma_lotes.srd:8)
- Popup: [wr_busfarma.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/valida2.pbl.src/wr_busfarma.srw:1)
- Lista del popup: [d_farma_list.srd](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/valida1.pbl.src/d_farma_list.srd:1)
- Estructura retorno: [str_farmacia.srs](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/valida1.pbl.src/str_farmacia.srs:1)

2. Datos/lista de mandatarias para usar como base:
[wp_mandatarias.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/farma.pbl.src/wp_mandatarias.srw:1)

Usa:
- ABM detalle: [d_mandataria.srd](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/farma.pbl.src/d_mandataria.srd:1)
- Lista: [d_listamanda.srd](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/farma.pbl.src/d_listamanda.srd:1)

3. Validación existente de mandataria por código/descripción:
[uo_nv_valdatos.sru](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/valida1.pbl.src/uo_nv_valdatos.sru:3743)

Funciones:
- `of_mandataria(integer a_manda, ref str_mandataria a_str)`
- `of_mandataria_row(string a_mandataria_deno, ref str_mandataria a_str)`

4. Ejemplo donde ya se ingresa mandataria por código y descripción separada:
[wp_notadebito_anulacion.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui19.pbl.src/wp_notadebito_anulacion.srw:376)

Ese evento valida `manda` por código y `ape` por descripción, pero si no encuentra coincidencia única no abre popup.

Conclusión técnica: para tu requerimiento, el mejor molde es `wp_farma_lotes + wr_busfarma`, adaptado a mandatarias usando `str_mandataria` y `d_listamanda`. La `d_expedientes_farma_cotiz` actual tiene `manda` como DDW en [d_expedientes_farma_cotiz.srd](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui14.pbl.src/d_expedientes_farma_cotiz.srd:53), y el código actual de carga valida `manda` sólo desde ese campo en [wp_ingexped_farm.srw](/home/luispicone/.openclaw/workspace/git/smg-code-presmed/ws_objects/liqui14.pbl.src/wp_ingexped_farm.srw:2231).

---

### [31/08/2026 17:21:10] Marcelo E. Paduan

la d_expedientes_farma_cotiz es una dw con codigo embebido.
 SELECT expedientes.ofi,
 expedientes.expe,
 expedientes.prepaga,
 expedientes.tipo,
 expedientes.recep_fecha,
 expedientes.revi_fecha,
 expedientes.perio,
 expedientes.manda,
 expedientes.deno,
 expedientes.entre_nro ,
 expedientes.grupo,
 expedientes.audi_vto,
 expedientes.fecha_entrega,
 isnull(expe_moneda.moneda,0),
 expe_moneda.cotizacion,
 expe_moneda.fecha_vigencia
 FROM expedientes, expe_moneda
 WHERE ( expedientes.ofi = :ofi ) AND
 ( expedientes.expe = :expe ) and
 ( expedientes.ofi *= expe_moneda.ofi ) and
 ( expedientes.expe *= expe_moneda.expe )


como hago para incorporar la descripcion de la mandataria y aplicar lo que vimos antes?

---

### [31/08/2026 17:21:44] Agente

Marcelo, para hacerlo prolijo no usaría `expedientes.deno` como descripción de mandataria, porque esa columna ya existe en la DW y corresponde a la descripción del expediente. Agregaría una columna nueva no actualizable, por ejemplo `manda_deno`.

En la SQL embebida de `d_expedientes_farma_cotiz`, agregaría `mandatarias` sólo para traer la descripción:

```sql
SELECT expedientes.ofi,
       expedientes.expe,
       expedientes.prepaga,
       expedientes.tipo,
       expedientes.recep_fecha,
       expedientes.revi_fecha,
       expedientes.perio,
       expedientes.manda,
       isnull(mandatarias.nombre, mandatarias.razon) as manda_deno,
       expedientes.deno,
       expedientes.entre_nro,
       expedientes.grupo,
       expedientes.audi_vto,
       expedientes.fecha_entrega,
       isnull(expe_moneda.moneda,0),
       expe_moneda.cotizacion,
       expe_moneda.fecha_vigencia
FROM expedientes,
     expe_moneda,
     mandatarias
WHERE ( expedientes.ofi = :ofi ) AND
      ( expedientes.expe = :expe ) AND
      ( expedientes.ofi *= expe_moneda.ofi ) AND
      ( expedientes.expe *= expe_moneda.expe ) AND
      ( expedientes.manda *= mandatarias.manda )
```

En la DW:
- `manda`: dejarlo como campo editable simple, sin DDW.
- `manda_deno`: agregarlo como campo editable visual para búsqueda por descripción, pero con `update = no`.
- `expedientes.manda` sigue siendo el único dato que se graba.

La lógica PB iría en el `itemchanged` de `dw_cab`:

```powerscript
case 'manda'
    ii_manda = long(data)

    if lib.of_vacio(ii_manda) then
        this.object.manda_deno[1] = ''
        mbox(parent,"Ingrese mandataria",1,0)
        valida.of_false_val("manda")
        return 1
    end if

    if val.of_mandataria(ii_manda, pr) = 0 then
        this.object.manda[1] = pr.manda
        this.object.manda_deno[1] = pr.razon

        // aca dejaria toda la logica actual que hoy depende de ii_manda:
        // CAI, dw_pend.Retrieve(ii_manda), grupo de pago, etc.
    else
        this.object.manda[1] = 0
        this.object.manda_deno[1] = ''
        mbox(parent, pr.error, 1, 0)
        valida.of_false_val("manda")
        return 1
    end if

    return 2
```

Y para descripción:

```powerscript
case 'manda_deno'
    ls_manda_deno = trim(data)

    if lib.of_vacio(ls_manda_deno) then
        this.object.manda[1] = 0
        return 2
    end if

    if val.of_mandataria_row(ls_manda_deno, pr) then
        this.object.manda[1] = pr.manda
        this.object.manda_deno[1] = pr.razon

        // disparar o reutilizar la misma rutina de carga post-mandataria
        return 2
    else
        // aca abriria el popup nuevo tipo wr_busmanda
        parent.Event ue_buscar_mandataria()
        return 2
    end if
```

Para el popup, no vi uno ya hecho de mandatarias. Yo lo armaría copiando el patrón de `wr_busfarma`, usando `d_listamanda` como lista y `str_mandataria` como retorno.

Objetos base para copiar:
- `wr_busfarma`
- `de_busfarma`
- `d_farma_list`
- `str_farmacia`

Adaptados a:
- `wr_busmanda`
- `de_busmanda`
- `d_listamanda`
- `str_mandataria`

Clave de diseño: cuando el usuario ingresa código o descripción y hay una única coincidencia, resolvés directo. Si hay 0 o más de 1, abrís `wr_busmanda`, el usuario elige y volvés con `CloseWithReturn` cargando `manda` y `manda_deno`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
