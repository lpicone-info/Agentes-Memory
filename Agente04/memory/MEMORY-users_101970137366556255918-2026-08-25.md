# Memoria de conversación — Marcelo E. Paduan

## Datos

- **Fecha:** 25/08/2026
- **Usuario:** Marcelo E. Paduan
- **ID Google Chat:** `users/101970137366556255918`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 26
- **Inicio:** 09:14:14
- **Fin:** 16:52:42

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/yq3ndyaaaae`

## Archivos revisados

- `803e7b4b-16b0-400f-be91-dd8378d6a647.jsonl`

---

## Transcripción completa

### [25/08/2026 09:14:14] Marcelo E. Paduan

hola 99

---

### [25/08/2026 09:15:27] Agente

Hola Marcelo, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [25/08/2026 09:15:42] Marcelo E. Paduan

este es el sp definitivo hasta el momento
prs_expe_farma_lotes
esta en el repositorio mnt mepaduan R30278 para que avancemos en el resto de los sp creando el pri, pru y el prd con los siguientes nombres pri_expe_farma_lote.sql, pru_expe_farma_lote.sql , prd_expe_farma_lote.sql
y guardamelos en el mismo repo

---

### [25/08/2026 09:20:51] Agente

Marcelo, listo. Creé en `/mnt/Agentes/mepaduan/R30278`:

- [pri_expe_farma_lote.sql](</mnt/Agentes/mepaduan/R30278/pri_expe_farma_lote.sql>)
- [pru_expe_farma_lote.sql](</mnt/Agentes/mepaduan/R30278/pru_expe_farma_lote.sql>)
- [prd_expe_farma_lote.sql](</mnt/Agentes/mepaduan/R30278/prd_expe_farma_lote.sql>)

Tomé como base funcional el `prs_expe_farma_lotes.sql` y los procedimientos legacy `pri_expe_farmacias`, `pru_xexpe_farmacias` y `prd_expe_farmacias`. Los nuevos SP quedaron con parámetros `@prm_`, `CREATE PROCEDURE` con paréntesis, sin `RTRIM` y sin `;`.

Validaciones ejecutadas: archivos creados, nombres de SP verificados, encoding `us-ascii`, sin BOM, finales `LF`, sin `RTRIM` ni punto y coma. Riesgo: no ejecuté validación contra Sybase; solo validación estática de archivos. Además, `/mnt/Agentes/mepaduan/R30278` no figura como working copy Git ni SVN desde consola.

---

### [25/08/2026 09:48:04] Marcelo E. Paduan

y si tomas la ventana wp_farma_lotes que te deje en el repo R30278 y le haces los ajustes necesarios para incoporar la funcionalidad del pri y prd
actualmente estas los botones pb_2 y pb_1, borrar y agregar
al agregar estimo que agrega una fila a la dw pero no se como lo graba, si es con el boton aceptar, evento ue_aceptar.
actualmente la ventana esta trayendo la informacion con el prs y estaria funcionando bien.
decime si podes ampliar la funcionalidad y dejarme la nueva version en el repositorio.

---

### [25/08/2026 09:51:01] Agente

Sí, Marcelo. Pude ampliarla y ya dejé la nueva versión en el repo.

Ajusté [wp_farma_lotes.srw](</mnt/Agentes/mepaduan/R30278/wp_farma_lotes.srw>) para que el `ue_aceptar` use los SP nuevos:

- `pri_expe_farma_lote` para altas
- `pru_expe_farma_lote` para modificaciones
- `prd_expe_farma_lote` para bajas

La lógica ya estaba armada: `pb_1` agrega una fila en la DW, `pb_2` marca la fila para borrar, y el grabado real se hace en `ue_aceptar`. Lo que faltaba era apuntar esa grabación a los SP nuevos y sus parámetros `@prm_`.

Validaciones ejecutadas: verifiqué que no queden referencias a `pri_expe_farmacias`, `pru_xexpe_farmacias` ni `prd_expe_farmacias` dentro de la ventana; confirmé que el archivo sigue en `UTF-16LE` con BOM y finales `CRLF`. Riesgo: no compilé en PowerBuilder ni ejecuté prueba funcional contra Sybase; la validación fue estática sobre el export `.srw`.

---

### [25/08/2026 10:56:08] Marcelo E. Paduan

bien. ahora te paso el evento dw_sel itemchanged
quiero que lo revises y la idea es que el dw_sel tiene 3 campos. si el usuario ingresa ejemplo manda_farma se realice el filtrado solo por ese campo si el resto de lso campos / filtros estan nulos o limpios
digamos cuando elijo manda_farma no tiene que condicionar a farma y razon. y lo mismo si cargo farma o razon no tiene que condicionar a manda_farma.
ahora si elijo farma o razon . estos tiene relacion entre ellos asi que si cargo un codigo farma tiene que traer y mostrar la razon y /o viceversa.

if not isvalid( val ) then return

string ls_null
long ll_null
datetime ldt_null
str_farmacia lfar

setnull(ls_null)
setnull(ll_null)
far = lfar

choose case is_colname
 case 'manda_farma'
 this.object.razon[1] = ls_null
 this.object.farma[1] = ll_null
 far.manda_farma = long( data )
 far.razon = ls_null
 case 'farma'
 this.object.farma[1] = ll_null
 this.object.farma[1] = ll_null
 far.manda_farma = ll_null
 far.razon = data

 case 'razon'
 this.object.manda_farma[1] = ll_null
 this.object.farma[1] = ll_null
 far.manda_farma = ll_null
 far.razon = data
end choose

/Si no existe la farmacia, activo la ventana de busqueda/
far.manda = exp.manda

if not lib.of_vacio(far.manda) and (not lib.of_vacio(far.manda_farma) or not lib.of_vacio(far.razon)) then
 if val.of_farmacia_row ( far ) then
 this.object.manda_farma[1] = far.manda_farma
 this.object.farma[1] = far.farma
 this.object.razon[1] = far.razon
 parent.Event ue_restaurar (0,0)
 else
 parent.Event ue_ayuda (0, 0)
 end if
 return 2
end if

/-------------------------------------------------------/

return 0

---

### [25/08/2026 10:58:16] Agente

Sí, Marcelo. Ajusté [wp_farma_lotes.srw](</mnt/Agentes/mepaduan/R30278/wp_farma_lotes.srw>) en el evento `dw_sel.itemchanged`.

Quedó así la lógica:

- Si cambia `manda_farma`, limpia `farma` y `razon`, y busca solo por `manda + manda_farma`.
- Si cambia `farma`, limpia `manda_farma` y `razon`, busca por `farma`, y si encuentra completa `manda_farma`, `farma` y `razon`.
- Si cambia `razon`, limpia `manda_farma` y `farma`, busca por `razon`, y si encuentra completa los datos relacionados.
- El retrieve de lotes sigue entrando por `parent.Event ue_restaurar(0,0)`.

Validé que el archivo siga en `UTF-16LE` con BOM y finales `CRLF`. No compilé en PowerBuilder; la validación fue estática sobre el `.srw`.

---

### [25/08/2026 10:59:28] Marcelo E. Paduan

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 10:59 AM (America/Buenos_Aires)
Reference UTC: 2026-08-25 13:59 UTC

---

### [25/08/2026 11:00:09] Agente

NO_REPLY

---

### [25/08/2026 11:00:10] Marcelo E. Paduan

no, quiero qu eme pases por aca la fraccion de codigo porque estoy modificando la ventana en el IDE. luego actualizo el archivo en el repo. ahora no. pasame solo el codigo

---

### [25/08/2026 11:00:21] Agente

```powerscript
if not isvalid( val ) then return

string ls_null
long ll_null
datetime ldt_null
str_farmacia lfar

setnull(ls_null)
setnull(ll_null)
far = lfar
far.manda = exp.manda

choose case is_colname
	case 'manda_farma'
		this.object.farma[1] = ll_null
		this.object.razon[1] = ls_null
		far.manda_farma = long(data)

	case 'farma'
		this.object.manda_farma[1] = ll_null
		this.object.razon[1] = ls_null
		far.farma = long(data)

	case 'razon'
		this.object.manda_farma[1] = ll_null
		this.object.farma[1] = ll_null
		far.razon = data
end choose

/*Si no existe la farmacia, activo la ventana de busqueda*/
if not lib.of_vacio(far.farma) or &
	(not lib.of_vacio(far.manda) and not lib.of_vacio(far.manda_farma)) or &
	not lib.of_vacio(far.razon) then

	if val.of_farmacia_row ( far ) then
		this.object.manda_farma[1] = far.manda_farma
		this.object.farma[1] = far.farma
		this.object.razon[1] = far.razon
		parent.Event ue_restaurar (0,0)
	else
		parent.Event ue_ayuda (0, 0)
	end if

	return 2
end if

/*-------------------------------------------------------*/

return 0
```

---

### [25/08/2026 11:20:07] Marcelo E. Paduan

fijate el punto 4.8 Estetica Swiss, resize, mover columnas y guardar del doc
R30278_analisis_desarrollo
y tenes disponible en el repo la ventana wp_farma_lotes.
fijate si podes hacer esa funcionalidad. si es sobre la dw te la dejo d_expe_farmacias en el repo tambien

---

### [25/08/2026 11:25:19] Agente

Marcelo, quedó aplicada la funcionalidad del punto 4.8 en los archivos del repo/carpeta `R30278`.

Archivos impactados:
- `/mnt/Agentes/mepaduan/R30278/wp_farma_lotes.srw`
- `/mnt/Agentes/mepaduan/R30278/d_expe_farmacias.srd`

Cambios realizados:
- `wp_farma_lotes`: dejé la ventana como `resizable = true`.
- Agregué evento `resize` para que `dw_lotes` crezca con la ventana y los botones inferiores acompañen el alto/ancho.
- `d_expe_farmacias`: habilité `grid.columnmove=yes`, que es el comportamiento necesario para mover columnas en la DataWindow.
- El guardado con `Ctrl + click derecho` ya queda cubierto por herencia de `dw_clase`, que tiene implementado `SaveAs()` en `rbuttondown`.

Validaciones:
- Confirmé que ambos archivos siguen en `UTF-16LE` con BOM.
- Confirmé finales de línea `CRLF`, sin mezcla.
- Confirmé que `wp_farma_lotes` usa `d_expe_farmacias`.
- Confirmé que existe un solo `event resize`.
- No pude validar por `svn diff/status` porque `/mnt/Agentes/mepaduan/R30278` no figura como working copy SVN/Git en disco.

Riesgo pendiente: no pude compilar ni probar desde el IDE PowerBuilder, así que queda validación funcional visual pendiente en PB.

---

### [25/08/2026 11:59:37] Marcelo E. Paduan

si se habre automaticamente
También se debe agregar la lógica al proceso para que al dar doble click
 sobre una fila en la grilla, se abra automáticamente la ventana de
Carga de Recetas


aca tengo qeu pasar o enviar datos como parametros o una estructura mas completa para carga la ventana destino que es la
También se debe agregar la lógica al proceso para que al dar doble click
 sobre una fila en la grilla, se abra automáticamente la ventana de
Carga de Recetas wp_carga_receta esta enviando/recibiendo la str str_expe?
decime si esta dw dw_farma_lote tiene que completarse con mas datos que vienen de la ventana que llama wp_farma_lotes?

---

### [25/08/2026 12:02:47] Agente

Marcelo, por lo que vi:

`wp_carga_receta` no recibe directamente `str_expe_far` en el `open`. El `choose case` acepta:
- `str_expe`
- `str_receta`

Entonces la llamada actual:

```powerscript
OpenSheetWithParm(wp_carga_receta, exp, ParentWindow(), 1 , Original!)
```

está bien como tipo de parámetro, pero para que abra parada en la farmacia/lote de la fila seleccionada, antes hay que cargar los globales que `wp_carga_receta` usa en `ue_postopen`:

```powerscript
event doubleclicked;call super::doubleclicked;long ll_row

ll_row = this.GetRow()
if ll_row < 1 then return

if this.object.lote[ll_row] = 0 then return

glob.gi_farma_last_ofi   = exp.ofi
glob.gl_farma_last_expe  = exp.expe
glob.gi_farma_last_farma = this.object.farma[ll_row]
glob.gi_farma_last_lote  = this.object.lote[ll_row]

OpenSheetWithParm(wp_carga_receta, exp, ParentWindow(), 1 , Original!)

end event
```

Respuesta puntual: sí, `dw_farma_lote` de `wp_carga_receta` necesita quedar completa, pero no hace falta mandarle toda la estructura desde `wp_farma_lotes` si mantenemos el patrón existente. Con `exp` + los globales `ofi/expe/farma/lote`, `wp_carga_receta` luego llama a `val.of_farma_lote_row(istr_expefar)` y completa `farma`, `razon`, `manda_farma`, `lote`, `deno`, `total`, `canti`, `presen_fecha`, etc.

El punto débil actual es que `rowfocuschanged` y otros eventos usan a veces `dw_sel.object.farma[1]`; si filtraste por `manda_farma`, ese valor puede venir nulo/cero. Para doble click conviene tomar siempre `farma` y `lote` desde la fila de `dw_lotes`, como en el fragmento de arriba.

---

### [25/08/2026 13:21:09] Marcelo E. Paduan

claro.. si si hay que tomarlo de la dw_lote
podes hacer esos ajustes en las ventanas y yo luego lo importo al IDE.

---

### [25/08/2026 13:24:05] Agente

Marcelo, listo. Dejé ajustado `/mnt/Agentes/mepaduan/R30278/wp_farma_lotes.srw`.

Cambios aplicados:
- Doble click en `dw_lotes`: ahora toma `ofi/expe/farma/lote` desde la fila seleccionada y carga los globales que usa `wp_carga_receta`.
- Cerrar lote y reabrir lote: ahora toman `farma` desde `dw_lotes`, no desde `dw_sel`.
- Estado al cambiar fila: ahora consulta con `farma/lote` de la fila.
- Borrar y liquidar: también toman la farmacia de la fila seleccionada.

Validé:
- Archivo sigue en `UTF-16LE` con BOM.
- Finales de línea siguen `CRLF`, sin mezcla.
- Hay un solo evento `doubleclicked`.
- No compilé en PowerBuilder; queda pendiente la prueba desde el IDE al importar.

---

### [25/08/2026 15:38:51] Marcelo E. Paduan

99, podes analizar la ventana e indicarme bajo que condiciones el boton cb_lote se oculta, deshabililta , cambia de nombre etc... porque estamos notando una funcionalidad variable y no conocemos el motivo. poder revisarlo=?
la ventana es la wp_farma_lotes y esta en el repo actualizada
y por otro lado el campo presen_fecha de la dw_lotes que valor por defecto toma cuando hacemos click al boton agregar, pb_1

---

### [25/08/2026 15:40:34] Agente

Marcelo, revisado.

En `wp_farma_lotes`, el botón `cb_lote` no se oculta por código propio. No encontré `cb_lote.visible = false`. Lo que cambia es `Enabled` y `Text`.

Condiciones detectadas:

- Al abrir la ventana:
  - Si `wf_cierre_liq(exp.perio, exp.ofi)` devuelve `false`, se deshabilita:
    ```powerscript
    cb_lote.enabled = false
    ```
  - Esto pasa junto con `cb_liquidar`, `cb_imp_cajas`, `cb_limpiar`, `cb_ace`.

- Al cambiar de fila en `dw_lotes`, evento `rowfocuschanged`:
  - Si la fila no es válida:
    ```powerscript
    uo_status.of_reset()
    return
    ```
    No cambia explícitamente `cb_lote`, pero limpia el indicador visual de estado.

  - Si `lote = 0`, o sea lote nuevo todavía no grabado:
    ```powerscript
    uo_status.of_reset()
    ```
    Tampoco cambia explícitamente `cb_lote.Enabled`, y esto puede explicar parte de la “funcionalidad variable”: puede quedar con el estado anterior si no se fuerza `cb_lote.Enabled = FALSE`.

  - Si `uo_status.of_set_status(ls)` devuelve `true`, evalúa `uo_status.is_stat`:
    ```powerscript
    case 'C'
        cb_lote.Enabled = TRUE
        cb_lote.Text = 'Cerrar L&ote'

    case 'A'
        cb_lote.Enabled = TRUE
        cb_lote.Text = 'Abrir L&ote'

    case 'L'
        cb_lote.Enabled = TRUE
        cb_lote.Text = 'Abrir L&ote'

    case else
        cb_lote.Enabled = FALSE
    ```

  - Si `uo_status.of_set_status(ls)` devuelve `false`:
    ```powerscript
    cb_lote.Enabled = FALSE
    ```

Traducción funcional:
- Estado `C` = En carga: botón habilitado como `Cerrar Lote`.
- Estado `A` = Auditoría: botón habilitado como `Abrir Lote`.
- Estado `L` = A liquidar: botón habilitado como `Abrir Lote`.
- Estado `O`, `Q`, `D` u otros: en esta ventana no tienen acción de lote, por eso queda deshabilitado.
- Si el SP `prs_farma_status_expe` devuelve un estado no contemplado o error lógico, `uo_status.of_set_status()` devuelve `false` y el botón queda deshabilitado.

Sobre `presen_fecha` al hacer click en `pb_1`:
- `pb_1` dispara:
  ```powerscript
  parent.triggerevent('ue_agregar')
  ```
- En `ue_agregar`, la fila nueva toma:
  ```powerscript
  dw_lotes.object.presen_fecha[li_fila] = id_fec
  ```
- Y `id_fec` se inicializa en el `open` así:
  ```powerscript
  id_fec = exp.recep_fecha
  ```

Entonces el valor por defecto de `dw_lotes.presen_fecha` al agregar es `exp.recep_fecha`, que viene de la ventana/proceso que abrió `wp_farma_lotes`.

Observación importante: para lotes nuevos con `lote = 0`, hoy el código no deshabilita explícitamente `cb_lote`; solo hace `uo_status.of_reset()`. Yo reforzaría ahí con `cb_lote.Enabled = FALSE` para evitar que quede arrastrado el estado del lote anterior.

---

### [25/08/2026 16:02:10] Usuario

/codex status

---

### [25/08/2026 16:02:10] Agente

Codex app-server: connected
Models: gpt-5.5, gpt-5.4, gpt-5.4-mini
Account: agente99＿ia@infomedical.com.ar
Rate limits: Codex: primary 31% left ⏱11m 58s · secondary 89% left ⏱6d 19h
MCP servers: 1
Skills: 5

---

### [25/08/2026 16:33:55] Marcelo E. Paduan

al alegir un item de la grilla
no me esta borrando el registro ni preguntando nada
string ls_deno
long li_canti,li_lote,li_canacu, li_farma
decimal{2} le_total,le_totacu
datetime ld_presen_fecha, ld_alta_fecha, ld_carga_fecha, ldt_periodo
long ll_rcount,ll,ll_dcount,ll_rc,ll_rd, ll_rece_nro
string ls_busca,ls_error,ls_ok, ls_nrocaja
decimal ld_afidto
integer li_ofi

dwItemStatus l_status

dw_lotes.AcceptText( )
ll_rcount = dw_lotes.RowCount()

//Cierre de Período - Liquidación
ldt_periodo = exp.perio
li_ofi = exp.ofi
if not wf_cierre_liq (ldt_periodo,li_ofi) then
 cb_liquidar.enabled = false
 cb_imp_cajas.enabled = false
 cb_limpiar.enabled = false
 cb_ace.enabled = false
 cb_lote.enabled = false
 return
else
end if

if dw_lotes.modifiedcount() + dw_lotes.deletedcount() = 0 then return

li_farma = dw_sel.object.farma[1]
for ll = 1 to ll_rcount
 if trim(dw_lotes.object.deno[ll])='' and &
 dw_lotes.object.canti[ll] < 1 and &
 dw_lotes.object.total[ll] < 1 then
 dw_lotes.object.lote[ll] = 0
 dw_lotes.deleteRow(ll)
 ll_rcount --
 end if
next

if li_farma <> ii_farma_calc then
 ii_farma_calc = li_farma
 select sum(canti),sum(total) into :ii_canti,:ie_total
 from expe_farmacias
 where ofi=:exp.ofi and expe=:exp.expe
 and (farma <> :li_farma)
 and baja_fecha is null;
 if sqlca.sqlcode <> 0 then mbox(this,'error del sistema',1,0)
end if

if isnull(ii_canti) then ii_canti = 0
if isnull(ie_total) then ie_total = 0
li_canacu = ii_canti
le_totacu = ie_total

for ll = 1 to ll_rcount
 if trim(dw_lotes.object.deno[ll])='' then
 mbox(this,'Complete denominación',1,0)
 dw_lotes.setcolumn('deno')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if dw_lotes.object.canti[ll] < 1 then
 mbox(this,'Complete cantidad',1,0)
 dw_lotes.setcolumn('canti')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if dw_lotes.object.total[ll] < 1 then
 mbox(this,'Complete total',1,0)
 dw_lotes.setcolumn('total')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if isnull(dw_lotes.object.presen_fecha[ll]) then
 mbox(this,'Complete fecha de presentación',1,0)
 dw_lotes.setcolumn('presen_fecha')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if
 if dw_lotes.object.presen_fecha[ll] > exp.recep_fecha then
 mbox(this,'Fecha de presentación mayor a recepción',1,0)
 dw_lotes.setcolumn('presen_fecha')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
 return
 end if

 l_status = dw_lotes.GetItemStatus(ll,'canti', Primary!)
 li_canacu = li_canacu + dw_lotes.object.canti[ll]
 if l_status <> NotModified! then /* se modifico despues del retrieve */
 if li_canacu > exp.compro_canti then
 mbox(this,'ATENCION : Excede la cantidad de comprobantes definidos para el expediente ('+string(li_canacu)+'/'+string(exp.compro_canti)+')',1,0)
 dw_lotes.setcolumn('canti')
 dw_lotes.setrow(ll)
 dw_lotes.setfocus()
// return
 end if
 end if

next

if mbox(this,'Acepta cambios ?',4,2) = 2 then
 triggerevent('ue_salir')
 return
end if

SetPointer(HourGlass!)

DECLARE ins_lote PROCEDURE FOR pri_expe_farma_lote
 @prm_ofi = :exp.ofi,
 @prm_expe = :exp.expe,
 @prm_farma= :li_farma,
 @prm_deno = :ls_deno,
 @prm_canti = :li_canti,
 @prm_total = :le_total,
 @prm_rece_nro = 0,
 @prm_ok = :ls_ok,
 @prm_alta_fecha =:gd_hoy,
 @prm_carga_fecha = null,
 @prm_actu_fecha = null,
 @prm_presen_fecha=:ld_presen_fecha,
 @prm_nro_caja = :ls_nrocaja,
 @prm_afi_dto = :ld_afidto,
 @prm_lote=null;

DECLARE mod_lote PROCEDURE FOR pru_expe_farma_lote
 @prm_ofi = :exp.ofi,
 @prm_expe = :exp.expe,
 @prm_farma= :li_farma,
 @prm_lote = :li_lote,
 @prm_deno = :ls_deno,
 @prm_canti = :li_canti,
 @prm_total = :le_total,
 @prm_rece_nro = :ll_rece_nro,
 @prm_ok = :ls_ok,
 @prm_alta_fecha = :ld_alta_fecha,
 @prm_carga_fecha = :ld_carga_fecha,
 @prm_actu_fecha = :gd_hoy,
 @prm_nro_caja = :ls_nrocaja,
 @prm_afi_dto = :ld_afidto,
 @prm_presen_fecha =:ld_presen_fecha;

DECLARE del_lote PROCEDURE FOR prd_expe_farma_lote
 @prm_ofi = :exp.ofi,
 @prm_expe = :exp.expe,
 @prm_farma= :li_farma,
 @prm_lote = :li_lote ;


ll_rcount = dw_lotes.RowCount()
ll_dcount = dw_lotes.DeletedCount()

/* trato los borrados */
if ll_dcount > 0 then
 for ll = 1 to ll_dcount
 li_lote = dw_lotes.GetItemNumber(ll,'lote',delete!,true)
 if li_lote > 0 then /* si 0 es una alta/baja junta,no hago nada */
 EXECUTE del_lote;
 IF SQLCA.SqlCode = -1 THEN
 Mbox ( "prd_expe_farma_lote", 2, 0 )
 Return
 END IF
 end if
 next
end if

if ll_rcount > 0 then
 for ll = 1 to ll_rcount
 l_status = dw_lotes.GetItemStatus(ll,0, Primary!)
 if l_status <> NotModified! then /* se modifico despues del retrieve */
 li_lote = dw_lotes.object.lote[ll]
 ls_deno = dw_lotes.object.deno[ll]
 li_canti= dw_lotes.object.canti[ll]
 le_total= dw_lotes.object.total[ll]
 ld_presen_fecha = dw_lotes.object.presen_fecha[ll]
 ll_rece_nro = dw_lotes.object.rece_nro[ll]
 ls_ok = dw_lotes.object.ok[ll]
 ld_alta_fecha = dw_lotes.object.alta_fecha[ll]
 ld_carga_fecha = dw_lotes.object.carga_fecha[ll]
 ld_afidto = dw_lotes.Object.afi_dto[ll]
 ls_nrocaja = dw_lotes.Object.nro_caja[ll]

 if li_lote = 0 then /* alta */
 EXECUTE ins_lote;
 IF SQLCA.SqlCode = -1 THEN
 Mbox ( "pri_expe_farma_lote", 2, 0 )
 Return
 else
 fetch ins_lote into :li_lote;
 close ins_lote;
 end if
 dw_lotes.object.lote[ll] = li_lote
 else
 EXECUTE mod_lote;
 IF SQLCA.SqlCode = -1 THEN
 Mbox ( "pru_expe_farma_lote", 2, 0 )
 Return
 end if
 end if
 end if
 next
end if

COMMIT;

cb_ace.enabled = true
dw_lotes.ResetUpdate()
f_reset_cambios()

this.Event ue_restaurar (0,0)
dw_lotes en la ventana wp_fama_lotes
quiero solo probar el prd_expe_farma_lote

---

### [25/08/2026 16:34:46] Agente

Marcelo, para probar **solo** `prd_expe_farma_lote`, no lo probaría desde `ue_aceptar` todavía, porque ahí todavía tenés este problema:

```powerscript
li_farma = dw_sel.object.farma[1]
```

Si filtraste por `manda_farma`, `dw_sel.farma` puede estar nulo/cero. Para borrar un lote elegido en la grilla, `farma` tiene que salir de `dw_lotes`.

Además, `pb_2` hoy no pregunta nada por diseño: solo hace `dw_lotes.DeleteRow(li_row)`. La pregunta aparece recién en `ue_aceptar`, si `DeletedCount() > 0`.

Para probar directo el `prd`, podés reemplazar temporalmente el evento `clicked` de `pb_2` por esto:

```powerscript
event clicked;call super::clicked;int li_row, li_lote, li_recetas, li_farma

li_row = dw_lotes.GetRow()
if li_row = 0 then return

li_farma = dw_lotes.object.farma[li_row]
li_lote  = dw_lotes.object.lote[li_row]

if lib.of_vacio(li_farma) or lib.of_vacio(li_lote) then return

select count(*)
into   :li_recetas
from   expe_farma_recetas
where  ofi = :exp.ofi
and    expe = :exp.expe
and    farma = :li_farma
and    lote = :li_lote
and    baja_fecha is null ;

if SQLCA.SQLCode <> 0 then
    MBox('expe_farma_recetas', 2, 0)
    return
end if

if not lib.of_vacio(li_recetas) then
    MBox(parent, 'No puede eliminarse, pues hay ' + string(li_recetas) + ' recetas cargadas.', 1, 0)
    return
end if

if MBox(this, 'Confirma borrar lote ' + string(li_lote) + ' de farmacia ' + string(li_farma) + ' ?', 4, 2) = 2 then return

DECLARE del_lote PROCEDURE FOR prd_expe_farma_lote
    @prm_ofi   = :exp.ofi,
    @prm_expe  = :exp.expe,
    @prm_farma = :li_farma,
    @prm_lote  = :li_lote ;

EXECUTE del_lote;

if SQLCA.SQLCode = -1 then
    MBox("prd_expe_farma_lote", 2, 0)
    ROLLBACK;
    return
end if

COMMIT;

this.Event ue_restaurar(0,0)

end event
```

Con esto probás exclusivamente el `prd_expe_farma_lote`, tomando `farma/lote` desde la fila seleccionada de `dw_lotes`, sin depender de `dw_sel` ni del `DeletedCount()` de la DataWindow.

---

### [25/08/2026 16:52:14] Marcelo E. Paduan

porque no ejecutamos direcgtamnte en pb_2 la validaciones y el prd? pasame el limpio el pb2 click y el ue acpetar sin el borrar (prd)

---

### [25/08/2026 16:52:42] Agente

Sí, Marcelo. Conviene más: `pb_2` borra directo con validación + `prd`, y `ue_aceptar` queda solo para alta/modificación.

**pb_2 clicked limpio:**
```powerscript
event clicked;call super::clicked;int li_row, li_lote, li_recetas, li_farma

li_row = dw_lotes.GetRow()
if li_row = 0 then return

li_farma = dw_lotes.object.farma[li_row]
li_lote  = dw_lotes.object.lote[li_row]

if lib.of_vacio(li_farma) or lib.of_vacio(li_lote) then return

select count(*)
into   :li_recetas
from   expe_farma_recetas
where  ofi = :exp.ofi
and    expe = :exp.expe
and    farma = :li_farma
and    lote = :li_lote
and    baja_fecha is null ;

if SQLCA.SQLCode <> 0 then
	MBox('expe_farma_recetas', 2, 0)
	return
end if

if not lib.of_vacio(li_recetas) then
	MBox(parent, 'No puede eliminarse, pues hay ' + string(li_recetas) + ' recetas cargadas.', 1, 0)
	return
end if

if MBox(this, 'Confirma borrar lote ' + string(li_lote) + ' de farmacia ' + string(li_farma) + ' ?', 4, 2) = 2 then return

DECLARE del_lote PROCEDURE FOR prd_expe_farma_lote
	@prm_ofi   = :exp.ofi,
	@prm_expe  = :exp.expe,
	@prm_farma = :li_farma,
	@prm_lote  = :li_lote ;

EXECUTE del_lote;

if SQLCA.SQLCode = -1 then
	MBox("prd_expe_farma_lote", 2, 0)
	ROLLBACK;
	return
end if

COMMIT;

this.Event ue_restaurar(0,0)

end event
```

**En `ue_aceptar`, sacá completo este bloque:**
```powerscript
DECLARE del_lote PROCEDURE FOR prd_expe_farma_lote
	@prm_ofi	= :exp.ofi,
	@prm_expe	= :exp.expe,
	@prm_farma= :li_farma,
	@prm_lote	= :li_lote ;
```

Y también sacá completo este bloque:
```powerscript
ll_dcount = dw_lotes.DeletedCount() 

/* trato los borrados */
if ll_dcount > 0 then
	for ll = 1 to ll_dcount
		li_lote = dw_lotes.GetItemNumber(ll,'lote',delete!,true)
		if li_lote > 0 then
			EXECUTE del_lote;
			IF SQLCA.SqlCode = -1 THEN
				Mbox ( "prd_expe_farma_lote", 2, 0 )
				Return 
			END IF
		end if
	next
end if
```

Y dentro del loop de grabación de altas/modificaciones, agregá `li_farma` desde la fila:

```powerscript
li_lote = dw_lotes.object.lote[ll]
li_farma = dw_lotes.object.farma[ll]
ls_deno = dw_lotes.object.deno[ll]
```

Así `ue_aceptar` ya no borra nada, y `prd_expe_farma_lote` queda probado exclusivamente desde `pb_2`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
