# 2026-06-14 - Memoria operativa

## AFILMED - Analisis Proceso Simulacion Norm. 2155 / Agendar

- Humana interlocutora: Norma.
- Repositorios usados para el analisis:
  - SVN: `~/.openclaw/workspace/svn/infomedical.LuHernandez-swiss-mp-prod-base_de_datos-sybase-sybase-DB1-sybase-afilmed-DDL-trunk`.
  - Git: `~/.openclaw/workspace/git/smg-code-afilmed`, branch `main`.
- Profile DB disponible y usado en consultas de solo lectura: `SwissDeVP10_afilmed`. No guardar ni exponer secretos.
- Se genero el documento final:
  - `/mnt/Agentes/Norma/AFILMED/ANALISIS/Simulacion_2155.docx`.
  - Fuente trazable: `/mnt/Agentes/Norma/AFILMED/ANALISIS/Simulacion_2155.md`.
- Validaciones realizadas al generar el documento: repositorios y carpeta destino existentes, branch `main` en Git, lectura de objetos PB/SQL, consulta de solo lectura al profile `SwissDeVP10_afilmed`, conversion con `pandoc`, y validacion interna del `.docx` con `unzip -t` sin errores.
- No se modifico codigo ni DDL para este analisis.

## AFILMED - Mensaje "El proceso ya fue ejecutado" en Simulacion 2155

- Se ubico el mensaje del flujo `Facturacion / Proceso Simulacion Norm. 2155 / Agendar` en el SP:
  - `procedures/afs_valid_mconfig_gral_batch.sql`.
- Condicion del mensaje:
  - El SP compara el periodo solicitado `@prm_perio` contra `masivos_config_gral_batch.proce_fecha` para el mismo `ente` y `tipo_proc_id`.
  - Compara solo anio-mes (`yyyymm`).
  - Si `yyyymm(@prm_perio) <= yyyymm(proce_fecha)`, setea `@var_error = 'El proceso ya fue ejecutado.'`, ejecuta `RAISERROR 99999 @var_error` y retorna `-1`.
- Punto de llamada PowerBuilder:
  - Ventana `wp_prefactura_n2155.srw`.
  - Funcion `wf_valid_mconfig_gral_batch()` declara y ejecuta el SP `afs_valid_mconfig_gral_batch`.
  - Evento `ue_agendar` invoca esa validacion antes de grabar la agenda.
  - Si retorna `-1`, limpia la pantalla con `triggerevent('ue_limpiar')`, setea `message.returnvalue = -1` y corta el flujo.
  - PowerBuilder toma `SQLCA.SQLErrText` y lo muestra con `mbox(ls_mensaje)`.
- Para Norm. 2155, el `tipo_proc_id` relevado es `PRESIM`.
- En `SwissDeVP10_afilmed`, al momento del analisis se verifico:
  - `ente = 2`.
  - `tipo_proc_id = PRESIM`.
  - `proce_fecha = 01/06/2026`.
- Implicancia operativa: con ese dato, el mensaje se dispara al intentar agendar/procesar `PRESIM` para periodos con `yyyymm <= 202606`.
- Este analisis fue agregado al documento:
  - `/mnt/Agentes/Norma/AFILMED/ANALISIS/Simulacion_2155.docx`.
  - Tambien se actualizo `/mnt/Agentes/Norma/AFILMED/ANALISIS/Simulacion_2155.md`.
- Validacion posterior: `.docx` validado con `unzip -t` sin errores y contenido agregado confirmado dentro del Word.

## AFILMED - Analisis Impresion Cuenta Corriente / Campos sombreados

- Documento base indicado por Norma: `/mnt/Agentes/Norma/AFILMED/Impresion_ctacte.docx`.
- Repositorios de referencia:
  - SVN DDL: `~/.openclaw/workspace/svn/infomedical.LuHernandez-swiss-mp-prod-base_de_datos-sybase-sybase-DB1-sybase-afilmed-DDL-trunk`.
  - Git AFILMED: `~/.openclaw/workspace/git/smg-code-afilmed`, branch `main`.
- La imagen mencionada por Norma no llego visible en el turno. Se respondio con la hipotesis tecnica de que los campos sombreados correspondian al bloque `RESUMEN DE DEUDA` del comprobante impreso.
- Capas y objetos ubicados para el calculo/visualizacion:
  - PowerBuilder arma la grilla del resumen en `ws_objects/facturacion.pbl.src/w_preview_compro_impre.srw`, subrutina `obtener_factura_impre`.
  - En esa rutina se carga el child DataWindow `dw_ctacte` y se setean campos desde `lds_compro_impre_primario`: `fecha = factu_perio`, `compro_tipo`, `compro_nro`, `tipo_saldo`, `total_factura`, `total`.
  - Layout del bloque: `ws_objects/prefactu.pbl.src/d_compro_impresion_ctate_3.srd`, donde se define `RESUMEN DE DEUDA` con columnas `FECHA`, `REFERENCIA`, `IMPORTE`.
  - El importe mostrado usa una expresion PowerBuilder/DataWindow con `gf_cambio_separador(If(...))`: si `tipo_saldo` coincide con la referencia del comprobante actual (`compro_tipo` + partes de `compro_nro`), muestra `total_factura`; si no, muestra `total`.
- Calculo SQL principal relevado:
  - `procedures/afs_compro_impresion_5.sql`.
  - Campos calculados: `tipo_saldo_1`, `total_factura_1`, `total_1`, `tipo_saldo_2`, `total_2`, `tipo_saldo_4`, `total_4`.
  - `total_factura_1` toma `CI.importe_total`.
  - `total_2` contempla `CI.saldo_acreedor` y diferencia `CI.impor1 - CI.importe_total` cuando corresponde.
  - `total_4` toma `CI.deuda1`.
- SP alternativo/relacionado:
  - `procedures/afs_compro_impresion_ctacte.sql`.
  - Devuelve filas para comprobante actual (`total_factura = importe_total`), `Saldo Acreedor` desde `saldo_acreedor` y diferencia `impor1 - importe_total`, y `Saldo Deudor` desde `deuda1`.
- Origen de saldos de cuenta corriente:
  - `procedures/afi_compro_impre_cta_cte_new.sql`.
  - Carga `#COMPRO_IMPRE_CTA_CTE` sumando comprobantes no cancelados y con saldo distinto de cero, excluyendo bajas/anulados.
  - Filtros clave: `COM.cance = 'N'`, `COM.saldo <> 0`, `COM.baja_fecha IS NULL`, `COM.anu_fecha IS NULL`.
- Pendiente si Norma retoma: pedir o revisar nuevamente la imagen adjunta para confirmar campo por campo contra el sombreado real.
