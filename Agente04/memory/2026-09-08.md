
## 2026-09-08 10:16 ART - Redmine 30337 / Afilmed MASTER-2587

- Usuario de la conversación: Walter Ferreyra, Git local configurado como `ext_waferreyra` con email `Walter.Ferreyra.Externo@swissmedical.com.ar`.
- Proyecto Git PowerBuilder: `git@gitlab.com:smg-code/afilmed/afilmed.git`, rama fija `MASTER-2587-Aumentos-Segmentados_20260601`, working copy local `~/.openclaw/workspace/git/ext_waferreyra-smg-code-afilmed-MASTER-2587-Aumentos-Segmentados_20260601`.
- SVN base de datos: `http://lv36papf-svncorp.swm.com.ar:8888/svn/swiss/mp/prod/base_de_datos/sybase/sybase-DB1/sybase-afilmed/DDL/branches/MASTER-2587-Aumentos-Segmentados`, usuario `infomedical.nicolas.paz`, working copy local `~/.openclaw/workspace/svn/infomedical.nicolas.paz-MASTER-2587-Aumentos-Segmentados`.
- DB validada para desarrollo: `SwissDevP10.Afilmed`, host/puerto `10.34.0.62:4100`, base `afilmed`, usuario `infomedical`, servidor Sybase `ASEDBDESA`. Se validó conexión con `select db_name(), @@servername`.
- Redmine relacionado: `https://redmine.infomedical.com.ar/issues/30337`. Desarrollo sugerido: agregar consulta de coeficientes de aumentos en `wp_consulta_afiliados` (`solicitudes.pbl`).
- Objetos PB definidos: tabpages `tp_coef_aumentos` y `tp_coef_aumentos_adic`; controles `dw_coef_aumentos` y `dw_coef_aumentos_adic`; DataObjects `d_consulta_afis_coef_aumentos` y `d_consulta_afis_coef_aumentos_adic`; SP select `afs_consulta_afis_coef_aumentos` y `afs_consulta_afis_coef_aumentos_adic`.
- Evidencia DB: en la rama SVN existen `afi_contrato_coeficientes_cuo`, `afi_contrato_coeficientes_adi`, `CONTRATO_COEFICIENTES_CUO` y `CONTRATO_COEFICIENTES_ADI`.
- Cambios ya aplicados pero no versionados: Git PB modificó `ws_objects/solicitudes.pbl.src/wp_consulta_afiliados.srw` y creó/actualizó `d_consulta_afis_coef_aumentos.srd`, `d_consulta_afis_coef_aumentos_adic.srd`. SVN DB creó/actualizó `procedures/afs_consulta_afis_coef_aumentos.sql` y `procedures/afs_consulta_afis_coef_aumentos_adic.sql`.
- Implementación aplicada: nuevas tabpages al final de `tab_consulta_afiliados`, integración de `retrieve`, `reset` y habilitación por `rowcount()`, DataObjects por SP, SP sobre `CONTRATO_COEFICIENTES_CUO`/`CONTRATO_COEFICIENTES_ADI`, validando `@prepaga/@contra` no nulos y filtrando vigentes con `baja_fecha IS NULL`.
- Supuesto técnico: `wp_consulta_afiliados` funciona como consulta Buscar/Limpiar y no tiene flujo de grabación; las nuevas DW quedaron como consulta sin update directo para no romper comportamiento preexistente.
- Validaciones reportadas OK: consultas equivalentes en Sybase DEV, CUO devuelve datos de prueba, ADI ejecuta sin error pero sin filas en DEV, SQL sin `;` ni `RTRIM`, `git diff --check` OK, encoding preservado (`.srw/.srd` UTF-8 con BOM y LF; SQL ASCII sin BOM y CRLF).
- No se hizo `git add`, `svn add`, commit ni push. No hacer branch switch en el repo registrado.
