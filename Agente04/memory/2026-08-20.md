
## 2026-08-20 10:50 ART - PRESMED DEV / PRS_3850 RECHA

- Usuario en Google Chat directo: Daniel. Prefiere trato directo y técnico.
- Trabajo realizado para PRESMED DEV sobre el árbol que cuelga de `dbo.pri_farma_expe_import_procesa`, enfocado en usos de `recha_motivos` y `manda_rechazos`.
- Carpeta de entregables usada y confirmada por el usuario: `/mnt/Agentes/d.acosta/PRS_3850_DB/ArchivosTemporales/pri_farma_expe_import_procesa_RECHA`.
- Primer entregable: contrato de evolución con inventario de usos, estructura de temporal, SQL de creación/carga y lista de procedimientos a modificar. No se modificó la base.
- Definiciones posteriores del usuario para refactorización:
  - Temporal definitiva: `#FARMA_RECHA`.
  - `recha_motivos` debe filtrarse estrictamente por `grupo = 'FAR'` y `habilitado = 'S'`.
  - Códigos no FAR, como código `1 / AFI`, y motivos deshabilitados no forman parte del proceso.
  - `manda_rechazos` debe considerar sólo registros vigentes con `baja_fecha IS NULL`.
  - Prioridad funcional: primero `manda_rechazos`; completar con `recha_motivos` grupo FAR habilitado.
- Archivos finales dejados en la carpeta:
  - `01_crea_temporal_motivos_rechazo.sql`
  - `02_carga_temporal_motivos_rechazo.sql`
  - `03_contrato_evolucion.txt`
  - `04_pri_impor_farma_expe_refactor.sql`
  - `05_prs_farma_expe_import_valida_refactor.sql`
- Propuesta implementada en scripts, no aplicada en base:
  - `dbo.pri_impor_farma_expe` crea/carga una sola vez la temporal `#FARMA_RECHA` fuera de procedimientos internos y la usa para `impor_farma_error` y para decidir `esta = 'E'`.
  - `dbo.prs_farma_expe_import_valida` usa `#FARMA_RECHA` para excluir tipo `N`.
- Validaciones informadas al usuario: no quedó el nombre anterior `#FARMA_EXPE_RECHA_MOTIVOS`, no quedaron finalizadores `;` en entregables, y no se ejecutaron cambios en la base.
