# Memoria curada diaria

- Fecha: 2026-08-03
- Usuario: Walter Ferreyra
- id_googlechat: users/112034177165901046099
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/jgr2byaaaae

## Resumen de la actividad

### Tema 1: Recuperación de working copy Git Afilmed y cierre de PowerBuilder

- Solicitud o contexto: Walter informó que al abrir el workspace Swiss Afilmed de Git PowerBuilder se cerraba tras una ventana de migración, y pidió cómo actualizar `E:\Repos\Swiss\Afilmed\Afilmed` para restaurar archivos originales.
- Respuesta o acciones del agente: Se indicó cerrar PowerBuilder, no aceptar la migración mientras la copia estuviera dañada, revisar rama/estado y usar `git fetch` con `git reset --hard origin/develop` o la rama vigente. También se explicó el uso controlado de `git clean -fdn` y `git clean -fd`, con advertencia de pérdida de cambios locales.
- Resultado: Walter informó que no quedaban carpetas `pblc` y que los archivos PB no volvían; se diagnosticó que estaba parado en `main` y se habían ejecutado comandos contra `origin/develop`/`origin/main`. Se entregaron comandos para listar `.pbw`, `.pbt`, `.pbl`, `.pbg` con `git ls-files`, `git ls-tree`, `find`, `git branch -a`, `git remote -v` y `git rev-parse --show-toplevel`.
- Estado: En curso

### Tema 2: Búsqueda de ventana base para Aumentos Segmentados

- Solicitud o contexto: Walter pidió buscar una ventana similar para copiar en la rama Git `MASTER-2587-Aumentos-Segmentados_20260601`.
- Respuesta o acciones del agente: Se encontró como mejor base `ws_objects/afinstal11.pbl.src/wp_abm_zona_vencimiento.srw`, con DataWindows `de_vencimiento_zona.srd` y `d_vencimiento_zona.srd`. Se identificaron los reemplazos esperados de tabla y procedimientos: `ZONAS_FACT`, `afs_zonas_fact`, `afs_zonas_vencimiento`, `afi_zonas_fact`, `afu_zonas_fact`, `afd_zonas_fact` hacia objetos de aumentos.
- Resultado: Se descartó como base principal `wp_aumentos_baja_moti.srw` por ser más simple y no manejar vigencias. La recomendación quedó lista para copiar y adaptar.
- Estado: Resuelto

### Tema 3: Generación de objetos PowerBuilder para `wp_abm_aumentos_zonas`

- Solicitud o contexto: Walter pidió copiar los objetos para la rama `MASTER-2587-Aumentos-Segmentados_20260601`, dejar los objetos en una sola PBL donde estuviera la window, renombrar window/DataWindows/SPs, cambiar título a `Zonas de aumentos`, adaptar referencias y trabajar base sobre SwissDevP10/Afilmed.
- Respuesta o acciones del agente: Primero se bloquearon ediciones sobre la working copy registrada porque la rama Git existente era fija `develop`; se pidió mail Git y credencial SVN. Luego Walter indicó dejar los objetos PB en su carpeta personal porque su ruta Windows no estaba montada en OpenClaw.
- Resultado: Se crearon en `/mnt/Agentes/wferreyra/afinstal11.pbl.src/` los archivos `wp_abm_aumentos_zonas.srw`, `de_aumentos_zonas.srd` y `d_aumentos_zonas.srd`. Se renombró la window, se ajustó el título, se cambiaron DataObjects y referencias a SPs nuevos, se dejó `d_aumentos_zonas` con `update="AUMENTOS_ZONAS"` y se validó que no quedaran referencias viejas. No se hizo rebuild, commit ni ejecución en base.
- Estado: Resuelto

### Tema 4: Objetos SQL para `AUMENTOS_ZONAS` y corrección por tabla ya existente

- Solicitud o contexto: Walter pidió dejar también los objetos de base en su carpeta personal. Luego aclaró que la tabla `AUMENTOS_ZONAS` ya existía en la rama y que todo debía adaptarse a esa tabla, no tomarse desde `ZONAS_FACT.sql`.
- Respuesta o acciones del agente: Primero se generaron archivos SQL en `/mnt/Agentes/wferreyra/base_de_datos_aumentos_zonas/` tomando como base objetos de `ZONAS_FACT`. Tras la aclaración, se rehizo el entregable sin generar tabla y sin tomar `ZONAS_FACT.sql` como base de tabla.
- Resultado: La versión corregida quedó en `/mnt/Agentes/wferreyra/base_de_datos_aumentos_zonas_rehecho/procedures/` con `afs_aumentos_zonas.sql`, `afi_aumentos_zonas.sql`, `afu_aumentos_zonas.sql` y `afd_aumentos_zonas.sql`. Los SPs quedaron orientados a `AUMENTOS_ZONAS` con columnas usadas por la window/DataWindows: `cod_zona`, `deno`, `alta_fecha`, `modi_fecha`, `baja_fecha`, `vigen_desde`, `vigen_hasta`. Se validó ASCII, CRLF, ausencia de `;`, ausencia de `RTRIM` y ausencia de referencias viejas. No se ejecutó SQL.
- Estado: Resuelto

### Tema 5: Validación de solapamiento de rangos e histórico en la ventana

- Solicitud o contexto: Walter pidió agregar validación de solapamiento de rangos de fechas en la ventana nueva, sin considerar dados de baja, e indicó que el botón Histórico no hacía nada.
- Respuesta o acciones del agente: Se corrigió `is_tabla` a `AUMENTOS_ZONAS`, se explicó que `w_clase_abm` ya provee histórico genérico si `is_tabla` está correcto, y se reforzó `ue_valida` para saltear filas con `baja_fecha` y comparar solo registros activos.
- Resultado: Walter probó y detectó que todavía podía cargar rangos solapados. Se corrigió nuevamente la validación: dejó de filtrar por `deno`, comparó cualquier zona activa contra cualquier otra activa, normalizó `Vigen. Hasta` al último día del mes, soportó vigencia abierta y dejó explícito el botón Histórico llamando a `ue_consulta_historico`. También se reforzaron `afi_aumentos_zonas.sql` y `afu_aumentos_zonas.sql` con `RAISERROR` para bloquear solapes en insert/update. Se advirtió que registros solapados ya cargados no se corrigen solos.
- Estado: Resuelto

### Tema 6: Búsqueda de ventana para detalle de provincias y partidos

- Solicitud o contexto: Walter pidió agregar a la window nueva un botón que abra otra ventana similar a una imagen, relacionada con el Redmine `https://redmine.infomedical.com.ar/issues/30169`, y luego pidió buscar ventanas que usen selección múltiple de provincias y partidos.
- Respuesta o acciones del agente: Se propuso `wp_asoci_zonas_vencimiento.srw` como mejor referencia funcional para asociación de zonas con provincias, aunque usa `TreeView` con drag/drop. Para estructura visual clásica se propuso `wp_partidos_hab.srw`. Se pidió API key de Redmine porque solo se contaba con usuario y no correspondía usar contraseña SVN como credencial Redmine.
- Resultado: A pedido de Walter se buscó también en Presmed. Se identificó como mejor candidata `ws_objects/ecco.pbl.src/wp_filtro.srw` con DataWindow `d_filtro_localidades_x_provincia.srd`, por manejar checkbox `checkeado`, partido, localidad, código postal y lógica de marcar/desmarcar múltiples registros. También se mencionaron referencias auxiliares `wr_sel_multiple.srw` y `wp_ecco_prestad_locas.srw`.
- Estado: En curso
