# Memoria curada diaria

- Fecha: 2026-07-23
- Usuario: Daniel Acosta
- id_googlechat: users/115318648074095705868
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/gbatvyaaaae

## Resumen de la actividad

### Tema 1: Búsqueda de tabla candidata en DEV-PRESMED

- Solicitud o contexto: Daniel pidió ubicar una tabla en DEV-PRESMED que tuviera `manda|conve|farma` o al menos `manda|farma|otra`, idealmente con índice por `manda|farma`.
- Respuesta o acciones del agente: Se consultó metadata de `SwissDeVP10_prestaci` y se compararon columnas e índices.
- Resultado: Se identificó `dbo.farmacias` como mejor candidato por tener `farma`, `manda`, `manda_farma`, PK por `farma` e índice `ik_farmacias (manda, manda_farma)`. Se aclaró que `parametros` y `parametros_log` tenían `manda/conve/farma`, pero no servían como tabla funcional principal.
- Estado: Resuelto

### Tema 2: Primera receta ABM PRESMED para `conve_produ_mandatarias`

- Solicitud o contexto: Daniel pidió construir una primera versión funcional de ABM PRESMED para `conve_produ_mandatarias` en `PRS_3850_DB`, usando `SwissDeVP10_prestaci` y la carpeta `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/conve_produ_mandatarias`.
- Respuesta o acciones del agente: Primero se bloqueó la generación porque la carpeta de salida no existía. Luego, tras confirmaciones y ajustes, se generaron versiones del ABM y se corrigieron errores detectados.
- Resultado: Se generaron objetos PowerBuilder y SQL en la carpeta indicada, incluyendo ventana `wp_conve_produ_mandatarias`, DataWindows, procedures `pri/pru/prd/prs_conve_produ_mandatarias.sql` y reporte. Se corrigió un error de compilación PowerBuilder reemplazando `decimal(data)` por `Dec(data)`. Más tarde se detectó que el enfoque inicial de dos DW no respetaba la ventana modelo de tres DW.
- Estado: En curso

### Tema 3: Receta ABM para `impor_control_manda_recha`

- Solicitud o contexto: Daniel pidió regenerar un ABM PRESMED para `impor_control_manda_recha`, usando DDL como fuente de verdad desde `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/impor_control_manda_recha.sql`.
- Respuesta o acciones del agente: Se generó la carpeta y objetos con patrón evolucionado basado en `wp_conve_farma_produ`, respetando DDL, PK `manda + recha`, scripts Sybase y reglas PRESMED.
- Resultado: Se generaron `wp_impor_control_manda_recha.srw`, `d_impor_control_manda_recha.srd`, `d_lista_impor_control_manda_recha.srd`, procedures `pri/pru/prd/prs_impor_control_manda_recha.sql` y reporte. Se validó ASCII, CRLF, sin BOM, SQL sin `;` ni `RTRIM`, nombres bajo 30 caracteres y `.srw` con línea vacía antes de cada `end event`. No se ejecutó SQL ni se compiló/importó en PB22.
- Estado: Resuelto

### Tema 4: Evolución de la receta HTML/JSON PRESMED ABM V2

- Solicitud o contexto: Daniel consultó cómo documentar fechas con calendario, maestros con DDDW y cómo evitar perder reglas acumuladas al generar nuevos pedidos. Luego pidió terminar un HTML V2 usable.
- Respuesta o acciones del agente: Se explicó la necesidad de reglas funcionales por columna y una memoria JSON acumulativa. Se creó y luego se completó el HTML V2.
- Resultado: Se generaron `/mnt/Agentes/d.acosta/Recetas/RECETA_PRESMED_ABM_V2.html` y `/mnt/Agentes/d.acosta/Recetas/RECETA_PRESMED_ABM_V2.json`, con preset de tres DW para `wp_conve_farma_produ`, mapeo obligatorio de sufijos `_2`, `_0`, `_1`, regla bloqueante para no fusionar DW, campos de rol/control/layout y exportación/importación JSON. También se actualizó la propuesta pendiente `presmed-abm-recipe-v2-20260723-b7d4e5798c`.
- Estado: Resuelto

### Tema 5: Corrección conceptual del modelo de replicación de tres DW

- Solicitud o contexto: Daniel señaló que si la ventana modelo tiene tres DataWindows se deben replicar tres, no reducir a dos ni inventar nombres. Pidió revisar cómo especificarlo.
- Respuesta o acciones del agente: Se reconoció el error y se acordó que `d_conve_farma_produ_2`, `_0` y `_1` deben mapearse respectivamente a `d_conve_produ_mandatarias_2`, `_0` y `_1`, vinculadas a `dw_filtro`, `dw_ingreso` y `dw_todo`.
- Resultado: Se incorporó la regla de replicar la cantidad real de DataWindows de la ventana modelo, conservar sufijos numéricos, roles, layout y no fusionar objetos. Esto quedó reflejado en V2 y fue base para V3.
- Estado: Resuelto

### Tema 6: Receta PRESMED ABM V3 y modelo preprocesado

- Solicitud o contexto: Daniel pidió una versión V3 que usara `MODEL_PROFILE_wp_conve_farma_produ_v1.json` como autoridad para controles, eventos, funciones, acciones y cantidad de DataWindows, preservando layout y resultsets del modelo.
- Respuesta o acciones del agente: Se generó una V3 inicial, se detectaron bloqueos por `cb_importar/ue_importar` no presentes en el profile viejo, se intentó `git pull` para obtener una versión nueva pero falló por SSH `Permission denied (publickey)`, y Daniel dejó los objetos modelo en Descargas.
- Resultado: Se revisaron `/mnt/Agentes/d.acosta/Descargas/wp_conve_farma_produ.srw` y las DW `_0`, `_1`, `_2`; se confirmó que la versión correcta sí tenía `cb_importar` y `ue_importar`. Se actualizaron `MODEL_PROFILE_wp_conve_farma_produ_v1.json`, `RECETA_PRESMED_ABM_V3.json` y `RECETA_PRESMED_ABM_V3.html` para tomar Descargas como fuente del modelo, activar importar y eliminar rastros viejos de “no detectado”.
- Estado: Resuelto

### Tema 7: Reglas de acentos y encoding en PB/DW

- Solicitud o contexto: Daniel remarcó que los acentos debían preservarse si el modelo base los tenía bien puestos.
- Respuesta o acciones del agente: Se formalizó que SQL continúa ASCII/CRLF, pero PB `.srw/.srd` no debe forzarse a ASCII si eso elimina acentos; al clonar ventana/DW se deben preservar textos visibles, eñes, signos y literales según la codificación real del modelo.
- Resultado: Se actualizaron `RECETA_PRESMED_ABM_V3.html`, `RECETA_PRESMED_ABM_V3.json` y `MODEL_PROFILE_wp_conve_farma_produ_v1.json`, y se revisó la propuesta durable `presmed-abm-recipe-v3-20260723-d6f11974ea`.
- Estado: Resuelto

### Tema 8: Reacuerdo del uso de mapeo en DW, ventana y procedures

- Solicitud o contexto: Daniel cuestionó para qué se creó el mapeo si al crear la DW solo se habían cambiado nombres de objetos. Luego definió el estándar correcto: duplicar objetos y después transformar columnas reconocidas según mapeo, sin cambiar diseño, orden ni cantidad de columnas; en procedures, duplicar la lógica del modelo, cambiar tabla base y aplicar columnas destino; para columnas del proceso que no existan en tabla destino, devolver valores compatibles como alias.
- Respuesta o acciones del agente: Se reconocieron incumplimientos: el mapeo se había usado solo en `prs_`, la DW se había renombrado sin transformar columnas, el `prs_` se había generado desde cero, y faltaba verificación formal de contrato. Se acordaron reemplazos básicos en ventana para `farma => conve`: variables `il_farma/ll_farma` a `il_conve/ll_conve`, accesos `.farma[` a `.conve[`, literales `'farma'` a `'conve'` cuando sean nombres de columna, `@farma` a `@conve`, y estructura `str_conve_farma_produ.farma` a `str_conve_produ_mandatarias.conve`.
- Resultado: El criterio V3 quedó ajustado para duplicar primero y transformar después por mapeo en DataWindows, PowerScript, variables, argumentos, parámetros y SQL. También se explicó que la configuración del HTML queda en `localStorage`, pero para que el agente la use debe descargarse y guardarse físicamente como `/mnt/Agentes/d.acosta/Recetas/RECETA_PRESMED_ABM_V3.json`.
- Estado: En curso

### Tema 9: Generación final V3 de `conve_produ_mandatarias`

- Solicitud o contexto: Daniel volvió a pasar el prompt V3 para generar `conve_produ_mandatarias`, con tres DataWindows, acciones seleccionadas y mapeo de columnas.
- Respuesta o acciones del agente: Hubo un bloqueo intermedio porque el prompt aún marcaba `cb_importar/ue_importar` con un profile viejo y contenía reglas contradictorias. Luego, con los objetos modelo correctos desde Descargas, se regeneró la V3.
- Resultado: Se generaron `wp_conve_produ_mandatarias.srw`, `d_conve_produ_mandatarias_2.srd`, `d_conve_produ_mandatarias_0.srd`, `d_conve_produ_mandatarias_1.srd`, `str_conve_produ_mandatarias.srs`, `prs_conve_produ_mandatarias.sql`, `pri_conve_produ_mandatarias.sql`, `pru_conve_produ_mandatarias.sql` y `REPORTE_RECETA_PRESMED_ABM.txt`. Se incluyeron `cb_importar` y `ue_importar`, se aplicaron `farma => conve`, `dto => descuento` y luego `fecha_vig => vigen_desde` en DW, PowerScript, argumentos, parámetros y SQL. Se validó UTF-16 LE con BOM y CRLF para PB/DW como el modelo, SQL ASCII CRLF sin `;` ni `RTRIM`, sin `decimal(data)`, con migración de validaciones a `lib.of_vacio`. No se compiló PB22, no se importó y no se ejecutó SQL.
- Estado: Resuelto

### Tema 10: Flushes de memoria por compaction

- Solicitud o contexto: Se recibieron pedidos automáticos de pre-compaction memory flush para escribir solo en `memory/2026-07-23.md` y responder `NO_REPLY` si no había nada que almacenar.
- Respuesta o acciones del agente: El agente respondió `NO_REPLY` en esas instancias.
- Resultado: No se agregaron registros nuevos desde esos flushes automáticos dentro de esta curación diaria.
- Estado: Informativo
