# Memoria 2026-08-27

- Julián pidió revisar en `SwissQAP10_prestaci` y el código de `smg-code-presmed` cómo se valida que una receta excede el período de presentación.
- Resultado técnico: en `smg-code-presmed` la validación no está implementada en PowerScript puro; el flujo PB arma contexto y delega a `prs_valida_of_receta`.
- Flujo identificado: `wp_farma_import_recetas.srw` carga `ticket_fecha` y `emi_fecha`, calcula `ld_presen` desde el período del expediente al último día del mes, llama a `pri_farma_batch`; luego `uo_nv_liquida_farma.sru` procesa con `of_procesa_recetas` / `of_procesa_receta_batch`; `of_valida_receta` llama a `valid.of_receta(...)`; `uo_nv_valdatos.sru` ejecuta `prs_valida_of_receta` pasando `@presen_fecha`, `@ticket_fecha`, `@emi_fecha`, tipo de receta, afiliado, prestador y otros datos.
- Regla activa observada en QA: `prs_valida_of_receta` obtiene normas desde `prs_valida_of_farma_normas`; si `datediff(dd, @ticket_fecha, @presen_fecha) > @presen_dias`, rechaza con código `102` (`Excede el limite de periodo de presentacion`). Si `@ticket_fecha > @presen_fecha`, rechaza con `106`. Si `datediff(dd, @emi_fecha, @ticket_fecha) > @rece_dias`, rechaza con `103`.
- Datos QA relevantes: `farma_normas` para prepaga `2/4`, tipos `C/G`, tenía `rece_dias = 30` y `presen_dias = 60`.
- Código PB relevante: cuando vuelve rechazo `102`, `uo_nv_liquida_farma.sru` agrega observación con la cantidad real de días calculada mediante `DaysAfter(ticket_fecha, presen_fecha)`.
- No se modificaron archivos durante la revisión; se hizo búsqueda de código PB y consultas read-only a `SwissQAP10_prestaci` sobre procedure activo, `recha_motivos` y `farma_normas`.
# Memoria 2026-08-27

- Julián pidió revisar en `SwissQAP10_prestaci` y el código de `smg-code-presmed` cómo se valida que una receta excede el período de presentación.
- Resultado técnico: en `smg-code-presmed` la validación no está implementada en PowerScript puro; el flujo PB arma contexto y delega a `prs_valida_of_receta`.
- Flujo identificado: `wp_farma_import_recetas.srw` carga `ticket_fecha` y `emi_fecha`, calcula `ld_presen` desde el período del expediente al último día del mes, llama a `pri_farma_batch`; luego `uo_nv_liquida_farma.sru` procesa con `of_procesa_recetas` / `of_procesa_receta_batch`; `of_valida_receta` llama a `valid.of_receta(...)`; `uo_nv_valdatos.sru` ejecuta `prs_valida_of_receta` pasando `@presen_fecha`, `@ticket_fecha`, `@emi_fecha`, tipo de receta, afiliado, prestador y otros datos.
- Regla activa observada en QA: `prs_valida_of_receta` obtiene normas desde `prs_valida_of_farma_normas`; si `datediff(dd, @ticket_fecha, @presen_fecha) > @presen_dias`, rechaza con código `102` (`Excede el limite de periodo de presentacion`). Si `@ticket_fecha > @presen_fecha`, rechaza con `106`. Si `datediff(dd, @emi_fecha, @ticket_fecha) > @rece_dias`, rechaza con `103`.
- Datos QA relevantes: `farma_normas` para prepaga `2/4`, tipos `C/G`, tenía `rece_dias = 30` y `presen_dias = 60`.
- Código PB relevante: cuando vuelve rechazo `102`, `uo_nv_liquida_farma.sru` agrega observación con la cantidad real de días calculada mediante `DaysAfter(ticket_fecha, presen_fecha)`.
- No se modificaron archivos durante la revisión; se hizo búsqueda de código PB y consultas read-only a `SwissQAP10_prestaci` sobre procedure activo, `recha_motivos` y `farma_normas`.

- Julián pidió generar un flujo de procesos para el importador masivo de recetas usando `SwissDEVP10_prestaci` y el branch/código `TAREA_29420_MODULO_FARMACIA` en `smg-code-presmed`.
- Se generó `/mnt/Agentes/jcastilla/Salidas/arbol.xlsx` con 8 columnas: origen, ventana PowerBuilder, acción de usuario, descripción del código PB hasta SP, nombre de SP, descripción del SP, tablas referenciadas incluidas temporales, y salidas del SP/ventana/DW/tablas. El archivo contenía 15 filas y cubría el flujo desde archivo de entrada, SP intervinientes, tablas temporales/físicas y cierre de importación.
- Validación declarada para ese XLSX: repo `smg-code-presmed` en branch `TAREA_29420_MODULO_FARMACIA`, profile `SwissDeVP10_prestaci`, dependencias reales de SP contra Sybase DEV, sin modificar código del repositorio.
- Julián pidió como preferencia que en cada nueva consulta o pedido se pregunte primero para qué número de Redmine corresponde y se registre la relación entre número de Redmine y prompt/pedido. Tras confirmación explícita, se agregó el punto 7 en `/mnt/Agentes/jcastilla/preferencias/preferencias.md` con esa regla. Validación: UTF-8 sin BOM y finales LF.
- Para el pedido "Busca en /mnt/Agentes una solucion de algun colega que sirva para validar la solucion de desarrollo", se preguntó Redmine según preferencia; Julián respondió `29420`.
- Para Redmine `29420`, se encontró material útil de Daniel Acosta en `/mnt/Agentes/d.acosta/PRS_3850_DB`, vinculado a `TAREA_29420_MODULO_FARMACIA`, Presmed, Farmacia, `farma_normas` y validación de recetas. Se dejó informe en `/mnt/Agentes/jcastilla/Salidas/R29420_busqueda_solucion_colega.txt`.
- Evidencia clave de Daniel para QA: validación de `presen_dias` comparando días entre expendio/ticket y presentación; regla base devuelve rechazo `102`, evolución de importación masiva aparece como rechazo `6072`. Archivos útiles: `INFORME_VALIDACIONES_FARMA_NORMAS.TXT`, `PRS_3850_of_valida_receta_farma_SYBASE.SQL`, `PRESMED_uso_farma_normas.SQL`, `PRESMED_CTRL_TXT.JSON`.
- Luego Julián pidió repetir la búsqueda sin limitar a Farmacia y aclaró que para eso no era necesario el número de Redmine. Se generó `/mnt/Agentes/jcastilla/Salidas/busqueda_ampliada_soluciones_validacion_desarrollo.txt`.
- Resultado de búsqueda ampliada: fuera de Farmacia no se encontró solución funcional equivalente que reemplace la evidencia de Daniel, pero sí material de Walter Guise como patrón transversal de QA para validar desarrollos: evidencia funcional, evidencia de código, flujo PB -> DW -> SP -> tablas, criterios de aceptación, matriz de pruebas y pendientes antes de aprobar. Antecedentes documentados: Walter Guise `R30027`, `R30220`, `R30116`.

- Para Redmine `29420`, Julián pidió revisar en `SwissQAP10_prestaci` y `smg-code-presmed` cómo se hace la validación "Producto con etiqueta excluida por validación de farmacia".
- Resultado técnico: en el código y base se identificó como **ética de venta excluida**, no como etiqueta. La condición es producto con `productos.eti = 'L'`, que se rechaza con `recha = 171`, motivo `Venta Libre`, grupo `FAR`, tipo `E`, `dto_tipo / cober_dto_tipo = XE`, descripción PB `Excl. por Etica de Vta.`.
- Flujo PB: durante `of_valida_receta`, cada ítem llama a `valid.of_produ_cober_precios(...)` desde `uo_nv_liquida_farma.sru`; el método está en `uo_nv_valdatos.sru` y ejecuta `prs_valida_of_produ_precio`.
- Flujo SP: `prs_valida_of_produ_precio` llama primero a `prs_valida_of_producto`, que obtiene `productos.eti`; si `@eti = 'L'`, setea `@eti_dto = 0`, `@li_ret = 171`, `@dto_tipo = 'XE'`.
- Persistencia/resultado: PB recibe el error, lo agrega con `of_guarda_errores`, y luego persiste mediante `pru_expe_farma_recetas`, `pri_expe_farma_rece_items`, `prd_expe_farma_errores_elec` y `pri_expe_farma_errores`.
- Se validó en QA que `recha_motivos` contiene el código `171` como `Venta Libre`, habilitado y de tipo error. Se registró la relación Redmine/prompt en `/mnt/Agentes/jcastilla/PROMPTS/redmine_prompts.md`. No se modificó código del repositorio.
