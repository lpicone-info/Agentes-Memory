# Memoria curada diaria

- Fecha: 2026-07-25
- Usuario: Daniel Acosta
- id_googlechat: users/115318648074095705868
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/gbatvyaaaae

## Resumen de la actividad

### Tema 1: Bootstrap y disponibilidad de La 99

- Solicitud o contexto: Daniel consultó si La 99 estaba activa.
- Respuesta o acciones del agente: Se ejecutó el bootstrap operativo obligatorio, leyendo `REGLAS.md`, `REPOSITORIOS.md`, `DB_PROFILE.md`, `USUARIOS.md` y las preferencias del usuario.
- Resultado: La 99 confirmó disponibilidad con las preferencias cargadas.
- Estado: Resuelto

### Tema 2: Enfoque Replica Code como contrato de generación

- Solicitud o contexto: Daniel planteó reemplazar prompts extensos llenos de límites por modelos procesados con pseudocódigo, para que la generación replique solo lo marcado y no agregue lógica no indicada.
- Respuesta o acciones del agente: Se evaluó el enfoque como un contrato de generación determinístico, separando modelo fijo, expansiones repetibles y expresiones calculadas. Se propuso que el modelo procesado preserve literal todo lo no marcado y que el pseudocódigo controle las zonas variables.
- Resultado: Quedó establecido el criterio: la IA debe ayudar a modelar, auditar y evolucionar contratos; la ejecución repetitiva de modelos aprobados conviene resolverla con un motor local determinístico.
- Estado: Informativo

### Tema 3: Rol de IA y motor local en Replica Code

- Solicitud o contexto: Daniel explicó que el preprocesamiento de objetos reales para separar fijo y variable aún no estaba desarrollado y que esa etapa requería inteligencia aplicada por La 99.
- Respuesta o acciones del agente: Se ordenó la arquitectura en dos etapas: La 99 como preprocesador inteligente de modelos desde código legado real, y un `.py` local como procesador determinístico de modelos ya aprobados.
- Resultado: Se definió un ciclo ideal: objeto original aceptado, modelo procesado candidato, reconstrucción local, comparación contra original, ajuste y aprobación en catálogo.
- Estado: Informativo

### Tema 4: Prototipo HTML para armar paquetes Replica Code

- Solicitud o contexto: Daniel pidió iniciar con un HTML para facilitar el armado de prompts, seleccionar modelo preprocesado u objeto parcial, indicar profile, objeto de base, carpeta y nombre de salida, cargar diccionario de pseudocódigo, metadata y salida esperada.
- Respuesta o acciones del agente: Se creó un primer prototipo en `/home/luispicone/.openclaw/workspace/replica-code-prompt/` con `index.html`, `DICCIONARIO_REPLICA_CODE.md` y `README.md`.
- Resultado: El prototipo permitió armar paquetes/prompt para crear, replicar o auditar modelos Replica Code. Se validó el JavaScript embebido.
- Estado: Resuelto

### Tema 5: Instalación de la receta en carpeta del usuario

- Solicitud o contexto: Daniel pidió dejar la herramienta en `/mnt/Agentes/d.acosta/RECETAS/` y mantener los HTML en mayúsculas.
- Respuesta o acciones del agente: Se copiaron y ajustaron los artefactos en destino como `RECETA_REPLICA_CODE_PROMPT.HTML`, `INDEX.HTML`, `DICCIONARIO_REPLICA_CODE.md` y `README.md`.
- Resultado: Los HTML quedaron en mayúsculas y sincronizados en la carpeta solicitada. Se validó el JavaScript.
- Estado: Resuelto

### Tema 6: Fuente primaria de metadata desde BD real

- Solicitud o contexto: Daniel cuestionó por qué pedir metadata si ya se indicaban profile y objeto real de base.
- Respuesta o acciones del agente: Se aclaró que la BD real debe ser fuente primaria cuando existe `profile + objeto`, y que la metadata manual debe quedar solo como snapshot, fallback o evidencia congelada. Luego Daniel pidió evolucionar la herramienta con ese criterio.
- Resultado: Se incorporó en los HTML el selector de origen de metadata: `bd_profile_objeto`, `snapshot_metadata` y `mixto_validar_bd_vs_snapshot`. Se actualizaron el diccionario a versión `0.2` y el README con reglas para BD primaria, snapshot y procedures.
- Estado: Resuelto

### Tema 7: Autosave local del HTML

- Solicitud o contexto: Daniel pidió que el HTML recordara lo último ingresado para no volver a cargar todo en cada uso.
- Respuesta o acciones del agente: Se agregó autosave local para campos, combos, textareas y contenidos de archivos cargados. El botón `Limpiar` también borra el borrador local.
- Resultado: `RECETA_REPLICA_CODE_PROMPT.HTML` e `INDEX.HTML` quedaron actualizados, sincronizados y con JavaScript validado.
- Estado: Resuelto

### Tema 8: Auditoría de paquete Replica Code PB1

- Solicitud o contexto: Daniel ejecutó una solicitud de auditoría Replica Code con modo `auditar_modelo`, tipo `tabla`, objeto `pri_conve_produ_mandatarias`, origen `bd_profile_objeto`, carpeta de salida `/mnt/Agentes/d.acosta/ReplicaCodex` y salida `REP_PB1_SALIDA.txt`.
- Respuesta o acciones del agente: Se procesó la auditoría y se generó `/mnt/Agentes/d.acosta/ReplicaCodex/REP_PB1_SALIDA.txt`. También se evolucionó el HTML para agregar validaciones previas del paquete.
- Resultado: La auditoría quedó parcial con bloqueos: faltaba `profile`, no estaba resuelta `{Ref:Modelo_N1}`, había inconsistencia entre tipo `tabla` y un modelo que declaraba procedure, y se detectó tensión entre `messagebox()` y el estándar corporativo `mbox(...)`.
- Estado: Pendiente

### Tema 9: Concepto de snapshot

- Solicitud o contexto: Daniel pidió explicación de qué significa `snapshot` en este flujo.
- Respuesta o acciones del agente: Se explicó como una foto congelada de metadata extraída en un momento exacto, útil para reproducibilidad, auditoría, comparación o trabajo sin conexión.
- Resultado: Quedó diferenciado el uso recomendado: BD real para estado actual, snapshot para evidencia congelada, y BD vs snapshot para detectar cambios.
- Estado: Informativo

### Tema 10: Reducción de tokens en prompts generados

- Solicitud o contexto: Daniel observó que el HTML incluía texto innecesario cuando se elegía `bd_profile_objeto`, y luego cuestionó el desperdicio de tokens al embeber todo lo seleccionado en el prompt.
- Respuesta o acciones del agente: Se ajustó el HTML para omitir ejemplos largos de snapshot cuando el origen es BD real. Luego se agregó un modo de contexto con dos opciones: referencias persistentes y portable embebido.
- Resultado: El prompt pasó a transportar intención y referencias, no archivos completos, salvo cuando se elija explícitamente el modo portable. Los HTML quedaron sincronizados y validados.
- Estado: Resuelto

### Tema 11: Índice persistente de contexto Replica Code

- Solicitud o contexto: Daniel propuso definir una carpeta de usuario y actualizar `MY_CONTEXTO_INDEX.txt` para que La 99 reconozca archivos subidos al sandbox sin reenviarlos completos.
- Respuesta o acciones del agente: Se creó/actualizó `/mnt/Agentes/d.acosta/Contexto/MY_CONTEXTO_INDEX.txt` con raíces, archivos vigentes, hashes y contrato de carga por referencias. También se generó `/mnt/Agentes/d.acosta/Contexto/PROMPT_REHIDRATAR_REPLICA_CODE.txt`.
- Resultado: Quedó definido un prompt corto para rehidratar el contexto Replica Code desde el índice persistente y trabajar por referencias.
- Estado: Resuelto

### Tema 12: Gobierno de recetas persistentes y espacio común

- Solicitud o contexto: Daniel planteó extender el esquema a otras recetas, separando fijo/variable, reduciendo tokens y preparando un modelo común accesible por varios programadores.
- Respuesta o acciones del agente: Se documentó el plan en `/mnt/Agentes/d.acosta/Contexto/PLAN_GOBIERNO_RECETAS_PERSISTENTES.md` y se registró en `MY_CONTEXTO_INDEX.txt`.
- Resultado: Quedó propuesto un gobierno de recetas persistentes con contrato fijo, modelo fijo, variables del caso, evidencia opcional, versión/hash y fundamento para solicitar un espacio común compartido.
- Estado: En curso
