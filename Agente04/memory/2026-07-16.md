
## 2026-07-16 12:48 UTC - Flush de memoria durable

- Usuario: Daniel / d.acosta. Preferencia persistente: cuando indique por error rutas de red con `/mnt/Agentes/Daniel/`, interpretar como `/mnt/Agentes/d.acosta/`.
- Proyecto activo: PRESMED / repo `PRS_3850_DB`, profile DB `SwissDeVP10_prestaci`.
- Carpeta de trabajo del proceso ABM: `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM`.
- Documento base analizado: `REQ - 30013 ABMs para Módulo de Farmacias Parametrización de errores.docx`; reporte generado: mismo nombre con extensión `.TXT`.
- Tabla objetivo: `exept_control_mandatraria`. Reglas acordadas: nombres de tablas/columnas en minúscula; agregar `id_control int identity not null` para acceso único del ABM; PK por `id_control`; la clave funcional anterior queda como índice único sobre `(recha, manda, vigencia_desde)`; `vigencia_hasta` puede ser null y no debe estar en PK.
- Scripts SQL generados en la carpeta de trabajo: `exept_control_mandatraria.sql`, `exept_control_mandatraria_fix.sql`, `pri_exept_control_mandatraria.sql`, `pru_exept_control_mandatraria.sql`, `prd_exept_control_mandatraria.sql`, `prs_exept_control_mandatraria.sql`.
- Restricción Sybase/SQL: identificadores máximo 30 caracteres. Nombres corregidos usados: `pk_exept_cm_id`, `ak_exept_cm_01`, `ik_exept_cm_01`, `fk_exept_cm_manda`, `fk_exept_cm_recha`.
- Objetos PB generados en la carpeta de trabajo: `wp_exept_control_mandatraria.srw`, `d_exept_control_mandatraria.srd`, `d_lista_exept_control_mandatraria.srd`.
- Modelo PB usado: ventana `wp_prepagas`, tabla dominante modelo `prepagas`, DWs `d_prepaga` y `d_listaprepa`.
- Nuevo estándar DW: evitar SQL embebido en `.srd`; usar datawindows basadas en stored procedure. `prs_exept_control_mandatraria` debe aceptar `@prm_concepto varchar(10)` con valores `'listado'` y `'registro'`; para `'registro'` usar `@prm_id int`.
- Regla PowerScript del usuario: no usar validación tradicional `isnull` ni `dato=''`; usar `lib.of_valid(<dato>)` para nulidad/vacío. No colocar comentarios dentro de objetos PowerBuilder porque causaron problemas de importación en PB22.
- Validaciones realizadas hasta ahora: objetos PB generados como UTF-8 con BOM y LF; SQL ASCII con CRLF; SQL sin `;` y sin `rtrim`; nombres de procedures dentro del límite; DW nuevas con `procedure=` y sin SQL embebido tipo `retrieve=`. No se importó en PB22 ni se ejecutó SQL contra base.
- Nota de estado: en `SwissDeVP10_prestaci`, la tabla existente fue observada todavía sin `id_control`; el ajuste está en scripts pero no confirmado como compilado.
- Receta propuesta para repetir el proceso PRESMED: `presmed-abm-from-doc` con id de propuesta `presmed-abm-from-doc-20260716-d4845a54af`; pendiente de aprobación/aplicación formal. Inputs definidos: `documento`, `carpeta_salida`, `profile_db`, `repo`, `tabla_objetivo`, `tablas_referencia`, `modo_compilacion`.
- HTML creado para armar prompts de la receta: `/mnt/Agentes/d.acosta/recetas/RECETA_PRESMED_ABM_DESDE_DOC.html`; índice actualizado: `/mnt/Agentes/d.acosta/recetas/RECETAS_INDEX.HTML`.
