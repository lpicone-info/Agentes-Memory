## 2026-09-01 16:41 ART - Trabajo farmacia / mandatarias

- Usuario Marcelo trabaja sobre repo Git de farmacia `smg-code-presmed` y carpeta compartida `/mnt/Agentes/mepaduan/r30278`.
- Pedido funcional: agregar busqueda de mandatarias desde `wp_ingexped_farm` mediante evento `ue_buscar_mandataria` que abre `wr_busmanda`, valida/carga `manda` y `manda_deno`, y centraliza efectos laterales en `wf_set_mandataria(long al_manda)`.
- Se implementaron en el repo Git objetos relacionados: `ws_objects/liqui14.pbl.src/wp_ingexped_farm.srw`, `ws_objects/valida2.pbl.src/wr_busmanda.srw`, `ws_objects/valida1.pbl.src/de_busmanda.srd`, `ws_objects/valida1.pbl.src/d_busmanda_list.srd`, y registro en `ws_objects/prestaci.pbl.src/presta.srj`.
- SP creado/dejado en `/mnt/Agentes/mepaduan/r30278/prs_busmanda.sql`.
- Marcelo pidio luego copia de objetos en `/mnt/Agentes/mepaduan/r30278`: `wr_busmanda.srw`, `de_busmanda.srd`, `d_busmanda_list.srd`.
- Problema detectado por Marcelo: al importar `wr_busmanda` se cerraba PowerBuilder. El dejo un objeto base exportado de la rama como `/mnt/Agentes/mepaduan/r30278/wr_busmanda_pb.srw`.
- Se hizo compare/merge contra `wr_busmanda_pb.srw` y se reemplazo `/mnt/Agentes/mepaduan/r30278/wr_busmanda.srw` con version solida para importacion PB: formato `UTF-16` con BOM y finales `CRLF`, conservando la logica nueva y quitando restos copiados de farmacia (`str_farmacia`, `str_bf`, `manda_farma`, `d_manda_list`). La version fusionada usa `str_mandataria`, `de_busmanda` y `d_busmanda_list`.
- Validaciones informadas: archivo decodifica con `iconv`, sin referencias a farmacia, sin espacios finales. `wr_busmanda_pb.srw` quedo intacto como referencia.
- Codigo central informado a Marcelo para `wf_set_mandataria(long al_manda)`: asigna `ii_manda`, evalua `iu_cai.uf_proceso_cai_f(ii_manda)` para mostrar/ocultar CAI y timer, ejecuta `dw_pend.Retrieve(ii_manda)`, recupera grupo con `idwc_pg.retrieve(ii_manda)` y si `grupo > 0` carga `dw_cab.object.grupo[1]`.

