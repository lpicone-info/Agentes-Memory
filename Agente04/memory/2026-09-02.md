
## 2026-09-02 11:15 America/Argentina/Buenos_Aires - R29420 farmacia

- Usuario en Google Chat directo: Julian. Prefiere trabajo técnico directo en español.
- Trabajo realizado sobre `SwissDEVP10_prestaci` y branch/tarea `TAREA_29420_MODULO_FARMACIA` en repo `smg-code-presmed`.
- Se analizó el flujo de carga de farmacia para `Impor_farma_cab`, `Impor_farma_det` e `impor_farma_error`.
- Archivos generados en `/mnt/Agentes/jcastilla/Salidas/`:
  - `R29420_mapa_carga_impor_farma.txt`: informe inicial del flujo.
  - `R29420_mapa_carga_1`: SVG inicial del flujo de datos desde campos del archivo de entrada a temporales y destino final.
  - `R29420_revision_procesos_tablas_finales.txt`: revisión de procesos adicionales que modifican tablas finales.
  - `R29420_mapa_carga_impor_farma_2.txt`: informe ampliado con acciones faltantes.
  - `R29420_mapa_carga_2.svg`: gráfico actualizado generado desde el informe ampliado; validado con parseo XML OK.
- Hallazgos técnicos clave:
  - La carga de `impor_farma_cab`, `impor_farma_det` e `impor_farma_error` ocurre en `dbo.pri_impor_farma_expe`, llamado por `dbo.pri_farma_expe_import_procesa`.
  - El flujo importa y homologa el archivo usando temporales como `#IMP_ARCHIVO_CRUDO`, `#det_fase_1`, `#det_fase_2`, `#FARMA_EXPE`.
  - Los rechazos se validan con `dbo.prs_farma_expe_import_valida` y se consolidan en `#FARMA_EXPE_RECHA` / `#FARMA_RECHA` antes de cargar `impor_farma_error`.
  - `impor_farma_error` guarda errores y avisos; `recha_tipo = 'E'` identifica error, otros valores corresponden a aviso.
  - Dentro del flujo masivo, el SP que modifica tablas finales es `dbo.pri_farma_expe_import`.
  - No se encontraron triggers sobre `expe_farmacias`, `expe_farma_recetas`, `expe_farma_rece_items` ni `expe_farma_errores`.
  - Acciones faltantes incorporadas al informe/gráfico: baja lógica por accion `REEMPLAZAR`, inserción en tablas finales, recálculo de totales/cantidades, generación de `expe_farma_errores`, actualización de estado en `impor_farma_det`, y listado de procesos externos/manuales/batch que también pueden modificar tablas finales.
  - Inconsistencia detectada: el DW de auditoría referencia `dbo.prs_impor_farma_audit`, pero en `SwissDEVP10_prestaci` se encontró `dbo.pri_impor_farma_audit`.

## 2026-09-02 14:53 America/Argentina/Buenos_Aires - Farmacia mandatarias e IVA

- Usuario en Google Chat directo: Marcelo. Prefiere respuestas técnicas directas en español, con referencias concretas a archivos/líneas cuando se revisan repos.
- Consulta SVN farmacia `PRS_3850_DB`: se buscó si existía un SP con sentencia PowerBuilder tipo `SELECT manda, razon, domi, loca, postal_codi, tele, obser, alta_fecha, baja_fecha, cuenta, cuit, iva INTO :a_str... FROM mandatarias WHERE manda = :a_manda`.
- Resultado: no se encontró SP con esa sentencia exacta. Relacionados hallados:
  - `procedures/prs_valida_farma_expe.sql`: valida mandataria del expediente existente/vigente (`baja_fecha IS NULL`) y `enti_tipo` configurado.
  - `procedures/prs_iva_mandataria.sql`: consulta `mandatarias` por `manda`, valida vigencia y devuelve si `iva = '1'`.
  - `procedures/prr_auto_mandatarias_sgi.sql`: devuelve columnas similares de `mandatarias`, pero filtra por `autorizaciones = @auto` y `baja_fecha IS NULL`, no por `manda`.
- DML solicitado para `recha_motivos`: se entregaron bloques `IF count = 0 INSERT ELSE UPDATE` para códigos `6067`/`6068`, denominaciones `Mandataria inexistente` y `Mandataria dada de baja`, copiando el resto de campos desde `recha = 101`.
- Revisión repo Git farmacia `TAREA_29420_MODULO_FARMACIA` sobre ventana `wp_ingexped_farm` y cálculo de IVA:
  - Se hizo inicialmente un cambio incorrecto hardcodeando `21.00`; el usuario lo rechazó correctamente porque necesitaba conocer el origen del valor, no fijarlo. Ese cambio fue revertido.
  - No debe quedar hardcode de `21.00` en `wp_ingexped_farm.srw` por ese intento.
  - Origen identificado de `rut.of_Tasa_Iva(this.object.fecha[1],100,ie_ivax,ie_retx)`: función `of_Tasa_Iva` en `ws_objects/liqui08.pbl.src/uo_nv_liquida.sru`, línea aproximada 389.
  - Consulta origen: `select iva_tasa, reten_tasa into :a_iva, :a_reten from iva_tasa where vigen_desde = (select max(b.vigen_desde) from iva_tasa b where b.vigen_desde < :a_fecha)`.
  - Tabla origen en SVN DB: `tables/iva_tasa.sql`, campos `vigen_desde`, `iva_tasa`, `ivan_tasa`, `reten_tasa`.
  - DW/mantenimiento `ws_objects/instal.pbl.src/d_iva_tasa_param.srd`: semántica `iva_tasa = Iva ins`, `ivan_tasa = Iva no ins`, `reten_tasa = Reten`.
  - Función similar encontrada: `ws_objects/liqui22.pbl.src/uo_nv_factu.sru`, función `of_alicuota_iva(ai_prepaga, as_contra, adt_fecha, ref adc_alicuota)`, cerca de línea 704. Lee `iva_tasa` e `ivan_tasa`, y según condiciones de prepaga/contrato/IVA calcula alícuota con `iva_tasa / 100` o suma `ivan_tasa / 100`.
  - Hipótesis técnica pendiente: para farmacia el 21% probablemente surge de usar `iva_tasa + ivan_tasa` bajo condición funcional, no de otra función simple que devuelva 21. Falta confirmar contra datos reales de `iva_tasa` vigente y condición aplicable al expediente/farmacia.

## 2026-09-02 16:42 America/Argentina/Buenos_Aires - Parametros IVA farmacia

- Usuario en Google Chat directo: Marcelo.
- Contexto funcional: lider de proyecto pidio agregar en ventana `wp_parametros_iva` de Liqui una nueva solapa para tabla especifica `IVA_TASA_FARMA`, basada en `iva_tasa`, con columnas `vigencia/iva/alta fecha/modi fecha/baja fecha/usuario`.
- Repo Git trabajado: `smg-code-presmed`, rama/tarea indicada por usuario como `mnt mepaduan 30278` (revisar estado exacto con git antes de continuar).
- Cambios avanzados:
  - `ws_objects/instal.pbl.src/wp_parametros_iva.srw`: agregada solapa `Tasa iva farmacia`, agregado `dw_iva_farma`, botones borrar/agregar con iconos equivalentes a `tp_2`; guardado orientado a SP `pru_iva_tasa_farma`.
  - `ws_objects/instal.pbl.src/d_iva_tasa_farma_param.srd`: nuevo DataWindow basado en `d_iva_tasa_param`, apuntando a `iva_tasa_farma`.
  - `ws_objects/prestaci.pbl.src/presta.srj`: registrado `d_iva_tasa_farma_param`.
- Cambios avanzados en SVN DDL `PRS_3850_DB`:
  - `tables/iva_tasa_farma.sql`: nueva tabla con `vigen_desde`, `iva_tasa`, `usuario`, `alta_fecha`, `modi_fecha`, `baja_fecha`.
  - `procedures/pru_iva_tasa_farma.sql`: SP para alta/update/baja logica por `vigen_desde`, tomando `pru_iva_tasa` como referencia.
  - `procedures/prs_consu_iva_tasa_farma_vigente.sql`: SP de consulta de tasa vigente por fecha para uso posterior desde calculo sin SQL embebido.
- Validaciones realizadas segun respuesta previa:
  - `git diff --check` sin errores.
  - SQL sin `;` final ni `RTRIM`.
  - PB/SRJ preservados UTF-8 con BOM.
  - SQL nuevos ASCII con CRLF.
  - No se compilo PowerBuilder ni se ejecuto SQL en base.
- Nota importante para continuar: antes de seguir, revisar diffs reales y encoding/line endings. Mantener regla de no usar codigo SQL embebido en PB; los calculos/consultas deben ir por SP/datawindows/dataobjects.

## 2026-09-02 17:15 America/Argentina/Buenos_Aires - Correcciones R30278 IVA farmacia

- Usuario en Google Chat directo: Marcelo.
- Pedido: dejar todos los objetos generados para la tarea en `/mnt/Agentes/mepaduan/R30278`.
- Objetos copiados inicialmente a `/mnt/Agentes/mepaduan/R30278`:
  - `wp_parametros_iva.srw`
  - `d_iva_tasa_farma_param.srd`
  - `iva_tasa_farma.sql`
  - `pru_iva_tasa_farma.sql`
  - `prs_consu_iva_tasa_farma_vigente.sql`
- Correccion posterior solicitada por Marcelo:
  - El DataWindow no debe llamarse `d_iva_tasa_farma_param`; debe llamarse `d_iva_tasa_farma`.
  - El DataWindow debe leer desde SP `prs_iva_tasa_farma`, sin SQL embebido.
- Estado dejado en `/mnt/Agentes/mepaduan/R30278`:
  - `wp_parametros_iva.srw`: solapa farmacia referencia `DataObject="d_iva_tasa_farma"`.
  - `d_iva_tasa_farma.srd`: nombre corregido; recupera con `procedure="1 execute dbo.prs_iva_tasa_farma;1 "`, sin `SELECT` embebido.
  - `prs_iva_tasa_farma.sql`: SP de lectura sobre `dbo.iva_tasa_farma`, devuelve vigencia, tasa, fechas, usuario, `movim` y `ori_iva_tasa`.
  - Se valido que no quedara referencia a `d_iva_tasa_farma_param` en los objetos de salida.
  - Se valido que el DW no tenga `retrieve=` embebido, y que el SQL nuevo no tenga `RTRIM` ni `;` finalizadores.
  - Encodings/EOL informados: `.srw/.srd` UTF-8 con BOM + LF; `.sql` ASCII + CRLF.
- Nota para continuidad: si se trabaja de nuevo sobre repo Git/SVN, revisar y sincronizar los nombres finales (`d_iva_tasa_farma`, `prs_iva_tasa_farma`) tambien en los fuentes del repo, no solo en `/mnt/Agentes/mepaduan/R30278`, porque las ultimas correcciones fueron confirmadas sobre la carpeta de entrega.
