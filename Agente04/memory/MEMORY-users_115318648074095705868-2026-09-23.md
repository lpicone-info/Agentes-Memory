# Memoria de conversación — Daniel Acosta

## Datos

- **Fecha:** 23/09/2026
- **Usuario:** Daniel Acosta
- **ID Google Chat:** `users/115318648074095705868`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 25
- **Inicio:** 10:51:47
- **Fin:** 14:08:31

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/gbatvyaaaae`

## Archivos revisados

- `chat.history` (`8907e88f-c784-469d-8eb6-9d9d8ac76ed0`)

---

## Transcripción completa

### [23/09/2026 10:51:47] Daniel Acosta

hola estas activa?

---

### [23/09/2026 10:51:57] Agente

Hola Daniel, sí, estoy activa. ¿En qué avanzamos?

---

### [23/09/2026 11:12:28] Daniel Acosta

vamos a trabaja como siempre sobre la base PRESTACI en DEV.

puntualmente necesitamos 
1. tener el arbol de ejecuciones de este proceso: pri_farma_expe_import_procesa
2. además, identificar los nombres de tablas temporales que se utilizan en todo el proceso, completo
3. identifica lo que necesita el proceso prs_farma_expe_import_valida para validar, específicamente temporales, solo nombres por el momento
4. reconocer dentro del proceso la carga masiva de la tabla: expe_farma_errores que basicamente refleja todos los errores del proceso anterior de validación.


reconocidos los INPUT, necesitamos armar un nuevo procedure que tenga la siguiente estructura
1. tomar como base el proceso pri_farma_expe_variables_calc donde las 5 primeros parámetros de entrada son los que necesitamos que contenga
2. tomar como ejemplo las primeras cargas de las temporales #expe_farmacias / #farma_expe_recetas/ #farma_expe_rece_items para que? para replicar la carga detallada de la temporal #FARMA_EXPE que necesita el proceso prs_farma_expe_import_valida, que por lo que entendemos la temporal de #farma_expe_rece_items podria completarla con los datos necesario, sino revisar que mas faltaria
3. este nuevo proceso que puede cargar la temporal #FARMA_EXPE y #FARMA_RECHA  debe luego llamar al prs_farma_expe_import_valida y este retorna las temporales con los errores en #FARMA_EXPE_RECHA
4. ahi es cuando el proceso nuevo toma los errores de la temporal #FARMA_EXPE_RECHA y junto a otras tablas insertan los errores encontrados.

En general, lo que se espera es ejecutar este proceso con un @ofi y @expe y luego alternativas de proceso donde podria ser @prm_lote_impor procesaria de nuevo todos los errores de un lote de importación, o bien para una farmacia con @prm_lote_farma o bien especifico @prm_rece_nro para un numero de receta y cargar los errores del proceso de validacion.

el nuevo proceso podria tener un nombre parecido a este pri_farma_expe_errores_repro
que se entendería como [reproceso de errores para un expediente de farmacia]

antes de inciar, analiza todo y detecta el listado de a. Certezas b. Inconsistencias y c. Ambiguedades para que puedan ser respondidas y recien puedas hacer el primer entregable.

---

### [23/09/2026 11:12:37] Agente

Voy a relevar primero los cuatro procesos y sus dependencias en la rama de PRESMED/DDL asociada a PRESTACI DEV, sin crear ni modificar procedures todavía. Separaré evidencia verificable, inconsistencias y ambigüedades; el primer entregable será únicamente ese análisis para cerrar definiciones antes del diseño.

---

### [23/09/2026 11:13:02] Agente

Detecté un bloqueo importante antes del análisis: la working copy SVN de la rama tiene conflictos locales y archivos no versionados dentro del conjunto de procedures de farmacia (`pri_farma_expe_import_procesa.sql`, `pri_Impor_farma_expe.sql`, `pri_farma_expe_import.sql`, entre otros). No los voy a resolver ni sobrescribir. Para separar el estado versionado de los cambios locales, relevaré la versión del repositorio mediante lectura (`svn cat`) y dejaré esta inconsistencia explícita en el análisis.

---

### [23/09/2026 11:17:30] Agente

Daniel, hice el relevamiento previo sobre **PRESTACI DEV**, usando la rama `PRS-3850-Reingenieria-LiqFarmacias_20260603`.

No se modificó código ni se ejecutaron escrituras en la base.

# 1. Certezas verificadas

## 1.1. Los procedimientos existen en PRESTACI DEV

Se verificó la existencia de:

- `pri_farma_expe_import_procesa`
- `pri_impor_farma_expe`
- `pri_farma_expe_import`
- `pri_farma_expe_variables_calc`
- `pri_farma_expe_calculos`
- `prs_farma_expe_import_valida`
- Tabla `expe_farma_errores`
- Tablas `expe_farmacias`, `expe_farma_recetas` y `expe_farma_rece_items`

## 1.2. Árbol de ejecución actual

```text
PB: wp_import_drogueria_masivo
        |
        v
pri_farma_expe_import_procesa
        |
        +--> pri_impor_farma_expe
        |       |
        |       +--> prs_farma_expe_import_valida
        |       +--> pri_expe_auditoria
        |       +--> carga impor_farma_det
        |       +--> carga impor_farma_error
        |
        +--> pri_farma_expe_import
        |       |
        |       +--> carga #farma_expe_recetas
        |       +--> carga #expe_farma_errores
        |       +--> inserta expe_farma_errores
        |
        +--> pri_farma_expe_variables_calc
        |
        +--> pri_farma_expe_calculos
```

La orquestación está en:

`procedures/pri_farma_expe_import_procesa.sql`

Actualmente ejecuta:

1. `pri_impor_farma_expe`
2. `pri_farma_expe_import`
3. `pri_farma_expe_variables_calc`
4. `pri_farma_expe_calculos`

Importante: la transacción explícita solamente cubre los dos primeros procesos. Luego se ejecutan `variables_calc` y `calculos` después del `COMMIT`.

Además, el control de errores de los dos últimos procesos está comentado en el procedure actual.

## 1.3. La aplicación PB crea `#FARMA_EXPE`

En:

`ws_objects/import2.pbl.src/wp_import_drogueria_masivo.srw`

La aplicación ejecuta:

```powerscript
DROP TABLE #FARMA_EXPE;
SELECT * INTO #FARMA_EXPE FROM #det_fase_2;
ALTER TABLE #FARMA_EXPE ADD recha int null,
    prepaga_afi smallint null,
    contra_afi char(15) null,
    inte_afi char(2) null,
    farmacia int null
```

Luego invoca:

```powerscript
dbo.pri_farma_expe_import_procesa
```

Por lo tanto, `pri_farma_expe_import_procesa` no crea originalmente `#FARMA_EXPE`; depende de que exista previamente en la misma conexión SQL.

# 2. Temporales utilizadas por todo el proceso

## `pri_impor_farma_expe`

- `#FARMA_EXPE`
- `#FARMA_EXPE_RECHA`
- `#FARMA_RECHA`

## `prs_farma_expe_import_valida`

- `#FARMA_EXPE`
- `#FARMA_EXPE_RECHA`
- `#FARMA_RECHA`
- `#TMP_FARMA_AUTORIZ`
- `#TMP_FARMA_PRODU_PRECIO`

## `pri_farma_expe_import`

- `#expe_farma_errores`
- `#farma_expe`
- `#farma_expe_recetas`
- `#farma_expe_recha`

## `pri_farma_expe_variables_calc`

- `#expe_farmacias`
- `#farma_expe_recetas`
- `#farma_expe_rece_items`

## `pri_farma_expe_calculos`

- `#CALC_RECETAS`
- `#CALC_FARMACIAS`

# 3. Temporales que necesita `prs_farma_expe_import_valida`

El procedure recibe solamente:

```sql
@prm_manda int,
@prm_perio smalldatetime
```

No recibe `#FARMA_EXPE` como parámetro. Por lo tanto, necesita que las temporales existan previamente en la misma sesión:

- `#FARMA_EXPE`
- `#FARMA_EXPE_RECHA`
- `#FARMA_RECHA`

Durante la validación crea y utiliza además:

- `#TMP_FARMA_AUTORIZ`
- `#TMP_FARMA_PRODU_PRECIO`

`#FARMA_EXPE_RECHA` es la salida funcional de errores de validación:

```text
fila_id
recha
```

El procedure también actualiza `#FARMA_EXPE.RECHA` tomando el menor rechazo encontrado por fila.

# 4. Carga actual de `expe_farma_errores`

La carga actual se realiza en:

`procedures/pri_farma_expe_import.sql`

El flujo es:

```text
Impor_farma_error
        |
        v
#expe_farma_errores
        |
        v
expe_farma_errores
```

La inserción final utiliza:

```sql
INSERT INTO expe_farma_errores (...)
SELECT ...
FROM #expe_farma_errores
```

Pero `#expe_farma_errores` se alimenta desde errores de `Impor_farma_error`, no directamente desde `#FARMA_EXPE_RECHA`.

Esto genera una diferencia importante con el proceso que se propone:

```text
prs_farma_expe_import_valida
        |
        v
#FARMA_EXPE_RECHA
        |
        v
nuevo procedure
        |
        v
expe_farma_errores
```

La nueva carga requeriría resolver explícitamente cómo transformar:

```text
#FARMA_EXPE_RECHA.fila_id
#FARMA_EXPE_RECHA.recha
```

en:

```text
ofi
expe
farma
lote
rece_nro
item_nro
recha
obser
vali
fechas
```

# 5. Comparación con `pri_farma_expe_variables_calc`

Los cinco parámetros solicitados existen y son:

```sql
@prm_ofi
@prm_expe
@prm_lote_impor
@prm_lote_farma
@prm_rece_nro
```

El procedure crea:

```text
#expe_farmacias
#farma_expe_recetas
#farma_expe_rece_items
```

La carga de `#farma_expe_rece_items` se basa en:

```text
expe_farma_recetas
expe_farma_rece_items
```

Sin embargo, esa temporal no contiene toda la información requerida por `#FARMA_EXPE`.

`#FARMA_EXPE` requiere, entre otros:

```text
fila_id
anio
mes
farma
comprobante
fec_prescripcion
fec_autoriz
prepaga
socio
inte
matricula
items
importe
qlineas
troquel
cantidad
cobertura
total
nro_autoriz
sucursal
```

En cambio, `#farma_expe_rece_items` contiene principalmente:

```text
farma
lote
rece_nro
item
produ
audi_canti
sucur
auto
descuentos
precio_manual
```

Por lo tanto, **no alcanza con copiar `#farma_expe_rece_items` hacia `#FARMA_EXPE`**. Será necesario definir una estrategia de reconstrucción combinando:

- `expe_farmacias`
- `expe_farma_recetas`
- `expe_farma_rece_items`
- posiblemente `impor_farma_det`
- posiblemente `impor_farma_cab`

# 6. Inconsistencias detectadas

## 6.1. Dos circuitos de errores diferentes

El circuito actual de importación utiliza:

```text
Impor_farma_error
expe_farma_errores
```

El procedure de validación utiliza:

```text
#FARMA_EXPE_RECHA
```

No existe actualmente una carga directa desde `#FARMA_EXPE_RECHA` hacia `expe_farma_errores`.

## 6.2. El nombre `#farma_expe_recha` no coincide con `#FARMA_EXPE_RECHA`

Existen dos temporales conceptualmente distintas:

```text
#farma_expe_recha
#FARMA_EXPE_RECHA
```

La primera pertenece a `pri_farma_expe_import` y la segunda a `prs_farma_expe_import_valida`.

Debe evitarse confundirlas en el nuevo procedure.

## 6.3. `pri_farma_expe_import_procesa` no controla correctamente los últimos procesos

Después del `COMMIT`, llama a:

```text
pri_farma_expe_variables_calc
pri_farma_expe_calculos
```

Pero el control de retorno y rollback de esas llamadas está comentado.

## 6.4. La working copy SVN está en conflicto

La working copy local contiene conflictos y archivos auxiliares no versionados en procedures relacionados, entre ellos:

- `pri_farma_expe_import_procesa.sql`
- `pri_farma_expe_import.sql`
- `pri_Impor_farma_expe.sql`
- `prs_farma_status_deb.sql`
- `prs_valida_farma_expe.sql`

Para este análisis utilicé la versión del repositorio mediante lectura autenticada, sin resolver ni sobrescribir los conflictos locales.

# 7. Ambigüedades que deben definirse

## Alcance del reproceso

1. ¿Los filtros serán mutuamente excluyentes?

```text
@prm_lote_impor
@prm_lote_farma
@prm_rece_nro
```

2. ¿Qué ocurre si se informan dos o tres filtros simultáneamente?

3. ¿`@prm_lote_impor` corresponde a:

```text
expe_farmacias.lote_importacion
```

o a otra tabla/campo?

4. ¿`@prm_lote_farma` corresponde a:

```text
expe_farmacias.lote
```

5. ¿`@prm_rece_nro` debe buscarse dentro del lote indicado o globalmente dentro del expediente?

## Datos para reconstruir `#FARMA_EXPE`

6. ¿La fuente principal debe ser `impor_farma_det` o las tablas permanentes `expe_farma_*`?

7. ¿De dónde debe obtenerse `fila_id`?

8. ¿Cómo debe calcularse `item_nro` para insertar en `expe_farma_errores`?

9. ¿Qué valor debe utilizarse para `socio`?

10. ¿Qué fecha debe utilizarse como `FEC_PRESCRIPCION` y cuál como `FEC_AUTORIZ`?

11. ¿`COMPROBANTE` debe ser `ticket_nro` o `rece_nro`?

12. ¿`IMPORTE`, `TOTAL`, `COBERTURA` y `CANTIDAD` deben provenir de valores de receta, de ítem o de ambas tablas?

13. ¿Debe conservarse exactamente
...(truncated)...

---

### [23/09/2026 11:20:35] Daniel Acosta

Y arracamos mal desde el inicio... asi que deberas reanalizar todo con esta nueva estructura.

text
nuevo procedure
        |
        v
#FARMA_EXPE y #FARMA_RECHA
        |
        v
prs_farma_expe_import_valida
        |
        v
#FARMA_EXPE_RECHA
        |
        v
expe_farma_errores

---

### [23/09/2026 11:21:04] Agente

Correcto: el flujo objetivo no debe partir de `pri_farma_expe_import_procesa`. Voy a reencuadrar el análisis como un procedimiento nuevo e independiente que construye las temporales de entrada, invoca la validación y persiste únicamente los rechazos resultantes. Mantendré separadas las dependencias históricas para no mezclarlas con el diseño nuevo.

---

### [23/09/2026 11:21:53] Agente

Correcto, Daniel. El análisis anterior mezcló el flujo productivo de importación con el nuevo reproceso. El diseño correcto queda así:

```text
nuevo procedure
        |
        +--> crea/carga #FARMA_EXPE
        |
        +--> crea/carga #FARMA_RECHA
        |
        v
prs_farma_expe_import_valida
        |
        v
#FARMA_EXPE_RECHA
        |
        v
expe_farma_errores
```

# Análisis corregido

## A. Certezas

### 1. El nuevo procedure será independiente

No debe ejecutar:

- `pri_farma_expe_import_procesa`
- `pri_impor_farma_expe`
- `pri_farma_expe_import`
- `pri_farma_expe_calculos`

Esos procedures pertenecen al flujo original de importación y no deben formar parte del reproceso de errores.

### 2. Los cinco parámetros base son los de `pri_farma_expe_variables_calc`

```sql
@prm_ofi
@prm_expe
@prm_lote_impor
@prm_lote_farma
@prm_rece_nro
```

La selección de datos debe basarse en esos filtros.

### 3. La validación recibe las temporales por sesión

`prs_farma_expe_import_valida` solamente recibe:

```sql
@prm_manda
@prm_perio
```

Por lo tanto, el nuevo procedure debe crear y cargar las temporales antes de llamarlo.

### 4. `#FARMA_EXPE` es la entrada principal

Debe tener exactamente la estructura requerida por `prs_farma_expe_import_valida`:

```text
fila_id
ANIO
MES
FARMA
COMPROBANTE
FEC_PRESCRIPCION
FEC_AUTORIZ
PREPAGA
SOCIO
INTE
MATRICULA
ITEMS
IMPORTE
QLINEAS
TROQUEL
CANTIDAD
COBERTURA
TOTAL
NRO_AUTORIZ
SUCURSAL
error_homolog
recha
prepaga_afi
contra_afi
inte_afi
farmacia
```

### 5. `#FARMA_RECHA` debe contener los motivos habilitados

La estructura esperada es:

```text
manda
recha
deno
grupo
tipo
origen
habilitado
```

La lógica existente de carga toma motivos de `recha_motivos` considerando:

```text
grupo = 'FAR'
habilitado IN ('S','B')
carac = 'E'
```

y los relaciona con `manda_rechazos`.

### 6. La salida de validación es `#FARMA_EXPE_RECHA`

La salida contiene:

```text
fila_id
recha
```

El nuevo procedure debe tomar exclusivamente esa temporal como fuente de errores detectados por la validación.

### 7. El destino final es `expe_farma_errores`

La inserción final debe completar:

```text
ofi
expe
error_nro
farma
lote
rece_nro
item_nro
recha
obser
esta
vali
alta_fecha
revi_fecha
actu_fecha
baja_fecha
```

`obser` debe obtenerse desde `#FARMA_RECHA.deno` o desde `recha_motivos.deno`.

# B. Temporales necesarias

Para que el nuevo flujo sea ejecutable, se deben considerar:

## Temporales de entrada/salida

- `#FARMA_EXPE`
- `#FARMA_RECHA`
- `#FARMA_EXPE_RECHA`

## Temporales auxiliares internas de la validación

El código fuente de `prs_farma_expe_import_valida` también declara:

- `#TMP_FARMA_AUTORIZ`
- `#TMP_FARMA_PRODU_PRECIO`

Aunque conceptualmente el flujo solicitado menciona solo `#FARMA_EXPE` y `#FARMA_RECHA`, estas dos temporales también son necesarias porque la validación las utiliza durante su ejecución.

Por lo tanto, el nuevo procedure deberá:

1. Crear `#FARMA_EXPE`.
2. Crear `#FARMA_EXPE_RECHA`.
3. Crear `#FARMA_RECHA`.
4. Crear las temporales auxiliares requeridas por la validación, o confirmar que la implementación final de `prs_farma_expe_import_valida` las cree internamente.
5. Cargar `#FARMA_EXPE`.
6. Cargar `#FARMA_RECHA`.
7. Ejecutar `prs_farma_expe_import_valida`.
8. Insertar los resultados de `#FARMA_EXPE_RECHA` en `expe_farma_errores`.

# C. Construcción de `#FARMA_EXPE`

La base para reconstruir la información debe ser:

```text
expe_farmacias
expe_farma_recetas
expe_farma_rece_items
```

La relación lógica sería:

```text
expe_farmacias
        |
        +--> expe_farma_recetas
                    |
                    +--> expe_farma_rece_items
```

Sin embargo, `expe_farma_rece_items` no contiene todos los campos de `#FARMA_EXPE`.

Por ejemplo, no contiene directamente:

- `SOCIO`
- `MATRICULA`
- `FEC_PRESCRIPCION`
- `FEC_AUTORIZ`
- `COMPROBANTE`
- `NRO_AUTORIZ`
- `COBERTURA` con el mismo significado del archivo original
- `QLINEAS`
- `TOTAL` de la receta

Por eso, la carga no puede ser una copia directa de `#farma_expe_rece_items`.

La reconstrucción deberá combinar campos de las tablas permanentes y definir cómo completar los datos que no existen o no tienen correspondencia directa.

# D. Persistencia corregida de errores

El flujo esperado será:

```text
#FARMA_EXPE_RECHA
        |
        +--> fila_id
        +--> recha
        |
        +--> relación con #FARMA_EXPE
        |       para obtener farmacia, lote, receta e ítem
        |
        +--> relación con #FARMA_RECHA
                para obtener descripción y tipo
        |
        v
expe_farma_errores
```

La relación mínima será:

```sql
#FARMA_EXPE_RECHA.fila_id = #FARMA_EXPE.fila_id
#FARMA_EXPE_RECHA.recha = #FARMA_RECHA.recha
```

El procedimiento no debería utilizar `Impor_farma_error` como fuente intermedia, porque esa tabla pertenece al flujo original de importación.

# E. Inconsistencias que siguen abiertas

## 1. Definición del `fila_id`

Se debe determinar cómo se genera:

```text
#FARMA_EXPE.fila_id
```

Debe ser estable durante todo el reproceso, porque es la clave que permite relacionar:

```text
#FARMA_EXPE
#FARMA_EXPE_RECHA
expe_farma_errores
```

## 2. Identificación del `item_nro`

`#FARMA_EXPE_RECHA` solo devuelve `fila_id` y `recha`.

Para completar `expe_farma_errores.item_nro`, se debe establecer si:

- `#FARMA_EXPE.ITEMS` representa el número de ítem.
- Debe obtenerse desde `expe_farma_rece_items.item`.
- Debe calcularse mediante un `ROW_NUMBER` lógico.
- Debe quedar en cero o nulo para errores de cabecera.

## 3. Identificación del lote

Debe definirse con precisión la diferencia entre:

```text
@prm_lote_impor
@prm_lote_farma
```

La interpretación más probable es:

```text
@prm_lote_impor = expe_farmacias.lote_importacion
@prm_lote_farma = expe_farmacias.lote
```

Esto debe confirmarse antes de codificar.

## 4. Error de cabecera o de ítem

Debe definirse si `#FARMA_EXPE` se cargará:

- Una fila por receta.
- Una fila por ítem.
- Una combinación de filas de cabecera e ítems.

La decisión impacta directamente en:

```text
fila_id
item_nro
cantidad de errores
```

# F. Ambigüedades que deben responderse

1. ¿El nuevo procedure debe crear también `#TMP_FARMA_AUTORIZ` y `#TMP_FARMA_PRODU_PRECIO`, o se modificará `prs_farma_expe_import_valida` para crearlas internamente?

2. ¿La granularidad de `#FARMA_EXPE` será una fila por ítem?

3. ¿Qué columna debe alimentar `#FARMA_EXPE.ITEMS`?

4. ¿Cómo se obtiene `item_nro` para `expe_farma_errores`?

5. ¿Qué tabla es la fuente definitiva de `COMPROBANTE`?

6. ¿Qué tabla es la fuente definitiva de `SOCIO` y `MATRICULA`?

7. ¿Los tres filtros son excluyentes o pueden combinarse?

8. ¿Los errores se agregan históricamente o se reemplazan para el lote/receta reprocesado?

9. ¿Debe generarse siempre un nuevo `error_nro`?

10. ¿Se deben persistir todos los rechazos generados o únicamente los de tipo `E` y `B`?

La corrección principal queda establecida: el nuevo procedure debe construir el contexto de validación desde las tablas permanentes, ejecutar `prs_farma_expe_import_valida` y persistir directamente el contenido resultante de `#FARMA_EXPE_RECHA` en `expe_farma_errores`.

---

### [23/09/2026 11:25:18] Daniel Acosta

me interesa detallar este punto... 

5. Cargar #FARMA_EXPE.

como esperas lograr ese objetivo ?

---

### [23/09/2026 11:26:31] Agente

La forma segura de cargar `#FARMA_EXPE` es **reconstruirla desde las tablas permanentes**, generando una fila por ítem de receta y respetando exactamente la estructura que espera `prs_farma_expe_import_valida`.

No utilizaría:

```sql
SELECT * INTO #FARMA_EXPE FROM #farma_expe_rece_items
```

porque las estructuras no son equivalentes.

## Flujo propuesto de carga

```text
1. Determinar farmacias/lotes alcanzados por los parámetros
                |
                v
2. Obtener recetas del alcance
                |
                v
3. Obtener ítems de esas recetas
                |
                v
4. Transformar cada ítem al formato de #FARMA_EXPE
                |
                v
5. Asignar un fila_id único
                |
                v
6. Insertar #FARMA_EXPE
```

## 1. Determinar el alcance

Se reutilizaría la lógica de los primeros cinco parámetros de `pri_farma_expe_variables_calc`:

```sql
@prm_ofi
@prm_expe
@prm_lote_impor
@prm_lote_farma
@prm_rece_nro
```

Primero se seleccionan las farmacias correspondientes:

```text
expe_farmacias
```

con estos filtros:

```text
ofi = @prm_ofi
expe = @prm_expe
baja_fecha IS NULL
liqui_fecha IS NULL en expedientes
```

Luego:

- Si se informa `@prm_lote_impor`, se filtra por `lote_importacion`.
- Si se informa `@prm_lote_farma`, se filtra por `lote`.
- Si se informa `@prm_rece_nro`, se filtra por receta.

Esto debería cargar una temporal auxiliar equivalente a:

```text
#expe_farmacias
```

## 2. Obtener las recetas

Desde:

```text
expe_farma_recetas
```

relacionada con las farmacias seleccionadas:

```text
ofi
expe
farma
lote
```

y filtrando:

```text
baja_fecha IS NULL
```

La temporal intermedia sería equivalente a:

```text
#farma_expe_recetas
```

## 3. Obtener los ítems

Desde:

```text
expe_farma_rece_items
```

relacionada con:

```text
ofi
expe
farma
lote
rece_nro
```

y también:

```text
baja_fecha IS NULL
```

La temporal intermedia sería equivalente a:

```text
#farma_expe_rece_items
```

## 4. Insertar una fila por ítem en `#FARMA_EXPE`

La carga final debería ser explícita, columna por columna:

```sql
INSERT INTO #FARMA_EXPE
(
    fila_id,
    ANIO,
    MES,
    FARMA,
    COMPROBANTE,
    FEC_PRESCRIPCION,
    FEC_AUTORIZ,
    PREPAGA,
    SOCIO,
    INTE,
    MATRICULA,
    ITEMS,
    IMPORTE,
    QLINEAS,
    TROQUEL,
    CANTIDAD,
    COBERTURA,
    TOTAL,
    NRO_AUTORIZ,
    SUCURSAL,
    error_homolog,
    recha,
    prepaga_afi,
    contra_afi,
    inte_afi,
    farmacia
)
SELECT
    ...
FROM #farma_expe_recetas R
INNER JOIN #farma_expe_rece_items I
    ON I.farma = R.farma
   AND I.lote = R.lote
   AND I.rece_nro = R.rece_nro
```

El `fila_id` debe ser generado de forma controlada y única dentro de la ejecución. No conviene reutilizar directamente `item` como `fila_id`, porque el mismo número de ítem puede repetirse en varias farmacias, lotes o recetas.

Una alternativa sería:

```text
fila_id = numeración correlativa de la ejecución
```

ordenada por:

```text
farma
lote
rece_nro
item
```

## 5. Mapeo conocido

Hay campos cuya correspondencia es relativamente directa:

| `#FARMA_EXPE` | Fuente probable |
|---|---|
| `FARMA` | `expe_farma_rece_items.farma` |
| `INTE` | `expe_farma_recetas.inte` |
| `PREPAGA` | `expe_farma_recetas.prepaga` |
| `ITEMS` | `expe_farma_rece_items.item` |
| `TROQUEL` | `expe_farma_rece_items.produ` |
| `CANTIDAD` | `audi_canti`; si es cero, `rece_canti` |
| `NRO_AUTORIZ` | `expe_farma_rece_items.auto` |
| `SUCURSAL` | `expe_farma_rece_items.sucur` |
| `recha` | `NULL` inicialmente |
| `error_homolog` | `NULL` inicialmente |
| `prepaga_afi` | `NULL`, lo completa la validación |
| `contra_afi` | `NULL`, lo completa la validación |
| `inte_afi` | `NULL`, lo completa la validación |
| `farmacia` | `NULL` o valor inicial, según la lógica de validación |

## 6. Campos que todavía no puedo completar sin definición funcional

La estructura permanente no tiene una correspondencia inequívoca para todos los campos de `#FARMA_EXPE`.

### `SOCIO`

`expe_farma_recetas` tiene:

```text
contra
inte
```

pero no tiene un campo claramente equivalente a `SOCIO`.

No se debe asumir que:

```text
SOCIO = contra
```

porque `contra` representa el contrato o número de afiliación contractual, no necesariamente el número de socio que utiliza la validación.

### `MATRICULA`

`expe_farma_recetas` tiene:

```text
ori_matri_naci
ori_matri_prov
aso_matri_naci
aso_matri_prov
```

Pero `#FARMA_EXPE` tiene solamente:

```text
MATRICULA
```

Hay que definir cuál de esas matrículas debe utilizarse y con qué prioridad.

### Fechas

Se deben definir las correspondencias:

```text
FEC_PRESCRIPCION
FEC_AUTORIZ
```

Las tablas tienen fechas como:

```text
ticket_fecha
emi_fecha
alta_fecha
actu_fecha
```

No es correcto elegir una por similitud de nombre.

### `COMPROBANTE`

Puede corresponder a:

```text
ticket_nro
```

pero debe confirmarse que ese es el valor que `prs_farma_expe_import_valida` espera en la columna `COMPROBANTE`.

### `IMPORTE`, `TOTAL` y `COBERTURA`

Hay valores en receta e ítem:

```text
rece_vta_precio
rece_afi_precio
audi_vta_precio
audi_afi_precio
dto_cober
```

Se debe definir si:

- `IMPORTE` es el valor del ítem.
- `TOTAL` es el total de la receta.
- `COBERTURA` es porcentaje o importe.
- Los valores deben recalcularse o reutilizarse de las tablas existentes.

### `QLINEAS`

Debe definirse si representa:

- cantidad de ítems de la receta;
- número de línea del archivo;
- cantidad total de renglones;
- otro dato histórico de importación.

## 7. Por qué el procedimiento debe cargar primero las auxiliares

Aunque el objetivo final sea solamente `#FARMA_EXPE`, mantener estas etapas facilita respetar la lógica vigente:

```text
#expe_farmacias
#farma_expe_recetas
#farma_expe_rece_items
        |
        v
#FARMA_EXPE
```

Así se separan:

- el alcance del reproceso;
- la obtención de recetas;
- la obtención de ítems;
- la transformación al formato de validación.

También permite que los filtros de lote y receta se apliquen antes de construir la temporal final.

## Conclusión

La carga de `#FARMA_EXPE` la lograría mediante un `INSERT` explícito, con una fila por ítem, partiendo de:

```text
expe_farmacias
expe_farma_recetas
expe_farma_rece_items
```

y utilizando temporales auxiliares equivalentes a las de `pri_farma_expe_variables_calc`.

La parte técnicamente definida es el alcance, la relación entre farmacias, recetas e ítems y los campos de identificación. Lo que todavía debe resolverse antes de escribir el procedure es el mapeo funcional de:

```text
SOCIO
MATRICULA
FEC_PRESCRIPCION
FEC_AUTORIZ
COMPROBANTE
IMPORTE
TOTAL
COBERTURA
QLINEAS
```

Esos campos son necesarios para que `prs_farma_expe_import_valida` reproduzca correctamente la validación original.

---

### [23/09/2026 11:47:44] Daniel Acosta

## 1. Definición del fila_id

Se debe determinar cómo se genera:
text
#FARMA_EXPE.fila_id
es un contador incremental de cada fila insertada

## 3. Identificación del lote

Debe definirse con precisión la diferencia entre:
text
@prm_lote_impor
@prm_lote_farma
esta claramente explicado y resuelto cuando analices en profundidad pri_farma_expe_variables_calc y en la descripcion de la necesidad se explico detalladamente.
revisa el pedido y pregunta con presicion que no te queda claro porque eso ya se explico.

## 4. Error de cabecera o de ítem

Debe definirse si #FARMA_EXPE se cargará:

Una fila por receta.
Una fila por ítem.
Una combinación de filas de cabecera e ítems.

Este púnto ya se explico y no se entendió, al informar que tomemos como base el proceso  pri_farma_expe_variables_calc y basicamente fuimos claros en decir a-estos parametros b-estas temporales y c.hasta esta carga que son los items , la conclusion sin ambiguedades es que la ultima temporal es la que tiene le detalle delo que deberiamos volcar a la temporal principal #FARMA_EXPE


1. ¿El nuevo procedure debe crear también #TMP_FARMA_AUTORIZ y #TMP_FARMA_PRODU_PRECIO, o se modificará prs_farma_expe_import_valida para crearlas internamente?

noooooooooooooooo!!! si usas un stored procedure y necesita de una o mas tablas pre-creadas o pre-cargadas NO SE NOS PUEDE OCURRIR modificar un PROCEDURE que ya funciona y te dice si me vas a usar estas son las pre-condiciones y debes aceptarlas tal cual ... asi que si, el nuevo proceso le tiene que preveer de lo necesario para que se ejecute.

4. ¿Cómo se obtiene item_nro para expe_farma_errores?
esa columna ya esta en EXPE_FARMA_RECETAS y si no esta presente en la temporal #farma_expe_recetas y esa deberia haber sido una propuesta tuya evolutiva, ya que estamos copiando toda la primera parte del proceso que te di como referencia.

5. ¿Qué tabla es la fuente definitiva de COMPROBANTE?
justamente aqui esperaba la solucion o propuesta de tu parte,ademas para SOCIO y MATRICULA que sale de analizar el proceso de validacion y de analizar todo el proceso pri_farma_expe_import_procesa ahi esta como se toma cada columna de la temporal #FARMA_EXPE para luego volcarlo a columnas de tablas fijas, lo que espeba ahora es que hagas vos ese proceso inverso.

ANALIZA TODO ESTO... 
termina de construir las certezas si es que fueron respondidas algunas ambiguedades y entregame la primera version.
desde esa base continuamos si es que no podes ofrecer nada mas..

---

### [23/09/2026 11:47:54] Agente

Voy a cerrar las ambigüedades que ya fueron respondidas por el requerimiento y rehacer el mapeo inverso desde `pri_farma_expe_import`/`prs_farma_expe_import_valida`: `#farma_expe_rece_items` será el detalle que alimenta una fila de `#FARMA_EXPE`, `item_nro` se conservará desde `expe_farma_recetas`, y el nuevo procedure preparará todas las temporales exigidas sin modificar la validación existente. Revisaré los campos concretos antes de emitir la primera versión.

---

### [23/09/2026 11:49:20] Agente

Daniel, acepto las correcciones. Rehago el análisis tomando como premisas definitivas:

- `fila_id` es un contador incremental de cada fila insertada en `#FARMA_EXPE`.
- Los cinco parámetros y sus alcances ya están definidos por `pri_farma_expe_variables_calc`.
- Se replica la secuencia:

```text
#expe_farmacias
        |
        v
#farma_expe_recetas
        |
        v
#farma_expe_rece_items
        |
        v
#FARMA_EXPE
```

- La última temporal contiene el detalle de ítems que debe volcarse a `#FARMA_EXPE`.
- El nuevo procedure debe crear y cargar todas las temporales necesarias para ejecutar `prs_farma_expe_import_valida`.
- `prs_farma_expe_import_valida` no se modifica.

# Primera versión del enfoque

## 1. Carga de las temporales base

El nuevo procedure tendrá como parámetros:

```sql
@prm_ofi
@prm_expe
@prm_lote_impor
@prm_lote_farma
@prm_rece_nro
@prm_mensaje OUTPUT
```

La primera parte será una reproducción controlada de `pri_farma_expe_variables_calc`:

```text
#expe_farmacias
#farma_expe_recetas
#farma_expe_rece_items
```

La diferencia es que `#farma_expe_recetas` debe conservar también los datos necesarios para el reproceso, incluyendo:

```text
item_nro
```

Y `#farma_expe_rece_items` debe conservar el detalle:

```text
item
produ
rece_vta_precio
rece_canti
audi_canti
sucur
auto
descuentos
precio_manual
```

La carga de `#farma_expe_rece_items` debe mantener exactamente la selección de registros determinada por:

```text
@prm_ofi
@prm_expe
@prm_lote_impor
@prm_lote_farma
@prm_rece_nro
```

# 2. Regla de generación de `fila_id`

Se implementará como contador incremental de la carga final:

```text
1
2
3
4
...
N
```

La numeración debe asignarse luego de obtener el conjunto final de ítems y antes de insertar en `#FARMA_EXPE`.

El orden debe ser estable:

```text
farma
lote
rece_nro
item
```

Conceptualmente:

```text
fila_id = contador incremental por cada fila de ítem
```

Ese mismo `fila_id` será utilizado posteriormente por:

```text
#FARMA_EXPE_RECHA
```

y permitirá relacionar cada rechazo con la fila original.

# 3. Una fila de `#FARMA_EXPE` por cada ítem

La carga final será:

```text
una fila #FARMA_EXPE
por cada fila de #farma_expe_rece_items
```

No se generará una fila adicional de cabecera.

Esto es consistente con la validación existente, porque `prs_farma_expe_import_valida` trabaja a nivel de:

- producto;
- cantidad;
- autorización;
- farmacia;
- receta;
- fila individual.

# 4. Mapeo inverso desde el proceso original

El proceso original convierte `#FARMA_EXPE` hacia `Impor_farma_det` con esta correspondencia:

```text
#FARMA_EXPE       -> Impor_farma_det
ANIO              -> anio
MES               -> mes
FARMA             -> farma
COMPROBANTE       -> ticket_nro
FEC_PRESCRIPCION  -> emi_fecha
FEC_AUTORIZ       -> ticket_fecha
PREPAGA           -> prepaga
SOCIO             -> contra
INTE              -> inte
MATRICULA         -> matri_presc
ITEMS             -> item_nro
IMPORTE           -> importe_total
QLINEAS           -> item_2
TROQUEL           -> troquel
CANTIDAD          -> cant
COBERTURA         -> cober
TOTAL             -> importe_total_2
NRO_AUTORIZ       -> nro_auto
SUCURSAL          -> sucu
```

Por lo tanto, la carga inversa propuesta para `#FARMA_EXPE` queda así.

## Mapeo propuesto

| Campo `#FARMA_EXPE` | Fuente para el nuevo procedure |
|---|---|
| `fila_id` | contador incremental |
| `ANIO` | año de `emi_fecha` |
| `MES` | mes de `emi_fecha` |
| `FARMA` | `#farma_expe_rece_items.farma` |
| `COMPROBANTE` | `#farma_expe_recetas.ticket_nro` |
| `FEC_PRESCRIPCION` | `#farma_expe_recetas.emi_fecha` |
| `FEC_AUTORIZ` | `#farma_expe_recetas.ticket_fecha` |
| `PREPAGA` | `#farma_expe_recetas.prepaga` |
| `SOCIO` | `#farma_expe_recetas.contra` |
| `INTE` | `#farma_expe_recetas.inte` |
| `MATRICULA` | matrícula de prescripción conservada en la fuente histórica disponible |
| `ITEMS` | `#farma_expe_rece_items.item` |
| `IMPORTE` | importe de venta del ítem o valor equivalente conservado |
| `QLINEAS` | cantidad de ítems de la receta |
| `TROQUEL` | producto/troquel conservado para el ítem |
| `CANTIDAD` | `audi_canti`; si es cero, `rece_canti` |
| `COBERTURA` | cobertura/descuento conservado del ítem o receta |
| `TOTAL` | `#farma_expe_rece_items.rece_vta_precio` |
| `NRO_AUTORIZ` | `#farma_expe_rece_items.auto` |
| `SUCURSAL` | `#farma_expe_rece_items.sucur` |
| `error_homolog` | `NULL` inicialmente |
| `recha` | `NULL` inicialmente |
| `prepaga_afi` | `NULL`; lo completa la validación |
| `contra_afi` | `NULL`; lo completa la validación |
| `inte_afi` | `NULL`; lo completa la validación |
| `farmacia` | `NULL` inicialmente; lo completa la validación |

# 5. Corrección sobre `item_nro`

La columna `item_nro` existe en:

```text
expe_farma_recetas
```

Por lo tanto, la propuesta correcta es:

1. Incorporarla explícitamente en `#farma_expe_recetas`.
2. Mantenerla durante la carga de las recetas.
3. Utilizarla al momento de preparar la información de errores.

Sin embargo, el detalle de cada ítem está en:

```text
expe_farma_rece_items.item
```

El proceso original utiliza ese valor para insertar el campo `item` de `expe_farma_rece_items`.

La primera versión debe conservar ambos valores:

```text
#farma_expe_recetas.item_nro
#farma_expe_rece_items.item
```

y utilizar el valor de detalle para identificar el error específico de cada renglón.

# 6. Temporales que debe crear el nuevo procedure

Como `prs_farma_expe_import_valida` no se modifica, el nuevo procedure debe precrear:

```text
#FARMA_EXPE
#FARMA_RECHA
#FARMA_EXPE_RECHA
#TMP_FARMA_AUTORIZ
#TMP_FARMA_PRODU_PRECIO
```

Además, antes de llamar a la validación debe cargar:

```text
#FARMA_EXPE
#FARMA_RECHA
```

El procedure de validación será utilizado tal cual está.

# 7. Carga de `#FARMA_RECHA`

Se reutilizará la lógica existente de `pri_impor_farma_expe`:

```text
recha_motivos
manda_rechazos
```

con:

```text
grupo = 'FAR'
habilitado IN ('S','B')
carac = 'E'
```

El resultado se cargará en:

```text
#FARMA_RECHA
```

incluyendo:

```text
manda
recha
deno
grupo
tipo
origen
habilitado
```

# 8. Persistencia de `#FARMA_EXPE_RECHA`

Luego de ejecutar:

```sql
prs_farma_expe_import_valida
```

se tomará:

```text
#FARMA_EXPE_RECHA.fila_id
#FARMA_EXPE_RECHA.recha
```

y se relacionará con:

```text
#FARMA_EXPE.fila_id
#FARMA_RECHA.recha
```

La carga final será:

```text
#FARMA_EXPE_RECHA
        |
        +--> #FARMA_EXPE
        |       obtiene farmacia, lote, receta, ítem
        |
        +--> #FARMA_RECHA
                obtiene descripción y tipo
        |
        v
expe_farma_errores
```

# 9. Puntos que ya quedan resueltos

Quedan cerradas estas definiciones:

- `fila_id`: contador incremental.
- Alcance: los cinco parámetros de `pri_farma_expe_variables_calc`.
- Base de datos fuente: `expe_farmacias`, `expe_farma_recetas`, `expe_farma_rece_items`.
- Temporal de detalle: `#farma_expe_rece_items`.
- Granularidad: una fila de `#FARMA_EXPE` por ítem.
- `item_nro`: debe conservarse en la cadena de temporales.
- El nuevo procedure debe crear todas las temporales exigidas por la validación.
- No se modifica `prs_farma_expe_import_valida`.
- La salida de validación es `#FARMA_EXPE_RECHA`.
- El destino final es `expe_farma_errores`.

# 10. Únicos puntos que todavía requieren verificación técnica

No quedan como ambigüedades de diseño general, sino como verificaciones concretas de disponibilidad de datos:

1. **MATRICULA**  
   El proceso original la recibía desde `#FARMA_EXPE.MATRICULA` y la guardaba en `Impor_farma_det.matri_presc`, pero el alta actual de `expe_farma_recetas` no la conserva explícitamente. Hay que verificar si sigue disponible en `Impor_farma_det` o en otra tabla histórica.

2. **TROQUEL original**  
   `expe_farma_rece_items.produ` puede contener el producto homologado, no necesariamente el troquel original. Hay que verificar si el troquel original está disponible en otra columna histórica.

3. **IMPORTE y COBERTURA originales**  
   Deb
...(truncated)...

---

### [23/09/2026 11:55:31] Daniel Acosta

1. **MATRICULA**  
   El proceso original la recibía desde #FARMA_EXPE.MATRICULA y la guardaba en Impor_farma_det.matri_presc, pero el alta actual de expe_farma_recetas no la conserva explícitamente. Hay que verificar si sigue disponible en Impor_farma_det o en otra tabla histórica.

cargala vacia por ahora.

2. **TROQUEL original**  
   expe_farma_rece_items.produ puede contener el producto homologado, no necesariamente el troquel original. Hay que verificar si el troquel original está disponible en otra columna histórica.

troquel y produ representan el mismo dato (a veces) asi que carga el .produ en la temporal donde dice TROQUEL y entreguemos la primera version.

3. **IMPORTE y COBERTURA originales**  
   Debe verificarse si la validación necesita el valor original del archivo o si alcanza con los valores calculados/conservados en expe_farma_rece_items. toma los valores actuales  y entreguemos la primera version.

---

### [23/09/2026 11:56:26] Agente

Perfecto. Con estas definiciones queda cerrada la primera versión del criterio de carga de `#FARMA_EXPE`.

# Primera versión consolidada

## 1. Granularidad

Se cargará:

```text
una fila de #FARMA_EXPE por cada fila de #farma_expe_rece_items
```

El flujo será:

```text
#expe_farmacias
        |
        v
#farma_expe_recetas
        |
        v
#farma_expe_rece_items
        |
        v
#FARMA_EXPE
```

No se generará una fila separada de cabecera.

## 2. `fila_id`

Se generará como contador incremental de la carga final:

```text
1, 2, 3, ..., N
```

El orden será estable por:

```text
farma
lote
rece_nro
item
```

Ese mismo valor será utilizado por `#FARMA_EXPE_RECHA.fila_id`.

## 3. Mapeo definido para `#FARMA_EXPE`

| Campo | Valor de primera versión |
|---|---|
| `fila_id` | Contador incremental |
| `ANIO` | Año de `#farma_expe_recetas.emi_fecha` |
| `MES` | Mes de `#farma_expe_recetas.emi_fecha` |
| `FARMA` | `#farma_expe_rece_items.farma` |
| `COMPROBANTE` | `#farma_expe_recetas.ticket_nro` |
| `FEC_PRESCRIPCION` | `#farma_expe_recetas.emi_fecha` |
| `FEC_AUTORIZ` | `#farma_expe_recetas.ticket_fecha` |
| `PREPAGA` | `#farma_expe_recetas.prepaga` |
| `SOCIO` | `#farma_expe_recetas.contra` |
| `INTE` | `#farma_expe_recetas.inte` |
| `MATRICULA` | Cadena vacía `''` |
| `ITEMS` | `#farma_expe_rece_items.item` |
| `IMPORTE` | `#farma_expe_rece_items.rece_vta_precio` |
| `QLINEAS` | Cantidad de ítems de la receta |
| `TROQUEL` | `#farma_expe_rece_items.produ` |
| `CANTIDAD` | `audi_canti`; si es cero, `rece_canti` |
| `COBERTURA` | `#farma_expe_rece_items.dto_cober` |
| `TOTAL` | Valor actual disponible en el detalle de ítem |
| `NRO_AUTORIZ` | `#farma_expe_rece_items.auto` |
| `SUCURSAL` | `#farma_expe_rece_items.sucur` |
| `error_homolog` | `NULL` |
| `recha` | `NULL` |
| `prepaga_afi` | `NULL` |
| `contra_afi` | `NULL` |
| `inte_afi` | `NULL` |
| `farmacia` | `NULL` |

## 4. Decisiones aplicadas

### Matrícula

Se carga vacía:

```sql
MATRICULA = ''
```

No se intenta reconstruirla desde otras columnas.

### Troquel

Se acepta que `produ` y `troquel` representan el mismo dato para esta primera versión:

```sql
TROQUEL = #farma_expe_rece_items.produ
```

### Importe

Se toma el valor actual del detalle:

```sql
IMPORTE = #farma_expe_rece_items.rece_vta_precio
```

### Cobertura

Se toma el valor actual calculado para el ítem:

```sql
COBERTURA = #farma_expe_rece_items.dto_cober
```

### Cantidad

Se conserva la misma lógica utilizada por `pri_farma_expe_variables_calc`:

```sql
CANTIDAD =
    CASE
        WHEN audi_canti > 0 THEN audi_canti
        ELSE rece_canti
    END
```

### Ítem del error

El error se asociará al número de ítem del detalle:

```sql
expe_farma_errores.item_nro = #farma_expe_rece_items.item
```

`#farma_expe_recetas.item_nro` se conservará en la temporal de recetas como dato complementario, pero la granularidad del nuevo reproceso será la de `expe_farma_rece_items.item`.

# 5. Temporales que preparará el nuevo procedure

El nuevo procedure deberá crear y cargar:

```text
#expe_farmacias
#farma_expe_recetas
#farma_expe_rece_items
#FARMA_EXPE
#FARMA_RECHA
#FARMA_EXPE_RECHA
#TMP_FARMA_AUTORIZ
#TMP_FARMA_PRODU_PRECIO
```

La validación se invocará sin modificarla:

```sql
EXEC dbo.prs_farma_expe_import_valida
     @prm_manda = @var_manda,
     @prm_perio = @var_perio
```

# 6. Flujo completo de la primera versión

```text
Nuevo procedure
    |
    +--> Carga #expe_farmacias según los 5 parámetros
    |
    +--> Carga #farma_expe_recetas
    |       incluyendo item_nro
    |
    +--> Carga #farma_expe_rece_items
    |
    +--> Genera fila_id incremental
    |
    +--> Carga #FARMA_EXPE, una fila por ítem
    |
    +--> Carga #FARMA_RECHA
    |
    +--> Prepara las temporales auxiliares de validación
    |
    +--> Ejecuta prs_farma_expe_import_valida
    |
    +--> Lee #FARMA_EXPE_RECHA
    |
    +--> Relaciona fila_id + recha
    |
    +--> Inserta en expe_farma_errores
```

# 7. Resultado

Con estas definiciones ya no queda pendiente decidir:

- cómo se genera `fila_id`;
- qué granularidad tiene `#FARMA_EXPE`;
- de dónde sale `TROQUEL`;
- de dónde sale `IMPORTE`;
- de dónde sale `COBERTURA`;
- cómo se obtiene `item_nro`;
- si se modifica o no `prs_farma_expe_import_valida`.

La primera versión queda planteada como un reproceso de errores basado en el detalle actual de `expe_farma_rece_items`, sin alterar los procedures existentes.

---

### [23/09/2026 12:01:17] Daniel Acosta

bueno para terminar dejame la primera version del proceso en :
\\infomed03n\Agentes\d.acosta\PRS_3850_DB\Tarea #30287 - Modo de Calculo

dejame un documento con todo lo que hablamos y en el orden para posteriores analisis y validaciones internas.

creame un documento o imagen tipo markdown con el nuevo proceso

---

### [23/09/2026 12:01:27] Agente

Voy a validar primero la carpeta de destino exacta. Si está disponible, crearé allí un único documento Markdown con: historial ordenado de decisiones, certezas cerradas, descartes, temporales, mapeos y el diseño de la primera versión del nuevo procedure. No tocaré procedures existentes ni resolveré conflictos SVN.

---

### [23/09/2026 12:02:30] Agente

La carpeta de destino existe y está vacía, por lo que es una referencia válida. Crearé un único Markdown allí, con el nombre `PRIMERA_VERSION_REPROCESO_ERRORES_FARMA.md`, incluyendo el recorrido de decisiones, el flujo corregido, temporales, mapeo de columnas, criterios cerrados, validaciones y pendientes técnicos.

---

### [23/09/2026 14:00:14] Daniel Acosta

continuar

---

### [23/09/2026 14:00:26] Agente

Continúo desde el punto exacto: la carpeta ya fue validada; el primer intento de escritura no creó el archivo por un error de formato del parche. Lo corrijo ahora y luego validaré existencia, contenido, encoding y finales de línea.

---

### [23/09/2026 14:03:17] Agente

Daniel, dejé creado y validado el documento Markdown en:

[PRIMERA_VERSION_REPROCESO_ERRORES_FARMA.md](/mnt/Agentes/d.acosta/PRS_3850_DB/Tarea%20%2330287%20-%20Modo%20de%20Calculo/PRIMERA_VERSION_REPROCESO_ERRORES_FARMA.md)

El documento contiene:

- Evolución ordenada del análisis.
- Flujo inicial descartado y flujo correcto.
- Parámetros del nuevo procedure.
- Temporales requeridas.
- Carga de `#expe_farmacias`, `#farma_expe_recetas` y `#farma_expe_rece_items`.
- Generación incremental de `fila_id`.
- Carga de `#FARMA_EXPE`, una fila por ítem.
- Mapeo completo de columnas.
- Decisiones sobre `MATRICULA`, `TROQUEL`, `IMPORTE`, `COBERTURA` y `CANTIDAD`.
- Carga de `#FARMA_RECHA`.
- Ejecución de `prs_farma_expe_import_valida`.
- Persistencia en `expe_farma_errores`.
- Diagrama Markdown del proceso.
- Validaciones, riesgos y límites conocidos.

Validé que el archivo quedó en UTF-8, con 452 líneas y sin modificar procedures existentes ni ejecutar escrituras en PRESTACI DEV.

---

### [23/09/2026 14:05:46] Daniel Acosta

y el stored procedure nuevo? el SQL donde esta...???

---

### [23/09/2026 14:05:58] Agente

Tenés razón: en la entrega anterior dejé solamente el documento de diseño; el SQL no quedó generado. Voy a crear ahora el script de primera versión del nuevo procedure en la misma carpeta de Tarea #30287, como **borrador no instalable**, sin ejecutarlo en PRESTACI DEV ni modificar el repositorio SVN. Antes verificaré la convención de encoding de los SQL cercanos y respetaré las restricciones del proyecto.

---

### [23/09/2026 14:08:31] Agente

Tenés razón: el SQL no estaba generado en la entrega anterior. Ya dejé creada la primera versión del stored procedure en:

[pri_farma_expe_errores_repro_v1.sql](/mnt/Agentes/d.acosta/PRS_3850_DB/Tarea%20%2330287%20-%20Modo%20de%20Calculo/pri_farma_expe_errores_repro_v1.sql)

El documento de diseño queda en:

[PRIMERA_VERSION_REPROCESO_ERRORES_FARMA.md](/mnt/Agentes/d.acosta/PRS_3850_DB/Tarea%20%2330287%20-%20Modo%20de%20Calculo/PRIMERA_VERSION_REPROCESO_ERRORES_FARMA.md)

El SQL incluye:

- Parámetros `@prm_ofi`, `@prm_expe`, `@prm_lote_impor`, `@prm_lote_farma`, `@prm_rece_nro`.
- Carga de `#expe_farmacias`.
- Carga de `#farma_expe_recetas`.
- Carga de `#farma_expe_rece_items`.
- Construcción de `#FARMA_EXPE`.
- `fila_id` incremental.
- `MATRICULA` vacía.
- `TROQUEL = produ`.
- Uso de valores actuales para importe y cobertura.
- Carga de `#FARMA_RECHA`.
- Preparación de las temporales auxiliares.
- Ejecución de `prs_farma_expe_import_valida`.
- Persistencia de `#FARMA_EXPE_RECHA` en `expe_farma_errores`.

Está identificado dentro del archivo como **borrador de primera versión no instalable**. No lo ejecuté en PRESTACI DEV ni modifiqué procedures existentes.

Validaciones estáticas realizadas:

- Archivo ASCII.
- 386 líneas.
- Sin punto y coma.
- Sin uso de `RTRIM`.
- Referencias verificadas a `#FARMA_EXPE`, `#FARMA_RECHA`, `#FARMA_EXPE_RECHA`, `prs_farma_expe_import_valida` y `expe_farma_errores`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
