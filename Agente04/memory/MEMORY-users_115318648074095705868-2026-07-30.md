# Memoria curada diaria

- Fecha: 2026-07-30
- Usuario: Daniel Acosta
- id_googlechat: users/115318648074095705868
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/gbatvyaaaae

## Resumen de la actividad

### Tema 1: Inicio de sesión y carga de preferencias

- Solicitud o contexto: Daniel consultó si el agente estaba activo.
- Respuesta o acciones del agente: Se ejecutó el bootstrap operativo obligatorio, leyendo `REGLAS.md`, `REPOSITORIOS.md`, `DB_PROFILE.md`, `USUARIOS.md` y las preferencias de Daniel.
- Resultado: El agente confirmó disponibilidad con preferencias cargadas.
- Estado: Resuelto

### Tema 2: Nueva regla durable para AJUSTES del Evolucionador

- Solicitud o contexto: Daniel informó que el EVOLUCIONADOR quedó funcionando correctamente con tres pantallas: revisión previa, repaso posterior por objeto y evidencia de éxito. Luego definió una nueva regla contractual para registrar ajustes mecánicos posteriores al cambio principal dentro de una sección `AJUSTES:` perteneciente al mismo `[CAMBIO NNNN]`.
- Respuesta o acciones del agente: Se creó la propuesta durable `contrato-evolucionador-ajustes-20260730-f1ba3d1f45`, incluyendo que `AJUSTES:` no genera cambio nuevo, no modifica la intención funcional y acumula ajustes mecánicos en el último cambio del archivo cuando correspondan varios cambios sobre el mismo objeto. También se registró el soporte inicial para `xreplace(buscar|reemplazar)` y `xreplace(buscar|reemplazar|cantidad)`.
- Resultado: La propuesta quedó creada y pendiente. Al intentar aplicarla tras la confirmación de Daniel, el gateway devolvió `Plugin approval required (gateway unavailable)`, por lo que no pudo aprobarse en ese momento.
- Estado: Pendiente

### Tema 3: Validación de importación para `conve_farma_produ`

- Solicitud o contexto: Daniel reportó errores en la ventana de importación de productos `wp_import_conve_farma_produ.srw` y pidió analizar objetos actualizados en `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/conve_farma_produ`. El problema funcional era que valores incompatibles con la estructura de tabla, como `dto decimal(5,2)` con valor `1234`, podían cancelar el proceso al llegar al `pri_`.
- Respuesta o acciones del agente: Se localizaron los objetos `wp_import_conve_farma_produ.srw` y `dw_import_conve_farma_produ.srd`, se analizó la estructura y se generaron propuestas de evolución DEV/EVO para validar formato después de cargar archivo, marcar rechazos en `result` y enviar al procedimiento solo filas sin rechazo previo.
- Resultado: Se dejaron `propuesta_evolucion_DEV.txt` y `propuesta_evolucion_EVO.txt` en la carpeta de salida. El criterio propuesto incluía tratar `produ`, `precio` y `dto` como texto en la DW, marcar mensajes como `[produ] formato no valido`, `[precio] formato no valido` y `[dto] formato no valido`, continuar el proceso y agregar formalmente XLS, XLSX y CSV al selector. Se validó que las propuestas tenían 9 cambios cada una y CRLF.
- Estado: Resuelto

### Tema 4: Retroalimentación sobre estándar de conservación de comportamiento previo

- Solicitud o contexto: Daniel entregó su versión compilada en `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/conve_farma_produ/30-07-2026_0836`, indicando que los archivos `_new.srw` y `_new.srd` eran los confirmados. Pidió comparar contra la propuesta del agente y comenzar a documentar criterios de desarrollo para evitar repetir propuestas rechazadas.
- Respuesta o acciones del agente: Se reconoció que fue acertado pasar `produ`, `precio` y `dto` a string, pero que la propuesta del agente fue demasiado invasiva porque modificaba `result` como texto libre y reconstruía colores, listas, expresiones, formatos y comportamiento que ya funcionaban. Se destacó que la versión de Daniel conservó `result` como `char(1)`, agregó códigos `X/Y/Z`, preservó la configuración previa de la DataWindow e integró `wf_valida_column()` dentro del loop existente de `wf_importar_registros()`.
- Resultado: Se creó la propuesta durable `criterio-conservacion-comportamiento-previo-20260730-286b741997`, pendiente de aplicar, con el criterio de buscar primero la modificación mínima viable, conservar comportamiento previo y ampliar solo ante dependencia técnica concreta. Se detectó además una mezcla técnica menor `CRLF/CR` en `wp_import_conve_farma_produ_new.srw`.
- Estado: Pendiente

### Tema 5: Flush de memoria por compactación

- Solicitud o contexto: Se recibió una instrucción automática de pre-compaction memory flush para guardar solo memorias durables en `memory/2026-07-30.md`, sin tocar archivos base y sin crear variantes timestamped.
- Respuesta o acciones del agente: Se evaluó el pedido y se respondió `NO_REPLY`.
- Resultado: No se registró contenido adicional por esa vía durante esa interacción.
- Estado: Informativo

### Tema 6: Generación ABM PRESMED para `conve_labo_mandatarias`

- Solicitud o contexto: Daniel convocó la receta `RECETA_PRESMED_ABM` para construir una primera versión funcional de ABM PRESMED usando repo `PRS_3850_DB`, profile `SwissDeVP10_prestaci`, tabla dominante `conve_labo_mandatarias`, metadata DB como fuente de estructura y carpeta de salida `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/conve_labo_mandatarias`.
- Respuesta o acciones del agente: Se regeneraron los objetos PowerBuilder y SQL esperados usando metadata real, con tabla y columnas en minúsculas, límite Sybase de 30 caracteres y reglas PRESMED para procedures.
- Resultado: Se entregaron `wp_conve_labo_mandatarias.srw`, `d_conve_labo_mandatarias.srd`, `d_lista_conve_labo_mandatarias.srd`, `pri_conve_labo_mandatarias.sql`, `pru_conve_labo_mandatarias.sql`, `prd_conve_labo_mandatarias.sql`, `prs_conve_labo_mandatarias.sql` y `REPORTE_RECETA_PRESMED_ABM.txt`. Se verificó que la tabla tenía `descuento decimal(18,2) null` y se incorporó en detalle, listado, alta, modificación, consulta y `sqlpreview`. Validaciones estáticas: ASCII y CRLF, SQL sin `;` ni `RTRIM`, procedures bajo 30 caracteres, DW sin SQL embebido y `.srw` sin `ISNULL`, sin comentarios y con línea vacía antes de cada `end event`. No se compiló PB22 ni se ejecutaron scripts SQL.
- Estado: Resuelto

### Tema 7: Separación de receta PRESMED de importación

- Solicitud o contexto: Daniel preguntó si podía usar `RECETA_PRESMED_ABM.html` con objetos de `/mnt/Agentes/d.acosta/PRS_3850_DB/Evolucionar/ModeloImport` para la tabla `conve_produ_mandatarias`. Luego confirmó que correspondía crear otro modelo para ventanas específicas de importación y aclaró que no debía pisarse la tabla, solo objetos de base de datos y PowerBuilder.
- Respuesta o acciones del agente: Se recomendó no usar la receta ABM clásica porque los objetos modelo eran una familia de importación, con ventana importadora, DW detalle, DW cabecera, procedure importador, validación de archivo, armado de listas, resultado por fila y soporte de Excel/CSV. Se propuso crear `RECETA_PRESMED_IMPORTACION_V1` con modelo base `wp_import_conve_farma_produ.srw`, `dw_import_conve_farma_produ.srd`, `dw_import_conve_farma_produ_cab.srd` y `pri_conve_farma_produ_importar.SQL`, excluyendo explícitamente el `.sql` de tabla.
- Resultado: Se creó la propuesta durable `receta-presmed-importacion-v1-20260730-2d5f7b61af`, pendiente de aplicar, con soportes `templates/RECETA_PRESMED_IMPORTACION_V1.html`, `templates/RECETA_PRESMED_IMPORTACION_V1.json` y `references/RECETA_PRESMED_IMPORTACION_V1_CODE.txt`. La regla central quedó como clonar y mapear, no reconstruir, usando metadata DB como fuente de verdad sin generar ni pisar DDL de tabla.
- Estado: Pendiente

### Tema 8: Procesamiento manual de receta de importación para `conve_produ_mandatarias`

- Solicitud o contexto: Daniel pidió procesar manualmente la nueva receta hasta que estuviera disponible. La carpeta destino fue `/mnt/Agentes/d.acosta/PRS_3850_DB/NuevosABM/conve_produ_mandatarias`.
- Respuesta o acciones del agente: Se generaron objetos PowerBuilder y SQL de importación clonando/mapeando desde el modelo de `conve_farma_produ`, sin generar ni pisar el SQL de tabla. Se usó `pri_conve_prod_manda_imp` porque `pri_conve_produ_mandatarias_importar` excedía el límite Sybase de 30 caracteres.
- Resultado: Se entregaron `wp_import_conve_produ_mandatarias.srw`, `dw_import_conve_produ_mandatarias.srd`, `dw_import_conve_produ_mandatarias_cab.srd`, `pri_conve_prod_manda_imp.SQL`, `str_conve_produ_mandatarias.srs` y `REPORTE_RECETA_PRESMED_IMPORTACION.txt`. Validaciones estáticas: PB UTF-16LE con CRLF, SQL ASCII con CRLF, SQL sin `;` ni `RTRIM`, sin referencias residuales a `conve_farma_produ`, `farma`, `dto` ni procedure modelo. `dto` quedó mapeado a `descuento`; `precio` se conservó como columna de archivo/modelo, pero no se grabó porque no existe en `conve_produ_mandatarias`. No se compiló PB22, no se importaron objetos y no se ejecutó SQL.
- Estado: Resuelto

### Tema 9: Renombre de tabla destino a `conve_produ_manda`

- Solicitud o contexto: Daniel evaluó reducir el nombre de `conve_produ_mandatarias` a `conve_produ_manda` para evitar nombres SQL forzados y arrancar limpio desde una tabla parecida existente.
- Respuesta o acciones del agente: Se recomendó el cambio porque respeta mejor el límite Sybase de 30 caracteres y permite nombres naturales como `pri_conve_produ_manda`, `pru_conve_produ_manda`, `prd_conve_produ_manda`, `prs_conve_produ_manda` y `pri_conve_produ_manda_imp`.
- Resultado: Quedó acordado descartar la generación anterior de `conve_produ_mandatarias` como prueba técnica y arrancar limpio con `conve_produ_manda` como tabla base.
- Estado: Resuelto

### Tema 10: Impacto de dependencias de `wp_conve_produ_mandatarias`

- Solicitud o contexto: Daniel pidió revisar dependencias de `wp_conve_produ_mandatarias` antes de cambiar la tabla a `conve_produ_manda`, con foco en procedures y DataWindows llamadas desde la ventana.
- Respuesta o acciones del agente: Se revisaron dependencias en la carpeta de `conve_produ_mandatarias`. La ventana dependía de `d_conve_produ_mandatarias_2`, `d_conve_produ_mandatarias_0`, `d_conve_produ_mandatarias_1`, `pri_conve_produ_mandatarias` y `pru_conve_produ_mandatarias`. Las DW `d_conve_produ_mandatarias_0.srd` y `d_conve_produ_mandatarias_1.srd` llamaban a `prs_conve_produ_mandatarias` con conceptos `registro` y `listado`; `d_conve_produ_mandatarias_2.srd` no llamaba procedure pero mantenía filtro/campo `farma`. También se detectó una llamada heredada incorrecta en el botón importar: `OpenSheetWithParm ( wp_import_produ_farma, ll_farma, gw_ventana, 1, Original! )`.
- Resultado: Se concluyó que el cambio a `conve_produ_manda` impacta SQL, DataWindows y ventana, por lo que conviene regenerar limpio la familia completa y evitar arrastrar `conve_produ_mandatarias`, `farma` y nombres SQL forzados. Daniel confirmó el criterio.
- Estado: Resuelto
