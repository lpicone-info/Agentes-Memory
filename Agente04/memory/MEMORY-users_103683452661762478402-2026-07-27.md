# Memoria curada diaria

- Fecha: 2026-07-27
- Usuario: Julián Castilla
- id_googlechat: users/103683452661762478402
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/snq5hyaaaae

## Resumen de la actividad

### Tema 1: Arranque de importación masiva de recetas de farmacia

- Solicitud o contexto: Julián pidió, usando `SwissDEVP10_prestaci` y la rama `TAREA_29420_MODULO_FARMACIA`, identificar cómo arranca la importación de recetas desde `Farma./Importaciones/Importacion Masiva de Resetas`.
- Respuesta o acciones del agente: Se revisó el flujo en el repositorio y la base indicada. Se identificó que el menú de Farmacia abre `wp_selexp_ing_farm`; desde el evento `ue_importar`, según `glob.gs_reglas[16]`, se abre una de estas ventanas: `wp_suberecetas`, `wp_farma_import_recetas2` o `wp_farma_import_recetas`. Para el modelo de mandatarias, `wp_farma_import_recetas` abre solo con `glob.gs_reglas[16] = 'C'`.
- Resultado: Se explicó el flujo principal: `wp_selexp_ing_farm.ue_importar` abre `wp_farma_import_recetas`, el `open` valida tipo de importación, expediente y datos previos, carga `d_homolog_formatos_pre`, y el botón `Leer Archivo` ejecuta `ue_aceptar` para leer archivos `DBF`, `TXT`, `DAT` o `CSV`.
- Estado: Resuelto

### Tema 2: Tablas de cobertura de farmacia por afiliado

- Solicitud o contexto: Julián consultó, usando `SwissDEVP10_prestaci` y `SwissDEVP10_afilmed`, dónde se registran afiliado y cobertura porcentual de farmacia, y dónde afiliado con código de cobertura numérico de tres dígitos.
- Respuesta o acciones del agente: Se revisaron tablas de ambas bases y se compararon estructuras por campos de afiliado, cobertura y vigencia.
- Resultado: Se informó que la cobertura porcentual individual está en `afilmed..AFI_CREDENCIALES`, con campo `dto_farma`, y que `prestaci..afi_dto_farma` / `afilmed..afi_dto_farma` guardan porcentaje por contrato/plan/vigencia en `medi_ambu_dto`, sin `inte`. El código de cobertura de tres dígitos está en `prestaci..ol_padron`, campo `dtofarmacia char(3)`, con campos relacionados como `dtonominal`, y en la tabla operativa/procesada `prestaci..creden_okey`.
- Estado: Resuelto

### Tema 3: Carga de `ol_padron_aplicado` en QA

- Solicitud o contexto: Julián pidió revisar, usando `SwissQAP10_prestaci`, qué stored procedure carga la tabla `ol_padron_aplicado`.
- Respuesta o acciones del agente: Se consultaron definiciones y referencias de stored procedures relacionados con padrón.
- Resultado: Se identificó como principal `ol_padron_novedades_jr`, que genera novedades, inserta primero en `ol_padron` y luego actualiza o inserta en `ol_padron_aplicado`. También se mencionaron variantes con lógica similar: `ol_padron_novedades_NOREM` y `ol_padron_novedades_ORIG`. Como proceso manual/específico por lote se identificó `dba_actualiza_ol_padron_apl`. Se descartó `ol_padron_italia` como cargador de esa tabla porque lee `ol_padron_aplicado` pero carga `ol_padron_aplicado_ITA`.
- Estado: Resuelto

### Tema 4: Carga de `ol_padron` y uso de `replica_db2`

- Solicitud o contexto: Julián pidió revisar qué stored procedure carga `ol_padron` en `SwissQAP10_prestaci` y luego preguntó cuál de los SP revisados no trabaja con `replica_db2`.
- Respuesta o acciones del agente: Se revisaron los SP del proceso de novedades y objetos auxiliares.
- Resultado: Se confirmó que `ol_padron_novedades_jr` es el SP principal que carga `ol_padron`, con temporales en `replica_db2..ol_padron_tmp` y posterior `insert into ol_padron`. También insertan en `ol_padron` las variantes `ol_padron_novedades_NOREM` y `ol_padron_novedades_ORIG`. `olu_situacion_terapeutica` solo actualiza una columna de `ol_padron`. De los SP revisados, los que no trabajan con `replica_db2` son `dba_actualiza_ol_padron_apl` y `olu_situacion_terapeutica`.
- Estado: Resuelto

### Tema 5: Registro de memoria previo a compactación

- Solicitud o contexto: La sesión incluyó un pedido automático de pre-compaction memory flush para guardar memorias durables en `memory/2026-07-27.md`.
- Respuesta o acciones del agente: La solicitud quedó registrada como contexto operativo de la sesión, separada de la memoria diaria curada por usuario.
- Resultado: No se registró contenido funcional nuevo de negocio asociado a ese pedido automático dentro de esta memoria diaria, salvo la existencia del evento.
- Estado: Informativo
