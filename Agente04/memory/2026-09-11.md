# Memoria diaria 2026-09-11

- Contexto técnico recurrente: repositorio PowerBuilder `smg-code-presmed`, working copy `/home/luispicone/.openclaw/workspace/git/EXT_MaPaduan-smg-code-presmed-medico-presmed-PRS-3850-Reingenieria-LiqFarmacias_20260603`, rama `PRS-3850-Reingenieria-LiqFarmacias_20260603`, base `SwissDeVP10_prestaci`.
- Se generó una Especificación Funcional R16 sobre notas/expedientes de farmacia y quedó en `/mnt/Agentes/fadan/PRESMED/R16_Especificacion_Funcional_Notas_Expedientes_Farmacia.docx` y `.md` (equivalente solicitado `A:\fadan\PRESMED`).
- Hallazgo funcional sobre `dbo.expe_farmacias.ok`: columna `char(1) NOT NULL`, sin check constraint/catálogo. Valores observados: `A`, `C`, `L`. Circuito normal: `A -> C -> L`; retornos por reapertura/reversión: `L -> C`, `C -> A`.
- Significados y grabado: `A` lote abierto/en carga, asignado por `wp_farma_lotes` y procesos de importación (`pri_farma_batch`, `pri_farma_batch_scienza*`, `pri_farma_expe_admifarm`, `pri_farma_expe_import`); `C` cierre de carga/en auditoría, principalmente por `pru_lote_cierre`; `L` auditoría finalizada/listo para liquidación, principalmente por `pru_lote_cierre_aud`.
- Reversiones: `pru_lote_reabre` asigna `C` si el lote auditado tenía fecha de auditoría y `A` si no la tenía; `pru_farma_borra_auditoria`, `prd_borra_audi_k` y `pru_expe_farma_arevisar` vuelven a `C`. Excepciones: `pri_farma_expe_conc` puede crear `L` directo para lote técnico 0%; `pru_cierro_lote` legado puede asignar `L` directo salteando validaciones.
- Ventanas/procedures relevantes: `wp_farma_lote_cie` invoca `pru_lote_cierre`; `wr_farma_lote_cie_aud` y `wr_farma_fast_audit_cie` invocan `pru_lote_cierre_aud`; `wr_farma_lote_abre` invoca `pru_lote_reabre`; `wr_farma_borra_aud` invoca reversión. `wp_carga_receta` permite operar solo con `A` y bloquea `C`/`L`.
- Diferenciar `expe_farmacias.ok` (estado del lote) de `expe_farma_recetas.ok` (estado de receta). La base no restringe físicamente otros caracteres; el control depende del código/procedures.
- Posible diferencia de versionado: PB referencia `pri/pru/prd_expe_farma_lote`, mientras la working copy SQL contiene nombres base `pri_expe_farmacias`, `pri_expe_farmacias2`, `prd_expe_farmacias`; validar contra objetos instalados antes de modificar.

- Para Redmine 29747 se relevó en la rama `PRS-3850-Reingenieria-LiqFarmacias_20260603` el circuito de importación a expedientes: formularios `wp_import_exp.srw` (tradicional) y `wp_import_exp_new.srw` (inteligente), ambos con `d_import_exp.srd`. Validaciones principales: acción obligatoria, prestador (`prs_prestad_imp_inv_prestac`), cierre de período (`Val.Of_cierre_perio_liq`), archivo TXT/DBF, tipo ambulatoria/internación, datos existentes, lugar, fecha de realización, afiliado por credencial/DNI/contrato, autorización y sucursal, valor/cantidad/bono, prestación y homologación, ICD, fecha de orden y demandante, efector/categoría de círculos, grupos de internación y duplicados/prevalidación SQL. Estados de fila: `E` error no sube, `A` aviso, vacío sin observaciones. Auditoría de importación mediante `pri_expe_auditoria_item` (`Imp Inicio`/`Imp Fin`).
- Para `expe_farma_auditoria` en la misma rama, la carga la realizan procedures SQL con `INSERT INTO expe_farma_auditoria`, no directamente los formularios. Principales SP: `pri_expe_farma_recetas` (AR alta), `pru_expe_farma_recetas` (RR/MR revisión/modificación), `pru_farma_fast_audit` (RE auditoría rápida), `pru_expe_farma_arevisar` (VR volver a revisión), `pru_lote_cierre` (CL), `pru_lote_cierre_aud` (CA), `pru_lote_reabre` (RL), `pru_xexpe_farmacias` (ML), `pru_farma_rece_afi_moroso` (RB), `prd_expe_farma_recetas` (BL/BR), `pru_farma_borra_auditoria` y `prd_borra_audi_k` (BA). Formularios disparadores: `wp_carga_receta`, `wp_audit_receta`, `wp_farma_fast_audit`, `wp_farma_lote_cie`, `wr_farma_fast_audit_cie`, `wr_farma_lote_cie_aud`, `wr_farma_lote_abre`, `wp_farma_cambia_rece`, `wr_aprueba_morosos`, `wp_farma_import_recetas2`, `wr_farma_borra_aud`.
- Formularios consumidores de `expe_farma_auditoria`: circuito principal `wp_ver_receta -> wr_farma_rece_audit -> d_farma_rece_audit -> prs_farma_rece_audit -> expe_farma_auditoria`; reporte general `wp_rep_farma_auditoria -> d_rep_farma_auditoria -> prs_farma_auditoria`; `wp_audit_receta` usa `prs_farma_rece_last_mod` para controlar última modificación (MAX(fecha_hora)); `wp_selexp_aud_farm` encadena auditoría rápida/detallada; `wp_farma_fast_audit` abre `wp_audit_receta`. Otros lectores SQL: `prs_expe_farma_all` y `prs_farma_rompe_expe_all`. No se halló invocación PB directa exacta para `prd_borra_audi_k` ni `pru_xexpe_farmacias` en la rama.

- En el repositorio local indicado por Marcelo (`mnt mepaduan r30278`), se modificó `/mnt/Agentes/mepaduan/r30278/prs_d_mandataria.sql` para contemplar `manda_modo_calculo`: se agregó el código/denominación del modo de cálculo y `LEFT JOIN` con `manda_modo_calculo` y `modo_calculo`. Se conservaron ASCII, CRLF y ausencia de BOM; no se ejecutó el procedimiento contra la base. Riesgo señalado: múltiples asociaciones de modo pueden duplicar filas.
- Se revisó `dbo.prs_d_listamanda`: actualmente no devuelve `cod_modo_calculo`; el campo `l.deno` corresponde a localidades. Para mostrar el modo en el listado también habría que modificar el SP y la DataWindow `d_busmanda_list.srd`, definiendo antes si se muestra un único modo o varios.
- Marcelo compartió el evento PowerBuilder `ue_grabar` del ABM de mandatarias. El alta llama a `pri_mandatarias`, la modificación a `pru_mandatarias`, la baja a `prd_mandatarias`; luego se actualizan grupos, teléfonos y se hace `commit`.
- Para grabar el modo de cálculo en alta/modificación, la propuesta fue: agregar `long ll_cod_modo_calculo`, leer `dwa_datos.Object.cod_modo_calculo[1]`, validar que sea mayor que cero, y pasar `@cod_modo_calculo = :ll_cod_modo_calculo` a `pri_mandatarias` y `pru_mandatarias`. También deben modificarse ambos SP para insertar o actualizar la relación en `manda_modo_calculo`, dejando un único modo activo y conservando histórico. `prs_d_mandataria.sql` y la DataWindow `d_mandataria` deben devolver/incorporar `cod_modo_calculo` para que el evento pueda leerlo. Quedó pendiente verificar la estructura exacta de la tabla y los fuentes reales antes de aplicar esos cambios al código PowerBuilder/SQL.
