# Memoria durable - 2026-09-15

## Tarea #29489 - whiguera

- Wilmer/whiguera definio regla activa: Lisa debe trabajar en rol consultivo y de guia tecnica, sin modificar repositorios ni bases de datos, sin commits. Puede crear/modificar archivos solo dentro de `A:\whiguera\Tarea #23986\Tarea #29489` cuando Wilmer lo solicite. No asumir pasos cerrados sin visto bueno de Wilmer.
- Repositorio asociado: `whiguera-Version84-Modelo2Gis`, rama `branches/Version84-Modelo2Gis`. Working copy validada en revision SVN `37812` el 2026-09-15.
- Paso 8 de la Tarea #29489 quedo cerrado tecnicamente: prueba final satisfactoria con retorno `1`, mensaje vacio, XML correcto, reutilizacion de PDF existente, sin generacion ni subida nueva, y sin diagnosticos temporales/MessageBox productivos.
- El objeto `uo_nv_soli_auto_adjuntos_pdf.sru` esta registrado en `autoriz5.pbg` y la logica de generacion/reutilizacion del PDF fue validada. Reglas confirmadas: reutiliza `archivo_path_full` si existe; genera/sube solo cuando esta vacio y `is_usa_api = 'S'`; construye XML con adjuntos.
- Objetivo principal pendiente de #29489: integrar el llamado del objeto al cambiar la solicitud a estado finalizado. Se confirmo que aun falta llamado productivo a `of_preparar_adjuntos_finalizada`.
- Validacion tecnica: `wp_soli_reg.srw` evento `ue_fin_gestion` actualmente ejecuta `pru_fin_gestion_solicitudes_auto`, hace `COMMIT` y refresca; no llama al NVO. `pru_fin_gestion_solicitudes_auto.sql` todavia llama directamente a notificaciones procesos `5` y `8`. `pri_solicitudes_auto_notif.sql` no recibe XML de adjuntos.
- Propuesta coordinada acordada: separar finalizacion y notificacion. `pru_fin_gestion_solicitudes_auto` debe conservar la finalizacion pero retirar llamadas directas a procesos `5` y `8`, y devolver el estado final calculado mediante parametro de salida. PowerBuilder debe ejecutar el NVO solo si el estado final es `F`, antes del `COMMIT`; si el NVO falla, hacer `ROLLBACK` y no notificar. Luego del `COMMIT`, ejecutar un procedimiento coordinador de notificacion pasando `il_soli` y XML de adjuntos.
- `pri_solicitudes_auto_notif` debe extenderse con parametro opcional `@prm_adjuntos XML = NULL`, parsear los adjuntos y adjuntarlos al correo antes de `afu_mail_envio_send`, usando el mecanismo existente compatible con ruta remota/API (`afi_mail_attach_binary`).
- Debe crearse procedimiento coordinador sugerido `pru_solicitudes_auto_notif_finalizada` para verificar estado `F`, enviar proceso `5`, evaluar condiciones actuales del proceso `8`, y evitar duplicados mediante auditoria.
- Antes de implementar, confirmar XML real producido por el NVO. La estructura objetivo propuesta es `<adjuntos><adjunto>...`, compatible con SQL `@prm_adjuntos.nodes('/adjuntos/adjunto')`.
- Casos de prueba obligatorios definidos: A) autorizacion con PDF existente; B) autorizacion sin PDF; C) carta de negacion finalizada; D) solicitud mixta; E) carta de negacion pendiente (NVO `-1`, sin COMMIT ni correos); F) solicitud queda en estado `V` (no NVO, no procesos 5/8).

## Cierre de jornada - avance del 2026-09-15

- Se valido el XML real generado por `uo_nv_soli_auto_adjuntos_pdf.sru`.
- XML de un adjunto validado con solicitud `25919741`, autorizacion `22596338`:
  - raiz: `d_soli_auto_adjuntos_mail_xml`
  - fila repetitiva: `d_soli_auto_adjuntos_mail_xml_row`
  - columnas: `soli_auto`, `sucur`, `auto`, `prestad`, `tipo_adjunto`, `file_name`, `archivo_path_full`
  - no usar `/adjuntos/adjunto`; el XPath correcto es `/d_soli_auto_adjuntos_mail_xml/d_soli_auto_adjuntos_mail_xml_row`.
- XML multiple validado con solicitud `25919878`:
  - autorizacion `22596369`, archivo `22596369.pdf`, tipo `A`
  - carta de negacion `22596368`, archivo `CartaNegacion_10019901_22596368.pdf`, tipo `N`
  - ambos registros aparecen como nodos hermanos `d_soli_auto_adjuntos_mail_xml_row`
  - retorno NVO `1`, mensaje vacio.
- Pruebas funcionales de `pri_solicitudes_auto_notif`:
  - proceso `5`: correo al prestador sin adjuntos.
  - proceso `8`: correo al afiliado con `22596338.pdf`; se verificaron `MAIL_ENVIO`, `MAIL_TO`, `MAIL_ATTACH_BINARY` y auditoria.
  - proceso `13`: correo al afiliado con dos adjuntos; se verificaron `22596369.pdf` y `CartaNegacion_10019901_22596368.pdf`, destinatario, auditoria y `proceso_id: 13`.
  - Para probar `8`/`13` se uso un email de QA porque algunas solicitudes no tenian email de afiliado. No asumir que ese email temporal quedo restaurado; verificarlo antes de documentar cierre funcional.
- Se confirmo el alcance funcional de adjuntos:
  - procesos `8`, `9`, `13`, `23`, `27` y `28` llevan PDFs.
  - proceso `5` no lleva PDFs.
  - proceso `5` notifica al prestador cuando el origen funcional es prestador.
  - procesos `8`, `9` o `13` notifican al afiliado segun autorizaciones autorizadas, negadas o mixtas.
  - proceso `23` corresponde a solicitud finalizada ingresada por prestador GIS.
  - procesos `27` y `28` son notificaciones parciales al prestador en estado `V`.
- `pru_fin_gestion_solicitudes_auto.sql` fue actualizado y validado en revision SVN `37816`:
  - firma con `@prm_soli_esta CHAR(1) = NULL OUTPUT`.
  - inicializa `@prm_soli_esta = NULL`.
  - devuelve el estado calculado por `prs_solicitudes_auto_esta_actual`.
  - conserva proceso `27` para estado `V`.
  - retiro de llamadas directas a procesos `5` y `8`.
- `pri_solicitudes_auto_notif.sql` fue actualizado y validado:
  - revision SVN `37820`: soporte de `@prm_adjuntos XML = NULL`, parseo del XML real y registro con `afi_mail_attach_binary`.
  - revision SVN `37821`: descripcion de auditoria `Notificacion solicitud parcial` para procesos `27` y `28`.
  - adjuntos habilitados para procesos `8`, `9`, `13`, `23`, `27`, `28`.
  - procesos `27` y `28` clasificados como destinatario prestador.
  - proceso `5` fuera de la lista de adjuntos.
  - formato validado: UTF-8 sin BOM y CRLF en la copia del repositorio.
- Se genero, a pedido de Wilmer, el entregable:
  - `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/pru_solicitudes_auto_notif_finalizada.sql`
  - archivo nuevo de 279 lineas, ISO-8859-1, sin BOM, CRLF.
  - no contiene punto y coma ni `RTRIM`.
  - no se modifico el repositorio ni la base de datos al generarlo.
- Contenido del procedimiento coordinador generado:
  - valida que la solicitud exista, este vigente y tenga estado `F`.
  - valida que los nodos XML pertenezcan a la misma solicitud.
  - cuenta autorizaciones vigentes en `SOLICITUDES_AUTO_AUTORIZACIONES` y `AUTORIZACIONES`.
  - clasifica todas autorizadas como proceso `8`, todas negadas como `9`, mixtas como `13`.
  - prioriza proceso `23` cuando `origen = 'G'` y `tipo_usuario = 'P'`.
  - envia proceso `5` sin `@prm_adjuntos` cuando `tipo_usuario = 'P'` y no existe auditoria previa.
  - envia una sola notificacion afiliado con `@prm_adjuntos`, evitando duplicados por auditoria.
  - no ejecuta `COMMIT`, `ROLLBACK` ni modifica `SOLICITUDES_AUTO`.
  - excluye `27` y `28` porque son parciales de estado `V`.
- Observacion para revisar al retomar:
  - el coordinador fue generado como propuesta de implementacion, no fue compilado en QA ni incorporado al repositorio.
  - falta confirmar con evidencia funcional si `origen = 'G'` y `tipo_usuario = 'P'` es exactamente el criterio para proceso `23`, y si proceso `23` reemplaza o acompana a `8`, `9` o `13`.
  - falta definir el manejo de ausencia de email del afiliado: el SP actual no crea `MAIL_ENVIO` si no hay destinatario valido.
  - falta integrar el coordinador en `wp_soli_reg.srw`, evento `ue_fin_gestion`, despues de ejecutar el NVO y despues del `COMMIT`.
  - falta validar los procesos `9`, `23`, `27` y `28` con casos reales.
- Punto exacto para continuar manana: `Paso 4: crear/validar el procedimiento coordinador de notificaciones finalizadas`, comenzando por revisar el archivo generado, confirmar la regla del proceso `23`, y luego ejecutar sus pruebas en QA. No pasar a la integracion PowerBuilder hasta que el coordinador compile y sus pruebas sean satisfactorias.
