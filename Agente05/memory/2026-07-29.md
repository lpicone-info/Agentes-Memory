# Memoria 2026-07-29

## Tarea #29489 - Notificaciones estado U / Anulada

- Wilmer indicó una regla de trabajo para esta sesión: no realizar modificaciones directas en el entorno ni commits; Lisa solo debe proponer y mostrar cambios por pantalla o archivo. La ejecución de modificaciones y commits queda exclusivamente a cargo de whiguera.
- Wilmer indicó no asumir que un paso está comprendido o cerrado automáticamente sin su visto bueno explícito.

### pru_solicitudes_auto_anula_bandeja

- Se retomó la validación pendiente de selección de plantillas 14 y 15 en `pru_solicitudes_auto_anula_bandeja`.
- No modificar todavía el SP porque faltan confirmar/crear los ids reales en `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` para:
  - `PRESTADOR POR FUERA DE LA RED`
  - `SOLICITUD POR MEDICAMENTOS`
- La especificación funcional menciona:
  - Notificación afiliado, solicitud `ANULADA`, motivo `Prestadores por Fuera de la red` / Médicos no adscritos, sección 3.3.1.
  - Notificación afiliado, solicitud `ANULADA`, motivo `Solicitud por medicamentos`, sección 3.3.2.
- Conclusión técnica: la tabla de motivo de anulación es `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`; aunque el nombre diga devolución, `SOLICITUDES_AUTO.anu_moti` tiene FK a esa tabla y se usa para motivos de anulación.
- La lógica `CASE WHEN @var_soli_tipo = 'D' THEN 15 ELSE 14 END` fue marcada como débil. La selección de plantillas debe depender del motivo de anulación (`@prm_anu_moti`), no de `soli_tipo`.
- Criterio conceptual pendiente de ids:
  - `@prm_anu_moti = <ID_PRESTADOR_FUERA_RED>` -> plantilla `14`.
  - `@prm_anu_moti = <ID_SOLICITUD_MEDICAMENTOS>` -> plantilla `15`.
- Wilmer confirmó que esos registros aún no existen y que debe consultarlo con funcional. Funcional está ocupado en una entrega. Este punto queda pendiente para retomar al finalizar los demás SP de `U - Anulada`.
- Para notificación prestador se concluyó que debe mantenerse doble condición:
  - `@var_notif_prestad = 'S'`
  - `@var_tipo_usuario_soli = 'P'`
- Motivo: `tipo_usuario = 'P'` identifica el circuito prestador; `notif_prestad = 'S'` es la parametrización del motivo en `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`. Usar solo `tipo_usuario = 'P'` podría enviar emails para motivos configurados sin notificación.
- Para afiliado se propuso usar doble condición equivalente:
  - `@var_notif_afi = 'S'`
  - `@var_tipo_usuario_soli IN ('A', 'E')`

### SP restantes relevantes de U - Anulada

Listado de SP identificados para revisar:

- `pru_solicitudes_auto_anula`
- `pru_solicitudes_auto_anula_total`
- `prs_control_vencimiento_devolucion_soli`
- `pru_solicitudes_auto_soli_esta`
- `prs_solicitudes_auto_tabla_estado_anulada`

### pru_solicitudes_auto_anula_total

- Se analizó en detalle cómo llamar la notificación.
- Conclusión: la notificación debe llamarse solo después de confirmar que la solicitud quedó en estado `U`.
- El SP tiene dos caminos:
  - Si `@var_soli_tipo <> 'A'`, el propio SP actualiza `SOLICITUDES_AUTO.soli_esta = 'U'`, guarda `anu_moti` y audita. La notificación puede ir después del `EXEC dbo.pri_solicitudes_auto_auditoria`.
  - Si `@var_soli_tipo = 'A'`, el SP llama a `pru_solicitudes_auto_soli_esta`; luego debe consultar `SOLICITUDES_AUTO.soli_esta` y notificar solo si quedó `U`.
- Se propuso agregar variables:
  - `@var_tipo_usuario_soli CHAR(1)`
  - `@var_soli_esta_final CHAR(1)`
  - `@var_notif_afi CHAR(1)`
  - `@var_notif_prestad CHAR(1)`
  - `@var_notif_afiliado_id SMALLINT`
- Se propuso leer `tipo_usuario` junto con `soli_tipo` desde `SOLICITUDES_AUTO`.
- Se propuso leer `notif_afi` y `notif_prestad` desde `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` después de resolver `@prm_anu_moti`, porque cuando `@prm_tipo_anu = 'A'`, el SP puede reemplazarlo por `@var_anu_moti_default_soli`.
- Para prestador en este SP, criterio propuesto:
  - Si `@var_notif_prestad = 'S'` y `@var_tipo_usuario_soli = 'P'`, llamar `dbo.pri_solicitudes_auto_notif` con `@prm_id = 6`.
- Para afiliado en este SP, criterio propuesto:
  - Si `@var_notif_afi = 'S'` y `@var_tipo_usuario_soli IN ('A','E')`, setear `@var_notif_afiliado_id` por `@prm_anu_moti`.
  - Plantilla 14 y 15 quedan pendientes de ids funcionales reales.
- Wilmer pidió tomar apunte de que en `pru_solicitudes_auto_anula_total` queda el mismo pendiente que en el SP anterior: resolver ids reales para `PRESTADOR POR FUERA DE LA RED` y `SOLICITUD POR MEDICAMENTOS`.

### pru_solicitudes_auto_anula

- Se revisó `pru_solicitudes_auto_anula`.
- Conclusión: no se recomienda agregar llamada a `pri_solicitudes_auto_notif`.
- Motivos:
  - El SP no recibe motivo de anulación de solicitud; recibe `@prm_auto_esta_moti SMALLINT`, que corresponde al motivo de la autorización.
  - Anula autorizaciones con `@prm_esta = 'U'` y `@prm_moti = @prm_auto_esta_moti`.
  - Luego llama a `pru_solicitudes_auto_soli_esta` sin pasar `@prm_anu_moti`, por lo que si la solicitud queda `U` queda sin motivo de anulación de solicitud.
  - Caller encontrado: `ars_interpolaridad_solicitudes_auto_anu`, flujo orientado a anulación de autorización.
  - Según la EF, las autorizaciones anuladas no deben notificar al afiliado ni al prestador.
- Decisión acordada con Wilmer: `pru_solicitudes_auto_anula` no se modifica para notificaciones.

### prs_control_vencimiento_devolucion_soli

- Se revisó `prs_control_vencimiento_devolucion_soli`.
- Conclusión: para notificación ya está resuelto correctamente y no debe usar plantillas `6`, `14` ni `15`.
- Este SP corresponde al caso automático de vencimiento de devolución, no a anulación manual por motivo funcional.
- Flujo actual identificado:
  - Busca `@var_anu_moti` desde `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` donde `moti_x_venc = 'S'`.
  - Busca solicitudes devueltas vencidas (`SA.soli_esta = 'D'`, origen `G/W/A`, workflow en `D`, vencimiento por minutos).
  - Actualiza solicitud a `U`, asigna `anu_moti`, audita con actividad `Anulación automática de solicitud por vencimiento`.
  - Define plantilla: `3` para `@csr_tipo_usuari = 'P'`, si no `17`.
  - Llama `dbo.pri_solicitudes_auto_notif` con `@var_notif_id`.
- Coincide con la EF:
  - Prestador por vencimiento devolución solicitud -> plantilla `3`.
  - Afiliado por cancelación automática vencimiento devolución solicitud -> plantilla `17`.
- Cambio técnico sugerido, pendiente de confirmación de Wilmer:
  - Agregar control si no existe configuración `moti_x_venc = 'S'`:

```sql
IF @var_anu_moti IS NULL
	BEGIN
		RAISERROR (99999, 16, 1, 'Falta configurar el motivo de anulación por vencimiento de devolución de solicitud')
		RETURN
	END
```

- Este SP no depende del pendiente funcional de ids 14 y 15.

## Estado al compactar

- No se realizaron modificaciones directas de SP ni commits, respetando la instrucción de Wilmer.
- El último punto pendiente de confirmación con Wilmer es si acepta para `prs_control_vencimiento_devolucion_soli` el control técnico de configuración faltante de `moti_x_venc = 'S'`.
- Luego corresponde continuar con el siguiente SP del bloque `U - Anulada`, probablemente `pru_solicitudes_auto_soli_esta` o `prs_solicitudes_auto_tabla_estado_anulada`, según el orden que Wilmer indique.
