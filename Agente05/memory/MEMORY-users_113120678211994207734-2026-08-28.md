# Memoria de conversación — Wilmer Higuera

## Datos

- **Fecha:** 28/08/2026
- **Usuario:** Wilmer Higuera
- **ID Google Chat:** `users/113120678211994207734`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 36
- **Inicio:** 08:56:25
- **Fin:** 19:12:29

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/qzugpyaaaae`

## Archivos revisados

- `6d8c60e0-8495-4ddf-9f5c-46e88fb51e4c.jsonl`

---

## Transcripción completa

### [28/08/2026 08:56:25] Wilmer Higuera

Hola lisa, buenos días

---

### [28/08/2026 08:57:15] Agente

Hola Wilmer, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [28/08/2026 08:58:43] Wilmer Higuera

Lisa, por favor recuerda estos dos puntos:
## Cosas que no hacer
1. NO realizar modificaciones directas en el entorno ni hacer Commits. El agente solo propone y muestra los cambios por pantalla o archivo; la ejecución de modificaciones y commits es responsabilidad exclusiva de whiguera.
2. NO asumir que un paso se ha comprendido o cerrado de forma automática sin el visto bueno de whiguera.

---

### [28/08/2026 08:58:50] Agente

Wilmer, confirmado.

Esos dos puntos ya están cargados en tus preferencias y los voy a respetar:

1. No realizaré modificaciones directas en el entorno ni haré commits.
2. No avanzaré de un paso al siguiente sin tu visto bueno explícito.

---

### [28/08/2026 09:22:45] Wilmer Higuera

Lisa, retomemos la Tarea 29489 desde el cierre operativo del 2026-08-27. Estamos en el CASO 39, Paso C - Validación PowerBuilder, para agregar las URLs faltantes url_directorio_medico y url_buscador_agencias.

---

### [28/08/2026 09:25:25] Agente

Wilmer, retomé el cierre correctamente.

Estamos en:

```text
Tarea 29489
CASO 39
Paso C - Validación PowerBuilder
Repositorio activo: whiguera-Version84-Modelo2Gis
Archivo a validar: exe_presmed/Source Infomedical/wp_param_auto.srw
```

Fuente recuperada: `memory/MEMORY-users_113120678211994207734-2026-08-27.md#L1511-L1726` y `memory/2026-08-27.md#L98-L115`.

Validé el objeto real y hoy `wp_param_auto.srw` contempla solo estas columnas URL:

```powerscript
url_webcoomeva
url_oficinavirtualmp
url_consulta_solicitudes
```

Todavía no aparecen:

```powerscript
url_directorio_medico
url_buscador_agencias
```

Para el **Paso C**, el ajuste PowerBuilder propuesto es agregar ambas columnas en los mismos lugares donde ya se tratan las URLs actuales.

**1. Evento `open`**

Actualmente:

```powerscript
idw_datos.object.url_webcoomeva.edit.case = "any"
idw_datos.object.url_oficinavirtualmp.edit.case = "any"
idw_datos.object.url_consulta_solicitudes.edit.case = "any"
```

Debe quedar:

```powerscript
idw_datos.object.url_webcoomeva.edit.case = "any"
idw_datos.object.url_oficinavirtualmp.edit.case = "any"
idw_datos.object.url_consulta_solicitudes.edit.case = "any"
idw_datos.object.url_directorio_medico.edit.case = "any"
idw_datos.object.url_buscador_agencias.edit.case = "any"
```

**2. Evento `ue_aceptar`**

Actualmente:

```powerscript
ELSEIF ls_col = "url_webcoomeva" OR ls_col = "url_oficinavirtualmp" OR ls_col = "url_consulta_solicitudes" THEN
```

Debe quedar:

```powerscript
ELSEIF ls_col = "url_webcoomeva" OR ls_col = "url_oficinavirtualmp" OR ls_col = "url_consulta_solicitudes" OR ls_col = "url_directorio_medico" OR ls_col = "url_buscador_agencias" THEN
```

**3. Evento `itemchanged`**

Actualmente:

```powerscript
CASE	"url_webcoomeva"	, "url_oficinavirtualmp", "url_consulta_solicitudes"
```

Debe quedar:

```powerscript
CASE	"url_webcoomeva"	, "url_oficinavirtualmp", "url_consulta_solicitudes", "url_directorio_medico", "url_buscador_agencias"
```

Esto hace que las dos nuevas URLs pasen por la misma validación actual:

```powerscript
IF NOT lib.of_vacio(trim(data)) THEN
	ls_url_cuerpo = trim(data)

	IF Pos(ls_url_cuerpo, "http://") = 1 THEN
		ls_url_cuerpo = Mid(ls_url_cuerpo, 8)
	ELSEIF Pos(ls_url_cuerpo, "https://") = 1 THEN
		ls_url_cuerpo = Mid(ls_url_cuerpo, 9)
	END IF

	IF Pos(ls_url_cuerpo, "//") > 0 THEN
		mbox(ParentWindow(), 'La dirección WEB ingresada no es válida (no debe contener doble barra "//").', 1, 0)
		ib_error = TRUE
		RETURN 1
	END IF

	IF NOT Match(string(data), "^[-A-Za-z0-9ñÑáéíóúÁÉÍÓÚ_./:]+[.][-A-Za-z0-9ñÑáéíóúÁÉÍÓÚ_./:?#=&%+~@!$'()*;,]+$") THEN
		mbox(ParentWindow(), 'La dirección WEB ingresada no es válida.', 1, 0)
		ib_error = TRUE
		RETURN 1
	END IF
END IF
```

**4. Evento `itemerror`**

Actualmente:

```powerscript
CASE "url_webcoomeva", "url_oficinavirtualmp", "url_consulta_solicitudes"
```

Debe quedar:

```powerscript
CASE "url_webcoomeva", "url_oficinavirtualmp", "url_consulta_solicitudes", "url_directorio_medico", "url_buscador_agencias"
```

Resultado esperado del Paso C: las nuevas columnas quedan con el mismo comportamiento que las URLs existentes: aceptan mayúsculas/minúsculas sin conversión, viajan por `@mail_text` en `pru_parametros`, validan formato URL en `itemchanged` y bloquean errores nativos de PowerBuilder con mensaje controlado en `itemerror`.

No modifiqué archivos ni hice commits. Quedo esperando tu visto bueno para dar por cerrado el **Paso C - Validación PowerBuilder** y recién ahí seguimos con el **Paso D - `pru_parametros.sql`**.

---

### [28/08/2026 09:51:14] Wilmer Higuera

Si, ya esta listo, pasemos al paso D

---

### [28/08/2026 09:52:05] Agente

Wilmer, validé el **Paso D - `pru_parametros.sql`**.

Archivo revisado:

```text
backend/procedures/pru_parametros.sql
```

El procedimiento recibe las modificaciones desde PowerBuilder usando:

```sql
@col       CHAR(45),
@mail_text TEXT = NULL
```

Y hoy ya guarda las URLs existentes así:

```sql
IF @col='url_webcoomeva'
	UPDATE parametros SET url_webcoomeva = @mail_text
	
IF @col='url_oficinavirtualmp'
	UPDATE parametros SET url_oficinavirtualmp = @mail_text
	
IF @col = 'url_consulta_solicitudes'
	UPDATE parametros SET url_consulta_solicitudes = @mail_text
```

Para incorporar las dos URLs nuevas, el bloque debe quedar así:

```sql
IF @col='url_webcoomeva'
	UPDATE parametros SET url_webcoomeva = @mail_text
	
IF @col='url_oficinavirtualmp'
	UPDATE parametros SET url_oficinavirtualmp = @mail_text
	
IF @col = 'url_consulta_solicitudes'
	UPDATE parametros SET url_consulta_solicitudes = @mail_text

IF @col = 'url_directorio_medico'
	UPDATE parametros SET url_directorio_medico = @mail_text

IF @col = 'url_buscador_agencias'
	UPDATE parametros SET url_buscador_agencias = @mail_text
```

La ubicación recomendada es inmediatamente después de `url_consulta_solicitudes`, porque funcionalmente pertenece al mismo grupo de URLs parametrizadas.

**Validación técnica**

- No se deben agregar parámetros nuevos al SP.
- No se debe tocar la llamada desde PowerBuilder si ya en Paso C quedó incluida la lógica para enviar estas columnas por `@mail_text`.
- No usar punto y coma, manteniendo la regla SQL vigente.
- Las columnas deben existir previamente en `PARAMETROS`; esto depende del Paso A.
- El DataWindow debe exponer esas columnas; esto depende del Paso B.

**Riesgo**

Si se agregan estos `IF` antes de que existan las columnas en tabla, el script del procedimiento puede fallar al compilar por columna inexistente.

No modifiqué archivos ni hice commits. Quedo esperando tu visto bueno para dar por cerrado el **Paso D - `pru_parametros.sql`** y pasar al **Paso E - `pri_solicitudes_auto_notif`**.

---

### [28/08/2026 10:32:13] Wilmer Higuera

Listo, pasemos al pase E

---

### [28/08/2026 10:33:17] Agente

Wilmer, validé el **Paso E - `pri_solicitudes_auto_notif.sql`**.

Archivo revisado:

```text
backend/procedures/pri_solicitudes_auto_notif.sql
```

Hoy el SP declara solo estas variables de URL:

```sql
@var_url_coomeva											VARCHAR(255)  ,
@var_oficinavirtualmp									VARCHAR(255)  ,
```

Y las carga desde `PARAMETROS` así:

```sql
SELECT 	@var_dife_dias_auto_orden = dias_auto_orden,
				@var_url_coomeva					= url_consulta_solicitudes,
				@var_oficinavirtualmp			= url_oficinavirtualmp
FROM PARAMETROS
```

Luego reemplaza en asunto y cuerpo:

```sql
SET @var_subjet = REPLACE(@var_subjet, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_subjet = REPLACE(@var_subjet, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))

SET @var_body = REPLACE(@var_body, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_body = REPLACE(@var_body, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
```

Para el Caso 39, el ajuste propuesto es agregar las dos variables nuevas y sus reemplazos.

**1. Declaración de variables**

Debe quedar así:

```sql
@var_url_coomeva											VARCHAR(255)  ,
@var_oficinavirtualmp									VARCHAR(255)  ,
@var_url_directorio_medico						VARCHAR(255)  ,
@var_url_buscador_agencias						VARCHAR(255)  ,
```

**2. Carga desde `PARAMETROS`**

Debe quedar así:

```sql
SELECT 	@var_dife_dias_auto_orden = dias_auto_orden,
				@var_url_coomeva					= url_consulta_solicitudes,
				@var_oficinavirtualmp			= url_oficinavirtualmp,
				@var_url_directorio_medico		= url_directorio_medico,
				@var_url_buscador_agencias		= url_buscador_agencias
FROM PARAMETROS
```

**3. Reemplazos en asunto**

Debe quedar así:

```sql
SET @var_subjet = REPLACE(@var_subjet, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_subjet = REPLACE(@var_subjet, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
SET @var_subjet = REPLACE(@var_subjet, '@url_directorio_medico', 					ISNULL(@var_url_directorio_medico, ''))
SET @var_subjet = REPLACE(@var_subjet, '@url_buscador_agencias', 					ISNULL(@var_url_buscador_agencias, ''))
```

**4. Reemplazos en cuerpo**

Debe quedar así:

```sql
SET @var_body = REPLACE(@var_body, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_body = REPLACE(@var_body, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
SET @var_body = REPLACE(@var_body, '@url_directorio_medico', 							ISNULL(@var_url_directorio_medico, ''))
SET @var_body = REPLACE(@var_body, '@url_buscador_agencias', 							ISNULL(@var_url_buscador_agencias, ''))
```

Resultado esperado: las plantillas podrán usar estas dos nuevas variables:

```text
@url_directorio_medico
@url_buscador_agencias
```

y el SP las reemplazará tanto en `subject` como en `body`.

Validación técnica: no se agregan parámetros al procedimiento, no se altera la lógica de destinatarios, no se modifica el armado de HTML y no se usan finalizadores `;`.

No modifiqué archivos ni hice commits. Quedo esperando tu visto bueno para cerrar el **Paso E** y pasar al **Paso F - Plantillas `SOLICITUDES_AUTO_NOTIF_PLANT.sql`**.

---

### [28/08/2026 10:37:10] Wilmer Higuera

Este punto valídalo con el SP local porfa, te lo adjunto, y me vuelves a dar el paso a paso muy detallado, si el SP esta diferente al tuyo haces un Update

---

### [28/08/2026 10:40:05] Agente

Wilmer, validé el SP adjunto contra el SP local.

El SP adjunto **sí está diferente** al local que había revisado antes. El adjunto ya trae cambios adicionales, por ejemplo variables de destinatario/prestador, fecha probable de respuesta, programa/plan y tiempos de gestión desde parámetros generales. Por eso hago **update del Paso E tomando como base el SP adjunto**, no el local.

**Paso E Actualizado**

Archivo base validado:

```text
pri_solicitudes_auto_notif---e7942000-4ef9-4f57-89af-ce0bb46d0c39.sql
```

En el adjunto, hoy solo están estas dos variables URL:

```sql
@var_url_coomeva											VARCHAR(255)  ,
@var_oficinavirtualmp									VARCHAR(255)  ,
```

Debe quedar:

```sql
@var_url_coomeva											VARCHAR(255)  ,
@var_oficinavirtualmp									VARCHAR(255)  ,
@var_url_directorio_medico						VARCHAR(255)  ,
@var_url_buscador_agencias						VARCHAR(255)  ,
```

Luego, en la carga desde `PARAMETROS`, hoy está así:

```sql
SELECT 	@var_dife_dias_auto_orden = dias_auto_orden,
				@var_url_coomeva					= url_consulta_solicitudes,
				@var_oficinavirtualmp			= url_oficinavirtualmp
FROM PARAMETROS
```

Debe quedar:

```sql
SELECT 	@var_dife_dias_auto_orden = dias_auto_orden,
				@var_url_coomeva					= url_consulta_solicitudes,
				@var_oficinavirtualmp			= url_oficinavirtualmp,
				@var_url_directorio_medico		= url_directorio_medico,
				@var_url_buscador_agencias		= url_buscador_agencias
FROM PARAMETROS
```

Después, en los reemplazos del asunto, hoy está:

```sql
SET @var_subjet = REPLACE(@var_subjet, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_subjet = REPLACE(@var_subjet, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
```

Debe quedar:

```sql
SET @var_subjet = REPLACE(@var_subjet, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_subjet = REPLACE(@var_subjet, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
SET @var_subjet = REPLACE(@var_subjet, '@url_directorio_medico', 					ISNULL(@var_url_directorio_medico, ''))
SET @var_subjet = REPLACE(@var_subjet, '@url_buscador_agencias', 					ISNULL(@var_url_buscador_agencias, ''))
```

Y en los reemplazos del cuerpo, hoy está:

```sql
SET @var_body = REPLACE(@var_body, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_body = REPLACE(@var_body, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
```

Debe quedar:

```sql
SET @var_body = REPLACE(@var_body, '@url_coomeva', 											ISNULL(@var_url_coomeva, '')) 
SET @var_body = REPLACE(@var_body, '@oficinavirtualmp', 								ISNULL(@var_oficinavirtualmp, ''))
SET @var_body = REPLACE(@var_body, '@url_directorio_medico', 							ISNULL(@var_url_directorio_medico, ''))
SET @var_body = REPLACE(@var_body, '@url_buscador_agencias', 							ISNULL(@var_url_buscador_agencias, ''))
```

Conclusión técnica: el ajuste sigue siendo el mismo conceptualmente, pero debe aplicarse sobre tu SP adjunto actualizado. No conviene aplicar el bloque sobre el SP local viejo, porque se podrían pisar cambios previos que el adjunto ya tiene.

No modifiqué archivos ni hice commits. Quedo esperando tu visto bueno para cerrar el **Paso E actualizado** y pasar al **Paso F - Plantillas**.

---

### [28/08/2026 10:50:24] Wilmer Higuera

Actualiza tu repositorio por favor y pasemos al paso F

---

### [28/08/2026 10:52:32] Agente

Wilmer, actualicé el repositorio SVN correctamente.

Quedó en:

```text
Repositorio: whiguera-Version84-Modelo2Gis
Revisión: 37696
```

La actualización ya trajo cambios en los objetos del Caso 39: `PARAMETROS.sql`, `dw_param_auto.srd`, `wp_param_auto.srw`, `pru_parametros.sql`, `pri_solicitudes_auto_notif.sql` y `SOLICITUDES_AUTO_NOTIF_PLANT.sql`.

Para el **Paso F - Plantillas**, el archivo a revisar es:

```text
backend/data_modifications/SOLICITUDES_AUTO_NOTIF_PLANT.sql
```

Validé las referencias actuales y encontré esto:

```text
@url_coomeva
@oficinavirtualmp
https://medicinaprepagada.coomeva.com.co/buscador-de-agencias/
```

**Diagnóstico del Paso F**

`@url_coomeva` está bien usado cuando el texto habla de consultar el estado de la solicitud en la página de Coomeva MP.

`@oficinavirtualmp` está bien usado cuando el texto habla de ingresar a Oficina Virtual para gestionar solicitudes.

Pero hay casos donde `@oficinavirtualmp` está siendo usado para consultar la red de prestadores/directorio médico. Ahí debe cambiarse por:

```text
@url_directorio_medico
```

También hay URLs fijas hardcodeadas para puntos de experiencia:

```text
https://medicinaprepagada.coomeva.com.co/buscador-de-agencias/
```

Ahí debe cambiarse por:

```text
@url_buscador_agencias
```

**Cambios propuestos por plantilla**

Plantilla `id = 13`

Actual:

```html
la cual podrá consultar de acuerdo a su programa <a href="@oficinavirtualmp">Aquí</a>.
```

Debe quedar:

```html
la cual podrá consultar de acuerdo a su programa <a href="@url_directorio_medico">Aquí</a>.
```

Motivo: el texto habla de red de prestadores, no de Oficina Virtual.

Plantilla `id = 14`

Actual:

```html
Lo invitamos a consultar con un médico de nuestra red de prestadores y así dar continuidad al trámite <a href="@oficinavirtualmp">Aquí</a>.
```

Debe quedar:

```html
Lo invitamos a consultar con un médico de nuestra red de prestadores y así dar continuidad al trámite <a href="@url_directorio_medico">Aquí</a>.
```

Motivo: corresponde al directorio médico/red de prestadores.

Plantilla `id = 16`

Actual:

```html
también puede acercarse directamente, consulte un punto de experiencia aquí: https://medicinaprepagada.coomeva.com.co/buscador-de-agencias/.
```

Debe quedar:

```html
también puede acercarse directamente, consulte un punto de experiencia <a href="@url_buscador_agencias">aquí</a>.
```

Motivo: reemplaza URL fija por parámetro.

Plantilla `id = 17`

Mismo cambio:

```html
también puede acercarse directamente, consulte un punto de experiencia <a href="@url_buscador_agencias">aquí</a>.
```

Plantilla `id = 18`

Mismo cambio:

```html
también puede acercarse directamente, consulte un punto de experiencia <a href="@url_buscador_agencias">aquí</a>.
```

Plantilla `id = 20`

Tiene dos ajustes.

Actual para red de prestadores:

```html
otro prestador de nuestra red el cual podrá consultar de acuerdo a su programa <a href="@oficinavirtualmp">Aquí</a>.
```

Debe quedar:

```html
otro prestador de nuestra red el cual podrá consultar de acuerdo a su programa <a href="@url_directorio_medico">Aquí</a>.
```

Actual para punto de experiencia:

```html
también puede acercarse directamente, consulte un punto de experiencia aquí: https://medicinaprepagada.coomeva.com.co/buscador-de-agencias/.
```

Debe quedar:

```html
también puede acercarse directamente, consulte un punto de experiencia <a href="@url_buscador_agencias">aquí</a>.
```

**No cambiar**

Estas referencias deben mantenerse:

```text
id 7  -> @url_coomeva
id 19 -> @url_coomeva
id 22 -> @url_coomeva
```

Estas también deben mantenerse con `@oficinavirtualmp` porque hablan de Oficina Virtual/Gestionar solicitud:

```text
id 21 -> @oficinavirtualmp
id 24 -> @oficinavirtualmp
```

Validación técnica: el SP actualizado ya reemplaza `@url_directorio_medico` y `@url_buscador_agencias` en asunto y cuerpo, así que el Paso F queda alineado con el Paso E.

No hice modificaciones manuales ni commits. Solo ejecuté el `svn update` que me pediste.

---

### [28/08/2026 11:00:26] Wilmer Higuera

En la ubicación A:\whiguera\Tarea #23986\Tarea #29489 esta el script SOLICITUDES_AUTO_NOTIF_PLANT.sql
, actualiza por favor este script con estos cambios para la plantilla

---

### [28/08/2026 11:02:53] Agente

Wilmer, listo. Actualicé el script en:

`/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/SOLICITUDES_AUTO_NOTIF_PLANT.sql`

Equivalente a:

`A:\whiguera\Tarea #23986\Tarea #29489\SOLICITUDES_AUTO_NOTIF_PLANT.sql`

Apliqué estos cambios del **Paso F**:

```text
id 13 -> @oficinavirtualmp reemplazado por @url_directorio_medico
id 14 -> @oficinavirtualmp reemplazado por @url_directorio_medico
id 16 -> URL fija buscador-de-agencias reemplazada por @url_buscador_agencias
id 17 -> URL fija buscador-de-agencias reemplazada por @url_buscador_agencias
id 18 -> URL fija buscador-de-agencias reemplazada por @url_buscador_agencias
id 20 -> @oficinavirtualmp reemplazado por @url_directorio_medico
id 20 -> URL fija buscador-de-agencias reemplazada por @url_buscador_agencias
```

Validaciones ejecutadas:

```text
Archivo existe: OK
Encoding: UTF-8
BOM: no
Finales de línea: LF
CRLF: 0
LF: 89
CR: 0
```

También validé que ya no quede la URL fija:

```text
https://medicinaprepagada.coomeva.com.co/buscador-de-agencias/
```

Y quedaron estas referencias finales:

```text
@url_coomeva -> id 7, 19, 22
@url_directorio_medico -> id 13, 14, 20
@url_buscador_agencias -> id 16, 17, 18, 20
@oficinavirtualmp -> id 21, 24
```

Nota operativa: el primer intento de edición in-place falló por el manejo de archivos temporales en la ruta montada; verifiqué el impacto y reconstruí el archivo desde la copia actualizada del repositorio aplicando solo los cambios del Paso F. No hice commit.

---

### [28/08/2026 12:40:28] Wilmer Higuera

Listo, ahora retomemos el caso 10 (Error - CASO 10), necesito saber en donde mas se usa este SP (ventana o proceso) y si al modificar el SP prs_afiliado_extras, no se afecte y siga funcionando sin problemas, que con las modificaciones que realicemos no se afecte en otras partes del aplictivo

---

### [28/08/2026 12:44:37] Agente

Wilmer, validé el impacto del **Caso 10 - `prs_afiliado_extras`**.

**Conclusión**
El SP `prs_afiliado_extras` se usa, según el repositorio actualizado, solo desde esta ventana:

[wp_soli_reg.srw](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/wp_soli_reg.srw:1542)

También validé en `InstallQA` con consulta read-only sobre dependencias SQL: no aparece ningún otro procedimiento que invoque `dbo.prs_afiliado_extras`. En base solo aparece el propio SP por búsqueda de definición.

**Dónde se usa**
En `wp_soli_reg.srw`, la función:

```powerscript
wf_afi_datos_extras()
```

declara y ejecuta:

```powerscript
DECLARE proc PROCEDURE FOR dbo.prs_afiliado_extras
```

Luego hace:

```powerscript
FETCH proc INTO :ls_email, :ls_tel;
```

y carga:

```powerscript
idw_afi.SetItem(1,'email_alter',ls_email)
idw_afi.SetItem(1,'tel_alter',ls_tel)
```

Ese `email_alter` después se usa en el registro de la solicitud:

[wp_soli_reg.srw](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/wp_soli_reg.srw:1891)

```powerscript
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

y se envía solo como:

```powerscript
@prm_email_afi_2 = :ls_email_afi_2
```

**Riesgo de afectar otras partes**
El riesgo es bajo si el cambio se hace acotado, porque:

1. No hay otros usos directos del SP en el código exportado.
2. No hay otros SPs dependientes en `InstallQA`.
3. `pru_registra_solicitudes_auto` ya tiene `@prm_email_afi_1` y ya guarda `email_afi_1`.
4. `pri_solicitudes_auto_notif` ya prioriza `email_afi_1` sobre `email_afi_2`.

El punto delicado es no romper el contrato actual de `prs_afiliado_extras`. No conviene eliminar parámetros ni cambiar el significado de `@prm_email2` y `@prm_sms2`.

**Cambio recomendado seguro**
Agregar el email principal como dato nuevo, preservando lo existente:

```sql
@prm_email1 VARCHAR(100) = NULL OUTPUT
```

Recomendación técnica: agregarlo al final de la firma del SP para no afectar llamadas posicionales existentes.

El SP debería seguir devolviendo:

```text
email2
sms2
```

y sumar `email1` sin cambiar el significado anterior.

Después, en `wp_soli_reg.srw`, agregar una variable de instancia, por ejemplo:

```powerscript
STRING is_email_afi_1
```

En `wf_afi_datos_extras()` traer:

```powerscript
email1
email2
sms2
```

Guardar `email1` en `is_email_afi_1`, y mantener `email2` en el campo visible actual:

```powerscript
idw_afi.SetItem(1,'email_alter',ls_email_2)
```

Finalmente, en `wf_registra_solicitudes_auto()`, enviar ambos:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

**Validación de regresión necesaria**
Probar una solicitud nueva desde PRESMED con el afiliado del Caso 10. Resultado esperado:

```text
SOLICITUDES_AUTO.email_afi_1 = email principal recuperado
SOLICITUDES_AUTO.email_afi_2 = email alternativo, si existe
pri_solicitudes_auto_notif envía al afiliado usando email_afi_1
```

No modifiqué archivos ni hice commits.

---

### [28/08/2026 13:15:47] Wilmer Higuera

Pre-compaction memory flush. Store durable memories only in memory/2026-08-28.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-28.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-28-HHMM.md); always use the canonical 2026-08-28.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 28th, 2026 - 1:15 PM (America/Buenos_Aires)
Reference UTC: 2026-08-28 16:15 UTC

---

### [28/08/2026 13:17:21] Agente

NO_REPLY

---

### [28/08/2026 13:17:21] Wilmer Higuera

Dame el pasos a paso con detalle del cambio en el SP prs_afiliado_extras, garantizando el pedido del funcional

---

### [28/08/2026 13:17:49] Agente

Wilmer, para garantizar el pedido funcional con el menor impacto, el cambio en `prs_afiliado_extras` debe ser **compatible con la llamada actual de PowerBuilder**.

Hoy `wp_soli_reg.srw` hace:

```powerscript
FETCH proc INTO :ls_email, :ls_tel;
```

Por eso **no conviene agregar una tercera columna al `SELECT` de salida**, porque rompería o desordenaría el `FETCH`.

**Paso A Paso SP**

1. Mantener la firma actual del SP sin cambiar parámetros:

```sql
@prm_email2 VARCHAR(100) = NULL OUTPUT,
@prm_sms2   VARCHAR(30)  = NULL OUTPUT
```

2. Cambiar la lógica interna para que `@var_email2` no busque solo el email anterior, sino que devuelva primero el **último email vigente** del afiliado.

3. La prioridad debe ser:

```text
1. Último email vigente en REGISTRO_UNICO_EMAILS
2. Si no existe, email anterior vigente, como fallback
3. Si no hay ninguno, NULL
```

4. El bloque actual:

```sql
@var_email2 = RUE2.email
```

debe pasar a:

```sql
@var_email2 = COALESCE(RUE.email, RUE2.email)
```

5. Mantener igual la salida actual:

```sql
SELECT 
       email2 = @var_email2,
       sms2   = @var_sms2
```

6. Mantener igual el modo output:

```sql
SET @prm_email2 = @var_email2
SET @prm_sms2   = @var_sms2
```

**Por qué esto garantiza el funcional**

Con este cambio, cuando PRESMED llame `prs_afiliado_extras`, el primer dato devuelto será el último email vigente del afiliado. PowerBuilder lo seguirá recibiendo en `ls_email`, lo cargará en `email_alter` y luego `wf_registra_solicitudes_auto()` lo enviará como `@prm_email_afi_2`.

Después `pri_solicitudes_auto_notif` ya usa esta prioridad:

```sql
ISNULL(NULLIF(@var_email_afi_1, ''), NULLIF(@var_email_afi_2, ''))
```

Entonces, aunque `email_afi_1` siga vacío, el mail al afiliado sale correctamente usando `email_afi_2`.

**Validación obligatoria**

Probar con el afiliado del Caso 10:

```text
prepaga = 1
contra  = 100199
inte    = 01
```

Resultado esperado:

```text
prs_afiliado_extras devuelve pruebasgisprestador@gmail.com
wp_soli_reg carga ese valor en email_alter
SOLICITUDES_AUTO.email_afi_2 queda informado
pri_solicitudes_auto_notif envía el mail al afiliado
```

Este enfoque no afecta otras ventanas ni procesos porque conserva la firma, conserva las dos columnas del `SELECT` y mantiene el contrato actual del SP.

---

### [28/08/2026 13:36:33] Wilmer Higuera

Ahora dame el paso a paso con detalle para guardar correctamente el email modificado

---

### [28/08/2026 13:37:02] Agente

Wilmer, para guardar correctamente el email modificado, el punto clave es este: en PRESMED el campo visible/editable es `email_alter`, y ese campo hoy se guarda como `SOLICITUDES_AUTO.email_afi_2`.

**Paso A Paso**

1. En `prs_afiliado_extras`, hacer que el SP devuelva el email correcto en la salida actual `email2`.

Debe quedar conceptualmente así:

```sql
@var_email2 = COALESCE(RUE.email, RUE2.email)
```

Así PowerBuilder recibe el último email vigente sin cambiar el contrato del SP.

2. En `wp_soli_reg.srw`, validar que `wf_afi_datos_extras()` siga cargando ese valor en el campo editable:

```powerscript
FETCH proc INTO :ls_email, :ls_tel;

IF NOT lib.of_vacio(ls_email) THEN
	idw_afi.SetItem(1,'email_alter',ls_email)
ELSE
	idw_afi.SetItem(1,'email_alter',ls_null)
END IF
```

3. Si el usuario modifica manualmente el email en pantalla, PowerBuilder ya valida el formato en el `itemchanged` de `email_alter`:

```powerscript
li_resp = lib.of_valida_mail(ls_mail)
```

Si no es válido, limpia el campo y no permite continuar.

4. Al registrar la solicitud, `wf_registra_solicitudes_auto()` toma el valor final desde pantalla:

```powerscript
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

5. Ese valor se envía al SP de registro:

```powerscript
@prm_email_afi_2 = :ls_email_afi_2
```

6. `pru_registra_solicitudes_auto` ya lo guarda en la tabla:

```sql
email_afi_2 = @prm_email_afi_2
```

Para una solicitud nueva, queda guardado en el `INSERT`. Para edición de solicitud, también queda guardado porque el `UPDATE` ya tiene:

```sql
email_afi_2 = @prm_email_afi_2
```

**Validación esperada**

Después de guardar la solicitud:

```sql
SELECT
    soli_auto,
    email_afi_1,
    email_afi_2
FROM SOLICITUDES_AUTO
WHERE soli_auto = <solicitud_generada>
```

Resultado esperado:

```text
email_afi_2 = email recuperado o modificado en PRESMED
```

Con esto el envío no se rompe, porque `pri_solicitudes_auto_notif` ya toma afiliado con prioridad:

```sql
email_afi_1
email_afi_2
```

Entonces, si `email_afi_1` está vacío, usa `email_afi_2`.

---

### [28/08/2026 14:11:35] Wilmer Higuera

Pero la parte de que esta guardando donde no es, ese es el otro pedido del funcional

---

### [28/08/2026 14:12:08] Agente

Tenés razón, Wilmer. Ahí hay que corregir el enfoque.

El pedido funcional no es solo “recuperar un email válido”, sino **guardarlo en el lugar correcto**. Si el email recuperado corresponde al email principal del afiliado, no debe terminar grabado como `email_afi_2`. Debe guardarse en:

```text
SOLICITUDES_AUTO.email_afi_1
```

y `email_afi_2` debe quedar reservado para el alternativo.

El flujo correcto queda así:

```text
prs_afiliado_extras
    devuelve email principal
    devuelve email alternativo

wp_soli_reg.srw
    guarda email principal en variable separada
    muestra email alternativo en email_alter, si corresponde

pru_registra_solicitudes_auto
    recibe @prm_email_afi_1
    recibe @prm_email_afi_2

SOLICITUDES_AUTO
    email_afi_1 = email principal
    email_afi_2 = email alternativo
```

Entonces el cambio no debe limitarse a hacer:

```sql
@var_email2 = COALESCE(RUE.email, RUE2.email)
```

porque eso solucionaría el envío, pero seguiría guardando el email principal en `email_afi_2`, que es justamente la parte incorrecta que marca el funcional.

La corrección completa debería ser:

```sql
@prm_email1 VARCHAR(100) = NULL OUTPUT,
@prm_email2 VARCHAR(100) = NULL OUTPUT
```

Donde:

```text
@prm_email1 = último email principal/vigente del afiliado
@prm_email2 = email alternativo/anterior si existe
```

Y en PowerBuilder:

```powerscript
STRING ls_email_afi_1, ls_email_afi_2
```

Luego, al registrar:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

Conclusión corregida: para garantizar el pedido funcional, hay que tocar **dos puntos**, no solo el SP.

```text
1. prs_afiliado_extras.sql
   Separar email principal y alternativo.

2. wp_soli_reg.srw
   Enviar email principal a @prm_email_afi_1 y alternativo a @prm_email_afi_2.
```

Así queda bien guardado y no solo “funcionando de casualidad” por fallback en la notificación.

---

### [28/08/2026 16:30:12] Wilmer Higuera

Espera lisa, al registrar la solicitud, no solo esta guardando el email editado del afiliado en el campo
email_afi_2, si no que al ingresar el numero de la solicitud y recuperar los datos, también usa el campo email_afi_2 para recuperar el email. Has un análisis completo en la ventana registro de solicitudes (wp_soli_reg), de donde mas se esta usando el campo email_afi_2, de forma que realicemos el cambio completo optimizada y de la mejor forma, me das el paso a paso detallado de lo que debemos modificar sin dañar ningún funcionamiento actual

---

### [28/08/2026 16:32:18] Agente

Wilmer, tenés razón: el cambio completo no puede limitarse al registro. En `wp_soli_reg` hay **dos momentos** donde hoy se usa `email_afi_2` como si fuera el email del afiliado:

```text
1. Alta de solicitud nueva:
   prs_afiliado_extras -> email_alter -> @prm_email_afi_2

2. Recupero de solicitud existente:
   prs_solicitudes_auto_afi -> email_alter = S.email_afi_2
```

Eso confirma el problema funcional: el email principal del afiliado está siendo tratado como “email alternativo”.

**Objetivo Correcto**

```text
email_afi_1 = email principal del afiliado
email_afi_2 = email alternativo del afiliado, si existe
email_alter = campo visible actual para mostrar/editar el email alternativo
```

Para no dañar funcionamiento actual, yo lo haría en 4 objetos, con cambio acotado.

**Paso 1 - `prs_afiliado_extras.sql`**

Hoy devuelve:

```sql
email2 = @var_email2,
sms2   = @var_sms2
```

Debe devolver también el email principal:

```sql
email1 = @var_email1,
email2 = @var_email2,
sms2   = @var_sms2
```

La lógica debe separar:

```text
@var_email1 = último email principal/vigente
@var_email2 = email alternativo/anterior si existe
```

Tomar como referencia la prioridad de `ars_afiliado`:

```text
1. Último MAIL_TO.email_to asociado al último SOLICITUDES_AUTO.mail_id_afi_mail
2. Si no hay MAIL_TO, REGISTRO_UNICO_EMAILS vigente
3. Para email alternativo, segundo MAIL_TO o segundo email vigente si existe
```

**Paso 2 - `de_ingre_auto_afi_sol_reg.srd`**

Este DataWindow hoy solo tiene:

```text
email_alter
tel_alter
```

y recupera con:

```text
procedure="1 execute prs_solicitudes_auto_afi :prm_soli"
```

Agregar una columna no visible:

```text
email_afi_1
```

Debe quedar como columna técnica para transportar el email principal dentro del DW, sin cambiar la pantalla.

Conceptualmente:

```text
email_afi_1 -> hidden / no editable
email_alter -> visible, mantiene email alternativo
```

No tocar la visual del usuario salvo que después funcional pida mostrar también el email principal.

**Paso 3 - `prs_solicitudes_auto_afi.sql`**

Hoy recupera:

```sql
email_alter = S.email_afi_2
```

Debe recuperar ambos:

```sql
email_afi_1 = S.email_afi_1,
email_alter = S.email_afi_2
```

Esto corrige el segundo problema que marcaste: cuando se ingresa una solicitud existente, la ventana ya no dependería solo de `email_afi_2`; también tendría cargado el principal real en `email_afi_1`.

**Paso 4 - `wp_soli_reg.srw`**

Agregar variable local o usar la nueva columna del DW. Recomendación más limpia: usar la columna nueva `email_afi_1` dentro de `idw_afi`.

En `wf_afi_datos_extras()` cambiar de:

```powerscript
STRING	ls_contra, ls_inte, ls_email, ls_tel, ls_null
```

a:

```powerscript
STRING	ls_contra, ls_inte, ls_email_afi_1, ls_email_afi_2, ls_tel, ls_null
```

Cambiar el `FETCH`:

```powerscript
FETCH proc INTO :ls_email_afi_1, :ls_email_afi_2, :ls_tel;
```

Luego cargar:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_1) THEN
	idw_afi.SetItem(1,'email_afi_1',ls_email_afi_1)
ELSE
	idw_afi.SetItem(1,'email_afi_1',ls_null)
END IF

IF NOT lib.of_vacio(ls_email_afi_2) THEN
	idw_afi.SetItem(1,'email_alter',ls_email_afi_2)
ELSE
	idw_afi.SetItem(1,'email_alter',ls_null)
END IF
```

En `wf_registra_solicitudes_auto()` cambiar:

```powerscript
STRING ls_tipo, ls_contra, ls_inte, ls_email_afi_2, ls_sms_2, ls_obs, ls_origen, ls_provisional
```

por:

```powerscript
STRING ls_tipo, ls_contra, ls_inte, ls_email_afi_1, ls_email_afi_2, ls_sms_2, ls_obs, ls_origen, ls_provisional
```

Agregar en el declare del SP:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

Y cargar variables así:

```powerscript
ls_email_afi_1 = idw_afi.object.email_afi_1[1]
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

**Punto importante sobre edición manual**

Si funcional espera que el campo visible `email_alter` sea el email principal editable, entonces hay que cambiar criterio de pantalla: `email_alter` debería guardar en `email_afi_1`, no en `email_afi_2`.

Pero por nombre técnico y etiqueta actual:

```text
Email Alter.
```

yo no lo mezclaría sin confirmación funcional. La solución más consistente es:

```text
email_afi_1: principal recuperado automáticamente
email_alter/email_afi_2: alternativo editable
```

**Validaciones**

1. Solicitud nueva PRESMED:
```sql
SELECT soli_auto, email_afi_1, email_afi_2
FROM SOLICITUDES_AUTO
WHERE soli_auto = <nueva_solicitud>
```

Esperado:

```text
email_afi_1 = email principal vigente
email_afi_2 = alternativo, si existe o si se editó el campo Email Alter.
```

2. Recuperar la solicitud por número en `wp_soli_reg`:
```text
email_afi_1 queda cargado internamente
email_alter muestra email_afi_2
```

3. Guardar edición:
```text
No debe borrar email_afi_1
Solo debe actualizar email_afi_2 si se modifica email_alter
```

4. Notificación:
```text
pri_solicitudes_auto_notif sigue usando email_afi_1 primero y email_afi_2 como fallback
```

Conclusión: el cambio completo y ordenado es tocar `prs_afiliado_extras`, `prs_solicitudes_auto_afi`, `de_ingre_auto_afi_sol_reg` y `wp_soli_reg`. No recomiendo tocar `pri_solicitudes_auto_notif`; ya está preparado para priorizar correctamente.

---

### [28/08/2026 18:30:02] Wilmer Higuera

Lisa, antes de realizar los cambios, valida en todo el sistema, para saber ese campo email_afi_2. de la tabla
SOLICITUDES_AUTO, donde se usa, SP, consultas, informes, reportes u otras ventana, para saber el impacto total y no romper nada

---

### [28/08/2026 18:36:17] Agente

Wilmer, validé el impacto de `SOLICITUDES_AUTO.email_afi_2` sin modificar nada.

**Resultado General**

En el repositorio y en `InstallQA`, `email_afi_2` se usa como **email alternativo del afiliado**, salvo en `wp_soli_reg`, donde el problema es que actualmente también se lo está usando como si fuera el único email recuperado/editable del afiliado.

Contra base `InstallQA`, los objetos compilados que referencian `email_afi_2` son 8 SPs:

```text
dbo.pri_solicitudes_auto_notif
dbo.prs_control_vencimiento_devolucion_soli
dbo.prs_solicitud_prestadores
dbo.prs_solicitudes_auto_afi
dbo.prs_solicitudes_auto_afiliado_bandeja
dbo.prs_valida_auto_webpos
dbo.pru_registra_solicitudes_auto
dbo.pru_wrapper_registra_solicitudes_auto
```

No aparecieron vistas, funciones ni triggers con referencia directa a `email_afi_2` en `sys.sql_modules`.

**Impacto Detectado**

1. **Registro / edición PRESMED**
   - [wp_soli_reg.srw](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/wp_soli_reg.srw:1542)
   - [wp_soli_reg.srw](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/wp_soli_reg.srw:1891)

   Problema principal:
   - `wf_afi_datos_extras()` carga el email devuelto por `prs_afiliado_extras` en `email_alter`.
   - `wf_registra_solicitudes_auto()` toma `email_alter` y lo manda como `@prm_email_afi_2`.
   - No existe columna técnica `email_afi_1` en el DataWindow de registro.
   - Al recuperar una solicitud existente, `prs_solicitudes_auto_afi` devuelve `email_alter = S.email_afi_2`.

2. **SP de recupero de datos en registro**
   - [prs_solicitudes_auto_afi.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/prs_solicitudes_auto_afi.sql:39)

   Hoy:
   ```sql
   email_alter = S.email_afi_2
   ```

   Esto está bien si `email_alter` representa email alternativo, pero falta devolver también `S.email_afi_1` para preservar el email principal al editar/guardar.

3. **SP de alta/edición**
   - [pru_registra_solicitudes_auto.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_registra_solicitudes_auto.sql:27)
   - [pru_registra_solicitudes_auto.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_registra_solicitudes_auto.sql:298)
   - [pru_registra_solicitudes_auto.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_registra_solicitudes_auto.sql:551)

   Alta:
   - Inserta `email_afi_1 = @prm_email_afi_1`
   - Inserta `email_afi_2 = @prm_email_afi_2`

   Edición:
   - Solo actualiza `email_afi_2`
   - No actualiza ni preserva explícitamente `email_afi_1`

   Punto de cuidado: si agregamos `@prm_email_afi_1` desde PowerBuilder, la edición debe preservar el email principal. No conviene tocar esto a ciegas sin que `wp_soli_reg` ya tenga el dato técnico cargado.

4. **Notificaciones**
   - [pri_solicitudes_auto_notif.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pri_solicitudes_auto_notif.sql:188)
   - [pri_solicitudes_auto_notif.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pri_solicitudes_auto_notif.sql:548)

   Este SP ya está correcto conceptualmente:
   ```sql
   usa email_afi_1 primero
   si email_afi_1 está vacío, usa email_afi_2
   ```

   Recomendación: **no modificar este SP** para el caso 10.

5. **Control de vencimiento / devolución**
   - [prs_control_vencimiento_devolucion_soli.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/prs_control_vencimiento_devolucion_soli.sql:66)

   También usa fallback correcto:
   ```sql
   si origen A/W: email_afi_1, si no existe email_afi_2
   ```

   Recomendación: **no modificar**.

6. **Bandeja / consulta / impresión**
   - [prs_solicitudes_auto_afiliado_bandeja.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/prs_solicitudes_auto_afiliado_bandeja.sql:32)
   - [prs_solicitud_prestadores.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/prs_solicitud_prestadores.sql:198)
   - [d_bandeja_afiliado.srd](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/d_bandeja_afiliado.srd:16)
   - [d_lista_solicitudes_prestador_consulta.srd](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/d_lista_solicitudes_prestador_consulta.srd:35)

   Estos objetos muestran ambos campos:
   ```text
   email_afi_1
   email_afi_2
   ```

   Recomendación: **no modificar**, porque ya respetan la separación email principal / email alternativo.

7. **WebPos / GIS**
   - [registrar_solicitud.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/controlador/registrar_solicitud.php:27)
   - [solicitudAuto.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/modelo/solicitudAuto.php:700)
   - [gene_noambu.js](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/js/gene_noambu.js:374)
   - [prs_valida_auto_webpos.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/prs_valida_auto_webpos.sql:1705)
   - [pru_wrapper_registra_solicitudes_auto.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_wrapper_registra_solicitudes_auto.sql:373)

   WebPos ya maneja:
   ```text
   email_afi  -> email principal
   email2_afi -> email alternativo
   ```

   Recomendación: **no tocar WebPos** para este caso, porque el error está en el circuito PowerBuilder `wp_soli_reg`.

**Conclusión Técnica**

El cambio completo y seguro debe limitarse al circuito PRESMED de registro/recupero:

```text
prs_afiliado_extras
prs_solicitudes_auto_afi
de_ingre_auto_afi_sol_reg.srd
wp_soli_reg.srw
```

Y revisar puntualmente si `pru_registra_solicitudes_auto` debe ajustar la edición para preservar `email_afi_1`.

No conviene modificar:
```text
pri_solicitudes_auto_notif
prs_control_vencimiento_devolucion_soli
prs_solicitudes_auto_afiliado_bandeja
prs_solicitud_prestadores
prs_valida_auto_webpos
pru_wrapper_registra_solicitudes_auto
WebPos PHP/JS
DataWindows de bandeja/consulta/impresión
```

Porque esos usos ya separan o consumen correctamente `email_afi_1` y `email_afi_2`.

**Diagnóstico Final**

El impacto real está controlado: `email_afi_2` se usa en varios lugares, pero el único punto donde rompe el pedido funcional es `wp_soli_reg`, porque no conserva `email_afi_1` y reutiliza `email_alter` como única fuente para guardar. La corrección debe agregar el email principal como dato técnico invisible en el DataWindow de registro y mantener `email_alter` exclusivamente como email alternativo.

---

### [28/08/2026 18:54:06] Wilmer Higuera

Analiza en el GIS al escribir el contrato y el inte como recupera el email del afiliado y lo comparas contra la función que se usa en wp_soli_reg, también comparas como se guarda en el GIS el email editado del prestador

---

### [28/08/2026 18:59:37] Agente

Wilmer, validé el circuito completo comparando **GIS/WebPos** contra **PowerBuilder `wp_soli_reg`** y el comportamiento del email editado del prestador.

Conclusión directa: **GIS está separando correctamente email principal y email alternativo del afiliado**. El problema está en `wp_soli_reg`, porque hoy usa `email_afi_2` como si fuera el único email operativo del afiliado en más de un momento: al cargar datos extras y al recuperar una solicitud.

**Comparación Afiliado: GIS vs `wp_soli_reg`**

En GIS/WebPos, al escribir contrato e integrante:

1. `WebPos/js/afiliado.js` ejecuta `cargar_afiliado()`.
2. Envía `prepaga`, `contra`, `inte`, `creden`, `sucur` a [afiliado_busqueda.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/controlador/afiliado_busqueda.php:12).
3. El controlador llama al modelo [afiliado.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/modelo/afiliado.php:184).
4. El modelo ejecuta `ars_afiliado`.
5. `ars_afiliado` devuelve dos campos separados:
   - `email`: email principal del afiliado.
   - `email2`: email alternativo del afiliado.
6. La pantalla muestra `email` como principal y `email2` como alternativo editable.

En PowerBuilder `wp_soli_reg`:

1. La función `wf_afi_datos_extras()` llama a `dbo.prs_afiliado_extras`.
2. Ese SP actualmente devuelve solo `email2` y `sms2`.
3. PowerBuilder asigna ese valor a `idw_afi.email_alter`.
4. Al registrar la solicitud, `wf_registra_solicitudes_auto()` toma `idw_afi.object.email_alter[1]` y lo envía como `@prm_email_afi_2`.

Eso confirma tu observación: **`wp_soli_reg` no está recuperando ni conservando técnicamente el email principal del afiliado en ese flujo**, mientras GIS sí lo hace.

**Comparación Email Prestador Editado**

En GIS/WebPos, el email del prestador editado funciona así:

1. Se recupera el prestador desde [frame_prestador.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/frames/frame_prestador.php:43).
2. Se consulta `prs_prestadores` con `@prm_origen = 'W'`.
3. `prs_prestadores` prioriza el último email guardado en `SOLICITUDES_AUTO.email_prestad` mediante `mail_id_prestad_mail`.
4. El usuario puede editar `emailPrestad`.
5. Al registrar, [registrar_solicitud.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/controlador/registrar_solicitud.php:27) envía ese valor.
6. [solicitudAuto.php](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/WebPos/modelo/solicitudAuto.php:664) lo pasa a `pru_registra_solicitudes_auto`.
7. El SP guarda snapshot en `SOLICITUDES_AUTO.email_prestad`.

Importante: **GIS no modifica el maestro `PRESTADORES.email`**. Guarda el email editado del prestador en la solicitud, igual que corresponde para trazabilidad.

PowerBuilder hace algo equivalente: carga `email_prestad`, permite editarlo y lo manda como `@prm_email_prestad`. Por eso, **el circuito del prestador no debería tocarse para resolver el problema del afiliado**.

**Cambio Recomendado para `wp_soli_reg`**

El ajuste seguro debe alinear PowerBuilder con GIS:

1. Modificar `prs_afiliado_extras` para que devuelva también el email principal del afiliado.
   - Mantener `@prm_email2` sin cambiar su significado.
   - Agregar un nuevo parámetro OUTPUT al final, por compatibilidad:
   ```sql
   @prm_email1 VARCHAR(100) = NULL OUTPUT
   ```

2. En `prs_afiliado_extras`, recuperar:
   - `@prm_email1`: email principal.
   - `@prm_email2`: email alternativo.
   - `@prm_sms2`: teléfono alternativo.

3. En el DataWindow de afiliado de `wp_soli_reg`, agregar un campo técnico oculto:
   ```text
   email_afi_1
   ```

4. En `wp_soli_reg.srw`, dentro de `wf_afi_datos_extras()`:
   - Recibir `ls_email_1`.
   - Recibir `ls_email_2`.
   - Guardar `ls_email_1` en el campo técnico `email_afi_1`.
   - Mantener `ls_email_2` en `email_alter`.

5. En `wf_registra_solicitudes_auto()`:
   - Enviar `email_afi_1` como `@prm_email_afi_1`.
   - Enviar `email_alter` como `@prm_email_afi_2`.

6. Revisar `prs_solicitudes_auto_afi`, porque al recuperar una solicitud por número también debe devolver ambos:
   ```text
   email_afi_1 -> principal
   email_afi_2 -> alternativo
   ```

7. Al cargar una solicitud existente en `wp_soli_reg`, poblar:
   - campo técnico `email_afi_1` con `SOLICITUDES_AUTO.email_afi_1`
   - campo visible `email_alter` con `SOLICITUDES_AUTO.email_afi_2`

8. No tocar GIS/WebPos para este punto.
9. No tocar lógica del email del prestador.
10. Probar alta y recupero de solicitud del Caso 10.

Resultado esperado:

```text
SOLICITUDES_AUTO.email_afi_1 = email principal del afiliado
SOLICITUDES_AUTO.email_afi_2 = email alternativo/editado del afiliado
SOLICITUDES_AUTO.email_prestad = email editado del prestador
```

Con esto se cumple el pedido funcional: el email principal del afiliado queda correctamente guardado y recuperado, y el alternativo deja de ocupar el lugar del principal. No realicé cambios en archivos ni commits.

---

### [28/08/2026 19:07:46] Wilmer Higuera

Pre-compaction memory flush. Store durable memories only in memory/2026-08-28.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-28.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-28-HHMM.md); always use the canonical 2026-08-28.md filename. If nothing to store, reply with NO_REPLY.
Current time: Friday, August 28th, 2026 - 7:07 PM (America/Buenos_Aires)
Reference UTC: 2026-08-28 22:07 UTC

---

### [28/08/2026 19:08:40] Agente

NO_REPLY

---

### [28/08/2026 19:08:41] Wilmer Higuera

Guarda lo trabajado el día de hoy, junto con el análisis y conclusión, de forma que mañana recuperes la información completa y podamos continuar con la tarea y me puedas dar el paso a paso detallado para realizar los cambios, dime como te escribo mañana para que recuperes toda la información y me des el paso a paso detallado para implementar los cambios. Hasta mañana y muchas gracias por tu colaboración.

---

### [28/08/2026 19:12:29] Agente

Wilmer, ya guardé el cierre de hoy en memoria curada específica de tu usuario:

`memory/MEMORY-users_113120678211994207734-2026-08-28.md`

Quedó registrado el análisis completo de la **Tarea 29489**, incluyendo:

- Caso 39, pasos C/D/E/F.
- Script de plantilla actualizado en la ruta de trabajo.
- Caso 10.
- Impacto de `prs_afiliado_extras`.
- Uso de `SOLICITUDES_AUTO.email_afi_2`.
- Comparación GIS/WebPos vs `wp_soli_reg`.
- Conclusión técnica.
- Cambios pendientes para mañana.
- Validación esperada.

Mañana escribime así:

```text
Lisa, retomemos la Tarea 29489 desde la memoria del 2026-08-28, Caso 10, para que me des el paso a paso detallado de implementación de los cambios en prs_afiliado_extras, wp_soli_reg, prs_solicitudes_auto_afi y la revisión de pru_registra_solicitudes_auto.
```

Con eso voy directo a recuperar la memoria de hoy y te preparo el paso a paso técnico completo, sin modificar archivos ni avanzar sin tu visto bueno.

Hasta mañana, Wilmer.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
