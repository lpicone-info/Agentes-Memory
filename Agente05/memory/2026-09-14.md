## Tarea #29489 - estado al 2026-09-14
- Usuario: Wilmer. Preferencia operativa: rol consultivo; no modificar directamente repositorios ni bases de datos ni ejecutar commits. Mostrar cambios/procedimientos para que Wilmer los ejecute y requerir confirmación explícita antes de cerrar pasos.
- Repositorio de trabajo: `whiguera-Version84-Modelo2Gis`; revisión vigente recuperada: `37801`. No se hicieron modificaciones ni commits desde el agente.
- Objeto bajo validación: `uo_nv_soli_auto_adjuntos_pdf`.
- Paso 1 completado: regeneración/compilación inicial exitosa (`Succeeded`).
- Paso 2/3 completados: ajuste visual en `of_generar_pdf_autorizada` validado para tipo `I`, opción `0`, usando solicitud `25919693`, autorización `22596276`, sucursal `7718`; resultado esperado y confirmado: `t_7 = Mod. Dic/2025`, `t_3 = MP-FT-1140`, `p_3` visible, PDF de Orden de Servicio correcto. Se retiró el MessageBox de debug antes de la prueba limpia.
- Importante para diagnóstico: las autorizaciones `22596441` y `22596442` son tipo `P` (Ambulatorio), por lo que su expresión de `t_3` devuelve correctamente `MP-FT-002`; no sirven para validar tipo `I`/`C`. La etiqueta `t_3` tenía expresión: `if(tipo = 'E' OR tipo = 'P', 'MP-FT-002', 'MP-FT-1140')`.
- La prueba `25920000` devolvió retorno `1`, XML con dos adjuntos y `archivo_path_full`; en ejecuciones posteriores se reutiliza el path existente y no se vuelve a entrar a `of_generar_pdf_autorizada`.
- Tabla que relaciona solicitudes y autorizaciones: `SOLICITUDES_AUTO_AUTORIZACIONES`. Relación documentada: `SOLICITUDES_AUTO.soli_auto = SOLICITUDES_AUTO_AUTORIZACIONES.soli_auto`; y `SAA.sucur/auto = AUTORIZACIONES.sucur/auto`.
- Profile de BD autorizado por el usuario: `InstallQA`.
- Pendiente actual: Paso 4, aplicar `is_usa_api` en `of_preparar_autorizada` y `of_preparar_negada`, después de la reutilización cuando `archivo_path_full` ya existe y antes de generar/subir un PDF nuevo. Regla: path existente se reutiliza independientemente de `is_usa_api`; path vacío + `is_usa_api = 'S'` permite generar/subir/actualizar; path vacío + valor distinto de `S` debe retornar error controlado sin generar, subir, actualizar ni agregar XML.
- Mensajes sugeridos: autorizada `No se puede preparar el adjunto de la autorización <auto> porque el parámetro USA API Adjuntos no está habilitado.`; negada equivalente con `autorización negada`.
- Validación pendiente de Paso 4: regenerar el NVO y probar un caso nuevo con `archivo_path_full` vacío y otro caso con path existente para confirmar generación/subida y reutilización respectivamente. El usuario pidió guía detallada; no ejecutar cambios por cuenta propia.


## Contexto SVN/GIS/Coomeva - 2026-09-14 18:58 ART
- Usuario en conversación reciente: Constantino. Idioma de trabajo: español técnico, directo.
- Se usó la skill `svn-checkout` para registrar la rama SVN `https://srv-infosvn.infomedical.com/svn/Coomeva/trunk/WebPos` para el usuario SVN `csiafas`.
- Working copy registrada: `~/.openclaw/workspace/svn/csiafas-Coomeva-trunk-WebPos`; revisión validada informada: `37804`.
- En la validación UI GIS de restablecimiento de contraseña se usó URL `http://10.194.0.15/coomeva/gis/prod/index.php` y BD/profile `PruebasCoreINFOMEDICAL`.
- El flujo de restablecimiento quedó bloqueado porque el usuario indicó no tener acceso al correo asociado. No se debe cambiar contraseña ni disparar recuperación sobre cuentas reales sin acceso/confirmación del correo de prueba.
- Tipo de documento GIS válido para NIT en UI: el combo no tiene `NIT`; usa `NI`. Tipos vistos: `CC`, `CE`, `NI`, `PA`, `RC`, `TI`.
- Para documento `NI 10123080`: no se encontró coincidencia activa en `PruebasCoreINFOMEDICAL`; el servicio de consulta de correo respondió sin correo asociado; no se envió solicitud ni se generó `RM3010-testIA`.
- Recuperación de email en GIS: no se hace por consulta directa a `PRESTADORES`; se consultan parámetros con `EXEC ars_parametros_generales @prm_claves = 'URLWebServiceGetEmail,URLWebServiceTokenEmail,URLWebServiceSistemaEmail,URLWebServiceNombreClaveEmail'` y se llama al servicio con JSON `{ "pty_numDoc": "<numero_documento>", "pty_tipDoc": "<tipo_documento>" }`; la app toma el email de `jsonRespuesta[0].EMAIL` y luego usa `EXEC ars_mensaje_reestablece @prm_email = '<email_recuperado>'`.

## Demonio de Cuentas Medicas - Terminos de Pago Default
- Repo/working copy usado: `~/.openclaw/workspace/svn/csiafas-Coomeva-trunk-exe_presmed-Source20Infomedical`.
- BD/profile usado para análisis: `PruebasCoreINFOMEDICAL`.
- El mensaje en `[dbo].[BATCH_CUENTAS_MEDICAS_LOG].descrip_estado = 'Términos de Pago Default inexistente / No Valido.'` se origina en el SP `dbo.afi_u_d_batch_ctas_med`, llamado desde `uo_proceso_cta_med.sru`.
- Validación principal del SP: busca `@var_grupo_pago = grupo` en `PRESTAD_PAGOS_GRUPOS` para el prestador (`prestad = @var_prestad`), vigente (`ISNULL(baja_fecha, '20500101') > GETDATE()`) y default (`es_default = 'S'`).
- El mensaje se dispara si `ISNULL(@var_grupo_pago, 0) < 1` y `@tipo_mov <> 'B'`. Casos: no existe grupo de pago para el prestador, no está marcado default, está dado de baja/no vigente, o `grupo` nulo/menor a 1.
- También puede aparecer el mismo mensaje si falla `EXEC dbo.prs_fecha_de_pago @var_grupo_pago, @fecha_recepcion, @fecha_compro, 'x', @var_fecha_venc OUT, @error OUT`.
- En PowerBuilder, si `afi_u_d_batch_ctas_med` devuelve error SQL, el demonio toma `SQLCA.sqlerrtext` como `ls_error` y lo graba en `BATCH_CUENTAS_MEDICAS_LOG.descrip_estado` mediante `batch_ctamed_log`. Referencias informadas: `uo_proceso_cta_med.sru` alrededor de líneas 244, 262, 371 y 1388.

## Tarea #29489 - cierre operativo y punto de continuidad al 2026-09-14
- Usuario: Wilmer Higuera, Google Chat `users/113120678211994207734`.
- Preferencia operativa vigente: trabajo consultivo y guía técnica; Wilmer ejecuta las modificaciones en PowerBuilder y los commits. Lisa puede actualizar la working copy cuando Wilmer lo solicita, pero no modifica directamente el código ni la base de datos y no debe considerar un paso cerrado sin confirmación explícita.
- Repositorio exclusivo: `whiguera-Version84-Modelo2Gis`.
- Working copy: `/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis`.
- Rama/remoto: `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version84-Modelo2Gis`.
- Profile de base de datos usado para las consultas funcionales: `InstallQA`.
- Objeto bajo trabajo: `exe_presmed/Source Infomedical/uo_nv_soli_auto_adjuntos_pdf.sru`.

### Sincronización solicitada por Wilmer
- Wilmer informó que volvió a comitiar el objeto y pidió actualizar la working copy.
- El `svn update` se ejecutó usando la configuración y credenciales aisladas registradas para `whiguera-Version84-Modelo2Gis`. El primer intento fue rechazado por validación del certificado; el reintento con confianza explícita del certificado completó correctamente.
- La working copy quedó en revisión SVN `37812`.
- La revisión `37812` corresponde al cambio de `uo_nv_soli_auto_adjuntos_pdf.sru`, con mensaje `Tarea #29489 - 7076: GAP # 1 Parametrización de mensajes de SOLICITUDES para envío de notificaciones EMAIL.`
- Autor informado por SVN: `whiguera@INFOMEDICAL`.
- Fecha del cambio: `2026-09-14 20:58:27 -0300`.
- Estado local posterior: sin modificaciones propias; permanecen únicamente los tres externos `X` conocidos: `exe_InfoMailer/pbls/impresion.pbl`, `exe_presmed/entidades.pbl` y `exe_presmed/ficha_afiliado.pbl`.

### Revisión del objeto después del update
- Se buscó en `uo_nv_soli_auto_adjuntos_pdf.sru` cualquier `MessageBox` y textos de diagnóstico como `Debug formato`, `Diagnóstico formato`, `Entrada método`, `Debug preparación` y `Validación PDF`.
- Resultado: no existe ninguna llamada `MessageBox` ni mensaje temporal de diagnóstico dentro del objeto.
- Se confirmó que la rama de cirugía está activa mediante `Modify("t_3.Text='MP-FT-1578'")`; la asignación directa anterior de cirugía está comentada y no se ejecuta.
- Se confirmó que permanecen activos los ajustes visuales: `t_7 = "Mod. Dic/2025"`, `p_3` para tipo `I`, `p_6` para tipo `C`, `p_5` para opción `5`, y el control de `is_usa_api` en `of_preparar_autorizada` y `of_preparar_negada`.
- Los mensajes de error devueltos mediante `as_mensaje` no son `MessageBox` de diagnóstico y forman parte del control funcional; no deben retirarse.
- Validación técnica del archivo: UTF-8 con BOM, finales CRLF, sin mezcla de finales de línea y sin diferencias locales después de la sincronización.

### Estado funcional validado durante la jornada
- Compilación inicial del NVO: `Succeeded`.
- Tipo `I`, opción `0`, solicitud `25919693`, autorización `22596276`: PDF correcto con `Mod. Dic/2025`, `MP-FT-1140`, `p_3` visible y formato de Orden de Servicio.
- Tipo `C`, opción `0`, solicitud `25919741`, autorización `22596338`: diagnóstico `ll_opcion = 0` y PDF correcto con Orden de Cirugía, `Mod. Dic/2025`, `MP-FT-1578` y `p_6` visible.
- Tipo `P`, autorizaciones `22596441` y `22596442`: la expresión original de `t_3` devuelve correctamente `MP-FT-002`; estos casos no sirven para validar tipo `I` o `C`.
- `is_usa_api` con valor distinto de `S` y `archivo_path_full` vacío: solicitud `23959952` devolvió `-1`, mensaje controlado y no generó XML/adjunto.
- `is_usa_api` con valor distinto de `S` y `archivo_path_full` informado: solicitud `24302855` devolvió `1` y reutilizó el adjunto existente.
- `is_usa_api = S` y ruta vacía: solicitud `23959952` generó/preparó correctamente una autorización y una carta de negación, con retorno `1`, XML y `archivo_path_full`.
- Solicitud mixta `25919878`: retorno `1`, XML con adjunto tipo `A` y tipo `N`; autorización y carta de negación abiertas y validadas visualmente.
- Solicitud con carta pendiente `25920092`: retorno `-1`, bloqueo completo antes de generar o subir adjuntos.
- Solicitud sin autorizaciones relacionadas `3836233` no es una `soli_auto` válida para esta prueba; el valor apareció correctamente como número de autorización dentro de la solicitud `25919673`.

### Punto exacto para continuar mañana: Paso 8
- Nombre del paso: `Paso 8: limpieza del entorno de prueba y validación final`.
- Pendiente principal: revisar la ventana/botón usado exclusivamente para probar el NVO y retirar el `MessageBox` temporal que muestra `Retorno`, `Mensaje` y `XML`, junto con la llamada temporal completa si ese botón no forma parte de la funcionalidad definitiva.
- No retirar los mensajes funcionales del NVO que se transportan en `as_mensaje`.
- Si el botón pertenece a una ventana funcional y la llamada al NVO debe conservarse, retirar solamente el `MessageBox` temporal.
- Después de la limpieza, guardar en PowerBuilder y regenerar `uo_nv_soli_auto_adjuntos_pdf`; resultado esperado: `Succeeded`.
- Realizar una prueba limpia sobre un caso ya procesado, por ejemplo solicitud `25919741` / autorización `22596338`, para comprobar que devuelve `1`, mensaje vacío y XML con el adjunto existente, sin mensajes de diagnóstico.
- La prueba de generación nueva de cirugía ya quedó validada con tipo `C`, `ll_opcion = 0`, `Mod. Dic/2025` y `MP-FT-1578`; no es necesario repetirla salvo que la limpieza altere el objeto.
- Criterio de cierre: botón sin `MessageBox` temporal, regeneración `Succeeded`, objeto sin diagnósticos temporales, reutilización correcta del adjunto y confirmación explícita de Wilmer.

### Texto recomendado para retomar
`Lisa, retomemos la Tarea #29489 desde la memoria del 2026-09-14; continuemos con el Paso 8: limpieza del entorno de prueba y validación final.`
