## 2026-08-05 - GIS / Coomeva / INSTALLQA

- Usuario: Constantino.
- Contexto técnico recurrente: rama SVN `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version84-Modelo2Gis`, aplicación GIS, base `INSTALLQA`.
- Investigación realizada: en GIS, contenedor `Información del Prestador`, campo `Correo electrónico del prestador`.
- Resultado maestro: el campo se almacena en `dbo.PRESTADORES.email`; existe segundo correo en `dbo.PRESTADORES.email_2`. DataWindow `d_prestador` mapea `email` y `email_2` a `PRESTADORES.email` y `PRESTADORES.email_2`. Ventana `wp_presta_insti` al grabar toma `idw_prestador.GetItemString(1, "email")` como `@prm_email` y `"email_2"` como `@prm_email_2`. En `INSTALLQA`, ambas columnas existen como `varchar(100) NULL`.
- Diferencia funcional importante: `Correo electrónico de Citas` no usa `PRESTADORES`; va por lugares de atención en `PRESTAD_LUGARES.email_citas`.
- Investigación realizada en `noambu_auto.php`: botón `GRABAR` ejecuta `registrar()`. Valida obligatorios, valida formato con `validaMail(emailPrestad)`, toma `#emailPrestad`, envía AJAX a `controlador/registrar_solicitud.php` como `email_prestad`, luego `Solicitud::registrar()` ejecuta `pru_registra_solicitudes_auto`.
- Resultado flujo `noambu_auto.php`: al grabar no actualiza `PRESTADORES.email`; guarda copia del mail ingresado en la solicitud en `dbo.SOLICITUDES_AUTO.email_prestad`, confirmado en `INSTALLQA` como `varchar(100) NULL`.

## 2026-08-05 12:54 ART - Auditoria rechazo auditor

- Usuario conversando: Barbara, Google Chat directo.
- Repositorio analizado: `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`.
- Tema: verificar si el rechazo realizado por un usuario auditor registra auditoria/bitacora/workflow cuando cambia estado de solicitud y responsable a ejecutivo.
- Primer analisis confundio el flujo con `pru_solicitudes_auto_devolucion`; Barbara aclaro que ese era otro proceso.
- Analisis correcto usando perfil/base `InstallQA`: existe `dbo.pru_solicitudes_auto_resuelta_auditoria`.
- Hallazgo: si la solicitud esta en `V` (Validacion Auditoria) y existe autorizacion asociada rechazada (`AUTORIZACIONES.esta = 'R'`), el SP toma el usuario origen del ultimo registro de `SOLICITUDES_AUTO_WORKFLOW` con `soli_esta <> 'V'` y llama a `dbo.pru_asignacion_soli_auto` con `@prm_new_estado = 'G'`.
- `dbo.pru_asignacion_soli_auto` actualiza `SOLICITUDES_AUTO.responsable_gestion`, cambia `SOLICITUDES_AUTO.soli_esta` a `G` (En Gestion), inserta auditoria en `SOLICITUDES_AUTO_AUDITORIA` con actividad `Asignacion de Solicitud`, cierra workflow anterior e inserta nuevo registro en `SOLICITUDES_AUTO_WORKFLOW`.
- Punto de control: el SP no valida explicitamente que el usuario origen sea ejecutivo; depende de que el workflow previo a `V` este bien armado.
- Importante: `pru_solicitudes_auto_resuelta_auditoria` fue encontrado en `InstallQA`, pero no en la working copy local `bgonzalez-Version84-Modelo2Gis`; hay desfasaje entre base y scripts versionados.
