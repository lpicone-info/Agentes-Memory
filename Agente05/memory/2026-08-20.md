# Memoria 2026-08-20

- Usuario: Bárbara. Conversación directa por Google Chat.
- Repositorio trabajado: `bgonzalez-Version84-Modelo2Gis`, working copy SVN en `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`.
- Se hizo `svn update` del repositorio solicitado; quedó inicialmente en revisión `37598`.
- Contexto funcional: caso `Caso_Reg` en `/mnt/Agentes/bgonzalez/`, módulo `exe_presmed`, base/perfil `InstallQA`.
- Análisis inicial: `wp_soli_reg` usa `de_soli_reg_oportunidad`, que llama a `dbo.prs_solicitudes_auto_oportunidad`; ese SP devolvía campos de oportunidad/tiempos en `NULL`, a diferencia de la bandeja (`prs_solicitudes_auto_tiempo_oportunidad_bandeja`) que ya calculaba días hábiles.
- Confirmación funcional de Bárbara: en Bandeja debe mostrarse `SOLICITUDES_AUTO.tiempo_gestion` como quedó por cambio reciente; no revertir ese criterio. Los cálculos deben ser por días/hours hábiles.
- Commit SVN `37599` con mensaje ` R26460 - Bandejas / Registro de Solicitudes`: se modificó `backend/procedures/prs_solicitudes_auto_oportunidad.sql` para calcular fecha ajustada, fecha oportunidad, holgura, ajuste y tiempo transcurrido de gestión con lógica hábil similar a bandeja. No se tocó el SP de bandeja en ese commit.
- Luego Bárbara preguntó por qué el tiempo transcurrido daba `0` en solicitud `25920106`; diagnóstico en `InstallQA`: alta/ajustada `18/08/2026 16:09`, cambio estado `18/08/2026 16:11`, diferencia 2 minutos. El cálculo hacía `DATEDIFF(MINUTE, ...)/60` con enteros, por eso `2/60 = 0`. Para `25920081`, el cambio de estado era anterior a la fecha ajustada y también quedaba `0`.
- Nueva definición funcional: mostrar minutos cuando el tiempo transcurrido sea menor a una hora, en Bandeja Auditoría y Registro de Solicitudes, solo para `Tiempo Transcurrido Gestión`.
- Commit SVN `37600` con mensaje ` R26460 - Bandejas / Registro de Solicitudes`: se modificaron `backend/procedures/prs_solicitudes_auto_oportunidad.sql`, `backend/procedures/prs_solicitudes_auto_tiempo_oportunidad_bandeja.sql`, `exe_presmed/Source Infomedical/de_soli_reg_oportunidad.srd` y `exe_presmed/Source Infomedical/d_bandeja_tiempo_oportunidad.srd` para mostrar el tiempo transcurrido como texto con horas y minutos, por ejemplo `0 hs 2 min`.
- Validaciones reportadas: `SET PARSEONLY ON` contra `InstallQA` para ambos SPs sin aplicar cambios, preservación de encoding/BOM/finales de línea, SPs sin `;`, estado final sin cambios propios pendientes salvo externos SVN (`X`).

- Caso nuevo analizado para Bandeja Auditoría -> botón Solicitudes en solapa Detalle: usuario `csiafas` abre solicitud `25920082` y aparecía mensaje `La solicitud 25920082 debe ser editada por el mismo usuario responsable`, cerrando `wp_soli_reg`.
- Diagnóstico: no cambiar `prs_valida_solicitudes_auto_edit`; el SP devuelve `-4` correctamente cuando `responsable_gestion` no coincide con el usuario. El problema era que `wp_soli_reg.srw` trataba ese error como bloqueante y cerraba la ventana.
- Commit SVN `37609` con mensaje `R26460 - Bandejas / Registro de Solicitudes`: se modificó `exe_presmed/Source Infomedical/wp_soli_reg.srw` para conservar el error `-4`, recuperar la solicitud y abrirla en modo consulta cuando el usuario no es responsable. Para otros errores se mantiene el comportamiento previo. Se agregó rutina `wf_modo_consulta()` para bloquear edición y acciones. No se modificó el SP de validación.
- Bárbara luego validó el botón `Limpiar` en esa instancia de consulta y decidió aplicar ella misma la recomendación de inhabilitarlo, porque `Limpiar` dispara `ue_limpiar`/`ue_restaurar` y podía dejar la ventana en estado inconsistente frente al modo consulta forzado.
- Caso de cálculo de `Tiempo Ajuste`: para solicitud `25920079`, con `tiempo_oportunidad = 48`, motivo rechazo `PRUEBA DESA` con `aju_tiempo_gestion = 10` y tiempo transcurrido de 1/2 minutos, Registro mostraba `58` pero Bandeja mostraba `10`.
- Diagnóstico: en `prs_solicitudes_auto_tiempo_oportunidad_bandeja.sql` la fórmula usada era `@var_ajuste_motivo - @var_tiempo_transcurrido_gestion`; en `prs_solicitudes_auto_oportunidad.sql` ya estaba correcta: `@var_tiempo_oportunidad + @var_ajuste_motivo - @var_tiempo_transcurrido_gestion`. Recomendación: modificar sólo SP de Bandeja para igualar fórmula; no tocar DataWindow ni Registro.
- Análisis de auditoría por rechazo de autorización con motivo que suma tiempo gestión: el estado rechazado/motivo impacta por `prs_valida_of_auto_remotas.sql`, que toma `auto_remotas.auto_esta_moti`, llama a `pru_autorizaciones.sql` y persiste en `AUTORIZACIONES.auto_esta_moti`. El punto recomendado para auditar no son los SPs de consulta/recalculo, sino `prs_valida_of_auto_remotas.sql` dentro del flujo donde la solicitud pasa de `Validacion Auditoria` a `En Gestion`.
- Commit SVN `37610` con mensaje `R26460 - Bandejas / Registro de Solicitudes`: se modificó `backend/procedures/prs_valida_of_auto_remotas.sql` para registrar auditoría en `SOLICITUDES_AUTO_AUDITORIA` cuando la solicitud pasa de `V` a `G` y existe autorización asociada con motivo cuyo `AUTO_ESTADOS_MOTIVOS.aju_tiempo_gestion > 0`. Texto agregado reportado: `Ajuste de tiempo de gestion por motivo de autorizacion: <motivo> - <horas> horas`.
- Validación del commit `37610`: `SET PARSEONLY ON` contra `InstallQA`, sin errores; estado final sin cambios propios pendientes, sólo externos SVN habituales.

- Análisis de solapa Devoluciones en Bandeja de Solicitudes/Auditoría: en `wp_bandeja_solicitudes.srw`, si `istr_rol.es_auditor = 'S'` se deshabilita `tab_1.tabpage_devolucion`; `wf_auditoria()` también deshabilita `tab_1.tabpage_devolucion`, `idw_devolver`, `cb_devolver` y `cb_refresh`. Por lo tanto, en Bandeja de Auditoría no debería operar Devoluciones.
- En Bandeja de Solicitudes, al seleccionar fila, Devoluciones se habilita sólo cuando `istr_rol.es_auditor <> 'S'`, `soli_esta = 'R'` y `origen` es `A` o `G`. Al ejecutar Devolver, además valida `wf_permite_editar()`; si el usuario no es responsable de gestión muestra `No puede devolver porque usted no es el Responsable en Gestión.`. Luego `ue_devolver()` exige motivo y llama a `pru_solicitudes_auto_devolucion`, que inserta en `SOLICITUDES_AUTO_DEVOLUCION`, audita y cambia `SOLICITUDES_AUTO.soli_esta = 'D'`.
- Análisis de Registro de Solicitudes, solapa Servicios a Solicitar, solicitud GIS ejemplo `25920085`: en `InstallQA` se confirmó `soli_esta = R`, `origen = G`, CUPS con `tiene_auto = N`. En `wp_soli_reg.srw`, `wf_retrieve_total()` habilita edición/acciones de servicios sólo para `(is_soli_esta = 'R' OR is_soli_esta = 'C') AND (ls_origen = 'A' OR ls_origen = 'P')`. Como `origen = G` no cumple, deshabilita `tab_1.tabpage_12.pb_ins_prestac`, `idw_prestac.object.b_borrar` y `idw_prestac.object.pb_deta_pres`.
- Condiciones actuales de Servicios a Solicitar: los botones se habilitan al restaurar/inicializar una solicitud nueva desde Registro de Solicitudes, y para solicitudes existentes sólo si estado `R` o `C` y origen `A` o `P`. En `d_soli_reg_serv.srd`, `b_borrar` sólo visible si `tiene_auto <> 'S'`, y columnas de prestación se protegen si `tiene_auto = 'S'`.
- Para permitir consultar conceptos en solicitudes `Origen G`, recomendación dada: no habilitar borrado ni inserción de CUPS. Separar consulta de edición: en `wp_soli_reg.srw`, permitir sólo `idw_prestac.object.pb_deta_pres.enabled = TRUE` para origen `G` en estado `R`/`C`, manteniendo `pb_ins_prestac` y `b_borrar` deshabilitados. Además pasar una marca de sólo consulta a `wp_prestac_concep_soli_reg`, porque esa ventana puede abrir editable; en modo consulta debería poner `dw_lista` y `dwa_datos` en `ReadOnly='yes'` y deshabilitar `cb_alta`, `cb_baja`, `cb_modifica`, `cb_limpiar`.
- Riesgo señalado: sólo habilitar `pb_deta_pres` podría permitir modificar conceptos, porque `wp_prestac_concep_soli_reg` devuelve el `DataStore` `ids_auto_item_deta` al cerrar. Eliminar CUPS para origen `G` quedó fuera de alcance y requiere definición funcional aparte, porque `wf_borrar()` borra también conceptos asociados y afecta solicitudes generadas por GIS.
# Memoria 2026-08-20

- Usuario: Bárbara. Conversación directa por Google Chat.
- Repositorio trabajado: `bgonzalez-Version84-Modelo2Gis`, working copy SVN en `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`.
- Se hizo `svn update` del repositorio solicitado; quedó inicialmente en revisión `37598`.
- Contexto funcional: caso `Caso_Reg` en `/mnt/Agentes/bgonzalez/`, módulo `exe_presmed`, base/perfil `InstallQA`.
- Análisis inicial: `wp_soli_reg` usa `de_soli_reg_oportunidad`, que llama a `dbo.prs_solicitudes_auto_oportunidad`; ese SP devolvía campos de oportunidad/tiempos en `NULL`, a diferencia de la bandeja (`prs_solicitudes_auto_tiempo_oportunidad_bandeja`) que ya calculaba días hábiles.
- Confirmación funcional de Bárbara: en Bandeja debe mostrarse `SOLICITUDES_AUTO.tiempo_gestion` como quedó por cambio reciente; no revertir ese criterio. Los cálculos deben ser por días/hours hábiles.
- Commit SVN `37599` con mensaje ` R26460 - Bandejas / Registro de Solicitudes`: se modificó `backend/procedures/prs_solicitudes_auto_oportunidad.sql` para calcular fecha ajustada, fecha oportunidad, holgura, ajuste y tiempo transcurrido de gestión con lógica hábil similar a bandeja. No se tocó el SP de bandeja en ese commit.
- Luego Bárbara preguntó por qué el tiempo transcurrido daba `0` en solicitud `25920106`; diagnóstico en `InstallQA`: alta/ajustada `18/08/2026 16:09`, cambio estado `18/08/2026 16:11`, diferencia 2 minutos. El cálculo hacía `DATEDIFF(MINUTE, ...)/60` con enteros, por eso `2/60 = 0`. Para `25920081`, el cambio de estado era anterior a la fecha ajustada y también quedaba `0`.
- Nueva definición funcional: mostrar minutos cuando el tiempo transcurrido sea menor a una hora, en Bandeja Auditoría y Registro de Solicitudes, solo para `Tiempo Transcurrido Gestión`.
- Commit SVN `37600` con mensaje ` R26460 - Bandejas / Registro de Solicitudes`: se modificaron `backend/procedures/prs_solicitudes_auto_oportunidad.sql`, `backend/procedures/prs_solicitudes_auto_tiempo_oportunidad_bandeja.sql`, `exe_presmed/Source Infomedical/de_soli_reg_oportunidad.srd` y `exe_presmed/Source Infomedical/d_bandeja_tiempo_oportunidad.srd` para mostrar el tiempo transcurrido como texto con horas y minutos, por ejemplo `0 hs 2 min`.
- Validaciones reportadas: `SET PARSEONLY ON` contra `InstallQA` para ambos SPs sin aplicar cambios, preservación de encoding/BOM/finales de línea, SPs sin `;`, estado final sin cambios propios pendientes salvo externos SVN (`X`).

- Caso nuevo analizado para Bandeja Auditoría -> botón Solicitudes en solapa Detalle: usuario `csiafas` abre solicitud `25920082` y aparecía mensaje `La solicitud 25920082 debe ser editada por el mismo usuario responsable`, cerrando `wp_soli_reg`.
- Diagnóstico: no cambiar `prs_valida_solicitudes_auto_edit`; el SP devuelve `-4` correctamente cuando `responsable_gestion` no coincide con el usuario. El problema era que `wp_soli_reg.srw` trataba ese error como bloqueante y cerraba la ventana.
- Commit SVN `37609` con mensaje `R26460 - Bandejas / Registro de Solicitudes`: se modificó `exe_presmed/Source Infomedical/wp_soli_reg.srw` para conservar el error `-4`, recuperar la solicitud y abrirla en modo consulta cuando el usuario no es responsable. Para otros errores se mantiene el comportamiento previo. Se agregó rutina `wf_modo_consulta()` para bloquear edición y acciones. No se modificó el SP de validación.
- Bárbara luego validó el botón `Limpiar` en esa instancia de consulta y decidió aplicar ella misma la recomendación de inhabilitarlo, porque `Limpiar` dispara `ue_limpiar`/`ue_restaurar` y podía dejar la ventana en estado inconsistente frente al modo consulta forzado.
- Caso de cálculo de `Tiempo Ajuste`: para solicitud `25920079`, con `tiempo_oportunidad = 48`, motivo rechazo `PRUEBA DESA` con `aju_tiempo_gestion = 10` y tiempo transcurrido de 1/2 minutos, Registro mostraba `58` pero Bandeja mostraba `10`.
- Diagnóstico: en `prs_solicitudes_auto_tiempo_oportunidad_bandeja.sql` la fórmula usada era `@var_ajuste_motivo - @var_tiempo_transcurrido_gestion`; en `prs_solicitudes_auto_oportunidad.sql` ya estaba correcta: `@var_tiempo_oportunidad + @var_ajuste_motivo - @var_tiempo_transcurrido_gestion`. Recomendación: modificar sólo SP de Bandeja para igualar fórmula; no tocar DataWindow ni Registro.
- Análisis de auditoría por rechazo de autorización con motivo que suma tiempo gestión: el estado rechazado/motivo impacta por `prs_valida_of_auto_remotas.sql`, que toma `auto_remotas.auto_esta_moti`, llama a `pru_autorizaciones.sql` y persiste en `AUTORIZACIONES.auto_esta_moti`. El punto recomendado para auditar no son los SPs de consulta/recalculo, sino `prs_valida_of_auto_remotas.sql` dentro del flujo donde la solicitud pasa de `Validacion Auditoria` a `En Gestion`.
- Commit SVN `37610` con mensaje `R26460 - Bandejas / Registro de Solicitudes`: se modificó `backend/procedures/prs_valida_of_auto_remotas.sql` para registrar auditoría en `SOLICITUDES_AUTO_AUDITORIA` cuando la solicitud pasa de `V` a `G` y existe autorización asociada con motivo cuyo `AUTO_ESTADOS_MOTIVOS.aju_tiempo_gestion > 0`. Texto agregado reportado: `Ajuste de tiempo de gestion por motivo de autorizacion: <motivo> - <horas> horas`.
- Validación del commit `37610`: `SET PARSEONLY ON` contra `InstallQA`, sin errores; estado final sin cambios propios pendientes, sólo externos SVN habituales.

- Análisis de solapa Devoluciones en Bandeja de Solicitudes/Auditoría: en `wp_bandeja_solicitudes.srw`, si `istr_rol.es_auditor = 'S'` se deshabilita `tab_1.tabpage_devolucion`; `wf_auditoria()` también deshabilita `tab_1.tabpage_devolucion`, `idw_devolver`, `cb_devolver` y `cb_refresh`. Por lo tanto, en Bandeja de Auditoría no debería operar Devoluciones.
- En Bandeja de Solicitudes, al seleccionar fila, Devoluciones se habilita sólo cuando `istr_rol.es_auditor <> 'S'`, `soli_esta = 'R'` y `origen` es `A` o `G`. Al ejecutar Devolver, además valida `wf_permite_editar()`; si el usuario no es responsable de gestión muestra `No puede devolver porque usted no es el Responsable en Gestión.`. Luego `ue_devolver()` exige motivo y llama a `pru_solicitudes_auto_devolucion`, que inserta en `SOLICITUDES_AUTO_DEVOLUCION`, audita y cambia `SOLICITUDES_AUTO.soli_esta = 'D'`.
- Análisis de Registro de Solicitudes, solapa Servicios a Solicitar, solicitud GIS ejemplo `25920085`: en `InstallQA` se confirmó `soli_esta = R`, `origen = G`, CUPS con `tiene_auto = N`. En `wp_soli_reg.srw`, `wf_retrieve_total()` habilita edición/acciones de servicios sólo para `(is_soli_esta = 'R' OR is_soli_esta = 'C') AND (ls_origen = 'A' OR ls_origen = 'P')`. Como `origen = G` no cumple, deshabilita `tab_1.tabpage_12.pb_ins_prestac`, `idw_prestac.object.b_borrar` y `idw_prestac.object.pb_deta_pres`.
- Condiciones actuales de Servicios a Solicitar: los botones se habilitan al restaurar/inicializar una solicitud nueva desde Registro de Solicitudes, y para solicitudes existentes sólo si estado `R` o `C` y origen `A` o `P`. En `d_soli_reg_serv.srd`, `b_borrar` sólo visible si `tiene_auto <> 'S'`, y columnas de prestación se protegen si `tiene_auto = 'S'`.
- Para permitir consultar conceptos en solicitudes `Origen G`, recomendación dada: no habilitar borrado ni inserción de CUPS. Separar consulta de edición: en `wp_soli_reg.srw`, permitir sólo `idw_prestac.object.pb_deta_pres.enabled = TRUE` para origen `G` en estado `R`/`C`, manteniendo `pb_ins_prestac` y `b_borrar` deshabilitados. Además pasar una marca de sólo consulta a `wp_prestac_concep_soli_reg`, porque esa ventana puede abrir editable; en modo consulta debería poner `dw_lista` y `dwa_datos` en `ReadOnly='yes'` y deshabilitar `cb_alta`, `cb_baja`, `cb_modifica`, `cb_limpiar`.
- Riesgo señalado: sólo habilitar `pb_deta_pres` podría permitir modificar conceptos, porque `wp_prestac_concep_soli_reg` devuelve el `DataStore` `ids_auto_item_deta` al cerrar. Eliminar CUPS para origen `G` quedó fuera de alcance y requiere definición funcional aparte, porque `wf_borrar()` borra también conceptos asociados y afecta solicitudes generadas por GIS.

- Usuario: Wilmer / whiguera. Preferencia operativa explícita para esta tarea: no realizar modificaciones directas ni commits salvo pedido puntual; trabajar de modo consultivo, proponer/instruir y no cerrar pasos sin visto bueno de Wilmer.
- Tarea `#29489` dentro de `A:\whiguera\Tarea #23986\Tarea #29489`, foco en notificaciones de solicitudes automáticas. Archivo generado/modificado en ruta Linux equivalente `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/`.
- Caso 18, `pri_solicitudes_auto_notif`: se retomó desde memoria 2026-08-19 punto `2. Guardar Texto Base De Auditoria`. SP local compartido hash/identificador `9b0548c5`. Se generó el SP final `pri_solicitudes_auto_notif.sql` reemplazando el bloque antiguo de envío por `@var_email` / `@var_email_2` con cursor `cur_destinatarios_notif` sobre `#DESTINATARIOS_NOTIF`.
- Lógica final del cursor Caso 18: valida cada destinatario con `dbo.afs_valida_mail`, crea `mail_id` independiente con `dbo.afi_mail_envio`, agrega destinatario con `dbo.afi_mail_to`, envía con `dbo.afu_mail_envio_send`, audita cada envío válido con `dbo.pri_solicitudes_auto_auditoria`, conserva primer `mail_id` válido tipo `P` en `@var_mail_id_prestad` y primer tipo `A` en `@var_mail_id_afi`, y actualiza `SOLICITUDES_AUTO` una sola vez al final. Se eliminó la línea incorrecta `SET @var_activi_base = @var_activi`.
- Validación Caso 18 prestador: solicitud `25920072`, `@prm_id = 1`, destinatarios vigentes en `SOLICITUDES_AUTO_PRESTAD`: `N1@N1.COM.AR`, `panchoAmej@meji.com.ar`, `PRUEBASGISPRESTADOR@GMAIL.COM`. Compiló SP. Al ejecutar, `mail_id_prestad_mail` cambió de `9989059` a `9989060`, `mail_id_afi_mail` permaneció `9989007`. Auditoría mostró envíos válidos `9989060` para `panchoAmej@meji.com.ar` y `9989061` para `PRUEBASGISPRESTADOR@GMAIL.COM`; `N1@N1.COM.AR` no fue auditado/enviado por validación `afs_valida_mail`.
- Validación Caso 18 afiliado/no regresión: misma solicitud `25920072`, afiliado `ALVARO DE, RUIZ CEBALLOS`, `email_afi_1 = email_afi_200260805@gmail.com`, `email_afi_2 = EMAIL_AFI_20260806@gmail.com`. Con `@prm_id = 7`, `mail_id_afi_mail` cambió de `9989007` a `9989062`, `mail_id_prestad_mail` quedó `9989060`. Auditoría mostró un envío válido tipo `A` a `email_afi_200260805@gmail.com`; el segundo email no fue enviado/auditado.
- Conclusión Caso 18: solicitud emitida para varios prestadores queda cubierta funcionalmente: múltiples destinatarios, validación individual, auditoría por email, primer `mail_id` válido por tipo y separación prestador/afiliado.
- Caso 24, revisión notificaciones estado Registrada: se creó/identificó solicitud GIS `25920114`, estado `R`, `prestad = 32`, `usuario = 10123080`, `tipo_usuario = P`, `origen = G`, `mail_id_prestad_mail = 9989063`, `mail_id_afi_mail = 9989064`. Auditoría confirmó notificación registrada tipo `P` y tipo `A`, carga de adjuntos y activación de solicitud. `SOLICITUDES_AUTO_WORKFLOW` dio `COUNT(*) = 0`.
- Diagnóstico Caso 24 WORKFLOW: no es problema de `pri_solicitudes_auto_notif`; la notificación de registrada sí sale. `dbo.pru_solicitudes_auto_workflow` inserta en `SOLICITUDES_AUTO_WORKFLOW` sólo si encuentra responsable. Para `25920114`, `responsable_gestion = NULL` y `responsable_auditoria = NULL`; en estado `R` requiere `responsable_gestion`, por eso no inserta workflow.
- Pendiente técnico acordado Caso 24: revisar luego `dbo.pru_registra_solicitudes_auto`, porque ahí la llamada a `dbo.pru_solicitudes_auto_workflow` está condicionada por `@var_responsable_gestion IS NOT NULL`. Analizar asignación por `ZZ_ROLES` / `ZZ_USUARI`, fallback `dbo.pru_proceso_asignacion_masiva` y `@var_motivo_no_asignacion`. Queda pendiente porque prioridad actual es notificación de solicitudes.
- Datos GIS usados según video funcional: METAUSUARIO con Tipo Identificación `Cédula de ciudadanía`, Nro. `1`, Nombre `1`, Apellido 1 `1`, Apellido 2 `1`, Email `1@1.com.ar`; PRESTADOR con rol `Usuario Operativo`, Tipo Identificación `Nro. Identificación Tributaria`, Nro. Identificación `10123080`; botón `Ingresar a GIS`. Rutas técnicas detectadas: `WebPos/index.php` y registro `WebPos/noambu_auto.php`, ambiente `InstallQA`.
- Caso 25, revisión notificaciones estado Devuelta: parametrización confirmada `@prm_id = 2` para Notificación PRESTADOR devuelta y `@prm_id = 16` para Notificación AFILIADO devuelta.
- Diagnóstico Caso 25 envío duplicado: en `dbo.pru_solicitudes_auto_devolucion`, cuando `@var_tipo_usuario_soli = 'P'`, el SP llama a `dbo.pri_solicitudes_auto_notif` con `@prm_id = 2` y también con `@prm_id = 16`; por eso una devolución de solicitud creada por prestador notifica también al afiliado. La regla funcional acordada: sólo para devolución, si `tipo_usuario = 'P'` enviar sólo `@prm_id = 2`; si `tipo_usuario IN ('A','E')` enviar sólo `@prm_id = 16`; origen Presmed no tiene devolución. Queda claro que debe quitarse el llamado a `@prm_id = 16` dentro del bloque `P`.
- Caso 25 cuerpo de mensaje: EF `R23986-EF_V4.doc` confirma procesos Prestador Devuelta y Afiliado Devuelta, destinos respectivos y variables de plazo `@dias_dev_txt` / `@dias_dev_nro`. En `InstallQA`, plantillas activas `id = 2` y `id = 16` no estaban iguales a la propuesta porque incluían textos iniciales de control (`NOTIFICACION AL PRESTADOR SOLICITUD DEVUELVA`, `NOTIFICACION AFILIADO ESTADO DEVUELTA`) y ninguna plantilla activa tenía tildes. Plantilla 2 usaba `@dias_dev_txt` / `@dias_dev_nro`; plantilla 16 usaba `@dife_dias_auto_orden`, diferencia sospechosa si el cuerpo esperado de afiliado también habla del plazo de devolución.
- Validación plantillas InstallQA: 26 plantillas activas, 0 con tildes; específicamente `id 2` y `id 16` tienen subject `Devolucion Solicitud No. @soli` / `Devolucion Solicitud No.@soli` y body con `devolvio` sin tilde. El bloque de motivo en plantilla 2 existe con tabla HTML: `El motivo por el cual se devolvio la solicitud fue`, columnas `Motivo` y `Observaciones`, variables `@moti_dev` y `@observ_dev`.
- Script `SOLICITUDES_AUTO_NOTIF_PLANT.sql` en `A:\whiguera\Tarea #23986\Tarea #29489`: se verificó contra EF que tenía negritas donde correspondía (`Estimado/Apreciado`, `Coomeva Medicina Prepagada`, encabezados `Motivo/Observaciones`, botón `Devolver`, cierres). A pedido de Wilmer se modificó el script sólo para agregar/corregir tildes y texto visible de plantillas, sin cambiar estructura SQL ni variables. Se validó que siguen siendo 26 plantillas y que las variables del script quedaron iguales. No se ejecutó en base de datos.
- Última solicitud candidata para probar devolución después de actualizar plantillas/fix: `25920115`, `soli_esta = R`, `tipo_usuario = P`, `origen = G`, `prestad = 32`, `mail_id_prestad_mail = 9989065`, `mail_id_afi_mail = 9989067`. Esperado con fix pendiente: al devolver, sólo auditoría/notificación `proceso_id: 2`, tipo `P`, sin nuevo envío `mail_id_afi_mail` por devolución.

## Registro bajo demanda - 2026-08-20 17:35

### Tarea 29489 / Caso 25 - Notificaciones estado Devuelta

- Solicitud o contexto: Wilmer devolvió la solicitud GIS prestador `25920114` para validar el Caso 25 después de corregir la lógica de devolución y el script de plantillas con tildes.
- Resultado de validación: la solicitud `25920114` quedó en estado `D`; `mail_id_prestad_mail` cambió a `9989068`; `mail_id_afi_mail` permaneció en `9989064`; auditoría mostró devolución tipo `P` y no apareció nueva auditoría tipo `A`.
- Cuerpo de mensaje enviado: el mail `9989068` salió con plantilla de prestador, saludo `Estimado Aliado`, tabla de `Motivo` y `Observaciones`, motivo `SOPORTES INCOMPLETOS`, botón `Devolver` en negrita y tildes correctas en textos como `devolvió`, `recepción`, `gestión`, `automática`, `botón`, `comuníquese`, `país`.
- Observación de parámetro: el cuerpo mostró plazo `cero (0) días`, coherente con el valor validado previamente en `InstallQA` para `vencimiento_devolucion_soli = 0`.
- Estado: Caso 25 validado funcionalmente. Regla confirmada: para solicitud origen GIS creada por prestador, al devolver se notifica sólo al prestador, no al afiliado.

### Tarea 29489 / Caso 26 - Notificaciones estado En Gestión

- Solicitud o contexto: Wilmer compartió el Caso 26. El funcional indica que el estado `EN GESTIÓN` sólo se asigna desde Presmed, con botones de editar/generar autorizaciones, y debe notificarse una sola vez, la primera vez que la solicitud queda en gestión.
- Parametrización identificada: existe proceso `@prm_id = 4`, `Notificación PRESTADOR, estado de solicitud EN GESTION`, y plantilla `id = 4`, `Gestión Solicitud No.@soli`.
- Flujo técnico identificado: botón Presmed `Generar Autorizaciones` en `wp_soli_reg.srw`, evento `cb_gen_auto.clicked`, llama `PARENT.EVENT ue_aceptar(1 /*genera*/, 0 /*verifica*/)` y termina ejecutando `dbo.pru_genera_auto_precarga` con `@prm_verifica = 'N'`.
- Diagnóstico en `dbo.pru_genera_auto_precarga`: el SP calcula estado actual con `dbo.prs_solicitudes_auto_esta_actual`, luego actualiza `SOLICITUDES_AUTO.soli_esta = @var_soli_esta`, `modi_fecha = @var_hoy`, `responsable_auditoria = @var_responsable_auditoria`, ejecuta workflow y audita `Generación de Autorizaciones`; pero no llama a `dbo.pri_solicitudes_auto_notif @prm_id = 4`.
- Evidencia de la solicitud del caso: solicitud `25920104` quedó en `soli_esta = G`; tiene auditoría de `Generación de Autorizaciones` y workflow en estado `G`; no tiene `mail_id_prestad_mail`; no aparece auditoría `Notificación solicitud en gestión`; sólo aparece notificación registrada al afiliado, tipo `A`.
- Corrección conceptual recomendada: insertar llamado a `dbo.pri_solicitudes_auto_notif` en `dbo.pru_genera_auto_precarga`, después del `UPDATE SOLICITUDES_AUTO` que persiste `@var_soli_esta` y antes del bloque `--WORKFLOW`, condicionado a `@var_soli_esta = 'G'` y a que no exista auditoría previa con `proceso_id: 4` para la solicitud.
- Bloque sugerido:

```sql
IF @var_soli_esta = 'G'
AND NOT EXISTS (
    SELECT 1
    FROM SOLICITUDES_AUTO_AUDITORIA
    WHERE soli_auto = @prm_soli_auto
      AND activi LIKE '%proceso_id: 4%'
)
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id        = 4,
        @prm_soli_auto = @prm_soli_auto
END
```

- Ubicación exacta recomendada: dentro de `dbo.pru_genera_auto_precarga`, buscar el bloque cerca del final:

```sql
UPDATE SOLICITUDES_AUTO
SET soli_esta                = @var_soli_esta,
    modi_fecha               = @var_hoy,
    responsable_auditoria    = @var_responsable_auditoria
WHERE soli_auto = @prm_soli_auto

--WORKFLOW
EXEC dbo.pru_solicitudes_auto_workflow
    @prm_soli_auto = @prm_soli_auto
```

Insertar el bloque de notificación entre el `UPDATE SOLICITUDES_AUTO` y el comentario `--WORKFLOW`.

- Motivo de esa ubicación: ese sector corre cuando `@prm_verifica = 'N'`, es decir, generación real de autorizaciones y no simulación; en ese punto ya se calculó y persistió el estado final. La condición `NOT EXISTS` evita notificar más de una vez el proceso `4`.
- Estado: pendiente de aplicar en el SP local/generar script según indique Wilmer, compilar y validar con una nueva solicitud que pase por Presmed a estado `G`. Resultado esperado: auditoría con `proceso_id: 4`, `notifica: Prestador`, `tipo_usuari = 'P'`, y actualización de `mail_id_prestad_mail` con nuevo `mail_id`.
