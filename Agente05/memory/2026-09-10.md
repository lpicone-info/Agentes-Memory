# Memoria diaria - 2026-09-10

## Preferencias vigentes de Wilmer
- Wilmer pidio recordar dos reglas operativas: NO realizar modificaciones directas en el entorno ni hacer commits; Lisa solo propone y muestra cambios por pantalla o archivo. La ejecucion de modificaciones y commits queda bajo responsabilidad exclusiva de whiguera.
- NO asumir que un paso fue comprendido o cerrado automaticamente sin visto bueno explicito de whiguera.

## Tarea #29489 - Caso 43 - Proceso 27 notificacion parcial
- Wilmer pidio retomar la Tarea #29489 del 09/09/2026, caso 43, proceso 27 de notificacion parcial.
- Objetivo funcional: revisar distribucion final de llamados y validar en `pri_solicitudes_auto_notif` la separacion de `@url_coomeva` y `@url_consulta_solicitudes` en QA.
- Repositorio asociado localizado: `svn/whiguera-Version84-Modelo2Gis`.
- No se ejecuto `svn update` porque Wilmer habia indicado no modificar directamente el entorno.
- Estado funcional recuperado: ambos tokens deben mapearse de forma independiente y resta verificar impacto sobre plantillas existentes.

## Analisis ventana `wp_auto_api_no_subidas`
- Wilmer pidio explicar los botones `cb_ace` Consultar y `cb_subir` Subir para un companero.
- La ventana lista autorizaciones cuyos PDF todavia no fueron subidos a la API.
- `cb_ace` hereda de `cb_ace_clase`; el `clicked` ejecuta `parent.postevent("ue_aceptar")`. `ue_aceptar` valida rango de fechas y hace `dw_sub.retrieve(ld_des, ld_has)`.
- `dw_sub` usa el DataWindow `d_auto_api_no_subidas`, cuyo SQL llama al SP `dbo.prs_auto_api_no_subidas` con `@prm_desde` y `@prm_hasta`.
- `cb_subir` redefine `clicked` y ejecuta `parent.postevent("ue_subir")`. Procesa todas las filas cargadas en `dw_sub`; valida parametro `USA API Adjuntos = 'S'`.
- SP/procedimientos usados por `cb_subir` segun flujo: `prs_auto_impresion_especial_opc`, `pru_auto_arch_path_full`, `prs_datos_generales_afilia_carta_nega_imp`, `pri_mail_autorizaciones`.
- La subida del archivo se realiza con `nvo_sube_adjunto.of_sube_adjunto_gral(...)`, usando API de adjuntos; no se observo llamada directa a SP dentro del codigo revisado.
- Flujos resumidos: `I/A` genera PDF, sube API y actualiza `archivo_path_full`; `I/N` genera carta negacion, sube PDF y actualiza `archivo_path_full`; `M/A` genera PDF y envia correo; `M/N` genera carta negacion y envia correo.

## Tarea #29489 - GAP #2 PDFs adjuntos en notificaciones EMAIL
- Tema retomado: PDFs adjuntos en notificaciones EMAIL de solicitudes auto finalizadas.
- Decision tecnica vigente: InfoMailer NO debe generar PDFs. Presmed debe generar o reutilizar PDFs al finalizar la solicitud, subirlos a API, actualizar `AUTORIZACIONES.archivo_path_full`, armar XML de adjuntos y llamar una sola vez a `pri_solicitudes_auto_notif`.
- Flujo objetivo: Presmed detecta solicitud finalizada; consulta autorizaciones asociadas; reutiliza PDF si existe `archivo_path_full`; genera/sube PDF si falta; actualiza path; arma XML; llama `pri_solicitudes_auto_notif`; el SP crea mail/destinatarios/relaciones, inserta `MAIL_ATTACH_BINARY`, registra `SOLICITUDES_AUTO_NOTIFICACIONES` y recien despues ejecuta `afu_mail_envio_send`.
- Objetos existentes a reutilizar: `wr_auto_impresion.srw` para decision de DW y generacion PDF con `SaveAs(..., PDF!, TRUE)`/`NativePDF!`; `nvo_sube_adjunto.of_sube_adjunto_gral`; `pru_auto_arch_path_full`; `dwc_carta_negacion.srd`/`prs_paginas_carta_negacion`; `afi_mail_attach_binary`; `wp_main_infomailer.srw` solo debe enviar adjuntos ya registrados.
- Objeto PB recomendado a crear: `uo_nv_soli_auto_adjuntos_pdf`, separado de `uo_nv_valida_auto`.
- Metodos propuestos: `of_preparar_adjuntos_finalizada(decimal adc_soli_auto, ref string as_adjuntos_xml, ref string as_mensaje)`, `of_generar_pdf_autorizada(...)`, `of_generar_pdf_negada(...)`, `of_subir_pdf_auto(...)`.
- DataObject propuesto: `d_soli_auto_autos_finalizada_pdf`, llamando a SP nuevo `prs_solicitudes_auto_adjuntos_finalizada`.
- Columnas minimas propuestas: `soli_auto`, `sucur`, `auto`, `esta`, `tipo`, `origen`, `prestad`, `archivo_path_full`, `carta_negacion`, `carta_estado`.
- DataWindow/DataStore auxiliar propuesto para XML: `d_soli_auto_adjuntos_mail_xml` con `soli_auto`, `sucur`, `auto`, `prestad`, `tipo_adjunto`, `file_name`, `archivo_path_full`.
- XML esperado:
```xml
<adjuntos>
 <adjunto>
  <soli_auto>25920125</soli_auto>
  <sucur>1</sucur>
  <auto>123456</auto>
  <prestad>32</prestad>
  <tipo_adjunto>A</tipo_adjunto>
  <file_name>123456.pdf</file_name>
  <archivo_path_full>Autorizaciones/2026/08/26/123456.pdf</archivo_path_full>
 </adjunto>
</adjuntos>
```
- SP nuevo propuesto: `prs_solicitudes_auto_adjuntos_finalizada`, leyendo `SOLICITUDES_AUTO`, `SOLICITUDES_AUTO_AUTORIZACIONES`, `AUTORIZACIONES` y para negadas `CARTA_NEGACION`.
- Reglas del SP: `SA.soli_auto = @prm_soli_auto`; bajas nulas en `SA`, `SAA`, `A`; `SA.soli_esta = 'F'`; `A.esta IN ('A','N')`; para negadas devolver solo si carta esta finalizada.
- Cambio propuesto en `pri_solicitudes_auto_notif`: agregar parametro opcional `@prm_adjuntos XML = NULL`; parsear XML; insertar adjuntos con `afi_mail_attach_binary` antes de `afu_mail_envio_send`.
- Regla de distribucion pendiente: afiliado recibe todos los PDFs; prestadores GIS reciben solo PDFs correspondientes a su `prestad`, salvo confirmacion funcional distinta para quirurgicas/hospitalarias.

## SOLICITUDES_AUTO_NOTIFICACIONES
- Wilmer pidio agregar insercion de registros para `SOLICITUDES_AUTO_NOTIFICACIONES`.
- Inicialmente se propuso tabla con `proceso_id`, `medio`, `tipo_destinatario`, `prestad`, `email_to`, `usuari`, pero Wilmer indico dejar solo campos minimos y quitar FK de `proceso_id`.
- Definicion final aprobada de tabla: `notificacion_id`, `soli_auto`, `mail_id`, `alta_fecha`, `modi_fecha`, `baja_fecha`.
- Secuencia definida: `SQC_SOLICITUDES_AUTO_NOTIFICACIONES`.
- SP definido: `dbo.pri_solicitudes_auto_notificaciones`.
- Uso esperado despues de generar `@out_mail_id`:
```sql
EXEC dbo.pri_solicitudes_auto_notificaciones
 @prm_soli_auto = @prm_soli_auto,
 @prm_mail_id = @out_mail_id
```
- Riesgo tecnico: ya no hay FK por `proceso_id`; conservar insercion minima.

## Pendientes antes de implementar #29489
- Cerrar procesos exactos con PDFs adjuntos: 8, 9, 13, 23, 5, 27, 28 u otros.
- Confirmar destinatarios de cada proceso: afiliado, prestadores GIS, prestador principal, combinaciones.
- Confirmar estado finalizada: si siempre es `SOLICITUDES_AUTO.soli_esta = 'F'` o si aplica `V` en parcial.
- Confirmar criterio exacto de carta negada finalizada: carta existente, vigente, impresa, firmada, finalizada, etc.
- Confirmar nombres finales de archivos adjuntos para autorizadas y negadas.
- Definir alcance del reenvio manual: reutiliza `MAIL_ATTACH_BINARY`, copia adjuntos, regenera mail/PDF o queda fuera.
- Confirmar contrato XML: valores de `tipo_adjunto`, nulidad de `prestad`, duplicados, tamanio maximo y comportamiento sin adjuntos.
- Definir transacciones/errores: si falla generacion/API/adjunto no crear mail pendiente ni ejecutar `afu_mail_envio_send`; si PDF subio pero falla notificacion, conservar `archivo_path_full` para reintento.

## Hallazgos tecnicos vigentes
- `pri_solicitudes_auto_notif` actualmente recibe solo `@prm_id` y `@prm_soli_auto`; el XML debe ser opcional para no romper llamados existentes.
- En la rama revisada, `pri_solicitudes_auto_notif` ejecuta `afu_mail_envio_send` antes de insertar adjuntos; el orden debe cambiar.
- El SP crea un mail por destinatario dentro de cursor; el XML debe filtrarse segun destinatario.
- En la rama actual, el SP asigna `@var_url_coomeva = url_consulta_solicitudes`, por lo que la separacion `@url_coomeva` / `@url_consulta_solicitudes` aun no esta implementada.
- Llamados encontrados a `pri_solicitudes_auto_notif`: `pru_fin_gestion_solicitudes_auto`, `pru_genera_auto_precarga`, `prs_valida_of_auto_remotas`, `pru_registra_solicitudes_auto`, `pru_solicitudes_auto_anula_bandeja`, `pru_solicitudes_auto_anula_total`, `pru_solicitudes_auto_devolucion`, `pru_solicitudes_auto_resuelta_pertinencia`, `prs_control_vencimiento_devolucion_soli`.
- Procesos 27/28 estan agregados en script de catalogo, pero las plantillas actuales tienen asunto `Cancelación Solicitud No.`, inconsistente con `Notificación PARCIAL PRESTADOR`; se debe corregir o confirmar.

## Paso a paso propuesto para iniciar implementacion
1. Cerrar contrato funcional: procesos, destinatarios, estados, cartas negadas y reenvio manual.
2. Auditar puntos de disparo a `pri_solicitudes_auto_notif` y seleccionar el punto que preparara adjuntos para finalizacion.
3. Validar modelo de datos en QA: `SOLICITUDES_AUTO_NOTIFICACIONES`, secuencia, SP minimo, claves/longitudes de `MAIL_AUTORIZACIONES` y `MAIL_ATTACH_BINARY`.
4. Crear diseno/script de `prs_solicitudes_auto_adjuntos_finalizada`.
5. Definir/crear NVO `uo_nv_soli_auto_adjuntos_pdf` en Presmed.
6. Integrar en el punto de finalizacion: detectar transicion a `F`, preparar adjuntos, detener notificacion si falla, llamar una sola vez a `pri_solicitudes_auto_notif` con XML.
7. Modificar `pri_solicitudes_auto_notif`: parametro XML opcional, parseo, destinatarios, `MAIL_AUTORIZACIONES`, `MAIL_ATTACH_BINARY`, `SOLICITUDES_AUTO_NOTIFICACIONES`, `afu_mail_envio_send` al final.
8. Corregir URLs: `@url_coomeva` debe venir de `PARAMETROS.url_webcoomeva`; `@url_consulta_solicitudes` de `PARAMETROS.url_consulta_solicitudes`.
9. Corregir/validar plantillas 27/28.
10. Pruebas QA: todas autorizadas, todas negadas, mixta, carta no terminada, PDF existente, PDF faltante, error API, prestador GIS, reintento, reenvio manual.


## Mapeo de vigencia efectiva AFILMED-PRESMED - R30339
- Se solicitó un mapeo técnico de impacto para el requerimiento "Vigencia efectiva de Programas AFILMED-PRESMED" sobre la base `PruebasCoreINFOMEDICAL`, sin modificar la base ni generar scripts SQL.
- Alcance acordado: identificar tablas, campos, relaciones físicas/lógicas, Stored Procedures candidatos, separación entre vigencia administrativa e histórica (`AFI_PLANES.VIGEN_DESDE`) y vigencia efectiva de cobertura (`AFI_PLANES.VIGEN_COBER`), riesgos y prioridades.
- Hallazgos de `AFI_PLANES`: clave primaria `prepaga, contra, vigen_desde`; ambos campos `NOT NULL`; 1.340.721 registros; 31 registros con `vigen_desde <> vigen_cober`; no se detectaron duplicados por `prepaga, contra, vigen_cober`; no existe un índice equivalente a los principales índices de `vigen_desde` sobre `vigen_cober`.
- Relaciones físicas declaradas: `plan_codi` y `plan_codi_emp` con `PLANES.plan_codi`; `prepaga` con `PREPAGAS.prepaga`; `tari` y `tari_emp` con `TARIFAS.tari`. Relaciones lógicas relevantes: `AFILIADOS`, `AFI_CREDENCIALES`, `AFI_TITULARES`, `AFI_ESTADOS_CONTRA`, `AFI_ADICIONALES`, autorizaciones, expedientes y prestaciones.
- Tablas funcionalmente relacionadas: `PLANES`, `AFI_PLANES_ESPECIALES`, `AFI_PLANES_CEM`, `AFI_ADICIONALES`, `AFI_CARENCIAS`, `AFI_CARENCIAS_A_INS`, `CARENCIAS`, `COBER_CARENCIAS`, `COBERTURAS`, `GRUPOS`, `TOPES`, `AUTORIZACIONES`, `AUTO_PRESTAC_ITEMS`, `AUTO_ESTADOS`, `EXPE_ORDENES`, `EXPEDIENTES`, `PRESTACIONES`, `PRESTADORES`, `CARTI_PRESTADORES`, y tablas de reintegros (`REI_ORD_*`, `REIN_ORDEN_*`, `CIRU_REINTEGROS`).
- Stored Procedures prioritarios para revisar por selección de cobertura: `prs_valida_of_afiliado`, `prs_cober_carencias`, `prs_valida_of_cobertura_tope`, `prs_valida_of_convenio_full`, `prs_valida_of_copagos`, `prs_valida_of_inter_cobertura`, `prs_valida_of_topes`, `prs_auto_topes_xafi`, `prs_saldotopes_xafi`, `prs_saldotopes_xafi_h`, `prs_situ_tiene_copago`, `prs_especialidades_cobertura` y `prs_afi_activos`.
- SP de prioridad secundaria/impacto indirecto: `prs_planes_princi_anexo` (ya usa `VIGEN_COBER` cuando `@prm_cobertura = 'S'`, pero el límite de adicionales sigue con `VIGEN_DESDE`), `prs_prestad_plan_codi_con_liq`, `prs_ficha_afi_planes`, `prs_rei_x_prestac`, `prs_rei_saldos`, `prs_rei_query`, `prs_rein_exporta_sistcont` y `prs_ult_plan_afiliado_de_baja`.
- No reemplazar automáticamente `VIGEN_DESDE` en `pri_afi_planes`, `prd_afi_planes`, `afu_afi_planes`, `afi_afi_planes`, `impins_AFI_PLANES`, `prs_valida_plan`, procesos de importación ni mantenimiento histórico, porque se usa para identificar la versión administrativa y forma parte de la clave primaria.
- `afu_afi_planes` inserta inicialmente `vigen_cober = vigen_desde` y `afu_impacta_cambio_cobertura` puede actualizar `vigen_cober = DATEADD(MM, tiempo_caren, vigen_desde)`. Esto confirma que `VIGEN_DESDE` es el inicio administrativo y `VIGEN_COBER` el inicio efectivo posterior a carencias.
- Criterio recomendado: conservar `VIGEN_DESDE` para joins/identificación histórica, altas, bajas, importaciones y facturación; usar el mayor `VIGEN_COBER <= fecha_de_evaluación` para decidir el programa aplicable a cobertura, autorizaciones y prestaciones.
- Riesgo principal: `AFI_ADICIONALES` no tiene `VIGEN_COBER`, sino `vigen_desde`/`vigen_hasta`; debe definirse cómo delimitar adicionales cuando el plan principal tiene fechas administrativa y efectiva distintas.
- Resultado: el cambio no debe aplicarse a los 355 módulos que referencian `AFI_PLANES.VIGEN_DESDE`; debe limitarse a las rutas funcionales de cobertura de PRESMED y validarse funcionalmente para reintegros, liquidaciones, topes y procesos históricos.
- Se generó el documento `/mnt/Agentes/amiro/R30339/Mapeo_Vigencia_Efectiva_AFILMED_PRESMED.docx` con el mapeo completo. Validaciones reportadas: integridad interna correcta, extracción de contenido correcta, conversión de prueba a PDF correcta, documento de 6 páginas. No se modificó la base de datos.

## Tarea #29489 - Cierre de sesión y preparación del Paso 2
- Wilmer confirmó el alcance funcional para solicitudes finalizadas (`SOLICITUDES_AUTO.soli_esta = 'F'`):
  - Proceso 8: notificación al afiliado con todas las autorizaciones aceptadas.
  - Proceso 9: notificación al afiliado con todas las autorizaciones negadas.
  - Proceso 13: notificación al afiliado para solicitud mixta, con aceptadas y negadas.
  - Proceso 23: notificación al afiliado cuando la solicitud finalizada fue ingresada por prestador GIS.
  - Proceso 5: notificación al prestador por solicitud finalizada, sin adjuntos.
  - Procesos 27 y 28: notificación parcial al prestador, sin adjuntos por ahora.
- Regla de destinatarios confirmada: los PDFs/carta de negación se adjuntan únicamente a los mails del afiliado. Las notificaciones al prestador salen sin XML de adjuntos y sin `MAIL_ATTACH_BINARY` en este alcance.
- El estado parcial `V` queda fuera de esta etapa. Se trabajará más adelante, después de hacer funcionar el estado finalizado `F`.
- Para una carta de negación, el criterio de finalización es `CARTA_NEGACION.final_fecha IS NOT NULL`. El criterio funcional para una solicitud con cartas negadas sin `final_fecha` sigue pendiente:
  1. no enviar el mail completo;
  2. enviar solo cartas finalizadas;
  3. en solicitud mixta, bloquear todo el mail aunque existan autorizadas;
  4. en solicitud mixta, enviar autorizadas y cartas finalizadas.
- Los nombres deben respetar `wr_auto_impresion`:
  - Autorizada: `<auto>.pdf`.
  - Negada: formato actual observado `CartaNegacion_<contrato><inte>_<auto>.pdf`, sin agregar un separador entre contrato e integrante.
- El reenvío manual, cuando corresponda, debe invocar la misma lógica automática: reutilizar PDFs existentes, generar/subir solo faltantes, actualizar `AUTORIZACIONES.archivo_path_full` y volver a notificar. No debe regenerar innecesariamente archivos ya disponibles.
- Wilmer actualizó la regla operativa: el agente puede crear o modificar archivos únicamente dentro de `A:\whiguera\Tarea #23986\Tarea #29489`, relacionados con la Tarea #29489 y solicitados por Wilmer. Continúa prohibido modificar directamente repositorios o bases de datos, hacer commits/publicaciones y cerrar pasos sin visto bueno explícito.
- Wilmer pidió alinear la working copy con su repositorio local. No se ejecutó `svn update`: la actualización quedó impedida por la restricción/errores de certificado SSL del servidor SVN; no se modificó la working copy del repositorio.
- Se generó el archivo de tarea:
  - Ruta equivalente Linux: `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/prs_solicitudes_auto_adjuntos_finalizada.sql`.
  - Ruta Windows solicitada: `A:\whiguera\Tarea #23986\Tarea #29489\prs_solicitudes_auto_adjuntos_finalizada.sql`.
  - Validaciones reportadas: ASCII, 117 líneas, finales LF, sin BOM, sin `;`, sin `RTRIM`.
  - No se instaló ni ejecutó el SP en QA desde el agente.
- El SP `prs_solicitudes_auto_adjuntos_finalizada` quedó diseñado para consultar `SOLICITUDES_AUTO`, `SOLICITUDES_AUTO_AUTORIZACIONES`, `AUTORIZACIONES` y `CARTA_NEGACION`, con bajas lógicas nulas, solicitud en estado `F` y autorizaciones en estado `A` o `N`.
- El SP devuelve, entre otros, `soli_auto`, `sucur`, `auto`, `esta`, `tipo`, `origen`, `prestad`, `contrato`, `inte`, `archivo_path_full`, `carta_negacion`, `carta_estado`, `carta_final_fecha`, `tipo_adjunto`, `file_name`, `archivo_existente` y `adjunto_disponible`.
- `carta_estado` quedó interpretado así:
  - `N`: no existe carta de negación.
  - `P`: existe carta, pero `final_fecha IS NULL`.
  - `F`: carta finalizada.
- Las cartas pendientes (`carta_estado = 'P'`) se recuperan para control y para que el futuro objeto PB pueda aplicar la decisión funcional; no deben entrar al XML hasta que estén finalizadas y la política de negocio esté definida.
- Casos encontrados y ejecutados para la prueba lógica del SP:
  - `25920000`: solo autorizadas, dos registros `A`.
  - `25919878`: caso mixto, una autorización `A` (`22596369`) y una negada `N` (`22596368`) con carta finalizada (`carta_estado = 'F'`, carta `237342`).
  - `25919673`: solo negada, autorización `3836233`, carta finalizada (`carta_estado = 'F'`, carta `29272`).
  - `25920092`: solo negada con carta pendiente (`carta_estado = 'P'`, `final_fecha IS NULL`), útil solo como caso de control.
- En los casos positivos, `archivo_path_full` estaba `NULL`; por eso `archivo_existente = 'N'` y `adjunto_disponible = 'N'`. Esto no se tomó como error: indica que el futuro PB debe generar/subir el PDF y actualizar el path.
- Conclusión de la sesión: el Paso 1 quedó validado técnicamente a nivel de diseño, generación y prueba lógica del SP. No se considera cerrado funcionalmente hasta resolver el tratamiento de cartas sin `final_fecha` y hasta que Wilmer confirme el resultado.

## Reanudación prevista - Paso 2
- La próxima sesión debe comenzar con el **Paso 2: diseño detallado del NVO `uo_nv_soli_auto_adjuntos_pdf`**, sin modificar todavía objetos del repositorio salvo que Wilmer lo solicite expresamente.
- Primer entregable del Paso 2:
  1. revisar nuevamente `wr_auto_impresion.srw` y los objetos relacionados para reutilizar su lógica;
  2. definir el contrato público `of_preparar_adjuntos_finalizada`;
  3. definir métodos privados para autorizadas, negadas, subida API, actualización de `archivo_path_full`, reutilización de archivos y armado XML;
  4. definir el DataObject/DataStore de entrada basado en `prs_solicitudes_auto_adjuntos_finalizada`;
  5. definir el DataStore auxiliar `d_soli_auto_adjuntos_mail_xml`;
  6. establecer códigos de retorno, mensajes, validaciones y comportamiento ante errores;
  7. dejar identificado cómo el NVO distinguirá notificación al afiliado con adjuntos de notificación al prestador sin adjuntos;
  8. revisar si el SP debe modificarse cuando llegue la respuesta funcional sobre cartas pendientes.
- Diseño funcional provisional para el NVO:
  - Aceptada con `archivo_path_full` no vacío: reutilizar.
  - Aceptada sin path: generar PDF como `wr_auto_impresion`, subirlo mediante `nvo_sube_adjunto.of_sube_adjunto_gral`, actualizar path y agregar al XML.
  - Negada con `carta_estado = 'F'` y path no vacío: reutilizar.
  - Negada con `carta_estado = 'F'` y path vacío: generar carta con la lógica existente, subir, actualizar path y agregar al XML.
  - Negada con `carta_estado = 'P'`: no agregar al XML; aplicar la regla funcional pendiente cuando sea informada.
  - Proceso 5, 27 y 28: no preparar XML para el destinatario prestador.
  - Proceso 8, 9, 13 y 23: preparar XML para el afiliado según corresponda.
- El Paso 2 debe dejar como salida un diseño revisable y, si Wilmer lo autoriza, los archivos auxiliares dentro de la carpeta de la tarea. No debe integrar todavía el NVO al flujo de finalización ni modificar `pri_solicitudes_auto_notif` hasta validar el diseño.
- Antes de modificar `prs_solicitudes_auto_adjuntos_finalizada` por la respuesta funcional, comparar la regla elegida con estas alternativas:
  - exclusión total de la solicitud si existe alguna negada pendiente;
  - devolución únicamente de cartas finalizadas;
  - devolución de aceptadas y negadas finalizadas en solicitud mixta;
  - señalización de estado para que PB bloquee o permita según la política.
- Forma recomendada de retomar mañana:
  - `Lisa, retomemos la Tarea #29489 desde el Paso 2. Usa el contexto guardado del 10/09/2026, diseña el NVO uo_nv_soli_auto_adjuntos_pdf y revisa si la respuesta funcional sobre cartas sin final_fecha requiere modificar prs_solicitudes_auto_adjuntos_finalizada.`
- Si Wilmer ya recibe la respuesta funcional, debe agregarla en el mismo mensaje indicando cuál alternativa aplica para solicitudes totalmente negadas y cuál para solicitudes mixtas. El agente no debe asumirla.
