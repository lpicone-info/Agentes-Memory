# Memoria 2026-09-16

## Tarea #29489 - Validacion e implementacion notificacion finalizada

- Se retomo la tarea desde la memoria del 2026-09-15 para continuar con el Paso 4: validar y probar `pru_solicitudes_auto_notif_finalizada`.
- Archivo validado: `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/pru_solicitudes_auto_notif_finalizada.sql` / `A:\whiguera\Tarea #23986\Tarea #29489\pru_solicitudes_auto_notif_finalizada.sql`.
- Validaciones finales del script: 287 lineas, encoding `ISO-8859-1`, finales `CRLF`, sin BOM UTF-8, sin `RTRIM`, sin `;`, sin `THROW`, con `RAISERROR` en `CATCH`, `DROP PROCEDURE` controlado con `IF EXISTS`, `GO` antes y despues del `CREATE PROCEDURE`.
- Firma validada del SP nuevo:
  ```sql
  CREATE PROCEDURE dbo.pru_solicitudes_auto_notif_finalizada
  (
      @prm_soli_auto NUMERIC(18,0),
      @prm_adjuntos  XML,
      @prm_usuari    CHAR(15) = NULL
  )
  ```
- Compatibilidad validada: `pri_solicitudes_auto_notif` ya recibe `@prm_adjuntos XML = NULL`; contempla adjuntos para procesos `8,9,13,23,27,28`; `pru_fin_gestion_solicitudes_auto` ya devuelve `@prm_soli_esta OUTPUT` y deja las finalizadas para el coordinador.
- Se indico que el script esta correcto para ejecutar en base, con condicion previa de tener desplegada la version actualizada de `pri_solicitudes_auto_notif`.

## Validaciones funcionales del SP coordinador

- Se preparo explicacion para funcional sobre errores devueltos por `pru_solicitudes_auto_notif_finalizada`:
  - Solicitud no existe o esta dada de baja.
  - Solicitud no esta en estado finalizada.
  - XML de adjuntos no corresponde a la solicitud finalizada.
  - Solicitud finalizada sin autorizaciones vigentes.
  - Solicitud con autorizaciones en estados no notificables.
  - No se puede determinar proceso de notificacion al afiliado.
- Respuesta funcional recibida: "Si la solicitud tiene estado finalizada, siempre tiene que haber una autorizacion con estado = A o estado = N".
- Conclusion: las validaciones del SP quedan funcionalmente justificadas. Es correcto cortar con error si una solicitud finalizada no tiene autorizaciones vigentes A/N, tiene estados distintos de A/N, o no permite determinar proceso `8`, `9`, `13` o `23`.

## Paso PB propuesto para `wp_soli_reg.srw` evento `ue_fin_gestion`

- Flujo recomendado:
  1. Ejecutar `pru_fin_gestion_solicitudes_auto`.
  2. Recibir `@prm_soli_esta OUTPUT`.
  3. Si el estado final no es `F`, hacer `COMMIT` normal y salir.
  4. Si el estado final es `F`, ejecutar NVO `uo_nv_soli_auto_adjuntos_pdf`.
  5. Si el NVO devuelve distinto de `1`, hacer `ROLLBACK`.
  6. Si el NVO devuelve `1`, hacer `COMMIT`.
  7. Despues del `COMMIT`, llamar a `pru_solicitudes_auto_notif_finalizada`.
  8. Refrescar pantalla.
- Se remarco que no hay que pasar todavia a otro objeto hasta aplicar/compilar `wp_soli_reg.srw`, probar una solicitud finalizada controlada y validar mail, auditoria y adjuntos.

## Prueba en InstallQA con solicitud 25920230

- Wilmer registro la solicitud `25920230`; se valido en `InstallQA`.
- Datos encontrados inicialmente:
  - `soli_auto`: 25920230
  - `soli_esta`: `F`
  - `soli_tipo`: `D`
  - `tipo_usuario`: `E`
  - `origen`: `P`
  - `prestad`: 4985
  - `email_prestad`: `papa@gmail.com`
  - `email_afi_1`: `pruebasgisprestador@gmail.com`
  - `email_afi_2`: `pruebasgisprestador@gmail.com`
  - `mail_id_afi_mail`: 9989275
  - `mail_id_prestad_mail`: `NULL`
- Autorizacion asociada:
  - `sucur`: 7600
  - `auto`: 22596543
  - estado autorizacion: `A`
  - `archivo_path_full`: `NULL`
  - `baja_fecha`: `NULL`
- Resumen funcional: sirve para probar el coordinador `pru_solicitudes_auto_notif_finalizada`, especificamente proceso `8` (todas autorizadas). No sirve como prueba completa ideal del click PB porque ya estaba en `F`.
- Auditoria previa: solo existia `proceso_id: 7`, mail `9989275`, subject `Registro Solicitud No.25920230`; no habia auditoria `proceso_id: 8`.
- Resultado esperado para esa solicitud: generar proceso `8`, mail finalizado al afiliado, adjuntar PDF de autorizacion `22596543`, no generar proceso `5` porque `tipo_usuario = E`, no generar proceso `23` porque `origen = P`, no GIS.

## Validacion posterior en InstallQA: no hizo notificacion finalizada

- Se confirmo que tras ejecucion desde PB no se genero notificacion finalizada.
- SP `dbo.pru_solicitudes_auto_notif_finalizada` existe OK y tiene parametros `@prm_soli_auto`, `@prm_adjuntos`, `@prm_usuari`.
- Solicitud `25920230` sigue en `F`, con `mail_id_afi_mail = 9989275`, que sigue siendo el mail viejo de registro.
- No aparece auditoria `proceso_id: 8`.
- No hay `MAIL_ATTACH_BINARY` relacionado al mail actual.
- Autorizacion `22596543` sigue con `archivo_path_full = NULL`.
- Auditorias recientes: `Fin de gestion explicita` a las 13:17 y 13:13; `Registro de workflow de solicitud. Estado: F`; `Generacion de Autorizaciones`; `Notificacion solicitud registrada. proceso_id: 7`.
- Conclusion tecnica: se ejecuto `pru_fin_gestion_solicitudes_auto`, pero no se ejecuto el flujo posterior del nuevo desarrollo (`NVO uo_nv_soli_auto_adjuntos_pdf`, `pru_solicitudes_auto_notif_finalizada`, `pri_solicitudes_auto_notif` proceso 8). Probable causa: se sigue ejecutando el `ue_fin_gestion` viejo o el cambio PB no esta compilado/desplegado.

## Problema detectado con `ls_soli_esta` en PowerBuilder

- Wilmer agrego mensajes y confirmo que entra al flujo, pero variable `ls_soli_esta` queda vacia.
- Se valido en `InstallQA` que la base esta bien:
  - `dbo.pru_fin_gestion_solicitudes_auto`
  - `@prm_soli_auto numeric`
  - `@prm_soli_esta char(1) OUTPUT`
- Prueba directa SQL del output devolvio correctamente `estado_output = F`.
- Esa prueba directa volvio a ejecutar `pru_fin_gestion_solicitudes_auto` sobre solicitud `25920230` y dejo auditoria nueva de `Fin de gestion explicita` a las 13:29; no genero notificacion finalizada.
- Diagnostico: el problema no esta en el SP, sino en como PowerBuilder captura `ls_soli_esta`.
- Sintaxis PB recomendada para capturar output:
  ```powerscript
  STRING ls_soli_esta

  ls_soli_esta = ""

  DECLARE fin_gestion PROCEDURE FOR dbo.pru_fin_gestion_solicitudes_auto
      @prm_soli_auto = :il_soli,
      @prm_soli_esta = :ls_soli_esta OUTPUT
  USING SQLCA;

  EXECUTE fin_gestion;

  IF SQLCA.sqlcode < 0 THEN
      mBox(THIS, SQLCA.sqlerrtext, 2, 1)
      CLOSE fin_gestion;
      RETURN
  END IF

  CLOSE fin_gestion;

  mBox(THIS, "Estado devuelto por pru_fin_gestion_solicitudes_auto: [" + ls_soli_esta + "]", 1, 0)
  ```
- Variante de sintaxis a probar si sigue vacio, usada en objetos del repo:
  ```powerscript
  DECLARE fin_gestion PROCEDURE FOR dbo.pru_fin_gestion_solicitudes_auto
      @prm_soli_auto = :il_soli,
      @prm_soli_esta = :ls_soli_esta OUTPUT USING SQLCA;
  ```
- Diagnostico final pendiente: si PB muestra vacio aunque SQL devuelve `F`, entonces el `DECLARE` ejecutado no esta tomando el parametro como `OUTPUT`, o el objeto no recompilo/regenero correctamente.

## Cierre nocturno whiguera / Tarea #29489 / notificaciones solicitud

- Usuario/sender: Wilmer Higuera (`users/113120678211994207734`).
- Wilmer pidio guardar nuevamente lo trabajado el dia de hoy y dejo como punto de retoma: **llamado del procedimiento `pri_solicitudes_auto_notificaciones`**.
- Error diagnosticado: `Mismatch between retrieve columns and fetch columns` al registrar solicitud en `InstallQA`.
- Causa confirmada: `dbo.pri_solicitudes_auto_notificaciones` emitia un result set intermedio con:

```sql
SELECT @var_notificacion_id
```

- Ese `SELECT` devuelve 1 columna y rompe el contrato de PowerBuilder, que en `pru_registra_solicitudes_auto` espera 3 columnas con:

```powerscript
FETCH proc INTO :il_soli, :li_error, :ls_deno_error
```

- Fix recomendado: quitar/comentar `SELECT @var_notificacion_id`; si el ID se necesita, devolverlo por parametro `OUTPUT`.
- Se reviso nuevamente el video `Leandro_generar_pdf_adjuntos.mp4` y su transcripcion. Se confirmo que para solicitud finalizada el sitio funcional de disparo no es el proceso de mail sino **supervision/gestion de autorizaciones**: al autorizar o negar una autorizacion, se recalcula la solicitud y puede pasar a `SOLICITUDES_AUTO.soli_esta = 'F'`.
- Procesos de finalizada definidos:
  - `5`: prestador, finalizada, sin adjuntos.
  - `8`: afiliado, finalizada todas autorizadas, con adjuntos.
  - `9`: afiliado, finalizada todas negadas, con adjuntos.
  - `13`: afiliado, finalizada mixta, con adjuntos.
  - `23`: afiliado, finalizada ingresada por prestador GIS, con adjuntos.
- Secuencia de integracion acordada:
  1. `wp_soli_reg.srw`, evento `ue_fin_gestion`, llama `pru_fin_gestion_solicitudes_auto`.
  2. El SP recalcula y devuelve estado final.
  3. Si transiciona a `F`, PowerBuilder llama `uo_nv_soli_auto_adjuntos_pdf.of_preparar_adjuntos_finalizada`.
  4. Si falla NVO, `ROLLBACK` y no notifica.
  5. Si sale bien, `COMMIT`.
  6. Luego llamar `pru_solicitudes_auto_notif_finalizada`.
  7. El coordinador llama `pri_solicitudes_auto_notif` con XML de adjuntos cuando corresponda.
- XML real del NVO ya validado previamente:

```text
/d_soli_auto_adjuntos_mail_xml/d_soli_auto_adjuntos_mail_xml_row
```

- Tambien se definio envio manual desde `wp_soli_reg.srw`:
  - Agregar boton `Enviar Notificacion` (`cb_notif_manual`).
  - Crear evento `ue_notif_manual`.
  - Abrir ventana auxiliar sugerida `wp_soli_notif_manual` para seleccionar proceso vigente.
  - Crear SP coordinador sugerido `pru_solicitudes_auto_notif_manual`.
  - Para procesos `8`, `9`, `13`, `23`, generar XML de adjuntos con el NVO antes de llamar el SP.
  - Para proceso `5`, no generar adjuntos.
  - `wp_soli_notificacion_plant` no debe enviar manualmente; solo parametriza plantillas.
- Frase sugerida para retomar:

```text
Lisa, retomemos la Tarea #29489 desde la memoria del 16/09/2026. Continuemos donde quedamos: llamado de `pri_solicitudes_auto_notificaciones` / `pri_solicitudes_auto_notif`, corrigiendo el SELECT intermedio y definiendo la integracion desde finalizacion con adjuntos y envio manual en `wp_soli_reg`.
```
