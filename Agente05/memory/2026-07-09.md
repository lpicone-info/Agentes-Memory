# Memoria durable - 2026-07-09

## Tarea #29489 - Wilmer / whiguera-Version84-Modelo2Gis

- Preferencias activas de Wilmer: trabajar exclusivamente con repositorio `whiguera-Version84-Modelo2Gis` para la Tarea `#29489`; entregables `.docx` solo en `A:\Wilmer\Tarea #29489` si Wilmer lo pide o si el volumen lo requiere; modo consultivo, sin modificaciones directas ni commits; avanzar paso a paso y no pasar al siguiente paso sin confirmacion explicita de Wilmer.
- Conversacion retomada desde trabajo del 08/07 sobre `backend/procedures/pri_solicitudes_auto_notif.sql`: revisar `pri_solicitudes_auto_notif`, corregir `OUTER APPLY` de `@observ_dev`, y continuar con `@espe` / `@tiempo_espe` segun RM 26476.
- Se verifico que el adjunto local mas reciente del SP (`pri_solicitudes_auto_notif---87ed82af...sql`) ya no tenia marcadores de conflicto, mantenia ASCII/CRLF, corregia el `OUTER APPLY` de devolucion usando `LEFT JOIN` a `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`, asignaba `@observ_dev` desde `SAD.obser`, reemplazaba `@observ_dev` en subject/body y usaba token `@afi_id` consistente con el DataWindow ya commiteado por Wilmer.
- En el workspace del agente, la working copy SVN aun aparecia conflictuada para `backend/procedures/pri_solicitudes_auto_notif.sql` y el DataWindow local seguia mostrando token viejo `@id_afi`; se concluyo que la working copy del agente no estaba sincronizada con lo que Wilmer ya ajusto/commiteo.
- Para `@observ_dev`, la correccion funcional validada es tomar la ultima devolucion no dada de baja de `SOLICITUDES_AUTO_DEVOLUCION` por `S.soli_auto`, con `LEFT JOIN` a `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` para no perder `SAD.obser` cuando `SAD.moti` sea NULL, ordenando por `SAD.item DESC`.
- Se consulto RM 26476 con usuario `whiguera`. Evidencia funcional: la solicitud llega a la bandeja del medico par especialista segun especialidad seleccionada; el tiempo se controla desde la llegada a esa bandeja hasta la resolucion del comite.
- Objetos relevantes detectados para RM 26476: `COMITE_ESPECIALISTAS` relaciona regional, usuario especialista y especialidad; `SOLICITUDES_AUTO_WORKFLOW` guarda usuario responsable por estado y fecha de inicio de actividad; estado `E` significa `Pendiente Comité Especialistas`; `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP` guarda tiempos parametrizados por tipo de solicitud, origen, tipo especial, VIP y colorimetria; en bandeja `colorimetria = 'R'` se usa como umbral vencido y fue propuesto como candidato para `@tiempo_espe`.
- Propuesta pendiente de validar con Wilmer para `@espe`: agregar `OUTER APPLY` que tome `TOP 1` de `SOLICITUDES_AUTO_WORKFLOW SAW` para la solicitud, `SAW.soli_esta = 'E'`, `SAW.baja_fecha IS NULL`, ordenado por `SAW.fecha_ini_activi DESC`; unir opcionalmente a `ZZ_USUARI ZU_ESP` por `ZU_ESP.usuari = SAW.usuari`; devolver nombre formateado con `dbo.afn_formatea_nombre_y_ape(...)` y fallback a `SAW.usuari`.
- Propuesta pendiente de validar con Wilmer para `@tiempo_espe`: agregar `OUTER APPLY` a `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP SATGE` filtrando por `SATGE.soli_tipo = S.soli_tipo`, `SATGE.origen = S.origen`, `SATGE.prestad_vip = ISNULL(P.vip, 'N')`, `SATGE.afi_tipo` calculado como `V` si `S.vip='S'`, `P` si `S.coomeva='S'`, `R` si `S.pro_riesgo='S'`, `A` si `S.arquetipo='S'`, sino `O`; `SATGE.colorimetria = 'R'`; `SATGE.baja_fecha IS NULL`; devolver `SATGE.tiempo_gestion`.
- Reemplazos propuestos para SP despues de declarar variables y asignarlas en SELECT principal: `@var_espe = ESP_MEDICO.espe`, `@var_tiempo_espe = TGE_ESP.tiempo_espe`; reemplazar `@espe` e `@tiempo_espe` tanto en `@var_subjet` como en `@var_body`, convirtiendo `@var_tiempo_espe` a `VARCHAR(30)`.
- Ultimo mensaje enviado a Wilmer: se propuso como siguiente paso validar la logica de `@espe` y `@tiempo_espe` antes de pasar a `@dias_comite_especialistas` y `@motivo_comite_especialistas`.

## Continuacion Tarea #29489 - pri_solicitudes_auto_notif / RM 26476

- Wilmer pregunto con que tipo de dato declarar las variables nuevas del SP. Se recomendo declarar `@var_espe VARCHAR(255)` porque el token `@espe` representa el medico comite especialista como texto, y `@var_tiempo_espe INT` porque `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP.tiempo_gestion` es entero. Para reemplazo de token, convertir `@var_tiempo_espe` con `CONVERT(VARCHAR(30), @var_tiempo_espe)`.
- Se propuso validar la logica sin ejecutar `pri_solicitudes_auto_notif`, porque ese SP envia correos. La validacion debe hacerse con consultas aisladas para evitar disparar notificaciones.
- Wilmer valido en base que no existen registros en `SOLICITUDES_AUTO_WORKFLOW` con `soli_esta = 'E'`. Esto invalido la primera hipotesis de obtener `@espe` desde workflow.
- Se propuso como segunda hipotesis obtener `@espe` desde `SOLICITUDES_AUTO.responsable_gestion`, cruzando con `ZZ_USUARI` y validando `ZZ_USUARI.soli_comite_espe = 'S'` o existencia en `COMITE_ESPECIALISTAS`.
- Wilmer compartio resultado real para solicitud `23959674`: `soli_esta = E`, `responsable_gestion = NULL`, `soli_comite_espe = NULL`, `espe = NULL`. Esto invalido tambien la logica de `responsable_gestion` para `@espe`.
- Estado actual de analisis: para `@espe` no se debe modificar todavia el SP. Falta confirmar el origen correcto desde parametrizacion de `COMITE_ESPECIALISTAS`, cruzando regional/oficina y especialidad de la solicitud.
- Se pidio a Wilmer validar una consulta base para solicitud `23959674` que devuelve datos de `SOLICITUDES_AUTO`, `SUCURSALES` y `PARTIDOS`. Resultado interpretado en conversacion: regional/ofi `1`, solicitud en estado `E`, sin `responsable_gestion`, `soli_tipo = H`, `origen = P`, prestador `8404`, flags `vip/coomeva/pro_riesgo/arquetipo = N`, por lo tanto `afi_tipo = O`.
- Para `@tiempo_espe`, la logica candidata sigue siendo `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP` filtrada por `soli_tipo`, `origen`, `prestad_vip = ISNULL(P.vip, 'N')`, `afi_tipo` calculado por los flags de la solicitud, `colorimetria = 'R'` y `baja_fecha IS NULL`.
- Proxima accion exacta al retomar: pedir/usar el resultado de la consulta contra `COMITE_ESPECIALISTAS` para `soli_auto = 23959674`, con `CE.ofi = PAR.regional`, `CE.baja_fecha IS NULL`, union a `ZZ_USUARI` y `ESPECIALIDADES`. Si devuelve mas de un medico, no elegir aun: falta identificar que especialidad de la solicitud debe matchear contra `COMITE_ESPECIALISTAS.espe`.
- Pausa solicitada por Wilmer: se ayudo a redactar respuesta para un funcional sobre posible dependencia entre mantis/RM `8596 - 29425`, `6654 - 29627`, `11752 - 29864`, `11784 - 29948`. Respuesta recomendada: no confirmar independencia solo por numero de Mantis/RM; validar matriz de objetos impactados por cada RM. Si `RM 29864 / Mantis 11752` no comparte SP, DataWindows, tablas, parametros ni logica comun con los otros RM, podrian implementar todo excepto ese; sin revisar objetos o detalle tecnico de desarrollo, no conviene dar OK definitivo a Coomeva.

## Actualizacion 2026-07-10 - Variables pendientes por definicion cliente

- Wilmer indico que las variables `@var_dias_comite_especialitas`, `@var_motivo_comite_especialistas`, `@var_espe` y `@var_tiempo_espe` deben dejarse para el final porque son temas pendientes de definicion por parte del cliente. No se deben tener en cuenta por el momento en el avance inmediato de la Tarea `#29489`.
- Contexto funcional: el RM/Mantis asociado a estos puntos esta en estado "Se Necesita Mas Informacion"; Coomeva enviaria una actualizacion del requerimiento. Hasta recibir esa definicion, no proponer implementacion definitiva para esos tokens ni modificar `pri_solicitudes_auto_notif.sql` por esas variables.
