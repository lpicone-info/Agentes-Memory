# Memoria curada diaria

- Fecha: 2026-07-29
- Usuario: John Marquinez Castillo
- id_googlechat: users/117759086016770935510
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/idcptyaaaae

## Resumen de la actividad

### Tema 1: Tablas usadas por ventanas de reembolsos para RM 26488

- Solicitud o contexto: John consultó, de acuerdo al RM 26488, en qué tablas se llenan las ventanas `Archivos anexos de reembolsos` y `Motivos por estado de reembolsos`.
- Respuesta o acciones del agente: Se revisaron los objetos de `Version84-Modelo2Gis` vinculados a las ventanas `wp_reembolsos.srw` y `wp_moti_reem.srw`.
- Resultado: Se informó que `Archivos anexos de Reembolsos` configura la tabla `REI_TIPO_DOCU`, porque la ventana setea `is_tabla = 'REI_TIPO_DOCU'` y graba mediante `pru_rei_tipo_docu`. También se informó que `Motivos por Estado de Reembolsos` configura la tabla `REI_ESTADOS_MOTIVOS`, porque la ventana setea `is_tabla = "REI_ESTADOS_MOTIVOS"` y graba mediante `pri_rei_estados_motivos`, `pru_rei_estados_motivos` o `prd_rei_estados_motivos`. Se aclaró que `REI_DOCU` es transaccional para documentos cargados en un reembolso puntual, no el ABM de archivos anexos.
- Estado: Resuelto

### Tema 2: Activación de `Copiar CUPS de otro Programa` en Definición de Cobertura

- Solicitud o contexto: John preguntó cómo activar el botón `Copiar CUPS de otro Programa` en la ventana `Definición de Cobertura`.
- Respuesta o acciones del agente: Se explicó la condición funcional detectada en el código de la ventana: debe existir un plan/programa seleccionado, vigente y sin cobertura CUPS cargada.
- Resultado: Se indicó que el botón queda deshabilitado si no hay plan seleccionado, si el plan está dado de baja o si ya tiene cobertura ambulatoria o de internación. Para habilitarlo, debe seleccionarse un programa vigente con `plan_codi` y sin cobertura cargada; al presionarlo abre `wr_plan_graba_desde` para copiar la cobertura desde otro programa.
- Estado: Resuelto

### Tema 3: Creación de un programa nuevo para cargar cobertura

- Solicitud o contexto: John preguntó, en la misma ventana `Definición de Cobertura`, cómo crear un programa nuevo para poder cargarle cobertura.
- Respuesta o acciones del agente: Se aclaró que el programa no se crea escribiendo directamente en el combo `Programa`, sino usando el botón `Activar`, que abre la ventana `Activación de Programa` (`wr_plan`).
- Resultado: Se detalló que debe cargar como mínimo código de programa nuevo, descripción, grupo, edad mínima, edad máxima y tipo de programa. Al guardar, el sistema registra el programa en `PLANES` mediante `pru_planes` e inicializa un registro base en `COBER_VARIOS` mediante `pru_cober_varios`. Luego vuelve a `Definición de Cobertura` con el programa seleccionado y, si no tiene CUPS cargados, queda habilitado para crear cobertura o copiar CUPS desde otro programa.
- Estado: Resuelto
