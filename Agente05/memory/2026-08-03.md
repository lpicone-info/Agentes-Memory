# Memoria 2026-08-03

## Contexto de trabajo
- Usuario: Bárbara.
- Repositorio analizado: `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`.
- Modo de trabajo solicitado en los análisis: revisar y explicar por chat, sin modificar archivos salvo indicación explícita.

## Hallazgos técnicos del día
- En backend, caso `SOLICITUDES_AUTO_NOTIF_PLANT_PROC`: el error `Cannot insert explicit value for identity column... SOLICITUDES_AUTO_NOTIF_PLANT_PROC` se explicó porque en la BD la columna `id` estaba como `IDENTITY`, pero el script correcto del repo define `id SMALLINT NOT NULL`. El `data_modification` inserta IDs explícitos y es coherente con `SMALLINT NOT NULL`; la corrección correcta es asegurar/recrear la tabla sin `IDENTITY`, no agregar `IDENTITY_INSERT` como solución principal.
- En `exe_presmed`, ventana `wp_bandeja_solicitudes`, búsqueda/ordenamiento por columna: `N° Solicitud` (`soli_auto`) y `Tipo Afiliado` (`especial`) no aparecían en `wr_find` porque sus columnas tenían `visible` dinámico (`1~tIF(...)`) y el flujo filtraba columnas por visibilidad exacta. La primera propuesta con `LEFT(ls_vis,1)` no alcanzó porque `Describe(col.visible)` podía devolver vacío en runtime. La solución que funcionó fue aceptar la columna mirando también el header asociado `columna_t`: si el detalle no informa visible, validar que exista texto de header y que `columna_t.visible` empiece con `1`.
- En `wp_bandeja_solicitudes`, solapa Autorizaciones: al consultar una autorización dada de baja desde la bandeja, el flujo usa `iuo_auto.of_autoriz(...)` y luego `of_consultar_auto`/`of_consultar_auto_editable`, que ante código 87 muestran solo `Autorización dada de baja` y retornan. Desde menú de Autorizaciones, el comportamiento deseado está en `w_clase_auto.ue_getauto`, que permite abrir la autorización y ofrecer ficha de auditoría. Propuesta sin modificar: en `dw_auto.buttonclicked` de `wp_bandeja_solicitudes`, después de recuperar `lds_auto`, si `baja_fecha` no es null, abrir la ventana correspondiente con `OPENWITHPARM(..., lds_auto)` según `origen`/`tipo`, evitando pasar por `of_consultar_auto`; dejar flujo actual para autorizaciones vigentes. No se necesitó consultar BD InstallQA.

## 2026-08-03 18:11 ART - Tarea #29489 / Notificaciones solicitudes auto

- Preferencia operativa explícita de Wilmer/whiguera: no realizar modificaciones directas en el entorno ni hacer commits; Lisa solo propone y muestra cambios por pantalla o archivo. La ejecución de modificaciones y commits queda a cargo exclusivo de whiguera.
- Preferencia operativa explícita de Wilmer/whiguera: no asumir que un paso fue comprendido o cerrado automáticamente sin su visto bueno explícito.
- Tarea #29489 retomada desde memoria 2026-07-31. Se trabajó sobre notificación de solicitud `U` anulada para procesos `6`, `14` y `15`.
- Para anulación, funcional confirmó que si `notif_plant_proc_id` no está parametrizado, no debe mostrar error ni bloquear; simplemente no se envía notificación al afiliado. Si está parametrizado, se dispara el proceso configurado.
- `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION.notif_plant_proc_id` quedó como fuente fuerte para decidir procesos afiliado `14/15`; no depender de `soli_tipo` ni de `notif_afi` para dispararlos.
- `pru_solicitudes_auto_motivos_devolucion`: Wilmer indicó que ya lo tenía cuadrado en local. Validado conceptualmente: parámetro `@prm_notif_plant_proc_id SMALLINT = NULL`, persistencia en UPDATE e INSERT.
- `pru_solicitudes_auto_anula_bandeja`: validado con SP local. Lógica final correcta: proceso `6` cuando `notif_prestad = 'S'` y `tipo_usuario = 'P'`; proceso `14/15` cuando `notif_plant_proc_id IS NOT NULL` y `tipo_usuario IN ('P','A','E')`; sin RAISERROR por plantilla NULL; sin variable vieja `@var_notif_afiliado_id`.
- `pru_solicitudes_auto_anula_total`: validado con SP local. Debe tener la misma lógica que bandeja en ambos caminos: anulación directa y camino `@var_soli_tipo = 'A'` después de confirmar `soli_esta_final = 'U'`. Usar `notif_prestad` y `notif_plant_proc_id`; eliminar lógica vieja con CASE hardcodeado e IDs placeholders.
- Para anulación `U`, pendientes sugeridos antes de cierre: compilar SPs en local; probar usuario `P` con `notif_prestad = S` y `notif_plant_proc_id = 14/15`; probar usuario `A/E` con `notif_plant_proc_id = 14/15`; probar motivo con `notif_plant_proc_id NULL` para confirmar que no falla ni notifica afiliado.
- Se inició análisis de notificación de solicitud `F` finalizada. Procesos del bloque FINALIZADA confirmados en catálogo: `5` prestador; afiliado `8`, `9`, `10`, `11`, `12`, `13`, `23`. Wilmer quiere iniciar con la pareja: proceso `5` PRESTADOR FINALIZADA y proceso `8` AFILIADO FINALIZADA - todas las autorizaciones autorizada.
- Punto crítico identificado para procesos afiliado finalizada: `EXEC dbo.afu_mail_envio_send @prm_mail_id = @out_mail_id` solo marca el mail para envío (`MAIL_ENVIO.status = 'NOE'`); no adjunta archivos por sí solo. Para adjuntar órdenes autorizadas, patrón visto: crear mail (`afi_mail_envio`), destinatario (`afi_mail_to`), insertar autorizaciones relacionadas en `MAIL_AUTORIZACIONES`, marcar `MAIL_ENVIO.genera_impresion_auto = 'S'`, luego llamar `afu_mail_envio_send`.
- Para proceso `8`, `pri_solicitudes_auto_notif` ya toma email afiliado desde `SOLICITUDES_AUTO.email_afi_1/email_afi_2`, cumpliendo “correo ingresado en interfaz”. Falta revisar/agregar adjunto de órdenes autorizadas dentro del SP común antes de `afu_mail_envio_send`.
- SP central que calcula estado final: `dbo.prs_solicitudes_auto_esta_actual`; calcula `F` cuando no quedan pendientes, pero no actualiza `SOLICITUDES_AUTO`.
- SPs ubicados que persisten estado `F` FINALIZADA: `dbo.pru_fin_gestion_solicitudes_auto`, `dbo.pru_genera_auto_precarga`, `dbo.pru_registra_solicitudes_auto`, `dbo.prs_valida_of_auto_remotas`. No incluir `pru_solicitudes_auto_soli_esta` para finalizada, porque maneja `A/P/N/U`.
- Patrón propuesto para proceso `5`: antes del cambio leer `soli_esta` anterior y `tipo_usuario`; después del UPDATE que deja `@var_soli_esta = 'F'`, ejecutar `pri_solicitudes_auto_notif @prm_id = 5` solo si estado anterior no era `F`, estado nuevo es `F`, y `tipo_usuario = 'P'`.
- Siguiente paso conversacional previsto: mostrar con detalle “como está / como debe quedar” en `pru_fin_gestion_solicitudes_auto` para agregar primero el llamado al proceso `5` PRESTADOR FINALIZADA, sin editar archivos directamente.
