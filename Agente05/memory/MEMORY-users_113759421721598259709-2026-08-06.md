# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Barbara Kimey Melendres Gonzalez
- id_googlechat: users/113759421721598259709
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/3roilyaaaae

## Resumen de la actividad

### Tema 1: Mensaje de autorización de Servicios de Salud en WebPos

- Solicitud o contexto: Barbara pidió analizar en `bgonzalez-Version84-Modelo2Gis`, dentro de `WebPos`, el mensaje `Usted no está autorizado para solicitar autorizaciones de Servicios de Salud`. Indicó usar el profile `PruebasCoreINFOMEDICAL` si hacía falta y pidió solo análisis, sin modificaciones.
- Respuesta o acciones del agente: Se revisó el flujo desde `login_user.php`, `usuario.php`, `ars_usuario_valido.sql` y `ars_prestador_vigente.sql`. También se consultó la base en modo lectura.
- Resultado: Se determinó que el mensaje no nace como validación JavaScript del front, sino como máscara funcional en `ars_prestador_vigente.sql`. El procedimiento reemplaza distintas causas internas por el mensaje genérico que ve el usuario.
- Causas posibles identificadas: prestador inexistente, retirado, inhabilitado, sin lugares de atención, sin entidad GIS o sin lugares vigentes con convenio, plan, prestación y valores vigentes.
- Estado: Resuelto.

### Tema 2: Diagnóstico puntual para NIT 52430421

- Solicitud o contexto: Barbara informó el CUIT/NIT `52430421` para diagnosticar la causa concreta del mensaje.
- Respuesta o acciones del agente: Se consultó `PruebasCoreINFOMEDICAL` en modo lectura y se cruzó contra la lógica de `ars_prestador_vigente.sql`.
- Resultado: La causa real fue `Prestador sin lugares de atención`.
- Evidencia: El prestador existe como `2708`, `VARGAS NATALIA`; no estaba retirado al día del análisis según el procedimiento, pero su único lugar de atención tenía `baja_fecha = 2026-08-15`. Como el SP exige `PRESTAD_LUGARES.baja_fecha IS NULL`, el lugar no pasa la validación aunque la baja sea futura.
- Estado: Resuelto.

### Tema 3: Solicitud 25920060 y ausencia de auditoría de solicitud

- Solicitud o contexto: Barbara retomó el caso de la solicitud `25920060` en `InstallQA`. La solicitud estaba en estado `V`; se ingresó como auditor, se rechazó la autorización y no apareció registro en `SOLICITUDES_AUTO_AUDITORIA`. Pidió identificar los stored procedures involucrados y dónde podría ir el cambio, solo análisis.
- Respuesta o acciones del agente: Se revisó el flujo en `WebPos`, procedimientos versionados y datos en `InstallQA`.
- Resultado: Se identificó como candidato principal `prs_valida_of_auto_remotas.sql`, bloque de supervisión con `@var_supervision = 'S'`. Ese bloque detecta autorizaciones rechazadas, actualiza `SOLICITUDES_AUTO.soli_esta = 'G'` y `audit_fecha`, pero no inserta en `SOLICITUDES_AUTO_AUDITORIA`.
- Procedimientos relacionados: `pri_solicitudes_auto_auditoria` como utilitario de inserción; `pru_solicitudes_auto_resuelta_auditoria` y `pru_asignacion_soli_auto` como candidatos conceptuales existentes en base pero no aparentes para el caso; `prs_solicitudes_auto_autorizaciones_auditoria` solo consulta `AUTO_AUDITORIA`.
- Estado: Resuelto.

### Tema 4: Caso puntual 25920059 en InstallQA

- Solicitud o contexto: Barbara generó un nuevo caso con solicitud `25920059`: responsable gestión `sa`, responsable auditoría `csiafas`, estado `Validacion de Auditoria` y dos autorizaciones en `Revision por Auditoria`. Luego ingresó como auditora, rechazó autorizaciones, la solicitud volvió a `G`, los responsables se mantuvieron y no se visualizó registro en `SOLICITUDES_AUTO_AUDITORIA`.
- Respuesta o acciones del agente: Se consultó `InstallQA` solo en lectura y se cruzó con PowerBuilder y SQL versionado.
- Resultado de datos: `SOLICITUDES_AUTO` quedó en `G`; `responsable_gestion` siguió `sa`; `responsable_auditoria` siguió `csiafas`; `audit_fecha` quedó del 06/08/2026; las autorizaciones `52-22596479` y `52-22596480` quedaron en `R` con motivo `ORDEN GRABADA DOBLE`; `AUTO_AUDITORIA` sí registró el rechazo, pero `SOLICITUDES_AUTO_AUDITORIA` no tuvo registros nuevos.
- Procedimientos involucrados: `w_clase_auto.srw` llama a `uo_nv_valida_auto.sru`; `uo_nv_valida_auto.sru` ejecuta `dbo.prs_valida_of_auto_remotas`; ese SP actualiza `SOLICITUDES_AUTO` a `G`; `pri_auto_auditoria` registra cambios de autorización; `pri_solicitudes_auto_auditoria` sería el utilitario para registrar auditoría de solicitud.
- Conclusión: El ajuste más directo debería evaluarse en `prs_valida_of_auto_remotas.sql`, inmediatamente después del `UPDATE SOLICITUDES_AUTO`, condicionado al cambio de estado real de la solicitud desde `V`.
- Estado: Resuelto.

### Tema 5: Impacto propuesto para auditar cambio de estado en `prs_valida_of_auto_remotas`

- Solicitud o contexto: Barbara pidió mostrar el impacto de modificación a realizar, sin aplicarlo.
- Respuesta o acciones del agente: Se propuso inicialmente auditar cuando la solicitud venía de `V`, volvía a `G` y la autorización procesada quedaba en `R`. Luego Barbara observó que convenía basarse en el estado final calculado por el SP, no repetir un `SELECT EXISTS`.
- Resultado ajustado: Se propuso auditar cuando `@var_cod_error = 0`, `@var_soli_esta_anterior = 'V'` y `@var_soli_esta IN ('G', 'F')`. La actividad sugerida se determina con `CASE`: de `Validacion Auditoria` a `En Gestion` cuando queda en `G`, o de `Validacion Auditoria` a `Finalizada` cuando queda en `F`.
- Criterio acordado: Registrar cambio de estado de solicitud para ambos resultados posibles del bloque (`G` y `F`), evitando duplicados al exigir que el estado anterior sea `V`.
- Estado: En curso.

### Tema 6: Ventana `wp_roles` y error de permisos al abrir

- Solicitud o contexto: Barbara preguntó qué SPs forman parte de la ventana `wp_roles` en `bgonzalez-Version84-Modelo2Gis`, porque tenía un error de permisos. Indicó usar `InstallQA` si hacía falta.
- Respuesta o acciones del agente: Se revisaron `wp_roles.srw`, `d_lista_roles.srd`, `de_roles.srd`, `dddw_roles`, clases base y permisos en `InstallQA`.
- Resultado inicial: Los SPs directos son `dbo.prs_roles` para recuperar roles y `dbo.pru_roles` para grabar. Por herencia de ABM intervienen `dbo.prs_titulo_ventana`, `dbo.afi_audit_abm_cab`, `dbo.afi_audit_abm_det`, `dbo.afs_audit_abm` y `dbo.sp_indice_pk`.
- Diagnóstico puntual: El error ocurría en el `open`, en `dw_lista.retrieve(ls_null)`. Se validó que `EXEC dbo.prs_roles @prm_usuari = NULL` y `EXEC dbo.prs_roles` funcionaban en `InstallQA`, y que permisos sobre `prs_roles` y `ZZ_ROLES` estaban disponibles.
- Conclusión: Si desde base no fallaba, el problema probable era entorno PowerBuilder, PBL/objeto DataWindow desactualizado o `dddw_roles` embebido, no un permiso real en `InstallQA`.
- Resultado final informado por Barbara: Con `fullbuild` se solucionó. Quedó trazado que el problema era objeto compilado/desactualizado en PowerBuilder; al recompilar `wp_roles`, `d_lista_roles` y el `dddw_roles` asociado dejó de fallar el `retrieve`.
- Estado: Resuelto.
