# Memoria curada diaria

- Fecha: 2026-08-06
- Usuario: Constantino Siafas
- id_googlechat: users/102676121336459112955
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/hpdxvyaaaae

## Resumen de la actividad

### Tema 1: Tablas que almacenan proceso, asunto y texto en parametrización de notificaciones

- Solicitud o contexto: Constantino indicó usar la rama SVN `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version84-Modelo2Gis`, módulo PRESMED y base `INSTALLQA`. Pidió ubicar el menú `Configuración\Solicitudes\Solicitudes\Parametrización de notificaciones de solicitudes`, ubicar el botón `ACTIVAR` y decir en qué tablas se almacenan los datos de Proceso, Asunto y Texto.
- Respuesta o acciones del agente: Se validó el repositorio local asociado a la rama, se ubicó el punto de menú en `m_main.srm` y la ventana `wp_soli_notificacion_plant.srw`. Se revisó la definición de la ventana, DataWindows y SP de grabado; también se validó en `InstallQA` la existencia de tablas y columnas.
- Resultado:
  - La ventana del menú es `wp_soli_notificacion_plant`.
  - `Proceso` se muestra desde `dbo.SOLICITUDES_AUTO_NOTIF_PLANT_PROC`, columna `deno`; el identificador seleccionado queda como `id`.
  - `Asunto` se almacena en `dbo.SOLICITUDES_AUTO_NOTIF_PLANT.subject`.
  - `Texto` se almacena en `dbo.SOLICITUDES_AUTO_NOTIF_PLANT.body`.
- Evidencia funcional: La ventana define `is_tabla = 'SOLICITUDES_AUTO_NOTIF_PLANT'` y graba mediante `dbo.pru_solicitudes_auto_notif_plant`, que actualiza o inserta `subject` y `body`. El combo de proceso usa `dddw_solicitudes_auto_notif_plant_proc`, que recupera `id` y `deno` desde `SOLICITUDES_AUTO_NOTIF_PLANT_PROC`.
- Estado: Resuelto.

### Tema 2: Origen de correos del afiliado en `noambu_auto.php`

- Solicitud o contexto: Constantino indicó usar la misma rama SVN, módulo GIS, base `INSTALLQA` y archivo `noambu_auto.php`. Pidió ubicar la sección `Información Afiliado`, la etiqueta `correo electronico del afiliado` y la etiqueta `correo electrónico 2 del afiliado`, e indicar de qué tabla y campo de base se completan.
- Respuesta o acciones del agente: Se revisó el flujo de `noambu_auto.php`, `afiliado.js` y el SP `dbo.ars_afiliado`. Se validaron en `InstallQA` las tablas y campos involucrados.
- Resultado para `Correo Electrónico del afiliado`: Se completa desde `COALESCE(MT.email_to, RUE.email)`. La prioridad es `MAIL_TO.email_to`, asociado al último `SOLICITUDES_AUTO.mail_id_afi_mail` vigente del afiliado; si no existe, usa `REGISTRO_UNICO_EMAILS.email`, asociado por `AFILIADOS.codigo_unico_persona`.
- Resultado para `Correo Electrónico 2 del afiliado`: Se completa desde `COALESCE(M2.email_to, RUE3.email, RUE2.email)`. La prioridad es un segundo `MAIL_TO.email_to` del mismo `mail_id`; si no existe, un segundo `REGISTRO_UNICO_EMAILS.email` vigente por tipo distinto; si tampoco existe, un email de vigencia anterior en `REGISTRO_UNICO_EMAILS.email`.
- Evidencia técnica: `afiliado.js` pinta `Correo Electrónico` con `data.email` y `Correo Electrónico 2` con `data.email2`; `ars_afiliado.sql` arma ambos valores.
- Estado: Resuelto.
