# Memoria de conversación — Wilmer Higuera

## Datos

- **Fecha:** 21/09/2026
- **Usuario:** Wilmer Higuera
- **ID Google Chat:** `users/113120678211994207734`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 18
- **Inicio:** 10:01:44
- **Fin:** 18:41:51

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/qzugpyaaaae`

## Archivos revisados

- `chat.history` (`4da5af4c-9cc9-48fb-8a7c-ec3c892d00a6`)

---

## Transcripción completa

### [21/09/2026 10:01:44] Wilmer Higuera

Buenos días Lisa

---

### [21/09/2026 10:01:56] Agente

Buenos días, Wilmer. Estoy disponible para continuar.

---

### [21/09/2026 10:03:47] Wilmer Higuera

Lisa, ten en cuenta estos puntos:
## Cosas que hacer
1. Leer e interpretar estas preferencias (especialmente el bloque de tareas activas y el documento funcional provisto) antes de continuar con las tareas habituales.
2. Adoptar un rol puramente consultivo y de guía técnico: proponer soluciones precisas basadas en el repositorio asignado y en los requerimientos dados.
3. Actualizar (update) el repositorio exclusivo para esta tarea a pedido por whiguera segun la rama que se este trabajando con whiguera.

## Cosas que no hacer
1. NO realizar modificaciones directas en en los repositorios ni bases de datos, ni hacer Commits. El agente puede crear y modificar archivos únicamente dentro de A:\whiguera\Tarea #23986\Tarea #29489, cuando estén relacionados con la Tarea #29489 y se solicite por parte de whiguera.El agente solo propone y muestra los cambios por pantalla o archivo; la ejecución de modificaciones y commits es responsabilidad exclusiva de whiguera.
2. NO asumir que un paso se ha comprendido o cerrado de forma automática sin el visto bueno de whiguera.

---

### [21/09/2026 10:03:55] Agente

Wilmer, revisaré la preferencia registrada para confirmar que estos criterios queden alineados con la forma de trabajo de la Tarea #29489.

---

### [21/09/2026 10:04:12] Agente

Wilmer, entendido. Aplicaré estos criterios para la Tarea **#29489**:

- Rol exclusivamente consultivo y de guía técnica.
- Trabajo únicamente sobre `whiguera-Version84-Modelo2Gis`.
- Actualización del repositorio solo cuando la solicites explícitamente.
- Sin modificaciones directas en repositorios o bases de datos.
- Sin commits.
- Los cambios se mostrarán para que los ejecutes manualmente.
- No consideraré ningún paso cerrado sin tu confirmación explícita.

Estos criterios ya constan en la preferencia registrada para tu usuario.  
Fuente: [memoria del 18/09/2026](/home/desarrollo-ia-05/.openclaw/workspace/memory/MEMORY-users_113120678211994207734-2026-09-18.md:43).

---

### [21/09/2026 10:08:44] Wilmer Higuera

Lisa en la ubicación A:\whiguera, esta el documento R26486-EF_V3.doc, por favor analiza cuidadosamente y me indicas el punto GAP 008 – Ajuste Proceso de asignación de solicitudes - Gestión de Autorizaciones Negadas que pide el funcional que debemos hacer, me das las indicaciones con todos los detalles por favor

---

### [21/09/2026 10:08:56] Agente

Wilmer, validaré primero que el documento `R26486-EF_V3.doc` esté disponible en la ubicación indicada y luego analizaré específicamente el GAP 008, separando requerimiento funcional, impacto técnico y pasos de implementación.

---

### [21/09/2026 10:15:51] Agente

Wilmer, ubiqué el documento en:

[R26486-EF_V3.doc](/mnt/Agentes/whiguera/R26486-EF_V3.doc)

También existe una copia en la carpeta de la tarea:

[R26486-EF_V3.doc](/mnt/Agentes/whiguera/Tarea%20%2323986/Tarea%20%2329489/R26486-EF_V3.doc)

Analicé el contenido del **GAP 008 – Ajuste Proceso de asignación de solicitudes – Gestión de Autorizaciones Negadas** y lo contrasté con el repositorio `whiguera-Version84-Modelo2Gis`.

## 1. Objetivo funcional

Cuando un auditor médico gestione una autorización y se cumpla todo lo siguiente:

1. La autorización queda en estado **Negado**.
2. Se registra el motivo de negación.
3. La carta de negación queda **Finalizada**.
4. El afiliado, programa y tipo de solicitud cumplen las reglas de complementariedad.
5. Existe un usuario elegible con rol parametrizado para **Gestión Complementariedad**.

La solicitud debe salir del circuito de auditoría y ser reasignada a la **Bandeja de Solicitudes**, asignándose como `responsable_gestion` a un ejecutivo habilitado para gestionar negaciones por complementariedad.

La solicitud **no debe continuar visible en la bandeja de auditoría**.

Si alguna condición no se cumple, debe continuar el flujo normal vigente.

---

# 2. Condiciones obligatorias del GAP 008

La reasignación especial solamente aplica cuando se cumple la combinación completa:

| Condición | Validación |
|---|---|
| Programa | `PLANES.complementariedad = 'S'` |
| EPS | `OBRAS_SOCIALES.complementariedad IN ('O','L')` |
| Tipo de solicitud | `SOLICITUDES_AUTO_TIPOS.complementariedad = 'S'` |
| Autorización | `AUTORIZACIONES.esta = 'N'` |
| Carta | `CARTA_NEGACION.final_fecha IS NOT NULL` y `baja_fecha IS NULL` |
| Estado anterior | La solicitud venía de auditoría, normalmente estado `V` |
| Usuario destino | Rol con `ZZ_ROLES.complementariedad = 'S'` |
| Ubicación usuario | Regional y sucursal compatibles con la solicitud |
| Disponibilidad | Usuario vigente, activo, conectado y con capacidad |
| Permisos | El rol puede atender el tipo de solicitud |

### Aclaración sobre EPS

El documento menciona `EPS.COMPLEMENTARIEDAD`, pero en este repositorio la entidad física equivalente es:

```text
OBRAS_SOCIALES.complementariedad
```

Esto coincide con el código existente de `prs_email_sol_auto`, donde se consulta:

```text
OBRAS_SOCIALES OS
PLANES PL
SOLICITUDES_AUTO_TIPOS SAT
```

---

# 3. Resultado funcional esperado

Cuando las condiciones aplican:

```text
Solicitud en auditoría
        |
        | Autorización negada
        | Carta de negación finalizada
        | Complementariedad aplicable
        v
Solicitud pasa a gestión
        |
        | responsable_gestion = usuario complementario
        | responsable_auditoria = NULL
        v
Visible en Bandeja de Solicitudes
No visible en Bandeja de Auditoría
```

El cambio funcional principal debe ser:

```text
SOLICITUDES_AUTO.soli_esta = 'G'
SOLICITUDES_AUTO.responsable_gestion = usuario elegido
SOLICITUDES_AUTO.responsable_auditoria = NULL
```

Además, debe registrarse:

- auditoría de la reasignación;
- cierre de la actividad anterior del workflow;
- nueva actividad de workflow;
- usuario responsable seleccionado;
- motivo o resultado de la asignación.

---

# 4. Relación con GAP 007

El GAP 007 define la parametrización que el GAP 008 debe consumir.

El documento indica que debe existir el rol:

```text
EJECUTIVO EXPERIENCIA NEGACIONES
```

y una marca:

```text
GESTIÓN COMPLEMENTARIEDAD = SI / NO
```

En el repositorio ya existe el campo:

```text
ZZ_ROLES.complementariedad
```

y también existe el procedimiento:

```text
pru_roles
```

que recibe:

```text
@prm_complementariedad
```

El script de datos contiene el rol 21:

```text
backend/data_modifications/zz_roles.sql
```

Sin embargo, encontré dos diferencias que deben revisarse:

### Diferencia 1: nombre del rol

El documento usa:

```text
Ejecutivo Experiencia Negaciones
```

El script usa:

```text
Ejecutivo Experiencias Negaciones
```

Debe definirse un único nombre oficial para evitar inconsistencias entre parametrización, consultas y documentación.

### Diferencia 2: parametrización actual en `InstallQA`

En la base, el rol 21 actualmente aparece como:

```text
rol                  = 21
deno                 = Ejecutivo Experiencias Negaciones
complementariedad    = N
es_ejecutivo         = N
recibe_gestion       = N
soli_diag            = S
soli_ciru            = N
soli_hospi           = N
soli_no_tipi         = N
```

Con esos valores, el rol no puede funcionar como destino normal de gestión porque:

- `complementariedad = 'N'`;
- `es_ejecutivo = 'N'`;
- `recibe_gestion = 'N'`;
- solamente tiene habilitado el tipo de solicitud `D`.

Esto no debe modificarse todavía sin confirmar la parametrización funcional definitiva.

---

# 5. Situación actual del proceso de asignación

El procedimiento principal existente es:

[pru_proceso_asignacion_masiva.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_proceso_asignacion_masiva.sql)

Actualmente:

- para gestión busca solicitudes en estados `C` y `R`;
- para auditoría busca estados `V` y `S`;
- selecciona usuarios con `ZZ_ROLES.es_ejecutivo = 'S'` o `ZZ_ROLES.es_auditor = 'S'`;
- valida regional, sucursal, sede, carga y conexión;
- no filtra usuarios por `ZZ_ROLES.complementariedad = 'S'`;
- no detecta autorización negada con carta finalizada;
- no distingue el caso especial de negación por complementariedad.

Por ese motivo, el GAP 008 **no queda implementado simplemente agregando una llamada genérica** a `pru_proceso_asignacion_masiva`.

Además, el procedimiento actual podría enviar la solicitud al circuito de auditoría nuevamente porque el estado `V` pertenece a la rama de auditoría.

---

# 6. Punto correcto para ejecutar la lógica

La lógica debe ejecutarse después de que se confirme la finalización de la carta de negación.

El procedimiento que actualiza la carta es:

[pru_carta_negacion.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_carta_negacion.sql)

La condición técnica de carta finalizada es:

```text
CARTA_NEGACION.final_fecha IS NOT NULL
AND CARTA_NEGACION.baja_fecha IS NULL
```

El procedimiento de recuperación de adjuntos ya usa ese mismo criterio:

[prs_solicitudes_auto_adjuntos_finalizada.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/prs_solicitudes_auto_adjuntos_finalizada.sql:35)

Por lo tanto, el punto de integración recomendado es:

1. Guardar la autorización como `N`.
2. Registrar motivo de negación.
3. Finalizar la carta.
4. Confirmar que `final_fecha` quedó grabada.
5. Ejecutar la validación GAP 008.
6. Si aplica, reasignar a gestión complementaria.
7. Si no aplica, continuar con el proceso normal.

No debe ejecutarse antes de finalizar la carta, porque en ese momento todavía no se puede demostrar que la condición funcional está completa.

---

# 7. Diseño técnico recomendado

## Paso 1: crear una validación reutilizable

Se recomienda crear un procedimiento específico, por ejemplo:

```text
pru_solicitud_complementariedad_reasigna
```

El procedimiento debería recibir:

```text
@prm_soli_auto
@prm_usuario_proceso
```

y devolver:

```text
@prm_aplica
@prm_usuario_asignado
@prm_motivo_no_aplica
```

La salida debería distinguir como mínimo:

```text
APLICA
NO APLICA POR CONDICIONES FUNCIONALES
APLICA PERO NO HAY USUARIO DISPONIBLE
ERROR TÉCNICO
```

## Paso 2: validar la solicitud

Consultar:

```text
SOLICITUDES_AUTO
```

Validar:

```text
baja_fecha IS NULL
soli_esta = 'V'
```

El estado anterior debe conservarse antes de actualizarlo. No conviene determinarlo después del cambio.

## Paso 3: identificar las autorizaciones vinculadas

Usar la relación:

```text
SOLICITUDES_AUTO_AUTORIZACIONES
    -> AUTORIZACIONES
```

Relación:

```text
SOLICITUDES_AUTO_AUTORIZACIONES.sucur = AUTORIZACIONES.sucur
SOLICITUDES_AUTO_AUTORIZACIONES.auto  = AUTORIZACIONES.auto
```

Filtra
...(truncated)...

---

### [21/09/2026 16:05:25] Wilmer Higuera

Espera Lisa, analiza en el mismo documento el punto GAP 004 – Ajuste Bandeja de AUDITORIA - Gestión de Autorizaciones Negadas, este punto es complementario con el punto GAP 008 – Ajuste Proceso de asignación de solicitudes - Gestión de Autorizaciones Negadas

---

### [21/09/2026 16:05:41] Agente

Wilmer, revisaré completo el GAP 004 y lo cruzaré con el GAP 008 para corregir o complementar el flujo técnico anterior, especialmente el disparador desde Auditoría, el estado de la solicitud y la supresión de notificaciones.

---

### [21/09/2026 16:11:11] Agente

Wilmer, el **GAP 004 es complementario y previo al GAP 008**. Al leer nuevamente el documento, encontré una precisión importante que ajusta el análisis anterior.

Documento revisado:

[R26486-EF_V3.doc](/mnt/Agentes/whiguera/R26486-EF_V3.doc)

## 1. Qué resuelve cada GAP

| GAP | Responsabilidad |
|---|---|
| **GAP 004** | Detectar la condición de negación por complementariedad desde la bandeja de Auditoría, impedir la notificación inmediata, marcar la solicitud y devolverla a Gestión |
| **GAP 008** | Definir a qué usuario de Gestión se reasigna la solicitud y aplicar los criterios de selección del usuario |
| **GAP 009** | Notificar posteriormente al afiliado cuando la gestión de complementariedad finalice |

Por lo tanto, el flujo completo es:

```text
Auditoría médica
      |
      | Autorización NEGADA
      | Carta de negación FINALIZADA
      v
GAP 004
      |
      | No notificar todavía
      | Marcar complementariedad
      | Cambiar solicitud a Gestión
      v
GAP 008
      |
      | Seleccionar usuario complementario
      | Reasignar solicitud
      v
Gestión de complementariedad
      |
      | Registrar número de caso EPS
      | Finalizar gestión
      v
GAP 009
      |
      | Notificar al afiliado
```

---

# 2. Qué exige exactamente el GAP 004

El documento indica que, desde la **Bandeja de Auditoría**, cuando la solicitud esté en estado:

```text
EN VALIDACIÓN DE AUDITORÍA
```

y exista una autorización con:

```text
Estado = NEGADO
Carta de negación = FINALIZADA
```

se deben validar también:

```text
EPS.COMPLEMENTARIEDAD IN ('O', 'L')
PLANES.COMPLEMENTARIEDAD = 'S'
SOLICITUDES_AUTO_TIPOS.COMPLEMENTARIEDAD = 'S'
```

Cuando las cuatro condiciones se cumplen, GAP 004 exige realizar tres acciones:

### Acción 1: no ejecutar notificación inmediata

No debe ejecutarse la notificación normal de negación definida por RM 23986 / Mantis 7076.

Esto es importante para la implementación actual de `w_clase_auto`, porque el flujo que venimos trabajando llama a:

```text
pru_solicitudes_auto_notif_finalizada
```

después de `ue_carta_negacion`.

Para los casos de complementariedad, esa llamada debe omitirse o finalizar correctamente sin crear notificación.

### Acción 2: reasignar la solicitud a Gestión

El estado debe cambiar a:

```text
SOLICITUDES_AUTO.soli_esta = 'G'
```

El documento denomina este estado:

```text
EN GESTIÓN
```

### Acción 3: marcar la solicitud

El documento exige una marca a nivel de solicitud:

```text
SOLICITUDES_AUTO.complementariedad = 'S'
```

Esta es una diferencia importante respecto del análisis anterior: el GAP 004 no solamente utiliza las marcas de programa, EPS y tipo de solicitud. También exige conservar el resultado en la propia solicitud.

---

# 3. Hallazgo de modelo de datos

Actualmente, en el repositorio:

[SOLICITUDES_AUTO.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/tables/SOLICITUDES_AUTO.sql)

no existe el campo:

```text
complementariedad
```

También verifiqué `InstallQA` y la columna todavía no existe en `SOLICITUDES_AUTO`.

Por lo tanto, antes de poder implementar completamente GAP 004/GAP 008, falta definir y ejecutar manualmente una modificación de modelo:

```text
SOLICITUDES_AUTO.complementariedad
```

El documento indica:

```text
Tipo: CHAR(1)
Valor por defecto: NULL
```

La decisión funcional pendiente es si se respetará literalmente `NULL` o si se utilizará:

```text
'N' = no aplica
'S' = aplica complementariedad
```

Para evitar ambigüedad en consultas futuras, técnicamente sería más seguro utilizar `N` como valor por defecto, pero esa decisión debe confirmarse porque el documento especifica `NULL`.

---

# 4. Qué significa “último ejecutivo responsable en gestión”

GAP 004 indica que la solicitud debe reasignarse al último ejecutivo que fue responsable de la gestión.

El documento propone obtenerlo desde:

```text
SOLICITUDES_AUTO_WORKFLOW
```

considerando:

```text
soli_auto = solicitud
soli_esta = 'G'
baja_fecha IS NULL
MAX(fecha_fin_activi)
```

La idea funcional es:

1. La solicitud estaba en Gestión.
2. Fue enviada a Auditoría.
3. El auditor negó una autorización.
4. Se finalizó la carta.
5. La solicitud vuelve a Gestión.
6. Debe conservar continuidad con el ejecutivo anterior.

Sin embargo, GAP 008 agrega una condición adicional: el usuario destino debe tener parametrizada la gestión de complementariedad.

Por eso existe una ambigüedad que debe resolverse antes de programar:

### Interpretación recomendada

Primero se obtiene el último responsable en gestión.

Luego se valida si ese usuario:

- sigue activo;
- pertenece al rol correcto;
- tiene `ZZ_ROLES.complementariedad = 'S'`;
- puede atender el tipo de solicitud;
- corresponde a la regional y sucursal;
- tiene capacidad disponible.

Si cumple, se reutiliza ese usuario.

Si no cumple, el proceso de asignación debe buscar otro usuario elegible con rol de complementariedad, conforme al GAP 008.

Esto permite cumplir simultáneamente:

- continuidad con el último ejecutivo;
- control de rol especial;
- control de regional y sucursal;
- control de disponibilidad.

---

# 5. Qué agrega GAP 008 sobre GAP 004

GAP 008 no reemplaza las acciones de GAP 004. Define cómo debe hacerse la asignación posterior.

Debe validar:

## Datos de la solicitud

```text
SOLICITUDES_AUTO.soli_auto
SOLICITUDES_AUTO.soli_tipo
SOLICITUDES_AUTO.sucur
SOLICITUDES_AUTO.soli_esta
SOLICITUDES_AUTO.responsable_gestion
SOLICITUDES_AUTO.responsable_auditoria
```

## Datos territoriales

La solicitud obtiene su regional y sucursal mediante:

```text
SOLICITUDES_AUTO.sucur
    -> SUCURSALES
    -> PARTIDOS
```

El usuario se valida mediante:

```text
ZZ_USUARI.liqui_ofi
ZZ_USUARI.sucursal
```

Debe existir correspondencia entre:

```text
PARTIDOS.regional = ZZ_USUARI.liqui_ofi
PARTIDOS.partido  = ZZ_USUARI.sucursal
```

## Datos del rol

El usuario debe relacionarse con:

```text
ZZ_USUARI.rol = ZZ_ROLES.rol
```

y cumplir:

```text
ZZ_ROLES.complementariedad = 'S'
```

Además, para que el proceso genérico de gestión lo considere, debe revisarse:

```text
ZZ_ROLES.es_ejecutivo = 'S'
ZZ_ROLES.recibe_gestion = 'S'
```

## Estado y disponibilidad

El usuario debe:

```text
ZZ_USUARI.baja_fecha IS NULL
GETDATE() BETWEEN ZZ_USUARI.vigdes AND ZZ_USUARI.vighas
```

No debe estar inactivado en:

```text
ZZ_USUARI_INACTIVACION
```

Y debe estar conectado a `prestaciones`, de acuerdo con la lógica vigente de:

[pru_proceso_asignacion_masiva.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_proceso_asignacion_masiva.sql:177)

---

# 6. Problema actual del asignador genérico

El procedimiento actual:

[pru_proceso_asignacion_masiva.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_proceso_asignacion_masiva.sql:139)

maneja:

```text
Gestión: C, R
Auditoría: V, S
```

Además:

- selecciona ejecutivos por `es_ejecutivo = 'S'`;
- selecciona auditores por `es_auditor = 'S'`;
- no filtra por `complementariedad = 'S'`;
- no busca cartas de negación finalizadas;
- no lee el último responsable de `SOLICITUDES_AUTO_WORKFLOW`;
- no marca `SOLICITUDES_AUTO.complementariedad`.

Por ese motivo, no conviene llamar directamente al asignador genérico cuando la solicitud está en estado `V`. Podría volver a tratarla como una solicitud de auditoría.

La solución debe ser una de estas dos:

### Opción recomendada

Crear un procedimiento especializado para la transición de complementariedad:

```text
pru_reasigna_solicitud_complementariedad
```

Este procedimiento:

1. valida GAP 004;
2. obtiene el último responsable de Gestión;
3. valida si puede reutilizarse;
4. busca usuario alternativo si es necesario;
5. actualiza estado y responsables;
6. marca complementariedad;
7. actualiza workflow;
8. registra auditoría.

### Opción alternativa

Agregar un modo explícito al asignador existente:

```text
@prm_modo_asignacion = 'COMPLEMENTARIEDAD'
...(truncated)...

---

### [21/09/2026 18:19:23] Wilmer Higuera

Bien, actualiza tu repositorio para que lo iguales  a mi repositorio local, después dame el paso a paso muy detallado de como realizamos esta implementación, ya la tabla SOLICITUDES_AUTO tiene el campo 
complementariedad

---

### [21/09/2026 18:19:50] Agente

Wilmer, primero revisaré el estado de la working copy y recuperaré el contexto técnico de GAP 004/GAP 008. Luego ejecutaré únicamente el `svn update` autorizado sobre `whiguera-Version84-Modelo2Gis`, sin commits, y validaré la revisión resultante antes de darte el paso a paso.

---

### [21/09/2026 18:24:47] Agente

Wilmer, actualicé la working copy con la rama SVN y validé la base.

## Estado de sincronización

- Repositorio: `whiguera-Version84-Modelo2Gis`
- Revisión local actual: **37863**
- Última revisión modificada: **37862**
- URL SVN validada: `Version84-Modelo2Gis`
- No se realizaron commits.
- No hay cambios locales pendientes; solo aparecen directorios externos ignorados por SVN.

Archivos actualizados desde tu repositorio local:

- `backend/tables/SOLICITUDES_AUTO.sql`
- `backend/procedures/prs_solicitudes_auto_oportunidad.sql`
- `backend/procedures/prs_solicitudes_auto_tiempo_oportunidad_bandeja.sql`
- `backend/procedures/prs_valida_of_auto_remotas.sql`
- `backend/procedures/pru_autorizaciones.sql`
- `exe_presmed/Source Infomedical/w_clase_auto.srw`
- `exe_presmed/Source Infomedical/uo_nv_soli_auto_notificacion.sru`

También confirmé en `InstallQA`:

```text
SOLICITUDES_AUTO.complementariedad
Tipo: CHAR(1)
Nullable: NO
Default: 'N'
```

El script actualizado está en:

[SOLICITUDES_AUTO.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/tables/SOLICITUDES_AUTO.sql:362)

---

# Implementación coordinada GAP 004 + GAP 008

## Paso 1. Confirmar el significado del campo

La columna debe manejarse así:

```text
'N' = la solicitud no está en gestión de complementariedad
'S' = la solicitud fue enviada a gestión de complementariedad
```

El campo no debe marcarse solamente porque exista una autorización negada. Debe marcarse únicamente cuando se cumplan todas las condiciones del GAP 004:

- autorización negada;
- carta de negación finalizada;
- EPS aplicable;
- programa aplicable;
- tipo de solicitud aplicable;
- solicitud proveniente de Auditoría.

---

## Paso 2. Identificar el punto de finalización de la carta

El objeto actual de carta es:

[wp_carta_negacion.srw](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/exe_presmed/Source%20Infomedical/wp_carta_negacion.srw)

El evento actual:

```text
ue_grabar
```

ejecuta:

```text
pru_carta_negacion
```

y guarda:

```text
CARTA_NEGACION.final_fecha
```

La carta queda finalizada cuando:

```text
final_fecha IS NOT NULL
AND baja_fecha IS NULL
```

El procedimiento de reasignación debe ejecutarse después de que `pru_carta_negacion` haya guardado la fecha de finalización, pero antes de confirmar definitivamente la transacción de la ventana.

El orden debe ser:

```text
Guardar carta
    ↓
Validar carta finalizada
    ↓
Evaluar GAP 004
    ↓
Ejecutar reasignación GAP 008 si corresponde
    ↓
Actualizar workflow y auditoría
    ↓
COMMIT
```

No debe ejecutarse antes de guardar la carta, porque todavía no existiría evidencia de que la carta está finalizada.

---

## Paso 3. Crear un procedimiento específico para la transición

Recomiendo crear un procedimiento independiente, por ejemplo:

```text
pru_reasigna_solicitud_complementariedad
```

No conviene modificar directamente el comportamiento general de:

```text
pru_proceso_asignacion_masiva
```

porque ese procedimiento atiende las asignaciones normales de Gestión y Auditoría.

El nuevo procedimiento debería recibir:

```text
@prm_soli_auto
@prm_sucur
@prm_auto
```

y devolver:

```text
@prm_aplica
@prm_usuario_asignado
@prm_motivo
```

Resultado esperado:

```text
@prm_aplica = 'S'
```

cuando la solicitud es enviada a complementariedad.

```text
@prm_aplica = 'N'
```

cuando simplemente no corresponde el flujo especial.

---

## Paso 4. Validar el estado original de la solicitud

Antes de realizar cualquier actualización, consultar:

```text
SOLICITUDES_AUTO
```

Validar:

```text
baja_fecha IS NULL
soli_esta = 'V'
```

El estado `V` representa la solicitud en validación de Auditoría.

El procedimiento debe conservar el estado original porque después de actualizarlo a `G` ya no sería posible determinar si realmente venía de Auditoría.

También debe evitar reasignar una solicitud que ya haya sido procesada por otro usuario o proceso.

---

## Paso 5. Obtener la autorización relacionada

Usar:

```text
SOLICITUDES_AUTO_AUTORIZACIONES
```

relacionada con:

```text
AUTORIZACIONES
```

por:

```text
sucur
auto
```

Validar que exista al menos una autorización vigente con:

```text
AUTORIZACIONES.esta = 'N'
AUTORIZACIONES.baja_fecha IS NULL
```

La solicitud no debe marcarse si la autorización está negada pero fue dada de baja.

---

## Paso 6. Validar la carta de negación

Relacionar:

```text
CARTA_NEGACION.sucur = AUTORIZACIONES.sucur
CARTA_NEGACION.auto  = AUTORIZACIONES.auto
```

La carta válida debe cumplir:

```text
CARTA_NEGACION.final_fecha IS NOT NULL
CARTA_NEGACION.baja_fecha IS NULL
```

La búsqueda debe tomar la carta vigente más reciente para evitar utilizar una carta histórica dada de baja.

Si la autorización está negada pero la carta todavía no está finalizada:

```text
No marcar complementariedad
No cambiar el estado
No reasignar
```

---

## Paso 7. Validar las tres parametrizaciones funcionales

### 7.1 EPS

Relacionar el afiliado con:

```text
AFILIADOS
OBRAS_SOCIALES
```

Validar:

```text
OBRAS_SOCIALES.complementariedad IN ('O', 'L')
```

Donde:

```text
O = gestión obligatoria
L = gestión opcional
```

### 7.2 Programa

Relacionar el plan de la autorización con:

```text
PLANES
```

Validar:

```text
PLANES.complementariedad = 'S'
```

### 7.3 Tipo de solicitud

Relacionar:

```text
SOLICITUDES_AUTO.soli_tipo
SOLICITUDES_AUTO_TIPOS.soli_tipo
```

Validar:

```text
SOLICITUDES_AUTO_TIPOS.complementariedad = 'S'
```

Si cualquiera de estas tres validaciones no se cumple, el flujo debe continuar como una negación normal.

---

## Paso 8. Obtener el último ejecutivo de Gestión

El GAP 004 indica que inicialmente debe buscarse el último ejecutivo responsable de la solicitud.

La fuente es:

```text
SOLICITUDES_AUTO_WORKFLOW
```

La búsqueda debe considerar:

```text
soli_auto = solicitud
soli_esta = 'G'
baja_fecha IS NULL
```

y tomar el registro con mayor:

```text
fecha_fin_activi
```

Ese usuario es el responsable anterior de Gestión.

---

## Paso 9. Validar si el último ejecutivo puede recibir complementariedad

El último ejecutivo no debe reutilizarse automáticamente.

Validar:

```text
ZZ_USUARI.baja_fecha IS NULL
GETDATE() BETWEEN ZZ_USUARI.vigdes AND ZZ_USUARI.vighas
```

Además:

```text
ZZ_ROLES.complementariedad = 'S'
ZZ_ROLES.es_ejecutivo = 'S'
ZZ_ROLES.recibe_gestion = 'S'
```

También debe:

- tener habilitado el tipo de solicitud;
- pertenecer a la regional correcta;
- pertenecer a la sucursal correcta;
- estar conectado a `prestaciones`;
- no estar inactivado;
- no haber alcanzado su carga máxima.

---

## Paso 10. Validar regional y sucursal

Para la solicitud, obtener:

```text
SOLICITUDES_AUTO.sucur
    → SUCURSALES
    → PARTIDOS
```

La solicitud proporciona:

```text
PARTIDOS.regional
PARTIDOS.partido
```

El usuario proporciona:

```text
ZZ_USUARI.liqui_ofi
ZZ_USUARI.sucursal
```

La correspondencia esperada es:

```text
ZZ_USUARI.liqui_ofi = PARTIDOS.regional
ZZ_USUARI.sucursal  = PARTIDOS.partido
```

No se debe elegir un usuario de otra regional o sucursal solamente porque tenga el rol correcto.

---

## Paso 11. Resolver el usuario destino

La prioridad recomendada es:

### Primera opción

Reutilizar el último responsable de Gestión si cumple todos los controles.

### Segunda opción

Si el último ejecutivo no cumple, buscar otro usuario con:

```text
ZZ_ROLES.complementariedad = 'S'
```

y con los demás criterios de asignación.

El orden de selección debe ser:

1. menor carga activa;
2. mayor tiempo desde la última asignación;
3. fecha de conexión como desempate.

Si no hay usuario disponible, el proceso no debe enviar la solicitud a un usuario común.

Debe devolver un motivo controlado, por ejemplo:

```text
No existe usuario disponible para gestión de complementariedad
```

---

## Paso 12. Actualizar `SOLICITUDES_AUTO`

La modificación debe hacerse dentro de una transacción:

```text
soli_esta = 'G'
complementariedad = 'S'
responsable_gestion = 
...(truncated)...

---

### [21/09/2026 18:38:48] Wilmer Higuera

Lisa, guarda lo trabajado el día de hoy, con todos los detalles de forma que mañana podamos continuar donde quedamos el día de hoy, me indicas como te escribo mañana para que recuperes lo trabajado el día de hoy y poder continuar en este punto donde quedamos, hasta mañana y muchas gracias por tu colaboración

---

### [21/09/2026 18:38:57] Agente

Wilmer, guardaré el cierre completo de hoy asociado únicamente a tu usuario, incluyendo la sincronización SVN, la validación de `SOLICITUDES_AUTO.complementariedad`, el análisis de GAP 004/GAP 008 y el punto exacto de continuación para mañana.

---

### [21/09/2026 18:40:13] Agente

Voy a guardar la memoria curada de hoy con el contexto funcional, técnico y el punto exacto de continuidad, sin modificar el repositorio ni la base de datos.

---

### [21/09/2026 18:41:51] Agente

Listo, Wilmer. Guardé la memoria curada del **21/09/2026** en:

[MEMORY-users_113120678211994207734-2026-09-21.md](/home/desarrollo-ia-05/.openclaw/workspace/memory/MEMORY-users_113120678211994207734-2026-09-21.md)

Registré:

- Análisis funcional de **GAP 004** y **GAP 008**.
- Condiciones de complementariedad.
- Repositorio sincronizado en revisión SVN **37863**.
- Validación del campo `SOLICITUDES_AUTO.complementariedad` en `InstallQA`.
- SP pendiente `pru_reasigna_solicitud_complementariedad`.
- Punto sugerido de integración después de finalizar la carta de negación y antes del `COMMIT`.
- Restricción de no modificar ni hacer commits sin tu solicitud explícita.

Mañana escribime:

> **Lisa, retomemos la Tarea #29489 desde la memoria del 21/09/2026. Continuemos con el diseño del SP `pru_reasigna_solicitud_complementariedad` para implementar GAP 004 y GAP 008 después de finalizar la carta de negación.**

Modo ejecutado: **bajo-demanda**.  
Archivo creado: **1**.  
Errores pendientes: **ninguno**.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
