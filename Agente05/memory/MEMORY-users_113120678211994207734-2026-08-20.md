# Memoria de conversación — Wilmer Higuera

## Datos

- **Fecha:** 20/08/2026
- **Usuario:** Wilmer Higuera
- **ID Google Chat:** `users/113120678211994207734`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 81
- **Inicio:** 08:50:50
- **Fin:** 18:51:07

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/qzugpyaaaae`

## Archivos revisados

- `44430506-72ee-4bbb-a9a4-07f5142d4665.jsonl`

---

## Transcripción completa

### [20/08/2026 08:50:50] Wilmer Higuera

Hola Lisa, buenos días

---

### [20/08/2026 08:52:01] Agente

Hola Wilmer, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [20/08/2026 08:52:30] Wilmer Higuera

Por favor, ten encuenta estos dos puntos:
## Cosas que no hacer
1. NO realizar modificaciones directas en el entorno ni hacer Commits. El agente solo propone y muestra los cambios por pantalla o archivo; la ejecución de modificaciones y commits es responsabilidad exclusiva de whiguera.
2. NO asumir que un paso se ha comprendido o cerrado de forma automática sin el visto bueno de whiguera.

---

### [20/08/2026 08:52:56] Agente

Entendido, Wilmer. Queda tomado como regla de trabajo para esta sesión:

1. No voy a realizar modificaciones directas en el entorno ni commits.
2. Voy a trabajar en modo consultivo: análisis, propuesta y código por pantalla o archivo solo si lo pedís.
3. No voy a dar ningún paso por cerrado ni avanzar al siguiente sin tu confirmación explícita.

---

### [20/08/2026 09:04:29] Wilmer Higuera

Lisa, retomemos la memoria del 2026-08-19 de la Tarea 29489, Caso 18, desde el punto "2. Guardar Texto Base De Auditoria" del SP pri_solicitudes_auto_notif.
No toque nada mas desde que te compartí el SP local 9b0548c5; sigamos con el reemplazo del bloque de envió por cursor.
Te compartiré de nuevo el SP para que me des las instrucciones exactas del paso a paso bien detallado para lo que nos falta

---

### [20/08/2026 09:07:04] Agente

Wilmer, revisé el SP adjunto y retomé la memoria del 2026-08-19. No modifiqué nada.

Estado actual confirmado:

- `@var_activi_base` ya está declarada y asignada correctamente con el `CASE` inicial.
- `@var_activi_auditoria`, `@var_dest_email`, `@var_dest_tipo`, `@var_dest_principal`, `@var_dest_orden`, `@var_mail_id_prestad` y `@var_mail_id_afi` ya están declaradas.
- La tabla `#DESTINATARIOS_NOTIF` ya se carga correctamente.
- Lo que falta es reemplazar completo el bloque viejo de envío que hoy va desde la línea 607 hasta la 709.

**Paso 1**

Eliminá completo este bloque actual:

```sql
BEGIN
    -- Controlar si deben enviar mail al responsable de la regional si el ejecutivo no tiene definido el email
    --      IF @crs_ofi IS NULL SET @crs_email = @var_mail_sin_ofi

    ... todo el envio por @var_email ...

    ... todo el envio por @var_email_2 ...
END
```

En tu archivo corresponde al bloque que empieza en la línea 607 y termina en la línea 709.

Muy importante: dentro de ese bloque está esta línea:

```sql
SET @var_activi_base = @var_activi
```

Esa línea debe desaparecer. `@var_activi` no está declarada y además ya habíamos definido que `@var_activi_base` no se pisa más.

**Paso 2**

En el mismo lugar donde estaba ese bloque, pegá este reemplazo:

```sql
DECLARE cur_destinatarios_notif CURSOR LOCAL FAST_FORWARD FOR
SELECT
    email,
    tipo,
    principal,
    orden
FROM #DESTINATARIOS_NOTIF
ORDER BY tipo, orden, email

OPEN cur_destinatarios_notif

FETCH cur_destinatarios_notif
INTO @var_dest_email, @var_dest_tipo, @var_dest_principal, @var_dest_orden

WHILE @@FETCH_STATUS = 0
BEGIN
    SET @var_valido = 0

    EXEC dbo.afs_valida_mail
        @prm_mail   = @var_dest_email,
        @prm_echo   = 'N',
        @prm_valido = @var_valido OUTPUT

    IF @var_valido = 1
    BEGIN
        EXEC dbo.afi_mail_envio
            @prm_mail_id                = @out_mail_id OUTPUT,
            @prm_subject                = @var_subject,
            @prm_template               = NULL,
            @prm_text                   = @var_full_html,
            @prm_email_from_description = @var_remitente,
            @prm_usuari                 = @var_user,
            @prm_html                   = 'S',
            @prm_echo                   = 'N'

        EXEC dbo.afi_mail_to
            @prm_mail_id  = @out_mail_id,
            @prm_email_to = @var_dest_email

        EXEC dbo.afu_mail_envio_send
            @prm_mail_id = @out_mail_id

        IF @var_dest_tipo = 'P'
        AND @var_mail_id_prestad IS NULL
        BEGIN
            SET @var_mail_id_prestad = @out_mail_id
        END

        IF @var_dest_tipo = 'A'
        AND @var_mail_id_afi IS NULL
        BEGIN
            SET @var_mail_id_afi = @out_mail_id
        END

        SET @var_activi_auditoria =
            @var_activi_base
            + '. mail_id: ' + CONVERT(VARCHAR(20), @out_mail_id)
            + '. email: ' + @var_dest_email
            + '. notifica: ' + CASE WHEN @var_dest_tipo = 'P' THEN 'Prestador' ELSE 'Afiliado' END
            + '. proceso_id: ' + CONVERT(VARCHAR(10), @prm_id)

        EXEC dbo.pri_solicitudes_auto_auditoria
            @prm_soli_auto   = @prm_soli_auto,
            @prm_activi      = @var_activi_auditoria,
            @prm_usuari      = @var_user,
            @prm_tipo_usuari = @var_dest_tipo
    END

    FETCH cur_destinatarios_notif
    INTO @var_dest_email, @var_dest_tipo, @var_dest_principal, @var_dest_orden
END

CLOSE cur_destinatarios_notif
DEALLOCATE cur_destinatarios_notif

UPDATE SOLICITUDES_AUTO
SET mail_id_prestad_mail = ISNULL(@var_mail_id_prestad, mail_id_prestad_mail),
    mail_id_afi_mail     = ISNULL(@var_mail_id_afi, mail_id_afi_mail)
WHERE soli_auto = @prm_soli_auto
```

**Qué cambia la lógica**

Ahora el envío ya no depende de `@var_email` ni de `@var_email_2`.

El SP toma todos los destinatarios cargados en `#DESTINATARIOS_NOTIF`, valida cada email, genera un `mail_id` independiente por destinatario, envía el correo, registra auditoría por cada envío válido y al final actualiza `SOLICITUDES_AUTO` una sola vez.

Para prestadores, `@var_mail_id_prestad` guarda el primer `mail_id` válido tipo `P`.

Para afiliado, `@var_mail_id_afi` guarda el primer `mail_id` válido tipo `A`.

**Validación del paso**

Después de aplicar solo este reemplazo, primero compilá el SP.

No sigas con pruebas funcionales todavía. Cuando compile, avisame y pasamos al siguiente paso de validación.

---

### [20/08/2026 09:33:04] Wilmer Higuera

Hazme un favor, toma el SP que te compartí hace un momento y en A:\whiguera\Tarea #23986\Tarea #29489 me generas el SP como debe quedar, así yo lo valido

---

### [20/08/2026 09:35:02] Agente

Listo, Wilmer. Generé el SP en:

`A:\whiguera\Tarea #23986\Tarea #29489\pri_solicitudes_auto_notif.sql`

Equivalente en Linux:

`/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/pri_solicitudes_auto_notif.sql`

No toqué el repositorio y no hice commit.

Validaciones realizadas:
- El archivo quedó creado correctamente.
- Conservé formato del adjunto: sin BOM y con finales de línea `CRLF`.
- Reemplacé el bloque viejo de envío por `@var_email` / `@var_email_2`.
- Eliminé la línea incorrecta `SET @var_activi_base = @var_activi`.
- Dejé el envío por cursor sobre `#DESTINATARIOS_NOTIF`.
- Dejé el `UPDATE SOLICITUDES_AUTO` una sola vez al final.

No ejecuté compilación en base de datos.

---

### [20/08/2026 10:18:40] Wilmer Higuera

Ya lo valide y esta bien, pero sabes, mejor si dejemos el EXEC dbo.afs_valida_mail, el punto que te dije que no, mejor incorpora el punto "4. Validar cada email con afs_valida_mail.", como lo usabamos en el proceso original, porfa, modifica el SP que generaste en A:\whiguera\Tarea #23986\Tarea #29489

---

### [20/08/2026 10:19:13] Agente

Wilmer, revisé el SP generado y el punto **4. Validar cada email con `afs_valida_mail`** ya está incorporado.

Está en el cursor, líneas 623 a 630:

```sql
SET @var_valido = 0

EXEC dbo.afs_valida_mail
    @prm_mail   = @var_dest_email,
    @prm_echo   = 'N',
    @prm_valido = @var_valido OUTPUT

IF @var_valido = 1
```

No hice cambios al archivo porque ya estaba como pedís: valida cada email de `#DESTINATARIOS_NOTIF` antes de ejecutar `afi_mail_envio`, igual que el proceso original.

Validé además:
- Archivo: `A:\whiguera\Tarea #23986\Tarea #29489\pri_solicitudes_auto_notif.sql`
- Sin BOM.
- Finales de línea `CRLF`.
- `afs_valida_mail` presente dentro del cursor.

---

### [20/08/2026 10:27:13] Wilmer Higuera

Ya compile el SP, ahora si validemos, te aviso como pediste.
"No sigas con pruebas funcionales todavía. Cuando compile, avísame y pasamos al siguiente paso de validación."

---

### [20/08/2026 10:27:52] Agente

Perfecto, Wilmer. Como ya compiló, el siguiente paso es **validación previa para `@prm_id = 1` prestador**, sin ejecutar todavía el envío hasta confirmar que la solicitud elegida tiene destinatarios correctos.

**Paso 1 - Elegir Solicitud De Prueba Prestador**

Usá una solicitud real/controlada que corresponda al flujo de prestador registrada. Reemplazá `<solicitud>` por el `soli_auto` que vas a validar.

Primero ejecutá esto:

```sql
SELECT
    S.soli_auto,
    S.soli_esta,
    S.fecha,
    S.prestad,
    S.email_prestad,
    P.email,
    P.email_2,
    mail_id_prestad_mail = S.mail_id_prestad_mail,
    mail_id_afi_mail     = S.mail_id_afi_mail
FROM SOLICITUDES_AUTO S
    LEFT JOIN PRESTADORES P ON P.prestad = S.prestad
WHERE S.soli_auto = <solicitud>
```

Después validá si tiene prestadores asociados en `SOLICITUDES_AUTO_PRESTAD`, porque ahora el SP prioriza esa tabla para armar múltiples destinatarios:

```sql
SELECT
    SAP.soli_auto,
    SAP.prestad,
    SAP.email,
    SAP.baja_fecha
FROM SOLICITUDES_AUTO_PRESTAD SAP
WHERE SAP.soli_auto = <solicitud>
ORDER BY
    CASE WHEN SAP.baja_fecha IS NULL THEN 0 ELSE 1 END,
    SAP.prestad,
    SAP.email
```

**Qué tenés que revisar**

1. Que la solicitud exista.
2. Que `S.prestad` tenga sentido para el caso.
3. Que haya al menos un email válido por alguna de estas vías:
   - `SOLICITUDES_AUTO_PRESTAD.email` vigente, con `baja_fecha IS NULL`
   - o fallback por `SOLICITUDES_AUTO.email_prestad`
   - o fallback por `PRESTADORES.email` / `PRESTADORES.email_2`
4. Anotá los valores actuales de:
   - `mail_id_prestad_mail`
   - `mail_id_afi_mail`

Todavía no ejecutes:

```sql
EXEC dbo.pri_solicitudes_auto_notif
```

Primero pasame el resultado de esas dos consultas o confirmame que la solicitud tiene destinatarios correctos. Ahí avanzamos al **Paso 2: ejecución controlada de `@prm_id = 1` y validación de mail/auditoría**.

---

### [20/08/2026 10:34:25] Wilmer Higuera

Mira el resultado de las dos consultas

---

### [20/08/2026 10:34:40] Agente

Perfecto, Wilmer. La solicitud **25920072** sirve para la prueba controlada de prestador.

Lectura del resultado:

- `SOLICITUDES_AUTO.prestad = 32`
- `email_prestad = panchoAmej@meji.com.ar`
- `PRESTADORES.email = pruebasgisprestador@gmail.com`
- `PRESTADORES.email_2 = franarme@yahoo.com`
- Ya tiene valores previos:
  - `mail_id_prestad_mail = 9989059`
  - `mail_id_afi_mail = 9989007`

En `SOLICITUDES_AUTO_PRESTAD` hay **3 destinatarios vigentes** porque todos tienen `baja_fecha = NULL`:

```text
N1@N1.COM.AR
panchoAmej@meji.com.ar
PRUEBASGISPRESTADOR@GMAIL.COM
```

Como el SP ahora toma primero `SOLICITUDES_AUTO_PRESTAD`, para `@prm_id = 1` debería intentar validar esos 3 emails y enviar solo los que pasen `afs_valida_mail`.

**Paso 2 - Ejecutar Prueba Controlada Prestador**

Antes de ejecutar, dejá anotados los `mail_id` actuales que viste en pantalla.

Ahora ejecutá:

```sql
EXEC dbo.pri_solicitudes_auto_notif
    @prm_soli_auto = 25920072,
    @prm_id        = 1
```

Después ejecutá estas validaciones:

```sql
SELECT
    soli_auto,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920072
```

Y luego:

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920072
ORDER BY fecha DESC
```

Lo esperado:

- `mail_id_prestad_mail` debería cambiar a un nuevo `mail_id` si al menos un email tipo prestador fue válido.
- `mail_id_afi_mail` no debería cambiar en esta prueba.
- En auditoría deberían aparecer registros por cada email válido enviado, con `tipo_usuari = 'P'`.

Pasame el resultado de esas dos consultas posteriores y validamos si el cursor quedó funcionando correctamente.

---

### [20/08/2026 10:55:28] Wilmer Higuera

Mira el resultado, si cambio

---

### [20/08/2026 10:55:44] Agente

Sí, Wilmer, cambió correctamente.

Validación del resultado:

- `mail_id_prestad_mail` cambió de `9989059` a `9989060`.
- `mail_id_afi_mail` quedó igual en `9989007`, correcto para prueba `@prm_id = 1`.
- Auditoría registró nuevas filas con:
  - `tipo_usuari = 'P'`
  - `usuario = sa`
  - fecha/hora `2026-08-20 10:51:11.xxx`

Eso confirma que el cursor envió al menos un mail válido tipo prestador y actualizó solo el campo de prestador.

Para cerrar bien esta validación, falta ver el texto completo de auditoría porque en la grilla está truncado. Ejecutá esta consulta:

```sql
SELECT TOP 10
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920072
  AND fecha_hora >= '2026-08-20 10:51:00'
ORDER BY fecha_hora DESC
```

Ahí deberíamos confirmar:

- qué `mail_id` quedó registrado en cada auditoría;
- qué email se notificó;
- si envió a 1, 2 o 3 destinatarios válidos;
- que todos los registros nuevos sean `tipo_usuari = 'P'`.

Pasame ese resultado y pasamos al siguiente caso: validación de afiliado con `@prm_id = 2` o el ID que corresponda según el flujo del SP.

---

### [20/08/2026 11:04:46] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 11:04:58] Agente

Correcto, Wilmer. La validación de prestador quedó bien.

Resultado confirmado:

- Se generaron **2 envíos válidos**:
  - `9989060` para `panchoAmej@meji.com.ar`
  - `9989061` para `PRUEBASGISPRESTADOR@GMAIL.COM`
- `N1@N1.COM.AR` no aparece en auditoría, por lo tanto no pasó `afs_valida_mail` o no fue enviado. Correcto.
- Ambos registros quedaron con:
  - `tipo_usuari = 'P'`
  - `notifica: Prestador`
  - `proceso_id: 1`
- `mail_id_prestad_mail` quedó con el primer mail válido: `9989060`.

Con esto queda validado el punto **4. Validar cada email con `afs_valida_mail`** y también el envío múltiple por cursor para prestador.

Siguiente paso: validar afiliado.

Primero ejecutá esta consulta previa para ver los datos de afiliado de la solicitud:

```sql
SELECT
    S.soli_auto,
    S.afiliado,
    A.email,
    mail_id_prestad_mail = S.mail_id_prestad_mail,
    mail_id_afi_mail     = S.mail_id_afi_mail
FROM SOLICITUDES_AUTO S
    LEFT JOIN AFILIADOS A ON A.afiliado = S.afiliado
WHERE S.soli_auto = 25920072
```

Todavía no ejecutes el SP para afiliado. Primero veamos si tiene email de afiliado válido y confirmamos el `@prm_id` correcto para ese flujo.

---

### [20/08/2026 11:07:07] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 11:07:59] Agente

Wilmer, el error es por mi consulta previa: usé columnas genéricas que no existen en esta base (`S.afiliado`, `A.afiliado`, `A.email`). En este SP el afiliado se relaciona así:

```sql
A.prepaga = S.prepaga
AND A.contra = S.contra
AND A.inte = S.inte
```

Y los emails de afiliado salen de `SOLICITUDES_AUTO`:

```sql
S.email_afi_1
S.email_afi_2
```

Ejecutá esta consulta corregida:

```sql
SELECT
    S.soli_auto,
    S.prepaga,
    S.contra,
    S.inte,
    afiliado = LTRIM(RTRIM(A.nombre + ' ' + ISNULL(A.nombre2, ''))) + ', ' +
               LTRIM(RTRIM(A.ape + ' ' + ISNULL(A.ape2, ''))),
    S.email_afi_1,
    S.email_afi_2,
    mail_id_prestad_mail = S.mail_id_prestad_mail,
    mail_id_afi_mail     = S.mail_id_afi_mail
FROM SOLICITUDES_AUTO S
    LEFT JOIN AFILIADOS A
        ON  A.prepaga = S.prepaga
        AND A.contra  = S.contra
        AND A.inte    = S.inte
WHERE S.soli_auto = 25920072
```

Con eso validamos si tiene email de afiliado cargado.

Para el flujo equivalente a “solicitud registrada” pero hacia afiliado, según el SP corresponde probar con `@prm_id = 7` después de revisar ese resultado.

---

### [20/08/2026 11:10:05] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 11:10:20] Agente

Bien, Wilmer. La solicitud **25920072** también sirve para validar afiliado.

Datos confirmados:

- Afiliado: `ALVARO DE, RUIZ CEBALLOS`
- `email_afi_1 = email_afi_200260805@gmail.com`
- `email_afi_2 = EMAIL_AFI_20260806@gmail.com`
- Estado actual antes de la prueba:
  - `mail_id_prestad_mail = 9989060`
  - `mail_id_afi_mail = 9989007`

Para el flujo de “solicitud registrada” hacia afiliado corresponde probar con:

```sql
EXEC dbo.pri_solicitudes_auto_notif
    @prm_soli_auto = 25920072,
    @prm_id        = 7
```

Después ejecutá:

```sql
SELECT
    soli_auto,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920072
```

Y auditoría:

```sql
SELECT TOP 10
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920072
  AND fecha_hora >= GETDATE() - 1
ORDER BY fecha_hora DESC
```

Lo esperado:

- `mail_id_prestad_mail` debe seguir en `9989060`.
- `mail_id_afi_mail` debe cambiar a un nuevo `mail_id`.
- Deben aparecer auditorías nuevas con `tipo_usuari = 'A'`.
- Deberían registrarse 2 envíos si ambos emails pasan `afs_valida_mail`.

Pasame el resultado y confirmamos el caso afiliado.

---

### [20/08/2026 11:13:55] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 11:14:06] Agente

Sí, Wilmer, el caso afiliado también cambió correctamente.

Validación:

- `mail_id_prestad_mail` quedó igual en `9989060`. Correcto.
- `mail_id_afi_mail` cambió de `9989007` a `9989062`. Correcto.
- Auditoría nueva con:
  - `tipo_usuari = 'A'`
  - fecha/hora `2026-08-20 11:11:53.800`

Esto confirma que para `@prm_id = 7` el SP envió al afiliado y actualizó solo `mail_id_afi_mail`.

Para confirmar si envió a los 2 emails de afiliado o solo a 1, ejecutá esta consulta con texto completo:

```sql
SELECT TOP 10
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920072
  AND fecha_hora >= '2026-08-20 11:11:00'
ORDER BY fecha_hora DESC
```

Lo esperado:

- Si ambos emails pasaron `afs_valida_mail`, deberían aparecer dos filas tipo `A`.
- Si aparece una sola fila, entonces solo un email fue válido o solo uno quedó cargado en `#DESTINATARIOS_NOTIF`.

Pasame esa salida y cerramos la validación afiliado.

---

### [20/08/2026 11:16:13] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 11:16:27] Agente

Validación afiliado correcta, Wilmer.

Resultado:

- Se generó **1 envío válido** para afiliado:
  - `mail_id: 9989062`
  - `email: email_afi_200260805@gmail.com`
  - `notifica: Afiliado`
  - `proceso_id: 7`
  - `tipo_usuari = 'A'`
- `mail_id_afi_mail` quedó actualizado con `9989062`.
- El segundo email `EMAIL_AFI_20260806@gmail.com` no quedó auditado. Eso indica que no fue enviado, probablemente porque no pasó `afs_valida_mail`.

Con esto queda validado:

1. Envío múltiple por cursor para prestador.
2. Validación individual de emails con `afs_valida_mail`.
3. Auditoría individual por email enviado.
4. Actualización de `mail_id_prestad_mail` con el primer envío válido tipo `P`.
5. Actualización de `mail_id_afi_mail` con el primer envío válido tipo `A`.
6. Separación correcta entre flujos prestador (`@prm_id = 1`) y afiliado (`@prm_id = 7`).

Para mí, el cambio principal del cursor queda funcionalmente validado.

---

### [20/08/2026 11:21:41] Wilmer Higuera

¿Nos falta algo para cumplir con el CASO 18 solicitud emitida para varios prestadores?

---

### [20/08/2026 11:22:18] Agente

Wilmer, para el **CASO 18: solicitud emitida para varios prestadores**, funcionalmente **no nos falta nada del cambio principal**.

Ya quedó validado lo crítico:

- La solicitud `25920072` tenía varios prestadores en `SOLICITUDES_AUTO_PRESTAD`.
- El SP tomó esos destinatarios desde `#DESTINATARIOS_NOTIF`.
- Validó cada email con `dbo.afs_valida_mail`.
- Envió solo los emails válidos.
- Generó auditoría individual por destinatario enviado.
- Actualizó `mail_id_prestad_mail` con el primer `mail_id` válido.
- No afectó `mail_id_afi_mail` durante el flujo prestador.
- También validamos que el flujo afiliado sigue funcionando aparte con `@prm_id = 7`.

Lo único que yo haría antes de darlo por cerrado ante la tarea es una **validación documental final**, no técnica nueva:

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920072
ORDER BY fecha_hora DESC
```

Y dejar anotado en la evidencia:

- Caso prestador `@prm_id = 1`.
- Se enviaron 2 correos válidos de 3 destinatarios cargados.
- El email inválido/no aceptado no generó envío ni auditoría.
- `mail_id_prestad_mail` quedó en `9989060`.
- `mail_id_afi_mail` no cambió en esa prueba.

Mi conclusión técnica: **el Caso 18 queda cubierto**. Lo que sigue sería preparar el resumen para Redmine con evidencia de compilación, prueba prestador múltiple y prueba de no regresión afiliado.

---

### [20/08/2026 12:22:45] Wilmer Higuera

Ahora ayúdame a validar este otro caso porfa:CASO 24DESCRIPCIÓN
REVISION DE NOTIFICACIONES ESTADO REGISTRADA

EVIDENCIA
SE REVISA NOTIFICACION
Adjunto imagen1 de este punto
registro la solicitud desde GIS
Adjunto imagen 2
reviso datos almacenados

se observa que NO se almacenan registros en tabla WORKFLOW

select * from SOLICITUDES_AUTO_WORKFLOW where soli_auto>='25920101'
Adjunto imagen 3
REVISAR

---

### [20/08/2026 12:23:50] Agente

Wilmer, sí. Para **CASO 24 - Notificaciones estado REGISTRADA**, validemos separado de Caso 18.

La imagen 1 confirma que existe parametrización para:

`Notificación PRESTADOR, estado de solicitud REGISTRADA`

Eso corresponde al flujo que venimos usando como `@prm_id = 1`.

Según la memoria de la tarea, para solicitudes registradas desde GIS el alta pasa por `pru_registra_solicitudes_auto`; la solicitud puede nacer en `C` y luego quedar en `R` por `pru_solicitudes_auto_adjuntos`. El punto técnico recomendado era disparar la notificación después de ese cambio, validando que quedó en `R`.

**Paso 1 - Identificar La Solicitud GIS**

Ejecutá primero:

```sql
SELECT
    soli_auto,
    soli_esta,
    fecha,
    prestad,
    email_prestad,
    email_afi_1,
    email_afi_2,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto >= 25920101
ORDER BY soli_auto
```

Necesitamos confirmar cuál fue la solicitud creada desde GIS y si quedó en estado `R`.

**Paso 2 - Validar WORKFLOW**

Ejecutá:

```sql
SELECT *
FROM SOLICITUDES_AUTO_WORKFLOW
WHERE soli_auto >= 25920101
ORDER BY soli_auto
```

Si no devuelve registros, confirmamos la evidencia del caso: **no se está registrando workflow** para esa solicitud.

**Paso 3 - Validar Si Hubo Notificación Aunque No Haya WORKFLOW**

Para la solicitud exacta, reemplazá `<solicitud>`:

```sql
SELECT
    soli_auto,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <solicitud>
```

Después:

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = <solicitud>
ORDER BY fecha_hora DESC
```

Lo que buscamos:

- Si `mail_id_prestad_mail` está `NULL` o no cambió, probablemente no se disparó notificación prestador.
- Si no hay auditoría con `proceso_id: 1`, no se ejecutó `pri_solicitudes_auto_notif` para prestador registrada.
- Si hay auditoría con `proceso_id: 1`, entonces la notificación sí salió y el problema sería solo de `WORKFLOW`.

**Paso 4 - Conclusión Según Resultado**

Escenario A: no hay `WORKFLOW` y tampoco auditoría/mail  
Entonces el caso apunta a que en el registro GIS falta disparar la lógica posterior al estado `R`, probablemente desde `pru_registra_solicitudes_auto`.

Escenario B: no hay `WORKFLOW`, pero sí hay mail/auditoría  
Entonces la notificación funciona y el faltante real es el registro en `SOLICITUDES_AUTO_WORKFLOW`.

Escenario C: hay `WORKFLOW`, pero no mail/auditoría  
Entonces hay que revisar si el workflow está registrando estado `R`, pero no llama a `pri_solicitudes_auto_notif`.

Pasame el resultado de esos 3 bloques y te digo exactamente cuál rama corresponde y qué SP debemos revisar primero.

---

### [20/08/2026 12:46:56] Wilmer Higuera

La hora de la tabla SOLICITUDES_AUTO_WORKFLOW tiene mucha diferencia con la de SOLICITUDES_AUTO, pensaría que se inserto después con otro proceso para esa solicitud, ya que esta en otro estado diferente a Registrado, te adjunto el resultado de las consultas

---

### [20/08/2026 12:47:14] Agente

Sí, Wilmer. Tu interpretación es correcta.

Para la solicitud `25920101`, la evidencia muestra esto:

- En `SOLICITUDES_AUTO`:
  - `soli_esta = D`
  - `fecha = 2026-08-16 20:43:08.640`

- En `SOLICITUDES_AUTO_WORKFLOW`:
  - registro con `soli_esta = V`
  - `fecha_ini_activi = 2026-08-16 21:37:13.107`
  - `fecha_fin_activi = NULL`
  - `fecha/modi_fecha/alta_fecha = 2026-08-16 21:37:13.107`

- En auditoría:
  - hay actividad inicial de carga/edición alrededor de `21:25` a `21:37`
  - luego hay devoluciones el `2026-08-16 22:06`
  - luego reasignación/devolución el `2026-08-18`

Conclusión técnica: **esta solicitud no sirve como evidencia limpia para validar “estado REGISTRADA” del Caso 24**, porque ya está en estado `D` y el workflow visible quedó en `V`, no en `R`. Además, los `mail_id` actuales `9989050` y `9989051` parecen corresponder a notificaciones posteriores de devolución, no necesariamente al registro inicial.

Para validar bien el Caso 24 necesitamos una solicitud recién creada desde GIS que quede en `R` y revisar inmediatamente:

```sql
SELECT
    soli_auto,
    soli_esta,
    fecha,
    prestad,
    email_prestad,
    email_afi_1,
    email_afi_2,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <nueva_solicitud>
```

```sql
SELECT *
FROM SOLICITUDES_AUTO_WORKFLOW
WHERE soli_auto = <nueva_solicitud>
ORDER BY alta_fecha, fecha_ini_activi
```

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = <nueva_solicitud>
ORDER BY fecha_hora DESC
```

Punto clave: para Caso 24 tenemos que capturar la evidencia **antes de que otro proceso la devuelva, reasigne o cambie de estado**. Esta `25920101` sirve como pista de que hubo procesos posteriores, pero no como prueba concluyente del estado `Registrada`.

---

### [20/08/2026 12:53:21] Wilmer Higuera

Lisa, no he ejecutado Gis, ¿Conoces el link para ingresar por Gis y registrar la solicitud que necesitamos?

---

### [20/08/2026 12:56:01] Agente

Wilmer, no tengo registrado un **link exacto de acceso a GIS** en memoria ni en los archivos operativos del workspace.

Lo que sí confirmé es la ruta técnica del módulo:

```text
WebPos/index.php
```

y para registrar la solicitud:

```text
WebPos/noambu_auto.php
```

O sea, si ya tenés el host de QA/InstallQA de GIS, el acceso debería ser algo del estilo:

```text
http://<servidor_gis>/WebPos/index.php
```

Después de ingresar, el flujo de registro va por:

```text
WebPos/noambu_auto.php
```

Dato técnico validado: esa copia de WebPos apunta a base `Installqa`, así que necesitamos entrar al GIS del ambiente QA, no producción.

No voy a inventarte el host. Si tenés algún acceso viejo, bookmark o captura del navegador, pasame solo la parte del dominio/IP y te confirmo la URL completa para entrar y registrar la solicitud.

---

### [20/08/2026 12:58:26] Wilmer Higuera

Ya tengo el link, mira, recuerdas según el video que te compartí, ¿Qué datos uso el funcional para ingresar a Gis?

---

### [20/08/2026 13:11:34] Agente

Sí, Wilmer. En el video el funcional ingresó con estos datos:

**METAUSUARIO**
```text
Tipo Identificación: Cédula de ciudadanía
Nro. Identificación: 1
Nombre: 1
Apellido 1: 1
Apellido 2: 1
Email: 1@1.com.ar
```

**PRESTADOR**
```text
Rol: Usuario Operativo
Tipo Identificación: Nro. Identificación Tributaria
Nro. Identificación: 10123080
```

Luego botón:

```text
Ingresar a GIS
```

Ese prestador `10123080` es el dato clave para registrar la solicitud desde GIS como usuario operativo.

---

### [20/08/2026 13:43:52] Wilmer Higuera

Ya voy a dar clic en Guardar, dame el sql para ver la ultima solicitud, por si no sale el aviso de la solicitud generada, saber que esa ultima es la nuestra para la prueba, porfa

---

### [20/08/2026 13:44:08] Agente

Claro, Wilmer. Apenas des clic en **Guardar**, ejecutá este SQL para identificar la última solicitud generada:

```sql
SELECT TOP 10
    soli_auto,
    soli_esta,
    fecha,
    prestad,
    email_prestad,
    email_afi_1,
    email_afi_2,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE fecha >= DATEADD(MINUTE, -10, GETDATE())
ORDER BY soli_auto DESC
```

Si querés verlo más directo, solo la última:

```sql
SELECT TOP 1
    soli_auto,
    soli_esta,
    fecha,
    prestad,
    email_prestad,
    email_afi_1,
    email_afi_2,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
ORDER BY soli_auto DESC
```

Cuando identifiques el `soli_auto`, no avances todavía. Pasame ese número y validamos inmediatamente `WORKFLOW`, auditoría y si quedó en estado `R`.

---

### [20/08/2026 13:45:43] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 13:45:53] Agente

Perfecto, Wilmer. La solicitud generada es:

```text
25920114
```

Ahora validemos inmediatamente el Caso 24 antes de que otro proceso la cambie.

Ejecutá estos SQL en este orden:

```sql
SELECT
    soli_auto,
    soli_esta,
    fecha,
    prestad,
    email_prestad,
    email_afi_1,
    email_afi_2,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920114
```

```sql
SELECT *
FROM SOLICITUDES_AUTO_WORKFLOW
WHERE soli_auto = 25920114
ORDER BY alta_fecha, fecha_ini_activi
```

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920114
ORDER BY fecha_hora DESC
```

Lo clave a revisar:

- Si `soli_esta = R`, tenemos la solicitud limpia para el caso.
- Si `SOLICITUDES_AUTO_WORKFLOW` no trae registros, se confirma el síntoma del Caso 24.
- Si no hay auditoría/mail con `proceso_id: 1`, entonces tampoco se disparó la notificación de registrada a prestador.

Pasame esos resultados y te digo el diagnóstico exacto.

---

### [20/08/2026 13:48:00] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 13:48:13] Agente

Wilmer, con esta evidencia el diagnóstico del **Caso 24** queda bastante claro:

- La solicitud `25920114` quedó en estado `R` correctamente.
- Se enviaron notificaciones:
  - `mail_id_prestad_mail = 9989063`
  - `mail_id_afi_mail = 9989064`
- Auditoría registró:
  - Notificación registrada tipo `P`
  - Notificación registrada tipo `A`
  - Carga de adjuntos
  - Activación de Solicitud
- Pero `SOLICITUDES_AUTO_WORKFLOW` no devolvió registros para `25920114`.

Conclusión técnica: **la notificación de estado Registrada sí funciona**, pero **no se está generando registro en `SOLICITUDES_AUTO_WORKFLOW`** para la solicitud creada desde GIS. El Caso 24 no apunta al SP `pri_solicitudes_auto_notif`; apunta al proceso de alta/activación/carga de adjuntos donde debería insertarse el workflow.

Para dejar evidencia completa, ejecutá esta consulta final con el texto completo de auditoría:

```sql
SELECT
    soli_auto,
    activi,
    usuario,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920114
ORDER BY fecha_hora
```

Y después esta para confirmar formalmente ausencia de workflow:

```sql
SELECT COUNT(*) AS cantidad_workflow
FROM SOLICITUDES_AUTO_WORKFLOW
WHERE soli_auto = 25920114
```

Si `cantidad_workflow = 0`, ya tenemos la evidencia limpia: solicitud nueva, estado `R`, notificaciones emitidas, auditoría registrada, workflow ausente.

---

### [20/08/2026 13:52:22] Wilmer Higuera

Si, dio cantidad = 0, ayúdame a validar porfa que SP es el que guarda en la tabla SOLICITUDES_AUTO_WORKFLOW, y luego un análisis de por que no esta guardando en esta tabla

---

### [20/08/2026 13:54:57] Agente

Sí, Wilmer. Con `cantidad_workflow = 0`, el SP que debemos revisar es:

`dbo.pru_solicitudes_auto_workflow`

Ese SP inserta en `SOLICITUDES_AUTO_WORKFLOW`, pero solo si encuentra responsable:

```sql
SELECT
    @var_responsable = CASE
                           WHEN soli_esta <> 'V' THEN responsable_gestion
                           ELSE responsable_auditoria
                       END,
    @var_soli_esta = soli_esta
FROM SOLICITUDES_AUTO
WHERE soli_auto = @prm_soli_auto

IF @var_responsable IS NOT NULL
BEGIN
    ...
    INSERT INTO SOLICITUDES_AUTO_WORKFLOW (...)
END
```

Para tu caso `25920114`, validá primero esto:

```sql
SELECT
    soli_auto,
    soli_esta,
    responsable_gestion,
    responsable_auditoria,
    usuario,
    tipo_usuario,
    origen,
    fecha,
    alta_fecha,
    modi_fecha
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920114
```

Y luego esto:

```sql
EXEC dbo.pru_solicitudes_auto_workflow
    @prm_soli_auto = 25920114

SELECT COUNT(*) AS cantidad_workflow
FROM SOLICITUDES_AUTO_WORKFLOW
WHERE soli_auto = 25920114
```

Mi análisis inicial:

El alta GIS entra por `WebPos/modelo/solicitudAuto.php`, que ejecuta `dbo.pru_registra_solicitudes_auto` con `@prm_origen = 'G'`.

Dentro de `pru_registra_solicitudes_auto`, el flujo hace esto:

1. Inserta la solicitud inicialmente en estado `C`.
2. Ejecuta `pru_solicitudes_auto_adjuntos`.
3. `pru_solicitudes_auto_adjuntos` cambia la solicitud a `R` si están todos los adjuntos obligatorios.
4. Ejecuta `pru_proceso_asignacion_masiva` para buscar `responsable_gestion`.
5. Actualiza `SOLICITUDES_AUTO.responsable_gestion`.
6. Solo si `@var_responsable_gestion IS NOT NULL`, llama a `pru_solicitudes_auto_workflow`.

Entonces, si la solicitud quedó en `R`, sí notificó, pero `WORKFLOW = 0`, la causa más probable es:

```text
No se asignó responsable_gestion al momento del alta.
```

Y como `pru_solicitudes_auto_workflow` requiere responsable no nulo, no inserta nada.

Para confirmarlo, si el primer SQL muestra:

```text
responsable_gestion = NULL
```

entonces el problema no está en notificaciones ni en adjuntos. Está en la asignación automática de responsable durante el alta GIS, probablemente en `pru_proceso_asignacion_masiva`: no encontró usuario elegible/logueado en `program_name = 'prestaciones'`, o no encontró usuario que cumpla tipo de solicitud, regional/sucursal/sucur, topes o parámetros especiales.

Pasame el resultado del primer SQL y te doy el diagnóstico cerrado con la consulta exacta para validar por qué no encontró responsable.

---

### [20/08/2026 14:04:22] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 14:04:36] Agente

Confirmado, Wilmer: ahí está la causa directa.

Para `25920114`:

```text
soli_esta = R
responsable_gestion = NULL
responsable_auditoria = NULL
usuario = 10123080
tipo_usuario = P
origen = G
```

Entonces `dbo.pru_solicitudes_auto_workflow` **no inserta en `SOLICITUDES_AUTO_WORKFLOW`** porque su condición depende de tener responsable:

```sql
IF @var_responsable IS NOT NULL
BEGIN
    INSERT INTO SOLICITUDES_AUTO_WORKFLOW (...)
END
```

Como la solicitud está en estado `R`, el SP intenta tomar:

```sql
responsable_gestion
```

pero viene `NULL`. Por eso `cantidad_workflow = 0`.

El siguiente punto a validar es **por qué no se asignó `responsable_gestion` en el alta GIS**. Ejecutá esto:

```sql
SELECT
    soli_auto,
    soli_esta,
    prestad,
    sucur,
    regional,
    tipo_soli_auto,
    responsable_gestion,
    responsable_auditoria,
    usuario,
    tipo_usuario,
    origen
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920114
```

Y luego necesitamos revisar si el proceso de asignación masiva encuentra candidatos:

```sql
EXEC dbo.pru_proceso_asignacion_masiva
    @prm_soli_auto = 25920114
```

Después volvé a consultar:

```sql
SELECT
    soli_auto,
    soli_esta,
    responsable_gestion,
    responsable_auditoria,
    modi_fecha
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920114
```

Si después de ejecutar `pru_proceso_asignacion_masiva` sigue `responsable_gestion = NULL`, el problema está en las reglas/candidatos de asignación automática. Si lo asigna, entonces el alta GIS no está llamando o no está persistiendo correctamente esa asignación antes de intentar crear workflow.

---

### [20/08/2026 14:06:33] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 14:07:11] Agente

Sí, Wilmer. Esos nombres de columna no existen en `SOLICITUDES_AUTO` en esta base. Mi consulta fue demasiado genérica.

Usá esta versión reducida con columnas ya confirmadas:

```sql
SELECT
    soli_auto,
    soli_esta,
    prestad,
    responsable_gestion,
    responsable_auditoria,
    usuario,
    tipo_usuario,
    origen,
    fecha,
    alta_fecha,
    modi_fecha
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920114
```

Para validar datos adicionales de clasificación sin asumir nombres, primero sacá la estructura:

```sql
SELECT
    COLUMN_NAME
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'SOLICITUDES_AUTO'
ORDER BY ORDINAL_POSITION
```

Después de eso ajustamos la consulta con los nombres reales.

Por ahora, lo importante ya quedó confirmado: `responsable_gestion = NULL`, y por eso `pru_solicitudes_auto_workflow` no inserta workflow para la solicitud en estado `R`.

---

### [20/08/2026 14:09:22] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 14:09:57] Agente

Confirmado, Wilmer.

La solicitud `25920114` quedó así:

```text
soli_esta = R
prestad = 32
responsable_gestion = NULL
responsable_auditoria = NULL
usuario = 10123080
tipo_usuario = P
origen = G
```

Diagnóstico directo: **no se generó workflow porque no hay responsable asignado**.

Para estado `R`, el workflow necesita `responsable_gestion`. Como está `NULL`, `dbo.pru_solicitudes_auto_workflow` no inserta en `SOLICITUDES_AUTO_WORKFLOW`.

No ejecutes todavía `pru_proceso_asignacion_masiva`, porque puede modificar la solicitud. Primero validemos por qué no asignó responsable.

Ejecutá este SQL para revisar si existen usuarios candidatos activos para asignación:

```sql
SELECT
    usuario,
    nombre,
    activo,
    responsable_gestion,
    responsable_auditoria,
    program_name,
    fecha_logueo
FROM USUARIOS
WHERE program_name = 'prestaciones'
ORDER BY fecha_logueo DESC
```

Si esa tabla no tiene esas columnas, pasame el error o la grilla. La hipótesis fuerte es esta:

```text
El alta GIS registró la solicitud en R y notificó correctamente,
pero el proceso de asignación no encontró responsable_gestion elegible.
Por eso no se insertó workflow.
```

Con el resultado de usuarios candidatos cerramos si el problema es falta de usuario logueado/elegible o una condición interna del SP de asignación.

---

### [20/08/2026 14:13:27] Wilmer Higuera

Mira esta imagen

---

### [20/08/2026 14:13:45] Agente

Sí, Wilmer. Esa imagen confirma que **no hay una tabla `USUARIOS` simple** visible en ese esquema; mi consulta anterior no aplica tal cual en esta base.

No ejecutes procesos todavía. Primero ubiquemos qué tabla usa realmente `pru_proceso_asignacion_masiva` para buscar responsables.

Ejecutá este SQL:

```sql
SELECT
    referenced_schema_name,
    referenced_entity_name,
    referenced_class_desc
FROM sys.sql_expression_dependencies
WHERE referencing_id = OBJECT_ID('dbo.pru_proceso_asignacion_masiva')
ORDER BY referenced_entity_name
```

Luego este para buscar objetos relacionados con usuarios/responsables:

```sql
SELECT
    name,
    type_desc
FROM sys.objects
WHERE name LIKE '%USUARI%'
   OR name LIKE '%USER%'
   OR name LIKE '%RESPONS%'
   OR name LIKE '%ASIGN%'
ORDER BY type_desc, name
```

Y este para confirmar si el SP menciona `responsable_gestion`, `prestaciones` o tablas de usuario:

```sql
SELECT
    OBJECT_SCHEMA_NAME(object_id) AS esquema,
    OBJECT_NAME(object_id) AS objeto
FROM sys.sql_modules
WHERE definition LIKE '%responsable_gestion%'
   OR definition LIKE '%prestaciones%'
   OR definition LIKE '%program_name%'
ORDER BY objeto
```

Con eso identificamos el objeto real que usa la asignación. La conclusión actual no cambia: `SOLICITUDES_AUTO_WORKFLOW` no se inserta porque `responsable_gestion` quedó `NULL`; ahora falta ubicar **por qué `pru_proceso_asignacion_masiva` no asignó responsable**.

---

### [20/08/2026 15:31:16] Wilmer Higuera

Ese tema lo debemos validar desde el SP pru_registra_solicitudes_auto, ya que con esa condición @var_responsable_gestion IS NOT NULL, no estaría entrando al SP pru_solicitudes_auto_workflow, toma apuntes por favor de este punto y lo dejamos pendiente, pues la prioridad es la notificación de solicitudes

---

### [20/08/2026 15:31:31] Agente

Apuntado, Wilmer.

Pendiente técnico para retomar después:

```text
CASO 24 - WORKFLOW
SP a revisar: dbo.pru_registra_solicitudes_auto

Hallazgo:
El registro en SOLICITUDES_AUTO_WORKFLOW depende de esta condición:

IF @var_responsable_gestion IS NOT NULL
BEGIN
    EXEC dbo.pru_solicitudes_auto_workflow @prm_soli_auto = @var_soli_auto
END

Para la solicitud 25920114:
responsable_gestion = NULL
responsable_auditoria = NULL
soli_esta = R
origen = G

Conclusión:
No entra a pru_solicitudes_auto_workflow porque @var_responsable_gestion queda NULL.
La causa debe analizarse dentro de pru_registra_solicitudes_auto, específicamente en la lógica que asigna @var_responsable_gestion:
1. Autoasignación por rol ZZ_ROLES / ZZ_USUARI.
2. Fallback por dbo.pru_proceso_asignacion_masiva.
3. Motivo de no asignación devuelto en @var_motivo_no_asignacion.
```

Y sí: lo dejamos pendiente. La prioridad sigue siendo **notificación de solicitudes**, no corregir workflow ahora.

---

### [20/08/2026 15:59:25] Wilmer Higuera

Pasemos al siguiente caso.
CASO 25DESCRIPCIÓN
REVISION DE NOTIFICACIONES ESTADO DEVOLUCION
EVIDENCIA
SE REVISA NOTIFICACION
Imagen 1
En la imagen se ve que tiene la tabla interna
devuelvo la solicitud generada en GIS
Imagen 2
REVISO DATOS DE DEVOLUCIÓN
REVISO CUERPO DE MENSAJE ESPERADO
Imagen 3
REVISO CUERPO DE MENSAJE ENVIADO POR EL PROCESO DE DEVOLUCIÓN
Imagen 4
REVISAR
SE CONTINUA CON LA REVISIÓN
SE OBSERVA QUE PARA EL CASO DE DEVOLUCIÓN
CUANDO SE DEVUELVE AL PRESTADOR, TAMBIEN SE ENVÍA EMAIL DE DEVOLUCIÓN AL AFILIADO
WILMER, TE DEJO EN CLARO LAS REGLAS DE DEVOLUCIÓN:
PARA CUANDO SE REALIZA UNA DEVOLUCIÓN DE UNA SOLICITUD REGISTRADA POR EL PRESTADOR SE ENVÍA NOTIFICACIÓN SOLO AL PRESTADOR
PARA CUANDO SE REALIZA UNA DEVOLUCIÓN DE UNA SOLICITUD REGISTRADA POR EL AFILIADO, SE ENVÍA NOTIFICACIÓN SOLO AL AFILIADO
Imagen 5
ACA NOTIFICA A LOS DOS
LO QUE HAY QUE HACER ES:
(SOLO PARA DEVOLUCION)
SOLICITUD ORIGEN GIS, GENERADA POR EL PRESTADOR, CUANDO SE DEVUELVE LA SOLICITUD AL PRESTADOR, SE NOTIFICA SOLO AL PRESTADOR Y NO AL AFILIADO.
CUANDO LA SOLICITUD ES ORIGEN APP Y EL QUE CREA LA SOLICITUD ES UN AFILIADO, CUANDO SE DEVUELVE SOLO SE NOTIFICA AL AFILIADO Y NO AL PRESTADOR.
ORIGEN PRESMED NO TIENE DEVOLUCION

---

### [20/08/2026 16:03:06] Agente

Wilmer, confirmado el Caso 25.

La parametrización muestra:

```text
@prm_id = 2  -> Notificación PRESTADOR, estado DEVUELTA
@prm_id = 16 -> Notificación AFILIADO, estado DEVUELTA
```

Y el problema está en `dbo.pru_solicitudes_auto_devolucion`, en este bloque:

```sql
IF @var_tipo_usuario_soli = 'P'
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id = 2,
        @prm_soli_auto = @prm_soli_auto
        
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id        = 16,
        @prm_soli_auto = @prm_soli_auto
END

IF @var_tipo_usuario_soli IN ('A', 'E')
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id = 16,
        @prm_soli_auto = @prm_soli_auto
```

Diagnóstico: cuando la solicitud fue creada por prestador (`tipo_usuario = 'P'`), el SP ejecuta **dos notificaciones**:

```text
2  = prestador devuelta
16 = afiliado devuelta
```

Eso explica exactamente la evidencia: al devolver al prestador, también se envía mail al afiliado.

La regla funcional que debemos aplicar para devolución sería:

```sql
IF @var_tipo_usuario_soli = 'P'
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id        = 2,
        @prm_soli_auto = @prm_soli_auto
END

IF @var_tipo_usuario_soli IN ('A', 'E')
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id        = 16,
        @prm_soli_auto = @prm_soli_auto
END
```

Es decir: **quitar el llamado a `@prm_id = 16` dentro del bloque `P`**.

Para validar antes de tocar nada, usá la solicitud GIS prestador que vas a devolver y ejecutá antes/después:

```sql
SELECT
    soli_auto,
    soli_esta,
    tipo_usuario,
    origen,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <solicitud>
```

Y después de devolver:

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuari,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = <solicitud>
ORDER BY fecha_hora DESC
```

Si aparece `proceso_id: 2` y `proceso_id: 16` para una solicitud `tipo_usuario = 'P'`, queda probado el defecto.

---

### [20/08/2026 16:07:11] Wilmer Higuera

Sin probarlo, esta claro que si debemos quitar ese llamado a notificaciones dentro de @var_tipo_usuario_soli = 'P' con , @prm_id = 16, esa ya queda listo, ahora miremos la parte de la diferencia en el cuerpo del mensaje, por favor

---

### [20/08/2026 16:14:58] Agente

Sí, Wilmer. Sin probar envío, el análisis del cuerpo apunta a `dbo.pri_solicitudes_auto_notif`.

Para devolución, el cuerpo se toma de:

```sql
SELECT
    @var_subjet = N.subject,
    @var_body   = N.body
FROM SOLICITUDES_AUTO_NOTIF_PLANT N
WHERE N.id = @prm_id
AND N.baja_fecha IS NULL
```

Entonces:

- `@prm_id = 2`: plantilla devolución prestador.
- `@prm_id = 16`: plantilla devolución afiliado.

La diferencia más importante que veo en plantilla es esta:

```text
Plantilla 2 - Prestador:
usa @dias_dev_txt y @dias_dev_nro
```

```text
Plantilla 16 - Afiliado:
usa @dife_dias_auto_orden
```

Eso es sospechoso. Para devolución, el tiempo correcto debería venir de parámetros de devolución:

```sql
@dias_dev_nro
@dias_dev_txt
```

porque esos salen de:

```sql
dbo.afn_ParametrosGrales('Presmed', 'Autorizaciones', 'vencimiento_devolucion_soli')
```

En cambio `@dife_dias_auto_orden` viene de `PARAMETROS.dias_auto_orden`, que parece más relacionado con vigencia de autorización/orden, no con plazo de gestión de devolución.

Para validar la diferencia exacta en base, ejecutá esto:

```sql
SELECT
    id,
    subject,
    body
FROM SOLICITUDES_AUTO_NOTIF_PLANT
WHERE id IN (2, 16)
```

Y para revisar los valores que reemplaza el SP:

```sql
SELECT
    dias_auto_orden,
    url_consulta_solicitudes,
    url_oficinavirtualmp
FROM PARAMETROS

SELECT
    vencimiento_devolucion_soli = dbo.afn_ParametrosGrales('Presmed', 'Autorizaciones', 'vencimiento_devolucion_soli'),
    vencimiento_devolucion_soli_auditoria = dbo.afn_ParametrosGrales('Presmed', 'Autorizaciones', 'vencimiento_devolucion_soli_auditoria')
```

Mi hipótesis técnica: además de quitar el llamado a `@prm_id = 16` cuando `tipo_usuario = 'P'`, hay que revisar/corregir la plantilla `id = 16` para que use `@dias_dev_txt (@dias_dev_nro)` en lugar de `@dife_dias_auto_orden`, si el cuerpo esperado para afiliado también habla de plazo de devolución.

---

### [20/08/2026 16:34:17] Wilmer Higuera

Valida por favor esos dos procesos en la especificación funcional y compara la plantilla de la especificación contra la que tenemos en la base de datos InstallQA, si están iguales, mejor a la ultima solicitud que registramos, le hacemos la devolución y validamos la plantilla que envía a ver como sale

---

### [20/08/2026 16:36:57] Agente

Wilmer, validé los dos procesos contra la EF y contra InstallQA.

La EF confirma estos procesos:

- `Notificación PRESTADOR, estado de solicitud DEVUELTA`: evento `Devolver Solicitud`, destino correo del prestador asociado.
- `Notificación AFILIADO, estado de solicitud DEVUELTA`: destino correo del afiliado ingresado.
- También define `@dias_dev_txt` / `@dias_dev_nro` como cantidad de días para resolver devolución/anulación automática.

Contra InstallQA, las plantillas activas son:

```text
id 2  = Prestador devuelta
id 16 = Afiliado devuelta
```

Pero **no están iguales** a la plantilla propuesta de la carpeta. En InstallQA ambas tienen texto agregado al inicio del body:

```text
id 2:  NOTIFICACION AL PRESTADOR SOLICITUD DEVUELVA
id 16: NOTIFICACION AFILIADO ESTADO DEVUELTA
```

Además, la diferencia funcional importante sigue siendo esta:

```text
id 2  usa @dias_dev_txt (@dias_dev_nro)
id 16 usa @dife_dias_auto_orden
```

En InstallQA los valores actuales son:

```text
PARAMETROS.dias_auto_orden = 45
vencimiento_devolucion_soli = 0
vencimiento_devolucion_soli_auditoria = 0
```

Conclusión: **no están iguales** y el cuerpo enviado puede salir distinto por esos textos de control y por el uso de `@dife_dias_auto_orden` en afiliado.

La última solicitud candidata es:

```text
soli_auto = 25920115
soli_esta = R
tipo_usuario = P
origen = G
prestad = 32
mail_id_prestad_mail = 9989065
mail_id_afi_mail = 9989067
```

Si querés validar cómo sale realmente, devolvé esa solicitud desde GIS/PRESMED y después ejecutá:

```sql
SELECT
    soli_auto,
    soli_esta,
    tipo_usuario,
    origen,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920115
```

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuario,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920115
ORDER BY fecha_hora DESC
```

Lo esperado con el fix pendiente aplicado sería: solo `proceso_id: 2`, solo tipo `P`, y `mail_id_afi_mail` sin nuevo envío por devolución.

---

### [20/08/2026 16:42:12] Wilmer Higuera

Una pregunta antes de hacer la devolución, en la base de datos, la plantilla 2 tiene este texto y la tabla?

---

### [20/08/2026 16:44:14] Agente

Sí, Wilmer. En la plantilla **2** que tenemos en el script de la tarea sí está ese texto y la tabla:

```html
<p>El motivo por el cual se devolvio la solicitud fue:</p>
<table border="1" cellpadding="4" cellspacing="0">
  <tr><th>Motivo</th><th>Observaciones</th></tr>
  <tr><td>@moti_dev</td><td>@observ_dev</td></tr>
</table>
```

Ojo: en base/script está sin tilde: `devolvio`, no `devolvió`. Visualmente en el correo puede verse igual de estructura, pero el texto exacto no tiene acento.

Para confirmarlo directo en InstallQA antes de devolver, ejecutá:

```sql
SELECT
    id,
    subject,
    body
FROM SOLICITUDES_AUTO_NOTIF_PLANT
WHERE id = 2
```

Y si querés una validación rápida:

```sql
SELECT
    tiene_texto_motivo = CHARINDEX('El motivo por el cual se devolvio la solicitud fue', body),
    tiene_moti_dev     = CHARINDEX('@moti_dev', body),
    tiene_observ_dev   = CHARINDEX('@observ_dev', body),
    tiene_table        = CHARINDEX('<table', body)
FROM SOLICITUDES_AUTO_NOTIF_PLANT
WHERE id = 2
```

Si todos devuelven un número mayor a `0`, la plantilla 2 en base tiene ese bloque.

---

### [20/08/2026 16:54:30] Wilmer Higuera

Valida en la base de datos si ninguna de las plantillas tiene tilde, porfa

---

### [20/08/2026 16:56:38] Agente

Sí, Wilmer. Validado en `InstallQA`.

Resultado:

```text
Plantillas activas: 26
Plantillas activas con tilde: 0

Total plantillas: 26
Total plantillas con tilde: 0
```

También validé específicamente las de devolución:

```text
id 2  -> subject: Devolucion Solicitud No. @soli
id 16 -> subject: Devolucion Solicitud No.@soli
```

Y en el body ambas tienen `devolvio` sin tilde:

```text
id 2  -> tiene "devolvio", no tiene "devolvió"
id 16 -> tiene "devolvio", no tiene "devolvió"
```

Conclusión: en la base `InstallQA`, ninguna plantilla tiene vocales acentuadas.

---

### [20/08/2026 17:02:25] Wilmer Higuera

Del scripts SOLICITUDES_AUTO_NOTIF_PLANT.sql ubicado en A:\whiguera\Tarea #23986\Tarea #29489, verifica si según la especificación solo le faltan las tildes, y si tiene las negritas donde corresponda, si solo faltan las tildes, modificas el scripts por favor quedando con las tildes, para después si realizamos la devolución y validamos si sale bien la plantilla en la notificación

---

### [20/08/2026 17:11:34] Agente

Listo, Wilmer. Validé contra la EF `R23986-EF_V4.doc` y el script tenía las negritas donde correspondía: `Estimado/Apreciado`, `Coomeva Medicina Prepagada`, encabezados `Motivo/Observaciones`, botón `Devolver`, cierres, etc.

Modifiqué el script:

`A:\whiguera\Tarea #23986\Tarea #29489\SOLICITUDES_AUTO_NOTIF_PLANT.sql`

Cambios aplicados:
- Solo corrección de tildes/texto visible de plantillas.
- No cambié estructura SQL.
- No cambié variables: validé que las variables del script quedaron iguales.
- Validé que siguen siendo 26 plantillas.
- Validé específicamente devolución `id = 2` y `id = 16`.

También comparé el script acentuado contra la propuesta original quitando tildes por transliteración, y no hubo diferencias. Eso confirma que el cambio quedó limitado a acentos. No ejecuté nada en base de datos.

---

### [20/08/2026 17:35:10] Wilmer Higuera

Pre-compaction memory flush. Store durable memories only in memory/2026-08-20.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-20.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-20-HHMM.md); always use the canonical 2026-08-20.md filename. If nothing to store, reply with NO_REPLY.
Current time: Thursday, August 20th, 2026 - 5:35 PM (America/Buenos_Aires)
Reference UTC: 2026-08-20 20:35 UTC

---

### [20/08/2026 17:37:06] Agente

NO_REPLY

---

### [20/08/2026 17:37:07] Wilmer Higuera

Listo, la devolví, mira, validemos la plantilla y si envió solo al prestador

---

### [20/08/2026 17:38:26] Agente

Perfecto, Wilmer. Validemos sobre la solicitud de la imagen:

```text
25920114
```

Ejecutá primero esto para confirmar si cambió solo el `mail_id_prestad_mail` o también el de afiliado:

```sql
SELECT
    soli_auto,
    soli_esta,
    tipo_usuario,
    origen,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920114
```

Luego validá auditoría de devolución:

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuario,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = 25920114
ORDER BY fecha_hora DESC
```

Lo que buscamos:

- Para solicitud GIS creada por prestador, debe aparecer solo:
  - `proceso_id: 2`
  - `notifica: Prestador`
  - `tipo_usuari = 'P'`
- No debería aparecer:
  - `proceso_id: 16`
  - `notifica: Afiliado`
  - `tipo_usuari = 'A'`

Para validar el cuerpo real del mail, primero tomá de auditoría el nuevo `mail_id` de devolución prestador. Luego ejecutá, ajustando el `mail_id`:

```sql
SELECT *
FROM MAIL_ENVIO
WHERE mail_id = <mail_id_devolucion_prestador>
```

Si esa tabla no existe en InstallQA, ejecutá esto para ubicar dónde quedó guardado el cuerpo:

```sql
SELECT
    t.name AS tabla,
    c.name AS columna
FROM sys.tables t
JOIN sys.columns c ON c.object_id = t.object_id
WHERE c.name LIKE '%mail%'
   OR c.name LIKE '%text%'
   OR c.name LIKE '%body%'
ORDER BY t.name, c.column_id
```

Pasame la auditoría y, si podés, el registro del mail. Con eso confirmamos si envió solo al prestador y si la plantilla salió con el texto/tabla esperado.

---

### [20/08/2026 17:43:27] Wilmer Higuera

Mira el resultado, te comparto dos imágenes

---

### [20/08/2026 17:43:42] Agente

Sí, Wilmer. Con esta imagen ya se confirma lo principal:

- Solicitud `25920114` quedó en estado `D`.
- `mail_id_prestad_mail` cambió a `9989068`.
- `mail_id_afi_mail` quedó igual en `9989064`.
- En auditoría aparece devolución con `tipo_usuari = 'P'`.
- No aparece nueva auditoría tipo `A` para devolución.

Conclusión: **la devolución envió solo al prestador**, como corresponde para solicitud origen `GIS`, creada por prestador.

Sobre la plantilla, el mail `9989068` muestra:

```text
Subject: Devolución Solicitud No. 25920114
Body inicia con: Estimado Aliado...
```

Eso está alineado con la plantilla prestador. Para validar el cuerpo completo y confirmar tabla de motivo/observación, ejecutá:

```sql
SELECT
    mail_id,
    subject,
    html,
    text
FROM MAIL_ENVIO
WHERE mail_id = 9989068
```

Si SSMS te corta el campo `text`, usá:

```sql
SELECT CAST(text AS VARCHAR(MAX)) AS cuerpo_mail
FROM MAIL_ENVIO
WHERE mail_id = 9989068
```

Pasame la segunda imagen o el cuerpo completo, y validamos si ya sale con tildes y con la tabla de devolución como quedó en el script.

---

### [20/08/2026 17:44:59] Wilmer Higuera

[User sent media without caption]

---

### [20/08/2026 17:45:08] Agente

Validación correcta, Wilmer.

Para la solicitud `25920114`:

- Se envió solo al prestador: `mail_id_prestad_mail = 9989068`.
- No se generó nuevo envío al afiliado: `mail_id_afi_mail` quedó en `9989064`.
- Auditoría de devolución quedó tipo `P`.
- El cuerpo usa plantilla de prestador:
  - `Estimado Aliado`
  - tabla con `Motivo` y `Observaciones`
  - motivo `SOPORTES INCOMPLETOS`
  - botón `Devolver` en negrita
  - tildes correctas: `devolvió`, `recepción`, `gestión`, `automática`, `botón`, `comuníquese`, `país`.

Punto observado: el plazo sale como **cero (0) días**, pero eso coincide con el parámetro que habíamos validado en InstallQA:

```text
vencimiento_devolucion_soli = 0
```

Conclusión: **Caso 25 prestador queda validado funcionalmente** para la regla de devolución GIS/prestador y para la plantilla enviada.

---

### [20/08/2026 18:03:57] Wilmer Higuera

Listo, ya quedo el caso 25, pasemos al caso 26, dice así:
CASO 26DESCRIPCIÓN
REVISION DE NOTIFICACIONES estado EN GESTION

EVIDENCIA
SE REVISA NOTIFICACIÓN
SE CREA SOLICITUD
Imagen 1
SE GENERAN AUTORIZACIONES
Imagen 2
LA SOLICITUD QUEDA EN ESTADO EN GESTIÓN
Imagen 3
SE REvisan emails enviados
se observa que se notifica por estado REGISTRADA
NO se observa envío de notificación por estado EN GESTION
Imagen 4
WILMER EL ESTADO EN GESTION SOLO SE ASIGNA DESDE PRESMED (BOTONES DE EDITAR, GENERAR AUTORIZACIONES)

SOLO SE ENVÍA/NOTIFICA UNA VEZ.(LA PRIMERA VEZ)


REVISAR

---

### [20/08/2026 18:16:58] Wilmer Higuera

Listo, ya quedo el caso 25, pasemos al caso 26, dice así:
CASO 26DESCRIPCIÓN
REVISION DE NOTIFICACIONES estado EN GESTION

EVIDENCIA
SE REVISA NOTIFICACIÓN
SE CREA SOLICITUD
Imagen 1
SE GENERAN AUTORIZACIONES
Imagen 2
LA SOLICITUD QUEDA EN ESTADO EN GESTIÓN
Imagen 3
SE REvisan emails enviados
se observa que se notifica por estado REGISTRADA
NO se observa envío de notificación por estado EN GESTION
Imagen 4
WILMER EL ESTADO EN GESTION SOLO SE ASIGNA DESDE PRESMED (BOTONES DE EDITAR, GENERAR AUTORIZACIONES)

SOLO SE ENVÍA/NOTIFICA UNA VEZ.(LA PRIMERA VEZ)


REVISAR

---

### [20/08/2026 18:16:59] Agente

Wilmer, para el **Caso 26 - estado EN GESTIÓN**, el diagnóstico inicial queda así:

El proceso de notificación existe:

```text
@prm_id = 4
Notificación PRESTADOR, estado de solicitud EN GESTION
```

La plantilla también existe en `SOLICITUDES_AUTO_NOTIF_PLANT.sql`:

```text
id 4 = Gestión Solicitud No.@soli
```

El flujo funcional de la imagen entra por Presmed, botón **Generar Autorizaciones**. En PowerBuilder ese botón llama:

```text
wp_soli_reg.srw
cb_gen_auto.clicked
PARENT.EVENT ue_aceptar(1 /*genera*/, 0 /*verifica*/)
```

Eso termina ejecutando:

```sql
dbo.pru_genera_auto_precarga
    @prm_verifica = 'N'
```

Dentro de `pru_genera_auto_precarga`, después de generar autorizaciones, se calcula el estado actual:

```sql
EXEC dbo.prs_solicitudes_auto_esta_actual
    @prm_soli_auto = @prm_soli_auto,
    @prm_soli_esta = @var_soli_esta OUTPUT
```

Y luego actualiza:

```sql
UPDATE SOLICITUDES_AUTO
SET soli_esta = @var_soli_esta,
    modi_fecha = @var_hoy,
    responsable_auditoria = @var_responsable_auditoria
WHERE soli_auto = @prm_soli_auto
```

Pero ahí **no hay llamada** a:

```sql
EXEC dbo.pri_solicitudes_auto_notif
    @prm_id = 4,
    @prm_soli_auto = @prm_soli_auto
```

Conclusión: el caso apunta a `dbo.pru_genera_auto_precarga`. La solicitud sí pasa a `G`, pero no se dispara la notificación `@prm_id = 4`.

Para validar con la solicitud del caso, ejecutá:

```sql
SELECT
    soli_auto,
    soli_esta,
    tipo_usuario,
    origen,
    mail_id_prestad_mail,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <solicitud>
```

```sql
SELECT TOP 20
    soli_auto,
    activi,
    usuario,
    tipo_usuari,
    fecha_hora
FROM SOLICITUDES_AUTO_AUDITORIA
WHERE soli_auto = <solicitud>
ORDER BY fecha_hora DESC
```

Lo esperado con el defecto actual:

- `soli_esta = G`.
- Hay auditoría de `Generación de Autorizaciones`.
- Hay notificación registrada `proceso_id: 1`.
- No hay auditoría con `proceso_id: 4`.

La corrección conceptual sería insertar el llamado a `pri_solicitudes_auto_notif @prm_id = 4` en `pru_genera_auto_precarga`, después del `UPDATE SOLICITUDES_AUTO`, solo cuando:

```sql
@prm_verifica = 'N'
AND @var_soli_esta = 'G'
AND no exista notificación previa proceso_id: 4 para esa solicitud
```

Así cumple la regla: **solo se notifica la primera vez que queda En Gestión**.

---

### [20/08/2026 18:30:08] Wilmer Higuera

Mira el resultado de las consultas, imagen 1.
Dame el paso a paso, indicándome con mucho detalle y con claridad donde insertar el llamado a la notificación

---

### [20/08/2026 18:30:39] Agente

Sí, Wilmer. La evidencia confirma el defecto:

- Solicitud `25920104` quedó en `soli_esta = G`.
- Tiene auditoría de `Generación de Autorizaciones`.
- Tiene workflow en estado `G`.
- No tiene `mail_id_prestad_mail`.
- No aparece auditoría `Notificación solicitud en gestión`.
- Solo aparece notificación registrada al afiliado, `tipo_usuari = A`.

El llamado debe ir en `dbo.pru_genera_auto_precarga`, dentro del bloque real de generación, no en simulación.

**Ubicación Exacta**

Buscá este bloque en `pru_genera_auto_precarga`, cerca del final del SP:

```sql
UPDATE SOLICITUDES_AUTO
SET soli_esta                = @var_soli_esta,
    modi_fecha               = @var_hoy,
    responsable_auditoria    = @var_responsable_auditoria
WHERE soli_auto = @prm_soli_auto

--WORKFLOW
EXEC dbo.pru_solicitudes_auto_workflow
    @prm_soli_auto = @prm_soli_auto

EXEC dbo.pri_solicitudes_auto_auditoria
    @prm_soli_auto   = @prm_soli_auto,
    @prm_activi      = 'Generación de Autorizaciones',
    @prm_usuari      = @var_usuari,
    @prm_tipo_usuari = 'E'
```

Insertá el llamado **después del `UPDATE SOLICITUDES_AUTO` y antes del `--WORKFLOW`**.

Debe quedar así:

```sql
UPDATE SOLICITUDES_AUTO
SET soli_esta                = @var_soli_esta,
    modi_fecha               = @var_hoy,
    responsable_auditoria    = @var_responsable_auditoria
WHERE soli_auto = @prm_soli_auto

IF @var_soli_esta = 'G'
AND NOT EXISTS (
    SELECT 1
    FROM SOLICITUDES_AUTO_AUDITORIA
    WHERE soli_auto = @prm_soli_auto
      AND activi LIKE '%proceso_id: 4%'
)
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id        = 4,
        @prm_soli_auto = @prm_soli_auto
END

--WORKFLOW
EXEC dbo.pru_solicitudes_auto_workflow
    @prm_soli_auto = @prm_soli_auto

EXEC dbo.pri_solicitudes_auto_auditoria
    @prm_soli_auto   = @prm_soli_auto,
    @prm_activi      = 'Generación de Autorizaciones',
    @prm_usuari      = @var_usuari,
    @prm_tipo_usuari = 'E'
```

**Por Qué Ahí**

Ese bloque solo corre cuando `@prm_verifica = 'N'`, o sea cuando se presiona **Generar Autorizaciones**, no cuando se simula.

Además, en ese punto ya se calculó el estado real con:

```sql
EXEC dbo.prs_solicitudes_auto_esta_actual
```

y ya se actualizó `SOLICITUDES_AUTO.soli_esta`.

La condición evita duplicados:

```sql
@var_soli_esta = 'G'
```

solo notifica si quedó En Gestión.

```sql
NOT EXISTS (... proceso_id: 4 ...)
```

solo notifica la primera vez.

Después de compilar, la validación esperada para una nueva prueba es que aparezca auditoría:

```text
Notificación solicitud en gestión. mail_id: ...
proceso_id: 4
tipo_usuari = P
```

y que `mail_id_prestad_mail` pase de `NULL` a un nuevo `mail_id`.

---

### [20/08/2026 18:49:28] Wilmer Higuera

Guarda lo que hemos realizado el día de hoy, con el detalle suficiente para mañana continuar donde quedamos. me indicas como te escribo para que recuperes donde quedamos y podamos continuar, hasta mañana y muchas gracias por tu colaboración.

---

### [20/08/2026 18:51:07] Agente

Listo, Wilmer. Dejé guardado el cierre de hoy en:

`memory/2026-08-20.md`

Quedó asentado con detalle:
- Caso 25 cerrado y validado.
- Caso 26 en curso.
- SP a revisar: `dbo.pru_genera_auto_precarga`.
- Punto exacto donde insertar el llamado a `pri_solicitudes_auto_notif @prm_id = 4`.
- Condición para que se notifique sólo la primera vez.
- Validación esperada para mañana.

Mañana escribime algo así:

```text
Lisa, retomemos la memoria del 2026-08-20 de la Tarea 29489, Caso 26, desde el punto del SP pru_genera_auto_precarga para insertar la notificación @prm_id = 4.
```

Con eso recupero el contexto y seguimos desde ahí.

Hasta mañana, Wilmer. Quedamos bien parados para continuar.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
