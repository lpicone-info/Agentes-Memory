# Memoria de conversación — Wilmer Higuera

## Datos

- **Fecha:** 29/08/2026
- **Usuario:** Wilmer Higuera
- **ID Google Chat:** `users/113120678211994207734`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 36
- **Inicio:** 12:58:19
- **Fin:** 18:10:06

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/qzugpyaaaae`

## Archivos revisados

- `6e432d1f-325d-4dca-9c60-d81bfa7c6a47.jsonl`

---

## Transcripción completa

### [29/08/2026 12:58:19] Wilmer Higuera

Hola Lisa, buenos días

---

### [29/08/2026 12:59:45] Agente

Hola Wilmer, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [29/08/2026 13:01:30] Wilmer Higuera

Lisa, ten en cuenta estos dos puntos:
## Cosas que no hacer
1. NO realizar modificaciones directas en el entorno ni hacer Commits. El agente solo propone y muestra los cambios por pantalla o archivo; la ejecución de modificaciones y commits es responsabilidad exclusiva de whiguera.
2. NO asumir que un paso se ha comprendido o cerrado de forma automática sin el visto bueno de whiguera.

---

### [29/08/2026 13:01:38] Agente

Wilmer, entendido.

Esos dos puntos ya están cargados en tus preferencias y los voy a respetar como criterio operativo para esta sesión:

1. No voy a realizar modificaciones directas en el entorno ni commits.
2. No voy a avanzar de un paso al siguiente sin tu confirmación explícita de que el paso anterior quedó comprendido, terminado, guardado o testeado.

---

### [29/08/2026 13:06:45] Wilmer Higuera

Lisa, retomemos la Tarea 29489 desde la memoria del 2026-08-28, Caso 10, para que me des el paso a paso detallado de implementación de los cambios en prs_afiliado_extras, wp_soli_reg, prs_solicitudes_auto_afi y la revisión de pru_registra_solicitudes_auto.

---

### [29/08/2026 13:10:01] Agente

Wilmer, retomé el **Caso 10 - Tarea 29489** desde memoria del 28/08 y lo validé contra el repositorio activo `whiguera-Version84-Modelo2Gis`. No modifiqué archivos.

La causa técnica sigue siendo la misma: en PRESMED se está tratando el mail alternativo como único dato operativo. `prs_afiliado_extras` hoy devuelve solo `email2/sms2`; `wp_soli_reg` lo carga en `email_alter`; y luego `wf_registra_solicitudes_auto()` lo envía a `pru_registra_solicitudes_auto` solo como `@prm_email_afi_2`. Memoria: `memory/2026-08-28.md#L18-L34`.

**Paso 1 - `prs_afiliado_extras.sql`**

Objetivo: devolver también el email principal, sin cambiar el significado actual de `@prm_email2` y `@prm_sms2`.

Cambio propuesto:

```sql
CREATE PROCEDURE dbo.prs_afiliado_extras
(
    @prm_prepaga SMALLINT,
    @prm_contra CHAR(15),
    @prm_inte CHAR(2),
    @prm_echo CHAR(1) = 'S',
    @prm_email2 VARCHAR(100) = NULL OUTPUT,
    @prm_sms2 VARCHAR(30) = NULL OUTPUT,
    @prm_email1 VARCHAR(100) = NULL OUTPUT
)
AS
BEGIN
    SET NOCOUNT ON
    SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED

    DECLARE
        @var_email1 VARCHAR(100),
        @var_email2 VARCHAR(100),
        @var_sms2 VARCHAR(30)

    SELECT
        @var_email1 = RUE.email,
        @var_email2 = RUE2.email,
        @var_sms2 = RUT2.tele
    FROM AFILIADOS A
    -- mantener los JOIN actuales sin cambiar criterios
    WHERE A.prepaga = @prm_prepaga
      AND A.contra = @prm_contra
      AND A.inte = @prm_inte

    IF @prm_echo = 'S'
    BEGIN
        SELECT
            email2 = @var_email2,
            sms2 = @var_sms2,
            email1 = @var_email1
    END
    ELSE
    BEGIN
        SET @prm_email2 = @var_email2
        SET @prm_sms2 = @var_sms2
        SET @prm_email1 = @var_email1
    END
END
GO
```

Punto clave: dejo `email2` y `sms2` primero en el `SELECT` para preservar el orden anterior y agrego `email1` al final.

**Paso 2 - `prs_solicitudes_auto_afi.sql`**

Objetivo: cuando se recupera una solicitud existente, devolver también `email_afi_1`, porque hoy solo vuelve `email_alter = S.email_afi_2`.

Cambio puntual en el `SELECT`:

```sql
email_afi_1 = S.email_afi_1,
email_alter = S.email_afi_2,
tel_alter = S.sms_afi_2,
```

Esto requiere ajustar también el DataWindow `de_ingre_auto_afi_sol_reg.srd` para incorporar la columna no visual `email_afi_1`; si no, `wp_soli_reg` no va a poder leerla desde `idw_afi`.

**Paso 3 - `wp_soli_reg.srw`**

Objetivo: mantener separados email principal y alternativo dentro de la ventana.

Agregar variable de instancia:

```powerscript
STRING is_email_afi_1
```

En `wf_afi_datos_extras()`, cambiar variables locales:

```powerscript
STRING ls_contra, ls_inte, ls_email_afi_1, ls_email_afi_2, ls_tel, ls_null
```

Modificar el `FETCH`:

```powerscript
FETCH proc INTO :ls_email_afi_2, :ls_tel, :ls_email_afi_1;
```

Después del `CLOSE proc`, guardar el principal en variable interna:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_1) THEN
    is_email_afi_1 = ls_email_afi_1
ELSE
    SetNull(is_email_afi_1)
END IF
```

Mantener el comportamiento visual actual:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_2) THEN
    idw_afi.SetItem(1,'email_alter',ls_email_afi_2)
ELSE
    idw_afi.SetItem(1,'email_alter',ls_null)
END IF
```

En `wf_registra_solicitudes_auto()`, agregar variable local:

```powerscript
STRING ls_email_afi_1
```

Antes del `EXECUTE`, cargarla:

```powerscript
ls_email_afi_1 = is_email_afi_1
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

Y en el `DECLARE proc` agregar el parámetro:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

Cuando se hace `idw_afi.Retrieve(il_soli)` para solicitud existente, después del retrieve cargar:

```powerscript
IF NOT lib.of_vacio(idw_afi.object.email_afi_1[1]) THEN
    is_email_afi_1 = idw_afi.object.email_afi_1[1]
ELSE
    SetNull(is_email_afi_1)
END IF
```

Y en limpieza de afiliado, limpiar también:

```powerscript
SetNull(is_email_afi_1)
```

**Paso 4 - `pru_registra_solicitudes_auto.sql`**

La parte de alta ya está bien: inserta `email_afi_1 = @prm_email_afi_1` y `email_afi_2 = @prm_email_afi_2`.

El cambio necesario está en edición. Hoy actualiza solo `email_afi_2`. Agregar `email_afi_1`:

```sql
UPDATE SOLICITUDES_AUTO
SET soli_tipo = @prm_tipo,
    prestad = @prm_prestad,
    lugar = @prm_lugar,
    modi_fecha = @var_hoy,
    ingre_icd = @prm_ingre_icd,
    email_afi_1 = @prm_email_afi_1,
    email_afi_2 = @prm_email_afi_2,
    sms_afi_2 = @prm_sms_2
WHERE soli_auto = @prm_soli_auto
```

**Validación esperada**

Crear o recuperar una solicitud PRESMED del Caso 10 y verificar:

```sql
SELECT soli_auto, email_afi_1, email_afi_2, mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <solicitud>
```

Resultado esperado:

```text
email_afi_1 = email principal del afiliado
email_afi_2 = email alternativo/editado
```

No tocaría `pri_solicitudes_auto_notif`: ya prioriza `email_afi_1` y usa `email_afi_2` como fallback. Tampoco tocaría WebPos/GIS ni bandejas, porque según la revisión del 28/08 esos flujos ya separan ambos campos correctamente.

---

### [29/08/2026 15:12:03] Wilmer Higuera

Espera, muéstrame como es la función en GIS para recuperar email1 y email2

---

### [29/08/2026 15:13:24] Agente

Wilmer, en GIS/WebPos el flujo recupera **email principal** y **email alternativo** ya separados.

El punto de entrada visual está en `WebPos/js/afiliado.js`, función `cargar_afiliado()`:

```javascript
$.post(
    "controlador/afiliado_busqueda.php",
    {'prepaga': prepaga, 'contra': contra, 'inte': inte, 'creden': creden, 'sucur' : sucur})
    .done(
        function(data)
        {
            ...
            cartel += '<div class="col-lg-4 text-break"><strong>Correo Electrónico:&nbsp;</strong><span id="emailAfi">' + $.trim(data.email) + '</span></div>';

            if (editable == "S") {
                cartel += ` <div class="col-lg-4 text-break d-flex align-items-start">
                                <strong>Correo Electrónico 2:&nbsp;</strong>
                                <input type="text" class="form-control flex-grow-1" id="email2Afi" name="email2Afi" autocomplete="off" spellcheck="false" maxlength="100" value="${$.trim(data.email2)}" onchange="$(this).removeClass('obligatorio');">
                            </div>`;
            } else {
                cartel += '<div class="col-lg-4 text-break"><strong>Correo Electrónico 2:&nbsp;</strong><span id="email2Afi">' + $.trim(data.email2) + '</span></div>';
            }
```

Ahí se ve claramente:

```text
data.email  -> Correo Electrónico principal
data.email2 -> Correo Electrónico 2 / alternativo
```

El controlador que responde ese JSON es `WebPos/controlador/afiliado_busqueda.php`:

```php
$afiliado = new Afiliado();
$afiliado->setPrepaga($prepaga);
$afiliado->setContra($contra);
$afiliado->setInte($inte);
$afiliado->setCreden($creden);

$afiliado->recuperar(date('Ymd'), $sucur);

header('Content-type: application/json; charset=utf-8');
echo json_encode($afiliado, JSON_FORCE_OBJECT);
```

La recuperación real está en `WebPos/modelo/afiliado.php`, método `recuperar()`:

```php
$sql = "EXEC ars_afiliado @prm_prepaga = ?, @prm_contra = ?, @prm_inte = ?, @prm_creden = ?, @prm_fecha = ?, @prm_sucur = ?";

$this->execute($sql, $params);

while( $row = sqlsrv_fetch_array( $this->stmt, SQLSRV_FETCH_ASSOC) ) {
    ...
    $this->setEmail($row['email']);
    $this->setEmail2($row['email2']);
    $this->setSms1($row['sms1']);
    $this->setSms2($row['sms2']);
}
```

Y luego lo expone al frontend en el `jsonSerialize()`:

```php
'email' => $this->getEmail(),
'email2' => $this->getEmail2(),
'sms1' => $this->getSms1(),
'sms2' => $this->getSms2(),
```

El SP que alimenta eso es `backend/procedures/ars_afiliado.sql`. En la firma ya tiene ambos parámetros separados:

```sql
@prm_email  VARCHAR(100) = NULL OUTPUT,
@prm_email2 VARCHAR(100) = NULL OUTPUT,
@prm_sms1   VARCHAR(30)  = NULL OUTPUT,
@prm_sms2   VARCHAR(30)  = NULL OUTPUT,
```

Y en la consulta asigna cada dato separado:

```sql
@var_email  = COALESCE(MT.email_to, RUE.email),
@var_email2 = COALESCE(M2.email_to, RUE3.email, RUE2.email),
@var_sms1   = COALESCE(MT2.destino, LTRIM(RTRIM(RUT.tele))),
@var_sms2   = COALESCE(MT3.destino, LTRIM(RTRIM(RUT2.tele)))
```

Cuando devuelve por `SELECT`, lo hace así:

```sql
email  = @var_email,
email2 = @var_email2,
sms1   = @var_sms1,
sms2   = @var_sms2
```

Conclusión técnica: GIS/WebPos ya tiene el patrón correcto. Recupera `email` como principal y `email2` como alternativo. Para PRESMED, el equivalente correcto sería que `prs_afiliado_extras` haga lo mismo: devolver `email1` y `email2` separados, y que `wp_soli_reg` mande ambos a `pru_registra_solicitudes_auto`.

---

### [29/08/2026 15:20:13] Wilmer Higuera

Si, ayúdame a igualar como devuelve los email de GIS, quede igual a Presmed, mira esto:
@var_email = COALESCE(MT.email_to, RUE.email) ,
@var_email2 = COALESCE(M2.email_to, RUE3.email, RUE2.email)

---

### [29/08/2026 15:21:06] Agente

Sí, Wilmer. Para igualarlo al criterio de GIS, el ajuste correcto en `prs_afiliado_extras` no es solo agregar `email1`; también hay que agregar el mismo criterio de **email alternativo** que usa `ars_afiliado`.

En GIS la lógica es:

```sql
@var_email  = COALESCE(MT.email_to, RUE.email),
@var_email2 = COALESCE(M2.email_to, RUE3.email, RUE2.email)
```

Para PRESMED, `prs_afiliado_extras` no tiene contexto de solicitud previa ni `mail_id`, por eso no puede usar `MT.email_to` ni `M2.email_to` sin agregar otro parámetro. Entonces la equivalencia limpia para este SP sería:

```sql
@var_email1 = RUE.email,
@var_email2 = COALESCE(RUE3.email, RUE2.email)
```

Es decir:

```text
RUE.email  -> email principal
RUE3.email -> segundo email de la misma vigencia, distinto tipo
RUE2.email -> email de vigencia anterior, como fallback
```

El cambio conceptual en `prs_afiliado_extras` quedaría así:

```sql
DECLARE
    @var_email1 VARCHAR(100),
    @var_email2 VARCHAR(100),
    @var_sms2   VARCHAR(30)

SELECT
    @var_email1 = RUE.email,
    @var_email2 = COALESCE(RUE3.email, RUE2.email),
    @var_sms2   = RUT2.tele

FROM AFILIADOS A

LEFT JOIN REGISTRO_UNICO_EMAILS RUE
ON  RUE.codigo_unico_persona = A.codigo_unico_persona
AND RUE.baja_fecha IS NULL
AND RUE.vigencia = (
    SELECT MAX(vigencia)
    FROM REGISTRO_UNICO_EMAILS
    WHERE codigo_unico_persona = RUE.codigo_unico_persona
    AND baja_fecha IS NULL
)
AND RUE.email_tipo = (
    SELECT MAX(email_tipo)
    FROM REGISTRO_UNICO_EMAILS
    WHERE codigo_unico_persona = RUE.codigo_unico_persona
    AND vigencia = RUE.vigencia
    AND baja_fecha IS NULL
)

LEFT JOIN REGISTRO_UNICO_EMAILS RUE2
ON  RUE2.codigo_unico_persona = RUE.codigo_unico_persona
AND RUE2.baja_fecha IS NULL
AND RUE2.vigencia = (
    SELECT MAX(vigencia)
    FROM REGISTRO_UNICO_EMAILS
    WHERE codigo_unico_persona = RUE2.codigo_unico_persona
    AND vigencia < RUE.vigencia
    AND baja_fecha IS NULL
)
AND RUE2.email_tipo = (
    SELECT MAX(email_tipo)
    FROM REGISTRO_UNICO_EMAILS
    WHERE codigo_unico_persona = RUE2.codigo_unico_persona
    AND vigencia = RUE2.vigencia
    AND baja_fecha IS NULL
)

LEFT JOIN REGISTRO_UNICO_EMAILS RUE3
ON  RUE3.codigo_unico_persona = RUE.codigo_unico_persona
AND RUE3.vigencia = RUE.vigencia
AND RUE3.baja_fecha IS NULL
AND RUE3.email_tipo = (
    SELECT MAX(email_tipo)
    FROM REGISTRO_UNICO_EMAILS
    WHERE codigo_unico_persona = RUE.codigo_unico_persona
    AND email_tipo <> RUE.email_tipo
    AND baja_fecha IS NULL
)

WHERE A.prepaga = @prm_prepaga
AND A.contra = @prm_contra
AND A.inte = @prm_inte
```

Y la firma del SP debería quedar agregando `@prm_email1` al final para no romper el contrato actual:

```sql
@prm_email2 VARCHAR(100) = NULL OUTPUT,
@prm_sms2   VARCHAR(30)  = NULL OUTPUT,
@prm_email1 VARCHAR(100) = NULL OUTPUT
```

La salida quedaría:

```sql
IF @prm_echo = 'S'
BEGIN
    SELECT
        email2 = @var_email2,
        sms2   = @var_sms2,
        email1 = @var_email1
END
ELSE
BEGIN
    SET @prm_email2 = @var_email2
    SET @prm_sms2   = @var_sms2
    SET @prm_email1 = @var_email1
END
```

Punto importante: en `ars_afiliado`, el `RUE3` actual está armado para buscar otro `email_tipo` dentro de la misma vigencia. Para PRESMED conviene copiar esa intención, pero usando `RUE.codigo_unico_persona` en el subquery de `RUE3`, porque es la referencia directa del email principal. Eso evita depender de que exista `RUE2`.

Con esto `prs_afiliado_extras` quedaría alineado al comportamiento GIS:

```text
email1 = principal
email2 = alternativo de misma vigencia o, si no existe, email anterior
sms2   = celular alternativo actual
```

Espero tu confirmación de este criterio antes de pasar al ajuste de `wp_soli_reg`.

---

### [29/08/2026 15:29:02] Wilmer Higuera

En prs_afiliado_extras, adicionemos la tablaMAIL_TO, eso si, como corresponde, con sus respectivas relaciones

---

### [29/08/2026 15:29:26] Agente

Sí, Wilmer. Entonces en `prs_afiliado_extras` lo igualamos más fiel a GIS agregando `MAIL_TO` y `M2`, pero antes hay que incorporar la búsqueda del último `mail_id_afi_mail` de `SOLICITUDES_AUTO`, porque `MAIL_TO` se relaciona por `mail_id`.

La lógica quedaría así:

```sql
DECLARE
    @var_email1    VARCHAR(100),
    @var_email2    VARCHAR(100),
    @var_sms2      VARCHAR(30),
    @var_mail_id   NUMERIC(18,0),
    @var_soli_auto NUMERIC(18,0)

SELECT
    @var_mail_id   = SA.mail_id_afi_mail,
    @var_soli_auto = SA.soli_auto
FROM SOLICITUDES_AUTO SA
WHERE SA.prepaga = @prm_prepaga
AND SA.contra = @prm_contra
AND SA.inte = @prm_inte
AND SA.soli_auto = (
    SELECT MAX(soli_auto)
    FROM SOLICITUDES_AUTO
    WHERE prepaga = SA.prepaga
    AND contra = SA.contra
    AND inte = SA.inte
    AND baja_fecha IS NULL
    AND mail_id_afi_mail IS NOT NULL
)
```

Después, dentro del `FROM AFILIADOS A`, se agregan las relaciones de `MAIL_TO` igual al patrón de `ars_afiliado`:

```sql
LEFT JOIN MAIL_TO MT
ON  MT.mail_id = @var_mail_id
AND MT.mail_to_id = (
    SELECT MIN(mail_to_id)
    FROM MAIL_TO
    WHERE mail_id = MT.mail_id
    AND baja_fecha IS NULL
)

LEFT JOIN MAIL_TO M2
ON  M2.mail_id = MT.mail_id
AND M2.mail_to_id = (
    SELECT MIN(mail_to_id)
    FROM MAIL_TO
    WHERE mail_id = MT.mail_id
    AND mail_to_id > MT.mail_to_id
    AND baja_fecha IS NULL
)
```

Y la asignación final de emails quedaría:

```sql
SELECT
    @var_email1 = COALESCE(MT.email_to, RUE.email),
    @var_email2 = COALESCE(M2.email_to, RUE3.email, RUE2.email),
    @var_sms2 = RUT2.tele
FROM AFILIADOS A

LEFT JOIN MAIL_TO MT
ON  MT.mail_id = @var_mail_id
AND MT.mail_to_id = (
    SELECT MIN(mail_to_id)
    FROM MAIL_TO
    WHERE mail_id = MT.mail_id
    AND baja_fecha IS NULL
)

LEFT JOIN MAIL_TO M2
ON  M2.mail_id = MT.mail_id
AND M2.mail_to_id = (
    SELECT MIN(mail_to_id)
    FROM MAIL_TO
    WHERE mail_id = MT.mail_id
    AND mail_to_id > MT.mail_to_id
    AND baja_fecha IS NULL
)

-- acá siguen RUE, RUE2, RUE3, RUT y RUT2

WHERE A.prepaga = @prm_prepaga
AND A.contra = @prm_contra
AND A.inte = @prm_inte
```

La salida del SP debería quedar así:

```sql
IF @prm_echo = 'S'
BEGIN
    SELECT
        email2 = @var_email2,
        sms2 = @var_sms2,
        email1 = @var_email1
END
ELSE
BEGIN
    SET @prm_email2 = @var_email2
    SET @prm_sms2 = @var_sms2
    SET @prm_email1 = @var_email1
END
```

Con esto `prs_afiliado_extras` queda alineado con GIS:

```text
email1 = COALESCE(MT.email_to, RUE.email)
email2 = COALESCE(M2.email_to, RUE3.email, RUE2.email)
```

Confirmame este Paso 1 como criterio cerrado y paso a mostrarte cómo quedaría el `prs_afiliado_extras` completo armado.

---

### [29/08/2026 15:35:05] Wilmer Higuera

Si, me parece bien, ahora en la ubicación A:\whiguera\Tarea #23986\Tarea #29489, genera el SP con el mismo nombre, pero con los cambios, por favor

---

### [29/08/2026 15:38:40] Agente

Wilmer, generado el SP en la ruta solicitada:

`/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/prs_afiliado_extras.sql`

Equivale a:

`A:\whiguera\Tarea #23986\Tarea #29489\prs_afiliado_extras.sql`

El archivo quedó con estos cambios:

```text
email1 = COALESCE(MT.email_to, RUE.email)
email2 = COALESCE(M2.email_to, RUE3.email, RUE2.email)
```

También agregué la relación con `SOLICITUDES_AUTO` para obtener el último `mail_id_afi_mail`, y desde ahí relacionar correctamente `MAIL_TO MT` y `MAIL_TO M2`.

Validaciones realizadas:

```text
Archivo existe: OK
Encoding: us-ascii
BOM UTF-8: No
Finales de línea: CRLF
RTRIM: No usado
Punto y coma: No usado
Repositorio fuente: no modificado
Commit: no realizado
```

Riesgo técnico pendiente: no ejecuté validación contra SQL Server; el archivo quedó generado como entregable para que lo revises/ejecutes en tu entorno.

---

### [29/08/2026 15:42:00] Wilmer Higuera

Lo ejecute en SQL el SP, ayúdame a probarlo, porfa

---

### [29/08/2026 15:42:20] Agente

Wilmer, perfecto. Probémoslo primero con una validación **read-only**, sin tocar datos.

Ejecutá este bloque reemplazando los datos del afiliado del Caso 10:

```sql
DECLARE
    @email1 VARCHAR(100),
    @email2 VARCHAR(100),
    @sms2   VARCHAR(30)

EXEC dbo.prs_afiliado_extras
    @prm_prepaga = <prepaga>,
    @prm_contra  = '<contra>',
    @prm_inte    = '<inte>',
    @prm_echo    = 'N',
    @prm_email2  = @email2 OUTPUT,
    @prm_sms2    = @sms2 OUTPUT,
    @prm_email1  = @email1 OUTPUT

SELECT
    email1 = @email1,
    email2 = @email2,
    sms2   = @sms2
```

Después validá contra la lógica base, para comparar de dónde salen esos valores:

```sql
SELECT TOP 10
    SA.soli_auto,
    SA.mail_id_afi_mail,
    SA.email_afi_1,
    SA.email_afi_2,
    SA.sms_afi_2
FROM SOLICITUDES_AUTO SA
WHERE SA.prepaga = <prepaga>
AND SA.contra = '<contra>'
AND SA.inte = '<inte>'
AND SA.baja_fecha IS NULL
ORDER BY SA.soli_auto DESC
```

Y si aparece `mail_id_afi_mail`, revisá los destinatarios históricos:

```sql
SELECT
    MT.mail_to_id,
    MT.email_to,
    MT.baja_fecha
FROM MAIL_TO MT
WHERE MT.mail_id = <mail_id_afi_mail>
ORDER BY MT.mail_to_id
```

**Resultado esperado**

```text
email1 debe salir desde:
1. MAIL_TO primer destinatario si existe mail histórico del afiliado
2. Si no existe, REGISTRO_UNICO_EMAILS principal

email2 debe salir desde:
1. MAIL_TO segundo destinatario si existe
2. Si no existe, segundo email vigente RUE3
3. Si no existe, email anterior RUE2
```

Para confirmar que quedó igual al criterio GIS, el caso ideal debería mostrar:

```text
email1 <> email2
email1 = correo principal del afiliado
email2 = correo alternativo / segundo correo
```

Pasame el resultado de esos tres `SELECT` y reviso si la salida quedó correcta antes de avanzar a `wp_soli_reg`.

---

### [29/08/2026 15:53:08] Wilmer Higuera

[User sent media without caption]

---

### [29/08/2026 15:54:01] Agente

Wilmer, con la imagen ya hay una señal concreta: el SP **no está tomando `MAIL_TO`**.

Según tu prueba:

```text
Última solicitud: soli_auto 25920147
mail_id_afi_mail: 9989143
MAIL_TO para 9989143: wilprueba@gmail.com
```

Entonces el resultado esperado del SP debería ser:

```text
email1 = wilprueba@gmail.com
email2 = fallback por RUE3/RUE2, porque no hay segundo MAIL_TO
```

Pero devolvió:

```text
email1 = pruebasgisprestador@gmail.com
email2 = pruebasgisprestador@gmail.com
```

Eso indica que `COALESCE(MT.email_to, RUE.email)` está cayendo a `RUE.email`, o sea `MT.email_to` llega `NULL`.

Probá este SQL de diagnóstico, sin modificar datos:

```sql
DECLARE @var_mail_id NUMERIC(18,0)

SELECT
    @var_mail_id = SA.mail_id_afi_mail
FROM SOLICITUDES_AUTO SA
WHERE SA.prepaga = <prepaga>
AND SA.contra = '<contra>'
AND SA.inte = '<inte>'
AND SA.soli_auto = (
    SELECT MAX(soli_auto)
    FROM SOLICITUDES_AUTO
    WHERE prepaga = SA.prepaga
    AND contra = SA.contra
    AND inte = SA.inte
    AND baja_fecha IS NULL
    AND mail_id_afi_mail IS NOT NULL
)

SELECT var_mail_id = @var_mail_id

SELECT
    MT.mail_id,
    MT.mail_to_id,
    MT.email_to,
    MT.baja_fecha
FROM MAIL_TO MT
WHERE MT.mail_id = @var_mail_id
AND MT.baja_fecha IS NULL
ORDER BY MT.mail_to_id
```

Si ahí `@var_mail_id` sale `NULL`, el problema está en la búsqueda de `SOLICITUDES_AUTO`.

Si `@var_mail_id = 9989143` y aparece `wilprueba@gmail.com`, entonces el ajuste debería ser simplificar el join de `MAIL_TO` usando `@var_mail_id` directamente dentro del subquery:

```sql
LEFT JOIN MAIL_TO MT
ON MT.mail_id = @var_mail_id
AND MT.mail_to_id = (
    SELECT MIN(mail_to_id)
    FROM MAIL_TO
    WHERE mail_id = @var_mail_id
    AND baja_fecha IS NULL
)
```

Y para `M2`:

```sql
LEFT JOIN MAIL_TO M2
ON M2.mail_id = @var_mail_id
AND M2.mail_to_id = (
    SELECT MIN(mail_to_id)
    FROM MAIL_TO
    WHERE mail_id = @var_mail_id
    AND mail_to_id > MT.mail_to_id
    AND baja_fecha IS NULL
)
```

Ese cambio mantiene la misma lógica, pero evita que el subquery dependa de `MT.mail_id` mientras el propio `MT` todavía se está resolviendo.

Pasame el resultado del bloque de diagnóstico y te confirmo si ese es el ajuste exacto para regenerar el SP.

---

### [29/08/2026 15:56:41] Wilmer Higuera

Espera, no tome el ultimo mail_id, había tomado el antepenúltima, ¿Cuál ide debo tomar para el ultimo sql?, te adjunto de nuevo la imagen

---

### [29/08/2026 15:56:50] Agente

Tenés que tomar el `mail_id_afi_mail` de la **primera fila**, porque tu consulta está ordenada por `soli_auto DESC`.

En la imagen, el último registro es:

```text
soli_auto        = 25920147
mail_id_afi_mail = 9989143
```

Entonces para el último SQL de `MAIL_TO` corresponde usar:

```sql
SELECT
    MT.mail_to_id,
    MT.email_to,
    MT.baja_fecha
FROM MAIL_TO MT
WHERE MT.mail_id = 9989143
ORDER BY MT.mail_to_id
```

Y según la nueva imagen, ese `mail_id = 9989143` devuelve:

```text
mail_to_id = 10000290
email_to   = pruebasgisprestador@gmail.com
```

Por eso el SP está devolviendo bien:

```text
email1 = pruebasgisprestador@gmail.com
email2 = pruebasgisprestador@gmail.com
```

La confusión venía de haber probado antes con otro `mail_id`, el `9989142`, que corresponde a la solicitud anterior `25920146` y tenía:

```text
email_to = wilprueba@gmail.com
```

Conclusión: para validar siempre usá el `mail_id_afi_mail` de la solicitud más reciente, que en tu grilla es la fila 1.

---

### [29/08/2026 15:58:38] Wilmer Higuera

Ya podemos pasar al siguiente punto?

---

### [29/08/2026 15:59:06] Agente

Sí, Wilmer. El **Paso 1 (`prs_afiliado_extras`) queda validado funcionalmente**: el SP está tomando el `mail_id_afi_mail` correcto de la última solicitud y resolviendo `email1/email2` con criterio GIS.

Podemos pasar al siguiente punto: **`wp_soli_reg.srw`**.

El objetivo ahí será ajustar la ventana para que:
1. Reciba también `email1` desde `prs_afiliado_extras`.
2. Guarde ese valor en una variable interna.
3. Mantenga `email_alter` como email alternativo visible/editable.
4. Envíe ambos campos a `pru_registra_solicitudes_auto`:
   ```powerscript
   @prm_email_afi_1 = email principal
   @prm_email_afi_2 = email alternativo
   ```

Antes de tocar nada, te muestro el cambio propuesto por partes y vos me das el visto bueno de cada bloque.

---

### [29/08/2026 16:08:46] Wilmer Higuera

Si, porfa, dame el paso a paso, bien detallado

---

### [29/08/2026 16:09:22] Agente

Sí, Wilmer. El siguiente punto es **`wp_soli_reg.srw`**, y el objetivo es que PRESMED deje de tratar el email alternativo como único email operativo.

La idea es esta:

```text
email principal   -> queda en variable interna de la ventana
email alternativo -> sigue en idw_afi.email_alter, visible/editable
```

Referencia de memoria: en `wp_soli_reg.srw`, el problema detectado fue que `wf_afi_datos_extras()` recibía solo `email2/sms2`, cargaba `email_alter`, y luego `wf_registra_solicitudes_auto()` enviaba ese dato como `@prm_email_afi_2`, sin enviar `email_afi_1`. Source: `memory/2026-08-28.md#L31`

**Paso 1: Agregar variable de instancia**

En `wp_soli_reg.srw`, buscar la sección de variables de instancia de la ventana.

Agregar:

```powerscript
STRING is_email_afi_1
```

Uso esperado:

```text
is_email_afi_1 = email principal del afiliado
idw_afi.email_alter = email alternativo editable
```

**Paso 2: Ajustar `wf_afi_datos_extras()`**

Buscar la función:

```powerscript
wf_afi_datos_extras()
```

Actualmente debe tener una lógica parecida a:

```powerscript
STRING ls_contra, ls_inte, ls_email, ls_tel, ls_null
```

Cambiar las variables locales para separar email principal y alternativo:

```powerscript
STRING ls_contra, ls_inte, ls_email_afi_1, ls_email_afi_2, ls_tel, ls_null
```

Luego buscar el `FETCH` del procedimiento `prs_afiliado_extras`.

Como el SP ahora devuelve:

```sql
email2, sms2, email1
```

El `FETCH` debe quedar en ese mismo orden:

```powerscript
FETCH proc INTO :ls_email_afi_2, :ls_tel, :ls_email_afi_1;
```

Importante: no invertir el orden. El primer dato sigue siendo `email2` para no romper el contrato anterior del SP.

**Paso 3: Guardar el email principal en variable interna**

Después del `CLOSE proc`, agregar la carga de `is_email_afi_1`:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_1) THEN
    is_email_afi_1 = ls_email_afi_1
ELSE
    SetNull(is_email_afi_1)
END IF
```

Esto no impacta la pantalla. Solo conserva el email principal para enviarlo después al registro de la solicitud.

**Paso 4: Mantener `email_alter` como email alternativo**

La lógica actual que carga `email_alter` debe quedar usando `ls_email_afi_2`.

Ejemplo:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_2) THEN
    idw_afi.SetItem(1, 'email_alter', ls_email_afi_2)
ELSE
    idw_afi.SetItem(1, 'email_alter', ls_null)
END IF
```

No debe cargarse `email_alter` con `ls_email_afi_1`.

Ese campo debe seguir representando:

```text
email_alter = email alternativo / email2
```

**Paso 5: Ajustar `wf_registra_solicitudes_auto()`**

Buscar la función:

```powerscript
wf_registra_solicitudes_auto()
```

Agregar una variable local para el email principal:

```powerscript
STRING ls_email_afi_1
```

Debe quedar junto a la variable ya existente del email alternativo, si existe:

```powerscript
STRING ls_email_afi_1, ls_email_afi_2
```

Antes del `DECLARE proc` o antes del `EXECUTE`, cargar los valores:

```powerscript
ls_email_afi_1 = is_email_afi_1
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

La lectura queda así:

```text
ls_email_afi_1 = email principal recuperado desde prs_afiliado_extras
ls_email_afi_2 = email alternativo que el usuario puede editar en pantalla
```

**Paso 6: Agregar parámetro al llamado de `pru_registra_solicitudes_auto`**

En el bloque donde se declara o ejecuta:

```powerscript
pru_registra_solicitudes_auto
```

Agregar el parámetro:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

Si ya existe:

```powerscript
@prm_email_afi_2 = :ls_email_afi_2,
```

No se elimina. Solo se agrega antes o cerca de ese parámetro el `@prm_email_afi_1`.

El resultado conceptual debe quedar:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
@prm_sms_2       = :ls_sms_2,
```

**Paso 7: Cuando se recupera una solicitud existente**

Este punto depende del siguiente SP/DataWindow: `prs_solicitudes_auto_afi`.

Cuando `idw_afi.Retrieve(il_soli)` recupera una solicitud existente, la ventana necesita volver a cargar `is_email_afi_1`.

Luego de:

```powerscript
idw_afi.Retrieve(il_soli)
```

Agregar:

```powerscript
IF NOT lib.of_vacio(idw_afi.object.email_afi_1[1]) THEN
    is_email_afi_1 = idw_afi.object.email_afi_1[1]
ELSE
    SetNull(is_email_afi_1)
END IF
```

Pero esto solo va a compilar si antes hacemos que el DataWindow tenga la columna `email_afi_1`.

Por eso este paso queda ligado al siguiente punto: ajustar `prs_solicitudes_auto_afi` y el `.srd`.

**Paso 8: Limpiar la variable cuando se limpia el afiliado**

Buscar la lógica donde se limpia el afiliado o se reinician los datos de `idw_afi`.

Agregar:

```powerscript
SetNull(is_email_afi_1)
```

Esto evita que quede un email principal anterior cargado en memoria si el usuario cambia o limpia afiliado.

**Validación esperada en PowerBuilder**

Después del cambio, probar este flujo:

1. Buscar afiliado.
2. Ejecutar `wf_afi_datos_extras()`.
3. Confirmar internamente:
   ```text
   is_email_afi_1 = email principal
   idw_afi.email_alter = email alternativo
   ```
4. Modificar `email_alter` en pantalla.
5. Registrar la solicitud.
6. Validar en SQL:
   ```sql
   SELECT
       soli_auto,
       email_afi_1,
       email_afi_2,
       sms_afi_2
   FROM SOLICITUDES_AUTO
   WHERE soli_auto = <solicitud>
   ```

Resultado esperado:

```text
email_afi_1 = email principal
email_afi_2 = email alternativo mostrado/editado en PRESMED
```

Mi recomendación: primero cerramos estos cambios de `wp_soli_reg.srw` hasta el punto donde llama a `pru_registra_solicitudes_auto`. Después pasamos al ajuste de `prs_solicitudes_auto_afi` + DataWindow, porque ahí se habilita correctamente la recuperación de solicitudes existentes.

---

### [29/08/2026 16:18:47] Wilmer Higuera

Espera, mira que en GIS muestra el correo principal solo como consulta, y el correo electrónico 2 editable, podemos hacer lo mismo, mostrar el correo principal, ya que el email 2 ya esta editable

---

### [29/08/2026 16:19:01] Agente

Sí, Wilmer. Ese criterio es mejor y queda más alineado con GIS.

Entonces para `wp_soli_reg` el diseño debería ser:

```text
Correo Electrónico      = email principal, solo consulta
Correo Electrónico 2    = email alternativo, editable
Celular Principal       = solo consulta
Celular Alternativo     = editable
```

A nivel implementación cambia esto:

1. En el DataWindow `idw_afi`, agregar una columna/campo visible para `email_afi_1` o equivalente, solo lectura.

2. En `wf_afi_datos_extras()`, cuando el SP devuelva:

```powerscript
FETCH proc INTO :ls_email_afi_2, :ls_tel, :ls_email_afi_1;
```

cargar ambos campos:

```powerscript
idw_afi.SetItem(1, 'email_afi_1', ls_email_afi_1)
idw_afi.SetItem(1, 'email_alter', ls_email_afi_2)
```

3. En el DataWindow, dejar:

```text
email_afi_1 / Correo Electrónico     -> Protect = 1
email_alter / Correo Electrónico 2   -> editable
```

4. En `wf_registra_solicitudes_auto()`, ya no tomaría el email principal solo desde una variable interna si está visible en el DW. Lo tomaría así:

```powerscript
ls_email_afi_1 = idw_afi.object.email_afi_1[1]
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

5. El llamado al SP queda:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

Conclusión: sí, hacemos lo mismo que GIS. Mostramos el correo principal como dato informativo no editable, y mantenemos el correo electrónico 2 como editable. Esto además evita que el usuario pierda visibilidad de cuál email principal se va a guardar en `SOLICITUDES_AUTO.email_afi_1`.

---

### [29/08/2026 16:23:54] Wilmer Higuera

si, bien, dame el paso a paso muy detallado, por favor, indicando con claridad en que parte se hace el cambio

---

### [29/08/2026 16:24:30] Agente

Sí, Wilmer. El cambio en `wp_soli_reg` lo haría así, separando claramente **qué se cambia**, **dónde** y **para qué**.

**Objetivo funcional**

Dejar PRESMED igual al criterio GIS:

```text
Correo Electrónico    -> email principal, visible, solo consulta
Correo Electrónico 2  -> email alternativo, visible y editable
```

El email principal debe viajar a:

```text
SOLICITUDES_AUTO.email_afi_1
```

El email alternativo debe seguir viajando a:

```text
SOLICITUDES_AUTO.email_afi_2
```

---

## Paso 1 - Ajustar el DataWindow del afiliado

Archivo probable:

```text
de_ingre_auto_afi_sol_reg.srd
```

Este es el DataWindow usado por `idw_afi` dentro de `wp_soli_reg`.

Ahí hay que agregar una columna nueva para mostrar el correo principal.

Agregar columna al SQL del DataWindow:

```sql
email_afi_1 = S.email_afi_1,
```

Debe quedar cerca de los datos actuales de email, por ejemplo:

```sql
email_afi_1 = S.email_afi_1,
email_alter = S.email_afi_2,
tel_alter   = S.sms_afi_2,
```

Luego en el diseño visual del DataWindow agregar el campo:

```text
Correo Electrónico
```

asociado a:

```text
email_afi_1
```

Y dejar el campo como solo consulta:

```text
Protect = 1
TabOrder = 0
```

El campo actual:

```text
email_alter
```

debe quedar como:

```text
Correo Electrónico 2
```

y seguir editable.

---

## Paso 2 - Ajustar `prs_solicitudes_auto_afi`

Archivo:

```text
prs_solicitudes_auto_afi.sql
```

Este SP alimenta el DataWindow cuando se recupera una solicitud existente.

En el `SELECT` donde ya devuelve el email alternativo:

```sql
email_alter = S.email_afi_2,
```

agregar antes:

```sql
email_afi_1 = S.email_afi_1,
```

Debe quedar así:

```sql
email_afi_1 = S.email_afi_1,
email_alter = S.email_afi_2,
tel_alter   = S.sms_afi_2,
```

Esto permite que al abrir una solicitud ya grabada, PRESMED muestre el email principal guardado.

---

## Paso 3 - Ajustar variables en `wp_soli_reg.srw`

Archivo:

```text
wp_soli_reg.srw
```

Buscar la función:

```powerscript
wf_afi_datos_extras()
```

Ahí actualmente debe existir una declaración parecida a:

```powerscript
STRING ls_contra, ls_inte, ls_email, ls_tel, ls_null
```

Cambiarla por:

```powerscript
STRING ls_contra, ls_inte, ls_email_afi_1, ls_email_afi_2, ls_tel, ls_null
```

La intención es clara:

```text
ls_email_afi_1 = correo principal
ls_email_afi_2 = correo electrónico 2 / alternativo
```

---

## Paso 4 - Ajustar el FETCH de `prs_afiliado_extras`

Dentro de `wf_afi_datos_extras()`, buscar el bloque donde se ejecuta:

```powerscript
prs_afiliado_extras
```

Como el SP ahora devuelve:

```sql
email2, sms2, email1
```

el `FETCH` debe respetar ese orden:

```powerscript
FETCH proc INTO :ls_email_afi_2, :ls_tel, :ls_email_afi_1;
```

Importante: no invertirlos.

El primer valor sigue siendo `email2` para respetar el comportamiento anterior del SP.

---

## Paso 5 - Cargar el email principal en pantalla

Después del `CLOSE proc`, agregar la carga del nuevo campo:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_1) THEN
    idw_afi.SetItem(1, 'email_afi_1', ls_email_afi_1)
ELSE
    idw_afi.SetItem(1, 'email_afi_1', ls_null)
END IF
```

Este campo será visible pero no editable.

---

## Paso 6 - Mantener el email alternativo editable

En la misma función `wf_afi_datos_extras()`, mantener la carga de `email_alter`, pero ahora usando explícitamente `ls_email_afi_2`:

```powerscript
IF NOT lib.of_vacio(ls_email_afi_2) THEN
    idw_afi.SetItem(1, 'email_alter', ls_email_afi_2)
ELSE
    idw_afi.SetItem(1, 'email_alter', ls_null)
END IF
```

Esto conserva el comportamiento actual:

```text
email_alter = campo editable
```

---

## Paso 7 - Ajustar `wf_registra_solicitudes_auto()`

En `wp_soli_reg.srw`, buscar la función:

```powerscript
wf_registra_solicitudes_auto()
```

Agregar o ajustar variables locales:

```powerscript
STRING ls_email_afi_1, ls_email_afi_2
```

Antes del llamado a:

```powerscript
pru_registra_solicitudes_auto
```

cargar ambos valores desde el DataWindow:

```powerscript
ls_email_afi_1 = idw_afi.object.email_afi_1[1]
ls_email_afi_2 = idw_afi.object.email_alter[1]
```

Así queda alineado con pantalla:

```text
email_afi_1 viene del campo solo consulta
email_afi_2 viene del campo editable
```

---

## Paso 8 - Agregar parámetro al llamado de `pru_registra_solicitudes_auto`

En el `DECLARE proc PROCEDURE FOR pru_registra_solicitudes_auto`, agregar:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
```

Debe quedar junto al parámetro existente del email alternativo:

```powerscript
@prm_email_afi_1 = :ls_email_afi_1,
@prm_email_afi_2 = :ls_email_afi_2,
```

No reemplazar `@prm_email_afi_2`.

Solo agregar el principal.

---

## Paso 9 - Limpiar ambos campos al limpiar afiliado

Buscar en `wp_soli_reg.srw` la parte donde se limpia el afiliado o se reinician los datos de `idw_afi`.

Ahí agregar:

```powerscript
idw_afi.SetItem(1, 'email_afi_1', ls_null)
idw_afi.SetItem(1, 'email_alter', ls_null)
```

Si esa limpieza ya existe para `email_alter`, solo agregar `email_afi_1`.

Esto evita que quede visible un correo principal de un afiliado anterior.

---

## Paso 10 - Validación funcional

Después de compilar, probar este flujo:

1. Buscar afiliado.
2. Confirmar en pantalla:
   ```text
   Correo Electrónico    = email principal
   Correo Electrónico 2  = email alternativo
   ```
3. Confirmar que `Correo Electrónico` no sea editable.
4. Confirmar que `Correo Electrónico 2` sí sea editable.
5. Registrar solicitud.
6. Validar en SQL:

```sql
SELECT
    soli_auto,
    email_afi_1,
    email_afi_2,
    sms_afi_2,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <solicitud>
```

Resultado esperado:

```text
email_afi_1 = correo principal mostrado en pantalla
email_afi_2 = correo electrónico 2 mostrado/editado en pantalla
```

Orden recomendado de implementación:

```text
1. prs_solicitudes_auto_afi.sql
2. de_ingre_auto_afi_sol_reg.srd
3. wp_soli_reg.srw - wf_afi_datos_extras()
4. wp_soli_reg.srw - wf_registra_solicitudes_auto()
5. wp_soli_reg.srw - limpieza de afiliado
6. compilación y prueba funcional
```

---

### [29/08/2026 17:53:00] Wilmer Higuera

Listo, guardo bien, ¿Queda algo pendiente o es todo?

---

### [29/08/2026 17:53:46] Agente

Wilmer, todavía no lo daría por cerrado al 100%. Si `wp_soli_reg` ya guardó bien, quedan estos puntos de control:

1. Validar que `prs_solicitudes_auto_afi` ya devuelva:
```sql
email_afi_1 = S.email_afi_1,
email_alter = S.email_afi_2,
```

2. Validar que el DataWindow `de_ingre_auto_afi_sol_reg.srd` tenga la columna/campo `email_afi_1` y que el campo esté visible como **Correo Electrónico** solo consulta.

3. Revisar `pru_registra_solicitudes_auto`: en alta ya debería guardar `email_afi_1`, pero en edición hay que confirmar que también actualice:
```sql
email_afi_1 = @prm_email_afi_1,
email_afi_2 = @prm_email_afi_2,
```

4. Compilar `wp_soli_reg` y probar flujo completo:
```text
Buscar afiliado
Ver Correo Electrónico principal solo consulta
Editar Correo Electrónico 2
Registrar solicitud
Validar SOLICITUDES_AUTO.email_afi_1 y email_afi_2
Recuperar la solicitud y verificar que ambos vuelvan bien
```

Conclusión: si ya guardaste `wp_soli_reg`, el pendiente principal es **confirmar/ajustar `prs_solicitudes_auto_afi`, el DataWindow y revisar `pru_registra_solicitudes_auto` en edición**. Después de esa prueba completa, sí lo podemos dar por cerrado.

---

### [29/08/2026 17:57:14] Wilmer Higuera

Te comento, registre una solicitud y después de limpiar, escribí el numero de la solicitud para que se recupere, la recupero bien

---

### [29/08/2026 17:57:23] Agente

Perfecto, Wilmer. Esa prueba cubre uno de los puntos más importantes: **alta + limpieza + recuperación por número de solicitud**.

Con eso queda validado que:

```text
wp_soli_reg guarda correctamente los emails
prs_solicitudes_auto_afi recupera bien la solicitud
el DataWindow vuelve a mostrar los datos correctamente
```

Para cerrar el caso, solo haría una última validación SQL sobre esa solicitud recuperada:

```sql
SELECT
    soli_auto,
    email_afi_1,
    email_afi_2,
    sms_afi_2,
    mail_id_afi_mail
FROM SOLICITUDES_AUTO
WHERE soli_auto = <numero_solicitud>
```

Resultado esperado:

```text
email_afi_1 = Correo Electrónico principal
email_afi_2 = Correo Electrónico 2 / editable
```

Si eso está correcto, para mí el circuito principal queda cerrado. Solo faltaría confirmar que `pru_registra_solicitudes_auto` también actualiza `email_afi_1` en edición, por si una solicitud existente se modifica y se vuelve a guardar.

---

### [29/08/2026 17:59:53] Wilmer Higuera

mira la imagen, registre una primero por GIS y luego otra por presmed

---

### [29/08/2026 18:00:27] Agente

Sí, Wilmer. La imagen confirma que el comportamiento quedó alineado:

```text
GIS      soli_auto 25920148
email_afi_1 = pruebasgisprestador@gmail.com
email_afi_2 = pruebasgispresmed_gis@gmail.com

PRESMED  soli_auto 25920149
email_afi_1 = pruebasgisprestador@gmail.com
email_afi_2 = pruebasgispresmed_presmed@gmail.com
```

Lectura técnica:

- `email_afi_1` quedó igual en ambos flujos: correcto, es el correo principal.
- `email_afi_2` queda distinto según lo editado/cargado en cada interfaz: correcto, es el correo electrónico 2 editable.
- PRESMED ya no está guardando el principal dentro de `email_afi_2`: ese era el problema central.

Lo único que yo validaría antes de cerrar definitivamente es la edición de una solicitud existente en PRESMED: abrir la `25920149`, guardar/modificar, y volver a consultar que siga actualizando ambos campos:

```sql
SELECT soli_auto, email_afi_1, email_afi_2
FROM SOLICITUDES_AUTO
WHERE soli_auto = 25920149
```

Si eso queda bien, para mí la Tarea 29489 queda funcionalmente cerrada en el circuito GIS/PRESMED.

---

### [29/08/2026 18:08:13] Wilmer Higuera

Listo, modifico de nuevo bien el campo email_afi_2, guarda por favor lo trabajado el días de hoy, con todos los detalles de forma que el lunes continuemos con el trabajo, me indicas como te escribo el lunes para que recuperes todo lo avanzado y podamos continuar. Muchas gracias, hasta el próximo lunes

---

### [29/08/2026 18:10:06] Agente

Listo, Wilmer. Dejé guardado lo trabajado hoy en:

`/home/desarrollo-ia-05/.openclaw/workspace/memory/MEMORY-users_113120678211994207734-2026-08-29.md`

Quedó registrado con detalle: contexto de la Tarea 29489, criterio GIS/PRESMED, cambios de `prs_afiliado_extras`, ajustes definidos para `wp_soli_reg`, `prs_solicitudes_auto_afi`, validaciones realizadas, pruebas exitosas y el pendiente recomendado sobre `pru_registra_solicitudes_auto`.

El lunes escribime así:

```text
Lisa, retomemos la Tarea 29489 desde la memoria del 2026-08-29, sobre el Caso 10 de emails GIS/PRESMED. Necesito continuar desde el punto pendiente de validacion/revision de pru_registra_solicitudes_auto y cierre de entregables.
```

Con eso recupero todo el avance y seguimos desde el punto correcto.

Gracias, Wilmer. Hasta el lunes.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
