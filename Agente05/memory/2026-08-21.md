
## 2026-08-21 - Tarea 29489 - Casos 26 a 31

### Preferencias operativas de Wilmer
- Wilmer pidio recordar dos reglas para la jornada: no realizar modificaciones directas en el entorno ni commits; el agente solo propone y muestra cambios por pantalla o archivo. La ejecucion y commits quedan bajo responsabilidad exclusiva de whiguera.
- No asumir que un paso fue comprendido o cerrado sin visto bueno explicito de whiguera.

### Caso 26 - Notificacion solicitud en gestion
- Caso 26 quedo resuelto por Wilmer.
- Contexto tecnico conservado: en `dbo.pru_genera_auto_precarga`, la notificacion `@prm_id = 4` debe ejecutarse despues del `UPDATE SOLICITUDES_AUTO` que persiste `soli_esta = @var_soli_esta` y antes del `--WORKFLOW` / `dbo.pru_solicitudes_auto_workflow`.
- Condicion acordada: notificar solo si `@var_soli_esta = 'G'` y no existe auditoria previa con `proceso_id: 4`, para evitar duplicados.
- Validacion esperada: auditoria con `Notificacion solicitud en gestion`, `proceso_id: 4`, `tipo_usuari = P`, y actualizacion de `SOLICITUDES_AUTO.mail_id_prestad_mail`.

### Caso 28 - Notificaciones estado solicitud anulada
- Se valido que la anulacion de solicitud GIS/WebPos no usa el mismo SP que Presmed.
- GIS/WebPos llama `dbo.pru_solicitudes_auto_anula_total` desde `WebPos/modelo/solicitudAuto.php`; el SP audita `Anulacion GIS` y actualiza `SOLICITUDES_AUTO.soli_esta = 'U'`.
- Presmed Bandeja llama `dbo.pru_solicitudes_auto_anula_bandeja` desde `wp_bandeja_solicitudes.srw`; audita `Anulacion Presmed Bandeja` y tiene logica de notificacion con `@prm_id = 6` para prestador y `@prm_id = 14/15` para afiliado segun motivo.
- Presmed clasico / ventana `wp_anula_solicitudes.srw` llama `dbo.prd_autorizaciones` y luego recalcula estado con `dbo.pru_solicitudes_auto_soli_esta`, con `@prm_origen = 'P'`.
- Wilmer encontro que la notificacion de anulacion no estaba mal en el SP: depende de la ventana `Motivos Devolucion / Anulacion`.
- Nombre tecnico de ventana: `wp_moti_dev_anu.srw`.
- DataWindow de captura: `d_moti_dev_anu.srd`.
- DataWindow lista: `d_lista_moti_dev_anu.srd`.
- DDDW relacionada al proceso: `dddw_solicitudes_auto_notif_plant_proc.srd`.
- Causa del caso: motivo parametrizado con proceso de afiliado para prestadores fuera de red y sin notificar al prestador.
- Se actualizo la working copy SVN `svn/whiguera-Version84-Modelo2Gis` a revision `37634`; llegaron cambios en `wp_moti_dev_anu.srw`, `d_moti_dev_anu.srd` y `dddw_solicitudes_auto_notif_plant_proc.srd`. No se hicieron commits ni cambios manuales.
- Para permitir quitar un proceso seleccionado sin crear DDDW ni SP nuevos, se propuso agregar en memoria una fila blanca/null al DataWindowChild de `notif_plant_proc_id` dentro de `wp_moti_dev_anu.srw`, evento `ue_restaurar`, despues de recuperar la DDDW actual. Wilmer confirmo: "Listo quedo bien".
- Sobre trazabilidad: se concluyo que no hace falta crear una tabla nueva para cubrir auditoria historica basica, porque `pri_solicitudes_auto_notif` llama `pri_solicitudes_auto_auditoria` y deja registro en `SOLICITUDES_AUTO_AUDITORIA.activi` con `mail_id`, `email`, `notifica: Prestador/Afiliado` y `proceso_id`.
- Aclaracion para funcional: `SOLICITUDES_AUTO.mail_id_prestad_mail` y `mail_id_afi_mail` son ultimas referencias operativas; el historial completo debe consultarse en `SOLICITUDES_AUTO_AUDITORIA`. Para reporting formal con columnas separadas podria evaluarse tabla nueva, pero no es obligatorio para Caso 28.

### Caso 29 - Registrada con varios prestadores existentes y uno inexistente
- Funcional espera notificar a todos los prestadores existentes registrados en `SOLICITUDES_AUTO_PRESTAD`, aunque compartan la misma direccion de correo.
- Causa raiz encontrada en `dbo.pri_solicitudes_auto_notif`: el bloque de prestadores agrupaba por email (`GROUP BY X.email`), lo que deduplicaba prestadores distintos con el mismo correo.
- Correccion conceptual del punto 1: eliminar la agrupacion por email y cargar una fila en `#DESTINATARIOS_NOTIF` por cada fila valida de `SOLICITUDES_AUTO_PRESTAD` con `prestad IS NOT NULL`, `baja_fecha IS NULL` y email no vacio. Mantener principal/orden segun `SAP.prestad = @var_prestad_id`.
- Wilmer probo y confirmo que las consultas salieron bien: quedaron envios/auditorias separados, incluyendo prestadores con emails iguales en distinta capitalizacion. Caso 29 punto 1 quedo validado.
- Punto 2: para prestador inicialmente inexistente (`SOLICITUDES_AUTO_PRESTAD.prestad IS NULL`), el SP que posteriormente completa el prestador por NIT es `dbo.pru_registra_solicitudes_auto`, con update desde `PRESTADORES P ON SAP.nit = P.cuit`.
- Analisis tecnico: no conviene llamar `pri_solicitudes_auto_notif @prm_id = 1` sin filtro, porque reenviaria a todos los prestadores existentes de la solicitud. Una solucion robusta seria capturar con `OUTPUT` las filas que pasaron de `prestad NULL` a valor asignado y permitir parametros opcionales en `pri_solicitudes_auto_notif` para notificar solo ese `nit/prestad`.
- Luego Wilmer compartio respuesta funcional: el segundo punto lo tratarian con proceso `24`. Se valido que proceso 24 corresponde a `Notificacion Ejecutivos de Atencion Integral y Ejecutivo de FONOSALUD / Notificacion Resuelta Convenios Medicos`, no al flujo `Registrada prestador`. Conclusion: no resolver ese punto dentro del flujo de registrada/prestador del Caso 29; queda diferido al analisis del proceso 24 / Convenios Medicos.

### Caso 30 - Notificaciones estado devolucion
- Flujo tecnico: solicitud creada desde GIS por prestador; Presmed la devuelve desde `wp_bandeja_solicitudes.srw`, evento `ue_devolver()`, llamando a `dbo.pru_solicitudes_auto_devolucion`.
- `pru_solicitudes_auto_devolucion` inserta en `SOLICITUDES_AUTO_DEVOLUCION`, audita `Devolucion solicitud`, actualiza `soli_esta = 'D'` y notifica segun creador: si `@var_tipo_usuario_soli = 'P'`, llama `pri_solicitudes_auto_notif @prm_id = 2`; si `A` o `E`, llama `@prm_id = 16`.
- Para solicitud creada por prestador, funcional espera solo notificar al prestador principal de la solicitud, no a otros prestadores para los cuales se solicita.
- Causa encontrada: el bloque de destinatarios prestador de `pri_solicitudes_auto_notif` se usaba para `@prm_id IN (1,2,3,4,5,6,21)`, tomando varios prestadores desde `SOLICITUDES_AUTO_PRESTAD`. Eso hacia que devolucion (`@prm_id = 2`) notificara tambien a prestadores relacionados.
- Correccion validada en SP local adjunto por Wilmer: cambiar ese bloque a `IF @prm_id IN (1)` para que solo registrada use varios prestadores. Con esto `@prm_id = 2` cae al fallback de prestador principal (`@var_email_prestad_1/@var_email_prestad_2` desde `SOLICITUDES_AUTO`).
- Observacion: este cambio tambien hace que `@prm_id = 3,4,5,6,21` usen solo prestador principal, lo cual queda alineado con lo analizado hasta ahora porque el unico proceso con varios prestadores pedido por funcional fue `@prm_id = 1`.
- Sobre los dos emails con mismo prestador `ARIAS JOSE`: ocurre porque `@prestad` se arma desde `SOLICITUDES_AUTO.prestad`; al limitar devolucion al prestador principal el sintoma deja de aplicar.
- Valor cero en dias de devolucion: el SP lee `dbo.afn_ParametrosGrales('Presmed','Autorizaciones','vencimiento_devolucion_soli')` en minutos y calcula `CEILING(minutos / 1440)`. `0` minutos muestra `0` dias; `4320` minutos muestra `3` dias.
- Respuesta sugerida a funcional: el cero no es error del SP ni del reemplazo en plantilla; correspondia a parametrizacion anterior o prueba previa. Con parametro actual `vencimiento_devolucion_soli = 4320`, el mail debe informar 3 dias. Los mails ya enviados no se recalculan.

### Caso 31 - Revision desde PDF R29489-TEST_20260727.pdf
- Se analizo el Caso 31 del documento ubicado por Wilmer en `A:\whiguera\Tarea #23986\Tarea #29489`.
- Flujo: anulacion desde GIS por parte del prestador.
- Entra por `WebPos/modelo/solicitudAuto.php`, que ejecuta `dbo.pru_solicitudes_auto_anula_total`.
- En `pru_solicitudes_auto_anula_total`, si el motivo tiene `notif_prestad = 'S'` y la solicitud fue creada por prestador (`tipo_usuario = 'P'`), se llama `dbo.pri_solicitudes_auto_notif @prm_id = 6`.
- Proceso correcto: `6 - Notificacion PRESTADOR, estado de solicitud ANULADA`.
- No corresponden procesos `14` ni `15` para este flujo, porque son notificaciones al afiliado en escenarios especificos: `14` medicos no adscritos/fuera de red; `15` medicamentos excluidos.
- Conclusion para Caso 31: no parece requerir proceso nuevo; debe validarse la configuracion del motivo de anulacion usado en GIS. Para cumplir lo esperado debe tener `notif_prestad = 'S'` y `notif_plant_proc_id = NULL`, de modo que se envie solo la notificacion al prestador con proceso 6 y no una notificacion dinamica al afiliado.
- Aplica el ajuste del Caso 30: en `pri_solicitudes_auto_notif`, el bloque de varios prestadores debe quedar solo para `@prm_id = 1`; si `@prm_id = 6` queda en ese bloque, podria notificar a prestadores relacionados y no solo al principal.
## 2026-08-21 - Tarea 29489 - Casos 26 a 31

### Preferencias operativas de Wilmer
- Wilmer pidio recordar dos reglas para la jornada: no realizar modificaciones directas en el entorno ni commits; el agente solo propone y muestra cambios por pantalla o archivo. La ejecucion y commits quedan bajo responsabilidad exclusiva de whiguera.
- No asumir que un paso fue comprendido o cerrado sin visto bueno explicito de whiguera.

### Caso 26 - Notificacion solicitud en gestion
- Caso 26 quedo resuelto por Wilmer.
- Contexto tecnico conservado: en `dbo.pru_genera_auto_precarga`, la notificacion `@prm_id = 4` debe ejecutarse despues del `UPDATE SOLICITUDES_AUTO` que persiste `soli_esta = @var_soli_esta` y antes del `--WORKFLOW` / `dbo.pru_solicitudes_auto_workflow`.
- Condicion acordada: notificar solo si `@var_soli_esta = 'G'` y no existe auditoria previa con `proceso_id: 4`, para evitar duplicados.
- Validacion esperada: auditoria con `Notificacion solicitud en gestion`, `proceso_id: 4`, `tipo_usuari = P`, y actualizacion de `SOLICITUDES_AUTO.mail_id_prestad_mail`.

### Caso 28 - Notificaciones estado solicitud anulada
- Se valido que la anulacion de solicitud GIS/WebPos no usa el mismo SP que Presmed.
- GIS/WebPos llama `dbo.pru_solicitudes_auto_anula_total` desde `WebPos/modelo/solicitudAuto.php`; el SP audita `Anulacion GIS` y actualiza `SOLICITUDES_AUTO.soli_esta = 'U'`.
- Presmed Bandeja llama `dbo.pru_solicitudes_auto_anula_bandeja` desde `wp_bandeja_solicitudes.srw`; audita `Anulacion Presmed Bandeja` y tiene logica de notificacion con `@prm_id = 6` para prestador y `@prm_id = 14/15` para afiliado segun motivo.
- Presmed clasico / ventana `wp_anula_solicitudes.srw` llama `dbo.prd_autorizaciones` y luego recalcula estado con `dbo.pru_solicitudes_auto_soli_esta`, con `@prm_origen = 'P'`.
- Wilmer encontro que la notificacion de anulacion no estaba mal en el SP: depende de la ventana `Motivos Devolucion / Anulacion`.
- Nombre tecnico de ventana: `wp_moti_dev_anu.srw`.
- DataWindow de captura: `d_moti_dev_anu.srd`.
- DataWindow lista: `d_lista_moti_dev_anu.srd`.
- DDDW relacionada al proceso: `dddw_solicitudes_auto_notif_plant_proc.srd`.
- Causa del caso: motivo parametrizado con proceso de afiliado para prestadores fuera de red y sin notificar al prestador.
- Se actualizo la working copy SVN `svn/whiguera-Version84-Modelo2Gis` a revision `37634`; llegaron cambios en `wp_moti_dev_anu.srw`, `d_moti_dev_anu.srd` y `dddw_solicitudes_auto_notif_plant_proc.srd`. No se hicieron commits ni cambios manuales.
- Para permitir quitar un proceso seleccionado sin crear DDDW ni SP nuevos, se propuso agregar en memoria una fila blanca/null al DataWindowChild de `notif_plant_proc_id` dentro de `wp_moti_dev_anu.srw`, evento `ue_restaurar`, despues de recuperar la DDDW actual. Wilmer confirmo: "Listo quedo bien".
- Sobre trazabilidad: se concluyo que no hace falta crear una tabla nueva para cubrir auditoria historica basica, porque `pri_solicitudes_auto_notif` llama `pri_solicitudes_auto_auditoria` y deja registro en `SOLICITUDES_AUTO_AUDITORIA.activi` con `mail_id`, `email`, `notifica: Prestador/Afiliado` y `proceso_id`.
- Aclaracion para funcional: `SOLICITUDES_AUTO.mail_id_prestad_mail` y `mail_id_afi_mail` son ultimas referencias operativas; el historial completo debe consultarse en `SOLICITUDES_AUTO_AUDITORIA`. Para reporting formal con columnas separadas podria evaluarse tabla nueva, pero no es obligatorio para Caso 28.

### Caso 29 - Registrada con varios prestadores existentes y uno inexistente
- Funcional espera notificar a todos los prestadores existentes registrados en `SOLICITUDES_AUTO_PRESTAD`, aunque compartan la misma direccion de correo.
- Causa raiz encontrada en `dbo.pri_solicitudes_auto_notif`: el bloque de prestadores agrupaba por email (`GROUP BY X.email`), lo que deduplicaba prestadores distintos con el mismo correo.
- Correccion conceptual del punto 1: eliminar la agrupacion por email y cargar una fila en `#DESTINATARIOS_NOTIF` por cada fila valida de `SOLICITUDES_AUTO_PRESTAD` con `prestad IS NOT NULL`, `baja_fecha IS NULL` y email no vacio. Mantener principal/orden segun `SAP.prestad = @var_prestad_id`.
- Wilmer probo y confirmo que las consultas salieron bien: quedaron envios/auditorias separados, incluyendo prestadores con emails iguales en distinta capitalizacion. Caso 29 punto 1 quedo validado.
- Punto 2: para prestador inicialmente inexistente (`SOLICITUDES_AUTO_PRESTAD.prestad IS NULL`), el SP que posteriormente completa el prestador por NIT es `dbo.pru_registra_solicitudes_auto`, con update desde `PRESTADORES P ON SAP.nit = P.cuit`.
- Analisis tecnico: no conviene llamar `pri_solicitudes_auto_notif @prm_id = 1` sin filtro, porque reenviaria a todos los prestadores existentes de la solicitud. Una solucion robusta seria capturar con `OUTPUT` las filas que pasaron de `prestad NULL` a valor asignado y permitir parametros opcionales en `pri_solicitudes_auto_notif` para notificar solo ese `nit/prestad`.
- Luego Wilmer compartio respuesta funcional: el segundo punto lo tratarian con proceso `24`. Se valido que proceso 24 corresponde a `Notificacion Ejecutivos de Atencion Integral y Ejecutivo de FONOSALUD / Notificacion Resuelta Convenios Medicos`, no al flujo `Registrada prestador`. Conclusion: no resolver ese punto dentro del flujo de registrada/prestador del Caso 29; queda diferido al analisis del proceso 24 / Convenios Medicos.

### Caso 30 - Notificaciones estado devolucion
- Flujo tecnico: solicitud creada desde GIS por prestador; Presmed la devuelve desde `wp_bandeja_solicitudes.srw`, evento `ue_devolver()`, llamando a `dbo.pru_solicitudes_auto_devolucion`.
- `pru_solicitudes_auto_devolucion` inserta en `SOLICITUDES_AUTO_DEVOLUCION`, audita `Devolucion solicitud`, actualiza `soli_esta = 'D'` y notifica segun creador: si `@var_tipo_usuario_soli = 'P'`, llama `pri_solicitudes_auto_notif @prm_id = 2`; si `A` o `E`, llama `@prm_id = 16`.
- Para solicitud creada por prestador, funcional espera solo notificar al prestador principal de la solicitud, no a otros prestadores para los cuales se solicita.
- Causa encontrada: el bloque de destinatarios prestador de `pri_solicitudes_auto_notif` se usaba para `@prm_id IN (1,2,3,4,5,6,21)`, tomando varios prestadores desde `SOLICITUDES_AUTO_PRESTAD`. Eso hacia que devolucion (`@prm_id = 2`) notificara tambien a prestadores relacionados.
- Correccion validada en SP local adjunto por Wilmer: cambiar ese bloque a `IF @prm_id IN (1)` para que solo registrada use varios prestadores. Con esto `@prm_id = 2` cae al fallback de prestador principal (`@var_email_prestad_1/@var_email_prestad_2` desde `SOLICITUDES_AUTO`).
- Observacion: este cambio tambien hace que `@prm_id = 3,4,5,6,21` usen solo prestador principal, lo cual queda alineado con lo analizado hasta ahora porque el unico proceso con varios prestadores pedido por funcional fue `@prm_id = 1`.
- Sobre los dos emails con mismo prestador `ARIAS JOSE`: ocurre porque `@prestad` se arma desde `SOLICITUDES_AUTO.prestad`; al limitar devolucion al prestador principal el sintoma deja de aplicar.
- Valor cero en dias de devolucion: el SP lee `dbo.afn_ParametrosGrales('Presmed','Autorizaciones','vencimiento_devolucion_soli')` en minutos y calcula `CEILING(minutos / 1440)`. `0` minutos muestra `0` dias; `4320` minutos muestra `3` dias.
- Respuesta sugerida a funcional: el cero no es error del SP ni del reemplazo en plantilla; correspondia a parametrizacion anterior o prueba previa. Con parametro actual `vencimiento_devolucion_soli = 4320`, el mail debe informar 3 dias. Los mails ya enviados no se recalculan.

### Caso 31 - Revision desde PDF R29489-TEST_20260727.pdf
- Se analizo el Caso 31 del documento ubicado por Wilmer en `A:\whiguera\Tarea #23986\Tarea #29489`.
- Flujo: anulacion desde GIS por parte del prestador.
- Entra por `WebPos/modelo/solicitudAuto.php`, que ejecuta `dbo.pru_solicitudes_auto_anula_total`.
- En `pru_solicitudes_auto_anula_total`, si el motivo tiene `notif_prestad = 'S'` y la solicitud fue creada por prestador (`tipo_usuario = 'P'`), se llama `dbo.pri_solicitudes_auto_notif @prm_id = 6`.
- Proceso correcto: `6 - Notificacion PRESTADOR, estado de solicitud ANULADA`.
- No corresponden procesos `14` ni `15` para este flujo, porque son notificaciones al afiliado en escenarios especificos: `14` medicos no adscritos/fuera de red; `15` medicamentos excluidos.
- Conclusion para Caso 31: no parece requerir proceso nuevo; debe validarse la configuracion del motivo de anulacion usado en GIS. Para cumplir lo esperado debe tener `notif_prestad = 'S'` y `notif_plant_proc_id = NULL`, de modo que se envie solo la notificacion al prestador con proceso 6 y no una notificacion dinamica al afiliado.
- Aplica el ajuste del Caso 30: en `pri_solicitudes_auto_notif`, el bloque de varios prestadores debe quedar solo para `@prm_id = 1`; si `@prm_id = 6` queda en ese bloque, podria notificar a prestadores relacionados y no solo al principal.

## 2026-08-21 - Tarea 29489 - Continuacion Casos 31 a 35

### Caso 31 - Aclaracion posterior
- Wilmer pregunto si el Caso 31 es el mismo criterio de que la notificacion de anulacion de solicitud depende de la ventana `Motivos Devolucion / Anulacion`.
- Respuesta confirmada: si, para este punto no debe hacerse cambio de codigo; la notificacion esta bien y depende de la parametrizacion del motivo.
- Configuracion esperada del motivo para anulacion por prestador: `notif_prestad = S` y `notif_plant_proc_id = NULL`.
- Si aparecen mails con subject `Registro Solicitud No...` o `Solicitud Finalizada No...`, no son ambos eventos de anulacion. El primero corresponde al registro/alta y el segundo indica probablemente un proceso dinamico mal asociado al motivo.
- Para anular por prestador, el proceso correcto sigue siendo `6 - Notificacion PRESTADOR, estado de solicitud ANULADA`; no se identifican procesos de anulacion al afiliado para esta etapa.
- Respuesta funcional sugerida: revisar parametrizacion del motivo de anulacion en `Motivos Devolucion / Anulacion`, no crear logica nueva.

### Caso 32 - Solicitud creada en GIS y anulada desde Presmed
- El Caso 32 fue analizado desde el PDF `R29489-TEST_20260727.pdf` en `A:\whiguera\Tarea #23986\Tarea #29489`.
- Es el mismo criterio funcional del Caso 31, pero el origen tecnico de anulacion cambia: la anulacion se realiza desde Presmed.
- Flujo tecnico: ventana `wp_bandeja_solicitudes.srw`; SP `dbo.pru_solicitudes_auto_anula_bandeja`.
- `pru_solicitudes_auto_anula_bandeja` lee `notif_prestad` y `notif_plant_proc_id` desde `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`.
- Si el motivo tiene `notif_prestad = 'S'` y la solicitud fue creada por prestador (`tipo_usuario = 'P'`), ejecuta `dbo.pri_solicitudes_auto_notif @prm_id = 6`.
- El proceso esperado es `6 - Notificacion PRESTADOR, estado de solicitud ANULADA`.
- Si `notif_plant_proc_id` no es null, el SP ejecuta tambien ese proceso dinamico, por lo que puede disparar afiliado o subjects incorrectos. Para este caso el motivo debe quedar con `notif_prestad = S` y `notif_plant_proc_id = NULL`.
- No hay que crear logica nueva; se debe asegurar configuracion correcta del motivo y mantener el ajuste de `pri_solicitudes_auto_notif` para que varios prestadores aplique solo a `@prm_id = 1`.

### Caso 33 - Solicitud generada en Presmed y anulada en Presmed
- El Caso 33 fue analizado desde el mismo PDF.
- Aunque el documento dice "idem caso anterior", tecnicamente difiere porque la solicitud fue creada en Presmed, por lo que `SOLICITUDES_AUTO.tipo_usuario` normalmente no sera `P`, sino `E` o equivalente.
- Flujo tecnico de anulacion Presmed por bandeja: `dbo.pru_solicitudes_auto_anula_bandeja`.
- El SP solo ejecuta proceso `6` al prestador si `@var_notif_prestad = 'S'` y `@var_tipo_usuario_soli = 'P'`.
- Para solicitud creada en Presmed, en principio no corresponde disparar automaticamente proceso 6, salvo definicion funcional expresa.
- Si aparece notificacion al afiliado o subject incorrecto, la causa probable vuelve a ser `notif_plant_proc_id` cargado en el motivo. Si no corresponde notificacion dinamica, debe quedar en `NULL`.
- Respuesta funcional sugerida: para solicitud generada y anulada en Presmed no aplica la misma regla automatica del proceso 6, condicionada a solicitudes creadas por prestador; revisar configuracion del motivo si se emite afiliado/subject incorrecto.
- El PDF tambien incluye un segundo hallazgo no relacionado con notificaciones: al cambiar direccion/lugar del prestador en solapa Prestadores, no cambia el dato en "servicios a otros prestadores". Se identifico como tema de sincronizacion/refresco entre solapas, no mezclarlo con notificaciones.

### Caso 34 - Solicitud generada en Presmed y anulada en GIS
- Wilmer pregunto si el Caso 34 tambien es el mismo caso de dependencia de `Motivos Devolucion / Anulacion`.
- Respuesta: si, tambien depende de la configuracion del motivo, con aclaracion tecnica por origen de la solicitud.
- Flujo tecnico: GIS/WebPos ejecuta `dbo.pru_solicitudes_auto_anula_total`, que lee `notif_prestad` y `notif_plant_proc_id` desde `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`.
- Si se dispara notificacion incorrecta, subject incorrecto o afiliado que no corresponde, el origen vuelve a ser parametrizacion del motivo.
- Como la solicitud fue generada en Presmed, normalmente `tipo_usuario` no deberia ser `P`; por tanto no deberia dispararse automaticamente `6 - Notificacion PRESTADOR, estado de solicitud ANULADA`, salvo que el dato haya quedado como `P` o funcional defina nueva regla.
- Configuracion esperada si no corresponde notificacion dinamica: `notif_plant_proc_id = NULL`.
- Solo si funcional define expresamente notificar al prestador tambien para solicitudes creadas en Presmed, habria que cambiar regla de negocio en SP.

### Caso 35 - Recupero de ultima direccion de email del prestador
- El Caso 35 no corresponde al caso de `Motivos Devolucion / Anulacion`; es otro flujo: carga/recupero de email de prestadores en ventana de solicitud Presmed.
- Evidencia funcional: la ventana no recupera la ultima direccion de email ingresada para el prestador. Consulta usada: `select * from SOLICITUDES_AUTO_PRESTAD where soli_auto >= '25920120' and prestad is not null`.
- Ventana involucrada: `wp_soli_reg.srw`.
- En "Servicios de Otros Prestadores", al ingresar NIT, el codigo llama `wf_buscar_nit(data, ll_prestad, ls_nombre, ls_email)` y luego setea `prestad`, `nombre` y `email`.
- `wf_buscar_nit` llama al SP `prs_prestad_nit`, que actualmente devuelve `@prm_email = email` desde `PRESTADORES`.
- Conclusion: hoy se recupera el email maestro de `PRESTADORES.email`, no la ultima direccion usada historicamente en `SOLICITUDES_AUTO_PRESTAD.email`.
- El dato historico si se guarda: `pru_registra_solicitudes_auto` inserta/actualiza `SOLICITUDES_AUTO_PRESTAD.email` con lo que llega desde la ventana.
- Logica esperada: buscar prestador por NIT como hoy; obtener prestad/nombre/email maestro; luego buscar ultimo email usado en `SOLICITUDES_AUTO_PRESTAD` para ese prestador/NIT; si existe, priorizarlo; si no existe, mantener email maestro.
- Consulta conceptual propuesta: `SELECT TOP 1 SAP.email FROM SOLICITUDES_AUTO_PRESTAD SAP WHERE SAP.prestad = @prm_prestad AND SAP.baja_fecha IS NULL AND SAP.email IS NOT NULL AND LTRIM(SAP.email) <> '' ORDER BY SAP.modi_fecha DESC, SAP.soli_auto DESC`.
- Propuesta PB: despues de `wf_buscar_nit(...)`, llamar algo como `wf_buscar_ultimo_email_prestad(ll_prestad, ls_email)` para reemplazar `ls_email` solo si existe historico.
- Respuesta funcional sugerida: actualmente la ventana recupera email desde maestro de prestadores; corresponde ajustar para priorizar el ultimo email vigente registrado en `SOLICITUDES_AUTO_PRESTAD`, manteniendo como respaldo el email de `PRESTADORES`.


### Caso 35 - Correccion de alcance y cierre de jornada
- Wilmer corrigio el analisis inicial del Caso 35: la imagen/evidencia no corresponde a `Servicios de Otros Prestadores`, sino a la ficha o pestaña `Prestadores`, seccion/datos del ejecutor, campo recientemente agregado `email_prestad`.
- Ese campo fue agregado por peticion funcional para que Presmed trabaje como GIS respecto al email operativo del prestador.
- Objeto PowerBuilder principal revisado: `exe_presmed/Source Infomedical/wp_soli_reg.srw`.
- Campo funcional/tecnico: `email_prestad` en la DataWindow de efector/prestador ejecutor de `wp_soli_reg`.
- Funcion actual en Presmed: `wf_buscar_email_prestad(long al_prestad, ref string as_email_prestad)`.
- Funcionamiento actual de `wf_buscar_email_prestad`:
  - Inicializa `as_email_prestad` y `ls_email` en NULL.
  - Si `al_prestad` es NULL o `<= 0`, retorna 0 sin email.
  - Llama `dbo.prs_prestadores` con `@criterio = 'PR'`, `@prestad = :al_prestad`, `@activos = 'N'`, `@prm_origen = 'W'`.
  - Hace `FETCH` del resultado y toma `ls_email` desde el SP.
  - Asigna `as_email_prestad = ls_email`.
  - Retorna 0.
- Punto exacto donde se usa: dentro del flujo de seteo del prestador ejecutor, luego de `wf_buscar_nit(ls_cuit, ll_prestad, ls_nombre, ls_email)`, se llama `wf_buscar_email_prestad(ll_prestad, ls_email)` y luego `a_dw.SetItem(1, 'email_prestad', ls_email)`.
- Al registrar la solicitud, `wp_soli_reg.srw` toma `idw_efec.Object.email_prestad[1]` y lo envia al SP `dbo.pru_registra_solicitudes_auto` como `@prm_email_prestad`.
- En backend, `pru_registra_solicitudes_auto` guarda ese valor en `SOLICITUDES_AUTO.email_prestad` tanto en insert como en update de la solicitud.
- En `dbo.pri_solicitudes_auto_notif`, el email operativo del prestador se prioriza asi: `@var_email_prestad_1 = CASE WHEN ISNULL(LTRIM(RTRIM(S.email_prestad)), '') <> '' THEN S.email_prestad ELSE P.email END`, y `@var_email_prestad_2 = P.email_2`.
- Por lo tanto, para notificaciones, `SOLICITUDES_AUTO.email_prestad` tiene prioridad sobre el email maestro `PRESTADORES.email`.
- Validacion de GIS/WebPos:
  - En `WebPos/frames/frame_prestador.php`, el campo `emailPrestad` se muestra editable con el valor `$email`.
  - En `WebPos/js/gene_noambu.js` y `WebPos/js/gene_auto.js`, `emailPrestad` es obligatorio y se valida formato.
  - En `WebPos/controlador/registrar_solicitud.php`, se toma `$_POST['email_prestad']`.
  - En `WebPos/modelo/solicitudAuto.php`, se envia a `dbo.pru_registra_solicitudes_auto` como `@prm_email_prestad`.
  - Es decir, GIS/WebPos conserva el email operativo ingresado/editado en pantalla y lo guarda en `SOLICITUDES_AUTO.email_prestad`.
- Conclusion final acordada con Wilmer para continuar manana: Presmed debe igualarse al comportamiento de GIS para el campo `email_prestad` de la pestaña Prestadores.
- Regla funcional esperada para Presmed:
  1. Al seleccionar/cargar el prestador ejecutor, recuperar primero el ultimo email operativo usado para ese mismo prestador desde `SOLICITUDES_AUTO.email_prestad`.
  2. Si existe ultimo email operativo no vacio, usarlo para precargar `email_prestad` en pantalla.
  3. Si no existe historico operativo, usar como respaldo el email maestro que hoy devuelve `prs_prestadores` desde `PRESTADORES.email`.
  4. Mantener el campo editable, igual que GIS.
  5. Al grabar, persistir lo editado en `SOLICITUDES_AUTO.email_prestad` mediante `@prm_email_prestad`, como ya ocurre.
- Consulta conceptual para recuperar ultimo email operativo del prestador ejecutor:
  `SELECT TOP 1 S.email_prestad FROM SOLICITUDES_AUTO S WHERE S.prestad = @prm_prestad AND S.baja_fecha IS NULL AND ISNULL(LTRIM(S.email_prestad), '') <> '' ORDER BY ISNULL(S.modi_fecha, S.fecha) DESC, S.soli_auto DESC`.
- Nota tecnica SQL para manana: respetar reglas del workspace, especialmente no usar `RTRIM` ni punto y coma en scripts SQL nuevos/modificados. Si se propone SQL, usar `LTRIM`/comparaciones permitidas, y preservar encoding/finales de linea si Wilmer decide editar.
- No se realizaron modificaciones directas ni commits. Wilmer mantiene la ejecucion de cambios y commits bajo su responsabilidad.
- Estado al cierre: Caso 35 queda pendiente para manana en el punto de definir paso a paso el ajuste en `wp_soli_reg.srw`/funcion auxiliar o SP/helper para que Presmed recupere `email_prestad` igual que GIS.
- Frase sugerida para retomar manana: "Lisa, retomemos la memoria del 2026-08-21 de la Tarea 29489, Caso 35, desde el punto de igualar Presmed a GIS para recuperar el ultimo email operativo del prestador en el campo email_prestad."
