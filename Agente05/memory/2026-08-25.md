# Memoria 2026-08-25

## Preferencias activas de Wilmer
- Wilmer indico dos reglas estrictas para la sesion: no realizar modificaciones directas en el entorno ni commits; Lisa solo debe proponer y mostrar cambios por pantalla o archivo. La ejecucion de modificaciones y commits queda a cargo exclusivo de whiguera.
- No asumir que un paso fue comprendido, aprobado, cerrado o testeado sin visto bueno explicito de whiguera.

## Tarea 29489 - InfoMailer / procesos 8, 9 y 13
- Se recupero memoria del 2026-08-24 y se continuo el analisis sin tocar codigo.
- Definicion funcional confirmada: proceso 8 adjunta ordenes autorizadas; proceso 9 adjunta cartas de negacion; proceso 13 debe enviar un solo email al afiliado con multiples adjuntos combinando ordenes autorizadas y cartas de negacion cuando correspondan.
- Se corrigio el criterio tecnico inicial: no conviene basar el diseno en `MAIL_AUTORIZACIONES.tipo` como eje principal, porque el campo puede tener uso previo/indefinido y en algunos SPs aparece comentado o reemplazado por valores fijos. Criterio recomendado: usar `AUTORIZACIONES.esta` para decidir el PDF (`A` = orden autorizada, `N` = carta de negacion), y validar carta finalizada con datos de `CARTA_NEGACION`.
- `prs_datos_email_auto` podria requerir ajuste para devolver `AUTORIZACIONES.esta` y datos necesarios de carta finalizada, en lugar de depender de `MAIL_AUTORIZACIONES.tipo`.
- Se valido que la carta de negacion no se resuelve solo con recuperar `dwc_carta_negacion`: en `wr_auto_impresion.srw` el flujo valida requisitos con `prs_carta_negacion_requisitos`, verifica firma, genera archivo fisico de firma con `uo_nv_valida_auto.of_genera_archivo_firma`, recupera `dwc_carta_negacion`, ejecuta/usa la logica de `prs_datos_generales_afilia_carta_nega_imp` y setea `dw_imprimir.object.dw_3.object.file_path[...] = ls_file` antes de generar el PDF.
- La ruta API de carta/adjunto no parece guardarse al finalizar la carta. La finalizacion actualiza `CARTA_NEGACION.final_fecha`; la ruta se guarda al imprimir/enviar/subir con `nvo_sube_adjunto.of_sube_adjunto_gral` y `pru_auto_arch_path_full`, destino `AUTORIZACIONES.archivo_path_full`.
- Opinion funcional recibida: InfoMailer no deberia generar PDFs porque su responsabilidad es enviar correos y podria convertirse en cuello de botella. Lisa estuvo de acuerdo.
- Nuevo rumbo recomendado: no convertir InfoMailer en generador principal. Los PDFs deben generarse en el flujo funcional donde se realiza la accion de notificacion desde Registro/Bandeja: solicitud `R` registrada, `D` devuelta, `G` en gestion, `U` anulada.
- Riesgo clave: hoy `pri_solicitudes_auto_notif` crea el mail y luego llama a `afu_mail_envio_send`, dejando el mail en estado disponible para InfoMailer (`NOE`). Si se generan adjuntos despues, InfoMailer podria tomar el mail antes de que esten listos.
- Diseno propuesto pendiente de aprobacion: separar el flujo en dos etapas. Etapa 1 crea mail y devuelve `mail_id` sin marcarlo para envio. Luego PowerBuilder genera PDFs, aplica firma en cartas, sube a API si corresponde, guarda `AUTORIZACIONES.archivo_path_full` y registra adjuntos en `MAIL_ATTACH` o `MAIL_ATTACH_BINARY`. Etapa 2 llama a `afu_mail_envio_send` solo cuando todos los adjuntos estan preparados.
- Lugares/procesos mencionados: `wp_soli_reg.srw` llama a `pru_registra_solicitudes_auto`; este cambia solicitud a `R` y llama a `pri_solicitudes_auto_notif`. Para devolucion: `pru_solicitudes_auto_devolucion` cambia a `D` y llama a `pri_solicitudes_auto_notif` con proceso 2 o 16. Para gestion: `pru_solicitudes_auto_resuelta_pertinencia` cambia a `G` y llama a `pri_solicitudes_auto_notif` con proceso 4. Para anulacion: SPs como `pru_solicitudes_auto_anula_bandeja`, `pru_solicitudes_auto_anula_total` y `prs_control_vencimiento_devolucion_soli` terminan llamando a `pri_solicitudes_auto_notif`.
# Memoria 2026-08-25

## Preferencias activas de Wilmer
- Wilmer indico dos reglas estrictas para la sesion: no realizar modificaciones directas en el entorno ni commits; Lisa solo debe proponer y mostrar cambios por pantalla o archivo. La ejecucion de modificaciones y commits queda a cargo exclusivo de whiguera.
- No asumir que un paso fue comprendido, aprobado, cerrado o testeado sin visto bueno explicito de whiguera.

## Tarea 29489 - InfoMailer / procesos 8, 9 y 13
- Se recupero memoria del 2026-08-24 y se continuo el analisis sin tocar codigo.
- Definicion funcional confirmada: proceso 8 adjunta ordenes autorizadas; proceso 9 adjunta cartas de negacion; proceso 13 debe enviar un solo email al afiliado con multiples adjuntos combinando ordenes autorizadas y cartas de negacion cuando correspondan.
- Se corrigio el criterio tecnico inicial: no conviene basar el diseno en `MAIL_AUTORIZACIONES.tipo` como eje principal, porque el campo puede tener uso previo/indefinido y en algunos SPs aparece comentado o reemplazado por valores fijos. Criterio recomendado: usar `AUTORIZACIONES.esta` para decidir el PDF (`A` = orden autorizada, `N` = carta de negacion), y validar carta finalizada con datos de `CARTA_NEGACION`.
- `prs_datos_email_auto` podria requerir ajuste para devolver `AUTORIZACIONES.esta` y datos necesarios de carta finalizada, en lugar de depender de `MAIL_AUTORIZACIONES.tipo`.
- Se valido que la carta de negacion no se resuelve solo con recuperar `dwc_carta_negacion`: en `wr_auto_impresion.srw` el flujo valida requisitos con `prs_carta_negacion_requisitos`, verifica firma, genera archivo fisico de firma con `uo_nv_valida_auto.of_genera_archivo_firma`, recupera `dwc_carta_negacion`, ejecuta/usa la logica de `prs_datos_generales_afilia_carta_nega_imp` y setea `dw_imprimir.object.dw_3.object.file_path[...] = ls_file` antes de generar el PDF.
- La ruta API de carta/adjunto no parece guardarse al finalizar la carta. La finalizacion actualiza `CARTA_NEGACION.final_fecha`; la ruta se guarda al imprimir/enviar/subir con `nvo_sube_adjunto.of_sube_adjunto_gral` y `pru_auto_arch_path_full`, destino `AUTORIZACIONES.archivo_path_full`.
- Opinion funcional recibida: InfoMailer no deberia generar PDFs porque su responsabilidad es enviar correos y podria convertirse en cuello de botella. Lisa estuvo de acuerdo.
- Nuevo rumbo recomendado: no convertir InfoMailer en generador principal. Los PDFs deben generarse en el flujo funcional donde se realiza la accion de notificacion desde Registro/Bandeja: solicitud `R` registrada, `D` devuelta, `G` en gestion, `U` anulada.
- Riesgo clave: hoy `pri_solicitudes_auto_notif` crea el mail y luego llama a `afu_mail_envio_send`, dejando el mail en estado disponible para InfoMailer (`NOE`). Si se generan adjuntos despues, InfoMailer podria tomar el mail antes de que esten listos.
- Diseno propuesto pendiente de aprobacion: separar el flujo en dos etapas. Etapa 1 crea mail y devuelve `mail_id` sin marcarlo para envio. Luego PowerBuilder genera PDFs, aplica firma en cartas, sube a API si corresponde, guarda `AUTORIZACIONES.archivo_path_full` y registra adjuntos en `MAIL_ATTACH` o `MAIL_ATTACH_BINARY`. Etapa 2 llama a `afu_mail_envio_send` solo cuando todos los adjuntos estan preparados.
- Lugares/procesos mencionados: `wp_soli_reg.srw` llama a `pru_registra_solicitudes_auto`; este cambia solicitud a `R` y llama a `pri_solicitudes_auto_notif`. Para devolucion: `pru_solicitudes_auto_devolucion` cambia a `D` y llama a `pri_solicitudes_auto_notif` con proceso 2 o 16. Para gestion: `pru_solicitudes_auto_resuelta_pertinencia` cambia a `G` y llama a `pri_solicitudes_auto_notif` con proceso 4. Para anulacion: SPs como `pru_solicitudes_auto_anula_bandeja`, `pru_solicitudes_auto_anula_total` y `prs_control_vencimiento_devolucion_soli` terminan llamando a `pri_solicitudes_auto_notif`.

## Trabajo con Barbara - bgonzalez-Version84-Modelo2Gis / casos 59 y 60
- Usuario en Google Chat directo: Barbara.
- Repositorio trabajado: `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`.
- Primer pedido: analizar diferencias entre mensajes de asignacion automatica a auditoria en `exe_presmed/Source Infomedical/wp_soli_reg.srw`.
- Resultado del analisis: `wf_mensaje_asignacion_auditoria()` muestra mensaje de asignacion exitosa si hay `responsable_auditoria`; muestra `La solicitud no pudo asignarse automaticamente a auditoria. <motivo>` si no hay responsable pero existe `motivo_no_asignacion`; muestra `La solicitud quedo pendiente de asignacion automatica a auditoria.` cuando no hay responsable ni motivo. El motivo lo arma `pru_proceso_asignacion_masiva` y llega por `pru_genera_auto_precarga` / `prs_salida_auto_precarga`.
- Documento inicial de analisis creado para Barbara: `/mnt/Agentes/bgonzalez/Analisis_Casos_59_60.md`.
- Caso 59 analizado: en `wp_soli_reg.srw`, al cambiar `nomen` se llamaba a `wf_borrar(row, 'N')`, provocando borrado visual de la fila CUPS y conceptos en memoria. Negocio confirmo que la modificacion debe regir solo para estados `R` y `C`, sin afectar funcionalidad actual, y que queda prohibido para CUPS que ya tengan autorizacion.
- Caso 60 analizado: `backend/procedures/pru_wrapper_registra_solicitudes_auto.sql` validaba el prestador, pero no armaba ni pasaba `@prm_otros_prestad`, por lo que `SOLICITUDES_AUTO_PRESTAD` no recibia fila.
- Se modifico `exe_presmed/Source Infomedical/wp_soli_reg.srw`: para origen `G` en estados `R/C`, se habilita eliminar CUPS y editar conceptos; se bloquea eliminar si el CUPS tiene autorizacion generada; al cambiar nomenclador ya no se elimina la fila completa sino que se limpian datos dependientes del CUPS.
- Se modifico `backend/procedures/pru_wrapper_registra_solicitudes_auto.sql`: el wrapper arma `@prm_otros_prestad` con el prestador validado y lo pasa a `pru_registra_solicitudes_auto`, para persistir `SOLICITUDES_AUTO_PRESTAD`.
- Commit SVN realizado: revision `r37655` con mensaje `R26460 - 0007468: Req. Bandeja de Auditoria - Modelos 2, 3 y 4 Fase 1 Proyecto GIS - Registro de Solicitudes`.
- Documento de casos de prueba creado: `/mnt/Agentes/bgonzalez/Casos_Prueba_Casos_59_60.md`.
- Validaciones realizadas: revision de `svn diff`, `svn status` limpio para fuentes modificados con externos `X` preexistentes, SQL validado con `SET PARSEONLY ON` sobre perfil/base `InstallQA`, sin errores reportados. Se preservaron encoding/BOM/finales de linea.

## Tarea 29489 - Ajuste de alcance finalizada y diseno de adjuntos
- Wilmer corrigio el alcance: no se requiere generar adjuntos desde acciones `R`, `D`, `G`, `U`; solo desde la ventana/flujo donde la solicitud pasa a Finalizada (`F`).
- Punto de entrada confirmado: `exe_presmed/Source Infomedical/wp_soli_reg.srw`, evento `ue_fin_gestion`, boton `cb_fin_gestion`, que ejecuta `dbo.pru_fin_gestion_solicitudes_auto`.
- Alcance funcional protegido: no afectar notificaciones existentes sin adjuntos (`R` registrada, `D` devuelta, `G` en gestion, `U` anulada). El cambio debe quedar encapsulado para `F` y procesos `8`, `9`, `13`.
- Diseno aprobado conceptualmente por Wilmer para propuesta: PowerBuilder genera/sube PDFs y registra adjuntos antes de liberar el mail; InfoMailer queda solo como enviador.
- No usar el bloque propuesto en `pri_solicitudes_auto_notif` que inserta `MAIL_AUTORIZACIONES` y setea `MAIL_ENVIO.genera_impresion_auto = 'S'`, porque eso reactivaria generacion desde InfoMailer. En el nuevo diseno, los PDFs deben quedar como adjuntos reales en `MAIL_ATTACH_BINARY`.
- Paso tecnico propuesto: agregar a `pri_solicitudes_auto_notif` parametros opcionales `@prm_marcar_para_envio CHAR(1) = 'S'` y `@out_mail_id INTEGER OUTPUT`; llamar a `afu_mail_envio_send` solo si `@prm_marcar_para_envio = 'S'`, manteniendo sin cambios los procesos existentes por default.
- Paso tecnico propuesto: en `pru_fin_gestion_solicitudes_auto`, cuando la solicitud quede en `F`, determinar proceso `8` si todas las autorizaciones vigentes estan en `A`, proceso `9` si todas estan en `N` con carta finalizada, y proceso `13` si hay mezcla `A` + `N` con carta finalizada. Llamar `pri_solicitudes_auto_notif` con `@prm_marcar_para_envio = 'N'` y devolver `mail_id` a PowerBuilder.
- Paso tecnico propuesto: crear `prs_solicitudes_auto_adjuntos_finalizada` para que PowerBuilder recupere `mail_id`, `soli_auto`, `sucur`, `auto`, `esta`, `tipo`, `origen`, `archivo_path_full`, datos de carta y bandera de carta finalizada.
- Paso tecnico propuesto en `wp_soli_reg.srw`: despues de ejecutar `pru_fin_gestion_solicitudes_auto`, si retorna proceso `8/9/13`, ejecutar `wf_preparar_adjuntos_notif_finalizada(il_soli, ll_mail_id, li_prm_id_notif)` antes del `COMMIT`.
- `wf_preparar_adjuntos_notif_finalizada` debe recorrer filas devueltas por el SP de adjuntos; si `A.esta = 'A'`, llamar rutina de orden autorizada; si `A.esta = 'N'`, llamar rutina de carta de negacion; registrar cada PDF con `afi_mail_attach_binary`; si todo sale bien, llamar `afu_mail_envio_send`; si falla algo, hacer rollback/no enviar mail parcial.
- Rutina de orden autorizada debe reutilizar la logica de `wr_auto_impresion.srw`: `prs_auto_impresion_especial_opc`, DataWindow `dc_orden_prestad_2` o `dc_orden_prestad_especial`, `Retrieve`, `SaveAs` PDF, `nvo_sube_adjunto.of_sube_adjunto_gral`, `pru_auto_arch_path_full`.
- Rutina de carta de negacion debe reutilizar circuito de `wr_auto_impresion.srw`: `prs_carta_negacion_requisitos`, validar carta/firma/rol, `uo_nv_valida_auto.of_genera_archivo_firma`, `dwc_carta_negacion.Retrieve`, `prs_datos_generales_afilia_carta_nega_imp`, inyectar firma en `dw_3.object.file_path[...]`, `SaveAs` PDF, subir a API, guardar `AUTORIZACIONES.archivo_path_full`, marcar carta como impresa con `pru_carta_negacion @prm_solo_impre = 'S'`, registrar en `MAIL_ATTACH_BINARY`.

## Tarea 29489 - Validacion hallazgos funcionales documento R29489-TEST_20260727.pdf
- Documento analizado: `R29489-TEST_20260727.pdf` en ruta Windows referida por Wilmer `A:\whiguera\Tarea #23986\Tarea #29489`. Se trabajo contra repositorio local SVN `whiguera-Version84-Modelo2Gis` y base `InstallQA` cuando se consulto.
- Caso 5: solicitud `25920128` validada como diagnostica (`soli_tipo = D`, `origen = P`, `coomeva = S`, `afi_tipo_calculado = P`, `prestad = 32`, `prestad_vip = N`, `mail_id_afi_mail = 9989102`).
- Caso 5: parametrizacion activa encontrada para `D / P / P / N`: colorimetria `R` = 17, `A` = 16, `V` = 8; por `ORDER BY` del SP toma `R`, por tanto el tiempo diagnostico esperado es `17 horas habiles`.
- Caso 5: reproduccion de `pri_solicitudes_auto_notif` devolvio `tiempo_ciru = 25`, `tiempo_dx = 17`, `tiempo_hosp_H = NULL`, `tiempo_T = 24`. El mail `9989102` tenia Diagnosticas con `17 horas habiles` y Hospitalarias vacio.
- Caso 5: conclusion comunicada a Wilmer: no fallo diagnosticas ni el envio; el vacio corresponde a Hospitalarias porque el SP busca `soli_tipo = 'H'` y en QA no hay registros activos `H`. Existen registros `T`, pero `SOLICITUDES_AUTO_TIPOS` indica `H = Solicitud Hospitalaria`, `T = Sin tipificar`, por lo que no se debe asumir equivalencia.
- Caso 5: respuesta sugerida al funcional: parametrizar tiempos hospitalarios `H` si la plantilla debe mostrar siempre esa linea, o ajustar plantilla/SP para no mostrar lineas sin valor.
- Caso 6: solicitud original del documento `25920064` tiene `tipo_usuario = E`, `origen = P`, `mail_id_prestad_mail = NULL`, `mail_id_afi_mail = NULL`. Para usuario ejecutivo/Presmed en registro no corresponde notificacion al prestador; por eso `mail_id_prestad_mail = NULL` no prueba error vigente.
- Caso 6: regresion del 24/08 uso solicitud `25920129` con `tipo_usuario = P`, `origen = G`, `mail_id_prestad_mail = 9989103`, `mail_id_afi_mail = 9989105`.
- Caso 6: mails generados en regresion: `9989103` a prestador principal `EMAIL_PRESTAD_GIS_20260824@infomedical.com.ar`, `9989104` a otro prestador `PRUEBASGISPRESTADOR@GMAIL.COM`, `9989105` a afiliado `pruebasgisprestador@gmail.com`.
- Caso 6: auditoria correcta: proceso `1` para prestadores con `mail_id 9989103` y `9989104`, proceso `22` para afiliado con `mail_id 9989105`.
- Caso 6: punto tecnico explicado: `SOLICITUDES_AUTO.mail_id_prestad_mail` solo guarda un mail_id; cuando hay varios prestadores, `pri_solicitudes_auto_notif` guarda el primer mail valido ahi y el detalle completo queda en `SOLICITUDES_AUTO_AUDITORIA`.
- Caso 6: conclusion comunicada: no reproduce como error actual en la regresion del 24/08. En el documento habia una consulta potencialmente confusa/typo: para revisar mails de `25920129`, una linea filtraba `subject like '%25920128%'`.

## Pendiente - Tarea 29489 / Caso 10
- Wilmer pidio dejar pendiente para resolver mas adelante el Caso 10 del documento `R29489-TEST_20260727.pdf`.
- Reclamo funcional: PRESMED debe recuperar la ultima direccion de email del afiliado.
- Validacion realizada en `InstallQA`: para afiliado `prepaga=1 / contra=100199 / inte=01`, solicitudes PRESMED de regresion `25920132` y `25920133` quedaron con `email_afi_1 = NULL`, `email_afi_2 = NULL`, `mail_id_afi_mail = NULL`, aunque existia email vigente en `REGISTRO_UNICO_EMAILS` (`pruebasgisprestador@gmail.com`) y antecedente de mail enviado al afiliado en solicitud `25920114`.
- Diagnostico tecnico: en PRESMED, `wp_soli_reg.srw` llama `wf_afi_datos_extras()`, que usa `prs_afiliado_extras`; este helper devuelve principalmente el segundo email historico/alternativo y puede retornar NULL si solo hay un email vigente. Luego `wf_registra_solicitudes_auto()` solo envia `@prm_email_afi_2 = :ls_email_afi_2`, sin enviar `@prm_email_afi_1`.
- Objetos candidatos para correccion futura: `backend/procedures/prs_afiliado_extras.sql` y `exe_presmed/Source Infomedical/wp_soli_reg.srw`. Revisar tambien `pru_registra_solicitudes_auto.sql`, que ya tiene y guarda `@prm_email_afi_1`, y `pri_solicitudes_auto_notif.sql`, que ya prioriza `email_afi_1` sobre `email_afi_2`.
- Enfoque recomendado: cambio acotado, no rediseño grande. Recuperar `email_afi_1` con prioridad equivalente a `ars_afiliado`: ultimo `MAIL_TO.email_to` del ultimo `SOLICITUDES_AUTO.mail_id_afi_mail` del afiliado; si no existe, email vigente de `REGISTRO_UNICO_EMAILS`. En PowerBuilder, pasar ese valor a `pru_registra_solicitudes_auto` como `@prm_email_afi_1`.

## Cierre operativo 2026-08-25 - Tarea 29489
- Humano principal: Wilmer Higuera. Reglas activas reiteradas: no modificar codigo ni hacer commits sin autorizacion expresa; Lisa debe proponer, analizar y mostrar evidencia, y no asumir aprobacion/cierre sin visto bueno de Wilmer.
- Repositorio base usado para analisis: `/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis`.
- Base usada para validaciones: profile `InstallQA`, SQL Server `172.16.10.124:28000`, base `InstallQA`.
- Documento funcional referido: `R29489-TEST_20260727.pdf`, ruta Windows informada por Wilmer `A:\whiguera\Tarea #23986\Tarea #29489`. En el entorno actual no quedo disponible el PDF localmente para Caso 37; cuando se intento buscar por `R29489-TEST_20260727.pdf` y patrones `*29489*.pdf`, solo aparecio `tmp/tarea29489`, sin PDF.

### Propuesta pendiente de aprobacion - PDFs finalizados procesos 8, 9 y 13
- Queda pendiente recibir aprobacion de la propuesta para implementar generacion/envio de adjuntos PDF en solicitudes finalizadas.
- Decision funcional/tecnica acordada: no cargar a InfoMailer con generacion de PDFs. InfoMailer debe quedar solo como enviador de mails con adjuntos ya registrados.
- Alcance corregido por Wilmer: no se deben tocar ni afectar las notificaciones sin adjuntos `R` registrada, `D` devuelta, `G` en gestion, `U` anulada. El cambio aplica solo al flujo que pasa la solicitud a Finalizada (`F`).
- Punto de entrada PowerBuilder confirmado: `exe_presmed/Source Infomedical/wp_soli_reg.srw`, boton `cb_fin_gestion`, evento `ue_fin_gestion`, que ejecuta `dbo.pru_fin_gestion_solicitudes_auto`.
- Procesos alcanzados: `8` para autorizaciones en estado autorizado, `9` para cartas de negacion finalizadas, `13` para combinacion de autorizadas + cartas de negacion en un mismo email.
- Criterio de decision de adjunto: no usar `MAIL_AUTORIZACIONES.tipo` como eje principal; usar `AUTORIZACIONES.esta` (`A` orden autorizada, `N` carta de negacion) y validar carta con `CARTA_NEGACION.final_fecha IS NOT NULL` y `CARTA_NEGACION.baja_fecha IS NULL`.
- Para cartas de negacion, no alcanza con recuperar `dwc_carta_negacion`; se debe replicar la logica de `wr_auto_impresion.srw`: `prs_carta_negacion_requisitos`, validar firma/rol/carta, generar archivo de firma con `uo_nv_valida_auto.of_genera_archivo_firma`, recuperar `dwc_carta_negacion`, usar `prs_datos_generales_afilia_carta_nega_imp` e inyectar `dw_imprimir.object.dw_3.object.file_path[...] = ls_file` antes de `SaveAs`.
- La ruta API no se guarda al finalizar carta; se guarda cuando se imprime/sube con `nvo_sube_adjunto.of_sube_adjunto_gral` y `pru_auto_arch_path_full`, destino `AUTORIZACIONES.archivo_path_full`.
- Propuesta tecnica para implementar cuando aprueben: agregar parametros opcionales en `pri_solicitudes_auto_notif` (`@prm_marcar_para_envio CHAR(1) = 'S'`, `@out_mail_id INTEGER OUTPUT`) para crear mail sin liberarlo cuando aplique finalizada con adjuntos; en `pru_fin_gestion_solicitudes_auto`, detectar proceso 8/9/13, crear mail con `@prm_marcar_para_envio='N'`, devolver `mail_id`; en `wp_soli_reg.srw`, antes del commit, generar/subir PDFs, registrar cada uno con `afi_mail_attach_binary`/`MAIL_ATTACH_BINARY`, y solo al final llamar `afu_mail_envio_send`. Si falla cualquier PDF/firma/subida, hacer rollback y no enviar mail parcial.

### Caso 5 - ERROR CASO 5
- Validacion contra `InstallQA`: solicitud `25920128`, `soli_tipo = D`, `origen = P`, `coomeva = S`, `afi_tipo_calculado = P`, `prestad = 32`, `prestad_vip = N`, `mail_id_afi_mail = 9989102`.
- Diagnosticas si salio correcto: parametrizacion activa para `D / P / P / N` devolvia colorimetria `R = 17`, `A = 16`, `V = 8`; por orden del SP toma `17`.
- Reproduccion de variables en `pri_solicitudes_auto_notif`: `tiempo_ciru = 25`, `tiempo_dx = 17`, `tiempo_hosp_H = NULL`, `tiempo_T = 24`.
- El mail `9989102` tenia Diagnosticas con `17 horas habiles` y Hospitalarias vacio.
- Conclusion comunicada: no fallo el envio ni el valor diagnostico; el vacio corresponde a Hospitalarias porque el SP busca `soli_tipo = 'H'` y QA no tiene registros activos `H`. Existen `T`, pero `SOLICITUDES_AUTO_TIPOS` indica `H = Solicitud Hospitalaria`, `T = Sin tipificar`.

### Caso 6 - ERROR CASO 6
- Primera conclusion corregida con observacion de Wilmer: el reclamo especifico estaba en detalle de email enviado al prestador.
- Solicitud de regresion 24/08: `25920129`, `soli_tipo = D`, `origen = G`, `tipo_usuario = P`, `afi_tipo_calculado = P`, `prestad_vip = N`, `mail_id_prestad_mail = 9989103`, `mail_id_afi_mail = 9989105`.
- Mails generados: `9989103` a prestador principal `EMAIL_PRESTAD_GIS_20260824@infomedical.com.ar`, `9989104` a otro prestador `PRUEBASGISPRESTADOR@GMAIL.COM`, `9989105` a afiliado `pruebasgisprestador@gmail.com`.
- Auditoria correcta para multiples prestadores: proceso `1` para prestadores (`9989103`, `9989104`) y proceso `22` para afiliado (`9989105`).
- Error reclamado en mail a prestador: tiempos de gestion salieron vacios porque `pri_solicitudes_auto_notif` busca `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP` con coincidencia exacta `SATGE.origen = S.origen`. La solicitud es `origen = G`, pero la parametrizacion activa estaba para `origen = P` (`D/P/P/N/R = 17`, `Q/P/P/N/R = 25`, `T/P/P/N/R = 24`). Para `G` no habia registros.
- Respuesta sugerida al funcional: caso valido para mail del prestador; definir si se parametriza origen `G` o si el SP debe normalizar GIS/prestador a los tiempos equivalentes de `P`.

### Caso 10 - Pendiente
- Reclamo: PRESMED debe recuperar la ultima direccion de email del afiliado.
- Validado contra `InstallQA`: afiliado `prepaga = 1`, `contra = 100199`, `inte = 01`; solicitudes PRESMED de regresion `25920132` y `25920133` quedaron en estado `C` con `email_afi_1 = NULL`, `email_afi_2 = NULL`, `mail_id_afi_mail = NULL`.
- Ese afiliado tenia email vigente en `REGISTRO_UNICO_EMAILS`: `pruebasgisprestador@gmail.com`, y antecedente de mail enviado al afiliado en solicitud `25920114`.
- Causa tecnica probable: `wp_soli_reg.srw` usa `wf_afi_datos_extras()`, que llama `prs_afiliado_extras`; ese helper devuelve principalmente el segundo email historico/alternativo y puede devolver NULL si solo hay un email vigente. Luego `wf_registra_solicitudes_auto()` solo pasa `@prm_email_afi_2 = :ls_email_afi_2`, sin pasar `@prm_email_afi_1`.
- Quedo asentado como pendiente para resolver mas adelante. Cambio recomendado acotado: ajustar `prs_afiliado_extras.sql` para devolver email principal con prioridad equivalente a `ars_afiliado` (ultimo `MAIL_TO.email_to` del ultimo `SOLICITUDES_AUTO.mail_id_afi_mail`; fallback a `REGISTRO_UNICO_EMAILS`) y ajustar `wp_soli_reg.srw` para pasar `@prm_email_afi_1` a `pru_registra_solicitudes_auto`.

### Caso 11 - ERROR CASO 11
- Reclamo: para solicitud sin tipificar (`T`), cuando desde PRESMED se seleccione un tipo, el email debe enviarse al afiliado y no al prestador, porque la solicitud fue generada por afiliado.
- Validacion contra `InstallQA`: solicitud `25920131`, `soli_tipo = D`, `soli_esta = R`, `tipo_usuario = A`, `origen = A`, `email_prestad = papa@gmail.com`, `email_afi_1 = pruebasgisprestador@gmail.com`, `email_afi_2 = emailafi_20260819@gmial.com.ar`, `mail_id_prestad_mail = NULL`, `mail_id_afi_mail = 9989107`.
- Mail enviado: `mail_id = 9989107`, subject `Registro Solicitud No.25920131`, fecha `24/08/2026 20:59`, destinatario `pruebasgisprestador@gmail.com`.
- Auditoria: `Notificacion solicitud registrada. mail_id: 9989107. email: pruebasgisprestador@gmail.com. notifica: Afiliado. proceso_id: 7`, usuario `sa`, `tipo_usuari = A`.
- Conclusion comunicada: en la regresion del 24/08 el Caso 11 salio correcto. No se genero mail al prestador (`mail_id_prestad_mail = NULL`) y se genero al afiliado por proceso `7`.
- Evidencia tecnica en codigo: en `pru_registra_solicitudes_auto.sql`, en edicion, cuando una solicitud estaba en `T` y queda en `D/H/Q`, se ejecuta la notificacion registrada. Si el tipo de usuario calculado queda en `A` o `E`, ejecuta `pri_solicitudes_auto_notif @prm_id = 7`, que en `pri_solicitudes_auto_notif.sql` corresponde a `notifica = Afiliado`.

### Caso 37 - Pendiente de insumo
- Wilmer pidio analizar Caso 37 del mismo documento y validarlo contra base.
- Se busco memoria previa con `memory_search` y no hubo contexto util del Caso 37.
- Se busco texto local `CASO 37`, `ERROR - CASO 37`, `caso 37` en memoria, tmp y repositorio de incidentes de Wilmer, sin resultados.
- Se busco el PDF `R29489-TEST_20260727.pdf` y patrones `*29489*TEST*.pdf`, `*29489*.pdf` en rutas locales relevantes; no estaba disponible localmente en esta sesion.
- Se consultaron en `InstallQA` solicitudes recientes `soli_auto >= 25920120` para buscar candidatas, pero sin el texto/captura del Caso 37 no se pudo identificar reclamo funcional ni solicitud exacta.
- Resultado comunicado a Wilmer: para continuar con Caso 37 hace falta que pegue el bloque del documento o una captura donde se vea descripcion, evidencia, solicitud usada, resultado esperado y resultado obtenido.

### Como retomar mañana
- Frase recomendada para Wilmer:
```text
Lisa, retomemos la memoria del 2026-08-25 de la Tarea 29489. Recupera el cierre operativo de hoy: propuesta pendiente para PDFs de finalizada procesos 8, 9 y 13, Caso 10 pendiente de recupero de email afiliado en PRESMED, y continuemos con el Caso 37 del documento R29489-TEST_20260727.pdf. Te comparto la captura/texto del Caso 37 para validarlo contra InstallQA.
```
