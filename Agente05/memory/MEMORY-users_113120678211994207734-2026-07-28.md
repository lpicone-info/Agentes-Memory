# Memoria curada diaria

- Fecha: 2026-07-28
- Usuario: Wilmer Higuera
- id_googlechat: users/113120678211994207734
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/qzugpyaaaae

## Resumen de la actividad

### Tema 1: Análisis de notificaciones de anulación en pru_solicitudes_auto_anula_bandeja

- Solicitud o contexto: El usuario venía revisando la lógica de anulación de solicitudes automáticas y consultó si el llamado actual a `pri_solicitudes_auto_notif` estaba débil para distinguir notificaciones de prestador y afiliado.
- Respuesta o acciones del agente: Se revisaron `pru_solicitudes_auto_anula_bandeja`, tablas y data modifications relacionadas con `SOLICITUDES_AUTO`, `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`, tipos de solicitud y plantillas de notificación.
- Resultado: Se confirmó que el SP leía `soli_tipo` y `soli_esta`, pero no `tipo_usuario`, por lo que no podía distinguir correctamente `tipo_usuario = 'P'` frente a `tipo_usuario IN ('A','E')`. También se detectó como criterio funcionalmente dudoso el cálculo `CASE WHEN @var_soli_tipo = 'D' THEN 15 ELSE 14 END`, porque `soli_tipo = 'D'` no identifica inequívocamente medicamentos.
- Estado: En curso

### Tema 2: Criterio correcto para seleccionar plantillas 14 y 15

- Solicitud o contexto: El usuario preguntó cómo mejorar la selección entre procesos/plantillas `14` y `15`, o cuál era la forma correcta de saber cuál corresponde.
- Respuesta o acciones del agente: Se explicó que la selección no debería depender de `soli_tipo`, sino del motivo de anulación (`@prm_anu_moti`) o de una definición explícita en `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`.
- Resultado: Se propuso como solución robusta agregar un campo de configuración, por ejemplo `notif_afi_plant_proc_id`, para que cada motivo defina la plantilla de afiliado. Como alternativa directa, si funcional confirma los ids reales, usar `@prm_anu_moti = 1 -> 14` y `@prm_anu_moti = 2 -> 15`.
- Estado: En curso

### Tema 3: Confirmación funcional sobre estado anulada y motivo de anulación

- Solicitud o contexto: El usuario informó que funcional indicó que el formato del email lo define el estado `anulada` y el motivo de anulación: motivo 1 `Médicos no adscritos / Prestadores fuera de la red`, motivo 2 `Solicitud por Medicamentos`.
- Respuesta o acciones del agente: Se ajustó el criterio propuesto: para prestador, usar `tipo_usuario = 'P'` con plantilla `6`; para afiliado o ejecutivo, usar `tipo_usuario IN ('A','E')` y plantilla según motivo: `@prm_anu_moti = 1 -> 14`, `@prm_anu_moti = 2 -> 15`.
- Resultado: Quedó definido el cambio conceptual, pero condicionado a confirmar que los ids `1` y `2` existan realmente con esos significados en la tabla operativa.
- Estado: En curso

### Tema 4: Código sugerido para modificar el SP sin aplicarlo

- Solicitud o contexto: El usuario pidió mostrar detalladamente qué debía adicionar al SP, sin pedir modificación directa.
- Respuesta o acciones del agente: Se indicó agregar la variable `@var_tipo_usuario_soli`, leer `tipo_usuario` desde `SOLICITUDES_AUTO`, condicionar la notificación de prestador a `@var_tipo_usuario_soli = 'P'`, y reemplazar la selección `soli_tipo = 'D' THEN 15 ELSE 14` por selección de plantilla según `@prm_anu_moti` para afiliado/ejecutivo.
- Resultado: Se entregaron bloques SQL detallados para reemplazo manual en `pru_solicitudes_auto_anula_bandeja`. No se modificó el SP ni se hizo commit.
- Estado: Pendiente

### Tema 5: Validación de ids 1 y 2 en bases registradas

- Solicitud o contexto: El usuario preguntó en qué tabla encontrar `prm_anu_moti = 1` y `2`, porque en `InstallQA` no estaban esos dos registros con el significado esperado, y consultó si existían en alguna base.
- Respuesta o acciones del agente: Se identificó la tabla `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` y se consultaron los perfiles registrados `InstallQA`, `InstallQA_89`, `PruebasCoreINFOMEDICAL` y `PruebasCorePrestadores`. Se validó la existencia de registros por id y por texto relacionado con medicamentos, médicos no adscritos, red y prestador. Las credenciales se usaron solo operativamente y no se conservaron valores en memoria.
- Resultado: En `InstallQA` no se encontraron registros con `id = 1` ni `id = 2`. En `InstallQA_89` sí existían `id = 1` y `id = 2`, pero correspondían a `Soportes Incompletos` y `Soportes Ilegibles`, no a los motivos informados por funcional. En `PruebasCoreINFOMEDICAL` y `PruebasCorePrestadores` no existía la tabla. Tampoco se encontraron esos motivos por texto en `InstallQA` ni `InstallQA_89`.
- Estado: Pendiente

### Tema 6: Memoria bajo demanda para retomar al día siguiente

- Solicitud o contexto: El usuario pidió guardar lo trabajado y lo que estaba en análisis, aclarando que aún no modificó el SP hasta validar correctamente el tema de selección de procesos `14` y `15`.
- Respuesta o acciones del agente: Se ejecutó la memoria bajo demanda y se creó `memory/MEMORY-users_113120678211994207734-2026-07-28.md` con el detalle del pendiente.
- Resultado: Quedó registrado que el punto pendiente para continuar es confirmar los ids reales de `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` para `Médicos no adscritos / Prestadores fuera de la red` y `Solicitud por Medicamentos` antes de modificar `pru_solicitudes_auto_anula_bandeja`. Frase sugerida para retomar: `Lisa, retomemos la Tarea #29489 desde la memoria del 2026-07-28: quedamos validando la seleccion de plantillas 14 y 15 en pru_solicitudes_auto_anula_bandeja. No modifique aun el SP porque falta confirmar los ids reales de SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION para Medicos no adscritos y Solicitud por Medicamentos.`
- Estado: Pendiente
