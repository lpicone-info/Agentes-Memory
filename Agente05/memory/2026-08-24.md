# Memoria 2026-08-24

## Preferencias operativas de Wilmer
- Wilmer indico explicitamente dos reglas de trabajo: no realizar modificaciones directas en el entorno ni hacer commits; el agente solo debe proponer y mostrar cambios por pantalla o archivo, y la ejecucion de modificaciones/commits queda exclusivamente a cargo de whiguera.
- No asumir que un paso fue comprendido o cerrado sin visto bueno explicito de whiguera.

## Tarea 29489 / Proceso 8 - Adjunta ordenes autorizadas
- Se recupero el contexto de cierre del 2026-08-22 para continuar con la Tarea 29489 en `A:\whiguera\Tarea #23986\Tarea #29489`.
- Archivo generado previamente: `pri_solicitudes_auto_notif.sql` en la ruta de la tarea. Validacion tecnica: bloque Proceso 8 ubicado despues de `afi_mail_to` y antes de `afu_mail_envio_send`; acotado a `IF @prm_id = 8 AND @var_dest_tipo = 'A'`; inserta en `MAIL_AUTORIZACIONES` autorizaciones activas `A.esta = 'A'`; evita duplicados por `mail_id/sucur/auto`; marca `MAIL_ENVIO.genera_impresion_auto = 'S'` solo si hay relacion real en `MAIL_AUTORIZACIONES`.
- Cautela funcional: `pri_solicitudes_auto_notif` no valida por si solo que todas las autorizaciones esten en `A`; esa regla debe venir protegida desde `pru_fin_gestion_solicitudes_auto`. La prueba debe hacerse desde Terminar Gestion, no ejecutando manualmente `pri_solicitudes_auto_notif`.
- Paso 5 de validacion antes de probar definido: validar solicitud candidata, autorizaciones activas, conteo de `total_autorizaciones_activas`, `autorizadas`, `no_autorizadas`, y ausencia de notificacion previa que contamine la lectura.

## InfoMailer - multiples adjuntos de autorizaciones
- Wilmer confirmo que se decidio la opcion 1: modificar InfoMailer para que `wf_genera_adjunto` recorra todas las filas devueltas por `prs_datos_email_auto` y agregue un PDF por cada autorizacion al arreglo `lstr_attach`.
- Videos de referencia disponibles en la ruta de tarea: `Leandro_guardar_adjunto_autorizacion_InfoMailer.mp4` y `Leandro_guardar_adjunto_autorizacion_InfoMailer_parte2.mp4`. No se logro transcripcion local completa; se analizaron capturas y codigo fuente.
- Diagnostico: `wp_main_infomailer.srw` llama `wf_genera_adjunto` una sola vez cuando `genera_impresion_auto = 'S'`; dentro de `wf_genera_adjunto` se ejecuta `prs_datos_email_auto` pero se hace un solo `FETCH`. `prs_datos_email_auto` puede devolver varias autorizaciones por `mail_id` desde `MAIL_AUTORIZACIONES`.
- InfoMailer ya soporta multiples adjuntos: los flujos de `MAIL_ATTACH` y `MAIL_ATTACH_BINARY` recorren filas y agregan cada archivo a `lstr_attach[]`; `nvuo_mailer` recorre todo `istr_attach[]`. Por eso el nuevo cambio debe replicar ese patron.
- Alcance acordado para Proceso 1: modificar principalmente `exe_InfoMailer/src/wp_main_infomailer.srw`; no cambiar `nvuo_mailer`, `n_smtp`, `str_attachment` ni `prs_datos_email_auto` para el caso de ordenes autorizadas salvo que luego el diseno de carta de negacion lo requiera.
- Criterios definidos: si falla la generacion de cualquier PDF requerido, no enviar el mail; mantener el nombre actual del PDF (`apellido + nombre + auto + .pdf`); no agregar auditoria funcional por cada PDF en InfoMailer; la auditoria funcional queda en el proceso que prepara el mail (`pri_solicitudes_auto_notif`).
- Ruta del PDF: debe guardarse igual al proceso existente de impresion de autorizaciones. Flujo requerido por cada autorizacion: generar PDF temporal local, subirlo con `nvo_sube_adjunto.of_sube_adjunto_gral`, recibir `ls_arch_path_full`, guardar ese valor en `AUTORIZACIONES.archivo_path_full` usando `pru_auto_arch_path_full`, y eliminar el temporal local luego del envio/uso. Si la autorizacion ya tiene `archivo_path_full`, se puede usar/descargar ese PDF existente como adjunto.
- Riesgo sobre procesos existentes: el cambio puede afectar procesos que ya usan `genera_impresion_auto = 'S'` (`aru_auto_envio`, `aru_soli_envio`) si se modifica mal; debe preservar el caso actual de una sola autorizacion y limpiar solo temporales generados por InfoMailer, no adjuntos existentes ni imagenes del template.

## Proceso 2 - Carta de negacion
- Wilmer planteo que el Proceso 2 debe ser similar al Proceso 1 pero con carta de negacion.
- Analisis tecnico: carta de negacion usa tabla `CARTA_NEGACION` con claves `sucur`, `auto`, `carta_negacion`; estado valido para usarla: `final_fecha IS NOT NULL` y `baja_fecha IS NULL`; impresion marcada con `impre_fecha`; DataWindow `dwc_carta_negacion`; SP de datos `prs_datos_generales_afilia_carta_nega_imp`.
- `CARTA_NEGACION` no tiene columna `archivo_path_full`; en el flujo actual la ruta del PDF de carta de negacion termina guardandose en `AUTORIZACIONES.archivo_path_full`, igual que las ordenes.
- Diseno propuesto para carta: usar `MAIL_AUTORIZACIONES.tipo` para distinguir tipo de adjunto: `NULL` o `'A'` = orden autorizada; `'C'` = carta de negacion. Esto evita crear tabla nueva.
- Para soportar carta de negacion, probablemente `prs_datos_email_auto` deberia devolver tambien `tipo_adjunto = ISNULL(MAIL_AUTORIZACIONES.tipo, 'A')` para que InfoMailer sepa si debe generar orden o carta.
- `pri_solicitudes_auto_notif` para procesos que pidan adjuntar carta de negacion debe insertar en `MAIL_AUTORIZACIONES` autorizaciones negadas con carta valida (`AUTORIZACIONES.esta = 'N'`, `CARTA_NEGACION.final_fecha IS NOT NULL`, `CARTA_NEGACION.baja_fecha IS NULL`) y grabar `MAIL_AUTORIZACIONES.tipo = 'C'`; luego marcar `MAIL_ENVIO.genera_impresion_auto = 'S'`.
- Faltan dos definiciones funcionales para cerrar Proceso 2: numero exacto de proceso de la EF correspondiente a notificacion afiliado finalizada con carta de negacion; y si debe adjuntar una sola carta o todas las cartas de negacion de la solicitud que esten finalizadas.

## Estado de trabajo
- No se realizaron modificaciones directas ni commits durante el analisis, respetando la preferencia de Wilmer.
- Siguiente paso esperado: preparar el paso a paso detallado de modificaciones para Proceso 1 y luego completar el diseno de Proceso 2 cuando Wilmer confirme las definiciones funcionales faltantes.

## Cierre ampliado 2026-08-24 - Tarea 29489 / InfoMailer procesos 8, 9 y 13
- Wilmer confirmo que son tres procesos funcionales a considerar en la EF de `pri_solicitudes_auto_notif` e InfoMailer:
  - Proceso 8: `Adjunta ordenes autorizadas`.
  - Proceso 9: `Adjunta carta de negacion`.
  - Proceso 13: `Adjunta CARTA DE NEGACION y Adjunta ORDENES MEDICAS`.
- Wilmer aclaro que en el Proceso 13, "ordenes medicas" significa la misma salida que "Adjunta ordenes autorizadas"; no es un tercer tipo de PDF distinto.
- Mapeo funcional cerrado:
  - Proceso 8: solicitud finalizada con todas las autorizaciones activas en estado `A` / autorizada; debe enviar un solo mail al afiliado con uno o varios PDF de ordenes autorizadas.
  - Proceso 9: solicitud finalizada con autorizaciones negadas; debe enviar un solo mail al afiliado con una o varias cartas de negacion, siempre que la carta este finalizada.
  - Proceso 13: solicitud finalizada con mezcla de autorizaciones autorizadas y negadas; debe enviar un solo mail al afiliado con los PDF de ordenes autorizadas y los PDF de cartas de negacion en el mismo correo.
- La parte tachada en la imagen compartida por Wilmer, relacionada con "al menos un CUPS no requiere auditoria medica", queda fuera de alcance y no debe implementarse segun el funcional.

## Diseno tecnico acordado para InfoMailer
- Cambio principal a preparar en `exe_InfoMailer/src/wp_main_infomailer.srw`.
- El problema actual esta en `wf_genera_adjunto`: cuando `MAIL_ENVIO.genera_impresion_auto = 'S'`, ejecuta `prs_datos_email_auto`, pero hace un solo `FETCH`; por eso solo genera y adjunta un PDF automatico.
- InfoMailer ya soporta multiples adjuntos cuando los PDF o archivos ya existen:
  - Recorre `MAIL_ATTACH` y agrega cada archivo a `lstr_attach[]`.
  - Recorre `MAIL_ATTACH_BINARY` y agrega cada archivo recuperado a `lstr_attach[]`.
  - Luego `nvuo_mailer` recorre todo el arreglo `istr_attach[]` y adjunta todos los archivos al envio SMTP.
- La modificacion debe replicar ese patron existente: por cada fila devuelta para el `mail_id`, generar/obtener el PDF correspondiente, agregar una nueva posicion en `lstr_attach[]`, setear `filename` e `inline = FALSE`, y enviar un unico mail con todos los adjuntos.
- No modificar:
  - `nvuo_mailer`.
  - `n_smtp`.
  - estructura `str_attachment`.
  - mecanismo general de envio SMTP.
- Mantener compatible el caso actual de una unica autorizacion, para no romper procesos existentes que ya usan `genera_impresion_auto = 'S'`, como `aru_auto_envio` y `aru_soli_envio`.
- Si falla la generacion, recuperacion o subida de cualquiera de los PDF requeridos, el mail no debe enviarse parcial. Debe comportarse de forma equivalente al manejo actual de adjuntos invalidos: registrar error tecnico/log y continuar con el siguiente mail sin ejecutar el envio de ese mail incompleto.
- No agregar auditoria funcional por cada PDF generado dentro de InfoMailer. El patron existente de multiples adjuntos no audita funcionalmente cada adjunto; InfoMailer solo deja logs tecnicos de mail. La auditoria funcional debe quedar en el proceso que prepara el mail, especialmente `pri_solicitudes_auto_notif`.

## Tipo de adjunto en MAIL_AUTORIZACIONES
- Usar `MAIL_AUTORIZACIONES.tipo` para distinguir que PDF debe generar InfoMailer:
  - `tipo IS NULL` o `tipo = 'A'`: orden autorizada.
  - `tipo = 'C'`: carta de negacion.
- Proceso 8 debe insertar en `MAIL_AUTORIZACIONES` solo autorizaciones activas/autorizadas con `tipo = 'A'`.
- Proceso 9 debe insertar en `MAIL_AUTORIZACIONES` autorizaciones negadas con carta de negacion valida/finalizada con `tipo = 'C'`.
- Proceso 13 debe insertar ambas clases de filas para el mismo `mail_id`:
  - autorizaciones `A` con `tipo = 'A'`;
  - autorizaciones `N` con carta de negacion finalizada con `tipo = 'C'`.
- `MAIL_ENVIO.genera_impresion_auto` debe quedar en `S` cuando existan filas reales en `MAIL_AUTORIZACIONES` que deban imprimirse/adjuntarse.

## SPs y objetos relacionados
- `prs_datos_email_auto` actualmente devuelve los datos para generar una autorizacion desde `MAIL_AUTORIZACIONES`. Para soportar carta de negacion, debe analizarse/agregarse que devuelva tambien `tipo_adjunto = ISNULL(MAIL_AUTORIZACIONES.tipo, 'A')`, para que InfoMailer sepa si genera orden autorizada o carta de negacion.
- `pri_solicitudes_auto_notif` es el SP donde se prepara el mail por proceso. Debe quedar preparado para los procesos 8, 9 y 13 segun el mapeo funcional.
- Para carta de negacion:
  - tabla: `CARTA_NEGACION`.
  - claves: `sucur`, `auto`, `carta_negacion`.
  - carta valida: `CARTA_NEGACION.final_fecha IS NOT NULL` y `CARTA_NEGACION.baja_fecha IS NULL`.
  - DataWindow de impresion identificada: `dwc_carta_negacion`.
  - SP de datos identificado: `prs_datos_generales_afilia_carta_nega_imp`.
- `CARTA_NEGACION` no tiene `archivo_path_full`. La ruta del PDF queda guardada en `AUTORIZACIONES.archivo_path_full`, igual que el proceso existente de impresion de autorizaciones.

## Guardado de ruta del PDF
- Wilmer remarco que guardar la ruta del PDF debe hacerse igual que el proceso existente cuando se genera el PDF.
- Flujo requerido por cada PDF nuevo:
  - generar PDF temporal local;
  - subirlo con `nvo_sube_adjunto.of_sube_adjunto_gral`;
  - recibir `ls_arch_path_full`;
  - guardar ese valor en `AUTORIZACIONES.archivo_path_full` usando `pru_auto_arch_path_full`;
  - agregar el archivo al arreglo de adjuntos `lstr_attach[]`;
  - limpiar/borrar solo los temporales generados por InfoMailer, sin borrar adjuntos existentes ni imagenes del template.
- Si la autorizacion ya tiene `AUTORIZACIONES.archivo_path_full`, el diseno debe evaluar usar/descargar ese PDF existente como adjunto antes de regenerarlo, siguiendo el criterio del flujo actual.
- Mantener nombre actual del PDF: `apellido + nombre + auto + .pdf` o equivalente actual ya usado por InfoMailer.

## Riesgos a controlar
- Riesgo principal: romper procesos existentes que ya usan `genera_impresion_auto = 'S'`.
- Controles de diseno:
  - preservar el caso de una sola autorizacion;
  - no tocar `nvuo_mailer`, `n_smtp` ni `str_attachment`;
  - usar el mismo arreglo `lstr_attach[]` que ya soporta multiples adjuntos;
  - limpiar solo temporales nuevos generados por InfoMailer;
  - si falla un PDF, no enviar mail parcial;
  - dejar logs tecnicos del error como hace hoy InfoMailer.

## Pruebas que Wilmer pidio contemplar para el informe
- Regresion de mail sin adjuntos: debe enviarse igual que antes.
- Regresion de mail con adjuntos existentes en `MAIL_ATTACH`: debe enviar multiples adjuntos como antes.
- Regresion de mail con adjuntos existentes en `MAIL_ATTACH_BINARY`: debe recuperar y enviar multiples adjuntos como antes.
- Regresion de autorizacion unica con `genera_impresion_auto = 'S'`: debe generar un PDF, guardar ruta y enviar un adjunto como antes.
- Proceso 8: solicitud finalizada con dos o mas autorizaciones `A`; debe generar/adjuntar todos los PDF de orden autorizada y enviar un unico mail.
- Proceso 9: solicitud finalizada con autorizaciones `N` con carta finalizada; debe generar/adjuntar las cartas de negacion y enviar un unico mail.
- Proceso 13: solicitud finalizada con autorizaciones mixtas `A` y `N`; debe generar/adjuntar ordenes autorizadas y cartas de negacion en el mismo mail.
- Prueba de fallo controlado: forzar error en generacion/subida/recuperacion de un PDF; resultado esperado: no enviar mail parcial, registrar error tecnico y no afectar el procesamiento del resto de mails.
- Validar en base que queden correctamente:
  - filas en `MAIL_AUTORIZACIONES` con `tipo` correcto;
  - `MAIL_ENVIO.genera_impresion_auto = 'S'`;
  - `AUTORIZACIONES.archivo_path_full` actualizado cuando se genera/sube PDF;
  - logs tecnicos de InfoMailer.

## Estado para continuar manana
- No se modificaron archivos de codigo ni se hicieron commits durante el analisis.
- Wilmer pidio continuar manana con la modificacion/adicion de InfoMailer para los procesos 8, 9 y 13.
- Al retomar, primero recuperar esta memoria y luego armar el paso a paso detallado de cambios antes de tocar codigo, porque Wilmer pidio no realizar modificaciones directas sin su visto bueno.
- Frase sugerida para retomar: "Lisa, recupera la memoria del 2026-08-24 de la Tarea 29489 / InfoMailer procesos 8, 9 y 13, y continuemos armando el paso a paso detallado de modificacion antes de tocar codigo."
