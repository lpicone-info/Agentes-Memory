# Memoria curada diaria

- Fecha: 2026-07-24
- Usuario: Wilmer Higuera
- id_googlechat: users/113120678211994207734
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/qzugpyaaaae

## Resumen de la actividad

### Tema 1: Notificacion de solicitud registrada en `pru_registra_solicitudes_auto`

- Solicitud o contexto: Wilmer trabajo sobre la Tarea `#29489` y el punto funcional `4.1.4`, relacionado con llamar `pri_solicitudes_auto_notif` cuando una solicitud queda en estado `R - Registrada`, contemplando alta y edicion de solicitud.
- Respuesta o acciones del agente: Se analizo el flujo desde GIS/WebPos y Presmed hacia `pru_registra_solicitudes_auto`. Se explico que GIS llega con `@prm_origen = 'G'` y que el SP deriva `@var_tipo_usuario = 'P'` para ese origen; Presmed usa `@prm_origen = 'P'` y queda como `E`; otros origenes pueden quedar como `A`.
- Resultado: Se propuso el bloque de notificacion para alta: si la solicitud queda en `R`, para `@var_tipo_usuario = 'P'` llamar plantilla `1` al prestador y plantilla `22` al afiliado cuando `@prm_origen = 'G'` o `7` en otro caso; para `A` o `E`, llamar plantilla `7` al afiliado. Se aclaro que para esta tarea no aplica SMS.
- Estado: Resuelto

### Tema 2: Notificacion en edicion y control de duplicados

- Solicitud o contexto: Wilmer pidio completar el codigo para `EDICION SOLICITUD` y luego observo que era necesario saber cuanto valia `soli_esta` al ingresar, para no notificar si la solicitud ya estaba en `R`.
- Respuesta o acciones del agente: Se propuso declarar `@var_soli_esta_inicial`, cargarlo antes de `pru_solicitudes_auto_adjuntos` y condicionar la notificacion a que el estado inicial no fuera `R` y que luego la solicitud exista en estado `R`.
- Resultado: Quedo definido que en edicion solo se debe notificar cuando hay transicion hacia `R`, por ejemplo de `C` a `R`, evitando reenviar notificaciones si el usuario solo guarda una solicitud ya registrada.
- Estado: Resuelto

### Tema 3: Correccion de error SQL al pasar `CASE` directo en `EXEC`

- Solicitud o contexto: Wilmer informo que, al ejecutar el SP modificado en SQL Server, aparecio un error de sintaxis relacionado con el uso de `CASE` directo en el parametro `@prm_id` del `EXEC dbo.pri_solicitudes_auto_notif`.
- Respuesta o acciones del agente: Se explico que en SQL Server era mejor cargar primero el valor en una variable y luego pasar esa variable al SP. Se propuso declarar `@var_prm_id_notif INT`, asignarla con `CASE WHEN @prm_origen = 'G' THEN 22 ELSE 7 END` y usarla en el `EXEC`.
- Resultado: Wilmer ejecuto el SP corregido correctamente y confirmo que, por el momento, quedo la notificacion para solicitud `REGISTRADA`.
- Estado: Resuelto

### Tema 4: Ubicacion del cambio a estado `D - Devuelta`

- Solicitud o contexto: Wilmer pidio ubicar el SP desde donde se cambia el estado de solicitud a `D - Devuelta`, para poder llamar la notificacion correspondiente, y queria saber si ocurria desde Presmed, GIS o ambos.
- Respuesta o acciones del agente: Se reviso el flujo de Presmed y GIS/WebPos. Se identifico que Presmed cambia a `D` mediante `wp_bandeja_solicitudes` -> evento `ue_devolver` -> `pru_solicitudes_auto_devolucion`, donde se ejecuta el `UPDATE SOLICITUDES_AUTO SET soli_esta = 'D'`. Se explico que GIS/WebPos no cambia a `D`; su accion de devolver aplica cuando la solicitud ya esta en `D` y la devuelve al circuito, cambiando a `R` mediante `aru_solicitudes_auto_devolucion`.
- Resultado: Quedo como punto de continuidad agregar el llamado a `pri_solicitudes_auto_notif` en `pru_solicitudes_auto_devolucion`, usando las plantillas `2` para prestador y `16` para afiliado, validando la transicion a `D`.
- Estado: Pendiente

### Tema 5: Memoria de continuidad para el lunes

- Solicitud o contexto: Wilmer pidio guardar lo trabajado para retomar el lunes desde el punto de la notificacion en `pru_solicitudes_auto_devolucion`.
- Respuesta o acciones del agente: Se registro una memoria operativa en `memory/2026-07-24.md` con el punto de continuidad.
- Resultado: Se indico que el lunes puede pedir: `Lisa, retomemos la Tarea #29489 desde la memoria: estado D - Devuelta y el llamado de notificacion en pru_solicitudes_auto_devolucion.`
- Estado: Resuelto
