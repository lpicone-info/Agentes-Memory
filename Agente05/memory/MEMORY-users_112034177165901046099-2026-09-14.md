# Memoria de conversación — Walter Ferreyra

## Datos

- **Fecha:** 14/09/2026
- **Usuario:** Walter Ferreyra
- **ID Google Chat:** `users/112034177165901046099`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 8
- **Inicio:** 15:06:01
- **Fin:** 16:53:31

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/3hf6byaaaae`

## Archivos revisados

- `19123874-6958-4658-8890-e6f6859bec20.jsonl`

---

## Transcripción completa

### [14/09/2026 15:06:01] Walter Ferreyra

lisa

---

### [14/09/2026 15:08:18] Agente

Hola Walter, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [14/09/2026 15:12:08] Walter Ferreyra

vamos a trabajar en el redmine 30184
base de datos pruebascoreinfomedical
usuario infomedicaltest
repositorio trunk
stored procedure prs_auto_api_no_subidas
demora y quiero mejorar la performance
encontre una forma, crear un indice,
quiero saber los riesgos de crear el siguiente indice

IF NOT EXISTS (SELECT 1 FROM SYSINDEXES WHERE name = 'ik_autorizaciones_4' AND OBJECT_NAME(id) = 'AUTORIZACIONES')
CREATE NONCLUSTERED INDEX ik_autorizaciones_4 ON dbo.AUTORIZACIONES
(
fecha ASC,
baja_fecha ASC
)
INCLUDE (sucur, auto, esta, opcion_impresion, tipo, origen, inte, contra, archivo_path_full)
ON [PRIMARY]

---

### [14/09/2026 15:13:18] Agente

Walter, necesito que confirmes cuál repositorio corresponde al Redmine 30184:

- `wferreyra-Coomeva-trunk`  
  Remoto: `Coomeva/trunk`
- `wferreyra-Coomeva-trunk-backend`  
  Remoto: `Coomeva/trunk/backend`

También existen copias de `trunk` asociadas a otros usuarios, pero para tu usuario las opciones son esas dos.

---

### [14/09/2026 15:14:43] Walter Ferreyra

wferreyra-Coomeva-trunk-backend

---

### [14/09/2026 15:30:46] Agente

Walter, **conclusión: el índice es técnicamente válido y mejora esta consulta, pero ya existe en `PruebasCoreINFOMEDICAL`**. No corresponde crearlo nuevamente.

**Evidencia**

- Redmine `30184`: la consulta debe filtrar por:
  - rango de `A.fecha`
  - `A.baja_fecha IS NULL`
  - estado `A` o estado `N` con carta finalizada
  - `archivo_path_full` nulo o vacío
  - existencia de un ítem con `nivel = 2` y sin baja
- La definición real de `dbo.prs_auto_api_no_subidas` coincide con esos filtros y utiliza `OPTION (RECOMPILE)`.
- La tabla tiene aproximadamente **27,6 millones de registros**.
- Ya existe:
  - `ik_autorizaciones_2 (fecha, esta)`: aproximadamente **1.012 MB**
  - `ik_autorizaciones_4 (fecha, baja_fecha)` con los `INCLUDE` propuestos: aproximadamente **1.346 MB**
- `ik_auto_prestac_items_1 (nivel, baja_fecha, sucur, auto)` ya soporta correctamente el `EXISTS` sobre `AUTO_PRESTAC_ITEMS`.

**Medición realizada**

Para un rango amplio con datos:

- Usando `ik_autorizaciones_4`: **2.626.717 filas**, aproximadamente **14 segundos**.
- Forzando `ik_autorizaciones_2`: superó los **120 segundos** y fue cancelada.

El beneficio principal de `ik_autorizaciones_4` proviene de que es **cubriente**: evita búsquedas adicionales sobre la tabla base para recuperar `sucur`, `auto`, `esta`, `opcion_impresion`, `tipo`, `origen`, `inte`, `contra` y `archivo_path_full`.

**Riesgos de crear el índice**

1. **Consumo de almacenamiento**  
   En esta base ocupa aproximadamente **1,35 GB**, unos **334 MB más** que `ik_autorizaciones_2`. En producción el tamaño puede ser diferente.

2. **Costo de creación**  
   Construirlo sobre 27,6 millones de filas puede consumir CPU, lecturas, espacio temporal y log de transacciones. Además, la creación puede bloquear inserciones, actualizaciones o procesos que utilicen `AUTORIZACIONES`, especialmente al no especificarse `ONLINE = ON`.

3. **Mayor costo de escritura**  
   Cada alta o modificación de `AUTORIZACIONES` deberá mantener este índice. El impacto es relevante porque incluye `archivo_path_full`, campo que el proceso actualiza al registrar el archivo generado.

4. **Solapamiento parcial**  
   El índice comparte `fecha` con `ik_autorizaciones_2`. No es completamente redundante porque el nuevo índice cubre la consulta, pero ambos deben mantenerse y ocupar espacio.

5. **La segunda clave no es el principal beneficio**  
   Como `fecha` se utiliza con un rango, `baja_fecha` normalmente no podrá aprovecharse como una búsqueda altamente selectiva. El mayor aporte está en evitar `key lookups` mediante las columnas incluidas.

6. **Posibles cambios de plan**  
   Al agregarlo, otras consultas podrían elegirlo y cambiar su consumo de memoria, lecturas o tiempo. En este procedimiento el riesgo de parameter sniffing es menor porque utiliza `OPTION (RECOMPILE)`, pero aun así debe validarse el plan real.

**Recomendación**

- Mantener `ik_autorizaciones_4`, porque la medición demuestra una mejora importante frente a `ik_autorizaciones_2`.
- No ejecutar el `CREATE INDEX` en `PruebasCoreINFOMEDICAL`: ya está creado.
- Registrar el índice en el script de instalación del repositorio, porque actualmente existe en la base pero no figura en [AUTORIZACIONES.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/wferreyra-Coomeva-trunk-backend/tables/AUTORIZACIONES.sql:97). De lo contrario, otra instalación podría no tenerlo.
- Para el script, conviene reemplazar la validación basada en `SYSINDEXES` por una validación moderna y específicamente asociada a `dbo.AUTORIZACIONES`:

```sql
IF NOT EXISTS
(
    SELECT 1
    FROM sys.indexes
    WHERE object_id = OBJECT_ID('dbo.AUTORIZACIONES')
      AND name = 'ik_autorizaciones_4'
)
CREATE NONCLUSTERED INDEX ik_autorizaciones_4 ON dbo.AUTORIZACIONES
(
    fecha ASC,
    baja_fecha ASC
)
INCLUDE
(
    sucur,
    auto,
    esta,
    opcion_impresion,
    tipo,
    origen,
    inte,
    contra,
    archivo_path_full
)
ON [PRIMARY]
```

No se modificó la base ni el repositorio.

---

### [14/09/2026 16:52:53] Walter Ferreyra

fijate en el trunk wferreyra-Coomeva-trunk quien usa este sp
pru_anulacion_masiva

---

### [14/09/2026 16:53:31] Agente

Walter, no puedo iniciar la búsqueda porque la referencia `wferreyra-Coomeva-trunk` no está disponible en el workspace:

`/home/desarrollo-ia-05/.openclaw/workspace/svn/wferreyra-Coomeva-trunk`

La working copy registrada para tu usuario que figura disponible es `wferreyra-Coomeva-trunk-backend`. En `REPOSITORIOS.md` no aparece una entrada `wferreyra-Coomeva-trunk`; la entrada `whiguera-Coomeva-trunk` corresponde a otro usuario.

Necesito que confirmes la ruta correcta o que se dé de alta esa working copy antes de buscar quién utiliza `pru_anulacion_masiva`.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
