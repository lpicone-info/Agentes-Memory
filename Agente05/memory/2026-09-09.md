
## 2026-09-09 16:40 ART - Pre-compaction flush

- Usuario: Walter. Prefiere trabajo directo y trazable sobre repositorios SVN/PowerBuilder/MS SQL, con respuestas tecnicas concisas por Google Chat.
- Redmine 30138: se trabajo el paso 5 para desarrollar el SP `pru_registra_solicitudes_auto_IA` sobre rama SVN `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version84-Modelo2Gis/backend` y fuentes PB `https://srv-infosvn.infomedical.com/svn/Coomeva/branches/Version84-Modelo2Gis/exe_presmed`; repositorio registrado `wferreyra-Version84-Modelo2Gis`.
- Redmine 30138: se dejo el SP en `/mnt/Agentes/wferreyra/coomeva/30138/pru_registra_solicitudes_auto_IA.sql` y el resumen en `/mnt/Agentes/wferreyra/coomeva/30138/pru_registra_solicitudes_auto_IA.md`. Validaciones reportadas: XML/prestaciones/conceptos IA, solicitud vigente con `procesaIA = 'S'`, `fecha_procesaIA IS NULL`, estado `R`, tipo `D/H/Q`, CUPS/conceptos vigentes, cantidad > 0, prestador/lugar, duplicados; proceso transaccional con inserciones en tablas de solicitudes/prestaciones/conceptos y auditoria; retorno `soli_auto`, `error`, `deno_error`.
- Redmine 30344: Walter pidio identificar ventana/objeto y revisar `DECLARE` del SP de retiro de reembolso en trunk presmed / BD `pruebascoreinfomedical`. Respuesta dada: ventana a reportar `Exportación Contable Reembolsos exportados`, objeto window `wp_expor_contable_xml`, SP visible en captura `pru_retirar_reembolsos`, mensaje `XML parsing: line 1, character 57, unable to switch the encoding`, y el `DECLARE` encontrado fue `DECLARE pru_retirar_reembolsos PROCEDURE FOR dbo.pru_retirar_reembolsos`, por lo tanto si tiene `dbo.`.
- Nota Redmine 30344: el `DECLARE` con `dbo.` fue encontrado en `wferreyra-Version84-Modelo2Gis/exe_presmed/Source Infomedical/wp_expor_contable_xml.srw`; en la working copy registrada del trunk `csiafas-Coomeva-trunk-exe_presmed-Source20Infomedical` ese bloque de retiro no aparecia.


## 2026-09-09 18:33 ART - Tarea #29489, caso 43

- Usuario: Wilmer. Se retomó la Tarea `#29489`, caso 43, autorización de referencia `22596528`, repositorio SVN `whiguera-Version84-Modelo2Gis`. El usuario indicó posteriormente que ya podía realizar modificaciones y actualizar tras haber hecho commit; antes de eso se respetó el modo consultivo sin modificar ni commitear.
- Objetivo funcional: notificar al prestador cuando una solicitud de autorización parcial queda en estado `V`, sin importar si el origen/tipo de usuario es prestador o ejecutivo.
- Cambios/diagnóstico del proceso 27:
  - En `pri_solicitudes_auto_notif` se adicionó el proceso `27` a la clasificación de destinatario Prestador y al bloque que obtiene el email.
  - Se adicionó el disparador del proceso `27` en `prs_valida_of_auto_remotas`, pero se determinó que ese procedimiento no maneja `@var_soli_esta = 'V'` en el punto evaluado, por lo que no era el lugar efectivo para este flujo.
  - Se identificó que `pru_fin_gestion_solicitudes_auto` calcula mediante `prs_solicitudes_auto_esta_actual` y persiste el estado, pero una prueba PRESMED llegó a `V` por edición/generación de autorizaciones.
  - Se identificó que `pru_registra_solicitudes_auto` también puede actualizar a `V`, pero en otra prueba el cambio real ocurrió dentro de `pru_genera_auto_precarga`; al retornar, el SP externo ya no cumplía las condiciones esperadas.
  - Punto recomendado para el llamado del proceso `27`: en `pru_genera_auto_precarga`, inmediatamente después del `UPDATE SOLICITUDES_AUTO` que persiste `@var_soli_esta`, antes del bloque de notificación del proceso `4`. Se recomendó mantener temporalmente el llamado en `pru_fin_gestion_solicitudes_auto` si ese flujo también aplica, y retirar el de `pru_registra_solicitudes_auto` si llega tarde.
  - En una prueba fallida adicional se encontró la causa final de generación: faltaba incluir `27` en el `IF @prm_id IN (1,2,3,4,5,6,21)` que inserta destinatarios en `#DESTINATARIOS_NOTIF`; aunque se había agregado en otros dos lugares, el cursor no tenía destinatarios y no se generaban `MAIL_ENVIO`, `MAIL_TO` ni auditoría `proceso_id: 27`.
  - En la prueba `25920216`, el proceso funcionó correctamente: se registró auditoría con `proceso_id: 27`, se generó `MAIL_ENVIO` y `MAIL_TO` para el email del prestador. El estado `NOE` y `fecha_envio = NULL` correspondían al procesamiento posterior del envío físico, no a un fallo del proceso 27.
- URLs en plantilla: el funcional aclaró que `@url_coomeva` y `@url_consulta_solicitudes` deben permanecer separados. El ajuste conceptual pendiente en `pri_solicitudes_auto_notif` es declarar/cargar una variable independiente para `PARAMETROS.url_consulta_solicitudes` y realizar reemplazos separados en asunto/cuerpo:
  - `@url_coomeva` debe mapear a `PARAMETROS.url_webcoomeva`.
  - `@url_consulta_solicitudes` debe mapear a `PARAMETROS.url_consulta_solicitudes`.
  - La plantilla del proceso 27 debe usar literalmente `@url_consulta_solicitudes` para el enlace de consulta.
- Estado actual: notificación parcial del proceso 27 validada en generación; pendiente confirmar/aplicar correctamente la separación de tokens URL y revisar impacto sobre plantillas existentes que usan `@url_coomeva`. No guardar credenciales ni API keys en memoria.

## 2026-09-09 - Continuación Tarea #29489 con Wilmer

- Usuario: Wilmer Higuera (`users/113120678211994207734`).
- Tarea: Redmine `#29489`.
- Caso funcional de referencia: caso `43`, autorización de referencia `22596528`.
- Repositorio SVN: `whiguera-Version84-Modelo2Gis`, rama `^/branches/Version84-Modelo2Gis`.
- Regla operativa del usuario: no modificar archivos, entorno ni realizar commits sin autorización explícita; no dar por cerrado un paso sin su visto bueno. Durante la primera parte se trabajó en modo consultivo. Luego Wilmer informó que ya había realizado los commits y autorizó la actualización/continuación del análisis.

### Objetivo funcional

- Cuando una solicitud de autorización parcial queda en estado `V`, se debe notificar al prestador.
- La notificación debe salir independientemente de que el origen de la solicitud sea prestador, ejecutivo u otro tipo de usuario.
- La notificación corresponde al proceso `27`.
- Debe evitarse la duplicación mediante una auditoría que identifique `proceso_id: 27`.

### Diagnóstico de las primeras pruebas

- En la autorización de referencia no había vínculo inicial con una solicitud en `SOLICITUDES_AUTO_AUTORIZACIONES`, por lo que no podía existir una solicitud (`soli_auto`) sobre la cual auditar o notificar.
- La autorización podía quedar en estado `P` (“Pendiente en Auditoría”), mientras que `prs_solicitudes_auto_esta_actual` calcula `V` principalmente cuando encuentra una autorización vinculada en estado `E`. Esto debe considerarse al diseñar la prueba y al validar la regla funcional.
- Se comprobó que la condición `@var_tipo_usuario_soli = 'P'` impedía la notificación cuando la solicitud provenía de ejecutivo (`tipo_usuario = 'E'`). El funcional confirmó que el origen no importa porque la parcial se gestiona desde PRESMED hacia el prestador. La condición de tipo de usuario debe eliminarse para el proceso 27.

### Procedimientos y puntos de disparo revisados

- `prs_valida_of_auto_remotas`: se había agregado allí un bloque con condiciones `@var_soli_esta = 'V'`, transición anterior distinta de `V`, tipo de usuario `P` y ausencia de auditoría 27. Se determinó que no era un punto confiable para este flujo porque el procedimiento trabaja con variables internas que manejan estados `F` y `G`, no el estado final persistido `V` en el momento esperado.
- `pru_fin_gestion_solicitudes_auto`: calcula el estado llamando a `prs_solicitudes_auto_esta_actual`, guarda el estado anterior, actualiza `SOLICITUDES_AUTO` y registra el workflow. Es un punto válido únicamente para el flujo real de finalización explícita que efectivamente cambie a `V`.
- `pru_registra_solicitudes_auto`: también puede recalcular y persistir estados `F`/`V`, pero una prueba mostró que llega tarde cuando la llamada interna ya cambió la solicitud. Se recomendó retirar de allí el llamado 27 si el flujo queda cubierto por el procedimiento interno correcto.
- `pru_genera_auto_precarga`: en las pruebas desde PRESMED/generación de autorizaciones fue el procedimiento que realmente calculó y persistió `SOLICITUDES_AUTO.soli_esta = 'V'`. El punto recomendado para el llamado 27 es inmediatamente después del `UPDATE SOLICITUDES_AUTO` que persiste `@var_soli_esta` y antes del bloque de notificación del proceso 4.

Bloque conceptual recomendado en el punto posterior al `UPDATE` de `SOLICITUDES_AUTO`:

```sql
IF @var_soli_esta = 'V'
AND NOT EXISTS (
    SELECT 1
    FROM SOLICITUDES_AUTO_AUDITORIA
    WHERE soli_auto = @prm_soli_auto
      AND activi LIKE '%proceso_id: 27%'
)
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id        = 27,
        @prm_soli_auto = @prm_soli_auto
END
```

- El control de transición anterior (`<> 'V'`) puede utilizarse si la variable del estado anterior está disponible y representa realmente el estado persistido antes del `UPDATE`. La auditoría `NOT EXISTS` debe mantenerse como protección contra duplicados.
- Recomendación de distribución: dejar el disparo principal en `pru_genera_auto_precarga`; mantener el de `pru_fin_gestion_solicitudes_auto` solo si existe otro flujo funcional que también deba notificar al pasar a `V`; retirar el de `pru_registra_solicitudes_auto` si quedó redundante o llega tarde. El bloque agregado en `prs_valida_of_auto_remotas` debe revisarse/removerse si no corresponde a ningún flujo efectivo, para evitar puntos duplicados o condiciones imposibles.

### Falla encontrada en `pri_solicitudes_auto_notif`

- En una prueba con la solicitud `25920215`, el disparador sí estaba instalado y la solicitud quedó en `V`, pero no se generó correo ni auditoría del proceso 27.
- Se había agregado `27` en:
  - la clasificación de procesos que notifican a `Prestador`;
  - el bloque que obtiene el email del prestador.
- Faltaba agregar `27` en el `IF` que inserta el destinatario en `#DESTINATARIOS_NOTIF`. El bloque todavía tenía solamente `(1,2,3,4,5,6,21)`.
- Resultado: el email base se calculaba, pero no se insertaba destinatario; el cursor no recorría filas y no se generaban `MAIL_ENVIO`, `MAIL_TO` ni auditoría `proceso_id: 27`.
- Corrección requerida: incluir `27` en ese tercer listado, conservando la validación de que no exista ya destinatario tipo `P` y que el email no sea vacío.

### Prueba exitosa

- En la solicitud `25920216`, la prueba sí generó correctamente la notificación parcial:
  - solicitud en estado `V`;
  - autorización vinculada;
  - auditoría con `proceso_id: 27`;
  - registro en `MAIL_ENVIO`;
  - registro en `MAIL_TO` con el email del prestador.
- El estado `MAIL_ENVIO = 'NOE'` y `MAIL_TO.fecha_envio = NULL` no representaron un fallo de generación. Significaron que el proceso 27 armó correctamente el correo y quedó pendiente el servicio/batch posterior que realiza el envío físico.
- Wilmer confirmó posteriormente que el envío salió bien. El objetivo de notificación parcial quedó funcionalmente validado.

### Separación de URLs en la plantilla

- El funcional confirmó que `@url_coomeva` y `@url_consulta_solicitudes` deben ser tokens independientes.
- La solución conceptual acordada:
  - `@url_coomeva` debe reemplazarse con `PARAMETROS.url_webcoomeva`;
  - `@url_consulta_solicitudes` debe reemplazarse con `PARAMETROS.url_consulta_solicitudes`;
  - la plantilla del proceso 27 debe usar literalmente `@url_consulta_solicitudes` para el enlace “Consultar Solicitud”.
- En `pri_solicitudes_auto_notif` se debe declarar/cargar una variable independiente para `url_consulta_solicitudes` desde `PARAMETROS` y aplicar reemplazos separados en asunto y cuerpo:

```sql
SET @var_subjet = REPLACE(@var_subjet, '@url_coomeva',
                          ISNULL(@var_url_coomeva, ''))
SET @var_body = REPLACE(@var_body, '@url_coomeva',
                        ISNULL(@var_url_coomeva, ''))

SET @var_subjet = REPLACE(@var_subjet, '@url_consulta_solicitudes',
                          ISNULL(@var_url_consulta_solicitudes, ''))
SET @var_body = REPLACE(@var_body, '@url_consulta_solicitudes',
                        ISNULL(@var_url_consulta_solicitudes, ''))
```

- Wilmer indicó al cierre que este punto también quedó listo. Queda como pendiente de mañana únicamente verificar en código/QA que el mapeo final no haya impactado plantillas existentes que usan `@url_coomeva`, y confirmar que la plantilla del proceso 27 conserva el token separado.

### Estado al cierre

- Notificación parcial del proceso 27: resuelta y validada en generación y envío.
- Inclusión del destinatario del prestador para proceso 27: resuelta como causa técnica.
- Eliminación de la dependencia del tipo de usuario `P`: definida funcionalmente; verificar que no quede una condición equivalente en otro punto.
- Punto principal de disparo: `pru_genera_auto_precarga`, posterior al `UPDATE SOLICITUDES_AUTO` que deja `V`.
- Distribución de llamados en los otros SP: revisar para eliminar duplicidad y conservar únicamente los flujos que realmente puedan producir la transición a `V`.
- Separación `@url_coomeva` / `@url_consulta_solicitudes`: funcionalmente definida y reportada como lista por Wilmer; pendiente validación final de impacto.
- No registrar credenciales, API keys, tokens ni secretos en memoria.

### Frase recomendada para retomar

“Lisa, retomemos la Tarea #29489 del 09/09/2026, caso 43, proceso 27 de notificación parcial. Revisemos la distribución final de los llamados y validemos en `pri_solicitudes_auto_notif` la separación de `@url_coomeva` y `@url_consulta_solicitudes` en QA.”
