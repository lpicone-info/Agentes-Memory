# Memoria curada diaria

- Fecha: 2026-07-30
- Usuario: Wilmer Higuera
- id_googlechat: users/113120678211994207734
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/qzugpyaaaae

## Resumen de la actividad

### Tema 1: Reglas activas de trabajo para Wilmer

- Solicitud o contexto: Wilmer pidió recordar dos condiciones operativas para la sesión: no realizar modificaciones directas en el entorno ni commits, y no asumir pasos como comprendidos o cerrados sin su visto bueno explícito.
- Respuesta o acciones del agente: Se aceptó trabajar en modo consultivo, proponiendo y mostrando cambios por pantalla sin ejecutarlos directamente, salvo pedido explícito posterior.
- Resultado: La regla quedó activa durante el análisis de la Tarea #29489.
- Estado: Informativo

### Tema 2: Retoma de la Tarea #29489 y corrección del análisis sobre vencimiento de devolución de solicitud

- Solicitud o contexto: Wilmer pidió retomar la Tarea #29489 desde la memoria del 2026-07-29, en el punto del SP `prs_control_vencimiento_devolucion_soli`. Al inicio estaba pendiente validar si la notificación con plantillas `3/17` ya estaba correcta y si convenía agregar validación de `moti_x_venc = 'S'`. Wilmer indicó luego que no se debía realizar la modificación de `moti_x_venc = 'S'` y señaló, con respaldo funcional, que el llamado actual no cubría toda la especificación.
- Respuesta o acciones del agente: Se revisó el SP `prs_control_vencimiento_devolucion_soli`, el SP `pri_solicitudes_auto_notif` y referencias relacionadas. Se corrigió la conclusión anterior: el SP de vencimiento no debía limitarse a elegir una sola plantilla con `CASE WHEN @csr_tipo_usuari = 'P' THEN 3 ELSE 17 END`, porque para solicitudes creadas por prestador correspondía notificar tanto al prestador como al afiliado.
- Resultado: Se descartó la propuesta de modificar `moti_x_venc = 'S'`. Se estableció que, sin considerar SMS, la lógica correcta para vencimiento devolución solicitud es: si `@csr_tipo_usuari = 'P'`, ejecutar `pri_solicitudes_auto_notif` con `@prm_id = 3` y luego con `@prm_id = 17`; para afiliado/no prestador, ejecutar solo `@prm_id = 17`.
- Estado: Resuelto

### Tema 3: Criterio de clasificación prestador vs afiliado en `prs_control_vencimiento_devolucion_soli`

- Solicitud o contexto: Wilmer compartió una parte del SQL donde `tipo_usuari` se calcula como `CASE WHEN SA.origen = 'G' THEN 'P' ELSE 'A' END`, para confirmar si podía guiar la lógica.
- Respuesta o acciones del agente: Se explicó que `SA.origen = 'G'` se trata como prestador y todo lo demás que entra al cursor se trata como afiliado/no prestador. Se indicó que no era necesario inventar otro valor para usuario Coomeva dentro de `tipo_usuari`; si Coomeva debe entrar en el proceso, el punto a validar es si su `SA.origen` está incluido en el filtro del cursor.
- Resultado: La lógica práctica quedó definida sobre `@csr_tipo_usuari`: `P` dispara plantillas `3` y `17`; cualquier otro valor dispara plantilla `17`.
- Estado: Resuelto

### Tema 4: Auditoría de anulación automática y auditoría de notificaciones

- Solicitud o contexto: Funcional pidió agregar auditoría a la notificación con datos básicos: número `mail_id`, a quién se notifica, prestador o afiliado, y número de proceso ID. Wilmer pidió analizar cómo se adicionaba la auditoría de `Anulación automática de solicitud por vencimiento` y cómo agregar la auditoría para la notificación.
- Respuesta o acciones del agente: Se verificó que `prs_control_vencimiento_devolucion_soli` ya inserta en `SOLICITUDES_AUTO_AUDITORIA` la actividad `Anulación automática de solicitud por vencimiento`. Se identificó que `pri_solicitudes_auto_notif` genera internamente `@out_mail_id` mediante `afi_mail_envio`, por lo que la auditoría de notificación debía ubicarse dentro de `pri_solicitudes_auto_notif`, después de cada `afu_mail_envio_send`, y no en el SP llamador. Se descartó agregar parámetros opcionales a `pri_solicitudes_auto_notif`, por decisión de Wilmer.
- Resultado: Se propuso centralizar la auditoría para todas las notificaciones dentro de `pri_solicitudes_auto_notif`, calculando una variable `@var_activi_base` con `CASE` según `@prm_id`, y registrando `mail_id`, destinatario (`Prestador`, `Afiliado`, `Ejecutivo`) y `proceso_id`.
- Estado: Resuelto

### Tema 5: Detalle técnico de auditoría dentro de `pri_solicitudes_auto_notif`

- Solicitud o contexto: Wilmer pidió más detalle sobre dónde adicionar la auditoría porque no identificaba dónde se enviaba el primer mail.
- Respuesta o acciones del agente: Se explicó que el primer mail se procesa en el bloque que valida `@var_email`: primero `afs_valida_mail`, luego `afi_mail_envio` genera `@out_mail_id`, luego `afi_mail_to` asocia el destinatario y finalmente `afu_mail_envio_send` envía el mail. Se indicó que la auditoría debe agregarse inmediatamente después de `EXEC dbo.afu_mail_envio_send @prm_mail_id = @out_mail_id`. Se aclaró que debe repetirse el mismo bloque para el segundo mail, dentro del bloque que valida `@var_email_2`.
- Resultado: Wilmer implementó el cambio en SQL Server. Al principio apareció error de sintaxis cerca de `+` porque se estaba concatenando directamente dentro del parámetro `@prm_activi` del `EXEC dbo.pri_solicitudes_auto_auditoria`. Se explicó que debía usarse una variable intermedia, por ejemplo `@var_activi_auditoria VARCHAR(512)`, asignarle el texto completo y luego pasar esa variable al parámetro `@prm_activi`. También se explicó que el error `Cannot find the object 'pri_solicitudes_auto_notif'` ocurrió porque el script hizo `DROP PROCEDURE` y el `CREATE PROCEDURE` falló por sintaxis, dejando el SP sin recrear.
- Estado: Resuelto

### Tema 6: Ejecución correcta de `pri_solicitudes_auto_notif`

- Solicitud o contexto: Wilmer informó que el SP ya ejecutó correctamente después de aplicar la corrección de la variable intermedia para la auditoría.
- Respuesta o acciones del agente: Se confirmó el estado técnico del ajuste.
- Resultado: `pri_solicitudes_auto_notif` quedó ejecutado correctamente con auditoría centralizada para todas las notificaciones. La auditoría registra `mail_id`, destinatario y `proceso_id`. `prs_control_vencimiento_devolucion_soli` quedó listo según lo trabajado en el día.
- Estado: Resuelto

### Tema 7: Pendiente procesos 14 y 15 en `pru_solicitudes_auto_anula_bandeja`

- Solicitud o contexto: Wilmer informó que habló con funcional y le confirmaron como solución la misma propuesta previamente: extender `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`, que ya tiene `notif_afi` y `notif_prestad`, agregando un campo nuevo como `notif_afi_plant_proc_id SMALLINT NULL`. El objetivo es que cada motivo defina explícitamente qué plantilla usar para afiliado: motivo médicos no adscritos o prestador fuera de red debe usar plantilla `14`; motivo solicitud por medicamentos debe usar plantilla `15`; otros motivos deben quedar en `NULL` o con el id que defina funcional.
- Respuesta o acciones del agente: Se revisó el modelo actual. La tabla `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` tiene `id`, `deno`, `tipo`, `origen`, `notif_afi`, `notif_prestad`, fechas y el agregado previo `moti_x_venc`. El SP `pru_solicitudes_auto_anula_bandeja` actualmente lee `notif_afi` y `notif_prestad` desde `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION`, pero decide la plantilla del afiliado con `SET @var_notif_afiliado_id = CASE WHEN @var_soli_tipo = 'D' THEN 15 ELSE 14 END`, lo cual es incorrecto porque la plantilla debe depender del motivo de anulación y no del tipo de solicitud.
- Resultado: Se definió la solución pendiente para mañana: agregar a `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` el campo `notif_afi_plant_proc_id SMALLINT NULL` con script incremental; evaluar FK opcional contra `SOLICITUDES_AUTO_NOTIF_PLANT_PROC(id)`; actualizar `prs_solicitudes_auto_motivos_devolucion` para devolver el campo; actualizar `pru_solicitudes_auto_motivos_devolucion` con parámetro `@prm_notif_afi_plant_proc_id SMALLINT = NULL` y persistencia en `UPDATE`/`INSERT`; actualizar `pru_solicitudes_auto_anula_bandeja` para cargar `@var_notif_afi_plant_proc_id` desde el motivo, reemplazar el `CASE` por el valor configurado y validar que si `notif_afi = 'S'` la plantilla no sea `NULL`, levantando `RAISERROR` si falta. También quedó pendiente actualizar datos reales: motivo `PRESTADOR POR FUERA DE LA RED` o su `deno` real con plantilla `14`, y motivo `SOLICITUD POR MEDICAMENTOS` con plantilla `15`. Antes de aplicar scripts, confirmar los `deno` exactos existentes en la base.
- Estado: Pendiente

### Tema 8: Memoria bajo demanda y cierre del día

- Solicitud o contexto: Wilmer pidió guardar todos los detalles posibles de lo trabajado y dejar asentado que no tocó todavía la tabla ni los cambios listados para procesos 14 y 15; esos cambios se realizarán mañana.
- Respuesta o acciones del agente: Se ejecutó la memoria bajo demanda para dejar trazabilidad del cierre y los pasos pendientes.
- Resultado: Para continuar mañana, se debe retomar desde la Tarea #29489 con foco en el pendiente de procesos `14` y `15`, partiendo de la solución definida: nuevo campo `notif_afi_plant_proc_id` en `SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION` y ajuste de `pru_solicitudes_auto_anula_bandeja` para usar la plantilla configurada por motivo. El texto sugerido para retomar fue: `Lisa, retomemos la Tarea #29489 desde la memoria del 2026-07-30. Continuemos con el pendiente de los procesos 14 y 15: agregar notif_afi_plant_proc_id en SOLICITUDES_AUTO_MOTIVOS_DEVOLUCION y ajustar pru_solicitudes_auto_anula_bandeja para usar la plantilla configurada por motivo.`
- Estado: Pendiente
