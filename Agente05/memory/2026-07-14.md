# Memoria durable - 2026-07-14

## Wilmer - Tarea #29489

- Preferencias activas de Wilmer para esta conversacion:
  - Tarea activa: #29489.
  - Repositorio exclusivo: `whiguera-Version84-Modelo2Gis`.
  - Ruta de entregables docx: `A:\Wilmer\Tarea #29489`.
  - Rol solicitado: consultivo y guia tecnica; no modificar archivos, no hacer commits.
  - Mostrar scripts, SP y explicaciones por pantalla salvo que Wilmer pida docx o el volumen sea excesivo.
  - Trabajar paso a paso; no avanzar al siguiente paso hasta confirmacion explicita de Wilmer.
  - Al cierre de tarea/jornada, generar resumen consolidado de scripts definitivos, SP modificados y objetos PowerBuilder intervenidos.

- Estado funcional retomado de la Tarea #29489:
  - Variables de comite especialista pendientes para el final y fuera del avance inmediato:
    - `@var_dias_comite_especialitas`
    - `@var_motivo_comite_especialistas`
    - `@var_espe`
    - `@var_tiempo_espe`
  - Equivalentes visibles sin prefijo `var_` tambien quedan pendientes:
    - `@dias_comite_especialistas`
    - `@motivo_comite_especialistas`
    - `@espe`
    - `@tiempo_espe`

- SP principal revisado: `backend/procedures/pri_solicitudes_auto_notif.sql`.
  - La working copy local SVN llego a mostrar conflicto textual, pero Wilmer indico que ya lo habia corregido/commiteado y se decidio tomar como valida la version adjunta limpia.
  - El adjunto del SP principal ya no contiene marcas de conflicto.
  - El adjunto usa `@afi_id` y no `@id_afi`.
  - Wilmer confirmo que corrigio el orden de reemplazo para que `@dias_dev_auditoria`, `@dias_dev_txt`, `@dias_dev_nro` se reemplacen antes de `@dia`.
  - Wilmer ejecuto consulta sobre `SOLICITUDES_AUTO_NOTIF_PLANT` buscando `@id_afi` y no arrojo resultados.
  - Se dio por valido continuar con el baseline del SP principal.

- PowerBuilder revisado:
  - En la working copy local aun aparecia `@id_afi` en `exe_presmed/Source Infomedical/de_notificacion_plant.srd`, linea 21.
  - El adjunto de Wilmer estaba corregido a `@afi_id = Tipo y numero de identificacion`.
  - Tambien se habian detectado referencias anteriores a `@id_afi` en `dw_param_auto_webpos.srd` y `dw_param_auto.srd`, pero para Tarea #29489 el foco directo era `de_notificacion_plant.srd`.

- SP de tablas revisados y decision tecnica:
  - Archivos revisados:
    - `prs_solicitudes_auto_pres_tabla_auto.sql`
    - `prs_solicitudes_auto_tabla_auto.sql`
    - `prs_solicitudes_auto_tabla_estado_anulada.sql`
  - Recomendacion dada y aprobada por Wilmer: no usar esos tres SP como estan.
  - Motivos: no devuelven la tabla generada, nombres inconsistentes, errores de cursor/variables, no integrados al flujo real, conversiones riesgosas, HTML incompleto, no cierran/liberan cursores.
  - Estrategia aprobada: crear un unico helper nuevo para tabla HTML y usarlo condicionalmente solo si la plantilla contiene placeholder.

- Paso 2 propuesto y aprobado para continuar:
  - Placeholder propuesto: `@tabla_autorizaciones`.
  - Helper nuevo sugerido: `dbo.prs_solicitudes_auto_tabla_html`.
  - Interfaz propuesta:
    - Entrada: `@prm_soli_auto NUMERIC(18)`.
    - Salida: `@prm_tabla_html VARCHAR(MAX) OUTPUT`.
  - Responsabilidad: generar tabla HTML con detalle de autorizaciones/prestaciones asociadas a la solicitud.
  - Columnas propuestas: Autorizacion, Prestacion, Estado, Motivo.
  - Integracion en `pri_solicitudes_auto_notif.sql`:
    - Declarar `@var_tabla_autorizaciones VARCHAR(MAX)`.
    - Ejecutar helper solo si `@var_subjet` o `@var_body` contienen `@tabla_autorizaciones`.
    - Reemplazar `@tabla_autorizaciones` en asunto y cuerpo con `ISNULL(@var_tabla_autorizaciones, '')`.
  - Wilmer respondio: "Si, me parece bien, continuemos por favor" y luego acepto continuar con el diseno del helper.

- Ultimo script mostrado a Wilmer antes de compaction:
  - `CREATE PROCEDURE dbo.prs_solicitudes_auto_tabla_html` con `SET NOCOUNT ON`, verificacion de existencia en `SOLICITUDES_AUTO_AUTORIZACIONES`, construccion HTML via concatenacion + `FOR XML PATH(''), TYPE`, escapando `&`, `<`, `>` en prestacion/estado/motivo.
  - Joins propuestos:
    - `SOLICITUDES_AUTO_AUTORIZACIONES SAA`
    - `AUTORIZACIONES A` por `sucur`, `auto`
    - `AUTO_PRESTAC_ITEMS API` por `sucur`, `auto`, `baja_fecha IS NULL`
    - `PRESTACIONES P` por `nomen`, `prestac`, `baja_fecha IS NULL`
    - `AUTO_ESTADOS AE` por `auto_esta = A.esta`
    - `AUTO_ESTADOS_MOTIVOS AEM` por `auto_esta_motivo = A.auto_esta_moti`, `baja_fecha IS NULL`
  - Se debe revisar con cuidado nombres reales de columnas/tablas antes de darlo como definitivo.

- Pendiente inmediato al retomar:
  - Continuar paso a paso con Wilmer desde la definicion definitiva del helper `prs_solicitudes_auto_tabla_html`.
  - Validar nombres reales de tablas/columnas en el repo antes de entregar script final.
  - No editar archivos locales; solo mostrar propuesta por pantalla, salvo instruccion explicita de Wilmer.
# Memoria durable - 2026-07-14

## Wilmer - Tarea #29489

- Preferencias activas de Wilmer para esta conversacion:
  - Tarea activa: #29489.
  - Repositorio exclusivo: `whiguera-Version84-Modelo2Gis`.
  - Ruta de entregables docx: `A:\Wilmer\Tarea #29489`.
  - Rol solicitado: consultivo y guia tecnica; no modificar archivos, no hacer commits.
  - Mostrar scripts, SP y explicaciones por pantalla salvo que Wilmer pida docx o el volumen sea excesivo.
  - Trabajar paso a paso; no avanzar al siguiente paso hasta confirmacion explicita de Wilmer.
  - Al cierre de tarea/jornada, generar resumen consolidado de scripts definitivos, SP modificados y objetos PowerBuilder intervenidos.

- Estado funcional retomado de la Tarea #29489:
  - Variables de comite especialista pendientes para el final y fuera del avance inmediato:
    - `@var_dias_comite_especialitas`
    - `@var_motivo_comite_especialistas`
    - `@var_espe`
    - `@var_tiempo_espe`
  - Equivalentes visibles sin prefijo `var_` tambien quedan pendientes:
    - `@dias_comite_especialistas`
    - `@motivo_comite_especialistas`
    - `@espe`
    - `@tiempo_espe`

- SP principal revisado: `backend/procedures/pri_solicitudes_auto_notif.sql`.
  - La working copy local SVN llego a mostrar conflicto textual, pero Wilmer indico que ya lo habia corregido/commiteado y se decidio tomar como valida la version adjunta limpia.
  - El adjunto del SP principal ya no contiene marcas de conflicto.
  - El adjunto usa `@afi_id` y no `@id_afi`.
  - Wilmer confirmo que corrigio el orden de reemplazo para que `@dias_dev_auditoria`, `@dias_dev_txt`, `@dias_dev_nro` se reemplacen antes de `@dia`.
  - Wilmer ejecuto consulta sobre `SOLICITUDES_AUTO_NOTIF_PLANT` buscando `@id_afi` y no arrojo resultados.
  - Se dio por valido continuar con el baseline del SP principal.

- PowerBuilder revisado:
  - En la working copy local aun aparecia `@id_afi` en `exe_presmed/Source Infomedical/de_notificacion_plant.srd`, linea 21.
  - El adjunto de Wilmer estaba corregido a `@afi_id = Tipo y numero de identificacion`.
  - Tambien se habian detectado referencias anteriores a `@id_afi` en `dw_param_auto_webpos.srd` y `dw_param_auto.srd`, pero para Tarea #29489 el foco directo era `de_notificacion_plant.srd`.

- SP de tablas revisados y decision tecnica:
  - Archivos revisados:
    - `prs_solicitudes_auto_pres_tabla_auto.sql`
    - `prs_solicitudes_auto_tabla_auto.sql`
    - `prs_solicitudes_auto_tabla_estado_anulada.sql`
  - Recomendacion dada y aprobada por Wilmer: no usar esos tres SP como estan.
  - Motivos: no devuelven la tabla generada, nombres inconsistentes, errores de cursor/variables, no integrados al flujo real, conversiones riesgosas, HTML incompleto, no cierran/liberan cursores.
  - Estrategia aprobada: crear un unico helper nuevo para tabla HTML y usarlo condicionalmente solo si la plantilla contiene placeholder.

- Paso 2 propuesto y aprobado para continuar:
  - Placeholder propuesto: `@tabla_autorizaciones`.
  - Helper nuevo sugerido: `dbo.prs_solicitudes_auto_tabla_html`.
  - Interfaz propuesta:
    - Entrada: `@prm_soli_auto NUMERIC(18)`.
    - Salida: `@prm_tabla_html VARCHAR(MAX) OUTPUT`.
  - Responsabilidad: generar tabla HTML con detalle de autorizaciones/prestaciones asociadas a la solicitud.
  - Columnas propuestas: Autorizacion, Prestacion, Estado, Motivo.
  - Integracion en `pri_solicitudes_auto_notif.sql`:
    - Declarar `@var_tabla_autorizaciones VARCHAR(MAX)`.
    - Ejecutar helper solo si `@var_subjet` o `@var_body` contienen `@tabla_autorizaciones`.
    - Reemplazar `@tabla_autorizaciones` en asunto y cuerpo con `ISNULL(@var_tabla_autorizaciones, '')`.
  - Wilmer respondio: "Si, me parece bien, continuemos por favor" y luego acepto continuar con el diseno del helper.

- Ultimo script mostrado a Wilmer antes de compaction:
  - `CREATE PROCEDURE dbo.prs_solicitudes_auto_tabla_html` con `SET NOCOUNT ON`, verificacion de existencia en `SOLICITUDES_AUTO_AUTORIZACIONES`, construccion HTML via concatenacion + `FOR XML PATH(''), TYPE`, escapando `&`, `<`, `>` en prestacion/estado/motivo.
  - Joins propuestos:
    - `SOLICITUDES_AUTO_AUTORIZACIONES SAA`
    - `AUTORIZACIONES A` por `sucur`, `auto`
    - `AUTO_PRESTAC_ITEMS API` por `sucur`, `auto`, `baja_fecha IS NULL`
    - `PRESTACIONES P` por `nomen`, `prestac`, `baja_fecha IS NULL`
    - `AUTO_ESTADOS AE` por `auto_esta = A.esta`
    - `AUTO_ESTADOS_MOTIVOS AEM` por `auto_esta_motivo = A.auto_esta_moti`, `baja_fecha IS NULL`
  - Se debe revisar con cuidado nombres reales de columnas/tablas antes de darlo como definitivo.

- Pendiente inmediato al retomar:
  - Continuar paso a paso con Wilmer desde la definicion definitiva del helper `prs_solicitudes_auto_tabla_html`.
  - Validar nombres reales de tablas/columnas en el repo antes de entregar script final.
  - No editar archivos locales; solo mostrar propuesta por pantalla, salvo instruccion explicita de Wilmer.

## Barbara - Tema Bandeja / R26460

- Conversacion directa con Barbara en Google Chat. Repositorio de trabajo: `bgonzalez-Version84-Modelo2Gis` / `bgonzalez-version84-modelo2gis` bajo `/home/desarrollo-ia-05/.openclaw/workspace/svn/`.
- Carpeta de documentos/entregables: `/mnt/Agentes/Barbara/TemaBandeja`.
- Perfil DB indicado para validaciones backend: `InstallQA`.

- Primer analisis realizado sobre documento `Casos_Bandeja4`:
  - Caso 109: se concluyo que no existe implementacion de `SOLICITUDES_AUTO.RESOL_AUDIT`; en repo no aparece `RESOL_AUDIT/resol_audit`, en `InstallQA` no existe la columna, y el boton existente es `Resuelta Pertinencia`.
  - `pru_solicitudes_auto_resuelta_pertinencia.sql` solo recibe `@prm_soli_auto`, valida estado `S`, responsable y cambia `soli_esta` a `G`; no recibe texto ni persiste resolucion.
  - Caso 110: `SOLICITUDES_AUTO_OPORTUNIDAD` no existe ni en repo ni en `InstallQA`; si existen calculos/consulta de oportunidad por `prs_solicitudes_auto_oportunidad` y `prs_solicitudes_auto_tiempo_oportunidad_bandeja`, consumidos por la solapa `Tiempo de Oportunidad`.

- Segundo analisis pedido sobre EF `R26460-EF-V1ok` limitado a `resol_audit` y `SOLICITUDES_AUTO_OPORTUNIDAD`:
  - Se dejo archivo de analisis en `/mnt/Agentes/Barbara/TemaBandeja/Analisis_R26460_resol_audit_oportunidad.md`.
  - No se modifico el repositorio.
  - Se valido EF, repo e `InstallQA`.

- Decisiones funcionales posteriores de Barbara:
  - `SOLICITUDES_AUTO_OPORTUNIDAD` queda desestimada definitivamente: no se realizo porque la informacion era solo informativa en la solapa `Tiempo de Oportunidad`; no tiene sentido crear tabla para persistir esos datos.
  - `resol_audit` tampoco se va a realizar de esa forma: la solicitud puede ir N veces entre ejecutivo y auditor; si hace falta dejar notas, ya existen observaciones en la solapa `Observaciones`; no siempre quien supervisa agrega notas.

- Nuevo analisis solicitado y respondido: impacto de agregar `SOLICITUDES_AUTO.audit_fecha DATETIME NULL`.
  - Objetivo funcional: marca de fecha/hora del ultimo retorno desde auditoria hacia gestion/ejecutivo, sin depender de notas.
  - Impacto obligatorio sugerido:
    - Agregar columna en `backend/tables/SOLICITUDES_AUTO.sql` con `ALTER TABLE dbo.SOLICITUDES_AUTO ADD audit_fecha DATETIME NULL` protegido por `IF NOT EXISTS`.
    - En `prs_valida_of_auto_remotas.sql`, setear `audit_fecha` cuando determina retorno al ejecutivo: caso `@var_supervision = 'S'` y estado resultante `@var_soli_esta = 'G'`. Se sugirio usar `@var_hoy DATETIME = GETDATE()` para consistencia.
    - En `pru_solicitudes_auto_resuelta_pertinencia.sql`, setear `audit_fecha` cuando el auditor usa el boton actual `Resuelta Pertinencia` y la solicitud pasa a `G`. Tambien conviene usar `@var_hoy` para `modi_fecha` y `audit_fecha`.
  - Impactos a revisar pero no recomendados sin mas regla funcional:
    - `pru_reasigna_soli_auto.sql`: reasigna responsables segun estado `S`/`V` vs otros; podria no representar retorno formal desde auditoria. Recomendacion dada: no tocarlo hasta confirmar regla precisa.
    - `pru_fin_gestion_solicitudes_auto.sql` y `pru_rev_soportes_solicitudes_auto.sql`: no marcarlos para `audit_fecha` porque son movimientos hacia auditoria/cierre, no retorno al ejecutivo.
  - Conclusion dada: modificacion minima coherente = columna `audit_fecha`, seteo en `prs_valida_of_auto_remotas` al volver a `G`, seteo en `pru_solicitudes_auto_resuelta_pertinencia` al resolver auditoria/pertinencia; no tocar oportunidad ni resol_audit.

- Implementacion ejecutada para Redmine 29478 / Tarea 0007468:
  - Barbara aprobo aplicar la conclusion anterior: agregar `SOLICITUDES_AUTO.audit_fecha DATETIME NULL` y setearla con `@var_hoy` en los retornos desde auditoria hacia gestion/ejecutivo.
  - Archivos modificados en `bgonzalez-Version84-Modelo2Gis`:
    - `backend/tables/SOLICITUDES_AUTO.sql`
    - `backend/procedures/prs_valida_of_auto_remotas.sql`
    - `backend/procedures/pru_solicitudes_auto_resuelta_pertinencia.sql`
  - En `prs_valida_of_auto_remotas.sql` se incorporo el seteo de `audit_fecha` cuando el resultado vuelve la solicitud a estado `G`.
  - En `pru_solicitudes_auto_resuelta_pertinencia.sql` se incorporo `@var_hoy` y se setean `modi_fecha` y `audit_fecha` de forma consistente.
  - Barbara dio doble confirmacion para ejecutar scripts en la base `InstallQA`.
  - Validaciones realizadas en `InstallQA`: la columna `SOLICITUDES_AUTO.audit_fecha` existe y ambos SPs contienen `audit_fecha` y `@var_hoy`.
  - Commit SVN realizado: `r37327`.
  - Mensaje de commit: `R29478 - 0007468: GAP 002 - Bandeja de AUDITORIA - Casos Drive`.

## Wilmer - Cierre jornada Tarea #29489 / R23986 notificaciones GIS

- Conversacion directa con Wilmer Higuera en Google Chat.
- Tarea activa: `#29489`.
- Repositorio exclusivo definido por preferencias de Wilmer: `whiguera-Version84-Modelo2Gis`.
- Ruta de entregables activa usada durante la jornada:
  - Windows: `A:\whiguera\Tarea #23986\Tarea #29489`
  - Linux/montaje: `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489`
- Rol acordado: consultivo/guia tecnica. Por defecto no modificar repositorio ni hacer commits; solo proponer, revisar y generar entregables cuando Wilmer lo pida expresamente.

- Estado funcional acordado al retomar:
  - Variables de comite especialista quedan para el final, pendientes de definicion del cliente, y no se deben considerar en el avance inmediato:
    - `@dias_comite_especialistas`
    - `@motivo_comite_especialistas`
    - `@espe`
    - `@tiempo_espe`

- SP principal `pri_solicitudes_auto_notif.sql`:
  - Wilmer indico que ya corrigio el orden de reemplazo de variables.
  - Se valido conceptualmente que el orden correcto es reemplazar primero:
    - `@dias_dev_auditoria`
    - `@dias_dev_txt`
    - `@dias_dev_nro`
    - y al final `@dia`
  - Wilmer ejecuto consulta de validacion para `@id_afi` en plantillas activas y no devolvio resultados.
  - Quedo definido usar `@afi_id` y no `@id_afi`.
  - Importante: la working copy local del SVN seguia figurando en conflicto para `backend/procedures/pri_solicitudes_auto_notif.sql`; no usar esa copia conflictiva como fuente final. Wilmer venia trabajando con un adjunto/version corregida.

- Analisis de los tres SP dejados por Cesar:
  - `prs_solicitudes_auto_pres_tabla_auto.sql`
  - `prs_solicitudes_auto_tabla_auto.sql`
  - `prs_solicitudes_auto_tabla_estado_anulada.sql`
  - Decision recomendada y aceptada: no usarlos como estan.
  - Motivos: errores de cursores/variables, nombres inconsistentes, no devuelven output, no estan integrados al flujo, HTML incompleto y riesgo de conversiones.

- Helper nuevo:
  - Primero se penso un placeholder generico `@tabla_autorizaciones`.
  - Luego Wilmer detecto correctamente que la EF no usa la misma tabla para todos los procesos.
  - Se cambio el diseno a placeholders especificos por tipo de tabla:
    - `@tabla_autorizaciones_prestador` para proceso `5`.
    - `@tabla_autorizaciones_autorizadas` para procesos `8` y `23`.
    - `@tabla_prestaciones_negadas` para procesos `9` y `12`.
    - `@tabla_autorizaciones_mixtas` para procesos `10` y `11`.
  - Se creo/actualizo entregable:
    - `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/prs_solicitudes_auto_tabla_html.sql`
  - Version actual del helper:
    - Procedimiento: `dbo.prs_solicitudes_auto_tabla_html`
    - Parametros:
      - `@prm_soli_auto NUMERIC(18,0)`
      - `@prm_tipo_tabla VARCHAR(30)`
      - `@prm_tabla_html VARCHAR(MAX) OUTPUT`
    - Tipos soportados:
      - `PRESTADOR`: columnas `Numero de Autorizacion`, `Decision`, `Motivo de Negacion`.
      - `AUTORIZADAS`: columnas `Numero de Autorizacion`, `Descripcion`, `Decision`.
      - `NEGADAS`: columnas `Descripcion`, `Decision`.
      - `MIXTAS`: columnas `Numero de Autorizacion`, `Descripcion`, `Decision`.
    - Correcciones tecnicas aplicadas:
      - `PRESTACIONES` no tiene columna `deno`; se usa `PRESTACIONES.abre_deno`.
      - `AUTORIZACIONES` no tiene `auto_esta_moti`; motivo de negacion se toma desde `RECHA_MOTIVOS_PRESMED` via `AUTO_PRESTAC_ITEMS.recha` o `AUTORIZACIONES.recha`.
      - Decision se toma desde `AUTO_PRESTAC_ITEMS.esta` cuando exista; si no, cae a `AUTORIZACIONES.esta`.
      - Se escapan caracteres HTML `&`, `<`, `>` en descripcion, decision y motivo.

- Script de plantillas:
  - Entregable actualizado:
    - `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/SOLICITUDES_AUTO_NOTIF_PLANT.sql`
  - Se modifico para hacer `UPDATE` si el registro existe e `INSERT` si no existe.
  - Usa tabla temporal `#SOLICITUDES_AUTO_NOTIF_PLANT`.
  - Se validaron 26 plantillas, comillas balanceadas y ausencia de placeholders obsoletos genericos:
    - no queda `@id_afi`
    - no queda `@tabla_autorizaciones` generico exacto
  - Nota: plantillas 19, 20 y 21 siguen relacionadas con comite/especialista y quedan pendientes hasta definicion del cliente.

- Bloque de integracion del SP principal:
  - Entregable creado:
    - `/mnt/Agentes/whiguera/Tarea #23986/Tarea #29489/pri_solicitudes_auto_notif_integracion_tablas_html.sql`
  - Contiene instrucciones para aplicar sobre la version limpia/resuelta de `pri_solicitudes_auto_notif.sql`:
    - declarar variables:
      - `@var_tabla_autorizaciones_prestador VARCHAR(MAX)`
      - `@var_tabla_autorizaciones_autorizadas VARCHAR(MAX)`
      - `@var_tabla_prestaciones_negadas VARCHAR(MAX)`
      - `@var_tabla_autorizaciones_mixtas VARCHAR(MAX)`
    - ejecutar `dbo.prs_solicitudes_auto_tabla_html` condicionalmente solo si `@var_subjet` o `@var_body` contienen el placeholder correspondiente.
    - reemplazar los cuatro placeholders en `@var_subjet` y `@var_body`.

- Validaciones realizadas:
  - Se extrajo texto de `R23986-EF_V4.doc` con `antiword` para comparar procesos y grillas.
  - Se verifico que `PRESTACIONES` tiene `abre_deno` y `exten_deno`, no `deno`.
  - Se verifico que `AUTORIZACIONES` tiene `recha` y `esta`, pero no `auto_esta_moti`.
  - Se verifico que `AUTO_PRESTAC_ITEMS` tiene `esta`, `recha`, `nomen`, `prestac`, `item`, `baja_fecha`.
  - Se verifico que `AUTO_ESTADOS` tiene `auto_esta`, `deno`.
  - Se verifico que `RECHA_MOTIVOS_PRESMED` tiene `recha`, `deno`, `baja_fecha`.
  - Comillas balanceadas en:
    - `SOLICITUDES_AUTO_NOTIF_PLANT.sql`
    - `prs_solicitudes_auto_tabla_html.sql`
    - `pri_solicitudes_auto_notif_integracion_tablas_html.sql`

- Prueba sugerida para retomar:
  - Wilmer habia usado `soli_auto = 25920000`, que tiene 2 autorizaciones y 4 prestaciones asociadas.
  - Probar el helper asi:
    - `EXEC dbo.prs_solicitudes_auto_tabla_html 25920000, 'AUTORIZADAS', @html OUTPUT`
    - `EXEC dbo.prs_solicitudes_auto_tabla_html 25920000, 'NEGADAS', @html OUTPUT`
    - `EXEC dbo.prs_solicitudes_auto_tabla_html 25920000, 'MIXTAS', @html OUTPUT`
    - `EXEC dbo.prs_solicitudes_auto_tabla_html 25920000, 'PRESTADOR', @html OUTPUT`
  - Revisar visualmente que cada HTML tenga columnas segun EF.

- Proximo paso exacto al continuar:
  1. Wilmer debe aplicar/compilar `prs_solicitudes_auto_tabla_html.sql`.
  2. Probarlo con `soli_auto = 25920000` para los cuatro tipos de tabla.
  3. Si las tablas HTML salen correctas, aplicar manualmente el bloque de `pri_solicitudes_auto_notif_integracion_tablas_html.sql` sobre la version limpia del SP principal.
  4. No probar todavia `pri_solicitudes_auto_notif` con envio real hasta validar primero el helper y el reemplazo de body en modo controlado.

- Frase recomendada para que Wilmer retome:
  - "Lisa, retomemos la Tarea #29489 desde el cierre del 14/07. Ya estan los entregables `SOLICITUDES_AUTO_NOTIF_PLANT.sql`, `prs_solicitudes_auto_tabla_html.sql` y `pri_solicitudes_auto_notif_integracion_tablas_html.sql`; seguimos probando el helper con `soli_auto = 25920000` y luego integramos el bloque al SP principal."
