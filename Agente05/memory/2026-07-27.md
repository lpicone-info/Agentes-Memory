
## 2026-07-27 15:55 ART - Pre-compaction memory

- Conversation with Barbara in Google Chat, direct chat. Agent identity in this workspace: Lisa / Agente05, technical architect focused on PowerBuilder 2022 and MS SQL, concise Spanish communication.
- Active repo used today: `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`, SVN, project area `exe_presmed/Source Infomedical`.
- Work on `wp_soli_reg.srw` for object `wp_soli_reg`:
  - Updated repo initially to SVN r37435/r37436 for analysis.
  - Analyzed requested visual change: remove tab `tabpage_fechas` / "Fechas Generacion" and move `de_soli_reg_fechas` DataWindow into tab `tabpage_12` / "Servicios a solicitar", above CUPS.
  - Identified needed changes: reconnect `idw_fecha`, replace `tabpage_fechas.Visible` with `idw_fecha.Visible`, change date-validation `SelectedTab = 10` to `SelectedTab = 5`, keep `itemchanged`, adjust resize/layout.
- Commit r37437 made in SVN with message `R29478 - 0007468: GAP 002 – Bandeja de AUDITORIA - Casos Drive`:
  - File: `wp_soli_reg.srw`.
  - Renamed moved control from `dw_1` to `dw_fecha`.
  - Set `idw_fecha = tab_1.tabpage_12.dw_fecha`.
  - Replaced visibility logic on removed tab with `idw_fecha.Visible`.
  - Changed date validation jumps from `SelectedTab = 10` to `SelectedTab = 5`.
  - Adjusted resize so `dw_prestac` accounts for the date block.
- Follow-up analysis: recommended keeping `Visible TRUE/FALSE` instead of `Enabled TRUE/FALSE` because it better preserves original behavior where the old date tab was unavailable when not applicable. Noted that `Enabled FALSE` would leave visible disabled fields and visual noise.
- Barbara noted invisible dates left blank space above CUPS. Recommended dynamic layout instead of switching to `Enabled`.
- Commit r37438 made in SVN with same message:
  - File: `wp_soli_reg.srw`.
  - Added routine `wf_layout_fechas`.
  - When `idw_fecha.Visible = TRUE`, CUPS (`idw_prestac`) and buttons stay below date block.
  - When false, CUPS and buttons move up to remove empty space.
  - `wf_resize_dw_height()` now calls `wf_layout_fechas()`.
  - Added calls after `idw_fecha.Visible = TRUE/FALSE`.
- Analyzed replacing common `MessageBox` after successful request registration with custom modal window matching screenshot `/mnt/Agentes/bgonzalez/imagen_mensaje.jpg`.
  - Original code in `wp_soli_reg.srw` built `ls_msj += 'Se registró la solicitud N° ' + STRING(il_soli) + ...` and used `MessageBox('Solicitud registrada', ls_msj, Question!, YesNoCancel!, 2)`.
  - Required preserving button semantics: Yes=register another for same affiliate -> `wf_limpiar('N')`; No=recover registered request info -> `wf_retrieve_total(TRUE)` then `RETURN 1`; Cancel=clean screen -> `wf_limpiar('S')`.
- Commit r37439 made in SVN with same message:
  - Files: `wp_soli_reg.srw`, new `wp_soli_reg_mensaje.srw`, `autoriz.pbg`.
  - Replaced standard `MessageBox('Solicitud registrada', ..., YesNoCancel!)` with modal `wp_soli_reg_mensaje`.
  - Window receives/display request number and has descriptive buttons returning 1/2/3 matching original flow.
  - Registered new window in `autoriz.pbg`.
- Latest user asked about screenshots `/mnt/Agentes/bgonzalez/archivos_gis` and `/mnt/Agentes/bgonzalez/archivos_power`: GIS web file picker shows option `Archivos personalizados`; Power executable uses native Windows dialog from `GetFileOpenName`.
  - Analysis given: yes, possible for the requested label/filter behavior.
  - In `wp_soli_reg.srw` around file attachment logic, filter currently has separate entries like `(*.pdf),*.pdf`, `(*.bmp),*.bmp`, etc., and calls `GetFileOpenName(ls_title, ls_selectedPath, ls_selectedFile, 'PDF', ls_filter)`.
  - Proposed change: set a single filter entry such as `Archivos personalizados (*.pdf;*.bmp;*.jpg;*.jpeg;*.png;*.gif;*.tif;*.tiff),*.pdf;*.bmp;*.jpg;*.jpeg;*.png;*.gif;*.tif;*.tiff`.
  - Existing extension validation after selection already controls allowed types, so core upload logic should not need changes.
  - Limitation explained: PowerBuilder/Windows dialog can change filter text but cannot replicate all browser-specific GIS options like mobile-device upload.
- Validation style used for commits: checked `svn status` clean except externals `X`; preserved UTF-8 BOM/CRLF where applicable; no PowerBuilder compilation available in this environment.
