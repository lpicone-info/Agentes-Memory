# Memoria de conversación — Wilmer Higuera

## Datos

- **Fecha:** 25/08/2026
- **Usuario:** Wilmer Higuera
- **ID Google Chat:** `users/113120678211994207734`
- **Canal:** Google Chat
- **Zona horaria:** America/Argentina/Buenos_Aires
- **Cantidad de intervenciones:** 66
- **Inicio:** 09:16:49
- **Fin:** 19:43:56

## Sesiones revisadas

- `agent:main:googlechat:direct:spaces/qzugpyaaaae`

## Archivos revisados

- `684e8663-d62d-465b-91b7-aa7f6430461c.jsonl`

---

## Transcripción completa

### [25/08/2026 09:16:49] Wilmer Higuera

Hola Lisa, buenos días

---

### [25/08/2026 09:18:39] Agente

Hola Wilmer, ya cargue tus preferencias y estoy disponible para trabajar. ¿En qué avanzamos?

---

### [25/08/2026 09:30:50] Wilmer Higuera

Recuerda por favor estos dos puntos:
## Cosas que no hacer
1. NO realizar modificaciones directas en el entorno ni hacer Commits. El agente solo propone y muestra los cambios por pantalla o archivo; la ejecución de modificaciones y commits es responsabilidad exclusiva de whiguera.
2. NO asumir que un paso se ha comprendido o cerrado de forma automática sin el visto bueno de whiguera.

---

### [25/08/2026 09:30:57] Agente

Wilmer, confirmado.

Esos dos puntos ya están cargados en tus preferencias y quedan como criterio activo para esta sesión:

1. No voy a realizar modificaciones directas en el entorno ni commits.
2. No voy a asumir ningún paso como comprendido, cerrado o testeado sin tu visto bueno explícito.

---

### [25/08/2026 10:53:23] Wilmer Higuera

Lisa, recupera la memoria del 2026-08-24 de la Tarea 29489 / InfoMailer procesos 8, 9 y 13, y continuemos armando el paso a paso detallado de modificación antes de tocar código.

---

### [25/08/2026 10:57:22] Agente

Wilmer, memoria recuperada y validada contra el repo activo. No modifiqué archivos.

**Evidencia**
- El cierre del 24/08 dejó definido que trabajamos sobre Tarea 29489, procesos 8, 9 y 13, sin tocar código todavía. Source: `memory/2026-08-24.md#L34-L129`
- El cambio principal está en `exe_InfoMailer/src/wp_main_infomailer.srw`.
- Hoy `ue_procesar` llama una sola vez a `wf_genera_adjunto(ll_mail_id, ls_attach_aux)` y luego agrega un único `ls_attach_aux` a `lstr_attach[]`.
- Hoy `wf_genera_adjunto` ejecuta `prs_datos_email_auto`, pero hace un solo `FETCH`, por eso solo toma una autorización.
- `prs_datos_email_auto.sql` puede devolver varias filas porque consulta `MAIL_AUTORIZACIONES` por `mail_id`.
- En el código actual, `wf_genera_adjunto` genera el PDF local, pero no veo ahí la subida con `nvo_sube_adjunto.of_sube_adjunto_gral` ni la llamada a `pru_auto_arch_path_full`. Ese punto queda como corrección necesaria para cumplir tu indicación de guardar la ruta igual que el proceso existente.

**Diseño General Aprobado**
1. Proceso 8: adjuntar todas las órdenes autorizadas.
2. Proceso 9: adjuntar cartas de negación finalizadas.
3. Proceso 13: adjuntar en el mismo mail órdenes autorizadas y cartas de negación.
4. El mail debe salir una sola vez con todos los adjuntos.
5. Si falla un PDF, no se debe enviar mail parcial.
6. No se toca `nvuo_mailer`, `n_smtp` ni `str_attachment`.
7. La auditoría funcional queda en `pri_solicitudes_auto_notif`; InfoMailer solo deja log técnico.

**Paso 1 Propuesto: Refactor Controlado En InfoMailer**
Modificar conceptualmente `wp_main_infomailer.srw` para que `wf_genera_adjunto` deje de devolver un solo archivo y pase a cargar varios adjuntos en `lstr_attach[]`.

Cambio de idea:

```powerscript
// Actual
li_rv = wf_genera_adjunto(ll_mail_id, ls_attach_aux)
```

Debe pasar a algo equivalente a:

```powerscript
// Propuesto
li_rv = wf_genera_adjunto(ll_mail_id, lstr_attach, ll_attach_counter_aux, ll_attach_binary_position)
```

La función recibiría:
- `al_mail_id`: mail a procesar.
- `ref str_attachment astr_attach[]`: arreglo existente de adjuntos.
- `ref long al_attach_counter`: contador actual del arreglo.
- `ref long al_attach_delete_position`: primera posición de adjuntos temporales generados para borrar al final.

Dentro de `wf_genera_adjunto`:
1. Ejecutar `prs_datos_email_auto`.
2. Hacer `FETCH` en bucle hasta terminar todas las filas.
3. Por cada autorización:
   - identificar `sucur`, `auto`, apellido, nombre y tipo de adjunto;
   - generar el PDF correspondiente;
   - subir el PDF con `nvo_sube_adjunto.of_sube_adjunto_gral`;
   - guardar `archivo_path_full` con `pru_auto_arch_path_full`;
   - agregar el PDF local a `lstr_attach[]`;
   - marcar `inline = FALSE`.
4. Si cualquier paso falla:
   - devolver error;
   - registrar mensaje técnico;
   - evitar envío parcial del mail.

**Definición Pendiente Antes Del Paso 2**
Para procesos 9 y 13 necesitamos que InfoMailer sepa si debe generar:
- orden autorizada;
- carta de negación.

El `prs_datos_email_auto` actual devuelve `A.tipo`, que corresponde al tipo de autorización, no al tipo de adjunto. Para resolver procesos 9 y 13, mi recomendación técnica es ajustar `prs_datos_email_auto` para devolver también un campo lógico, por ejemplo:

```sql
tipo_adjunto = ISNULL(MA.tipo, 'A')
```

Donde:
- `A` = orden autorizada;
- `C` = carta de negación.

Confirmame este **Paso 1** y esta definición sobre `tipo_adjunto`; recién con tu visto bueno armamos el bloque exacto de modificación PowerBuilder línea por línea.

---

### [25/08/2026 11:19:37] Wilmer Higuera

Lisa, pero tu ayer me escribiste esto, por eso di por hecho que estaba todo claro, confirma, ¿Qué duda tenemos o que información nos falta?, a continuación te comparto lo que me escribiste ayer:
Correcto, Wilmer. Entonces queda definido así:

**Proceso 13** debe manejar ambos tipos de adjunto en el mismo email:

Autorizaciones con AUTORIZACIONES.esta = 'A' → adjuntar **órdenes autorizadas**, usando el mismo circuito del Proceso 8.
Autorizaciones con AUTORIZACIONES.esta = 'N' y carta de negación finalizada → adjuntar **carta de negación**, usando el circuito del Proceso 9.

La lógica esperada sería:
text
Proceso 13
Solicitud FINALIZADA
Tiene al menos una autorización A
Tiene al menos una autorización N con carta de negación finalizada

Resultado:
1 solo email al afiliado
N adjuntos:
 - PDF(s) de órdenes autorizadas
 - PDF(s) de carta(s) de negación

Y técnicamente en MAIL_AUTORIZACIONES lo podemos distinguir con tipo:
text
tipo = 'A' -> orden autorizada
tipo = 'C' -> carta de negación

Entonces el diseño queda consistente:

Proceso 8 inserta solo tipo 'A'.
Proceso 9 inserta solo tipo 'C'.
Proceso 13 inserta tipo 'A' y tipo 'C' para el mismo mail_id.

Con esto ya queda claro que “órdenes médicas” en el funcional equivale a **órdenes autorizadas**, no es un tercer tipo de PDF distinto.

---

### [25/08/2026 11:20:07] Agente

Sí, Wilmer: confirmo que **funcionalmente no tenemos dudas**.

Lo que quedó claro ayer es exactamente esto:

- Proceso 8: `tipo = 'A'`, adjunta órdenes autorizadas.
- Proceso 9: `tipo = 'C'`, adjunta cartas de negación.
- Proceso 13: mezcla en el mismo `mail_id` filas `tipo = 'A'` y `tipo = 'C'`.
- “Órdenes médicas” equivale a órdenes autorizadas, no es un tercer PDF.
- `MAIL_AUTORIZACIONES.tipo` es el dato que debe distinguir qué PDF generar.
- `prs_datos_email_auto` debe devolver ese tipo de adjunto para que InfoMailer decida si genera orden o carta.
- `wp_main_infomailer.srw` debe recorrer todas las filas y adjuntarlas al mismo email.

La “duda” que mencioné recién no era funcional, fue una mala forma de expresarlo de mi parte. Lo correcto es decirlo así:

**No falta información funcional.**  
Lo que falta es armar el paso a paso técnico exacto de modificación, separando:

1. Ajuste en `pri_solicitudes_auto_notif` para insertar en `MAIL_AUTORIZACIONES` los registros correctos con `tipo = 'A'` o `tipo = 'C'`.
2. Ajuste en `prs_datos_email_auto` para devolver `MAIL_AUTORIZACIONES.tipo` como tipo de adjunto.
3. Ajuste en `wp_main_infomailer.srw` para recorrer todas las filas, generar cada PDF, guardar la ruta como corresponde y cargar todos los adjuntos en `lstr_attach[]`.

Punto técnico a validar con cuidado: en el código actual que revisé hoy, `wf_genera_adjunto` genera el PDF local, pero no veo en esa función la subida con `nvo_sube_adjunto.of_sube_adjunto_gral` ni la llamada a `pru_auto_arch_path_full`. Entonces eso no es una duda del requerimiento, es una validación técnica obligatoria para que el paso a paso respete tu indicación: **guardar la ruta igual que el circuito existente**.

---

### [25/08/2026 11:26:48] Wilmer Higuera

Lisa, mira estas observaciones del compañero desarrollador, de pronto nos sirva para validar el punto técnico:
Hay cosas que no me parece bien, eso de usar el campo tipo de
mail_autorizaciones no estoy de acuerdo ese campo por algo se debe usar el
prs_datos_email_auto hay que ver donde se usa y se podría agregar el estado de
la autorización que esta en esta de la tabla autorizaciones.
También valida exactamente en que parte se genera el PDF de la carta de negación, como y donde guarda la ruta





Lo que no me queda claro acá dice que se graban en
autorizaciones en la ruta de API, eso habría que corroborarlo, si se guarda
cuando finaliza la carta que se sube la Api o cuando Se imprime Yo pensé que se
hacía cuando Se imprime yo no me acuerdo

---

### [25/08/2026 11:29:37] Agente

Wilmer, las observaciones del compañero sirven y ajustan el diseño.

Confirmación técnica:

1. **No conviene basarnos en `MAIL_AUTORIZACIONES.tipo` como eje del diseño.**  
   El campo existe en la tabla, pero en el código actual casi no se usa funcionalmente. Encontré incluso SPs donde `MA.tipo` está comentado y se devuelve fijo `tipo = 'E'`. Entonces el compañero tiene razón: antes de usar ese campo como criterio fuerte, habría que saber para qué fue creado.

2. **Mejor criterio para InfoMailer:** usar `AUTORIZACIONES.esta`.  
   Para nuestro caso alcanza y es más natural:
   - `A.esta = 'A'` → generar orden autorizada.
   - `A.esta = 'N'` + carta finalizada → generar carta de negación.

3. **`prs_datos_email_auto` sí debe modificarse, pero no necesariamente para traer `MAIL_AUTORIZACIONES.tipo`.**  
   La propuesta ajustada sería que devuelva:
   - `sucur`
   - `auto`
   - `AUTORIZACIONES.tipo` para formato de orden, como hoy
   - `ape`
   - `nombre`
   - `AUTORIZACIONES.esta`
   - indicador o dato de carta finalizada cuando aplique

4. **Dónde se genera el PDF de carta de negación:**  
   Está en `exe_presmed/Source Infomedical/wr_auto_impresion.srw`.  
   Cuando `au.esta = 'N'`, recupera requisitos de carta con `prs_carta_negacion_requisitos`, carga `dw_imprimir` con `dwc_carta_negacion`, ejecuta `prs_datos_generales_afilia_carta_nega_imp`, y luego genera el PDF desde `dw_imprimir`.

5. **Cuándo se guarda la ruta API:**  
   No se guarda al finalizar la carta.  
   La finalización de carta actualiza `CARTA_NEGACION.final_fecha`.  
   La ruta API se guarda cuando se imprime/envía y está activo `USA API Adjuntos`, usando:
   - `nvo_sube_adjunto.of_sube_adjunto_gral`
   - `pru_auto_arch_path_full`
   - destino final: `AUTORIZACIONES.archivo_path_full`

Entonces corrijo el diseño anterior:

**No usaría `MAIL_AUTORIZACIONES.tipo` como fuente principal.**  
Usaría `AUTORIZACIONES.esta` para decidir el tipo de PDF, y `CARTA_NEGACION.final_fecha IS NOT NULL` + `CARTA_NEGACION.baja_fecha IS NULL` para habilitar carta de negación.

No falta información funcional. Lo que cambió es el criterio técnico recomendado: reemplazar la idea de `tipo = 'A'/'C'` por decisión basada en estado real de la autorización.

---

### [25/08/2026 11:58:22] Wilmer Higuera

Mira este comentario del compañero desarrollador para ver si nos sirve de algo:
Parece que esta bien.. pero queda medio indefinido como es eso de la firma en el datawindow de la carta de negación, porque usa algo de eso en wr_auto_impresion

---

### [25/08/2026 11:59:01] Agente

Sí, Wilmer, el comentario sirve y es correcto: **la firma no está resuelta automáticamente solo por usar `dwc_carta_negacion`**.

Validé el circuito y queda así:

**Evidencia**
- En `wr_auto_impresion.srw`, para autorización negada `au.esta = 'N'`, primero valida requisitos con `prs_carta_negacion_requisitos`.
- Ese SP devuelve tres datos funcionales: rol, firma y estado de carta.
- Si `ls_firma = 'N'`, el proceso se detiene con: `El usuario no tiene firma digital.`
- Si pasa la validación, PowerBuilder ejecuta:

```powerscript
ls_file = lnvo_auto.of_genera_archivo_firma( au.sucur, au.auto, au.origen )
```

Después recupera la carta:

```powerscript
dw_imprimir.retrieve(au.sucur, au.auto)
```

Y luego inyecta la firma en el DataWindow compuesto:

```powerscript
dw_imprimir.object.dw_3.object.file_path[li_cant] = ls_file
```

**Conclusión Técnica**
Para InfoMailer no alcanza con hacer:

```powerscript
lds_carta.DataObject = 'dwc_carta_negacion'
lds_carta.Retrieve(ll_sucur, ll_auto)
lds_carta.SaveAs(...)
```

Porque así podríamos generar una carta sin firma o con el subreporte `dw_3` sin `file_path`.

**Ajuste al diseño**
Cuando InfoMailer genere carta de negación debe replicar esta parte del circuito de `wr_auto_impresion`:

1. Validar carta con `prs_carta_negacion_requisitos`.
2. Si no tiene carta, no generar PDF.
3. Si no está finalizada, no generar PDF.
4. Si no tiene firma o rol, no generar PDF.
5. Generar archivo físico de firma con `uo_nv_valida_auto.of_genera_archivo_firma`.
6. Recuperar `dwc_carta_negacion`.
7. Ejecutar/usar la lógica equivalente a `prs_datos_generales_afilia_carta_nega_imp` para ubicar la fila donde cargar la firma.
8. Setear `dw_3.object.file_path[...] = ls_file`.
9. Recién ahí hacer `SaveAs` a PDF.
10. Subir a API y guardar ruta en `AUTORIZACIONES.archivo_path_full`.

Entonces agregamos un punto más al paso a paso: **la generación de carta en InfoMailer debe incluir explícitamente la inyección de firma en `dw_3.file_path`, igual que `wr_auto_impresion`**.

---

### [25/08/2026 12:40:24] Wilmer Higuera

Lisa, antes de continuar te comento la opinión del funcional:
Me comenta que no está de acuerdo que modifiquemos el InfoMailer, ya que su tarea es enviar los correos, que se puede generar un cuello de botella si además se le adiciona como tarea el generar PDF, que esto se debería hacer en otra parte.
¿Qué opinas Lisa?

---

### [25/08/2026 12:40:39] Agente

Wilmer, estoy de acuerdo con la observación funcional.

Técnicamente, **InfoMailer no debería convertirse en generador masivo de PDFs**. Su responsabilidad natural es tomar mails pendientes, resolver destinatarios/adjuntos ya disponibles y enviarlos. Si le agregamos generación de varias órdenes y cartas por cada mail, queda mezclando tres responsabilidades:

1. envío de correo;
2. generación documental;
3. persistencia/subida de archivos a API.

Eso aumenta el riesgo de cuello de botella y también complica el manejo de errores. Por ejemplo: si falla la firma de una carta, la subida API o la generación de un PDF, el proceso de envío queda trabado con lógica que no le pertenece.

Mi recomendación ajustada sería esta:

**No modificar InfoMailer como generador principal.**

El mejor diseño sería preparar los PDFs antes, en el proceso funcional que arma la notificación, probablemente alrededor de `pri_solicitudes_auto_notif` o en un componente/proceso previo asociado a esa tarea. Ese proceso debería:

- identificar las autorizaciones del proceso 8, 9 o 13;
- generar cada PDF correspondiente;
- para cartas, aplicar la lógica de firma igual que `wr_auto_impresion`;
- subir cada PDF a la API si corresponde;
- guardar la ruta en `AUTORIZACIONES.archivo_path_full`;
- registrar los adjuntos en `MAIL_ATTACH` o `MAIL_ATTACH_BINARY`;
- dejar el mail listo para que InfoMailer solo lo envíe con múltiples adjuntos, usando el circuito que ya existe.

Con ese enfoque, InfoMailer queda limpio: **solo envía**. Y la generación de documentos queda en el flujo funcional de la solicitud, donde además tiene más sentido auditar y controlar reglas de negocio.

Conclusión: cambiaría el rumbo del diseño. Mantendría InfoMailer sin ampliarlo para generar PDFs y buscaría implementar una etapa previa que genere/suba/registre los adjuntos antes de llamar a `afu_mail_envio_send`.

---

### [25/08/2026 13:15:10] Wilmer Higuera

Lisa, si bien, ese proceso de generar los PDF y guardarlos, debemos hacerlo desde la ventana de registro, donde se realice la acción de:
Notificación solicitud R – registrada
Notificación solicitud D – Devuelta
Notificación solicitud G - En Gestión
Notificación solicitud U - Anulada
Que seria desde Registro desde Registrar solicitudes y la bandeja

---

### [25/08/2026 13:17:00] Agente

Wilmer, sí, este enfoque tiene más sentido arquitectónico que cargarle la generación a InfoMailer.

Pero hay una condición técnica importante: **si la generación de PDFs se hace desde la ventana PowerBuilder, la notificación no puede quedar marcada para envío antes de adjuntar los PDFs**, porque InfoMailer podría tomar el mail cuando todavía no están listos los adjuntos.

Hoy el flujo que vi funciona así:

- `wp_soli_reg.srw` llama a `pru_registra_solicitudes_auto`.
- `pru_registra_solicitudes_auto` cambia la solicitud a `R` y llama a `pri_solicitudes_auto_notif`.
- `pri_solicitudes_auto_notif` crea el mail con `afi_mail_envio`, crea destinatario con `afi_mail_to` y luego llama a `afu_mail_envio_send`.
- `afu_mail_envio_send` cambia el estado del mail a `NOE`, dejándolo disponible para InfoMailer.

Para devolución pasa algo parecido:

- `pru_solicitudes_auto_devolucion` cambia a `D`.
- Luego llama a `pri_solicitudes_auto_notif` con proceso `2` o `16`.

Para en gestión:

- `pru_solicitudes_auto_resuelta_pertinencia` cambia a `G`.
- Luego llama a `pri_solicitudes_auto_notif` con proceso `4`.

Para anulada:

- Hay SPs como `pru_solicitudes_auto_anula_bandeja`, `pru_solicitudes_auto_anula_total` y `prs_control_vencimiento_devolucion_soli` que terminan llamando a `pri_solicitudes_auto_notif`.

**Mi Opinión Técnica**
El lugar correcto no es InfoMailer. El lugar correcto es el flujo funcional de la acción: registro/bandeja/fin de gestión/anulación/devolución, porque ahí todavía estamos dentro del contexto de negocio y PowerBuilder puede generar correctamente los PDFs, incluyendo firma, DataWindow, subida API y guardado de ruta.

Pero no lo haría “después” de llamar al SP actual tal como está, porque el SP ya deja el mail listo para envío. Haría un ajuste de diseño así:

1. La acción funcional cambia el estado de la solicitud.
2. Se crea el mail, pero todavía no se marca como `NOE`.
3. Desde PowerBuilder se generan los PDFs requeridos.
4. Se suben a API si corresponde.
5. Se guarda `AUTORIZACIONES.archivo_path_full`.
6. Se registran los adjuntos en `MAIL_ATTACH` o `MAIL_ATTACH_BINARY`.
7. Recién al final se llama a `afu_mail_envio_send`.
8. InfoMailer solo envía.

Entonces la solución más limpia sería **separar en dos etapas lo que hoy hace `pri_solicitudes_auto_notif` junto**:

- etapa 1: crear mail y devolver `mail_id`;
- etapa 2: marcar mail para envío cuando los adjuntos ya están preparados.

Con eso respetamos la responsabilidad de InfoMailer y evitamos el cuello de botella.

---

### [25/08/2026 13:24:11] Wilmer Higuera

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 1:24 PM (America/Buenos_Aires)
Reference UTC: 2026-08-25 16:24 UTC

---

### [25/08/2026 13:24:57] Agente

NO_REPLY

---

### [25/08/2026 13:24:58] Wilmer Higuera

Espera, te di un mal dato, solo necesitamos la ventana desde donde se pasa la solicitud a Finalizada

---

### [25/08/2026 13:25:30] Agente

Correcto, Wilmer. Corregimos el alcance.

Ya no hablamos de R, D, G ni U. Para esta parte solo necesitamos la acción que pasa la solicitud a **Finalizada (`F`)**.

El punto real es:

- Ventana: `exe_presmed/Source Infomedical/wp_soli_reg.srw`
- Evento: `ue_fin_gestion`
- Botón: `cb_fin_gestion`
- SP llamado: `dbo.pru_fin_gestion_solicitudes_auto`

El flujo actual es:

```text
wp_soli_reg.srw
  botón Terminar Gestión
    -> ue_fin_gestion()
       -> EXEC dbo.pru_fin_gestion_solicitudes_auto @prm_soli_auto = :il_soli
```

Y dentro de `pru_fin_gestion_solicitudes_auto` se determina si la solicitud queda en `F`. Si queda finalizada, hoy llama a:

```sql
EXEC dbo.pri_solicitudes_auto_notif
    @prm_id = 8,
    @prm_soli_auto = @prm_soli_auto
```

pero solo para el caso donde todas las autorizaciones están en `A`.

Entonces el nuevo diseño debe concentrarse en **Fin de Gestión**:

1. Usuario presiona **Terminar Gestión** desde `wp_soli_reg`.
2. `pru_fin_gestion_solicitudes_auto` calcula y actualiza la solicitud a `F`.
3. Antes de dejar el mail listo para InfoMailer, se debe preparar la notificación finalizada:
   - proceso 8 si todas son autorizadas;
   - proceso 9 si todas son negadas con carta finalizada;
   - proceso 13 si hay mezcla de autorizadas y negadas con carta finalizada.
4. Los PDFs deben generarse/prepararse en este flujo, no dentro de InfoMailer.
5. InfoMailer queda solo como enviador de correos con adjuntos ya registrados.

No modifiqué archivos.

---

### [25/08/2026 13:28:34] Wilmer Higuera

Si, bien, ahora dame una propuesta detallada, paso a paso

---

### [25/08/2026 13:29:15] Agente

Wilmer, propuesta detallada ajustada al nuevo criterio: **no modificar InfoMailer para generar PDFs**. La generación debe quedar en el flujo de **Terminar Gestión** desde `wp_soli_reg`.

**Objetivo**
Cuando la solicitud pasa a `F` Finalizada, preparar el mail final con todos los PDFs antes de marcarlo para envío. InfoMailer solo debe encontrar el mail pendiente con adjuntos ya registrados.

**Paso 1: Mantener El Punto De Entrada**
El punto de entrada sigue siendo:

```text
wp_soli_reg.srw
  cb_fin_gestion.clicked
    -> ue_fin_gestion()
       -> dbo.pru_fin_gestion_solicitudes_auto
```

No conviene duplicar lógica en otra ventana. La acción funcional real es **Terminar Gestión**.

**Paso 2: Ajustar El SP De Fin De Gestión**
En `pru_fin_gestion_solicitudes_auto`, después de determinar que la solicitud quedó en `F`, hay que reemplazar la lógica actual limitada al proceso 8.

Hoy solo contempla:

```sql
IF @var_soli_esta = 'F'
AND ...
AND todas las autorizaciones son A
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id = 8,
        @prm_soli_auto = @prm_soli_auto
END
```

Debe pasar a determinar tres escenarios:

```text
Todas A       -> proceso 8
Todas N válidas -> proceso 9
Mezcla A/N válida -> proceso 13
```

Reglas:

```text
Autorizada:
AUTORIZACIONES.esta = 'A'
AUTORIZACIONES.baja_fecha IS NULL

Negada válida:
AUTORIZACIONES.esta = 'N'
AUTORIZACIONES.baja_fecha IS NULL
CARTA_NEGACION.final_fecha IS NOT NULL
CARTA_NEGACION.baja_fecha IS NULL
```

**Paso 3: Crear Una Rutina Única De Preparación De Notificación Finalizada**
No pondría toda la lógica dentro de `pru_fin_gestion_solicitudes_auto`, para no ensuciarlo.

Propondría un SP nuevo o rutina equivalente:

```sql
pri_solicitudes_auto_notif_finalizada
```

Responsabilidad:

1. Recibir `@prm_soli_auto`.
2. Detectar si aplica proceso 8, 9 o 13.
3. Crear el mail.
4. Devolver el `mail_id`.
5. Registrar qué autorizaciones deben tener PDF.
6. No dejar el mail listo para envío hasta que los adjuntos estén preparados.

**Paso 4: Separar La Creación Del Mail Del Envío**
Hoy `pri_solicitudes_auto_notif` hace todo junto:

```text
crea MAIL_ENVIO
crea MAIL_TO
llama afu_mail_envio_send
```

Para este caso necesitamos una variante controlada:

```text
crear mail en estado CON
crear destinatarios
devolver mail_id
generar/registrar adjuntos
recién ahí llamar afu_mail_envio_send
```

Esto evita que InfoMailer tome el mail antes de que existan los PDFs.

**Paso 5: Generar Los PDFs Desde PowerBuilder**
La generación debe hacerse en PowerBuilder, no en SQL, porque depende de DataWindows, firma y API.

Desde `wp_soli_reg.srw`, después de ejecutar `pru_fin_gestion_solicitudes_auto`, habría que tener una rutina tipo:

```powerscript
wf_preparar_adjuntos_notif_finalizada(il_soli)
```

Esa rutina debería:

1. Consultar qué mail/proceso/autorizaciones quedaron pendientes de adjunto.
2. Recorrer cada autorización.
3. Si `A.esta = 'A'`, generar orden autorizada.
4. Si `A.esta = 'N'`, generar carta de negación.
5. Registrar cada PDF como adjunto del mail.
6. Marcar el mail para envío al final.

**Paso 6: Reutilizar Lógica De Orden Autorizada**
Para autorizaciones `A`, reutilizar el circuito de `wr_auto_impresion.srw`:

```text
wf_recargar_datos
DataObject dc_orden_prestad_2 o dc_orden_prestad_especial
SaveAs PDF
of_sube_adjunto_gral
pru_auto_arch_path_full
```

No debe inventarse otro DataWindow.

**Paso 7: Reutilizar Lógica De Carta De Negación**
Para autorizaciones `N`, replicar el circuito validado:

```text
prs_carta_negacion_requisitos
validar carta generada/finalizada
validar firma
validar rol
of_genera_archivo_firma
dwc_carta_negacion.retrieve(sucur, auto)
prs_datos_generales_afilia_carta_nega_imp
dw_imprimir.object.dw_3.object.file_path[...] = archivo_firma
SaveAs PDF
of_sube_adjunto_gral
pru_auto_arch_path_full
pru_carta_negacion con solo_impre = 'S'
```

Punto clave: sin inyectar `dw_3.file_path`, la carta puede salir sin firma.

**Paso 8: Registrar Adjuntos Para InfoMailer**
Una vez generado cada PDF, registrar el adjunto en el circuito existente.

Preferencia técnica:

```text
MAIL_ATTACH_BINARY con archivo_path_full
```

Porque ya existe `MAIL_ATTACH_BINARY.archivo_path_full` y el InfoMailer ya sabe bajar adjuntos desde API mediante `wf_get_attach_file`.

Alternativa:

```text
MAIL_ATTACH
```

Solo si se deja una ruta local accesible al InfoMailer, lo cual es más frágil.

**Paso 9: Recién Ahí Marcar Mail Para Envío**
Cuando todos los PDFs estén generados, subidos y registrados:

```sql
EXEC dbo.afu_mail_envio_send @prm_mail_id = @mail_id
```

Si falla un PDF:

```text
No llamar afu_mail_envio_send
Dejar log/auditoría funcional
No enviar mail parcial
```

**Paso 10: Cambios Esperados Por Objeto**
Objetos a tocar conceptualmente:

```text
wp_soli_reg.srw
```

Agregar rutina PowerBuilder para preparar adjuntos al terminar gestión.

```text
pru_fin_gestion_solicitudes_auto.sql
```

Cambiar la lógica final para contemplar procesos 8, 9 y 13.

```text
pri_solicitudes_auto_notif.sql
```

Adaptar o parametrizar para permitir crear mail sin enviarlo inmediatamente.

Posible nuevo SP:

```text
pri_solicitudes_auto_notif_finalizada.sql
```

Para encapsular detección de proceso 8/9/13 y creación controlada del mail.

Posible nuevo SP de consulta:

```text
prs_solicitudes_auto_adjuntos_finalizada.sql
```

Para que PowerBuilder recupere las autorizaciones a imprimir con:

```text
sucur
auto
esta
tipo
origen
archivo_path_full
carta_negacion
carta_finalizada
```

**Validaciones Funcionales**
1. Solicitud finalizada con todas autorizadas: debe disparar proceso 8.
2. Solicitud finalizada con todas negadas y cartas finalizadas: debe disparar proceso 9.
3. Solicitud finalizada mixta: debe disparar proceso 13.
4. Solicitud con negada sin carta finalizada: no debe enviar mail incompleto.
5. Carta sin firma válida: no debe generar/enviar.
6. PDF ya existente en `AUTORIZACIONES.archivo_path_full`: definir si se reutiliza o se regenera; por consistencia, primero reutilizar si existe y es accesible.
7. InfoMailer debe enviar el mail usando adjuntos ya registrados, sin generar nada.

**Conclusión**
La solución más sólida es mover la generación al flujo de **Terminar Gestión** en `wp_soli_reg`, crear el mail en estado no enviable, preparar todos los PDFs y adjuntos, y solo al final habilitar el envío. InfoMailer queda sin cambio estructural y sin carga documental.

---

### [25/08/2026 13:46:18] Wilmer Higuera

Recuerda que este cambio no debe afectar o dañar estos procesos que no llevan adjuntos y están funcionando:
Notificación solicitud R – registrada
Notificación solicitud D – Devuelta
Notificación solicitud G - En Gestión
Notificación solicitud U - Anulada

---

### [25/08/2026 13:46:31] Agente

Wilmer, confirmado. Ese es un criterio obligatorio del diseño.

La modificación debe quedar **encapsulada solo para solicitud Finalizada (`F`)** y específicamente para los procesos:

```text
8  -> finalizada con órdenes autorizadas
9  -> finalizada con cartas de negación
13 -> finalizada mixta: órdenes autorizadas + cartas de negación
```

No debe tocar ni alterar el comportamiento de:

```text
R -> Notificación solicitud registrada
D -> Notificación solicitud devuelta
G -> Notificación solicitud en gestión
U -> Notificación solicitud anulada
```

Para garantizarlo, el ajuste no debería cambiar la lógica general de `pri_solicitudes_auto_notif` para todos los procesos. Lo correcto sería aplicar una rama específica, condicionada por:

```sql
@var_soli_esta = 'F'
```

y por los `@prm_id` involucrados:

```sql
@prm_id IN (8, 9, 13)
```

Criterio de seguridad:

```text
Los procesos 1, 2, 3, 4, 6, 7, 14, 15, 16, 17, 18, 22, 23, 24, 25 y 26 deben seguir creando y enviando mail igual que hoy, sin adjuntos automáticos nuevos.
```

Entonces el diseño queda así:

```text
Solo Finalizada:
  generar/preparar adjuntos
  registrar adjuntos
  habilitar envío

R/D/G/U:
  flujo actual intacto
  crear mail
  destinatarios
  afu_mail_envio_send
  InfoMailer envía como siempre
```

No vamos a modificar nada común que pueda cambiar el flujo de esas notificaciones funcionando.

---

### [25/08/2026 13:52:56] Wilmer Higuera

Otro punto para que lo adiciones al proceso si hace falta, en el SP pri_solicitudes_auto_notif, realizamos la adición de esta parte:
F @prm_id = 8
 AND @var_dest_tipo = 'A'
 BEGIN
 INSERT INTO MAIL_AUTORIZACIONES
 (
 mail_id,
 sucur,
 auto,
 alta_fecha,
 modi_fecha,
 baja_fecha
 )
 SELECT
 mail_id = @out_mail_id,
 sucur = SAA.sucur,
 auto = SAA.auto,
 alta_fecha = GETDATE(),
 modi_fecha = GETDATE(),
 baja_fecha = NULL
 FROM SOLICITUDES_AUTO_AUTORIZACIONES SAA
 INNER JOIN AUTORIZACIONES A
 ON A.sucur = SAA.sucur
 AND A.auto = SAA.auto
 WHERE SAA.soli_auto = @prm_soli_auto
 AND SAA.baja_fecha IS NULL
 AND A.baja_fecha IS NULL
 AND A.esta = 'A'
 AND NOT EXISTS (
 SELECT 1
 FROM MAIL_AUTORIZACIONES MA
 WHERE MA.mail_id = @out_mail_id
 AND MA.sucur = SAA.sucur
 AND MA.auto = SAA.auto
 AND MA.baja_fecha IS NULL
 )

 IF EXISTS (
 SELECT 1
 FROM MAIL_AUTORIZACIONES MA
 WHERE MA.mail_id = @out_mail_id
 AND MA.baja_fecha IS NULL
 )
 BEGIN
 UPDATE MAIL_ENVIO
 SET genera_impresion_auto = 'S'
 WHERE mail_id = @out_mail_id
 END
 END
¿Lo necesitamos, y si se necesita, se necesita el otro para cartas negadas?

---

### [25/08/2026 13:53:30] Agente

Wilmer, con el diseño nuevo **ese bloque ya no lo necesitamos así**.

Ese bloque servía para el diseño anterior, donde InfoMailer iba a ver:

```sql
MAIL_ENVIO.genera_impresion_auto = 'S'
```

y con eso iba a generar automáticamente los PDFs desde `MAIL_AUTORIZACIONES`.

Pero ahora decidimos otra arquitectura:

```text
PowerBuilder genera/sube los PDFs antes
PowerBuilder registra los adjuntos
InfoMailer solo envía
```

Entonces, si dejamos ese bloque tal como está, estaríamos reactivando el camino viejo:

```sql
UPDATE MAIL_ENVIO
SET genera_impresion_auto = 'S'
```

Eso le dice al InfoMailer: “generá impresión automática”. Justamente es lo que queremos evitar.

**Qué sí necesitamos**
Necesitamos registrar los PDFs generados como adjuntos reales del mail, preferentemente con:

```sql
EXEC dbo.afi_mail_attach_binary
    @prm_mail_id   = @out_mail_id,
    @prm_file_name = @nombre_pdf,
    @prm_full_path = @archivo_path_full
```

Ese SP inserta en `MAIL_ATTACH_BINARY` con `archivo_path_full`. Luego InfoMailer ya sabe recuperarlo con `afs_mail_attach_binary` y bajarlo con `wf_get_attach_file`.

**Para órdenes autorizadas**
No insertar en `MAIL_AUTORIZACIONES` para que InfoMailer genere.

Sí generar el PDF desde PowerBuilder, subirlo a API, guardar `AUTORIZACIONES.archivo_path_full`, y registrar:

```sql
MAIL_ATTACH_BINARY
```

**Para cartas negadas**
Sí, se necesita “el otro”, pero no como `MAIL_AUTORIZACIONES.tipo = 'C'`.

Se necesita el mismo circuito de adjunto real:

```text
generar carta PDF
inyectar firma
subir a API
guardar AUTORIZACIONES.archivo_path_full
registrar en MAIL_ATTACH_BINARY
```

**Conclusión**
No usaría ese bloque en el nuevo diseño.

Lo reemplazaría por esta regla:

```text
Para procesos 8, 9 y 13:
NO actualizar MAIL_ENVIO.genera_impresion_auto = 'S'
NO depender de MAIL_AUTORIZACIONES para que InfoMailer genere
SÍ registrar cada PDF final en MAIL_ATTACH_BINARY
Recién después llamar afu_mail_envio_send
```

Así evitamos tocar la responsabilidad de InfoMailer y no afectamos procesos sin adjuntos.

---

### [25/08/2026 13:58:40] Wilmer Higuera

Bien, dame ahora si el paso a paso con mucho detalle de como vamos a realizar las modificaciones/adiciones en Powerbuilder, todo el proceso completo hasta que ya se envien los email con los adjuntos ya sean de autorizaciones con estado autorizado, la carta de negación, o los dos combinados, paso a paso para iniciar la implementación

---

### [25/08/2026 13:59:22] Agente

Wilmer, este sería el paso a paso completo para iniciar implementación, ya con el diseño corregido: **PowerBuilder genera los PDFs y los registra como adjuntos; InfoMailer solo envía**.

**Alcance**
Solo aplica cuando la solicitud pasa a **Finalizada (`F`)** desde:

```text
wp_soli_reg.srw
  cb_fin_gestion.clicked
    -> ue_fin_gestion()
       -> pru_fin_gestion_solicitudes_auto
```

No debe afectar notificaciones `R`, `D`, `G`, `U`.

**1. Ajustar El Flujo De Fin De Gestión**
En `wp_soli_reg.srw`, el evento `ue_fin_gestion()` hoy ejecuta el SP y luego hace commit:

```powerscript
EXECUTE fin_gestion;
COMMIT;
wf_retrieve_total(TRUE)
```

La modificación conceptual será:

```text
1. Ejecutar pru_fin_gestion_solicitudes_auto.
2. Confirmar que la solicitud quedó en F.
3. Consultar si se generó una notificación finalizada con mail_id.
4. Si hay mail finalizado con adjuntos pendientes:
   - generar PDFs;
   - subir PDFs a API;
   - registrar adjuntos en MAIL_ATTACH_BINARY;
   - habilitar envío del mail.
5. Hacer commit solo si todo el circuito terminó correctamente.
```

**2. Evitar Que El Mail Se Envíe Antes De Tener Adjuntos**
Hoy `pri_solicitudes_auto_notif` llama internamente:

```sql
EXEC dbo.afu_mail_envio_send @prm_mail_id = @out_mail_id
```

Eso deja el mail en `NOE`, disponible para InfoMailer.

Para procesos `8`, `9` y `13`, necesitamos que el mail quede creado pero **no enviado todavía**. Propuesta:

Agregar parámetro opcional a `pri_solicitudes_auto_notif`:

```sql
@prm_marcar_para_envio CHAR(1) = 'S',
@out_mail_id INTEGER = NULL OUTPUT
```

Y cambiar la llamada a `afu_mail_envio_send` para que solo ejecute si:

```sql
@prm_marcar_para_envio = 'S'
```

Así los procesos existentes siguen igual, porque por defecto queda `'S'`.

Para finalizadas con adjuntos, llamaríamos:

```sql
EXEC dbo.pri_solicitudes_auto_notif
    @prm_id = 8 / 9 / 13,
    @prm_soli_auto = @prm_soli_auto,
    @prm_marcar_para_envio = 'N',
    @out_mail_id = @out_mail_id OUTPUT
```

**3. Determinar El Proceso Correcto**
En `pru_fin_gestion_solicitudes_auto`, cuando la solicitud queda en `F`, determinar:

```text
Proceso 8:
todas las autorizaciones vigentes están en A.

Proceso 9:
todas las autorizaciones vigentes están en N
y todas las N tienen carta de negación finalizada.

Proceso 13:
hay al menos una autorización A
y al menos una autorización N con carta de negación finalizada.
```

No usar `MAIL_AUTORIZACIONES.tipo` como criterio principal.

Criterios SQL:

```sql
A.baja_fecha IS NULL
SAA.baja_fecha IS NULL
A.esta = 'A'
```

Para carta:

```sql
A.esta = 'N'
CN.final_fecha IS NOT NULL
CN.baja_fecha IS NULL
```

**4. Crear Consulta Para Adjuntos Pendientes**
Crear un SP de apoyo, por ejemplo:

```text
prs_solicitudes_auto_adjuntos_finalizada
```

Debe devolver a PowerBuilder las autorizaciones a imprimir:

```text
mail_id
soli_auto
sucur
auto
esta
tipo_autorizacion
origen
archivo_path_full
carta_negacion
carta_finalizada
```

La lógica:

```text
A.esta = 'A' -> generar orden autorizada
A.esta = 'N' -> generar carta de negación
```

**5. Agregar Rutina PowerBuilder Principal**
En `wp_soli_reg.srw`, agregar función/evento interno:

```powerscript
wf_preparar_adjuntos_notif_finalizada(al_soli_auto, al_mail_id)
```

Responsabilidad:

```text
1. Recuperar filas desde prs_solicitudes_auto_adjuntos_finalizada.
2. Recorrer cada autorización.
3. Según esta:
   A -> wf_generar_adjunto_orden(...)
   N -> wf_generar_adjunto_carta_negacion(...)
4. Si todas salen bien:
   llamar afu_mail_envio_send.
5. Si una falla:
   rollback;
   no marcar mail para envío;
   mostrar error técnico.
```

**6. Generar PDF De Orden Autorizada**
Crear rutina PowerBuilder:

```powerscript
wf_generar_adjunto_orden(ai_sucur, al_auto, as_arch_path_full, as_file_name)
```

Debe replicar la lógica de `wr_auto_impresion.srw`:

```text
1. Crear DataStore/DataWindow.
2. Determinar formato con prs_auto_impresion_especial_opc.
3. Usar dc_orden_prestad_2 o dc_orden_prestad_especial.
4. Recuperar datos con Retrieve(sucur, auto, 'C' o modo correspondiente).
5. Configurar PDF NativePDF.
6. Guardar PDF temporal.
7. Subir con nvo_sube_adjunto.of_sube_adjunto_gral.
8. Guardar ruta con pru_auto_arch_path_full.
9. Registrar en MAIL_ATTACH_BINARY.
10. Borrar temporal local.
```

**7. Generar PDF De Carta De Negación**
Crear rutina PowerBuilder:

```powerscript
wf_generar_adjunto_carta_negacion(ai_sucur, al_auto, as_arch_path_full, as_file_name)
```

Debe replicar el circuito validado de `wr_auto_impresion.srw`:

```text
1. Ejecutar prs_carta_negacion_requisitos.
2. Si no tiene carta: error.
3. Si carta no está finalizada: error.
4. Si no tiene firma: error.
5. Si no tiene rol: error.
6. Generar archivo de firma:
   uo_nv_valida_auto.of_genera_archivo_firma(sucur, auto, origen)
7. Crear DataWindow/DataStore con dwc_carta_negacion.
8. Retrieve(sucur, auto).
9. Ejecutar prs_datos_generales_afilia_carta_nega_imp.
10. Inyectar firma:
    dw_imprimir.object.dw_3.object.file_path[...] = archivo_firma
11. Guardar PDF temporal.
12. Subir con nvo_sube_adjunto.of_sube_adjunto_gral.
13. Guardar ruta con pru_auto_arch_path_full.
14. Marcar carta como impresa con pru_carta_negacion @prm_solo_impre = 'S'.
15. Registrar en MAIL_ATTACH_BINARY.
16. Borrar temporal local.
```

**8. Registrar Adjuntos**
No usar `MAIL_AUTORIZACIONES` para que InfoMailer genere.

Por cada PDF generado o reutilizado, llamar:

```sql
EXEC dbo.afi_mail_attach_binary
    @prm_mail_id   = @mail_id,
    @prm_file_name = @nombre_pdf,
    @prm_full_path = @archivo_path_full
```

InfoMailer ya recupera `MAIL_ATTACH_BINARY` mediante `afs_mail_attach_binary` y descarga el archivo usando `archivo_path_full`.

**9. Habilitar Envío Al Final**
Solo cuando todos los adjuntos estén correctos:

```sql
EXEC dbo.afu_mail_envio_send @prm_mail_id = @mail_id
```

Si falla una orden o carta:

```text
No ejecutar afu_mail_envio_send.
No enviar mail parcial.
Rollback.
Mostrar/loguear causa.
```

**10. Validaciones**
Casos mínimos:

```text
Proceso 8: dos autorizaciones A -> un mail, dos órdenes adjuntas.
Proceso 9: dos autorizaciones N con carta finalizada -> un mail, dos cartas adjuntas.
Proceso 13: una A y una N finalizada -> un mail, orden + carta.
Negada sin carta finalizada -> no envía mail.
Carta sin firma/rol -> no envía mail.
R/D/G/U -> siguen enviando igual que antes, sin adjuntos nuevos.
```

Conclusión técnica: empezaría por modificar primero `pri_solicitudes_auto_notif` para permitir crear mail sin marcar envío, luego `pru_fin_gestion_solicitudes_auto` para detectar `8/9/13`, y recién después agregaría en `wp_soli_reg.srw` las rutinas PowerBuilder de generación, subida y registro de adjuntos.

---

### [25/08/2026 14:54:18] Wilmer Higuera

Lo veo todo bien, Lisa, por favor, pásame el dato resumido para un informe y luego para iniciar la modificación pásame un paso a paso muy detallado, de como estaba antes el código a modificar y después de la modificación, me muestras bien detallado de lo que debo hacer, el sitio, evento SP, todo el paso a pado con el código que vamos a modificar/adicionar

---

### [25/08/2026 14:56:05] Agente

Wilmer, informe resumido y guía de implementación. No se modifica InfoMailer como generador de PDFs.

**Informe Resumido**
La solución cambia el diseño inicial: los PDFs de órdenes autorizadas y cartas de negación se deben generar desde el flujo funcional de **Terminar Gestión** en `wp_soli_reg.srw`, antes de liberar el mail para envío. InfoMailer queda con su responsabilidad actual: leer mails pendientes y enviar adjuntos ya registrados.  
El cambio solo aplica para solicitud **Finalizada (`F`)**, procesos `8`, `9` y `13`. No debe afectar notificaciones `R`, `D`, `G` ni `U`.

**Objetos Afectados**
- `exe_presmed/Source Infomedical/wp_soli_reg.srw`
- `backend/procedures/pru_fin_gestion_solicitudes_auto.sql`
- `backend/procedures/pri_solicitudes_auto_notif.sql`
- Nuevo SP propuesto: `prs_solicitudes_auto_adjuntos_finalizada.sql`

**Paso 1: Ajustar `pri_solicitudes_auto_notif`**
Antes:

```sql
CREATE PROCEDURE dbo.pri_solicitudes_auto_notif (
    @prm_id         SMALLINT,
    @prm_soli_auto  NUMERIC(18)
)
```

Después:

```sql
CREATE PROCEDURE dbo.pri_solicitudes_auto_notif (
    @prm_id                 SMALLINT,
    @prm_soli_auto          NUMERIC(18),
    @prm_marcar_para_envio  CHAR(1) = 'S',
    @out_mail_id            INTEGER = NULL OUTPUT
)
```

Antes, siempre libera el mail:

```sql
EXEC dbo.afu_mail_envio_send
    @prm_mail_id = @out_mail_id
```

Después:

```sql
IF @prm_marcar_para_envio = 'S'
BEGIN
    EXEC dbo.afu_mail_envio_send
        @prm_mail_id = @out_mail_id
END
```

Impacto: todos los procesos existentes siguen igual porque el valor default es `'S'`.

**Paso 2: Ajustar `pru_fin_gestion_solicitudes_auto`**
Agregar salidas:

```sql
@out_prm_id_notif  SMALLINT = NULL OUTPUT,
@out_mail_id_afi   INTEGER = NULL OUTPUT
```

Reemplazar la lógica actual que solo contempla proceso `8`.

Antes:

```sql
IF @var_soli_esta = 'F'
AND ISNULL(@var_soli_esta_anterior, '') <> 'F'
AND @var_tipo_usuario_soli IN ('A', 'E')
AND ... todas A ...
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id = 8,
        @prm_soli_auto = @prm_soli_auto
END
```

Después, calcular proceso:

```sql
DECLARE @var_cant_total INTEGER,
        @var_cant_a INTEGER,
        @var_cant_n INTEGER,
        @var_cant_n_carta_final INTEGER,
        @var_prm_id_notif SMALLINT

SELECT @var_cant_total = COUNT(1)
FROM SOLICITUDES_AUTO_AUTORIZACIONES SAA
INNER JOIN AUTORIZACIONES A
ON A.sucur = SAA.sucur
AND A.auto = SAA.auto
WHERE SAA.soli_auto = @prm_soli_auto
AND SAA.baja_fecha IS NULL
AND A.baja_fecha IS NULL
AND A.esta IN ('A', 'N')
```

Luego determinar:

```sql
-- Proceso 8: todas autorizadas
-- Proceso 9: todas negadas con carta finalizada
-- Proceso 13: mezcla A/N con carta finalizada
```

Y llamar:

```sql
EXEC dbo.pri_solicitudes_auto_notif
    @prm_id = @var_prm_id_notif,
    @prm_soli_auto = @prm_soli_auto,
    @prm_marcar_para_envio = 'N',
    @out_mail_id = @out_mail_id_afi OUTPUT

SET @out_prm_id_notif = @var_prm_id_notif
```

**Paso 3: Crear Consulta De Adjuntos**
Nuevo SP:

```sql
CREATE PROCEDURE dbo.prs_solicitudes_auto_adjuntos_finalizada (
    @prm_soli_auto NUMERIC(18),
    @prm_mail_id INTEGER
)
AS
BEGIN
    SELECT
        mail_id = @prm_mail_id,
        SAA.sucur,
        SAA.auto,
        A.esta,
        A.tipo,
        A.origen,
        A.archivo_path_full,
        carta_negacion = CN.carta_negacion,
        carta_finalizada = CASE
            WHEN CN.final_fecha IS NOT NULL AND CN.baja_fecha IS NULL THEN 'S'
            ELSE 'N'
        END
    FROM SOLICITUDES_AUTO_AUTORIZACIONES SAA
    INNER JOIN AUTORIZACIONES A
    ON A.sucur = SAA.sucur
    AND A.auto = SAA.auto
    LEFT JOIN CARTA_NEGACION CN
    ON CN.sucur = A.sucur
    AND CN.auto = A.auto
    AND CN.baja_fecha IS NULL
    WHERE SAA.soli_auto = @prm_soli_auto
    AND SAA.baja_fecha IS NULL
    AND A.baja_fecha IS NULL
    AND A.esta IN ('A', 'N')
END
GO
```

**Paso 4: Ajustar `wp_soli_reg.srw` Evento `ue_fin_gestion`**
Antes:

```powerscript
DECLARE fin_gestion PROCEDURE FOR dbo.pru_fin_gestion_solicitudes_auto
    @prm_soli_auto = :il_soli
USING SQLCA;

EXECUTE fin_gestion;

COMMIT;
SQLCA.autocommit = TRUE
wf_retrieve_total(TRUE)
```

Después:

```powerscript
LONG ll_mail_id
INTEGER li_prm_id_notif, li_rv

DECLARE fin_gestion PROCEDURE FOR dbo.pru_fin_gestion_solicitudes_auto
    @prm_soli_auto      = :il_soli,
    @out_prm_id_notif   = :li_prm_id_notif OUTPUT,
    @out_mail_id_afi    = :ll_mail_id OUTPUT
USING SQLCA;

EXECUTE fin_gestion;

IF SQLCA.sqlcode < 0 THEN
    ROLLBACK;
    mBox(THIS, SQLCA.sqlerrtext, 2, 1)
    CLOSE fin_gestion;
    RETURN
END IF

CLOSE fin_gestion;

IF li_prm_id_notif = 8 OR li_prm_id_notif = 9 OR li_prm_id_notif = 13 THEN
    li_rv = wf_preparar_adjuntos_notif_finalizada(il_soli, ll_mail_id, li_prm_id_notif)

    IF li_rv < 0 THEN
        ROLLBACK;
        SQLCA.autocommit = TRUE
        RETURN
    END IF
END IF

COMMIT;
SQLCA.autocommit = TRUE
wf_retrieve_total(TRUE)
```

**Paso 5: Agregar Función PowerBuilder Principal**
En `wp_soli_reg.srw`:

```powerscript
public function integer wf_preparar_adjuntos_notif_finalizada (long al_soli_auto, long al_mail_id, integer ai_prm_id_notif)
```

Debe hacer:

```text
1. Recuperar autorizaciones con prs_solicitudes_auto_adjuntos_finalizada.
2. Por cada fila:
   - si esta = 'A', llamar wf_generar_adjunto_orden
   - si esta = 'N', llamar wf_generar_adjunto_carta_negacion
3. Registrar cada PDF en MAIL_ATTACH_BINARY.
4. Si todo terminó bien, ejecutar afu_mail_envio_send.
5. Si algo falla, devolver -1 y no enviar el mail.
```

**Paso 6: Función Para Orden Autorizada**
Agregar:

```powerscript
public function integer wf_generar_adjunto_orden (integer ai_sucur, long al_auto, ref string as_file_name, ref string as_arch_path_full)
```

Debe replicar `wr_auto_impresion.srw`:
- usar `prs_auto_impresion_especial_opc`
- usar `dc_orden_prestad_2` o `dc_orden_prestad_especial`
- `Retrieve(ai_sucur, al_auto, 'C')`
- `SaveAs` PDF temporal
- `nvo_sube_adjunto.of_sube_adjunto_gral`
- `pru_auto_arch_path_full`
- devolver nombre y ruta API

**Paso 7: Función Para Carta De Negación**
Agregar:

```powerscript
public function integer wf_generar_adjunto_carta_negacion (integer ai_sucur, long al_auto, string as_origen, ref string as_file_name, ref string as_arch_path_full)
```

Debe replicar `wr_auto_impresion.srw`:
- ejecutar `prs_carta_negacion_requisitos`
- validar carta generada/finalizada
- validar firma y rol
- generar firma con `uo_nv_valida_auto.of_genera_archivo_firma`
- usar `dwc_carta_negacion`
- `Retrieve(ai_sucur, al_auto)`
- inyectar firma en `dw_3.object.file_path[...]`
- `SaveAs` PDF
- subir a API
- guardar `AUTORIZACIONES.archivo_path_full`
- marcar carta impresa con `pru_carta_negacion @prm_solo_impre = 'S'`

**Paso 8: Registrar Cada Adjunto**
Por cada PDF generado:

```sql
EXEC dbo.afi_mail_attach_binary
    @prm_mail_id = @out_mail_id,
    @prm_file_name = @var_file_name,
    @prm_full_path = @var_archivo_path_full
```

No usar:

```sql
UPDATE MAIL_ENVIO
SET genera_impresion_auto = 'S'
```

**Paso 9: Liberar El Mail**
Solo al final:

```sql
EXEC dbo.afu_mail_envio_send
    @prm_mail_id = @out_mail_id
```

**Validación**
- Proceso 8: varias `A`, un mail con varias órdenes.
- Proceso 9: varias `N`, un mail con varias cartas.
- Proceso 13: mezcla `A` y `N`, un mail con ambos tipos.
- Si falla firma, carta o subida API: rollback y no se envía parcial.
- R/D/G/U sin cambios.

Quedo esperando tu visto bueno del **Paso 1: `pri_solicitudes_auto_notif`** para avanzar recién con el detalle del primer bloque exacto.

---

### [25/08/2026 16:50:42] Wilmer Higuera

Lisa, ya envié la propuesta para que realicemos los cambios, mientras tanto ayúdame a validar unos hallazgos del funcional en los proceso que ya tenemos como listos, en el documento R29489-TEST_20260727.pdf en A:\whiguera\Tarea #23986\Tarea #29489 en el Caso 5, (ERROR - CASO 5)

---

### [25/08/2026 16:58:10] Agente

Wilmer, validé el **ERROR - CASO 5** contra el PDF y el código local. No hice modificaciones.

**Resumen para informe**

El hallazgo original del Caso 5 parece estar **parcialmente corregido en código**: actualmente `pru_registra_solicitudes_auto` llama a `pri_solicitudes_auto_notif` para notificar según origen/tipo de usuario, y `pri_solicitudes_auto_notif` ya registra `mail_id_prestad_mail`, `mail_id_afi_mail` y auditoría por cada mail enviado.

Lo que sí queda vigente como hallazgo técnico es la **variable de solicitudes hospitalarias vacía** en el cuerpo del mail:

```html
<strong>Solicitudes Hospitalarias:</strong> horas hábiles
```

La causa probable no está en el `REPLACE`, porque el SP sí reemplaza `@tiempo_gestion_hosp`. El problema es que `@var_tiempo_gestion_hosp` queda `NULL` si no existe una fila activa en `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP` para esta combinación:

```sql
soli_tipo = 'H'
origen = S.origen
prestad_vip = ISNULL(P.vip, 'N')
afi_tipo = CASE ...
baja_fecha IS NULL
```

Archivos revisados:
- [pri_solicitudes_auto_notif.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pri_solicitudes_auto_notif.sql:302)
- [pru_registra_solicitudes_auto.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/procedures/pru_registra_solicitudes_auto.sql:498)
- [SOLICITUDES_AUTO_TIEMPO_GESTION_ESP.sql](/home/desarrollo-ia-05/.openclaw/workspace/svn/whiguera-Version84-Modelo2Gis/backend/tables/SOLICITUDES_AUTO_TIEMPO_GESTION_ESP.sql:1)

**Conclusión técnica**

1. El envío a Prestador/Afiliado del Caso 5 está encaminado:
   - Si `@prm_origen = 'G'`, se toma como Prestador y dispara proceso `1` al prestador y proceso `22` al afiliado.
   - Si `@prm_origen = 'P'`, se toma como Ejecutivo y dispara proceso `7` al afiliado.
   - Si el origen es otro, cae como Afiliado y dispara proceso `7`.

2. La auditoría y relación con `SOLICITUDES_AUTO` ya están contempladas:
   - Guarda `mail_id_prestad_mail`
   - Guarda `mail_id_afi_mail`
   - Ejecuta `pri_solicitudes_auto_auditoria`

3. El error vigente es de datos/configuración o de fallback:
   - Falta la configuración hospitalaria en `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP`, o
   - El SP debería usar `tiempo_oport_h` como respaldo cuando no encuentre configuración específica.

**Validación pendiente en QA**

Antes de tocar código, pediría validar en `InstallQA` con la solicitud `25920128`:

```sql
SELECT
    SA.soli_auto,
    SA.soli_tipo,
    SA.origen,
    SA.vip,
    SA.coomeva,
    SA.pro_riesgo,
    SA.arquetipo,
    PR.vip AS prestad_vip,
    afi_tipo = CASE
        WHEN SA.vip = 'S' THEN 'V'
        WHEN SA.coomeva = 'S' THEN 'P'
        WHEN SA.pro_riesgo = 'S' THEN 'R'
        WHEN SA.arquetipo = 'S' THEN 'A'
        ELSE 'O'
    END
FROM SOLICITUDES_AUTO SA
LEFT JOIN PRESTADORES PR
ON PR.prestad = SA.prestad
WHERE SA.soli_auto = 25920128
```

Y luego:

```sql
SELECT *
FROM SOLICITUDES_AUTO_TIEMPO_GESTION_ESP
WHERE soli_tipo = 'H'
AND baja_fecha IS NULL
```

Mi recomendación: primero confirmar si falta el registro de configuración. Si falta, probablemente sea **corrección de datos parametrizados**. Si funcionalmente quieren que nunca salga vacío aunque falte matriz específica, entonces propondría ajustar `pri_solicitudes_auto_notif` con fallback a `tiempo_oport_h`.

---

### [25/08/2026 17:10:40] Wilmer Higuera

[User sent media without caption]

---

### [25/08/2026 17:11:54] Agente

Wilmer, la captura confirma el hallazgo técnico.

**Evidencia**
- La solicitud `25920128` tiene:
  - `soli_tipo = 'D'`
  - `origen = 'P'`
  - `coomeva = 'S'`
  - `afi_tipo = 'P'`
  - `prestad_vip = 'N'`

- La segunda consulta para configuración hospitalaria `soli_tipo = 'H'` no devuelve registros.

**Conclusión**
El texto vacío:

```html
Solicitudes Hospitalarias: horas hábiles
```

se explica porque `@var_tiempo_gestion_hosp` queda `NULL`.

Pero hay un punto más importante: **este caso puntual no es hospitalario**, es `soli_tipo = 'D'`. Entonces el Caso 5 no debería depender de la configuración `H` para su valor principal. Si la plantilla muestra tiempos de cirugía, diagnóstico y hospitalarias como texto general, entonces falta parametrización `H`; pero si el funcional espera el tiempo específico de la solicitud, para esta solicitud debería evaluarse la matriz de `D`.

**Diagnóstico**
- No parece error del `REPLACE`.
- No parece error de envío.
- Es un problema de dato/configuración o de diseño de fallback.
- Para esta solicitud, la matriz relevante por tipo real sería:

```sql
soli_tipo = 'D'
origen = 'P'
afi_tipo = 'P'
prestad_vip = 'N'
baja_fecha IS NULL
```

**Recomendación para el informe**
El ERROR - CASO 5 queda validado como inconsistencia de parametrización/fallback en tiempos de gestión. La solicitud usada es diagnóstica (`D`), pero el campo de solicitudes hospitalarias queda vacío porque no existe configuración vigente para `H`. Se debe definir funcionalmente si la plantilla debe mostrar siempre los tres tiempos con configuración obligatoria para `Q`, `D` y `H`, o si debe mostrar únicamente el tiempo correspondiente al `soli_tipo` de la solicitud.

---

### [25/08/2026 17:14:14] Wilmer Higuera

Entonces cual es la respuesta para el funcional?

---

### [25/08/2026 17:14:24] Agente

Wilmer, la respuesta para el funcional sería:

El **ERROR - CASO 5** no corresponde a un problema de envío ni de reemplazo de variables en el SP. El mail se genera y se envía correctamente.

La causa validada es que la plantilla muestra el bloque:

```text
Solicitudes Hospitalarias: horas hábiles
```

pero el valor queda vacío porque no existe parametrización vigente para solicitudes hospitalarias (`soli_tipo = 'H'`) en `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP`.

Además, la solicitud del caso probado `25920128` no es hospitalaria, sino diagnóstica:

```text
soli_tipo = 'D'
origen = 'P'
afi_tipo = 'P'
prestad_vip = 'N'
```

Por lo tanto, necesitamos definición funcional:

1. Si la plantilla debe mostrar siempre los tres tiempos: cirugía, diagnóstico y hospitalarias, entonces falta parametrizar también el tiempo hospitalario.
2. Si la plantilla debe mostrar solo el tiempo correspondiente al tipo real de la solicitud, entonces se debe ajustar la plantilla/lógica para no mostrar el bloque hospitalario cuando la solicitud no sea `H`.

Mi recomendación técnica: tratarlo como **parametrización faltante o definición funcional de plantilla**, no como error del proceso de envío.

---

### [25/08/2026 17:18:58] Wilmer Higuera

Espera, tengo una duda, si la solicitud es diagnóstica, no debió sacar uno de los valores para diagnóstica qu están en la imagen?

---

### [25/08/2026 17:20:09] Agente

Sí, Wilmer. Tu duda es correcta.

Para una solicitud diagnóstica con:

```text
soli_tipo = D
origen = P
afi_tipo = P
prestad_vip = N
```

el SP **sí debería tomar un valor diagnóstico** desde la tabla de la imagen. Con esos datos, por el `ORDER BY` actual, tomaría primero colorimetría `R`, o sea:

```text
D / P / P / N / R -> tiempo_gestion = 17
```

Pero el valor que vimos vacío era este:

```text
Solicitudes Hospitalarias: horas hábiles
```

Ese no usa las filas `D`; usa la variable:

```sql
@var_tiempo_gestion_hosp
```

y el SP la busca con:

```sql
SATGE.soli_tipo = 'H'
```

En tu imagen no hay registros `H`; hay registros:

```text
D
Q
T
```

Entonces el punto fino es este:

- Si el vacío está en **Diagnósticas**, entonces sí sería error, porque debería traer `17`.
- Si el vacío está en **Hospitalarias**, el problema es que el SP busca `H`, pero la tabla no tiene `H`.
- Si funcionalmente `T` representa Hospitalarias, entonces el SP está usando mal el código y debería buscar `T` en vez de `H`.
- Si Hospitalarias realmente debe ser `H`, entonces falta parametrización para `H`.

Respuesta para el funcional:

> La solicitud diagnóstica sí tiene parametrización y debería mostrar el tiempo diagnóstico, que para la combinación validada sería 17 horas hábiles. El valor vacío observado corresponde al bloque de Hospitalarias, no al diagnóstico. Para Hospitalarias el SP busca `soli_tipo = 'H'`, pero en la parametrización actual no existen filas `H`; solo hay `D`, `Q` y `T`. Se debe confirmar si Hospitalarias corresponde a código `H` o `T`. Según esa definición, el ajuste será de parametrización o de código.

---

### [25/08/2026 17:24:23] Wilmer Higuera

Espera, olvida que el funcional dijo que era hospitalaria, si miras los datos de esa autorización, que tipo es y debió salir vacia?

---

### [25/08/2026 17:24:53] Wilmer Higuera

Espera, olvida que el funcional dijo que era hospitalaria, si miras los datos de esa autorización, que tipo es y debió salir vacia?

---

### [25/08/2026 17:24:53] Agente

Wilmer, olvidando lo de “hospitalaria”: con los datos validados, esa solicitud/autorización corresponde a **Diagnóstica**:

```text
soli_tipo = 'D'
origen = 'P'
afi_tipo = 'P'
prestad_vip = 'N'
```

Entonces **la línea de Diagnósticas no debió salir vacía**. Para esa combinación sí había parametrización en la imagen, y por lo revisado debía tomar el valor diagnóstico, probablemente:

```text
D / P / P / N / R -> 17 horas hábiles
```

Lo que sí podía salir vacío era la línea de **Hospitalarias**, porque el SP busca `soli_tipo = 'H'` y en la parametrización vista no había registros `H`, solo `D`, `Q` y `T`.

Respuesta corta para el funcional:

> Revisando los datos reales del caso, la solicitud no es hospitalaria, es diagnóstica (`soli_tipo = D`). Por lo tanto, el tiempo de Diagnósticas no debería salir vacío; debería tomar el valor parametrizado para `D / P / P / N`, que según la tabla corresponde a 17 horas hábiles. Si lo que salió vacío fue Hospitalarias, eso se explica porque no hay parametrización con `soli_tipo = H`, pero eso no afecta que Diagnósticas sí debía informarse.

---

### [25/08/2026 17:29:36] Wilmer Higuera

Ahora como debió salir 17 ya que era diagnostica, ¿Qué tenemos mal y debemos coregir?

---

### [25/08/2026 17:30:52] Wilmer Higuera

Ahora como debió salir 17 ya que era diagnostica, ¿Qué tenemos mal y debemos coregir?

---

### [25/08/2026 17:30:52] Agente

Wilmer: esa solicitud es **Diagnóstica (`soli_tipo = 'D'`)**, por lo tanto **el valor diagnóstico no debió salir vacío**. Debió salir `17 horas hábiles` si existe activa la fila `D / P / P / N / R`.

Lo que tenemos mal puede ser una de estas dos cosas:

1. **Parametrización exacta no encontrada en QA**
   El SP `pri_solicitudes_auto_notif` busca el tiempo diagnóstico con coincidencia exacta:

```sql
SATGE.soli_tipo = 'D'
SATGE.origen = S.origen
SATGE.prestad_vip = ISNULL(P.vip, 'N')
SATGE.afi_tipo = CASE ...
SATGE.baja_fecha IS NULL
```

Si alguna columna no coincide exactamente, `@var_tiempo_gestion_dx` queda `NULL` y el mail sale vacío.

2. **Dato real distinto al asumido**
   Aunque vimos que la solicitud parece `D / P / P / N`, hay que validar contra la misma base donde se generó el mail. Si en esa base no existe activa la fila `D / P / P / N / R`, o si `P.vip` no es `N`, no va a traer 17.

Lo que debemos corregir depende de esa validación:

```sql
SELECT *
FROM SOLICITUDES_AUTO_TIEMPO_GESTION_ESP
WHERE soli_tipo = 'D'
AND origen = 'P'
AND afi_tipo = 'P'
AND prestad_vip = 'N'
AND baja_fecha IS NULL
ORDER BY CASE colorimetria
    WHEN 'R' THEN 1
    WHEN 'A' THEN 2
    WHEN 'V' THEN 3
    ELSE 4
END
```

Si esa consulta devuelve `17`, entonces el problema está en cómo el SP calcula alguno de los criterios desde `SOLICITUDES_AUTO`/`PRESTADORES`.  
Si no devuelve nada, la corrección es de **parametrización**.

Respuesta al funcional:

> La autorización corresponde a una solicitud Diagnóstica (`D`), por lo tanto el tiempo diagnóstico no debía salir vacío. Para la combinación validada debería tomar 17 horas hábiles. El error está en que `pri_solicitudes_auto_notif` no encontró una parametrización activa exacta para `D / P / P / N`, o alguno de esos datos reales no coincide en QA. Debemos corregir la parametrización o ajustar el criterio de búsqueda/fallback del SP para que no deje vacío el valor diagnóstico.

---

### [25/08/2026 17:39:09] Wilmer Higuera

Espera, si encontró datos, te voy a pedir hagas una análisis completo, donde tomes los datos de la solicitud y realices las consultas contra la base de datos y me indiques donde fallo y porque

---

### [25/08/2026 17:42:57] Agente

Wilmer, validé contra `InstallQA` y la conclusión cambia un poco: **para Diagnóstica no falló**. El mail generado sí tiene `17 horas hábiles` en la línea diagnóstica.

**Datos Reales De La Solicitud**
Solicitud: `25920128`

Resultado consultado:

```text
soli_tipo = D
origen = P
soli_vip = N
coomeva = S
pro_riesgo = N
arquetipo = N
prestad = 32
prestad_vip = N
afi_tipo_calculado = P
mail_id_afi_mail = 9989102
```

**Parametrización Encontrada**
Para `D / P / P / N` existen datos activos:

```text
D / P / P / N / R -> 17
D / P / P / N / A -> 16
D / P / P / N / V -> 8
```

Por el `ORDER BY` del SP, toma primero `R`, entonces el valor esperado para diagnóstico es:

```text
17 horas hábiles
```

**Reproducción Del SP**
La lógica de `pri_solicitudes_auto_notif` devuelve:

```text
tiempo_ciru   = 25
tiempo_dx     = 17
tiempo_hosp_H = NULL
tiempo_T      = 24
```

**Mail Grabado**
El mail `9989102` quedó así:

```text
Solicitudes Quirúrgicas: 25 horas hábiles
Solicitudes Procedimientos Diagnósticos y/o Tratamientos Ambulatorios: 17 horas hábiles
Solicitudes Hospitalarias:  horas hábiles
```

**Dónde Falló**
No falló la búsqueda diagnóstica.  
El fallo está en la línea de **Hospitalarias**.

El SP busca hospitalarias así:

```sql
SATGE.soli_tipo = 'H'
```

Pero en la tabla no hay parametrización activa para `H`:

```text
cant_H = 0
```

Sí hay registros para `T`:

```text
cant_T = 3
```

Pero `SOLICITUDES_AUTO_TIPOS` dice:

```text
H = Solicitud Hospitalaria
T = Sin tipificar
```

Entonces no corresponde asumir técnicamente que `T` sea hospitalaria.

**Qué Debemos Corregir**
Para este caso puntual, **no debemos corregir Diagnóstica**, porque salió bien con `17`.

Lo que debemos corregir es una de estas dos alternativas, según definición funcional:

1. Si la plantilla debe mostrar siempre Quirúrgicas, Diagnósticas y Hospitalarias:  
   falta parametrizar `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP` para `soli_tipo = 'H'`.

2. Si no debe mostrarse una línea sin valor:  
   ajustar `pri_solicitudes_auto_notif` o la plantilla para no dejar el texto `Solicitudes Hospitalarias: horas hábiles` cuando no exista valor.

Mi recomendación para responder al funcional:

> Validamos la solicitud `25920128` y corresponde a Diagnóstica. El tiempo diagnóstico sí fue encontrado y el mail lo grabó correctamente con 17 horas hábiles. El vacío reportado no corresponde al valor diagnóstico, sino al bloque de Hospitalarias. La causa es que el SP busca parametrización con `soli_tipo = 'H'`, pero en QA no existen registros activos para `H`. Se debe definir si se parametrizan tiempos hospitalarios o si la plantilla debe omitir esa línea cuando no haya valor.

---

### [25/08/2026 17:50:59] Wilmer Higuera

Listo, ya me quedo claro y lo respondí el Caso 5, ahora por favor en el mismo documento analiza el Caso 6 (ERROR - CASO 6)

---

### [25/08/2026 17:57:46] Agente

Wilmer, analicé el **ERROR - CASO 6** del PDF. No modifiqué archivos.

**Conclusión**
El Caso 6, como error actual, **no reproduce en la regresión del 24/08**.

El caso original del documento usa la solicitud `25920064`, pero esa solicitud tiene:

```text
soli_auto = 25920064
tipo_usuario = E
origen = P
mail_id_prestad_mail = NULL
mail_id_afi_mail = NULL
```

Para `tipo_usuario = E`, en registro de solicitud **no corresponde notificación al prestador**. Según el flujo actual, `pru_registra_solicitudes_auto` solo llama:

```sql
pri_solicitudes_auto_notif @prm_id = 7
```

o sea, notificación al afiliado. Por eso `mail_id_prestad_mail = NULL` en `25920064` no es necesariamente error.

En la regresión del 24/08, la solicitud relevante es `25920129`:

```text
soli_auto = 25920129
tipo_usuario = P
origen = G
mail_id_prestad_mail = 9989103
mail_id_afi_mail = 9989105
```

Y se generaron estos mails:

```text
9989103 -> Prestador principal: EMAIL_PRESTAD_GIS_20260824@infomedical.com.ar
9989104 -> Otro prestador: PRUEBASGISPRESTADOR@GMAIL.COM
9989105 -> Afiliado: pruebasgisprestador@gmail.com
```

La auditoría también está correcta:

```text
proceso_id 1  -> Prestador, mail_id 9989103
proceso_id 1  -> Prestador, mail_id 9989104
proceso_id 22 -> Afiliado, mail_id 9989105
```

**Punto técnico importante**
`SOLICITUDES_AUTO.mail_id_prestad_mail` solo puede guardar **un mail_id**. Cuando hay varios prestadores, el SP guarda el primer mail válido en ese campo y deja el historial completo en `SOLICITUDES_AUTO_AUDITORIA`.

Eso coincide con el código actual de `pri_solicitudes_auto_notif`: solo asigna `@var_mail_id_prestad` si está `NULL`, y luego actualiza `SOLICITUDES_AUTO.mail_id_prestad_mail`.

**Respuesta sugerida al funcional**
> Se valida el Caso 6. Para la solicitud original `25920064`, el dato `mail_id_prestad_mail` queda vacío porque la solicitud fue generada como usuario ejecutivo/Presmed (`tipo_usuario = E`, `origen = P`), por lo tanto no corresponde notificación al prestador en registro. En la regresión `25920129`, generada por GIS/prestador (`tipo_usuario = P`, `origen = G`), el sistema sí guarda `mail_id_prestad_mail = 9989103`, envía notificaciones a los prestadores y al afiliado, y deja el detalle completo en auditoría. No se identifica error vigente de guardado para este caso.

Además, en el documento hay una consulta con posible typo: para revisar mails de `25920129`, una línea filtra `subject like '%25920128%'`. Eso puede confundir la validación.

---

### [25/08/2026 18:01:43] Wilmer Higuera

Pre-compaction memory flush. Store durable memories only in memory/2026-08-25.md (create memory/ if needed). Treat workspace bootstrap/reference files such as MEMORY.md, DREAMS.md, SOUL.md, TOOLS.md, and AGENTS.md as read-only during this flush; never overwrite, replace, or edit them. If memory/2026-08-25.md already exists, APPEND new content only and do not overwrite existing entries. Do NOT create timestamped variant files (e.g., 2026-08-25-HHMM.md); always use the canonical 2026-08-25.md filename. If nothing to store, reply with NO_REPLY.
Current time: Tuesday, August 25th, 2026 - 6:01 PM (America/Buenos_Aires)
Reference UTC: 2026-08-25 21:01 UTC

---

### [25/08/2026 18:02:48] Agente

NO_REPLY

---

### [25/08/2026 18:02:49] Wilmer Higuera

Espera, mira esta parte, esta es la que están reclamado:
reviso detalle de email enviado al prestador
Imagen 1
REVISAR

---

### [25/08/2026 18:07:12] Agente

Wilmer, sí: esta captura reclama otra cosa distinta al Caso 5.

Validé en `InstallQA` la solicitud `25920129` y el problema está claro:

```text
soli_auto = 25920129
soli_tipo = D
origen = G
tipo_usuario = P
afi_tipo_calculado = P
prestad_vip = N
mail_id_prestad_mail = 9989103
```

El mail del prestador `9989103` quedó grabado con los tres valores vacíos:

```text
Solicitudes Quirúrgicas:  horas hábiles
Solicitudes Procedimientos Diagnósticos y/o Tratamientos Ambulatorios:  horas hábiles
Solicitudes Hospitalarias:  horas hábiles
```

**Dónde falló**

No falló el `REPLACE`. Falló la búsqueda de parametrización, porque `pri_solicitudes_auto_notif` busca tiempos con coincidencia exacta usando:

```sql
SATGE.origen = S.origen
```

Para esta solicitud `S.origen = 'G'`, pero la parametrización activa encontrada está cargada para:

```text
origen = P
```

Ejemplo existente:

```text
D / P / P / N / R -> 17
Q / P / P / N / R -> 25
T / P / P / N / R -> 24
```

Para `origen = G` no encontró nada:

```text
Q / G / P / N -> 0 registros
D / G / P / N -> 0 registros
H / G / P / N -> 0 registros
```

**Respuesta para el funcional**

> El reclamo del Caso 6 es válido para el mail enviado al prestador. La solicitud `25920129` es diagnóstica, pero fue registrada con `origen = G`. El SP busca los tiempos de gestión por coincidencia exacta contra `SOLICITUDES_AUTO_TIEMPO_GESTION_ESP`, incluyendo el origen. En QA existen tiempos parametrizados para `origen = P`, pero no para `origen = G`; por eso el mail al prestador salió con las horas vacías. Se debe definir si corresponde cargar la parametrización para origen `G`, o si para este flujo GIS/prestador el SP debe tomar los tiempos equivalentes de `origen = P`.

Mi recomendación técnica: si funcionalmente `G` representa el registro desde GIS/prestador y debe usar los mismos tiempos que `P`, conviene ajustar el SP para normalizar ese origen en la consulta de tiempos, en lugar de duplicar parametrización.

---

### [25/08/2026 18:25:11] Wilmer Higuera

Listo, bien, ahora pasemos al caso 10 (Error - CASO 10), analiza principalmente la parte final contra la base de datos.
ESPERADO. que PRESMED recupere la última dirección de email del afiliado.
REVISAR REGRESION REGRESION 24/08/2024
REVISAR

---

### [25/08/2026 18:34:22] Agente

Wilmer, validé el **Caso 10** contra `InstallQA`. No modifiqué nada.

**Conclusión:** el reclamo es válido para PRESMED.

En la regresión final aparecen estas solicitudes PRESMED para el afiliado `prepaga=1 / contra=100199 / inte=01`:

```text
25920132 | E | P | email_afi_1 NULL | email_afi_2 NULL
25920133 | E | P | email_afi_1 NULL | email_afi_2 NULL
```

Pero ese afiliado sí tenía email disponible:

```text
REGISTRO_UNICO_EMAILS: pruebasgisprestador@gmail.com
Último mail enviado afiliado: 25920114 -> pruebasgisprestador@gmail.com
```

**Dónde falla**

El problema está en el flujo PRESMED de registro:

- `wp_soli_reg.srw` carga el afiliado.
- Luego `wf_afi_datos_extras()` llama a `prs_afiliado_extras`.
- `prs_afiliado_extras` devuelve el **segundo email histórico**, no el último/principal.
- Para el afiliado `100199/01`, como solo existe un email vigente, devuelve `NULL`.
- Después `wf_registra_solicitudes_auto()` solo envía:

```powerscript
@prm_email_afi_2 = :ls_email_afi_2
```

No está enviando `@prm_email_afi_1`.

Entonces PRESMED termina grabando:

```text
SOLICITUDES_AUTO.email_afi_1 = NULL
SOLICITUDES_AUTO.email_afi_2 = NULL
```

**Corrección necesaria**

Debemos ajustar PRESMED para que al registrar la solicitud recupere y guarde la última dirección válida del afiliado como `email_afi_1`.

La fuente recomendada debe seguir la misma prioridad que ya se ve en `ars_afiliado`:

```text
1. Último MAIL_TO asociado al último SOLICITUDES_AUTO.mail_id_afi_mail del afiliado.
2. Si no existe, REGISTRO_UNICO_EMAILS vigente.
```

Respuesta para funcional:

> Se valida el Caso 10. PRESMED no está recuperando correctamente la última dirección de email del afiliado. En la regresión, las solicitudes `25920132` y `25920133` quedaron con `email_afi_1` y `email_afi_2` vacíos, aunque el afiliado tenía email vigente y mails previos enviados. La causa es que el registro PRESMED solo guarda `email_afi_2` desde `email_alter`, y el helper actual no devuelve el último email principal. Corresponde ajustar el recupero/guardado para cargar la última dirección válida en `email_afi_1`.

---

### [25/08/2026 18:43:08] Wilmer Higuera

Para realizar esta corrección, es muy grande el cambio, dame el paso a paso de donde y como debemos hacer el cambio

---

### [25/08/2026 18:44:00] Agente

No es un cambio grande si lo acotamos bien. Yo lo haría en **dos puntos controlados**: recuperar correctamente el email principal del afiliado y guardarlo al registrar la solicitud desde PRESMED.

**Paso A Paso**

1. **SP helper de recupero**
   
   Revisar/modificar `prs_afiliado_extras.sql`.

   Hoy devuelve solo:

   ```sql
   @prm_email2
   @prm_sms2
   ```

   y busca el email anterior, no el último/principal. Por eso para el afiliado del Caso 10 devuelve `NULL`.

   Propuesta: hacer que también devuelva el último email principal:

   ```sql
   @prm_email1 VARCHAR(100) = NULL OUTPUT,
   @prm_email2 VARCHAR(100) = NULL OUTPUT
   ```

   Prioridad para `email1`:

   ```text
   1. Último MAIL_TO.email_to del último SOLICITUDES_AUTO.mail_id_afi_mail del afiliado.
   2. Si no existe, último REGISTRO_UNICO_EMAILS vigente.
   ```

2. **PowerBuilder: `wp_soli_reg.srw`**

   Función actual:

   ```powerscript
   wf_afi_datos_extras()
   ```

   Hoy declara:

   ```powerscript
   STRING ls_contra, ls_inte, ls_email, ls_tel, ls_null
   ```

   Cambiar conceptualmente a:

   ```powerscript
   STRING ls_contra, ls_inte, ls_email_1, ls_email_2, ls_tel, ls_null
   ```

   Y la llamada al SP debe traer ambos:

   ```powerscript
   DECLARE proc PROCEDURE FOR dbo.prs_afiliado_extras
       @prm_prepaga = :li_prepaga,
       @prm_contra  = :ls_contra,
       @prm_inte    = :ls_inte,
       @prm_email1  = :ls_email_1,
       @prm_email2  = :ls_email_2,
       @prm_sms2    = :ls_tel
   USING sqlca;
   ```

   Luego guardar `ls_email_1` en una variable de instancia o campo disponible para usar al registrar. `ls_email_2` seguiría alimentando el campo visible actual:

   ```powerscript
   idw_afi.SetItem(1,'email_alter',ls_email_2)
   ```

3. **PowerBuilder: registro de solicitud**

   En `wf_registra_solicitudes_auto()` hoy se envía solo:

   ```powerscript
   @prm_email_afi_2 = :ls_email_afi_2
   ```

   Falta enviar:

   ```powerscript
   @prm_email_afi_1 = :ls_email_afi_1
   ```

   Entonces debe quedar:

   ```powerscript
   @prm_email_afi_1 = :ls_email_afi_1,
   @prm_email_afi_2 = :ls_email_afi_2
   ```

4. **SP de registro**

   `pru_registra_solicitudes_auto.sql` ya tiene el parámetro:

   ```sql
   @prm_email_afi_1 VARCHAR(100) = NULL
   ```

   y ya guarda:

   ```sql
   email_afi_1 = @prm_email_afi_1,
   email_afi_2 = @prm_email_afi_2
   ```

   Por eso acá probablemente no hay que hacer cambio grande, solo validar que al editar no se pierda `email_afi_1`.

5. **SP de notificación**

   `pri_solicitudes_auto_notif.sql` ya usa la prioridad correcta:

   ```sql
   ISNULL(NULLIF(@var_email_afi_1, ''),
          NULLIF(@var_email_afi_2, ''))
   ```

   Si logramos que PRESMED grabe `email_afi_1`, el envío queda corregido sin tocar el InfoMailer ni la lógica general de notificaciones.

**Validación**

Probar con una solicitud nueva PRESMED del afiliado del Caso 10. Esperado:

```text
SOLICITUDES_AUTO.email_afi_1 = último email recuperado
SOLICITUDES_AUTO.email_afi_2 = email alternativo, si existe
MAIL_TO.email_to = email_afi_1
```

Cambio acotado: `prs_afiliado_extras.sql` + `wp_soli_reg.srw`. `pru_registra_solicitudes_auto` y `pri_solicitudes_auto_notif` se revisan, pero en principio no requieren rediseño.

---

### [25/08/2026 18:54:05] Wilmer Higuera

Toma el apunte de este caso como pendiente por favor, para resolverlo mas adelante

---

### [25/08/2026 18:54:53] Agente

Listo, Wilmer. Dejé el **Caso 10** asentado como pendiente en `memory/2026-08-25.md`.

Quedó registrado el reclamo, la validación en `InstallQA`, la causa técnica probable y los objetos candidatos para resolverlo después: `prs_afiliado_extras.sql`, `wp_soli_reg.srw`, con revisión de `pru_registra_solicitudes_auto.sql` y `pri_solicitudes_auto_notif.sql`.

---

### [25/08/2026 18:56:56] Wilmer Higuera

Pasemos al caso 11 (ERROR - CASO 11), valida en el mismo documento y contra la base de datos, pero con énfasis en la parte final que dice:
ESPERADO: Que para una solicitud SIN TIPIFICAR, cuando desde PRESMED se seleccione un TIPO, el email se envíe al AFILIADO no al PRESTADOR, ya que la solicitud fue generada por el afiliado y no por el Prestador.
REVISAR REGRESION REGRESION 24/08/2024


REVISAR

---

### [25/08/2026 19:01:01] Agente

Wilmer, validé el **ERROR - CASO 11** contra `InstallQA`. No modifiqué nada.

**Conclusión:** en la regresión del 24/08 el caso **sale correcto**.

Solicitud validada:

```text
soli_auto = 25920131
soli_tipo = D
soli_esta = R
tipo_usuario = A
origen = A
mail_id_prestad_mail = NULL
mail_id_afi_mail = 9989107
```

Auditoría:

```text
Notificación solicitud registrada.
mail_id: 9989107
email: pruebasgisprestador@gmail.com
notifica: Afiliado
proceso_id: 7
```

Mail enviado:

```text
mail_id = 9989107
subject = Registro Solicitud No.25920131
destinatario = pruebasgisprestador@gmail.com
```

**Dónde se valida en código**

En `pru_registra_solicitudes_auto.sql`, cuando una solicitud estaba en `T` y al editar queda en `D/H/Q`, entra al bloque de notificación registrada. Si el flujo viene desde PRESMED, `@prm_origen = 'P'`, entonces:

```sql
@var_tipo_usuario = 'E'
```

y por esta condición:

```sql
IF @var_tipo_usuario IN ('A', 'E')
BEGIN
    EXEC dbo.pri_solicitudes_auto_notif
        @prm_id = 7,
        @prm_soli_auto = @prm_soli_auto
END
```

se envía al **Afiliado**, no al prestador.

Respuesta para funcional:

> Se revisa el Caso 11 en regresión 24/08. Para la solicitud `25920131`, originalmente generada por afiliado y luego tipificada desde PRESMED, el sistema generó únicamente notificación al afiliado con proceso `7`. No se generó mail al prestador (`mail_id_prestad_mail = NULL`). Por lo tanto, el comportamiento esperado se cumple en la regresión.

---

### [25/08/2026 19:07:39] Wilmer Higuera

Puedes mostrarme el dato de la REGRESION 24/08/2024 en la base de datos, donde se envió bien el correo al afiliado para tenerlo como evidencia, por favor

---

### [25/08/2026 19:08:07] Agente

Wilmer, evidencia de `InstallQA` para la regresión 24/08:

```text
Solicitud: 25920131
Tipo solicitud: D
Estado: R
tipo_usuario: A
origen: A
mail_id_prestad_mail: NULL
mail_id_afi_mail: 9989107
```

Mail enviado:

```text
mail_id: 9989107
subject: Registro Solicitud No.25920131
fecha: 24/08/2026 20:59
destinatario: pruebasgisprestador@gmail.com
```

Auditoría:

```text
Notificación solicitud registrada.
mail_id: 9989107
email: pruebasgisprestador@gmail.com
notifica: Afiliado
proceso_id: 7
usuario: sa
tipo_usuari: A
```

Conclusión para evidencia: **no se generó mail al prestador** (`mail_id_prestad_mail = NULL`) y sí se generó correctamente al **afiliado** por proceso `7`.

---

### [25/08/2026 19:34:02] Wilmer Higuera

Listo Lisa, analiza por favor el caso 37 en el mismo documento, lo validas contra la base de datos

---

### [25/08/2026 19:36:17] Agente

Wilmer, intenté ubicar el **Caso 37** en memoria, tmp y en el PDF local, pero en esta sesión **no tengo disponible el documento `R29489-TEST_20260727.pdf`** ni quedó texto extraído del Caso 37.

Sí validé contra `InstallQA` las solicitudes recientes de regresión desde `25920120` hasta `25920133`, pero con esos datos solos no puedo identificar cuál corresponde al Caso 37 ni cuál es el reclamo funcional específico.

Últimas candidatas visibles en base:

```text
25920133 | D | C | E | P | sin mails
25920132 | H | C | E | P | sin mails
25920131 | D | R | A | A | mail afiliado 9989107
25920130 | D | R | E | P | mail afiliado 9989106
25920129 | D | R | P | G | mail prestador 9989103 / afiliado 9989105
25920128 | D | R | E | P | mail afiliado 9989102
```

Para analizarlo bien necesito que me pegues el bloque del **Caso 37** del documento o una captura donde se vea:

```text
Descripción
Evidencia
Solicitud usada
Resultado esperado
Resultado obtenido
```

Con eso lo cruzo contra `InstallQA` y te devuelvo la conclusión técnica con evidencia, como hicimos con los casos 5, 6, 10 y 11.

---

### [25/08/2026 19:42:33] Wilmer Higuera

Guarda por favor lo trabajado el día de hoy con lujo de detalle, de forma que mañana puedas recuperar la información y podamos continuar con el caso 37, queda pendiente que nos aprueben la propuesta para que realicemos los cambios para la generación de pdf con carta de negación y autorizaciones en estado autorizado, me indicas como te escribo mañana para que recuperes la información y podamos continuar con el trabajo, hasta mañana, muchas gracias por tu colaboración

---

### [25/08/2026 19:43:56] Agente

Listo, Wilmer. Guardé el cierre completo de hoy en `memory/2026-08-25.md`.

Mañana escribime así:

```text
Lisa, retomemos la memoria del 2026-08-25 de la Tarea 29489. Recupera el cierre operativo de hoy: propuesta pendiente para PDFs de finalizada procesos 8, 9 y 13, Caso 10 pendiente de recupero de email afiliado en PRESMED, y continuemos con el Caso 37 del documento R29489-TEST_20260727.pdf. Te comparto la captura/texto del Caso 37 para validarlo contra InstallQA.
```

Quedó asentado que el Caso 37 necesita el bloque/captura del documento para poder identificar la solicitud y validar correctamente contra base.

Hasta mañana, Wilmer. Gracias a vos.

---

## Origen

Memoria generada automáticamente a partir del historial local de sesiones de OpenClaw.
