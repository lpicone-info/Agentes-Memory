# Memoria durable - 2026-07-23

## CSIAFAS / TRUNK / INFOMAILER
- Usuario consulto usando repositorios registrados CSIAFAS y repositorio TRUNK para ubicar INFOMAILER.
- Se analizo flujo de Infomailer relacionado con envio de solicitudes/autorizaciones por correo a prestadores.
- Hallazgo: configuracion tecnica de envio reside en `MAIL_CONFIG` con `server`, `port`, `TLS`, `user`, `password` y `requiere_autenticacion`.
- Hallazgo: los envios se registran en `MAIL_ENVIO`, `MAIL_TO` y `MAIL_AUTORIZACIONES`; el estado `NOE` queda preparado para que el mailer lo procese.
- Respuesta dada sobre cambio de buzon/direccion: validar si cambia cuenta SMTP real o solo remitente visible; si cambia cuenta, actualizar usuario/password y validar servidor, puerto, TLS, autenticacion, relay, SPF/DKIM/DMARC, prueba controlada, monitoreo de estados `ENV`, `ENE`, `ENP`, `NOE` y respaldo para rollback. No deberia requerir cambio de codigo si el nuevo buzon mantiene el esquema SMTP soportado.
- Consulta posterior: condiciones para mostrar mensaje "No se pudo enviar el Mail".
- Hallazgo: el mensaje visible aparece cuando `nvo_infomailer.of_afi_mail_envio_simple(...)` devuelve `-1`; la pantalla muestra `as_mensaje`, donde puede venir "No se pudo enviar el Mail".
- Evidencia citada: `wr_auto_impresion.srw` lineas 346 y 360 llama a `of_afi_mail_envio_simple`; si `li_ret_mail = -1`, ejecuta `MBox(THIS, as_mensaje, 2, 1)` y retorna `FALSE`.
- Evidencia adicional: `wp_extracto_prestad.srw` linea 587 usa mismo patron con rollback y `MBox(PARENT, as_mensaje)`; `wr_expe_concep_glosa.srw` linea 197 usa mismo patron.
- Condiciones probables para `-1`: falla de configuracion SMTP, credenciales invalidas, servidor/puerto inaccesible, incompatibilidad TLS/autenticacion, destinatario invalido o rechazado, error al generar/adjuntar archivo, o error interno al insertar/configurar envio.
- Dato operativo: en flujo exitoso muchas veces no confirma envio inmediato; muestra "Correo configurado para envio" porque deja el registro preparado para procesamiento posterior.
