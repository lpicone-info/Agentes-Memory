
## 2026-08-19 20:00 ART - Investigación AUTORIZACIONES / AUTO_AUDITORIA

- Usuario: Constantino. Conversación directa por Google Chat.
- Contexto de trabajo indicado por el usuario: TRUNK `csiafas-Coomeva-trunk-backend`, base `PruebasCoreINFOMEDICAL`, funcionalidad `AUTORIZACIONES`.
- Investigación: `AUTO_AUDITORIA.ACTIVI = 'Reversión estado autorización'`.
  - Origen técnico: procedimiento `dbo.pru_auto_revertir_estado`.
  - Evidencia en repo: `svn/csiafas-Coomeva-trunk-backend/procedures/pru_auto_revertir_estado.sql`, llamada a `pri_auto_auditoria` con `@activi = 'Reversión estado autorización'` aprox. línea 87.
  - Disparador funcional PowerBuilder: `w_clase_auto`, evento `ue_carta_negacion`, vía `wf_revertir_estado`, que ejecuta `dbo.pru_auto_revertir_estado`; archivo `svn/csiafas-Coomeva-trunk-exe_presmed-Source20Infomedical/w_clase_auto.srw` aprox. línea 6926.
  - Caso funcional: autorización negada en supervisión (`esta = 'N'`, `is_supervision = 'S'`), carta de negación no continuada/finalizada, revierte estado si nivel 2.
  - `pru_auto_revertir_estado` no asigna `AUTORIZACIONES.baja_fecha`; actualiza `esta`, `auto_esta_moti`, limpia `usuario_nego`, `usuario_superviso`, `super_fecha`, `cumplimiento`. No actualiza `recha`.

- Investigación: `AUTO_AUDITORIA.ACTIVI = 'Retiro de Autorización'`.
  - Orígenes técnicos encontrados en `PruebasCoreINFOMEDICAL` / TRUNK:
    1. `dbo.prd_autorizaciones`: baja individual; inserta auditoría `Retiro de Autorización`; archivo `procedures/prd_autorizaciones.sql` aprox. línea 133. Asigna `baja_fecha` y `auto_esta_moti = @prm_moti`.
    2. `dbo.pru_anulacion_masiva`: anulación masiva; archivo `procedures/pru_anulacion_masiva.sql` aprox. línea 139. Asigna `baja_fecha` y `auto_esta_moti = @prm_motivo`.
    3. `dbo.pri_parse_archivo_auto_anul`: importación/proceso por archivo; archivo `procedures/pri_parse_archivo_auto_anul.sql` aprox. línea 572. Asigna `baja_fecha` y `auto_esta_moti = T.anu_moti`.
  - Llamadores PB relevantes:
    - `wp_anula_solicitudes.srw`, evento `ue_anular`, llama `prd_autorizaciones` aprox. línea 113.
    - `uo_nv_valida_auto.sru`, método `of_auto_baja`, llama `prd_autorizaciones` aprox. línea 2300.
    - `wp_auto_importac.srw`, baja de autorización, llama `prd_autorizaciones` aprox. línea 695.
    - `wp_anu_masiva_auto.srw`, evento `ue_anular`, llama `pru_anulacion_masiva` aprox. línea 206.
    - `d_importar_auto_anul.srd` referencia el proceso de importación/anulación por archivo.
  - Los tres procesos asignan `baja_fecha`. Como motivo técnico usan `auto_esta_moti`; no asignan `AUTORIZACIONES.recha`. En anulación masiva/importación además limpian `obser = NULL`. No hay triggers sobre `AUTORIZACIONES` que completen motivo por detrás.

- Investigación: origen de `AUTORIZACIONES.auto_esta_moti IS NULL`.
  - Origen principal: alta/modificación estándar por `dbo.pru_autorizaciones`.
  - `@prm_auto_esta_moti` tiene default `NULL` aprox. línea 104 de `procedures/pru_autorizaciones.sql`.
  - `pru_autorizaciones` graba directo `auto_esta_moti` en `UPDATE` y `INSERT` aprox. líneas 226 y 508.
  - Flujo PB estándar `uo_nv_valida_auto` llama `pru_autorizaciones`. Una llamada antigua no envía `@prm_auto_esta_moti` y aplica default `NULL` aprox. línea 904. Otra lo envía desde `auto_esta_mo`, que puede viajar nulo si el DataWindow no trae valor aprox. líneas 1799 y 1638.
  - En `PruebasCoreINFOMEDICAL` se observó volumen alto: 22.198.403 registros con `auto_esta_moti IS NULL`, mayormente autorizaciones vigentes `esta = 'A'` y orígenes `C`, `G`, `I`, `P`; comportamiento histórico/normal para autorizaciones sin motivo de estado general.

- Investigación: origen de `AUTORIZACIONES.baja_fecha IS NOT NULL` con `auto_esta_moti IS NULL`.
  - En `PruebasCoreINFOMEDICAL` se detectaron 69 autorizaciones con esa condición.
  - Distribución mencionada al usuario: 66 históricas de 2019, mayormente `origen = 'C'`, usuario `IVR`, con auditoría solo de `Activación de Autorización`; no apareció auditoría de baja para esas fechas. Evidencia apunta a actualización directa o proceso sin auditoría en TRUNK.
  - 3 recientes tenían auditoría `Retiro de Autorización` seguida por `Reversión estado autorización`.
  - Patrón reciente: `prd_autorizaciones` asigna `baja_fecha` y motivo; luego `pru_auto_revertir_estado` puede dejar `auto_esta_moti = NULL` al tomar el valor anterior desde `AUTO_REMOTAS` y regrabarlo en `AUTORIZACIONES`, sin tocar `baja_fecha`.
  - Otro procedimiento capaz de generar la condición si el motivo ya era nulo: `pru_serv_auto_previa_prestac`, que asigna `baja_fecha` y `esta = 'U'` pero no actualiza `auto_esta_moti`; archivo `procedures/pru_serv_auto_previa_prestac.sql` aprox. línea 86.
