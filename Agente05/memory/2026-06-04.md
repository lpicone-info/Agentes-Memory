
## 2026-06-04 19:02 UTC - PRESMED Reembolsos investigation

User asked to use only these repositories for the PRESMED/Reembolsos investigation:
- `csiafas-Coomeva-trunk-exe_presmed-Source20Infomedical`
- `csiafas-Coomeva-trunk-backend`

Located the PRESMED supervision flow:
- `m_main.srm` opens `wp_rei_principal` in supervision mode with `lstr_rei_principal.ss_modo="S"` and `opensheetwithparm(wp_rei_principal, ...)`.
- Window: `wp_rei_principal.srw`, technical title `Reembolsos`.
- Auditoria tab: `tab_1.pag_4`, text `Auditoría`.

Button `Vuelve a Pendiente` in `wp_rei_principal.srw`:
- On click it gets selected row from `idw_supervision`, clears `usuario`, sets selected item `rein_esta="A"`, sets header `idw_ord_cabecera.Object.rein_esta[1]="D"`, then calls `wf_supervision()`.
- It does not call a stored procedure directly on click.
- Persistence occurs later in `ue_grabar()` via `DECLARE ord_item PROCEDURE FOR pru_rei_ord_item` / `EXECUTE ord_item`.
- Backend SP: `dbo.pru_rei_ord_item` (`procedures/pru_rei_ord_item.sql`), updating `rei_ord_item` fields including `rein_esta` and `usuario`; on state change it calls `dbo.pri_rei_audita`.
- Auditoria grid retrieval uses `dbo.prs_rei_ord_item` from `dw_rei_supervision.srd`.

Button `Volver a Edición` in same window:
- `wp_rei_principal.srw`, control `cb_a_edicion`, text `Volver a Edición`.
- On click it only proceeds if `idw_ord_cabecera.object.rein_esta[1] = "D"`; otherwise disables itself and returns.
- It sets `idw_ord_cabecera.object.rein_esta[1] = 'H'`, clears `super_fecha`, sets `ib_nopregunte = TRUE` and `ib_grabar = TRUE`, then calls `wf_graba_vuelta_edicion()`.
- `wf_graba_vuelta_edicion()` triggers `ue_pregrabar`, calls `ue_grabar()`, restores, selects tab 1, and shows message `El Reembolso ha vuelto a estado de Edición.`
- Persistence SP: `dbo.pru_rei_ord_cabecera`, declared/executed in `ue_grabar()` as `DECLARE ord_cabecera PROCEDURE FOR pru_rei_ord_cabecera` / `EXECUTE ord_cabecera`.
- Backend SP file: `csiafas-Coomeva-trunk-backend/procedures/pru_rei_ord_cabecera.sql`; updates `rei_ord_cabecera.rein_esta` and `super_fecha`; registers state-change audit through `dbo.pri_rei_audita`.
