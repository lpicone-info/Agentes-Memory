# Memoria durable - 2026-07-30

## Conversación Google Chat con Bárbara

- Identidad operativa: agente Lisa / Agente05, arquitecta de sistemas, foco PowerBuilder 2022 y MS SQL. Responder en español técnico, directo y auditable.
- Repositorio trabajado: `/home/desarrollo-ia-05/.openclaw/workspace/svn/bgonzalez-Version84-Modelo2Gis`, módulo `exe_presmed`, carpeta `Source Infomedical`.
- Caso `wp_soli_reg`: se analizó sin modificar. Pedido: cuando una solicitud se consulta en estado terminal Finalizada/Anulada no permitir modificar ni eliminar datos de la solapa `Servicios a solicitar`. Hallazgo: estados `F = Finalizada`, `U = Anulada`; `wf_retrieve_total` ya deja `idw_prestac` en `ReadOnly='yes'` y deshabilita `pb_ins_prestac`, pero los botones internos del DW `d_soli_reg_serv` quedan activos. Recomendación dada: en el bloque de solo lectura deshabilitar `idw_prestac.object.b_borrar.enabled = FALSE` y `idw_prestac.object.pb_deta_pres.enabled = FALSE`; en `ue_restaurar` volverlos a `TRUE`. Motivo: `b_borrar` puede eliminar filas y `pb_deta_pres` abre detalle con ABM de conceptos.
- Caso `wp_bandeja_solicitudes` / solapa `Autorizaciones`: se analizó sin modificar. Pedido: refrescar datos luego de abrir autorización como auditor y cambiar estado/motivo. Primera recomendación fue `idw_auto.Retrieve(idc_soli_auto)` luego de `iuo_auto.of_consultar_auto_editable(...)` en `buttonclicked` de `dw_auto`. Luego Bárbara indicó que no retornaba desde la ventana. Diagnóstico actualizado: ventanas de autorización heredan de `w_clase_auto` y no son `response!`, por lo que `OpenWithParm` no es punto confiable de retorno. Recomendación actual: usar bandera en `wp_bandeja_solicitudes` (`ib_refrescar_auto_pendiente`, `idc_soli_auto_refrescar`) antes de abrir autorización editable y refrescar en `activate` con `idw_auto.Retrieve(idc_soli_auto)` y opcionalmente `idw_auto_auditoria.Retrieve(idc_soli_auto)`, reiniciando timer. Bárbara respondió: `Ok, bien. Eso ya está.`
- Caso `wp_bandeja_solicitudes` + backend `InstallQA` / Bitácora Auditoría: se analizó sin modificar. La pestaña `Bitácora Solicitudes` usa `d_bandeja_soli_auditoria.srd`, SP `dbo.prs_solicitud_auditoria`, tabla `SOLICITUDES_AUTO_AUDITORIA`; insert estándar `dbo.pri_solicitudes_auto_auditoria` con columnas `soli_auto`, `fecha_hora`, `activi`, `usuario`, `tipo_usuari`.
- Bitácora devolución: en `wp_bandeja_solicitudes`, acción `ue_devolver` llama `pru_solicitudes_auto_devolucion`; backend ya inserta actividad `Devolución solicitud`. Recomendación: revisar solo si debe enriquecer texto con motivo/observación o estado anterior/nuevo.
- Bitácora cambio de estado: cobertura parcial existente. SPs con registro ya detectados: `pru_solicitudes_auto_anula_bandeja` (`Anulación Presmed Bandeja`), `pru_solicitudes_auto_resuelta_pertinencia` (cambio a `En Gestion`, devolución a gestión y ajuste de tiempo si aplica), `pru_fin_gestion_solicitudes_auto` (`Fin de gestión explícita`), `pru_rev_soportes_solicitudes_auto` (`Derivación a auditoría de soportes`). Punto principal a revisar/modificar: `pru_asignacion_soli_auto`, usado por `pru_solicitudes_auto_resuelta_auditoria` para cambios como `V -> G` o `V -> F`; hoy registra solo `Asignacion de Solicitud`. Recomendación dada: capturar estado anterior y registrar actividad tipo `Cambio de estado de solicitud: <estado_anterior> -> <estado_nuevo>` y/o `Asignación responsable gestión: <usuario>`.
- Bitácora ajuste de tiempo: hay ajuste por motivo de autorización en `AUTO_ESTADOS_MOTIVOS.aju_tiempo_gestion`, aplicado/consultado por `pru_solicitudes_auto_resuelta_pertinencia`, que ya registra `Ajuste de tiempo de gestion por motivo de autorizacion: <motivo> - <horas> horas` cuando `aju_tiempo_gestion > 0`. Configuración general de tiempos (`wp_solicitudes_auto_tiempo_gestion`, `wp_solicitudes_auto_tiempo_gestion_esp`, `wp_auto_estados_motivos`; SPs `pru_solicitudes_auto_tiempo_gestion`, `pru_solicitudes_auto_tiempo_gestion_esp`, `pru_auto_estados_motivos`) no opera sobre solicitud específica, por lo que no conviene registrarla en `SOLICITUDES_AUTO_AUDITORIA`; si se pide auditoría de parametrización, debe ser bitácora administrativa separada.
- Recomendación PB adicional para Bitácora: luego de acciones en `wp_bandeja_solicitudes`, refrescar `idw_soli_auditoria.Retrieve(idc_soli_auto)` si la solapa ya está abierta; puntos candidatos: después de `ue_devolver`, después de `ue_anular`, y en el `activate` agregado para retorno de autorizaciones/auditoría.
