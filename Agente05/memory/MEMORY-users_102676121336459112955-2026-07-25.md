# Memoria curada diaria

- Fecha: 2026-07-25
- Usuario: Constantino Siafas
- id_googlechat: users/102676121336459112955
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/hpdxvyaaaae

## Resumen de la actividad

### Tema 1: Analisis de Registro de solicitud PRESMED y servicios a solicitar

- Solicitud o contexto: Constantino pidio analizar la rama SVN `Version84-Modelo2Gis`, usando la base `INSTALLQA`, para ubicar en PRESMED el menu `Solicitudes/Gestion de solicitudes/Registro de solicitud`, la solapa `Servicios a solicitar` y la ventana `Asignacion de conceptos a CUPS`. Necesitaba saber como se llena la lista de seleccion de nomencladores, los controles y validaciones de `nomenclador`, `cups` y `cantidad`, y los controles de `Prestador`, `Lugar` y `concepto` en la ventana asociada.
- Respuesta o acciones del agente: Se validaron las reglas operativas, `DB_PROFILE.md`, `REPOSITORIOS.md`, la working copy SVN `csiafas-Version84-Modelo2Gis` y el proyecto PRESMED bajo `exe_presmed/Source Infomedical`. Se buscaron referencias con `rg`, se ubicaron objetos PowerBuilder/DataWindow y se revisaron archivos como `m_main.srm`, `wp_soli_reg.srw`, `w_soli_conceptos.srw`, `d_soli_reg_serv.srd` y DataWindows relacionados.
- Resultado: Se informo que el menu abre `wp_soli_reg`; la solapa `Servicios a solicitar` usa la grilla `idw_prestac`; la lista de nomencladores depende de un DataWindowChild/selector asociado y de reglas de nivel de nomenclador/prestacion; y la ventana `Asignacion de conceptos a CUPS` usa controles y validaciones sobre prestador, lugar y concepto, incluyendo la forma en que se recupera o filtra el concepto. No se copiaron credenciales de `INSTALLQA`.
- Estado: Resuelto

### Tema 2: Controles por estado para insertar o eliminar servicios y conceptos CUPS

- Solicitud o contexto: Constantino continuo con el analisis y pidio controles y validaciones relacionados con estados de una solicitud que permitan insertar o eliminar registros en la solapa `Servicios a solicitar` y en la ventana `Asignacion de conceptos a CUPS`.
- Respuesta o acciones del agente: Se reviso la logica de `wp_soli_reg.srw` y objetos asociados para identificar habilitacion/edicion de la grilla de prestaciones, boton de insercion, eliminacion y apertura de conceptos. Tambien se reviso la ventana de conceptos para entender condiciones de alta/baja segun estado y contexto de la solicitud.
- Resultado: Se explico que en solicitudes nuevas o restauradas se habilita la edicion de `idw_prestac` y el boton de agregar servicios; se detallaron restricciones cuando la solicitud queda en estados o modos que bloquean edicion; y se separo el comportamiento de la solapa principal del comportamiento de la ventana de conceptos CUPS.
- Estado: Resuelto

### Tema 3: Boton REGISTRAR, SP ejecutados, validaciones e inserciones SQL

- Solicitud o contexto: Constantino pidio, dentro de `Registro de solicitud`, ubicar el boton `REGISTRAR` y detallar que SP se ejecuta, que validaciones corren y que sentencias SQL insertan registros en `SOLICITUDES_AUTO_PRESTAC` y `SOLICITUDES_AUTO_PRESTAC_CONCEP`.
- Respuesta o acciones del agente: Primero se pidio precision porque el mensaje inicial mencionaba solo `boton`. Luego Constantino aclaro `REGISTRAR`. Se reviso `wp_soli_reg.srw`, el evento del boton `cb_alta`, el flujo `ue_aceptar(0,0)`, `ue_validar(0)`, `ue_agregar(0,0)` y `wf_registra_solicitudes_auto()`, junto con las sentencias de insercion y validaciones relacionadas.
- Resultado: Se informo que `REGISTRAR` corresponde a `cb_alta`; que el flujo termina en el registro de la solicitud y ejecucion del SP principal de registro; que para `Registrar`, al operar con `ai_generacion = 0`, no aplican validaciones exclusivas de generacion/autorizacion; y se detallaron validaciones generales como tipo de solicitud obligatorio, bloqueo de tipo `T/Sin Tipificar`, motivo obligatorio si la solicitud es provisional, afiliado/integrante/diagnostico/ejecutor/lugar y existencia de al menos un CUP. La respuesta fue interrumpida por el usuario con `cancelar` y el agente confirmo la cancelacion.
- Estado: En curso

### Tema 4: Campos obligatorios al cambiar tipo de solicitud

- Solicitud o contexto: Constantino retomo el punto anterior y pidio detalle de que campos se vuelven obligatorios al cambiar el tipo de solicitud en la ventana de registro de solicitudes PRESMED.
- Respuesta o acciones del agente: Se reviso `wp_soli_reg.srw`, especialmente el `itemchanged` de `idw_dg` para la columna `soli_tipo`, y los DataWindows `de_auto_inter_prestac.srd` y `de_auto_inter_ciru.srd`. Tambien se verificaron validaciones de `inter_fecha`, `ambu_inter`, `rea_fecha`, `urgen_progra` y campos relacionados.
- Resultado: Se explico que el cambio de `soli_tipo` muestra u oculta la seccion de internacion/cirugia y ajusta logica de provisional. Para tipo `H`, se muestra `de_auto_inter_prestac` y `inter_fecha/Fecha Hospitalizacion` queda obligatoria. Para tipo `Q`, se muestra `de_auto_inter_ciru` y `inter_fecha` es obligatoria solo cuando `ambu_inter = I`; si `ambu_inter = A`, se protege y limpia. Tambien se informo que al cambiar a un tipo distinto de `H` se fuerza `provisional = N` y se limpia `provisional_motivo`, por lo que el motivo provisional solo resulta obligatorio si `provisional = S`.
- Estado: Resuelto
