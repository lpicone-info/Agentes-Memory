
## 2026-07-20 - CSIAFAS / TRUNK / BD consultas funcionales

- Usuario: Constantino. Suele pedir trazabilidad técnica usando repositorios registrados para CSIAFAS, repositorio TRUNK y una base específica.
- Para `InstallQA`, SP `dbo.pru_proceso_asignacion_masiva`:
  - Toma solicitudes para auditoría cuando `solicitudes_auto.soli_esta IN ('V','S')` y `responsable_auditoria IS NULL`.
  - `soli_esta = 'V'` corresponde a `Validación Auditoría` en `SOLICITUDES_AUTO_ESTADOS`.
  - `responsable_auditoria` se completa solo en el flujo auditor, cuando `@var_tipo_rol = 2`.
  - `@var_tipo_rol` inicia en `1` y el `WHILE @var_tipo_rol <= 2` lo lleva a `2` en la segunda iteración. Rol 1 asigna gestión (`soli_esta IN ('C','R')`, `responsable_gestion`); rol 2 asigna auditoría (`soli_esta IN ('V','S')`, `responsable_auditoria`).
  - El `UPDATE` real de auditoría hace `SET responsable_auditoria = SA.usuario_asignado` sobre `SOLICITUDES_AUTO`, condicionado por `S.responsable_auditoria IS NULL` y `SA.usuario_asignado IS NOT NULL`.
  - Ese update se ejecuta cuando `@prm_soli_auto IS NULL`; si se pasa solicitud puntual, devuelve usuario por parámetro output pero no actualiza `responsable_auditoria`.
- Para `InstallQA`, campos de `solicitudes_auto` calculados al alta en `dbo.pru_registra_solicitudes_auto` llamando a `prs_calcula_atributos_afiliado_solicitudes_auto`:
  - `vip`, `coomeva`, `pro_riesgo`, `arquetipo` arrancan en `'N'`.
  - `vip = 'S'` si contrato está en cuenta VIP vigente (`AFI_CLASE` + `CUENTAS_VIP`), o contrato tiene estado VIP en `AFI_ESTADOS_CONTRA`, o integrante tiene estado VIP en `AFI_ESTADOS`.
  - `coomeva = 'S'` si afiliado está en `ASOCIADOS_COOPERATIVA` con estado en `('9','10','11','12','13','14','15')`, vigente/no dado de baja.
  - `pro_riesgo = 'S'` si contrato tiene plan vigente en `AFI_PLANES` cuyo plan en `PLANES` tiene `prog_riesgo_salud = 'S'`.
  - `arquetipo = 'S'` si afiliado tiene perfil vigente en `REGISTRO_UNICO_PERFILAMIENTO` asociado a `PERFILES_TIPO` con `es_arquetipo_mi = 'S'` o `es_marca_valor = 'S'`.
  - En la rama de edición de `pru_registra_solicitudes_auto` no se vio recálculo/actualización de esos cuatro campos.
- Para consultas del afiliado `100149`, se indicó que en `InstallQA` puede aparecer como `contra = '100149'` y también como `codigo_unico_persona = 100149`; por defecto se respondieron consultas usando `contra = '100149'`, porque la lógica usa `prepaga/contra/inte`.
- Para `PruebasCoreInfomedical`, PRESMED, menú/ruta de configuración de solicitudes consultada:
  - No se encontró literal exacto `Inactivación Activación Usuarios` en `ZZ_MENUES`.
  - El ítem funcional equivalente hallado fue `Solicitudes > Configuración de Solicitudes > Reglas para Reactivación y Operación retorno`.
  - Tabla de almacenamiento: `dbo.REGLAS_REACTIVACION_RETORNO`.
  - Evidencia en TRUNK backend: `tables/REGLAS_REACTIVACION_RETORNO.sql`, `procedures/afs_reglas_reactivacion_retorno.sql`, `procedures/afu_reglas_reactivacion_retorno.sql`; `afu_reglas_reactivacion_retorno` hace `UPDATE/INSERT` sobre esa tabla.

## 2026-07-20 - Bárbara / TemaBandeja / bgonzalez-version84-modelo2gis

- Usuario: Bárbara. Referencia funcional persistente: Redmine `29478`.
- Pidió análisis sin modificaciones de código/datos sobre casos `Casos_Bandeja6` y `Casos_Bandeja7` ubicados en `/mnt/Agentes/bgonzalez/TemaBandeja`, manteniendo posición sobre el repo `bgonzalez-version84-modelo2gis`.
- Entregables creados en `/mnt/Agentes/bgonzalez/TemaBandeja`:
  - `Analisis_Casos_Bandeja6.md`
  - `Analisis_Casos_Bandeja7.md`
  - `Analisis_Unificado_Casos_Bandeja6_7.md`
  - `Modificaciones_Requeridas_Casos_Bandeja6_7.md`
- Para `Casos_Bandeja6`, el foco del análisis fue:
  - Parametrización de motivos/horas en `AUTO_ESTADOS_MOTIVOS`.
  - Falta de auditoría en `pru_solicitudes_auto_resuelta_pertinencia.sql`.
  - Falta de disparo de notificaciones cuando el ajuste de tiempo aumenta.
- Para `Casos_Bandeja7`, se validó con base `InstallQA` solo en lectura:
  - Los motivos existen.
  - `Solicitud Ampliacion de Historia Clinica` figura asociada a estado `P` cuando el caso pide `R`.
  - Los motivos revisados tienen `aju_tiempo_gestion = 0`.
  - Inconsistencia relevante: `prs_solicitudes_auto_autorizaciones_bandeja.sql` devuelve motivo como texto, pero `d_bandeja_auto.srd` lo declara como `long`, lo que puede explicar visualizaciones como `0`.
- El análisis unificado consolidó Bandeja6+Bandeja7, validó datos necesarios contra `InstallQA`, y organizó diagnóstico por eje funcional y posibles futuras modificaciones por etapa.
- Datos validados en `InstallQA` para Redmine `29478`:
  - Estados relevantes: `P = Pendiente en Auditoria`, `R = Rechazado`, `D = Auditoria`, `E = Revision por Auditoria`.
  - `auto_esta_motivo = 30`: `FALTA DE SOPORTES EN HISTORIA CLINICA`, estado `R`, `aju_tiempo_gestion = 48`, `falta_soporte = S`, vigente.
  - `auto_esta_motivo = 31`: `SOLICITUD DE AMPLIACION DE HISTORIA CLINICA`, estado `P`, `aju_tiempo_gestion = 0`, vigente. EF/caso pide estado `R` y 120 horas.
  - `auto_esta_motivo = 33`: `SOLICITUD DE SEGUNDO CONCEPTO`, estado `P`, `aju_tiempo_gestion = 0`, vigente. EF/caso pide 360 horas.
  - `Comite Regional de Salud` no existe en `AUTO_ESTADOS_MOTIVOS`; debe crearse para estado `P` con `aju_tiempo_gestion = 0` si se implementan los casos 73/77.
  - Plantillas vigentes relacionadas: ids `18`, `19`, `20`, `21`, `25`, `26`.
- Documento `Modificaciones_Requeridas_Casos_Bandeja6_7.md`:
  - Cubre todos los casos requeridos de Bandeja6 y Bandeja7.
  - Contempla dos escenarios: mantener motivos existentes o crear nuevos.
  - Organiza modificaciones futuras por componente: scripts de datos, persistencia del motivo, visualización en bandeja, cálculo de tiempo, resolución de pertinencia, auditoría y notificaciones.
  - Archivos candidatos principales: `w_clase_auto.srw`, `wp_bandeja_solicitudes.srw`, `d_bandeja_auto.srd`, `d_bandeja_tiempo_oportunidad.srd`, `d_bandeja_soli_auditoria.srd`, `dddw_esta_motivos*.srd`, `AUTO_ESTADOS_MOTIVOS.sql`, `pru_autorizaciones.sql`, `prs_solicitudes_auto_autorizaciones_bandeja.sql`, `prs_solicitudes_auto_tiempo_oportunidad_bandeja.sql`, `pru_solicitudes_auto_resuelta_pertinencia.sql`, `pru_solicitudes_auto_workflow.sql`, `pri_solicitudes_auto_auditoria.sql`, `pri_solicitudes_auto_notif.sql`.
- Restricción mantenida: no se modificaron fuentes del repo ni datos de base. `svn status` mostró solo externos `X`, sin cambios de fuentes.
