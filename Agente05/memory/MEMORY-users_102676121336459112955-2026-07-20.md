# Memoria curada diaria

- Fecha: 2026-07-20
- Usuario: Constantino Siafas
- id_googlechat: users/102676121336459112955
- Modo de actualizacion: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/hpdxvyaaaae

## Resumen de la actividad

### Tema 1: Diagnostico de solicitud no recuperada por prs_solicitudes_auto_bandeja

- Solicitud o contexto: Constantino consulto por que la ejecucion de `dbo.prs_solicitudes_auto_bandeja` sobre `InstallQA` no recuperaba la solicitud `SOLICITUDES_AUTO.soli_auto = 25920016`, aunque esperaba verla en la bandeja de auditorias.
- Respuesta o acciones del agente: Se reviso la fila en `InstallQA`, los parametros usados en la ejecucion del procedimiento y la logica dinamica de filtrado del SP.
- Resultado: Se determino que la solicitud existia, era de tipo `D`, tenia fecha `2026-07-18`, prepaga `1`, estado `V`, `responsable_gestion = sa`, `responsable_auditoria = csiafas` y no estaba dada de baja. La ejecucion pasaba `@prm_usu_ges = csiafas`, y el SP filtra por `SA.responsable_gestion = @prm_usu_ges`, no por `responsable_auditoria`. Por eso la solicitud quedaba fuera del resultado.
- Estado: Resuelto

### Tema 2: Condiciones para filtrar por responsable_auditoria

- Solicitud o contexto: Constantino pregunto que condiciones debian darse para que el SP filtrara por `responsable_auditoria`.
- Respuesta o acciones del agente: Se verifico la definicion vigente de `dbo.prs_solicitudes_auto_bandeja` en `InstallQA`, buscando el uso de `responsable_auditoria` y `responsable_gestion`.
- Resultado: Se confirmo que el SP no contiene ninguna condicion que cambie el filtro hacia `SA.responsable_auditoria`. El parametro `@prm_usu_ges` filtra por `SA.responsable_gestion`, incluyendo el caso especial `(Sin Asignar)`. El flag de auditor habilita otros filtros de auditoria, pero no cambia el campo de usuario usado para filtrar. Para recuperar solicitudes por `responsable_auditoria = csiafas`, habria que ajustar el SP o usar otro procedimiento que contemple ese campo.
- Estado: Resuelto
