# Memoria 2026-07-28

## Preferencias operativas de Wilmer
- Wilmer pidió explícitamente no realizar modificaciones directas en el entorno ni hacer commits. Lisa debe proponer y mostrar cambios por pantalla o archivo; la ejecución de modificaciones y commits queda a cargo exclusivo de whiguera.
- Wilmer pidió no asumir que un paso se comprendió o quedó cerrado automáticamente sin su visto bueno explícito.

## Tarea #29489 - Solicitudes auto, notificaciones por cambio de estado
- Contexto activo: repo SVN `whiguera-Version84-Modelo2Gis`, tarea `#29489`.
- Wilmer indicó que ya quedó por el momento la notificación para solicitud `G - En Gestion`; él todavía maneja los cambios de SP, Lisa solo asesora/proporciona bloques.
- Para estado `G` se concluyó que la plantilla `@prm_id = 4` corresponde solo a solicitudes cuyo `tipo_usuario = 'P'` porque cae en grupo de plantillas de prestador dentro de `pri_solicitudes_auto_notif`.
- Para `pru_solicitudes_auto_resuelta_pertinencia`, no hace falta validar estado anterior contra `G` porque el SP valida previamente que la solicitud esté en `S` antes de pasarla a `G`.
- Para `prs_valida_of_auto_remotas`, se propuso guardar `@var_soli_esta_anterior` y `@var_tipo_usuario_soli`, y llamar `pri_solicitudes_auto_notif @prm_id = 4` solo si `@var_cod_error = 0`, nuevo estado `@var_soli_esta = 'G'`, estado anterior distinto de `G`, y `tipo_usuario = 'P'`.
- `@var_cod_error` viene de `prs_obtiene_usuari_soli` como OUTPUT: `0` OK, `1` no encontro usuario creador, `2` usuario inactivo, `3` usuario no vigente. En `prs_valida_of_auto_remotas` se usa para asignar responsable, marcar usuario activo y actualizar `audit_fecha` solo cuando no hubo error.

## Tarea #29489 - Estado `U - Anulada`
- Se ubicaron SP que pueden cambiar `SOLICITUDES_AUTO.soli_esta` a `U`:
  - `pru_solicitudes_auto_anula_bandeja`: origen visto desde Presmed `wp_bandeja_solicitudes.srw`; ya llama notificaciones con prestador `@prm_id = 6` y afiliado `@prm_id = 14/15`.
  - `pru_solicitudes_auto_anula_total`: auditoria `Anulación GIS`; cambia directo a `U`; no se vio notificación.
  - `pru_solicitudes_auto_soli_esta`: recalculador central; pone `U` si todas las autorizaciones asociadas estan anuladas; lo llaman flujos Presmed y GIS/backend.
  - `prs_control_vencimiento_devolucion_soli`: cambia a `U` por vencimiento de devolución; ya tiene notificación propia con prestador `@prm_id = 3` y afiliado `@prm_id = 17`.
- Sobre `pru_solicitudes_auto_anula_bandeja`, Wilmer consultó si el llamado actual está bien porque no valida `@var_tipo_usuario = 'P'`, `@var_tipo_usuario IN ('A','E')`, estado anterior ni si corresponde proceso 14 o 15.
- Análisis de `pru_solicitudes_auto_anula_bandeja`: el control de estado anterior está parcialmente cubierto porque lee `@var_soli_esta = soli_esta` y hace `IF @var_soli_esta = 'U' RAISERROR`, evitando reenviar si ya estaba anulada antes de entrar. Pero no lee `tipo_usuario`, por lo tanto no distingue si debe notificar prestador o afiliado/ejecutivo.
- La lógica actual dudosa para afiliado era `SET @var_notif_afiliado_id = CASE WHEN @var_soli_tipo = 'D' THEN 15 ELSE 14 END`; se concluyó que es funcionalmente débil porque `soli_tipo = 'D'` no significa exclusivamente medicamentos.
- Dato funcional aportado por Wilmer: el formato del email lo define el estado `Anulada` y el motivo de anulación: motivo 1 = Medicos no adscritos / prestadores fuera de red; motivo 2 = Solicitud por Medicamentos.
- Criterio final para seleccionar plantilla en `pru_solicitudes_auto_anula_bandeja`: no usar `soli_tipo`; usar `@prm_anu_moti`:
  - `@prm_anu_moti = 1` -> plantilla afiliado `@prm_id = 14`.
  - `@prm_anu_moti = 2` -> plantilla afiliado `@prm_id = 15`.
  - otros motivos -> `NULL` o definición funcional pendiente.
- Criterio final de notificación para anulación en bandeja:
  - Prestador: solo si `@var_notif_prestad = 'S'` y `@var_tipo_usuario_soli = 'P'`, plantilla `6`.
  - Afiliado/Ejecutivo: solo si `@var_notif_afi = 'S'`, `@var_tipo_usuario_soli IN ('A','E')` y `@var_notif_afiliado_id IS NOT NULL`, plantilla según motivo `1 -> 14`, `2 -> 15`.
