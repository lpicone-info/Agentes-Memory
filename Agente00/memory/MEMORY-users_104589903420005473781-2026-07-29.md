# Memoria curada diaria

- Fecha: 2026-07-29
- Usuario: Luciana Estevarena
- id_googlechat: users/104589903420005473781
- Modo de actualización: diario
- Sesiones revisadas:
  - agent:main:googlechat:direct:spaces/0qzffyaaaae

## Resumen de la actividad

### Tema 1: Inicio de sesión y preferencias de trabajo

- Solicitud o contexto: Luciana inició la conversación con un saludo.
- Respuesta o acciones del agente: Se ejecutó el bootstrap operativo de la sesión, se leyeron reglas, repositorios, perfiles y la preferencia asociada a `users/104589903420005473781` desde `USUARIOS.md`.
- Resultado: El agente cargó las preferencias de Luciana y respondió usando el trato indicado para ella.
- Estado: Resuelto

### Tema 2: Actualización de tarea 43 en NemeSys

- Solicitud o contexto: Luciana pidió modificar la tarea `43` con avance `55%`, comentario `Prueba enviada a Andrea esperando devolución`, y realizar commit y push.
- Respuesta o acciones del agente: Se trabajó en `/home/luispicone/.openclaw/workspace/git/lpicone-info-NemeSys`, se ubicó la tarea en `tareas_nemesys/TAREAS.md`, se actualizó la fila correspondiente y se validó el diff.
- Resultado: Se realizó commit y push con `38386f8 Actualiza avance de tarea 43`. El repositorio quedó limpio y sincronizado con `origin/main`.
- Estado: Resuelto

### Tema 3: Alta de tareas 48 y 49 en NemeSys

- Solicitud o contexto: Luciana pidió agregar dos tareas pendientes con propietario Luciana: `Subir documentacion a global ISO` y `Subir facturas de Chat GPT a Drive`, con commit y push.
- Respuesta o acciones del agente: Se editó `tareas_nemesys/TAREAS.md` en el repositorio `lpicone-info-NemeSys`, agregando las filas `48` y `49` con estado `Pendiente`, avance `0%` y campos restantes sin fecha ni comentario.
- Resultado: Se realizó commit y push con `4513940 Agrega tareas pendientes de documentacion y facturas`. Se validó repo limpio, cambio acotado, UTF-8 sin BOM y finales LF.
- Estado: Resuelto

### Tema 4: Disponibilidad y ejecución de reporte-pendientes-cliente para ROISA

- Solicitud o contexto: Luciana consultó si estaba disponible la skill `reporte-pendientes-cliente` y luego pidió ejecutarla para el cliente `ROISA`, dejando el archivo en su carpeta.
- Respuesta o acciones del agente: Se confirmó disponibilidad de la skill y se ejecutó usando la credencial Redmine asociada a Luciana según sus preferencias.
- Resultado: Se generó `/mnt/Agentes/lestevarena/roisa.xlsx` con `27` filas: `11` de Presupuestos y `16` de Soporte, correspondientes a las áreas `Presupuestos ROISA` y `Soporte ROISA`. Se validó existencia y estructura del archivo `.xlsx`.
- Estado: Resuelto

### Tema 5: Estados de tickets InvGate y método de consulta

- Solicitud o contexto: Luciana preguntó cuáles son los estados posibles de un ticket en InvGate y luego pidió el método usado para listarlos.
- Respuesta o acciones del agente: Se consultó InvGate en modo lectura con el endpoint de catálogo `GET /api/v1/incident.attributes.status`, usando el scope `api.v1.incident.attributes.status:get` y evidencia operativa previa registrada en memoria.
- Resultado: Se listaron los estados `Nuevo`, `Abierto`, `Pendiente`, `En espera`, `Solucionado`, `Cerrado`, `Rechazado` y `Cancelado`. Luego se explicó el método: validación de endpoint, obtención de token OAuth y lectura de campos `id` y `name` del JSON devuelto.
- Estado: Resuelto

### Tema 6: Cruce de estado InvGate con otros atributos

- Solicitud o contexto: Luciana preguntó si un estado podía combinarse con otro atributo, por ejemplo `En espera` más `de Redmine`.
- Respuesta o acciones del agente: Se validó que `GET /api/v1/incidents.by.status` permite filtrar por estado y que `GET /api/v1/incidents` permite cruzar los IDs resultantes con campos de detalle como tipo, categoría, prioridad, origen, grupo asignado, agente, usuario y fechas.
- Resultado: Se concluyó que `estado + otro atributo` es viable según el atributo. Para `estado 4 + mesa Redmine`, no se pudo garantizar el cruce directo porque el detalle consultado no devuelve `helpdesk_id`; se indicó que haría falta una vista, export o endpoint que incluya la mesa.
- Estado: Informativo

### Tema 7: Estado del ticket InvGate 6008 y campo Esperando por

- Solicitud o contexto: Luciana pidió consultar el estado del ticket `6008` sin usar skill y luego preguntó cómo obtener el dato resaltado en una imagen, referido al campo `Esperando por`.
- Respuesta o acciones del agente: Se consultó InvGate directamente. Para el estado se usó el detalle del ticket y para el dato resaltado se identificó que el endpoint simple del ticket no devuelve `waiting_for`, mientras que `GET /api/v1/incidents.details.by.view?view_id=<ID_DE_VISTA>` sí devuelve esa columna cuando se consulta la vista correcta.
- Resultado: El ticket `6008` estaba en estado `En espera` (`status_id 4`) con título `Codigo de Seguridad Invalido (Error)`. Para `Esperando por`, se indicó que el endpoint de vista devuelve el campo `waiting_for`; también se resolvió que `user_id=60` corresponde a `Federico Tozzoli`. No se pudo ubicar el ticket `6008` en las vistas conocidas `1` y `2`, por lo que quedó condicionado a conocer el `view_id` correcto de la pantalla.
- Estado: Pendiente

### Tema 8: Conteo y análisis de tickets por vistas InvGate

- Solicitud o contexto: Luciana pidió contar tickets en las vistas `1` y `7`, y luego consultar datos del ticket `5995` usando la vista `7`.
- Respuesta o acciones del agente: Se consultó `GET /api/v1/incidents.details.by.view` para las vistas indicadas. Para el ticket `5995`, se leyó la fila de la vista `7`, se interpretaron los campos de cliente y `waiting_for`, y se resolvió la empresa del cliente consultando la relación usuario-empresa en InvGate.
- Resultado: La vista `1` tenía `80` tickets y la vista `7` tenía `436` tickets. En la vista `7`, el ticket `5995` figuraba con cliente `C. Lioi` (`user_id 150`), `Esperando por` igual a `Waiting for the customer` asociado al cliente, y empresa `ROISA`.
- Estado: Resuelto
