# Memoria operativa - 2026-05-21

## Skill docu-video

- Bootstrap operativo ejecutado conforme `AGENTS.md`: lectura completa de `REGLAS.md` en bloques de hasta 12 lineas hasta `<!-- EOF REGLAS.md -->`, luego `REPOSITORIOS.md` y `DB_PROFILE.md`.
- Contexto: Luis probo/analizo la skill `docu-video` con salida indicada en `/mnt/Agentes/...`; se identifico que escribir `audio/`, `frames/`, `transcript/`, `analysis/`, `output/`, `logs/` y checkpoints directo sobre una ruta remota genera demasiado I/O y riesgo de caidas.
- Estado previo observado: una ejecucion quedo interrumpida durante `transcripcion` despues de reinicio del gateway. El checkpoint mostraba `transcripcion|iniciado` sin `transcripcion|finalizado`; no habia proceso `python`/`whisper` activo y `transcript/` seguia vacio.
- Limpieza local previa: tras confirmacion reforzada 2/2, se borro solo la carpeta temporal local `~/.openclaw/workspace/docu-video/tmp/20260521-172905-aumentos-diferenciales-20260325-140600-grabacion-de-la-reunion`, que contenia una copia local del video de aproximadamente 728M. No se toco `/mnt/Agentes/...` ni el video original.
- Decision de diseno aprobada: la skill debe procesar siempre en carpeta local temporal y usar la ruta indicada por el usuario solo como destino final de publicacion.
- Archivo modificado: `workspace/skills/docu-video/SKILL.md`.
- Cambio aplicado en la skill:
  - `CARPETA_DESTINO_FINAL` queda definida como destino de publicacion, no carpeta de trabajo.
  - Se crea siempre `CARPETA_EJECUCION_LOCAL` bajo `~/.openclaw/workspace/docu-video/runs/<timestamp>-<nombre-normalizado>/`.
  - Toda la estructura de trabajo vive localmente: `input/`, `context/`, `audio/`, `frames/`, `frames/relevantes/`, `transcript/`, `analysis/`, `output/`, `logs/`, `tmp/`.
  - Video y documentos puntuales se copian al run local; repositorios registrados o fuentes locales amplias no se copian completos, se consultan en origen y se documentan referencias/resumen/evidencia necesaria.
  - Todo procesamiento pesado ocurre localmente: integridad, metadata, audio, transcripcion, frames, OCR, analisis, DOCX, logs y checkpoints.
  - Solo tras validar localmente se publica el entregable completo al destino final.
  - En aborto se intenta publicar diagnostico minimo saneado en `docu-video-abortado-<timestamp>/` dentro del destino final, si es escribible.
  - En exito, aborto controlado o error inesperado, se borra siempre la carpeta local de ejecucion para evitar acumulacion de disco.
  - Al inicio de una nueva ejecucion se agrego limpieza de `runs/` stale cuando no haya procesos activos asociados.
- Validaciones reportadas luego del cambio: UTF-8 OK, sin BOM, finales LF, sin CRLF ni CR sueltos, bloques Markdown balanceados y revision de terminos viejos para evitar instrucciones contradictorias de procesamiento directo en destino remoto.
- No se hizo commit ni push.

## Sincronizacion de skill docu-video entre agentes

- Luis pidio copiar la version final local de `docu-video` a las otras instalaciones.
- Inventario operativo remoto usado por SSH:
  - Agente01: `10.194.0.27`
  - Agente02: `10.194.0.30`
  - Agente03: `10.194.0.32`
  - Agente04: `10.194.0.33`
- Se cargaron las claves SSH mediante `~/.openclaw/secrets/ssh-agent/load-agent-ssh-keys.sh`.
- Se creo/valido en cada agente la ruta:
  - `~/.openclaw/workspace/skills/docu-video/SKILL.md`
- Checksum final comun de `docu-video` en Agente00, Agente01, Agente02, Agente03 y Agente04:
  - `5bbb874ad7b5c202d30a9625d484804773100850738bdb63171cd0aac9386f9a`
- Validaciones ejecutadas en Agente01-04:
  - UTF-8 OK.
  - Sin BOM.
  - LF, sin CRLF ni CR sueltos.
  - Bloques Markdown balanceados.
- Luis cerro el tema `docu-video` por el dia.

## REGLAS.md - Regla 30

- Luis modifico manualmente `REGLAS.md`, puntualmente la Regla 30.
- Nueva Regla 30 observada:
  - Titulo: `Restriccion de secretos tecnicos en canales externos`.
  - En Discord y otros canales externos, el agente no debe revelar secretos tecnicos ni informacion interna del runtime/agente/OpenClaw.
  - La restriccion incluye passwords, API keys, tokens, claves privadas, archivos de credenciales, rutas internas sensibles, configuracion interna, sesiones, mecanismos de coordinacion o datos equivalentes.
  - Excepcion: no aplica a datos funcionales de negocio consultados en bases de datos usando profiles registrados en `DB_PROFILE.md`.
- Validacion local de `REGLAS.md`:
  - UTF-8 OK.
  - Sin BOM.
  - LF, sin CRLF ni CR sueltos.
  - Marcador final correcto: `<!-- EOF REGLAS.md -->`.
- Luis pidio copiar `REGLAS.md` a Agente01, Agente02, Agente03 y Agente04.
- Se hizo backup previo en cada agente remoto:
  - `~/.openclaw/workspace/.backups/REGLAS/REGLAS.md.20260521T212108Z`
- Checksum final comun de `REGLAS.md` en Agente00, Agente01, Agente02, Agente03 y Agente04:
  - `cd0e31847ed49ba4efac08b7033355a7b785aafeadd22a83833c081b4448e2c6`
- Validaciones ejecutadas en Agente01-04:
  - UTF-8 OK.
  - Sin BOM.
  - LF, sin CRLF ni CR sueltos.
  - Marcador final correcto: `<!-- EOF REGLAS.md -->`.

## Tarea 3 - lpicone-info/NemeSys

- Luis pidio actualizar comentarios de la tarea 3, agregar que las pruebas fueron exitosas, ponerla como finalizada y hacer push.
- Se actualizo el registro de tareas del repo `lpicone-info-NemeSys`:
  - Tarea 3 movida a `Tareas completadas`.
  - Estado: `Finalizada`.
  - Avance: `100%`.
  - Fecha de finalizacion: `2026-05-21`.
  - Comentarios actualizados con pruebas exitosas y referencia al instructivo unico.
- Commit publicado:
  - `94a6b8e Finaliza tarea 3 de montaje Agentes`
- Validacion reportada:
  - `main` quedo sincronizada con `origin/main`.
  - Remoto `origin/main`: `94a6b8ee9b0f52df92ee153a9312cdaa51e95c06`.
