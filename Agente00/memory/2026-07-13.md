# Memory - 2026-07-13

## Reporte diario Luciana / prueba 08-07-2026
- Usuario pidio ejecutar el procedimiento de `/mnt/Agentes/Luciana/reporte/reportediario.md` usando fecha fija `08/07/2026`, no fecha actual.
- El reporte hace referencia a archivos relacionados en la misma carpeta, incluyendo usuarios y criterios Redmine. Para la prueba inicial se limito a `agente_01 / Maxxuel` y sus usuarios: Belkis, Gabriel, Sebastian y Savina.
- El envio correcto debe hacerse desde agente00 usando la skill `gog`, tal como indica el reporte, con la cuenta configurada `agente00_ia@infomedical.com.ar`. No usar `mail` local como reemplazo si el reporte exige `gog`.
- En agente00 el binario `gog` no estaba inicialmente en PATH; se instalo localmente `gogcli v0.34.0` desde release oficial, con checksum validado. El reenvio del reporte de Maxxuel por `gog gmail send` fue exitoso: `messageId 19f5ba334c41ae81`, `threadId 19f5ba334c41ae81`.
- Luego el usuario pidio generar y enviar, para la misma fecha `08/07/2026`, los reportes de `agente_04`, `agente_05` y `agente_06`. Se enviaron por `gog` desde `agente00_ia@infomedical.com.ar` a `lestevarena@infomedical.com.ar`.
- Resultados de envio: `La 99` messageId `19f5bd632d444c88`; `Barbie` messageId `19f5bd63abf0eafa`; `Homero` messageId `19f5bd64133c2cfd`.
- Nota operativa registrada en el informe de `agente_06 / Homero`: faltaba credencial Redmine local para `dgiuseppe`.

## 2026-07-13 - Reportes diarios enviados

- Usuario solicitó generar informes desde `/mnt/Agentes/lestevarena/reporte/reportediario.md` para fecha `08/07/2026`, haciendo que la fecha de actualización del reporte también refiera a `08/07/2026`.
- Se generaron y enviaron correos HTML desde `agente00_ia@infomedical.com.ar` a `lestevarena@infomedical.com.ar` para:
  - Agente02 / Camaron: asunto `Reporte de actividad diaria Camaron 08/07/2026`, Gmail `message_id` `19f5d03a3684ef3a`.
  - Agente06 / Homero: asunto `Reporte de actividad diaria Homero 08/07/2026`, Gmail `message_id` `19f5d03a89c64e92`. En el informe se indicó que para Diego Di Giuseppe no se pudo consultar pendientes porque Agente06 no tenía credencial Redmine registrada y la resolución por API devolvió restricción.
  - Agente03 / Stan: asunto `Reporte de actividad diaria Stan 08/07/2026`, Gmail `message_id` `19f5d0c4f3a1abaf`.
- En los reportes se validó actividad real por timestamp del `08/07` y Redmines pendientes por criterio antes del envío mediante `gog` local.
