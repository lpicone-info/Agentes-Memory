# 2026-06-10

- Se creó la skill local `dbprofile-add` en `skills/dbprofile-add/SKILL.md`; por decisión del operador debe contener únicamente `SKILL.md`, sin scripts ni `agents/openai.yaml`.
- `dbprofile-add` solicita los datos por chat usando `Motor: MS SQL o Sybase SQL` sin corchetes, valida conexión efectiva y registra el alta en `DB_PROFILE.md`. Si falta un driver compatible, debe instalar silenciosamente `python3-pymssql` mediante `apt-get` y continuar, sin realizar actualizaciones generales.
- La skill `dbprofile-add` fue copiada y validada en `Agente01` a `Agente06`.
- Se agregó en el `REGLAS.md` local la Regla 31: todo pedido o intención de agregar, crear, registrar o dar de alta un profile de base de datos debe invocar obligatoriamente `dbprofile-add`; está prohibida el alta manual en `DB_PROFILE.md`.
- Se registró correctamente el profile `InstallQA_89` en el `DB_PROFILE.md` local después de validar conexión MS SQL. No almacenar credenciales en memoria.
- Se creó la skill local `dbprofile-delete` en `skills/dbprofile-delete/SKILL.md`; contiene únicamente `SKILL.md`. Solicita el nombre exacto, valida una única coincidencia, oculta el password al mostrar datos, exige doble confirmación y elimina solo el bloque correspondiente de `DB_PROFILE.md`.
- Cuando el operador pide copiar algo "al resto de agentes", se refiere a distribuirlo desde NemeSys hacia `Agente01` a `Agente06`, validando destinos y resultado.
