# Memory - 2026-06-23

## InvGate / Skill invgate-get
- Usuario pidio crear/mejorar la skill `invgate-get` para consultas GET de InvGate. La skill fue aplicada y debe usarse cuando la intencion sea obtener/consultar/listar/leer/analizar/interpretar datos de InvGate.
- Instruccion operativa persistente del usuario: cada mejora que se haga sobre `invgate-get` debe aplicarse inmediatamente, no dejarla pending, para que pueda probarla por UI.
- Regla funcional acordada por el usuario: la skill no debe frenarse ante ambiguedades de lectura; debe responder con la mejor interpretacion disponible en modo best-effort, declarando interpretacion, fuente, filtros, limites y datos extraidos. El usuario luego pedira correcciones o confirmara el formato.
- Fuera de alcance de `invgate-get`: crear, actualizar, modificar, cerrar, comentar, cambiar estado, asignar, eliminar o cualquier accion de escritura en InvGate. Para eso se requiere otra skill/procedimiento.
- Capacidades vigentes conocidas: listar estados, listar tickets por estado, consultar tickets por IDs, consultar ticket puntual, leer comentarios de ticket, listar tickets ultima hora, listar tickets por vista, listar detalle de tickets por vista, listar tickets por fecha.
- Endpoints InvGate relevantes aprendidos: `GET /api/v1/incident.attributes.status`, `GET /api/v1/incidents.by.status`, `GET /api/v1/incidents`, `GET /api/v1/incident`, `GET /api/v1/incident.comment`, `GET /api/v1/incidents.last.hour`, `GET /api/v1/incidents.by.view`, `GET /api/v1/incidents.details.by.view`, `GET /api/v1/data.export`.
- Aprendizaje tecnico aplicado: `/api/v1/incidents` devuelve un diccionario indexado por ID, no una lista; la skill fue ajustada para parsear esa forma.
- Aprendizaje tecnico aplicado: fallback de fecha usa `incidents.by.status` con `status_ids[]`, paginacion `limit`/`offset`, detalle por lotes via `/incidents`, y filtro local por fechas.
- Para pedidos ambiguos como "tickets cargados ayer", la skill debe devolver ambos grupos cuando aplique: creados por `created_at` y con actividad/ultima actualizacion por `last_update`; no limitarse solo a `created_at`.
- Ultima prueba realizada el 2026-06-23: "ayer" interpretado como `2026-06-22`. Se revisaron 5867 IDs. Resultado: 0 tickets creados (`created_at`) el 2026-06-22 y 20 tickets con actividad/ultima actualizacion (`last_update`) el 2026-06-22.
- Tickets con `last_update` el 2026-06-22 encontrados en la prueba: #3302, #5150, #5257, #5409, #5513, #5574, #5738, #5758, #5803, #5827, #5832, #5837, #5845, #5856, #5859, #5862, #5864, #5865, #5868, #5869.
- Proposal IDs importantes: `invgate-get-20260623-5eea4a61d5` aplicada inicialmente; `invgate-get-20260623-34a822afd2` aplicada para vistas/ultima hora/fecha; `invgate-get-20260623-1d9b539d1d` aplicada para parsing diccionario y fallback por fecha; `invgate-get-20260623-f08a2baf17` aplicada para devolver creados y actualizados/cargados por actividad.
- Tarea de trazabilidad usada: `/home/luispicone/.openclaw/workspace/git/lpicone-info-NemeSys/tareas_nemesys/tarea-16-invgate-integracion.md`; `TAREAS.md` habia sido avanzado al menos a 75% durante estas mejoras.

## Actualizacion posterior - InvGate / invgate-get (17:24 UTC)
- Correccion funcional vigente: cuando el usuario pide tickets `cargados`, `registrados`, `creados` o `dados de alta` en una fecha o rango, debe filtrarse exclusivamente por `created_at` / fecha de carga. No debe buscar ni mezclar tickets modificados en esa fecha. `last_update` solo aplica si el usuario pide explicitamente `modificados`, `actualizados`, `movidos`, `trabajados` o `con actividad`.
- Regla de salida vigente para cualquier listado filtrado de tickets InvGate: no mostrar codigos tecnicos como dato principal. Consultar los catalogos necesarios y mostrar descripciones entendibles; si algo no resuelve, mostrar `No disponible` y dejar el codigo solo en limites/observaciones.
- Columnas vigentes para listados de tickets: `Ticket`, `Titulo`, `Cargado`, `Empresa`, `Estado`, `Prioridad`, `Asignado`. `Empresa` debe ir inmediatamente despues de `Cargado`. `Titulo` completo, sin truncar. `Cargado` en formato `dd/mm/yyyy hh:mm`.
- `Estado` debe resolver descripcion desde catalogo de estados. `Prioridad` debe resolver descripcion desde catalogo de prioridades. `Asignado` debe ser el nombre de la persona asignada al ticket, resolviendo `assigned_id` contra `GET /api/v1/users`; si no hay nombre completo, usar usuario o email como descripcion entendible.
- `Empresa` identifica la empresa/cliente del ticket. Primero intentar campos directos del ticket (`company`, `client`, `customer`, `organization`, `empresa` o equivalentes). Si no existe campo directo, resolver `user_id` contra `GET /api/v1/users` como solicitante/cliente. No usar `assigned_id` para Empresa.
- Se quito la columna `Asignado Grupo`; no debe mostrarse grupo asignado en listados de tickets salvo pedido explicito.
- Usuario indico: no modificar `/home/luispicone/.openclaw/workspace/git/lpicone-info-NemeSys/tareas_nemesys/tarea-16-invgate-integracion.md` salvo pedido explicito.
- Propuestas aplicadas relevantes posteriores: `invgate-get-20260623-ef19ac4296` (cargados por `created_at`), `invgate-get-20260623-57c6830b80` (columnas con descripciones, sin codigos), `invgate-get-20260623-db112031e7` (Asignado persona via users), `invgate-get-20260623-88668aa5be` (columna Empresa despues de Cargado).
- Ultimos resultados de referencia: consulta de tickets cargados el `2026-06-22` encontro 0 coincidencias por `created_at`; consulta de tickets cargados el `19/06/2026` encontro 4 tickets (#5866, #5867, #5868, #5869); consulta de tickets cargados entre `16/06/2026` y `19/06/2026` encontro 26 tickets (#5844 a #5869).
# Memory - 2026-06-23

## InvGate / Skill invgate-get
- Usuario pidio crear/mejorar la skill `invgate-get` para consultas GET de InvGate. La skill fue aplicada y debe usarse cuando la intencion sea obtener/consultar/listar/leer/analizar/interpretar datos de InvGate.
- Instruccion operativa persistente del usuario: cada mejora que se haga sobre `invgate-get` debe aplicarse inmediatamente, no dejarla pending, para que pueda probarla por UI.
- Regla funcional acordada por el usuario: la skill no debe frenarse ante ambiguedades de lectura; debe responder con la mejor interpretacion disponible en modo best-effort, declarando interpretacion, fuente, filtros, limites y datos extraidos. El usuario luego pedira correcciones o confirmara el formato.
- Fuera de alcance de `invgate-get`: crear, actualizar, modificar, cerrar, comentar, cambiar estado, asignar, eliminar o cualquier accion de escritura en InvGate. Para eso se requiere otra skill/procedimiento.
- Capacidades vigentes conocidas: listar estados, listar tickets por estado, consultar tickets por IDs, consultar ticket puntual, leer comentarios de ticket, listar tickets ultima hora, listar tickets por vista, listar detalle de tickets por vista, listar tickets por fecha.
- Endpoints InvGate relevantes aprendidos: `GET /api/v1/incident.attributes.status`, `GET /api/v1/incidents.by.status`, `GET /api/v1/incidents`, `GET /api/v1/incident`, `GET /api/v1/incident.comment`, `GET /api/v1/incidents.last.hour`, `GET /api/v1/incidents.by.view`, `GET /api/v1/incidents.details.by.view`, `GET /api/v1/data.export`.
- Aprendizaje tecnico aplicado: `/api/v1/incidents` devuelve un diccionario indexado por ID, no una lista; la skill fue ajustada para parsear esa forma.
- Aprendizaje tecnico aplicado: fallback de fecha usa `incidents.by.status` con `status_ids[]`, paginacion `limit`/`offset`, detalle por lotes via `/incidents`, y filtro local por fechas.
- Para pedidos ambiguos como "tickets cargados ayer", la skill debe devolver ambos grupos cuando aplique: creados por `created_at` y con actividad/ultima actualizacion por `last_update`; no limitarse solo a `created_at`.
- Ultima prueba realizada el 2026-06-23: "ayer" interpretado como `2026-06-22`. Se revisaron 5867 IDs. Resultado: 0 tickets creados (`created_at`) el 2026-06-22 y 20 tickets con actividad/ultima actualizacion (`last_update`) el 2026-06-22.
- Tickets con `last_update` el 2026-06-22 encontrados en la prueba: #3302, #5150, #5257, #5409, #5513, #5574, #5738, #5758, #5803, #5827, #5832, #5837, #5845, #5856, #5859, #5862, #5864, #5865, #5868, #5869.
- Proposal IDs importantes: `invgate-get-20260623-5eea4a61d5` aplicada inicialmente; `invgate-get-20260623-34a822afd2` aplicada para vistas/ultima hora/fecha; `invgate-get-20260623-1d9b539d1d` aplicada para parsing diccionario y fallback por fecha; `invgate-get-20260623-f08a2baf17` aplicada para devolver creados y actualizados/cargados por actividad.
- Tarea de trazabilidad usada: `/home/luispicone/.openclaw/workspace/git/lpicone-info-NemeSys/tareas_nemesys/tarea-16-invgate-integracion.md`; `TAREAS.md` habia sido avanzado al menos a 75% durante estas mejoras.

## Actualizacion posterior - InvGate / invgate-get (17:24 UTC)
- Correccion funcional vigente: cuando el usuario pide tickets `cargados`, `registrados`, `creados` o `dados de alta` en una fecha o rango, debe filtrarse exclusivamente por `created_at` / fecha de carga. No debe buscar ni mezclar tickets modificados en esa fecha. `last_update` solo aplica si el usuario pide explicitamente `modificados`, `actualizados`, `movidos`, `trabajados` o `con actividad`.
- Regla de salida vigente para cualquier listado filtrado de tickets InvGate: no mostrar codigos tecnicos como dato principal. Consultar los catalogos necesarios y mostrar descripciones entendibles; si algo no resuelve, mostrar `No disponible` y dejar el codigo solo en limites/observaciones.
- Columnas vigentes para listados de tickets: `Ticket`, `Titulo`, `Cargado`, `Empresa`, `Estado`, `Prioridad`, `Asignado`. `Empresa` debe ir inmediatamente despues de `Cargado`. `Titulo` completo, sin truncar. `Cargado` en formato `dd/mm/yyyy hh:mm`.
- `Estado` debe resolver descripcion desde catalogo de estados. `Prioridad` debe resolver descripcion desde catalogo de prioridades. `Asignado` debe ser el nombre de la persona asignada al ticket, resolviendo `assigned_id` contra `GET /api/v1/users`; si no hay nombre completo, usar usuario o email como descripcion entendible.
- `Empresa` identifica la empresa/cliente del ticket. Primero intentar campos directos del ticket (`company`, `client`, `customer`, `organization`, `empresa` o equivalentes). Si no existe campo directo, resolver `user_id` contra `GET /api/v1/users` como solicitante/cliente. No usar `assigned_id` para Empresa.
- Se quito la columna `Asignado Grupo`; no debe mostrarse grupo asignado en listados de tickets salvo pedido explicito.
- Usuario indico: no modificar `/home/luispicone/.openclaw/workspace/git/lpicone-info-NemeSys/tareas_nemesys/tarea-16-invgate-integracion.md` salvo pedido explicito.
- Propuestas aplicadas relevantes posteriores: `invgate-get-20260623-ef19ac4296` (cargados por `created_at`), `invgate-get-20260623-57c6830b80` (columnas con descripciones, sin codigos), `invgate-get-20260623-db112031e7` (Asignado persona via users), `invgate-get-20260623-88668aa5be` (columna Empresa despues de Cargado).
- Ultimos resultados de referencia: consulta de tickets cargados el `2026-06-22` encontro 0 coincidencias por `created_at`; consulta de tickets cargados el `19/06/2026` encontro 4 tickets (#5866, #5867, #5868, #5869); consulta de tickets cargados entre `16/06/2026` y `19/06/2026` encontro 26 tickets (#5844 a #5869).

## Actualizacion posterior - InvGate empresa originadora y ultimo ticket (20:13 UTC)
- Se corrigio/valido conceptualmente que la empresa originadora real del ticket se obtiene cruzando `ticket.user_id` contra membresias de companias: `GET /api/v1/companies` + `GET /api/v1/companies.users`. No debe inferirse desde nombre/email del usuario cuando exista esa relacion. Ejemplos validados: #5844 -> SANAR; #5845 -> PensandoSalud; #5866 -> SOS Salud; #5868 -> Premedic. Proposal aplicada: `invgate-get-20260623-5228c256b8`.
- Reconsulta de tickets cargados entre `16/06/2026` y `19/06/2026` encontro 26 tickets, ahora con empresas corregidas por compania del solicitante: #5844 SANAR, #5845 PensandoSalud, #5846 PensandoSalud, #5847 Mutual Rio IV, #5848 Consalud, #5849 PensandoSalud, #5850 Mutual Rio IV, #5851 Mutual Rio IV, #5852 Ospecon, #5853 PensandoSalud, #5854 Ospic Cine, #5855 Igualdad Salud, #5856 PensandoSalud, #5857 PensandoSalud, #5858 Premedic, #5859 PensandoSalud, #5860 Mutual Rio IV, #5861 Igualdad Salud, #5862 Celer, #5863 ROISA, #5864 ROISA, #5865 SBSalud, #5866 SOS Salud, #5867 Celer, #5868 Premedic, #5869 Premedic.
- Consulta de tickets ingresados el `2026-06-23` hasta `18:34 UTC` encontro 0 tickets por `created_at`.
- Error/limitacion importante detectada: cuando se pidio "ultimo ticket cargado en InvGate", la skill respondio #5869 usando criterio de maximo `created_at` dentro del universo obtenido por fallback (`incidents.by.status` + detalle). El usuario aclaro que existen numeros de ticket mas altos. La skill no debe presentar ese resultado como "ultimo absoluto".
- Criterios a separar en la skill: "ultimo por fecha de carga" = mayor `created_at`; "ultimo numero de ticket" = mayor `id`/`pretty_id`. Para "ultimo numero" se necesita una fuente exacta que incluya todos los tickets ordenables por numero descendente, idealmente una vista/reporte InvGate oficial con limit 1. Si no hay vista/reporte o endpoint exacto, declarar limite y no garantizar.
- Intento de listar vistas/reportes InvGate por API: endpoints documentados disponibles requieren identificador previo: `GET /api/v1/incidents.by.view` requiere `view_id`, `GET /api/v1/incidents.details.by.view` requiere `view_id`, `GET /api/v1/data.export` requiere `id`. Rutas candidatas para listar (`/api/v1/views`, `/api/v1/incidents.views`, `/api/v1/reports`, `/api/v1/data.exports`, `/api/v1/filters`) respondieron 404. Conclusion: no hay endpoint confirmado para enumerar todas las vistas/reportes con las credenciales/API disponibles.
- Prueba directa pedida por usuario con `view_id=1`: `incidents.by.view` respondio OK y devolvio IDs con `limit=10`: 42, 189, 284, 288, 716, 812, 1485, 1670, 1753, 2284. `incidents.details.by.view` respondio OK, total 74 registros, columnas visibles: Cliente, Ticket, ID, Esperando por, Prioridad, Ultima actualizacion. Parece vista filtrada, no vista de todos los tickets ni de ultimo ticket.
- Ticket #5869 detalle profundo: titulo `Token incorrecto`, empresa Premedic, estado Solucionado, prioridad Alta, solicitante Leandro Ezequiel Perez, asignado Jean Gonzalez Pinto, categoria Presmed, cargado `19/06/2026 21:26`, ultima actualizacion/solucionado `22/06/2026 13:21`, 6 adjuntos. Descripcion: reportan que se podia cargar cualquier numero de token y validar igual.
- Ticket #5869 comentario de resolucion: Jean explico que el parametro `Generan Token de seguridad` estaba en `N`, lo que hace pasar de largo la validacion del token. Al activarlo, tokens invalidos empiezan a rechazarse. Las transacciones sujetas a token no estaban como obligatorias (`obliga_token` en N), y se pueden activar por script previo pedido por ticket indicando cuales transacciones deben ser obligatorias.
- Adjuntos revisados del ticket #5869: `hkTFD47gy0.png` muestra parametro `Generan token de seguridad` en N; `hZZvjuGQnH.png` muestra prueba de token invalido rechazado; `e7TNvIakFX.png` muestra proceso rechazado por `Codigo de Seguridad invalido`; `Codigo de Seguridad Token.pdf` es manual de usuario de token; `okIQzsTm1u.png` muestra SQL con `obliga_token` en N para transacciones; `0MiOQdoSp9.png` muestra auditoria del parametro sin registros. Archivos descargados temporalmente en `/tmp/invgate_ticket_5869_attachments/` durante la inspeccion.
