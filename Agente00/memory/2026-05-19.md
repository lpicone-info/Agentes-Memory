# Memoria operativa - 2026-05-19

## Cierre de jornada

El usuario indico: "por hoy terminamos" y pidio guardar todo en memoria persistente.

## Repositorio operativo

Repositorio trabajado:

- Nombre logico: `lpicone-info-NemeSys`
- Ruta local: `/home/luispicone/.openclaw/workspace/git/lpicone-info-NemeSys`
- Remoto: `git@github.com:lpicone-info/NemeSys.git`
- Branch: `main`

Acciones Git realizadas durante la jornada:

- Se hizo commit de la actualizacion de la tarea 7 Google Drive.
- El primer push fallo por autenticacion SSH porque Git no estaba usando la clave dedicada.
- Se valido la clave `ssh-keys/NemeSys/github_nemesys_ed25519` contra GitHub.
- El push fue rechazado luego por remoto adelantado.
- Con autorizacion humana explicita, se hizo `git rebase origin/main` y luego push.
- Commit publicado final: `a7e618d Actualiza analisis de Google Drive`.
- Despues se hizo `git pull --ff-only origin main`; resultado: `Already up to date`.

## Tarea 7 - Google Drive

Archivos actualizados y publicados:

- `tareas_nemesys/TAREAS.md`
- `tareas_nemesys/tarea-7-google-drive.md`

Estado documentado:

- La tarea 7 paso a "Analisis actualizado", 35%.
- El enfoque recomendado cambio a `rclone mount` con OAuth Desktop App y servicio `systemd --user`.
- Ruta candidata de montaje: `/home/luispicone/gdrive`.
- Servicio recomendado: `rclone-gdrive-openclaw.service`.
- Se documento que el scope `drive` es amplio y conviene limitar con `root_folder_id` si se usa una carpeta especifica.
- Quedaron bloqueos: definir cuenta/proyecto Google, Drive completo vs carpeta especifica, Folder ID si aplica, autorizaciones para instalar dependencias, configurar OAuth, crear servicio y habilitar linger.

## Tarea 3 - Unidad N Windows

Archivo adjunto recibido y usado como base funcional:

- Instructivo para montar recurso Windows `\\infomed03n\Repo_Swiss2\Agentes` en Debian como `/mnt/unidad_n`.

Archivos actualizados localmente:

- `tareas_nemesys/TAREAS.md`
- `tareas_nemesys/tarea-3-unidad-red-windows.md`
- `tareas_nemesys/tarea-3-implementacion-montaje-red.md`

Estado documentado:

- La tarea 3 paso a "Analisis actualizado", 35%.
- El enfoque quedo centrado en montar `//infomed03n/Repo_Swiss2/Agentes` como `/mnt/unidad_n`.
- Usuario Windows local documentado: `agentes`.
- Archivo de credenciales definido: `/etc/samba/cred-unidad_n`.
- Persistencia definida mediante `/etc/fstab` con `nofail,_netdev,x-systemd.automount`.
- Protecciones de credenciales documentadas: `root:root`, `chmod 600`, `chattr +i`.
- Version SMB indicada por el instructivo: `vers=3.0`.
- Alternativa documentada si falla montar subcarpeta directa: montar `//infomed03n/Repo_Swiss2` en `/mnt/repo_swiss2` y usar `/mnt/repo_swiss2/Agentes`.
- No se ejecuto implementacion real de sistema.
- No se recibio ni documento password.
- No se instalo `cifs-utils`.
- No se creo `/mnt/unidad_n`.
- No se creo ni modifico `/etc/samba/cred-unidad_n`.
- No se modifico `/etc/fstab`.

Validaciones documentales realizadas sobre los cambios de tarea 3:

- `git diff --check` OK.
- Finales de linea conservados en `LF`.
- `tarea-3-unidad-red-windows.md` y `tarea-3-implementacion-montaje-red.md` verificados como ASCII.
- `TAREAS.md` verificado como UTF-8.

Estado Git al cierre respecto del repo `lpicone-info-NemeSys`:

- Hay cambios locales sin commit en los tres archivos de tarea 3.
- Ultimo commit remoto/local publicado: `a7e618d Actualiza analisis de Google Drive`.
- No se hizo commit ni push de la tarea 3 porque el usuario no lo pidio explicitamente despues del reenfoque.

## Pendientes para retomar

1. Si el usuario pide publicar lo de tarea 3, revisar diff, confirmar estado limpio esperado, commitear y pushear los tres archivos modificados.
2. Si el usuario pide implementar unidad N, avanzar un paso por vez:
   - validar UID/GID del usuario Linux efectivo;
   - pedir autorizacion para instalar `cifs-utils`;
   - crear `/mnt/unidad_n`;
   - crear credencial segura en `/etc/samba/cred-unidad_n`;
   - probar montaje manual;
   - persistir en `/etc/fstab`;
   - validar automount y escritura.
3. Mantener la password fuera de chat, repo, markdown e historial visible.
